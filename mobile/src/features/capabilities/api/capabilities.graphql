query MobileViewerCapabilities($ownerId: String, $fresh: Boolean) {
  viewerCapabilities(ownerId: $ownerId, fresh: $fresh) {
    fresh
    grants {
      action
      outcome
      reason
    }
  }
}
