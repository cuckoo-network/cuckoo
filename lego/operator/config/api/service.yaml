apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: system
  labels:
    app.kubernetes.io/name: bex-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bex-api
  ports:
    - name: http
      port: 80
      targetPort: http
