# operator/CLAUDE.md

The **operator** module (`github.com/bex-co/bex/lego/operator`) is the **mechanism** layer: the manager reconciles `App`/`Database` CRs into runtime objects. No business logic, no DB. It imports the `types/` module (the CRD contract) and **never** the `backend/` module — the `App` CR is the contract between them.

## Codegen (the footgun)

- The CRD **types live in the sibling `types/` module** (`../types/v1alpha1`), not here. After editing `../types/v1alpha1/*_types.go`, run `make manifests generate` from `lego/operator/` and keep the output.
- controller-gen reads CRD/deepcopy markers from `../types/...` and RBAC markers from `./...`; deepcopy lands in `../types/v1alpha1/zz_generated.deepcopy.go`, the CRD YAML in `config/crd/bases/`. Both are **generated — never hand-edit**.
- `make test` runs codegen automatically; envtest binaries download to `bin/` on first run (version derives from `k8s.io/api` in go.mod). **CI-enforced** on every push/PR touching `lego/operator/**` or `lego/types/**` (`.github/workflows/operator-test.yml`); the test suite must pass before `deploy.yml` proceeds.

## Layering (mechanism only)

- `cmd/manager/` — the manager entrypoint (`go build ./cmd/manager`).
- `internal/controller/` — mechanism, per runtime: `kubernetes` (Deployment/Service/Ingress) and `opensandbox` (sandbox lifecycle). No business logic.
- `internal/build/` — build plane: CNB / Dockerfile → Zot registry.
- `internal/runtime/` — OpenSandbox client.

The Render-facing API, authz, API keys, and metrics — the **business logic** — live in the `backend/` module (bex-api). See [`../backend/CLAUDE.md`](../backend/CLAUDE.md).

## Environment variables

Read by this module's entrypoints: `cmd/manager` (operator), `cmd/activator`, `cmd/pg-sni-proxy`, `cmd/kv-sni-proxy`, `cmd/egress-meter`, `cmd/staticserver` (static-server). bex-api and ssh-gateway variables live in [`../backend/CLAUDE.md`](../backend/CLAUDE.md); dashboard SSR variables in `dashboard/CLAUDE.md`.

| Component | Variable | Meaning |
| --- | --- | --- |
| operator | `BEX_RUNTIME` | `kubernetes` (Deployments) or `opensandbox` (host sandboxes) |
| operator | `BEX_REGISTRY`, `BEX_KPACK_REGISTRY`, `BEX_CNB_BUILDER` | canonical image registry (Zot, e.g. `zot.bex-registry.svc:5000`), optional kpack push alias (local HTTP default `zot.local:5000`; omit to use the canonical registry), and the CNB builder imported by the platform `ClusterBuilder` |
| operator | `BEX_REGISTRY_PUSH_SECRET`, `BEX_REGISTRY_PULL_SECRET` | registry authn (w7/m8 + w2/m59, docs/ADR022-tenant-isolation.md § Registry access control): Zot denies anonymous catalog/pull/push. `PUSH_SECRET` names the build-namespace bootstrap/shared-fallback docker config used by post-build Skopeo/Cosign and by the admission verifier; with `BEX_REGISTRY_NS`, the operator instead mirrors each repository-scoped `reg-pull-<name>` credential into `bex-build` for that App's push/sign phases. BuildKit exports an OCI archive and never receives either output credential. `PULL_SECRET` is the legacy shared kubelet `imagePullSecret` fallback when per-App credentials are disabled; unset ⇒ unauthenticated dev behavior. |
| operator | `BEX_REGISTRY_NS` | namespace of the Zot registry Secrets (`zot-htpasswd`, `zot-config`) managed by the operator for per-App pull credentials (w7/m36, docs/ADR022-tenant-isolation.md § Per-App pull credentials); set ⇒ operator mints per-App `reg-pull-<name>` Secrets + htpasswd entries + Zot ACL entries, superseding the shared `BEX_REGISTRY_PULL_SECRET`; unset ⇒ shared pull-secret path (byte-identical to w7/m8) |
| operator | `BEX_REGISTRY_BUILD_PULL_SECRET` | build-namespace dockerconfigjson Secret used as the `imagePullSecret` for the static-site publish Job's extract initContainer, which pulls the just-built tenant image from Zot (w9/m44, docs/ADR029-static-sites.md). The apps-namespace tenant pull secret (`reg-pull-<name>` / `BEX_REGISTRY_PULL_SECRET`) is **unreachable** when the publish Job runs in a separate `BEX_BUILD_NAMESPACE`, so this is a distinct build-ns credential (`scripts/registry-secrets.sh`'s `bex-registry-pull`, the `bex-builder` user with wildcard read); defaults to `bex-registry-pull` when `BEX_REGISTRY_PUSH_SECRET` is set; unset ⇒ anonymous pull (dev, byte-identical) |
| operator | `BEX_ZOT_HTPASSWD_SECRET`, `BEX_ZOT_CONFIG_SECRET` | names of the Zot htpasswd and config Secrets in `BEX_REGISTRY_NS` (defaults `zot-htpasswd` and `zot-config`); override only when the Secrets are renamed |
| operator | `BEX_REGISTRY_DUAL_READ` | `1` ⇒ during a supervised pre-m75→scoped registry migration, grant each scoped App READ on its bare-name legacy repo (`internal/registry` dual-read). **Off by default** (round-21 finding 4): the legacy repo key drops the workspace and the grant does no ownership check, so leaving it always-on let a scoped App read a same-named legacy repo owned by another workspace. Turning it off never removes an existing grant (the grant helper only adds), so a migration already in flight is unaffected; enable it only for the migration window, then tombstone via `cmd/registry-migrate` |
| operator | `BEX_ZOT_RETENTION_COUNT` | `mostRecentlyPushedCount` in the Zot per-App repo retention policy (default `5`); increase to retain more historical image tags; decrease to save registry storage |
| operator | `BEX_BUILD_CACHE` | `registry` ⇒ Dockerfile and native builds reuse layers through a per-App `<app-repo>-cache` repository in Zot (w7/m86, docs/ADR060 D3). BuildKit exports its cache to a local OCI layout and the same credentialed skopeo phases that push the image carry it in and out, so the container running tenant `RUN` steps still holds no registry credential. The cache repo inherits the App's own Zot ACL, so a workspace can never read another's layers. Both directions are best-effort: a missing, deleted or corrupt cache degrades to a clean build. Anything else (including unset) ⇒ byte-identical to before the feature existed |
| operator | `BEX_BUILD_NAMESPACE` | namespace the in-cluster BuildKit Jobs and kpack Images/builds run in; unset ⇒ the App's own namespace. Does NOT govern the pre-deploy Jobs (`predeploy-<name>-gen-<generation>`, w1/m33, docs/ADR004-app-deployment.md § Pre-deploy command): those always run in the App's own namespace (ADR043 D8 co-location) so a migration can reach workspace datastores through the tenant same-namespace allow |
| operator | `BEX_APPS_NAMESPACE` | tenant App/Database/KeyValue namespace whose Secrets the manager caches (default `default`); must match bex-api's `BEX_API_NAMESPACE` / `BEX_CP_APPS_NAMESPACE` and the namespace containing `bex-operator-apps` RoleBinding. Other resource kinds remain watched cluster-wide |
| operator | `BEX_TENANT_SIGNING_KEY_SECRET`, `BEX_TENANT_SIGNING_IMAGE` | opt-in tenant-image signing + admission verification (w6/006, w7/m11): a Secret name (keys `cosign.key`+`cosign.password`+`cosign.pub`, in the build namespace) that makes the build Job cosign-sign each pushed tenant image (build+push becomes an initContainer, cosign runs as the main container); when `cosign.pub` is also present in the Secret the `/validate-v1-pod` admission webhook rejects pods with unsigned or tampered tenant images; unset ⇒ tenant images unsigned and unverified (default, byte-identical). `BEX_TENANT_SIGNING_IMAGE` overrides the cosign image |
| operator | `BEX_OPENSANDBOX_URL` | OpenSandbox endpoint (opensandbox runtime) |
| operator | `BEX_BASE_DOMAIN`, `BEX_CLUSTER_ISSUER` | `*.onbex.co` app URLs, cert-manager issuer |
| operator | `BEX_DB_DOMAIN` | public managed-Postgres hostnames `<name>.<domain>` via Traefik TCP/SNI (docs/ADR009-postgresql-management.md); unset ⇒ internal-only |
| operator | `BEX_PROM_URL` | Prometheus base URL for the Postgres disk-autoscaling loop's already-scraped `kubelet_volume_stats_*` series; unset ⇒ the field round-trips but automatic disk growth is disabled |
| operator | `BEX_DB_BACKUP_DESTINATION`, `BEX_DB_BACKUP_ENDPOINT`, `BEX_DB_BACKUP_S3_SECRET` | Non-secret contract for managed-Postgres backups + PITR (docs/ADR009-postgresql-management.md): it must match the namespaced `bex-tenant-postgres` Barman Cloud `ObjectStore`, and supplies the S3 URL prefix, endpoint, and Secret name used by logical-export/purge Jobs. The Secret contains `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`; any variable unset ⇒ backups disabled for every plan (recovery unavailable) |
| operator | `BEX_KV_BACKUP_DESTINATION`, `BEX_KV_BACKUP_ENDPOINT`, `BEX_KV_BACKUP_S3_SECRET` | Non-secret contract for paid-plan managed-KeyValue RDB backups (docs/ADR021-keyvalue-management.md): destination prefix (production `s3://bex-tfstate/keyvalue`), S3-compatible endpoint, and apps-namespace Secret name. The Secret contains `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`; all three set ⇒ each non-Free instance gets a staggered nightly coherent RDB snapshot with keep-7 retention and delete-time prefix purge; any unset ⇒ backups disabled and no CronJobs/finalizer are added to new instances |
| operator | `BEX_KV_DOMAIN` | public managed key-value (Valkey) hostnames `<name>.<domain>` via Traefik TCP/SNI (docs/ADR021-keyvalue-management.md); unset ⇒ internal-only |
| operator | `BEX_BACKUP_AGE_PUBLIC_KEY` | ADR050 Tier A opt-in: the `age` recipient **public** key (safe to set in clear) that makes the paid-KeyValue backup Job age-encrypt each RDB before upload (`.rdb.gz.age`). The private half stays out-of-band in `.env`/CI (`AGE_BACKUP_PRIVATE_KEY`), consumed only by `scripts/restore-*.sh` to decrypt; unset ⇒ plain `.gz` upload (byte-identical to pre-ADR050). The encrypt stage runs the operator's OWN image (`/backup-encrypt`, `filippo.io/age` compiled in — w7/m85), so it installs and downloads nothing beside the plaintext RDB; with this set and that image unresolvable the reconcile fails closed. The two static backup CronJobs (etcd, OpenBao) read the same public key from the optional `bex-backup-age` ConfigMap in their namespace instead (docs/ADR050-encrypted-platform-backups.md) |
| operator | `BEX_BACKUP_HELPER_IMAGE`, `POD_NAME` | Which image the Key Value backup encrypt stage runs. Normally neither is set by hand: `POD_NAME` comes from the downward API in `config/manager/manager.yaml` and the operator reads its own Pod (`internal/selfimage`) so the stage always matches the digest the operator was rolled out with. `BEX_BACKUP_HELPER_IMAGE` overrides that for a manager running outside a Pod (`make run`). Neither is needed unless `BEX_BACKUP_AGE_PUBLIC_KEY` is set; that combination with no image resolves fails closed rather than uploading plaintext |
| operator | `BEX_DISK_SNAPSHOT_ENDPOINT`, `BEX_DISK_SNAPSHOT_BUCKET`, `BEX_DISK_SNAPSHOT_PREFIX`, `BEX_DISK_SNAPSHOT_REGION`, `BEX_DISK_SNAPSHOT_S3_SECRET`, `BEX_DISK_SNAPSHOT_AGE_PUBLIC_KEY`, `BEX_DISK_SNAPSHOT_AGE_SECRET` | Nightly persistent-disk snapshots (docs/ADR082-persistent-disks.md D5). Hetzner has no volume snapshots at any level, so each disk-bearing App gets a CronJob that streams its volume (`tar \| gzip \| age`, one pass, nothing staged) into an S3-compatible bucket with 7-day retention, and a purge Job when the disk is detached. Endpoint + bucket + S3 secret + **age public key** must ALL be set or no snapshot is taken: unlike the KeyValue path's opt-in Tier A encryption, a disk snapshot is a full copy of a tenant filesystem leaving the cluster, so bex takes none rather than an unencrypted one. The age pair is **dedicated to disks**, not `AGE_BACKUP_*` — restore needs the decrypt half in-cluster (`…_AGE_SECRET`, key `private`), and ADR050 keeps the platform key out of it. The canonical Secret pair lives in the apps namespace (`BEX_APPS_NAMESPACE`, seeded by `scripts/disk-snapshot-secret.sh`) and is **projected into each disk-bearing App's own namespace at reconcile time** (w2/033, the datastore `BackupSourceNamespace` pattern) — a workspace namespace minted after the script ran still gets its credential; a missing canonical pair sets `SnapshotCredentialUnavailable` instead of scheduling a doomed CronJob. Any unset ⇒ no CronJob, no purge Job, byte-identical to before the feature |
| operator | `BEX_ACTIVATOR_SERVICE` | k8s Service name of the wake activator (e.g. `bex-activator`); unset ⇒ auto-sleep disabled. The Service lives in the operator's own namespace (`POD_NAMESPACE`, default `bex-system`), so an auto-hibernated App in a per-tenant namespace is fronted by an App-owned `bex-activator-<app>` ExternalName alias rather than this name directly — an Ingress backend resolves only within the Ingress's namespace, and naming the platform Service directly is what left every sleeping free service answering Traefik's default 404 (w6/m47) |
| operator | `BEX_ACTIVATOR_PORT` | activator service port (default `8888`) |
| operator | `BEX_DISK_STORAGE_CLASS` | StorageClass backing persistent service disks (`spec.disk`, docs/ADR082-persistent-disks.md). Unset ⇒ the claim names no class and Kubernetes uses the cluster **default** — what the CAPD mock runs (`local-path`; the LUKS class cannot exist without the hcloud CSI driver). **Production sets `hcloud-volumes-luks`** in the prod overlay (`lego/operator/config/prod/kustomization.yaml`, w2/m86) for encryption at rest; both former gating prerequisites were verified live 2026-09-02 — `cryptsetup` ships inside the hcloud-csi-driver image, and the kubelet (not the CSI SA) reads the per-disk passphrase Secret. `scripts/gitops-validate.sh` pins the prod-on/local-off split and the class's `${pvc.name}-luks` template ↔ `DiskLUKSSecretName` agreement. Apps without `spec.disk` never read this |
| activator | `BEX_ACTIVATOR_ADDR` | listen address (default `:8888`) |
| pg-sni-proxy | `BEX_PROXY_ADDR`, `BEX_PROXY_METRICS_ADDR`, `BEX_PROXY_PROTOCOL_TRUSTED_CIDRS` | TCP listen address (default `:5432`), internal Prometheus address (default `:9092`), and comma-separated immediate-peer CIDRs trusted to assert the public client through PROXY protocol; the proxy handles PostgreSQL SSLRequest preamble, routes by SNI to CNPG backends using `BEX_DB_DOMAIN`, and meters backend→client bytes (docs/ADR009-postgresql-management.md §SNI proxy) |
| pg-sni-proxy | `BEX_PROXY_MAX_CONNS`, `BEX_PROXY_MAX_CONNS_PER_SOURCE`, `BEX_PROXY_MAX_CONNS_PER_WORKSPACE`, `BEX_PROXY_MAX_CONNS_PER_RESOURCE`, `BEX_PROXY_IDLE_TIMEOUT`, `BEX_PROXY_MAX_LIFETIME` | connection-admission + copy-loop bounds (F6): global concurrent cap (`1024`), per-source cap after PROXY resolution (`128`), post-SNI per-workspace/resource fairness caps (`256`/`64`), and idle (`1h`) + max-lifetime (`24h`) budgets; overload sheds immediately and is metered rather than queueing. `0` disables that dimension |
| kv-sni-proxy | `BEX_KV_PROXY_ADDR`, `BEX_KV_PROXY_METRICS_ADDR`, `BEX_KV_DOMAIN`, `BEX_PROXY_PROTOCOL_TRUSTED_CIDRS` | public TLS/SNI front door (defaults `:6379` and `:9093`), wildcard domain, and comma-separated immediate-peer CIDRs trusted to assert the public client through PROXY protocol; routes exact public KeyValue intent to Valkey TLS port `6380`, enforces source allowlists, and meters backend→client bytes |
| kv-sni-proxy | `BEX_KV_PROXY_MAX_CONNS`, `BEX_KV_PROXY_MAX_CONNS_PER_SOURCE`, `BEX_KV_PROXY_MAX_CONNS_PER_WORKSPACE`, `BEX_KV_PROXY_MAX_CONNS_PER_RESOURCE`, `BEX_KV_PROXY_IDLE_TIMEOUT`, `BEX_KV_PROXY_MAX_LIFETIME` | the Valkey front door's mirror of the pg-sni-proxy bounds, with defaults `1024` / `128` / `256` / `64` / `1h` / `24h`; `0` disables that dimension |
| egress-meter | `BEX_EGRESS_NODE_NAME`, `BEX_EGRESS_METRICS_ADDR`, `BEX_EGRESS_EXCLUDED_CIDRS`, `BEX_APPS_NAMESPACE` | node identity, internal Prometheus address (default `:9091`), extra comma-separated private/cluster CIDRs, and watched App namespace for the post-policy direct-public L3 meter |
| operator | `BEX_STATIC_S3_ENDPOINT`, `BEX_STATIC_S3_BUCKET`, `BEX_STATIC_S3_REGION`, `BEX_STATIC_PUBLISH_S3_SECRET` | static-site (`static_site`) publish target (docs/ADR029-static-sites.md): S3-compatible endpoint + a bucket **dedicated to static content** (never `bex-tfstate`) + optional region + a bucket-scoped publish/purge Secret name in `BEX_BUILD_NAMESPACE` (production `bex-static-publish-s3`); before dispatch the operator requires both AWS credential keys, and any setting/Secret missing ⇒ the App fails with `StaticCredentialUnavailable` |
| operator | `BEX_STATIC_SERVER_SERVICE`, `BEX_STATIC_SERVER_PORT` | k8s Service name + port (default `8080`) of the shared static-server a static-site host's Ingress backs onto (docs/ADR029-static-sites.md); service unset ⇒ `static_site` serving unavailable |
| static-server | `BEX_STATIC_ADDR`, `BEX_STATIC_NAMESPACE`, `BEX_STATIC_S3_ENDPOINT`/`_BUCKET`/`_REGION`, `BEX_STATIC_CACHE_BYTES`, `BEX_STATIC_RESYNC` | listen addr (default `:8080`), watched App ns (empty ⇒ all), object-store origin (endpoint/bucket unset ⇒ degraded 503 mode), in-memory cache budget (default 256 MiB), host→site refresh interval (default `10s`); AWS creds come only from required `bex-system/bex-static-read-s3`, a read-only `bex-static` identity distinct from the publisher; startup fails if its signed origin check is denied |
| operator | `BEX_APP_RECONCILE_WORKERS` | concurrent App reconcile loops (default `1`, production `2`). **Post-D1** (`w2/m72`, builds are observed non-blocking) this is a pure responsiveness knob — the two build caps below carry the concurrency policy; the pre-D1 "workers == concurrent builds" equation is retired (ADR034 → docs/ADR060 D1/D7) |
| operator | `BEX_MAX_CONCURRENT_BUILDS` | max concurrent active build Jobs **per workspace**; `0` = unlimited. Newest-wins per App is always active (independent of this cap) |
| operator | `BEX_MAX_ACTIVE_BUILDS` | max concurrent active builds **cluster-wide**, across every workspace (w7/m83, docs/ADR060 D6): the per-workspace cap bounds one tenant's fan-out but not the estate's, and after D1 admission is the build plane's only concurrency control. A dispatch beyond it sets `BuildQueued` and requeues rather than starting; a racing double-create is self-corrected by re-counting after create and deleting the **newest** build, never the running one. Production sets `4` = the D8 pools' physical concurrency (2 warm lg slots + 2 elastic burst slots), so it can never throttle below what the pools can run; `0` = unlimited (byte-identical to before). Observable as `bex_builds_active` / `bex_builds_queued` |
