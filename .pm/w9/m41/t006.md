---
id: w9/m41/t006
title: Closeout
worker: worker9
status: todo
estimate: 15m
depends_on: [w9/m41/t005]
---

## Objective

When t001–t005 are complete and the DoD holds, close the milestone.

## Context

- DoD: `renderKeyValue` comment matches its code; `.pm/w6/016.md` sits in `.pm/w6/done/016.md`; every datastore/service REST read path emits `owner`/`region`/`dashboardUrl`/`updatedAt` identically where source data exists (drift fixed or documented); a cross-sibling parity test locks it.

## Steps

1. Verify the DoD is actually met (not just code written) — build/test/lint green, comment correct, note moved, parity test present and passing.
2. Set every remaining task `status: done`, move each `tNNN.md` to `w9/m41/done/`, mark rows `— **DONE**` and set `**Status:** done` in the milestone README.
3. Move the whole `w9/m41/` directory to `w9/done/m41/`; check `- [x]` in `.pm/w9/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w9/m41/**`, `.pm/w9/README.md`

## Acceptance criteria

- [ ] DoD verified real; milestone moved to `w9/done/m41/`; workstream checkbox checked.

## Out of scope

- Closing before the DoD holds.
