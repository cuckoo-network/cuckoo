---
id: w9/m41/t001
title: Fix stale renderKeyValue comment + retire resolved w6/016
worker: worker9
status: todo
estimate: 20m
depends_on: []
---

## Objective

Correct the one doc comment that contradicts the shipped keyvalue wire shape, and retire the `w6/016` inbox note now that its owner/region/dashboardUrl/updatedAt gap is fully closed by the `resourcemeta` adoption.

## Context

- `lego/backend/internal/keyvalue/rest.go:52-54` — the `renderKeyValue` doc comment claims "Region/version are deliberately omitted rather than faked — bex doesn't track either." But `renderKeyValues` sets `rendered.Region = s.Metadata.PlatformRegion()` and `rendered.DashboardURL = s.Metadata.DashboardURL("keyvalue", kv.ID)`. Only `version` is genuinely omitted.
- `.pm/w6/016.md` — the cross-cutting note "Postgres/KeyValue/Service REST objects never populate Render's owner/region/dashboardUrl/updatedAt". Now resolved: `internal/resourcemeta` (ResolveOwners, PlatformRegion, DashboardURL, UpdatedAt) is wired into postgres (`rest.go` `renderPostgres`), apps (`render.go` `renderService`), and keyvalue (`rest.go` `renderKeyValue`).
- `.pm/w6/done/` already exists.

## Steps

1. Edit the `renderKeyValue` comment so it describes reality: `region`/`dashboardUrl` ARE populated (via `resourcemeta`); only `version` is omitted (bex doesn't track it).
2. `git mv .pm/w6/016.md .pm/w6/done/016.md` (or `mv`); add a one-line "Retired 2026-07-15 — resolved by resourcemeta adoption (w9/m41)" header at the top for provenance.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `lego/backend/internal/keyvalue/rest.go`
- `.pm/w6/016.md` → `.pm/w6/done/016.md`

## Acceptance criteria

- [ ] The `renderKeyValue` comment no longer claims `region`/`dashboardUrl` are omitted; it correctly states only `version` is.
- [ ] `.pm/w6/016.md` no longer exists; `.pm/w6/done/016.md` exists with a retirement header.
- [ ] `cd lego/backend && go build ./...` still green (comment-only code change).

## Out of scope

- Any behavior change to what fields are emitted (t002 audits/fixes that).
