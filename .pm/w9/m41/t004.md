---
id: w9/m41/t004
title: Simplify
worker: worker9
status: todo
estimate: 20m
depends_on: [w9/m41/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Diff is small: a comment fix (t001) plus any read-path routing fixes (t002). If t002 fixed handlers that bypassed the `render*` layer, check whether the three sibling packages can share more of the wire-shaping via `resourcemeta` helpers instead of near-duplicate `renderOwner`/`ownerToRender`/`keyValueOwner` constructions.

## Steps

1. `/simplify` over the milestone diff.
2. Apply only behavior-preserving reductions; re-run `go test ./...` + `make lint-backend`.

## Files

- The files touched by t001/t002.

## Acceptance criteria

- [ ] `/simplify` run; any behavior-preserving reduction applied or a note recorded that none was warranted.
- [ ] `go test ./...` and `make lint-backend` green.

## Out of scope

- Behavior changes (belongs in t002).
