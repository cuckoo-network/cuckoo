---
id: w9/m58/t001
title: Field-shape decision from the m57 capture
worker: worker9
status: todo
estimate: 30m
depends_on: []
---

## Objective

Turn `docs/render-artifacts/service-addresses.md` (w9/m57 t001) into the concrete field contract this milestone implements: exact REST field name(s) for the internal address on `web_service` and `private_service` `serviceDetails`, whether pserv `url` changes meaning, and the GraphQL/MCP projections — written into ADR041 D4 before any code.

## Context

- Gate: w9/m57 must have shipped (address resolvable, capture exists). If the capture found Render exposes **no** REST internal-address field (dashboard-only), the REST scope here shrinks to matching that truthfully — bex then decides extension-vs-omission the way `get_usage`/`suspend_keyvalue` documented bex extensions.
- Adapter conventions: one core verb, three thin fragments (`lego/backend/CLAUDE.md`); field naming follows Render's captured JSON exactly (the m38 "one error dialect" lesson: copy the wire shape, don't approximate).
- `sshAddress` precedent for an address-bearing serviceDetails field: `BEX_SSH_HOST` handling in bex-api.

## Steps

1. Extract the captured field names/values; decide the bex mapping table (REST/GraphQL/MCP × web/pserv).
2. Record it in ADR041 D4 (replacing the "gated on a live capture" sentence) + note any bex-extension calls.
3. Sanity-check against `serviceDetails.url` current behavior (t005 of m57 changed the value, not the field).

## Files

- `docs/ADR041-service-addresses.md` (D4)
- `docs/render-artifacts/service-addresses.md` (read)

## Acceptance criteria

- [ ] A field-mapping table exists in ADR041 D4 with a capture citation per row.
- [ ] Extension-vs-parity is explicit for every field bex adds beyond the capture.

## Out of scope

- Implementation (t002/t003).
