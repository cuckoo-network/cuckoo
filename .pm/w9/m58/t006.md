---
id: w9/m58/t006
title: "Test coverage: adapters + dashboard"
worker: worker9
status: todo
estimate: 45m
depends_on: [w9/m58/t004]
---

## Objective

Standing closing task: meaningful tests for the surface behavior — cross-adapter equality, type gating, legacy fallback, and the dashboard affordance.

## Context

- Backend: a three-surface parity test in the style of the m41/m42 cross-sibling harness (same service ⇒ same address value on REST/GraphQL/MCP); omission tests for worker/cron/static; legacy slug-less fallback value.
- Dashboard: component tests for the Connect affordance (shown/hidden by type, copy action, pserv label); deploy-header presence.

## Steps

1. Add the parity + omission + fallback tests where t002's unit tests left gaps.
2. Add the dashboard component tests.
3. Run all suites.

## Files

- `lego/backend/internal/apps/*_test.go` (or the shared parity harness location)
- `dashboard/src/**/*.test.tsx`

## Acceptance criteria

- [ ] Each behavior above has a regression-catching assertion; no snapshot filler.

## Out of scope

- m57 mechanism tests (done there).
