---
id: w9/m58/t005
title: "Simplify: /simplify over the changed code"
worker: worker9
status: todo
estimate: 20m
depends_on: [w9/m58/t004]
---

## Objective

Standing closing task: run `/simplify` over m58's diff (apps feature derivation + three adapter fragments + dashboard components); behavior-preserving cleanups only.

## Context

- Candidate: the address derivation helper vs the operator's — same string from one contract-level rule if a shared home exists in `types/` (cf. `EffectiveHosts` living on the CRD contract for exactly this reason).
- Candidate: dashboard copy-button reuse (datastore reveals) — no third copy-pattern.

## Steps

1. `/simplify` scoped to the m58 diff; apply accepted findings.
2. Re-run backend + dashboard suites.

## Files

- as flagged

## Acceptance criteria

- [ ] Review ran; findings applied; suites green.

## Out of scope

- Behavior changes.
