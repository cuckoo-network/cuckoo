---
id: w9/m58/t004
title: "Render parity: three-surface + UI consistency sweep"
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m58/t003]
---

## Objective

Standing closing task: verify the new internal-address surface is consistent across REST, GraphQL, MCP, and the dashboard, matches the captured Render behavior, and lands in the parity ledger; flag drift as follow-ups.

## Context

- Compare live bex readbacks + UI against `docs/render-artifacts/service-addresses.md`; add/annotate the address rows in `docs/ADR018-render-parity.md` with evidence (the ledger's ✅/◐ discipline).
- Check the official Render CLI (`docs/cli-compatibility-checklist.md`) for any command that prints the address fields — regrade rows if touched.

## Steps

1. Live sweep: one web + one pserv across all four surfaces; values identical, shapes match the capture.
2. Update ADR018 rows + checklist rows touched; file follow-ups for any divergence not covered by ADR041 D5.

## Files

- `docs/ADR018-render-parity.md`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] Ledger rows carry dated evidence; every divergence is D5-documented or filed.

## Out of scope

- New feature work discovered by the sweep (file it, don't build it).
