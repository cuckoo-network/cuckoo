---
id: w9/m39/t005
title: Assess CLI psql out-of-box friction — record the answer on the checklist row
worker: worker9
status: todo
estimate: 30m
depends_on: []
---

## Objective

`docs/cli-compatibility-checklist.md:89` (`psql --command`, ◐) records that end-to-end access requires `BEX_DB_DOMAIN` and an IP allow-list permitting the caller — manual setup Render users don't need. Decide what a sane default looks like (resolvable public DB domain in the standard deploy? documented allow-list guidance? better error text pointing at the missing config?), record the answer on the checklist row, and promote to real work if it turns into any. Absorbs note `w9/007`.

## Context

- Source: `/pm-brainstorm` round 14's CLI-checklist miner.
- Related mechanisms: `BEX_DB_DOMAIN` Traefik TCP/SNI routing (`docs/ADR009-postgresql-management.md` §SNI proxy), ipAllowList semantics (`w4/m28`/`m29` in flight — coordinate, don't collide).
- Distinct from the KeyValue `red-` datastore-id routing gap (owned by `w9/m6`).

## Steps

1. Reproduce the friction on dev-9 or prod config review: what exactly fails for a fresh workspace running `render psql`?
2. Write the assessment (sane-default recommendation + error-text gaps) into the checklist row.
3. If concrete bex-api/operator work falls out, file it (`w9/NNN` note or the owning workstream) and link it from the row.

## Files

- `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] The `psql` row records the friction assessment and either "no work" or a filed owner.

## Out of scope

- Implementing the chosen default (file it; this task is the assessment).
