---
id: w9/m39/t006
title: Render parity — sweep every surface the dialect fixes touch
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m39/t001, w9/m39/t002, w9/m39/t003, w9/m39/t004, w9/m39/t005]
---

## Objective

Standing closing task. Confirm the dialect fixes are consistent across every surface they expose: the 429 and revoke bodies over REST, and that GraphQL/MCP error paths touching the same features didn't drift (same fields, semantics, error shapes — `docs/ADR006-bex-api.md`). Compare against Render's behavior and flag any drift as follow-up work rather than silently diverging.

## Context

- `lego/backend/internal/logs/`, `lego/backend/internal/cliauth/`.
- The deliberate survivors list (device endpoints, :8091 store API) must stay annotated and justified.

## Steps

1. Grep for remaining bare-`{"error"}` non-2xx writes; verify each is an annotated deliberate survivor.
2. Check the GraphQL and MCP adapters for the logs feature reject over-limit subscriptions with consistent semantics.
3. File any drift as `w9/NNN` notes.

## Files

- `lego/backend/internal/logs/`, `lego/backend/internal/cliauth/`

## Acceptance criteria

- [ ] No unannotated second-dialect error body remains; any drift found is filed, not silently left.

## Out of scope

- Fixing newly found drift (file it).
