---
id: w9/m39/t003
title: Live render login bad-key probe — attach evidence to the checklist login row
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m39/t001]
---

## Objective

Run the live CLI probe m38's closeout recommended but didn't run: `render login` with a bad key against a dev-9 stack, confirming the CLI surfaces `message: "unauthorized"` verbatim (previously a generic "unknown error" off the text/plain body). Attach the captured output as evidence on the checklist's login row. Absorbs note `w9/009`.

## Context

- Filed by `w9/done/m38/done/t003.md` (Render parity, 2026-07-15): the unified 401 `{"error","message","id"}` body was verified via `internal/api.TestErrorDialectIsRenderShaped` but never against the real CLI.
- Stack: `bash .pm/w9/dev-9/up.sh`; locally built `render` binary (the `render-oss/cli` checkout used by w9/m2 — never fork/modify it, DO_NOT_DO).
- Depends on t001 only to avoid capturing evidence against a tree that's about to change error bodies again.

## Steps

1. Bring up dev-9; point the CLI at it per `docs/cli-compatibility-checklist.md`'s harness notes.
2. `render login` (or the API-key path the row documents) with a deliberately invalid key; capture stdout/stderr.
3. Update the login row in `docs/cli-compatibility-checklist.md` with the evidence; `npx prettier@3.4.2 --write` the file.

## Files

- `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] The login row cites captured live output showing the Render-shaped `unauthorized` message.

## Out of scope

- Any CLI code change (DO_NOT_DO — gaps become bex-api work).
