---
id: w9/m40/t005
title: Closeout
worker: worker9
status: todo
estimate: 15m
depends_on: [w9/m40/t004]
---

## Objective

When the other tasks are complete **and the definition of done is actually met**, close the milestone: set every remaining task's `status: done`, move each `tNNN.md` to `w9/m40/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone README, move the whole `w9/m40/` directory to `w9/done/m40/`, and check `- [x]` in the workstream README.

## Acceptance criteria

- [ ] DoD verified real (no store anywhere without `spec.name`; prod smoke passed), not merely scripts-ran.
- [ ] Board state synced in all three places.
