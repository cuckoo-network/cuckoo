---
id: w9/m57/t008
title: "Test coverage: slug Service, fromService resolution, status.URL"
worker: worker9
status: todo
estimate: 45m
depends_on: [w9/m57/t006]
---

## Objective

Standing closing task: meaningful tests for m57's shipped behavior and its failure modes — beyond the per-task tests, cover the interactions.

## Context

- Interaction seams worth asserting: slug == name (one Service, not two fighting reconciles); slug immutability across re-deploys (t004 reuse); type change web→worker deletes both Services; legacy slug-less path byte-identical (operator + backend); NetworkPolicy reachability unaffected (unit-level: policy objects unchanged by m57).
- Suites: operator envtest (`make test` from `lego/operator/`), backend `go test ./...`; live matrix `make verify-tenant-isolation` if a cluster is up.

## Steps

1. Add the interaction tests above where per-task tests left gaps.
2. Negative tests: fromService referencing a worker still errors (deploy.go:1424 rule); slug Service never created for cron/static/worker.
3. Run all suites.

## Files

- `lego/operator/internal/controller/*_test.go`
- `lego/backend/internal/apps/*_test.go`

## Acceptance criteria

- [ ] Each listed seam has an assertion that fails on regression.
- [ ] No tautological/snapshot filler.

## Out of scope

- m58 surface tests.
