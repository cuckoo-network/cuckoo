---
id: w9/m57/t007
title: "Simplify: /simplify over the changed code"
worker: worker9
status: todo
estimate: 20m
depends_on: [w9/m57/t006]
---

## Objective

Standing closing task: run `/simplify` over the code m57 changed (operator Service reconcile, backend `resolveRef` plumbing, URL derivation) — reuse/simplification/efficiency, behavior-preserving.

## Context

- Candidate: the slug-Service reconcile vs the existing Service reconcile — one helper, not a copy.
- Candidate: `resolveRef`'s grown inputs (names/ports/slugs) — a sibling-info struct beats three parallel maps.

## Steps

1. `/simplify` scoped to the m57 diff.
2. Apply accepted findings; re-run `make test` + backend tests.

## Files

- as flagged by the review

## Acceptance criteria

- [ ] Review ran; accepted findings applied; suites green.

## Out of scope

- Behavior changes; bug hunting (that's /code-review).
