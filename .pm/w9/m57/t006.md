---
id: w9/m57/t006
title: "Render parity: sweep every surface the address values changed on"
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m57/t004, w9/m57/t005]
---

## Objective

Standing closing task: verify the address-value changes (t004 injected literals, t005 status.URL) read back consistently across REST, GraphQL, and MCP, and against t001's captured Render behavior; flag any drift as follow-up work instead of silently diverging.

## Context

- bex-api is one core + thin adapters (`docs/ADR006-bex-api.md`); a value change must not fork per surface.
- Compare against `docs/render-artifacts/service-addresses.md` (t001). The parity ledger (`docs/ADR018-render-parity.md`) gains/updates address notes per ADR041 Consequences.
- Dashboard: anywhere `serviceDetails.url`/`status.URL` renders (service detail, lists) must tolerate the new value shape.

## Steps

1. Read back a store-managed pserv + web service over REST, GraphQL, MCP: identical address values on all three.
2. Grep the dashboard for consumers of the url fields; verify rendering (no `.svc` assumptions).
3. Diff observed bex behavior vs the t001 capture; file follow-ups (inbox notes or m58 tasks) for any drift.
4. Update ADR018 rows/notes touched.

## Files

- `docs/ADR018-render-parity.md`
- follow-up notes under `.pm/w9/` as found

## Acceptance criteria

- [ ] Three-surface readback identical; dashboard renders the new values.
- [ ] Every divergence vs the capture is either a documented conscious divergence (ADR041 D5) or a filed follow-up.

## Out of scope

- Building the internal-address field/UX (m58).
