---
id: w9/m60/t009
title: "Entry attribution: split the graph-pinned route pages + verify libs"
worker: worker9
status: todo
estimate: 1h
depends_on: [w9/m60/t005]
---

## Objective

Absorbed from `w5/043` (w5/m67's follow-up): m67 proved entry pinning is **graph-dependent** — some exported route pages were already split, others pinned into the entry — so the remaining ~21 allowlisted route pages and the heavy per-feature libraries need analyzer-driven attribution, not guesswork. Attribute the post-t001–t005 entry chunk and split what's actually pinned.

## Context

- After m67 the entry was ~5.5 MB raw / ~1.18 MB gzip; t001–t004 will have shrunk it further — attribute what remains.
- The ~21 remaining exported route pages are allowlisted in `dashboard/src/routes/__tests__/no-new-route-component-export.test.ts` (m67); m67's finding: e.g. `services.$serviceId.scaling` was already its own chunk despite its export, while `agents`/`databases` were entry-pinned.
- Libs to confirm absent from the entry (the sweep believes they're already split — verify, don't re-do): `@xterm/*` (own ~290 kB chunk), `@tiptap/*`, `ai`/`@ai-sdk/react` (`session-conversation-impl` ~500 kB chunk), `@ory/elements-react` (`use-ory-flow` ~205 kB), `react-markdown` + `remark-*`/`rehype-*`.
- Tool: the t005 visualizer (`ANALYZE=1 yarn build`).

## Steps

1. Run the analyzer; list which allowlisted route pages actually sit in the entry chunk and which libs (if any) leak into it.
2. For each entry-pinned page: break the pinning edge (usually a shared-module import chain) so it becomes a lazy route chunk; shrink the allowlist accordingly. Document any page deliberately left pinned and why.
3. Re-verify chunk-sharing constraints for the `pendingComponent` routes (m67 t002's concern) still hold.
4. Record the final entry raw+gzip in the milestone README next to t005's numbers.

## Files

- `dashboard/src/routes/*` and the shared modules pinning them
- `dashboard/src/routes/__tests__/no-new-route-component-export.test.ts` (allowlist shrink)
- `.pm/w9/m60/README.md` (numbers)

## Acceptance criteria

- [ ] Every allowlisted route page is dispositioned: split (allowlist entry removed) or documented-pinned with a reason.
- [ ] Analyzer output confirms none of the listed heavy libs are in the entry chunk.
- [ ] Full dashboard suite + `yarn build` green; final entry size recorded.

## Out of scope

- A CI bundle-size budget (note it as follow-up if the numbers justify it); new lib splits beyond the listed set.
