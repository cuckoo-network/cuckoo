---
id: w9/m3/t012
title: Closeout — verify the DoD, move tasks and milestone to done, and sync the w9 roadmap
worker: worker9
status: todo
estimate: 15m
depends_on: [w9/m3/t010, w9/m3/t011]
---

## Objective

Close the milestone only after the stable-id rename contract is implemented, tested, documented, and live in production plus every `dev-*` environment.

## Context

- Code completion alone does not satisfy this milestone: its DoD includes non-destructive migration and live rollout evidence across ten environments.
- `.pm` completion requires task files and the whole milestone directory to move into `done/`, with status synchronized in task frontmatter, milestone README, and the w9 roadmap.

## Steps

1. Re-verify every Definition of Done clause, including sanitized dev-1…dev-10 and production no-recreation evidence.
2. Confirm all required suites, CLI verifier, Markdown formatting, and repo status checks pass; distinguish unrelated pre-existing failures explicitly.
3. Mark every task done and move each task file to `w9/m3/done/`, marking every table row `— **DONE**` and `**Status:** done`.
4. Move the whole milestone directory to `w9/done/m3/` and check the m3 row in `w9/README.md`; leave no tombstone at `w9/m3/`.

## Files

- `.pm/w9/m3/`
- `.pm/w9/done/m3/`
- `.pm/w9/README.md`

## Acceptance criteria

- [ ] The observable DoD is true in code, docs, dev-1…dev-10, and production.
- [ ] All task frontmatter, milestone rows/status, and the workstream checkbox agree.
- [ ] The completed milestone exists only at `.pm/w9/done/m3/`; `.pm/w9/m3/` no longer exists.
- [ ] Closeout evidence contains no secrets or kubeconfig contents.

## Out of scope

- Marking the milestone done while any rollout, parity, test, or documentation requirement remains incomplete.
