---
id: w9/m3/t010
title: Simplify — run $simplify over the identity, lookup, migration, and UI changes
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m3/t009]
---

## Objective

Run the standing simplification pass over everything changed by the milestone, removing duplicated identity/name logic and unnecessary migration/UI complexity without changing behavior.

## Context

- The highest regression risk is reintroducing `metadata.name == display name` assumptions in one of many Postgres consumers.
- Legacy fallback, name uniqueness, lookup/authz, and adapter mapping should each have one obvious implementation point.
- Migration and rollout scripts need clear, bounded, idempotent control flow because they run against production state.

## Steps

1. Invoke `$simplify` on the milestone diff and inspect backend, operator, dashboard, scripts, and docs findings.
2. Consolidate duplicated effective-name, lookup, validation, and mapper logic; keep package boundaries and operator → types ← backend direction intact.
3. Simplify shell migration/verifier paths while retaining dry-run, explicit apply, namespace isolation, and no-secret-output guards.
4. Re-run the focused and full relevant suites after behavior-preserving changes.

## Files

- `lego/types/v1alpha1/`
- `lego/backend/internal/id/`
- `lego/backend/internal/postgres/`
- `lego/operator/internal/controller/`
- `dashboard/src/features/databases/`
- `scripts/postgres-name-migrate.sh`
- `scripts/postgres-rename-verify.sh`

## Acceptance criteria

- [ ] `$simplify` findings are applied or explicitly declined with reasons in the milestone evidence.
- [ ] Effective display-name fallback and rename validation each have one canonical implementation.
- [ ] No behavior, compatibility, authorization, or rollout-safety regression is introduced.
- [ ] Relevant suites remain green.

## Out of scope

- Broad refactors outside code touched by this milestone.

