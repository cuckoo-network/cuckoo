# Assess CLI `psql` out-of-box friction

`docs/cli-compatibility-checklist.md:89` (`psql --command`, ◐): "end-to-end access requires `BEX_DB_DOMAIN` and an IP allow-list that permits the caller" — manual setup Render users don't need. Sub-hour assessment: what would a sane default look like (resolvable public DB domain in the standard deploy? documented allow-list guidance? better error text pointing at the missing config)? Record the answer on the checklist row; promote if it turns into real work. Distinct from the KeyValue `red-` datastore-id routing gap (owned by `w9/m6`).

Source: `/pm-brainstorm` round 14, 2026-07-15 (CLI-checklist miner).
