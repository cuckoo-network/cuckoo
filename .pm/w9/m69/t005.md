---
id: w9/m69/t005
title: Simplify
worker: worker9
status: todo
estimate: 20m
depends_on: [w9/m69/t004]
---

## Objective

Run `/simplify` over the skeleton + pending-config changes (reuse / simplification / efficiency; behavior-preserving).

## Context

- Scope: the ~8 list-route skeletons (t001) and the pending-config audit (t002).
- Prefer one shared skeleton idiom over per-route bespoke placeholders.

## Steps

1. Run `/simplify` over the diff.
2. Consolidate duplicated skeleton markup into a shared component if it reduces code; behavior-preserving only.

## Files

- The files changed by t001–t002.

## Acceptance criteria

- [ ] `/simplify` applied; behavior unchanged; tests green.

## Out of scope

- Bug hunting (`/code-review`).
