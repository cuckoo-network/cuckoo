---
id: w9/m69/t006
title: Test coverage
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m69/t004]
---

## Objective

Add meaningful tests for the pending UX: covered list routes render a content skeleton while pending, and the persistent-shell invariant still holds.

## Context

- Assert real behavior: a route in its pending state renders skeleton placeholders (not a centered spinner), scoped to the content region.
- Guard the persistent-shell invariant if not already covered (sidebar not re-created) so a pending-config change can't silently reintroduce the flash.

## Steps

1. Add a test asserting a covered list route's pending state renders its skeleton.
2. Add/confirm a test guarding that the pending fallback stays within the content region (not full-viewport).
3. Keep it behavior-asserting, not snapshot-everything.

## Files

- `dashboard/src/routes/__tests__/*` and/or `common/root-route/__tests__/*`

## Acceptance criteria

- [ ] Skeleton-while-pending test passes and fails if the skeleton is removed.
- [ ] Content-region-scoping guard passes.
- [ ] `yarn test` green.

## Out of scope

- Snapshot-everything tests.
