---
id: w9/m69/t003
title: Verify no flash / no double-mount across the audited routes
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m69/t001, w9/m69/t002]
---

## Objective

Prove the skeletons + pending-config changes preserve the persistent-shell guarantee: no white flash, no chrome remount, on the list routes and the audited detail routes.

## Context

- The persistent-shell ship (`da6b55b2`) verified this via a stamped-sidebar DOM walk (same node survives navigation, zero subtree removals). Reuse that method.
- Regression risk: removing a `pendingComponent` could reintroduce a content-region blank; a skeleton wired wrong could paint full-viewport.

## Steps

1. Playwright walk across the ~8 list routes and the audited detail routes; assert the stamped sidebar node persists and `sidebarRemovals === 0`.
2. Throttle the network so pending shows; assert a content skeleton (not a full-viewport blank) appears in the content region.
3. Confirm no console hydration/chunk errors.

## Files

- Playwright interaction (record results in `.pm/w9/m69/README.md`).

## Acceptance criteria

- [ ] Sidebar node persists across every audited navigation (0 removals).
- [ ] Pending state paints a content skeleton in the content region, never full-viewport.
- [ ] No console errors.

## Out of scope

- Automated coverage (t006).
