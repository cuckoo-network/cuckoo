---
id: w9/m69/t002
title: "Audit + rationalize the redundant pendingMs:0 / component-as-pending"
worker: worker9
status: todo
estimate: 45m
depends_on: []
---

## Objective

Re-examine the per-route pending config now that the shell is persistent: each `pendingComponent: RouteComponent` + `pendingMs: 0` was a workaround for the old full-viewport blank, some of which no longer earns its place. Keep what still helps, remove the rest, and update the docs.

## Context

- `grep` finds 2 `pendingComponent: RouteComponent` routes and 9 `pendingMs: 0` occurrences in `dashboard/src/routes/*.tsx`.
- Pre-persistent-shell, these made a detail route render its own chrome+skeleton at 0ms instead of the chrome-less router blank. Now the chrome always persists, so the reason shifts to "show the detail frame's skeleton immediately" — still valid for some, redundant for others.
- `dashboard/CLAUDE.md` § Navigation pending states documents the old rationale and must be updated to match whatever this audit concludes (the persistent-shell ship already amended that section — keep it truthful).

## Steps

1. For each occurrence, decide: does it still improve the pending UX given the persistent shell? Keep + note why, or remove.
2. Ensure any route that keeps `pendingComponent: RouteComponent` still satisfies the chunk-sharing constraint (interacts with w5/m67/t002).
3. Update `dashboard/CLAUDE.md` § Navigation pending states so the documented pattern matches the code.

## Files

- The detail route modules with `pendingMs: 0` / `pendingComponent: RouteComponent`
- `dashboard/CLAUDE.md`

## Acceptance criteria

- [ ] Every `pendingMs: 0` / component-as-pending occurrence is either justified (documented) or removed.
- [ ] `CLAUDE.md` § Navigation pending states matches the resulting code.
- [ ] No white flash / double-mount regression (verified in t003).

## Out of scope

- The list-route skeletons (t001).
