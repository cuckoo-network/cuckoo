# 051 — Auth polish residuals from the 2026-08-19 Playwright hunt (sub-hour)

Filed by `/pm` handoff of live `dashboard.bex.co` findings after **w9/m89** took the real bugs. Type: polish / upstream-Ory leftovers. **Sub-hour**, so an inbox note, not a milestone.

Not in m89 (either Ory Elements/upstream, SPA convention, or too small alone):

- **Provider label casing** — live button text is `Sign in with github` / `Sign up with github` (lowercase brand). Fix only if an Ory intl/override is cheap; otherwise leave until Elements upgrade.
- **zh login mixed English** — after language switch, chrome is Chinese (`欢迎回来`) but Ory still shows `E-Mail` and mixed "email / github" fragments. Ory Elements locale coverage; don't invent a parallel form.
- **SPA unknown paths return HTTP 200** with a 404 page (`Page not found ・ bex Dashboard`). Common for CSR/SSR apps; only change if we intentionally want HTTP 404 for crawlers/monitors.
- **`?next=https://evil…` stays in the address bar** — **not a bug**: Kratos `return_to` is already forced to `https://dashboard.bex.co/` via `safeNext`. Optional UX: scrub the query after normalize (cosmetic).
- **Authenticated surface hunt** — services/agents/logs/billing/settings were not exercised (all bounce to login without a session). Needs a logged-in Playwright profile before filing product bugs.

Evidence: `.playwright-mcp/hunt2.json`, `hunt3.json`, `hunt-*.png`.
