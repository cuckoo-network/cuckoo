# URGENT: prod deploy blocked — stale KV conformance allowlist entry + supersession backlog

Found 2026-08-20 verifying w9/m91 live: the cron fix (and cli/db/kv fixes) are **built but not on prod**. Live bex-api still accepts `99 99 * * *` (creates → App Failed); live dashboard still shows pre-m91 chrome.

## Immediate blocker (quick fix)

The current HEAD's `deploy (bex via Argo)` run **failed** at `test-backend → go test ./... (backend)`:

```
--- FAIL: TestConformanceAllowlistEntries/retrieve-redis
  conformance_test.go:395: allowlist entry "ADR018 §Managed Key Value / §Resource
  metadata contract: omitted unknown metadata and unsupported provider version"
  (contains="/version: property \"version\" is missing") did not match any raw
  validation error — bex may now conform on this point; remove the entry.
```

- Root cause: commit **`1e1a39a4 fix: report effective key value version`** (another worker, w7 KV) made bex start reporting the KV `version`, so the conformance allowlist entry that asserted "version is missing" is now stale and the guard test fails.
- **Fix:** remove the obsolete Key Value entry at `lego/backend/internal/api/conformance_allowlist_test.go:96–104` (the `retrieve-redis` / "unsupported provider version" block), run `cd lego/backend && go test ./internal/api/`, then re-ship the current HEAD. ~15 min. Not a cron issue — `w9/m91`'s own `test-backend` passed at `d8d72c1d`.

## Systemic finding (deploy reliability)

A burst of rapid commits caused a **supersession cascade**: each `deploy (bex via Argo)` run's `check-supersession` concluded NEUTRAL (built images, **skipped pin + rollout**) because a newer commit had already landed, and the final run failed. The last real digest-pin write-back is `d77604fe` (image `0ac1b6b5baec`), **older than** the cli/cron/database/kv fixes — so a backlog sits built-but-unshipped behind one failing test. A green run for the current HEAD (after the allowlist fix) will pin + roll the whole backlog at once.

Note: a superseded run reports `success` (NEUTRAL) but rolls nothing — the `chore(deploy): pin … [skip ci]` write-back commit is the real "it shipped" signal; its **absence** after a push means that push did not actually deploy.

## DONE (2026-08-21)

Removed the stale `retrieve-redis` `version` needle from `conformance_allowlist_test.go` (and dropped "and unsupported provider version" from its description). `go test ./internal/api/` green — the deploy-blocking `TestConformanceAllowlistEntries/retrieve-redis` now passes. Unblocks the built-but-unshipped backlog (cli/cron/database/kv) once the current HEAD re-ships green.
