---
id: w9/m89/t003
title: Align verification (+ forgot) hero copy with the Ory step shown
worker: worker9
status: done
estimate: 25m
depends_on: []
---

## Objective

Make `/auth/verification` (and forgot-password if still drifting) hero copy match the first Ory Elements step the user actually sees.

## Context

- Live repro: verification hero = "Verify your email" / "Enter the code we sent…"; card = "Enter the email address associated with your account" + Email field (`hunt-verification.png`, `hunt2.json`).
- Locales: `auth.verificationTitle` / `auth.verificationSubtitle` in `dashboard/src/features/auth/locales/{en,zh}.ts`.
- Page: `dashboard/src/features/auth/pages/verification-page/index.tsx` (always mounts `<Verification>` for a fresh flow — first node is email, code comes after submit / emailed link with `?flow=`).
- Forgot page: subtitle mentions email/recovery code; card says identifier / one-time access code — fix in the same pass if still mismatched after a quick live check.

## Steps

1. Rewrite `auth.verificationSubtitle` (and zh) for the email-first step; optionally soft-title so a code step later still reads sensibly, or keep a neutral subtitle ("Confirm the email address for your account").
2. Align forgot-password hero strings with the recovery card if they still contradict.
3. Smoke the page in unit/render test or a short Playwright note if cheap.

## Files

- `dashboard/src/features/auth/locales/en.ts`
- `dashboard/src/features/auth/locales/zh.ts`
- `dashboard/src/features/auth/pages/verification-page/` (+ existing `__tests__` if present)
- optionally forgot-password locales / page

## Acceptance criteria

- [ ] Fresh `/auth/verification` no longer tells the user to enter a code while showing only an email field.
- [ ] zh strings updated in lockstep.
- [ ] Forgot-password hero/card no longer contradict each other on a cold load (or explicitly deferred with a one-line note in the task closeout if Ory node text is the only remaining drift).

## Out of scope

- Replacing Ory Elements verification UI.
- Changing Kratos verification flow configuration.
