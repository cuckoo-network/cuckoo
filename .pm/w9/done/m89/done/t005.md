---
id: w9/m89/t005
title: Render parity
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m89/t001, w9/m89/t002, w9/m89/t003, w9/m89/t004]
---

## Objective

Check that the auth-surface fixes stay consistent with Render's human-facing login / device-verification expectations and do not invent silent API drift.

## Context

- Surfaces touched: dashboard auth UI only (logout, device, verification/forgot copy, login Ory overrides). No bex-api REST/GraphQL/MCP change expected.
- Compare against Render dashboard login / CLI device confirm UX where relevant; Kratos + Ory Elements are deliberate IdP differences — flag, don't clone.

## Steps

1. Re-walk the four fixed URLs unauthenticated; note any remaining copy/UX drift vs Render.
2. Confirm no accidental API surface change; if a server handler status code changed for `/auth/device`, document it.
3. File residual polish (GitHub casing, SPA 404 status, Ory zh leftovers) under `w9/051` rather than expanding scope.

## Files

- touched auth pages + `docs/ADR012-auth.md` only if a behavior note is warranted (prefer not)

## Acceptance criteria

- [ ] Parity notes recorded in this task (or a short comment in the milestone README) for any deliberate divergence.
- [ ] No undocumented REST/GraphQL/MCP change.

## Out of scope

- Building Render's IdP or SSO surfaces.
