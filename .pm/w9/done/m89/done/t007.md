---
id: w9/m89/t007
title: Test coverage
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m89/t005]
---

## Objective

Add meaningful tests for the behaviors this milestone shipped — real failure modes, not tautologies.

## Context

- Must cover: no-session logout success path; device missing-code branded response (not raw text/plain); verification locale/render assertion if useful; login empty-submit / duplicate-id guard if overrides landed.
- Prefer extending `logout.test.ts`, hydra-device / device-confirm tests, and ory override tests.

## Steps

1. Add/extend unit tests for t001–t004 acceptance criteria.
2. Run `yarn test` (scoped packages) and fix failures.
3. Avoid snapshot-everything or tests that only assert string constants exist.

## Files

- `dashboard/src/common/lib/ory/__tests__/logout.test.ts`
- device / hydra-device tests
- ory config / login tests as needed

## Acceptance criteria

- [ ] No-session logout and missing device code have non-vacuous automated coverage.
- [ ] `yarn test` green for touched suites.

## Out of scope

- Full authenticated Playwright product hunt (needs a session; separate future note).
