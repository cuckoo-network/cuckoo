---
id: w9/m6/t001
title: Capture Render's Key Value id/rename contract (OpenAPI + live docs)
worker: worker9
status: done
estimate: 30m
depends_on: []
---

## Objective

Pin exactly how Render identifies and renames Key Value stores — id prefix/shape, which fields are mutable via PATCH, and how the CLI routes `red-` ids — so the bex mirror is evidence-backed, not guessed.

## Context

- Pinned Render OpenAPI (api-docs.render.com/openapi/render-public-api-1.json) — `key-value` family.
- `w9/done/m3` captured the equivalent Postgres (`dpg-`) contract; follow its artifact format.
- `docs/cli-compatibility-checklist.md` RC5 records the current deviation.

## Steps

1. Extract the key-value schemas + PATCH body from the pinned OpenAPI; note the `red-` id examples.
2. Capture rename behavior from Render's live docs/dashboard where the API is ambiguous.
3. Write `docs/render-artifacts/key-value-identity.md` with the pinned contract.

## Files

- docs/render-artifacts/key-value-identity.md (new)

## Acceptance criteria

- [ ] Artifact records id shape, mutable-name semantics, and CLI routing expectations with sources.

## Out of scope

- Any code change.
