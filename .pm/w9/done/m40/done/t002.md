---
id: w9/m40/t002
title: Production backfill via the Postgres-precedent runbook + official-CLI rename smoke
worker: worker9
status: done
estimate: 1.5h
depends_on: [w9/m40/t001]
---

## Objective

Run the same backfill against production following the runbook shape the Postgres `spec.name` backfill used, then prove the rename surface live with `keyvalue-rename-cli-smoke.sh` and the official Render CLI.

## Context

- Precedent runbook: the Postgres `spec.name` backfill referenced by `w9/done/m6/done/t006.md:47` ("follows the same runbook Postgres used").
- Production access: the path w9/m39's live probes used (official CLI v2.21.0 evidence, 2026-07-15).
- The migrate script is idempotent + non-destructive; still snapshot the before-state (`kubectl get keyvalues -o yaml`) first.
- Never commit or print kubeconfig contents (repo rule).

## Steps

1. Snapshot production KV CRs (before-state, redacted as needed).
2. Run `scripts/keyvalue-name-migrate.sh` against production; capture output.
3. Run `scripts/keyvalue-rename-cli-smoke.sh` with the official CLI against production; capture the pass.
4. Record evidence in the w9/m6 doc trail; note completion against `w9/done/m6/README.md:36`'s remaining line.

## Files

- Evidence/op-log under `.pm/w9/m40/` (moved to `done/` at closeout)

## Acceptance criteria

- [x] No production KV store lacks `spec.name` after the run.
- [x] CLI rename smoke passes against production.
- [x] Before/after evidence recorded.

## Out of scope

- Any KV capability change — this is rollout only.
