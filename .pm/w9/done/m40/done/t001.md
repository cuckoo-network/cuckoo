---
id: w9/m40/t001
title: Backfill dev-1…dev-10 — run keyvalue-name-migrate.sh per environment, capture evidence
worker: worker9
status: done
estimate: 1h
depends_on: []
---

## Objective

Run the idempotent, non-destructive `scripts/keyvalue-name-migrate.sh` against every dev-N environment so all pre-m6 Key Value stores gain `spec.name`, capturing before/after identity output per environment.

## Context

- Script + smoke: `scripts/keyvalue-name-migrate.sh`, `scripts/keyvalue-rename-cli-smoke.sh` (both shipped by w9/m6; dev-9 already migrated as the live proof).
- Dev environments: `.pm/wN/dev-N/` stacks on the shared kind/CAPD cluster (namespaces `dev-N`, ports in `dev-N/ports.env`). Some may have no KV stores — a clean "nothing to migrate" run is valid evidence.
- The script is idempotent: re-running on dev-9 must be a no-op (assert that too).

## Steps

1. For each dev-N: list KV stores, run the migrate script, capture before/after `spec.name`/id output.
2. Re-run on dev-9 to demonstrate idempotence.
3. Record per-environment evidence in the op log.

## Files

- `scripts/keyvalue-name-migrate.sh` (run, not edit — edit only if a real defect surfaces)

## Acceptance criteria

- [x] Every dev-N either migrated (evidence captured) or proven empty of KV stores.
- [x] Idempotence demonstrated on an already-migrated environment.

## Out of scope

- Production (t002).
