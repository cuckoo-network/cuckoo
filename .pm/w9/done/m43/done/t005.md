---
id: w9/m43/t005
title: "Simplify: /simplify over the changed operator/registry code"
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m43/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Scope: `lego/operator/internal/registry/` + the `app_controller.go` wiring from t002/t003.

## Steps

1. Run `/simplify` scoped to the milestone's diff; apply accepted findings.
2. `make test` green from `lego/operator/`.

## Files

- `lego/operator/internal/registry/`
- `lego/operator/internal/controller/app_controller.go`

## Acceptance criteria

- [x] `/simplify` ran (4 parallel reviewers: reuse/simplification/efficiency/altitude); applied: success memoization, repo-scoped probe, write-anchored grace, rotation/revocation integration, `registryHosted` helper dedupe, scheme-aware probe URL, `readPassword` dedupe, exported zot pod-label constants (used by the envtest), bounce-error visibility, comment de-duplication, shared default HTTP client. Skipped with reasons: `DeleteAllOf` (would widen RBAC to `deletecollection`), removing the `BounceCooldown` field (kept symmetric with `ActivationGrace`, blessed by the altitude review), build-aware bounce deferral (documented as accepted blast radius instead), cached-client pull-Secret read (memoization made it cold-path)
- [x] `make test` + `make lint` pass after applying

Ran ahead of t004 (the rollout changes no operator code); if t004 somehow does, re-run over its diff.

## Out of scope

- New behavior
