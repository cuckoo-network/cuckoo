---
id: w9/m57/t001
title: Live-capture Render's address surfaces
worker: worker9
status: done
estimate: 45m
depends_on: []
---

## Objective

Capture what Render actually shows/returns for service addresses so m57's surfaced values and m58's field names copy evidence, not guesses: the REST `serviceDetails` shape for a `web_service` **and** a `private_service` (does a pserv carry `url`? what value?), the dashboard **Connect → Internal** tab, and the deploy-detail header's internal address.

## Context

- ADR041 "Render's model" table is doc-derived; gap 4 explicitly gates the REST shape on this capture ("bex matches the captured shape rather than inventing one").
- Method precedent: `docs/render-artifacts/duplicate-service-names.md` (docs-fallback + user-observed dashboard) and `docs/render-artifacts/cli-services-create-update.md` (redacted live captures). A live Render account beats docs-fallback; use whichever is available, and record the method line at the top as the artifacts convention does.
- Known evidence to anchor: `elasticsearch-2j3e:9200` / `http://elastic-qeqj:10000` (render.com/docs/private-network); internal hostname = slug; scope = region + workspace; deploy-header internal address noted in `docs/render-artifacts/deploy-detail-page.md`.

## Steps

1. Capture `GET /v1/services/{id}` for a live `web_service` and a `private_service` (or the OpenAPI schema if no live account): exact field names/values for public url and any internal-address field.
2. Capture the dashboard Connect menu (Internal tab) for both types + the pserv "Service Address" label + the deploy-detail header address; screenshots or transcribed text.
3. Confirm whether the internal hostname is the suffixed slug (create a name-colliding service if live access allows).
4. Write `docs/render-artifacts/service-addresses.md` (method line, captures, redactions); update ADR041's evidence table with any corrections.

## Files

- `docs/render-artifacts/service-addresses.md` (new)
- `docs/ADR041-service-addresses.md` (evidence corrections only)

## Acceptance criteria

- [ ] The artifact answers: does a pserv's `serviceDetails` carry `url`, and with what value shape?
- [ ] Connect → Internal content for web + pserv recorded, including the exact internal-address string format.
- [ ] Method + date line present; anything unverifiable is marked docs-fallback, never presented as live capture.

## Out of scope

- Changing any bex surface (t004/t005, m58).
