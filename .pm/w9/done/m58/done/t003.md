---
id: w9/m58/t003
title: "Dashboard Connect UX: Internal tab, Service Address, deploy header"
worker: worker9
status: done
estimate: 90m
depends_on: [w9/m58/t002]
---

## Objective

The dashboard grows Render's Connect affordance for the internal address: a Connect-style control on the service detail page (Internal tab content = `<slug>:<port>` + copy button), a private service's detail labeling it **Service Address**, and the deploy-detail header showing the internal address — closing the deliberate-scope note in `docs/render-artifacts/deploy-detail-page.md`.

## Context

- Read `dashboard/CLAUDE.md` before editing (repo rule). Data comes from t002's GraphQL field via the existing Apollo service queries.
- Layout reference: the m57 t001 capture (Connect → Internal tab screenshots/transcript); reuse the existing copy-button pattern from the datastore connection-info reveals (`/keyvalue/$id`, databases detail) rather than inventing one.
- Type gating: web + private only; static/worker/cron show nothing (mirrors the w5/m48 type-gated IA precedent).
- Deploy-detail header: the milestone that built the page scoped the internal address out because the API didn't supply one — t002 now does.

## Steps

1. Add the Connect/Internal affordance to the service detail page; pserv variant labeled Service Address.
2. Add the internal address to the deploy-detail header per the original capture.
3. Update `deploy-detail-page.md`'s deliberate-scope row (superseded, dated).
4. `yarn test` + typecheck/lint; Playwright screenshot evidence (bare filename → `.playwright-mcp/`).

## Files

- `dashboard/src/**` (service detail, deploy detail header components)
- `docs/render-artifacts/deploy-detail-page.md`

## Acceptance criteria

- [ ] Web + pserv show the address with copy; worker/cron/static show nothing.
- [ ] Deploy header shows it; screenshot captured.
- [ ] Dashboard suites green.

## Out of scope

- Shell/SSH connect tabs (w2/m39/m55); datastore connect surfaces.
