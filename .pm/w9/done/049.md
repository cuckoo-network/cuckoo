# 049 — Honest-error residual beyond the shared MetricSection (w9/m86 follow-up)

Filed by `/pm-brainstorm for w9` 2026-08-18. Type: scope follow-up (not a defect the shared path — m86 already fixed the shared component). **Sub-hour**, so an inbox note, not a milestone.

`w9/m86` fixed the swallow-error-into-empty-state bug in the **shared** `MetricSection` (`metric-section.tsx` — the `errorEmpty` branch + `MetricError` card), so `network-metrics-card`, `datastore-metrics-panel`, and `application-metrics-card` already distinguish a **failed** query (error card) from a **genuinely empty** window. The residual is the panels that do **not** route their error through `MetricSection`:

- **`dashboard/src/features/services/components/scaling-recent-metrics.tsx`** — its custom instance-count branch is `loading / no-data / no-limit / chart` with **no `result.error` branch**, so a failed fetch reads as "No data captured…". Add an `errorEmpty` branch mirroring `metric-section.tsx` (`const errorEmpty = !!result.error && result.series.length === 0` → `MetricError`).
- **Usage page charts** — spot-check any chart on the usage surface that isn't a `MetricSection`; apply the same error-vs-empty distinction if it swallows.

Same lesson as m86: the error-vs-empty conflation is exactly what hid the `w5/m71` wrong-identifier bug for months. Add/extend the per-panel test to assert the error card renders on `result.error` (non-vacuous). One small pass.
</content>

---

## Resolution — 2026-08-22

**Status: DONE (no production change needed).** The note's premise was stale: m86's `errorEmpty` branch lives in the shared `MetricSection` (`dashboard/src/features/metrics/components/metric-section.tsx`) and is evaluated before children render, and every `ScalingRecentMetrics` section routes its result through `MetricSection` — the children's error-swallowing branches are unreachable while `result.error` is set. Adding the prescribed per-panel branch would have been dead code. The usage surface has no non-`MetricSection` metric charts, and its one data read (`usage-page.tsx`) already alerts on error. Locked in with a per-panel regression test (`dashboard/src/features/services/components/__tests__/scaling-recent-metrics.test.tsx`, 3 tests, proven non-vacuous by deleting the shared branch). One milder adjacent observation recorded for the future: a failed `memory_limit`/`cpu_limit` secondary query reads as "No limit configured" rather than an error.
