---
id: w9/m85/t002
title: Fix the type drift the new pass surfaces
worker: worker9
status: done
estimate: 60m
depends_on: [w9/m85/t001]
---

## Objective

Repair every type error the t001 pass surfaces across existing test fixtures so `yarn typecheck` is green again with test files included.

## Context

- Drift is expected wherever a fixture was hand-built to mirror a GraphQL/type shape that later changed — exactly the silent-rot the note describes.
- Fixes must correct the fixture to the real type, not loosen the type or `@ts-expect-error` past a genuine mismatch.

## Steps

1. Run the t001 typecheck; enumerate the errors.
2. Fix each fixture to the current type (regenerate/align mock shapes, not cast-away).
3. Re-run until green; confirm `yarn test` still passes.

## Files

- whichever `dashboard/src/**/*.test.{ts,tsx}` files the pass flags

## Acceptance criteria

- [ ] `yarn typecheck` (app + test projects) green
- [ ] `yarn test` green
- [ ] No mismatch silenced with a blanket cast/`@ts-expect-error`

## Out of scope

- The guard tripwire (t004)
