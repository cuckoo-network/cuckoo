---
id: w9/m86/t001
title: Audit the chart/metric panels for the empty-vs-error conflation
worker: worker9
status: done
estimate: 25m
depends_on: []
---

## Objective

Enumerate every dashboard chart/metric panel that renders a query error as the empty "No data in range" state (the swallow-all-errors pattern), so t002 fixes the class, not just the one reported panel.

## Context

- Reported panel: `dashboard/src/features/metrics/components/datastore-metrics-panel.tsx` (only error state is the exact `"metrics source not configured"` string; `errorPolicy: "all"` ⇒ every other error is `data === undefined` ⇒ empty state).
- Shared pieces: `dashboard/src/features/metrics/components/metric-section.tsx`, `dashboard/src/features/metrics/lib/graphql-series.ts`.
- Check the service-metrics siblings and any other consumer of `metric-section` / the series hook for the same conflation.

## Steps

1. Grep the metrics feature for `errorPolicy: "all"` and empty-state renders that ignore `result.error`.
2. List each affected panel and whether it exposes `result.error` today.
3. Decide the shared fix shape (an error card in `metric-section` vs per-panel) — prefer the shared seam.

## Files

- `dashboard/src/features/metrics/**` (read-only audit)

## Acceptance criteria

- [ ] A written list of affected panels + the chosen shared-fix seam
- [ ] Confirmation of whether service-metrics siblings share the pattern

## Out of scope

- The fix itself (t002)
