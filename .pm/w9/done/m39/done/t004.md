---
id: w9/m39/t004
title: Verify the official CLI's interactive workspace set flow — flip the ◐ row or file the wire gap
worker: worker9
status: done
estimate: 30m
depends_on: []
---

## Objective

`docs/cli-compatibility-checklist.md:78` records `workspace set` as ◐: "The interactive flow was not tested. `RENDER_WORKSPACE` is the supported deterministic override." Only the env-var path is proven against bex. Run the interactive workspace selector against bex-api, capture the outcome, and either flip the row to ✅ with evidence or file the wire gap it surfaces as bex-api work. Absorbs note `w9/006`.

## Context

- Source: `/pm-brainstorm` round 14's CLI-checklist miner; stranded as a note since.
- Stack: dev-9 (`bash .pm/w9/dev-9/up.sh`), locally built `render` binary.
- The interactive flow lists workspaces from `GET /v1/owners` — bex's owners read API (`docs/render-artifacts/owners-api.md`).

## Steps

1. Bring up dev-9 with at least two workspaces on the test identity (so the selector actually selects).
2. Run `render workspace set` interactively; capture the transcript.
3. Flip the row with evidence, or file the bex-api wire gap (new `w9/NNN` note or add-task) and record it on the row.

## Files

- `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] The `workspace set` row is ✅-with-evidence or ◐-with-a-filed-owner — no longer untested.

## Out of scope

- CLI code changes (DO_NOT_DO).
