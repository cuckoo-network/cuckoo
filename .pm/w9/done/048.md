# 048 — Extend role-aware disable-with-reason to env-var writes + build filters (w9/m84 follow-up)

**Disposition 2026-08-17:** reconciled and retired. The environment/datastore portion is materialized as `w2/m74`, corrected to the backend's actual relation split. Build Filters are explicitly excluded by user direction. The optional Team hide-to-disable change remains declined because w9/m84 deliberately matched Render's hidden presentation. No open work remains in this note.

Filed at w9/m84 closeout, 2026-08-17. Type: scope follow-up (not a defect — the server refuses correctly today; this is UX consistency).

m84 gated the create-gated build/deploy config, cron command, and statement-logging controls, plus `can_view_sensitive` reveal and `can_manage_billing`, with disable-with-reason via `useCapabilities`. A few adjacent `can_create` controls were left ungated for scope and remain "editable → 403 on save" for a contributor:

- **Env-var add / edit / delete** (`SetEnvVar` / delete = `can_create`) — `env-var-row.tsx` (Edit pencil, Delete), the "Add variable" form, and the workspace **env-groups** editor that shares `EnvVarsEditor`. m84 gated the row's **reveal** (`can_view_sensitive`) but not the writes. Thread `canCreate` from the panel the same way `canReveal` is threaded.
- **Build Filters editor** (`BuildFilterEditor` → `setBuildFilter`) — its relation should be confirmed (likely `can_create`); if so, gate its inputs.
- **Datastore lifecycle controls** (delete Postgres/Key Value, plan change) — originally summarized here as `can_create`; reconciliation found the actual split is delete = `can_create`, plan/suspend/resume/restart = `can_operate`.

All use the same pattern already in place: definitive capability denial plus `disabled` / `disabledReason` (`EditableFieldRow`) or `PermissionTooltip` (plain buttons). Also optionally considered: convert Team member-management controls from hide to disable-with-reason if the Render-consistent hide is later judged less legible than a reason (m84 deliberately kept hide — Render also hides these for non-admins).

Optional live proof was a logged-in browser walk (contributor vs admin) confirming the disabled-with-reason states render. It was deferred at m84 close because a role-seeded prod/dev session was not raisable in-session; per-role component tests assert the behavior.
