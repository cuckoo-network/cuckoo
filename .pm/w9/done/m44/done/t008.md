---
id: w9/m44/t008
title: "Test coverage: harness wired as a rerunnable guard + tests for shipped fixes"
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m44/t006]
---

## Objective

Make the sweep durable: the harness is documented and rerunnable against any bex deployment (the `cli-compat.sh verify` pattern), and every t005 code fix carries meaningful regression tests asserting the behavior and its failure mode.

## Context

- Candidate wiring: a `scripts/samples-lifecycle.sh all` mode + a short section in `docs/cli-compatibility-checklist.md` or the harness header documenting env/setup; do NOT add it to CI (it deploys to prod) — it's an on-demand guard like `cli-compat.sh verify`.
- Fixes in `lego/operator/` need unit/envtest coverage; `lego/backend/` fixes need the module's test style (`go test ./...`).

## Steps

1. Harness `all` mode + docs.
2. Per-fix regression tests; suites green.

## Files

- `scripts/samples-lifecycle.sh`, test files next to each fix

## Acceptance criteria

- [ ] `samples-lifecycle.sh all` documented and runnable by a fresh session
- [ ] Each shipped fix has a test that fails if the fix regresses
- [ ] `make test` (operator) and/or backend `go test ./...` green as applicable

## Out of scope

- CI wiring (deliberately on-demand)
