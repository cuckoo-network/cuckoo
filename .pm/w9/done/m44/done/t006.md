---
id: w9/m44/t006
title: "Render parity: any fixes must land consistently on REST/GraphQL/MCP/dashboard"
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m44/t005]
---

## Objective

For every t005 fix that touched a tenant-facing surface (suspend/resume/delete semantics, service metadata, error shapes), verify the change is consistent across REST, GraphQL, and MCP in `lego/backend/` and the `dashboard/` UI, and compare against render.com's behavior — flag drift as follow-up rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` (one core, thin adapters), `docs/ADR018-render-parity.md` (the ledger — update touched rows' evidence), `docs/cli-compatibility-checklist.md` (add/refresh rows for any CLI gap the sweep documented).

## Steps

1. Enumerate t005's surface-touching fixes; skip if none (note that here and close).
2. Per fix: exercise the verb on all surfaces; compare with Render's documented/captured behavior.
3. Update ledger/checklist evidence cells.

## Files

- `lego/backend/`, `dashboard/`, `docs/ADR018-render-parity.md`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] Each surface-touching fix verified on REST+GraphQL+MCP+dashboard, or drift filed as follow-up
- [ ] Ledger/checklist evidence current

## Out of scope

- New parity features
