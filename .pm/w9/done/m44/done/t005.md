---
id: w9/m44/t005
title: "Fix problems the sweep found (small: in-milestone; large: file follow-ups)"
worker: worker9
status: todo
estimate: 60m
depends_on: [w9/m44/t003, w9/m44/t004]
---

## Objective

Work the t003/t004 triage list: fix what fits in this milestone, file crisp follow-ups (inbox notes or `/pm` milestones with evidence) for what doesn't, and re-run the affected sample's lifecycle to green after each fix.

## Context

- The user's mandate is explicit: "fix problems if there are" — bias toward fixing here; a follow-up filing needs a reason (size, cross-workstream ownership).
- Likely candidates based on session knowledge: suspend/resume behavior for static sites and cron on prod (first systematic exercise), buildpack/docker builds for hello-node/hello-python on the Hetzner cluster, stack-demo's `fromDatabase` wiring outside the blueprint path, deletion residue (m43 added revocation-bounce — verify it holds under the sweep).
- Every fix that touches operator/backend code needs the affected sample's lifecycle re-run on prod as its verification, not just unit tests.

## Steps

1. Triage the recorded failures; order by user impact.
2. Fix + re-run per item; keep the evidence log updated.
3. File follow-ups for the remainder with concrete repro pointers.

## Files

- wherever the failures point (`lego/operator/`, `lego/backend/`, `deploy/gitops/`, docs)

## Acceptance criteria

- [ ] Every sweep failure is either re-run-green after a fix or has a filed follow-up with evidence
- [ ] No sample left in a broken state on prod (clean up test resources)

## Out of scope

- Surface-parity review of the fixes (t006)
