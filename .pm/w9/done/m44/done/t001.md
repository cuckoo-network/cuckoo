---
id: w9/m44/t001
title: "Unblock the deploy pipeline: fix the red dashboard test, land the m43 digest, re-enable Argo"
worker: worker9
status: done
estimate: 45m
depends_on: []
---

## Objective

Make `deploy.yml` green again so CI builds and pins the m43 operator image, then restore the Argo automation suspended during m43's manual rollout. This is the "after deployment" precondition for every other task here.

## Context

- `test-dashboard / dashboard` has failed on the last 5 `deploy.yml` runs (since 2026-07-17T19:35Z — pre-existing, unrelated to m43's operator commit `f9e82786`), so `build-and-deploy` is skipped every push. The failing output shows a lucide chevron SVG in what looks like a component-test diff — start at the dashboard test log for run 29618324670.
- Prod currently runs the locally-imported `bex-operator:m43-94abc84b` (uncommitted-at-the-time code, now committed as `f9e82786`); the Argo `bex-operator` Application still pins the old digest, and automated sync is suspended on **both** `bex-operator` and `bex-platform-prod` (the app-of-apps selfHeal reverts child patches). Re-enable commands: `w9/done/m43/done/t004.md`.
- Order matters: fix test → push → `deploy.yml` green → CI patches the digest pin → operator rolls onto the CI-built image (same code) → re-enable both automated sync policies → confirm Argo does not revert anything (the registry RBAC Role must survive — it is now committed in `deploy/gitops/base/operator-registry-rbac.yaml`).

## Steps

1. Reproduce the dashboard test failure locally (`cd dashboard && yarn test`), fix root cause (no snapshot-blind updates — understand the diff first).
2. Ship the fix; watch `deploy.yml` through `build-and-deploy`; verify the `bex-operator` Application's kustomize image digest was updated and the operator pod runs the ghcr image.
3. Re-enable automated sync on `bex-operator` and `bex-platform-prod`; watch one sync cycle: operator Deployment stable, `bex-operator-registry` Role keeps its pods rule.

## Files

- `dashboard/` (failing test + fix)
- Argo Applications (in-cluster patches only)

## Acceptance criteria

- [ ] `deploy.yml` fully green on a main push (all test jobs + build-and-deploy)
- [ ] Operator Deployment image is the CI-pinned ghcr digest built from `f9e82786` or later
- [ ] Both Applications back on `automated: {prune: true, selfHeal: true}`; RBAC Role intact after a sync

## Out of scope

- The lifecycle sweep itself (t002+)

## Evidence (closeout 2026-07-17)

- `deploy.yml` fully green from `61a18797`; CI pinned the operator+dashboard digest (latest pin `bf4a22fb` → built from `7085362d`, which includes the m43 operator and the publish-Job fix `dbb828ed`).
- Prod verified live this session (Hetzner kubeconfig): every Argo Application `Synced/Healthy` with `automated: {prune: true, selfHeal: true}` — including `bex-operator` and `bex-platform-prod`; running operator/bex-api digest matches the current gitops pin (`a96db398…`); `bex-operator-registry` Role present in `bex-registry` with its pods rule intact.
