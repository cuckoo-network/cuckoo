---
id: w9/m1/t006
title: Render parity check across REST/GraphQL/MCP/UI
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m1/t004, w9/m1/t005]
---

## Objective

Verify this milestone's surfaces are consistent with each other and with Render, and record any deliberate drift as follow-up instead of silently diverging.

## Context

- Single-deploy read now exists on REST + MCP + GraphQL: same fields, same not-found shape (`docs/ADR006-bex-api.md` one-core rule).
- Logs window args now exist on REST + MCP + GraphQL: same cap (`BEX_MAX_QUERY_HOURS`), same malformed-time errors.
- Render reference: the inspected deploy page (`/web/srv-…/deploys/dep-…`, 2026-07-14) and Render's public API (`GET /v1/services/{id}/deploys/{deployId}`). Known candidate gaps to assess and ledger: per-deploy **commit hash/message** (Render shows it; bex has no per-deploy commit metadata), a dedicated **Deploys tab** (bex folds history into Events), and the `?r=` time-range URL param.
- Update `docs/ADR018-render-parity.md`'s Deploys row (UI cell) with evidence.

## Steps

1. Diff the three API surfaces' single-deploy + windowed-logs behavior (fields, errors, caps) — fix any drift found.
2. Compare the shipped page against the Render reference; file each accepted gap as an inbox note in `w9/` (or flag for promotion) rather than expanding this milestone.
3. Update the parity ledger row; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- `w9/NNN.md` inbox notes for accepted gaps

## Acceptance criteria

- [ ] REST/GraphQL/MCP answer single-deploy + windowed logs identically (spot-checked, drift fixed or ticketed).
- [ ] Parity ledger updated with the new UI evidence.
- [ ] Accepted gaps recorded as inbox notes with the Render evidence.

## Out of scope

- Building any of the flagged follow-ups.
