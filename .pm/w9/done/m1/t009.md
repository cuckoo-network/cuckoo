---
id: w9/m1/t009
title: Closeout
worker: worker9
status: done
estimate: 15m
depends_on: [w9/m1/t008]
---

## Objective

Close the milestone once its definition of done actually holds: Manual Deploy verifiably lands on a live-updating per-deploy page with build/pre-deploy/app logs, on prod-shaped config and under `yarn dev:local`.

## Context

- `.pm` conventions: a done milestone moves whole — tasks to `w9/m1/done/`, then `w9/m1/` → `w9/done/m1/`, checkbox in `w9/README.md`, no tombstone left behind.

## Steps

1. Re-verify the milestone DoD end-to-end (click Manual Deploy, watch the deploy page go live).
2. Set every task `status: done`; move each `tNNN.md` into `w9/m1/done/`.
3. Mark every README row `— **DONE**`, set `**Status:** done`.
4. Move `w9/m1/` → `w9/done/m1/`; check `- [x]` in `w9/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w9/m1/**`, `.pm/w9/README.md`

## Acceptance criteria

- [ ] DoD verified before any move.
- [ ] Board synced in all three places; old path gone.

## Out of scope

- Any new feature work discovered during verification (inbox-note it).
