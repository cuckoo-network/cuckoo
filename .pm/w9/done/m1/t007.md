---
id: w9/m1/t007
title: Simplify pass over the milestone's diff
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m1/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving. Likely targets: shared deploy-status→badge mapping between the Events tab and the new deploy header; the GraphQL time-parse helper vs rest.go's; log-viewer reuse seams.

## Context

- Diff scope: `lego/backend/internal/{deploys,logs}/`, `dashboard/src/features/deploys/`, touched services/logs feature files, `dashboard/scripts/local-bex.mjs`.

## Steps

1. `/simplify` over the milestone diff; apply accepted findings.
2. Re-run backend + dashboard suites.

## Files

- Whatever the pass touches within the milestone's diff.

## Acceptance criteria

- [ ] Simplify findings applied or explicitly declined with reasons.
- [ ] All suites still pass; behavior unchanged.

## Out of scope

- Bug hunting (that's /code-review's job, not this task).
