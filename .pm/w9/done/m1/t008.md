---
id: w9/m1/t008
title: Test coverage for the deploy page + new GraphQL args
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m1/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real assertions on behavior and failure modes, not snapshot padding.

## Context

- Backend: `lego/backend/internal/deploys/` (GraphQL single-deploy: found / not-found / other-service's deployId), `lego/backend/internal/logs/` (GraphQL window: filters correctly, malformed time errors naming the field, absent args byte-identical, cap enforced — mirror the REST cases at `logs_test.go:353`).
- Dashboard (vitest, `__tests__` adjacent): `use-deploy` poll-until-terminal (stops on terminal status), deploy header renders each status + pre-deploy outcome, log panel windows its query and interleaves types, trigger-hook navigation (navigates on success, not on rejection), events-row links.

## Steps

1. Backend table-driven cases for the two new GraphQL surfaces.
2. Dashboard unit tests for the new hooks/components + navigation behavior.
3. Full suites: backend `go test ./...`, dashboard `yarn test`.

## Files

- `lego/backend/internal/deploys/*_test.go`
- `lego/backend/internal/logs/*_test.go`
- `dashboard/src/features/deploys/**/__tests__/`
- `dashboard/src/features/services/**/__tests__/` (trigger/rollback navigation)

## Acceptance criteria

- [ ] Every acceptance criterion of t001–t004 has a test that fails if the behavior regresses.
- [ ] Failure modes covered: not-found deploy, cross-service deployId, malformed times, failed trigger not navigating.
- [ ] Full backend + dashboard suites pass.

## Out of scope

- E2E browser tests (no Playwright harness in CI for the dashboard).
