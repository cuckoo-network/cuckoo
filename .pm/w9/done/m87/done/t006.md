---
id: w9/m87/t006
title: Test coverage — per-role component tests for each gated surface
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m87/t004]
---

## Objective

Add meaningful per-role component tests asserting both states — contributor-disabled (with reason) and admin-enabled — for each of the three gated surfaces, mirroring the existing `build-deploy-section` / `parameter-overrides-editor` / `connection-info-panel` per-role tests from `w9/m84`.

## Context

- The tests must assert real behavior: that the control is `disabled` and exposes the reason for a role lacking `can_create`, and is interactive for a role that holds it — not snapshot-everything or tautological checks.
- Cover the shared `EnvVarsEditor` under **both** mount contexts (service Environment tab + env-groups editor) so a future regression in either consumer is caught.

## Steps

1. Add per-role tests for the env-var write controls (Add form, row Edit, row Delete) in both consumers of `EnvVarsEditor`.
2. Add per-role tests for the Build Filters editor.
3. Add per-role tests for the datastore danger-zone delete + plan-change controls (Postgres + Key Value).

## Files

- `dashboard/src/features/**/__tests__/` alongside each gated component

## Acceptance criteria

- [x] Each gated surface has a test asserting contributor-disabled-with-reason AND admin-enabled.
- [x] Tests fail if the gating is removed (non-vacuous).
- [x] `yarn test` green.

## Out of scope

- A live logged-in browser walk (optional, deferred — the per-role component tests are the DoD evidence, as in m84).
</content>
