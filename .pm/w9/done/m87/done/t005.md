---
id: w9/m87/t005
title: Simplify
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m87/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (the `canCreate` threading in `EnvVarsEditor`, `BuildFilterEditor`, and the datastore danger-zone components) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Three surfaces now thread a `canCreate` capability with the same disable-with-reason shape; watch for a reusable helper or a shared prop the way `canReveal` is threaded, and collapse duplication.

## Steps

1. `/simplify` over the diff from t001–t003.
2. Apply only conflict-free, behavior-preserving wins (e.g. a shared capability-prop or a small helper if three near-identical call sites emerged).

## Files

- the files changed by t001–t003

## Acceptance criteria

- [x] `/simplify` run over the milestone diff; safe wins applied, behavior preserved.
- [x] `yarn typecheck && yarn lint && yarn test` green.

## Out of scope

- Refactors beyond this milestone's diff.
</content>
