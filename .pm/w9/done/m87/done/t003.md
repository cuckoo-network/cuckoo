---
id: w9/m87/t003
title: Gate the datastore lifecycle danger-zone controls (delete PG/KV, plan change)
worker: worker9
status: done
estimate: 40m
depends_on: []
---

## Objective

Disable-with-reason the datastore Settings **danger-zone** controls — delete Postgres, delete Key Value, and plan change (all `can_create`) — for a contributor who lacks the relation, so the danger zone stops rendering functional-looking controls that 403 on save.

## Context

- `.pm/w9/done/048.md`: the datastore Settings danger zone still renders delete + plan-change for contributors; these are `can_create`.
- Same disable-with-reason pattern as t001/t002 and `w9/m84`.
- Applies to both Postgres and Key Value datastore Settings pages (mirror the change across both).

## Steps

1. Find the datastore Settings danger-zone components for Postgres and Key Value (delete buttons + plan-change control).
2. Thread `canCreate` from `useCapabilities()` into each.
3. Disable delete + plan-change with a reason when the relation is absent (PermissionTooltip on the destructive buttons; EditableFieldRow `disabledReason` on the plan control).

## Files

- `dashboard/src/features/databases/**` danger-zone / settings components (Postgres)
- `dashboard/src/features/**` Key Value settings danger zone
- (the plan-change control on each)

## Acceptance criteria

- [x] A contributor (no `can_create`) sees delete Postgres and delete Key Value disabled with a reason. Plan change is `RelCanOperate` (not `can_create`): viewers disabled-with-reason; contributors stay enabled — gated to the server, not the inbox guess.
- [x] An admin sees them enabled and functional.
- [x] The destructive confirmation flows are unchanged for an admin.

## Out of scope

- Env-var writes (t001), Build Filters (t002).
- Team member-management controls (Render keeps these **hidden** for non-admins; converting hide→disable is the optional add-task noted in the milestone README, not this task).
</content>
