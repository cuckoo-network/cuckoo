---
id: w9/m87/t002
title: Gate the Build Filters editor (BuildFilterEditor → setBuildFilter)
worker: worker9
status: done
estimate: 30m
depends_on: []
---

## Objective

Confirm the authorization relation behind `setBuildFilter` and, if it is `can_create` (as expected), disable the `BuildFilterEditor` inputs with an explanatory reason for a contributor who lacks it — so the editor is no longer "editable → 403 on save".

## Context

- `BuildFilterEditor` writes via `setBuildFilter`; `.pm/w9/done/048.md` flags the relation as "likely `can_create`" and asks to confirm before gating.
- Same disable-with-reason pattern as `w9/m84` and t001: `useCapabilities()` + `disabled` / `disabledReason` / `PermissionTooltip`.

## Steps

1. Trace `setBuildFilter` to its bex-api resolver/service and confirm the authorization relation (expected `can_create`). Record the confirmed relation in the task/PR.
2. Thread the corresponding capability from `useCapabilities()` into `BuildFilterEditor`.
3. Disable the editor inputs + save with a reason when the capability is absent.

## Files

- `dashboard/src/features/**/BuildFilterEditor*` (build filters editor)
- reference: the `setBuildFilter` resolver in `lego/backend/` (to confirm the relation)

## Acceptance criteria

- [x] The `setBuildFilter` authorization relation is confirmed and noted.
- [x] A contributor lacking that relation sees the Build Filters inputs + save disabled with a reason.
- [x] An admin sees them enabled and functional.

## Notes

Confirmed `SetBuildFilter` is `RelCanOperate` (`lego/backend/internal/apps/service.go`), same class as Auto-Deploy — it filters which future pushes redeploy, it does not choose the code a build runs. UI gated to `canOperate`: **viewers** disabled-with-reason; **contributors stay enabled**. Inbox's `can_create` guess was wrong; t002 says gate to the actual relation.

## Out of scope

- Env-var writes (t001), datastore lifecycle danger zone (t003).
- Changing the server relation — this task gates the UI to match the server, not the reverse. If the relation is unexpectedly **not** `can_create`, gate to whatever the server actually enforces and note the divergence.
</content>
