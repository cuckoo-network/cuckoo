---
id: w9/m87/t004
title: Render parity — UI role-gating consistency
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m87/t001, w9/m87/t002, w9/m87/t003]
---

## Objective

Confirm the disabled-with-reason treatment shipped by t001–t003 is consistent with how Render itself handles the same controls for a non-admin, and that no wire surface drifted.

## Context

- This milestone touches only the `dashboard/` surface. The REST/GraphQL/MCP relations (`can_create`) are already the authority and are unchanged — there is no bex-api wire change to reconcile.
- The parity concern is UI legibility: Render disables/hides these controls for contributors; bex chooses **disable-with-reason** (consistent with `w9/m84`).

## Steps

1. Confirm no REST/GraphQL/MCP field/semantic/error-shape change was introduced (the tasks only read existing capabilities and toggle `disabled`).
2. Compare each gated control against Render's own contributor treatment (disable vs hide) and confirm bex's disable-with-reason is a deliberate, documented choice consistent with m84.
3. Flag any genuine drift (e.g. a control where Render's behavior differs materially) as follow-up rather than diverging silently.

## Files

- `dashboard/` (verification only)
- `docs/ADR018-render-parity.md` if a drift row needs a note

## Acceptance criteria

- [x] Confirmed: no REST/GraphQL/MCP surface change.
- [x] Each gated control's disable-with-reason treatment is confirmed consistent with m84 / Render's contributor handling, or any drift is filed as follow-up.

## Notes

Dashboard-only. Disable-with-reason vs Render hide is the same deliberate m84 choice; ADR018 viewer-capability bullet updated. Team member-management stays hide. `SetBuildFilter` + datastore `SetPlan` are `can_operate` (contributor-editable). Live `EnvironmentEditor` reveal still ungated — filed `.pm/w9/050.md`.

## Out of scope

- New wire features — this is a consistency check, not a surface change.
</content>
