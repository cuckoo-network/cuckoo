---
id: w9/m90/t007
title: Simplify the changed code
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m90/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency;
behavior-preserving).

## Context

- Changed areas: `apps/deploy.go` (billing gate), `metrics/source.go` + `egressquery` (escaping),
  `core/http.go` (500 body), `agentsession/signature.go` + `mint.go` + `sshgateway/nonce.go` (nonce),
  `registrycreds/pullsecret.go`.
- Look for a shared billing-gate helper the Blueprint path and interactive paths can both call (avoid
  duplicating the `RequirePlanBilling` semantics), and a single quote-safe escaper reused by both the
  Loki and Prometheus builders.

## Steps

1. Run `/simplify` over the milestone diff.
2. Apply behavior-preserving cleanups (dedup, hoist shared helpers, remove now-dead branches).
3. Re-run the backend suite + lint.

## Files

- The files changed by t001–t005.

## Acceptance criteria

- [ ] No behavior change; suites + lint green.
- [ ] Any shared helper opportunities (billing gate, quote-safe escaper) taken or explicitly declined
      with a reason.

## Out of scope

- New functionality.
