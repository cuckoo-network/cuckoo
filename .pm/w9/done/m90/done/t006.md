---
id: w9/m90/t006
title: Render parity — billing-refusal + 500 error dialect across REST/GraphQL/MCP
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m90/t001, w9/m90/t002, w9/m90/t003, w9/m90/t004, w9/m90/t005]
---

## Objective

Ensure the shipped changes are consistent across every surface they expose. This milestone touches
two error surfaces: the new billing-enforcement refusal on the Blueprint path (t001) and the
generic-500 body (t003), both of which reach REST, GraphQL, and MCP.

## Context

- bex-api is Render-API-compatible (`docs/ADR006-bex-api.md`); every error path must speak the one
  Render error dialect (the m38 contract) with the same fields/semantics across REST/GraphQL/MCP.
- t001 adds an `ErrBillingEnforced` refusal on Blueprint deploy/sync — confirm it surfaces with the
  same coded shape the interactive create paths already return (Service/Postgres/KeyValue create),
  across all three adapters and the dashboard blueprint-deploy flow.
- t003 changes the unclassified-5xx body — confirm REST and GraphQL agree (graphql-go formatter) and
  MCP tool errors are consistent.

## Steps

1. Compare the billing-enforcement error shape (code, message, HTTP/JSON status) between the Blueprint
   path and the interactive create paths across REST, GraphQL, and MCP; fix any drift.
2. Confirm the generic-500 change is consistent on REST + GraphQL + MCP.
3. Check the dashboard renders the Blueprint billing-enforcement refusal sensibly (reuses the existing
   billing-required/error surface); flag any UI drift as follow-up rather than diverging silently.
4. Note the parity result (or any follow-up filed) in the milestone README `## Source + Goal linkage`.

## Files

- `lego/backend/internal/apps/` (rest/graphql/mcp fragments), `lego/backend/internal/core/http.go`
- `dashboard/` (blueprint deploy error surface — verify only)

## Acceptance criteria

- [ ] Blueprint billing-enforcement refusal is byte-consistent with the interactive-create refusal
      across REST/GraphQL/MCP.
- [ ] Generic-500 body is consistent across surfaces.
- [ ] Any dashboard/Render drift is documented as follow-up, not silently accepted.

## Out of scope

- New dashboard UX beyond confirming the refusal renders.
