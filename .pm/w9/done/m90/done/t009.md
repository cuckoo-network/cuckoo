---
id: w9/m90/t009
title: Closeout
worker: worker9
status: done
estimate: 10m
depends_on: [w9/m90/t008]
---

## Objective

Close the milestone once its definition of done actually holds.

## Steps

1. Confirm the DoD in the milestone README is met: Blueprint billing bypass fixed + regression-tested,
   and all five hardening items shipped with tests; all three backend suites + lint green.
2. Set every remaining task's `status: done`, move each `tNNN.md` to `w9/m90/done/`, mark every row
   `— **DONE**` and set `**Status:** done` in the milestone README.
3. Move the whole `w9/m90/` directory to `w9/done/m90/` and check `- [x]` in the w9 README.
4. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [ ] DoD verified real (not merely code-written).
- [ ] Milestone moved to `w9/done/m90/`; w9 README box checked; statuses synced in all three places.

## Out of scope

- Any remaining follow-ups become new inbox notes, not reasons to hold closeout.
