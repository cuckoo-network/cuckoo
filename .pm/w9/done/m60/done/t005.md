---
id: w9/m60/t005
title: "Bundle guardrails: visualizer, vendor manualChunks, hidden sourcemaps"
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m60/t001, w9/m60/t002, w9/m60/t003, w9/m60/t004]
---

## Objective

Make bundle weight visible and cache-stable so this milestone's win doesn't erode: add a dev-only bundle visualizer, split stable vendor code into `manualChunks`, stop shipping public sourcemaps, and record the before/after entry-chunk numbers.

## Context

- `dashboard/vite.config.ts:421-425` — `build` today: no `rollupOptions.manualChunks`, `sourcemap: true` (emits public `.map` files), no analysis tooling anywhere in the repo.
- Baseline (pre-milestone, from the committed `.output`): entry chunk 5.95 MB raw / 1.31 MB gzip. w5/m67 records its own route-split delta; this task records the entry-chunk delta from t001–t004.

## Steps

1. Add `rollup-plugin-visualizer` gated behind an env flag (e.g. `ANALYZE=1 yarn build`), output gitignored.
2. Add `manualChunks` peeling React/ReactDOM, Radix, and Apollo into vendor chunks; verify no circular-init breakage (`yarn build` + smoke-run the SSR output).
3. Switch `sourcemap: true` → `"hidden"`; note the debugging implication in `dashboard/CLAUDE.md`.
4. Measure and record in the milestone README: entry-chunk raw+gzip before (baseline above) and after t001–t004 + this split.

## Files

- `dashboard/vite.config.ts`
- `dashboard/package.json` (devDependency)
- `dashboard/CLAUDE.md`
- `.pm/w9/m60/README.md` (numbers)

## Acceptance criteria

- [ ] `ANALYZE=1 yarn build` emits a treemap; normal builds don't.
- [ ] Vendor chunks are content-stable across an unrelated source change (rebuild twice, hashes of vendor chunks unchanged).
- [ ] No `.map` files in the public assets output.
- [ ] Before/after numbers recorded in the milestone README.

## Out of scope

- A CI size budget/regression gate (file as a follow-up note if the visualizer shows it's warranted).
