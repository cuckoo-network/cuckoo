---
id: w9/m88/t002
title: Large plan cards: LimitsFor bullets + catalog fees
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m88/t001]
---

## Objective

Make `PlanPicker` a Render-shaped radiogroup of large cards: name, **catalog monthly fee**, one-line pitch, capability bullets from `store.LimitsFor`. Fees must stay 30% off Render as already set in `pricing.yaml`.

## Context

- Catalog (source of truth): `lego/backend/internal/pricing/pricing.yaml` `workspace.usdPerMonth` — Hobby `0`, Pro `17.50`, Scale `349.30`. Enterprise is **not** a catalog SKU (`WorkspaceUSDPerMonth` returns `listed=false`).
- Locales already say `$0/mo` / `$17.50/mo` / `$349.30/mo` / `Custom terms` (`workspaces.plan*Billing`). Keep those strings in lockstep with the YAML; never `$25` / `$499`.
- Caps: Hobby 1 member / 25 services / 5 workspaces per user; Pro Admin+Developer; Scale+ extra roles (`lego/backend/internal/store/plans.go` `LimitsFor`).
- Capture of Render's card anatomy: `docs/render-artifacts/workspace-plan-change.md` (name, price, pitch, bullets, select/selected). Reuse this picker in `change-plan-dialog.tsx` — both surfaces must stay identical.
- Usage footnote (`workspaces.planUsageBillingNote`) stays: resource-tier usage billed separately.

## Steps

1. Restyle `PlanPicker` to a 2×2 (or stacked) grid of large cards; selected state = radio `aria-checked` + visible "selected".
2. Add per-plan bullets (i18n) matching `LimitsFor` — do not invent bandwidth/custom-domain/build-minute allowances bex does not meter per workspace plan.
3. Keep Enterprise selectable with **Custom terms** (no contact-sales disable unless t006 flags it as required follow-up). Do not add an Enterprise `usdPerMonth`.
4. Assert tests reject Render's $25/$499 and require the catalog three.

## Files

- `dashboard/src/features/workspaces/components/plan-picker.tsx`
- `dashboard/src/features/workspaces/components/__tests__/plan-picker.test.tsx`
- `dashboard/src/features/workspaces/locales/{en,zh}.ts`
- `dashboard/src/features/workspaces/types.ts` (catalog comments only)

## Acceptance criteria

- [ ] Cards show `$0/mo`, `$17.50/mo`, `$349.30/mo`, `Custom terms`.
- [ ] Hobby bullets name 1 member, 25 services, 5 Hobby workspaces/user.
- [ ] Pro/Scale bullets name unlimited members/services; Scale names extra roles.
- [ ] Change-plan dialog still uses the same picker (no second catalog).
- [ ] Tests fail if someone pastes Render's $25/$499 back in.

## Out of scope

- Attaching licensed Stripe Prices (`BillableMeterNames` stays usage-only).
- Cloning Render's bandwidth/custom-domain/build-minute included allowances onto workspace cards (those are usage meters, not plan SKUs).
