---
id: w9/m88/t001
title: Un-card the page: h1, slug field, FormPageSkeleton
worker: worker9
status: done
estimate: 45m
depends_on: []
---

## Objective

Replace the nested `Card` on `/new/workspace` with Render's create-page frame: a page-level heading, a labelled slug field with helper text, Cancel + Create Workspace in a page footer, and a content-shaped pending skeleton.

## Context

- Today: `dashboard/src/routes/new.workspace.tsx` wraps name + `PlanPicker` in `max-w-2xl` `Card` titled "Create a workspace".
- Render (docs + `docs/render-artifacts/workspace-plan-change.md` card anatomy): page `h1`, then fields, then plan radiogroup, then footer actions — not a settings card.
- Pending: `FormPageSkeleton` already exists (`dashboard/src/common/components/detail-skeletons.tsx`, `w9/m69`). Other `*.new` routes use it as `pendingComponent`; `/new/workspace` still falls through to `RoutePending`.
- Keep `staticData: { chrome: true }` and the one-rail rule. Keep DNS-label validation (`WORKSPACE_NAME_RE`); do not freeform names.

## Steps

1. Lift the `h1` out of `CardTitle`; drop the wrapping Card (or keep primitives only for the slug control if needed).
2. Relabel the name field as a **workspace slug**, helper text that it is used in URLs/resource names, live `WORKSPACE_NAME_RE` error unchanged.
3. Set `pendingComponent` to `FormPageSkeleton` (same code-split grouping as other create routes).
4. Keep Hobby default, Enter-to-submit, Cancel → `/`, create → select returned id → `/`.

## Files

- `dashboard/src/routes/new.workspace.tsx`
- `dashboard/src/routes/__tests__/new.workspace.test.tsx`
- `dashboard/src/features/workspaces/locales/{en,zh}.ts`
- `dashboard/src/common/components/detail-skeletons.tsx` (only if the skeleton needs a form-shaped variant)

## Acceptance criteria

- [ ] The page heading is a real `h1` ("Create a workspace"), not a card title.
- [ ] Slug helper + validation copy are visible; invalid slugs still block submit.
- [ ] Slow navigation to `/new/workspace` shows `FormPageSkeleton`, not the bare spinner.
- [ ] Existing post-create landing tests still pass.

## Out of scope

- Plan-card redesign (t002). Payment panel (t003). Switcher (t004). Freeform display names.
