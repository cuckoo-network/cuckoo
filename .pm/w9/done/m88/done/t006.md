---
id: w9/m88/t006
title: Render parity
worker: worker9
status: done
estimate: 25m
depends_on: [w9/m88/t001, w9/m88/t002, w9/m88/t003, w9/m88/t004, w9/m88/t005]
---

## Objective

Check the shipped create flow against Render's dashboard and against bex's GraphQL-only workspace mutations. Flag drift; do not invent REST/MCP create or Render's $25/$499.

## Context

- Surfaces: dashboard `/new/workspace` + switcher (this milestone); GraphQL `createWorkspace` already exists (`w6/m1`). REST `/v1/owners` is read-only; MCP has no workspace create (ADR018).
- Compare: t005 artifact + `https://render.com/docs/team-members` create steps + `docs/render-artifacts/workspace-plan-change.md`.
- Prices: bex catalog 30% off is **intentional** drift from Render's sticker, not a bug.

## Steps

1. Confirm GraphQL create args/errors unchanged (name + plan; Hobby cap inline).
2. Confirm no accidental REST/MCP workspace-create endpoint.
3. From the t005 table, file inbox notes for anything still worth doing (Enterprise "Get in touch", licensed SKU collection) instead of quietly coding it here.

## Files

- `docs/ADR018-render-parity.md` (workspace lifecycle UI cell — only if the row's evidence needs a one-line update)
- `.pm/w9/NNN.md` inbox notes if new follow-ups appear

## Acceptance criteria

- [ ] REST/MCP still have no workspace-create mutation.
- [ ] Dashboard prices are the catalog 30%-off set, documented as deliberate vs Render's $25/$499.
- [ ] Any remaining UX drift is in an inbox note or the t005 artifact, not unmentioned.

## Out of scope

- Implementing licensed Stripe collection or freeform names.
