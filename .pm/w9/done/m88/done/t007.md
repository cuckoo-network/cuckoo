---
id: w9/m88/t007
title: Simplify
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m88/t006]
---

## Objective

Run `/simplify` over the dashboard files this milestone changed. Behavior-preserving only.

## Context

- Likely reuse: `PlanPicker` already shared with change-plan dialog; `BillingOnboardingView` must not be copied.
- Watch for a second plan-catalog in locales vs `pricing.yaml` comments drifting.

## Steps

1. Run `/simplify` on `dashboard/src/routes/new.workspace.tsx`, `plan-picker.tsx`, `workspace-switcher.tsx`, and any payment panel extracted in t003.
2. Apply reuse/altitude cleanups; do not restyle the page a second time.

## Files

- files t001–t004 actually touched

## Acceptance criteria

- [ ] `/simplify` ran; applied notes recorded in this task's closeout (or "nothing to apply").
- [ ] One PlanPicker, one billing onboarding view.

## Out of scope

- New features.

## Simplify notes

One `PlanPicker` (create page + change-plan dialog). One `BillingOnboardingView` (payment panel reuses it; no fork). Shared `workspacePlanNameKey` for the switcher and settings badge. Payment-gate predicate extracted to `createBlockedByPayment` so the route does not inline the Hobby vs Pro vs gate-off matrix.

