---
id: w9/m88/t008
title: Test coverage
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m88/t006]
---

## Objective

Add tests that pin the create-page shape, catalog fees, Hobby vs paid payment gating, and switcher items. Assert real UI behavior, not snapshots of the whole page.

## Context

- Existing: `new.workspace.test.tsx` (post-create landing), `plan-picker.test.tsx` (fees), `workspace-switcher.test.tsx`.
- Fail closed on Render sticker prices and on Create enabled for Pro when the gate is on and billing is not ready.

## Steps

1. Extend plan-picker tests: bullets present; `$25`/`$499` absent; catalog three present.
2. Route tests: `h1` present; invalid slug blocks submit; Pro+gate-on disables Create until ready; Hobby does not.
3. Switcher tests: `+ New Workspace`, plan sublabel, Billing → `/billing`.
4. `yarn test` the touched files; no tautological snapshots.

## Files

- `dashboard/src/routes/__tests__/new.workspace.test.tsx`
- `dashboard/src/features/workspaces/components/__tests__/plan-picker.test.tsx`
- `dashboard/src/features/workspaces/components/__tests__/workspace-switcher.test.tsx`

## Acceptance criteria

- [ ] Tests fail if catalog fees revert to "No workspace fee" or to Render's $25/$499.
- [ ] Tests fail if Pro+gate-on allows Create without readiness.
- [ ] `yarn test` green for the files above.

## Out of scope

- Backend `createWorkspace` tests (already in `w6/m1`).
- Stripe catalog script tests (workspace SKUs are intentionally not meters).
