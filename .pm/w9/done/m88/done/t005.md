---
id: w9/m88/t005
title: Authenticated Playwright walk vs Render; pin the artifact
worker: worker9
status: done
estimate: 35m
depends_on: [w9/m88/t001, w9/m88/t002, w9/m88/t003, w9/m88/t004]
---

## Objective

Drive both dashboards signed-in to `/new/workspace`, screenshot + accessibility dump, and pin a render-artifact that records remaining drift. Unauthenticated login-bounce is **not** enough (already captured 2026-08-19).

## Context

- Prior unauthenticated Playwright: `.playwright-mcp/render-new-workspace.png` and `bex-new-workspace.png` (login walls, `next=/new/workspace` preserved).
- Method: Playwright MCP to `.playwright-mcp/` (bare filenames). Do not commit `.env` or session cookies. Do not submit a real paid plan change on Render.
- Write `docs/render-artifacts/new-workspace.md` in the same shape as `workspace-plan-change.md` / `workspace-lifecycle.md`.

## Steps

1. Sign in to Render and Bex (scratch or existing; do not print secrets). Open `/new/workspace` on both.
2. Screenshot full page; dump headings, radios, price strings, payment section presence, footer buttons.
3. Confirm Bex prices are $0 / $17.50 / $349.30, not $25 / $499.
4. Write the artifact: layout, slug vs freeform name, payment panel, switcher, landing; table of match / deliberate drift / follow-up.

## Files

- `docs/render-artifacts/new-workspace.md` (new)
- `.playwright-mcp/bex-new-workspace-auth.png` (and Render counterpart; gitignored)

## Acceptance criteria

- [ ] Artifact exists and names the catalog fees.
- [ ] Screenshots were taken of the **authenticated** form, not the login page.
- [ ] Remaining drift (freeform names, licensed SKU collection, Enterprise contact-sales) is listed as follow-up, not silently "fixed" in copy.

## Out of scope

- Creating a real paid Render workspace or attaching a live card on production Bex beyond what t003 already does in the UI.
