---
id: w9/m88/t004
title: "Switcher: + New Workspace, plan sublabel, Billing"
worker: worker9
status: done
estimate: 30m
depends_on: []
---

## Objective

Align the top-left workspace switcher with Render's captured menu: plan as a sublabel on each workspace, **+ New Workspace**, and a Billing item that goes to `/billing`.

## Context

- Capture: `docs/render-artifacts/workspace-lifecycle.md` — listbox contents include Billing, Workspace settings, Switch Workspace (name + plan), New Workspace (`/new/workspace`).
- Today: `dashboard/src/features/workspaces/components/workspace-switcher.tsx` lists names only, then Settings, then "New Workspace" (no `+`).
- Billing page is `/billing` (`w5/m70`); `/usage` redirects there. Do not invent `/w/{id}/billing`.
- `WorkspaceView.plan` is already on the switcher query.

## Steps

1. Locale `workspaces.switcherNew` → `+ New Workspace` (zh equivalent).
2. Each workspace row: name + muted plan label (Hobby/Pro/Scale/Enterprise via existing name keys).
3. Add a Billing menu item → `navigate({ to: "/billing" })`, placed with Settings (Render puts Billing first; match that order if it still reads clean in the one-rail header).
4. Keep current-workspace checkmark and selection persistence.

## Files

- `dashboard/src/features/workspaces/components/workspace-switcher.tsx`
- `dashboard/src/features/workspaces/components/__tests__/workspace-switcher.test.tsx`
- `dashboard/src/features/workspaces/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] Menu item reads `+ New Workspace` and still routes to `/new/workspace`.
- [ ] Each listed workspace shows its plan under/beside the name.
- [ ] Billing navigates to `/billing`.
- [ ] Existing switch-and-settings tests updated, not deleted.

## Out of scope

- Render's separate Billing route per `tea-id`.
- Organizations / multi-workspace org switcher (Scale+; DO_NOT_DO enterprise SSO/orgs).
