---
id: w9/m83/t005
title: Test coverage — regression tests for every preserved viewer behavior
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m83/t003]
---

## Objective

Add meaningful regression tests so the virtualization cannot silently break the viewer's behaviors. Tests must assert real behavior, not snapshot-everything.

## Context

- Uses the t001 harness. Behaviors to lock down: only-visible-rows (t003 has the core assertion — extend to a second buffer size), follow-to-bottom, scroll-up-breaks-follow, jump-to-latest, instance chips, request chips, wrap mode, nowrap mode, and visible-row text selection (the decided tradeoff).

## Steps

1. Add per-behavior tests for follow/pin/jump, chips, and wrap/nowrap.
2. Add a test asserting the documented text-selection behavior (visible rows selectable).
3. Ensure tests fail if virtualization is removed or a behavior regresses (non-vacuous).

## Files

- `dashboard/src/**/log-line-list.test.tsx`.

## Acceptance criteria

- [ ] Regression tests cover follow/pin/jump, chips, wrap/nowrap, and text selection.
- [ ] Tests are non-vacuous (fail on regression), suite green.

## Out of scope

- Profiling (t003).
