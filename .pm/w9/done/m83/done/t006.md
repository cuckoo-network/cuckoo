---
id: w9/m83/t006
title: Closeout
worker: worker9
status: done
estimate: 15m
depends_on: [w9/m83/t005]
---

## Objective

Close the milestone once its definition of done actually holds — virtualization is real, verified by a working harness, all preserved behaviors are tested, the profile is captured, and the retained-line cap decision is made.

## Context

- Do not run until the DoD in `README.md` is met (a milestone lands in `done/` when its observable end state is real, not merely when code is written).

## Steps

1. Confirm every other task is `status: done` and the DoD holds.
2. Set all remaining task frontmatter to `status: done`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `README.md`.
4. Move each `tNNN.md` to `w9/m83/done/`, then move the whole `w9/m83/` to `w9/done/m83/`.
5. Check `- [x]` for m83 in `w9/README.md`.
6. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w9/m83/**`, `.pm/w9/README.md`.

## Acceptance criteria

- [ ] DoD verified; milestone moved to `w9/done/m83/`; workstream box checked.

## Out of scope

- Any further virtualization work (file a fresh note).
