---
id: w9/m62/t006
title: Test coverage
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m62/t004]
---

## Objective

Add meaningful tests so the polling gates and the primed-mount behavior can't silently regress.

## Context

- The existing resource-hook tests around `skipPollWhenHidden` show the pattern for asserting gate wiring.
- The high-value assertion for t004: mounting a primed hook against a pre-populated Apollo cache issues zero network requests.

## Steps

1. A sweep-style guard test: every hook that passes `pollInterval` also passes `skipPollAttempt` (static assertion over the hooks, or per-hook unit tests for the newly gated ones — pick the form that fails loudly on a new ungated poller).
2. `payment-required`: test the poll stops when `paymentMethodReady` and re-arms on reopen (MockedProvider).
3. Deploy detail: test timeline/log panels mount (skeletons visible) while the header query is still in flight.
4. Primed mount: pre-populate the cache with a primed query result, mount the hook, assert no additional network operation fires (Apollo `MockedProvider` with zero remaining mocks passes only if no fetch happens).

## Files

- `dashboard/src/features/**/__tests__/` alongside the touched hooks/components

## Acceptance criteria

- [ ] A future hook adding an ungated `pollInterval` fails CI (or the newly gated hooks are each asserted).
- [ ] The zero-refetch-on-primed-mount behavior is asserted; full suite green.
