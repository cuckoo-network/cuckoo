---
id: w9/m62/t003
title: "Usage/billing pollers: gate, and stop the 2s poll once satisfied"
worker: worker9
status: done
estimate: 30m
depends_on: []
---

## Objective

Gate the usage/billing pollers on visibility, stop the payment-required dialog's 2s poll once the payment method is bound, and fix the one `database-insights` leg its four siblings already gate.

## Context

- `dashboard/src/features/usage/context/payment-required.tsx:56` — `pollInterval: 2_000` while the dialog is open; no visibility gate, and it keeps polling after `paymentMethodReady` flips true.
- `use-resource-limits.ts:45` (15s), `use-usage.ts:101` (60s), `use-billing-onboarding.ts:90` (15s) — no gate.
- `dashboard/src/features/databases/hooks/use-database-insights.ts:78` — the `parameterOverrides` leg lacks `skipPollAttempt` while the sibling legs at lines 48/55/62/69 have it.

## Steps

1. Add `skipPollAttempt: skipPollWhenHidden` to each listed hook/query.
2. In `payment-required.tsx`, stop polling (or `skip`) once `paymentMethodReady` is true; re-arm if the dialog reopens.
3. Manual check: open the payment dialog, complete/simulate readiness — polling stops; background tab — quiet.

## Files

- `dashboard/src/features/usage/context/payment-required.tsx`
- `dashboard/src/features/usage/hooks/use-resource-limits.ts`
- `dashboard/src/features/usage/hooks/use-usage.ts`
- `dashboard/src/features/usage/hooks/use-billing-onboarding.ts`
- `dashboard/src/features/databases/hooks/use-database-insights.ts`

## Acceptance criteria

- [ ] No usage/billing/insights query polls while hidden.
- [ ] The 2s poll ceases once `paymentMethodReady`; dialog reopen re-arms it.
- [ ] Existing usage/billing tests green.

## Out of scope

- Changing any poll interval values; billing UX changes.
