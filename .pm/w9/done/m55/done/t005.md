---
id: w9/m55/t005
title: "Render parity: compare dead-id behavior with Render's dashboard"
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m55/t002, w9/m55/t003, w9/m55/t004]
---

## Objective

Standing closing task. This milestone changes only the dashboard UI — REST/GraphQL/MCP error shapes are untouched — so the parity check here is dashboard-vs-Render-dashboard: what does dashboard.render.com do when a logged-in user visits a dead resource URL (e.g. `dashboard.render.com/d/dpg-garbage`)? Record the observed behavior and flag any drift bex should follow up on rather than silently diverging.

## Context

- `docs/ADR018-render-parity.md` is the parity ledger; dashboard behavior notes land there when a row exists, otherwise a short note in the milestone README suffices.
- bex's chosen behavior (redirect to `/` + toast; nested deploy → parent Deploys tab) is a user decision 2026-07-17 — if Render does something else (e.g. its own inline 404), record the divergence as deliberate, don't reopen the decision.
- Verify the REST error dialect really is untouched: a garbage-id GraphQL query from the dashboard still yields the same wire response as before this milestone (no backend edits should exist in the diff).

## Steps

1. Check Render's dashboard behavior for a dead service/postgres/env-group URL (docs, or a live account if available; otherwise record "unverified — docs only" honestly).
2. Diff review: confirm the milestone's changes are `dashboard/` only.
3. Record findings (parity ledger row or milestone README note); file follow-ups for any drift worth closing.

## Files

- `docs/ADR018-render-parity.md` (only if a relevant row exists)
- `.pm/w9/m55/README.md` (note)

## Acceptance criteria

- [ ] Render's dead-id dashboard behavior recorded with evidence (or explicitly marked unverifiable).
- [ ] Confirmed no `lego/` changes in the milestone diff.

## Out of scope

- Changing REST/GraphQL/MCP error responses.
