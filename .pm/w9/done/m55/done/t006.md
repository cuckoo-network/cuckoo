---
id: w9/m55/t006
title: "Simplify: /simplify over the changed code"
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m55/t005]
---

## Objective

Standing closing task. Run `/simplify` over the code this milestone changed (the shared helper/hook, the eight touched routes, deploy-detail-page) — reuse / simplification / efficiency, behavior-preserving. The migration deletes eight bespoke inline not-found blocks; verify no dead code, unused locale keys, or now-redundant `notFound` plumbing (e.g. `useWebhook`'s `notFound` flag, `classifyEnvGroupError`) survives unreferenced.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; `yarn typecheck && yarn lint && yarn test`.

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly declined.
- [ ] No unused not-found locale keys or dead branches remain.
