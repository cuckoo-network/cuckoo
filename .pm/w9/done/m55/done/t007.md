---
id: w9/m55/t007
title: "Test coverage: redirect vs stay-put (error) branches per route family"
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m55/t005]
---

## Objective

Standing closing task. Add meaningful vitest coverage for the behavior this milestone shipped: settled not-found redirects (to `/`, or to the Deploys tab for the nested case) and — the regression that matters most — a query **error never redirects**. No snapshot-everything or tautological tests.

## Context

- Tests live in `__tests__` next to the code (`dashboard/CLAUDE.md`).
- The highest-value assertions: (1) the shared helper's three-state contract (partly done in t001 — extend, don't duplicate); (2) one route per family (a flat route, the services shell, the deploy detail) mounted with mocked Apollo results for not-found vs error, asserting navigation happened vs the inline error UI rendered; (3) the toast fires exactly once per redirect.

## Steps

1. Extend t001's helper/hook tests if gaps remain.
2. Route-level tests: databases (flat), services shell (guarded Outlet), deploy detail (nested target) — not-found ⇒ redirected; error ⇒ inline error UI, no navigation.
3. `yarn test` green; spot-check `yarn test:coverage` on the new modules.

## Files

- `dashboard/src/common/**/__tests__/`
- `dashboard/src/routes/`-adjacent or feature `__tests__/` per repo convention

## Acceptance criteria

- [ ] Error-state stay-put is asserted for every tested family (the anti-bounce guarantee).
- [ ] Not-found redirect target asserted, including the nested deploy target.
- [ ] All suites green.
