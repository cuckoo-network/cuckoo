# Live `render login` bad-key probe against the new 401 body

Filed by `w9/done/m38/done/t003.md` (Render parity, 2026-07-15): the m38 closeout verified the unified 401 `{"error","message","id"}` body via `internal/api.TestErrorDialectIsRenderShaped` but did **not** run the live CLI probe — "Recommended follow-up: run the live `render login` probe on the next dev-9 session and attach output to the `docs/cli-compatibility-checklist.md` login row."

Do it: dev-9 stack up, locally built `render` binary, `render login` with a bad key, confirm the CLI surfaces `message: "unauthorized"` verbatim (previously a generic "unknown error" off the text/plain body), attach the output as evidence on the checklist's login row.

~30m. Source: `/pm-brainstorm` round 15's closeout-residual sweep, 2026-07-15.
