---
id: w9/m56/t008
title: "Simplify: /simplify over the changed code"
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m56/t007]
---

## Objective

Standing closing task. Run `$simplify` over the milestone diff, focusing on reuse of the existing Database/KeyValue allowlist entry model, conversion helpers, GraphQL input types, dashboard editor, and CLI verifier utilities.

## Steps

1. Run `$simplify` scoped to the milestone's code diff.
2. Apply behavior-preserving findings or record why a suggestion is declined.
3. Remove redundant flat/structured conversions and duplicated dashboard form code without removing compatibility paths.
4. Re-run affected Go, dashboard, and verifier tests.

## Acceptance criteria

- [ ] `$simplify` was run and findings were applied or explicitly declined.
- [ ] One canonical conversion path defines entry-to-CIDR fallback/precedence per layer.
- [ ] No dead compatibility fields, stale comments, or duplicated networking editor logic remain.
- [ ] Affected suites remain green.
