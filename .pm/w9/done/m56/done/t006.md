---
id: w9/m56/t006
title: Regrade the checklist from captured official-CLI evidence
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m56/t004, w9/m56/t005]
---

## Objective

Update the service create/update section and top-level findings in `docs/cli-compatibility-checklist.md` from a fresh run of the maintained verifier, removing stale limitations only when the unmodified official CLI and readback evidence prove them resolved.

## Context

- The checklist is an evidence ledger, not an implementation wish list. `[x]` requires a successful official-CLI call and exact readback; unavailable dependencies remain `[~]` unless the configured-capabilities run is captured.
- `--runtime`, `--previews`, and arbitrary platform regions are boundary classifications from t001, not gaps to paper over.
- The current intended-divergence paragraph says service allowlists drop descriptions; this must disappear only after REST and dashboard/adapter regression coverage is green.

## Steps

1. Run the dependency-free and configured-capabilities verifier legs with official CLI v2.21.0 and record date/target/capabilities without credentials.
2. Regrade every service create/update flag, updating the top-level real-gap and intended-divergence summaries consistently.
3. Link the durable verifier and detailed artifact from the checklist's verification method.
4. Keep `--runtime`, `--previews`, and clone/region wording aligned with t001's evidence; do not use `[x]` for a workaround-only path.
5. Run Markdown formatting.

## Files

- `docs/cli-compatibility-checklist.md`
- `docs/render-artifacts/cli-services-create-update.md`
- `scripts/cli-compat.sh`

## Acceptance criteria

- [ ] Every service create/update row agrees with the latest verifier evidence and the legend.
- [ ] The real-gap and intended-divergence summaries contain no stale description-loss claim or misclassified upstream/client guard.
- [ ] Supported environment-backed flags are `[x]` only when the configured-capabilities run passed exact readback.
- [ ] `npx prettier@3.4.2 --write "**/*.md"` leaves Markdown formatted.

## Out of scope

- Regrading unrelated KeyValue, Postgres, Skills, or session-command gaps.
