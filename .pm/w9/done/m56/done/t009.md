---
id: w9/m56/t009
title: "Test coverage: storage compatibility, adapters, UI, and CLI harness"
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m56/t007]
---

## Objective

Standing closing task. Add meaningful regression coverage for the behavior this milestone ships, especially legacy CR compatibility, description preservation, conflicting inputs, clear semantics, UI resubmission, and non-vacuous official-CLI verification.

## Steps

1. Add types/operator tests for flat-only Apps, structured entries, precedence, clear, invalid CIDRs, and exact Traefik ranges.
2. Add backend tests for REST create/PATCH/GET, GraphQL legacy + structured mutations, MCP legacy + structured tools, and conflicting inputs.
3. Add dashboard tests that load an entry with a description, edit/save it, and prove the submitted mutation preserves it; cover validation and clear.
4. Extend `scripts/cli-compat.sh mutation-check` or the new verifier's self-test so removing/skipping each create/update leg is detected.
5. Run `make test` from `lego/operator/`, `go test ./...` from `lego/backend/`, and dashboard typecheck/lint/tests.

## Acceptance criteria

- [ ] Tests fail against the pre-milestone description-dropping behavior.
- [ ] Legacy flat CRs and existing flat GraphQL/MCP clients are explicitly covered.
- [ ] Failure modes include invalid CIDR, conflicting legacy/structured input, dependency unavailable, and cleanup after verifier failure.
- [ ] Operator/types, backend, dashboard, and verifier suites are green.
