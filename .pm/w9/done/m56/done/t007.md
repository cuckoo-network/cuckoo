---
id: w9/m56/t007
title: "Render parity: audit every service create/update surface"
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m56/t006]
---

## Objective

Standing parity task. Compare the shipped service allowlist and mutation behavior with Render's REST contract and ensure bex's REST, GraphQL, MCP, dashboard, Blueprint, and operator projections agree on entry ordering, descriptions, clear semantics, validation, and compatibility aliases.

## Steps

1. Compare official CLI request/readback evidence and Render API documentation for service `ipAllowList` create/PATCH shapes.
2. Audit REST, GraphQL, MCP, dashboard, Blueprint, and operator paths for one Core meaning; add a follow-up PM note for any adjacent drift that cannot be fixed inside this milestone.
3. Confirm deliberate differences remain explicit: preview environments are a non-goal, runtime update is blocked upstream, and region metadata remains truthful.
4. Update `docs/ADR018-render-parity.md` only if its service networking row/evidence changes.

## Acceptance criteria

- [ ] REST matches Render's ordered `{cidrBlock, description}` shape and clear semantics.
- [ ] GraphQL, MCP, dashboard, Blueprint, and operator neither drop descriptions nor change effective CIDRs.
- [ ] Compatibility aliases are tested and documented; no surface silently prefers conflicting inputs.
- [ ] Any remaining drift has an explicit checklist classification or filed PM follow-up.
