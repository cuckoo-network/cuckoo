---
id: w9/m38/t005
title: Test coverage
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m38/t003]
---

## Objective

Add a regression test locking the error dialect: assert `Content-Type: application/json` and a non-empty `message` field on the unauthenticated 401, a metrics param 400, and the deploy-hook 405. Tests must assert real behavior, not snapshot bodies.

## Files

- `lego/backend/internal/api/` and/or `internal/metrics/` test files

## Acceptance criteria

- [ ] Test fails if any of the three paths regresses to `http.Error`/bare-`{"error"}`.
- [ ] `cd lego/backend && go test ./...` passes.
