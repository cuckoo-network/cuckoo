---
id: w9/m92/t010
title: Simplify
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m92/t009]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification /
efficiency; behavior-preserving only).

## Context

Touches `dashboard/src/features/agent-sessions/`,
`dashboard/src/common/components/dashboard-layout/agent-sessions-nav-section.tsx`,
`dashboard/src/routes/agents.tsx`, and
`lego/backend/internal/agentsessions/` / `lego/backend/internal/sshgateway/agentattach/`
depending on what t001/t005 touched server-side.

## Steps

1. Run `/simplify` scoped to the files this milestone changed.
2. Apply any behavior-preserving cleanups it surfaces.

## Files

- All files touched by t001-t008.

## Acceptance criteria

- [ ] `/simplify` run recorded; any applied changes preserve behavior
      (existing + new tests still pass).

## Out of scope

- Bug hunting — this is quality-only, per `/simplify`'s own scope.
