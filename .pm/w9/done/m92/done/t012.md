---
id: w9/m92/t012
title: Closeout
worker: worker9
status: done
estimate: 10m
depends_on: [w9/m92/t011]
---

## Objective

Close the milestone once its definition of done is actually met: set every
remaining task's `status: done`, move each `tNNN.md` to `w9/m92/done/`, mark
every row `— **DONE**` and set `**Status:** done` in `w9/m92/README.md`, move
the whole `w9/m92/` directory to `w9/done/m92/`, and check `- [x]` for **m92**
in `w9/README.md`.

## Context

Per `.pm/CLAUDE.md` and the `/pm` skill's `done` subcommand — completing this
task *is* the move.

## Steps

1. Verify every item in `w9/m92/README.md`'s "Definition of done" actually
   holds (not just that code was written).
2. Run `/pm done w9/m92/t012` (or the equivalent manual steps) to perform the
   milestone move.

## Files

- `w9/m92/README.md`, `w9/README.md`, every `w9/m92/t*.md`.

## Acceptance criteria

- [ ] Milestone directory moved to `w9/done/m92/`, no stub left at the old
      path.
- [ ] `w9/README.md`'s **m92** line checked `- [x]`.

## Out of scope

- N/A — this task is the closeout mechanism itself.
