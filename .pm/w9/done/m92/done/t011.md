---
id: w9/m92/t011
title: Test coverage
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m92/t009]
---

## Objective

Add meaningful tests for every behavior this milestone shipped. Tests must
assert real behavior and failure modes — no trivial, tautological, or
snapshot-everything tests.

## Context

Each task (t001-t008) lists its own acceptance-criteria tests; this task is
the sweep to confirm they're all actually in place and cohesive, plus fill any
gap.

## Steps

1. Confirm each of t001-t008's acceptance-criteria tests exist and are
   non-vacuous (they fail without the fix, pass with it).
2. Run the full dashboard suite (`yarn typecheck && yarn lint && yarn test`)
   and backend suite (`go test ./...` + `make lint` from `lego/operator/`,
   plus `cd lego/backend && go test ./...` if t001/t005 touched backend code).
3. Fill any coverage gap the per-task lists left implicit.

## Files

- Test files added/modified across t001-t008.

## Acceptance criteria

- [ ] `yarn typecheck && yarn lint && yarn test` green in `dashboard/`.
- [ ] `go test ./...` + `make lint-backend` green in `lego/backend/` (if
      touched).
- [ ] Every fixed bug has at least one regression test that would fail on the
      pre-fix code.

## Out of scope

- End-to-end/live-cluster verification beyond what's practical in CI (note as
  residual if a live check is warranted, e.g. for t005's transcript replay).
