---
id: w9/m92/t009
title: Render parity check (REST/GraphQL/MCP + dashboard)
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m92/t001, w9/m92/t002, w9/m92/t003, w9/m92/t004, w9/m92/t005, w9/m92/t006, w9/m92/t007, w9/m92/t008]
---

## Objective

Check that t001's `prNumber` null-vs-zero fix and t003's `archived` filter
value rename are consistent across every surface bex-api exposes — REST,
GraphQL, and MCP (`lego/backend/`) — and the dashboard, per bex-api's
Render-compatibility goal (`docs/ADR006-bex-api.md`).

## Context

- t001 changes how `prNumber` is serialized when there's no PR — verify REST
  JSON, GraphQL, and MCP tool responses all agree (`null`/absent, not `0`).
- t003 changes the dashboard's local `archived` filter-key representation —
  verify it still maps correctly to whatever value the backend's `archived`
  GraphQL variable (and REST equivalent, if any) expects; the backend contract
  itself should not need to change unless t003 uncovered a real mismatch.
- t002, t004, t006, t007, t008 are dashboard-only (no REST/GraphQL/MCP surface
  change) — confirm that's still true after implementation.

## Steps

1. Compare `prNumber` across `lego/backend/internal/agentsessions/`'s REST
   handler, GraphQL resolver, and MCP tool (if agent sessions are exposed via
   MCP) for a session with no PR.
2. Compare the `archived` filter's accepted values across REST/GraphQL/MCP and
   the dashboard's post-t003 representation.
3. Record any drift found as follow-up work rather than silently diverging.

## Files

- `lego/backend/internal/agentsessions/`
- `dashboard/src/features/agent-sessions/`

## Acceptance criteria

- [ ] `prNumber` is `null`/absent (not `0`) for a no-PR session on every
      surface that exposes it.
- [ ] `archived` filtering behaves identically via REST/GraphQL/MCP and the
      dashboard.
- [ ] Any discovered drift is filed as a follow-up note, not silently ignored.

## Out of scope

- New Render-parity surfaces unrelated to this milestone's changes.
