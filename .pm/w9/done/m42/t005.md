---
id: w9/m42/t005
title: Render parity — cross-surface check vs Render's datastore objects
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m42/t004]
---

## Objective

Verify the shipped fields are consistent across REST/GraphQL/MCP/UI and match Render's datastore object semantics (pinned OpenAPI + CLI types); flag drift as follow-up rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md`, pinned Render OpenAPI, `docs/cli-compatibility-checklist.md` datastore rows.

## Steps

1. Same store via all surfaces; diff.
2. Diff vs Render's schema; file follow-ups for any drift.

## Files

- `docs/ADR018-render-parity.md` (evidence)

## Acceptance criteria

- [ ] No unrecorded cross-surface or Render-shape drift remains.

## Out of scope

- Fixing large drift inline.
