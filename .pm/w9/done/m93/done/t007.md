---
id: w9/m93/t007
title: Closeout
worker: worker9
status: done
estimate: 10m
depends_on: [w9/m93/t006]
---

## Objective

Close the milestone once its definition of done is actually met — not merely when
the code is written.

## Context

- DoD in the milestone README. Note that t003's DoD includes a **live** verifier
  run (or an explicitly recorded added-but-live-pending status); do not close on
  written-but-unrun if the live leg is the stated end state.

## Steps

1. Confirm every other task is `status: done` and the DoD holds (backend guards
   green + non-vacuous, verifier legs added + live-run status recorded, any
   image-shape residual resolved-or-accepted-with-evidence).
2. Set remaining tasks `status: done`; move each `tNNN.md` to `w9/m93/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in this README.
4. Move `w9/m93/` to `w9/done/m93/` and check `- [x]` in the w9 README.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w9/m93/**`, `.pm/w9/README.md`.

## Acceptance criteria

- [ ] Milestone moved to `w9/done/m93/`, w9 README box checked, statuses synced.

## Out of scope

- Any implementation work (must be finished in t001–t006 first).
