---
id: w9/m93/t004
title: Render parity check
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m93/t001, w9/m93/t002, w9/m93/t003]
---

## Objective

Verify any read-surface change this milestone lands stays Render-consistent
across every surface, and compare against render.com behavior.

## Context

- If t001 resolves the prebuilt-image round-trip by deriving
  `serviceDetails.runtime`/`env` (and an `envSpecificDetails` shape) for image
  web services, that read change must be identical on REST, GraphQL (`runtime`
  field), and MCP `get_service` — the same `toRenderService(view)` feeds all
  three, but confirm no surface special-cases it.
- The dashboard consumes the GraphQL surface; check it renders the derived
  runtime sanely (no new "unknown"/blank state) if it surfaces runtime at all.
- Compare the emitted `runtime`/`env`/`envSpecificDetails` against a real
  render.com service of the same shape (captured artifact) and flag drift as
  follow-up rather than diverging silently.

## Steps

1. Enumerate the surfaces touched by t001–t003's changes.
2. Confirm REST/GraphQL/MCP agreement (extend the cross-surface assertion in
   `internal/apps/builder_test.go` if a new shape was added).
3. Check the dashboard path for any runtime display.
4. Diff against render.com evidence; record any residual as a checklist/ADR018
   note.

## Files

- `docs/ADR018-render-parity.md`, `docs/cli-compatibility-checklist.md` (notes
  only), plus test files if a cross-surface assertion is added.

## Acceptance criteria

- [ ] Every read-surface change is consistent across REST/GraphQL/MCP (+ dashboard).
- [ ] Compared against render.com; drift flagged, not silently shipped.

## Out of scope

- Net-new features beyond t001–t003.
