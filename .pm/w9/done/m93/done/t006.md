---
id: w9/m93/t006
title: Test coverage — prove every new guard is non-vacuous
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m93/t004]
---

## Objective

Prove the guards this milestone adds actually fail when the behavior they protect
regresses — a guard that can't fail is worse than none (it paints a false green).
Mirror the w9/m4 `mutation-check` discipline.

## Context

- t001 round-trip matrix, t002 totality guard, and the existing
  `TestEffectiveRuntimeReadBack` / `TestDockerfileServiceRuntimeIsConsistentAcrossSurfaces`.
- The regression to prove-against is exactly w4/052: `effectiveRuntime`
  returning "" for a Dockerfile-via-builder service.

## Steps

1. Spot-mutate `effectiveRuntime` (e.g. make the docker branch return "") and
   confirm t001 + t002 + the w4/052 guards fail; revert.
2. Spot-mutate the read wiring (e.g. `view()` back to `a.Spec.Runtime`) and
   confirm the cross-surface test fails; revert.
3. Confirm each verifier leg added in t003 is non-vacuous (its assertion fails if
   the field doesn't round-trip) — reuse `cli-compat.sh mutation-check` style if
   applicable, else reason it explicitly in the leg.
4. Add any missing behavioral assertion the mutation exercise reveals.

## Files

- The milestone's test files; notes in the milestone README if a leg is proven
  by reasoning rather than an automated mutation.

## Acceptance criteria

- [ ] Each new/updated guard demonstrably fails under a targeted regression and
      passes after revert.
- [ ] No tautological or assert-nothing checks introduced.

## Out of scope

- Coverage of unrelated code.
