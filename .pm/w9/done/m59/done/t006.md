---
id: w9/m59/t006
title: Test coverage
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m59/t005]
---

## Objective

Add meaningful regression coverage for Bex launcher defaults, explicit overrides, credential/config isolation, and failure behavior before release.

## Context

- Standing closing task for this milestone.
- Tests must verify real process and HTTP behavior rather than only snapshotting source strings.

## Steps

1. Cover default Bex host/config resolution and each supported BEX override.
2. Cover Render-environment precedence and protection of an existing Render config directory.
3. Cover unauthenticated, expired/refreshable, and logged-out behavior against controlled HTTP/Hydra boundaries.
4. Run the appropriate upstream and Bex compatibility suites.

## Files

- `cli/` tests
- existing auth/CLI test harnesses as needed

## Acceptance criteria

- [ ] Tests cover default, override, and invalid/missing-credential paths.
- [ ] Tests prove no credential material is logged and no Render local state is mutated.
- [ ] Relevant launcher, backend, and compatibility suites pass.

## Out of scope

- Artificial coverage targets or snapshot-only tests.
