---
id: w1/m37/t004
title: Custom-page `uri` support
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m37/t003]
---

## Objective

A non-empty `uri` serves the tenant's own maintenance page per t001's captured semantics (redirect vs fetch-and-serve vs path) instead of the default; empty falls back to the default page. Validate the value (absolute URL or path per capture; junk ⇒ named 400 at the API layer, t005 coordinates).

## Acceptance criteria

- [ ] Custom page served when set (verified against the mock cluster); empty ⇒ default; invalid rejected.
