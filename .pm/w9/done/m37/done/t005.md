---
id: w1/m37/t005
title: REST/GraphQL/MCP with Render's object shape
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m37/t002]
---

## Objective

`maintenanceMode {enabled, uri}` on create + PATCH + read across the three adapters with Render's exact object shape (required keys per the pinned spec); non-web types get a named rejection; one core verb, thin adapters.

## Files

- `lego/backend/internal/apps/{service,rest,graphql,mcp}.go` (+ `AppView`)

## Acceptance criteria

- [ ] Round-trip + type rejection identical on all three surfaces (adapter-parity test).
