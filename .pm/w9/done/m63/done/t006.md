---
id: w9/m63/t006
title: Test coverage
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m63/t004]
---

## Objective

Add meaningful tests for the virtualization and loading-state behaviors this milestone shipped.

## Steps

1. Virtualization: with a 1,000-line buffer, assert the DOM row count is bounded («50) and that appending a line does not re-parse existing lines (spy on the ingest parser).
2. Follow behavior: appending while followed scrolls to bottom; a user scroll-up breaks follow (existing behavior preserved under the virtualizer).
3. Metrics loading: while the query is in flight, assert the shimmer renders and "No data in range" does not; resolved-empty renders the empty state.
4. Logs/Scaling: `NonStaticRoute` loading renders the skeleton fallback (not `null`); the scaling page renders the reserved card skeleton while autoscaling loads.

## Files

- `dashboard/src/features/logs/components/__tests__/`
- `dashboard/src/features/metrics/components/__tests__/`
- `dashboard/src/features/services/components/__tests__/`

## Acceptance criteria

- [ ] All four behavior families asserted non-tautologically; full dashboard suite green.
