---
id: w9/m63/t004
title: Render parity
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m63/t001, w9/m63/t002, w9/m63/t003]
---

## Objective

This milestone changes visible UI loading/streaming states on the service detail tabs. Check the result against render.com's equivalents and flag drift as follow-up rather than silently diverging. UI-only — no REST/GraphQL/MCP surface is touched.

## Context

- Compare: Render's service log tail (scrolling/follow behavior, loading presentation), metrics-chart loading states, and any skeleton/shimmer idioms on their detail tabs.
- Prior captures live under `.playwright-mcp/` and `docs/render-artifacts/` (`metrics-page.md`, logs-related walk notes) — reuse before taking new ones; take fresh captures only if a live Render session is available.

## Steps

1. Walk bex's Logs (live tail on a busy build) + Metrics tabs side by side with the Render evidence.
2. Record consistency/divergence notes (append to the relevant `docs/render-artifacts/` file); file any real drift as an inbox note.

## Files

- `docs/render-artifacts/` (evidence updates)

## Acceptance criteria

- [ ] Parity check recorded with evidence pointers; any drift filed as follow-up, none silently ignored.
