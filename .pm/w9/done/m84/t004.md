---
id: w9/m84/t004
title: Extend disable-with-reason to the other standing role boundaries
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m84/t003]
---

## Objective

Give the same disable-with-reason treatment to the role boundaries the dashboard already ignores beyond the m68 four — `can_view_sensitive` (connection strings / env values reveal), `can_manage_billing` (billing surface), and `can_manage` (members management). m68 did not create this class; it just added members to it.

## Context

- `.pm/w1/047.md` pairing list: these are pre-existing boundaries the dashboard renders unconditionally today.
- Reuse the t002 capabilities context and the t003 disable-with-reason component/pattern.

## Steps

1. `can_view_sensitive`: gate the reveal affordances on connection strings / env values (disable reveal with a role reason when false).
2. `can_manage_billing`: gate the billing mutation controls on the billing page.
3. `can_manage`: gate member add/remove/role-change controls on the Team page.
4. Keep copy specific per capability.

## Files

- connection-string / env-value reveal components (`dashboard/src/features/**`)
- billing surface components
- Team / members page components

## Acceptance criteria

- [ ] Each of the three boundaries renders disabled-with-reason for a member lacking the capability; enabled for one holding it
- [ ] Consistent copy/pattern with t003
- [ ] `yarn typecheck && yarn lint && yarn test` green

## Out of scope

- Server-side changes (the server is already authoritative — t001 only reads capabilities)
