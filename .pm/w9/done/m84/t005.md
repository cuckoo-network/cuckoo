---
id: w9/m84/t005
title: Render parity (closing)
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m84/t004]
---

## Objective

Check the viewer-capabilities change is consistent across every surface it exposes and matches Render's behavior, flagging any drift as follow-up rather than silently diverging.

## Context

- bex-api is meant to be Render-compatible (`docs/ADR006-bex-api.md`); the projection is new GraphQL surface consumed by `dashboard/`.
- Render's dashboard gates controls by workspace role; capture/compare its role-gating shape where evidence is available.

## Steps

1. Confirm the `viewer`/`capabilities` projection is coherent on GraphQL (primary consumer) and decide whether REST/MCP need a mirror (note the decision either way — capability booleans may be GraphQL-only if REST/MCP callers are machine clients that already get 403s).
2. Compare the disable-with-reason UX against Render's role-gated controls; record parity or documented divergence in `docs/ADR018-render-parity.md` if a row applies.
3. File any drift as a follow-up note.

## Files

- `docs/ADR018-render-parity.md` (if a row applies)
- `docs/ADR006-bex-api.md` (if the projection is documented there)

## Acceptance criteria

- [ ] Cross-surface consistency confirmed (or the GraphQL-only decision recorded with rationale)
- [ ] Parity/divergence vs Render recorded; drift filed as follow-up

## Out of scope

- New feature work beyond parity reconciliation
