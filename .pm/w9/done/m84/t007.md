---
id: w9/m84/t007
title: Test coverage (closing)
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m84/t005]
---

## Objective

Add meaningful tests for the gating behavior this milestone shipped — real behavior and failure modes, not tautologies.

## Context

- The core assertion: a control is disabled-with-reason for a role lacking the capability and enabled for one holding it, per control.

## Steps

1. Backend: test the capabilities projection resolves correct booleans per role from the authz source.
2. Dashboard: test that each affected control renders disabled-with-reason under a contributor capability set and enabled under an admin set.
3. Assert the disabled control has no reachable save path for a contributor.

## Files

- backend projection test (`lego/backend/internal/...`)
- dashboard component/gating tests (`dashboard/src/features/**`)

## Acceptance criteria

- [ ] Per-role gating tests for the affected controls
- [ ] Projection correctness test
- [ ] All suites green

## Out of scope

- Live browser walk (defer to an inbox note if a live substrate is needed)
