---
id: w9/m5/t001
title: "REST: honor cursor/limit on the secret-files list"
worker: worker9
status: done
estimate: 30m
depends_on: []
---

## Objective

`GET /v1/services/{id}/secret-files` stops ignoring `cursor`/`limit`: apply the w9/m10 stable-name cursor (secret-file names are the stable key, same as env-var keys) so pagination survives interleaved adds/removes; the emitted `{secretFile, cursor}` per-item shape stays.

## Context

- `.pm/w9/done/005.md` (the note); w9/m10's env-vars cursor implementation — reuse its helper, don't fork it.
- Secret-files feature: `lego/backend/internal/secrets/` (the w1/m16 surface).

## Files

- `lego/backend/internal/secrets/` (+ the shared cursor helper's home)

## Acceptance criteria

- [x] `limit` truncates with a resumable cursor; unknown/malformed cursor is a named 400; conformance suite clean on the route.

## Out of scope

- GraphQL/MCP/dashboard (t002); env-group content pagination (the note's explicit exclusion).
