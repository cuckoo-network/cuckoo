---
id: w9/m10/t004
title: Dashboard: Generate-value affordance + paged list
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m10/t002, w9/m10/t003]
---

## Objective

Surface both features in the dashboard: a "Generate" affordance on the Environment tab's add-var row and the create wizard's env-vars step (Render's env editor has one), and make the env list consume the paged query without UX regression.

## Context

- `dashboard/src/features/` Environment tab (w4/m6.5) + create wizard env step (w5/m20); read `dashboard/CLAUDE.md` before editing.

## Files

- `dashboard/src/features/` (environment + service-create), generated GraphQL definitions

## Acceptance criteria

- [ ] Clicking Generate mints and displays the value inline before save; large env sets render fully via paging; `yarn test` green.

## Out of scope

- Env-group editor parity (only if trivially shared).
