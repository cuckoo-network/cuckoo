---
id: w9/m10/t005
title: Render parity
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m10/t004]
---

## Objective

Check consistency across REST/GraphQL/MCP + UI (same fields, semantics, error shapes) and compare against Render's actual `generateValue` + env-var pagination behavior (OpenAPI + docs). Update the ADR006 divergence note + ADR018 env-vars row; flag remaining drift as follow-ups, never silently.

## Acceptance criteria

- [ ] ADR006/ADR018 updated; any drift filed.
