---
id: w9/m10/t002
title: Thread `generateValue` through REST · GraphQL · MCP
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m10/t001]
---

## Objective

Expose t001's core verb identically on all three adapters: REST env-var set/replace bodies, GraphQL `setEnvVars`/`setEnvVar` inputs, MCP env-var tools — same field name, same semantics, same error shapes (thin adapters, no per-surface logic).

## Files

- `lego/backend/internal/secrets/{rest,graphql,mcp}.go`

## Acceptance criteria

- [ ] The same `generateValue` request succeeds identically on all three surfaces (adapter-parity test, the `TestResourceKindSurfacesAcrossAdapters` pattern).

## Out of scope

- Dashboard (t004).
