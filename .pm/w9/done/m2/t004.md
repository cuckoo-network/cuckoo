---
id: w9/m2/t004
title: "Map every ✖/◐ row to an owner; cross-link the checklist from ADR018"
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m2/t003]
---

## Objective

No orphan gaps: every ✖/◐ row in the checklist either points at an existing open milestone/inbox note that already owns the underlying bex-api gap, or gets a new inbox note filed in the owning workstream (sizing rule applies — sub-hour fixes are notes, larger ones are flagged for a future `/pm promote`). Then cross-link `docs/ADR018-render-parity.md` to the checklist so the parity ledger acknowledges the CLI as a tracked surface.

## Context

- Likely overlaps to check before filing anything new: `w8/m13` (datastore list cursor-envelope — the CLI's list pagination may trip on the same flat arrays the conformance allowlist documents), `w2/m36` (cron runs), open deploy-surface milestones (`w2/m38`).
- Fixes are always against bex-api (or documented harness workarounds) — never CLI patches, per the DO_NOT_DO entry.

## Steps

1. For each ✖/◐ row, grep the board for an existing owner; record `wN/mN`-or-note in the row.
2. File new inbox notes for unowned gaps (owning workstream, terse, citing the checklist row).
3. Add a CLI-surface pointer/paragraph to `docs/ADR018-render-parity.md` referencing the checklist.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/cli-compatibility-checklist.md` (owner column filled)
- `docs/ADR018-render-parity.md` (cross-link)
- `.pm/wN/NNN.md` (new notes as needed)

## Acceptance criteria

- [ ] Every ✖/◐ checklist row names an owner that exists on the board; ADR018 references the checklist.

## Out of scope

- Implementing any of the fixes.
