---
id: w9/m2/t003
title: "Write docs/cli-compatibility-checklist.md (one row per command, ✅/◐/✖/— + evidence) and index it"
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m2/t002]
---

## Objective

Turn t002's evidence into `docs/cli-compatibility-checklist.md`: one row per documented CLI command/family, marked ✅ (works) / ◐ (works with named drift) / ✖ (fails) / — (deliberate non-goal, citing the DO_NOT_DO entry), each with its evidence, mirroring `docs/ADR018-render-parity.md`'s cell conventions so the two ledgers read the same way.

## Context

- Header must record the harness: CLI version pinned, how the host override + auth were done (from t001), bex-api version/commit, and the run date — so a re-run is comparable.
- Expected-✖-by-design rows (ssh, jobs, anything hosted-exec) are marked `—` with the `.pm/DO_NOT_DO.md` citation, exactly like ADR018 handles non-goals.
- Repo rule: new docs are indexed in the root `CLAUDE.md` docs index; markdown is prettier-checked.

## Steps

1. Draft the checklist table from the evidence log; keep raw evidence linked or inlined per row.
2. Add the harness/reproduction header.
3. Add the file to `CLAUDE.md`'s docs index with a one-line description.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/cli-compatibility-checklist.md` (new)
- `CLAUDE.md` (docs index line)

## Acceptance criteria

- [ ] `docs/cli-compatibility-checklist.md` exists, covers every documented command with a ✅/◐/✖/— cell + evidence, records the reproduction harness in its header, and is indexed in `CLAUDE.md`.

## Out of scope

- Owner mapping + ADR018 cross-link (t004).
