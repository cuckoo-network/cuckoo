---
id: w9/m68/t005
title: Simplify
worker: worker9
status: done
estimate: 20m
depends_on: [w9/m68/t004]
---

## Objective

Run `/simplify` over the loader/prefetch code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Scope: the loaders added to `index.tsx`, the detail routes, and the chosen list route.
- Look for a reusable loader helper if the same cause-aware `client.query` shape is repeated.

## Steps

1. Run `/simplify` over the diff.
2. Extract a shared prefetch helper if it removes duplication; apply behavior-preserving cleanups only.

## Files

- The files changed by t002–t003.

## Acceptance criteria

- [ ] `/simplify` applied; behavior unchanged; tests green.

## Out of scope

- Bug hunting (`/code-review`).
