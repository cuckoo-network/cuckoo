---
id: w9/m91/t008
title: Closeout
worker: worker9
status: done
estimate: 10m
depends_on: [w9/m91/t007]
---

## Objective

Close the milestone once its definition of done actually holds: an invalid cron schedule cannot be persisted from any surface, and the cron create/edit forms match Render's chrome + schedule UX.

## Context

- Do not run until the DoD is real: verify on a running environment (dev-9 or prod-like) that saving `99 99 * * *` on the Settings page is rejected and the App does **not** go `Failed`.

## Steps

1. Confirm every other task is `status: done` and the DoD is met.
2. Set remaining tasks `status: done`, move each `tNNN.md` to `w9/m91/done/`, mark rows `— **DONE**` and `**Status:** done` in the milestone README.
3. Move `w9/m91/` to `w9/done/m91/` and check `- [x]` in the w9 README.

## Files

- `.pm/w9/m91/` → `.pm/w9/done/m91/`
- `.pm/w9/README.md`

## Acceptance criteria

- [ ] DoD verified on a live environment (bad schedule rejected, no `Failed`).
- [ ] Milestone moved to `w9/done/m91/`, box checked.

## Out of scope

- New scope (file follow-ups as notes).
