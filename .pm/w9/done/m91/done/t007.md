---
id: w9/m91/t007
title: Test coverage
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m91/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: server-side rejection of invalid cron schedules on the update path, and the dashboard edit-form blocking save on an invalid schedule. Tests must assert real behavior and failure modes.

## Context

- Backend: table-driven cases for the update path — invalid schedules (`99 99 * * *`, `not a cron`, wrong field count) rejected with the Render error shape and no CR write; valid schedules accepted. Assert GraphQL/MCP go through the same validation.
- Dashboard: a test that an invalid schedule in the Settings edit form disables Save and shows the inline error, and that a valid one re-enables it.

## Steps

1. Add backend update-path validation tests in `lego/backend/`.
2. Add a dashboard edit-form validation test in `dashboard/`.

## Files

- `lego/backend/internal/.../*_test.go`
- `dashboard/src/.../*.test.tsx`

## Acceptance criteria

- [ ] Backend test fails if update-path validation is removed.
- [ ] Dashboard test fails if the edit-form Save is enabled on an invalid schedule.
- [ ] `cd lego/backend && go test ./...` and `dashboard` tests green.

## Out of scope

- Coverage-gaming/tautological tests.
