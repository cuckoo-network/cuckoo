---
id: w9/m91/t003
title: Cron create-form chrome — "New Cron Job" title/heading/subtitle
worker: worker9
status: done
estimate: 30m
depends_on: []
---

## Objective

When the cron_job type is selected, the create page should read as a cron form, not a web service: fix the page `<title>`, `<h1>`, and subtitle so they say "New Cron Job" / cron-appropriate copy instead of the generic "New Service" + "Deploy a web service from a Git repo or Docker image."

## Context

- Observed 2026-08-20: `/services/new?type=cron_job` shows title "New Service", heading "New Service", subtitle "Deploy a web service from a Git repo or Docker image." — all web-service copy for a cron job.
- Render uses a dedicated "New Cron Job" title/heading (page `/cron/new`).
- bex reaches the cron form via New → New Service → Cron Job (a submenu), while Render exposes Cron Job as a top-level New-menu item. Consider whether the type-aware heading is enough or whether the submenu placement is worth a small discoverability tweak — the heading/subtitle fix is the required part; the menu placement is a secondary, optional consideration to note, not necessarily change.

## Steps

1. Make the create page's title/heading/subtitle type-aware (cron_job → cron copy). Other service types keep their current copy.
2. Verify the copy for the other four service types is unchanged.

## Files

- `dashboard/src/...` new-service page (title + heading + subtitle, keyed on the selected `type`)

## Acceptance criteria

- [ ] `/services/new?type=cron_job` shows a cron-specific document title, `<h1>`, and subtitle.
- [ ] Web Service / Private Service / Background Worker / Static Site copy is unchanged.

## Out of scope

- Schedule field UX (t004).
- Restructuring the New menu (record only).
