---
id: w9/m91/t005
title: Render parity check (REST/GraphQL/MCP + dashboard)
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m91/t001, w9/m91/t002, w9/m91/t003, w9/m91/t004]
---

## Objective

Verify the cron schedule-validation change and the form-consistency work are consistent across every surface they touch, and compare against render.com behavior, flagging any residual drift as follow-up rather than silently diverging.

## Context

- Surfaces: REST, GraphQL, MCP cron update in `lego/backend/` (same fields, semantics, error shapes — bex-api is Render-compatible, `docs/ADR006-bex-api.md`) and the dashboard cron create/edit forms (`dashboard/CLAUDE.md`).
- Render reference (captured 2026-08-20): create + edit both reject an invalid schedule with `Invalid cron spec`; "New Cron Job" page chrome; live "every N minutes" preview; pre-filled `*/5 * * * *`; cron instance types start at Starter with per-minute pricing (no Free) — the pricing-model divergence is tracked separately in `w9/054`, not here.

## Steps

1. Confirm invalid-schedule rejection is identical in shape across REST/GraphQL/MCP.
2. Confirm the dashboard error/UX matches the create form and, where reasonable, Render's wording.
3. Update `docs/ADR018-render-parity.md` cron row(s) with the new state; file any leftover drift as a note.

## Files

- `docs/ADR018-render-parity.md`
- `docs/ADR038-cron-jobs.md` (if behavior notes need updating)

## Acceptance criteria

- [ ] REST/GraphQL/MCP invalid-schedule errors verified consistent.
- [ ] Parity ledger updated; residual drift filed as follow-up notes.

## Out of scope

- Free-tier / pricing-model decision (`w9/054`).
