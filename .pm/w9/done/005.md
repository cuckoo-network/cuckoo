Paginate service secret-file lists. Render's public OpenAPI gives `GET /services/{serviceId}/secret-files` the same `cursor`/`limit` contract as env vars; bex already emits `{secretFile, cursor}` items but ignores those query params, and its GraphQL/MCP/dashboard list paths remain unpaged. Follow the w9/m10 stable-name cursor pattern without broadening into env-group content pagination.

> Promoted to **w9/m5** (secret-file list pagination) 2026-07-15; note moved to `done/`.
