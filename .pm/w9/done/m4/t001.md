---
id: w9/m4/t001
title: "Postgres lifecycle legs in cli-compat verify"
worker: worker9
status: done
estimate: 45m
depends_on: []
---

## Objective

Add self-creating, trap-cleaned postgres legs to `scripts/cli-compat.sh verify`: create with `--ip-allow-list` (RC12), list + get-by-name (RC3/RC7), the update flags — `--plan`, `--disk-size-gb`, `--high-availability`, `--ip-allow-list`/`--clear-ip-allow-list` (RC11/RC12) — rename (RC5, `scripts/postgres-rename-cli-smoke.sh` exists to fold in or call), suspend/resume, delete. Every leg asserts the whole relevant field set via the existing `checkFields` helper, plus at least one negative (unknown id → non-zero with the shared 404 semantics).

## Context

- `scripts/cli-compat.sh` + `.pm/w9/done/m2/verify.sh` — the harness and the keyvalues-leg pattern to mirror (unique names, trap cleanup, no residue on failure).
- The checklist's RC11/RC12 rows list the exact flag evidence to re-assert.

## Files

- `scripts/cli-compat.sh` / `.pm/w9/done/m2/verify.sh` (wherever verify's legs live)

## Acceptance criteria

- [ ] Green run covers the full postgres lifecycle with whole-shape assertions; failed runs leave no Database behind; unknown-id leg fails non-zero.

## Out of scope

- New bex-api behavior (this is verification only; a real gap found becomes a filed note, not an inline fix).
