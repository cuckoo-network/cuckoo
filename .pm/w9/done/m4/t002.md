---
id: w9/m4/t002
title: "Services CRUD + deploys + logs legs in cli-compat verify"
worker: worker9
status: done
estimate: 45m
depends_on: []
---

## Objective

Same pattern for the remaining big families: `services create` (assert the RC9 `serviceAndDeploy` envelope), `update`, `delete`; `deploys list` (RC2's `image` object), `deploys create --confirm` (RC10's `clearCache` string) and `cancel`; `logs --resources <name>` (RC7 name resolution + RC8's required `nextStartTime`/`nextEndTime` on an empty result — explicitly run a no-match query).

## Context

- The existing self-created one-replica service leg (from the `services instances` work) — extend rather than duplicate its lifecycle.
- RC evidence strings in the checklist name the exact regression shapes each leg must catch.

## Files

- `scripts/cli-compat.sh` / the verify script

## Acceptance criteria

- [ ] Green run covers services CRUD, deploys list/create/cancel, and both logs cases (matching + empty window); whole-shape assertions; trap cleanup.

## Out of scope

- SSH/psql legs (interactive; owned by w2/m39's harness and the checklist's psql config note).
