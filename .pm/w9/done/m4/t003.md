---
id: w9/m4/t003
title: "Environments + real device-token logout leg; update CLI coverage truthfully"
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m4/t001, w9/m4/t002, w4/m27/t005]
---

## Objective

Add the environments legs (seeded Project → list shows the RC15 cursor envelope decoding to real values; unknown id → 404 semantics) and the real device-token `logout` leg after `w4/m27/t005` ships. The logout proof must start from an actual browser/device login with `RENDER_API_KEY` unset, verify the access + refresh chain is revoked, and verify the shared public client remains. Then update the checklist so automated coverage is stated exactly.

## Files

- `scripts/cli-compat.sh` / the verify script
- the `w4/m27/t005` official-CLI auth harness
- `docs/cli-compatibility-checklist.md` (Reproducing section)

## Acceptance criteria

- [ ] One full green `verify` run recorded (date + bex-api commit) in the checklist header; browser login/logout is never inferred from an API-key-configured invocation.
- [ ] The logout leg proves old access + refresh credentials fail while the fixed shared client and a second user's session remain valid.

## Out of scope

- New checklist census rows (the command set didn't change).
