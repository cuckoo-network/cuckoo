---
id: w9/m63/t003
title: "Logs/Scaling tabs: skeletons instead of null/spinner + reserved slot"
worker: worker9
status: todo
estimate: 45m
depends_on: []
---

## Objective

The Logs tab renders a blank body while the service type resolves, then a bare spinner while the first page loads; the Scaling tab's manual-scaling card pops in after autoscaling state resolves, shifting the layout. Hold each space with content-shaped placeholders.

## Context

- `dashboard/src/features/services/components/non-static-route.tsx:34-36` — `holdWhileLoading` returns `null` (used by the logs route, `routes/services.$serviceId.logs.tsx:57`): blank tab body until `useServer` resolves.
- `dashboard/src/features/logs/components/log-viewer.tsx:161-165` — loading-first state is a centered `Loader2` + text.
- `dashboard/src/routes/services.$serviceId.scaling.tsx:45` — `scalable && !autoscaling.loading && !autoscaling.enabled` withholds the manual card until `useAutoscaling` resolves; `AutoscalingSection` itself already skeletons correctly (`autoscaling-section.tsx:264-272`).
- w9/m69 (list-route skeletons) will have established the skeleton idiom — reuse its components/classes where they fit; monospace-bar rows for log lines.

## Steps

1. Give `NonStaticRoute`'s `holdWhileLoading` a skeleton fallback prop (log-panel-shaped for the logs route) instead of `null`.
2. Replace the `log-viewer` loading spinner with skeleton log lines (a handful of monospace-width bars).
3. Reserve the manual-scaling card slot with a card skeleton while `autoscaling.loading`.
4. Throttled-network walk: Logs and Scaling tabs never show a blank region or layout pop-in.

## Files

- `dashboard/src/features/services/components/non-static-route.tsx`
- `dashboard/src/routes/services.$serviceId.logs.tsx`
- `dashboard/src/features/logs/components/log-viewer.tsx`
- `dashboard/src/routes/services.$serviceId.scaling.tsx`

## Acceptance criteria

- [ ] Logs tab shows a skeleton (never blank/`null`) during both the type-resolve and first-page phases.
- [ ] Scaling tab has no layout shift when the manual card appears.
- [ ] Other `NonStaticRoute` consumers are unaffected (or deliberately given fallbacks — enumerate them).

## Out of scope

- The m69 list routes; optimistic mutation UI (recorded separately).
