---
id: w9/m61/t004
title: Test coverage
worker: worker9
status: todo
estimate: 40m
depends_on: [w9/m61/t002]
---

## Objective

Add meaningful tests for the compression behavior and its carve-outs.

## Context

- The dangerous regressions: SSE accidentally compressed (breaks live tail), `Vary` missing (cache poisoning through a shared cache), identity clients broken.

## Steps

1. Handler-level tests: gzip round-trip on a JSON route (decoded body equals uncompressed body, `Vary: Accept-Encoding` set); identity when no `Accept-Encoding`; no compression on a `text/event-stream` response and flush passthrough works (`httptest` + a flushing handler).
2. A test that the gzip writer pool doesn't corrupt concurrent responses (parallel requests, distinct bodies).

## Files

- `lego/backend/internal/api/gzip_test.go`

## Acceptance criteria

- [ ] All listed behaviors asserted non-tautologically; backend suite green.
