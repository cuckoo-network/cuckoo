---
id: w9/m61/t002
title: "End-to-end verification: SSE untouched, payload reduction measured"
worker: worker9
status: todo
estimate: 45m
depends_on: [w9/m61/t001]
---

## Objective

Prove the compression is real and harmless end to end: measure the reduction on representative payloads, verify the live-tail SSE path still streams per-event, and confirm the official Render CLI is unaffected.

## Context

- dev-9 stack: `bash .pm/w9/dev-9/up.sh`, ports in `dev-9/ports.env`.
- Representative payloads: a metrics-series GraphQL query (highly repetitive JSON) and a logs query; both compress 70–85% typically.
- `scripts/cli-compat.sh verify` is the CLI-surface regression harness (52 assertions).

## Steps

1. Against dev-9: capture raw vs `--compressed` byte counts for the two representative reads; record ratios in the milestone README.
2. Live-tail a deploy's logs via the SSE endpoint; confirm events arrive incrementally (no buffering until close).
3. Run `scripts/cli-compat.sh verify` against dev-9 — all green.
4. Confirm the dashboard against dev-9 works (one browse session: services list, a detail page, metrics tab).

## Files

- `.pm/w9/m61/README.md` (recorded numbers)

## Acceptance criteria

- [ ] Measured ≥ 70% reduction on the metrics and logs reads, recorded in the README.
- [ ] SSE live tail verified incremental.
- [ ] `cli-compat.sh verify` green; dashboard browse session normal.

## Out of scope

- Production rollout measurement (rides the normal deploy).
