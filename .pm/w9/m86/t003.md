---
id: w9/m86/t003
title: Render parity (closing)
worker: worker9
status: todo
estimate: 15m
depends_on: [w9/m86/t002]
---

## Objective

Confirm the error-state change is UI-consistent and compare against Render's chart error UX; flag drift rather than silently diverging.

## Context

- UI-only change (a dashboard error card); no REST/GraphQL/MCP surface change, but it is a user-facing surface, so parity applies lightly.

## Steps

1. Compare the datastore/service metrics error UX against Render's equivalent chart error behavior where evidence exists.
2. Confirm consistency with the dashboard's own logs/metrics error-card pattern.
3. Record parity/divergence if an ADR018 row applies.

## Files

- `docs/ADR018-render-parity.md` (only if a row applies)

## Acceptance criteria

- [ ] Error UX consistent with the dashboard's existing error cards
- [ ] Parity/divergence vs Render noted; any drift filed

## Out of scope

- New feature work
