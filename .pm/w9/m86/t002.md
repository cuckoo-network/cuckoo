---
id: w9/m86/t002
title: Surface result.error as a generic error card
worker: worker9
status: todo
estimate: 40m
depends_on: [w9/m86/t001]
---

## Objective

Render a distinct error card when a metrics query errors, separate from the genuinely-empty "No data in range" state, across the panels the t001 audit identified.

## Context

- `useDatastoreMetrics` already returns `result.error` — the panel ignores it.
- The logs/metrics pages already have a generic error card pattern to match.

## Steps

1. At the shared seam (per t001), branch on `result.error` first → error card; else empty window → "No data in range"; else data → chart.
2. Keep the recognized `"metrics source not configured"` message as its own specific state (unchanged copy).
3. Apply to every panel the audit flagged (datastore + any service-metrics siblings).

## Files

- `dashboard/src/features/metrics/components/datastore-metrics-panel.tsx`
- `dashboard/src/features/metrics/components/metric-section.tsx`
- `dashboard/src/features/metrics/lib/graphql-series.ts`
- any sibling panels from t001

## Acceptance criteria

- [ ] An errored query renders the error card, not the empty state
- [ ] A genuinely empty window still renders "No data in range"
- [ ] `"metrics source not configured"` keeps its specific state
- [ ] `yarn typecheck && yarn lint && yarn test` green

## Out of scope

- Backend metrics changes
