---
id: w9/m86/t005
title: Test coverage (closing)
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m86/t003]
---

## Objective

Add meaningful tests asserting the error-vs-empty distinction — the exact conflation that hid the `w5/m71` bug.

## Context

- Failure mode under test: a query that returns `result.error` must not render as the empty window.

## Steps

1. Test: an errored metrics query renders the error card.
2. Test: a genuinely empty window renders "No data in range".
3. Test: the `"metrics source not configured"` case keeps its specific state.
4. Cover each panel the audit fixed.

## Files

- `dashboard/src/features/metrics/**` test files

## Acceptance criteria

- [ ] Error / empty / not-configured branches each asserted
- [ ] All suites green

## Out of scope

- Live browser walk
