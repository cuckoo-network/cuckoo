---
id: w9/m86/t006
title: Closeout
worker: worker9
status: todo
estimate: 10m
depends_on: [w9/m86/t005]
---

## Objective

Close the milestone once the definition of done is actually met.

## Steps

1. Confirm the DoD: errored queries show the error card, empty windows show "No data in range", tests assert both, siblings fixed if the audit found them.
2. Set every remaining task `status: done`, move each `tNNN.md` to `w9/m86/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone README, move the whole `w9/m86/` to `w9/done/m86/`, and check `- [x]` in the w9 README.
3. If the t001 audit found only the one panel and the work came in under the milestone bar, record that in the closeout note.

## Acceptance criteria

- [ ] DoD verified real
- [ ] Milestone moved to `w9/done/m86/`; w9 README box checked

## Out of scope

- Any remaining implementation
