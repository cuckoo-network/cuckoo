# 047 — Deferred live flame-graph confirmation for w9/m83 log virtualization

**Filed:** 2026-08-16, during w9/m83 closeout. **Type:** deferred verification (not a code gap; same pattern as `044`/`046`).

m83 virtualized the log list and the DoD's before/after "profile" is **captured structurally**: a jsdom-geometry test renders a **constant ~34 DOM rows** for a 1,000- **and** 5,000-line buffer (vs one row per line before) — **96.6% / 99.3% fewer row nodes**. React reconciliation is O(mounted nodes), so the rendered-node count _is_ the DOM-reconciliation cost, and it's now buffer-independent. This is the dominant, measurable result and it is regression-guarded in `log-line-list.test.tsx`.

The remaining leg is an **optional live confirmation** (app not raisable in-session):

- With the dashboard running against a busy tail (`yarn dev:local` streams synthetic logs), open React DevTools Profiler and record a commit while lines stream: confirm per-commit work no longer scales with buffer depth (scroll to the new 5,000-line cap and confirm the mounted row count stays ~a viewport's worth, and that a wrapped-line resize doesn't jank).
- Optionally sanity-check nowrap horizontal scroll + in-window text selection in a real browser (the two behaviors the spacer-flow rendering was chosen to preserve; unit-tested but not eyeballed live).

Low risk: the node-count bound is proven; this only puts a flame graph next to the structural number.
