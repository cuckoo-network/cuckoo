---
id: w8/m10/t003
title: Cursor pagination on env-var list endpoints
worker: worker8
status: todo
estimate: 45m
depends_on: []
---

## Objective

Add `cursor`/`limit` to the env-var list endpoints, using the established per-item `{object, cursor}` envelope (the deploys/audit pattern), on REST and GraphQL; MCP mirrors whatever Render's tool accepts.

## Context

- Env-var list is currently unpaginated (`docs/ADR006-bex-api.md` divergence note). Keys live in OpenBao — check what ordering guarantee the store gives before defining cursor semantics (stable order is a pagination prerequisite; sort keys server-side if needed).
- Backwards compatibility: no `cursor`/`limit` supplied ⇒ current full-list behavior unchanged.

## Files

- `lego/backend/internal/secrets/{service,rest,graphql,mcp}.go`

## Acceptance criteria

- [ ] Paging through a >limit env set visits every var exactly once; omitted params keep today's behavior byte-identical.

## Out of scope

- Paginating secret-files/env-group lists (follow-up if Render paginates those too — note in t005).
