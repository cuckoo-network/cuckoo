---
id: w9/m4/t005
title: "Test coverage — mutation-prove the legs fail loudly"
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m4/t003]
---

## Objective

The verify script IS this milestone's test surface — so its own failure modes are what need proving. For at least one leg per family, demonstrate the leg exits non-zero against a deliberately broken shape (e.g. a proxy/stub returning a pre-RC field set, or a temporarily reverted serializer in a throwaway build): the RC14 lesson is that a leg that can't fail is worthless. Record the mutation-check evidence in the milestone.

## Acceptance criteria

- [ ] Per-family mutation checks recorded (broken shape → non-zero exit); no leg passes vacuously.
