---
id: w9/m4/t003
title: "Environments + logout legs; update the checklist's Reproducing section"
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m4/t001, w9/m4/t002]
---

## Objective

Add the environments legs (seeded Project → list shows the RC15 cursor envelope decoding to real values; unknown id → 404 semantics) and the `logout` OAuth-revoke leg, then rewrite the checklist's "Reproducing the ✅ rows" paragraph so it states the coverage truthfully — no family listed as manually-verified-only anymore, and the "Not yet covered" sentence deleted only if it's actually true.

## Files

- `scripts/cli-compat.sh` / the verify script
- `docs/cli-compatibility-checklist.md` (Reproducing section)

## Acceptance criteria

- [ ] One full green `verify` run recorded (date + bex-api commit) in the checklist header; the Reproducing section matches reality.

## Out of scope

- New checklist census rows (the command set didn't change).
