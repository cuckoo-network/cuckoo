---
id: w9/m38/t002
title: Route metrics 400s + deploy-hook 405 through core.WriteErr; sweep remaining sites
worker: worker9
status: todo
estimate: 40m
depends_on: []
---

## Objective

Convert the remaining bare-`{"error"}` writers — the four `/v1/metrics/*` param 400s and the deploy-hook 405 — to `core.WriteErr`, then grep-sweep `lego/backend` for any other handler-level `http.Error`/ad-hoc error JSON and fix or annotate each survivor.

## Context

- `lego/backend/internal/metrics/rest.go:78,82,107,111` — `core.WriteJSON(w, 400, map[string]string{"error": err.Error()})` for malformed params/over-window queries, while the same handlers' downstream errors already use `core.WriteErr` — one endpoint, two dialects.
- `lego/backend/internal/deploys/deployhook.go:270` — 405 `{"error":"method not allowed"}`.
- Sweep patterns: `http.Error(`, `map[string]string{"error"`, `{"error":` in non-test `lego/backend` code.

## Steps

1. Swap the five known sites to `core.WriteErr`, preserving status codes and message text.
2. Run the sweep; convert other handler-level offenders, or leave a one-line comment where a divergent shape is load-bearing (e.g. a Render-specified different envelope).
3. `cd lego/backend && go build ./... && go test ./...`.

## Files

- `lego/backend/internal/metrics/rest.go`
- `lego/backend/internal/deploys/deployhook.go`
- any sweep hits

## Acceptance criteria

- [ ] Metrics param 400s and the deploy-hook 405 carry `message` + JSON content type.
- [ ] Sweep is clean or every survivor carries a why-comment.

## Out of scope

- t001's auth/GraphQL sites.
