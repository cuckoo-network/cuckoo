---
id: w9/m43/t003
title: RBAC + config plumbing for the reload mechanism
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m43/t002]
---

## Objective

Give the operator exactly the permissions and configuration t002's mechanism needs — nothing broader — and keep every environment mirror in sync.

## Context

- The operator today has Secret CRUD in `BEX_REGISTRY_NS` (w7/m36) but no pod verbs there; the verify-then-bounce design adds `pods: get,list,delete` scoped to the zot namespace.
- RBAC comes from kubebuilder markers (`make manifests` regenerates `lego/operator/config/rbac/`); prod/dev clusters may also carry hand-applied RoleBindings (see memory `bare-bex-cluster-restore.md`).
- If t002 added any knob (probe timeout, bounce cooldown, zot pod selector), it needs: CLAUDE.md env-var table row, `.env.example` + `.env.template` mirroring (name + comment only, repo rule), and dev-N `up.sh`/values plumbing where relevant.

## Steps

1. Add the narrowest RBAC markers for the reload mechanism; `make manifests` and review the generated diff.
2. Plumb any new env knobs end to end (manager flags/env → CLAUDE.md table → `.env.example`/`.env.template`).
3. Deploy to the mock cluster (`make deploy IMG=…` or the dev-N path) and prove the mechanism works under the generated RBAC (no `forbidden` in operator logs).

## Files

- `lego/operator/internal/controller/app_controller.go` (RBAC markers)
- `lego/operator/config/rbac/` (generated)
- `CLAUDE.md`, `.env.example`, `.env.template` (only if a knob was added)

## Acceptance criteria

- [ ] Operator can execute the reload under its own ServiceAccount on the mock cluster (no forbidden errors)
- [ ] RBAC grants nothing beyond what t002 uses
- [ ] Env mirrors in sync if any var was added

## Out of scope

- Prod rollout (t004)
