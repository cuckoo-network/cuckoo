---
id: w9/m5/t002
title: "GraphQL/MCP pagination args + dashboard list"
worker: worker9
status: todo
estimate: 40m
depends_on: [w9/m5/t001]
---

## Objective

Mirror w9/m10's env-vars pagination shapes onto the secret-files list in GraphQL and MCP (same arg names/semantics), and make the dashboard's secret-files section consume the paged list (load-more or the pattern the env-vars UI uses — match it).

## Files

- `lego/backend/internal/secrets/` (graphql.go/mcp.go), `dashboard/src/features/` (the secret-files section)

## Acceptance criteria

- [ ] Three surfaces page with identical semantics; dashboard renders beyond one page correctly.
