---
id: w9/m2/t005
title: "Simplify — /simplify over the code this milestone changed"
worker: worker9
status: todo
estimate: 20m
depends_on: [w9/m2/t004]
---

## Objective

Run `/simplify` over whatever this milestone actually changed — most likely just the harness script (`scripts/cli-compat.sh` or equivalent) and doc plumbing. If the milestone shipped no code at all, record that explicitly and close.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff (or an explicit "docs-only, no code shipped" note recorded here).
