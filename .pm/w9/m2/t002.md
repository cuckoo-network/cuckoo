---
id: w9/m2/t002
title: "Walk the full documented CLI command surface against bex-api; capture evidence per command"
worker: worker9
status: todo
estimate: 45m
depends_on: [w9/m2/t001]
---

## Objective

Run every command documented at https://render.com/docs/cli through the t001 harness against a live bex-api (mock cluster with at least one deployed service, one Postgres, one Key Value), and capture the raw outcome of each — success output, degraded output (works but fields missing/wrong), or the exact error.

## Context

- Command families to cover (verify the current list against the live docs page — it is the census source of truth): `login`/`whoami`, workspace set/list, `services` (list/show/non-interactive `-o json`), `deploys` (list/create), `logs` (query + `--tail`), `psql`, `ssh`, `jobs`, `restart`, interactive mode vs `-o json --confirm` non-interactive flags.
- Some families are expected ✖ by standing anti-goals (e.g. `ssh`, one-off `jobs` — hosted exec is off-roadmap per `.pm/DO_NOT_DO.md`); still run them and record the actual failure shape — an honest error beats an undocumented hang.
- Seed the cluster so list/show commands have real data: `scripts/deploy-sample.sh`, a `Database`, a `KeyValue`.

## Steps

1. Enumerate the documented command set from the live docs page (pin the date in the notes).
2. Run each against bex-api via the harness; save each command + full output to an evidence file (`.pm/w9/m2/evidence-cli.md` or a scratch log the checklist cites).
3. Note per command: works / works-with-drift (name the drift) / fails (name the failing bex endpoint or missing behavior).

## Files

- evidence notes for t003 (path recorded in the checklist)

## Acceptance criteria

- [ ] Every command family documented on render.com/docs/cli has a captured, dated run against bex-api with its raw outcome — none skipped, including expected-✖ ones.

## Out of scope

- Judging/owning the gaps (t004); writing the checklist itself (t003).
