---
id: w9/m6/t004
title: REST/GraphQL/MCP: route by `red-` id + rename verb
worker: worker9
status: todo
estimate: 45m
depends_on: [w9/m6/t003]
---

## Objective

All three API surfaces address Key Value stores by `red-` id (name accepted only where Render accepts it) and expose the Render-shaped rename (PATCH name / mutation / tool), matching t001's pinned contract.

## Context

- `lego/backend/internal/keyvalue/rest.go`, `graphql.go`, `mcp.go`; Postgres equivalents from w9/m3 are the template.
- Error shapes: 404 nonexistent vs 403 cross-tenant per w6/m22 conventions.

## Steps

1. id-based routing on GET/PATCH/DELETE/suspend/resume/plan verbs.
2. Rename verb on REST PATCH + GraphQL mutation + MCP tool, one core verb.
3. List/get responses emit the `red-` id everywhere Render does.

## Files

- lego/backend/internal/keyvalue/

## Acceptance criteria

- [ ] Same rename + id routing behavior on all three surfaces with shared core logic; audit log records the rename.

## Out of scope

- Dashboard (t005).
