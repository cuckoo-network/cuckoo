---
id: w9/m6/t003
title: Store + projector: stable id column, mutable name
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m6/t002]
---

## Objective

Decouple identity from name in the control-plane store and the rows→CR projector so the CR/k8s name stays stable while the display name becomes mutable — mirroring w9/m3's Postgres split.

## Context

- `lego/backend/internal/store` keyvalue rows; projector in the control-plane package.
- w9/m3 decided: k8s object name stays the immutable slug; display name lives in the row/CR spec.

## Steps

1. Thread the stable id + mutable display name through store reads/writes.
2. Projector projects by id, never by display name.
3. Rename never touches the k8s object name or connection hostnames.

## Files

- lego/backend/internal/store/
- lego/backend/internal/keyvalue/

## Acceptance criteria

- [ ] Renaming a store changes only the display name; CR name, URLs, and id are unchanged.

## Out of scope

- Surface verbs (t004).
