---
id: w9/m42/t001
title: Add region/dashboardUrl/updatedAt to Postgres + KeyValue GraphQL types via resourcemeta
worker: worker9
status: todo
estimate: 45m
depends_on: []
---

## Objective

The Postgres and KeyValue GraphQL object types gain `region`, `dashboardUrl`, and `updatedAt` fields with exactly the REST semantics: values from the shared `resourcemeta` helpers (`PlatformRegion()`, `DashboardURL(kind, id)`, authoritative mutation/reconciliation timestamp), omitted/null consistently when unconfigured.

## Context

- REST reference: `lego/backend/internal/postgres/rest.go:44-45,68-69` and the keyvalue sibling — both populate via `resourcemeta` (w6/m28).
- GraphQL targets: `lego/backend/internal/postgres/graphql.go`, `lego/backend/internal/keyvalue/graphql.go` — currently zero hits for these fields.
- The neutral Views don't carry these fields ("only `AppView` bakes them in" — m41's finding); thread metadata the way REST does rather than widening the Views casually — pick the seam deliberately.

## Steps

1. Decide the seam (resolver-level `resourcemeta` calls vs View fields) — match how REST computes them, note the choice.
2. Add the three fields to both GraphQL types (list + single-get paths).
3. Update the dumped GraphQL schema fixture if one exists (`TestDumpGraphQLSchema` pattern).

## Files

- `lego/backend/internal/postgres/graphql.go`
- `lego/backend/internal/keyvalue/graphql.go`

## Acceptance criteria

- [ ] GraphQL returns the same `region`/`dashboardUrl`/`updatedAt` as REST for the same store, including the unconfigured-null case.

## Out of scope

- MCP (t002); the flat-`ownerId` dialect (a confirmed, documented divergence — leave it).
