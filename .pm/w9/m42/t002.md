---
id: w9/m42/t002
title: Mirror region/dashboardUrl/updatedAt on the MCP get/list tool outputs
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m42/t001]
---

## Objective

The MCP get/list tools for Postgres and Key Value return the same three metadata fields as REST and (post-t001) GraphQL, so an agent sees the same object a human does.

## Context

- `lego/backend/internal/postgres/mcp.go`, `lego/backend/internal/keyvalue/mcp.go` — result structs currently omit the fields.
- Reuse t001's seam; don't compute metadata twice.

## Steps

1. Add the fields to both MCP result shapes (get + list).
2. Refresh tool descriptions if they enumerate returned fields.

## Files

- `lego/backend/internal/postgres/mcp.go`
- `lego/backend/internal/keyvalue/mcp.go`

## Acceptance criteria

- [ ] MCP tool output matches REST field-for-field on `region`/`dashboardUrl`/`updatedAt` (including omission when unconfigured).

## Out of scope

- New MCP tools.
