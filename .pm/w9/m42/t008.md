---
id: w9/m42/t008
title: Closeout — move to done/ when the DoD holds
worker: worker9
status: todo
estimate: 15m
depends_on: [w9/m42/t007]
---

## Objective

When all tasks are complete **and the DoD actually holds** (identical metadata across the three surfaces, dashboard region visible when configured), close per `.pm/CLAUDE.md`: statuses `done`, tasks to `done/`, README rows `— **DONE**` + `**Status:** done`, move `w9/m42/` to `w9/done/m42/`, check the box in `w9/README.md`.

## Context

- DoD verification can run against dev-9 (`.pm/w9/dev-9/`).

## Steps

1. Verify the DoD; record evidence.
2. Execute the closeout moves.

## Files

- `.pm/w9/m42/**`, `.pm/w9/README.md`

## Acceptance criteria

- [ ] `w9/done/m42/` exists, no tombstone at `w9/m42/`, workstream box checked.

## Out of scope

- New feature work.
