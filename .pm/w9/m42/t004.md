---
id: w9/m42/t004
title: Dashboard: render region on database/keyvalue detail pages
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m42/t003]
---

## Objective

The database and keyvalue detail pages show the store's region when the platform configures one (`BEX_REGION`), matching Render's datastore detail display. `dashboardUrl` is deliberately not rendered (self-referential inside the dashboard); `updatedAt` display is optional — add it only where a natural slot exists.

## Context

- `dashboard/src/features/databases/`, `dashboard/src/features/keyvalue/` detail components; fragments in each feature's `api/*.graphql` need the new field + codegen.
- `create-database-dialog.tsx:65` carries an old "region deferred" note — retire it if now false.
- Locale keys en + zh.

## Steps

1. Add `region` to the detail fragments, regen types, render with an unconfigured-empty state (omit the row, don't show a blank).
2. Update/remove the stale deferral note.
3. Component test for configured vs omitted.

## Files

- `dashboard/src/features/databases/**`, `dashboard/src/features/keyvalue/**`

## Acceptance criteria

- [ ] With `BEX_REGION` set, both detail pages show the region; without it, no empty region row renders.

## Out of scope

- Region *selection* at create (bex has one platform region; selection is not a thing).
