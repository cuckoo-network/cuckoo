---
id: w9/m42/t003
title: Extend m41's cross-sibling parity tests to lock the new fields; update ADR018
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m42/t002]
---

## Objective

The cross-sibling parity tests w9/m41 added (locking `owner`/`region`/`dashboardUrl`/`updatedAt` across read paths) extend to assert the GraphQL/MCP surfaces now carry the fields — so a future surface can't drop one silently — and `docs/ADR018-render-parity.md`'s "recorded as a follow-up" note flips to closed with evidence.

## Context

- m41's tests live alongside the audited read paths (see `w9/done/m41/` for names); they currently ratify the omission — invert the expectation.

## Steps

1. Extend/invert the parity assertions for GraphQL + MCP on both datastore kinds.
2. Update the ADR018 divergence note (and any m41-era comment that says the fields are REST-only).

## Files

- Backend parity test files (per m41), `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] A test fails if GraphQL or MCP drops any of the three fields; ADR018 reflects the close.

## Out of scope

- The GraphQL flat-`ownerId` dialect note (stays as-is).
