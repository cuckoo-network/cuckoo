# ADR018 stale-marker sweep

Three ◐ markers in `docs/ADR018-render-parity.md` look stale beyond what `w4/m20/t003` already owns (MFA + email-recovery identity rows) — verify each against the code/board and flip or keep with evidence:

- **Cron row** still defers "Build Command" (line ~32) — but `w6/m22`'s closeout claims Render-native runtime/build/start compatibility shipped for kpack. Check whether a git-sourced cron now honors a build command.
- **Header rules · redirects/rewrites, UI ◐** (line ~90) — the cell says ◐ while its own evidence text says "editable in the static-site Settings section." Reconcile: if the Settings editor is real, the UI cell is ✅.
- **Email recovery / verification ◐** (line ~115) — "live SMTP courier → w4/m7"; `w4/m7` closed 2026-07-12. Likely ✅ now.

Sub-hour doc sweep, no code. Distinct from `w4/m20/t003` (which owns the MFA-row-cites-open-m11 + email-recovery pointer) — coordinate so the two don't collide on the email-recovery row.

Source: `/pm-brainstorm more milestones for each worker` round 4, 2026-07-14.
