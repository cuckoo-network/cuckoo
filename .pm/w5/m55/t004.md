---
id: w5/m55/t004
title: Reconcile section-order / grouping drift surfaced by the walk
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m55/t002, w5/m55/t003]
---

## Objective

Apply the low-risk IA fixes t001 surfaced so the Postgres and Key Value detail pages read in Render's section order / grouping, or — for anything larger than a reorder — file it as a `w5/NNN` inbox note rather than expanding this milestone. Type/plan gating must still hold.

## Context

- t001's drift list; the datastore detail routes/components.
- Precedent: w5/m52 reordered the services Settings page to Render's section order (fold-in, split, bottom danger area) — apply the same judgement, but keep this task to reorders/labels, not new features.

## Steps

1. Apply each in-scope reorder/label/grouping fix.
2. File any out-of-scope gap (a missing card, a new control) as a `w5/NNN` inbox note with the Render evidence.

## Files

- datastore detail route(s) + section components as needed.

## Acceptance criteria

- [ ] In-scope IA drift closed; out-of-scope items filed as inbox notes (not silently dropped).
- [ ] Type/plan gating unchanged; suite green.

## Out of scope

- New cards/controls Render has that bex lacks — file, don't build here.
