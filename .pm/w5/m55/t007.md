---
id: w5/m55/t007
title: Test coverage — behavior tests for the migrated datastore fields
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m55/t005]
---

## Objective

Add/update meaningful behavior tests for the migrated Postgres + Key Value fields: each renders disabled with a pencil, edit enables the control, Save is dirty-gated, Cancel restores, and the existing mutation fires with the correct value (after confirm where applicable). Tests assert real interaction outcomes — no snapshot-everything, no tautologies.

## Context

- Test layout: `dashboard/src/features/databases/components/__tests__/` and `dashboard/src/features/keyvalue/components/__tests__/`.
- The shared `EditableFieldRow` state machine is already covered by its own test (w5/m50); these tests cover the datastore instances.

## Steps

1. Cover each migrated field's dirty-gated save / cancel-restore and its mutation wiring.
2. Cover plan-gated non-editability where it applies (Key Value on Free).

## Files

- `dashboard/src/features/databases/components/__tests__/` + `dashboard/src/features/keyvalue/components/__tests__/`

## Acceptance criteria

- [ ] New/updated tests fail if the dirty gate, cancel-restore, or mutation wiring regress.
- [ ] Full dashboard suite green.

## Out of scope

- E2E browser automation (the dev-5 walk lives in t005/t008).
