---
id: w5/m27/t002
title: Blueprints list page + route + sidebar nav
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m27/t001]
---

## Objective

Add a Blueprints list page at `/blueprints`, reachable from the sidebar, listing the workspace's registered blueprints with an empty state that explains blueprints auto-register on `deploy`.

## Context

- Route pattern: flat file-based routes in `dashboard/src/routes/`.
- Sidebar nav component (same one m26 adds "Env Groups" to).
- Data from t001's `useBlueprints()`.

## Steps

1. Create `routes/blueprints.tsx` (list) with page title/meta.
2. Render rows (name, repo, branch, status); add a per-row link to the detail page (t003).
3. Empty state: copy explaining a blueprint auto-registers on every repo-backed `deploy` (no manual "create blueprint" — no authoring picker).
4. Add a "Blueprints" sidebar nav entry.

## Files

- `dashboard/src/routes/blueprints.tsx` (new)
- `dashboard/src/features/blueprints/components/*` (list)
- sidebar nav component

## Acceptance criteria

- [ ] `/blueprints` lists registered blueprints (name/repo/branch/status); empty state explains auto-registration.
- [ ] Sidebar exposes a "Blueprints" nav item.

## Out of scope

- Detail page (t003), validate flow (t004).
