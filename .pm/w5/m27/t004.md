---
id: w5/m27/t004
title: Validate flow — dry-run with per-entry errors
worker: worker5
status: todo
estimate: 35m
depends_on: [w5/m27/t002]
---

## Objective

Add a "Validate" affordance (on the detail page, and/or per-row on the list) that runs `validateBlueprint` as a dry-run and surfaces per-entry errors — the agent pre-flight primitive, exposed to humans. No apply.

## Context

- `validateBlueprint` = dry-run, per-entry errors, no apply (docs/ADR018-render-parity.md; `POST /v1/blueprints/validate`). Rejected fields (`envVarGroups`/`fromGroup`/`generateValue`/`sync:false`/`fromService.envVarKey`) return a named error — surface those verbatim.
- Hook from t001 (`useValidateBlueprint`).

## Steps

1. Add a "Validate" button that calls `validateBlueprint` for the blueprint's manifest.
2. Render the result: a green "valid" state or a list of per-entry errors (entry + message), including named rejected-field errors.

## Files

- `dashboard/src/features/blueprints/components/*` (validate panel/dialog)

## Acceptance criteria

- [ ] Validate runs a dry-run (no apply) and shows per-entry errors distinctly from a valid result.
- [ ] Named rejected-field errors are shown verbatim, not swallowed.

## Out of scope

- Empty/error states + locales (t005).
