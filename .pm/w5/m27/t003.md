---
id: w5/m27/t003
title: Blueprint detail — manifest view + sync action
worker: worker5
status: todo
estimate: 40m
depends_on: [w5/m27/t001]
---

## Objective

Add a Blueprint detail page at `/blueprints/$blueprintId` showing the read-only manifest (`bex.yml`), the last-sync status, and a "Sync" action (with confirmation) that triggers an idempotent re-apply.

## Context

- `syncBlueprint` is all-or-nothing (same path as `deploy`), no sync-delete of removed entries (docs/ADR018-render-parity.md "Blueprint" row divergences).
- Manifest is the stored `manifest` field on the blueprint row (render.yaml-shaped `bex.yml`).

## Steps

1. Create `routes/blueprints.$blueprintId.tsx`.
2. Render the manifest read-only (monospace / code block) + metadata (repo/branch/status).
3. Add a "Sync" button with a confirm dialog → `syncBlueprint`; surface success/failure and refresh status.

## Files

- `dashboard/src/routes/blueprints.$blueprintId.tsx` (new)
- `dashboard/src/features/blueprints/components/*` (detail + sync)

## Acceptance criteria

- [ ] Detail page shows the manifest read-only + status; "Sync" triggers `syncBlueprint` behind a confirm and reflects the outcome.

## Out of scope

- Validate flow (t004); locales (t005).
