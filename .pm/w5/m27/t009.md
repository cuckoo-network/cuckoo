---
id: w5/m27/t009
title: Test coverage — Blueprints page behavior
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m27/t007]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — list, manifest/sync, and validate flows — asserting real behavior and failure modes, not trivial snapshots.

## Context

- Test pattern: `dashboard/src/**/__tests__/*.test.tsx` with mocked hooks.

## Steps

1. List page: renders blueprints; empty state (with auto-registration copy) when none.
2. Detail: manifest renders; Sync behind confirm calls `syncBlueprint` and reflects outcome.
3. Validate: valid result vs per-entry errors (including a named rejected-field error).
4. Failure mode: a sync/validate error surfaces (not a silent no-op).

## Files

- `dashboard/src/features/blueprints/**/__tests__/*` (new)
- `dashboard/src/routes/__tests__/blueprints*.test.tsx` (new)

## Acceptance criteria

- [ ] Tests assert list/manifest/sync/validate behavior + at least one failure mode; `yarn test` passes.
- [ ] No tautological/snapshot-everything tests.

## Out of scope

- Live cluster verification (t006, already done).
