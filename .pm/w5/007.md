# Events tab + deploy list UI (Rollback/Cancel actions) — gated on w3/m7 + w2/m10

The service-detail Events/Deploys UI half of two backend milestones materialized 2026-07-12: `w3/m7` ships `GET /v1/services/{id}/events` + GraphQL `serviceEvents` (activity feed), and `w2/m10` ships deploy cancel + rollback verbs. UI scope when promoted: an Events tab on the service detail (Render IA — newest-first feed, type icons, actor, relative time, cursor "load more") and a deploy list with per-deploy **Rollback** / mid-build **Cancel** actions (confirm dialogs; rollback provenance rendered). GraphQL consumers exist by design (`serviceEvents`, `deploys(serviceId)`, `cancelDeploy`/`rollbackService`). Verify against Render's live Events tab via Playwright MCP before building (capture → `docs/render-artifacts/`).

Promote once both backends land; likely milestone-sized (tab + list + two mutations + tests).

Source: `/pm-brainstorm more` 2026-07-12 (proposals A + B, UI halves deliberately split out per the w5 pattern).
