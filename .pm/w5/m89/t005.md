---
id: w5/m89/t005
title: "Complete historical choices, localized states, and matching pending layout"
worker: worker5
status: todo
estimate: 30m
depends_on: ["w5/m89/t004"]
---

## Objective

Complete the selection experience across historical data, empty windows, and responsive loading states.

## Context

- A 12-hour graph can contain terminated replicas absent from live instance listing.
- The repository requires pending skeleton geometry to match ready content at the same viewport.
- Both English and Chinese UI strings and narrow-mobile controls are required.

## Steps

1. Ensure the selected-window instance choices include historical samples and retain an explicit safe state if a selection leaves the window.
2. Complete localized loading/error/no-match states and responsive control layout.
3. Update the Metrics route's actual pending skeleton and its manifest/fixtures so its bounds, control slots, and chart regions match ready state.

## Files

- `dashboard/src/features/metrics/components/application-metrics-card.tsx`
- `dashboard/src/features/metrics/hooks/use-metrics-filter-values.ts`
- `dashboard/src/routes/services.$serviceId.metrics.tsx`
- `dashboard/src/common/components/route-skeletons.tsx`
- `dashboard/src/common/lib/route-skeleton-manifest.ts`
- `dashboard/src/features/metrics/locales/en.ts`
- `dashboard/src/features/metrics/locales/zh.ts`

## Acceptance criteria

- [ ] A terminated replica present in the query window can still be selected; removing it from a new window does not silently select everything.
- [ ] English/Chinese loading, error, and empty states describe the actual result.
- [ ] Pending and ready geometry match in side-by-side desktop and narrow-mobile checks.

## Out of scope

- A route-wide skeleton redesign.
- New saved-view or cross-service analytics products.
