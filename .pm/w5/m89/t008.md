---
id: w5/m89/t008
title: "Test coverage"
worker: worker5
status: todo
estimate: 60m
depends_on: ["w5/m89/t006", "w5/m89/t007"]
---

## Objective

Add and run meaningful behavior and failure-mode coverage for the implemented contract, then establish the milestone's observable live evidence.

## Context

- Selection must precede aggregation, and missing samples cannot be padded with zero.
- The pending skeleton must match the exact ready geometry on desktop and narrow mobile, including the new control row.

## Steps

1. Add regressions that fail against the pre-fix behavior; use real stores/concurrency or nontrivial series fixtures where the invariant requires them.
2. Run focused metrics property/table/adapter tests and the backend suite plus backend lint.
3. Regenerate schema/client types if needed, then run dashboard typecheck, lint, tests, and production build for the changed route.
4. Record the live multi-replica controls and desktop/mobile pending-versus-ready evidence; remove disposable fixtures.

## Files

- `lego/backend/internal/metrics/`
- `dashboard/src/features/metrics/components/__tests__/application-metrics-card.test.tsx`
- `dashboard/src/features/metrics/hooks/__tests__/use-metrics.test.ts`
- `dashboard/src/routes/__tests__/services.$serviceId.metrics.test.tsx`
- `dashboard/src/common/components/__tests__/route-skeletons.test.tsx`
- `dashboard/src/routes/__tests__/route-skeleton-manifest.test.ts`

## Acceptance criteria

- [ ] The 10/30 fixture proves selection plus MIN=10, MAX=30, AVG=20 and distinguishes raw mode.
- [ ] Failure coverage includes missing timestamps, mixed limits, stale/historical choices, unknown/foreign/oversized selection, and unsupported aggregation.
- [ ] REST/GraphQL/MCP contract cases produce equivalent values/errors; frontend tests assert query variables and displayed results without duplicating the production implementation.
- [ ] A disposable dev-5 multi-replica walk and forced pending/ready comparisons at desktop and narrow-mobile widths pass; evidence and cleanup are recorded.

## Out of scope

- Tautological assertions, mock-echo tests, retries/sleeps/skips that mask failures.
- Marking credential-gated live acceptance complete without the required evidence.
