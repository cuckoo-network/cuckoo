---
id: w5/m33/t002
title: REST create accepts `envVars`/`secretFiles`/`serviceIds` atomically
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m33/t001]
---

## Objective

`POST /env-groups` optionally carries initial contents and service links, applied atomically with the group mint — a validation failure (bad var name, foreign serviceId) leaves no orphan group.

## Context

- `lego/backend/internal/envgroups/` (service + rest); w6/m24's workspace attribution; the existing add-var/link verbs are the building blocks.
- OpenBao-backed contents (docs/ADR013): reuse the existing write path.

## Steps

1. Decode + validate the full body before minting anything.
2. Mint group → write contents → link services in one core verb; unwind on failure (no orphan row — the w4/m23 `017` precedent).

## Files

- lego/backend/internal/envgroups/

## Acceptance criteria

- [ ] Populated create round-trips; an invalid serviceId fails the whole create with a named error and zero side effects.

## Out of scope

- Other adapters (t003).
