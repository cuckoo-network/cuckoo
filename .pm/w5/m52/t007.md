---
id: w5/m52/t007
title: Test coverage — section structure + Region row tests
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m52/t005]
---

## Objective

Behavior tests for the restructure: per-type section order/grouping (web full sequence; static/cron reduced sets), subdomain toggle living inside Custom Domains with working semantics, Deploy Hook inside Deploy, Region row shown/hidden by value, and the bottom danger area's type-worded labels + intact confirm flows.

## Context

- Route-level render tests precedent under `dashboard/src/features/services/components/__tests__/` and route tests.

## Steps

1. Section-order assertion per service type.
2. Region present/absent, subdomain toggle behavior in its new home, danger-area labels.

## Files

- `dashboard/src/features/services/components/__tests__/` + route tests

## Acceptance criteria

- [ ] Tests fail on section-order or grouping regressions; full suite green.
