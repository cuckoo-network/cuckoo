---
id: w5/m52/t003
title: Fold Platform Subdomain into Custom Domains; match Render's section order
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m52/t002]
---

## Objective

Nest the platform-subdomain toggle inside the Custom Domains card as a closing "bex Subdomain" block (Render nests "Render Subdomain" inside Custom Domains with the reachable-URL sentence), removing the standalone Platform Subdomain section. Then reorder the page's sections to Render's sequence: General › Build › Deploy › Custom Domains › Networking (bex IP allowlist keeps this slot; Render's Networking holds Edge Caching, a non-goal) › Notifications › Health Checks › Maintenance Mode.

## Context

- `dashboard/src/features/services/components/platform-subdomain-section.tsx` (w7/m31) + `custom-domains-section.tsx`.
- Section order today (route file): Settings › Build & Deploy › Health Checks › Custom Domains › Platform Subdomain › Networking › Maintenance › Notifications › Deploy Hook › Suspend › Danger.
- Registry Credential section (bex-only, w5/m37): keep adjacent to Build (it configures image pulls for builds/deploys).
- Type gating: static sites/cron keep their reduced section sets (w5/m48, w5/m11).

## Steps

1. Move the subdomain toggle markup into the Custom Domains card; keep the switch semantics + reachable-URL copy.
2. Reorder section composition in `services.$serviceId.settings.tsx`.
3. Verify each type variant's page against its Render counterpart's order.

## Files

- `dashboard/src/features/services/components/custom-domains-section.tsx`
- `dashboard/src/features/services/components/platform-subdomain-section.tsx` (absorbed/removed)
- `dashboard/src/routes/services.$serviceId.settings.tsx`
- tests under `__tests__/`

## Acceptance criteria

- [ ] Subdomain toggle renders inside Custom Domains with unchanged behavior; standalone section gone.
- [ ] Section order matches the DoD sequence for web services; type variants stay coherent.

## Out of scope

- PR Previews / Edge Caching / Log Stream sections (non-goals, `.pm/DO_NOT_DO.md`).
