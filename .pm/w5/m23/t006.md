---
id: w5/m23/t006
title: "Live verification against the Render capture"
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m23/t003, w5/m23/t004, w5/m23/t005]
---

## Objective

Compare the shipped UI against t001's Render capture and confirm the filters behave correctly end-to-end.

## Steps

1. Side-by-side comparison against t001's capture — confirm control placement/labels are consistent (or divergence is deliberate and documented).
2. Confirm `type=request` returns real Traefik access lines with the new chip rendering.
3. Confirm `level=error` isolates error lines on a planted error.

## Files

- none (live verification)

## Acceptance criteria

- [ ] The shipped UI matches the Render capture's shape (or documented divergence).
- [ ] Both filter scenarios behave as specified.
