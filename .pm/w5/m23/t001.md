---
id: w5/m23/t001
title: "Capture Render's live Logs filter UI"
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Capture Render's live Logs filter UI via Playwright before building anything, so controls/labels/placement are render.com-consistent rather than guessed.

## Context

- `.pm/w5/done/008.md` (or wherever the source note lands once promoted) — the original instruction to capture first.
- `docs/render-artifacts/` — the convention for storing live captures.

## Steps

1. Navigate to a real Render service's Logs page with Playwright.
2. Exercise the filter controls (type, level, statusCode, method, path, instance) and capture screenshots + DOM structure.
3. Save to `docs/render-artifacts/logs-filter-ui.md` (or similar).

## Files

- `docs/render-artifacts/logs-filter-ui.md`

## Acceptance criteria

- [ ] A live capture exists documenting Render's filter control placement, labels, and interaction pattern.

## Out of scope

- Building the UI — t002+.
