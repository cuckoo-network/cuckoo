---
id: w5/m13/t001
title: Capture Render's Build & Deploy settings section live
worker: worker5
status: todo
estimate: 25m
depends_on: []
---

## Objective

Capture Render's Settings → Build & Deploy section live (Playwright) as the layout reference for this milestone's UI, same as the pattern used in `w5/m7`'s Render reference capture.

## Context

- `w5/m7`'s README documents the equivalent capture for the Instance Type section — reuse that workflow.
- Render docs: https://render.com/docs/monorepo-support#setting-a-root-directory.
- Screenshots land in `.playwright-mcp/` (gitignored) per repo convention — pass a bare filename.

## Steps

1. Navigate to a live Render service's Settings page, Build & Deploy section (or the equivalent docs screenshot if a live account isn't available).
2. Capture the Root Directory field's label, placeholder/help text, and save/validation behavior.
3. Save the reference as a snapshot or screenshot under `.playwright-mcp/` and note it in this milestone's README (following `w5/m7`'s "Render reference" pattern).

## Files

- `.pm/w5/m13/README.md` (add captured reference notes)
- `.playwright-mcp/` (screenshots, gitignored)

## Acceptance criteria

- [ ] Render's Build & Deploy section layout/copy is documented (screenshot or written element inventory)
- [ ] Reference noted in the milestone README for t002/t003 to build against

## Out of scope

- Building the actual dashboard section (t002).
