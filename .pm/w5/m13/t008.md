---
id: w5/m13/t008
title: "Closeout: verify DoD, mark done, move to done/"
worker: worker5
status: todo
estimate: 15m
depends_on: [w5/m13/t007]
---

## Objective

Verify the milestone's Definition of Done actually holds, then close it out: mark every task done, move task files to `done/`, update the milestone `README.md` status, move the whole milestone directory to `w5/done/m13/`, and check its box in `w5/README.md`.

## Context

- DoD (from the milestone README): a signed-in user views and edits an App's Root Directory from Settings → Build & Deploy, matching Render's field semantics; saving updates `spec.rootDir` via GraphQL and triggers a scoped rebuild; verified live.

## Steps

1. Re-confirm t004's live verification still holds (re-run if code changed since).
2. If the DoD holds: set every task's `status: done`, move each `tNNN.md` to `w5/m13/done/`, mark every row `— DONE` and `**Status:** done` in the milestone README, move `w5/m13/` to `w5/done/m13/`, check `- [x]` in `w5/README.md`.
3. If the DoD does not hold, do not close — file what's missing as a new task instead.

## Files

- `.pm/w5/m13/README.md`
- `.pm/w5/README.md`
- All `.pm/w5/m13/tNNN.md` files

## Acceptance criteria

- [ ] DoD reconfirmed live before closing
- [ ] Milestone moved to `w5/done/m13/` only after confirmation

## Out of scope

- Any new implementation work.
