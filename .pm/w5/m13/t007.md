---
id: w5/m13/t007
title: Test coverage for Build & Deploy section
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m13/t005]
---

## Objective

Add meaningful UI/query tests for the Build & Deploy section's real behavior and failure modes — not trivial/snapshot-everything tests.

## Context

- Follow the existing dashboard test patterns used by `w5/m7` (catalog query, picker states, mutation wiring).

## Steps

1. Test the section renders correctly for an App with `rootDir` unset (repo-root default state) and set (shows the value).
2. Test Save stays disabled until the value changes, and the confirm/rollout-warning flow fires on save.
3. Test the mutation call and success/error toast paths.

## Files

- Test files colocated with `dashboard/src/routes/services.$serviceId.settings.tsx` and the relevant GraphQL query/mutation files.

## Acceptance criteria

- [ ] Tests cover unset vs. set `rootDir` states
- [ ] Tests cover Save-disabled-until-changed and the confirm flow
- [ ] Tests cover mutation success/error paths
- [ ] Dashboard test suite green

## Out of scope

- End-to-end live verification (covered by t004, already done before this task runs).
