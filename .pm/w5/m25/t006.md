---
id: w5/m25/t006
title: "Render parity — full-surface consistency check"
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m25/t005]
---

## Objective

Confirm the new dashboard UI is consistent with the REST/GraphQL/MCP contract `w1/m32` already shipped — same fields, same semantics, no UI-only drift — and update `docs/ADR018-render-parity.md`'s Projects & Environments row from ◐ to ✅ for the UI column.

## Context

- `docs/ADR018-render-parity.md` "Projects & environments (grouping)" row.
- `lego/backend/internal/environments/{rest,graphql,mcp}.go` for the authoritative contract.

## Steps

1. Diff the dashboard's environment fields/actions against the backend contract (list/get/create/rename/delete/setEnvironmentServices) for any omitted or renamed field.
2. Update the ADR018 row's UI column and evidence pointer.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] No field/action exposed by the backend is silently missing from the UI (or the omission is explicitly noted as deliberate).
- [ ] ADR018 updated.

## Out of scope

- New backend surface changes (none expected).
