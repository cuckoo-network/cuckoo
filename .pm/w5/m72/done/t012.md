---
id: w5/m72/t012
title: Make final Git publication idempotent and protocol-correct
worker: worker5
status: done
estimate: 60m
depends_on: [w5/m72/t011]
---

## Objective

Correct the final delivery edge exposed by `ags-da2daqui706c739sqvtg`: decide publication from the freshly observed remote ref, preserve force-with-lease conflict protection, accept legal shallow metadata, and clear stale failure text when a retry becomes active.

## Context

- The failed command reported HTTP 403 and `Everything up-to-date` for candidate `7f3fc6b4…`.
- The next turn succeeded on the same repository and opened draft PR #57, so the failure is an edge in final publication state handling rather than a global Git credential outage.

## Steps

1. Add pre-push and post-error remote-OID reconciliation around the exact scanned candidate.
2. Keep a different remote OID as a hard concurrent-update failure.
3. Parse bounded legal receive-pack shallow declarations without weakening exact-ref confinement.
4. Clear `failure_reason` at accepted-turn and successful-dispatch boundaries.

## Files

- `lego/agent-image/driver/src/delivery.ts`
- `lego/backend/internal/sshgateway/agentcred/agentcred.go`
- `lego/backend/internal/store/agentsessions.go`

## Acceptance criteria

- [x] Exact-candidate replay is idempotent success.
- [x] A different remote candidate remains rejected.
- [x] Push errors remain failures unless a fresh read proves the exact scanned OID.
- [x] Active retries do not retain the old terminal reason.

## Out of scope

- Changing Git providers or weakening repository/branch confinement.
