---
id: w5/m72/t009
title: Simplify
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m72/t008]
---

## Objective

Run `/simplify` over the milestone's database-grant, driver cleanup, OpenSandbox status, gateway terminal-signal, and Completer changes; collapse duplicated terminal/error/cleanup logic while preserving least privilege and fail-closed behavior.

## Context

- The incident crossed four components that already contain overlapping terminal and cleanup classifications.
- Simplification must not merge trust boundaries: bex-api remains DB/business logic without `pods/exec`, and the gateway remains the isolated Kubernetes exec boundary.

## Steps

1. Review all changed code for duplicated cleanup calls, error translation, status mapping, retry loops, and privilege declarations.
2. Reuse one typed terminal vocabulary and one credential-cleanup verdict where architectural boundaries allow.
3. Remove stale comments/branches and apply measurable efficiency improvements without weakening checks.

## Files

- All implementation files changed by t001–t008

## Acceptance criteria

- [x] `/simplify` findings are applied or explicitly rejected with a concrete safety/architecture reason.
- [x] No duplicate scrub, terminal mapping, or role-grant source remains where one shared implementation is valid.
- [x] Operator/backend/gateway trust boundaries and least privilege remain intact.

## Out of scope

- Behavior changes unrelated to this incident.
- Broad refactoring of OpenSandbox or the agent-session API.
