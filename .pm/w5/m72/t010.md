---
id: w5/m72/t010
title: Test coverage
worker: worker5
status: todo
estimate: 60m
depends_on: [w5/m72/t008, w5/m72/t009]
---

## Objective

Add durable regression coverage for every incident seam and run the complete affected gates, including tests that would have failed on the production permission drift, large-repository scrub, duplicate cleanup crash, and stale BatchSandbox/failed-Pod combination.

## Context

- Existing suites all passed during the incident because they apply fresh grants before testing, intentionally expect the byte-limit error, mock terminal OpenSandbox state, and never exercise a real failed child Pod plus stale BatchSandbox.
- A post-deploy authenticated replay smoke is necessary because unit tests cannot prove the installed production role matches the released schema.

## Steps

1. Add DB-role drift/preflight tests and exercise the full `agentattach.Store` under the scoped role.
2. Add bounded >1 GiB sparse-tree, Git-object needle, cleanup-failure, ACP child-exit, and status-server survival driver tests.
3. Add pinned-controller real-CR tests for Failed Pod projection and backend/gateway tests for stale Pending plus terminal Pod.
4. Extend `scripts/agent-session-verify.sh` with authenticated durable replay, forced driver crash, convergence deadline, and no-retry-storm assertions.
5. Run agent-image tests, backend build/tests/integration, dashboard tests/typecheck/lint/build, Go lint, and GitOps/deploy validation.

## Files

- `lego/agent-image/driver/test/`
- `lego/backend/internal/sshgateway/dbrole/dbrole_integration_test.go`
- `lego/backend/internal/sshgateway/agentattach/agentattach_test.go`
- `lego/backend/internal/sshgateway/sandboxsse/sandboxsse_test.go`
- `lego/backend/internal/sandbox/*_test.go`
- `lego/backend/internal/agentsessions/completion_test.go`
- `scripts/agent-session-verify.sh`
- GitOps/deploy validation tests

## Acceptance criteria

- [ ] Each of the four production failure layers has a regression that fails against the pre-milestone behavior.
- [ ] The real-controller test observes terminal projection from a failed Pod, not a mocked terminal response.
- [ ] The live smoke validates installed-role privilege, authenticated replay, forced-crash convergence, and bounded log volume.
- [ ] All affected unit, integration, build, lint, dashboard, and deployment validation gates pass.

## Out of scope

- Tautological mocks or snapshot-only coverage.
- Requiring a multi-gigabyte checked-in fixture.
