---
id: w5/m37/t003
title: Attach/detach a credential in service settings' image section
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m37/t001]
---

## Objective

In service settings, let a user change or clear the registry credential on an existing image-backed service — the second of Render's two attach points.

## Context

- `dashboard/src/routes/services.$serviceId.settings.tsx` — has no registry attach today.
- Reuse the t002 selector component; patch via the updated mutation from t001.
- Only shown for image-sourced services.

## Steps

1. Add the selector (with current value + explicit "None") to the image section of settings.
2. Wire to the patch mutation; surface success/error per the page's existing pattern.
3. Component test: change + clear round-trip.

## Files

- `dashboard/src/routes/services.$serviceId.settings.tsx`
- `dashboard/src/features/services/` (component + test)

## Acceptance criteria

- [ ] Changing and clearing the credential persists and re-renders from a fresh query.

## Out of scope

- Redeploy-on-change semantics beyond what the backend patch already does.
