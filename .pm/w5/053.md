# Extend log-shipper's request-host allowlist to the platform edge hosts

Why: during a platform-API outage the obs availability dashboard's 5xx ratio rises but its Loki log panel stays empty — the shipper retains request lines only for tenant apps + `dashboard.bex.co`, so the panel cannot explain exactly the incidents it exists for (ADR088 §6 review triage item 2, 2026-09-08).

Extend the w4/m88 BOUNDED `RequestHost` allowlist in `deploy/gitops/base/log-shipper.yaml` (~line 600) beyond `dashboard.bex.co` to the platform edge hosts `api.bex.co`, `oauth.bex.co`, `auth.bex.co`, `obs.bex.co` — each retained under bounded labels (`namespace`/`service` fixed per host, `host` stays line-only), same cardinality discipline as w4/m88. Update the shipper's unit assertions the way w4/m88's were written, and drop the "not yet retained" caveat from the availability dashboard's 5xx log panel description (`deploy/gitops/base/values/grafana.values.yaml`) once lines actually land.
