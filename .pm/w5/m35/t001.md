---
id: w5/m35/t001
title: Parameter-overrides editor (add/edit/remove + save via the existing saveParameters)
worker: worker5
status: todo
estimate: 60m
depends_on: []
---

## Objective

The Insights panel's Parameter Overrides section becomes an editor: add/edit/remove override rows, save through the already-exported `saveParameters`, with backend validation errors surfaced inline and the table refreshing to the applied state.

## Context

- Dead-wired hook: `dashboard/src/features/databases/hooks/use-database-insights.ts:61-96` (`setParameters` mutation + `saveParameters` export, zero consumers).
- Read-only section: `dashboard/src/features/databases/components/insights-panel.tsx:214-244` (name/setting/source table).
- Backend contract: `PUT /v1/postgres/{id}/parameter-overrides` (w2/m25) — replace-style; check which parameters the backend accepts and surface its rejection messages verbatim.
- Follow `dashboard/CLAUDE.md` conventions; en + zh locales.

## Steps

1. Editor UI on the existing section (respect the panel's current layout patterns — the access-control/recovery panels are the house style).
2. Wire `saveParameters`; optimistic-or-refetch per the feature's existing pattern.
3. Locale strings (en/zh).

## Files

- `dashboard/src/features/databases/components/insights-panel.tsx` (or a split-out component)
- `dashboard/src/features/databases/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] Live on dev-N: add an override, save, see it applied (source flips to override); remove it, save, see it reverted
- [ ] A backend-rejected parameter shows its error inline without losing edits

## Out of scope

- New backend validation; parameter documentation/catalog UI
