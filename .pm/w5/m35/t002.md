---
id: w5/m35/t002
title: Resource-caps display — "N of M" services/Postgres/Key Value where limits are set
worker: worker5
status: todo
estimate: 45m
depends_on: []
---

## Objective

Workspaces with configured caps (`BEX_MAX_SERVICES`/`BEX_MAX_POSTGRES`/`BEX_MAX_KEYVALUES`) see used-vs-limit in the dashboard before hitting the cap error; unlimited (limit 0) shows nothing.

## Context

- Backend: GraphQL `workspaceLimits: ResourceLimits` with `ResourceCap {used, limit}` (`lego/backend/internal/workspaces/graphql.go:46-61`); types already in `dashboard/src/graphql/definitions.ts:1952-1962` — write the `.graphql` operation + hook.
- Placement: the usage page (`features/usage/components/usage-page.tsx`) as a caps row, or workspace settings — pick whichever reads naturally beside existing content and note the choice.

## Steps

1. `.graphql` operation + hook; component with the three caps (hidden when unlimited).
2. Near-limit affordance (e.g. warning tone at ≥80%) consistent with the design system.
3. Locale strings (en/zh).

## Files

- `dashboard/src/features/usage/` (or `features/workspaces/`), `dashboard/src/features/*/api/*.graphql`, locales

## Acceptance criteria

- [ ] A capped workspace shows correct used/limit for all three resource kinds; an uncapped one shows nothing
- [ ] Values update after creating/deleting a resource

## Out of scope

- Enforcement changes; REST/MCP (already shipped)
