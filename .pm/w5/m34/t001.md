---
id: w5/m34/t001
title: Surface session_aal2_required from fetchSession
worker: worker5
status: todo
estimate: 45m
depends_on: []
---

## Objective

Make the dashboard's session fetch recognize Kratos's `session_aal2_required` error (the 403 an aal1 session gets when aal2 is enforced) and return it as a typed state alongside authenticated/anonymous — instead of the login page rediscovering it by probing.

## Context

- `.pm/w4/done/m11/` (the shipped MFA milestone) — its t005 altitude finding is the spec; read it first.
- Session plumbing: `dashboard/src/` `session.ts` / `auth.ts` (per the m11 README's pointer); Kratos returns `error.id === "session_aal2_required"` on `/sessions/whoami` for step-up-required sessions.

## Steps

1. Read the m11 t005 finding + current `fetchSession`/hook flow.
2. Add the typed `aal2Required` state to the session result; plumb through the hook.
3. `cd dashboard && yarn test` (and typecheck).

## Files

- `dashboard/src/**/session.ts`, `auth.ts` (locate exact paths from m11)

## Acceptance criteria

- [ ] An aal1 session against an aal2-enforced identity yields the typed state from `fetchSession`, no probe requests elsewhere.

## Out of scope

- The redirect itself (t002).
