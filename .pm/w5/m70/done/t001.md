---
id: w5/m70/t001
title: Rename the nav/route surface: /billing real, /usage shim, deliberate-rename notes
worker: worker5
status: done
estimate: 45m
depends_on: []
---

## Objective

Make `/billing` the real route for the money page and "Billing" its nav name, with `/usage` kept as a redirect shim — a front-of-house rename only. The wire surface (REST `/v1/usage`, GraphQL `usage`, MCP `get_usage` — ADR023's bex extension), the `features/usage/` directory, and the `usage.*` i18n key prefixes all stay.

## Context

- Nav item: `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx:61` — `{ labelKey: "common.navUsage", to: "/usage", icon: BarChart3 }`.
- Current routes: `dashboard/src/routes/usage.tsx` (real page), `dashboard/src/routes/billing.$.tsx` (Render-URL alias currently redirecting `/billing/*` → `/usage`; `/billing/update-plan` → `/workspace/settings?plan=change`). The rename inverts the alias direction.
- Persistent-shell rules in `dashboard/CLAUDE.md` (chrome `staticData`, pending components) apply to the new route.
- Deliberate-rename records: `docs/ADR023-usage-metering.md` ("Usage is the deliberate counterpart") and the `/billing.$` route comment both need updating so the rename doesn't read as drift.

## Steps

1. Create `routes/billing.tsx` as the real route (move the route config from `usage.tsx`, keep `chrome: true` + pending behavior); turn `usage.tsx` into a redirect shim to `/billing` preserving search/query.
2. Update `billing.$.tsx`: `/billing/update-plan` behavior unchanged; the catch-all now lands on `/billing` (no more bounce to `/usage`); rewrite its comment for the post-rename world.
3. Sidebar: label key value → "Billing" (adjust the `common.navUsage` en/zh messages, or introduce `common.navBilling` — smallest diff wins), icon → a credit-card/receipt-class lucide icon (named import, never the barrel).
4. Update ADR023's counterpart note to record the dashboard page rename (API name unchanged).
5. `yarn typecheck && yarn lint && yarn test`; prettier on touched markdown.

## Files

- `dashboard/src/routes/{usage.tsx,billing.tsx,billing.$.tsx}`
- `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx`
- `dashboard/src/common/locales/{en,zh}.ts`
- `docs/ADR023-usage-metering.md`

## Acceptance criteria

- [ ] `/billing` renders the page; `/usage` (with query) redirects to `/billing` preserving it; `/billing/update-plan` opens the change-plan dialog.
- [ ] Sidebar reads "Billing" (en) / corresponding zh, with the new icon; no route renders its own `<aside>` (one-rail test still green).
- [ ] REST/GraphQL/MCP usage surfaces untouched (no backend diff in this task).
- [ ] ADR023 + `billing.$.tsx` comments describe the rename as deliberate.

## Out of scope

- Page-internal restructure (t002); credits (t003/t004); renaming `features/usage/` or `usage.*` i18n keys.
