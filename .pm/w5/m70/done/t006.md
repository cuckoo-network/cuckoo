---
id: w5/m70/t006
title: Simplify
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m70/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (routes/nav, page restructure, billing service credits block, credits UI) — reuse / simplification / efficiency, strictly behavior-preserving.

## Context

- Touched areas: `dashboard/src/routes/`, `dashboard/src/features/usage/`, `lego/backend/internal/billing/`.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; re-run both suites.

## Files

- (as found)

## Acceptance criteria

- [ ] Findings triaged (applied or rejected with reason); suites green after.

## Out of scope

- New behavior.

## Result

Three parallel review agents (reuse / quality / efficiency) over the m70 diff. Applied:

- read.go: per-grant call cap now counts examined active grants (depleted grants could previously make the N+1 unbounded); list page size 100→25; shared `sumAvailableCreditBalances` helper dedupes the two summing loops AND restores the USD guard the per-grant copy had dropped (a non-USD grant would have been silently summed as USD); `Currency: available.Currency` replaces a redundant literal.
- usage-page.tsx: applied/due hoisted out of JSX (deleted `usdString`); `formatDateISO` replaces hand-rolled `.slice(0,10)`; redundant WHY comment deleted; `$` moved into both locale messages consistently.
- route-inventory.ts: two entries moved to their alphabetical positions.
- usage-page.test.tsx: heading assertion switched to `getByRole` per repo convention.
- Prettier re-run over touched files.

Rejected with reason:

- Slim trend GraphQL document (efficiency #3): attempted then reverted — the usage resolver builds the full Summary regardless of field selection, so a slimmer document saves response bytes only; a real fix needs resolver-level lazy fields or a dedicated trend verb (new behavior, out of /simplify scope). Trend's 2 historical-month BillingFor chains remain a mount-time-only, gracefully-degrading cost.
- Billing cache TTL (30s) vs poll (60s): steady state ~6-9 Stripe calls/min/open page — bounded, left as is.
- Stub-server route-table duplication in stripe_test.go: the file's established convention.
