---
id: w5/m70/t007
title: Test coverage
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m70/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real behavior and failure modes, not snapshot filler.

## Context

- Highest-value cases: `/usage` → `/billing` redirect preserves query; `/billing/update-plan` alias intact; credits block absent-not-zeroed on Stripe failure and on permission-denied (backend stub tests); three-surface shape equality for the credits block; stat card hidden at zero; gate copy present for a credit-holding cardless workspace.

## Steps

1. Backend: stub-Stripe tests for the four credit-read outcomes; a cross-surface shape test if the existing parity-test pattern fits.
2. Dashboard: route-shim tests + credits component tests (hidden/shown/expiry/copy).

## Files

- `lego/backend/internal/billing/*_test.go`
- `dashboard/src/features/usage/components/__tests__/`, `dashboard/src/routes/__tests__/`

## Acceptance criteria

- [ ] Every acceptance criterion in t001–t004 has at least one asserting test; both suites green.

## Out of scope

- Coverage-gaming tests.

## Result

- Backend: 3 new stub-Stripe tests (populated w/ voided+expired filtering & expiry sort, zero-balance short-circuit incl. no-grant-calls assertion, 403-degrades-to-omitted w/ preview intact); `TestBillingCrossSurfaceParity` extended to credits (REST==MCP==GraphQL field-by-field); PG regression for the reconciler livemode filter; reconciler mode-scoping unit test.
- Dashboard: `billing-redirects.test.tsx` (new — /usage→/billing query-preserving replace-redirect, /billing/update-plan → change-plan dialog, splat fold-back); usage-page credits suite (hidden-at-zero, full render incl. expiry note + gate copy, partial-coverage amount due, never-expiring note omitted); hook mapping test extended; section-order, sidebar, w-alias, route-heads, route-inventory, skeleton tests updated for the rename.
- Suites: backend `go test ./...` green + vet clean; dashboard 318 files / 2176 tests green; typecheck 0 errors.
