# Offline codegen reconciliation: dump schema → offline `yarn codegen` → diff hand-spliced types

Both w5/m58 and w5/m60 shipped **hand-spliced** GraphQL types ("no live codegen session" in their closeouts) — exactly the drift class the codegen memory warns about. The offline path already exists end to end and needs no live bex-api:

1. `cd lego/backend && SCHEMA_DUMP_PATH=/tmp/schema.json go test ./internal/api/ -run TestDumpGraphQLSchema -count=1` (the dump hook exists for exactly this — see the test's own comment).
2. `cd dashboard && SCHEMA_JSON=/tmp/schema.json yarn codegen` (`codegen.ts` prefers `SCHEMA_JSON` over the live endpoint).
3. Diff the regenerated output against the hand-spliced `definitions.ts` regions; reconcile any splice drift — **splice per-feature, never wholesale-accept the regenerated file** (definitions.ts is partly hand-maintained; full-codegen loses the hand-maintained parts).
4. Record the two-command recipe in `dashboard/CLAUDE.md` so future no-session splices (m58/m60 pattern) get reconciled routinely instead of accumulating.

~40m. Source: `/pm-brainstorm more for w5` round 3, 2026-07-30 (proposal 4); m58/m60 closeout "hand-spliced" records.
