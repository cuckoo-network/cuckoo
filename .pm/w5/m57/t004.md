---
id: w5/m57/t004
title: Render parity — live static-site page walk vs render.com
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m57/t001, w5/m57/t002, w5/m57/t003]
---

## Objective

Confirm the static-site page matches Render's `/static/<id>` page across the surfaces this milestone touched (dashboard UI only — no REST/GraphQL/MCP change), and log any residual drift as follow-up rather than diverging silently.

## Context

- This milestone is UI/routing only; the static-site data surface (publishPath/routes/headers) already shipped across REST/GraphQL/MCP in prior milestones (docs/ADR029-static-sites.md, w1/m21, w5/m48). The parity check here is a live UI walk, not a cross-adapter check.
- Reference: `dashboard.render.com/static/srv-d2prh63e5dus73bdbru0`; bex target `/static|services/srv-d9e5sbd5qe4s73b1mjq0`.

## Steps

1. On a non-prod dashboard (dev-5 or equivalent), open a static service and walk: URL scheme (`/static/<id>` + sub-routes), sidebar (Events/Settings · Metrics · Environment/Redirects-Rewrites/Headers), landing tab (Events), Settings sections (no Region, no PR Previews).
2. Compare side-by-side with the live Render static page; note any remaining divergences and whether they are intentional (Previews excluded per DO_NOT_DO #27; the `/services` → `/static` reverse-redirect behavior).
3. Record evidence + any drift in the milestone README or a `docs/render-artifacts/` note.

## Files

- Verification only (no product code) — evidence under `.playwright-mcp/` or `docs/render-artifacts/`.

## Acceptance criteria

- [ ] Documented side-by-side confirming URL, sidebar, landing, and Settings parity for static sites.
- [ ] Residual drift (if any) logged as follow-up; intentional exclusions (Previews) noted.

## Out of scope

- Building any excluded surface (Previews / PR Previews / preview environments).
