---
id: w5/m28/t003
title: Query history + result pagination
worker: worker5
status: todo
estimate: 1h
depends_on: [w5/m28/t002]
---

## Objective

Add basic query history (recently run queries, client-side is sufficient for v1) and pagination for large result sets.

## Steps

1. Client-side query history (localStorage or session-scoped) with a way to re-run a past query.
2. Paginate results beyond a reasonable row threshold, matching the row-cap decided in t004.

## Files

- `dashboard/` (SQL console component)

## Acceptance criteria

- [ ] Recently run queries are recallable within a session.
- [ ] Large result sets don't freeze the UI (paginated or virtualized).

## Out of scope

- Server-side persisted query history.
