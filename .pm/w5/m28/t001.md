---
id: w5/m28/t001
title: Backend - scoped SQL-execution endpoint
worker: worker5
status: todo
estimate: 1.5h
depends_on: []
---

## Objective

Add a REST/GraphQL endpoint that executes an arbitrary SQL query against a named managed Postgres, reusing the connection-resolution logic behind the existing MCP `query_render_postgres` tool.

## Context

- The MCP tool `query_render_postgres` already resolves a connection and executes queries — find its implementation and extract/reuse the connection logic rather than duplicating it.
- Authz should match the Databases page's existing scoping (`AuthorizeDatabase`).

## Steps

1. Locate the MCP tool's query execution + connection-resolution code.
2. Add a REST endpoint (and/or GraphQL mutation) that takes a Database name + SQL string, executes it using the same connection logic, and returns rows/columns or an error.
3. Authorize via the existing `AuthorizeDatabase` chokepoint.

## Files

- `lego/backend/internal/postgres/*.go`

## Acceptance criteria

- [ ] Endpoint executes a query and returns tabular results.
- [ ] Unauthorized callers are rejected.

## Out of scope

- Guard rails (timeout/row-limit/confirmation) — t004.
