---
id: w5/m68/t003
title: Move service/static detail + one list route's query into the loader
worker: worker5
status: todo
estimate: 1h
depends_on: [w5/m68/t002]
---

## Objective

Extend the loader-prefetch pattern established for the overview to the service/static detail pages and one high-traffic list route.

## Context

- `services.$serviceId` / `static.$serviceId` already loader-fetch their *title* (the `server` query for the document head); the page's fuller data still loads in the component body.
- Reuse the t002 shape (cause-aware fetch policy, cache-first on `stay`) so tab switches within a detail section don't refire.
- Pick the list route with the clearest measured waterfall from t001 (e.g. blueprints or env-groups).

## Steps

1. Add/extend the detail-route loader to prime the page's primary query (not just the title), reading cache-first in the component.
2. Do the same for the chosen list route.
3. Confirm tab switches and search-param changes reuse cache (no refetch storm), matching the documented `stay` behavior.

## Files

- `dashboard/src/routes/services.$serviceId.tsx`, `dashboard/src/routes/static.$serviceId.tsx`
- one list route (`dashboard/src/routes/blueprints.tsx` or `env-groups.tsx`)

## Acceptance criteria

- [ ] Detail + chosen list route prefetch primary data on hover and render warm on mount.
- [ ] Tab/search-param changes within detail don't refire the primary query.
- [ ] No hydration mismatch; all tests green.

## Out of scope

- Every remaining list route — this milestone is scoped to the hottest paths; the rest can follow as a note if the win holds.
