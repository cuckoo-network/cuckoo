---
id: w5/m68/t004
title: Verify SSR hydration + intent-prefetch-on-hover end to end
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m68/t003]
---

## Objective

Prove the prefetch works end to end: hover starts the fetch, mount shows data, SSR hydrates without mismatch or double-fetch, and latency beat the t001 baseline.

## Context

- Use Playwright against `yarn dev:local` (the `local-bex` stub) or the deployed dashboard.
- The persistent-shell walk (`da6b55b2`) established the DOM-instrumentation approach — reuse it for network-timing assertions.

## Steps

1. Playwright: hover a covered link, assert the primary request is in-flight/complete before the click.
2. After navigation, assert the page shows data (not a skeleton) on first paint for the warm case.
3. Confirm no hydration warning in console and no duplicate primary request on load.
4. Record the latency delta vs the t001 baseline in the milestone README.

## Files

- Playwright interaction (no committed test needed here beyond t006's coverage); record results in `.pm/w5/m68/README.md`.

## Acceptance criteria

- [ ] Hover-prefetch proven for the covered routes.
- [ ] Warm mount renders data, not a skeleton.
- [ ] No hydration mismatch / double-fetch; latency improved vs baseline.

## Out of scope

- Automated regression coverage (t006).
