---
id: w5/m68/t001
title: Measure baseline navigation latency (spike)
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Record the current post-navigation data-load latency for the routes this milestone targets, so the win is measured, not assumed — and to confirm the waterfall actually exists before moving queries.

## Context

- Targets: overview (`routes/index.tsx` → `useServices`/`useDatabases`/`useKeyValues`/`useProjects`), service detail (`services.$serviceId` + tabs), one list route.
- The waterfall: navigation completes → page mounts → component-body hook fires the query → skeleton until it resolves.
- Tooling: browser devtools (network waterfall + a `performance.mark` around mount→data-ready), against `yarn dev:local` or the deployed dashboard.

## Steps

1. Instrument mount→data-ready for each target route (or read it off the network waterfall).
2. Record the median time-to-data on hover-then-click for each target.
3. Note which queries fire only after mount (the ones worth relocating).

## Files

- No source change (measurement only); record numbers in `.pm/w5/m68/README.md` or a scratch note.

## Acceptance criteria

- [ ] Baseline time-to-data recorded for overview + service detail + one list route.
- [ ] The post-mount fetch waterfall is confirmed present (go) or absent (re-scope/no-go).

## Out of scope

- Any query relocation (t002+).
