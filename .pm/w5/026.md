Review and ship the uncommitted Render-style dashboard topbar navigation implementation from the 2026-07-18 user request. The shared dashboard layout now provides route-aware page context; switchable Project / Environment / Service breadcrumbs; lazy workspace-wide `Cmd/Ctrl+K` search; persistent New, help, and account actions; and responsive desktop/mobile behavior across authenticated pages.

Handoff evidence:

- Research and decisions: `docs/render-artifacts/topbar-navigation.md`, based on the repo's authenticated Render captures plus Render's dashboard guide and enhanced-navigation changelog.
- Main implementation: `dashboard/src/common/components/dashboard-layout/{dashboard-header,dashboard-breadcrumbs,global-search,new-resource-menu}.tsx` and the shared layout wiring in `index.tsx`.
- Localized visible and accessible copy added in both `dashboard/src/common/locales/en.ts` and `zh.ts`.
- Focused coverage lives in `dashboard-topbar.test.tsx`; existing service and Key Value route tests were adjusted for the new persistent header context.
- Final verification passed: `yarn lint`, all 253 dashboard test files / 1,570 tests, `yarn build`, `git diff --check`, and desktop/mobile browser checks against a local production-shaped stub.

Before shipping, review the uncommitted diff as one dashboard-navigation change and rerun the dashboard gates if it has been rebased. Resource search hooks intentionally mount only while the command dialog is open, so the persistent header adds no ordinary page-load queries. No REST, GraphQL, MCP, operator, or backend contract changed. Do not commit or push except through `$ship`.
