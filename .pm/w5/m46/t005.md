---
id: w5/m46/t005
title: Simplify the PTY and pgcli verification code
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m46/t004]
---

## Objective

Run a behavior-preserving simplification pass over the PTY driver and `pgcli` orchestration so the security boundaries, timeout behavior, and cleanup paths remain obvious and maintainable.

## Context

- Generic PTY mechanics should not be duplicated in Postgres-specific shell code.
- Secret redaction, child termination, and cleanup are safety behavior, not optional verbosity to refactor away.
- `scripts/ssh-verify.sh` is a useful precedent, but this milestone should share only genuinely reusable mechanics rather than entangling SSH and Postgres fixtures.

## Steps

1. Review the milestone diff for duplicated process control, marker parsing, temp lifecycle, and command validation.
2. Consolidate behaviorally identical code while keeping secret boundaries and failure messages explicit.
3. Remove unused modes and over-general abstractions; rerun focused tests after each accepted change.

## Files

- `scripts/render-cli-pty.py`
- `scripts/pgcli-compat-verify.sh`
- `scripts/pgcli-verify-client.sh`
- `scripts/cli-compat.sh`

## Acceptance criteria

- [ ] One clear PTY implementation owns terminal allocation, timeout, and child teardown.
- [ ] Postgres-specific code owns fixture, URI-contract, SQL-marker, and cleanup behavior without leaking secrets.
- [ ] All focused tests and the live verification remain green after simplification.

## Out of scope

- Refactoring unrelated CLI, SSH, or database verification scripts.
