---
id: w5/m17/t005
title: Render parity — wizard vocabulary/flow vs the t001 captures + cross-surface create consistency; ledger Static-site UI cell ◐→✅
worker: worker5
status: todo
estimate: 25m
depends_on: [w5/m17/t004]
---

## Objective

Verify the shipped wizard against render.com, not against intentions: side-by-side the five type flows with the t001 captures, confirm the UI creates exactly what REST/GraphQL/MCP create for the same inputs (same defaults, same required fields), and update the parity ledger with evidence.

## Context

- Baseline: the t001 `docs/render-artifacts/` capture — per user instruction at materialization ("ensure they are render.com consistent"), this comparison is binding: any mismatch is either fixed or documented as deliberate divergence in the ledger row, never silent.
- Cross-surface check: `docs/ADR006-bex-api.md` (one core, thin adapters) — the wizard must not invent defaults the other adapters don't apply.
- Ledger rows to update: "Static site" (UI ◐ → ✅ with evidence), "Create service (web / private)" UI evidence, and the cron-job row's create note — `docs/ADR018-render-parity.md`.

## Steps

1. Walk each type's create flow in the bex dashboard next to the t001 capture; list every field/label/default/validation mismatch.
2. For one service per type, diff the resulting App spec against an equivalent REST create — defaults and required-ness must match.
3. Fix trivial drift; file non-trivial drift as follow-up inbox notes; update the ledger rows with evidence pointers.
4. `npx prettier@3.4.2 --write "**/*.md"` after doc edits.

## Files

- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/` (evidence refresh)

## Acceptance criteria

- [ ] Ledger Static-site row UI cell reads ✅ with an evidence pointer
- [ ] Every remaining divergence vs the capture is written down (ledger or follow-up note) — none silent
- [ ] UI-created and REST-created services of the same type produce equivalent specs

## Out of scope

- New feature work discovered during comparison (file notes instead)
