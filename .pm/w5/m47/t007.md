---
id: w5/m47/t007
title: Closeout
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m47/t006]
---

## Objective

Close the milestone once its definition of done actually holds: cron (and every service type) is deep-linkable and first-class in the create flow, with passing gates.

## Context

- Only close when the DoD in the milestone README is real — a Render `/cron/new` deep link lands on a cron-preselected wizard, a first-class "Cron Job" New entry exists, and `yarn typecheck && yarn lint && yarn test` pass.

## Steps

1. Verify the DoD end-to-end (ideally a quick browser check via `yarn dev:local`).
2. Run `/pm done w5/m47/t007` last — set all remaining tasks `status: done`, move each `tNNN.md` to `w5/m47/done/`, mark rows `— **DONE**` + `**Status:** done`, move the whole `w5/m47/` dir to `w5/done/m47/`, and check `- [x]` in the w5 README.

## Files

- `.pm/w5/m47/**`, `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD verified real (not just code-written).
- [ ] Milestone moved to `w5/done/m47/` and the w5 README box checked.

## Out of scope

- Any new feature work.
