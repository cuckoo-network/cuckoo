---
id: w5/m31/t003
title: "Env-group membership dialog (envGroupIds full-replace)"
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Let a user manage which env groups belong to an environment from the environment card, mirroring `manage-resources-dialog.tsx` (checklist + full-replace save) over the w6/m24 `envGroupIds` membership verbs.

## Context

- `dashboard/src/features/environments/manage-resources-dialog.tsx` + `resource-checklist.tsx` — the exact pattern to extend/reuse.
- Backend membership verbs shipped in w6/m24 (same-workspace, full-replace).

## Files

- `dashboard/src/features/environments/` (+ env-groups list query reuse from `features/env-groups`)

## Acceptance criteria

- [ ] Membership editable; save round-trips; cross-workspace groups never offered; tests.

## Out of scope

- Env-group content editing (exists on the env-groups page).
