---
id: w5/m31/t005
title: "Project/Environment selectors on the Postgres + Key Value create pages"
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Add the Project/Environment selector to the managed Postgres and Key Value create paths, mirroring the service wizard's selector (w2/m37). The REST/GraphQL/MCP create inputs already accept `environmentId` and auto-join the parent Project — this is purely the dashboard create-path gap (`w5/014`).

## Context

- Service-wizard selector precedent: `dashboard/src/routes/services.new.tsx` (the w2/m37 `environmentId` wiring) — reuse its hook/component.
- KV create: `dashboard/src/routes/keyvalue.new.tsx`; Postgres create lives on the databases page's create flow (no `databases.new.tsx` route — locate the create form/dialog).

## Files

- `dashboard/src/routes/keyvalue.new.tsx`, the Postgres create form, shared selector component

## Acceptance criteria

- [ ] Creating either datastore with an environment selected lands it in that environment (verify via the environment card's resource list); optional (empty = none); tests.

## Out of scope

- Moving existing datastores between environments (exists via manage-resources).
