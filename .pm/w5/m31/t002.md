---
id: w5/m31/t002
title: "ACL editor on the environment card"
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m31/t001]
---

## Objective

Make `protectedStatus` (toggle), `networkIsolationEnabled` (toggle), and `ipAllowList` (add/remove CIDR entries) editable from the environment card, calling the shipped `setEnvironmentACL` GraphQL mutation (full-replace semantics — send the complete ACL each time).

## Context

- `dashboard/src/features/environments/` — `environment-card.tsx`, `environments-panel.tsx`; the fields already flow through `environments.graphql`'s `EnvironmentFields`.
- Backend: w6/m19 (`SetACL`); coordinate with w4/m23/t004's wire-shape change if it lands first (GraphQL core stays string-list, so likely no dashboard impact — verify).

## Files

- `dashboard/src/features/environments/` (new `acl-section`/dialog component + hook + `.graphql` mutation file, the feature's inline-edit pattern)

## Acceptance criteria

- [ ] All three ACL fields editable and persisted (re-read shows the change); locale strings in `en`+`zh`; component tests.

## Out of scope

- The membership dialog (t003); enforcement UX on destructive verbs (t004).
