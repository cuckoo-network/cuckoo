---
id: w5/m90/t003
title: "Expose consistent percentage semantics through API adapters and hooks"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m90/t002]
---

## Objective

Make clients consume the shared corrected metric semantics.

## Context

- See [milestone scope and evidence](README.md); this corrects a specific incomplete m89 criterion.
- Use isolated dev-5 for live verification and read cascading instructions before implementation.

## Steps

1. Inspect existing REST/GraphQL/MCP percentage support and preserve compatible defaults.
2. Wire the supported normalized result through thin adapters and the dashboard query/hook; generate schema types using repository conventions if needed.
3. Remove the dashboard’s single aggregate-denominator calculation and verify units and error mapping.

## Files

- `lego/backend/internal/metrics/rest.go`
- `lego/backend/internal/metrics/graphql.go`
- `lego/backend/internal/metrics/mcp.go`
- `dashboard/src/features/metrics/api/metrics.graphql`
- `dashboard/src/features/metrics/hooks/use-metrics.ts`
- `dashboard/src/features/metrics/components/application-metrics-card.tsx`

## Acceptance criteria

- [ ] All exposed adapters agree on normalized values and missing-denominator behavior.
- [ ] The dashboard does not independently calculate replica aggregation or divide by one latest aggregate limit.

## Out of scope

- External metric drains, billing/metering changes, a chart-renderer rewrite, and unrelated telemetry work.
- Instance-selection persistence belongs to w5/m91.
