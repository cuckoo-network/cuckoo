---
id: w5/m90/t008
title: "Closeout"
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m90/t007]
---

## Objective

Close the milestone only after every acceptance criterion and live evidence gate holds.

## Context

- See [milestone scope and evidence](README.md); this corrects a specific incomplete m89 criterion.
- Use isolated dev-5 for live verification and read cascading instructions before implementation.

## Steps

1. Verify all prior tasks and the complete milestone definition of done against recorded evidence.
2. Use /pm done to synchronize task frontmatter, task-table DONE markers, and workstream status; move task files into done/.
3. Complete this task last, move the whole milestone into w5/done/, and check its workstream entry without leaving stubs.

## Files

- `.pm/w5/m90/`
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] All tasks and the milestone definition of done are actually complete; no deferred walkthrough is marked passed.
- [ ] Milestone and task moves, statuses, table markers, and workstream checkbox agree.

## Blocked 2026-09-08 (worker5 session, code complete, not closed)

t001–t007 are done and their files moved to `done/`; the README records full
test evidence. t008 cannot complete here: the dev-5 live walkthrough
(mixed-limit fixture proving selection/rollout/deleted-instance behavior, plus
desktop + narrow-mobile pending/ready captures) needs a working cluster and
browser, and neither is executable from this environment (see README Evidence).
To close in the next session with a working dev-5:

1. `bash scripts/dev-env.sh 5 up`, deploy a two-replica service with mixed CPU/memory limits, roll it mid-window, delete a replica.
2. Exercise REST (`?percentage=true&aggregateAllMethod=AVG`), GraphQL (`percentage:true`), MCP, and dashboard Percentage/Total tabs; confirm 80/50 raw, MIN/MAX/AVG 50/80/65, `Limits vary`, and the unavailable state.
3. Force pending and compare geometry with ready at desktop + narrow-mobile widths.
4. Record captures in README Evidence, check the two remaining DoD boxes, set this file to `status: done`, move it to `done/`, `mv .pm/w5/m90 .pm/w5/done/m90` (then `rmdir` the original — no tombstone), and flip the m90 workstream checkbox to `[x]`.

## Progress 2026-09-09 (cluster recovered, service-level walkthrough green)

Environment fixed: Docker Desktop bin on `PATH`, workload kubeconfig regenerated
via `clusterctl` (direct-CP port 55003), corrupt 0107 migration worked around in
dev-5's disposable `bex-db` only (repo untouched — the `N|` prefixes are still
in the committed file and will break every fresh database until repaired),
Prometheus + kube-state-metrics installed (`m90fix`/`m90prom`, left running for
the next session), dev-5 up with bex-api on `:54050`.

Proven live against a disposable `m90walk/m90mix` fixture (2 replicas,
500m/512Mi → 1CPU/1Gi → 500m/512Mi rollouts, ~0.3-core burner; fixture deleted
after): per-instance own-limit normalization (CPU 10/10, memory 18/18 points),
AVG/MIN/MAX over normalized series (7 points each), raw-name INSTANCE
selection (1 of N, unknown → empty), deleted-pod truncation without zero-fill,
and per-generation denominators (0.5 vs 1.0 in-window) — all through the real
`NewPrometheusResourceSource` + `NewPrometheusResourceLimitSource` +
`Service.Metrics`. Full output in README Evidence. Still open: authed
REST/GraphQL/MCP/dashboard checks (need a Kratos session + real bex App) and
the pending/ready captures. Reuse: re-forward with
`kubectl -n m90prom port-forward deploy/m90fix-prometheus-server 59091:9090`
(forward dies with the session); use ≥120s resolution for CPU rate windows.

## Progress 2026-09-09 (second session — authed REST/GraphQL/MCP green, gate bug fixed, dashboard captures still open)

Real bex App fixture `srv-daggflhjg4r5rcjp5af0` (`m90mix`, workspace
`tea-daggc49jg4r8hjs0vaeg`): starter → standard rollout produced a live mixed
500m/1CPU window; new RS reached 2/2 ready after adding `httpd -p 3000` to the
burner (operator startup probe); service SUSPENDED afterwards (0 pods,
retained). bex-api rebuilt from tree + restarted with
`BEX_PROM_URL=http://localhost:59091` for the walkthrough (dev-state only),
then dev-5 restored to standard via `dev-env.sh 5 up` (healthy, fix compiled
in — re-apply the manual restart + `:59091` forward next session).

Live results: REST DoD call 200 with AVG/MIN/MAX mutually consistent,
per-pod truncation, own-limit Total↔percentage agreement, INSTANCE 1-of-5,
unknown → `[]`, memory `%` AVG, GraphQL `percentage:true` + AVG, MCP
`get_metrics` percentage + AVG — all green (numbers in README Evidence).
Found + fixed a real bug: the strict Render-query gate 400d `percentage` and
`aggregateAllMethod` on all App-metrics REST paths
(`lego/backend/internal/api/render_openapi.go` + composed-server regression,
both uncommitted in tree). Still open: dashboard Percentage/Total tab checks
and desktop + narrow-mobile pending/ready captures (no browser in this
environment). Handoff creds (mode 600):
`/tmp/m90walk-email.txt`, `/tmp/m90walk-pw.txt`,
`/tmp/m90walk-session.txt`, `/tmp/m90walk-srv.txt`. t008 stays `todo`; DoD
boxes stay unchecked per the no-close-without-live-evidence rule.

## Out of scope

- External metric drains, billing/metering changes, a chart-renderer rewrite, and unrelated telemetry work.
- Instance-selection persistence belongs to w5/m91.
