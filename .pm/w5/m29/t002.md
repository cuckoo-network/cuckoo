---
id: w5/m29/t002
title: GraphQL wiring: per-deploy query + hooks
worker: worker5
status: todo
estimate: 40m
depends_on: [w5/m29/t001]
---

## Objective

Wire the page to the existing per-deploy GraphQL read (w2/m5's get-deploy surface): fragment, generated definitions, hook with polling while the deploy is non-terminal.

## Files

- `dashboard/src/features/` (deploys api/hooks), `dashboard/src/graphql/definitions.ts` (generated)

## Acceptance criteria

- [ ] Page data comes from the real query; non-terminal deploys poll and settle when terminal.
