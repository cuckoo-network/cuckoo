---
id: w5/m29/t004
title: Build-log pane over `type=build` (store-absent state)
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m29/t003]
---

## Objective

For git-sourced deploys, render the build log via the logs query with `type=build` scoped to this deploy. Store-less (503) or pre-w7/m28: an explanatory state ("build logs need the log store"), the w5/m23 pattern — never a broken pane.

## Context

- **Cross-milestone dependency: `w7/m28`.** If it hasn't landed, ship the pane with its absent-state only and note it in the closeout.

## Acceptance criteria

- [ ] With the store + m28: real build lines render; without: the explanatory state; image-backed deploys hide the pane.
