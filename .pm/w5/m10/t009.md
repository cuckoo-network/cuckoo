---
id: w5/m10/t009
title: Closeout
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m10/t008]
---

## Objective

Close the milestone once the Definition of Done actually holds: unknown ids 404 against the fixed stub, two stub services render distinctly, the add-domain flow shows the correct DNS record with copy + re-check on all four surfaces, and every check (backend `go test`, dashboard `lint`/`typecheck`/`test`) is green. Completing this task _is_ the move.

## Context

- `.pm/CLAUDE.md` conventions: set each task `status: done`, move `tNNN.md` → `w5/m10/done/`, mark rows `— **DONE**` + `**Status:** done` in the milestone README, move `w5/m10/` → `w5/done/m10/`, check the box in `w5/README.md`, and move inbox note `w5/006.md` → `w5/done/006.md` (this milestone absorbs it).
- Do not run until the DoD is real — verified behavior, not merely written code.

## Steps

1. Re-verify the DoD end to end (stub + dashboard + backend surfaces).
2. Flip all task statuses/rows, move task files to `done/`, set milestone status done.
3. Move the milestone to `w5/done/m10/`, check the workstream box, retire `w5/006.md` to `w5/done/`.

## Files

- `.pm/w5/m10/**`, `.pm/w5/README.md`, `.pm/w5/006.md`

## Acceptance criteria

- [ ] DoD verified before any status flip.
- [ ] All statuses/rows/checkbox in sync; milestone dir moved whole; no tombstone left at the old path.
- [ ] `w5/006` retired to `w5/done/` with the milestone.

## Out of scope

- Any implementation.
