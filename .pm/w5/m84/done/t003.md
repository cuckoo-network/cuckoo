---
id: w5/m84/t003
title: Surface the applied rung - turn annotation + dashboard restored/fresh-context hint
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m84/t001]
---

## Objective

Every fresh-generation turn records which continuity rung applied (`session-load` / `transcript-reprime` / `task-redelivery` / `none`), and the dashboard renders an honest, unobtrusive hint — the UI never again implies conversational fidelity the agent doesn't have.

## Context

- Carrier: a `data-` part on the turn (ADR047 D3 amendment) riding the existing typed-parts mapping (`acp-map.ts`/`session.ts`) — byte-transparent through gateway/tee per the verbatim-forward invariants.
- Dashboard: agent-session view (`dashboard/src/features/agent-sessions/`) renders the annotation as a small system line (e.g. "resumed with restored context" / "agent restarted — context rebuilt from history"). Skeleton/pending-state rule applies if any layout changes.

## Acceptance criteria

- [ ] Annotation present on fresh-generation turns, absent on same-generation turns.
- [ ] Dashboard shows the hint in both states; `yarn test` green.

## Out of scope

- Any REST/GraphQL/MCP schema change.
