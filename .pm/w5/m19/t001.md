---
id: w5/m19/t001
title: "Root-cause confirmation: trace SSR routes/loaders using getClient()"
worker: worker5
status: todo
estimate: 20m
depends_on: []
---

## Objective

Confirm the scope of the bug before fixing it: enumerate every SSR-rendered route/loader that depends on `getClient()` (`dashboard/src/common/apollo/client.ts`'s isomorphic `.server(createApolloSsrClient)`), and confirm each is silently receiving an unauthenticated response today (no `Authorization` header ever reaches bex-api server-side, since `bex-dashboard-token` is never set).

## Context

- `dashboard/src/common/apollo/client.ts` — `getClient()`, the isomorphic entry point.
- `dashboard/src/common/apollo/factory.server.ts:33-44` — the broken SSR client.
- `dashboard/src/common/apollo/factory.client.ts` — the working client-side reference (`credentials: "include"`).
- `dashboard/src/routes/__root.tsx` — likely the primary SSR data-fetch point; check for others.

## Steps

1. Grep for `getClient()` usage and `useSuspenseQuery`/loader-level Apollo calls across `dashboard/src/routes/`.
2. For each hit, confirm (via logging or a manual curl replicating the SSR request with no `Authorization` header) that bex-api returns an unauthenticated/empty response.
3. Note any route whose behavior depends on this (e.g. auth-gate redirects) for t003.

## Files

- `dashboard/src/common/apollo/client.ts`
- `dashboard/src/routes/` (read-only survey)

## Acceptance criteria

- [ ] A written list of every SSR-dependent route/loader and its current (broken) auth behavior.

## Out of scope

- The fix itself — t002.
