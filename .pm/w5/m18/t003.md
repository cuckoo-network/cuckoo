---
id: w5/m18/t003
title: "Dashboard: cron-deploy-section editable — edit-in-place Schedule + Command, cron validation, save + convergence messaging"
worker: worker5
status: todo
estimate: 35m
depends_on: [w5/m18/t001]
---

## Objective

Turn the read-only cron Deploy section into an editable one: Schedule and Command get edit-in-place controls with client-side cron validation, saving calls the t001 mutation, and the UI communicates what happens next (the CronJob converges; the next run follows the new schedule) — matching Render's cron Deploy-section editing behavior.

## Context

- Component: `dashboard/src/features/services/components/cron-deploy-section.tsx` — carries the explicit "read-only for now (the write path is a follow-on)" comment; remove it when the write path lands.
- Editing pattern precedent: Build & Deploy's Root Directory edit (w5/m13, `build-deploy-section.tsx`) — same save/cancel/error idiom, reuse it.
- Compare against Render's cron Deploy section live (Playwright capture if the w5/m11 artifacts don't already show edit mode) — labels, validation messages, save affordance; capture → `docs/render-artifacts/` if new.
- Client cron validation must not be stricter than the backend's (a value the backend accepts must not be blocked client-side).
- i18n: en + zh strings.

## Steps

1. Capture/confirm Render's cron edit affordances (reuse w5/m11 artifacts where sufficient).
2. Add the GraphQL mutation document + hook (`features/services/api/`, `hooks/`), regenerate types.
3. Make Schedule + Command editable with validation, save, error, and convergence messaging; update the Apollo cache with the mutation result.
4. Locale strings (en + zh); remove the read-only deferral comment.

## Files

- `dashboard/src/features/services/components/cron-deploy-section.tsx`
- `dashboard/src/features/services/api/` (new mutation document)
- `dashboard/src/features/services/hooks/` (new hook)
- locale files (en/zh)

## Acceptance criteria

- [ ] Editing schedule/command and saving updates the service; UI reflects the new values from the mutation result
- [ ] Invalid cron expression blocked with an actionable message consistent with the backend's error
- [ ] Edit affordance/labels match Render's cron Deploy section (drift documented)

## Out of scope

- Build Command / Log Stream / Notifications fields (still deferred — ledger-tracked ◐)
