---
id: w5/m18/t002
title: "MCP: make update_cron_job functional (Render ships a non-functional stub) or document the omission"
worker: worker5
status: todo
estimate: 25m
depends_on: [w5/m18/t001]
---

## Objective

Decide and implement the MCP side against Render's actual behavior: Render's official MCP server ships `update_cron_job` as a non-functional stub that returns "use the dashboard/API". bex's precedent (static routes, w1/m21) is to make functional what Render stubs — do that here (a real `update_cron_job` tool updating schedule/command via the t001 core verb), or, if rejected, document the omission in the ledger row as deliberately mirroring Render's stub.

## Context

- Render MCP reference: `render-oss/render-mcp-server` v0.3.0 — `update_cron_job` is one of three non-functional update stubs (see docs/ADR018-render-parity.md §Method).
- Precedent for going functional: "bex makes functional what Render's MCP ships as a stub" (static routes/headers row).
- MCP tools live in `lego/backend/internal/apps/mcp.go` (see `run_cron_job`, `create_cron_job` for shape conventions); tool args mirror Render's stub's declared schema where one exists.

## Steps

1. Check the stub's declared input schema in render-mcp-server; mirror its arg names.
2. Implement `update_cron_job` delegating to the t001 core verb; same validation/error text as REST/GraphQL.
3. Register the tool; verify over `api mcp-stdio` locally.
4. If going functional is rejected during review, write the mirroring-the-stub rationale into the ledger cron row instead.

## Files

- `lego/backend/internal/apps/mcp.go`

## Acceptance criteria

- [ ] Either a functional `update_cron_job` (same semantics as REST/GraphQL, verified via stdio) or a documented omission in the ledger — never an undocumented gap
- [ ] Tool arg names match Render's stub schema where it declares one

## Out of scope

- The other two Render stub tools (`update_web_service`, `update_static_site`) — separate decisions, not this milestone
