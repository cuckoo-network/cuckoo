---
id: w5/m18/t007
title: Closeout — DoD met → move milestone to done/
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m18/t006]
---

## Objective

Close w5/m18 once the definition of done actually holds — a schedule/command edit from any surface converges the CronJob and the ledger caveat is retired — not merely when the code is written.

## Context

- Closeout procedure: `.claude/commands/pm.md` (`done` subcommand step 4).

## Steps

1. Verify the DoD (edit a live cron job's schedule from the dashboard; confirm the CronJob object changed; check the ledger row).
2. Sync statuses, move tasks to `w5/m18/done/`, set `**Status:** done`.
3. Move `w5/m18/` → `w5/done/m18/`; check `- [x]` in `w5/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m18/` → `.pm/w5/done/m18/`
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD verified before any move
- [ ] Status synced in all three places; no tombstone at the old path

## Out of scope

- New work discovered at closeout (file inbox notes)
