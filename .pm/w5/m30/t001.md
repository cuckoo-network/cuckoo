---
id: w5/m30/t001
title: `startCommand` inline-edit in Settings → Build & Deploy
worker: worker5
status: todo
estimate: 40m
depends_on: []
---

## Objective

Add a `startCommand` inline-edit row to the service Settings → Build & Deploy section, mirroring the existing `rootDir` control (pencil → input → confirm → mutation), so a start command is editable after create — not create-only as it is today.

## Context

- Pattern to copy: `dashboard/src/features/services/hooks/use-root-dir.ts` + `.../api/root-dir.graphql` + the row in `features/services/components/build-deploy-section.tsx`.
- Backend already accepts it: `w6/m21` wired `startCommand` through `updateService`/GraphQL/MCP + the operator container command. Check whether a `setStartCommand`-style mutation exists or the field rides `updateService`; reuse whatever `rootDir` uses.
- Read `dashboard/CLAUDE.md` before editing.

## Files

- `dashboard/src/features/services/` (new hook + `.graphql` + build-deploy-section row), generated `graphql/definitions.ts`

## Acceptance criteria

- [ ] Editing `startCommand` in Settings persists through save/reload in a real browser; `yarn test` green.

## Out of scope

- `dockerfilePath` (t002/t003).
