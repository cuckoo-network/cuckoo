---
id: w5/m30/t003
title: `dockerfilePath` in the create wizard's docker-runtime branch
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Add a `dockerfilePath` field to the service-create wizard's `runtime: docker` branch, beside the `startCommand` field already there, wired through the existing `createService` path — so a non-default Dockerfile path is settable at create, not only after.

## Context

- `dashboard/src/routes/services.new.tsx` (docker-runtime branch) + `use-create-service.ts` + `services.graphql`'s `createService` — `startCommand` is already threaded here; `dockerfilePath` follows the same wire.

## Files

- `dashboard/src/routes/services.new.tsx`, `dashboard/src/features/services/hooks/use-create-service.ts`, `services.graphql`, generated definitions

## Acceptance criteria

- [ ] Creating a docker-runtime service with a custom `dockerfilePath` from the wizard produces a service built from that path (verified against the mock cluster); `yarn test` green.
