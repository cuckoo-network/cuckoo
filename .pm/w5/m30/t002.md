---
id: w5/m30/t002
title: `dockerfilePath` inline-edit in the same section
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m30/t001]
---

## Objective

Add a `dockerfilePath` inline-edit row beside t001's, reusing the same pattern — the field is currently settable nowhere in the dashboard despite `runtime: docker` being a first-class option.

## Context

- Only show it where it applies (docker-runtime / Dockerfile-built services) — gate the row the way the section gates its other build-only fields; don't render it for image-backed or CNB services if that's meaningless (verify against how `runtime` drives the section).
- Backend done (`w6/m21`, `App.Spec.DockerfilePath`).

## Files

- `dashboard/src/features/services/` (hook + `.graphql` + section row), generated definitions

## Acceptance criteria

- [ ] `dockerfilePath` edits persist through save/reload; the row appears only for the applicable runtime; `yarn test` green.
