---
id: w5/m59/t003
title: Apply sub-hour fixes in place; file bounded notes for larger gaps
worker: worker5
status: todo
estimate: 60m
depends_on: [w5/m59/t002]
---

## Objective

Drain t002's gap list: implement every sub-hour fix directly in this milestone (the m57 precedent — its walk found and fixed the static "Free"-chip 404 in-milestone), and file each larger gap as a bounded w5 inbox note with the evidence pointer, so nothing from the walk is dropped.

## Context

- Sub-hour fixes are dashboard-side by default (`dashboard/src/features/{team,env-groups,usage,notifications,webhooks,blueprints}/`, routes in `dashboard/src/routes/`); a gap needing backend surface changes is automatically a note, not an in-walk fix.
- Next free w5 inbox number: `028` (scan `.pm/w5/` first — numbers to date are burned through `027`, all in `done/`).
- Notes follow the inbox template: plain terse markdown, one gap each, with the t002 evidence pointer and a rough size; bundling several tiny same-page items into one note is fine (m36 absorption precedent).
- Every fix keeps en + zh locales in sync and updates/adds component tests beside the touched components (t006 audits coverage after).

## Steps

1. Partition t002's non-match rows: sub-hour dashboard fixes vs notes.
2. Implement the fixes; verify each against its Render capture in the browser.
3. Write the inbox notes (`w5/028.md`, `029.md`, …) for the rest; run `npx prettier@3.4.2 --write "**/*.md"`.
4. Update the verdict table rows to their final state (fixed / filed / excluded).

## Files

- `dashboard/src/features/` + `dashboard/src/routes/` (fixes)
- `.pm/w5/0NN.md` (new notes)

## Acceptance criteria

- [ ] Every t002 gap row ends as: fixed (with browser verification), a filed note, or a named intentional exclusion — zero unresolved rows.
- [ ] `cd dashboard && yarn typecheck && yarn lint && yarn test` pass on the fixed tree.

## Out of scope

- Multi-hour implementations (that's what the notes are for); backend/API changes.
