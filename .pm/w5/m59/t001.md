---
id: w5/m59/t001
title: Live authenticated Render captures of the workspace-family pages
worker: worker5
status: todo
estimate: 60m
depends_on: []
---

## Objective

Capture the current Render dashboard state of every workspace-scoped page family bex mirrors — Team/members, Env Groups (list + detail), Billing/Usage, Notification settings, Webhooks (list + detail/settings), Blueprints (list + detail) — as the comparison baseline for t002.

## Context

- Method precedent: `w5/done/m32` (page-by-page walk) and `w5/done/m57` (family walk); prior captures referenced from `docs/render-artifacts/` show the expected artifact quality.
- Playwright MCP writes to `.playwright-mcp/` — pass **bare** filenames (repo rule).
- Capture both visual state and interaction affordances (menus opened, edit states, empty states where reachable); note plan-gated elements explicitly rather than skipping them.
- Known non-goals to record as exclusions, not gaps: external log/metric drains, Slack delivery, PR previews, payment-method gates on workspace create/plan-change (`.pm/DO_NOT_DO.md`).

## Steps

1. Log into dashboard.render.com with the live account; walk each family: Team, Env Groups, Billing + Usage, Notification settings, Webhooks, Blueprints.
2. Per page: full-page capture + captures of key interactions (add-member dialog, env-group editors, webhook create/settings, blueprint sync affordances); note DOM-verified section/heading order where layout matters (the m52 technique).
3. Index the captures (filename → page/interaction) in a scratch list for t002.

## Files

- `.playwright-mcp/` (captures, bare filenames)

## Acceptance criteria

- [ ] Every listed family has at least one full-page capture plus its key interaction states, indexed for t002.
- [ ] Plan-gated or account-state-gated elements are noted as such rather than silently missing.

## Out of scope

- Any bex-side work (t002); service-scoped or static-site pages (walked in m50–m57).
