---
id: w5/m15/t008
title: Test coverage — form validation, source-tab state, create error surfaces, connect prompt
worker: worker5
status: todo
estimate: 40m
depends_on: [w5/m15/t006]
---

## Objective

Standing closing task. Add meaningful dashboard tests for the wizard's behavior and failure modes.

## Context

- Highest-value coverage: name/env-key validation blocks submit with messages; switching source tabs preserves/clears the right fields; repo pick pre-fills repo+branch; no-connection and 503 render the connect prompt; `createService` error maps onto the form; duplicate-name warns; success navigates to the service route.
- Use the dashboard's existing test setup (per `dashboard/CLAUDE.md`); mock Apollo, no live backend.

## Steps

1. Component/integration tests for the flows above.
2. Dashboard test suite green.

## Files

- `dashboard/src/features/services/create/*.test.tsx`

## Acceptance criteria

- [ ] Each listed behavior has a test that fails on regression; no snapshot-everything tests

## Out of scope

- E2E cluster automation (t005 was the live gate)
