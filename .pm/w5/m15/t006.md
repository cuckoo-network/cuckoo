---
id: w5/m15/t006
title: Render parity — wizard vs Render's new-service flow; flip the create-service UI cell
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m15/t005]
---

## Objective

Standing closing task. Compare the shipped wizard against Render's `/web/new` (t005's capture): source options, field set, defaults, post-create landing. Flag drift as follow-up; update [docs/render-parity.md](../../../docs/render-parity.md) — the "Create service" row's UI cell ✖→✅/◐ with the deliberate omissions (region/runtime/build+start commands) documented, and retire the "API-first, no dashboard create wizard" note.

## Context

- The wizard is UI-only over an already-parity-verified create surface, so the sweep is UI-vs-Render plus confirming the mutation input it sends matches what REST/MCP send for the same create.

## Steps

1. Side-by-side vs the t005 capture; file drift.
2. Ledger update; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/render-parity.md`

## Acceptance criteria

- [ ] UI cell updated with evidence; omissions documented, not silent

## Out of scope

- Building the omitted fields
