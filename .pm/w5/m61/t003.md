---
id: w5/m61/t003
title: "Wire onto the Databases detail page + the Key Value detail page"
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m61/t002]
---

## Objective

Mount the shared panel on both datastore detail pages at the placement t001 pinned (per Render's datastore IA), so Logs sits naturally among the existing panels (metrics, SQL console, recovery, networking) on each page.

## Context

- Host routes: `dashboard/src/routes/databases.$databaseId.tsx`, `dashboard/src/routes/keyvalue.$keyValueId.tsx`; the pages' panel composition shows where sections mount and how they receive the resource id/name.
- Kubernetes App-name vs typed-id resolution: follow how the existing datastore panels pass identity (the w5 `022.md` lesson — typed ids resolve to the k8s name server-side; the UI passes what the operation expects).
- If t001 chose a collapsed/secondary placement (Render tabs vs bex's single-page layout), record the layout divergence for t005 instead of inventing a tab system.

## Steps

1. Mount the panel on the Databases page with the database's identity; verify against a live/stub read.
2. Same for the Key Value page.
3. Route-level tests (or extend the pages' existing tests) asserting the panel mounts and receives the right identity.

## Files

- `dashboard/src/routes/databases.$databaseId.tsx`
- `dashboard/src/routes/keyvalue.$keyValueId.tsx`
- Existing page/route tests

## Acceptance criteria

- [ ] Both pages render the Logs panel in the pinned placement, fed by the correct resource identity.
- [ ] Dashboard suite passes.

## Out of scope

- New tab routes/IA restructuring (record as divergence if Render's tabbed layout differs).
