---
id: w5/m61/t004
title: "Live dev-5 proof (real CNPG/Valkey log lines) + Render-capture comparison evidence"
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m61/t003]
---

## Objective

Prove both panels against real datastores: provision a Postgres and a Key Value on dev-5, generate log-producing activity, and verify real CNPG/Valkey lines render over range changes; capture the evidence and the storeless state for t005's comparison.

## Context

- dev-5 (`bash .pm/w5/dev-5/up.sh`) reuses the shared cluster's CNPG operator; the Databases/Key Value create flows provision real instances. Postgres logs appear on connections/errors (a bad-password attempt is a reliable line generator); Valkey logs on startup/config events.
- If dev-5 lacks Loki and t001 found the reads Loki-gated, prove the store-unavailable state on dev-5 and the populated state against prod read-only (note which side proved what — the m59-walk convention).
- Captures to `.playwright-mcp/`, bare filenames.

## Steps

1. Provision both datastores; generate activity that must produce server log lines.
2. Verify both panels render those lines; exercise range presets; capture.
3. Verify the storeless and empty states live; capture.
4. Write the evidence notes (what proved where) for t005.

## Files

- `.playwright-mcp/` (captures)

## Acceptance criteria

- [ ] Real Postgres and Valkey log lines seen in the panels, with the generating action identifiable in the lines.
- [ ] Store-unavailable and empty states observed live, not just unit-tested.

## Out of scope

- Render-side comparison verdicts (t005).
