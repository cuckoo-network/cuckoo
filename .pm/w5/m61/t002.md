---
id: w5/m61/t002
title: "Shared datastore-logs panel component (query, range, store-unavailable state, en/zh)"
worker: worker5
status: todo
estimate: 75m
depends_on: [w5/m61/t001]
---

## Objective

Build one shared datastore-logs panel component implementing t001's pinned design — log rows over a selectable range, loading/empty/error states, and the explicit store-unavailable state — parameterized by the datastore kind so both pages (t003) mount the same component against their respective read.

## Context

- Add the `DatabaseLogs` / `KeyValueLogs` operations to the owning features' `.graphql` files (`dashboard/src/features/databases/api/databases.graphql`, `dashboard/src/features/keyvalue/api/`) and splice generated types per `dashboard/CLAUDE.md`.
- Reuse from `dashboard/src/features/logs/` per t001's inventory (row rendering, range select — the m56 shared presets — and the store-unavailable presentation); the shared-panel precedent is `DatastoreMetricsPanel` (`dashboard/src/features/metrics/components/datastore-metrics-panel.tsx`, w5/m24), which already abstracts one panel over both datastore kinds.
- Locales: both features' `en.ts`/`zh.ts` (or the shared component's own locale block, following where `DatastoreMetricsPanel` put its strings).

## Steps

1. Add the two operations + a hook (one hook parameterized by kind, or two thin hooks over one core) fetching rows for a range.
2. Build the panel: header + range select, virtualized/plain row list per the service-logs idiom, loading/empty/error/store-unavailable states.
3. en + zh strings; component tests for rows-rendered, range change refetch, and each non-happy state.

## Files

- `dashboard/src/features/databases/api/databases.graphql` (+ spliced types)
- `dashboard/src/features/keyvalue/api/` (+ spliced types)
- New shared panel component (co-locate per the `DatastoreMetricsPanel` precedent)
- Feature locales `en.ts`/`zh.ts`

## Acceptance criteria

- [ ] One component serves both kinds; rows render with timestamps; range changes refetch.
- [ ] Store-unavailable, empty, and error states each render distinct explicit copy.
- [ ] `cd dashboard && yarn typecheck && yarn lint && yarn test` pass.

## Out of scope

- Page wiring (t003); SSE live tail unless t001 found read support.
