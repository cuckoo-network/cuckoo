---
id: w5/m63/t011
title: "Docs: ADR050 status update, ADR031 re-drill cadence, follow-up inbox notes"
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m63/t010]
---

## Objective

Close the documentation loop once t001–t010 ship: flip ADR050's status, extend ADR031's re-drill cadence table, and file the ADR's Follow-ups as inbox notes so they aren't lost.

## Context

- `docs/ADR050-encrypted-platform-backups.md`'s Status line currently reads "Proposed."
- `docs/ADR031-platform-data-backup.md` §Re-drill cadence has one row per store; each needs its trigger condition extended to cover "or after an age-key/credential-scoping change" per store, and t010's drill needs a row/link.
- ADR050's Follow-ups: Barman Cloud plugin SSE-C/customer-key tracking, Object Lock/WORM retention on `bex-tfstate` prefixes, an age-key rotation runbook (mirroring ADR036/ADR037).

## Steps

1. Update ADR050's Status line to `Accepted (w5/m63)` (or `Implemented`, matching whichever convention the repo uses for a just-shipped ADR at the time this task runs — check a recently-closed ADR like ADR046 for the exact phrasing) with the drill record link from t010.
2. Add/extend ADR031's §Re-drill cadence rows to mention the age-decrypt/credential-scoping dependency, and add t010's drill to §Drill records.
3. File three inbox notes for the Follow-ups (`.pm/w5/NNN.md` × 3, or another workstream if a better home exists — decide at implementation time using the same reasoning this milestone's own placement question raised).
4. Run `npx prettier@3.4.2 --write "**/*.md"` over everything touched.

## Files

- `docs/ADR050-encrypted-platform-backups.md`
- `docs/ADR031-platform-data-backup.md`
- new `.pm/*/NNN.md` inbox notes (×3)

## Acceptance criteria

- [ ] ADR050's Status line no longer says "Proposed" and links the drill evidence.
- [ ] ADR031's re-drill cadence and drill-records sections reflect this milestone's work.
- [ ] Three follow-up inbox notes exist, one per ADR050 Follow-up item.
- [ ] Prettier is clean on every touched markdown file.

## Out of scope

- Actually implementing any of the three follow-ups — this task only files them.
