---
id: w5/m63/t012
title: Simplify
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m63/t011]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, and efficiency cleanups, behavior-preserving only.

## Context

- Touches: `deploy/gitops/charts/etcd-backup/cronjob.yaml`, `deploy/gitops/charts/openbao-backup/cronjob.yaml`, `deploy/gitops/charts/barman-cloud-objectstores/*.yaml`, `lego/operator/internal/controller/keyvalue_backup.go`, `infra/wasabi/backup-*-policy.json`, `scripts/backup-s3-credentials.sh`, `scripts/restore-*.sh`.
- Three near-identical age-encrypt steps land in t002/t003/t004 across two YAML files and one Go file — check whether a shared shell snippet or Go helper is warranted without over-abstracting a three-call-site pattern.

## Steps

1. Run `/simplify` scoped to the files above.
2. Apply the fixes it identifies.

## Files

- Whatever `/simplify` flags within the files listed above.

## Acceptance criteria

- [ ] `/simplify` has run over this milestone's changed files.
- [ ] Its findings are applied or explicitly dismissed with a one-line reason.
- [ ] No behavior change from the simplification pass (same test results before/after).

## Out of scope

- New features or behavior changes — this task is quality-only.
