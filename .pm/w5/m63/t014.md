---
id: w5/m63/t014
title: Closeout
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m63/t013]
---

## Objective

Close the milestone once its definition of done is actually met, not merely once the code is written.

## Context

- Per `.pm/CLAUDE.md`'s standing closing-task convention: this task's completion **is** the move. Do not run it until every item in this milestone's README `## Definition of done` holds for real (live-verified, not just implemented).

## Steps

1. Confirm every DoD item in `.pm/w5/m63/README.md` is true against the live system, not just the code.
2. Set every task's `status: done` in its frontmatter.
3. Move each `tNNN.md` into `.pm/w5/m63/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in `.pm/w5/m63/README.md`.
5. Move the whole `.pm/w5/m63/` directory to `.pm/w5/done/m63/`.
6. Check `- [x]` for **m63** in `.pm/w5/README.md`.

## Files

- Every file under `.pm/w5/m63/`
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] `.pm/w5/m63/` no longer exists; `.pm/w5/done/m63/` does, with every task marked done.
- [ ] `.pm/w5/README.md`'s m63 checkbox is checked with a done summary line matching this milestone's actual outcome.

## Out of scope

- Any new implementation work — if the DoD isn't met, fix the gap in an earlier task first, don't close early.
