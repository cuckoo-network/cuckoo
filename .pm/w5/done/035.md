# HELD until w3/m41 lands: dashboard agent-session UI (ADR047 wave 2)

> **Promoted 2026-08-02** → `w1/m64` (relocated to w1 by user direction; phase-1 increment: list · composer · detail · steering on the synthesized `UIMessage[]` render layer, per ADR047 **D9**). The live-stream half (useChat + gateway SSE) stays gated on `w3/009`, which also now owns the D9-exposed API gaps (attach verb, per-turn history, diff stats).

ADR047 phase 2's dashboard surface — deliberately **not** materialized until the phase-1 integration milestone (w3/m41) is done (user decision, `/pm-brainstorm` 2026-08-01). Head start is allowed: the wire format is the standard Vercel AI SDK UI-message stream, so UI work can begin against a mocked stream before the gateway proxy (w3/009) ships.

Scope when promoted (~4h, a real milestone):

- Session pages: list/create (repo, branch, prompt, agent, egress allowlist), session detail.
- Live stream view on `useChat` + AI Elements over the gateway SSE (HMAC ticket from the m39 create/attach verbs); raw stream parts (`includeRawChunks`) rendered as plans/diffs/terminal output.
- Evidence + draft-PR link display (m41 result fields); steering input posts a new prompt turn.
- Honest degraded states: gateway/proxy env unset ⇒ the attach surface says so (the house 503 pattern), fire-and-forget still works.

Integration, not protocol work (ADR047 D8). Companions: `w3/009` (gateway proxy + transcripts — the live-stream dependency), `w7/023` (token metering).
