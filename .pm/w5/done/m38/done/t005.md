---
id: w5/m38/t005
title: Test coverage — meaningful tests for the shipped behavior
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m38/t003]
---

## Objective

Component/hook tests asserting the real contract: description round-trip, optionality, legacy plain-string entries rendering, and the deny-all empty state surviving the swap.

## Context

- Follow the environments feature's existing `__tests__` conventions.

## Steps

1. Review t002's tests; extend for legacy-entry + empty-state cases.

## Files

- `dashboard/src/features/environments/**/__tests__/`

## Acceptance criteria

- [x] Tests fail if descriptions stop round-tripping or the empty state regresses.

## Out of scope

- Backend tests (no backend change).

## Completion evidence

- Focused environment hook/component run: 4 files, 21 tests passed.
- Full dashboard run: 216 files, 1,279 tests passed; lint, typecheck, and the production build also passed.
- Tests cover described and description-less entries, add/edit/remove, legacy string normalization, the deny-all empty state, mutation failure, and refetch behavior.
