---
id: w5/m38/t004
title: Simplify — /simplify over the milestone's diff
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m38/t003]
---

## Objective

Run `/simplify` over the milestone's diff — particularly whether the env rule-row editor and the datastore access panel should share a component now that their shapes match.

## Steps

1. `/simplify`; apply or explicitly decline findings.

## Files

- As flagged.

## Acceptance criteria

- [x] Findings applied or declined with reasons.

## Out of scope

- Behavior changes.

## Completion evidence

- Applied one `updateEntry` helper and one functional `removeEntry` helper instead of duplicating list rewrites in both inputs and the trash action.
- Declined a shared Environment/datastore component: their data shape now matches, but the datastore owns a section-local Save action while the Environment editor saves the ACL triple in its dialog footer; abstracting both would add callback and translation plumbing without removing meaningful behavior.
