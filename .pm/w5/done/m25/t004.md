---
id: w5/m25/t004
title: "Empty/loading/error states + en/zh locale strings"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m25/t002, w5/m25/t003]
---

## Objective

Round out the feature with proper loading/empty/error UI states and full i18n coverage, matching the rest of the dashboard's conventions.

## Context

- Locale files: `dashboard/src/features/projects/locales/{en,zh}.ts` as the pattern to mirror.
- Error states should distinguish "no environments yet" (empty, with a CTA to create one) from a query error.

## Steps

1. Add `dashboard/src/features/environments/locales/en.ts` + `zh.ts` covering all new UI copy (empty state, dialogs, confirmations, toasts).
2. Wire loading skeletons for the environment sections and an error state on query failure.
3. Register the new locale namespace in `dashboard/src/i18n/index.ts`.

## Files

- `dashboard/src/features/environments/locales/en.ts`
- `dashboard/src/features/environments/locales/zh.ts`
- `dashboard/src/i18n/index.ts`

## Acceptance criteria

- [ ] No hardcoded English strings remain in the new components (lint/i18n check passes).
- [ ] A project with zero environments shows an empty state with a "Create environment" CTA, not a blank section.

## Out of scope

- Live verification (t005).
