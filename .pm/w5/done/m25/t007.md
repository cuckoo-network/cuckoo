---
id: w5/m25/t007
title: "Simplify — /simplify over the code this milestone changed"
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m25/t006]
---

## Objective

Run `/simplify` over every file this milestone touched and apply behavior-preserving cleanups (reuse, simplification, efficiency).

## Context

- Scope: everything under `dashboard/src/features/environments/`, `dashboard/src/features/projects/` diffs, and `docs/ADR018-render-parity.md`.

## Steps

1. Run `/simplify` over the diff.
2. Apply and verify fixes (`yarn test`, `yarn lint`).

## Files

- Whatever `/simplify` flags.

## Acceptance criteria

- [ ] `/simplify` run completed; findings applied or explicitly declined with reason.
- [ ] `yarn test` + `yarn lint` green after changes.

## Out of scope

- New feature work.
