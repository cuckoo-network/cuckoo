---
id: w5/m25/t008
title: "Test coverage — meaningful tests for the behavior this milestone shipped"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m25/t006]
---

## Objective

Add tests that assert real behavior and failure modes for the Environments dashboard UI — not tautological/snapshot-only coverage.

## Context

- Existing precedent: `dashboard/src/features/projects/hooks/__tests__/*.test.ts`, `dashboard/src/features/projects/components/__tests__/*.test.tsx`.

## Steps

1. Unit-test the grouping logic (t002) with fixtures covering multi-environment + ungrouped services.
2. Component-test the create/rename/delete dialogs and the assign-to-environment action, asserting the correct mutation is called with correct args and the UI reflects the optimistic/refetched state.
3. Hook-test error states (query failure, mutation failure) render the expected fallback, not a silent no-op.

## Files

- `dashboard/src/features/environments/**/__tests__/*.test.{ts,tsx}`

## Acceptance criteria

- [ ] Tests fail if the grouping logic mis-assigns a service to the wrong environment.
- [ ] Tests fail if a mutation is called with the wrong arguments (e.g. wrong environment id).
- [ ] `yarn test` green.

## Out of scope

- E2E/Playwright automation (t005 covers manual/live verification for this milestone).
