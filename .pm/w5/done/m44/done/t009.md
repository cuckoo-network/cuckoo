---
id: w5/m44/t009
title: Render parity check + durable evidence + ADR018 surface audit
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m44/t008]
---

## Objective

Run the standing user-surface parity check: compare the shipped page against the same live Render service, verify REST/GraphQL/MCP share the Core batch semantics, and update the durable evidence and parity ledger without erasing deliberate divergences.

## Context

- Evidence starts in `docs/render-artifacts/service-environment-page.md`.
- ADR018 currently says env-var and secret-file writes roll pods immediately; update those claims only where the new mode actually changes behavior.
- Environment-group writes may still roll linked services immediately; do not falsely close that separate divergence.
- Render's `Datastore URL` remains excluded by `.pm/DO_NOT_DO.md` and must stay documented as drift.

## Steps

1. Re-walk Render and bex desktop/mobile states, save menus, error paths, and group create/link.
2. Verify equivalent batch validation, authorization, redaction, save mode, and error shapes on REST/GraphQL/MCP.
3. Update the evidence artifact with shipped screenshots/observations and accepted drift.
4. Audit and update ADR018 rows for env vars, secret files, and environment groups only where evidence supports it.

## Files

- `docs/render-artifacts/service-environment-page.md`
- `docs/ADR018-render-parity.md`
- `docs/ADR006-bex-api.md` if the batch extension needs documentation
- REST/GraphQL/MCP conformance tests under `lego/backend/internal/`

## Acceptance criteria

- [ ] Live comparison covers view/edit/import/export/generate/upload/save/cancel/group/mobile states.
- [ ] Cross-surface tests prove one Core contract and leak-safe errors.
- [ ] ADR018 contains no stale immediate-roll or UI claims and retains honest group/Datastore URL drift.
- [ ] No anti-goal is silently reopened.

## Out of scope

- Implementing drift discovered during this closing check; file bounded follow-up work instead.
