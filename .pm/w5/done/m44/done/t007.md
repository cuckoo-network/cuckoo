---
id: w5/m44/t007
title: Linked Environment Groups section + complete create/link path
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m44/t001]
---

## Objective

Match Render's service-side group UX: title the card `Linked Environment Groups`, distinguish linked groups from available candidates, expose link/unlink only, and route both page-level and empty-state New Environment Group actions through bex's complete create dialog with the current service preselected.

## Context

- Current service card lists every workspace group with Link/Unlink plus workspace-destructive Delete and an inline name-only create form.
- The complete `NewEnvGroupDialog` already accepts initial vars, files, and service links on `/env-groups`; reuse it rather than maintaining a second incomplete creator.
- Workspace-level edit/delete remain on the env-group list/detail pages. Service-local values continue to win on key collision.

## Steps

1. Add the Environment page heading and Create environment group action.
2. Rework the card into linked and available states with clear counts/content previews and no delete action.
3. Reuse/extend `NewEnvGroupDialog` so creation can preselect the current service and returns to a linked, refetched state.
4. Preserve forbidden/unavailable/empty states and direct links to group detail/management.

## Files

- `dashboard/src/routes/services.$serviceId.env.tsx`
- `dashboard/src/features/services/components/env-groups-panel.tsx`
- `dashboard/src/features/env-groups/components/new-env-group-dialog.tsx`
- `dashboard/src/features/env-groups/hooks/use-env-groups.ts`
- `dashboard/src/features/{services,env-groups}/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] Service page deletion of a workspace group is impossible; link/unlink remain available.
- [ ] Empty state says whether there are no linked or no available groups and offers the correct next action.
- [ ] Creating a populated group with the current service selected returns to the service page linked in one mutation.
- [ ] Existing workspace env-group create/edit/delete flows remain unchanged.

## Out of scope

- Changing group precedence or group write rollout semantics.
