---
id: w5/m65/t003
title: "dashboard: remove the Evidence side panel + inline PrCard"
worker: worker5
status: done
estimate: 40m
depends_on: []
---

## Objective

The web session detail page is chat-only: delete the Evidence Sheet + its header toggle and the inline draft-PR card. The header's existing `#N` draft-PR badge stays as the sole PR affordance for opted-in sessions.

## Context

- Route: `dashboard/src/routes/agents_.$agentSessionId.tsx` — `EvidenceSidePanel`, the `evidenceOpen` state, and the `onOpenEvidence` prop threading.
- `session-detail-header.tsx` — the Evidence toggle button (~line 156) and `onOpenEvidence` prop; **keep** the inline `#N` PR badge.
- `session-chat-column.tsx` — the `PrCard` in the transcript footer and the `onOpenEvidence` prop.
- Delete: `components/evidence-panel.tsx`, `components/pr-card.tsx`, `components/__tests__/evidence-panel.test.tsx`; update `__tests__/session-chat-column.test.tsx`.
- Client data layer: drop the `evidence` selection from the agent-session GraphQL operation and the mapper/types (`lib/mapper.ts` `toEvidenceView`, `types.ts` `AgentSessionEvidenceView`) — nothing consumes it after PrCard goes. The **server** wire field stays (do not touch backend schema). Reconcile `src/graphql/definitions.ts` per the hand-splice rule in `dashboard/CLAUDE.md` (offline codegen; splice per-feature, never wholesale).
- Locale keys: remove now-unused `agentSessions.evidence*` / `agentSessions.pr*` keys from `features/agent-sessions/locales/{en,zh}.ts` — but keep any key the header badge or failure callout still uses (the locale-parity test will catch drift).
- The `local-bex.mjs` dev stub serves `evidence`/`testOutput` fixtures — trim if the client operation no longer requests them.

## Steps

1. Remove `EvidenceSidePanel` + state from the route; remove the toggle from the header; remove `PrCard` + `onOpenEvidence` from the chat column.
2. Delete `evidence-panel.tsx`, `pr-card.tsx`, and their tests; fix touched tests.
3. Drop `evidence` from the client GraphQL operation, mapper, and types; reconcile `definitions.ts` offline.
4. Prune unused locale keys (en + zh).

## Files

- `dashboard/src/routes/agents_.$agentSessionId.tsx`
- `dashboard/src/features/agent-sessions/components/{session-detail-header,session-chat-column,evidence-panel,pr-card}.tsx` (+ `__tests__/`)
- `dashboard/src/features/agent-sessions/{lib/mapper.ts,types.ts,locales/en.ts,locales/zh.ts}`
- `dashboard/src/graphql/definitions.ts`, `dashboard/scripts/local-bex.mjs`

## Acceptance criteria

- [ ] No Evidence UI (panel, toggle) and no PrCard render on the session detail page; the header `#N` badge still renders for a session with `prNumber`.
- [ ] `grep -ri "evidence" dashboard/src/features/agent-sessions` returns no component/consumer hits (locale/tests cleaned).
- [ ] `cd dashboard && yarn typecheck && yarn lint && yarn test` passes.

## Out of scope

- Any replacement PR affordance in the chat (follow-up note if wanted); mobile (t005).
