---
id: w5/m65/t008
title: "Test coverage for the shipped behavior"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m65/t006]
---

## Objective

Meaningful tests for the milestone's behavior and failure modes — not coverage games.

## Steps

1. Completer: opted-out + pushed ⇒ completed with `headSha`, no `OpenDraftPullRequest` call; opted-out + nothing pushed ⇒ unchanged no-op path; opted-in ⇒ PR opened, body free of evidence sections; opted-in with no git connection ⇒ still fails with the existing reason.
2. Create surfaces: `openPr` default false and round-trips on REST/GraphQL/MCP.
3. Dashboard: composer default vs toggled wire payload; detail page renders no evidence/PrCard while the header `#N` badge renders for a PR-bearing session.
4. Mobile: detail screen snapshot/logic test asserting the Evidence section is gone.

## Acceptance criteria

- [ ] Backend `go test ./...`, dashboard `yarn test`, and mobile suite pass with the new assertions.
