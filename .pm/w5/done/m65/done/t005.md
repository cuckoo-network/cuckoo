---
id: w5/m65/t005
title: "mobile: remove the Evidence section from the session detail screen"
worker: worker5
status: done
estimate: 30m
depends_on: []
---

## Objective

The mobile session detail screen drops its Evidence rendering, matching the web (the transcript is the record). Marked "可选" by the user — do it; it is small and leaving one surface showing the garbage defeats the point.

## Context

- `mobile/src/features/agent-sessions/detail/evidence.ts` + `__tests__/evidence.test.ts` — the mobile evidence view model.
- `mobile/src/features/agent-sessions/detail/session-detail-screen.tsx` — renders the section.
- `mobile/src/translations/{en,zh}.ts` reference `testOutput`/evidence keys; `mobile/src/generated-graphql/index.ts` carries the evidence selection — drop it from the mobile query and regenerate/splice consistently with how mobile manages generated types; `mobile/src/__tests__/mobile-scope-policy.test.ts` mentions `testOutput` — update if it asserts on the evidence surface.
- Keep any mobile PR affordance as-is (supervision-first, ADR048); this task is Evidence-only.

## Steps

1. Remove the Evidence section from `session-detail-screen.tsx`; delete `evidence.ts` + its test.
2. Drop the evidence selection from the mobile GraphQL query/generated types; prune translations.
3. Run the mobile test suite.

## Files

- `mobile/src/features/agent-sessions/detail/{evidence.ts,session-detail-screen.tsx,__tests__/}`
- `mobile/src/translations/{en,zh}.ts`, `mobile/src/generated-graphql/index.ts`, `mobile/src/__tests__/mobile-scope-policy.test.ts`

## Acceptance criteria

- [ ] No Evidence section renders on the mobile session detail screen; `grep -ri "evidence" mobile/src/features/agent-sessions` returns no consumer hits.
- [ ] Mobile test suite passes.

## Out of scope

- Mobile PR affordances; a mobile composer `openPr` control (mobile creation UX, if any, is a follow-up).
