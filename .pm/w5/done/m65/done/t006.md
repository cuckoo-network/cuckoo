---
id: w5/m65/t006
title: "Render parity (cross-surface consistency: REST/GraphQL/MCP/web/mobile)"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m65/t002, w5/m65/t004, w5/m65/t005]
---

## Objective

Verify the milestone's changes land consistently across every surface they expose. Render has no cloud coding-agent product (agent sessions are a bex extension, ADR047), so there is no render.com behavior to compare — this pass is the cross-surface half of the standing task.

## Steps

1. `openPr`: same name, default, and semantics on REST, GraphQL, and MCP create; error shapes consistent; list/get responses still carry `prUrl`/`prNumber`/`evidence` unchanged (wire compatibility).
2. Evidence removal: no UI consumer left on web or mobile; the wire field still serves data (spot-check GraphQL/REST reads).
3. Docs: ADR047 D8 reflects opt-in delivery; flag any drift found as follow-up inbox notes rather than expanding this milestone.

## Files

- `lego/backend/internal/agentsessions/{rest.go,graphql.go,mcp.go}` (read), `docs/ADR047-cloud-coding-agent-sessions.md`

## Acceptance criteria

- [ ] The three API surfaces accept/serve identical `openPr` semantics; wire `evidence` intact.
- [ ] Any discovered drift is filed as a `w5/NNN.md` note.
