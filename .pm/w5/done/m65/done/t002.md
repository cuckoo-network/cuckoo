---
id: w5/m65/t002
title: "bex-api: strip the evidence sections from the draft-PR body"
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m65/t001]
---

## Objective

An opted-in session's draft-PR body carries only the session metadata — session id, branch, head SHA, and the API link. Drop the evidence-derived sections: **Changed files**, **Commands run**, **Test output (tail)**, and the truncation note (the PR's own diff shows the files; the "test output" is misclassified tool output).

## Context

- `prBody()` in `lego/backend/internal/agentsessions/completion.go` (~line 334) builds the body; the evidence sections are lines ~345–363.
- After this, `prBody` no longer needs the `Evidence` parameter — simplify the signature.

## Steps

1. Reduce `prBody` to the metadata block (opened-by line, Session/Branch/Head, API link); remove the `ev Evidence` parameter and its call-site argument.
2. Update/extend the PR-body test to assert the evidence sections are absent.

## Files

- `lego/backend/internal/agentsessions/{completion.go,completion_test.go}`

## Acceptance criteria

- [ ] A generated PR body contains no "Changed files", "Commands run", "Test output", or "Evidence truncated" text (asserted in a test).
- [ ] Session id, branch, head SHA, and API link remain.
- [ ] `cd lego/backend && go test ./...` passes.

## Out of scope

- Removing the evidence harvest/storage or the wire field (stays per user direction).
