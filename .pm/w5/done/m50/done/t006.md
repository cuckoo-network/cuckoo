---
id: w5/m50/t006
title: Simplify — run /simplify over the changed code
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m50/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (the shared row component and every migrated row/section) for reuse, simplification, and efficiency cleanups; behavior-preserving only.

## Context

- The migration should delete substantial bespoke edit-state code from each row — verify nothing vestigial remains (unused `editing` state, dead icon imports, duplicated dirty checks).

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; re-run tests.

## Files

- Files changed in t001–t004.

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; suite green.

## Out of scope

- Bug hunting (that's /code-review territory).
