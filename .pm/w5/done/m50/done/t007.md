---
id: w5/m50/t007
title: Test coverage — behavior tests for the shared row + migrated fields
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m50/t005]
---

## Objective

Add meaningful behavior tests for what this milestone shipped: the shared row's state machine (disabled → edit → dirty-gated save / cancel / escape / confirm-hook abort) and at least one migrated instance of each shape (text row, select row, confirm-dialog row). Tests assert real interaction outcomes — no snapshot-everything, no tautologies.

## Context

- Test layout precedent: `dashboard/src/features/services/components/__tests__/`.
- The dashboard suite runs via `yarn test` (CI-enforced by deploy.yml).

## Steps

1. Cover the shared component's full state machine including focus behavior.
2. Cover one representative migrated row per shape, asserting the mutation is called with the draft value only after confirm.
3. Cover the "Save disabled until dirty" and "Cancel restores" paths at the row-integration level.

## Files

- `dashboard/src/features/services/components/__tests__/` (new/updated tests)

## Acceptance criteria

- [ ] New tests fail if the dirty gate, cancel-restore, or confirm ordering regress.
- [ ] Full dashboard suite green.

## Out of scope

- E2E browser automation (dev-5 walk lives in t005/t008).
