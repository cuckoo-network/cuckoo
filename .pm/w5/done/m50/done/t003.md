---
id: w5/m50/t003
title: Migrate Build & Deploy text rows (Branch, Root Dir, commands, Dockerfile Path, publish path)
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m50/t001]
---

## Objective

Migrate every text-editable row in the Build & Deploy section (and the static-site section where shared) to the shared editable-field row: Branch, Root Directory, Start/Docker Command, Pre-Deploy Command, Build Command (static sites today; all types after w5/m51), Dockerfile Path, and the static-site publish path. Rows that trigger a rebuild keep their confirm dialog via the row's confirm hook.

## Context

- `dashboard/src/features/services/components/build-deploy-section.tsx` — hosts most rows; each currently swaps text for input on edit.
- `dashboard/src/features/services/components/static-site-section.tsx` — publish path + static Build Command (w7/m41); already carries root-dir prefixes from w5/m48 — reuse that as the prefix-slot precedent.
- Hooks unchanged: `use-branch.ts`, `use-root-dir.ts`, `use-start-command.ts`, `use-pre-deploy-command.ts`, `use-build-command.ts`, `use-dockerfile-path.ts`, `use-static-site.ts`.
- Root Directory and Branch edits trigger rebuild confirmations today — preserve exactly.

## Steps

1. Replace each row's bespoke edit state with `EditableFieldRow`, threading existing validation and confirm dialogs through the confirm hook.
2. Keep the "Docker Command" label swap for docker-runtime services (w5/m30) and the Optional badges (Root Directory, Pre-Deploy).
3. Update Build Filters only if its Add-path inputs conflict visually; the list editor itself is out of scope.
4. Update tests for each migrated row.

## Files

- `dashboard/src/features/services/components/build-deploy-section.tsx`
- `dashboard/src/features/services/components/static-site-section.tsx`
- matching files under `__tests__/`

## Acceptance criteria

- [ ] Every listed field renders as a disabled input + pencil; edit/cancel/save-dirty flow per t001.
- [ ] Rebuild-confirm dialogs still appear for the fields that had them, before any mutation fires.
- [ ] Docker vs native label/row gating unchanged; cron/static type gating unchanged.
- [ ] Suite green.

## Out of scope

- Un-gating Build Command for non-static types (w5/m51).
- Build Filters list editor redesign; Source row editing (w5/m54).
