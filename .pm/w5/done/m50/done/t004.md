---
id: w5/m50/t004
title: Migrate remaining rows (Health Check Path, cron Schedule/Command, Notifications select)
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m50/t002, w5/m50/t003]
---

## Objective

Finish the migration: Health Check Path, the cron job's Schedule and Command rows, and the Notifications override (the first select-shaped row, using t001's select variant). Then sweep the settings route for any leftover text-plus-Edit rows and remove now-dead bespoke edit scaffolding.

## Context

- `dashboard/src/features/services/components/health-check-path-row.tsx` (w5/m22; default "/", web/private only).
- `dashboard/src/features/services/components/cron-deploy-section.tsx` — Schedule (5-field crontab validation) + Command (w5/m18).
- `dashboard/src/features/services/components/service-notifications-row.tsx` — combobox over default/none/failure/all (w4/m21); becomes a disabled select + pencil like Render's `notificationsToSend` row.
- The settings route composes everything: `dashboard/src/routes/services.$serviceId.settings.tsx`.

## Steps

1. Migrate the three row groups to `EditableFieldRow` (text/select variants).
2. Grep the settings route's component tree for remaining `Pencil`-plus-plain-text rows; migrate or explicitly record why a row stays (switches stay switches).
3. Update tests.

## Files

- `dashboard/src/features/services/components/health-check-path-row.tsx`
- `dashboard/src/features/services/components/cron-deploy-section.tsx`
- `dashboard/src/features/services/components/service-notifications-row.tsx`
- matching files under `__tests__/`

## Acceptance criteria

- [ ] All three row groups follow the t001 pattern (select variant for Notifications).
- [ ] Crontab validation errors still block save; schedule preview text unchanged.
- [ ] No settings row on any service type renders value-as-plain-text with a separate Edit button (verified via a dev-5 walk of web + static + cron settings).
- [ ] Suite green.

## Out of scope

- Auto-Deploy select row (w5/m53 — it lands on the same select variant).
- Toggle-shaped rows (maintenance switch, subdomain switch) — Render keeps these as switches too.
