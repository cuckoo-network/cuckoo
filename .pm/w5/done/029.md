# 029 — Live browser walk for w5/m60 dashboard dead-ends (deferred proof)

**Filed:** 2026-07-30, during w5/m60 closeout. **Type:** deferred verification (not a code gap).

> **Update (w1/043, 2026-07-31): the substrate block is CLEARED.** The shared CAPD cluster was recreated and dev-5 raised with `status.sh`'s verification inventory **ALL GREEN**. The walk below is now runnable locally (start the dashboard with the command `up.sh` prints, then execute the steps).

> **Update (w5/m62, 2026-07-30):** the block is now precisely characterized and the dev-5 tooling is repaired to run this walk automatically once the substrate is healthy. `dev-5/up.sh` is self-sufficient (self-installs CNPG, adds Loki, fail-fast preflight incl. a default-StorageClass check) and `status.sh` asserts a verification inventory. The remaining block is **not** dev-5's scripts but the shared CAPD `bex` cluster's degradation (no default StorageClass, CNPG-1.30 webhook crash on kind, node scheduling at max) — a `scripts/mock-cluster.sh` reprovision. **To run this walk:** reprovision the cluster, `bash .pm/w5/dev-5/up.sh`, confirm `bash .pm/w5/dev-5/status.sh` inventory is all-green, then execute the steps below.

w5/m60 shipped three dashboard closures over already-shipped, tested backend verbs — fully verified by the dashboard suite (typecheck + lint + 1,705 tests). The one DoD item that could **not** be exercised in-session is the **live browser walk** (t005): drive each closure end-to-end against a live stack. Same infra block as note `028` — `dev-5` couldn't be raised (shared kind cluster missing the CNPG `postgresql.cnpg.io/v1` CRDs), no authenticated prod/Render session available.

**To close this note (post-ship, ~20m):** on the deployed dashboard, for a **cron-job** service:

1. **Trigger Run** — the runs panel's Trigger Run button, confirm → a new run appears in history with live status; trigger again while it's active → the backend's `ForbidConcurrent` rejection shows inline; the button is disabled while a run is active.
2. **Run detail** — expand a history row → status, absolute start/finish, duration, and run id load via `cronJobRun`.
3. **Notify-on-fail override** (already shipped) — Settings → Notifications → change the four-state select (`Use workspace default | All | Only failures | None`) and confirm it round-trips on reload.
4. **Registry-credential edit** — Settings → Registry Credentials → Edit a credential: rename + change username saves (blank token keeps the secret); enter a token to rotate; host is read-only; the stored token is never shown.

Capture screenshots to `.playwright-mcp/` and note results in `cron-runs.md` / `notify-on-fail.md`, then move this note to `done/`.

## Progress (2026-08-08) — substrate rebuilt, cron walk parts 1–2 verified live

The "dev-5 couldn't be raised" block is **cleared**. The local CAPD app cluster was found degraded (apiserver crash-looping, etcd timeouts, `bex-lb` port remapped); a full **destroy + recreate** (`kubectl delete cluster bex` → `scripts/mock-cluster.sh`, installing Calico early via the LB port to unblock the provision wait) plus operator deploy + `.pm/w5/dev-5/up.sh` brought up a working stack (bex-api :54050, dashboard :50050, Kratos, Loki, Mailpit). Registered a dev user and signed in — authenticated dashboard confirmed.

**Cron Job walk (Playwright, dev-5, screenshots in `.playwright-mcp/`):**

- **Trigger Run (part 1) — VERIFIED.** Created a `cron_job` (`dev5-cron`, `busybox:1.37`, `*/5 * * * *`); the operator reconciled it into a k8s CronJob. On **Events → Recent Runs**, clicked **Trigger Run** → confirm dialog "Trigger a run now?" → confirmed; `spec.runAt` was set on the App CR and the run appeared in the panel with live **Running** status. The **Trigger Run button is disabled while a run is active** (the `hasActiveRun` gate). Screenshots: `cron-runs-before-trigger.png`, `cron-runs-after-trigger.png`.
- **Run detail (part 2) — VERIFIED.** Expanded a run row → **Status / Started (absolute) / Finished / Duration / Run ID (`crr-…`)** load via `cronJobRun`. Screenshot: `cron-run-detail.png`.
- **Not yet walked:** notify-on-fail override + registry-credential edit (parts 3–4).

**Substrate note (local dev only):** on this resource-pressured host (~34 containers, load ~7), the operator manager crash-looped on Kubernetes **leader-election lease timeouts** (`10.128.0.1:443` context deadline exceeded via Calico). Disabling leader election on the single-replica dev deployment (`kubectl -n bex-system patch deploy bex-controller-manager` to drop `--leader-elect`) stabilized it, after which the pending Trigger Run reconciled. Not a product issue — a local-substrate workaround. Cron run **pods** were slow to complete (image-pull/host pressure), so terminal `Succeeded` status wasn't captured; the run-lifecycle UI (create → active → detail) is proven regardless.

Remaining to fully close: parts 3–4 (notify-on-fail, registry-cred edit) and, ideally, a terminal-status capture once the host is less loaded.

## Completed — 2026-09-06

Remaining parts 3–4 verified on production with owned disposable fixtures. Notifications: selected and saved all four values (`all`, `failure`, `none`, `default`), reloaded each time, and confirmed both the visible option and REST `notificationsToSend` match. Registry credential: changed name and username through keyboard input, saved with blank token, reloaded and confirmed both values; host was disabled; entered a replacement token, saved, reloaded and confirmed the token field remains blank. Evidence: `.playwright-mcp/w5-notifications-result.json`, `w5-registry-result.json`, and matching screenshots. Earlier cron parts 1–2 retain their dated live evidence above.

Scratch service `srv-daehl8vlbafc73cihc50` and credential `rgc-daehmknlbafc73cihd1g` deleted through public APIs; subsequent GETs returned 404 (`w5-inbox-cleanup.json`). This closes the remaining named checks; no new cron terminal-status proof is claimed.
