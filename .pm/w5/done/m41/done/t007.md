---
id: w5/m41/t007
title: "Render parity: live populated-project re-walk and ledger/evidence refresh"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m41/t004, w5/m41/t005, w5/m41/t006]
---

## Objective

Standing parity task: verify the complete populated Project flow against Render and ensure the dashboard, GraphQL metadata, and documented contract agree.

## Steps

1. Seed a dev Project with two Environments and multiple resource kinds/statuses, including at least two services plus Postgres and Key Value.
2. Walk Environment selection, search/type filters, contextual create, metadata, per-row Options, multi-select, and Move in Chromium against the real stack.
3. Compare the same controls to the redacted Render evidence from t001; classify any residual as fixed, deliberate, or newly filed.
4. Verify the GraphQL metadata selected by the UI matches REST/MCP sources where those surfaces expose the same facts.
5. Refresh `docs/render-artifacts/dashboard-walk/workspace.md` and the Projects & Environments/UI evidence in `docs/ADR018-render-parity.md` without overclaiming.
6. Remove only the temporary resources created for this verification.

## Files

- `docs/render-artifacts/dashboard-walk/workspace.md`
- `docs/ADR018-render-parity.md`
- affected verification scripts if a reusable check is warranted

## Acceptance criteria

- [x] The full Definition of Done passes in a real browser against populated data.
- [x] Metadata agrees with backend truth and Move survives reload.
- [x] Every residual is documented or filed; none is silently hidden behind a ✅ claim.
- [x] Temporary verification data is removed.

## Out of scope

- Fixing unrelated dashboard pages found during the re-walk; file them in the owning workstream.

## Completion evidence

Production Chromium passed the two-Environment, four-kind operating flow and reload checks. GraphQL, REST, and MCP agreed on stable ids, placement, and timestamps after the canonical-membership fix. The MCP replica-affinity residual is documented in `w2/015`; all fixture resources and local verifier processes were removed.
