---
id: w5/m64/t006
title: Render parity
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m64/t004, w5/m64/t005]
---

## Objective

Confirm this milestone changed **only** the dashboard's layout and introduced no cross-surface drift.

## Context

- The standing Render-parity task normally diffs a change across REST / GraphQL / MCP in `lego/backend/` plus the dashboard UI, against equivalent render.com behavior (`docs/ADR006-bex-api.md`, `docs/ADR018-render-parity.md`).
- **render.com ships no agent-sessions product**, so there is no Render behavior to compare against for this surface. The reference is ADR047 + the captured Devin evidence (see the milestone README's "Render parity task — included, with a noted reference swap"). The check therefore inverts: rather than proving parity with Render, prove this milestone touched **zero** backend surface.
- Expected: no files changed under `lego/`. The agent-sessions GraphQL documents (`dashboard/src/features/agent-sessions/api/agent-sessions.graphql`) should also be unchanged — the rail consumes the existing `useAgentSessions` query.

## Steps

1. `git diff --stat main -- lego/` over the milestone's commits — expect empty. If not empty, stop and explain why a layout milestone touched the backend.
2. Confirm `dashboard/src/features/agent-sessions/api/agent-sessions.graphql` is unchanged and no new query/field was introduced.
3. Confirm the surfaces that _describe_ agent sessions (REST/GraphQL/MCP) are untouched, so no ADR018 cell changes state.
4. Confirm ADR018 needs no edit; if any row's UI cell references the agents sidebar, update it to match reality.
5. Note the outcome in the milestone README's Status line.

## Files

- verification only; `docs/ADR018-render-parity.md` only if a cell is genuinely stale

## Acceptance criteria

- [ ] No changes under `lego/` attributable to this milestone.
- [ ] No GraphQL document or backend field added/changed.
- [ ] ADR018 confirmed accurate (edited only if a cell was already stale).
- [ ] Outcome recorded in the milestone README.

## Out of scope

- Backend work of any kind — if this task finds a backend need, file it as an inbox note.
