---
id: w5/m64/t009
title: Closeout
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m64/t007, w5/m64/t008]
---

## Objective

Close the milestone once its definition of done actually holds — the observable end state, not merely the code being written.

## Context

- The DoD's headline is user-observable: **one rail on `/agents`**. Since the defect was reported against the deployed `dashboard.bex.co/agents`, closeout should confirm it there (or on a local dev stack per `.pm/w5/dev-5/`) — a passing test plus a green deploy is the bar, and a screenshot into `.playwright-mcp/` is cheap proof.
- If the live check cannot be run in-session (no authenticated session, substrate down), do **not** hold the milestone open indefinitely: file a `w5/NNN` inbox note for the deferred live walk, following the precedent of `.pm/w5/028.md` / `029.md` / `031.md`, and close on the automated gates.

## Steps

1. Verify the DoD line by line against the shipped code.
2. Confirm one rail on `/agents` and `/agents/<id>` — live if reachable, screenshot to `.playwright-mcp/` with bare filenames; otherwise file the deferred-walk note.
3. Set every remaining task `status: done`, move each `tNNN.md` to `.pm/w5/m64/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in `.pm/w5/m64/README.md`.
5. Move the whole `.pm/w5/m64/` directory to `.pm/w5/done/m64/` (`mv` the directory, then `rmdir` the original — leave no tombstone or stub).
6. Check `- [x] **m64**` in `.pm/w5/README.md`.
7. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m64/**`, `.pm/w5/README.md`, and a new `.pm/w5/NNN.md` only if the live walk is deferred

## Acceptance criteria

- [ ] Every DoD item verified, not assumed.
- [ ] One rail confirmed live, or a deferred-walk note filed with exact steps.
- [ ] All tasks `status: done` and moved to `done/`.
- [ ] Milestone moved to `.pm/w5/done/m64/` with nothing left at the old path.
- [ ] `- [x] **m64**` checked in the workstream README.
- [ ] Prettier clean.

## Out of scope

- Any new feature work surfaced during closeout — file it as an inbox note.
