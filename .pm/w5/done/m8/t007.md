---
id: w5/m8/t007
title: Simplify — run /simplify over the code this milestone changed
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m8/t006]
---

## Objective

Run `/simplify` over the dashboard + backend code this milestone touched (features/databases, routes, the catalog-query extension) — reuse / simplification / efficiency only, behavior-preserving.

## Context

- Standing closing task (board convention, `.claude/skills/pm/SKILL.md`).
- Watch for: duplicated list/detail state idioms vs. `features/services`, formatter reuse (`lib/format.ts`), dialog/confirm idiom reuse, and the lazy connection-info hook not re-implementing an existing Apollo pattern.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply confirmed findings; keep behavior identical (tests stay green).

## Files

- Whatever the milestone's diff touched under `dashboard/src/` and `lego/backend/`.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's changes; confirmed findings applied or explicitly rejected with reasons.
- [ ] `yarn test` + Go tests still green afterward.

## Out of scope

- Bug hunting (that's `/code-review`); new features.
