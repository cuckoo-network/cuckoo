---
id: w5/m45/t007
title: Add meaningful success, failure, and navigation regression coverage
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m45/t005]
---

## Objective

Add route-level and component-level regression coverage that enforces every behavioral class in the post-create matrix, including immutable-id routing, navigation ordering, non-navigation on failure, and one-time-secret retention.

## Context

- Existing candidate suites include `index.test.tsx`, `services.new.test.tsx`, `keyvalue.new.test.tsx`, `env-groups.test.tsx`, and `webhooks_.new.test.tsx`.
- The Postgres regression must reproduce the dialog search-param close followed by the final detail navigation so a close callback cannot win the race.
- Assertions should cover representative classes rather than snapshotting markup.

## Steps

1. Add focused route tests for all standalone outcomes named in the DoD, including returned deploy-id preference for services.
2. Add representative tests for contextual refresh/retention and one-time-secret acknowledgement flows.
3. Add rejected-mutation tests proving that routes do not change.
4. Run dashboard typecheck, lint, focused tests, and the full dashboard test suite.

## Files

- `dashboard/src/routes/__tests__/index.test.tsx`
- `dashboard/src/routes/__tests__/services.new.test.tsx`
- `dashboard/src/routes/__tests__/keyvalue.new.test.tsx`
- `dashboard/src/routes/__tests__/env-groups.test.tsx`
- `dashboard/src/routes/__tests__/webhooks_.new.test.tsx`
- Relevant feature component tests identified by t001

## Acceptance criteria

- [x] Tests fail if a successful standalone create returns to a list or uses a name-derived route.
- [x] Tests fail if modal/search cleanup overwrites the final detail route.
- [x] Tests fail if a rejected create navigates or a one-time secret disappears before acknowledgement.
- [x] `cd dashboard && yarn typecheck && yarn lint && yarn test` passes.

## Out of scope

- Tautological snapshots or backend test expansion where no backend behavior changes.
