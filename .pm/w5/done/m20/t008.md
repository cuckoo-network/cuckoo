---
id: w5/m19/t008
title: Closeout — verify DoD met, then move the milestone to done/
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m19/t006, w5/m19/t007]
---

## Objective

When every other task is complete **and the definition of done actually holds** (a wizard-created service boots with its env vars on first deploy; the read-back semantics are resolved and tested; all surfaces consistent), close the milestone.

## Context

- Closeout is the move: set remaining tasks `status: done`, move each `tNNN.md` to `w5/m19/done/`, mark rows `— **DONE**` + `**Status:** done` in the milestone README, move the whole `w5/m19/` to `w5/done/m19/`, check `- [x]` in `w5/README.md`.
- Do not run until the DoD is observably real — verified against a running service, not merely written code.

## Steps

1. Re-read the milestone DoD; verify each clause with evidence (a real create-with-env → running app check).
2. Execute the closeout moves via `/pm done w5/m19/t008` (its step 4 performs the milestone move).

## Files

- `.pm/w5/m19/` → `.pm/w5/done/m19/`
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD clauses each verified with evidence before any file moves
- [ ] Status synced in all three places (task frontmatter, milestone README, workstream README)

## Out of scope

- Closing with an unmet DoD ("code written" ≠ done)
