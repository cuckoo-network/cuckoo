---
id: w5/m19/t004
title: i18n (en/zh) + validation and empty states
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m19/t003]
---

## Objective

Localize the new wizard section (en + zh, the repo's two locales) and give it the same validation behavior the Environment tab already has, so the wizard editor doesn't feel like a second, stricter/looser env-var dialect.

## Context

- Locale files: `dashboard/src/features/services/locales/en.ts` / `zh.ts` (w5/m17 pattern — every new user-facing string lands in both).
- Validation reference: whatever the Environment tab (`env-var-row.tsx` / `use-env-vars.ts`) enforces for duplicate keys, empty keys, and empty values — mirror it, don't invent.

## Steps

1. Extract all new strings into `en.ts`/`zh.ts`; no hardcoded literals in the section component.
2. Mirror the Environment tab's duplicate-/empty-key handling; disable Create-button submission only for states the tab would also reject.
3. Extend the t003 tests with a validation case (duplicate key) and a locale-render sanity check.

## Files

- `dashboard/src/features/services/locales/en.ts`
- `dashboard/src/features/services/locales/zh.ts`
- the t003 section component + tests

## Acceptance criteria

- [ ] Section renders fully translated under both locales; no literal English fallbacks in zh
- [ ] Duplicate/empty-key behavior matches the Environment tab's, with a test proving it

## Out of scope

- New validation rules the Environment tab doesn't have
