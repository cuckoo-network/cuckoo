---
id: w5/m19/t007
title: Test coverage — meaningful tests for create-with-env behavior + failure modes
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m19/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — asserting real behavior and failure modes, never trivial/tautological/snapshot-everything coverage.

## Context

- Backend precedent: `lego/backend/internal/apps/cron_settings_test.go` (w5/m18's surface-parity test style — assert the three adapters produce the same spec).
- Dashboard precedent: `dashboard/src/features/services/components/__tests__/` (w5/m17's wizard tests), `hooks/__tests__/`.

## Steps

1. Backend: cross-surface test — REST body vs GraphQL args vs MCP args with the same env vars yield identical `spec.Env`; malformed input (empty key) rejected consistently; the t002 read-back decision has a regression test.
2. Dashboard: wizard flow test — add rows → submit → mutation called with the right `envVars`; duplicate-key validation blocks submit; zero-row create unchanged.

## Files

- `lego/backend/internal/apps/*_test.go`
- `dashboard/src/features/services/**/__tests__/`

## Acceptance criteria

- [ ] Each new test fails if its behavior regresses (spot-check by mutating the code under test)
- [ ] Both suites green

## Out of scope

- E2E/browser coverage beyond the existing harness patterns
