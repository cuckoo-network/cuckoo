---
id: w5/m19/t006
title: Simplify — `/simplify` over the code this milestone changed
worker: worker5
status: todo
estimate: 15m
depends_on: [w5/m19/t005]
---

## Objective

Run `/simplify` over the code this milestone touched — reuse, simplification, efficiency — behavior-preserving only.

## Context

- Backend: `lego/backend/internal/apps/graphql.go` (+ any secrets read-back change from t002).
- Dashboard: the wizard section component, `services.new.tsx`, hooks.
- Likely reuse win: one shared `{key,value}` GraphQL input type across secrets/envgroups/apps instead of three.

## Steps

1. `/simplify` over the changed files; apply behavior-preserving improvements.
2. Re-run backend + dashboard test suites.

## Files

- files changed by t002–t004

## Acceptance criteria

- [ ] No behavior change (tests still green: `go test ./...`, `yarn test`)
- [ ] Duplication introduced by this milestone removed or consciously kept with a reason

## Out of scope

- Refactors beyond this milestone's diff
