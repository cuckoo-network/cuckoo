---
id: w5/m19/t005
title: Render parity — REST/GraphQL/MCP/UI consistency for create-time env vars vs render.com
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m19/t004]
---

## Objective

Verify the milestone left all four surfaces consistent — same fields, semantics, and error shapes for create-time env vars on REST, GraphQL, and MCP (`lego/backend/`), and a wizard whose behavior matches render.com's — and flag any drift as follow-up work rather than silently diverging.

## Context

- bex-api is meant to be Render-compatible: `docs/ADR006-bex-api.md` (one core, thin adapters).
- The parity ledger row to update: `docs/ADR018-render-parity.md` "Create service (web / private)" (and the wizard reference in its Evidence cell).
- Specific check this milestone owes: on Render, do vars set at create appear in the service's Environment tab afterwards? Compare with t002's resolved read-back semantics; any mismatch is a documented divergence or a filed follow-up.

## Steps

1. Diff the three adapters' create-with-envVars behavior (field name, `{key,value}` shape, empty-list handling, error on malformed input).
2. Compare the shipped wizard section against t001's capture; note deliberate deviations.
3. Update the ADR018 create row + `docs/render-artifacts/new-service-wizard.md` parity-decisions table (flip "Env vars inline" from ✖).
4. File follow-up notes for any drift found; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/new-service-wizard.md`

## Acceptance criteria

- [ ] All three API surfaces demonstrably accept the same env-var create shape with the same semantics
- [ ] Ledger + capture doc updated; every divergence is documented or filed, none silent

## Out of scope

- Fixing drift beyond trivial (file notes instead)
