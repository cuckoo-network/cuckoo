---
id: w5/m19/t003
title: Wizard UI — env-var editor in the Settings step, wired into the create mutation
worker: worker5
status: todo
estimate: 1h
depends_on: [w5/m19/t001, w5/m19/t002]
---

## Objective

Add an env-var editor to the `/services/new` Settings step — laid out per t001's capture (likely a collapsed Advanced-style section, per render.com/docs/web-services) — and thread its rows into the `createService` mutation t002 extends, for every source type (git provider, public Git URL, existing image).

## Context

- Wizard route: `dashboard/src/routes/services.new.tsx` (w5/m15 three-tab wizard, w5/m17 type coverage).
- Reusable editor pieces from the Environment tab: `dashboard/src/features/services/components/env-var-row.tsx` (+ the env-vars panel patterns and `use-env-vars.ts` hook shapes) — reuse the row component/styles; the wizard variant is local state, no mutation-per-row.
- GraphQL operations live in `dashboard/src/features/services/api/services.graphql`; regenerate `dashboard/src/graphql/definitions.ts` (codegen — see `dashboard/CLAUDE.md`).
- Render offers env-at-create for all three source methods (identical docs wording), so the section shows on every source tab — unlike branch/rootDir which hide for image.

## Steps

1. Build the wizard env-var section per t001's captured placement/affordances (add row, remove row, key/value inputs; paste-a-`.env` only if t001 shows Render's wizard has it).
2. Extend the `CreateService` operation in `services.graphql` with `envVars`, run codegen, thread the wizard state into the mutation.
3. Keep the section collapsed/secondary per capture; empty state = zero rows, no requirement to add any.
4. Component/unit tests following the existing wizard test patterns (`features/services/components/__tests__/`).

## Files

- `dashboard/src/routes/services.new.tsx`
- `dashboard/src/features/services/components/` (new wizard env-var section; reuse `env-var-row.tsx`)
- `dashboard/src/features/services/api/services.graphql`
- `dashboard/src/graphql/definitions.ts` (codegen)

## Acceptance criteria

- [ ] Env vars entered in the wizard arrive on the created service (visible per t002's resolved read-back path) and the app boots with them on first deploy
- [ ] Section present for all three source types; zero-row create still works unchanged
- [ ] `yarn test` and `yarn lint` green in `dashboard/`

## Out of scope

- Secret files, health check path, region/runtime/build/start fields (gated or deliberate omissions — see milestone README)
- i18n strings beyond placeholders (t004)
