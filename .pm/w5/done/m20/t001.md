---
id: w5/m19/t001
title: Live-capture Render's wizard env-var section (placement + affordances) → extend the capture doc
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Pin down where and how Render's new-service wizard exposes env vars — the existing capture was a docs-fallback (no live login that session) and its claim conflicts with Render's docs on placement — so t003 builds the render.com-consistent layout instead of a guess.

## Context

- `docs/render-artifacts/new-service-wizard.md` (w5/m15 t005) lists "**Env vars** | Key/value table inline (add row button)" as a top-level Step-3 field, but its **Method** line admits docs-fallback.
- render.com/docs/web-services (verified 2026-07-12) instead says: "Under the **Advanced** section, you can set environment variables and secrets, add a persistent disk, set a health check path, and more" — i.e. inside the collapsed **Advanced** section, identical wording for all three source methods (Git provider, public Git URL, existing image).
- Render's env editor vocabulary elsewhere: **+ Add Environment Variable** and **Add from .env** (verified against render.com/docs/configure-environment-variables 2026-07-08, recorded in `.pm/DO_NOT_DO.md`'s db-picker entry). Whether the wizard's section offers "Add from .env" and/or a "Generate" value affordance is unconfirmed.
- Playwright MCP screenshots: pass **bare** filenames so they land in `.playwright-mcp/` (repo rule).

## Steps

1. Drive Playwright MCP to `dashboard.render.com/web/new` (login required; if no live session is available, re-verify from render.com/docs and mark the method line accordingly — do not upgrade the capture's confidence without a live look).
2. Record: section placement (top-level table vs collapsed Advanced), row-editor shape (key/value inputs, add/remove), extra affordances (Add from .env, Generate value, secret-file entry beside it), and behavior per source type.
3. Update `docs/render-artifacts/new-service-wizard.md` — fix the Step-3 table row, the "collapsed sections" note, and the parity-decisions table; bump the **Captured**/**Method** line.

## Files

- `docs/render-artifacts/new-service-wizard.md`
- `.playwright-mcp/` (screenshots, bare filenames)

## Acceptance criteria

- [ ] The capture doc states the env-var section's placement and affordances with a dated method line (live capture, or explicitly still docs-fallback with the conflict resolved from docs)
- [ ] t003's layout question (inline table vs collapsed Advanced-style section) is answerable from the doc alone

## Out of scope

- Building any UI (t003)
- Capturing Health Check Path / Scaling / persistent-disk details beyond noting they exist (gated/non-goal elsewhere)
