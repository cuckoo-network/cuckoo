---
id: w5/m75/t004
title: "Dashboard: ownerId threading on every git surface + claim CTA on the recovery affordance"
worker: worker5
status: done
estimate: 60m
depends_on: [w5/m75/t003]
---

## Objective

ADR078 §6: every git hook passes `ownerId: currentWorkspaceId` (deferring while null) so the GitHub card, repo picker, connect, claim, and disconnect act on the **selected** workspace — closing the live-verified wrong-tenant bind. Plus §3a's UI: a claim CTA (`claimGit`) on the `missing_state` recovery affordance and the Settings card, for binding an already-installed account.

## Context

- The bug: `dashboard/src/features/git/hooks/*` and `use-repos.ts` issue unscoped operations; `use-services.ts:43-46` is the pattern to copy (`useWorkspace()` → `variables: { ownerId }`, skip until resolved).
- GraphQL docs (`src/features/git/api/git.graphql`) need `$ownerId` variables on `GitConnections`, `Repos`, `RepoBranches`, `ConnectGit`, `DisconnectGit` + new `ClaimGit`; regen types via the offline schema-dump path (backend `TestDumpGraphQLSchema` → `SCHEMA_JSON yarn codegen`, splice per-feature).
- Claim CTA: on `git_error=missing_state` / `no_claimable_installation` / `ambiguous_installation` render the explanation + a "Claim installed account" button firing `claimGit` and navigating to `claimUrl`; also offer claim alongside "Connect another account" on the Settings card (secondary action).
- Workspace switch must refetch: Apollo re-runs on variable change — verify the card updates without a manual reload (the live bug's symptom).

## Steps

1. Thread `ownerId` through `use-git-connection.ts` (both hooks), `use-repos.ts`, `use-connect-git.ts`, `use-disconnect-git.ts`; add `use-claim-git.ts`.
2. Update `git.graphql` + regen/splice `definitions.ts`.
3. Recovery + Settings claim CTA with locale strings (en+zh).
4. Update affected tests/mocks; `yarn typecheck && yarn lint && yarn test`.

## Files

- `dashboard/src/features/git/**`, `dashboard/src/features/services/hooks/use-repos.ts`, `dashboard/src/graphql/definitions.ts`

## Acceptance criteria

- [ ] With workspace B selected, all git operations carry B's ownerId (component test asserts variables); switching workspaces refetches the card.
- [ ] Claim CTA fires claimGit and navigates to the returned claimUrl; error codes render bounded messages.
- [ ] `yarn typecheck`, `yarn lint`, `yarn test` green.

## Out of scope

- Backend behavior (t001-t003).
