---
id: w5/m75/t007
title: "Test coverage — claim resolution matrix, preflight, ownerId threading"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m75/t005]
---

## Objective

Meaningful tests for the revision's behavior:

1. **Claim resolution matrix** (service): exactly-one unbound admin candidate binds; zero ⇒ `no_claimable_installation`; several ⇒ `ambiguous_installation`; a bound-elsewhere installation excluded; a non-admin-reachable installation excluded; nonce consumed on every path; subject mismatch refused; fresh can_manage enforced.
2. **Callback branch selection** (rest): absent installation_id ⇒ claim branch; malformed ⇒ `invalid_installation` unchanged; install branch byte-identical.
3. **Preflight**: Verifier nil ⇒ both start verbs refuse with no transaction row.
4. **Dashboard**: hooks pass ownerId (variables asserted), defer while null, claim CTA wiring, workspace-switch refetch.

## Acceptance criteria

- [ ] All above automated; backend + dashboard suites green in CI.

## Out of scope

- The live production walk (t008).
