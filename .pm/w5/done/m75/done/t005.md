---
id: w5/m75/t005
title: "Render parity — cross-surface consistency; record why claim is REST/GraphQL-only"
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m75/t004]
---

## Objective

Verify REST and GraphQL expose identical claim shapes/error codes, dashboard renders exactly what the API returns, and record in ADR018 that the claim flow is the workspace-bound model's answer to Render's "direct github.com install just works" — and why it is deliberately absent from MCP (browser OAuth ceremony; an agent cannot complete it, matching connect's existing MCP absence).

## Steps

1. Diff `POST /v1/git/claim` vs `claimGit` field-by-field; fix drift in code.
2. ADR018 git rows: add the claim-flow note to the recorded divergence.

## Files

- `docs/ADR018-render-parity.md`, any drift found

## Acceptance criteria

- [ ] Shapes/codes identical ×2 surfaces; MCP exclusion recorded with rationale.
- [ ] ADR018 updated.

## Out of scope

- New parity features.
