# Key Value dashboard follow-ups: metrics tab + maxmemoryPolicy/persistenceMode create fields — backend-gated

Repairs a ledger claim: `docs/ADR018-render-parity.md`'s Key Value row says these UI omissions were "filed as a w5/m12 follow-up," but no such note existed until this one. Both halves are **backend-gated** — park until the gates open:

- **KV detail metrics tab** (Render's Key Value page shows memory/connections): needs KV resource-metrics series on the metrics API first — untracked today (`w3/m10/t002` covers KV **PVC usage** only, no memory/connections series).
- **`maxmemoryPolicy` / `persistenceMode` create-form fields**: need `KeyValue` CR fields first ("omitted rather than faked" per the ledger) — w1 mechanism territory, untracked.

When promoted, file the companion backend notes (w3 metrics series, w1 CR fields) if they still don't exist; the UI halves alone are sub-hour each once the backends land.

Source: `/pm-brainstorm more for w5` 2026-07-12 (ADR018 KV-row follow-up claim with no note behind it).
