---
id: w5/m30/t008
title: Test coverage
worker: worker5
status: done
estimate: 50m
depends_on: [w5/m30/t006]
---

## Objective

Add meaningful regression coverage for the setting mutations, dashboard projections/editors, creation flow, applicability gates, and failure modes shipped by this milestone.

## Context

- Assert observable state and mutation variables; do not use snapshot-everything or tautological tests.
- Existing test seams include backend app service/GraphQL tests, hook tests, `build-deploy-section.test.tsx`, Settings route tests, and create-service hook/route tests.

## Steps

1. Fill any behavior or failure-mode gaps left by the implementation tasks.
2. Exercise set/clear, persisted-value rendering, mutation failure, wrong-runtime hiding, runtime switching, and invalid/unauthorized backend requests.
3. Run the affected package suites, then the full backend and dashboard gates.

## Files

- `lego/backend/internal/apps/*_test.go`
- `dashboard/src/features/services/**/__tests__/*.test.{ts,tsx}`
- `dashboard/src/routes/__tests__/*.test.tsx`

## Acceptance criteria

- [x] Tests fail if either setting stops round-tripping, a stale Dockerfile path leaks across runtime changes, or an inapplicable control appears.
- [x] Tests cover backend rejection of unauthorized, invalid-path, and wrong-runtime mutations.
- [x] `cd lego/backend && go test ./...` passes.
- [x] `cd dashboard && yarn typecheck && yarn lint && yarn test` passes.

## Out of scope

- Artificial coverage-percentage work that does not protect milestone behavior.

## Resolution

Added backend authorization/validation/applicability and round-trip tests plus dashboard projection, editor, failure, runtime-switching, route-gating, and create-variable coverage.
