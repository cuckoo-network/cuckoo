# Fix `docs/ADR008-vision.md` stale "managed databases" non-goal

`docs/ADR008-vision.md` §Non-goals still says "**Managed databases** — bring your own Postgres", but `GOAL.md` #4 is PostgreSQL and bex-api ships the full Render-compatible `/v1/postgres` surface (`docs/ADR006-bex-api.md` §Managed Postgres, `Database` CRD, CNPG). One-line doc fix: drop or rewrite the non-goal to match reality (e.g. scope the non-goal to *non-Postgres* managed datastores). Sub-hour; can ride along with w5/m8 t001. Run prettier on the doc after.

Source: `/pm-brainstorm more milestones for w5` (2026-07-08).
