---
id: w5/m43/t004
title: Render parity check (standing)
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m43/t003]
---

## Objective

Confirm surface consistency: this is a UI-only change — REST/GraphQL/MCP are untouched (the feature reuses the shipped `serviceEvents` GraphQL read), so parity work reduces to matching Render's captured treatments and the backend's wire vocabulary.

## Acceptance criteria

- [x] Marker success/failure mapping keyed to `store.RenderDeployStatus`'s wire enum (succeeded|canceled|failed), verified in `lego/backend/internal/store/store.go`.
- [x] Visual treatments (dashed line, kind-styled badges, cluster count, hover popover, deploy links) match the t001 Render capture; deliberate divergences (no zoom-to-cluster, no checkbox-tree filter) recorded in the milestone README.
