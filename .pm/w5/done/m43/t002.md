---
id: w5/m43/t002
title: chart-events lib — event→marker mapping + clustering
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m43/t001]
---

## Objective

A pure lib the charts consume: map the `serviceEvents` feed to typed chart markers for the current window, and cluster markers whose x positions would overlap.

## Files

- `dashboard/src/features/metrics/lib/chart-events.ts`

## Acceptance criteria

- [x] `toChartEventMarkers(events, startTime, endTime)`: window-filters, sorts, maps type→kind/labelKey (deploy_started → start; deploy_ended by wire `deployStatus` succeeded|canceled|failed, defensive raw statuses; lifecycle types → info; unknown → humanized fallback).
- [x] `clusterMarkers(markers, xForT, minX, maxX)`: greedy 20-unit clustering, out-of-domain x clamped to the plot edges.
- [x] `labelKey` typed as a closed union so `t()` stays type-checked.
