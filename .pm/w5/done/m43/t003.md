---
id: w5/m43/t003
title: Marker overlay component wired into both chart kinds + cards
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m43/t002]
---

## Objective

Render-style marker rendering: `ChartEventLines` (dashed vertical SVG lines) + `ChartEventStrip` (HTML badge strip above the chart with kind-styled 20px badges, count badges for clusters, hover tooltip, links to the deploy detail page or the Events tab).

## Files

- `dashboard/src/features/metrics/components/chart-event-markers.tsx` (new)
- `dashboard/src/features/metrics/components/svg-line-chart.tsx`, `svg-bar-chart.tsx` — optional `markers`/`markersServiceId` props; line chart positions via its `xForT`, bar chart via a time-linear fractional slot index
- `dashboard/src/features/metrics/components/application-metrics-card.tsx`, `network-metrics-card.tsx` — thread markers to every section
- `dashboard/src/routes/services.$serviceId.metrics.tsx` — derive markers from the existing `useServiceEvents` feed (Apollo dedupes against the timeline's identical query)
- `dashboard/src/features/metrics/locales/en.ts`, `zh.ts` — `metrics.event*` keys

## Acceptance criteria

- [x] All six charts show lines + badges for in-window events; empty window renders nothing (no layout shift).
- [x] Badges are keyboard-focusable links with aria-labels; typecheck + lint clean.
