---
id: w5/m43/t006
title: End-to-end verification against the extended dev stub
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m43/t004, w5/m43/t005]
---

## Objective

Drive the real flow: markers need non-empty charts and in-window events, which the dev stub previously never served — extend it, then verify in the browser.

## Files

- `dashboard/scripts/local-bex.mjs` — `syntheticMetrics()` (deterministic per-metric waveforms honoring the query's start/end/resolution; correct `MetricSeries` typenames/units) + four boot-relative events in the real wire vocabulary (deploy_started, deploy_ended succeeded-with-commit, deploy_ended failed, server_restarted), full `ServiceEventDetails` field sets so Apollo's cache stays warning-free for the new rows.

## Acceptance criteria

- [x] `yarn local-bex` + `yarn dev:local`: all six charts on `/services/eden-cms-v2/metrics` render waveforms with four kind-styled markers at correct x positions.
- [x] Hover shows "Deploy failed 7:37:09 PM / Click to view details"; clicking lands on `/services/eden-cms-v2/deploys/dep-failed-000`.
- [x] Remaining console missing-field warnings are pre-existing stub gaps, not from the new fixtures.
