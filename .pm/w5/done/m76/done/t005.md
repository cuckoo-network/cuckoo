---
id: w5/m76/t005
title: "Test coverage — swap transitions, validation, dialog wiring"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m76/t003]
---

## Objective

Meaningful tests for the shipped behavior: backend transition matrix (repo→image, image→repo, repo→repo, branch-only — clean single-source spec, no implicit deploy, autoDeploy preserved, next-deploy clone secret resolves via the new repo's owner), surface parity of the three fields, and dashboard component tests (card display per source kind, dialog modes, mutation variables, no-auto-deploy note rendered).

## Acceptance criteria

- [x] Transition matrix + parity + component tests land and assert real behavior/failure modes; suites green in CI.

## Out of scope

- Live walk (t006).
