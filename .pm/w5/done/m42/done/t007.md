---
id: w5/m42/t007
title: "Simplify: /simplify over the code this milestone changed"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m42/t006]
---

## Objective

Run `/simplify` over the files this milestone touched — reuse, simplification, efficiency; strictly behavior-preserving. Prime candidates: `metrics-filters.tsx` should have shrunk to a thin toolbar (range + event controls) — check whether it still earns its abstractions; the moved state (percentage, quantile, statusCode) should leave no orphaned props or dead locale keys.

## Context

- Changed surface: `features/metrics/{lib/range.ts, components/{metrics-filters,application-metrics-card,network-metrics-card}.tsx, locales/*}`, `routes/services.$serviceId.metrics.tsx`, `routes/services.$serviceId.tsx`, the tab-route `head()`s, and the Logs-side range consumers.

## Steps

1. `/simplify` the changed files; apply behavior-preserving reductions (dead keys, unused props, duplicated preset/label maps).
2. `yarn typecheck && yarn lint && yarn test` to prove behavior held.

## Files

- (as listed in Context — whatever the diff actually touched)

## Acceptance criteria

- [ ] No dead locale keys / unused props / duplicate preset maps remain from the control moves.
- [ ] Full dashboard suite passes unchanged.

## Out of scope

- Behavior changes of any kind.
