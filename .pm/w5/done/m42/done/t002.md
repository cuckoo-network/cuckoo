---
id: w5/m42/t002
title: "Event timeline: default-hidden show/hide toggle, event filter stays in the toolbar"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m42/t001]
---

## Objective

Match Render's event-timeline treatment on the Metrics page: the timeline is hidden by default and revealed by a toolbar toggle button; the event-type filter lives in the toolbar (Render: a filter-events icon button on the left, a show-event-timeline icon toggle on the right), not on an always-open card.

## Context

- Captured live 2026-07-17: Render's toolbar is `[filter-events icon] [range ▾] … [show-timeline icon]`; no timeline card is visible until toggled.
- bex today: `routes/services.$serviceId.metrics.tsx` renders `<EventTimeline …>` unconditionally between the filter bar and the cards, with its own "All events" filter combobox in the card header (shipped by w5/m36 — this task refines placement, it must not remove the feature).
- `EventTimeline` lives in `dashboard/src/features/events/components/event-timeline.tsx`; keep the component intact and move the visibility/filter state up into the route (or a thin toolbar wrapper in `metrics-filters.tsx`).

## Steps

1. Add toolbar controls: an event-filter control (reuse the timeline's existing filter options) and a show/hide toggle with a pressed/expanded state, i18n'd via `features/metrics` or `features/events` locales.
2. Default the timeline to hidden; render `<EventTimeline>` only when toggled on, passing the toolbar's filter value down.
3. Preserve deep-link behavior the timeline already has (deploy links) — no data change.
4. Verify on dev-5: toggle reveals/hides; filter narrows events; hidden is the initial state.

## Files

- `dashboard/src/routes/services.$serviceId.metrics.tsx`
- `dashboard/src/features/metrics/components/metrics-filters.tsx`
- `dashboard/src/features/events/components/event-timeline.tsx` (filter prop lift, if needed)
- `dashboard/src/features/{metrics,events}/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] Initial Metrics render shows no timeline; the toolbar toggle reveals it and toggles back.
- [ ] The event-type filter works from the toolbar while the timeline is shown.
- [ ] Toggle and filter have accessible labels in en + zh.
- [ ] `yarn typecheck && yarn lint && yarn test` pass.

## Out of scope

- Any change to the events data source or the Events tab.
