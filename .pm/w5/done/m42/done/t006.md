---
id: w5/m42/t006
title: "Render parity: cross-surface check + docs/render-artifacts/metrics-page.md"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m42/t002, w5/m42/t003, w5/m42/t004, w5/m42/t005]
---

## Objective

Verify the milestone changed only UI scoping — REST, GraphQL, and MCP metrics surfaces (`lego/backend/internal/metrics`) are untouched and still Render-consistent — and record the side-by-side evidence plus the accepted drift list in a new `docs/render-artifacts/metrics-page.md`.

## Context

- The 2026-07-17 captures to encode: Render toolbar `[filter-events] [range ▾ default 12h] [show-timeline]`; range presets 30m/1h/4h/12h/24h/2d/7d/14d (+30d disabled, Custom — excluded); Application card tabs + Limit/Manage-scaling links; Network card Status Code|Host|Path filters; Response Times Percentile All/p50/p75/p90(default)/p99; Total Requests count; title `backend-v2 ・ Web Service ・ Render Dashboard`.
- Accepted drift to record (with reasons): Host/Path filters (metrics API rejects them, w3/m12); percentile "All" (single-`quantile` GraphQL parameter); "Last 30 days" + "Custom" ranges; the observability-integrations banner (external metric drains are a `DO_NOT_DO` non-goal); group-by set (bex all/status/method vs Render's); bandwidth = HTTP-egress-only honest subset (existing note in `metrics.graphql`).
- Check `docs/ADR018-render-parity.md`'s Metrics row — update the UI cell/evidence pointer if its description no longer matches the shipped page.

## Steps

1. Re-walk both live pages (Render `srv-d2rnr3jipnbc73deuvgg`, bex dev-5 or prod) with the shipped changes; screenshot to `.playwright-mcp/` and distill into `docs/render-artifacts/metrics-page.md` (structure precedent: `docs/render-artifacts/deploy-detail-page.md`).
2. Confirm no `lego/backend` change was needed (pure `dashboard/`); if any slipped in, check REST/GraphQL/MCP stayed consistent with each other and Render.
3. Sync `docs/ADR018-render-parity.md` Metrics row evidence; file any newly-noticed gap as a w5 inbox note rather than expanding this milestone.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/render-artifacts/metrics-page.md` (new)
- `docs/ADR018-render-parity.md` (evidence pointer if stale)

## Acceptance criteria

- [ ] `docs/render-artifacts/metrics-page.md` exists with the side-by-side structure and the accepted-drift table.
- [ ] ADR018's Metrics row evidence is current.
- [ ] Any residual gap is filed as an inbox note, not silently dropped.

## Out of scope

- Building any of the recorded-drift items.
