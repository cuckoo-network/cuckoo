---
id: w5/m42/t008
title: "Test coverage: presets, moved controls, timeline toggle, title format"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m42/t006]
---

## Objective

Add meaningful vitest coverage for the behavior this milestone shipped — assertions on real behavior and failure modes, not snapshots.

## Context

- Existing suites to extend, not duplicate: `features/metrics/components/__tests__/{application-metrics-card,network-metrics-card}.test.tsx`, `routes/__tests__/services.$serviceId.test.tsx`, and the logs feature's tests that exercise the shared range presets.

## Steps

1. Range: new preset list resolves correct span/resolution; retired ids (`3h`/`6h`/`1d`) degrade to the default via `parseRangePreset`; default is `12h`.
2. Application card: Percentage/Total tabs live in the card and switch the query mode; Limit links render with the configured value and point at the plan route; no-limit state still honest.
3. Network card: Status Code filter drives the query from the card; Percentile p50/p75/p90/p99 maps to quantile 0.5/0.75/0.9/0.99 with p90 default; Total Requests count sums the visible window and hides when empty.
4. Timeline: hidden by default, toggle reveals, filter narrows.
5. Title: post-load `document.title` equals `<name> · <type label> · bex dashboard` for two service types; id-only before resolution.

## Files

- `dashboard/src/features/metrics/components/__tests__/*`
- `dashboard/src/routes/__tests__/services.$serviceId.test.tsx`
- logs-feature tests touching range presets

## Acceptance criteria

- [ ] Each behavior above has a failing-first-capable assertion (no tautological or snapshot-everything tests).
- [ ] `yarn test` passes; suite count grows accordingly.

## Out of scope

- E2E/browser automation (manual dev-5 verification already required per task).
