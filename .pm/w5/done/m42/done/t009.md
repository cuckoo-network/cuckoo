---
id: w5/m42/t009
title: "Closeout"
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m42/t008]
---

## Objective

Close the milestone once the definition of done actually holds on a live dashboard (dev-5 or prod), not merely when the code is written.

## Context

- DoD: `.pm/w5/m42/README.md` — Render-scoped toolbar/cards, hidden-by-default timeline, Render-shaped titles, green `yarn typecheck && yarn lint && yarn test`, evidence artifact in place.

## Steps

1. Re-check the DoD end-to-end in a browser against a live bex dashboard.
2. Set every remaining task `status: done`, move each `tNNN.md` to `w5/m42/done/`, mark every README row `— **DONE**`, set `**Status:** done`.
3. Move `w5/m42/` to `w5/done/m42/` (whole directory, no tombstone) and check the `- [x]` box in `w5/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m42/**`, `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD verified live before any move.
- [ ] Status synced in all three places; `w5/m42/` no longer exists outside `done/`.

## Out of scope

- New feature work (file inbox notes instead).
