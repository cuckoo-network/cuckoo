---
id: w5/m70/t008
title: Closeout
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m70/t007]
---

## Objective

Close the milestone once every other task is complete **and the definition of done actually holds** (verified on a real workspace with a live credit grant, not merely code-written).

## Context

- Conventions: `.pm/CLAUDE.md` — done items move to `done/` folders; sync status in all three places.

## Steps

1. Verify the milestone README's definition of done end to end (a live-granted workspace shows balance/expiry/adjusted due on all four surfaces; redirects hold; wire surface unchanged).
2. Set every task `status: done`, move each `tNNN.md` to `w5/m70/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w5/m70/` → `w5/done/m70/`; check `- [x]` in `w5/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m70/`, `.pm/w5/README.md`

## Acceptance criteria

- [x] Milestone in `w5/done/m70/`, statuses synced in all three places, DoD verified not assumed.

## Out of scope

- —

## Result

Live verification 2026-08-19 UTC (no customer/grant ids, no keys):

1. **Redirects (prod dashboard, unauthenticated).** `https://dashboard.bex.co/usage` → 307 `/billing`. Query preserved (`/usage?billing=success&foo=bar` → `/billing?billing=success&foo=bar`). `/billing` exists and is auth-gated (`/auth/login?next=%2Fbilling`). `/billing/update-plan` → `/workspace/settings?plan=change`.
2. **Live Stripe credit grant.** One live grant, category `paid`, not voided, no expiry, remaining **100000 cents = $1000 USD**. Same filters the backend uses (`credit_balance_summary` `applicability_scope` + `price_type=metered`) return `summary_available_cents 100000` and per-grant remaining `100000` — the credits block would populate (hidden-at-zero does not apply).
3. **Prod bex-api credit reads succeed.** Port-forward of `bex-api` `:8091/metrics`: `bex_billing_enabled 1`; `bex_billing_operations_total{operation="credit_read",result="success"} 8`; no `credit_read`/`error` series. The restricted runtime key has the runbook §2 credit-read permission; degrade-by-omission is not firing in production.
4. **Tests.** `cd lego/backend && go test ./internal/billing/ ./internal/usage/ -count=1` ok. Dashboard `billing-redirects.test.tsx` + `usage-page.test.tsx` credits cases: 29/29. Gate copy (`usage.creditsCardStillRequired`) asserted. Wire `/v1/usage` / GraphQL `usage` / MCP `get_usage` remain usage-named; credits attach identically (t005/t007).
5. **Authenticated `/billing` screenshot not taken** (no dashboard login in this session). The live grant + successful prod `credit_read` is the observable proof that the workspace holding the grant gets a non-null `credits` block; UI rendering of that block is covered by the usage-page tests.

Moved whole to `w5/done/m70/`.
