---
id: w5/m70/t002
title: Restructure the page: Billing section above Usage section
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m70/t001]
---

## Objective

Give the renamed page a Render-shaped internal order: a **Billing** section first (onboarding/readiness card, payment actions, lifecycle alerts, invoice preview), then a **Usage** section (metered detail, resource caps). One page, two labeled sections — no split into separate routes at current scale.

## Context

- Page: `dashboard/src/features/usage/components/usage-page.tsx` (section components already exist inside it; `BillingOnboardingCard` is imported at line ~59).
- Reference: dashboard.render.com workspace billing page — cost summary + payment + credits up top, spend detail below.
- Layout rhythm rules (space-y-6, card grids) in `dashboard/CLAUDE.md`.

## Steps

1. Reorder `usage-page.tsx`: Billing block (onboarding card + invoice preview/estimated cost) above the metered-usage/caps block; add section headings via `usage.*` locale keys (en/zh).
2. Keep every existing data hook/query untouched — this is layout-only.
3. Update affected component tests / snapshots if any assert order.

## Files

- `dashboard/src/features/usage/components/usage-page.tsx`
- `dashboard/src/features/usage/locales/{en,zh}.ts`
- `dashboard/src/features/usage/components/__tests__/`

## Acceptance criteria

- [ ] Billing section renders above Usage section with clear headings in both locales.
- [ ] No data-fetch behavior change (same queries, same degrade paths).
- [ ] `yarn test` green.

## Out of scope

- Credits UI (t004); invoice history list / spend-by-service (future work, not this milestone).
