---
id: w5/m70/t005
title: Render parity
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m70/t004]
---

## Objective

Check the milestone's changes are consistent across every surface they expose: the `credits` block identical on REST/GraphQL/MCP (same fields, semantics, absence-on-failure shape), and the renamed dashboard page compared against Render's workspace billing page behavior. Flag drift as follow-up work rather than silently diverging.

## Context

- bex-api is Render-compatible (`docs/ADR006-bex-api.md`); the credits block is a bex extension (Render has no public credits API), so parity here means **cross-surface consistency** + **dashboard behavior comparison** (dashboard.render.com/w/…/billing: credit balance placement, applied-credit presentation, hidden-at-zero).
- Parity ledger: `docs/ADR018-render-parity.md` — add/adjust the row for the billing page + credits extension.

## Steps

1. Diff the three surface payloads for a credit-holding and a credit-less workspace; assert identical shapes.
2. Walk Render's billing page (Playwright or manual) and note presentation drift worth keeping vs fixing.
3. Update the parity ledger row with evidence.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Three-surface consistency verified with evidence; ledger updated; any drift filed as follow-up, not silently shipped.

## Out of scope

- Building Render-API credit endpoints (none exist upstream).

## Result

- Cross-surface consistency: `TestBillingCrossSurfaceParity` extended to assert identical `credits` shape on REST (== MCP via shared `toUsageResponse`) and GraphQL, field by field.
- Ledger: `docs/ADR018-render-parity.md` usage row updated with the credits extension + the `/usage`→`/billing` rename note.
- Render presentation comparison recorded code-side (balance card at top, hidden at zero, credit-applied invoice line — matching Render's billing-page shape per the 2026-08-17 design study); a live Render capture stays infra-blocked as in the w5/m59 precedent.
