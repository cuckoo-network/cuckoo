---
id: w5/m57/t005
title: Simplify — reuse/simplification pass over the changes
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m57/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (routing/alias, nav grouping, Settings gating) for reuse, simplification, and efficiency — behavior-preserving.

## Context

- Touched: `render-alias.ts`, `static.$.tsx`, `service-nav.tsx`, `service-root-redirect.ts`, `service-detail-header.tsx`, `services.$serviceId.settings.tsx`, and any new `/static` route.
- Watch for duplicated `isStaticSite` type-branching — keep the nav single-source-of-truth in `service-nav.tsx` and reuse the existing `service-type.ts` helpers rather than forking checks.

## Steps

1. Run `/simplify` on the working tree.
2. Apply behavior-preserving cleanups; keep `yarn typecheck && yarn lint && yarn test` green.

## Files

- The files this milestone changed.

## Acceptance criteria

- [ ] `/simplify` findings addressed or consciously deferred with a note.
- [ ] Gates green.

## Out of scope

- Behavior changes / new features.
