---
id: w5/m57/t006
title: Test coverage — routing, landing, nav, settings gating
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m57/t004]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: canonical `/static/<id>` + `/services` reverse-redirect for static, Events-first landing + no Deploys tab for static (other types unchanged), and Region hidden on static Settings.

## Context

- Tests live in `__tests__` dirs adjacent to code (dashboard/CLAUDE.md). Existing nav/alias tests likely under `dashboard/src/common/lib/__tests__/` and `dashboard/src/features/services/**/__tests__/`.

## Steps

1. `serviceNavGroups("static")` → top-level [Events, Settings], Monitor [Metrics], Manage [Environment, Redirects, Headers], no Deploys; and a non-static type still yields Deploys-first. Cover the `null`/loading case (no Deploys flash for static).
2. Redirect helpers: static service root → `/events`; non-static → `/deploys`; `/services/<static-id>` → `/static/<id>`; `/web|cron/<id>` alias still → `/services/<id>`.
3. Settings: Region row rendered for non-static, absent for static.

## Files

- `dashboard/src/features/services/components/__tests__/service-nav.test.ts(x)`
- `dashboard/src/common/lib/__tests__/render-alias.test.ts`
- `dashboard/src/features/services/lib/__tests__/service-root-redirect.test.ts` (new if absent)
- A settings-route test asserting the Region gate.

## Acceptance criteria

- [ ] Tests assert real behavior + the non-static-unchanged invariant, not tautologies.
- [ ] `yarn test` green.

## Out of scope

- E2E/browser tests (covered by the t004 live walk).
