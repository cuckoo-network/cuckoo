---
id: w5/m23/t002
title: "Filter controls fed by logLabelValues"
worker: worker5
status: todo
estimate: 1h
depends_on: [w5/m23/t001]
---

## Objective

Add filter controls for `level`/`statusCode`/`method`/`path`/`instance` to the Logs tab, with dropdown values live-fed by the `logLabelValues` GraphQL query `w3/m8` shipped.

## Context

- `dashboard/src/features/logs/` (or wherever `log-filter-bar.tsx` lives) — the existing `type`/`text` filter bar to extend, per `w5/008`'s note that stale "can't honor" comments were already corrected in place by `w3/m8`.
- GraphQL `logLabelValues(label, resource, …filters)` → `[String]` — the dropdown feed, scoped to the service (labels: `host`|`instance`|`level`|`method`|`statusCode`|`type`).
- `logs(resource, type, text, level, instance, host, statusCode, method, path, direction, limit)` — the query these new filters thread into.

## Steps

1. Add the five new filter controls to `log-filter-bar.tsx`.
2. Wire each to a `logLabelValues` query for live dropdown population, scoped to the current service.
3. Thread selected filter values into the `logs` query.

## Files

- `dashboard/src/features/logs/` (filter bar + query hook)

## Acceptance criteria

- [ ] All five filters are present, populated with real values, and correctly narrow the log results.

## Out of scope

- URL sync — t003. Request-line rendering — t004.
