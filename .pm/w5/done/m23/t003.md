---
id: w5/m23/t003
title: "URL sync for filter state"
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m23/t002]
---

## Objective

Reflect the Logs tab's filter state in the route's URL query params, matching the dashboard's existing filter-URL convention elsewhere (so a filtered log view is shareable/bookmarkable).

## Context

- Check an existing filtered-view route (e.g. the metrics page's filter params, if any) for the established param-naming convention to reuse.

## Steps

1. Sync each filter control's value to/from the route's search params.
2. Confirm navigating with a pre-filled URL (e.g. `?level=error`) pre-populates the filter bar and query.

## Files

- `dashboard/src/routes/services.$serviceId.logs.tsx` (or wherever the Logs route lives)

## Acceptance criteria

- [ ] Filter state round-trips through the URL correctly.
