---
id: w5/m23/t005
title: "503 store-unavailable state"
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m23/t002]
---

## Objective

When the durable log store isn't wired (`BEX_LOKI_URL` unset, dev-mode), `type=request` and the store-only filters now return 503 (the honesty rule) rather than an empty page. Show an explanatory state ("request logs need the log store") instead of a generic error toast.

## Context

- `w5/008`'s note: prod has Loki wired, so this is a dev-mode-only state, but it should still be handled gracefully rather than surfacing as a raw error.

## Steps

1. Detect the 503 response from a store-only filter query.
2. Render an explanatory empty-state message instead of the generic error toast.

## Files

- `dashboard/src/features/logs/` (query error handling)

## Acceptance criteria

- [ ] A 503 from a store-only filter shows the explanatory state, not a generic error toast.
