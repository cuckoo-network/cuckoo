---
id: w5/m80/t007
title: Simplify
worker: worker5
status: done
estimate: 25m
depends_on: [w5/m80/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — the driver timeout config, the model-proxy fast-fail path, the completer teardown/reclaim grace, and the `failureReason` population/mapper — for reuse, simplification, and efficiency without changing behavior.

## Context

- Touched paths: `lego/agent-image/driver/src/config.ts`; `lego/backend/internal/sshgateway/modelproxy/modelproxy.go`; `lego/backend/internal/agentsessions/{service,completion}.go`; `dashboard/src/features/agent-sessions/lib/mapper.ts`.
- Watch for a reusable failure-classification seam shared between the fast-fail (t003), timeout (t002), and reason-population (t005) paths rather than three parallel switch statements.

## Steps

1. `/simplify` over the milestone's diff.
2. Consolidate any duplicated failure-classification / reason-string logic into one seam.
3. Keep behavior identical; rely on t008's tests to prove it.

## Files

- The milestone's changed files (above)

## Acceptance criteria

- [ ] `/simplify` applied; no behavior change.
- [ ] Failure-classification/reason logic is not duplicated across the timeout, fast-fail, and completeness paths.

## Out of scope

- New behavior or new failure classes.
