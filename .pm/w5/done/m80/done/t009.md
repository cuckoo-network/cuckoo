---
id: w5/m80/t009
title: Closeout
worker: worker5
status: done
estimate: 10m
depends_on: [w5/m80/t007, w5/m80/t008]
---

## Objective

Close the milestone once its definition of done actually holds: a bad-key session fails in seconds with a clear reason, the turn timeout is a sane bound, failed sandboxes reclaim promptly, and every failure class renders a non-empty reason — all verified, not merely coded.

## Context

- Closing this task triggers the milestone move (`/pm done w5/m80/t009`): set every task `status: done`, move each `tNNN.md` to `w5/m80/done/`, mark rows `— DONE` and `**Status:** done` in the milestone README, move `w5/m80/` to `w5/done/m80/`, and check `- [x]` in `w5/README.md`.
- Also mark inbox note `w5/048` closed (absorbed by t005) — move `w5/048.md` to `w5/done/048.md` and update the w5 README inbox line.

## Steps

1. Confirm the DoD holds against `dev-5` evidence (auth fast-fail seconds; timeout bound; failed-sandbox reclaim; reason completeness).
2. Ensure ADR047 (+ ADR059 for reclaim) reflect the shipped behavior.
3. Close `w5/048` as absorbed.
4. Run the milestone move.

## Files

- `.pm/w5/m80/*`, `.pm/w5/README.md`, `.pm/w5/048.md`

## Acceptance criteria

- [ ] DoD verified against real evidence, not just code.
- [ ] `w5/048` closed/moved as absorbed.
- [ ] Milestone moved to `w5/done/m80/` and checked in the w5 README.

## Out of scope

- Any remaining follow-ups → new inbox notes, not held open here.
