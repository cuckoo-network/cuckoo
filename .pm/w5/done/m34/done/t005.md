---
id: w5/m34/t005
title: Test coverage
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m34/t003]
---

## Objective

Add tests asserting: `fetchSession` maps the Kratos `session_aal2_required` error to the typed state (mocked response); the session guard redirects with `aal=aal2` exactly once for that state and not for authenticated/anonymous. Assert behavior and failure modes, not snapshots.

## Files

- `dashboard/src/` session/auth tests

## Acceptance criteria

- [ ] Both behaviors locked; `yarn test` passes.
