---
id: w5/m34/t002
title: Explicit aal=aal2 redirect; remove the trial-and-error path
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m34/t001]
---

## Objective

When the session state is `aal2Required`, redirect to the login/step-up route with an explicit `aal=aal2` search param (route schema updated accordingly), and delete the login page's retry/rediscovery logic that inferred step-up by trial-and-error.

## Context

- The m11 README names the touch set: `session.ts`/`auth.ts`/route schema.
- Kratos login flow accepts `aal=aal2` to mint a step-up flow directly.

## Steps

1. Add `aal` to the login route's validated search params.
2. Redirect with `aal=aal2` from the session guard; pass it into the Kratos flow creation.
3. Remove the old rediscovery branch; ensure plain aal1 login is untouched.
4. `yarn test` + typecheck.

## Files

- `dashboard/src/routes/` login route + session guard

## Acceptance criteria

- [ ] Step-up lands in one redirect with `aal=aal2` visible in the URL; aal1 login unchanged.
- [ ] The trial-and-error branch is gone (grep confirms).

## Out of scope

- Enrollment UX (settings page) — unchanged from m11.
