---
id: w5/m5/t001
title: Capture Render's service-detail IA via Playwright (service side-nav + overview panel) as the design source
worker: worker5
status: done
estimate: 30m
depends_on: []
---

## Objective

Capture Render's service-detail page as the design reference for this milestone — the **left service sidebar** (ungrouped Events/Settings, then a MONITOR group {Logs, Metrics} and a MANAGE group {Environment, Shell, Scaling, Previews, Disk, One-Off Jobs}) and the overview panel layout — and record the mapping from Render's overview fields to bex-api's `server(id)` fields. Render has no "Overview" item (the service root lands on Events/deploys); bex adds an Overview landing. bex ships a subset for now: Overview + MONITOR › Logs. Captured 2026-07-06: the logs page + service sidebar (`.playwright-mcp/render-service-logs.png`) and the service root / overview-deploys landing (`.playwright-mcp/render-service-overview.png`); the IA, logs contract, and the overview field mapping (with gaps flagged) are recorded in the milestone README's "Render reference".

## Context

- Render dashboard service-detail page (log in via Playwright MCP; screenshots to `.playwright-mcp/`, bare filenames).
- bex-api `server(id)`/`Service` fields (`docs/ADR006-bex-api.md`): `phase`, `serviceDetails.url`, `revision`, `replicas`, `createdAt`, string `suspended` enum, `dashboardUrl`.
- bex's subset: Overview (bex landing) + MONITOR › Logs (no Metrics/Events/Settings/Environment yet).
- Service nav is a left sidebar (grouped MONITOR/MANAGE), not a top tab bar — see the milestone README's "Render reference".

## Steps

1. Navigate Render's service root; screenshot the overview/deploys landing panel to `.playwright-mcp/render-service-overview.png`. (The logs page + service sidebar are already captured in `render-service-logs.png`.)
2. Note the sidebar groups, overview panel fields/layout, and header action placement.
3. Record the Render-field → `server(id)`-field mapping in the milestone README, flagging any Render overview field bex-api does not expose.

## Files

- `.pm/w5/m5/README.md` (design notes)
- `.playwright-mcp/render-service-logs.png`, `.playwright-mcp/render-service-overview.png` (gitignored)

## Acceptance criteria

- [x] Render service screenshots (sidebar/logs + overview landing) exist in `.playwright-mcp/`.
- [x] The README documents the service-nav structure, the overview field mapping, and the chosen subset with gaps flagged.

## Out of scope

- Building the route (t002+).
