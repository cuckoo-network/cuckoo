# Custom-domains section in service Settings — blocked on w1/003

Add Render's custom-domains section to the service Settings tab (list domains, add with DNS instructions, delete, cert/verification status) once bex-api serves the custom-domains API (`w1/003`). Model on Render's settings IA (capture live first, per the m7 pattern). **Blocked:** no backend surface today — do not build dashboard-first (pillar-1 API-first).

Source: `/pm-brainstorm more milestones for w5` (2026-07-08); w5/m7 explicitly left the rest of Render's Settings page as future milestones.
