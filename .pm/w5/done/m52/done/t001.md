---
id: w5/m52/t001
title: General card: rename + read-only Region row
worker: worker5
status: done
estimate: 30m
depends_on: []
---

## Objective

Rename the first settings card from "Settings" to "General" (Render's heading) and add a read-only Region row showing the platform placement (e.g. `fsn1`), hidden when the installation sets no region. Render renders Region as a disabled textbox with explanatory copy; match that using the m50 row in permanently-disabled mode (no pencil).

## Context

- REST already projects `region` onto service metadata from `BEX_REGION` (w1/m53; CLAUDE.md env table: "unset ⇒ region omitted, never inferred").
- Check whether the GraphQL `Server`/service-details type exposes `region`; if not, add a read-only field in `lego/backend/internal/api` GraphQL schema and splice it into the dashboard's generated types — **`dashboard` codegen caveat: `definitions.ts` is partly hand-maintained; never full-codegen it, splice the new field per-feature** (see repo memory note on offline-codegen drift).
- Route: `dashboard/src/routes/services.$serviceId.settings.tsx` (card heading + row placement).
- Keep bex's Idle timeout and Max shutdown delay rows in General (bex extensions; Render's General has Name/Region/Instance Type).

## Steps

1. Rename the card heading (en/zh locales).
2. Expose `region` through GraphQL if missing; splice dashboard types per-feature.
3. Add the read-only Region row under Name (Render's order), hidden when region is empty.

## Files

- `dashboard/src/routes/services.$serviceId.settings.tsx`
- `lego/backend/internal/api` (GraphQL schema, only if region is missing)
- `dashboard/src/features/services/api/` + generated definitions splice

## Acceptance criteria

- [ ] Card titled "General"; Region row shows the configured region read-only; absent when unset.
- [ ] No regression in the card's existing rows.

## Out of scope

- Making region editable (installation-stamped by design, w1/m53).
