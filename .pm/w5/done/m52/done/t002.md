---
id: w5/m52/t002
title: Split Build vs Deploy cards; move Deploy Hook into Deploy
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m52/t001]
---

## Objective

Split the merged "Build & Deploy" card into Render's two cards — **Build** (Source, Branch, Root Directory, Build Command, Build Filters) and **Deploy** (Pre-Deploy Command, Start Command, Auto-Deploy) — and move the standalone Deploy Hook section into the Deploy card as its last row (Render's placement).

## Context

- `dashboard/src/features/services/components/build-deploy-section.tsx` — currently one card; split without changing any row's behavior (rows may already be m50-migrated).
- `dashboard/src/features/services/components/deploy-hook-section.tsx` — becomes a row group inside Deploy; keep reveal/copy/regenerate semantics and the `BEX_API_PUBLIC_URL` copy behavior.
- Type gating: cron keeps its Schedule/Command rows (today in `cron-deploy-section.tsx`) — decide placement (Render puts cron Command/Schedule in Build/Deploy-equivalent cards; keep bex's current cron grouping unless trivially alignable).
- Docker runtime: Dockerfile Path lives in Build (Render's placement).

## Steps

1. Split the section component (or wrap rows in two cards) preserving row order within each.
2. Relocate Deploy Hook rendering; delete the standalone section placement in the route.
3. Update locale headings (Build / Deploy) en/zh.

## Files

- `dashboard/src/features/services/components/build-deploy-section.tsx`
- `dashboard/src/features/services/components/deploy-hook-section.tsx`
- `dashboard/src/routes/services.$serviceId.settings.tsx`
- tests under `__tests__/`

## Acceptance criteria

- [ ] Two cards render with Render's row distribution for web/private/worker; Deploy Hook is the Deploy card's last row.
- [ ] Cron/static/docker gating unchanged in behavior.
- [ ] No mutation/query changes.

## Out of scope

- Auto-Deploy's select presentation (w5/m53); Git Credentials row (no bex equivalent — GitHub App covers it, note any decision in ADR018 via t005).
