---
id: w5/m52/t004
title: Bottom danger area: Suspend + Delete as Render-style closing actions
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m52/t003]
---

## Objective

Align the page's closing actions with Render: the settings page ends with "Suspend <Type>" and "Delete <Type>" actions (Render renders two plain buttons at the very bottom). Keep bex's explanatory copy and the type-to-confirm sudo dialog for delete (w5/m24) and the suspend/resume state handling (w1/m7, w1/m40) — this is presentation alignment, not a semantics change.

## Context

- `dashboard/src/features/services/components/suspend-service-card.tsx` + `delete-service-card.tsx` — today two separate cards ("Suspend Service", "Danger Zone").
- Render (live 2026-07-27): bottom buttons labeled "Suspend Web Service" / "Delete Web Service" (type-worded).
- Type wording exists already for the delete sudo phrase (`sudo delete <type words> <name>`, w5/024 note).

## Steps

1. Merge/restyle into a single bottom danger area with type-worded action labels (en/zh).
2. Keep resume rendering for already-suspended services.
3. Update tests for labels/structure.

## Files

- `dashboard/src/features/services/components/suspend-service-card.tsx`
- `dashboard/src/features/services/components/delete-service-card.tsx`
- `dashboard/src/routes/services.$serviceId.settings.tsx`
- tests under `__tests__/`

## Acceptance criteria

- [ ] Page ends with type-worded Suspend + Delete actions; confirm flows unchanged.
- [ ] Suspended services still surface Resume.

## Out of scope

- Any change to the underlying lifecycle verbs.
