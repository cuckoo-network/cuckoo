---
id: w5/m59/t005
title: Simplify (/simplify over t003's fix diff)
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m59/t004]
---

## Objective

Run `/simplify` over the code t003 changed — the in-milestone dashboard fixes — hunting reuse/simplification/efficiency wins while preserving behavior. Scope is only the milestone's diff; a walk with zero code fixes makes this a recorded no-op.

## Steps

1. `/simplify` scoped to t003's diff in `dashboard/src/`.
2. Apply accepted findings; suites stay green. If t003 shipped no code changes, record "no diff — no-op" here and close.

## Files

- `dashboard/src/` (t003's touched files only)

## Acceptance criteria

- [ ] `/simplify` run over the fix diff with findings applied or rejected explicitly — or a recorded no-op if there was no diff.
- [ ] `cd dashboard && yarn test` passes.

## Out of scope

- Simplifying untouched features.
