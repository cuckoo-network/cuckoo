---
id: w5/m59/t006
title: Test coverage for the in-milestone fixes
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m59/t004]
---

## Objective

Ensure every fix t003 shipped has a meaningful asserting test — behavior and failure modes, not snapshots — so the walk's corrections can't silently regress. A zero-fix walk makes this a recorded no-op.

## Context

- Tests live beside the touched components (`dashboard/src/features/**/__tests__/`, route tests in `dashboard/src/routes/__tests__/`); follow each feature's existing test idiom.
- The m57 precedent: its one fix (the static header chip gate) shipped with a gating assertion.

## Steps

1. Audit t003's fixes for coverage; add a test per fixed behavior that fails on the pre-fix tree.
2. Run the dashboard suite and record the file/test counts.

## Files

- `dashboard/src/features/**/__tests__/`

## Acceptance criteria

- [ ] Each t003 fix has at least one test asserting the corrected behavior — or a recorded no-op if t003 shipped none.
- [ ] `cd dashboard && yarn test` passes.

## Out of scope

- Coverage for filed-but-unbuilt notes.
