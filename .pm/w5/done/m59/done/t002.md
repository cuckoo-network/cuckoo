---
id: w5/m59/t002
title: bex side-by-side walk (dev-5 or prod) with a per-page verdict table
worker: worker5
status: done
estimate: 60m
depends_on: [w5/m59/t001]
---

## Objective

Walk the same page families on a live bex dashboard against t001's captures and produce the per-page verdict table: match / gap (with severity + rough size) / bex-extension / intentional exclusion.

## Context

- bex routes to walk: `settings.tsx` + `workspace.settings.tsx` (Team lives in workspace settings; `features/team/`), `env-groups.tsx` + `env-groups_.$groupId.tsx`, `usage.tsx` (+ `features/usage/billing-onboarding`), `notifications.tsx`, `webhooks.tsx` + `webhook.$webhookId.*`, `blueprints.tsx` + `blueprints.$blueprintId.tsx`.
- Stack: dev-5 (`bash .pm/w5/dev-5/up.sh`) or prod read-only; prefer whichever exercises real data (billing needs the Stripe-enabled deployment to show onboarding truthfully — note which side was used per page).
- Verdict discipline (m57 precedent): every Render affordance gets a row; drift is *found*, not assumed; bex-only extensions are recorded as extensions, not failures.
- Do not file DO_NOT_DO non-goals as gaps (drains, Slack, previews, payment gates).

## Steps

1. Walk each bex page side by side with the matching Render capture; record per-affordance verdicts (layout, controls, states, copy-level only where Render's copy is load-bearing, e.g. confirm phrases).
2. Capture bex-side screenshots for every row where the verdict is not an obvious match.
3. Assemble the draft verdict table (page → affordance → verdict → evidence) — the raw input for t003/t004.

## Files

- `.playwright-mcp/` (bex captures)
- Draft table (carried into `docs/render-artifacts/` by t004)

## Acceptance criteria

- [ ] Every family from t001 has a complete verdict table with evidence pointers on both sides.
- [ ] Each non-match row is classified: sub-hour fix (→ t003), bounded note (→ t003 files it), or intentional exclusion (named DO_NOT_DO item).

## Out of scope

- Fixing anything (t003); artifact polish (t004).
