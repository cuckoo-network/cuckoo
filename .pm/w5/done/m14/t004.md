---
id: w5/m14/t004
title: i18n (en/zh) for the danger-zone copy + confirm dialog
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m14/t002]
---

## Objective

Add English and Chinese locale strings for the danger-zone section and type-to-confirm dialog introduced in t002.

## Context

- `dashboard/src/features/services/locales/en.ts` is the existing locale file for this feature; a matching `zh.ts` (or equivalent) sibling holds translations — follow the existing env-var/secret-file delete-confirmation string pattern already in `en.ts` (`services.envDeleteConfirmTitle`, etc.) as the naming convention.

## Steps

1. Add `services.deleteServiceConfirmTitle` / `...Body` / `...Success` / `...Error` (and any button labels) to `en.ts`.
2. Add the matching Chinese translations to the zh locale file.
3. Wire the danger-zone component (t002) to use the new keys instead of hardcoded strings.

## Files

- `dashboard/src/features/services/locales/en.ts`
- `dashboard/src/features/services/locales/zh.ts` (or equivalent)
- `dashboard/src/features/services/components/danger-zone.tsx`

## Acceptance criteria

- [ ] No hardcoded English strings remain in the danger-zone component.
- [ ] Switching the dashboard locale to Chinese renders the translated danger-zone copy.

## Out of scope

- Any locale beyond en/zh.
