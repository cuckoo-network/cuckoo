---
id: w5/m86/t007
title: E2E + docs: allow/deny consent flow verification, env inventories
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m86/t002, w5/m86/t003, w5/m86/t005]
---

## Objective

Prove the whole gate end to end the way the repo already proves OAuth flows — throwaway dockerized Hydra + Kratos, the real dashboard consent route, the real bex-api — and finish the documentation trail (ADR088 §5).

## Context

- Model: `scripts/auth-oauth21-e2e.sh` (RFC discovery → authorize → Kratos-native login → consent → code → token, driving both blessed and unblessed consent paths with curl).
- Two legs to add (new script or an extension): an ops-workspace member completes the code flow for the obs client and the id_token carries the mapped `ops_role`; a customer identity outside the ops workspace is bounced with `error=access_denied` and no code.
- Env inventories are cascading and code-adjacent: backend vars in `lego/backend/CLAUDE.md`, dashboard SSR vars in `dashboard/CLAUDE.md`, names in `.env.example`.

## Steps

1. Add/extend the e2e script with the allow + deny legs (ops client, PKCE S256, no audience).
2. Verify `.env.example` carries every new name (no values) and both CLAUDE.md inventories are complete.
3. Run the script locally (Docker) and record the result in this milestone's README.

## Files

- `scripts/auth-obs-e2e.sh` (new) or `scripts/auth-oauth21-e2e.sh` (extended)
- `lego/backend/CLAUDE.md`, `dashboard/CLAUDE.md`, `.env.example`

## Acceptance criteria

- [ ] Allow leg: token minted, id_token contains `ops_role` mapped from the member's workspace role
- [ ] Deny leg: `error=access_denied`, no authorization code issued
- [ ] Env inventories + `.env.example` complete for every variable this milestone introduced

## Out of scope

- Live prod verification (post-merge via Argo CD — recorded in the milestone README when done)
