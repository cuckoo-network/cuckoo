---
id: w5/m86/t008
title: Simplify — /simplify over the code this milestone changed
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m86/t006, w5/m86/t007]
---

## Objective

Run `/simplify` over the milestone's changed code (consent acceptor branch, bex-api ops-role verb, workspace guards, bootstrap script) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Standing closing task per `.pm` conventions.
- Watch for: the ops gate duplicating logic between the two consent entry points (should share one function, the `pkceSatisfied` precedent), and the ops-role verb reinventing OpenFGA plumbing that `core.Base` already has.

## Steps

1. Run `/simplify`; apply the cleanups; keep suites green.

## Files

- Whatever the milestone diff touched

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly declined with reasons; all suites still green

## Out of scope

- Bug hunting (that's `/code-review`, not this task)
