---
id: w5/m86/t010
title: Closeout
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m86/t009]
---

## Objective

Close w5/m86 once the definition of done actually holds: all suites green, gitops validates, gate/guard behaviors tested, e2e allow+deny legs recorded.

## Context

- Standing final task per `.pm` conventions. Completing it _is_ the move.
- Live `obs.bex.co` verification happens post-merge via Argo CD; record the outcome in the milestone README (a pending live check does not block closeout if the DoD's testable items hold, but note it explicitly).

## Steps

1. Verify the milestone README's definition of done item by item.
2. Set every remaining task `status: done`, move `tNNN.md` files to `w5/m86/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w5/m86/` to `w5/done/m86/`; check `- [x]` in `w5/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m86/**`, `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD verified item by item before any move
- [ ] Status synced in all three places (workstream README, milestone README, task frontmatter)

## Out of scope

- New feature work discovered en route (file as inbox notes)
