---
id: w5/m86/t009
title: Test coverage — meaningful tests for gate, guards, and claims
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m86/t006, w5/m86/t007]
---

## Objective

Audit and complete the milestone's test coverage: every behavior shipped (ops gate allow/deny per role, claim stamping, fail-closed verb outage, constant-time bearer, workspace guards, seat exemption, unset-env inertness) has a test that asserts real behavior and failure modes.

## Context

- Standing closing task per `.pm` conventions — no trivial/tautological/snapshot-everything tests.
- The security-sensitive negatives matter most: verb unreachable ⇒ reject (never accept ungated), wrong bearer ⇒ 401 before any OpenFGA read, non-ops client byte-identical accept body.

## Steps

1. Enumerate shipped behaviors vs existing tests; fill gaps in the backend and dashboard suites.
2. Spot-check by mutation: flip the gate's deny default locally and confirm a test fails.

## Files

- `dashboard/src/common/server-fn/__tests__/hydra-consent.test.ts`
- `lego/backend/internal/api/` + `internal/workspaces/` test files

## Acceptance criteria

- [ ] Every acceptance criterion from t004–t006 maps to at least one asserting test
- [ ] A deliberately broken gate (deny default flipped) fails at least one test

## Out of scope

- E2E legs (t007 owns them)
