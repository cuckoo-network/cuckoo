---
id: w5/m63/t013
title: Test coverage
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m63/t012]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real assertions on real failure modes, not coverage padding.

## Context

- `lego/operator/internal/controller/keyvalue_backup.go` (t004) already has envtest coverage per ADR021 §Verification — extend it to assert the new `encrypt` initContainer's presence, position, and command.
- `scripts/backup-s3-credentials.sh` (t006) needs its own hermetic CI gate mirroring `static-s3-credentials.sh`'s (ShellCheck + PATH-shimmed positive/negative access-matrix tests), per ADR031's existing "a script change must re-earn a live drill record" discipline.
- Restore scripts (t008/t009) already have CI-level ShellCheck + DRY_RUN/corrupt-input/bad-parameter tests (ADR031 §Restore runbooks) — extend rather than duplicate.

## Steps

1. Add/extend Go tests asserting the `kvbak` Job spec's encrypt-step ordering and command (t004).
2. Add ShellCheck + hermetic access-matrix tests for `backup-s3-credentials.sh` (t006).
3. Extend the existing restore-script CI tests to cover the new decrypt/reader-credential steps (t008/t009), including at least one deliberately-corrupt `.age` input and one wrong-key-decrypt-fails case.

## Files

- `lego/operator/internal/controller/keyvalue_backup_test.go` (or equivalent)
- new test file(s) for `scripts/backup-s3-credentials.sh`
- existing restore-script test files

## Acceptance criteria

- [ ] Every new code path (encrypt step, IAM policy scoping, decrypt step, reader-credential swap) has at least one test that fails if the behavior regresses.
- [ ] A wrong/absent age key produces a test-asserted decrypt failure, not a silent pass-through.
- [ ] CI is green across `make test` (operator), `go test ./...` (backend, if touched), and the new shell-script test gates.

## Out of scope

- Live production drills — that's t010, already done before this task runs.
