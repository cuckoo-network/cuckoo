---
id: w5/m1/t005
title: Verify install/typecheck/lint/build/dev all boot clean
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m1/t004]
---

## Objective

Confirm the stripped, rebranded `dashboard/` is a fully working project end to end — not just "files deleted," but installable, type-clean, lintable, and buildable.

## Context

- Scripts available (`dashboard/package.json`): `yarn install`, `yarn typecheck` (runs `generate-routes` + `tsc -b`), `yarn lint` (runs `generate-routes` + `tsc -b` + `eslint .`), `yarn build`, `yarn dev`.
- Deletions in t002 likely left dangling imports, unused `codegen.ts`/GraphQL config, or orphaned i18n keys — this task is where those surface and get fixed.
- If `codegen.ts` was left pointing at the deleted `api-gateway.graphql`, either remove the `codegen` script or point it at nothing until a future milestone wires real GraphQL — don't leave a script that fails if run.

## Steps

1. `cd dashboard && yarn install`
2. `yarn typecheck` — fix any type errors from removed modules.
3. `yarn lint` — fix any lint errors (unused imports, etc.).
4. `yarn build` — confirm it completes.
5. `yarn dev` — confirm the dev server boots and the sample route from t004 renders (manual check or curl the port).
6. Final sweep: `grep -ril beancount dashboard --exclude-dir=node_modules` returns nothing.

## Files

- Whatever t002–t004 touched; this task only fixes fallout, no new scope.

## Acceptance criteria

- [ ] `yarn install`, `yarn typecheck`, `yarn lint`, `yarn build` all exit 0 from `dashboard/`.
- [ ] `yarn dev` boots and serves the sample route without errors.
- [ ] `grep -ril beancount dashboard --exclude-dir=node_modules` returns nothing.

## Out of scope

- CI wiring for `dashboard/` (no existing CI references it yet — future milestone if desired).
