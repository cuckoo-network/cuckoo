---
id: w5/m1/t006
title: Simplify
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m1/t005]
---

## Objective

Run `/simplify` over everything this milestone touched in `dashboard/` — reuse, simplification, efficiency, and altitude cleanups, behavior-preserving.

## Context

- This milestone did a lot of deletion (t002) and some rewriting (t003/t004) under time pressure to get a clean scaffold; likely leftovers include unused dependencies in `package.json` (from removed features like Plaid/OAuth/AI SDK if nothing else uses them), orphaned config (`codegen.ts`, `.tanstack` config referencing removed routes), or dead `common/` helpers only the deleted features used.

## Steps

1. Run `/simplify` scoped to `dashboard/`.
2. Apply behavior-preserving cleanups it finds (unused deps, dead code, redundant config).
3. Re-run `yarn typecheck && yarn lint && yarn build` after changes to confirm nothing broke.

## Files

- `dashboard/` (whatever `/simplify` flags)

## Acceptance criteria

- [ ] `/simplify` run completed over `dashboard/`.
- [ ] `yarn typecheck && yarn lint && yarn build` still pass after applying its suggestions.

## Out of scope

- New features or behavior changes — cleanup only.
