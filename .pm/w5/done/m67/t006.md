---
id: w5/m67/t006
title: Test coverage
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m67/t002, w5/m67/t003, w5/m67/t004]
---

## Objective

Ensure the shipped behavior is covered by meaningful tests — primarily the guard (t003) and a smoke check that the affected routes still mount.

## Context

- The guard test (t003) is the primary regression protection; confirm it asserts real behavior (fails on a reintroduced export).
- Add/confirm a mount smoke test for at least one component-as-pending route so a chunk-grouping regression surfaces in CI.

## Steps

1. Verify the t003 guard fails on a seeded violation and passes on the tree.
2. Add a lightweight render/mount test for a `pendingComponent: RouteComponent` route if not already covered.

## Files

- `dashboard/src/routes/__tests__/*.ts(x)`

## Acceptance criteria

- [ ] Guard test proven to fail on a violation.
- [ ] A component-as-pending route has mount coverage.
- [ ] `yarn test` green.

## Out of scope

- Tautological/snapshot-everything tests.
