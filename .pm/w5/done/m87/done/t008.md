---
id: w5/m87/t008
title: "Test coverage"
worker: worker5
status: done
estimate: 60m
depends_on: ["w5/m87/t006", "w5/m87/t007"]
---

## Objective

Add and run meaningful behavior and failure-mode coverage for the implemented contract, then establish the milestone's observable live evidence.

## Context

- The original w4 findings include live GraphQL/dashboard reproduction; adapter and datastore coverage was code-traced and needs explicit proof.
- Identity mapping must survive history retention and preserve exact live SSH targeting.

## Steps

1. Add regressions that fail against the pre-fix behavior; use real stores/concurrency or nontrivial series fixtures where the invariant requires them.
2. Run affected backend package tests, including the identity/authorization/translation failure cases, then the backend suite and backend lint.
3. Run dashboard typecheck, lint, and tests for changed consumers. Exercise the existing SSH/CLI verifier only against a disposable authorized fixture.
4. Record exact command results and evidence boundaries; clean up the fixtures.

## Files

- `lego/backend/internal/id/id_test.go`
- `lego/backend/internal/apps/`
- `lego/backend/internal/logs/`
- `lego/backend/internal/metrics/`
- `dashboard/src/features/logs/`
- `dashboard/src/features/metrics/components/__tests__/`
- `scripts/ssh-verify.sh`
- `scripts/cli-compat.sh`

## Acceptance criteria

- [ ] Regressions fail against raw pod-name emission and a filter that projects output without translating input.
- [ ] Tests cover current/ranged/limit/discovery, history/live tail, App/Postgres/Key Value, replacement, unknown/foreign IDs, and compatible existing live selectors.
- [ ] A disposable dev-5 walkthrough proves matching IDs and filtering before and after pod replacement, plus an eligible exact-instance SSH/official-CLI control; cleanup and evidence are recorded.

## Out of scope

- Tautological assertions, mock-echo tests, retries/sleeps/skips that mask failures.
- Marking credential-gated live acceptance complete without the required evidence.
