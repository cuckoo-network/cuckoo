---
id: w5/m82/t006
title: Live E2E - entire agent-session workflow end to end on production
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m82/t005]
---

## Objective

After the fixed image ships, run the **entire** agent-session workflow end to end on production and verify every stage live — this is the milestone's definition-of-done proof, and specifically re-runs the exact scenario that failed (`bex-co/bex-security`, 887 refs).

## Context

- Requires `/ship` + `deploy.yml` green + the pinned platform image rolled out to `bex-ssh-gateway` (and bex-api) on `hetzner-prod` first.
- The failed reference session: `ags-da9l9e5a801s739cb2ig` (`tea-d98210cbbpdc73dcrkvg`), failure `HTTP 502 … expected 'packfile'` at clone; audit trail showed per-attempt `info/refs` + `ls-refs` allowed, pack-fetch dying upstream.
- Prior live-verification precedents: w5/m72 (production session advancing a PR), w5/m80 (fast attributed failures), `scripts/agent-session-verify.sh` (w5/m65 — check it still matches current defaults before leaning on it).
- Watch stations: dashboard `https://dashboard.bex.co/agents/<id>` states, `bex-api` completer logs, gateway logs (t003's new lines should stay silent on success), `audit_events` mint/proxy `allowed` rows, and the repo's `bex-agent/*` branch on GitHub.

## Steps

1. Confirm the prod gateway/bex-api pods run the shipped digest.
2. Create a fresh agent session against `bex-co/bex-security` with a small real task (and `openPr` per current default); watch provisioning → clone → turn streaming in the dashboard.
3. Verify the clone succeeded (no 502; gateway logs clean; audit trail shows the pack-fetch `ProxyCredential allowed`).
4. Let the turn complete; verify delivery: session branch pushed through the proxy (this exercises t002's receive-pack path if the push body lands in the gzip band), terminal `completed` status, transcript replay after reattach.
5. Also exercise steer (one follow-up turn) and session teardown/idle reclaim so the workflow is covered end to end, not just the clone.
6. Record evidence (session id, timestamps, audit rows, branch/PR link) in the milestone README before closeout.

## Files

- `.pm/w5/m82/README.md` (evidence record)
- `scripts/agent-session-verify.sh` (reuse/adjust if applicable)

## Acceptance criteria

- [ ] Fresh production session on `bex-co/bex-security` reaches `completed` with a successful clone (zero 502s) and a pushed `bex-agent/*` branch.
- [ ] Steer turn + reattach replay verified live.
- [ ] Evidence recorded in the milestone README.

## Out of scope

- Fixing unrelated live findings — file them as inbox notes / new tasks instead of scope-creeping this run.
