# 012 — Correct stale UI cells in the parity ledger (Service events + Deploys shipped in m21)

Doc-sync only (sub-hour → inbox note, not a milestone).

`docs/ADR018-render-parity.md` still marks these UI cells **✖ → `w5/007`**, but they **shipped in `w5/m21`** (done 2026-07-13; `dashboard/src/routes/services.$serviceId.events.tsx` exists):

- §Services & lifecycle → **Service events / activity feed** UI cell (currently `✖`, note "UI ✖: Events tab → w5/007").
- §Deploys → all four rows' UI cells: **Trigger a deploy**, **List / get deploy objects**, **Cancel deploy**, **Rollback** (each currently `✖`, note "UI → w5/007").

Fix:

1. Flip those five UI cells to **✅** and update the evidence notes to point at `w5/m21` (`services.$serviceId.events.tsx` — Events tab + deploy list with Manual Deploy / Cancel / Rollback).
2. Update the Gap-backlog rows' "UI → `w5/007`" pointers (Deploy cancel+rollback, Service events feed) to "UI done `w5/m21`".
3. `npx prettier@3.4.2 --write "**/*.md"` after.

No code — ledger bookkeeping only.
