---
id: w5/m37/t006
title: Test coverage — meaningful tests for the shipped behavior
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m37/t004]
---

## Objective

Standing closing task. Audit the milestone diff for untested behavior beyond t002/t003's component tests — e.g. the create mutation omitting the field entirely when unset (not sending `null`), the settings clear path, selector hidden for git sources — and add meaningful tests. Never tautological coverage.

## Context

- `dashboard/` test idiom per `dashboard/CLAUDE.md`; the `use-secret-files.test.ts` style of asserting real behavior is the bar.

## Steps

1. Review the diff for untested branches.
2. Add the missing tests; `yarn test`.

## Files

- `dashboard/src/features/services/`

## Acceptance criteria

- [x] Every new behavior branch has a test that fails if it regresses.

## Out of scope

- Coverage-number chasing.
