---
id: w5/m37/t004
title: Render parity — compare against Render's private-image create + settings flows
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m37/t002, w5/m37/t003, w5/m37/t008]
---

## Objective

Standing closing task. Compare the shipped attach flows against Render's dashboard (selector placement, empty state, "None" semantics, whether Render triggers a redeploy on change) and confirm cross-surface consistency: the same `registryCredentialId` value set from the UI reads back identically over REST/GraphQL/MCP (w6/m31's surface). Flag any drift as follow-up notes rather than silently diverging.

## Context

- `docs/ADR018-render-parity.md` registry-credentials row; `docs/render-artifacts/` captures if present.

## Steps

1. Walk Render's flow (docs/captures); note divergences.
2. Cross-surface read-back check on a dev stack.
3. File drift as `w5/NNN` / `w6/NNN` notes.

## Files

- `docs/ADR018-render-parity.md` (row evidence update if warranted)

## Acceptance criteria

- [x] Divergences are either matched or filed with owners — none silent.

## Out of scope

- Fixing backend drift (file to w6).
