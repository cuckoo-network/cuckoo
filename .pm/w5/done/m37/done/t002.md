---
id: w5/m37/t002
title: Credential selector on the image source tab of the create flow
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m37/t001]
---

## Objective

On the service-create flow's image source tab, add a registry-credential `<Select>` populated from the existing credentials hook (default "No credential — public image"), wired into the create mutation, matching Render's private-image deploy flow.

## Context

- `dashboard/src/routes/services.new.tsx:83` — `SourceTab = "github" | "git" | "image"`, `imageVal` state; no credential control today.
- `dashboard/src/features/registry-credentials/` — `use-registry-credentials` list hook + panel components to reuse (shadcn idiom per `dashboard/CLAUDE.md`).
- Render's flow shows the selector only for the image source, with a link to create a credential if none exist.

## Steps

1. Add the selector to the image tab (hidden for git/github sources); empty state links to the settings credentials panel.
2. Pass the selection through the create mutation.
3. Component test for render + selection + omission-when-public.

## Files

- `dashboard/src/routes/services.new.tsx`
- `dashboard/src/features/services/` (component + test)

## Acceptance criteria

- [x] Creating from a private image with a selected credential sends `registryCredentialId`; public-image create sends the backend's explicit empty/no-auth value.

## Out of scope

- Credential CRUD (exists in settings).
