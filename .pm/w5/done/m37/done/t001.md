---
id: w5/m37/t001
title: Plumb registryCredentialId through the dashboard's create/patch GraphQL ops
worker: worker5
status: done
estimate: 30m
depends_on: []
---

## Objective

Add `$registryCredentialId` to the dashboard's service create and update GraphQL operations and regenerate the typed artifacts, so the UI tasks have a wire to attach to.

## Context

- **Gate:** `w6/m31` must have landed `registryCredentialId` on the backend GraphQL create/patch resolvers first (as of 2026-07-15 the REST layer rejects the field as `unsupportedField` at `lego/backend/internal/apps/rest.go:274,598,855` and GraphQL has no binding — exactly what m31 removes). Check m31's status before starting; do not stub against a schema that doesn't expose the field.
- `dashboard/src/features/services/api/services.graphql` — the create mutation passes `image: $image` with no credential variable today.
- Codegen: the repo's generated `definitions.ts`/`operations.ts` flow (`dashboard/CLAUDE.md`).

## Steps

1. Confirm the field exists in the backend schema (dev-5 stack or the checked-in generated schema).
2. Add the variable to create + update operations; regenerate; typecheck.

## Files

- `dashboard/src/features/services/api/services.graphql`
- generated GraphQL artifacts

## Acceptance criteria

- [x] Create/patch operations accept and forward `registryCredentialId`; `yarn test` + typecheck pass.

## Out of scope

- Any backend change (w6/m31 owns it).
