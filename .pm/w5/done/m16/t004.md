---
id: w5/m16/t004
title: Place stepper in Settings, respecting type-aware hiding
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m16/t003]
---

## Objective

Place the stepper in `dashboard/src/routes/services.$serviceId.settings.tsx`, hidden for cron jobs per `w5/m11`'s type-aware settings pattern (a cron job has no HTTP traffic to scale).

## Context

- `w5/m11` already established the type-aware pattern: hide Custom Domains + Idle timeout, show Schedule + Command for `type=cron_job`.

## Steps

1. Add the manual-scaling section to the Settings route, following m11's existing type-branch structure.
2. Gate it out for `type=cron_job`; keep it for web/worker/private services.

## Files

- `dashboard/src/routes/services.$serviceId.settings.tsx`

## Acceptance criteria

- [ ] Cron job Settings pages do not show the stepper; web/worker services do.

## Out of scope

- Tests (t005).
