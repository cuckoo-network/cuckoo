---
id: w5/m51/t003
title: Render parity — cross-surface consistency check
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m51/t002]
---

## Objective

Check buildCommand consistency across every surface: REST PATCH `/v1/services/{id}` (Render field shape), GraphQL `setBuildCommand`, MCP `set_build_command`, and the new UI row — same per-type gating semantics (native vs docker vs image-backed), same clear-to-default behavior, same error shapes. Compare against Render's live page and API docs; flag drift as follow-ups.

## Context

- `docs/ADR018-render-parity.md` build-command row — update the UI cell evidence (static-only → all native types).
- `lego/backend/internal/api` — verify PATCH rejects/ignores buildCommand for docker-runtime consistently with what the UI now implies.

## Steps

1. Exercise the four surfaces against dev-5 for web/static/cron/docker services.
2. Update the ADR018 UI cell + evidence pointer.
3. File follow-up notes for any drift.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Four-surface walk recorded; ADR018 cell updated; drift filed or none found.

## Out of scope

- Fixing backend drift found (file as notes unless trivial).
