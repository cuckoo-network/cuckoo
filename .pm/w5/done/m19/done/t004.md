---
id: w5/m19/t004
title: "Confirm unauthenticated-visitor SSR behavior is unaffected"
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m19/t002]
---

## Objective

Confirm that a visitor with no Kratos session still gets correct public-route SSR behavior after t002's fix — no `Cookie` header is forwarded when there's no session, and this doesn't crash or misbehave.

## Steps

1. Load a public route (e.g. a login page) with no session cookie present; confirm SSR renders correctly with no `Cookie` header sent to bex-api.
2. Confirm no error/crash when `getCookie`/the session lookup returns nothing.

## Acceptance criteria

- [ ] Unauthenticated SSR requests behave exactly as before t002 (no crash, correct public rendering).
