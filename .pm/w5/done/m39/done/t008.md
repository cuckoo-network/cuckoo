---
id: w5/m39/t008
title: Test coverage (standing)
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m39/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped, asserting real behavior and failure modes — not snapshots or tautologies.

## Context

- Dashboard: alias-redirect tests per segment incl. sub-path + query preservation, and the negative case (unknown segment → 404 catch-all).
- Backend: `dashboardUrl` per service type (all five segments + fallback), postgres `/d/`, key-value `/r/`, and the unset-`BEX_DASHBOARD_URL` omit case staying intact.

## Steps

1. Fill gaps t002–t004's inline tests left (especially cross-adapter equality and the omit-when-unset case).
2. `yarn test` + `cd lego/backend && go test ./...` green.

## Files

- `dashboard/src/routes/__tests__/`
- `lego/backend/internal/api/resource_metadata_test.go` and per-package `*_test.go`

## Acceptance criteria

- [ ] Every emitted URL shape and every alias redirect has a failing-first-capable assertion.
- [ ] Suites pass.

## Out of scope

- Coverage-gaming tests.
