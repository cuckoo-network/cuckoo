---
id: w5/m39/t001
title: Capture Render dashboard URL-scheme evidence → docs/render-artifacts/dashboard-routes.md
worker: worker5
status: done
estimate: 30m
depends_on: []
---

## Objective

Write the evidence document the rest of the milestone implements against: one table of Render's dashboard URL patterns per resource type (and per service sub-tab), each row with its source — the `render-oss/cli` source, live `dashboard.render.com` captures where a session is available, or public docs fallback (the `docs/render-artifacts/` house method).

## Context

- Primary source already verified 2026-07-16: `render-oss/cli` `pkg/dashboard/dashboard.go` maps resource → segment: `web`, `worker`, `pserv`, `static`, `cron`, `r` (Key Value), `d` (Postgres), `wf` (workflows — bex non-goal, DO_NOT_DO); deploys `/{segment}/{id}/deploys/{deployId}`; unknown types fall back to `web`.
- Open questions to settle in this task: Render's individual blueprint path (`/blueprint/{id}` vs `/blueprints/{id}` — `dashboard.render.com/blueprint/new` exists) and env-group detail path (believed `/env-groups/{evg-id}`); service sub-tab paths (`/deploys`, `/events`, `/logs`, `/env`, `/settings`, `/metrics`, `/scaling`) vs bex's tab routes (`dashboard/src/routes/services.$serviceId.*.tsx`).
- Existing captures follow `docs/render-artifacts/*.md` conventions (method + date header; screenshots to `.playwright-mcp/`, bare filenames).

## Steps

1. Re-read `render-oss/cli` `pkg/dashboard/dashboard.go` (raw GitHub) and record the exact switch.
2. If a live Render session is available, walk one service/postgres/keyvalue/blueprint/env-group and record the address-bar URLs (Playwright MCP); otherwise mark rows docs-fallback.
3. Write `docs/render-artifacts/dashboard-routes.md`: pattern table (resource → path), sub-tab table, bex-current column, and the alias/redirect decision from the milestone README.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/render-artifacts/dashboard-routes.md` (new)

## Acceptance criteria

- [ ] Every resource type bex serves (service×5 types, postgres, key value, project, env group, blueprint) has a row with a cited source.
- [ ] Service sub-tab mapping (Render path ↔ bex route file) is explicit, including `/deploys/{deployId}`.

## Out of scope

- Any code change; workflows (`wf`) routes (off-roadmap).
