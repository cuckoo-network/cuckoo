---
id: w5/m39/t003
title: Dashboard alias routes — /d/$id → /databases, /r/$id → /keyvalue
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m39/t001]
---

## Objective

Alias Render's datastore paths: `/d/$databaseId` redirects to `/databases/$databaseId` and `/r/$keyValueId` redirects to `/keyvalue/$keyValueId`, reusing t002's redirect helper. Verify (and fix if t001 found drift) the already-matching `/project/$projectId`, `/env-groups/$groupId`, and blueprint paths.

## Context

- Canonical routes: `dashboard/src/routes/databases.$databaseId.tsx`, `keyvalue.$keyValueId.tsx`, `project.$projectId.tsx`, `env-groups_.$groupId.tsx`, `blueprints.$blueprintId.tsx`.
- Render segments per `render-oss/cli`: `d` (Postgres, `dpg-` ids), `r` (Key Value, `red-` ids) — bex mints the same prefixes (`lego/backend/internal/id/id.go`).
- If t001 shows Render's blueprint detail is `/blueprint/{id}` (singular), add that alias too.

## Steps

1. Add `/d/$id` and `/r/$id` alias routes redirecting to the canonical pages (query preserved).
2. Add any blueprint/env-group alias t001's table requires.
3. Route tests for each alias.

## Files

- `dashboard/src/routes/` (new alias route files)
- `dashboard/src/routes/__tests__/`

## Acceptance criteria

- [ ] `/d/dpg-…` and `/r/red-…` land on the correct detail pages.
- [ ] Any t001-identified blueprint/env-group drift is aliased or explicitly recorded as none.

## Out of scope

- Datastore sub-tab aliasing beyond what t001 documents Render actually has.
