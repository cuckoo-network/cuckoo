---
id: w5/m39/t006
title: Render parity (standing)
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m39/t004, w5/m39/t005]
---

## Objective

Check the milestone's change is consistent across every surface it exposes: `dashboardUrl` fields agree across REST, GraphQL, and MCP in `lego/backend/` (same values, same type-aware segments, same omit-when-unset behavior), and the dashboard alias routes cover every URL shape bex-api now emits. Compare against Render's real behavior (t001's evidence) and file any drift as follow-up notes rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` (one core, thin adapters); `docs/render-artifacts/dashboard-routes.md` (t001).
- Check: REST services/postgres/key-value lists + detail, GraphQL `dashboardUrl` resolvers, MCP tool outputs, and the dashboard's own internal links (should they surface Render-shaped URLs anywhere user-copyable?).

## Steps

1. Exercise each adapter (dev-5 stack or unit fixtures) and diff `dashboardUrl` values.
2. Click through each alias route on the running dashboard.
3. File follow-up inbox notes for any drift found.

## Files

- (verification only; notes under `.pm/w5/` if drift found)

## Acceptance criteria

- [ ] All three API surfaces emit identical Render-shaped `dashboardUrl` values.
- [ ] Every emitted URL shape resolves on the dashboard.

## Out of scope

- Fixing unrelated parity drift discovered along the way (file notes).
