---
id: w5/m39/t007
title: Simplify (standing)
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m39/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (dashboard alias routes + backend `resourcemeta`/`apps`/`postgres`/`keyvalue` URL construction): reuse, simplification, efficiency — behavior-preserving only.

## Context

- Likely targets: five near-identical alias route files (shared helper), duplicated segment-mapping tables between dashboard and backend.

## Steps

1. `/simplify` scoped to the milestone's diff; apply safe findings.
2. Re-run `yarn test` (dashboard) and `go test ./...` (backend).

## Files

- Files touched by t002–t004

## Acceptance criteria

- [ ] No behavior change; tests still pass.

## Out of scope

- Bug hunting (that's /code-review territory).
