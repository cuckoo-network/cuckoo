---
id: w5/m39/t005
title: Update docs/cli-compatibility-checklist.md + ADR018 parity-ledger evidence
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m39/t004]
---

## Objective

Record the shipped compatibility in the two evidence ledgers: the CLI checklist rows whose open-in-dashboard URLs previously 404ed, and any `docs/ADR018-render-parity.md` cells/notes that mention `dashboardUrl` shape or dashboard deep links.

## Context

- `docs/cli-compatibility-checklist.md` is the fifth-surface ledger (w9/m2 owns it); the Render CLI constructs `<dashboard>/{segment}/{id}` URLs client-side — after t002–t004 these resolve.
- `docs/ADR018-render-parity.md` is the capability ledger; check the service/postgres/key-value metadata rows for `dashboardUrl` notes.
- Cross-link `docs/render-artifacts/dashboard-routes.md` (t001) as the evidence source.

## Steps

1. Sweep both ledgers for rows affected by Render-shaped dashboard URLs; update status/evidence with pointers to t001's artifact and the shipped routes.
2. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/cli-compatibility-checklist.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] No ledger row still claims dashboard URLs diverge from Render's shape.
- [ ] Evidence cells cite `docs/render-artifacts/dashboard-routes.md`.

## Out of scope

- Re-running the full CLI checklist (only the dashboard-URL-affected rows).
