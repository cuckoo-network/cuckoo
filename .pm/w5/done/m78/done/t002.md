---
id: w5/m78/t002
title: "Remove duplicate Archived navigation (sidebar vs list tabs)"
worker: worker5
status: done
estimate: 25m
depends_on: []
---

## Objective

Expose **one** Archived entry point on `/agents` — not both the global sidebar section (`agent-sessions-nav-section.tsx`) and the list filter tabs on `agents.tsx`.

## Context

- QA screenshot `qa-agents-02-archived.png`: "Archived" appears in the left nav rail and again as a tab above the session list.
- `dashboard/src/routes/agents.tsx` — list tabs (`Recent` / `Archived` / `All`) with `?archived=` search param.
- `dashboard/src/common/components/dashboard-layout/agent-sessions-nav-section.tsx` — sidebar Archived link to the same filter.
- m64 folded agent sessions into the single nav rail; the list tabs predate that merge.

## Steps

1. Pick one canonical control (recommend: keep list tabs on `/agents`, remove sidebar Archived duplicate — matches Devin list-header pattern).
2. Preserve deep links (`?archived=archived`) and back-navigation from detail (`fromArchived` search on `agents_.$agentSessionId.tsx`).
3. Update locales/tests that assert both entry points.

## Files

- `dashboard/src/common/components/dashboard-layout/agent-sessions-nav-section.tsx`
- `dashboard/src/routes/agents.tsx`
- related tests under `dashboard/src/features/agent-sessions/` and layout tests

## Acceptance criteria

- [ ] Exactly one visible Archived control on `/agents`.
- [ ] Archived filter still works via URL and detail back-link.

## Out of scope

- Mobile nav (unless the same duplicate exists there).
