---
id: w5/m78/t003
title: "Steering redispatch: show provisioning/wait, not stream unavailable"
worker: worker5
status: done
estimate: 35m
depends_on: []
---

## Objective

When a steering turn triggers redispatch (new sandbox provisioning), the conversation column should show a **provisioning / waiting** state — not the generic "conversation stream is unavailable" error.

## Context

- QA: after steer, UI showed "stream unavailable" while session was `redispatching` / sandbox still provisioning.
- `dashboard/src/features/agent-sessions/components/session-chat-column.tsx` — provisioning gate comment at line 27.
- `session-conversation-impl.tsx` / `steering-composer.tsx` — stream error vs phase priority order likely treats a transient detach as hard failure.
- Existing test fixture at `session-conversation-impl.test.tsx:455` asserts the unavailable string — extend with redispatch/provisioning case.

## Steps

1. Map session phases `creating` / `redispatching` / non-terminal + no attach ticket → provisioning UI (reuse existing spinner/copy if present).
2. Reserve "stream unavailable" for genuine attach/stream failures after provisioning should have completed.
3. Add/adjust unit test for redispatch phase ordering.

## Files

- `dashboard/src/features/agent-sessions/components/session-chat-column.tsx`
- `dashboard/src/features/agent-sessions/components/session-conversation-impl.tsx`
- `dashboard/src/features/agent-sessions/components/steering-composer.tsx` (if phase passed here)
- `dashboard/src/features/agent-sessions/components/__tests__/session-conversation-impl.test.tsx`

## Acceptance criteria

- [ ] Redispatching session: no "stream unavailable" banner while phase is non-terminal and sandbox is provisioning.
- [ ] Completed/failed sessions: existing transcript / error behavior unchanged.

## Out of scope

- Backend redispatch timing or sandbox-create failures (see `w5/048.md`).
