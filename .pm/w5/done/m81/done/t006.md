---
id: w5/m81/t006
title: Closeout
worker: worker5
status: done
estimate: 10m
depends_on: [w5/m81/t005]
---

## Objective

Close the milestone once the new lifecycle series are visible on `/metrics` with correct labels, the SLO baseline is recorded in ADR047, and the emission tests pass.

## Context

- Closing this task triggers the milestone move (`/pm done w5/m81/t006`): set every task `status: done`, move each `tNNN.md` to `w5/m81/done/`, mark rows `— DONE` and `**Status:** done` in the milestone README, move `w5/m81/` to `w5/done/m81/`, and check `- [x]` in `w5/README.md`.

## Steps

1. Confirm all new series render on `/metrics` with correct names/labels and the tests pass.
2. Confirm ADR047 carries the SLO baseline.
3. Run the milestone move.

## Files

- `.pm/w5/m81/*`, `.pm/w5/README.md`

## Acceptance criteria

- [ ] All new series verified on `/metrics`; tests green.
- [ ] ADR047 SLO baseline in place.
- [ ] Milestone moved to `w5/done/m81/` and checked in the w5 README.

## Out of scope

- Any follow-ups → new inbox notes.
