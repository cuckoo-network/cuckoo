---
id: w5/m81/t004
title: Simplify
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m81/t003]
---

## Objective

Run `/simplify` over the instrumentation added by this milestone — metric definitions, the emit points in the Completer/dispatch/gateway, and the registry wiring — for reuse and efficiency without changing what is emitted.

## Context

- Touched paths: `lego/backend/internal/agentsessions/completion_metrics.go`, `completion.go`, `service.go`; `lego/backend/internal/sandbox/service.go`; `lego/backend/internal/sshgateway/modelproxy/modelproxy.go`; `lego/backend/internal/api/server.go`.
- Watch for a single metrics-emit helper rather than duplicated timer/observe boilerplate across the three emit sites.

## Steps

1. `/simplify` over the milestone's diff.
2. Consolidate duplicated timer/observe boilerplate into one helper where it reads cleanly.
3. Keep the emitted series and labels identical.

## Files

- The milestone's changed files (above)

## Acceptance criteria

- [ ] `/simplify` applied; emitted series/labels unchanged.
- [ ] No duplicated timer/observe boilerplate across emit sites.

## Out of scope

- New metrics or new emit points.
