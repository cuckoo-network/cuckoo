---
id: w5/m81/t005
title: Test coverage
worker: worker5
status: done
estimate: 40m
depends_on: [w5/m81/t004]
---

## Objective

Add meaningful tests that the new lifecycle metrics register and emit with correct labels on real session transitions — asserting behavior (a terminal transition observes a duration; a first model call marks time-to-first-call), not tautological registry presence alone.

## Context

- Backend suites run against real Postgres + OpenFGA in CI; the Completer and model proxy have existing table-driven tests (`completion_test.go`, `modelproxy_test.go`).
- Prometheus test helpers (`testutil.ToFloat64`/`CollectAndCount`) can assert observations without snapshotting values.

## Steps

1. Assert a terminal turn/session transition observes the turn-duration and terminal-convergence histograms once (and steer/rehydrate do not double-count).
2. Assert a sandbox reaching running observes the provision-latency signal, and a failed-to-provision path is labelled distinctly.
3. Assert the first proxied model request marks time-to-first-model-call once per session.

## Files

- `lego/backend/internal/agentsessions/completion_test.go`
- `lego/backend/internal/sshgateway/modelproxy/modelproxy_test.go`
- new/extended metrics test alongside `completion_metrics.go`

## Acceptance criteria

- [ ] Turn-duration/convergence observations asserted (once each; no double-count on steer/rehydrate).
- [ ] Provision-latency and time-to-first-model-call observations asserted with correct labels.
- [ ] Backend `go test ./...` passes.

## Out of scope

- Value-snapshot tests (assert emission + labels + count, not exact durations).
