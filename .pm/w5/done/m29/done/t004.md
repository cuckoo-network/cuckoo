---
id: w5/m29/t004
title: Build-log pane over `type=build` (store-absent state)
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m29/t003]
---

## Objective

Render deploy-window logs through the existing viewer, including `type=build` alongside pre-deploy and app lines. Store-less build history gets the explicit "build logs need the log store" note — never a broken pane.

## Context

- **Cross-milestone dependency: `w7/m28`.** If it hasn't landed, ship the pane with its absent-state only and note it in the closeout.

## Acceptance criteria

- [x] With the store + m28: real build lines render interleaved chronologically; without it: the explanatory note appears while non-build log legs remain usable.
