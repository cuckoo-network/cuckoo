---
id: w5/m29/t003
title: Status timeline (deploy row + service events)
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m29/t002]
---

## Objective

Render a vertical timeline for the deploy: created → build/running transitions → terminal state, composed from the deploy row's timestamps + the service-events feed (w3/m7) filtered to this deploy's window.

## Acceptance criteria

- [x] Live, failed, and canceled deploys each render an honest timeline (no fabricated intermediate states bex doesn't track).
