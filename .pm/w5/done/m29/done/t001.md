---
id: w5/m29/t001
title: Deploy detail route + header (status · trigger · commit)
worker: worker5
status: done
estimate: 45m
depends_on: []
---

## Objective

Add the `/services/$serviceId/deploys/$deployId` route with a header rendering the deploy's status chip, trigger, commit (id/message when git-sourced), image, and started/finished timestamps — Render's deploy-page header shape.

## Context

- Deploys list + actions shipped in w5/m21 (`features/` deploys, `services.$serviceId.events.tsx` siblings); read `dashboard/CLAUDE.md` first.
- Preserve w9/001's commit id/message fields and tolerate the fuller status vocabulary filed under w2/m38.

## Files

- `dashboard/src/routes/`, `dashboard/src/features/` (deploys)

## Acceptance criteria

- [x] Route renders for a real deploy id; unknown id shows a not-found state; `yarn test` green.
