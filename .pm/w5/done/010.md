# Cron settings: Auto Deploy toggle for git-sourced cron jobs (w5/m18 deferral)

Sub-hour, unblocked. `dashboard/src/routes/services.$serviceId.settings.tsx:82-91` renders `CronDeploySection` **instead of** `BuildDeploySection`, so a git-sourced cron job loses the Auto Deploy toggle (and the Root Directory display) even though the backend `setAutoDeploy` path is type-agnostic (w2/m9). Surface the toggle for cron — fold it into `CronDeploySection` or render the Build & Deploy section alongside — after verifying placement against the w5/m18 Render cron capture and the ADR018 cron row's deferral list (Build Command and Log Stream stay deferred/non-goal; Notifications is `w3/m9`'s).

Source: `/pm-brainstorm more for w5` 2026-07-12 (the w5/m18 cron-row ◐ deferral).
