---
id: w5/m83/t002
title: Live E2E - fresh production session on bex-co/web-beancount completes
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m83/t001]
---

## Objective

After rollout, a fresh production agent session on `bex-co/web-beancount` (549 MiB clone pack) clones through the proxy without truncation and completes its turn — the exact scenario `ags-da9mh5vj596c73en5eq0` failed on every attempt.

## Steps

1. Confirm prod `bex-ssh-gateway` runs the pinned digest carrying t001.
2. Create a fresh session (small task) on `bex-co/web-beancount`; poll to `completed`.
3. Verify: single clone attempt in `audit_events` (pack fetch `ProxyCredential allowed` once, ~30+ s stream), gateway logs free of `response_cap`/`stream` failures, delivery branch pushed.
4. Record evidence in the milestone README.

## Acceptance criteria

- [ ] Session reaches `completed` with a successful large-pack clone (no retry loop).
- [ ] Evidence recorded.
