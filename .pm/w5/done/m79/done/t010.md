---
id: w5/m79/t010
title: Workspace create and settings skeletons
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m79/t009]
---

## Objective

Complete `/new/workspace` and `/workspace/settings` with form/card skeletons that never guess workspace identity, plan, or permissions.

## Context

- Create already uses `FormPageSkeleton`; Settings has in-page `CardSkeleton`s.
- Workspace switching must clear the prior workspace before new data becomes ready.

## Steps

1. Exercise cold route loads and in-place workspace changes.
2. Reconcile route-level and data-level skeleton ownership.

## Files

- `dashboard/src/routes/new.workspace.tsx`
- `dashboard/src/routes/workspace.settings.tsx`
- workspace route tests

## Acceptance criteria

- [ ] Both URLs reserve their final form/card layout.
- [ ] No prior workspace name, plan, or enabled action survives a switch.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Workspace pricing or membership semantics.
