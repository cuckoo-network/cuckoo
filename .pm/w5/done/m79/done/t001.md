---
id: w5/m79/t001
title: Freeze the 82-route manifest and skeleton contract
worker: worker5
status: done
estimate: 45m
depends_on: []
---

## Objective

Turn the generated route tree into a fail-closed, test-readable manifest that assigns every route a skeleton owner or an explicit no-skeleton disposition.

## Context

- Source of truth: `dashboard/src/routeTree.gen.ts` (`FileRoutesByFullPath`, 82 patterns on 2026-08-24).
- Reuse `detail-skeletons.tsx`, `list-route-skeletons.test.ts`, and the pending-state rules in `dashboard/CLAUDE.md`.

## Steps

1. Add the manifest and deterministic pending-delay test seam.
2. Encode the contract for rendering, redirect, layout/index, catch-all, and server-only routes.
3. Fail when a generated path is missing or a stale path survives.

## Files

- `dashboard/src/routes/__tests__/`
- `dashboard/src/common/components/detail-skeletons.tsx`
- `dashboard/CLAUDE.md`

## Acceptance criteria

- [ ] All 82 paths have exactly one disposition.
- [ ] Adding an undispositioned route fails the focused test.
- [ ] Tests can hold a route in pending state without production sleeps.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Route-specific visual changes; t002-t023 own them.
