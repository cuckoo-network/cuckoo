---
id: w5/m79/t018
title: Service Scaling, Plan, and Disk skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t017]
---

## Objective

Audit `/services/$serviceId/scaling`, `/plan`, and `/disk` sequentially so capacity/billing/storage controls never pop in or imply availability before type, plan, and capability data settle.

## Context

- Scaling and Plan have partial skeletons; Disk is a newer paid-only surface.

## Steps

1. Exercise supported, unsupported, free, paid, and permission-loading branches.
2. Add or align card/form skeletons and reserve action slots.

## Files

- `dashboard/src/routes/services.$serviceId.{scaling,plan,disk}.tsx`
- related feature tests

## Acceptance criteria

- [ ] Each URL has a route-specific loading shape.
- [ ] Unsupported/paid/capability copy appears only after the deciding data resolves.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Plan prices, autoscaling mechanics, or disk provisioning.
