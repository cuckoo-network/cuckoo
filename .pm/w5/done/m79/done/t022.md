---
id: w5/m79/t022
title: Static-site Headers, Redirects, and Settings skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t021]
---

## Objective

Complete `/static/$serviceId/headers`, `/redirects`, and `/settings` with stable editor/form skeletons and safe write gating.

## Context

- Header/redirect routes share implementations with the service tree; Settings is static-specific through the common detail shell.

## Steps

1. Delay service, capability, repo/branch, and rules data for each URL.
2. Reconcile duplicate adapters and keep one loading owner per panel.

## Files

- `dashboard/src/routes/static.$serviceId.{headers,redirects,settings}.tsx`
- shared static settings/rules tests

## Acceptance criteria

- [ ] All three URLs preserve final editor/form geometry.
- [ ] No old rules or enabled writes survive a service-id transition.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Header, rewrite, redirect, or service-update semantics.
