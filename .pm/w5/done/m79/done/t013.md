---
id: w5/m79/t013
title: Logout, consent, device, success, and invite skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t012]
---

## Objective

Complete `/auth/logout`, `/auth/consent`, `/auth/device` (layout + index), `/auth/device/success`, and `/invite` one at a time with truthful auth-shell pending states.

## Context

- Consent/device routes bridge server handlers and client navigation; `/invite` may be anonymous or authenticated.
- Preserve the shell treatment shipped in `w10/m7-m8`.

## Steps

1. Delay document handlers, loader handoff, and invite token/session checks for each route.
2. Fix blank documents, stale client identity, and indefinite skeletons.

## Files

- `dashboard/src/routes/auth.{logout,consent,device*}.tsx`
- `dashboard/src/routes/invite.tsx`
- related auth/invite tests

## Acceptance criteria

- [ ] Every route reaches ready, redirect, or explicit error from its skeleton.
- [ ] The device layout/index pair renders one page, never a blank or duplicate shell.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Hydra/Kratos protocol changes.
