---
id: w5/m79/t005
title: Blueprints list, create, and detail skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t004]
---

## Objective

Complete `/blueprints`, `/blueprints/new`, and `/blueprints/$blueprintId` in that order with list-, form-, and detail-shaped loading UI.

## Context

- The routes already use `ListPageSkeleton`, `FormPageSkeleton`, and detail `CardSkeleton`s.
- GitHub connection/repo/branch and sync-preview subloads must not collapse the form or expose stale data.

## Steps

1. Test each URL under route and data delay.
2. Correct mismatched layout, duplicate loading layers, and stale blueprint transitions.

## Files

- `dashboard/src/routes/blueprints*.tsx`
- blueprint route/component tests

## Acceptance criteria

- [ ] All three URLs have distinct final-shape skeletons.
- [ ] Detail-id changes and create-form dependencies never show prior resolved content.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Blueprint sync diagnostics owned by `w10/m11`.
