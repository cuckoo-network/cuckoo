---
id: w5/m79/t002
title: Overview route skeleton
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m79/t001]
---

## Objective

Make `/` reserve the final project/resource-table shape while its four overview queries are cold.

## Context

- `dashboard/src/routes/index.tsx` already has an in-page `ResourceTable` skeleton; audit it rather than duplicating it.
- The shell/header must remain mounted and warm-cache navigation must not flash a skeleton.

## Steps

1. Exercise cold, partial, warm, error, and empty states.
2. Fix only shape, stale-copy, or layout-shift gaps.

## Files

- `dashboard/src/routes/index.tsx`
- `dashboard/src/routes/__tests__/index.test.tsx`

## Acceptance criteria

- [ ] Cold `/` shows a content-shaped skeleton; partial data remains visible.
- [ ] Warm data paints without a skeleton flash or duplicate fetch.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Changing overview data queries or resource semantics.
