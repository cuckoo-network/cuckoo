---
id: w5/m79/t016
title: Service Logs and Metrics skeletons
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m79/t015]
---

## Objective

Verify `/services/$serviceId/logs` and `/services/$serviceId/metrics` against the high-traffic skeleton baseline from `w9/m63` and close any remaining cold-load gaps.

## Context

- `LogPanelSkeleton` and chart-height skeletons already exist; do not duplicate them.
- Loading must not appear as “No data in range” or an empty log viewer.

## Steps

1. Delay service-type, initial logs, live-tail, metric, and event-timeline sources.
2. Fix only uncovered transition or geometry defects.

## Files

- `dashboard/src/routes/services.$serviceId.{logs,metrics}.tsx`
- log/metric components and tests

## Acceptance criteria

- [ ] Neither route goes blank or shows an empty/error claim during loading.
- [ ] Existing virtualization and polling behavior are unchanged.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Chart queries, log retention, or virtualization changes.
