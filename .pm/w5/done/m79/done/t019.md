---
id: w5/m79/t019
title: Service Shell, Headers, and Redirects skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t018]
---

## Objective

Complete `/services/$serviceId/shell`, `/headers`, and `/redirects` with truthful command/editor skeletons and type-gated transitions.

## Context

- Shell needs service address/status/SSH-key state; Headers and Redirects are static-site capabilities reachable through the shared service tree.

## Steps

1. Delay service type and each page's data source.
2. Prevent command leakage, wrong-type flashes, blank editors, and enabled writes before readiness.

## Files

- `dashboard/src/routes/services.$serviceId.{shell,headers,redirects}.tsx`
- related tests

## Acceptance criteria

- [ ] All three URLs reserve their final command/editor/card geometry.
- [ ] Sensitive command details and type-specific controls appear only when authorized data is ready.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- SSH execution or static-rule semantics.
