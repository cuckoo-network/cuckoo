---
id: w5/m79/t017
title: Service Environment and Settings skeletons
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m79/t016]
---

## Objective

Complete `/services/$serviceId/env` and `/services/$serviceId/settings` with stable field/table placeholders and no stale editable values.

## Context

- Settings has `FieldRowsSkeleton`; Environment uses shared service/env components and capability gates.

## Steps

1. Delay service, capability, env-var, repo, and branch data separately.
2. Ensure no old service values remain editable during id changes.

## Files

- `dashboard/src/routes/services.$serviceId.{env,settings}.tsx`
- service settings/environment tests

## Acceptance criteria

- [ ] Both routes preserve final form/table geometry.
- [ ] Save/delete controls remain disabled until resource and capability state are ready.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Settings field semantics or authorization policy.
