---
id: w5/m79/t014
title: Service create, shell/root redirect, and deploy-list skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t013]
---

## Objective

Complete `/services/new`, the `/services/$serviceId` shell/index redirect pair, and `/services/$serviceId/deploys` without generic pending UI or duplicate service chrome.

## Context

- The parent route reuses `RouteComponent` as `pendingComponent`; the index performs a data-dependent type redirect.
- Create depends on connections, repos, registries, projects, and environments.

## Steps

1. Verify create-form dependency delays.
2. Verify parent/index/deploy-list transitions for compute and static service ids.
3. Preserve component/pending chunk sharing.

## Files

- `dashboard/src/routes/services.new.tsx`
- `dashboard/src/routes/services.$serviceId.tsx`
- `dashboard/src/routes/services.$serviceId.index.tsx`
- `dashboard/src/routes/services.$serviceId.deploys.index.tsx`

## Acceptance criteria

- [ ] Create and deploy-list shapes are stable under cold data.
- [ ] Root redirect shows one service frame and lands on the correct type route.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Service create/deploy semantics.
