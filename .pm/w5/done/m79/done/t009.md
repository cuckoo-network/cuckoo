---
id: w5/m79/t009
title: Postgres and Key Value detail/create skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t008]
---

## Objective

Audit `/databases/$databaseId`, `/keyvalue/$keyValueId`, and `/keyvalue/new` one by one, keeping metadata/cards/forms stable while catalog and resource data resolve.

## Context

- Both detail routes already use `MetadataListSkeleton` and `CardSkeleton`; Key Value create has catalog-dependent fields.
- Connection secrets must never appear in skeleton or stale content.

## Steps

1. Verify each detail route's shell and lazy panels.
2. Add/fix the create-route form skeleton and remove overlapping loading layers.

## Files

- `dashboard/src/routes/databases.$databaseId.tsx`
- `dashboard/src/routes/keyvalue*.tsx`
- datastore route tests

## Acceptance criteria

- [ ] All three URLs have faithful, non-sensitive skeletons.
- [ ] Resource-id transitions clear old metadata before the new result settles.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Datastore provisioning behavior or plan catalog changes.
