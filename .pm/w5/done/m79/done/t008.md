---
id: w5/m79/t008
title: Project overview and settings skeletons
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m79/t007]
---

## Objective

Replace the generic pending spinner on `/project/$projectId` and `/project/$projectId/settings` with the contextual project frame and page-shaped content.

## Context

- `project.$projectId.tsx` currently mounts `RoutePending` inside the project shell.
- The index loads resource groups; Settings loads project state and destructive controls.

## Steps

1. Add a reusable project-frame skeleton.
2. Verify root and Settings independently, including project-id changes and not-found redirects.

## Files

- `dashboard/src/routes/project.$projectId*.tsx`
- project route tests

## Acceptance criteria

- [ ] Both URLs retain contextual navigation and reserve final content geometry.
- [ ] No generic centered spinner, stale project name, or enabled destructive control appears while loading.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Project CRUD behavior.
