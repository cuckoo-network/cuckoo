---
id: w5/m79/t027
title: Test coverage
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t025]
---

## Objective

Add meaningful automated coverage for the route manifest and every skeleton family, including failure modes that would restore blank, generic, stale, or duplicate loading UI.

## Context

- Extend behavior tests; do not snapshot every route.
- Tests must prove route coverage, pending-to-ready transition, no stale resource, and redirect/server exclusions.

## Steps

1. Add manifest completeness and disposition tests.
2. Add representative behavior tests for each skeleton family and layout/index ownership pattern, asserting the route-specific structural regions that must survive the pending-to-ready swap rather than only checking that some skeleton exists.
3. Run dashboard typecheck, lint, and full test suite.

## Files

- `dashboard/src/routes/__tests__/`
- adjacent feature/component tests

## Acceptance criteria

- [ ] Tests fail if a route loses its disposition or regresses to blank/generic/stale UI.
- [ ] `yarn typecheck`, `yarn lint`, and `yarn test` pass.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Tautological snapshots or coverage-only assertions.
