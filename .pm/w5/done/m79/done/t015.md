---
id: w5/m79/t015
title: Service deploy-detail and Events skeletons
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m79/t014]
---

## Objective

Make `/services/$serviceId/deploys/$deployId` and `/services/$serviceId/events` reserve timeline, log, and action geometry while independent queries resolve.

## Context

- Deploy detail intentionally mounts regions in parallel; Events already has row skeletons.

## Steps

1. Delay header, deploy window, log, and event queries independently.
2. Remove false empty states, stale actions, and layout jumps.

## Files

- `dashboard/src/routes/services.$serviceId.deploys.$deployId.tsx`
- `dashboard/src/routes/services.$serviceId.events.tsx`
- deploy/event components and tests

## Acceptance criteria

- [ ] Both URLs keep the service frame and show region-specific skeletons.
- [ ] Actions stay unavailable until their exact resource state is known.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Deploy lifecycle behavior.
