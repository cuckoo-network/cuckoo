---
id: w5/m79/t011
title: Account settings skeleton
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m79/t010]
---

## Objective

Give `/settings` one stable account-settings skeleton across Kratos flow, active-session, connected-agent, API-key, and SSH-key initialization.

## Context

- Independent panels currently resolve at different times; the page must preserve useful ready sections without a full-page reset.

## Steps

1. Delay each data source separately and together.
2. Fix blank panels, generic spinners, and layout shifts with panel-shaped placeholders.

## Files

- `dashboard/src/routes/settings.tsx`
- `dashboard/src/features/auth/pages/settings-page/`
- settings tests

## Acceptance criteria

- [ ] Cold load has a stable page frame; partial readiness remains visible.
- [ ] No session/token/key value is represented by fake skeleton text.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Account security mutations.
