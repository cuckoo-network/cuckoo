---
id: w5/m79/t007
title: Webhooks list, create, detail, and settings skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t006]
---

## Objective

Complete `/webhooks`, `/webhooks/new`, `/webhook/$webhookId`, and `/webhook/$webhookId/settings` sequentially using their actual list/form/detail shapes.

## Context

- The detail shell owns loading for both child routes; child components return `null` until it resolves.
- Preserve one shell and one loading owner.

## Steps

1. Exercise each URL with route, endpoint, and event-type delays.
2. Fix blank children, duplicate shells, and geometry mismatch.

## Files

- `dashboard/src/routes/webhook*.tsx`
- `dashboard/src/routes/webhooks*.tsx`
- adjacent tests

## Acceptance criteria

- [ ] All four URLs render intentional pending UI.
- [ ] Detail and Settings share one stable shell without blank content or duplication.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Webhook delivery semantics.
