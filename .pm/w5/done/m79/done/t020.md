---
id: w5/m79/t020
title: Static-site shell and deploy-route skeletons
worker: worker5
status: done
estimate: 1h
depends_on: [w5/m79/t019]
---

## Objective

Complete the `/static/$serviceId` shell/index pair plus `/deploys` and `/deploys/$deployId`, preserving static-site chrome while root type resolution and deploy data load.

## Context

- Bare static roots redirect to Events; non-static ids redirect back to the compute tree.
- Deploy routes remain generated even when normal static navigation emphasizes Events.

## Steps

1. Delay parent/index type resolution and each deploy route.
2. Verify cross-tree redirects, direct deep links, and one-shell ownership.

## Files

- `dashboard/src/routes/static.$serviceId.tsx`
- `dashboard/src/routes/static.$serviceId.index.tsx`
- `dashboard/src/routes/static.$serviceId.deploys*.tsx`

## Acceptance criteria

- [ ] Every path has a stable static frame or an immediate correct redirect.
- [ ] No compute/static chrome swap or duplicate shell occurs.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Changing static-site information architecture.
