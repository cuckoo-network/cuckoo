---
id: w5/m79/t006
title: Environment Groups list and detail skeletons
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m79/t005]
---

## Objective

Make `/env-groups` and `/env-groups/$groupId` load with stable list/detail shapes and no hydration or stale-group flash.

## Context

- The list has route and in-page skeletons; detail reuses its page as `pendingComponent` and has a known hydration-risk history.

## Steps

1. Delay list data, then detail resource/services/scope data.
2. Remove competing placeholders and align the surviving skeleton with resolved cards.

## Files

- `dashboard/src/routes/env-groups.tsx`
- `dashboard/src/routes/env-groups_.$groupId.tsx`
- adjacent tests

## Acceptance criteria

- [ ] Both routes remain dimensionally stable through pending-to-ready.
- [ ] Unknown ids redirect without flashing another group's content.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Fixing unrelated hydration causes tracked elsewhere.
