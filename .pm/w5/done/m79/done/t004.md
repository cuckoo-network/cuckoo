---
id: w5/m79/t004
title: Billing and Notifications skeletons
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m79/t003]
---

## Objective

Audit `/billing` and `/notifications` sequentially, replacing generic or semantically premature placeholders with their final card/list shapes.

## Context

- Both routes use `ListPageSkeleton`; `/billing` also has card-level loading and active `w10/m11` work.
- Preserve user changes from m11 and avoid overlap with its Charges-copy fix.

## Steps

1. Verify route-pending and query-loading phases for each URL.
2. Align skeleton geometry without rendering resolved billing/notification claims early.

## Files

- `dashboard/src/routes/billing.tsx`
- `dashboard/src/routes/notifications.tsx`
- related feature components/tests

## Acceptance criteria

- [ ] Each route's pending state matches its resolved sections.
- [ ] No billing amount, invoice claim, or notification state is guessed while loading.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- `w10/m11`'s Billing copy and Blueprint diagnostics.
