---
id: w5/m79/t003
title: Agents list and detail skeletons
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m79/t002]
---

## Objective

Verify and refine `/agents` and `/agents/$agentSessionId` one at a time so composer/recents and conversation loading match their final layouts.

## Context

- `AgentsPageSkeleton`, `AgentSessionDetailPending`, and `ConversationSkeleton` already exist from prior work.
- Do not regress the one-rail invariant or duplicate the conversation column.

## Steps

1. Delay `/agents`, then its detail route, and compare skeleton geometry to resolved content.
2. Fix only route-specific mismatch, stale-session bleed, or double-mount behavior.

## Files

- `dashboard/src/routes/agents.tsx`
- `dashboard/src/routes/agents_.$agentSessionId.tsx`
- adjacent route tests

## Acceptance criteria

- [ ] Both routes have distinct, truthful skeletons and stable chrome.
- [ ] Navigating between session ids never retains the previous session's content.

## Shared shape-parity gate

- [ ] At desktop and narrow-mobile widths, every rendering route this task owns has pending-versus-ready evidence showing the same outer bounds, padding, max-width, responsive columns, heading/action slots, tabs, and always-present major content regions with stable heights. A shared skeleton is acceptable only when that geometry actually matches; routes explicitly classified as redirects or server-only render no skeleton.

## Out of scope

- Agent-session API or transcript behavior.
