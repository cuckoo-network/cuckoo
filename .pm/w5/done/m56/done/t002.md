---
id: w5/m56/t002
title: Metrics UI — percentile "All" overlays the multi-quantile series
worker: worker5
status: done
estimate: 40m
depends_on: [w5/m56/t001]
---

## Objective

Add an "All" option to the Network card's percentile control that, when selected, overlays the p50/p90/p99 series (from t001's multi-quantile read) on one chart with a legend distinguishing them. Splice the new query shape into the dashboard's generated GraphQL types per the codegen-splice note.

## Context

- Metrics feature UI: `dashboard/src/features/metrics/` (the Network card + its percentile control shipped in w5/m42).
- Codegen caveat: `dashboard/src/graphql/definitions.ts` is partly hand-maintained — splice the new field per-feature, never full-codegen (repo memory note).
- Chart component + series styling already exist (w5/m43 added event markers); reuse the chart's multi-series support.

## Steps

1. Splice the multi-quantile query/type into `definitions.ts` + the metrics hook.
2. Add "All" to the percentile control; on "All", request all quantiles and render the overlaid series + legend.
3. Component/hook tests for the "All" path.

## Files

- `dashboard/src/features/metrics/` (percentile control, chart, hook)
- `dashboard/src/graphql/definitions.ts` (splice)

## Acceptance criteria

- [ ] Percentile control offers "All"; selecting it overlays p50/p90/p99 with a legend.
- [ ] Single-percentile selection unchanged; typecheck/lint green.

## Out of scope

- Backend multi-quantile (t001); range presets (t003).
