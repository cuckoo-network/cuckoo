---
id: w5/m56/t006
title: Test coverage — multi-quantile read + percentile "All" + range presets
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m56/t004]
---

## Objective

Add meaningful tests: the backend metrics read returns all requested quantiles (and honors store-less/over-window); the UI percentile "All" overlays the series; and the range dropdown's "Last 30 days" + "Custom" bounds behave (including rejection past `BEX_MAX_QUERY_HOURS`). No tautologies.

## Context

- Backend tests: `lego/backend/internal/metrics/`. Dashboard tests: `dashboard/src/features/metrics/**/__tests__/`.

## Steps

1. Backend: multi-quantile response + error-path tests.
2. UI: percentile "All" overlay + range-preset/custom-bound tests.

## Files

- `lego/backend/internal/metrics/*_test.go`; `dashboard/src/features/metrics/**/__tests__/`

## Acceptance criteria

- [ ] Tests fail if the multi-quantile shape, the "All" overlay, or the range bounds regress.
- [ ] Backend + dashboard suites green.

## Out of scope

- E2E browser automation (the dev-5 walk lives in t004/t007).
