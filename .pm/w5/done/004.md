# Manual-scaling section in service Settings — blocked on w1/004

Add Render's manual-scaling control (instance-count stepper + save/confirm) to the service Settings tab once bex-api serves the scale verb (`w1/004`). Capture Render's scaling UI live first (m7 pattern); show current `replicas` from the existing `services` query. **Blocked:** no backend scale API today — do not build dashboard-first (pillar-1 API-first).

Source: `/pm-brainstorm more milestones for w5` (2026-07-08); w5/m7 left the rest of Render's Settings page as future milestones.
