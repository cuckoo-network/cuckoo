---
id: w5/m53/t003
title: Dashboard: Render-style disabled trigger select replaces the bare switch
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m53/t002]
---

## Objective

Replace the Auto-Deploy switch in the Deploy card with Render's control: a disabled select showing the current trigger ("On Commit" / "Off") plus the pencil-edit flow (enable select → choose → Cancel/"Save changes" gated on change), using the m50 select-variant row. Copy mirrors Render's ("By default, bex automatically deploys your service whenever you update its code…").

## Context

- Current switch: in `dashboard/src/features/services/components/build-deploy-section.tsx` wired to `use-auto-deploy.ts` (w2/m9); cron's Auto-Deploy toggle (w5 note 010) follows the same path.
- m50/t001's `EditableFieldRow` select variant is the building block (this task depends on it having landed).
- en/zh strings for the two option labels + description.

## Steps

1. Swap the switch for the select row on every type that shows Auto-Deploy (web/private/worker/cron/static, repo-backed).
2. Wire to the t002 write path; optimistic update semantics preserved.
3. Update tests.

## Files

- `dashboard/src/features/services/components/build-deploy-section.tsx`
- `dashboard/src/features/services/hooks/use-auto-deploy.ts`
- tests under `__tests__/`

## Acceptance criteria

- [ ] Auto-Deploy renders as a disabled select "On Commit"/"Off" with pencil-edit on all repo-backed types; no switch remains.
- [ ] Saving Off then On round-trips through the backend and re-renders correctly.

## Out of scope

- An "After CI Checks Pass" option (divergence, t004).
