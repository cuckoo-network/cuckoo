---
id: w5/m53/t002
title: GraphQL + MCP: expose the trigger consistently with REST
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m53/t001]
---

## Objective

Mirror t001 on the other adapters (one core, thin adapters — `docs/ADR006-bex-api.md`): the GraphQL service type gains an `autoDeployTrigger` read consistent with REST, the existing `setAutoDeploy` mutation stays, and a trigger-accepting write path exists (extend `setAutoDeploy` with an optional trigger arg or add `setAutoDeployTrigger` — follow the narrow-setter precedent of w5/m30). The MCP `set_auto_deploy` tool accepts/reports the same vocabulary.

## Context

- GraphQL schema + resolvers: `lego/backend/internal/api` (GraphQL adapter).
- MCP tools mirror the setters (w5/m9 precedent).
- Dashboard generated types: **splice per-feature; never full-codegen `definitions.ts`** (repo memory: offline-codegen drift).

## Steps

1. Add the read field + write path in GraphQL; keep boolean semantics underneath.
2. Update the MCP tool's input schema/description to accept the trigger vocabulary.
3. Splice dashboard codegen types for the new field/operation.

## Files

- `lego/backend/internal/api/` (GraphQL + MCP)
- `dashboard/src/features/services/api/auto-deploy.graphql` + definitions splice
- backend tests alongside

## Acceptance criteria

- [ ] GraphQL read matches REST emit for the same service state.
- [ ] Write path round-trips `commit`/`off`; `checksPass` rejected with the same message as REST.
- [ ] MCP tool consistent; adapter parity asserted in tests.

## Out of scope

- The dashboard control (t003).
