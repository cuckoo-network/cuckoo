---
id: w5/m53/t004
title: Document the checksPass divergence (ADR018 + CLI checklist)
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m53/t001]
---

## Objective

Record "After CI Checks Pass" as a conscious divergence: bex accepts `off|commit` and rejects `checksPass` with an explanatory error because bex has no GitHub checks integration. Update the auto-deploy row in `docs/ADR018-render-parity.md` (◐ with the trigger nuance) and add/refresh the relevant row in `docs/cli-compatibility-checklist.md` if the official CLI sends the trigger field on service update.

## Context

- ADR018 is the parity ledger; divergences are flagged there, never silent (repo rule).
- Check `render-oss/cli` service-update payloads for `autoDeployTrigger` usage to know whether the CLI checklist needs a row.

## Steps

1. Update the ADR018 row with evidence pointers (walk date, REST behavior).
2. Verify CLI payload shape; update `docs/cli-compatibility-checklist.md` accordingly.
3. `npx prettier@3.4.2 --write` the touched docs.

## Files

- `docs/ADR018-render-parity.md`
- `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] Both docs record the trigger support + checksPass divergence with evidence.

## Out of scope

- Building checksPass.
