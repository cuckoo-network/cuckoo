---
id: w5/m2/t003
title: Polish login + register pages
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m2/t002]
---

## Objective

Bring `dashboard/src/features/auth/pages/login-page/` and `register-page/` up to the spacing/typography pattern captured in t001, so the hand-built shell (hero copy + feature panel) and the Ory-rendered form card read as one cohesive design instead of two visually distinct halves.

## Context

- `dashboard/src/features/auth/components/auth-page-shell/index.tsx` — the shared two-column shell both pages use; any spacing/typography fix here applies to both at once.
- `dashboard/src/features/auth/components/auth-page-shell/auth-features.ts` — the feature-panel copy/icons; review for visual balance against the reference's stat-card density (t001).
- Ory's rendered form card (via `<Login>`/`<Registration>`) uses its own internal spacing driven by the CSS variables bridged in `dashboard/src/style.css` (the `:root { --ory-*, --interface-*, --border-radius-* }` block added when Ory Elements was first wired up) — check whether these need tightening/loosening to match the reference's density rather than assuming the original bridge got it right on the first pass.
- Review both pages in a real browser (dev server or the deployed pod) — this is a visual task, not a types/logic task.

## Steps

1. Render both pages, screenshot or snapshot each.
2. Compare against the reference pattern from t001 (spacing scale, typography).
3. Adjust `auth-page-shell`, the CSS variable bridge, and/or page-specific copy/layout until both pages read as polished and consistent with each other and with the dashboard's own sample page.

## Files

- `dashboard/src/features/auth/components/auth-page-shell/index.tsx`
- `dashboard/src/features/auth/components/auth-page-shell/auth-features.ts`
- `dashboard/src/style.css` (Ory CSS variable bridge, if needed)
- `dashboard/src/features/auth/pages/login-page/index.tsx`, `register-page/index.tsx`

## Acceptance criteria

- [ ] Login and register pages visually reviewed (screenshot/snapshot) and match each other's spacing/typography.
- [ ] `yarn typecheck && yarn lint` pass.

## Out of scope

- Forgot-password/settings/logout pages (t004/t005).
