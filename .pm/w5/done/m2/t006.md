---
id: w5/m2/t006
title: Simplify
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m2/t005]
---

## Objective

Run `/simplify` over everything this milestone touched in `dashboard/` — reuse, simplification, efficiency, and altitude cleanups, behavior-preserving.

## Context

- This milestone likely duplicates spacing/typography values across several page files before consolidating — watch for repeated Tailwind class strings that should become a shared constant or a small shared component (e.g. a "polished section" wrapper), especially between `login-page`/`register-page` (already sharing `auth-page-shell`) and the newly-touched `forgot-password-page`/`settings-page`/`logout-page`/`routes/index.tsx`.
- Check whether the Ory branding fix (t002) left any now-dead CSS variables or unused component-override code if the config-flag approach alone worked.

## Steps

1. Run `/simplify` scoped to `dashboard/`.
2. Apply behavior-preserving cleanups it finds.
3. Re-run `yarn typecheck && yarn lint && yarn build` after changes to confirm nothing broke.

## Files

- `dashboard/` (whatever `/simplify` flags)

## Acceptance criteria

- [ ] `/simplify` run completed over `dashboard/`.
- [ ] `yarn typecheck && yarn lint && yarn build` still pass after applying its suggestions.

## Out of scope

- New features or behavior changes — cleanup only.
