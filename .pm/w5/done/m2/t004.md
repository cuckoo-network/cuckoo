---
id: w5/m2/t004
title: Polish forgot-password + settings pages
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m2/t002]
---

## Objective

Bring `dashboard/src/features/auth/pages/forgot-password-page/` and `settings-page/` up to the same spacing/typography pattern (t001) applied to login/register in t003, including the two-step recovery UX (email → code) and the settings page's profile/password sections.

## Context

- `forgot-password-page/index.tsx` — currently a single centered `Card` (no two-column shell); confirm this is the right treatment vs. reusing `AuthPageShell`, and either way make the card's internal spacing consistent with t003's fixes.
- `settings-page/index.tsx` — wraps `<Settings>` in `DashboardLayout`, not the auth two-column shell (it's a post-login page) — review its content width/padding against the reference's stat-card/section density from t001, and check the `SessionProvider`-wrapped `<Settings>` card doesn't look cramped or misaligned inside `DashboardLayout`'s main content area.
- Also reachable as `/auth/reset-password` (`dashboard/src/routes/auth.reset-password.tsx`, same `SettingsPage` component) — check that route too, not just `/settings`.

## Steps

1. Render forgot-password and settings pages (including the `/auth/reset-password` alias), screenshot/snapshot each state (forgot-password's initial + success states; settings' profile + password sections).
2. Compare against t001's pattern and t003's applied fixes for consistency.
3. Adjust spacing/layout as needed.

## Files

- `dashboard/src/features/auth/pages/forgot-password-page/index.tsx`
- `dashboard/src/features/auth/pages/settings-page/index.tsx`

## Acceptance criteria

- [ ] Forgot-password page (both the email-entry and any success/code state) visually reviewed and consistent with login/register's polish.
- [ ] Settings page (`/settings` and `/auth/reset-password`) visually reviewed and consistent within `DashboardLayout`.
- [ ] `yarn typecheck && yarn lint` pass.

## Out of scope

- Logout page and the dashboard sample page (t005).
