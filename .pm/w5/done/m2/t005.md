---
id: w5/m2/t005
title: Polish logout page + dashboard sample page/sidebar; final review pass
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m2/t003, w5/m2/t004]
---

## Objective

Finish the visual pass on `logout-page` and the authenticated dashboard shell (`routes/index.tsx` sample page + `dashboard-layout`/`dashboard-sidebar`), then do one final page-by-page review across every page this milestone touched to confirm they all read as one consistent product.

## Context

- `dashboard/src/features/auth/pages/logout-page/index.tsx` — standalone centered page (icon + status text), simplest of the set; check spacing/typography against t001's pattern.
- `dashboard/src/routes/index.tsx` — the sample "Services" `Card`/`Table`; apply the stat-card-density pattern from t001 if it makes the page feel more like the reference (e.g. summary stat cards above the table, matching `overview-stat-card.tsx`'s composition) — judgment call, keep it simple since this is still sample data.
- `dashboard/src/common/components/dashboard-layout/{index.tsx,dashboard-sidebar.tsx}` — the persistent chrome every authenticated page sits in; review header height/padding and sidebar spacing against the reference `ledger-layout` pattern from t001.

## Steps

1. Render the logout page and the dashboard sample page, screenshot/snapshot each.
2. Apply t001's pattern to both.
3. Do a final pass: open every page this milestone touched (login, register, forgot-password, settings, logout, dashboard sample) in sequence and confirm consistent spacing, typography, and no leftover Ory branding (re-check t002's fix still holds after any CSS changes from t003/t004).

## Files

- `dashboard/src/features/auth/pages/logout-page/index.tsx`
- `dashboard/src/routes/index.tsx`
- `dashboard/src/common/components/dashboard-layout/index.tsx`
- `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx`

## Acceptance criteria

- [ ] Logout page and dashboard sample page visually reviewed and consistent with the rest.
- [ ] A final one-by-one pass across all six pages confirms consistent polish and zero visible Ory branding.
- [ ] `yarn typecheck && yarn lint && yarn build` all pass.

## Out of scope

- New features on the sample page (e.g. real bex-api wiring) — still sample data, per `dashboard/README.md`'s Status section.
