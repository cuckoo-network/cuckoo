---
id: w5/m4/t005
title: Verify the services page live against prod bex-api
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m4/t003, w5/m4/t004]
---

## Objective

Verify the wired services list end-to-end against the production bex-api (`api.bex.co`) while logged in via Kratos, exercising the list render, the three states, and each lifecycle action, and capture a screenshot.

## Context

- Prod GraphQL: `https://api.bex.co/graphql`; requires a real Kratos session (`docs/ADR012-auth.md`).
- Real Apps exist to act on (e.g. `eden-cms-v2`, `beancount-cms`) — pick a non-critical one for suspend/resume/restart, or restart-only if suspending a live App is undesirable.
- Screenshots to `.playwright-mcp/` (bare filenames).

## Steps

1. Log in via Kratos; load the dashboard home against prod.
2. Confirm the list, stat tiles, and states render from live data.
3. Exercise a lifecycle action on a safe App; confirm the badge converges.
4. Screenshot the live page to `.playwright-mcp/dashboard-services-live.png`.

## Files

- `.playwright-mcp/dashboard-services-live.png` (screenshot, gitignored)

## Acceptance criteria

- [ ] The services page renders live prod data logged in via Kratos.
- [ ] A lifecycle action converges against a real App.
- [ ] Screenshot captured to `.playwright-mcp/`.

## Out of scope

- Automated tests (t007).
