---
id: w5/m71/t003
title: Per-turn transcript identity and partial-harvest merge
worker: worker5
status: done
estimate: 60m
depends_on: [w5/m71/t002]
---

## Objective

Replace global driver ordinal identity with `(session, turn, part_index)` idempotency while retaining a monotonic per-session replay cursor. Make all writers share the contract and let completion merge the full current-turn log after any live prefix.

## Acceptance criteria

- [x] A turn-2 local index zero cannot collide with turn 1.
- [x] Re-attaching/re-harvesting is idempotent.
- [x] A stored prefix followed by completion harvest stores the missing suffix.
