---
id: w5/m60/t008
title: "Test coverage: trigger/refresh, override round-trip, edit readback, error paths"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m60/t006]
---

## Objective

Close the coverage gaps for the three closures with tests that assert real behavior and failure modes: trigger→history refresh and the active-run rejection; override read/write/inherited-vs-overridden display; credential edit prefill, keep-token-on-blank, rotate, and never-echo-the-secret.

## Context

- Test homes: `dashboard/src/features/services/hooks/__tests__/` (extend the existing `use-cron-runs` coverage), the settings/section component tests, `dashboard/src/features/registry-credentials/**/__tests__/`.
- t001–t004 each shipped baseline tests — this audits against the DoD list and adds what's missing, not duplicates.

## Steps

1. Audit existing coverage against the DoD behaviors; add the missing assertions (especially the failure paths: active-run rejection, unknown run id, server error on rotate).
2. Run the suite; record file/test counts.

## Files

- `dashboard/src/features/services/**/__tests__/`
- `dashboard/src/features/registry-credentials/**/__tests__/`

## Acceptance criteria

- [ ] Every DoD behavior and named failure mode has an asserting test that fails on regression.
- [ ] `cd dashboard && yarn test` passes.

## Out of scope

- Backend tests (verbs pre-exist with their own coverage); e2e.
