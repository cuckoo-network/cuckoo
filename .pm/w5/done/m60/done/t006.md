---
id: w5/m60/t006
title: "Render parity: each closure consistent with its Render capture + cross-surface"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m60/t005]
---

## Objective

Compare each shipped closure against its Render anchor and bex's other surfaces: the Trigger Run interaction vs `docs/render-artifacts/cron-runs.md`, the override dropdown vs `docs/render-artifacts/notify-on-fail.md`, credential editing vs Render's registry-credential management — and confirm the UI exposes the same semantics as bex's REST/MCP/CLI verbs; file drift rather than silently diverging.

## Context

- The backend verbs already exist on all machine surfaces (ADR038 cron verbs incl. MCP `run_cron_job`; notifications REST per `notify-on-fail.md`; registry credentials REST/MCP) — this task checks the UI half matches their semantics (option vocabulary, error shapes, at-most-one-run behavior), not that the verbs work.
- Update stale ADR018 cells or artifact claims only where the walk proves them wrong; new gaps become bounded notes.

## Steps

1. Per closure: written verdict (match / named divergence) against the pinned artifact, refreshing the Render capture only if the artifact predates a visible Render change.
2. Cross-surface check: the UI's option values/semantics equal the REST/GraphQL/MCP contract for each verb.
3. File notes for any divergence; correct any stale artifact/ledger claim; prettier pass.

## Files

- `docs/render-artifacts/cron-runs.md`, `docs/render-artifacts/notify-on-fail.md` (verdict updates if warranted)
- `docs/ADR018-render-parity.md` (only if a cell changes)

## Acceptance criteria

- [ ] A written verdict exists per closure; every divergence is fixed or filed.
- [ ] No cross-surface semantic drift between the UI and the existing verbs.

## Out of scope

- Building any newly-found Render capability (file it).
