---
id: w5/m60/t007
title: "Simplify (/simplify over the milestone's diff)"
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m60/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — the cron-runs hook/panel, the Settings notifications section, and the registry-credentials feature — hunting reuse/simplification wins while preserving behavior.

## Context

- Likely targets: the create/edit registry-credential dialogs sharing one form component; the runs panel's trigger/cancel actions sharing confirm plumbing; select-row reuse between the m53 autoDeployTrigger control and the new override dropdown.

## Steps

1. `/simplify` scoped to the m60 diff in `dashboard/src/`.
2. Apply accepted findings; suites stay green.

## Files

- `dashboard/src/features/services/`, `dashboard/src/features/registry-credentials/` (t001–t004's touched files)

## Acceptance criteria

- [ ] `/simplify` run with findings applied or explicitly rejected.
- [ ] `cd dashboard && yarn test` passes.

## Out of scope

- New behavior.
