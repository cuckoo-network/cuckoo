---
id: w5/m61/t005
title: "Render parity: panel vs Render's datastore Logs tabs + cross-surface consistency"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m61/t004]
---

## Objective

Compare the shipped panels against live captures of Render's Postgres and Key Value Logs tabs (layout, controls, row shape, empty/tail behavior) and check the UI's semantics against the reads' other bex surfaces; record verdicts and file drift rather than silently diverging.

## Context

- Capture Render's datastore Logs tabs live (Playwright, `.playwright-mcp/`) — no pinned artifact exists yet for these tabs; this task creates the comparison record (a short `docs/render-artifacts/` note or a section in an existing datastore artifact).
- Cross-surface: whatever REST/MCP counterparts t001 mapped for `databaseLogs`/`keyValueLogs` must agree with the UI on range semantics and storeless behavior; asymmetries found (e.g. a read existing on GraphQL only) are recorded as filed follow-ups, not silently accepted.
- Known-non-goal guard: external log drains stay excluded (DO_NOT_DO) — don't file Render's drain banner as a gap.

## Steps

1. Capture Render's two Logs tabs; write per-affordance verdicts against the bex panels.
2. Check UI ↔ machine-surface consistency for both reads; note asymmetries.
3. Record the evidence note in `docs/render-artifacts/`; update ADR018 only if a cell's claim changes; file notes for divergences; prettier pass.

## Files

- `docs/render-artifacts/` (new or extended datastore-logs note)
- `docs/ADR018-render-parity.md` (only if a cell changes)
- `.playwright-mcp/` (Render captures)

## Acceptance criteria

- [ ] Written verdicts exist for both panels vs Render, evidence-linked.
- [ ] Every divergence and cross-surface asymmetry is fixed or filed — none silently accepted.

## Out of scope

- Building live-tail or drains if Render has them (file as follow-up with the verdict).
