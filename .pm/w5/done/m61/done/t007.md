---
id: w5/m61/t007
title: "Test coverage: rendering, range, store-unavailable, error and empty states"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m61/t005]
---

## Objective

Audit and complete the milestone's test coverage: the shared panel's row rendering and range-refetch behavior, all four non-happy states (loading/empty/error/store-unavailable) with distinct copy, both page mounts passing correct identity, and the hook's argument construction per t001's pinned contract — asserting behavior, never snapshots.

## Context

- Test homes: the shared component's `__tests__/`, `dashboard/src/features/databases/**/__tests__/`, `dashboard/src/features/keyvalue/**/__tests__/`, and the route tests t003 touched.
- t002/t003 shipped baseline tests — this closes gaps against the DoD, not duplicates.

## Steps

1. Audit against the DoD behavior list; add the missing assertions (distinct-copy checks for each state; identity passed per page; range → new query variables).
2. Run the suite; record counts.

## Files

- Component/feature/route `__tests__/` directories touched by t002–t003

## Acceptance criteria

- [ ] Every DoD behavior and non-happy state has an asserting test that fails on regression.
- [ ] `cd dashboard && yarn test` passes.

## Out of scope

- Backend read tests (`logs_test.go` pre-exists); e2e.
