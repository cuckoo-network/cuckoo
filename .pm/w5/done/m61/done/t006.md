---
id: w5/m61/t006
title: "Simplify (/simplify over the milestone's diff)"
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m61/t005]
---

## Objective

Run `/simplify` over the milestone's diff — the shared panel, hooks/operations, and page wiring — hunting reuse wins (especially against `dashboard/src/features/logs/` and `DatastoreMetricsPanel`) while preserving behavior.

## Steps

1. `/simplify` scoped to the m61 diff in `dashboard/src/`.
2. Apply accepted findings; suites stay green.

## Files

- The shared panel component + `dashboard/src/features/databases/`, `dashboard/src/features/keyvalue/` touched files

## Acceptance criteria

- [ ] `/simplify` run with findings applied or explicitly rejected.
- [ ] `cd dashboard && yarn test` passes.

## Out of scope

- New behavior.
