---
id: w5/m32/t001
title: "Walk Render live: capture every page's controls and states"
worker: worker5
status: done
estimate: 60m
depends_on: []
---

## Objective

Drive Render's real dashboard with Playwright over the full page inventory (service: overview, settings sections, environment, logs, metrics, events, deploy list + detail, scaling; datastores: Postgres + Key Value pages; workspace: team, usage, audit, env groups, projects/environments, settings) and record, per page: sections, controls, empty states, and any flow bex is known to ship. Screenshots to `.playwright-mcp/` (bare filenames), notes to `docs/render-artifacts/dashboard-walk/`.

## Files

- `docs/render-artifacts/dashboard-walk/` (one file per page family)

## Acceptance criteria

- [x] Every page family captured with dated notes + screenshots; the inventory list itself recorded so t002 walks the identical route.

## Out of scope

- Judging bex against it (t002).

## Resolution

Walked 30 authenticated live Render routes covering the complete inventory.
Semantic reports and bare-name screenshots are retained under
`.playwright-mcp/`; the dated, redacted route inventory is tracked in
`docs/render-artifacts/dashboard-walk/README.md`. The account had no safe Key
Value detail fixture, so that page is explicitly recorded as unreachable rather
than silently skipped.
