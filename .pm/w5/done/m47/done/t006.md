---
id: w5/m47/t006
title: Test coverage — ?type= prefill + alias mapping
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m47/t004]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: the wizard preselecting `serviceType` from `search.type` (including the unknown-value fallback) and the render-alias carrying the type on each per-type create URL.

## Context

- Dashboard tests live in `__tests__` dirs adjacent to the code (`dashboard/CLAUDE.md`); run with `yarn test` (CI-enforced).
- Existing suites to extend: `routes/__tests__/services.new.test.tsx`, a render-alias test under `common/lib/__tests__/`.

## Steps

1. Wizard: assert `?type=cron_job` (and each valid type) preselects that type card; assert absent/unknown `?type=` falls back to `web_service`.
2. render-alias: assert each `/{web,worker,pserv,static,cron}/new` maps to `/services/new?type=<serviceType>`, and that detail-route aliases are unchanged.
3. If a New-menu "Cron Job" entry was added, assert it links to `/services/new?type=cron_job`.

## Files

- `dashboard/src/routes/__tests__/services.new.test.tsx`
- `dashboard/src/common/lib/__tests__/render-alias.test.ts`
- New-menu component test(s) as applicable

## Acceptance criteria

- [ ] Tests assert real preselection + fallback behavior and the full alias mapping (positive and negative cases), not snapshots.
- [ ] `yarn test` passes.

## Out of scope

- Backend tests (no backend change).
