---
id: w5/m47/t005
title: Simplify the changed create-entry code
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m47/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (wizard prefill, render-alias mapping, New-menu entries) — behavior-preserving reuse/simplification/efficiency cleanup only.

## Context

- Touched files: `services.new.tsx`, `create-context.ts`, `render-alias.ts`, the three New-menu components, and the locale files.
- Watch for a shared segment→type map that could be reused between `render-alias.ts` and the New-menu entries instead of duplicated.

## Steps

1. `/simplify` the diff.
2. Prefer a single source of truth for the segment↔serviceType mapping if it's currently duplicated across the alias and the menus.

## Files

- the m47 working diff

## Acceptance criteria

- [ ] No behavior change; `yarn typecheck && yarn lint && yarn test` still pass.

## Out of scope

- New behavior or new tests (t006).
