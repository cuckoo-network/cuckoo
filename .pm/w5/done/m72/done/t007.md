---
id: w5/m72/t007
title: Deploy, repair the stranded session, and run the production workflow
worker: worker5
status: done
estimate: 60m
depends_on: [w5/m72/t002, w5/m72/t004, w5/m72/t006]
---

## Objective

Deploy the integrated repair through the normal platform path, let normal reconciliation terminalize the reported zombie where possible, safely reclaim its dead sandbox residue, and verify a fresh large-repository agent session from create through replay and delivery.

## Context

- Reported session: `ags-da1prbt040bc73aj5230`; workspace `tea-d98210cbbpdc73dcrkvg`; sandbox `3b34b8f2-55a9-42b0-a117-f94f5477d6e5`.
- The old Pod is Failed/exit 1, the BatchSandbox is Pending, the DB row is running, there is one durable incomplete user turn, and no branch/PR delivery exists.
- Production mutation/deploy must follow the repository's normal authorization, rollout, and rollback practices; do not hand-edit the row into a misleading success.

## Steps

1. Run all pre-deploy gates and deploy the platform/controller/agent image changes through the normal workflow.
2. Observe whether the repaired lifecycle automatically transitions the reported session; if historical state needs a one-time repair, use an audited, narrowly targeted failed/canceled transition and reclaim only its exact sandbox.
3. Refresh the reported dashboard route and confirm durable history/error state is readable and honest.
4. Start a fresh authorized session against `bex-co/beancount-cms-v2`; verify Git credential retry, model proxy, scrub, status persistence, optional delivery, attach replay, refresh, and teardown.
5. Confirm the gateway/API logs and metrics contain no permission denial or repeated dead-container poll for either session.

## Files

- `.github/workflows/deploy.yml`
- `scripts/agent-session-verify.sh`
- `docs/ADR047-cloud-coding-agent-sessions.md`
- Production session/sandbox resources named above (audited operator action)

## Acceptance criteria

- [x] The reported row leaves `running`, preserves recoverable history, records an honest bounded failure reason, and owns no live/stranded sandbox resources.
- [x] The dashboard route no longer shows the generic stream-unavailable state for a DB-privilege failure.
- [x] A fresh `beancount-cms-v2` run reaches success/delivery or a truthful terminal failure; it never dies solely on repository aggregate size or remains live after its Pod exits.
- [x] Production shows zero new SQLSTATE `42501` turn-replay failures and zero sustained `container not found` polling after the verification window.
- [x] Evidence records image digests, timestamps, phases, and redacted request outcomes without credentials or transcript secrets.

## Out of scope

- Fabricating the missing assistant transcript or PR for the original failed turn.
- Repairing unrelated historical sessions without separately resolving their exact ownership and failure state.
