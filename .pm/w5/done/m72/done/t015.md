---
id: w5/m72/t015
title: Add and run delivery regression coverage
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m72/t013, w5/m72/t014]
---

## Objective

Prove the missed publication, protocol, and stale-state branches with behavior tests and run the complete affected suites.

## Context

- The old delivery suite covered normal push and conflicting force-with-lease, but not an exact candidate already at the remote.
- The old gateway test constructed only one minimal command packet.

## Steps

1. Add a real bare-repository exact-candidate replay test.
2. Add shallow receive-pack acceptance and malformed-order rejection tests.
3. Add store coverage for failure clearing.
4. Run driver tests/typecheck and the complete backend suite.

## Files

- `lego/agent-image/driver/test/delivery.test.ts`
- `lego/backend/internal/sshgateway/agentcred/agentcred_test.go`
- `lego/backend/internal/store/store_pg_test.go`

## Acceptance criteria

- [x] Driver tests pass 51/51 and TypeScript typecheck passes.
- [x] `go test ./...` passes in `lego/backend`.
- [x] `git diff --check` passes.

## Out of scope

- Replacing the production live-workflow gate.
