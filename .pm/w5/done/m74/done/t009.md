---
id: w5/m74/t009
title: "Closeout — verify DoD, sync status, move to done/"
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m74/t008]
---

## Objective

When every other task is complete **and the milestone's definition of done actually holds** (the five ADR078 verification points, ideally walked live against a real GitHub account: org + personal installation on one workspace), close the milestone.

## Context

- The DoD includes a live element (a real second-installation connect + grouped picker + per-installation token mint) — verify on dev-5 or production before closing, and record evidence in this task or the milestone README.
- Also flip `docs/ADR078-github-workspace-connections.md` **Status:** proposed → accepted (implemented by w5/m74) and update its CLAUDE.md index line.

## Steps

1. Verify DoD points 1–5; record evidence.
2. Flip ADR078 status; sync the CLAUDE.md docs-index line.
3. Set every remaining task `status: done`, move each `tNNN.md` to `w5/m74/done/`, mark rows `— **DONE**`, set `**Status:** done` in the milestone README.
4. Move the whole `w5/m74/` to `w5/done/m74/` and check `- [x]` in `w5/README.md`.

## Files

- `.pm/w5/m74/**`, `.pm/w5/README.md`, `docs/ADR078-github-workspace-connections.md`, `CLAUDE.md`

## Acceptance criteria

- [ ] DoD evidence recorded; ADR078 marked accepted.
- [ ] Status synced in all three places; milestone lives at `w5/done/m74/` with no tombstone at the old path.

## Out of scope

- Any new feature work discovered during verification — file as inbox notes.
