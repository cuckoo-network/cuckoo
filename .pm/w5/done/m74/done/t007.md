---
id: w5/m74/t007
title: "Simplify — /simplify over the milestone's changed code"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m74/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (store, github service, adapters, agentsession mint, dashboard git/services features) — reuse, simplification, efficiency; strictly behavior-preserving.

## Context

- Likely candidates: the owner→connection helper vs scattered lookups; duplicated singular/plural adapter code; the per-connection concurrent ListRepos fan-out.

## Steps

1. `/simplify` scoped to the milestone diff.
2. Apply accepted fixes; re-run backend + dashboard test suites.

## Files

- as identified by the review

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly declined with reasons.
- [ ] `cd lego/backend && go test ./...` and dashboard `yarn test` still green.

## Out of scope

- Bug hunting (that's t008 / `/code-review` territory).
