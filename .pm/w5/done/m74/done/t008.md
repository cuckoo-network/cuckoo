---
id: w5/m74/t008
title: "Test coverage — migration, quota, owner resolution, entry points, compatibility"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m74/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — asserting real behavior and failure modes across the ADR075 verification matrix, never tautological coverage.

## Context

- Integration tests gated on `BEX_TEST_DB_URI` run for real in CI (`backend-test.yml`) — the migration and quota tests belong there.
- Existing three-proof connect tests must keep passing unmodified (t002 criterion); this task adds the NEW behavior's coverage.

## Steps

1. Store/migration: multi-row insert, PK conflict, down-migration guard, quota boundary (N, N+1, `0`-disabled).
2. Service: second-connection bind; cross-workspace `ErrConflict`; idempotent re-bind refreshing `account_login`; owner→connection resolution incl. no-match fallthrough for every consumer (clone token, branches, blueprint fetch, commit resolve, agent mint).
3. Webhook: two installations → two workspaces, each delivery confined to its own scope (extend the existing fail-closed tests).
4. Adapters: singular-alias byte-compatibility for one connection; ambiguity + quota coded errors identical ×3 surfaces.
5. Dashboard: picker grouping, stateful-connect wiring, `missing_state` recovery rendering (component tests).

## Files

- `lego/backend/internal/store/*_test.go`, `lego/backend/internal/github/*_test.go`, `lego/backend/internal/agentsession/*_test.go`, `lego/backend/internal/apps/webhook_test.go`
- `dashboard/src/features/{git,services}/**/*.test.tsx`

## Acceptance criteria

- [ ] Every ADR075 verification point 1–5 has at least one automated test or a recorded manual-verification note where automation is impossible (live GitHub round-trip).
- [ ] Backend + dashboard suites green in CI.

## Out of scope

- Live end-to-end GitHub install walk (manual, recorded at closeout).
