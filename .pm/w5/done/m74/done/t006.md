---
id: w5/m74/t006
title: "Render parity — cross-surface consistency + record the deliberate divergence in ADR018"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m74/t005]
---

## Objective

Verify the milestone's change is consistent across every surface it exposes — REST, GraphQL, MCP (same fields, semantics, error shapes) and the dashboard — and record the deliberate Render divergence in the parity ledger: bex is workspace-bound multi-installation by design; Render is user-bound with per-service credential pointers.

## Context

- The git-connection/repo surface is a bex extension (Render lists repos only through its private dashboard API; its MCP has no repo tools), so the comparison target is bex's own cross-surface consistency plus an honest ledger note — per ADR078 §1 and ADR026 §5.
- Render's model for the ledger note: user-bound Git Deployment Credentials, workspace-agnostic installations, per-service credential swap, documented creator-leaves breakage (sources cited in ADR078 Context).

## Steps

1. Diff the three surfaces' connection-set and repo payloads field by field; fix any drift (names, optionality, error codes) rather than documenting it.
2. Update `docs/ADR018-render-parity.md` git rows: divergence note (workspace-bound by design, superset multi-account picker, subset no per-member credentials) + evidence pointers.
3. Confirm the dashboard renders exactly what the API returns (no client-side reinterpretation of connection state).

## Files

- `docs/ADR018-render-parity.md`
- any adapter drift found in `lego/backend/internal/github/`

## Acceptance criteria

- [ ] REST/GraphQL/MCP payloads and coded errors verified identical (evidence in the task or ADR018 note).
- [ ] ADR018 git rows updated with the ADR078 divergence + link.
- [ ] Any found drift fixed in-code, not just noted.

## Out of scope

- New parity features beyond this milestone's surface.
