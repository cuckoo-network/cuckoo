---
id: w5/m54/t002
title: "Dashboard: searchable Branch combobox with free-text fallback"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m54/t001]
---

## Objective

Upgrade the Branch row to Render's control: a searchable combobox (typeahead over the t001 branch feed, "Search" placeholder) that saves through the existing `setBranch` verb with the rebuild confirm intact. When the feed reports unconnected (or the service's repo isn't GitHub-App-reachable), degrade to the current validated free-text editor — never a dead control.

## Context

- Current editor: Branch row in `dashboard/src/features/services/components/build-deploy-section.tsx` + `use-branch.ts` (w5/m48: empty resets to main).
- Render reference (live 2026-07-27): combobox with embedded search textbox showing `main`.
- Combobox primitive: shadcn command/combobox pattern already used elsewhere in the dashboard (check `dashboard/src/common/components/ui/`).
- Row chrome: m50's `EditableFieldRow` (select/custom-control slot).

## Steps

1. Build the combobox row (search filter, loading state, fallback path).
2. Keep empty-resets-to-main and rebuild-confirm semantics.
3. en/zh strings; tests.

## Files

- `dashboard/src/features/services/components/build-deploy-section.tsx`
- `dashboard/src/features/services/hooks/use-branch.ts` (feed wiring)
- tests under `__tests__/`

## Acceptance criteria

- [ ] Connected: typing filters real branches; selecting saves via `setBranch` after confirm.
- [ ] Unconnected: free-text editor with today's validation; no dead control.

## Out of scope

- Changing `setBranch` semantics.
