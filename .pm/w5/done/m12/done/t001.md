---
id: w5/m12/t001
title: Capture Render's KV dashboard live (`/new/redis`, list, detail) via Playwright
worker: worker5
status: done
estimate: 30m
depends_on: []
---

## Objective

Capture the design source before building: Render's `/new/redis` create form (every field, plan tier cards, defaults), the Key Value list (columns, status chips, empty state), and a KV instance's detail page (tabs, connection-info presentation, reveal/copy affordances, suspend UI) — screenshots + notes, the m7/m11.5 pattern.

## Context

- User's pointer: dashboard.render.com/new/redis. A live Render account with a free KV instance is available from the parity audit (or create one on the free plan and delete it after).
- Screenshots → `.playwright-mcp/` (bare filenames); notes → `docs/render-artifacts/`.
- Coordinate with w2/m7/t001 (API capture) — one shared capture note is fine; this task owns the visual/UX half.

## Steps

1. Walk `/new/redis` end to end (create a free instance); record fields, validation, plan cards, post-create redirect.
2. Capture list + detail (Overview/Connect info/Settings tabs as they exist), including the masked-reveal behavior.
3. Delete the test instance; write the capture note.

## Files

- `docs/render-artifacts/` (capture note + screenshot references)

## Acceptance criteria

- [ ] Create form, list, and detail captured with annotated screenshots.
- [ ] Connection-info UX (mask/reveal/copy, internal vs external) documented.

## Out of scope

- Implementation; API-shape capture (w2/m7/t001).
