---
id: w5/m12/t007
title: Simplify — `/simplify` over the m12 diff
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m12/t006]
---

## Objective

Run `/simplify` over the m12 diff. Prime suspect: duplication with `features/databases/` — the list/detail/connection-reveal structures are siblings; genuinely shared pieces (status chips, credential mask/reveal panel, stat cards) should become shared components rather than parallel copies. Behavior-preserving only.

## Context

- `dashboard/src/features/{databases,keyvalue}/` after t003–t005.

## Steps

1. `/simplify` scoped to the m12 diff; apply or consciously decline.
2. Re-run lint/typecheck/test.

## Files

- Whatever the m12 diff touched.

## Acceptance criteria

- [ ] Findings applied or declined explicitly; checks green.

## Out of scope

- Bug hunting; new tests (t008).
