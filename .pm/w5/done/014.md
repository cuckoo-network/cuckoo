# Environment selectors on datastore create pages

Add Project/Environment selectors to the managed Postgres and Key Value create pages, mirroring the service wizard's w2/m37 selector. Their REST/GraphQL/MCP create inputs already accept `environmentId` and auto-join the parent Project; this is the remaining dashboard create-path gap.

> Folded into **w5/m31** (Environments UX round 2, t005) 2026-07-15; note moved to `done/`.
