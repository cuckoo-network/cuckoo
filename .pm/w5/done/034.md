# Add production `yarn build` to dashboard-test.yml

`dashboard-test.yml` runs only `yarn typecheck && yarn lint && yarn test` — no production build. An SSR/production-build break passes PR CI and only surfaces post-merge, when `deploy.yml`'s dashboard image build fails. One CI step (`yarn build` after the test step, same job) closes the gap pre-merge.

**Superseded if the proposed E2E-suite milestone is ever materialized** — its harness runs the production build in CI anyway (brainstorm round 4, proposal 1); in that case fold this into the harness task and close this note.

~20m. Source: `/pm-brainstorm more for w5` round 4, 2026-07-30 (proposal 3); code-inventory sweep of `.github/workflows/dashboard-test.yml`.
