---
id: w5/m26/t003
title: Env Group detail page — vars/secret-files editors + linked services
worker: worker5
status: done
estimate: 40m
depends_on: [w5/m26/t001]
---

## Objective

Add an Env Group detail page at `/env-groups/$groupId` that edits the group's env vars and secret files (reusing the existing Environment-tab editors), renames/deletes the group, and lists the services linked to it with link/unlink controls.

## Context

- Reuse the editors already built for the service Environment tab: `dashboard/src/features/services/components/env-groups-panel.tsx` (and the env-var / secret-file editor components it composes) — promote to shared components if they're service-coupled.
- Data + mutations from t001 (`useEnvGroup`, setVars, set/delete secret file, delete group, link/unlink).

## Steps

1. Create `routes/env-groups.$groupId.tsx`.
2. Render env-vars editor + secret-files editor bound to the group (not a service).
3. Add rename + delete (typed-confirm, mirror existing danger-zone patterns) and a linked-services list with link/unlink actions.

## Files

- `dashboard/src/routes/env-groups.$groupId.tsx` (new)
- `dashboard/src/features/env-groups/components/*` (detail + editors)
- `dashboard/src/features/services/components/env-groups-panel.tsx` (extract shared editors if needed)

## Acceptance criteria

- [ ] Detail page edits vars + secret files for a group with no service context; changes persist via the mutations.
- [ ] Group can be renamed and deleted; linked-services list supports link/unlink.

## Out of scope

- Create dialog (t004); locale strings (t005).
