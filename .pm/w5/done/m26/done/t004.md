---
id: w5/m26/t004
title: Create-group dialog + link/unlink-service action
worker: worker5
status: done
estimate: 40m
depends_on: [w5/m26/t001]
---

## Objective

Add a "New Env Group" create dialog (mirroring the existing create-dialog pattern) and the link/unlink-service affordance used from the detail page.

## Context

- Mirror `dashboard/src/features/projects/components/new-project-dialog.tsx` (controlled dialog + name field + submit) — the same shape `create-database-dialog.tsx` follows.
- Uses t001's `createEnvGroup` + `linkEnvGroup`/`unlinkEnvGroup` hooks.

## Steps

1. Build a `NewEnvGroupDialog` (name field, validation, submit → `createEnvGroup` → route to the new group's detail page).
2. Build a link-service control (pick a service → `linkEnvGroup`) and an unlink action on each linked-service row.

## Files

- `dashboard/src/features/env-groups/components/new-env-group-dialog.tsx` (new)
- `dashboard/src/features/env-groups/components/*` (link/unlink control)

## Acceptance criteria

- [ ] "New Env Group" creates a group and lands on its detail page.
- [ ] A service can be linked to and unlinked from a group from the detail page.

## Out of scope

- Empty/error states + locales (t005); live verification (t006).
