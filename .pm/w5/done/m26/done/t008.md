---
id: w5/m26/t008
title: Simplify — /simplify over the milestone's code
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m26/t007]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving) — especially the shared-editor extraction so the service Environment tab and the new pages don't duplicate logic.

## Context

- New/changed: `dashboard/src/features/env-groups/**`, `routes/env-groups*.tsx`, extracted shared editors from `features/services/components/env-groups-panel.tsx`, sidebar nav.

## Steps

1. Run `/simplify` over the changed dashboard code.
2. Apply behavior-preserving reuse/simplification fixes (dedupe editors/hooks shared with the service tab).

## Files

- the files this milestone touched

## Acceptance criteria

- [ ] `/simplify` findings addressed or consciously deferred; no behavior change; tests still green.

## Out of scope

- Bug hunting (that's `/code-review`).
