---
id: w5/m26/t009
title: Test coverage — Env Groups page behavior
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m26/t007]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — the list, detail, and create/link flows — asserting real behavior and failure modes, not trivial snapshots.

## Context

- Test pattern: `dashboard/src/**/__tests__/*.test.tsx` with mocked hooks (see `features/databases/components/__tests__/create-database-dialog.test.tsx`, `routes/__tests__/*`).

## Steps

1. List page: renders groups with counts; empty state when none.
2. Detail page: edits vars/secret files; rename/delete; link/unlink updates the linked-services list.
3. Create dialog: create → routes to detail; validation blocks empty name.
4. Failure modes: mutation error surfaces (not a silent no-op); a group with no linked services is still editable.

## Files

- `dashboard/src/features/env-groups/**/__tests__/*` (new)
- `dashboard/src/routes/__tests__/env-groups*.test.tsx` (new)

## Acceptance criteria

- [ ] Tests assert list/detail/create/link behavior + at least one failure mode; `yarn test` passes.
- [ ] No tautological/snapshot-everything tests.

## Out of scope

- Live cluster verification (t006, already done).
