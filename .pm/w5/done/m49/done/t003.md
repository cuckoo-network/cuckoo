---
id: w5/m49/t003
title: Diagnose the production deletion stall and safely retire its exact fixture
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m49/t002]
---

## Objective

Determine which cleanup step is keeping the exact production App in `Deleting`, distinguish code regression from rollout/configuration drift, and complete its teardown without bypassing required external cleanup.

## Context

- Fixture: public id `srv-d9f9oalju7gs73fvngqg`, final display name `cli-oapi-r-220737-u`, created from `https://github.com/render-examples/go-gin.git` and deleted during its first build.
- CLI DELETE returned success; deploy list and instance list became empty, yet direct GET and repeated service lists continued to return phase `Deleting` after several minutes.
- `handleAppDeletion` waits on execution artifacts, registry/static/TLS artifacts, and per-App registry credential revocation. The current observation does not identify which pending/error branch is responsible.

## Steps

1. Inspect the production App deletion timestamp/finalizers, operator generation, structured reconciliation logs/events, and UID-scoped artifact inventory without printing credentials or kubeconfig data.
2. Identify the exact pending or failing cleanup operation and establish whether production is running the expected `w2/m61` cleanup code/configuration.
3. Repair or retry the failing cleanup through its owning mechanism, verify every relevant external artifact is absent, and only then allow the finalizer to clear.
4. Record a sanitized root-cause timeline and feed the proven reproduction into t004; do not guess from the top-level `Deleting` phase.

## Files

- `lego/operator/internal/controller/app_controller.go`
- `lego/operator/internal/build/`
- `lego/operator/internal/registry/`
- `scripts/delete-audit.sh`
- `.pm/w5/done/m49/evidence/`

## Acceptance criteria

- [x] The stall is attributed to one concrete code, rollout, dependency, or artifact-state cause with production evidence.
- [x] The exact fixture reaches HTTP 404 and is absent from CLI list results only after its UID-scoped external/build/credential/TLS inventory is proven empty.
- [x] No finalizer is forcibly removed while cleanup is unverified, and no unrelated production resource is mutated.
- [x] Evidence contains resource ids, timestamps, versions, named cleanup stages, and sanitized outcomes only—no secrets or raw kubeconfig/API responses.

## Out of scope

- Bulk-deleting other `Deleting` resources, changing retention policy, or accepting manual finalizer removal as the fix.
