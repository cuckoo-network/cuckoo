---
id: w5/m49/t008
title: Complete exact-payload finalizer-failure and verifier regression coverage
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m49/t006]
---

## Objective

Add meaningful coverage for the exact production failure modes and run the proportionate backend/operator quality gates without relying on happy-path snapshots.

## Context

- Inline task tests establish the fixes; this closing test pass must cover interactions and negative controls that would otherwise regress silently.
- The two important boundaries are OpenAPI acceptance followed by strict handler decoding, and DELETE acknowledgement followed by actual Kubernetes/API absence.

## Steps

1. Cover exact CLI POST/PATCH payloads, matching/conflicting/unauthorized image owners, registry credential set/clear/omit, and genuinely unknown image fields.
2. Cover delete during queued/running/partially-created first build, transient delete failures, operator restart, repeated reconcile, and same-name recreation.
3. Add verifier self-tests for stuck GET, stuck list, timeout, interrupted cleanup, overlong fixture name prevention, and redaction sentinels.
4. Run backend `go test ./...`, operator `make test`, relevant lint/race targets, shell syntax/self-tests, Prettier, and `git diff --check`.

## Files

- `lego/backend/internal/apps/*_test.go`
- `lego/operator/internal/controller/app_deletion_test.go`
- `lego/operator/internal/build/*_test.go`
- `scripts/cli-services-parity-self-test.sh`
- `scripts/cli-services-parity-verify.sh`

## Acceptance criteria

- [x] Reverting nested-owner support makes the exact official-CLI POST/PATCH regressions fail.
- [x] Reintroducing the proven finalizer stall makes deterministic delete/restart/residue tests fail.
- [x] Verifier self-tests reject acknowledgement-only, stuck visibility, leaked fixtures, and planted-secret output.
- [x] Required backend/operator/lint/race/shell/format gates pass and are recorded without credentials.

## Out of scope

- Tautological struct-only tests, snapshotting whole authenticated payloads, or requiring production access in fast deterministic suites.
