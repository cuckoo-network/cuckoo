---
id: w5/m49/t007
title: Simplify the REST image model cleanup state machine and verifier
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m49/t006]
---

## Objective

Run `$simplify` over the milestone changes so workspace resolution, cleanup pending/error aggregation, and CLI polling each have one obvious implementation without weakening behavior.

## Context

- The request passes through both OpenAPI and strict Go decoding; ownership validation should not be duplicated inconsistently across create and patch.
- Finalizer code must distinguish done, pending, and failed stages and preserve all errors.
- Verifier cleanup and acceptance polling must share deadlines and diagnostics without swallowing failures.

## Steps

1. Review changed REST, operator, and verifier code for duplicate ownership resolution, artifact selection, polling, and error handling.
2. Consolidate behaviorally identical paths while keeping field-named errors, retry state, and secret boundaries explicit.
3. Remove dead compatibility shims or speculative abstractions introduced during diagnosis.
4. Re-run the focused exact-payload, finalizer, and verifier tests after simplification.

## Files

- `lego/backend/internal/apps/rest.go`
- `lego/operator/internal/controller/app_controller.go`
- `lego/operator/internal/build/`
- `scripts/cli-services-parity-verify.sh`
- `scripts/delete-audit.sh`

## Acceptance criteria

- [x] Create and patch share one clear nested-owner validation rule.
- [x] Finalizer done/pending/error semantics remain explicit, joined, and test-covered.
- [x] Verifier polling and cleanup have one bounded implementation and preserve all failure diagnostics.
- [x] Every focused regression still passes after the behavior-preserving simplification.

## Out of scope

- Generalizing unrelated resource adapters or controller finalizers.
