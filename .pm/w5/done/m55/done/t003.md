---
id: w5/m55/t003
title: Migrate Key Value detail fields (Name, Maxmemory Policy) to EditableFieldRow
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m55/t001]
---

## Objective

Replace the Key Value detail page's bespoke single-value edit controls (Name — Input+Save; Maxmemory Policy — save-on-change select) with the shared `EditableFieldRow` (text variant for Name, select variant for Maxmemory Policy: disabled until pencil, then Cancel/"Save changes" with dirty gating). Behavior identical to today; only the interaction shell changes.

## Context

- `dashboard/src/features/keyvalue/components/key-value-name-section.tsx`, `key-value-maxmemory-policy-section.tsx`.
- Shared component + select variant: `dashboard/src/features/services/components/editable-field-row.tsx` (the Notifications/Auto-Deploy rows are the select-variant precedent).
- Maxmemory-policy hook/mutation stays as-is; the select stops saving on change and instead saves via the row's "Save changes".

## Steps

1. Migrate Name → `EditableFieldRow` text variant; Maxmemory Policy → select variant (`options`).
2. Preserve validation (name format) and the plan-gating (Free-plan lock) as `disabled`/`disabledReason`... (use the row's `disabled` prop where a field is not editable on the current plan).
3. Update the component unit tests to the new interaction.

## Files

- `dashboard/src/features/keyvalue/components/key-value-name-section.tsx`
- `dashboard/src/features/keyvalue/components/key-value-maxmemory-policy-section.tsx`
- matching files under `__tests__/`

## Acceptance criteria

- [ ] Name + Maxmemory Policy render disabled with a pencil; edit → Cancel/"Save changes" per w5/m50.
- [ ] Same mutations fire with the same values; plan-gated fields stay non-editable.
- [ ] Existing tests updated; suite green.

## Out of scope

- Databases fields (t002); the plan-picker page (a dedicated radio-card page, not an inline field).
