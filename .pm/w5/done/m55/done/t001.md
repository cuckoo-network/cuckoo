---
id: w5/m55/t001
title: Live walk — Render Postgres + Key Value detail vs bex; inventory inline-editable fields + drift
worker: worker5
status: done
estimate: 40m
depends_on: []
---

## Objective

Walk Render's managed Postgres and Key Value detail pages against bex's, and inventory (a) every single-value inline-editable field on the bex pages and how it currently edits (always-editable Input+Save, save-on-change select, etc.), and (b) any section-order / grouping / labelling drift versus Render. Produce the concrete migration list t002/t003 execute and the drift list t004 reconciles. Verify Render itself uses the disabled-input/select + pencil edit-in-place pattern on these pages (the assumption behind this milestone).

## Context

- bex datastore detail components (pre-m50 patterns): `dashboard/src/features/databases/components/` (`database-name-section.tsx` uses Input+Save; also `database-version-control.tsx`, `database-disk-autoscaling-control.tsx`, `ha-panel.tsx`, `database-plan-section.tsx`, `parameter-overrides-editor.tsx`) and `dashboard/src/features/keyvalue/components/` (`key-value-name-section.tsx`, `key-value-maxmemory-policy-section.tsx`, `key-value-plan-section.tsx`).
- The target pattern + its variants: `dashboard/src/features/services/components/editable-field-row.tsx` (w5/m50–m54: text/number, select, searchable-combobox).
- Routes: `dashboard/src/routes/database.$databaseId.tsx` (or equivalent) and `dashboard/src/routes/keyvalue.$keyValueId.tsx`.
- Reference: the m50 settings walk method + `.playwright-mcp/render-settings-full.png`; capture Render's Postgres/Key Value pages fresh.

## Steps

1. Live-walk Render's Postgres + Key Value detail pages (authenticated), capturing each editable field's interaction and the section order.
2. Walk bex's equivalents (dev-5 or `local-bex` stub) and record each single-value field's current edit pattern.
3. Write the migration list (which fields → which `EditableFieldRow` variant, with validation/confirm notes) and the IA-drift list.

## Files

- (read-only) the components/routes above; record findings in the milestone README or a scratch note for t002–t004.

## Acceptance criteria

- [ ] Every inline-editable single-value field on both pages is listed with its current pattern and target variant.
- [ ] Render's edit-in-place pattern on these pages is confirmed (or the divergence is recorded so the milestone is reshaped).
- [ ] IA/section-order drift, if any, is enumerated for t004.

## Out of scope

- The parameter-overrides bulk editor and access-control CIDR list (multi-row editors, not single-value fields) — note them but do not migrate.
