---
id: w5/m55/t002
title: Migrate Databases detail fields (Name, Version, single-value settings) to EditableFieldRow
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m55/t001]
---

## Objective

Replace the bespoke always-editable edit controls for the Databases detail page's single-value fields (from t001's inventory — at minimum database Name and Version) with the shared `EditableFieldRow` (disabled input/select + pencil → Cancel/"Save changes", dirty-gated), preserving each field's existing validation, mutation call, and any confirm dialog. Behavior identical to today; only the interaction shell changes.

## Context

- `dashboard/src/features/databases/components/database-name-section.tsx` (Input + Save today), `database-version-control.tsx`, and any other single-value fields t001 flags.
- Shared component: `dashboard/src/features/services/components/editable-field-row.tsx` (import as-is; consider moving it to a shared `common/components` location only if t001 shows datastore reuse warrants it — otherwise import cross-feature).
- Hooks stay as-is (e.g. `use-database-name`, plan/version setters) — the mutation contract does not change.

## Steps

1. Swap each field's markup for `EditableFieldRow`, threading the existing save handler through `onSave` and any confirm through `confirm`.
2. Preserve field-specific dirty/validation rules (e.g. name format, allowed version targets).
3. Update the affected component unit tests to the new interaction.

## Files

- `dashboard/src/features/databases/components/database-name-section.tsx` (+ others per t001)
- matching files under `__tests__/`

## Acceptance criteria

- [ ] Each migrated field renders disabled with a pencil; edit flow matches w5/m50.
- [ ] Save semantics identical to today (same GraphQL mutations; no new operations).
- [ ] Existing tests updated (not deleted); suite green.

## Out of scope

- Key Value fields (t003); multi-row editors (parameter overrides, access-control list).
