---
id: w5/m55/t005
title: Render parity — cross-surface consistency check
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m55/t004]
---

## Objective

Compare the finished Postgres + Key Value detail pages against Render's live pages interaction-for-interaction (disabled inputs/selects, pencil enable+focus, Cancel/"Save changes" dirty gating, confirm dialogs) across the migrated fields. This milestone changes no REST/GraphQL/MCP semantics — the check is UI-behavioral; flag any drift as follow-up notes rather than silently diverging.

## Context

- Reference: t001's Render captures; `docs/ADR018-render-parity.md` datastore rows.

## Steps

1. Side-by-side dev-5 vs render.com walk of each migrated field on both pages.
2. Record divergences (intentional or follow-up) in the milestone README or as `w5/NNN.md` inbox notes.
3. Touch up ADR018 evidence pointers on the datastore rows if their edit-pattern notes are now stale.

## Files

- `docs/ADR018-render-parity.md` (evidence pointers only, if stale)

## Acceptance criteria

- [ ] Walk performed and outcome recorded (clean, or notes filed).

## Out of scope

- New feature gaps discovered during the walk (file as inbox notes).
