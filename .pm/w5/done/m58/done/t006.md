---
id: w5/m58/t006
title: "Live dev-5 proof + update docs/render-artifacts/metrics-page.md's drift list"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m58/t005]
---

## Objective

Prove the feature end to end on a live stack and retire the recorded drift: drive real traffic with distinct hosts/paths at a dev-5 service, watch the filters change the Network card, and remove Host/Path from the metrics-page drift list.

## Context

- dev-5 stack: `bash .pm/w5/dev-5/up.sh` / `status.sh` (isolated ports, `dev-5` namespaces). Loki must be configured for the Path filter path; if dev-5 lacks Loki, prove the store-unavailable state there and the filtered state on prod read-only.
- The drift record to update: `docs/render-artifacts/metrics-page.md` (m42's accepted-drift list; m56 already removed the percentile/range entries — follow its edit shape).
- Screenshots go to `.playwright-mcp/` via bare filenames (repo rule).

## Steps

1. Bring up dev-5, deploy/reuse a service, generate requests across ≥2 hosts (custom-domain or platform host) and ≥3 paths with mixed statuses.
2. In the browser: populate both dropdowns from discovery, apply each filter, and verify the requests/status/response-time sections change; capture screenshots.
3. Verify the honest states live: no-Loki (store-unavailable) and empty-discovery.
4. Edit `metrics-page.md`: move Host/Path filters from the drift list to the matched behavior, linking evidence; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/render-artifacts/metrics-page.md`
- `.playwright-mcp/` (captures)

## Acceptance criteria

- [ ] A live walk shows discovery-populated Host and Path filters visibly changing the Network card's series, with captures recorded.
- [ ] The store-unavailable state was observed live, not just unit-tested.
- [ ] `metrics-page.md` no longer lists Host/Path filters as drift.

## Out of scope

- Render-side re-capture (t007 owns the comparison).
