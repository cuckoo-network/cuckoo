---
id: w5/m58/t009
title: "Test coverage: filtered reads, discovery, reject/store-unavailable paths, UI states"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m58/t007]
---

## Objective

Close the meaningful-coverage gaps for the behavior m58 shipped: assert the filtered query construction, both honest failure modes, cross-surface field parity, and the card's filter states — real behavior and failure modes, never tautological or snapshot-everything tests.

## Context

- Backend seams: `metrics_test.go` (query construction per t001's matrix; store-unavailable when Loki-sourced + `BEX_LOKI_URL` unset; named reject for unsupported combinations; unfiltered queries unchanged), `logs_test.go` (discovery scope).
- Cross-surface parity precedent: the existing adapter-parity test shapes (e.g. `TestEventSurfaceParity`) — assert REST/GraphQL/MCP accept the same filters and produce dialect-correct errors.
- Dashboard: populated/empty/store-unavailable/clear-filter card states; the hook passing filters through to the operation.

## Steps

1. Audit t002–t005's tests against the list above; add what's missing rather than duplicating.
2. Add a cross-surface parity assertion for the new filter fields.
3. Run both suites and record counts.

## Files

- `lego/backend/internal/metrics/metrics_test.go`
- `lego/backend/internal/logs/logs_test.go`
- `dashboard/src/features/metrics/components/__tests__/`, `.../hooks/__tests__/`

## Acceptance criteria

- [ ] Every DoD behavior (filtering, discovery, store-unavailable, named reject, cross-surface parity, UI states) has at least one asserting test that fails if the behavior regresses.
- [ ] `cd lego/backend && go test ./...` and `cd dashboard && yarn test` pass.

## Out of scope

- Coverage-number chasing; e2e cluster tests (t006 was the live proof).
