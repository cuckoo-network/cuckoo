---
id: w5/m58/t010
title: Closeout
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m58/t009]
---

## Objective

Close the milestone once every other task is complete **and the definition of done is actually met** — the live filtered walk happened, the honest states were observed, all three surfaces agree, and the drift record is updated.

## Context

- Closeout is the move: set remaining tasks `status: done`, move each `tNNN.md` to `w5/m58/done/`, mark rows `— **DONE**` + `**Status:** done` in the milestone README, move `w5/m58/` to `w5/done/m58/`, check `- [x]` in `w5/README.md`. Never leave a tombstone at the old path.

## Steps

1. Verify the DoD line by line against real evidence (t006 captures, t007 verdicts, suite runs).
2. Perform the closeout moves and status syncs (all three places).
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m58/` → `.pm/w5/done/m58/`
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD verified true before any move.
- [ ] `w5/m58/` exists only under `w5/done/`; workstream checkbox checked; statuses in sync in all three places.

## Out of scope

- Closing with unmet DoD items — file follow-ups instead and leave the milestone open.
