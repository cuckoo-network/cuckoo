---
id: w5/m58/t008
title: "Simplify (/simplify over the milestone's diff)"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m58/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — the metrics core/adapters, any logs-discovery extension, and the Network-card UI — hunting reuse, simplification, and efficiency wins while preserving behavior.

## Context

- Likely targets: shared query-construction between the m56 multi-quantile read and the new filtered read; duplicated filter-threading across the three adapters; dropdown/state logic shared between the Status Code filter and the new Host/Path filters.

## Steps

1. `/simplify` scoped to the m58 diff (`lego/backend/internal/metrics/`, touched `internal/logs/` files, `dashboard/src/features/metrics/`).
2. Apply accepted simplifications; keep behavior byte-identical (tests stay green).

## Files

- `lego/backend/internal/metrics/`
- `dashboard/src/features/metrics/`

## Acceptance criteria

- [ ] `/simplify` was run over the milestone's diff and each accepted finding applied (or rejection noted).
- [ ] Backend + dashboard suites still pass.

## Out of scope

- New behavior of any kind.
