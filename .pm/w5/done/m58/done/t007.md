---
id: w5/m58/t007
title: "Render parity: filter fields/semantics consistent across REST/GraphQL/MCP/UI vs Render"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m58/t006]
---

## Objective

Check the shipped filters against Render on every surface: REST parameter spelling/semantics vs Render's metrics API, identical fields across bex's REST/GraphQL/MCP, and the Network-card UI vs Render's captured Host/Path controls; file any drift as follow-up work rather than silently diverging.

## Context

- bex-api is Render-compatible (`docs/ADR006-bex-api.md`); Render's metrics endpoints document host/path filter parameters — compare names, value formats (exact vs prefix path matching), and multi-value behavior.
- UI comparison baseline: the m42 live capture in `docs/render-artifacts/metrics-page.md` plus a fresh Render capture if the card has changed.
- Update the ADR018 row / gap-backlog only if a cell's claim changes.

## Steps

1. Diff bex's REST parameters against Render's documented metrics filter parameters; record match or named divergence in `metrics-page.md`.
2. Verify the three bex surfaces expose one spelling and one error dialect each (no cross-surface drift).
3. Compare the card UI (placement, default state, clearability, multi-select or single) against Render's; record verdicts.
4. File bounded notes for any drift found; correct any stale ADR018 claim.

## Files

- `docs/render-artifacts/metrics-page.md`
- `docs/ADR018-render-parity.md` (only if a cell changes)

## Acceptance criteria

- [ ] A written verdict exists per surface (REST/GraphQL/MCP/UI): match, or a named + filed divergence.
- [ ] No silent drift: every divergence is either fixed in-milestone or has a note.

## Out of scope

- Building any newly-discovered Render metrics capability beyond Host/Path (file it instead).
