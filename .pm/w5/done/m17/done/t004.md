---
id: w5/m17/t004
title: Extend the CreateService GraphQL document + use-create-service with type/schedule/command/publishPath; per-type redirect & deploy progress
worker: worker5
status: done
estimate: 25m
depends_on: [w5/m17/t003]
---

## Objective

Wire the new wizard state to the backend: the `CreateService` mutation document and `use-create-service` hook carry `type`, `schedule`, `command`, and `publishPath`, and post-create navigation lands on a service-detail view that makes sense for the type (e.g. a cron job shows its schedule, a static site its publish progress).

## Context

- Mutation document: `dashboard/src/features/services/api/services.graphql` — currently sends only `name/repo/image/branch/rootDir/plan/autoDeploy`.
- Backend `createService` already accepts all four new args plus `routes`/`headers` (`lego/backend/internal/apps/graphql.go:454-473`) — client-side change only; run the dashboard GraphQL codegen after editing the document.
- Hook: `dashboard/src/features/services/hooks/use-create-service.ts`.
- Post-create redirect today: service detail with deploy progress (w5/m15). Detail pages are already type-aware (w5/m11, w1/m21).

## Steps

1. Add the four variables to the `CreateService` document (omit `plan` for static sites — plan-less) and regenerate types.
2. Thread wizard state → mutation variables in `use-create-service`; only send fields valid for the selected type.
3. Verify post-create redirect renders sensibly per type (no URL section for worker/private; cron shows schedule).
4. Update the Apollo cache/services-list refresh as the existing hook does (keep behavior identical for web).

## Files

- `dashboard/src/features/services/api/services.graphql`
- `dashboard/src/features/services/hooks/use-create-service.ts`
- `dashboard/src/routes/services.new.tsx`

## Acceptance criteria

- [ ] Creating each type sends exactly the fields valid for it (assert payloads in tests later, t007)
- [ ] A created cron job's detail shows the submitted schedule/command; static site shows publishPath
- [ ] Web-service create behavior is byte-identical to pre-milestone

## Out of scope

- Backend changes (none needed); wizard collection of static `routes`/`headers`
