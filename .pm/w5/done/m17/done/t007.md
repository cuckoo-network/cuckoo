---
id: w5/m17/t007
title: Test coverage — per-type submit payloads, conditional-field visibility, validation failure modes
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m17/t005]
---

## Objective

Meaningful tests for the behavior this milestone shipped: for each type, the wizard renders the right fields and submits the right mutation variables; validation failures actually block submit. Assert real behavior and failure modes — no snapshot-everything.

## Context

- Existing wizard tests: `dashboard/src/routes/__tests__/` (w5/m15 added create-wizard coverage — extend the same harness/mocks).
- Highest-value assertions: (1) mutation variables per type — cron sends `schedule`/`command`, static sends `publishPath` and no `plan`, web payload unchanged; (2) field visibility flips with type selection; (3) invalid cron expression → submit blocked + message; (4) missing publishPath handling per capture semantics.

## Steps

1. Extend the wizard test suite with per-type render + submit-variable assertions (mock Apollo, inspect mutation variables).
2. Add validation failure cases (bad cron, empty required fields).
3. Run the dashboard test suite.

## Files

- `dashboard/src/routes/__tests__/` (wizard tests)

## Acceptance criteria

- [ ] A test per type asserts the exact mutation-variable set sent
- [ ] Validation failure tests exist and fail if validation is removed
- [ ] Suite green

## Out of scope

- E2E against a live cluster (the parity task already did live comparison)
