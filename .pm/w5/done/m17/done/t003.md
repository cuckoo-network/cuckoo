---
id: w5/m17/t003
title: "Per-type conditional fields: publishPath (static), schedule+command (cron), no-URL note (worker/private)"
worker: worker5
status: done
estimate: 35m
depends_on: [w5/m17/t002]
---

## Objective

Make the wizard's Settings step type-aware, mirroring the t001 capture: a static site asks for a publish directory and drops the instance-type grid; a cron job asks for a schedule and command with cron-expression validation; workers/private services note there will be no public URL. Show exactly the fields Render shows per type — omit what bex can't honor rather than inventing fields.

## Context

- Current Settings form (web-only): name, branch/rootDir (git sources), instance-type card grid, auto-deploy toggle — `dashboard/src/routes/services.new.tsx`.
- Static site: `publishPath` (default per capture, e.g. `dist`/`build`); no instance type (Render static sites are plan-less), no port.
- Cron job: `schedule` (required, cron expression) + `command`; validate the expression client-side with the same leniency the backend accepts (see `lego/types` schedule handling / backend validation) — error strings per the capture.
- The type-aware Settings *detail* pages already do this hide/show dance (w5/m11 `cron-deploy-section`, w1/m21 `static-site-section`) — mirror their vocabulary.
- i18n: en + zh.

## Steps

1. Conditionally render per-type fields in the Settings step, driven by the t002 type state.
2. Cron: schedule + command inputs, client-side cron validation, required-ness matching the backend (schedule required for cron_job).
3. Static: publishPath input with capture-matching default + helper text; hide instance-type grid and port-derived UI.
4. Worker/private: informational no-public-URL note per the capture.
5. Locale strings (en + zh).

## Files

- `dashboard/src/routes/services.new.tsx`
- locale files (en/zh)

## Acceptance criteria

- [ ] Static form shows publishPath, hides the instance-type grid; cron form shows schedule+command with validation; worker/private show the no-URL note
- [ ] Field labels/defaults/validation messages match the t001 capture (drift documented)
- [ ] Invalid cron expression blocks submit with an actionable message

## Out of scope

- Actually sending the new fields (t004); routes/headers editing for static sites (already in Settings detail, not the wizard — Render's wizard doesn't collect them either)
