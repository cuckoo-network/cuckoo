---
id: w5/m17/t002
title: Type picker (Web / Private / Static Site / Background Worker / Cron Job) wired into the wizard flow
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m17/t001]
---

## Objective

Add service-type selection to `/services/new`, matching the t001 capture's presentation (names, descriptions, ordering) so a user picks a type before or alongside the source step, and the rest of the wizard adapts to it.

## Context

- The wizard is a single route file: `dashboard/src/routes/services.new.tsx` (three source tabs: GitHub repo picker, public Git URL, existing image).
- Backend accepted values: `web_service` (default) · `private_service` · `background_worker` · `cron_job` · `static_site` (`lego/backend/internal/apps/graphql.go` `createService` args).
- Follow the t001 capture for whether Render picks type first (separate `/new/<type>` entries) or in-form — mirror that IA, adapted to bex's single-route wizard.
- i18n: all new strings in both `en` and `zh` locale files (repo convention since w5/m3).

## Steps

1. Add the type-selection UI (cards or entry list per the capture) with the five types; default `web_service`.
2. Thread the selected type through wizard state; source tabs stay valid for all types except image-only/static rules the capture dictates.
3. Reuse `service-type-badge`/`lib/service-type.ts` vocabulary so list, detail, and wizard name types identically.
4. Add locale strings (en + zh).

## Files

- `dashboard/src/routes/services.new.tsx`
- `dashboard/src/features/services/lib/service-type.ts` (reuse; extend only if a type is missing)
- `dashboard/src/features/services/locales/en.ts`, `zh.ts` (or `common/locales` — match existing placement)

## Acceptance criteria

- [ ] All five types selectable; selection visibly drives the rest of the form
- [ ] Type names/descriptions match the t001 render.com capture (drift documented, not silent)
- [ ] Strings localized in en + zh

## Out of scope

- Per-type conditional fields (t003); mutation changes (t004)
