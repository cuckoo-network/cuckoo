---
id: w5/m17/t008
title: Closeout — DoD met → move milestone to done/
worker: worker5
status: done
estimate: 10m
depends_on: [w5/m17/t007]
---

## Objective

Close w5/m17 once its definition of done actually holds — all five types creatable from `/services/new`, capture-consistent, ledger updated — not merely when the code is written.

## Context

- Closeout procedure: `.claude/skills/pm/SKILL.md` (`done` subcommand step 4) — statuses synced, tasks to `done/`, milestone dir to `w5/done/m17/`, workstream checkbox checked.

## Steps

1. Verify the DoD end-to-end (create one of each type; check ledger cells).
2. Set remaining task statuses `done`, move task files to `w5/m17/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w5/m17/` → `w5/done/m17/`; check `- [x]` in `w5/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m17/` → `.pm/w5/done/m17/`
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD verified before any move
- [ ] Status synced in all three places; no tombstone left at the old path

## Out of scope

- New work discovered at closeout (file inbox notes)
