# Paid KeyValue backups broken since 2026-08-23 — ship fix, close purge + alerting gaps

> Promoted to **[w8/m30](../../w8/m30/README.md)** 2026-08-25 (user handoff); this note is retained as the incident summary only.

Why: the 2026-08-25 backup verification ([drill record](../../docs/drills/2026-08-25-backup-verification.md)) found all three paid KeyValue instances at zero-to-one recovery points against a ≤24 h RPO. Two compounding production defects: (1) the w7/m77 namespace cutover's CR deletes ran the purge finalizer against the **shared per-id prefix**, erasing all pre-cutover objects; (2) w7/m85's `/backup-encrypt` wrote `0600` ciphertext the capability-dropped (`DAC_OVERRIDE`-less) upload stage cannot read — every scheduled kvbak Job has failed since 2026-08-23. `BackupCronJobStale` fired for 2 days with no human action. Same-session mitigation minted one fresh recovery point per instance via one-off Jobs with `upload.runAsUser` matching the encrypt UID; the nightly pipeline stays broken until the fix ships.

Work items:

- **Ship** the pending working-tree fix (`lego/operator/cmd/backup-encrypt/main.go` → `0644` + cross-UID readability regression test) and roll the operator; blocked on the self-hosted runner fleet, which is **down** (every Actions job queued since 2026-08-25T05:01Z) — restore runners first.
- After rollout, confirm the next scheduled `kvbak-*` runs succeed in both tenant namespaces and `BackupCronJobStale` resolves; manually trigger one Job per instance if the RPO window is close.
- Decide cutover purge semantics: a migration delete must not purge the recreated instance's backup prefix (suspend/skip the purge finalizer on cutover deletes, or adopt the ADR050 Object Lock/WORM follow-up so even a purge cannot destroy retained objects). Update `docs/runbooks/datastore-namespace-cutover.md`.
- Route `BackupCronJobStale`/`PlatformDatabaseBackupStale` to a channel a human actually sees (ADR052 notification path or pager); a critical backup alert firing 2 days unnoticed is its own defect.
- Complete the queued OpenBao restore drill (run `32814333448`, target `restore-bao-verify`) once runners return — it also re-proves ADR050 Tier A decrypt custody (etcd/OpenBao/KV share one age recipient). Cancel and re-dispatch if stale.
- KeyValue live-restore re-drill (ADR031 cadence: snapshot-job change is a trigger; both m85 and this fix qualify).

Acceptance: three consecutive nightly kvbak successes across all instances, alert quiet, purge-on-cutover decision recorded, OpenBao drill green.
