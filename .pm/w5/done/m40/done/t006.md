---
id: w5/m40/t006
title: Simplify — /simplify over the milestone's diff
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m40/t005]
---

## Objective

Run `/simplify` over the milestone diff, focusing on shared deploy metadata/duration formatting, action eligibility, and whether list/detail state components can be reduced without hiding semantic differences.

## Steps

1. Run `/simplify`; apply findings or record concise reasons for declining them.
2. Remove duplicate timestamp, duration, status, and search-normalization logic where one shared helper is clearer.
3. Keep log-store-unavailable, empty-result, and live-tail-disconnected branches separate even if their layouts share primitives.

## Files

- As flagged by the milestone diff.

## Acceptance criteria

- [ ] Findings are applied or declined with reasons.
- [ ] List and detail share the same deploy duration/status vocabulary without collapsing distinct log states.

## Out of scope

- New behavior or speculative generic abstractions outside deploy/service presentation.
