---
id: w5/m40/t004
title: Live dev-5 acceptance: failed and successful deploy paths against the corrected Render target
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m40/t001, w5/m40/t002, w5/m40/t003]
---

## Objective

Exercise the finished pages in a real dev-5 browser session with both a terminal failure and a successful/live deploy, then repair bounded dashboard drift found by that walk.

## Context

- The corrected reference is Render Web Service `https://dashboard.render.com/web/srv-d2rnr3jipnbc73deuvgg?`, not the earlier static-site URL.
- Existing baseline captures are gitignored under `.playwright-mcp/`: `bex-node-hello-deploy-list.png`, `bex-node-hello-deploy-detail.png`, `render-web-deploy-list.png`, and `render-web-deploy-detail.png`.
- The initial dev-5 service had only one failed deploy, which cannot prove live-state facts or rollback eligibility.

## Steps

1. Bring up/check the isolated `.pm/w5/dev-5/` stack without touching other workers' namespaces or ports.
2. Produce or select one failed deploy and one successful/live deploy with deterministic metadata; do not mutate the Render reference service.
3. Verify search + status filter composition, count semantics before/after Load more, row/detail metadata, truthful storeless logs, runtime, and phase/deploy coherence.
4. Verify rollback appears only for an eligible successful historical deploy and shares the established confirmation behavior.
5. Check desktop and narrow viewport keyboard/focus behavior; repair only bounded w5 drift and record any cross-layer blocker as an owner-specific follow-up.

## Files

- `dashboard/src/features/deploys/**`
- `dashboard/src/features/services/**`
- `.pm/w5/dev-5/**` only if a deterministic fixture/helper needs a reusable adjustment
- `.playwright-mcp/` bare-filename evidence (gitignored)

## Acceptance criteria

- [ ] One failed and one successful/live dev-5 deploy exercise the complete DoD in a real browser.
- [ ] Rollback eligibility is observed on successful history, not inferred from the failed-only fixture.
- [ ] Storeless dev-5 remains explicitly store unavailable; the test does not pass by substituting fake retained logs.
- [ ] Narrow-width and keyboard interaction have no blocked row/action targets.
- [ ] Residual non-w5 divergence has a concrete owner and evidence.

## Out of scope

- Changing Render state, production infrastructure, persistent disks, previews, one-off jobs, or external log drains.
