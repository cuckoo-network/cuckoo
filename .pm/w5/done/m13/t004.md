---
id: w5/m13/t004
title: Live verify Root Directory end to end
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m13/t003]
---

## Objective

Verify the Root Directory dashboard flow works end to end against a real (or mock-cluster) monorepo test app: setting it from the UI triggers a subdirectory-scoped build, and the milestone's Definition of Done is genuinely met before closeout.

## Context

- Reuses the monorepo test repo set up in `w1/m18/t010`'s closeout verification, if still available, rather than creating a second one.
- `scripts/mock-cluster.sh` / `scripts/app-apply.sh` for a local verification path if prod isn't convenient.

## Steps

1. Deploy a monorepo test App via the dashboard's create flow (or an existing one), set Root Directory to a subdirectory via the new UI.
2. Confirm the build succeeds and only builds from that subdirectory (watch the BuildKit Job/logs).
3. Push a commit outside the root dir and confirm no redeploy fires; push one inside it and confirm a redeploy does.
4. Screenshot the working Settings section for the milestone record.

## Files

- None (verification task).

## Acceptance criteria

- [ ] UI-driven Root Directory change results in a correctly-scoped build, observed live
- [ ] Screenshot captured showing the working Build & Deploy section

## Out of scope

- Any new implementation — fix-forward only if verification surfaces a bug in t002/t003.
