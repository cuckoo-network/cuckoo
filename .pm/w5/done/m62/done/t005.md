---
id: w5/m62/t005
title: "Execute what note 028 supports on dev-5; close it or narrow it to the prod-only residue"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m62/t002, w5/m62/t003]
---

## Objective

Run as much of m58's deferred live walk (note `028` — Host/Path metrics filters) as the repaired dev-5 supports: at minimum the discovery-populated Host dropdown, the Path input, and the store-gated honest state against real Loki; then either close the note or rewrite it to name exactly the residue only prod can prove.

## Context

- m58's filters read the **request-log** store (`type=request`, Traefik-origin streams). t002 established what dev-5's pipeline produces: if request-shaped streams (host/path labels) are attainable locally, the full filtered-series walk runs here; if only app-pod streams exist, the filtered-series proof is the prod residue and this task proves discovery + honest states + the named-400 paths.
- The walk spec is m58's own t006 (`w5/done/m58/done/t006.md`); store-unavailable is proven by pointing the dev-5 bex-api at no Loki (unset `BEX_LOKI_URL`, restart) — now trivially togglable.
- Closing/narrowing the note: either move `028.md` → `done/` with evidence, or rewrite it in place to only the prod-only items with a one-line rationale; sync the m58 closure line in the w5 README.

## Steps

1. With `status.sh` green, drive the metrics Network card: discovery values, Host/Path filter application, store-gated state via the Loki toggle; capture.
2. If t002's pipeline yields request-labeled streams, verify filtered series change with real traffic; else document the exact residue.
3. Close or narrow note `028`; README sync; prettier pass.

## Files

- `.pm/w5/028.md` (→ `done/` or narrowed in place)
- `.pm/w5/README.md` (inbox + m58 line sync)
- `.playwright-mcp/` (captures)

## Acceptance criteria

- [ ] Discovery, filter controls, and the store-unavailable state proven live on dev-5 with captures.
- [ ] Note `028` is closed, or contains only named prod-only items with rationale — no vague remainder.

## Out of scope

- Building a Traefik request-log emulation if t002 didn't already yield it (that residue is exactly what stays in the note).
