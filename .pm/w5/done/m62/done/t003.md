---
id: w5/m62/t003
title: "status.sh verification inventory: CNPG, Loki ingest→query, bex-api log reads non-503"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m62/t002]
---

## Objective

Extend `dev-5/status.sh` from a process/pod/HTTP health check into the verification inventory a live walk needs: every component a w5 DoD walk depends on gets an explicit pass/fail line, so "is dev-5 walk-ready?" is one command instead of a mid-walk discovery.

## Context

- Current `status.sh` checks processes, pods, HTTP. The failure mode it missed: the stack partially rises and the gap surfaces only when a milestone's live proof reaches the broken piece.
- Inventory to assert: CNPG operator + the three dev-5 Clusters healthy; Loki reachable **and** round-tripping (write a probe line or find a recent one, query it back); bex-api up and its log verb returning non-503 with the store configured; dashboard dev server reachability hint; the port-forwards from `ports.env` alive.
- Exit non-zero on any failure with the failing line clearly marked — sessions script against this.

## Steps

1. Add the inventory checks to `status.sh`, one labeled line each, following its existing output style.
2. Make the Loki check a real round-trip (ingest→query), not a bare HTTP 200.
3. Verify: full-green on the risen stack; break one piece (scale down Loki) and confirm the exact line fails and the exit code is non-zero.

## Files

- `.pm/w5/dev-5/status.sh`

## Acceptance criteria

- [ ] `status.sh` green implies every component a w5 live walk needs is actually usable (round-trip-verified where applicable).
- [ ] A broken component produces a named failing line + non-zero exit, demonstrated by breaking one.

## Out of scope

- Auto-repair (status reports; up.sh repairs).
