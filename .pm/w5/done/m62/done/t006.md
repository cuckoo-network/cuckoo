---
id: w5/m62/t006
title: "Simplify (/simplify over the dev-5 script diff)"
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m62/t004, w5/m62/t005]
---

## Objective

Run `/simplify` over the milestone's script diff — `up.sh`, `down.sh`, `status.sh` — hunting duplication between the new install/preflight/inventory blocks and the scripts' existing idioms, while preserving behavior.

## Context

- Likely targets: shared helper for the labeled pass/fail output lines; one precondition-check pattern reused across preflight and inventory; install-step idiom consistency with the existing helm/apply blocks.

## Steps

1. `/simplify` scoped to the m62 diff under `.pm/w5/dev-5/`.
2. Apply accepted findings; re-run `up.sh` (idempotent) + `status.sh` to confirm behavior preserved.

## Files

- `.pm/w5/dev-5/up.sh`, `down.sh`, `status.sh`

## Acceptance criteria

- [ ] `/simplify` run with findings applied or explicitly rejected.
- [ ] `up.sh` re-run and `status.sh` full-green after the changes.

## Out of scope

- New checks or behavior.
