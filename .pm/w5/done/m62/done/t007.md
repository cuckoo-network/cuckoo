---
id: w5/m62/t007
title: "Test coverage: preflight/inventory assertions are real checks that fail on the broken states"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m62/t004, w5/m62/t005]
---

## Objective

Prove the guards guard: for each failure mode this milestone was created to catch (CNPG absent, Loki down, store-unconfigured bex-api, dead port-forward), demonstrate the corresponding preflight/inventory check actually fails on it — negative verification, not happy-path-only — and record the matrix.

## Context

- Shell scripts don't ride the unit-test suites; the meaningful equivalent is a documented break-it matrix: induce each broken state, run the check, record the named failing line + exit code. `bash -n` / shellcheck over the diff is the cheap static layer on top.
- Record the matrix in the milestone (this task file or the closeout evidence) so the next dev-5 regression has a reproduction recipe.

## Steps

1. Enumerate the guarded failure modes from t001–t003; induce each (scale down / unset / kill) and record the check's failure output + exit code.
2. Run shellcheck (or `bash -n` at minimum) over the three scripts; fix findings in the diff's scope.
3. Restore the stack to full-green.

## Files

- `.pm/w5/dev-5/up.sh`, `status.sh` (fixes only if the matrix exposes a check that passes when it shouldn't)
- The break-it matrix record (this file or milestone evidence)

## Acceptance criteria

- [ ] Every guarded failure mode has a recorded induced-failure run showing the named check failing with non-zero exit.
- [ ] Static check clean over the script diff; stack restored to green afterward.

## Out of scope

- CI automation of the matrix (file a note if it seems worth it).
