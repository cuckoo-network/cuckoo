---
id: w5/m62/t004
title: "Execute the note-029 walk (cron Trigger Run/detail · notify override · credential rotate)"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m62/t001]
---

## Objective

Run the live browser walk m60 deferred (note `029`): on the repaired dev-5 stack, verify the three shipped dead-end closures end to end — cron Trigger Run with the active-run rejection, run-detail expansion, the notify-on-fail override round-trip, and registry-credential edit/rotate with secret-preservation — then close the note.

## Context

- The walk script is m60's own t005 (`w5/done/m60/done/t005.md`): cron-job service with a short command, registry-credential fixture, the specific edge cases (double-trigger rejection, blank-token keep). Needs CNPG (t001) but **not** Loki — hence only t001 as dependency.
- Reminder from m60's closeout: the override editor writes via `setNotificationsToSend` (4-state), not the legacy `setNotifyOnFail` — verify the shipped behavior, not the round-2 proposal text.
- Captures to `.playwright-mcp/`, bare filenames. Closing the note: move `029.md` → `.pm/w5/done/029.md` and update the m60 closure line in the w5 README to record the walk completed.

## Steps

1. `up.sh` + `status.sh` green; create/reuse the cron service and a registry credential.
2. Walk all three closures including edge cases; capture each.
3. Close note `029` (move to `done/`, README sync); note any defect found as a new bounded note instead of silently fixing beyond sub-hour scope.

## Files

- `.pm/w5/029.md` → `.pm/w5/done/029.md`
- `.pm/w5/README.md` (inbox + m60 line sync)
- `.playwright-mcp/` (captures)

## Acceptance criteria

- [ ] All three m60 closures verified live with captures, including the rejection and secret-preservation edge cases.
- [ ] Note `029` closed with the evidence pointer; any live-found defect filed as its own note.

## Out of scope

- The 028 walk (t005); fixing live-found defects beyond sub-hour.
