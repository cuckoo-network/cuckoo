---
id: w5/m31/t007
title: "Simplify — /simplify over the code this milestone changed"
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m31/t006]
---

## Objective

Run `/simplify` over the dashboard diff; apply behavior-preserving findings (shared dialog/selector reuse is the likely win).

## Acceptance criteria

- [x] `/simplify` run; findings applied or declined with reasons.

## Resolution

The simplify pass extracted the duplicated Project/Environment selectors, centralized protected-error parsing and the typed retry dialog, propagated one `RunServiceAction` result type, and keyed retry dialogs so stale typed text cannot pre-arm a later action. Delete keeps its embedded two-stage dialog because the immutable-id gate must transition in place to the server-issued phrase.
