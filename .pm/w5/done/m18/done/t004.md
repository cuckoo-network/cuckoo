---
id: w5/m18/t004
title: Render parity — REST/GraphQL/MCP/UI consistency vs Render's cron Deploy section + OpenAPI PATCH shapes; ledger cron caveat retired
worker: worker5
status: done
estimate: 25m
depends_on: [w5/m18/t002, w5/m18/t003]
---

## Objective

Verify the shipped write path is one behavior across four surfaces and Render-consistent: the same schedule/command edit through REST `PATCH`, the GraphQL mutation, MCP, and the dashboard produces the same converged CronJob and the same rejection for invalid input; field names/shapes compared against Render's OpenAPI `PATCH /services/{id}` (`serviceDetails.schedule`) and the live cron Deploy section. Update the ledger.

## Context

- Binding baseline per user instruction at materialization ("ensure they are render.com consistent", 2026-07-12): Render OpenAPI (`api-docs.render.com/openapi/render-public-api-1.json`) + the live dashboard capture from t003/w5/m11.
- Ledger row to update: cron-job row in `docs/ADR018-render-parity.md` — retire "the write path is a follow-on"; keep the remaining ◐ items (Build Command, Log Stream, Notifications) explicitly listed.
- Cross-surface method precedent: `TestAuditSurfaceParity`-style same-verb proof where practical.

## Steps

1. Same edit through all four surfaces → identical resulting spec; same invalid input → same error class/message on each.
2. Diff REST/GraphQL/MCP field names against Render's OpenAPI cron `PATCH` vocabulary; flag drift as fix or documented divergence.
3. Update the ledger cron row (evidence pointers); `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- (evidence) `docs/render-artifacts/`

## Acceptance criteria

- [ ] Four-surface equivalence demonstrated (spec result + error behavior)
- [ ] Ledger cron row updated: write-path caveat gone, remaining ◐ items still explicit
- [ ] Any Render-shape drift documented, never silent

## Out of scope

- Closing the remaining cron ◐ items
