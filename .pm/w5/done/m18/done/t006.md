---
id: w5/m18/t006
title: Test coverage — mutation validation, non-cron 4xx, UI save/error states
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m18/t004]
---

## Objective

Meaningful tests for the write path's real behavior and failure modes across the layers this milestone touched — not tautological coverage.

## Context

- Backend: table-driven tests beside the existing apps adapter tests — valid update converges the view; invalid cron rejected; schedule/command on a non-cron service → 4xx matching REST's; GraphQL and MCP delegate to the same verb (assert equivalence, the `TestAuditSurfaceParity` pattern).
- Dashboard: component tests for edit → save (mutation variables asserted), server-error surfacing, validation blocking submit — beside the existing `cron-deploy-section`/`build-deploy-section` tests.

## Steps

1. Backend adapter tests (GraphQL + MCP if functional): success, invalid cron, non-cron 4xx, authz denial.
2. Dashboard tests: edit-in-place happy path, validation block, server error state.
3. `cd lego/backend && go test ./...` + dashboard suite green.

## Files

- `lego/backend/internal/apps/` test files
- `dashboard/src/features/services/components/__tests__/`

## Acceptance criteria

- [ ] A failing core verb or removed validation makes a test fail (no tautologies)
- [ ] Non-cron 4xx and invalid-cron cases covered on backend and UI
- [ ] Both suites green

## Out of scope

- E2E cluster convergence (covered by t004's live verification)
