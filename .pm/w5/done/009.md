# Health Check Path field in service Settings — gated on w1/m23/t001

Render's Settings → Health & Alerts exposes **Health Check Path** for web services; the parity ledger's "Health checks" row is UI ✖ (REST ◐ accepts `serviceDetails.healthCheckPath` on create only; GraphQL/MCP ✖). Do **not** build UI yet: `w1/m23/t001` decides wire-`healthCheckPath`-into-a-ReadinessProbe **or drop the field** — UI before that resolves is speculative.

If t001 keeps the field: the update surfaces (REST `PATCH healthCheckPath`, a GraphQL setter, MCP) need a backend home first — file that as a w2 inbox note then, and promote this alongside it. Capture Render's Health & Alerts section live (→ `docs/render-artifacts/`) at build time for render.com consistency.

Source: `/pm-brainstorm more for w5` 2026-07-12.
