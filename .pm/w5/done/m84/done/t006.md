---
id: w5/m84/t006
title: Live E2E - production resume + steer-redispatch answer a context-dependent prompt
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m84/t005]
---

## Objective

After ship + rollout (platform image AND the agent-sandbox image — the driver lives there), prove all three DoD legs live on production and record evidence in the milestone README.

## Steps

1. Fresh session with a real turn → let it hibernate (or force via idle) → resume → steer "continue where you left off, summarize what you did" → assert an in-context answer (rung 1 or 2 per adapter support).
2. Steer a completed live session (redispatch path) with a context-dependent prompt → in-context answer.
3. Recreate the `ags-da9mh5vj596c73en5eq0` shape (or resume it, with the user's okay): resume after setup-only-failures → "try again" retries the task.
4. Verify the dashboard hint states; capture the turn annotations from the transcript.

## Acceptance criteria

- [x] All three legs pass live; evidence (session ids, annotations, timestamps) recorded in the milestone README.
