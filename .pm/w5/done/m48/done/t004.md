---
id: w5/m48/t004
title: "Settings static gaps: no Instance Type row, name-not-id value, root-dir prefix, hook URL check"
worker: worker5
status: done
estimate: 45m
depends_on: []
---

## Objective

Close the small per-row Settings gaps the live comparison surfaced for static sites: bex renders an Instance Type row (Render has no instance-type concept for static sites), the Service Name row displays the `srv-…` id instead of the name, inputs lack Render's root-directory prefix affordance, and the Deploy Hook section showed no inline URL on the inspected service.

## Context

- Evidence: bex `.playwright-mcp/bex-settings.png` + a11y snapshot `page-2026-07-19T05-25-29-838Z.yml` (Instance Type row e167–e171; name row e162–e166 shows `srv-d9e5sbd5qe4s73b1mjq0`); Render `.playwright-mcp/render-settings.png` (no instance row; Name textbox prefilled `eden-cms`; `eden-cms/ $` prefix on Build Command and `eden-cms/` on Publish Directory; Deploy Hook URL inline with show-secret/copy/regenerate).
- Name row: `display-name-row.tsx` — looks like a displayName-unset fallback to id; the header correctly shows `static-site`, so fall back to slug/name, never the id.
- Instance Type row: `instance-type-row.tsx`, rendered from `services.$serviceId.settings.tsx` General card — gate off for `static_site` (t001 hides the Plan tab it links to).
- Root-dir prefix: Build Command (`build-deploy-section.tsx`) and Publish directory (`static-site-section.tsx`) inputs should show a `<rootDir>/` prefix when root directory is set — display-only affordance.
- Deploy Hook: `deploy-hook-section.tsx` — on srv-d9e5sbd5qe4s73b1mjq0 only a disabled "Regenerate Hook" button rendered. Verify on a healthy store-managed service whether the URL + copy renders; if it's a load/error state on this service only, document; if the URL genuinely never displays inline, fix to Render's shape (URL + show/copy + regenerate).

## Steps

1. Gate the Instance Type row off for `static_site` in the settings General card.
2. Fix `display-name-row.tsx` fallback: value = displayName ?? name/slug, never the id.
3. Add the root-dir prefix affordance to Build Command + Publish directory inputs.
4. Verify/fix Deploy Hook inline URL display; capture evidence either way.

## Files

- `dashboard/src/routes/services.$serviceId.settings.tsx`
- `dashboard/src/features/services/components/display-name-row.tsx`
- `dashboard/src/features/services/components/build-deploy-section.tsx`
- `dashboard/src/features/services/components/static-site-section.tsx`
- `dashboard/src/features/services/components/deploy-hook-section.tsx`

## Acceptance criteria

- [x] Static-site Settings shows no Instance Type row; web-service Settings unchanged.
- [x] Service Name row prefills the human name for a service with no displayName set.
- [x] With a root directory set, Build Command and Publish directory inputs display the `<rootDir>/` prefix.
- [x] Deploy Hook renders the URL inline with copy (or the load-state cause is documented with evidence).

## Deploy Hook verification (2026-07-18)

The section already renders Render's shape once the `DeployHook` query resolves: masked URL input + reveal/copy + Regenerate (browser-verified against the dev stub). The prod capture that showed only a disabled "Regenerate Hook" was the query's in-flight loading state (skeleton + disabled button) at snapshot time, not a missing feature — no code change needed beyond the m48 items.

## Out of scope

- Section regrouping to Render's General/Build/Deploy order (cosmetic; revisit only if a later parity walk flags it).
