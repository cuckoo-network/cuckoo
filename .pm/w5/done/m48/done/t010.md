---
id: w5/m48/t010
title: Closeout — verify DoD, mark done, move milestone to done/
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m48/t009]
---

## Objective

Close w5/m48 only when its definition of done actually holds: walk the milestone README's DoD against a live static site (never-successfully-deployed for the URL check), then perform the board moves.

## Context

- DoD lives in `w5/m48/README.md`. The URL-from-creation check needs a fresh static site (dev-5 publish or prod srv-d9e5sbd5qe4s73b1mjq0 if still un-deployed).
- `.pm/CLAUDE.md` move conventions: no tombstones; the milestone directory must not exist at its old path after the move.

## Steps

1. Verify every DoD bullet live; capture evidence.
2. Set every remaining task `status: done`, move `tNNN.md` files to `w5/m48/done/`, mark all rows `— **DONE**`, set `**Status:** done`.
3. Move `w5/m48/` → `w5/done/m48/`; check the `- [x]` box in `w5/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m48/**`, `.pm/w5/README.md`

## Acceptance criteria

- [x] DoD verified live before any move.
- [x] Board state consistent in all three places (task frontmatter, milestone README, workstream README).

## Out of scope

- Shipping (`/ship` is user-initiated).
