---
id: w5/m48/t009
title: Test coverage — meaningful tests for type-gating, URL derivation, branch update
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m48/t007]
---

## Objective

Add tests that assert the real behavior this milestone shipped and its failure modes — no tautological or snapshot-everything tests.

## Context

- Dashboard (vitest): static-site nav/settings gating (static hides Shell/Scaling/Plan/Instance-Type; web keeps them), redirects/headers pages render + save, header URL row present from creation, display-name fallback never shows the id, root-dir prefix rendering.
- Backend (Go): derived-URL projection (set vs unset `BEX_BASE_DOMAIN`, subdomain policy off ⇒ omitted; identical across REST/GraphQL adapters), branch update path (valid change persists + projects to the CR; invalid/unauthorized rejected in Render's error dialect).
- Static metrics gating: static type never issues the pod CPU/mem queries.

## Steps

1. Dashboard component/route tests per above.
2. Backend unit/integration tests in `lego/backend` (respect `BEX_TEST_DB_URI` gating conventions).
3. Full suites: `cd lego/backend && go test ./...`, dashboard `yarn test`.

## Files

- `dashboard/src/features/services/**/*.test.tsx` (new/extended)
- `lego/backend/internal/apps/…_test.go`

## Acceptance criteria

- [x] Each new behavior has at least one test that fails if the behavior regresses (verified by inverting once locally or reasoning recorded).
- [x] All suites green.

## Out of scope

- E2E kind-cluster runs (`make test-e2e`).
