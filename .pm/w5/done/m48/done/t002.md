---
id: w5/m48/t002
title: Dedicated Redirects/Rewrites + Headers pages under Manage (move editors out of Settings)
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m48/t001]
---

## Objective

Give static sites Render's two dedicated Manage sidebar pages — **Redirects/Rewrites** and **Headers** — by moving the existing editors out of the Settings page. Content parity already shipped (w1/m21, `static-site-section.tsx`); only the information architecture differs from Render.

## Context

- Render static sidebar has `…/redirects` and `…/headers` as first-class Manage pages (evidence: `.playwright-mcp/page-2026-07-19T05-25-48-576Z.yml` lines 76–81); its Settings page contains neither editor.
- bex embeds both editors in Settings via `dashboard/src/features/services/components/static-site-section.tsx` (routes editor + headers editor + publish path).
- Route-alias precedent: w5/m39 added `/static/$id/*` Render-shaped redirect aliases — the new pages should be reachable through those too (`/static/srv-…/redirects` → `/services/srv-…/redirects`).
- Publish directory stays in Settings (Render keeps Publish Directory in Settings → Build).

## Steps

1. Create `services.$serviceId.redirects.tsx` and `services.$serviceId.headers.tsx` routes rendering the existing routes/headers editors (extract them from `static-site-section.tsx` into reusable components if not already).
2. Add both entries to the Manage group in `service-nav.tsx`, gated to `static_site`.
3. Slim the Settings "Static Site" card to Publish directory only (keep a link to the two new pages).
4. Confirm the m39 alias routes cover `/static/$id/redirects` and `/static/$id/headers`.
5. en/zh locale entries for the two nav labels + page titles.

## Files

- `dashboard/src/routes/services.$serviceId.redirects.tsx` (new)
- `dashboard/src/routes/services.$serviceId.headers.tsx` (new)
- `dashboard/src/features/services/components/static-site-section.tsx`
- `dashboard/src/features/services/components/service-nav.tsx`

## Acceptance criteria

- [x] Static-site sidebar shows Redirects/Rewrites and Headers under Manage; both pages edit and save rules exactly as the Settings-embedded editors did.
- [x] Settings' Static Site card no longer contains the two editors (publish dir remains).
- [x] Non-static services show neither page nor nav entry.

## Out of scope

- Any change to the rules' backend semantics (`SetStaticRoutes`/`SetStaticHeaders` stay as-is).
