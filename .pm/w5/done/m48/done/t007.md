---
id: w5/m48/t007
title: Render parity — REST/GraphQL/MCP/UI consistency for every surface this milestone touched
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m48/t001, w5/m48/t002, w5/m48/t003, w5/m48/t004, w5/m48/t005, w5/m48/t006]
---

## Objective

Check that every change this milestone shipped is consistent across the surfaces it exposes — REST, GraphQL, MCP (`lego/backend/`, docs/ADR006-bex-api.md) and the dashboard — and compare against live render.com behavior, flagging drift as follow-up instead of silently diverging.

## Context

- Highest-risk cross-surface items: the derived platform URL (t003 — must be identical in REST `serviceDetails.url`, GraphQL, MCP, dashboard) and branch editability (t005 — Render's `PATCH /v1/services` accepts branch; verify wire shape against Render's API docs/behavior).
- Update `docs/ADR018-render-parity.md` static-site rows (and `docs/cli-compatibility-checklist.md` if the CLI reads url/branch) with evidence for any cell that changes.
- Comparison anchors captured 2026-07-18: `.playwright-mcp/render-settings.png`, `render-web-settings.png`, a11y snapshots `page-2026-07-19T05-25-*.yml`.

## Steps

1. Diff the four surfaces for url/branch/name fields on a static site (curl REST, GraphQL query, MCP tool output, dashboard).
2. Re-walk the Render static-site settings + sidebar against the new bex UI; list remaining deliberate drift (Previews, Git Credentials, section grouping) in the parity ledger row notes.
3. Update ADR018 rows + evidence pointers; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- `docs/cli-compatibility-checklist.md` (only if affected)

## Acceptance criteria

- [x] url/name/branch identical across REST/GraphQL/MCP/dashboard for a static site.
- [x] ADR018 static-site rows carry current evidence; deliberate drift is listed, not hidden.

## Out of scope

- New feature work discovered during the walk (file as inbox notes).
