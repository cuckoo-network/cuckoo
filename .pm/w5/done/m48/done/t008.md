---
id: w5/m48/t008
title: Simplify — /simplify over the changed code
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m48/t007]
---

## Objective

Run `/simplify` over the code this milestone changed (dashboard type-gating, new routes, URL derivation, branch setter) — reuse/simplification/efficiency, behavior-preserving.

## Context

- Likely candidates: the type-gating predicates (cron gating from w5/m11 + new static gating should share one helper, not fork), the extracted routes/headers editor components, and any duplicated URL-derivation logic between backend layers.

## Steps

1. `/simplify` on the milestone diff; apply accepted findings.
2. Dashboard + backend test suites stay green.

## Files

- Whatever the milestone diff touched.

## Acceptance criteria

- [x] `/simplify` run recorded; accepted findings applied; suites green.

## Run record (2026-07-18)

Four parallel reviewers (reuse / simplification / efficiency / altitude) over the m48 diff. **Applied:** (1) `AppSpec.EffectiveHosts` promoted to `lego/types` (the documented home of contract-level host policy, next to `PlatformSubdomain`) — the operator's two `effectiveHosts` mirrors and bex-api's `pendingPublicURL` now all delegate to it, collapsing three hand-copies of the precedence rule to one; (2) explicit-empty branch now resets to the default in the shared verb (the setter family's "empty clears" convention) instead of a client-side `|| "main"` hardcode; (3) `rootDirPrefix` helper in `features/services/lib/format.ts` deduplicates the prefix normalization; (4) shared `StaticEdgeRulePage` shell deduplicates the two edge-rule route files (guard/loading/card logic in one place); (5) `serviceNavGroups` flattened onto module-level item constants; (6) the dev stub's service fixtures now spread one `SERVICE_DEFAULTS` so a newly selected schema field lands once; (7) Logs route holds rendering while the type is unknown (`holdWhileLoading`) so a static site's direct `/logs` URL never burns an SSE connection it immediately tears down. **Skipped:** a `serviceCapabilities(type)` map (house style is per-concern predicates; revisit if a second type needs tab gating), a scalar-setter hook factory (`use-branch` follows the established five-hook convention), and interpolating the display default branch out of locale copy (git refs aren't translated; display keys stay). All suites re-ran green after the fixes (operator envtest included — the delegation touched `lego/operator`).

## Out of scope

- Bug hunting (that's /code-review, not this task).
