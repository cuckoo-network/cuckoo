---
id: w5/m76/t001
title: "Backend: verify + align PATCH source-swap semantics (repo↔image, no auto-deploy, ×3 surfaces)"
worker: worker5
status: todo
estimate: 45m
depends_on: []
---

## Objective

Confirm (and repair where needed) that the existing `PATCH /v1/services/{id}` source fields implement Render's Update-Source semantics end to end: setting `repo`/`branch` on an image-backed service switches it to repo-backed (clearing the image), setting `image` on a repo-backed service switches it to image-backed (clearing repo/branch), neither transition triggers a deploy, `autoDeploy` is untouched, and GraphQL's `updateService` (or equivalent) carries the same fields with the same semantics.

## Context

- `lego/backend/internal/apps/rest.go:63-67` — the PATCH body already has `Repo *string`, `Image *imageRef`, `Branch *string`; `internal/apps/service.go:3107,3139` handle `patch.Repo`. What is UNVERIFIED: the repo⇄image cross-transitions (does setting one clear the other, or can a service end up with both?), whether a source change enqueues a build (it must not — Render: "Changes will not be deployed automatically"), and owner-scoped clone-secret refresh on the NEXT deploy after a repo change (ADR078 §4 resolution should just work — verify).
- Render reference: `PATCH /v1/services/{id}` accepts `repo`, `branch`, `autoDeploy`, `image`; type immutable ([api-docs.render.com/reference/update-service](https://api-docs.render.com/reference/update-service), May-2026 Update-Source changelog).
- GraphQL: check `updateService`/patch mutation field coverage in `internal/apps/graphql.go`; MCP `update_service` (w1/073 folded per-field tools into patch-shaped tools).

## Steps

1. Write transition tests first (repo→image, image→repo, repo→other-repo, branch-only) asserting final spec shape + no build/deploy row created.
2. Fix any transition that leaves a hybrid spec or triggers a deploy.
3. Verify GraphQL/MCP patch parity for the three fields; align if drifted.

## Files

- `lego/backend/internal/apps/{service.go,rest.go,graphql.go,mcp.go}` + tests

## Acceptance criteria

- [ ] All four transitions produce a clean single-source spec, no implicit deploy, autoDeploy preserved.
- [ ] ×3 surfaces accept the same fields with the same semantics; suites green.

## Out of scope

- Any new verb; dashboard (t002).
