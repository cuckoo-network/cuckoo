# Deferred dev-5 agent-sessions Playwright QA re-run

Why: the 2026-08-22 QA pass mixed **env gaps** (OpenBao k8s auth → 502, sandbox quota, missing `VITE_AGENT_STREAM_URL`, workspace cookie) with **product** bugs; env was repaired manually — a clean re-run on the stabilized overlay is needed before closing m78.

Scope (~45m when `dev-5` agent overlay is up per `docs/runbooks/agent-sessions-local-dev.md`):

1. `bash /tmp/bex-rebuild/start-bex-api-dev5.sh` (or future `dev-env.sh 5 up --agents`).
2. Dashboard with `VITE_AGENT_STREAM_URL=http://localhost:18083`.
3. `qa-login.sh` + patch `bex.selectedWorkspaceId` cookie (or extend `qa-login.sh` to set it).
4. Re-run `.playwright-mcp/agent-sessions-qa.mjs`; archive screenshots + `qa-agents-report.json`.
5. Confirm m78 fixes if landed; file any new gaps as inbox notes.

Blocked on: stable bex-api process (foreground or scripted restart) and m78 for the three known UI bugs.
