---
id: w5/m38/t001
title: Swap the env ACL query/mutation to ipAllowListEntries + hook
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

The environments feature's GraphQL operations read `ipAllowListEntries {cidrBlock, description}` and write via the entries-typed mutation arg, replacing the `[String!]` form in the fragments/hook while keeping the wire behavior for entries without descriptions identical.

## Context

- `dashboard/src/features/environments/api/environments.graphql:63-75` (`SetEnvironmentACL($ipAllowList: [String!])`), `use-set-environment-acl.ts`, `use-environments.ts`.
- Backend already serves both forms (`lego/backend/internal/environments/graphql.go:46,189`); no backend change expected.
- Regen codegen types the feature's usual way (offline `TestDumpGraphQLSchema` pattern if needed).

## Steps

1. Update fragments + mutation to the entries form; regen types.
2. Update the hook's mapping (string[] ⇄ entries) at one seam.

## Files

- `dashboard/src/features/environments/api/environments.graphql`, `hooks/use-set-environment-acl.ts`, `hooks/use-environments.ts`

## Acceptance criteria

- [ ] Reads and writes flow through `ipAllowListEntries`; description-less entries round-trip unchanged.

## Out of scope

- UI rendering (t002).
