---
id: w5/m38/t002
title: Editor UI — per-rule description input + display, locales
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m38/t001]
---

## Objective

The environment settings dialog's rule editor gains a description field per rule (add + display + edit), visually and behaviorally matching the datastore access-control panel, with en/zh locale keys.

## Context

- Template: `dashboard/src/features/databases/components/access-control-panel.tsx:49-91`.
- Target: `dashboard/src/features/environments/components/environment-settings-dialog.tsx`; locales in `features/environments/locales/{en,zh}.ts`.

## Steps

1. Add the description input/display per rule row; keep deny-all empty-state copy intact.
2. Locale keys en + zh.
3. Component test: add/edit/remove with and without description.

## Files

- `dashboard/src/features/environments/components/environment-settings-dialog.tsx`, locales, `__tests__/`

## Acceptance criteria

- [ ] A rule's description is enterable, visible after save/reload, and optional.

## Out of scope

- Datastore panels (already correct).
