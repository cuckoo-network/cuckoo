# Logs-tab structured filter UI — gated on w3/m8

UI half of the parity ledger's "Request / HTTP logs + structured filters" row: filter controls on the service Logs tab (type, level, statusCode, method, path, instance — Render's filter vocabulary), with dropdown values fed by the label-values discovery `w3/m8` ships (`list_log_label_values` / labels surface). Before building, capture Render's live Logs filter UI via Playwright (→ `docs/render-artifacts/`) so controls/labels/placement are render.com-consistent, not guessed.

Promote once `w3/m8` lands (itself gated on w3/m5 closeout); likely milestone-sized (filter state + URL sync + label-value queries + capture comparison + tests).

Source: `/pm-brainstorm more for w5` 2026-07-12.
