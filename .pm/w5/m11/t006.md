---
id: w5/m11/t006
title: Closeout
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m11/t005]
---

## Objective

Close the milestone once its definition of done is met: Settings page is service-type-aware (cron jobs: no Custom Domains, no Idle timeout; shows Deploy/Schedule/Command); tests pass; parity ledger updated.

## Steps

1. Confirm DoD: cron Settings shows Deploy section, not Custom Domains/Idle timeout; web service Settings unchanged
2. Run `yarn lint && yarn typecheck` — green
3. Mark all tasks done, move to `w5/m11/done/`
4. Move `w5/m11/` to `w5/done/m11/`
5. Check `- [x] **m11**` in `w5/README.md`
6. Run `npx prettier@3.4.2 --write "**/*.md"`

## Acceptance criteria

- [ ] All t001–t005 done
- [ ] `yarn lint && yarn typecheck` green
- [ ] `w5/done/m11/` exists, `w5/m11/` does not
- [ ] `w5/README.md` shows `- [x] **m11**`
