---
id: w5/m16/t009
title: Closeout
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m16/t007, w5/m16/t008]
---

## Objective

Close the milestone: verify the definition of done actually holds, then move `w5/m16/` to `w5/done/m16/`.

## Context

- Per `.claude/commands/pm.md`: this task's completion **is** the move — set every remaining task's `status: done`, move each `tNNN.md` to `w5/m16/done/`, mark every row `— DONE` and `**Status:** done` in this milestone's `README.md`, move the whole `w5/m16/` directory to `w5/done/m16/`, and check `- [x]` in `w5/README.md`.

## Steps

1. Verify the DoD in this milestone's `README.md` is actually met.
2. Set every task's frontmatter `status: done`.
3. Move each `tNNN.md` to `w5/m16/done/`.
4. Update this `README.md`'s task table (`— DONE`) and `**Status:** done`.
5. Move `w5/m16/` → `w5/done/m16/`.
6. Check the `- [x]` box for m16 in `w5/README.md`.

## Files

- `.pm/w5/m16/**`
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD verified true, not assumed.
- [ ] Milestone moved to `w5/done/m16/` with no tombstone left at the old path.
