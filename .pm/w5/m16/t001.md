---
id: w5/m16/t001
title: Capture Render's live manual-scaling UI
worker: worker5
status: todo
estimate: 20m
depends_on: []
---

## Objective

Capture Render's live manual-scaling UI (instance-count stepper, save/confirm affordance) in `docs/render-artifacts/`, following the same live-capture pattern used for m7's instance-type picker.

## Context

- `docs/render-artifacts/` already holds prior live captures (e.g. workspace-lifecycle, team-members).
- Render's manual-scaling control lives on a service's Settings page.

## Steps

1. Navigate to a Render service's Settings page and screenshot/record the manual-scaling section (stepper, current count, save/confirm flow, any bounds messaging).
2. Note the exact interaction pattern: does it save immediately per click, or require a confirm step?
3. Save the capture to `docs/render-artifacts/` with a descriptive filename.

## Files

- `docs/render-artifacts/` (new capture file)

## Acceptance criteria

- [ ] A live capture of Render's manual-scaling UI exists in `docs/render-artifacts/`.

## Out of scope

- Implementing the component (t002).
