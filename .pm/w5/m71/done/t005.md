---
id: w5/m71/t005
title: Test coverage (closing)
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m71/t004]
---

## Objective

Ensure the milestone ships meaningful tests for the behavior it changed — beyond whatever t001/t002 already added, close any gap that would let this regress silently again.

## Context

- t001 adds the dashboard route assertion (panel receives `keyValue.id`).
- t002 adds backend regression tests (sources receive the resolved CR name).
- The silent-empty failure mode came from the panel swallowing errors; if coverage there is cheap (assert the unavailable/error branch), add it — otherwise file a follow-up inbox note.

## Acceptance criteria

- [ ] A future change reverting either identifier (dashboard) or the matcher source (backend) fails a test.
- [ ] `yarn test` and `go test ./...` green.
