---
id: w5/m71/t004
title: Simplify (closing)
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m71/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — `dashboard/src/routes/keyvalue.$keyValueId.tsx`, its route test, and `lego/backend/internal/metrics/datastore.go` (+ test). Reuse / simplification / efficiency only; behavior-preserving.

## Steps

1. Invoke the `simplify` skill scoped to the milestone's diff.
2. Apply warranted fixes; re-run the touched suites.

## Acceptance criteria

- [ ] `/simplify` run; findings applied or consciously rejected; suites still green.
