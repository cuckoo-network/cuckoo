---
id: w5/m71/t003
title: Render parity (closing)
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m71/t001, w5/m71/t002]
---

## Objective

Confirm the fix is consistent across every surface the datastore-metrics feature exposes: REST (`GET /v1/metrics/{disk,disk-capacity,kv-memory,kv-connections}`), GraphQL (`datastoreMetrics`), MCP (the datastore metric tool), and the dashboard UI.

## Context

- The datastore-metrics API is a bex extension — Render has no datastore-scoped metrics, so there is no render.com behavior to compare against; parity here = internal consistency across bex's surfaces.
- All three API surfaces funnel into `metrics.Service.DatastoreMetrics`, so t002's change lands everywhere at once — the check is that no adapter bypasses it.

## Steps

1. Verify REST + GraphQL + MCP each accept the `red-…` id and return the same series shape/error semantics after t002.
2. Verify the dashboard page renders series after t001.
3. Flag any drift as a follow-up inbox note rather than silently diverging.

## Files

- none (verification only)

## Acceptance criteria

- [ ] Same identifier contract and results on REST, GraphQL, MCP, UI.
