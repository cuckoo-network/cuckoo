---
id: w5/m66/t006
title: "Test coverage — offline mapping tests vs fixtures/acp-agent.mjs (extended); no live cluster"
worker: worker5
status: done
estimate: 50m
depends_on: [w5/m66/t004]
---

## Objective

Lock the new behavior with offline tests that need no cluster or model key: the driver's ACP `session/update` → typed-part mapping, run against the fixture ACP agent, plus the client rendering of the typed parts. Tests must assert real behavior and failure modes — not tautologies.

## Context

- `lego/agent-image/driver/fixtures/acp-agent.mjs` is a fake ACP agent that speaks the real JSON-RPC and emits real `session/update` variants; `test/driver.test.ts` already drives a headless turn against it. Extend the fixture to also emit `available_commands` (and any variant currently dropped) so the "no silent drop" guarantee is tested.
- The old test asserted `hub.history.some(p=>p.type==="data-acp")` and matched `"type":"plan"|"diff"|"terminal"` (driver.test.ts:178-184). Replace with assertions on the new typed parts.

## Steps

1. Extend `fixtures/acp-agent.mjs` with `available_commands` + a couple more realistic updates.
2. Rewrite the driver assertions: expect `data-acp-plan`/`data-acp-diff`/`data-acp-terminal` parts, real `tool-*` parts carrying the true title/kind, `text`/`reasoning` parts, and NO generic `data-acp` and NO `acp_provider_agent_dynamic_tool`. Assert every fixture variant produced at least one mapped part (no drop).
3. Keep/adjust the redaction, POST /turn, resume (`loadSession`), and timeout tests for the new code paths.
4. Client: replace tests that exercised `classifyAcpData`/`unwrapAcpTool` with tests rendering the typed parts to plan/diff/terminal/tool blocks.
5. Run the driver suite (`node --test`) + dashboard tests green.

## Files

- `lego/agent-image/driver/fixtures/acp-agent.mjs`, `test/driver.test.ts`
- `dashboard/src/features/agent-sessions/**` tests

## Acceptance criteria

- [ ] Driver tests assert the typed parts, real tool titles, absence of generic `data-acp` + the synthetic tool name, and no dropped fixture variant.
- [ ] Client tests assert typed parts render to the correct blocks with streaming-state handling.
- [ ] Both suites green offline; no tautological/snapshot-everything tests.

## Out of scope

- Live prod E2E with a real `claude-code-acp` + model key (documented follow-up, not a merge gate).
