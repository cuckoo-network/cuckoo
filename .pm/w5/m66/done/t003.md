---
id: w5/m66/t003
title: "Client: render typed parts directly; delete unwrapAcpTool + collapse classifyAcpData"
worker: worker5
status: done
estimate: 60m
depends_on: [w5/m66/t002]
---

## Objective

Now that the driver emits typed parts — real `tool-*` parts with true title/kind, and `data-acp-plan`/`data-acp-diff`/`data-acp-terminal` — delete the client-side reconstruction: remove `unwrapAcpTool` (no more synthetic single-tool collapse to reverse) and reduce `classifyAcpData` from a guessy `type ?? sessionUpdate` switch to a thin typed switch over the explicit `data-acp-*` part types (or inline rendering per type). The rendered transcript must look identical to before (plan checklist, diff/terminal/tool activity groups).

## Context

- `dashboard/src/features/agent-sessions/lib/acp-parts.ts`: `classifyAcpData` (switch on `record.type ?? record.sessionUpdate`), `unwrapAcpTool` (reverses the provider's `{toolCallId,toolName,args}` envelope + `isTrivialAck`), `isToolPart`/`toolPartInfo`, `acpPartData` (returns `part.data` iff `type==="data-acp"`), `AgentUIMessage`.
- `session-conversation-impl.tsx`: `useChat({dataPartSchemas:{acp: acpDataSchema}})` + `buildBlocks(parts)` folds parts → plan block / activity group (diff/terminal/command/tool) / text / reasoning.
- New part types from t002: register the `data-acp-plan|diff|terminal` schemas with `useChat` (replacing the single `acp` data schema), and render `tool-*` parts via the existing `isToolPart`/`toolPartInfo` path (now with real names, so `unwrapAcpTool` is unnecessary).
- Keep the m44/m45 visual treatment (Devin-style groups, PlanBlock, ActivityGroup) — this is plumbing, not a redesign.

## Steps

1. Update `useChat` data-part schema registration to the explicit `data-acp-plan|diff|terminal` (drop the catch-all `acp`).
2. Replace `classifyAcpData` with direct handling of the three typed data parts in `buildBlocks` (or a thin typed helper); delete the `sessionUpdate`/`command`/`unknown` reconstruction branches that only existed for the collapsed/log form.
3. Delete `unwrapAcpTool` + `isTrivialAck`; render `tool-*` parts directly via `toolPartInfo` with the real name/kind.
4. Verify in `yarn dev:local` + the agent-stream loop that plan/diff/terminal/tool render identically, including streaming states.

## Files

- `dashboard/src/features/agent-sessions/lib/acp-parts.ts`
- `dashboard/src/features/agent-sessions/**` transcript renderer (`session-conversation-impl.tsx`, PlanBlock/ActivityGroup), transport wiring.

## Acceptance criteria

- [ ] `unwrapAcpTool`/`isTrivialAck` deleted; `tool-*` parts render with real title/kind.
- [ ] `classifyAcpData` removed or reduced to a thin typed switch over `data-acp-plan|diff|terminal`; the `sessionUpdate`/`command`/`unknown` guess branches are gone.
- [ ] `useChat` registers the explicit typed data-part schemas.
- [ ] A live/local session renders plan, diff, terminal, and tool parts with no visual regression.

## Out of scope

- Mobile deep refactor (consistency checked in t004; file a w11 follow-up if it hard-codes the old shape).
