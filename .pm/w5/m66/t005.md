---
id: w5/m66/t005
title: "Simplify — /simplify over the driver + client changes"
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m66/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (driver `acp.ts`/`session.ts`/`main.ts`, dashboard `acp-parts.ts` + renderers) for reuse / simplification / efficiency, behavior-preserving. The point of this milestone is deletion — make sure no vestigial abstraction (a leftover `data-acp` compat shim, a now-single-consumer helper, dead types) survives the swap.

## Context

- The change removes two reassembly layers (driver `rawUIMessagePart`, client `classifyAcpData`); after removal several helpers/types may have a single or zero callers.
- Keep behavior identical — no functional change, just remove indirection the harness layer made redundant.

## Steps

1. Run `/simplify` over the milestone's changed files.
2. Apply the behavior-preserving cleanups (dead code, redundant wrappers, over-general helpers now single-use).
3. Re-run the driver build + a local agent turn to confirm no behavior change.

## Files

- `lego/agent-image/driver/src/*`, `dashboard/` agent-session feature files touched in t002/t003.

## Acceptance criteria

- [ ] `/simplify` run; behavior-preserving cleanups applied.
- [ ] No vestigial `data-acp` shim / dead types / single-use wrappers left from the swap.
- [ ] Driver build + local turn still green.

## Out of scope

- Bug hunting (that's `/code-review`), and any new feature work.
