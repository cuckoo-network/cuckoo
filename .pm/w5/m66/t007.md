---
id: w5/m66/t007
title: "Closeout"
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m66/t006]
---

## Objective

Close the milestone once the definition of done is actually met — the driver drives `@agentclientprotocol/sdk` directly (no harness layer; t001 rejected it), the generic `data-acp` round-trip is deleted on both sides, byte-transparency and cross-consumer rendering are verified, tests are green. Do not run until that end state is real.

## Context

- Closeout _is_ the move: running `/pm done w5/m66/t007` last triggers the milestone directory move.
- Hard gate reminder: if t001 halted the milestone (ACP under-exposes typed parts through the harness layer), do NOT closeout as done — record the halt and leave the milestone open or convert to a findings note per the user's call.

## Steps

1. Confirm the DoD holds (driver swapped, `data-acp` deleted, byte-transparency intact, dashboard + mobile consistent, tests green).
2. Set every remaining task `status: done`, move each `tNNN.md` to `w5/m66/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
4. Move the whole `w5/m66/` directory to `w5/done/m66/` and check `- [x]` in `.pm/w5/README.md`.

## Files

- `.pm/w5/m66/*`, `.pm/w5/README.md`.

## Acceptance criteria

- [ ] DoD verified real (not merely code-written).
- [ ] Milestone moved to `w5/done/m66/`; workstream README box checked.

## Out of scope

- Any implementation work (all in t001–t006).
