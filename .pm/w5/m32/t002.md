---
id: w5/m32/t002
title: "Walk bex the same route; per-page diff table"
worker: worker5
status: todo
estimate: 60m
depends_on: [w5/m32/t001]
---

## Objective

Same inventory, same order, against bex's dashboard on the mock cluster (seeded: a git web service, a cron, a static site, a Postgres, a Key Value, a project+environment, a second member). Produce the per-page diff table: matches / missing control / divergent behavior / bex-extra — with a screenshot pair per divergence.

## Files

- `docs/render-artifacts/dashboard-walk/` (the diff tables)

## Acceptance criteria

- [ ] Every page pair has a verdict row; no page skipped silently (unreachable pages recorded as such).

## Out of scope

- Fixing anything found (t003 files notes).
