---
id: w5/m60/t005
title: "Live dev-5 proof of all three + parity evidence notes"
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m60/t002, w5/m60/t003, w5/m60/t004]
---

## Objective

Prove all three closures against a live stack — trigger a real cron run and watch it complete, round-trip a notify-on-fail override, rotate a real registry credential — and record the evidence the parity task (t006) will compare against Render.

## Context

- dev-5 stack: `bash .pm/w5/dev-5/up.sh` / `status.sh`; a cron-job service (create one if none exists — the create wizard supports `cron_job`) and a registry credential fixture are needed.
- The cron trigger's full loop: Trigger Run → operator creates the run (`spec.runAt`, ADR038) → status lands in history. The active-run rejection can be proven by triggering twice quickly on a long-running command.
- Captures to `.playwright-mcp/` with bare filenames.

## Steps

1. Bring up dev-5; ensure a cron-job service with a short command and a registry credential exist.
2. Walk each closure in the browser: trigger + observe run completion + double-trigger rejection; set/clear the notification override with reloads; edit + rotate the credential and verify the attached-service path still resolves.
3. Capture screenshots per closure; note anything that diverges from the pinned artifacts for t006.

## Files

- `.playwright-mcp/` (captures)

## Acceptance criteria

- [ ] All three closures verified live end to end, including the rejection/preserve-secret edge cases, with captures recorded.
- [ ] Divergence notes (if any) written down as input to t006.

## Out of scope

- Render-side comparison verdicts (t006).
