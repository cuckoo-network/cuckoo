# 028 — Live browser walk for w5/m58 Host/Path metrics filters (deferred proof)

**Filed:** 2026-07-30, during w5/m58 closeout. **Type:** deferred verification (not a code gap).

> **Update (w1/043, 2026-07-31): the substrate block is CLEARED.** The shared CAPD cluster was recreated (`scripts/mock-cluster.sh`, which now also installs the local-path default StorageClass + cert-manager and pins the apiserver-dependent controllers to the control-plane node), dev-5 was raised, and `status.sh`'s verification inventory is **ALL GREEN** (default StorageClass · CNPG ready · Loki ready · ingest→query round-trip · bex-api :54050). The Host-filter + store-unavailable walk below is now runnable locally; the Path/request half stays prod-only as noted.

> **Update (w5/m62, 2026-07-30):** block narrowed. dev-5 now installs a minimal Loki + `type=app` log-shipper and wires bex-api's `BEX_LOKI_URL` (`up.sh`), so the **store-unavailable→populated** transition and the **Host filter** (host discovers from App URLs, filtered series from Loki app logs) are provable on a healthy dev-5. The **Path/request-log** half needs the Traefik `type=request` pipeline (prod-scale; dev-5 ships only `type=app`) — that stays **prod-only residue**. Remaining block is the shared CAPD cluster's degradation (no default StorageClass, CNPG webhook crash), not dev-5's scripts — reprovision via `scripts/mock-cluster.sh`, then `up.sh` + green `status.sh` inventory, then walk the Host + store-unavailable states here and the Path/request series on prod read-only.

w5/m58 shipped Host/Path filters on the service Metrics Network card across REST/GraphQL/MCP/UI, fully verified by the CI-enforced automated gates (backend `go test ./...` incl. `TestHostPathFiltersRouteToLogStore`/`TestLokiRequestQueryFor`/`TestHostPathFilterCrossSurfaceParity`; dashboard typecheck + lint + 1,691 tests). The one DoD item that could **not** be exercised in-session is the **live browser walk** (t006): drive real multi-host/path traffic at a live service and watch the Network card's series change to the filtered subset, plus observe the store-unavailable state live.

**Why deferred:** the local `dev-5` stack could not be raised — the shared kind cluster is missing the CNPG `postgresql.cnpg.io/v1` CRDs the dev-5 Ory/DB stack depends on, and it lacks Loki (which the filtered-series path needs). No authenticated prod/Render browser session was available either.

**To close this note (post-ship, ~15m):** on the deployed dashboard (`dashboard.bex.co`, real Prometheus + Loki), open a Web Service's `/services/<id>/metrics`, then on the Network card:

1. Open the **Host** dropdown — confirm it lists the App's real hosts (from `logLabelValues`).
2. Pick a host — confirm Total Requests / Status codes / Response Times visibly change to that host's subset.
3. Type a **Path** and press Enter — confirm the same sections filter; clear it with the ×.
4. (Optional, on a Loki-less deployment) confirm the two sections show the explicit "Host and Path filters need the log store" state, not a silent unfiltered chart.

Capture screenshots to `.playwright-mcp/` and note the result in `docs/render-artifacts/metrics-page.md`'s "Live-proof status (t006)" paragraph, then move this note to `done/`.

## Progress (2026-08-08) — substrate cleared, metrics walk still pending

The substrate block is **cleared** and reproducible: the degraded local CAPD cluster was destroyed + recreated and the full dev-5 stack (bex-api, dashboard, Kratos, **Loki**, Mailpit) raised, with an authenticated dashboard session confirmed (see `.pm/w5/029.md` for the exact recreate recipe + screenshots). So the Host-filter + store-unavailable walk is now runnable locally.

The metrics **Host-filter walk itself was not performed** this session — it needs a deployed **Web Service** producing multi-host traffic + `type=app` Loki logs, and on the current resource-pressured host the operator's workload execution was the bottleneck (cron run pods were slow to complete; same leader-election instability documented in `029.md`, worked around by disabling `--leader-elect`). The Path/request half remains prod-only regardless (dev-5 ships only `type=app`, not the Traefik `type=request` pipeline). Next session (or a less-loaded host): deploy a Web Service, drive multi-host requests, then walk steps 1–4 above.
