---
id: w5/m56/t003
title: Shared range dropdown — add "Last 30 days" preset + a "Custom" start/end picker
worker: worker5
status: todo
estimate: 40m
depends_on: []
---

## Objective

Add a "Last 30 days" preset and a "Custom" start/end range to the shared range dropdown used by both Metrics and Logs (w5/m42 unified them), bounded by `BEX_MAX_QUERY_HOURS`. bex adds these ungated (Render plan-gates them).

## Context

- The shared range control lives in the metrics/logs shared toolbar (w5/m42 — "single range dropdown (Render presets, default 12h; shared with Logs)"). Find it under `dashboard/src/features/metrics/` or a shared `common` toolbar component.
- `BEX_MAX_QUERY_HOURS` (default 720h/30d) bounds log/metrics windows — a "Custom" range beyond it must be rejected honestly (the existing over-window path), not silently clamped.

## Steps

1. Add "Last 30 days" to the preset list.
2. Add a "Custom" option with a start/end date-time picker; validate against `BEX_MAX_QUERY_HOURS`.
3. Wire both into the existing query window state (URL-backed if the toolbar already is); tests for the new presets + custom bounds.

## Files

- the shared metrics/logs range-dropdown component (+ tests)

## Acceptance criteria

- [ ] "Last 30 days" + "Custom" appear on both Metrics and Logs; Custom respects `BEX_MAX_QUERY_HOURS`.
- [ ] Existing presets + URL-backed state unchanged; suite green.

## Out of scope

- Percentile "All" (t001/t002).
