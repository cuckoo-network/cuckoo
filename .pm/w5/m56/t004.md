---
id: w5/m56/t004
title: Render parity — cross-surface consistency check
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m56/t002, w5/m56/t003]
---

## Objective

Verify the new metrics controls against Render's live metrics page and its API: the percentile "All" overlay and the "Last 30 days" / "Custom" ranges match Render's captured behavior, and the multi-quantile field is consistent across REST/GraphQL/MCP. Flag any drift as follow-up notes.

## Context

- Reference: `docs/render-artifacts/metrics-page.md` + a fresh Render capture; the m42/m43 metrics rows in `docs/ADR018-render-parity.md`.

## Steps

1. Side-by-side dev-5 vs render.com walk of the percentile "All" overlay + range presets.
2. Confirm REST/GraphQL/MCP return the same multi-quantile shape.
3. Update ADR018 metrics-row evidence + retire the m42 "recorded drift" note now that these ship.

## Files

- `docs/ADR018-render-parity.md` (metrics rows — evidence + retire the drift note)

## Acceptance criteria

- [ ] Walk performed; ADR018 metrics drift note retired; any residual drift filed.

## Out of scope

- New feature gaps (file as inbox notes).
