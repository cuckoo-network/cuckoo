---
id: w5/m5/t003
title: Service-scoped tab chrome; fold the metrics route in as the Metrics tab
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m5/t002]
---

## Objective

Add a service-scoped tab nav (Overview / Metrics / Logs) mirroring Render's service-detail nav, and restructure routing so the existing metrics page renders as the Metrics tab while its old deep-link still resolves. Logs is a labeled placeholder tab until m6.

## Context

- TanStack Router file-based routes under `src/routes/`; existing `services.$serviceId.metrics.tsx`.
- A shared service-detail layout route (`services.$serviceId.tsx`) can host the tab nav + `<Outlet/>`.
- Tab set per t001 Render reference.

## Steps

1. Add a `services.$serviceId` layout route rendering the overview header + tab nav + `<Outlet/>`.
2. Nest overview (index) and metrics under it; keep `/services/$serviceId/metrics` resolving to the same metrics page.
3. Add a Logs tab that routes to a labeled "coming soon" placeholder (content in m6).
4. Localize tab labels (`services.*`).

## Files

- `src/routes/services.$serviceId.tsx` (new layout route)
- `src/routes/services.$serviceId.metrics.tsx` (nested under the layout)
- `src/routes/services.$serviceId.index.tsx`
- `src/features/services/components/` (tab nav component)
- `src/features/services/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] The service page shows an Overview / Metrics / Logs tab nav.
- [ ] The metrics page renders as the Metrics tab and the old `/services/$serviceId/metrics` deep-link still resolves.
- [ ] The Logs tab is a labeled placeholder, not a broken route.

## Out of scope

- Logs content (m6).
