---
id: w1/m5/t002
title: Verify build-from-git end-to-end (node pulls Zot)
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m5/t001]
---

## Objective

Prove the built image runs: node pulls it from Zot and the App becomes Ready.

## Steps

1. Create an `App{repo: ...}` → build Job runs → image in Zot.
2. Confirm the node pulls the image and the App reaches Running with a `status.url`.

## Acceptance criteria

- [ ] A build-from-git App runs from the in-cluster-built image with no manual `ctr` import.

## Out of scope

- Webhook auto-deploy (t003).
