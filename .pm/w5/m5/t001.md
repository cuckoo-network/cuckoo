---
id: w5/m5/t001
title: Capture Render's service-detail IA via Playwright as the design source
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Capture Render's service-detail page as the design reference for this milestone — the tab nav (Events/Metrics/Logs/Environment/Settings) and the overview panel layout — and record the mapping from Render's overview fields to bex-api's `server(id)` fields.

## Context

- Render dashboard service-detail page (log in via Playwright MCP; screenshots to `.playwright-mcp/`, bare filenames).
- bex-api `server(id)`/`Service` fields (`docs/bex-api.md`): `phase`, `serviceDetails.url`, `revision`, `replicas`, `createdAt`, string `suspended` enum, `dashboardUrl`.
- bex's tab subset: Overview / Metrics / Logs (no Environment/Settings yet).

## Steps

1. Navigate Render's service-detail page; screenshot the overview + tab nav to `.playwright-mcp/render-service-detail.png`.
2. Note the tab set, overview panel fields/layout, and header action placement.
3. Record the Render-field → `server(id)`-field mapping in the milestone README, flagging any Render overview field bex-api does not expose.

## Files

- `.pm/w5/m5/README.md` (design notes)
- `.playwright-mcp/render-service-detail.png` (gitignored)

## Acceptance criteria

- [ ] A Render service-detail screenshot exists in `.playwright-mcp/`.
- [ ] The README documents the overview field mapping and the chosen tab subset with gaps flagged.

## Out of scope

- Building the route (t002+).
