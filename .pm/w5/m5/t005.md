---
id: w5/m5/t005
title: Simplify
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m5/t003, w5/m5/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (overview route, tab chrome, header actions, routing restructure), behavior-preserving.

## Context

- Files touched by m5: `src/routes/services.$serviceId*.tsx`, `src/features/services/**`, `src/graphql/definitions.ts`.

## Steps

1. Run `/simplify` scoped to the m5 diff.
2. Apply behavior-preserving cleanups; re-run `yarn lint && yarn typecheck && yarn test`.

## Files

- Files changed in m5.

## Acceptance criteria

- [ ] `/simplify` applied; no behavior change; checks green.

## Out of scope

- New behavior.
