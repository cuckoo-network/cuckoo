---
id: w5/m75/t003
title: "Surfaces: claimGit GraphQL mutation + POST /v1/git/claim REST"
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m75/t002]
---

## Objective

Expose `StartClaim` on REST and GraphQL over the one core: `POST /v1/git/claim` and `claimGit(ownerId)` both returning `{claimUrl}`. Deliberately NOT on MCP — the claim is a browser OAuth ceremony an agent cannot complete (t005 records the reason).

## Context

- Mirror `POST /v1/git/connect` / `connectGit` shapes (`rest.go:37`, `graphql.go` mutations); both take the optional `ownerId`.
- New bounded `git_error` codes (`no_claimable_installation`, `ambiguous_installation`) must round-trip `writeCallbackFailure` for both redirect and JSON modes.

## Steps

1. REST route + GraphQL mutation registration.
2. Extend the callback failure-code mapping; keep codes bounded (no free text).

## Files

- `lego/backend/internal/github/{rest.go,graphql.go}`

## Acceptance criteria

- [ ] Both surfaces return the same `{claimUrl}` shape and identical error codes; `TestAuthzGuardsEveryVerb` covers `StartClaim`.
- [ ] `make lint-backend` green.

## Out of scope

- Dashboard consumption (t004); MCP (recorded exclusion).
