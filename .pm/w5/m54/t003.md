---
id: w5/m54/t003
title: "Dashboard: Source row edit affordance (change connected repo)"
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m54/t001]
---

## Objective

Make the Build card's Source row editable: an Edit affordance opens the repo picker (the create wizard's component, GitHub-App repo list) and switching repos PATCHes `repo` on the service behind an explicit rebuild-confirm dialog that spells out the consequence (next deploy builds from the new repo/branch). Free-text URL entry remains available for public `https://` repos, mirroring the create wizard.

## Context

- Repo picker: create-wizard components (w5/m15) + `use-repos.ts`.
- Backend: REST PATCH already accepts `repo` (explore report); confirm the GraphQL update path exists or add a narrow setter following w5/m30's precedent (splice codegen per-feature).
- Branch implications: switching repos may invalidate the current branch — after a repo switch, surface the Branch row (t002) for confirmation/reset.

## Steps

1. Add Edit to the Source row opening the picker dialog.
2. Wire the write path (existing update or narrow setter) + rebuild confirm.
3. Handle the branch-invalidated follow-up state; tests.

## Files

- `dashboard/src/features/services/components/build-deploy-section.tsx`
- create-wizard picker component reuse (`dashboard/src/features/services/`)
- `lego/backend/internal/api/` (only if a GraphQL setter is missing)
- tests under `__tests__/`

## Acceptance criteria

- [ ] Repo switch round-trips and triggers a rebuild from the new source after explicit confirm.
- [ ] Cancel leaves everything untouched; picker shows the App's repos + free-text URL path.

## Out of scope

- Migrating deploy history or webhooks beyond what the backend already does on repo change.
