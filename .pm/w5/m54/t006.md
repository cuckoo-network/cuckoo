---
id: w5/m54/t006
title: Test coverage — branch feed, fallback, and repo-switch tests
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m54/t004]
---

## Objective

Behavior tests: backend branch-list (connected, unconnected degrade, env-unset 503, pagination cap), dashboard combobox (filtering, selection saves after confirm, fallback to free text), and the source-switch flow (confirm gate, PATCH payload, cancel purity, branch-invalidation surfacing).

## Context

- Backend tests beside the github integration + GraphQL adapter; dashboard tests under `__tests__/`.

## Steps

1. Backend feed tests (mock GitHub API per existing integration-test precedent).
2. Dashboard interaction tests for both rows.

## Files

- `lego/backend/internal/` tests
- `dashboard/src/features/services/components/__tests__/`

## Acceptance criteria

- [ ] Tests fail on degrade-path or confirm-gate regressions; both suites green.
