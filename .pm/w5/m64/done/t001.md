---
id: w5/m64/t001
title: "Agent-sessions branch in DashboardSidebar: nav groups + contextual Recent section, one rail"
worker: worker5
status: done
estimate: 60m
depends_on: []
---

## Objective

Give `DashboardSidebar` a third contextual branch for `/agents*`, shaped like Devin's rail: the existing workspace nav groups on top, a `Recent` agent-sessions section below them, all inside the single `<Sidebar collapsible="icon">`. This is the structural half — t002 ports the affordances onto it.

## Context

- `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx:70-80` already route-switches inside the one rail:

  ```tsx
  const { projectId, serviceId } = useParams({ strict: false });
  if (projectId) return <ProjectSidebar projectId={projectId} />;
  if (serviceId) return <ServiceSidebar serviceId={serviceId} />;
  return (<Sidebar collapsible="icon">…<SidebarNavGroups groups={NAV_GROUPS} …/>…</Sidebar>);
  ```

  This task adds the agents case. Note the difference from the two existing branches: they **replace** the nav, the agents branch **augments** it (Devin keeps nav above the list) — so it extends the default return rather than returning a sibling component.

- `/agents` has no route param to switch on (`agents.tsx` is a flat route; the detail route is `agents_.$agentSessionId.tsx` with param `agentSessionId`). Switch on `pathname` — `useRouterState` is already read in this component — plus the `agentSessionId` param for the detail route. `nav-active.ts`'s `isNavItemActive` is the existing helper for path matching; reuse it rather than hand-rolling `startsWith`.
- Devin's measured geometry, for reference (not pixel-matching): rail 300px, nav rows 30px, contextual list rows 44px two-line, section header 32px with its own icon buttons, footer pinned. bex's rail is narrower; keep bex's existing `Sidebar` width and rhythm — consistency with our own rail beats copying Devin's numbers.
- Devin evidence: `.playwright-mcp/devin-home.png` (nav + Recent together in one rail), `devin-review.png` (same slot, different contents).

## Steps

1. Add a `SidebarGroup` component for the sessions section — `SidebarGroupLabel` = the `Recent` heading — under `dashboard-layout/`, or export it from `features/agent-sessions/` and consume it here (pick per the dashboard's feature-boundary convention: layout owns the rail, the feature owns the rows).
2. In `DashboardSidebar`, detect the agents context and render the default rail **plus** that group.
3. Feed it from `useAgentSessions` (`features/agent-sessions/hooks/use-agent-sessions.ts`), preserving today's polling behavior — `agents.tsx`'s list pane currently passes `{ poll: false }` because "the sidebar renders alongside and owns the poll"; keep exactly one poller after the move.
4. Render each session as a `SidebarMenuItem`/`SidebarMenuButton` linking to `/agents/$agentSessionId`, active when it matches the route param.
5. Leave `session-sidebar.tsx` in place and untouched for now — t003 removes it, so the tree stays green between tasks.

## Files

- `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx`
- new: `dashboard/src/common/components/dashboard-layout/agent-sessions-nav-section.tsx` (or the feature-owned equivalent)
- `dashboard/src/features/agent-sessions/hooks/use-agent-sessions.ts` (read-only unless the poll owner changes)

## Acceptance criteria

- [ ] On `/agents`, `DashboardSidebar` renders the workspace nav groups **and** a `Recent` sessions group in one `<Sidebar>`.
- [ ] On `/agents/<id>`, the same rail renders and the matching session row is marked active.
- [ ] On `/`, `/project/*`, `/services/*`, `/usage`, `/workspace/settings`, **no** sessions group renders (contextual, per Devin).
- [ ] Exactly one `useAgentSessions` poller is live on `/agents` (no double-polling with the page's list pane).
- [ ] Dashboard typecheck + lint + existing tests still pass (the old rail still renders; the duplicate is expected until t003).

## Out of scope

- Removing the old `<aside>` (t003) and the affordance port (t002).
- Any change to `ProjectSidebar`/`ServiceSidebar`.
