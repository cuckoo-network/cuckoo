---
id: w5/m64/t003
title: Delete the second rail from /agents + /agents_.$agentSessionId; keep the escape hatches
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m64/t001, w5/m64/t002]
---

## Objective

Remove `<SessionSidebar>` from both agent routes and delete the component, so exactly one rail remains. This is the task that actually fixes the reported defect.

## Context

- `dashboard/src/routes/agents.tsx` renders `<DashboardLayout>` with **no** `sidebar` prop (so `DashboardSidebar` renders) and then `<SessionSidebar activeId="" />` inside the body — the two-rail bug:

  ```tsx
  <DashboardLayout>
    <div className="flex min-h-0 flex-1">
      <SessionSidebar activeId="" />
      <div className="flex min-w-0 flex-1 flex-col">…</div>
    </div>
  </DashboardLayout>
  ```

- `dashboard/src/routes/agents_.$agentSessionId.tsx` does the same in **two** places — the detail view (line ~68, `activeId={agentSessionId}`) and a second branch at line ~195 (`activeId=""`, likely the not-found/error state). Both must go.
- `DashboardLayout` (`dashboard-layout/index.tsx`) accepts an optional `sidebar` prop that **replaces** `DashboardSidebar`. It is currently unused by every route — the project/service rails switch inside `DashboardSidebar` instead. Do **not** reach for the prop here: the agents rail augments the nav rather than replacing it, so t001's in-component branch is the right seam. Leaving the prop unused is fine; flag it to t007 (Simplify) as a possible dead parameter.
- Preserve: `?view=list` (`validateSearch` in `agents.tsx`) and the detail page's right-hand evidence/PR panel from w3/m44.

## Steps

1. Remove the `SessionSidebar` import + usage from `agents.tsx`; collapse the now-redundant flex wrapper.
2. Remove both usages from `agents_.$agentSessionId.tsx`, including the second branch — grep the file for `SessionSidebar` and confirm zero remain.
3. Delete `features/agent-sessions/components/session-sidebar.tsx`.
4. Repoint or retire `.../__tests__/session-sidebar.test.tsx` — its assertions become the new section's tests (t008 owns the coverage; do not simply delete the cases).
5. `grep -rn "SessionSidebar" dashboard/src` must return nothing.
6. Re-check the `SessionListPane` poll comment in `agents.tsx` ("the sidebar renders alongside and owns the poll") — it still holds after the move, but confirm the sentence names the right owner.

## Files

- `dashboard/src/routes/agents.tsx`
- `dashboard/src/routes/agents_.$agentSessionId.tsx`
- delete: `dashboard/src/features/agent-sessions/components/session-sidebar.tsx`
- `dashboard/src/features/agent-sessions/components/__tests__/session-sidebar.test.tsx`

## Acceptance criteria

- [ ] `grep -rn "SessionSidebar" dashboard/src` returns no results.
- [ ] `/agents` and `/agents/<id>` each render exactly one sidebar landmark.
- [ ] `/agents?view=list` still renders the standalone sessions table.
- [ ] The detail page's right-hand evidence/PR panel and the docked composer are unchanged.
- [ ] Dashboard typecheck + lint + full test suite green.

## Out of scope

- Collapsed/responsive behavior (t004).
- Removing `DashboardLayout`'s unused `sidebar` prop — hand to t007.
