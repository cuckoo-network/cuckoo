---
id: w5/m64/t007
title: Simplify
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m64/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

Specific candidates this milestone is expected to surface:

- **`DashboardLayout`'s `sidebar` prop** (`dashboard-layout/index.tsx`) — optional, and after t003 no route passes it (project/service/agents all branch inside `DashboardSidebar` instead). Likely dead; remove it and the `sidebar || <DashboardSidebar />` fallback if nothing consumes it.
- **The flex wrappers in `agents.tsx` / `agents_.$agentSessionId.tsx`** that existed only to sit the old `<aside>` beside the content — collapse them once the rail is gone.
- **`DashboardSidebar`'s branch chain** — three sequential `if`s plus an augmenting branch; check whether it reads better as one switch and whether the agents predicate belongs in `nav-active.ts` next to `isNavItemActive`.
- **Duplicate filtering/state** between the sidebar list and `SessionListPane` now that both read the same cache.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the findings; keep behavior identical.
3. Re-run typecheck + lint + tests.

## Files

- the milestone's touched files, plus `dashboard/src/common/components/dashboard-layout/index.tsx`

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly declined with a reason.
- [ ] The `sidebar` prop is removed or its remaining consumer named.
- [ ] Dashboard typecheck + lint + tests green; no behavior change.

## Out of scope

- Bug hunting (that is `/code-review`) and any refactor of `ProjectSidebar`/`ServiceSidebar`.
