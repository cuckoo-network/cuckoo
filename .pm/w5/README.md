# w5 — project workstream (worker5)

**Worker:** worker5. This is a general-purpose bex workstream. It may take work anywhere in the project; the milestones below are scheduled work and historical records, not a permanent purpose, specialty, or ownership boundary.

## Local dev environment

Develop against `.pm/w5/dev-5/`, this worker's own isolated stack on the shared local kind/CAPD cluster — never the shared cluster's default `auth`/`bex-system` namespaces or standard ports (5173/4433/4445/8090/8091/5432), which any other worker's session may also be using. `dev-5` gets its own Kratos + Hydra + Mailpit (namespace `dev-5-auth`) and app namespace (`dev-5`), reusing the shared cluster's CNPG operator and bex operator, plus a locally-built `bex-api` on dedicated ports derived from N=5 (`dev-5/ports.env`) so it never collides with any other workstream's `dev-N`.

- `bash scripts/dev-env.sh 5 up` — bring it up (idempotent — safe to re-run)
- `bash scripts/dev-env.sh 5 status` — health check (processes, pods, HTTP) + a pass/fail verification inventory
- `bash scripts/dev-env.sh 5 down` — tear it down (leaves the shared cluster and every other workstream's `dev-N` untouched)
- `bash scripts/dev-env.sh 5 clean` — reclaim `logs/` and `bin/` (refuses while the environment is up)

`up` prints the dashboard command to point at it once bex-api is running. One shared
implementation serves every workstream since `w1/m72`; `.pm/w5/dev-5/` keeps only
`ports.env` (a generated record of the derivation), this README, and `.gitignore`.

## Milestones

- [x] **m73** — Key Value metrics: dashboard sends typed id + bex-api matches on the resolved CR name (6 tasks) — **DONE 2026-08-18, moved to `done/m73/`**. Materialized 2026-08-17 as `m71/` in collision with agent-session persistence; renumbered at triage. `resource={keyValue.id}`; PromQL matchers from authorized CR `metadata.name`; `w5/045` error-swallow shipped as `w9/m86`. See `done/m73/`.
- [x] **m72** — Agent-session production recovery: stream grants, large-repo scrub, terminal convergence, and idempotent Git delivery (17 tasks) — **DONE 2026-08-18, moved to `done/m72/`** after correcting the reopened `bex-co/bex` final-push edge. Production run 32194179340 deployed the corrected digests; fresh session `ags-da2ege4qlbqc73e84d0g` advanced draft PR #58 twice (`3810e437…` → `8f4e03c4…`) with non-empty durable replay and no stale `failureReason`; live attach/reattach and cleanup also passed.
- [x] **m71** — Agent-session conversation persistence closure (10 tasks) — completed 2026-08-17. Durable per-turn prompt intents; `(session, turn, part_index)` transcript idempotency; partial-harvest merge; honest truncation/completeness; role-correct replay in the dashboard; no-prompt rehydrate without a phantom turn; concurrent-Steer/old-sandbox-binding closure; and refusal of non-durable live POST. See `done/m71/`.
- [x] **m70** — Billing page: rename Usage→Billing + remaining-credit display (8 tasks) — **DONE 2026-08-19, moved to `done/m70/`**. `/billing` is the real page (`/usage` query-preserving shim); read-only `credits` block on REST/GraphQL/MCP + dashboard. Live Stripe grant remaining $1000; prod `credit_read` success=8. ADR046 gate unchanged; grant management stays ADR071. See `done/m70/`.
- _(**m68** — intent-preload data prefetch — and **m69** — perceived-latency polish — from the same 2026-08-16 `/pm-brainstorm` trio as m67 were **relocated to `w9/m68` and `w9/m69`** at user direction 2026-08-16, joining the w9 perf batch m60–m63; of the three, only m67 was worked here.)_
- [x] **m67** — Route bundle code-splitting: split the heaviest route pages out of the client entry (7 tasks) — **DONE 2026-08-16**, moved to `done/m67/` ← from `/pm-brainstorm "还有什么地方可以优化的？"` 2026-08-16 (proposal 1). Measured that a route file's exported page component is **eagerly bundled into the client entry** (not code-split); dropped the 4 unimported exports (`AgentSessionsPage`/`DatabaseDetailPage`/`ServicePlanPage`/`WorkspaceSettingsPage`), splitting them into lazy route chunks and cutting the entry **1,312 → 1,176 kB gzip (−10.4 %)** with total JS unchanged and 2,116 tests green. Added `no-new-route-component-export.test.ts` (allowlist + fails on new, proven on a seeded violation). The premise wobble mid-implementation (production prints 0 warnings, so it briefly looked already-split) was resolved by entry-size measurement. The remaining 21 exported pages are **graph-dependent** (some already split despite their export) → systematic bundle-analyzer entry-attribution pass filed as `043`. Render parity **omitted** (build/bundle change, no surface).
- [ ] **m66** — Simplify the ACP call chain (direct `@agentclientprotocol/sdk` client + typed parts) (7 tasks) ← promoted from `w3/018` 2026-08-14. **t001 rejected the Vercel harness layer** (forces `ai@7` + sandbox-orchestration opposite to bex's in-pod driver). Pivot: drive `@agentclientprotocol/sdk` `ClientSideConnection` directly and map `session/update` → typed UI parts. **t001–t004 + t006 done; t005 `/simplify` (delete leftover `unwrapAcpTool`) + t007 closeout remain.** Live `claude-code-acp` E2E is a documented follow-up, not a merge gate. Touches ADR047 D3/D9.
- [x] **m65** — Agent sessions: retire the Evidence digest + make draft-PR delivery opt-in (9 tasks) ← from user directive 2026-08-09 (live walk of `ags-d9sle9558lbc738s7220`: the Evidence panel showed "0 commits" + misclassified grep noise as "Test output" on every session — the ADR047 D4 digest was a fire-and-forget-era artifact superseded by the ADR051 durable transcript). **DONE 2026-08-09, moved to `done/m65/`**: the session view is chat-only on web and mobile (evidence panel + header toggle + inline PrCard deleted, header `#N` badge kept), the PR body is session metadata only, and a draft PR is opened **only when asked for** (`agentConfig.openPr`, default off, identical on REST/GraphQL/MCP, persisted so it survives steer turns; unreadable config fails safe to opted-out). Wire `evidence` field deliberately retained for API consumers. `/simplify` caught five real defects — including that `scripts/agent-session-verify.sh` would have failed on every run (leg 1 asserted a PR on a now-default session; fixed + new leg 1b verifies the default), a seventh `AgentSessionView` fixture the sweep missed with no gate catching it (→ `041`), and a stale `steer_agent_session` MCP description promising an unconditional PR. Backend/dashboard/mobile suites + build + bundles all green; live `agent-session-verify.sh` run still pending (needs a real cluster + GitHub App)
- [x] **m63** — Encrypted platform backups + write-scoped backup credentials (ADR050) (14 tasks) ← from `docs/ADR050-encrypted-platform-backups.md`, handed off 2026-08-04 — **done 2026-08-07, moved to `done/m63/`**: Tier A `age` encryption + Tier B SSE shipped and verified live (etcd/OpenBao end-to-end, all three ObjectStores on `AES256`); write-only per-store credential rotation built (`scripts/backup-s3-credentials.sh`) with prod enablement deferred by operator decision → follow-ups `036`/`037`/`038`, pre-existing KV backup blocker → `039`. **No dashboard surface** — filed here at explicit user direction despite the mismatch with this workstream's theme (Render parity closing task omitted, see the milestone's Source + Goal linkage)
- [x] **m64** — One nav rail: fold the agent-sessions sidebar into `DashboardSidebar` (Devin-consistent) (9 tasks) ← from user directive 2026-08-07 ("`/agents` has two side bars, which is incorrect … we need to be consistent with devin for nav bar"), grounded in a live Playwright study of the user's Devin org the same day (`.playwright-mcp/devin-*.png`). Materialized as `w3/m78` and **moved here at user direction 2026-08-07**; the surface's prior milestones stay in w3 (`w3/m44`, `w3/m45`). Direct follow-on to **w3/m45 t004**, which built the sessions rail as a bespoke `<aside>` inside the page body — `/agents` is the only route that renders a second sidebar instead of branching inside `DashboardSidebar` like `/project/*` and `/services/*` already do. Adds a third, **augmenting** branch (global nav above a section-scoped `Recent` list, per Devin) and deletes the second rail. Pure dashboard layout — no backend surface. — done 2026-08-07, shipped `f486653d` + deployed green (run 31242352292), moved to `done/m64/`; live prod walk deferred to `040`
- [x] **m1** — Scaffold dashboard from beancount-dashboard, stripped to sample content (7 tasks) ← from user request 2026-07-05 — done 2026-07-06, moved to `done/m1/`
- [x] **m2** — Polish dashboard UI: beancount-style layout, remove Ory branding (7 tasks) ← from user request 2026-07-06 — done 2026-07-06, moved to `done/m2/`
- [x] **m3** — Internationalization (i18n), including Ory Elements (8 tasks) ← from user request 2026-07-06 — done 2026-07-06, moved to `done/m3/`
- [x] **m4** — Services list + lifecycle actions, Render-consistent, wired to bex-api (7 tasks) ← from `/pm-brainstorm` 2026-07-06 — done 2026-07-08, moved to `done/m4/`
- [x] **m5** — Service overview page + Render-style service IA (Overview / Logs) (6 tasks) ← from `/pm-brainstorm` 2026-07-06 — done 2026-07-06, moved to `done/m5/`
- [x] **m6** — Live logs page (Render-consistent: historical query + SSE live-tail) (6 tasks) ← from `/pm-brainstorm` 2026-07-06 — done 2026-07-08, moved to `done/m6/`
- [x] **m7** — Service Settings + instance-type picker (Render parity) (6 tasks) ← from user request 2026-07-08, Render settings/plan pages captured live; UI half of w1/m8's plan API — done 2026-07-08, moved to `done/m7/`
- [x] **m8** — Databases page (managed Postgres, Render-consistent) (8 tasks) ← promoted from `001` via `/pm-brainstorm` 2026-07-08 — done 2026-07-08, moved to `done/m8/`
- [x] **m9** — MCP `query_render_postgres` — read-only SQL for agents (5 tasks) ← from `/pm-brainstorm for w2` 2026-07-08, reassigned to w5 (original `w2/m6` label collided with w5's done m6) — done 2026-07-08, moved to `done/m9/`
- [x] **m10** — Trustworthy dev stub (multi-service) + custom-domain DNS instructions end to end (9 tasks) ← from user report 2026-07-09 (phantom service in `local-bex`) + promoted `006` — done 2026-07-09, moved to `done/m10/`
- [x] **m11** — Type-aware service settings (cron jobs: hide Custom Domains + Idle timeout; show Schedule + Command) (6 tasks) ← from Playwright comparison 2026-07-09 — done 2026-07-09, moved to `done/m11/`
- [x] **m12** — Key Value dashboard (create / list / detail, Render-consistent) (9 tasks) ← from user parity report 2026-07-09 (dashboard.render.com/new/redis); UI half of `w2/m7`, on the w1/m14 mechanism — done 2026-07-09, moved to `done/m12/`
- [x] **m13** — Build & Deploy settings section (Root Directory) (8 tasks) ← from `/pm-brainstorm for w1` 2026-07-09 (Root Directory topic); UI half of `w1/m18`, gated on it — done 2026-07-10, moved to `done/m13/`
- [x] **m14** — Delete service: dashboard danger-zone action (8 tasks) ← from `/pm-brainstorm new milestones` 2026-07-09, unblocked — w2/m4 done 2026-07-09; done 2026-07-11, moved to `done/m14/`
- [x] **m15** — New-service create wizard (source picker · repo picker · deploy) (9 tasks) ← from `/pm-brainstorm for w2` 2026-07-11 (Render `/web/new` parity); done 2026-07-11, moved to `done/m15/`
- [x] **m16** — Manual-scaling section in service Settings (9 tasks) ← promoted from `004` 2026-07-11, unblocked by `w2/m12` (backend scale API shipped 2026-07-08) — done 2026-07-12, moved to `done/m16/`
- [x] **m17** — Create wizard: all service types (static site · cron job · background worker · private service) (8 tasks; DONE 2026-07-12 — type picker + per-type fields shipped, 14 new tests pass, Static-site UI ◐→✅ in parity ledger) ← from `/pm-brainstorm more for w5` 2026-07-12; pure UI, backend `createService` already accepts every field
- [x] **m18** — Cron-job Settings write path (Schedule + Command editable) (7 tasks) ← from `/pm-brainstorm more for w5` 2026-07-12; retires the w5/m11 read-only deferral, includes the missing GraphQL/MCP half (w5/m9 precedent) — done 2026-07-12, moved to `done/m18/`
- [x] **m19** — Fix dashboard SSR authentication: forward the real Kratos session cookie (8 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13 (live bug: `factory.server.ts` sends a Bearer token from a cookie nothing ever sets) — done 2026-07-13, moved to `done/m19/`
- [x] **m20** — Create wizard: env vars at create (+ GraphQL `createService(envVars:)`) (8 tasks) ← from `/pm-brainstorm more for w5` 2026-07-12; closes the create-surface asymmetry (REST/MCP accept `envVars`, GraphQL now does too); `EnvVarInput` moved to `gqlutil` to avoid schema collision — done 2026-07-13, moved to `done/m20/`
- [x] **m21** — Events tab + deploy list UI (Rollback/Cancel + Manual Deploy) (9 tasks) ← promoted from `007` 2026-07-12; gates w3/m7 + w2/m10 + w2/006 all materialized in session — done 2026-07-13, moved to `done/m21/`
- [x] **m22** — Health Check Path in service Settings (9 tasks) ← promoted from `009` 2026-07-12; gate w1/m23/t001 materialized in session — done 2026-07-13, moved to `done/m22/`
- [x] **m23** — Logs-tab structured filter UI (10 tasks) ← promoted from `008` 2026-07-13 (unblocked — `w3/m8` landed 2026-07-12) — done 2026-07-13 (implemented directly, not task-by-task): level/method/statusCode/instance dropdowns fed by `logLabelValues` (static fallbacks), request-path input, request-line method/status chips, live-tail gating, store-unavailable (503) state; Request/HTTP-logs UI ◐→✅ in ADR018. Moved to `done/m23/`
- [x] **m24** — Key Value follow-ups: `maxmemoryPolicy`/`persistenceMode` create fields (KeyValue CR + operator Valkey reconcile + REST/GraphQL/MCP + create form, Free-plan-locked) **and** Key Value metrics — memory + connected-clients **folded into the w3/m10 `DatastoreMetricsPanel`/`datastoreMetrics` framework** (redis_exporter sidecar → Prometheus `valkey-instances` scrape → `kv_memory`/`kv_connections` on the shared `DatastoreMetrics` verb), not a parallel panel ← implemented directly from inbox `011` 2026-07-13 (note in `done/011.md`); repairs the ADR018 "filed as a w5/m12 follow-up" claim
- [x] **m25** — Environments dashboard UX (9 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13 (`docs/ADR018-render-parity.md` "Projects & environments" row, UI ◐: "Environments has no dashboard UX yet"); backend fully shipped `w1/m32` — done 2026-07-13: `features/environments/` panel on the project page (create/rename/delete + Manage-services with auto-join), ADR018 UI ◐→✅. Moved to `done/m25/`
- [x] **m26** — Workspace-level Env Groups page (10 tasks) — **DONE 2026-07-14** ← from `/pm-brainstorm more tasks for w5 to achieve feature parity` 2026-07-13 (`docs/ADR018-render-parity.md` "Environment groups" row, UI service-scoped only — no workspace-level Env Groups page); backend `internal/envgroups` fully shipped
- [x] **m27** — ~~Blueprints dashboard surface~~ **stale entry, fixed 2026-07-13**: this scope shipped as `w7/m27` (done 2026-07-13, list/detail/manifest/validate/sync, live-verified) — a concurrent `/pm-brainstorm` session materialized the identical proposal under `w7` before this README line was ever promoted to a real `w5/m27/` directory. No `w5/m27/` directory ever existed; nothing to move. See `w7/README.md`'s `m27` entry for the actual work.
- [x] **m28** — Dashboard SQL console for managed Postgres (8 tasks) ← from `/pm-brainstorm more` 2026-07-13 (second pass; `docs/ADR018-render-parity.md` "Read-only SQL query" row, UI cell) — done 2026-07-14, moved to `done/m28/`
- [x] **m29** — Deploy detail page: build log + status timeline (9 tasks; DONE 2026-07-14 — exact deploy links, truthful row/event timeline, Git `type=build` pane + store-unavailable state, shared Cancel/Rollback, full eleven-status UI vocabulary; real mock-cluster deploy click-through passed; backend commit/lifecycle depth filed as `w2/m37`) ← from `/pm-brainstorm more milestones for each worker` 2026-07-14 (Render's per-deploy page; w5/m21 shipped the list only); build-log pane consumes `w7/m28`
- [x] **m30** — Dockerfile path + start command dashboard controls (9 tasks) ← promotes `w6/015.md` (filed by `w6/m21`'s parity check) via `/pm-brainstorm` round 5 + `$pm`, 2026-07-14; closes missing detail projection/narrow GraphQL setters plus the UI gaps (`dockerfilePath` absent, `startCommand` create-only) — **DONE**
- [x] **m31** — Environments UX round 2: ACLs, membership, confirmations, create-selectors (9 tasks) ← from `/pm-brainstorm` rounds 8–9, 2026-07-15; folds `014`; done 2026-07-15 — ACL + membership editors, exact protected delete/suspend/Blueprint-sync retries, and shared datastore create selectors shipped and passed the mock-cluster browser DoD; moved to `done/m31/`
- [x] **m32** — Dashboard parity walk: page-by-page against Render (6 tasks) — **done 2026-07-15** ([done/m32](done/m32/README.md)): authenticated live Render + mock-cluster bex walk across all shipped page families; reproducible three-family artifact; six bounded w5 notes plus cross-layer `w3/m28`; ADR018 service-root evidence corrected and static route/header UI ◐→✅
- [x] **m33** — Env Groups create completeness: initial contents + metadata (9 tasks) — **DONE 2026-07-15** ← from `/pm-brainstorm` round 12, 2026-07-15 (ADR018:62 — Render's POST accepts initial `envVars`/`secretFiles`/`serviceIds`, bex creates empty groups; dashboard omits `ownerId`/timestamps); one POST/one dialog mints a populated, linked group
- [x] **m34** — Lift aal2 step-up detection into the session fetch (6 tasks) ← from `/pm-brainstorm` round 13, 2026-07-15; closed as a duplicate residual because the exact scope already shipped and was verified in `w4/m17` (commit `8bc5ca43`)
- [x] **m35** — Dashboard dead-ends: Postgres parameter-overrides editor + workspace resource caps (6 tasks) — **DONE 2026-07-15** ← from `/pm-brainstorm` round 14, 2026-07-15 (capability-diff miner: `use-database-insights.ts:61-96` exports a `saveParameters` mutation hook with zero consumers while `insights-panel.tsx:214-244` renders read-only; `workspaceLimits {used, limit}` exists only in the generated schema — no operation, no component)
- [x] **m36** — Dashboard-walk follow-through: close the round-14 gap list (10 tasks) ← from `/pm-brainstorm` round 15, 2026-07-15; absorbs notes `015`–`020` (the six page-level affordances the m32 walk proved Render has and bex lacks — service root → Deploys, Logs time range, Environment Export, Metrics event timeline, Team search, Project resource search/filters), bundled per the w8/m16 note-absorption precedent — done 2026-07-15, moved to `done/m36/`
- [x] **m37** — Registry-credential attach: complete the private-image deploy flow (8 tasks) ← from `/pm-brainstorm` round 16, 2026-07-15; done 2026-07-15 — shared create/settings picker, explicit clear, detail readback, tests, and live dev-5 GraphQL/browser proof; moved to `done/m37/`
- [x] **m38** — Environment IP rules UI: description-carrying entries (6 tasks) ← from `/pm-brainstorm` round 21, 2026-07-15; done 2026-07-16 — entry-shaped GraphQL read/write, editable optional descriptions, legacy fallback, en/zh UI, parity evidence, 1,279 dashboard tests, and live dev-5 reload round-trip; moved to `done/m38/`
- [x] **m39** — Render dashboard-route compatibility: `/web/srv-…`-shaped deep links + Render-shaped `dashboardUrl` (9 tasks) ← from user request 2026-07-16 (research: Render CLI `pkg/dashboard/dashboard.go` segment map — bex ids already Render-prefixed, only path segments differ) — done 2026-07-16: 7 redirect-alias routes (`/web|worker|pserv|static|cron/$` → `/services`, `/d/$` → `/databases`, `/r/$` → `/keyvalue`, sub-paths + query preserved, live-verified 307s), bex-api `dashboardUrl` now type-aware Render-shaped (`resourcemeta.ServiceDashboardRoute`), evidence in `docs/render-artifacts/dashboard-routes.md`, CLI-checklist row added; ADR018 had no stale row to fix. Moved to `done/m39/`
- [x] **m40** — Deploys parity polish: searchable rich history + actionable diagnostics (8 tasks) — **DONE 2026-07-16** ([done/m40](done/m40/README.md); [parity evidence](../../docs/render-artifacts/deploy-detail-page.md)) ← searchable/honest history, historical-only rollback, shared detail metadata, truthful storeless diagnostics, and separate service/latest-deploy header facts; failed + live/deactivated dev-5 paths and corrected Render Web Service re-walk passed; full dashboard suite 216 files / 1,270 tests
- [x] **m41** — Populated project overview parity (10 tasks) — **DONE 2026-07-17** ([done/m41](done/m41/README.md)) ← one URL-selected Environment, authoritative Runtime/Region/Updated facts, contextual create, mixed-kind bulk Move, canonical stable memberships, and reload-safe Unassigned access; clean-room production browser + GraphQL/REST/MCP proof passed and all fixtures were removed
- [x] **m42** — Metrics page simplification: Render's card-level controls + Render-shaped title (9 tasks) ← user request 2026-07-17, live authenticated capture of both metrics pages: single range dropdown (Render presets, default 12h; shared with Logs, which keeps its 1h default), default-hidden event timeline, Percentage/Total + Limit links on the Application card, Status Code + per-section Percentile (p90) on the Network card, `<name> · <type> · bex dashboard` titles; Host/Path filters + percentile "All" + Custom/30d ranges recorded as drift, observability-drain banner excluded per DO_NOT_DO — done 2026-07-17 (dev-5 live walk + 1,363 tests; [docs/render-artifacts/metrics-page.md](../../docs/render-artifacts/metrics-page.md)), moved to [done/m42](done/m42/README.md)
- [x] **m43** — Chart event markers on the service Metrics tab (Render parity) (6 tasks) — **DONE 2026-07-17** ([done/m43](done/m43/README.md)) ← from user request 2026-07-17, implemented directly from a live Render capture: every metrics chart overlays in-window service events as a vertical dashed line + kind-styled icon badge (started = neutral dashed, live = green, failed = red, lifecycle = dot), overlap-clustered with count badges, hover tooltip (label + time), badges linking to the deploy detail page; `chart-events` lib + `chart-event-markers` overlay wired into both chart kinds via the existing `serviceEvents` feed, en/zh locales, unit tests, `local-bex` stub extended (synthetic metric waveforms + wire-vocabulary events) for the browser-verified end-to-end proof.
- [x] **m44** — Service Environment page parity: staged editor, bulk import/export, rollout choices, secret-file upload, and linked-group UX (12 tasks) — **DONE 2026-07-17** ([done/m44](done/m44/README.md); [parity evidence](../../docs/render-artifacts/service-environment-page.md)) ← from user request 2026-07-17; live Render capture, production-shaped desktop/mobile browser proof, and full backend/operator/dashboard gates
- [x] **m45** — Cross-resource post-create landing contract (8 tasks) — **DONE 2026-07-17** ([done/m45](done/m45/README.md); [landing evidence](../../docs/render-artifacts/post-create-landings.md)) ← promoted from `025` after the user expanded the request from Postgres to every dashboard resource-creation flow; exhaustive standalone/contextual/mint-once inventory, immutable-id landings, desktop+narrow Chrome proof, and 1,526 dashboard tests on the rebased tree
- [x] **m46** — Automated PTY verification of official Render CLI `pgcli` (7 tasks) ← from user request 2026-07-18; unmodified Render CLI v2.21.0 + real pgcli 4.5.0 passed id/name SQL probes on non-production dev-5, with bounded PTY, redaction, cleanup, and regression proof — done 2026-07-18, moved to `done/m46/`
- [x] **m47** — Cron (and per-type) create discoverability: deep-linkable wizard + first-class New entries (7 tasks) — **DONE 2026-07-18** ([done/m47](done/m47/README.md)) ← from user request 2026-07-18 ("I don't see cron in the dashboard"); root cause was the create-entry flow, not a missing feature. Fixed: wizard preselects its type from `?type=` (single-source `SERVICE_TYPES` + validated search schema), `render-alias` maps every `/{web,worker,pserv,static,cron}/new` to `/services/new?type=…`, and both "New" menus surface a shared per-type service submenu (Cron Job first-class). Create-side follow-on to m39's detail deep-links; typecheck + lint + 1591 dashboard tests green (live browser proof blocked by a squatting :8099 process)
- [x] **m48** — Static-site parity: type-gated IA + always-on site URL (10 tasks) ← from user research request 2026-07-18 (live Playwright comparison of bex vs Render static-site settings/sidebar; evidence in `.playwright-mcp/`) — done 2026-07-18, moved to `done/m48/`: static sidebar drops Logs/Shell/Scaling/Plan and gains dedicated Redirects/Rewrites + Headers pages (direct URLs redirect); header drops Instances + Connect and shows the URL; Settings hides Instance Type, prefills the name (never the id), and carries root-dir prefixes; Metrics drops the pod CPU/mem card; platform URL now derives from creation via `AppSpec.EffectiveHosts` (promoted to `lego/types`, all three host-rule copies collapsed) across REST/GraphQL/MCP/UI (live dev-5 proof); Branch editable on all surfaces (GraphQL `setBranch` + MCP `set_branch`; empty resets to main in the shared verb); PR Previews excluded (DO_NOT_DO)
- [x] **m49** — Official Render CLI service CRUD: nested image ownership + deletion convergence (9 tasks) — **DONE 2026-07-21 UTC** ([done/m49](done/m49/README.md)) ← unmodified CLI v2.21.0 passed production image CRUD and immediate first-build deletion; the original and final acceptance fixtures reached GET 404/list absence with zero cluster/Zot residue
- [x] **m50** — Settings edit-in-place parity: always-rendered disabled inputs (Render pattern) (8 tasks) ← from user request 2026-07-26; live Render vs bex settings walk 2026-07-26/27 (Render's Name-field edit interaction verified live; capture `.playwright-mcp/render-settings-full.png`) — done 2026-07-27, moved to `done/m50/`
- [x] **m51** — Build Command for all native-runtime services + root-dir command prefix (6 tasks) ← same 2026-07-26 walk ("the build command is missing"); pure-UI gap — backend `setBuildCommand` already full-surface (w7/m41); sequence after m50/t001 — done 2026-07-27, moved to `done/m51/`
- [x] **m52** — Settings IA alignment with Render's section layout (8 tasks) ← same walk (Render heading order captured via DOM query); General rename + read-only Region row + Build/Deploy split + subdomain fold-in + bottom Suspend/Delete; sequence after m50–m51 — done 2026-07-27, moved to `done/m52/`
- [x] **m53** — Auto-Deploy trigger parity: `autoDeployTrigger` select across REST/GraphQL/MCP/UI (8 tasks) ← same walk (Render renders a disabled "On Commit" select, API carries `off|commit|checksPass`); mapped onto existing `spec.autoDeploy` boolean, `checksPass` a documented divergence; UI half consumes m50/t001's select variant — done 2026-07-27, moved to `done/m53/`
- [x] **m54** — Source + Branch pickers (GitHub-App-backed) (7 tasks) ← same walk, lower priority; retires the w5/m48 branch free-text + read-only Source conscious divergences via the existing GitHub App/repo-picker infrastructure — done 2026-07-27, moved to `done/m54/`
- [x] **m55** — Datastore detail-page edit-in-place consistency (Postgres + Key Value) (8 tasks) ← from `/pm-brainstorm` 2026-07-27 ("还有哪些 inconsistency 值得做的？"); brings the Databases + Key Value detail pages' single-value fields onto the w5/m50 `EditableFieldRow` disabled-input+pencil pattern, retiring the pre-m50 always-editable Input+Save rows — pure-UI over shipped setter verbs, Render-consistent. Live Postgres walk confirmed Render's Name pencil + read-only Version+upgrade-link; Name folded into the General/Details card lead — done 2026-07-27, moved to `done/m55/`
- [x] **m56** — Metrics percentile "All" overlay + Custom/Last-30-days ranges (7 tasks) ← from same `/pm-brainstorm` 2026-07-27; closes the two metrics-page drifts w5/m42 recorded — a multi-quantile backend read (REST/GraphQL/MCP) feeding a percentile "All" overlay, plus "Last 30 days"/"Custom" range presets shared with Logs (bex ungated). Live Render metrics walk confirmed the percentile/range controls; one `MetricsWithQuantiles` core across REST/GraphQL/MCP — done 2026-07-27, moved to `done/m56/`
- [x] **m57** — Static-site page Render-parity polish: canonical `/static/<id>` URL, Events-first IA (no Deploys tab), no Region row (7 tasks) ← from user request 2026-07-28 (live Render vs bex static-site walk, `.playwright-mcp/`); gap-closing follow-on to m48 — the "Redirects/Rewrites" rename was already shipped; Previews/PR Previews stay excluded (DO_NOT_DO #27). Live walk confirmed parity + fixed one drift (static header "Free" plan chip → 404); evidence in `docs/render-artifacts/static-site-page-walk.md` — done 2026-07-30, moved to `done/m57/`
- [x] **m58** — Metrics Network card Host/Path filters (10 tasks) — **DONE 2026-07-30**, moved to `done/m58/` ← from `/pm-brainstorm for w5` 2026-07-30 (proposal 1); closes the last w5/m42 recorded drift (Host/Path network filters — the metrics API rejected them, w3/m12), sibling of m56's percentile/range closure. Both filters served from the request-log store (Loki) — the one backend with a per-request host/path axis (Prometheus has none, even with `addRoutersLabels`): backend Loki-aggregated read (`RequestLogMetrics` source, LogQL `rate`/`quantile_over_time`) routed from the metrics core, REST/GraphQL/MCP surface, Network-card Host dropdown (logs `logLabelValues` discovery) + free-text Path input, store-gated `ErrLogStoreUnavailable` honest state, bandwidth+host/path named 400. Drift retired in `metrics-page.md`, ADR018:150 corrected. Verified by CI gates (backend 40 pkgs + golangci-lint; dashboard typecheck+lint+1,691 tests); live browser walk infra-blocked in-session (dev-5 unraisable — missing CNPG CRDs + no Loki), deferred to post-ship prod as open note `028`
- [x] **m59** — Workspace-family parity walk: Team · Env Groups · Usage/Billing · Notifications · Webhooks · Blueprints (7 tasks) — **DONE 2026-07-30 (code-based audit; live captures deferred)**, moved to `done/m59/` ← from `/pm-brainstorm for w5` 2026-07-30. Live Render + bex captures were infra-blocked (no Render session; dev-5 unraisable; no prod session), so delivered as a **code-based parity audit** cross-referenced against the pinned Render artifacts + ADR018: **verified parity across all six families** (all ship a first-class page, every relevant ADR018 row ✅, active maintenance since m32, no missing pages / unconsumed capabilities / stale cells). Evidence: `docs/render-artifacts/workspace-family-walk.md` + ADR018 walk-record. The deferred live Render-side drift check is open note `031`
- [x] **m60** — Dashboard dead-ends round 2: cron Trigger Run · notify-on-fail override · registry-credential edit (9 tasks) — **DONE 2026-07-30**, moved to `done/m60/` ← from `/pm-brainstorm more for w5` round 2, 2026-07-30. Shipped cron **Trigger Run** (confirm + refetch + inline `ForbidConcurrent` rejection, `runCronJob`) and **run detail** on expandable history rows (`cronJobRun`), plus registry-credential **edit/rotate** (`updateRegistryCredential` + `registryCredential` readback prefill; host read-only; blank token keeps the secret). The `setNotifyOnFail` "dead-end" was a **false positive** — the per-service override is already wired via the authoritative `setNotificationsToSend` (4-state); the legacy 3-state `setNotifyOnFail` stays intentionally unconsumed (it clears the richer field). GraphQL types hand-spliced (no live codegen session). Verified by dashboard typecheck + lint + 1,705 tests; live browser walk infra-blocked in-session, deferred as open note `029`
- [x] **m61** — Datastore Logs panels (Postgres + Key Value) (8 tasks) — **DONE 2026-07-30 (ALREADY-SATISFIED, no code)**, moved to `done/m61/` ← from same round (proposal 3). The t001 design probe found the goal already met: both datastore pages already ship mounted, feature-complete Logs tabs (`PostgresLogViewer` @ `databases.$databaseId.tsx:142`, `KeyValueLogViewer` @ `keyvalue.$keyValueId.tsx:127`) with range/instance/text filters, `LogLineList` rows, and the store-unavailable honest state — meeting+exceeding the DoD. They source from the shared `Logs` operation; the backend `logs` verb routes `dpg-`/`red-` ids to `queryDatabaseLogs`/`queryKeyValueLogs` (the same code the dedicated fields call), so `databaseLogs`/`keyValueLogs` stay intentionally unconsumed (redundant). Same stale-miner class as m60/t003. Minor range-preset consistency polish filed as open note `030`
- [x] **m62** — Repair dev-5 to verification-complete + burn down deferred walks (8 tasks) — **DONE 2026-07-30 (script repairs landed + validated; live walks blocked by a deeper shared-cluster degradation)**, moved to `done/m62/` ← from `/pm-brainstorm more for w5` round 3, 2026-07-30 (proposal 1). Shipped: `up.sh` self-installs CNPG (0.29.0, no platform nodeSelector — CRD-establish validated) + deploys minimal Loki + a `type=app` Alloy shipper (`values/{loki,log-shipper}.values.yaml`) + wires `BEX_LOKI_URL`; a **fail-fast preflight** now checks tools/reachability/**default-StorageClass** (the true root cause — the cluster came up storage-less, not just CNPG-less); `status.sh` gained a **verification inventory** that asserts (exits non-zero) StorageClass/CNPG-ready/Loki-ready/Loki ingest→query/bex-api-reachable — demonstrated real by turning red on the broken cluster and the StorageClass check flipping green when fixed. The live walks (notes 028/029/031) stay open but **precisely characterized**: blocked by the shared CAPD `bex` cluster's degradation (no default StorageClass, CNPG-1.30 webhook `:9443` crash on kind, node scheduling at max) — a `scripts/mock-cluster.sh` reprovision, not a dev-5 script bug; the repaired scripts run the walks automatically on a healthy cluster

## Inbox

Open after 2026-08-18 triage (done / delete / keep / promote). No new milestone was created: remaining notes are either sub-hour verification, explicit watch-items, or a recorded non-goal.

- **`028.md`** (keep) — deferred live browser walk for w5/m58 Host/Path metrics filters. CI-verified; substrate cleared 2026-07-31; Host-filter walk itself still pending (needs a Web Service with multi-host traffic). Path/request half is prod-only (`type=request`).
- **`029.md`** (keep) — deferred live walk for w5/m60. Parts 1–2 (cron Trigger Run + run detail) verified live on dev-5 2026-08-08. Remaining: notify-on-fail override + registry-credential edit (~10m).
- **`031.md`** (keep) — remaining live Render-side drift checks for w5/m59. Environment Groups closed by **`w2/m73` (done 2026-08-17)**. Still open: Team · Usage/Billing · Notifications · Webhooks · Blueprints (~75m when both dashboards are authenticated).
- **`036.md`** (keep — watch item) — ADR050 follow-up: Tier B customer-key encryption for Barman-backed Postgres. Gated on upstream `plugin-barman-cloud` SSE-C or an encrypting S3 proxy; do not promote until one of those exists.
- **`037.md`** (keep) — ADR050 follow-up: Object Lock / WORM on `bex-tfstate` backup prefixes. Premature until write-only per-store credentials are enabled in prod (built, operator-deferred).
- **`038.md`** (keep) — ADR050 follow-up: `age` backup-key rotation runbook + restore `_PREVIOUS` key. Procedure is already specified in ADR050 §1; no rotation is scheduled. Inbox-sized until someone actually rotates.
- **`042.md`** (keep — decision record) — mobile composer has no draft-PR opt-in after m65. Deliberate per ADR048 (configuration stays desktop). Re-open only if mobile session-create becomes common or `openPr` stops being configuration.

- _(`039.md` **DONE 2026-08-18**, moved to `done/`) — KeyValue backup NetworkPolicy live-green on prod: `red-d9p49kdrtmes73c34ovg-backup-egress` since 2026-08-08; `kvbak-red-…` CronJob `lastSuccessfulTime=2026-08-19T03:36:15Z`; Jobs succeeded 2026-08-17/18/19 after failing through 2026-08-08._
- _(`040.md` **DONE 2026-08-18**, moved to `done/`) — m64 one-rail `/agents` local walk + deployed digest accepted; no remaining code gap._
- _(`041.md` **promoted → `w9/m85`**, moved to `done/`) — dashboard typecheck covers test files._
- _(`043.md` **absorbed into `w9/m60`** 2026-08-16 — the initial-JS attribution pass joined the w9 perf batch: m60 t005/t009 carry the analyzer + attribution + graph-pinned route-page scope, and m60 t001–t004 land the additionally-attributed entry dead weight (lucide barrel, i18n descriptions, dual-locale, react-intl). Note moved to `done/`.)_
- _(`044.md` **promoted → `w5/m73`**, moved to `done/`) — Key Value metrics typed-id fix. Originally numbered m71; collision-repaired at triage._
- _(`045.md` **promoted → `w9/m86`**, moved to `done/`) — honest error states for datastore metric charts._
- _(`035.md` promoted 2026-08-02 → **`w1/m64`** (relocated to w1 by user direction; briefly materialized here as `w5/m63`) — the ADR047 D9 design pass fixed the architecture (one `UIMessage[]` render layer, two swappable data sources), so the phase-1 increment materialized under the note's own head-start clause (w3/m41 implementation-complete, live E2E pending); the phase-2 live-stream half remains with `w3/009`. Note moved to `done/`.)_
- _(`030.md` **DONE 2026-07-30**, moved to `done/`) — the datastore Logs viewers (Postgres + Key Value) now use the shared `RangeSelect` (the m56 range ladder + Custom picker) via a shared `datastore-log-range` helper (window-based hooks), replacing the private 1h/6h/24h enum; unused range-preset locale keys removed._
- _(`032.md` **DONE 2026-07-30**, moved to `done/`) — dumped the backend schema → offline `yarn codegen` → the regenerated `definitions.ts` was **byte-identical** to the committed file: the m60 hand-splices (cron/registry-credential) are validated with **zero drift** (m58 added no new types). The two-command offline recipe is now recorded in `dashboard/CLAUDE.md` § Offline codegen._
- _(`033.md` **DONE 2026-07-30**, moved to `done/`) — the existing `locale-parity.test.ts` covered only 4 hardcoded namespaces; rewrote it to **auto-discover all 24** locale pairs via `import.meta.glob` (key parity + consistent-prefix + non-empty message, with an intentional-gap allowlist). zh-lags-en drift in any feature now fails CI; all 24 currently in parity (73 assertions)._
- _(`034.md` **DONE 2026-07-30**, moved to `done/`) — added a `production build` step (`yarn build`) to `dashboard-test.yml` after typecheck/lint/test, so an SSR/production-build break fails PR CI instead of surfacing post-merge in `deploy.yml`'s image build. Verified the build passes locally before adding the gate._

_(`026.md` implemented 2026-07-18 in `f415ac81` — the shared authenticated layout now has contextual Project / Environment / Service breadcrumbs and switchers, lazy `Cmd/Ctrl+K` resource search, and persistent create/help/account actions; reviewed and reverified on the rebased tree 2026-07-19 with lint, 261 test files / 1,613 tests, and the production build green; moved to `done/`.)_

_(`025.md` promoted to **m45** 2026-07-17 after the scope expanded from the Postgres redirect defect to an exhaustive post-create landing contract; note moved to `done/`.)_

_(`024.md` implemented 2026-07-17 — all destructive type-to-confirm gates unified on Render's sudo-command dialog pattern (shared `SudoCommandField`, bold phrase as body copy, "Sudo Command" input label; service delete now `sudo delete <type words> <name>`); completed as bounded single-session work and recorded directly in `done/`.)_

_(`023.md` implemented 2026-07-17 — compact clickable instance slugs and an exact, removable instance filter on deploy-detail log rows; completed as sub-hour work and moved to `done/`.)_

_(`021.md` implemented 2026-07-17 — compact clickable instance slugs on service log rows; completed as sub-hour work and moved to `done/`.)_

_(`022.md` implemented 2026-07-17 — typed `srv-…` metrics requests now use the resolved Kubernetes App name for pod, cAdvisor, and Traefik lookups while preserving the requested public identity; regression coverage spans CPU, memory, limits, instances, request metrics, filters, REST/GraphQL/MCP, and legacy label-less Apps; moved to `done/`.)_

_(`015.md`–`020.md` absorbed into **m36** 2026-07-15; notes moved to `done/`)_

_(`014.md` folded into **m31** (t005) 2026-07-15; note moved to `done/`)_

_(`013.md` absorbed into completed `w10/m4` 2026-07-15; note moved to `done/`)_

_(`012.md` applied 2026-07-13 — flipped the 5 stale UI cells in `docs/ADR018-render-parity.md` [Service events, Trigger a deploy, List/get deploy objects, Cancel deploy, Rollback] from `✖` to `✅` pointing at `w5/m21`, plus the two gap-backlog rows [`Deploy cancel + rollback`, `Service events feed`]; also closed two additionally-stale gap-backlog rows found in the same pass [`Health-check path → readiness probe`, `Env groups + secret files`, both already shipped], moved to `done/012.md`)_

> `001.md` promoted to m8; `002.md` retired as superseded by open `w4/m8` (API keys in the dashboard) — both moved to `done/` 2026-07-08. `005.md` (ADR008-vision.md non-goal fix) done alongside m8 — moved to `done/` 2026-07-08. `003.md` promoted to `w1/m11.5` (custom-domains dashboard) on 2026-07-09 — moved to `done/`. `006.md` (post-add DNS/CNAME instructions) shipped in m10 — moved to `done/` 2026-07-09. `004.md` promoted to **m16** (manual-scaling settings) 2026-07-11 — moved to `done/`. `007.md` promoted to **m21** (Events tab dashboard) 2026-07-13 — moved to `done/`. `009.md` promoted to `w2/m13` (health check path full-surface parity) 2026-07-12 — moved to `done/`; dashboard Settings form shipped as **m22** 2026-07-13. `008.md` promoted to **m23** 2026-07-13 — moved to `done/`; **m23** implemented directly + done 2026-07-13, moved to `done/m23/`. `010.md` (cron Auto Deploy toggle) implemented directly 2026-07-13 (sub-hour, no milestone per DO_NOT_DO) — moved to `done/`: `services.$serviceId.settings.tsx` renders Build & Deploy alongside the cron Deploy section for a git-sourced cron. `011.md` (Key Value follow-ups) shipped as **m24** 2026-07-13 — moved to `done/`.
