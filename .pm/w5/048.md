# Agent session sandbox-create failures: surface `failureReason` in dashboard

Why: QA session `ags-da51u99jg4reds94nrv0` showed **Failed / sandbox create failed** with an **empty** `failureReason` in the UI while bex-api logged the real cause (`sandbox did not become ready … plan limit`). Users cannot remediate without logs.

Likely touch (~30m):

- Verify `failureReason` is populated on the API view when dispatch fails (`lego/backend/internal/agentsessions/` dispatch path).
- If populated: dashboard detail/list mappers drop or fail to render it — fix `dashboard/src/features/agent-sessions/lib/mapper.ts` + failed-session callout.
- If empty at source: map OpenSandbox capacity/timeout errors into `failureReason` at fail time.

Acceptance: failed sandbox-create sessions show the capacity-limit message (or a generic actionable fallback), not a blank failure card.

Promote to a milestone if backend + dashboard + ×3-surface parity is needed.
