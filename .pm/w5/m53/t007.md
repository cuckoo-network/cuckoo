---
id: w5/m53/t007
title: Test coverage — mapping matrix + UI select tests
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m53/t005]
---

## Objective

Tests that pin the contract: REST emit-consistency (`autoDeploy` vs `autoDeployTrigger` never disagree), PATCH mapping matrix (trigger only / boolean only / both with precedence / `checksPass` rejection shape), GraphQL/MCP mirroring, and the dashboard select's render + save flow per repo-backed type.

## Context

- Backend: table-driven tests beside the apps adapter (`cd lego/backend && go test ./...`).
- Dashboard: `__tests__/` next to the section component.

## Steps

1. Backend matrix tests (REST + GraphQL + MCP).
2. Dashboard select behavior tests.

## Files

- `lego/backend/internal/api/` tests
- `dashboard/src/features/services/components/__tests__/`

## Acceptance criteria

- [ ] Tests fail on any emit inconsistency, precedence break, or rejection-shape change; both suites green.
