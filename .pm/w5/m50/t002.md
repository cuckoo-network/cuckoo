---
id: w5/m50/t002
title: Migrate General + Maintenance rows (Service Name, Max Shutdown Delay, custom page URL)
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m50/t001]
---

## Objective

Replace the bespoke plain-text-plus-Edit rows in the General card and the Maintenance Mode card with the shared editable-field row: Service Name (display name), Max Shutdown Delay, and the custom maintenance-page URL. Behavior (validation, fallback-to-name semantics for display name, seconds bounds for shutdown delay, URL validation for the maintenance page) must be unchanged.

## Context

- `dashboard/src/features/services/components/display-name-row.tsx` — current text-until-edit pattern; keeps the "prefill with name, never the id" rule from w5/m48/t004.
- `dashboard/src/features/services/components/max-shutdown-delay-row.tsx` — 1–300s bounds, default 30.
- `dashboard/src/features/services/components/maintenance-mode-section.tsx` — the custom URL currently renders as an always-enabled textbox with a Save button; align it to disabled-until-edit. The maintenance on/off switch stays a switch (Render uses a switch too).
- Hooks stay as-is: `use-display-name.ts`, `use-max-shutdown-delay.ts`, `use-maintenance-mode.ts`.

## Steps

1. Swap each row's markup for `EditableFieldRow`, passing existing save handlers.
2. Preserve the display-name dirty rule (empty draft with no current display name is not a change).
3. Update the row components' unit tests to the new interaction.

## Files

- `dashboard/src/features/services/components/display-name-row.tsx`
- `dashboard/src/features/services/components/max-shutdown-delay-row.tsx`
- `dashboard/src/features/services/components/maintenance-mode-section.tsx`
- matching files under `__tests__/`

## Acceptance criteria

- [ ] All three fields render as disabled inputs with pencil buttons; edit flow matches t001's pattern.
- [ ] Save semantics identical to today (values land via the same GraphQL mutations; no new operations).
- [ ] Existing tests updated, not deleted; suite green.

## Out of scope

- Idle-timeout row (read-only text today — unchanged).
- Instance Type row (link to /plan, matches Render — unchanged).
