---
id: w5/m36/t005
title: "Team table: accessible member search"
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Live Render's Team page includes member search; bex's accepted-member table and role actions are complete, but a larger workspace cannot filter the list. Add accessible client-side search over the loaded member identity fields, preserving the existing role/action behavior.

## Context

- Absorbed from `w5/019` (dashboard walk — Team, 2026-07-15; `render-walk-workspace-team.png` vs `bex-walk-workspace-team-and-audit.png`).

## Steps

1. Add a search input filtering the loaded members by name/email (client-side).
2. Distinct empty vs no-match states; keyboard accessibility.
3. Tests: filtering without hiding pending/error state.

## Files

- dashboard/src/features/ (workspace Team components)
- affected tests

## Acceptance criteria

- [ ] Name/email search is keyboard accessible.
- [ ] Empty and no-match states are distinct; role actions unaffected.
- [ ] Tests cover filtering without masking pending/error states.

## Out of scope

- Server-side member search/pagination (member counts don't warrant it yet).
