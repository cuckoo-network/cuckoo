---
id: w5/m36/t006
title: "Project resources: URL-owned search + type filters"
worker: worker5
status: todo
estimate: 45m
depends_on: []
---

## Objective

Live Render's project/environment resource table has search plus All/Services/Env Groups filters; bex's project page supports grouping and resource management but becomes scan-only as the resource count grows. Add URL-owned search and type filters to the project/environment resource view, using the resource categories the page actually supports.

## Context

- Absorbed from `w5/020` (dashboard walk — Projects, 2026-07-15; `render-walk-projects.png` vs `bex-walk-project.png`).
- Do not invent unsupported resource families — filter over what the page renders today.

## Steps

1. Add URL-owned search + All/Services/Env Groups type filters, scoped to the selected environment.
2. Accessible no-match states.
3. Tests: compose search+filter, survive refresh, environment scoping.

## Files

- dashboard/src/features/projects/ (project detail resource view)
- affected tests

## Acceptance criteria

- [ ] Search and type filters compose and survive refresh via URL state.
- [ ] Filtering stays scoped to the selected environment; no-match states accessible.
- [ ] Tests cover composition and persistence.

## Out of scope

- New resource categories; workspace-global resource search.
