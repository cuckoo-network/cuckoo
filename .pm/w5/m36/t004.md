---
id: w5/m36/t004
title: "Metrics page: event-timeline overlay"
worker: worker5
status: todo
estimate: 45m
depends_on: []
---

## Objective

Live Render overlays a filterable event timeline above application metrics so a deploy or lifecycle change can be correlated with chart movement. bex has Metrics and Events separately but no correlation strip on Metrics. Add the service-event timeline to Metrics using the existing events query and time window.

## Context

- Absorbed from `w5/018` (dashboard walk — Metrics, 2026-07-15; `render-walk-service-metrics.png` vs `bex-walk-service-metrics.png`).
- Request-metric host/path functionality stays out — owned by `w3/m18`.
- Do not duplicate event-fetch logic; reuse the events query from the Events tab.

## Steps

1. Render events within the selected metrics range as a timeline strip on the Metrics page.
2. Offer the equivalent event filtering Render shows.
3. Tests: range/filter propagation, empty states.

## Files

- dashboard/src/features/metrics/ (panel components)
- dashboard/src/features/events/ (shared query reuse)
- affected tests

## Acceptance criteria

- [ ] Metrics displays events within the selected range with filtering.
- [ ] Tests cover range/filter propagation and empty states without duplicating event-fetch logic.

## Out of scope

- Backend host/path metrics filters (`w3/m18`); new event types.
