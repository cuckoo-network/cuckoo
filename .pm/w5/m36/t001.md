---
id: w5/m36/t001
title: Service root lands on Deploys, not Events
worker: worker5
status: todo
estimate: 15m
depends_on: []
---

## Objective

Live Render opens a service at its deploy history; Events is a separate tab. bex currently redirects the service root to Events even though its Deploys page is already complete. Change the default service-detail redirect to Deploys, preserving direct `/events` links.

## Context

- Absorbed from `w5/015` (dashboard walk — Service root, 2026-07-15; `render-walk-service-overview.png` vs `bex-walk-service-overview.png`).
- The stale source comment on the redirect should be updated alongside.

## Steps

1. Change the service-root redirect target from Events to Deploys.
2. Update route tests and the stale comment.

## Files

- dashboard/src/routes/ (service-detail index route)
- affected route tests

## Acceptance criteria

- [ ] Opening `/services/<id>` lands on Deploys.
- [ ] Events remains directly reachable; redirect/navigation tests pin both routes.

## Out of scope

- Any change to the Deploys or Events page content.
