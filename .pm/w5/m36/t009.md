---
id: w5/m36/t009
title: Test coverage
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m36/t007]
---

## Objective

Add meaningful tests for the behavior this milestone shipped, closing gaps between the per-task tests: URL round-trips (set → refresh → restored) for t002/t006, the masked-secret rule for t003 under a degraded secrets backend, and the redirect pin for t001. Tests must assert real behavior and failure modes.

## Context

- `yarn test` from `dashboard/` is the suite; follow the existing feature-test patterns.

## Steps

1. Review t001–t006's tests for interaction gaps; add missing cases.
2. `yarn test` green.

## Files

- dashboard/src/features/**/__tests__/

## Acceptance criteria

- [ ] URL round-trip and masked-secret failure modes covered.
- [ ] `yarn test` green.

## Out of scope

- Playwright E2E (a mock-cluster click-through belongs to closeout verification, not the unit suite).
