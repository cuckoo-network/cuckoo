---
id: w5/m36/t010
title: Closeout
worker: worker5
status: todo
estimate: 15m
depends_on: [w5/m36/t009]
---

## Objective

When the other tasks are complete **and the definition of done is actually met**, close the milestone: set remaining tasks `status: done`, move each `tNNN.md` to `w5/m36/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone README, move the whole `w5/m36/` directory to `w5/done/m36/`, and check `- [x]` in `w5/README.md`. Notes `w5/015`–`020` were moved to `w5/done/` at materialization — confirm they're there.

## Context

- Do not run until the DoD holds — all six pages observably match their captures.

## Steps

1. Verify the DoD end state (drive the real dashboard).
2. `/pm done w5/m36/t010` (triggers the milestone move).

## Files

- .pm/w5/m36/ → .pm/w5/done/m36/
- .pm/w5/README.md

## Acceptance criteria

- [ ] DoD verified in a real browser; milestone moved whole; status synced in all three places.

## Out of scope

- New work discovered at closeout — file as inbox notes.
