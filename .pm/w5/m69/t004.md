---
id: w5/m69/t004
title: Render parity
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m69/t003]
---

## Objective

Check the dashboard's list-loading UX against render.com's and keep it consistent, flagging any drift as follow-up rather than silently diverging.

## Context

- This milestone changes visible UI loading states (skeletons vs spinner) — the parity check is **UI-only**; no REST/GraphQL/MCP surface is touched.
- Render's dashboard uses content skeletons for its list/detail loads; compare shape/behavior against the pinned Render artifacts (`docs/render-artifacts/`) or a live capture if available.

## Steps

1. Compare the new list-route skeletons against Render's equivalent loading UX (shape, placement, no full-page blank).
2. Confirm dashboard consistency across the list routes (one skeleton idiom, not N).
3. Record parity/drift in the milestone README; file follow-up notes for any intentional divergence.

## Files

- `dashboard/` (UI only); `docs/render-artifacts/` (reference)

## Acceptance criteria

- [ ] List-loading UX compared to Render and judged consistent (or drift filed).
- [ ] Skeleton idiom is uniform across the covered routes.

## Out of scope

- Backend surface parity (none touched).
