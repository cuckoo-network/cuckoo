---
id: w5/m51/t005
title: Test coverage — gating + prefix behavior tests
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m51/t003]
---

## Objective

Behavior tests for the milestone's ship: Build Command row present for native repo-backed types and absent for docker/image-backed; save path calls `setBuildCommand` with the draft; prefix renders from rootDir and never leaks into saved values.

## Context

- `dashboard/src/features/services/components/__tests__/build-deploy-section.test.tsx` and static-site tests.

## Steps

1. Per-type gating matrix test (web native / web docker / cron native / image-backed / static).
2. Prefix rendering + save-value purity tests.

## Files

- `dashboard/src/features/services/components/__tests__/`

## Acceptance criteria

- [ ] Tests fail if gating or prefix behavior regress; full suite green.
