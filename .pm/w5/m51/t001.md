---
id: w5/m51/t001
title: Un-gate the Build Command row for all native-runtime repo-backed services
worker: worker5
status: todo
estimate: 30m
depends_on: []
---

## Objective

Render the Build Command editable row for every repo-backed service whose runtime is native (non-docker) — web, private, background worker, and cron — not just static sites. Docker-runtime services keep showing Dockerfile Path instead (Render's behavior). Wire the row to the existing `useBuildCommand` hook / `setBuildCommand` mutation; empty value clears the command (runtime default build).

## Context

- Gap: `dashboard/src/features/services/components/build-deploy-section.tsx` omits Build Command for non-static types; `static-site-section.tsx` has it (w7/m41).
- Backend fully supports all types: `spec.buildCommand` in `lego/types/v1alpha1/app_types.go`, GraphQL `setBuildCommand`, REST PATCH, MCP `set_build_command`.
- Hook exists: `dashboard/src/features/services/hooks/use-build-command.ts`.
- Render reference (live 2026-07-27): Build section shows "Build Command — Render runs this command to build your app before each deploy" for a Node web service.
- Use m50's `EditableFieldRow` if landed; otherwise follow the current row pattern and let m50/t003 migrate it.

## Steps

1. Add the Build Command row to `build-deploy-section.tsx`, gated on repo-backed + native runtime (reuse the existing docker/native gating that picks Start vs Docker Command labels).
2. Confirm edit triggers the rebuild-confirm dialog like Start Command does.
3. en/zh locale strings (reuse the static-site ones if keyed generically).

## Files

- `dashboard/src/features/services/components/build-deploy-section.tsx`
- `dashboard/src/features/services/components/__tests__/build-deploy-section.test.tsx`

## Acceptance criteria

- [ ] Node web service on dev-5 shows Build Command; saving persists via `setBuildCommand` and redeploys per existing semantics.
- [ ] Docker-runtime service shows Dockerfile Path and no Build Command row.
- [ ] Image-backed (non-repo) services show neither.

## Out of scope

- Prompt prefix (t002); backend changes (none needed).
