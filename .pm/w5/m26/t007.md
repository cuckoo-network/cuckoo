---
id: w5/m26/t007
title: Render parity — full-surface check
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m26/t006]
---

## Objective

Confirm the new workspace-level Env Groups UI is consistent with the env-group contract already shipped on REST/GraphQL/MCP (`lego/backend/internal/envgroups`) and with Render's own Env Groups page, flagging any drift as follow-up rather than silently diverging.

## Context

- Backend contract + divergences: `docs/ADR018-render-parity.md` "Environment groups" row ("reads are keys/names-only; a group-var change rolls every linked service").
- bex-api is meant to be Render-compatible (`docs/ADR006-bex-api.md`); the UI must not invent fields or semantics the API doesn't have.

## Steps

1. Diff the UI's fields/actions against the GraphQL/MCP/REST env-group surface (create/delete/setVars/set+deleteSecretFile/link/unlink) — same names, same reveal-per-value semantics, same "rolls linked services" behavior surfaced to the user.
2. Compare page IA against Render's Env Groups page (workspace-level list → group detail → link services).
3. Note any drift in `docs/ADR018-render-parity.md` (flip the Environment-groups UI note from "service-scoped only" to reflect the new page) and file follow-ups for anything deferred.

## Files

- `docs/ADR018-render-parity.md` (update the Environment-groups row's UI note)

## Acceptance criteria

- [ ] UI fields/semantics match the REST/GraphQL/MCP contract; any divergence is documented, not silent.
- [ ] The parity ledger's Environment-groups row reflects the shipped workspace-level page.

## Out of scope

- New backend fields (this is a UI-over-existing-contract milestone).
