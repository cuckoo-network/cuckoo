---
id: w5/m26/t002
title: Env Groups list page + route + sidebar nav
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m26/t001]
---

## Objective

Add a workspace-level Env Groups list page at `/env-groups`, reachable from the sidebar, showing every env group with its var/file and linked-service counts, an empty state, and a "New Env Group" action.

## Context

- Route pattern mirrors the flat file-based routes in `dashboard/src/routes/` (e.g. `keyvalue.new.tsx`, `usage.tsx`).
- Sidebar: `dashboard/src/features/**/dashboard-sidebar.tsx` (the nav that today collapses Services/Databases/Key Value into "Projects").
- Data from t001's `useEnvGroups()`.

## Steps

1. Create `routes/env-groups.tsx` (list) with a page title/meta.
2. Render a table/cards of groups (name, #vars, #secret files, #linked services), an empty state, and a "New Env Group" button that opens the create dialog (t004).
3. Add an "Env Groups" entry to the sidebar nav.

## Files

- `dashboard/src/routes/env-groups.tsx` (new)
- `dashboard/src/features/env-groups/components/*` (list component)
- sidebar nav component (add entry)

## Acceptance criteria

- [ ] `/env-groups` lists all workspace env groups with counts; empty state shows when there are none.
- [ ] Sidebar exposes an "Env Groups" nav item that routes to the page.

## Out of scope

- Detail page (t003), create dialog internals (t004).
