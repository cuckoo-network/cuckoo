---
id: w5/m26/t010
title: Closeout — DoD met → move milestone to done/
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m26/t009]
---

## Objective

When every other task is complete **and the DoD actually holds** (workspace-level Env Groups page fully manages groups end to end, verified against the mock cluster), close the milestone.

## Context

- Closeout procedure: `.claude/commands/pm.md` `done` subcommand step 4 — this task's completion _is_ the milestone move.

## Steps

1. Confirm the DoD: list/create/rename/delete + vars/secret-files edit + link/unlink all work from `/env-groups`, service-tab-free, verified live.
2. Run `/pm done w5/m26/t010` last → set all tasks `status: done`, move each to `done/`, mark rows `— **DONE**`, set `**Status:** done`, move the whole `w5/m26/` dir to `w5/done/m26/`, check `- [x]` in the w5 README.

## Files

- all `w5/m26/*` (moved to `w5/done/m26/`)
- `.pm/w5/README.md` (check the box)

## Acceptance criteria

- [ ] DoD verified true (not just code-complete); milestone moved to `w5/done/m26/`; w5 README box checked.

## Out of scope

- Any remaining implementation (must be done before this runs).
