---
id: w5/m26/t005
title: Empty/loading/error states + en/zh locales
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m26/t002, w5/m26/t003]
---

## Objective

Give the list and detail pages proper empty/loading/error states and add all `en`/`zh` locale strings for the new Env Groups surface.

## Context

- Locale pattern: `dashboard/src/features/<feature>/locales/{en,zh}.ts` (see `features/projects/locales/*`, `features/keyvalue/locales/*`).
- States should match existing feature conventions (skeleton/spinner loading, explanatory empty state, non-generic error).

## Steps

1. Add loading + empty + error states to the list (t002) and detail (t003) pages.
2. Add `en` strings for every user-visible label/message; mirror them in `zh`.

## Files

- `dashboard/src/features/env-groups/locales/en.ts` (new)
- `dashboard/src/features/env-groups/locales/zh.ts` (new)
- list/detail components (wire states + strings)

## Acceptance criteria

- [ ] No raw/untranslated strings on the Env Groups pages; `en` and `zh` are in sync.
- [ ] Loading, empty, and error states render distinctly (not a blank page or a generic crash).

## Out of scope

- Live cluster verification (t006).
