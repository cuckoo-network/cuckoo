---
id: w5/m77/t006
title: Test coverage — profile validation, batching, ordering, and failure recovery
worker: worker5
status: todo
estimate: 60m
depends_on: [w5/m77/t005]
---

## Objective

Add meaningful regression and performance-shape tests for the simplified profile and stream paths. Tests must prove observable behavior, operation-count reduction, ordering, bounds, and recovery failure modes without brittle wall-clock assertions.

## Context

- Driver tests can use `fixtures/acp-agent.mjs`; gateway/store tests already provide fake streams and real-Postgres coverage.
- Operation counters and deterministic gates are preferable to flaky absolute latency thresholds.
- Full compatibility means dashboard/mobile receive the same AI SDK v6 parts and terminal replay remains complete after the sandbox disappears.

## Steps

1. Test valid and invalid profile manifests across backend, driver, and image verification.
2. Test batch thresholds, sparse timer flush, terminal flush, concurrent duplicates, reconnect prefix handling, quotas, and store errors.
3. Test one log sink per turn, encoded-frame reuse, byte-identical replay, limits, redaction, and disk/write failure.
4. Run driver, backend unit/integration, dashboard agent-session, mobile agent-session, and relevant lint/build gates.
5. Record before/after deterministic operation counts and any live verification deferral in milestone evidence.

## Files

- `lego/agent-image/driver/test/`
- `lego/backend/internal/sshgateway/agentattach/*_test.go`
- `lego/backend/internal/store/*_test.go`
- `dashboard/src/features/agent-sessions/**`
- `mobile/src/features/agent-sessions/**`
- `.pm/w5/m77/evidence/`

## Acceptance criteria

- [ ] Tests prove profile drift and unsafe/incomplete entries fail closed.
- [ ] Tests prove transaction/file-open/encoding counts fall while output order and bytes remain stable.
- [ ] Failure-mode tests cover disconnect, store failure, disk failure, quota, truncation, restart/reconnect, and completion harvest.
- [ ] Relevant driver, backend, dashboard, mobile, lint, and build gates pass.
- [ ] No tautological tests or snapshot-everything assertions are added.

## Out of scope

- Requiring a production model credential for CI.
- General benchmark infrastructure outside the agent-session path.
