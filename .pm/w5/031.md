# 031 — Live Render-side drift check for the workspace-family walk (w5/m59 deferred)

**Filed:** 2026-07-30, during w5/m59 closeout. **Type:** deferred verification (the milestone's live-capture half).

w5/m59 walked the six workspace-scoped page families (Team · Env Groups · Usage/Billing · Notifications · Webhooks · Blueprints) as a **code-based parity audit** (`docs/render-artifacts/workspace-family-walk.md`): all six ship a first-class page, every relevant ADR018 row is ✅, and no missing pages / dashboard-unconsumed capabilities / stale cells were found. The one DoD element that could **not** be produced in-session is the **live authenticated Render + bex side-by-side capture** — no Render account/browser session was available, `dev-5` couldn't be raised (shared kind cluster missing the CNPG `postgresql.cnpg.io/v1` CRDs), and no authenticated prod bex session was available.

The code audit catches missing pages / unconsumed verbs / stale cells; it **cannot** catch Render-side visual/interaction drift since the artifacts were pinned (a Render redesign, a new control Render added to one of these pages).

**To close this note (~90m, when a Render account + a live bex dashboard are available):** authenticate to Render and to the deployed bex dashboard; for each of the six families capture both sides (Playwright, `.playwright-mcp/`, bare filenames); diff against `workspace-family-walk.md`'s verdict rows; apply any sub-hour fix in place (with tests) and file bounded notes for larger gaps; update the artifact's verdict rows from `match (code-side)` to `match (live)` or the found drift; correct any ADR018 cell the live walk proves stale. Then move this note to `done/`.
