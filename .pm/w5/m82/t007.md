---
id: w5/m82/t007
title: Closeout
worker: worker5
status: todo
estimate: 15m
depends_on: [w5/m82/t006]
---

## Objective

When the other tasks are complete **and the definition of done is actually met** (t006's live production E2E passed with recorded evidence), close the milestone.

## Context

- Board convention: completing this task _is_ the move — run `/pm done w5/m82/t007` last.

## Steps

1. Verify every task t001–t006 is `status: done`, in `done/`, and the DoD in the milestone README holds (live evidence recorded).
2. Set this task `status: done`, move it to `w5/m82/done/`, mark all README rows `— **DONE**`, set `**Status:** done`.
3. Move `w5/m82/` → `w5/done/m82/`; check `- [x]` **m82** in `w5/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m82/` → `.pm/w5/done/m82/`
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] Milestone directory lives at `w5/done/m82/` with no stub left behind; statuses in sync across all three places.

## Out of scope

- Closing with an unmet DoD (e.g. E2E deferred) — if blocked, the milestone stays open with the blocker recorded.
