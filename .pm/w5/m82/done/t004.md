---
id: w5/m82/t004
title: Simplify — /simplify over the changed code
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m82/t002, w5/m82/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving) and apply the fixes.

## Context

- Changed surface: `lego/backend/internal/sshgateway/agentcred/agentcred.go` + tests (and the metrics type if t003 extended it).
- Watch for: duplicated body-wrapping logic between the upload-pack and receive-pack branches, header-forward conditionals that could collapse, and test-helper duplication in `agentcred_test.go`.

## Steps

1. Invoke `/simplify` scoped to the milestone's diff.
2. Apply accepted cleanups; keep behavior byte-identical (tests stay green).

## Files

- `lego/backend/internal/sshgateway/agentcred/`

## Acceptance criteria

- [ ] `/simplify` findings triaged (applied or explicitly declined with reason).
- [ ] `cd lego/backend && go test ./internal/sshgateway/...` and `make lint-backend` green after cleanups.

## Out of scope

- Bug hunting (`/code-review` territory) and any behavior change.
