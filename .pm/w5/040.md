# 040 — Live browser walk for w5/m64 one-rail `/agents` (deferred proof)

**Filed:** 2026-08-07, during w5/m64 closeout. **Type:** deferred verification (not a code gap).

w5/m64 folded the agent-sessions sidebar into `DashboardSidebar`, so `/agents` renders one rail instead of two. **Shipped and deployed:** `f486653d`, live in prod as an ancestor of `ca70ee6c` (deploy (bex via Argo) green, run 31242352292 — build + sign + GitOps digest pin + Argo sync + `rollout status` all succeeded).

Verified in-session on the local `dev:local` + `local-bex` stack — one rail on `/agents` and `/agents/<id>`, active-row highlight, collapsed icon rail dropping the list, PR badge on the status line (screenshots `.playwright-mcp/bex-agents-one-rail.png` (pre-fix badge collision), `bex-agents-rail-fixed.png`, `bex-agents-detail.png`, `bex-agents-collapsed.png`) — plus the automated gates: dashboard typecheck + lint + 295 files / 1980 tests, including the `one-rail-invariant` guard and 8 new nav-section tests.

**Why deferred:** the defect was reported against the deployed `dashboard.bex.co/agents`, and no authenticated prod browser session was available in-session (`/agents` redirected to `/auth/login`). The local walk proves the same code; only the prod-data rendering is unwitnessed.

**To close this note (~10m, with a logged-in prod session):** open `https://dashboard.bex.co/agents`, then:

1. Confirm **one** left rail — global nav (Projects/Blueprints/Agents/Environment Groups + Integrations + Workspace) with a `Recent` sessions section beneath it, and no second `<aside>`.
2. Open a session → the rail is unchanged, the open session's row is highlighted, and the evidence/PR panel is on the **right**.
3. Collapse with ⌘B → nav icons remain, the sessions list disappears; expand restores it.
4. Check a session row carrying a PR → the `⑂ #N` badge sits on the status line (not colliding with the title) and opens GitHub without navigating the row.
5. `Recent` → search filters by title/repo; `…` reaches `/agents?view=list`; `O` from a non-typing focus opens the composer.
6. Narrow the window below `lg` → sessions remain reachable via the mobile drawer.

Capture screenshots to `.playwright-mcp/` (bare filenames), note the result in `docs/ADR047-cloud-coding-agent-sessions.md` § D9a, then move this note to `done/`.
