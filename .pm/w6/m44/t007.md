---
id: w6/m44/t007
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m44/t006]
---

## Objective

Close the milestone once its definition of done actually holds on a live environment — not merely once the code is written.

## Context

- The DoD is live-verifiable end to end: probe the three GraphQL verbs with dead ids, then visit `/services/<dead>`, `/databases/<dead>`, `/keyvalue/<dead>`, `/blueprints/<dead>` signed in.
- Because this is a regression of `w9/m55`, record in the closeout notes what let it regress (no assertion on the dead-id response shape) so the next milestone built on `KeyVerb` inherits the lesson.

## Steps

1. Re-run the live verification from the DoD and paste the results into the milestone README's closeout notes, including t004's parity table.
2. `/pm done` each remaining task, this one last — which moves `w6/m44/` to `w6/done/m44/` and checks its box in the workstream README.

## Files

- `.pm/w6/m44/README.md`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified live and recorded.
- [ ] Milestone moved to `w6/done/m44/`, box checked, no stub left behind.

## Out of scope

- Any new work surfaced during verification — file it separately.
