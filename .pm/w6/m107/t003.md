---
id: w6/m107/t003
title: Apply the primitive to the 3 suppressHydrationWarning-guarded sites and the 3 formatDateLong siblings
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m107/t001]
---

## Objective

Fix the call sites where the React error #418 is already suppressed (so no console noise, but the wrong value is still shown until an unrelated re-render), plus the `formatDateLong` call sites sharing the identical root cause.

## Context

- `dashboard/src/features/env-groups/components/env-group-metadata.tsx:57-58` — **live-reproduced this run**: fresh `page.goto` to `/env-groups/<id>` showed `Created: August 26, 2026 at 8:28 PM` for an instant whose correct viewer-local (America/Los_Angeles) conversion is `1:28 PM`. The `suppressHydrationWarning` guard here (added for `w6/030`) stops the console error but does not fix the value — replace it with t001's primitive so the value itself is correct on first paint.
- `dashboard/src/features/audit/components/audit-event-row.tsx:22-26` — same shape (`<TableCell suppressHydrationWarning>{formatDateTime(event.timestamp)}</TableCell>`), not individually re-navigated this run but byte-identical code pattern.
- `dashboard/src/routes/env-groups.tsx:236-240` — the env-groups **list** page's "Updated" column, same shape, explicit `w6/030` comment ("Local-timezone text: SSR and the browser disagree by design, so the mismatch is expected").
- `formatDateLong` siblings (narrower manifestation — wrong only when the instant falls within the ~7h UTC/local gap around a calendar-day boundary; not live-reproduced this run, **Unverified**, but same root function so the same fix applies): `dashboard/src/features/workspaces/components/workspace-details-card.tsx:138`, `dashboard/src/features/webhooks/components/webhook-detail-header.tsx:25`, `dashboard/src/features/registry-credentials/components/registry-credential-row.tsx:88`.

## Steps

1. Replace each `suppressHydrationWarning`-guarded `formatDateTime`/`formatDateLong` call with t001's primitive; remove the now-unnecessary `suppressHydrationWarning` prop (the primitive makes it moot — SSR and pre-mount client markup are identical by construction) unless the primitive's own implementation still needs it internally.
2. Live-verify `env-group-metadata.tsx`'s exact repro from this run: fresh `page.goto` to the same or an equivalent Environment Group detail page shows the viewer-local time matching a `page.evaluate`d client computation of the same raw `createdAt`.
3. For the 3 `formatDateLong` siblings, construct a test instant that falls within the UTC/local boundary-crossing window (e.g. an instant that is one calendar day in UTC and the previous/next day in `America/Los_Angeles`) and confirm the date shown is the viewer-local calendar day, not the UTC one.

## Files

- `dashboard/src/features/env-groups/components/env-group-metadata.tsx`
- `dashboard/src/features/audit/components/audit-event-row.tsx`
- `dashboard/src/routes/env-groups.tsx`
- `dashboard/src/features/workspaces/components/workspace-details-card.tsx`
- `dashboard/src/features/webhooks/components/webhook-detail-header.tsx`
- `dashboard/src/features/registry-credentials/components/registry-credential-row.tsx`

## Acceptance criteria

- [ ] Fresh `page.goto` to `/env-groups/<id>` shows the viewer-local `Created`/`Updated` time, matching a `page.evaluate`d client computation of the raw GraphQL instant — not the SSR-container value.
- [ ] The env-groups list page's "Updated" column shows the same correct value on a fresh load.
- [ ] `audit-event-row.tsx`'s timestamp column shows the correct value on a fresh load of the audit log page.
- [ ] A boundary-crossing instant renders the viewer-local calendar day (not the UTC day) for all 3 `formatDateLong` sites.

## Out of scope

- The unguarded high-severity sites — `t002`.
