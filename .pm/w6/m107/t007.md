---
id: w6/m107/t007
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m107/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving only.

## Steps

1. Confirm every call site converged on the same shared primitive rather than each growing its own local variant.
2. Check whether `formatDateTime`/`formatDateLong`'s own doc comments should be updated to no longer claim they're safe to call directly from a render body (steer future callers toward the primitive).
3. Look for any now-dead `suppressHydrationWarning` props left over from the old stopgap treatment that the new primitive makes unnecessary.

## Files

- Whatever t001-t004 touched.

## Acceptance criteria

- [ ] No duplicated mount-gate/placeholder logic across call sites — all route through t001's primitive.
- [ ] `formatDateTime`/`formatDateLong`'s doc comments accurately describe the SSR/timezone caveat and point callers at the primitive.

## Out of scope

- Anything outside this milestone's diff.
