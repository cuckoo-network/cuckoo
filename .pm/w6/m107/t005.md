---
id: w6/m107/t005
title: Live verification across the repaired routes
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m107/t002, w6/m107/t003, w6/m107/t004]
---

## Objective

Confirm the fix live, on production, via fresh full-page navigations — not client-side transitions, which never reproduced the bug even before the fix.

## Steps

1. For at least 4 of the repaired routes (recommend: an Environment Group detail page, a database with recovery enabled, the webhooks list, a webhook's delivery card), do a fresh `page.goto()` and:
   - Capture the raw instant via a direct GraphQL/REST query from inside the page (`page.evaluate` + `fetch(..., {credentials:'include'})`).
   - Compute the expected viewer-local text via `page.evaluate(() => new Date(raw).toLocaleString())` or an equivalent client-side computation.
   - Compare against the displayed text (via `browser_find` or an accessibility snapshot) — they must agree.
   - Run `browser_console_messages({level:"error"})` scoped to just that navigation and confirm zero React error #418.
2. Confirm the `w6/m97`-verification session's exact repro instant (`evg-da7kok7kh5kc73fbqvlg` if still reachable, or an equivalent fresh Environment Group) now shows the correct time on a fresh load.
3. Confirm the deploy detail page control case (`deploy-header.tsx`) is unaffected — still shows the correct time, now for the right reason (t004's defensive fix) rather than by the skeleton-gate accident.

## Files

- No source changes — verification only.

## Acceptance criteria

- [ ] All 4+ routes show the correct viewer-local time on a fresh full-page load, confirmed against a live client-side computation of the same raw instant.
- [ ] Zero React error #418 on any of the previously-unguarded routes.
- [ ] Evidence (the exact queries/values compared) recorded in this task or the milestone README for the next person to re-run.

## Out of scope

- Fixing anything found here that wasn't already scoped by t002/t003/t004 — file a new finding instead of scope-creeping this task.
