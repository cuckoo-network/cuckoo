---
id: w6/m119/t008
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m119/t006, w6/m119/t007]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet live, re-running the paired probe (free private service and free worker, both at `idleTTLSeconds: 60`, watched past three minutes). Confirm the deployed build contains the fix first.
2. Confirm the Settings idle control renders only where its sentence is true.
3. Set every remaining task's `status: done`.
4. Move each `tNNN.md` to `w6/m119/done/`.
5. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
6. Move the whole `w6/m119/` directory to `w6/done/m119/`.
7. Check `- [x]` for `m119` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live, not assumed — including the full idle window, which cannot be shortcut.
- [ ] The README's post-triage verification boundary is resolved or restated precisely in the closeout evidence.
- [ ] Milestone moved to `w6/done/m119/` with all statuses in sync.

## Superseded — see the triage update below

## Current closeout boundary (2026-08-27)

- Production is reachable and healthy, but its Argo applications are synced to
  revisions that do not contain the uncommitted m119 changes. A read-only App
  inventory found one `private_service`; it is `starter`, has
  `idleTTLSeconds: 0`, and is not a valid free-idle fixture. No production
  resource was mutated to manufacture evidence.
- The stale local `bex` workload cluster was deleted for a clean rebuild. Kind
  cannot finish deleting the stopped `bex-mgmt-control-plane` container because
  the local Docker runtime hangs even on inspect/remove. Restarting the whole
  container runtime would affect unrelated running workloads and is not an
  m119-scoped action.
- Closeout therefore still requires an authorized `$ship`/deployment, followed
  by the paired free-private/free-worker probe and dashboard Settings check.
  Local unit/envtest coverage is complete but is not substituted for this live
  acceptance criterion.

## Out of scope

- New work surfaced during verification — file it separately.

## Triage update (2026-08-27, later) — the "uncommitted" premise is stale

The m119 implementation is **committed**: `9917191f` ("fix(services): enforce free-tier lifecycle limits") carries both this milestone's private-service exclusion and `w6/m118`'s instance cap, and it is an ancestor of `HEAD`. `autoSleepEligible` (`lego/operator/internal/controller/app_controller.go:1635-1638`) now reads `web := app.Spec.Type == "" || app.Spec.Type == appv1alpha1.TypeWebService`, so the fix is in the tree and in history. Do not re-argue "uncommitted changes".

**What actually remains is the deploy, and it has not happened.** `9917191f` is **not** an ancestor of `050d40e4`, the last commit `deploy.yml` shipped successfully; `HEAD` is 54 commits past that point. The `deploy.yml` run on `94c0a185` (id `33143365056`) was still `pending` at triage time, with several preceding runs `cancelled` by push-concurrency and `344ead76` `failed`. So the live paired probe (free private service + free worker at `idleTTLSeconds: 60`, watched past three minutes) is blocked on a green deploy that includes `9917191f`, not on writing code.
