---
id: w6/m119/t008
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m119/t006, w6/m119/t007]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet live, re-running the paired probe (free private service and free worker, both at `idleTTLSeconds: 60`, watched past three minutes). Confirm the deployed build contains the fix first.
2. Verify Resume on a service that is scaled to zero.
3. Confirm the Settings idle control renders only where its sentence is true.
4. Set every remaining task's `status: done`.
5. Move each `tNNN.md` to `w6/m119/done/`.
6. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
7. Move the whole `w6/m119/` directory to `w6/done/m119/`.
8. Check `- [x]` for `m119` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live, not assumed — including the full idle window, which cannot be shortcut.
- [ ] The three "Unverified this run" items are each verified or restated as still-unverified in the closeout evidence.
- [ ] Milestone moved to `w6/done/m119/` with all statuses in sync.

## Out of scope

- New work surfaced during verification — file it separately.
