---
id: w6/m119/t006
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m119/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- The specific target is the one this milestone exists because of: "can this App be auto-slept?" is currently answered by `worker := Spec.Type == TypeBackgroundWorker` (`:1710`) in one place and `autoSleepEligible` (`:1638`) in another, and the dashboard answers a third version of it across two files. After the fix there should be one predicate with a name that states the question — routing-wakeable, not is-worker.
- `autoSleepEligible` and `shouldAutoHibernate` are deliberately separate (`w6/m94`) so the routing and scheduling questions can differ mid-wake; do not merge those two. The comment above them explains why.

## Steps

1. Run `/simplify` over the touched files.
2. Confirm one named predicate answers "is this type auto-sleep eligible", and that the dashboard's gate derives from the same rule rather than restating it.

## Files

- files touched by t002/t003/t004

## Acceptance criteria

- [ ] `/simplify` run; behavior-preserving changes applied.
- [ ] One predicate, named for the question it answers.
- [ ] `autoSleepEligible`/`shouldAutoHibernate` remain separate.

## Out of scope

- Behavior changes.
