---
id: w6/m111/t003
title: Apply t002's outcome to deploy-log-panel, and count hasActiveLogFilters' callers
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m111/t002]
---

## Objective

Make sure the empty-state fix covers every surface that renders these strings, and establish the shared predicate's real caller count instead of assuming this milestone found them all.

## Context

- `dashboard/src/features/deploys/components/deploy-log-panel.tsx:178-179` renders `logs.emptyTitle` / `logs.emptyBody` **directly**, with no `filtered` branch at all — so whatever t002 settles must reach it or the false claim survives on the deploy tab.
- That panel behaves acceptably today only because a deploy's log window is implicitly the deploy itself. It is correct by accident, not by construction, and therefore needs a regression test rather than being left alone.
- `useLiveLogs` has two consumers (the service Logs tab and this panel); `subscribeUrl` has one caller. `hasActiveLogFilters` is a shared predicate whose caller count this milestone has **not** measured.

## Steps

1. `grep -rn "hasActiveLogFilters" dashboard/src --include='*.ts' --include='*.tsx'` and record the exact count and every call site — no estimate.
2. Decide global-vs-allowlisted for t002's change across those call sites, and say which in the milestone README.
3. Apply the state machine to `deploy-log-panel.tsx`, or record why the deploy window makes it inapplicable — with the reason, not the assumption.
4. Verify the claim that a deploy's log window is always the deploy's own span; it is currently marked unverified in the milestone README.

## Files

- `dashboard/src/features/deploys/components/deploy-log-panel.tsx`
- wherever step 1's grep leads

## Acceptance criteria

- [ ] `hasActiveLogFilters`' caller count is measured and listed.
- [ ] The deploy log panel either branches correctly or carries a written reason it need not.
- [ ] Call sites that behave correctly today have regression tests, not just the broken one.
- [ ] The "deploy window is always the deploy's span" claim is verified or restated as still unverified.

## Out of scope

- Datastore log tabs (`datastore-log-range.ts`) — noted as unverified in the milestone README; if they share the defect they are their own finding.
