---
id: w6/m111/t005
title: Simplify — /simplify over the code this milestone changed
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m111/t004]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency — behavior-preserving only.

## Context

- The likely candidate is `log-viewer.tsx`'s window plumbing: after t001 the same resolved window feeds both the history query and the stream, so the two paths may collapse onto one source of truth instead of two props.
- t002 may leave both an enum and the old boolean predicate in place; only one should survive.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; record any skipped with the reason.

## Files

- Whatever this milestone changed.

## Acceptance criteria

- [ ] `/simplify` run; applied and skipped findings both listed.
- [ ] No behavior change — t006's tests and the DoD clicks still pass.

## Out of scope

- Refactors beyond this milestone's diff.
