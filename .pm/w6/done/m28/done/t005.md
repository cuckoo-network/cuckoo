---
id: w6/m28/t005
title: Reconcile Key Value's partial owner adapter and add its remaining truthful metadata
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m28/t002]
---

## Objective

Replace Key Value's interim id-as-name owner with the shared owner contract and complete its region, dashboard URL, and update timestamp fields consistently with Service and Postgres.

## Context

- `lego/backend/internal/keyvalue/rest.go` already has a Render-only adapter and nested owner, landed during parallel CLI compatibility work.
- Its current owner uses `OwnerID` for both `id` and `name` and omits email, so it must be reconciled rather than duplicated.
- Key Value has its own dashboard route and CR lifecycle; neither should be inferred from Postgres strings by convention.

## Steps

1. Replace the local owner placeholder with the shared resolved owner value.
2. Add region, dashboard URL, and authoritative update time with the t001 omission rules.
3. Apply the mapping consistently to list/get/create/patch responses and pagination envelopes.
4. Preserve nested options, IP allow-list shape, stable ids, native ownerId semantics, and authorization.
5. Cover legacy labels, unset dashboard/region config, and timestamp advancement.

## Files

- `lego/backend/internal/keyvalue/rest.go`
- `lego/backend/internal/keyvalue/service.go`
- `lego/backend/internal/keyvalue/*test.go`
- `lego/backend/internal/api/server.go`
- `lego/types/api/v1alpha1/keyvalue_types.go`

## Acceptance criteria

- [x] Key Value owner names come from the workspace, not a copied id.
- [x] Region/dashboard/update fields follow the same source and omission contract as the other resource types.
- [x] Existing official-CLI decoding for owner/options/IP allow lists remains green.
- [x] No REST-only nesting leaks into GraphQL/MCP response types.

## Out of scope

- Adding a Key Value version field without an authoritative operator/data-plane source.
