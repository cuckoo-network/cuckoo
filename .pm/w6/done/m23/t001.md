---
id: w6/m23/t001
title: Capture Render's pairing behavior live
worker: worker6
status: done
estimate: 30m
depends_on: []
---

## Objective

Pin the exact behavior to mirror before building: on Render, add an apex — is `www` auto-added as a second domain, or served as a redirect? Add `www.foo.com` first — is the apex auto-added? What do deletes do to the pair? What about `app.foo.com` (no pairing expected)? Record in `docs/render-artifacts/custom-domain-pairing.md`.

## Context

- render.com/docs/custom-domains + the existing capture `docs/render-artifacts/custom-domain-dns-instructions.md` (w5/m10).

## Acceptance criteria

- [x] Capture answers add/delete/redirect semantics for both directions; names what bex will mirror vs diverge on.
