---
id: w6/m137/t003
title: Enforce at stored inbox reads and unread counts
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m137/t001]
---

## Objective

Stored inbox items and unread counts pass the t001 destination-eligibility policy, so a role downgrade removes unauthorized historic items from the visible projection and badge count.

## Context

- `lego/backend/internal/notifications/inbox.go` — list/get/count authorize only `RelCanView` (:65/:92/:117) scoped to tenant+subject; the destination resource's relation is never checked, so a downgraded member still reads historic agent metadata.
- Durable records may be retained internally; only the API-visible projection and counts must filter.
- Mark-read and personal opt-in/out remain available to read-only members (own inbox only).

## Steps

1. Filter list/get/unread-count through the policy (batch the relation checks; avoid an N+1 per row).
2. Ensure filtering failure (authz outage) suppresses gated rows rather than exposing them, without failing the whole inbox for ungated rows.
3. Keep mark-read/opt-in paths untouched by resource gating.

## Files

- `lego/backend/internal/notifications/inbox.go`

## Acceptance criteria

- [ ] Test proven red pre-fix: after a Contributor→Viewer downgrade, historic agent items vanish from list and badge count via the API.
- [ ] Ungated service-supervision items survive an agent-eligibility refusal in the same page.
- [ ] No per-row sequential checker calls on the hot list path (batched or cached within the request).

## Out of scope

- Deleting durable records; client-side badge logic (w6/m138).
