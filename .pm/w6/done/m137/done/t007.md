---
id: w6/m137/t007
title: Test coverage
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m137/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — the ADR087 acceptance rows, proven red against pre-fix behavior.

## Context

- Key scenarios: Viewer/Billing receives no agent metadata via push, deferred delivery, inbox, or counts; push queued before downgrade dropped at send; historic inbox filtered after downgrade; authz outage defers without broadening; generic payload content.

## Steps

1. Cover projection, pre-send re-check, and inbox/count filtering with the package's existing fake/store harness.
2. Add one integration path against real Postgres + OpenFGA if the existing notification harness supports it cheaply.

## Acceptance criteria

- [ ] Every DoD clause has a test that fails when its behavior regresses; the two headline disclosure tests were verified red pre-fix.
- [ ] `cd lego/backend && go test ./...` green; `make lint` clean.
