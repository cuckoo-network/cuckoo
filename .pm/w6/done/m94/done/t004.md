---
id: w6/m94/t004
title: Simplify
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m94/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Touches `lego/operator/internal/controller/app_controller.go`'s hibernate/park/ingress reconcile path only.

## Steps

1. Run `/simplify` over the diff from t002.
2. Apply any behavior-preserving cleanups it finds.

## Files

- Whatever t002 touched.

## Acceptance criteria

- [ ] `/simplify` has run over the milestone's diff and its applicable findings are applied.

## Out of scope

- Any change that alters behavior — this task is cleanup only.

## Findings (2026-08-25)

The `gocyclo` gate did the reviewing: my first cut pushed `reconcileKubernetes` from 30 to 36, over the limit of 30. Rather than suppress it, the additions were folded down and one genuinely cohesive block was extracted — the function is now **28**, below where it started.

- `holdHibernateForRouting` absorbs the maintenance/worker guards _and_ returns the replica count to roll, so the call site is one call plus one error check instead of three nested conditions.
- The hold's early return moved into `parkKubernetes`, which is already the "we are parking" branch and the natural home for "…except not yet".
- `reconcileDiskLifecycle` extracts the disk/snapshot/restore sequence that ran inline: three calls and a halt, entirely self-contained, and the only part of the function unrelated to rolling the workload. This is what bought the headroom, and it makes the remaining body read as one story (replicas → workload → routing → park/report).

No behavior change in any of it — the full operator suite, including every pre-existing disk, hibernate, wake and maintenance test, passes unchanged.
