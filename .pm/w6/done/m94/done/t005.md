---
id: w6/m94/t005
title: Test coverage
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m94/t004]
---

## Objective

Add a meaningful regression test for the fix — a race condition can't be reliably reproduced by timing alone in a test, so this needs to assert the actual mechanism the fix changes (write ordering, or an explicit propagation gate), not just re-run the live repro and hope.

## Context

- The bug is a timing race between two independent Kubernetes writes (Deployment scale-to-0, Ingress backend swap) and Traefik's asynchronous config propagation — not something `envtest` (no real Traefik) can reproduce end-to-end.
- Meaningful test options: (a) if the fix reorders writes, assert the order directly (e.g. a fake/recording client that captures write sequence, or splitting the two operations into separately-testable functions and asserting call order); (b) if the fix adds a propagation wait/requeue, assert that the reconcile actually waits/requeues rather than proceeding immediately; (c) a control-case test proving the non-auto-hibernating paths (manual suspend, maintenance mode) are unaffected by whatever changed.
- Do not write a test that merely re-asserts today's already-correct end states (Deployment has 0 replicas, Ingress points at the activator) without asserting the _ordering/timing_ property the fix actually changes — that would pass against the pre-fix code too and prove nothing.

## Steps

1. Write a test that fails against the pre-fix code specifically on the ordering/gating property (not just the end state).
2. Write a control-case test for manual suspend and maintenance-mode routing, proving they're unaffected.
3. Run the full operator suite (`make test` from `lego/operator/`) to confirm nothing else broke.

## Files

- `lego/operator/internal/controller/*_test.go`

## Acceptance criteria

- [ ] A test proving the fix's ordering/gating property, red pre-fix / green post-fix.
- [ ] A control-case test proving manual suspend and maintenance-mode routing are unaffected.
- [ ] Full operator test suite passes.

## Out of scope

- Attempting to deterministically reproduce the live race end-to-end in envtest (no real Traefik) — test the mechanism the fix changes instead.

## Coverage added (2026-08-25)

| test | asserts |
| --- | --- |
| `TestHibernateSwapsRouteBeforeDrainingPods` | the hibernate pass writes the activator route, **keeps the pods**, and requeues after `hibernateRoutingGrace`; the following pass scales to 0 with the route still on the activator |
| `TestWakeRestoresAppServiceAsIngressBackend` (updated) | the route does **not** return to the App's Service while no pod is ready, and does once one is — the resume race, in the test that previously asserted the racy behavior |
| `TestMaintenanceHibernatesWithoutRoutingHold` | maintenance mode still hibernates its workload; the hold must not apply where the route will never name the activator |

Red-verified against the pre-fix code: 3 failures across the two directions.

Gates green: `make test` (operator, 145 specs, 81.0% controller coverage), `make lint` (all four modules, `gocyclo` clean), `cd lego/backend && go test ./...`, `cd lego/cli && go test ./...`. No dashboard change, so `yarn test` is unaffected.

**Not covered by test:** the propagation grace itself. `hibernateRoutingGrace` covers a delay in software the operator cannot observe (Traefik ingesting an Ingress), so no unit test can prove 10s is enough — only the live trials in `w6/040` can, and they need a working deploy. The tests above pin the _mechanism_ (route first, pods kept, requeue, then drain); the constant's value is a judgement recorded in its own doc comment.
