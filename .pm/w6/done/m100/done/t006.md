---
id: w6/m100/t006
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m100/t005]
---

## Objective

Close `w6/m100` once its Definition of done is actually met — not merely once the code is written.

## Steps

1. Re-verify the milestone's Definition of done live (or via the envtest suite if a live repro is impractical at closeout time): a deploy queued behind a build-failed sibling reaches a terminal state within the normal build-pickup window, the w2/m82 anti-regression holds, and Cancel still works.
2. Set every task's `status: done`, mark every row `— **DONE**` and `**Status:** done` in this README.
3. Move each `tNNN.md` to `w6/m100/done/`.
4. Move the whole `w6/m100/` directory to `w6/done/m100/`.
5. Check `- [x]` for `m100` in `w6/README.md`.
6. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [x] DoD re-verified, not assumed.
- [x] Board moved and statuses synced across task frontmatter, milestone README, and workstream README.

## Result

Definition of done re-verified against the envtest/unit suites rather than assumed (a live repro needs the fix deployed, which `/ship` + the deploy pipeline do, not this milestone):

- **Live-shaped repro, fixed** — `build_failed_pending_release_envtest_test.go` drives the exact race (first deploy building → second trigger lands → first fails via `PodFailurePolicy`/exit 90) and asserts the second generation's build is dispatched on the very next reconcile, then completes and rolls out. Verified red against pre-fix code.
- **w2/m82 anti-regression holds** — `build_terminal_requeue_envtest_test.go` passes unmodified in behavior (its helper closures now call the shared fixtures; assertions untouched), and a second spec pins the same property from this milestone's own fixture: no re-error, no self-requeue, exactly one metered outcome.
- **Operator state actually advances** — asserted on the App CR itself (`Status.ReleaseGeneration` 1 → 2, `PendingReleaseGeneration` → 0, a real `bld-…-gen-2` Job on the envtest API server), not inferred from a deploy row.
- **Backend consumer verified** — `TestQueuedRowIsPromotedOnceReleaseGenerationAdvances` covers the half `supersededDeployStatus`'s `OverlapPending` branch never had: it now asserts the row is promoted once `appReleaseGeneration(app)` advances, alongside the existing "keeps waiting" assertion.
- **Rollback covered** — `TestRollbackQueuedBehindFailedBuildReachesTerminalState` drives `CreateRollbackDeploy` racing a build that then fails; the rollback's row reaches `build_in_progress`, explicitly failing the test if it is still `queued`.
- **Cancel keeps working** — `internal/deploys` Cancel tests re-run green, and `TestRecordedBuildFailureNeverRepaintsAClosedRow` pins that a row already past its build phase is never repainted `build_failed` by the durable verdict.
- **Unverified, carried forward as the milestone specified** — live end-to-end for webhook auto-deploy, Blueprint sync, env-var/secret restart, and 3+-deep chains. All share the `spec.restartedAt` mechanism the envtest exercises, so they are covered mechanically but not live-reproduced. New this milestone (see t005): the single-slot bound on `ConditionBuild` when two builds fail faster than one bex-api resync.

Gates: `make test` (operator), `cd lego/backend && go test ./...`, `cd lego/types && go test ./...`, `cd lego/cli && go test ./...`, and `make lint` (all four modules, 0 issues) — all green. No CRD schema change (`make manifests` produced no diff: `status.conditions` is already a free-form condition list).
