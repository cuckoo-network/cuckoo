---
id: w6/m41/t006
title: "Test coverage: real outages unchanged, phantoms suppressed"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m41/t004]
---

## Objective

Prove both halves: phantom edges are suppressed, and genuine outages are completely unaffected. The second half is the one that matters most — a guard that also swallows real failures is far worse than the phantom pairs it replaces.

## Context

- `t001` supplies the phantom reproduction; this task adds the surrounding regression suite.
- The must-not-break baseline: `TestPGObservedCrashEdgeEmitsExactlyOnePair` (real-Postgres, drives Running → CrashLoopBackOff → recovered across repeated resync observations) and `TestPushWorkerEnqueuesObservedLifecycleFacts` (full chain to a policy-enabled member's durable inbox). Both must stay green **unmodified** — modifying them to accommodate the guard would defeat the point.
- Cases worth pinning beyond the happy path: a service with no prior healthy checkpoint (must record), a genuine second outage after a real recovery (must record — its transition is legitimately newer), and a stale conclusion arriving between two real edges.
- Do not game coverage: assert on recorded edges and delivered notifications, not on internal call counts.

## Steps

1. Add the "real second outage after recovery still records" case — the most likely false-suppression regression.
2. Add the no-prior-checkpoint case (records; fails open toward reporting).
3. Add the stale-conclusion-between-real-edges case.
4. Assert the rejection counter moves only on genuinely rejected conclusions.
5. Confirm the two m78 tests pass unmodified.

## Files

- `lego/backend/internal/store/reconciler_test.go` and the push/webhook projection tests

## Acceptance criteria

- [ ] A genuine second outage after recovery still records exactly one pair.
- [ ] No-prior-checkpoint records the edge.
- [ ] A stale conclusion between two real edges is rejected without disturbing either.
- [ ] Rejection counter asserted, and moves only for rejections.
- [ ] `TestPGObservedCrashEdgeEmitsExactlyOnePair` and `TestPushWorkerEnqueuesObservedLifecycleFacts` green and unmodified.
- [ ] `cd lego/backend && go test ./...` green.

## Out of scope

- Live control-plane staleness injection against a real cluster.
