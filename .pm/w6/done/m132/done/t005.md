---
id: w6/m132/t005
title: Render parity sweep (REST/GraphQL/MCP/dashboard + official Render CLI)
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m132/t002, w6/m132/t003]
---

## Objective

Confirm the restored behaviour is consistent across every surface that exposes it, and against render.com's equivalent.

## Closeout evidence (2026-09-09)

- REST `serviceDetails.sshAddress` on paid Running starter: `srv-…@ssh.bex.co`; free plan omits `sshAddress` (`null`).
- Shared view path unchanged (`internal/apps/render.go`) — REST/GraphQL/MCP parity holds.
- Official Render CLI `v2.21.0` under a real PTY (`expect`): `render ssh --output interactive qa-200243-longname-metrics -- echo CLI_SSH_OK` printed `CLI_SSH_OK` and the ssh wrapper logged destination `srv-dagcoku7dt4c73f45lig@ssh.bex.co`.
- ADR018 SSH row already reconciled with the regression + fix + guard.

## Acceptance criteria

- [x] The three API surfaces agree, and the dashboard matches.
- [x] The unmodified official CLI completes the shell path.
- [x] ADR018's SSH row matches shipped behaviour.
