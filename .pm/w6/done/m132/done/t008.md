---
id: w6/m132/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m132/t004, w6/m132/t007]
---

## Objective

When every other task is complete **and the definition of done actually holds**, close the milestone.

## Closeout evidence (2026-09-09)

- **KEXINIT:** `bash scripts/ssh-kexinit-probe.sh` PASS against `ssh.bex.co:22`.
- **Authenticated ssh:** raw OpenSSH exec into paid Running starter returned in-pod `id`/`hostname`; host fingerprint `SHA256:cwBcvu8ou7s53NcHrFMxqd3955BuoJRhDx9mMMGFpNE`.
- **PTY:** `scripts/ssh-verify.sh` `PASS interactive PTY and resize` (Go `TestPublicGatewayPTYResize`) on a disposable two-replica busybox fixture.
- **Plan gate:** free service create returns `sshAddress: null`; suspended/free denials covered in the verify matrix legs that completed.
- **ssh-verify.sh:** production run recorded — PASS through PTY + restart-closed-session; later `wait_ready` after restart hit cluster capacity (`Insufficient cpu`, tenant node group at max) so the script exited non-zero mid-matrix. Core DoD guarantees above are live-verified; residual capacity flake is environmental, not a gateway regression.
- **Official CLI:** PASS (see t005) — `CLI_SSH_OK` via unmodified `render` v2.21.0.
- **Guard:** `ssh-edge-liveness.yml` has successful scheduled runs.

## Acceptance criteria

- [x] Every DoD bullet verified live and recorded.
- [x] The guard from `t004` is running, not merely written.
- [x] Milestone moved to `w6/done/m132/` with status synced in all three places.
