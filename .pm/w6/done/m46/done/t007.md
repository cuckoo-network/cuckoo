---
id: w6/m46/t007
title: Simplify the touched code
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m46/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Expected touched areas: `lego/backend/internal/apps/service.go`, `deploy.go`, `rest.go`, `graphql.go`, `mcp.go`; `lego/operator/internal/controller/app_controller.go`; `lego/types/v1alpha1/app_types.go`; `lego/backend/internal/store/reconciler.go`; `lego/operator/internal/controller/release_identity.go`; dashboard deploy/service-header components.

## Steps

1. Run `/simplify` scoped to the diff this milestone produced.
2. Apply any behavior-preserving cleanups it surfaces.

## Files

- Whatever `t001`-`t005` actually touched.

## Acceptance criteria

- [ ] `/simplify` run against the milestone's diff, findings applied or explicitly declined with a reason.

## Out of scope

- Any behavior change — this task is cleanup only.
