---
id: w6/m138/t009
title: Test coverage
worker: worker6
status: done
estimate: 60m
depends_on: [w6/m138/t007]
---

## Objective

Add meaningful tests for the shipped view policy — render **and** network assertions per the ADR087 acceptance table, not snapshot noise.

## Context

- Mandatory scenarios: all five roles × both matrices with zero associated requests from hidden regions (including prefetch/restored navigation); Viewer/Billing session/log links render nothing and fetch nothing; Contributor's allowed action set works while rollback/composer are absent; checker outage/null snapshot/unknown enum fail closed with accurate reasons; read-only vs unavailable vs legitimately-empty distinguished; permitted cards survive a restricted-read refusal; boundary tests for workspace A↔B and same-workspace role change.
- `src/__tests__/mobile-scope-policy.test.ts` stays mandatory but does not substitute for behavioral tests.
- OS-level behavior (tab transitions, focus, announcements) needs physical-device evidence — record what was device-verified vs simulated.

## Steps

1. Build a role-matrix test harness (mock capability responses per role) driving screen-level integration tests with a request-recording Apollo link.
2. Cover the access-change transition table from t005.
3. Run the full required check set including both platform bundles.

## Acceptance criteria

- [ ] Matrix tests fail if any cell's render or request behavior drifts.
- [ ] All required checks green: `yarn format:check`, `yarn typecheck`, `yarn lint`, `yarn test:unit`, `yarn expo:check`, `yarn bundle:ios`, `yarn bundle:android`.
- [ ] Device-verified vs simulated evidence recorded in the milestone README.
