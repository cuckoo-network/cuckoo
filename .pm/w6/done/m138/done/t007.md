---
id: w6/m138/t007
title: "Render parity: record deliberate role-matrix divergences"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m138/t005, w6/m138/t006]
---

## Objective

Compare the shipped mobile role experience against Render's documented role behavior and the ADR's own matrices; record the deliberate divergences and flag any unintended drift as follow-up work rather than silently diverging.

## Context

- Deliberate, documented divergences (not bugs): Billing keeps bex's general supervision reads (Render's Billing lacks observability); Contributor cannot roll back (Render's can); the Sessions tab is role-dependent (departs from Apple tab-stability guidance) — all recorded in ADR087 with rationale.
- Mobile is a native client with no REST/GraphQL/MCP surface of its own; cross-surface API alignment was m136/m137's parity work. This pass checks the *experience* matrix as shipped vs the ADR + Render docs.

## Steps

1. Walk both ADR087 matrices against the built app per role (five roles × navigation + detail rows).
2. Record the three deliberate divergences in `docs/ADR018-render-parity.md` (or confirm m136's row covers them).
3. File inbox notes for any unintended drift found.

## Files

- `docs/ADR018-render-parity.md`
- `docs/ADR087-mobile-role-views.md` (status/date touch-up if the matrices changed during implementation)

## Acceptance criteria

- [ ] Every matrix cell verified as shipped or filed as drift; zero silent divergence.

## Out of scope

- Changing bex authorization to match Render (rejected alternative in ADR087).
