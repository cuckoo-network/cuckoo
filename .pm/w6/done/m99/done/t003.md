---
id: w6/m99/t003
title: "Regression tests: ungranted-repo case shows the truthful hint/state; the w2/m9 granted-repo control case is unchanged; REST/GraphQL/MCP agree on both"
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m99/t002]
---

## Objective

Cover both branches this bug conflated — a github.com repo NOT in the connection's grant, and one that IS (the `w2/m9` control case) — on both the backend field (t001) and the dashboard hint (t002), so this exact regression (verifying only the passing case) cannot recur silently.

## Context

- `w2/m9`'s own verification (`.pm/w2/done/m9/README.md:32`) tested only the granted-repo case; no test ever constructed "connection live, repo is github.com, repo NOT granted."
- `dashboard/src/features/services/components/__tests__/build-deploy-section.test.tsx:75` currently mocks `useRepos: () => ({ repos: [], loading: false, error: undefined })` unconditionally — this mock must vary between the two test cases (empty vs. containing the service's repo) to actually exercise the branch, and the service prop must carry t001's field.
- `lego/operator/internal/controller/protected_secret_test.go`'s pattern (self-reference accepted, malicious reference refused) is the same "test the boundary you actually found, not just the one you already knew about" shape this task should follow — see `w6/m97/t002` for the sibling precedent.

## Steps

1. Backend: unit test `render.go`'s mapping (or wherever t001 lands the field) for both `CloneToken` outcomes (`ok=true`/`ok=false`) and for `s.GitHub == nil` / `a.Spec.Repo == ""`.
2. Backend: integration-level test (or table test against the shared mapper) asserting REST response, GraphQL response, and MCP `get_service` structured content all carry the identical value for the new field, for both outcomes.
3. Dashboard: `build-deploy-section.test.tsx` — add a case with `useRepos` returning a list that does NOT include the service's `repo` (and `connection.connected: true`) asserting the manual-webhook hint renders; add/keep a case where `repos` DOES include it asserting the GitHub-app hint renders (the `w2/m9` control case, now actually asserted rather than assumed).
4. Confirm `yarn test` and the backend/operator suites stay green.

## Files

- `lego/backend/internal/apps/render_test.go` (or new file colocated with wherever t001's logic lands)
- `lego/backend/internal/apps/mcp_test.go` / equivalent MCP structured-content test
- `dashboard/src/features/services/components/__tests__/build-deploy-section.test.tsx`

## Acceptance criteria

- [ ] A backend test fails on the pre-t001 code (repo not granted still reports GitHub-app-deliverable) and passes after.
- [ ] A dashboard test fails on the pre-t002 code (ungranted repo still shows "via GitHub app" whenever any connection exists) and passes after.
- [ ] A test explicitly covers the granted-repo control case and would fail if t002's fix accidentally flipped it too.
- [ ] REST/GraphQL/MCP parity for the new field is asserted in one place, not left to three independent, potentially-drifting tests.

## Out of scope

- End-to-end live re-verification against production (that's this milestone's Definition of done, checked at Closeout, not a task-level unit test).
