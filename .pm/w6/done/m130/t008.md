---
id: w6/m130/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m130/t007]
---

## Objective

When every other task is complete **and the milestone's definition of done actually holds**, close the milestone.

## Context

- Do not run this until the DoD is real, not merely coded. The DoD's bullets are live-verifiable: the four `POST` probes plus the `ipAllowList` control, the four-type `GET` capture, the `curl -sSk` edge checks, and `setSubdomainPolicy` refused per type.
- The `Unverified` section of the milestone README lists what this hunt did *not* observe — the PATCH nesting rejection, the MCP surface, and real-client behaviour. Those must be verified here or explicitly re-carried, not quietly dropped.

## Steps

1. Re-run the DoD probes against the deployed build and record the responses.
2. Set every remaining task's `status: done`, move each `tNNN.md` to `w6/m130/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
3. Move `w6/m130/` to `w6/done/m130/`, leaving no stub at the old path, and check `- [x]` in `.pm/w6/README.md`.

## Files

- `.pm/w6/m130/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified live and recorded.
- [ ] Milestone moved to `w6/done/m130/` with status synced in all three places.

## Out of scope

- New findings discovered during verification — file them separately.
