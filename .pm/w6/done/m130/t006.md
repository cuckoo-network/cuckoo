---
id: w6/m130/t006
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m130/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency — strictly behavior-preserving.

## Context

- Touched: `lego/backend/internal/apps/rest.go`, `render.go`, `service.go`, and whichever of `graphql.go`/`mcp.go` `t004` moved.
- There is an obvious reuse target: the dual-accept precedence handling for `schedule`/`command`/`publishPath`/`preDeployCommand`/`renderSubdomainPolicy` now repeats the same shape five times via `preferTopLevel`. Consider whether it collapses cleanly, without obscuring the per-field comments that document Render's location for each.

## Steps

1. Run `/simplify` over the changed files.
2. Keep behaviour identical; the tests from `t007` and the existing suites must stay green.

## Files

- The files this milestone changed.

## Acceptance criteria

- [ ] `/simplify` run over this milestone's diff; changes are behavior-preserving.
- [ ] `make lint` and the backend suite pass.

## Out of scope

- Refactors beyond the code this milestone touched.
