---
id: w6/m44/t005
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m44/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- The likely hot spot is the resolver-helper change from t001: if it fixed only three call sites instead of the shared `KeyVerb`/`IDVerb` shape, that is exactly the duplication `/simplify` should catch.
- The dashboard side should reuse the existing `useNotFoundRedirect` + `resource-load-error` pattern rather than introducing a second not-found path.

## Steps

1. `/simplify` over the milestone's diff.
2. Apply what it finds; skip false positives without arguing.

## Files

- whatever t001–t003 touched

## Acceptance criteria

- [ ] `/simplify` run and its findings applied or explicitly skipped.
- [ ] All suites still green.

## Out of scope

- Behavior changes.
