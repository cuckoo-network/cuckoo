---
id: w6/m36/t002
title: Batch registry-credential name resolution in restServices
worker: worker6
status: todo
estimate: 30m
depends_on: []
---

## Objective

`restServices` resolves credential names for the whole page in one lookup — an `IN (…)` query (or per-page memo) keyed by credential id, mirroring the `resourcemeta.ResolveOwners` batching one line above — instead of one `GetRegistryCredential` query per bound service.

## Context

- `lego/backend/internal/apps/render.go:340-365`; the per-item call is `s.RegistryCreds.RegistryCredentialName(ctx, workspaceID, id)`.
- Mind the per-item `workspaceID` fallback (`s.Tenant(ctx)` when `OwnerID` is empty) — keep its semantics in the batched form.

## Steps

1. Add a batch method on the `RegistryCreds` seam (or memoize within the call) preserving workspace scoping.
2. Test: a page with N bound services performs one credential lookup (assert via a counting fake).

## Files

- `lego/backend/internal/apps/render.go` + the RegistryCreds interface/impl + tests

## Acceptance criteria

- [ ] Counting-fake test proves one lookup per page; rendered summaries unchanged.

## Out of scope

- Changing the `registryCredential` wire shape.
