---
id: w6/m1/t008
title: Simplify — /simplify over the code this milestone changed
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m1/t007]
---

## Objective

Run `/simplify` over the milestone's diff (store migration, core verbs, GraphQL adapter): reuse, simplification, efficiency — behavior-preserving only. Special attention to the m9-mint vs CreateWorkspace shared path (one write path) and the teardown sequencing in DeleteWorkspace.

## Context

- Changed areas: `lego/backend/internal/{store,core,api,secrets,authz}`.

## Steps

1. `/simplify` scoped to the milestone's changed files.
2. Apply behavior-preserving findings; re-run backend tests.

## Files

- `lego/backend/internal/`

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly declined with reasons
- [ ] `go test ./...` green in `lego/backend/`

## Out of scope

- Bug hunting (`/code-review`'s job).
