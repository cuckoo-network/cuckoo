---
id: w6/m1/t001
title: Capture live Render workspace settings/rename/delete semantics → docs/render-artifacts/
worker: worker6
status: done
estimate: 30m
depends_on: []
---

## Objective

Fill the one gap the deep-research pass could not verify (report §Open questions 1): what Render's workspace settings surface actually does — rename, delete (confirmation guard wording, what happens to services/datastores, any grace period), and whether ownership transfer exists. Capture it live from dashboard.render.com so t004's delete semantics clone reality instead of guessing.

## Context

- `w6/RESEARCH-workspaces.md` — creation flow and switcher are verified; settings/delete are not (dashboard-only, behind login).
- Precedent: w5/m7 and w3/m3 captured Render pages live via the Playwright MCP; shapes went to `docs/render-artifacts/`.
- Screenshots go to `.playwright-mcp/` (bare filenames, per CLAUDE.md).

## Steps

1. Log into dashboard.render.com (Playwright MCP), open a workspace's Settings.
2. Record the settings page fields; exercise rename; open (without confirming) the delete flow — capture guard text, stated consequences for services/databases, and any transfer-ownership option.
3. Note the workspace dropdown/creation flow shapes in passing (plan cards, payment step skippability for Hobby) to confirm report findings 2–3.
4. Write the captured shapes/wording to `docs/render-artifacts/workspace-lifecycle.md` (+ screenshots in `.playwright-mcp/`).

## Files

- `docs/render-artifacts/workspace-lifecycle.md` (new)

## Acceptance criteria

- [ ] Delete-flow guard semantics (confirmation mechanism + stated resource consequences) documented verbatim
- [ ] Rename behavior and any transfer/ownership option documented (or its absence stated)
- [ ] Report findings 2–3 (creation flow, Hobby payment-step skippability) confirmed or corrected in the artifact

## Out of scope

- Building anything — capture only.
- Members/roles pages (w4/m12 captures its own surface).
