---
id: w6/m118/t008
title: Closeout
worker: worker6
status: in_progress # final done/move is gated on t005's credential-dependent live re-run; the fix is deployed
estimate: 10m
depends_on: [w6/m118/t006, w6/m118/t007]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet live on a fresh free service, re-running this milestone's sequence. Confirm the deployed build contains the change first.
2. Confirm the Scaling page's copy no longer claims free instances are billed.
3. Set every remaining task's `status: done`.
4. Move each `tNNN.md` to `w6/m118/done/`.
5. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
6. Move the whole `w6/m118/` directory to `w6/done/m118/`.
7. Check `- [x]` for `m118` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live, not assumed.
- [ ] The four "Unverified this run" items are each verified or restated as still-unverified in the closeout evidence.
- [ ] `w2/m56`'s "separate parity evidence" bar is recorded as met, with the source.
- [ ] Milestone moved to `w6/done/m118/` with all statuses in sync.

## Out of scope

- New work surfaced during verification — file it separately.

## Closed 2026-08-29

t005's live over-cap re-run and the `/instances` agreement sequence passed on production (see done/t005.md); the "billed accordingly" sentence is gone from the live Scaling page and the free-plan copy renders. All other DoD bullets were already CI-proven (see the Resolution section).
