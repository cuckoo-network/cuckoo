---
id: w6/m98/t007
title: "Closeout"
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m98/t005, w6/m98/t006]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet in `../README.md` against the live product (or the deployed state, whichever the deploy pipeline allows at close time).
2. Set every remaining task's `status: done`.
3. Move each `tNNN.md` to `w6/m98/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
5. Move the whole `w6/m98/` directory to `w6/done/m98/`.
6. Check `- [x]` for `m98` in `w6/README.md`.

## Acceptance criteria

- [x] DoD in `../README.md` verified live, not assumed.
- [x] Milestone moved to `w6/done/m98/` with all statuses in sync.

## Closeout evidence (2026-08-26, 19th `/qa-find-bugs` run)

The fix (`6dec02f9`, "fix(billing): report the gross current-period charge instead of a credit-netted zero") is a confirmed ancestor of `8bbc87639b24` — the commit `deploy.yml` most recently built and shipped successfully (`gh run view 33007338010`: `build-and-deploy` succeeded, no supersession annotation) and the commit the production pin (`991530f9`, `chore(deploy): pin platform images to 8bbc87639b24`) points at. Live-reprobed the exact repro from the filing, in-page (`fetch('https://api.bex.co/graphql', {credentials:'include'})`) against the same workspace (`tea-d98210cbbpdc73dcrkvg`):

```
query { usage(ownerId: "tea-d98210cbbpdc73dcrkvg") { estimatedCost { totalUsd }
  billing { currentCost { amountUsd } credits { availableUsd grants { name remainingUsd } } invoices { amountUsd } } } }
=> { "estimatedCost": { "totalUsd": "78.40" },
     "billing": { "currentCost": { "amountUsd": "29.47" },
                  "credits": { "availableUsd": "1000.00", "grants": [{ "name": "superadmin's privilege", "remainingUsd": "1000.00" }] },
                  "invoices": [] } }
```

Dashboard `/billing` agrees: "Total month to date $29.47 USD", "Amount due after credits $0.00 USD", "Credits applied −$29.47 → amount due $0.00" — the exact defect (a nonzero charge tree reading "$0.00 month to date") is gone. This settles t007's own step 2 disambiguation: `creditsAppliedUsd`-equivalent (`Credits applied`) is nonzero ($29.47), so the live cause was **(a)** — credit-grant consumption netting the invoice preview to zero — matching t001's spike finding exactly.

**One DoD bullet does not hold as literally written, for a reason outside this milestone's fix.** The DoD's exact-match bullet ("'Total month to date' … equals the sum of the category tree above it, modulo sub-cent rounding") does not hold right now: the charge tree sums to $78.35 (Services $23.63 + Postgres $27.95 + Key Value $23.02 + Sandboxes $3.75) against Stripe's `currentCost.amountUsd` of $29.47 — a $48.88 gap, far past sub-cent. Traced this to the codebase's own documented mechanism rather than a residual bug: `docs/ADR040-billing-metronome.md`'s seal-then-emit outbox (`BEX_STRIPE_SEAL_HOURS`, hourly-retry meter-event emitter) means Stripe's rated `currentCost` only reflects usage that has cleared the seal window and actually been exported as a Stripe meter event, while `estimatedCost.totalUsd` is bex's own real-time `pricing.yaml` estimate computed directly from `usage_hourly` with no such lag (ADR030). The Charges card's own copy ("Accrued so far this period, **as rated by Stripe**") already discloses this qualifier. This is expected skew inherent to the basic-Billing-Meters architecture ADR071 chose, not a defect this milestone's fix could or should close — the milestone's actual defect (a real nonzero charge collapsing to a literal "$0.00") is fixed, live-verified above. Recorded here rather than re-opened as a new bug so a future hunt does not mistake the estimate/rated gap for a regression of this fix.

`finalizedInvoices()` (`read.go:288`) still has no live finalized invoice on this workspace to observe (`billing.invoices: []`, unchanged from filing) — covered by the same code path and by `t006`'s test fixture per the DoD's own allowance; not independently live-verified, as anticipated.
