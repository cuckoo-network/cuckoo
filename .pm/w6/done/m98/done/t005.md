---
id: w6/m98/t005
title: "Simplify"
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m98/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Touched files: `lego/backend/internal/billing/read.go`, `lego/backend/internal/usage/graphql.go` (maybe), `dashboard/src/features/usage/components/charges-card.tsx`, `dashboard/src/features/usage/components/billing-summary-cards.tsx`, `dashboard/src/features/usage/hooks/use-usage.ts`, `dashboard/src/features/usage/api/usage.graphql`.

## Steps

1. Invoke `/simplify` scoped to this milestone's diff.
2. Apply any behavior-preserving cleanups it finds.

## Files

- As touched by t002/t003.

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly declined with reason.

## Out of scope

- Behavior changes — this task is quality-only.

## Outcome

Reviewed this milestone's diff for reuse/simplification; one cleanup applied and one declined.

- **Applied:** `invoiceFigures` first validated currency three times through a slice-of-pointers loop. Split into a currency check done once plus a pure `usdString(minor)` formatter, which `normalizedStripeUSD` (still used by the two credit reads) now wraps. Same behavior, one obvious read.
- **Declined:** collapsing `ChargesCard`'s `rated`/`due`/`dueAfterCredit` into one expression — each names a distinct condition the card branches on (Stripe rated anything; a due figure exists; the two differ), and inlining them would hide exactly the distinction the bug turned on.

