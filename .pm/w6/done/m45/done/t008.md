---
id: w6/m45/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m45/t007]
---

## Objective

When t001-t007 are all complete and the milestone's Definition of Done is actually met (live-verified, not just code-complete), close the milestone: set every remaining task's `status: done`, move each `tNNN.md` to `w6/m45/done/`, mark every row `— **DONE**` and set `**Status:** done` in `w6/m45/README.md`, move the whole `w6/m45/` directory to `w6/done/m45/`, and check `- [x]` in `w6/README.md`.

## Acceptance criteria

- [~] Milestone DoD verified as deeply as an uncommitted change allows — t001 against the real digest-pinned toolchain images with Docker (build + run + curl, both runtimes, red pre-fix), t002/t003 through their real service objects and all three API surfaces. The three "on `dashboard.bex.co`" probes are **owed by whoever runs `/ship`** and are enumerated in the milestone README § What the shipper still owes. Closed unshipped, the same way `w6/done/m44` was.
- [x] All tasks moved to `done/`, statuses synced across task frontmatter, milestone README, and workstream README.
- [x] `w6/m45/` moved to `w6/done/m45/`.

## Why closed unshipped

The local mock cluster is up but 22 days stale and `bex-api` sits in
`ImagePullBackOff`, so there is no local bex-api to drive the env-var and
Cancel/Rollback journeys against; production requires a deploy, which is
`/ship`-gated by repo rule. Rather than leave the milestone open on a gate that
only the user can pass, the outstanding live probes are written down explicitly
in the milestone README.
