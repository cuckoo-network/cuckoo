---
id: w6/m45/t007
title: Test coverage for the fixed behaviors
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m45/t005]
---

## Objective

Add meaningful regression tests for t001-t004's fixes. Tests must assert real behavior and failure modes, proven red on the pre-fix code — not trivial or tautological.

## Steps

1. t001: a test (unit and/or integration) asserting native builds keep `PATH` for every runtime in `nativeRuntimeImages`, proven red on the pre-fix `-lc` wrapper.
2. t002: a test asserting `envVars(serviceId)` includes a creation-time literal var, proven red on the pre-fix code.
3. t003: a test asserting Cancel/Rollback's `refetchQueries` includes `"Server"`, proven red on the pre-fix code.
4. t004: tests for whichever of the three sub-findings were fixed.

## Files

- `lego/operator/internal/build/native_test.go`
- `lego/backend/internal/apps/*_test.go`
- `dashboard/src/features/deploys/components/__tests__/deploy-actions.test.tsx`
- Wherever t004's fixes land.

## Acceptance criteria

- [x] Each fixed behavior has a regression test proven red on the pre-fix code.
- [x] No trivial/tautological/snapshot-everything tests.

## Outcome — each test, and the failure it actually saw

| fix | test | proven red on pre-fix code |
| --- | --- | --- |
| t001 | `native_test.go` `TestNativeDockerfileKeepsToolchainPATH` — every runtime in `nativeRuntimeImages`, RUN **and** CMD | `rust/go/elixir/node/python/ruby: login shell resets PATH to /usr/local/sbin:…, dropping the toolchain PATH` |
| t001 | real Docker build of the generated Dockerfile (not a text assertion) | `go: command not found` / `cargo: command not found`, exit 127 — the production failure exactly |
| t002 | `create_payload_test.go` `TestCreateTimeSecretsThreadThroughEverySurface` (REST + GraphQL + MCP) | `seed call = service "" env map[string]string(nil)` on all three |
| t002 | `create_payload_test.go` `TestCreationTimeEnvVarsLeaveReferencesAndUnprojectableNamesOnTheSpec` | `seeded env = map[string]string(nil), want only the projectable literal` |
| t002 | `files_test.go` `TestPrepareCreateEnvVarsSeedsStoreAndFirstAppRevision` — seed → commit → list/edit/get/delete round trip | n/a: the seeding entry point did not exist pre-fix |
| t002 | `files_test.go` `TestPrepareCreateEnvVarsRejectsBadKeyWithoutWriting` — value never in the error, store untouched | n/a: new entry point |
| t002 | `files_test.go` `TestAbortCreateSecretsRemovesEnvProjectionAndStore` | n/a: new entry point |
| t002 (guard) | `TestCreationTimeEnvVarsStayOnSpecWithoutASecretsStore` — store-less create unchanged | n/a: pins the no-regression half |
| t003 | `deploy-actions.test.tsx` "refetches the service header's own query after cancel and rollback" | `expected [ 'Deploys', 'ServiceEvents' ] to deeply equal [ Array(3) ]` |
| t004 | `deploy-header.test.tsx` "separates the commit message from the commit date with real text" | `expected 'abc1234 fix: Docker build contextJuly…' to match /Docker build context\s+·\s+\S/` |
| t004 (item 1) | already covered by `map.test.ts:29` + `use-live-logs.test.ts:138` (commit `6e35aa9a`) | pre-existing, red without that fix |
| t005 | `files_test.go` `TestCreationTimeEnvVarIsVisibleOnEverySecretsReadSurface` — REST, GraphQL, MCP | n/a: reads a store only the new seeder can fill |

Suites: `make test` (operator) ✅, `cd lego/backend && go test ./...` ✅,
`make lint` (4 modules, 0 issues) ✅, `dashboard/yarn lint` ✅,
`dashboard/yarn test` 2521 passed ✅.

## Out of scope

- Coverage for code t001-t004 didn't touch.
