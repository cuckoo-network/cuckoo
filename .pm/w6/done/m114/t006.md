---
id: w6/m114/t006
title: Test coverage
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m114/t004]
---

## Objective

Cover the round trip, not just the field — a test asserting one string equals another would have passed for `w4/m19` and still let this ship.

## Context

- The strongest available assertion is the loop itself: generate → validate → `valid: true`. That is what failed live and it is cheap to express in-process, since both halves live in `internal/apps`.
- `blueprint_generate_test.go` exists (it sets `Runtime: "docker"` at `:52`), so there is a fixture pattern to extend.
- Databases and Key Value are correct today **because** they read `Spec.Name` — they are the regression surface, not the control to skip.

## Steps

1. Add a generate → `validateBlueprint` round-trip test asserting `valid: true` with an empty `errors`, per service kind t002 enumerated.
2. Assert the exported name equals the public name for a tenant-prefixed App, and equals `a.Name` for a legacy bare-named App.
3. Assert no `tea-` prefix appears anywhere in the generated manifest.
4. Add a regression test for databases and key values, whose output must stay byte-identical.
5. `cd lego/backend && go test ./...`.

## Files

- `lego/backend/internal/apps/blueprint_generate_test.go`

## Acceptance criteria

- [ ] A generate → validate round-trip test exists and covers each kind from t002.
- [ ] Both the prefixed and the legacy-bare name cases are covered.
- [ ] A no-`tea-`-anywhere assertion exists over the whole manifest.
- [ ] Datastore output has a byte-identical regression test.
- [ ] `go test ./...` passes.

## Out of scope

- Coverage of code this milestone did not change.
