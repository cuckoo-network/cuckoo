---
id: w6/m2/t007
title: Simplify — /simplify over the code this milestone changed
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m2/t006]
---

## Objective

Run `/simplify` over the milestone's diff (REST handlers, MCP tool registry/session context, ownerId sweep): reuse/simplification/efficiency, behavior-preserving. Watch for the three adapters re-deriving workspace lists separately instead of sharing one core verb.

## Context

- Changed areas: `lego/backend/internal/{api,core}` plus feature packages touched by the ownerId sweep.

## Steps

1. `/simplify` scoped to the milestone's changed files.
2. Apply findings; re-run backend tests.

## Files

- `lego/backend/internal/`

## Acceptance criteria

- [x] Findings applied or explicitly declined with reasons (4 review agents: reuse/simplification/efficiency/altitude; applied MatchingLabels pushdown, dead switch-case cleanup, dead assertion removal, WorkspaceSelections read/write split; declined moving `resolveOwnerID` into `core` — would pull the MCP SDK into the transport-free kernel)
- [x] `go test ./...` green in `lego/backend/` (+ `golangci-lint run`: 0 issues)

## Out of scope

- Bug hunting (`/code-review`'s job).
