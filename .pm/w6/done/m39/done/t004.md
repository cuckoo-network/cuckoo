---
id: w6/m39/t004
title: Simplify the code this milestone changed
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m39/t003]
---

## Objective

Run `/simplify` over the code this milestone touched — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- Changed surface: `lego/backend/internal/store/namespaces.go` (identity constant, `managedLabels`, `pruneOrphans`), the reconciler's construction site, `.env.example`, `CLAUDE.md`, and the `dev-N` harness scripts.
- Watch specifically for: the identity-comparison logic duplicated between the list selector and the per-item filter, and any label-map construction that now has two near-identical builders.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the clearly-correct findings; record anything declined and why.

## Files

- the diff produced by `t001`–`t003`

## Acceptance criteria

- [ ] `/simplify` run over this milestone's diff; findings applied or explicitly declined with a reason.
- [ ] Behavior unchanged — `t005`'s tests and `cd lego/backend && go test ./...` stay green.
- [ ] `make lint-backend` (from `lego/operator/`) clean.

## Out of scope

- Bug hunting (that is `/code-review`) and any behavior change.
