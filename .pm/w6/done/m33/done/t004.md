---
id: w6/m33/t004
title: Field-parity fix per t001's evidence (REST ownerId/createdAt or record as bex extensions)
worker: worker6
status: done
estimate: 25m
depends_on: [w6/m33/t001]
---

## Objective

Resolve the REST-vs-GraphQL field drift on the environment object: if Render's schema carries `ownerId`/`createdAt`, add them to `renderEnvironment`; if not, record the GraphQL fields as bex extensions in ADR032/ADR018 — either way the drift stops being undocumented.

## Context

- GraphQL resolves both (`lego/backend/internal/environments/graphql.go:36-37`, `service.go:172-173`); REST omits them (`rest.go:51-64`).

## Steps

1. Apply t001's evidence; change the REST render or write the extension note.

## Files

- `lego/backend/internal/environments/rest.go` and/or `docs/ADR032-environments.md` + `docs/ADR018-render-parity.md`

## Acceptance criteria

- [x] REST and GraphQL field sets either match or the difference is documented with spec evidence

## Evidence

The captured Render schema has neither `ownerId` nor timestamps. REST remains exact; ADR018, ADR032, and the capture document record GraphQL `ownerId`/`createdAt` as bex extensions, with a regression test that guards both sides.

## Out of scope

- Other resources' field drift
