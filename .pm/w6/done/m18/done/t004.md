---
id: w6/m18/t004
title: "Fix use-current-workspace.ts; thread currentWorkspaceId into Usage + API-keys pages"
worker: worker6
status: done
estimate: 40m
depends_on: []
---

## Objective

Fix the dashboard bug where `use-current-workspace.ts` returns `workspaces[0]` (the account's first workspace) instead of the switcher's actual current selection, and wire the Usage and API-keys pages to pass `currentWorkspaceId` as `ownerId` on their queries/mutations.

## Context

- `dashboard/src/features/team/hooks/use-current-workspace.ts` and `team-panel.tsx` currently read `workspaces[0]`.
- The same class of bug was fixed for the audit log in `w6/m14/t006` — same fix shape applies here.
- Usage page: `dashboard/src/features/usage/` (or equivalent). API-keys page: `dashboard/src/features/api-keys/` (or equivalent) — confirm exact paths during implementation.

## Steps

1. Fix `use-current-workspace.ts` to read the workspace switcher's actual selection (mirror the m14/t006 fix), and update `team-panel.tsx`'s usage of it.
2. Thread `currentWorkspaceId` as `ownerId` into the Usage page's query and the API-keys page's list/create calls.

## Files

- `dashboard/src/features/team/hooks/use-current-workspace.ts`
- `dashboard/src/features/team/components/team-panel.tsx`
- Usage + API-keys page/hook files (confirm exact paths)

## Acceptance criteria

- [ ] Switching workspaces in the switcher changes what the Usage and API-keys pages show, without a full reload.
- [ ] `team-panel.tsx` no longer references `workspaces[0]` directly.

## Out of scope

- Backend surfaces (t001–t003).
