---
id: w6/m18/t007
title: "Simplify — /simplify over the code this milestone changed"
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m18/t006]
---

## Objective

Run `/simplify` over every file this milestone touched and apply behavior-preserving cleanups.

## Context

- Scope: `lego/backend/internal/{usage,apikeys,github}/*`, `dashboard/src/features/team/*`, plus whichever Usage/API-keys page files t004 touched.

## Steps

1. Run `/simplify` over the diff.
2. Apply and verify fixes (`go test ./...`, `go vet`, `yarn test`, `yarn lint`).

## Files

- Whatever `/simplify` flags.

## Acceptance criteria

- [ ] `/simplify` run completed; findings applied or explicitly declined with reason.
- [ ] Backend + dashboard test suites green after changes.

## Out of scope

- New feature work.
