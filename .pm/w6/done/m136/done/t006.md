---
id: w6/m136/t006
title: "Render parity: capability contract aligned across REST/GraphQL/MCP"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m136/t004, w6/m136/t005]
---

## Objective

Verify the extended capability contract is consistent wherever it is exposed (REST, GraphQL, MCP — same fields, semantics, error shapes) and record the deliberate divergences from Render's role model rather than silently drifting.

## Context

- ADR087 requirement 5: keep existing callers compatible and the backend's REST/GraphQL/MCP semantics aligned wherever the capability contract is exposed.
- Render's role docs (captured in ADR087 §External evidence): bex deliberately differs on Billing's general supervision reads and Contributor's inability to roll back. These are documented divergences, not bugs.
- `docs/ADR018-render-parity.md` is the parity ledger.

## Steps

1. Enumerate every surface exposing capabilities; align field names/semantics or document why a surface omits the extension.
2. Compare the shipped contract against Render's documented role behavior; add/refresh the ADR018 row noting the two deliberate divergences.
3. File follow-up notes for any unintended drift found.

## Files

- `lego/backend/internal/members/graphql.go` + REST/MCP adapters if present
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] One documented contract; no surface returns a different tri-state/reason vocabulary.
- [ ] ADR018 records the Billing-reads and Contributor-rollback divergences with evidence links.

## Out of scope

- Changing bex authorization to match Render (ADR087 explicitly rejects cloning Render's matrix).
