---
id: w6/m136/t008
title: Test coverage
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m136/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real failure modes, not tautologies.

## Context

- Highest-value scenarios (ADR087 acceptance rows): checker outage → `unavailable` fail-closed; missing OAuth scope vs FGA denial reasons; foreign-workspace resource rejection; precondition projection equals execution-path enforcement; fresh evaluation observes a just-revoked grant.

## Steps

1. Cover each scenario at the members/core seam with fakes; add one real-Postgres+OpenFGA integration path if the harness is cheap to extend.
2. Prove new tests red against the pre-milestone behavior where feasible.

## Acceptance criteria

- [ ] Every DoD clause has at least one test that fails when its behavior regresses.
- [ ] `cd lego/backend && go test ./...` green; `make lint` clean.
