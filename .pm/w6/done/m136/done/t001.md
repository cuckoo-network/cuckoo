---
id: w6/m136/t001
title: Pin the ADR087 action matrix with backend regression tests
worker: worker6
status: done
estimate: 45m
depends_on: []
---

## Objective

Write backend regression tests that pin the relations/classifications ADR087's matrices depend on, so any future drift in an action's gate is caught before it silently invalidates the mobile view policy.

## Context

- `lego/backend/internal/deploys/service.go` — Rollback requires `RelCanCreate` (~:765); Cancel `RelCanOperate` (~:657); Trigger uses `core.LifecycleOrCreate` (~:414) — `can_operate` for a bare redeploy, escalating to `can_create` when an image/commit override is supplied.
- `lego/backend/internal/agentsessions/service.go` — List (~:705), Transcript (~:859), Get (~:922) all require `can_operate`.
- `lego/backend/internal/api/scope_matrix_overrides.go:78` — `Query.databaseProcesses` is `OpClassSensitive` (also REST `/processes` and MCP `list_postgres_processes`).
- `deploy/gitops/authz/model.fga:39-58` — five roles; `can_view` all five; `can_view_logs`/`can_operate` contributor/developer/admin; `can_create` developer/admin; `can_manage_billing` billing/admin.
- Existing guard suites to extend rather than duplicate: `TestAuthzGuardsEveryVerb`, `TestEveryTargetedVerbIsNamedOrExcused` (see w6/m17).

## Steps

1. Add a table-driven test asserting each ADR087 matrix row's governing relation/scope class against the actual gate in code (reuse the AST/constant-propagation sweep from the existing guard suites where possible).
2. Assert the FGA model's role→relation grants for the five roles (parse `model.fga` or assert via the test OpenFGA store).
3. Prove the test red by locally flipping one relation, then restore.

## Files

- `lego/backend/internal/api/` (new or extended matrix test)
- `deploy/gitops/authz/model.fga` (read-only reference)

## Acceptance criteria

- [ ] A single test failure names the drifted verb/relation when any pinned gate changes.
- [ ] Trigger's bare-vs-override escalation (`LifecycleOrCreate`) is asserted explicitly.
- [ ] Backend `go test ./...` green.

## Out of scope

- Changing any gate (this task records reality; m138 consumes it).
