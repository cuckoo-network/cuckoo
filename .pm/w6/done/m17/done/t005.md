---
id: w6/m17/t005
title: "Live verification: the bob/tea-team/tea-mine scenario"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m17/t004]
---

## Objective

Reproduce the exact scenario `w6/013` diagnosed end-to-end against a running backend and confirm it now succeeds.

## Steps

1. Create two workspaces: `tea-team` (invite a user as `viewer`) and let that same user's personal workspace `tea-mine` exist with them as `admin`.
2. Create a service `mine-web` in `tea-mine`.
3. As that user (whose default workspace is now `tea-team`, the older membership), call restart on `mine-web`.
4. Confirm it succeeds (previously would have 403'd).

## Files

- none (live verification)

## Acceptance criteria

- [ ] The scenario succeeds live, matching the fix's intent.
