---
id: w6/m17/t008
title: "Test coverage: final gap sweep"
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m17/t006]
---

## Objective

t004 already added the primary regression tests (existing suites + the `bob`/`tea-team`/`tea-mine` reproduction). This is a final sweep for any gap: confirm coverage on `AuthorizeApp`'s 403-before-404 edge case and the `List(ownerID)` fold from t003.

## Acceptance criteria

- [ ] `AuthorizeApp`'s fetch-then-authorize-fallback path (resource genuinely doesn't exist) has a direct test.
- [ ] `List(ownerID)`'s membership-checked behavior has a direct test.
