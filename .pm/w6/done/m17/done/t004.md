---
id: w6/m17/t004
title: "Regression tests: existing suites stay green + reproduction case"
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m17/t002, w6/m17/t003]
---

## Objective

Confirm `TestAuthzGuardsEveryVerb` and `TestEveryTargetedVerbIsNamedOrExcused` stay green after the migration, and add a permanent regression test for the exact scenario `w6/013` diagnosed.

## Context

- `TestAuthzGuardsEveryVerb` — the 403-before-404 sweep against an empty fake client.
- `TestEveryTargetedVerbIsNamedOrExcused` — keys off `AuthorizeTarget` usage; the events vocabulary must follow the new entry point.
- The reproduction scenario from `w6/013`: `bob`'s default workspace is `tea-team` (invited `viewer`), his own workspace is `tea-mine` (admin), and a service `mine-web` lives in `tea-mine`. Before the fix, `svc.Restart(bob, "mine-web")` incorrectly returns forbidden (checked against `tea-team`).

## Steps

1. Run both existing suites, fix any fallout from the t002/t003 migration.
2. Add a new test reproducing the `bob`/`tea-team`/`tea-mine` scenario, asserting it now succeeds.

## Files

- wherever the authz test suites live (`lego/backend/internal/core/` or the relevant service test files)

## Acceptance criteria

- [ ] Both existing suites pass unchanged.
- [ ] The new reproduction test passes (previously would have failed).
