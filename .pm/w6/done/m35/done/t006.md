---
id: w6/m35/t006
title: Test coverage — each filter narrows; rejected params 400 explicitly
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m35/t004]
---

## Objective

Add meaningful tests for the filter contract: each accepted filter narrows the page (including repeated/comma-separated forms and combinations), pages stay full-length under filtering, and every rejected param returns the explicit named 400.

## Context

- The pre-fix failure mode: filters accepted but silently ignored — a test seeding two groups and filtering by `name` would have caught it.

## Steps

1. Table-driven REST tests per filter and per rejected param.
2. A full-page-under-filter regression (filter before paginate).
3. Surface tests for whatever t003 decided on GraphQL/MCP.

## Files

- `lego/backend/internal/envgroups/*_test.go`

## Acceptance criteria

- [x] Every accepted param has a narrowing test; every rejected param has a 400 test
- [x] Full-page-under-filter regression present

## Evidence

`TestREST_EnvGroupListFilters` covers every array and timestamp dimension plus OR/AND composition. `TestREST_EnvGroupListRejectsInvalidTimestampsByName` checks all four malformed scalar params for HTTP 400, `id=bad_request`, and the parameter name. `TestREST_EnvGroupFiltersBeforePagination` proves a selective query still fills a two-item page. `go build ./...`, `go test ./...`, and `make lint-backend` passed.

## Out of scope

- Trivial/tautological coverage
