---
id: w6/m35/t002
title: Implement the accepted filters in ListEnvGroups + REST
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m35/t001]
---

## Objective

Implement the filters t001 marked "implement" — repeated/comma-separated `name`, repeated `ownerId`, `environmentId`, and the timestamp filters — in the env-groups list path, and return an explicit 400 for any param t001 marked "reject".

## Context

- `lego/backend/internal/envgroups/rest.go:51-57` — current single-`ownerId` list handler.
- `lego/backend/internal/envgroups/service.go` — `ListEnvGroups`; filtering should narrow before pagination so pages stay full (the events/projects lesson).
- `environmentId` implies a membership join — see how environments resolve env-group membership (w2/m40 / w6/m24 machinery).
- Explicit-reject precedent: `environments/rest.go`'s named-unsupported 400.

## Steps

1. Extend the service/store list call with the accepted filter set (filter before paginating).
2. Parse repeated + comma-separated query forms in REST.
3. Reject unsupported params with the Render-shaped error envelope, naming the param.

## Files

- `lego/backend/internal/envgroups/rest.go`
- `lego/backend/internal/envgroups/service.go` (+ store layer as needed)

## Acceptance criteria

- [x] Each accepted filter narrows results; combinations compose (AND across params, OR within a repeated param)
- [x] Pages stay full-length under filtering
- [x] Rejected params 400 with the param named, Render error envelope

## Evidence

`EnvGroupListFilter` filters metadata before secret-free views and paging. `ownerId` alternatives each traverse `listScopedMeta`, preserving membership checks; name, Environment, and timestamp dimensions narrow the union. All documented filters are supported, so the rejection path applies only to malformed timestamps, which return `bad_request` and name the parameter.

## Out of scope

- GraphQL/MCP (t003); tests beyond quick self-checks (t006)
