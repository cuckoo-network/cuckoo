---
id: w6/m35/t003
title: GraphQL/MCP list dialect decision + wiring or documented divergence
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m35/t002]
---

## Objective

Decide whether the new filters extend to the GraphQL/MCP env-group lists or stay REST-only as a documented bex-dialect divergence, following the environments-list precedent, and implement whichever is decided.

## Context

- The environments list kept Render query-param filters REST-only (GraphQL/MCP take `projectId` only) as deliberate dialect scope — w6/m33.
- `lego/backend/internal/envgroups/graphql.go`, `mcp.go`.

## Steps

1. Check what the GraphQL/MCP env-group lists accept today.
2. Apply the environments precedent unless env-group consumers (dashboard, MCP agents) demonstrably need a filter.
3. Wire or document; update MCP tool descriptions if behavior changes.

## Files

- `lego/backend/internal/envgroups/graphql.go`, `mcp.go`

## Acceptance criteria

- [x] Decision recorded with rationale; wiring or documentation matches it
- [x] MCP tool descriptions truthful after the change

## Decision

REST alone receives Render's filters. GraphQL `envGroups` and MCP `list_env_groups` are bex-native surfaces and retain their existing single-owner plus optional paging dialect, exactly like Environments. Code comments, the MCP description, ADR018, and the capture artifact state this explicitly.

## Out of scope

- Dashboard list-filter UI
