---
id: w6/m35/t004
title: Render parity — filter semantics/error shapes vs Render across touched surfaces
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m35/t003]
---

## Objective

Verify the shipped filter behavior matches Render's documented env-groups list semantics (param forms, combination behavior, error shapes) across every surface this milestone touched; flag drift as follow-up notes rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md`; the pinned OpenAPI snapshot from t001.

## Steps

1. Compare each implemented filter's behavior against the spec.
2. Verify error shapes match the Render envelope on all touched surfaces.
3. File any drift.

## Files

- `lego/backend/internal/envgroups/` (verification)

## Acceptance criteria

- [x] Per-param parity confirmed or drift filed

## Evidence

All eight documented filters match the official parameter types and strict boundary meanings. Array alternatives accept both client encodings; invalid timestamps use the shared Render error envelope. No REST drift remains. The deliberate GraphQL/MCP dialect scope is documented rather than presented as Render behavior.

## Out of scope

- New filter params beyond the spec
