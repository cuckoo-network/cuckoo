---
id: w6/m14/t010
title: "Test coverage: resolution order, membership-checked override, wrong-workspace 403 regressions"
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m14/t008]
---

## Objective

Meaningful tests for the milestone's real failure modes, beyond what t001–t006 added inline: the deterministic resolution order under concurrent memberships (real-Postgres), the non-member `ownerId` 403 across all three surfaces (parity-style table test), and the specific pre-fix regression — an owner reading their workspace-B app while implicit resolution picks A must not 403 (`core.Base.GetApp` gate). No tautological or snapshot-everything tests.

## Context

- Standing closing task (`.claude/skills/pm/SKILL.md`).
- Existing patterns: `TestAuthzGuardsEveryVerb`, `store_pg_test.go`, the surface-parity tests (`TestAuditSurfaceParity`).

## Steps

1. Gap-check t001–t006's inline tests against the DoD; add what's missing at the right layer.
2. Backend `go test ./...` + dashboard `yarn vitest run` green.

## Files

- lego/backend/internal/core/, store/, apps/ test files; dashboard test files as needed

## Acceptance criteria

- [ ] The wrong-workspace-403 regression has a dedicated failing-before test.
- [ ] Non-member `ownerId` rejection covered on REST, GraphQL, and MCP.

## Out of scope

- E2E kind-cluster tests (`make test-e2e`).
