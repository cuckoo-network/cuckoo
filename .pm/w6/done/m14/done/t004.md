---
id: w6/m14/t004
title: "GraphQL creates gain ownerId; MCP creates resolve via session select_workspace"
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m14/t002]
---

## Objective

Fan t003's contract to the other two surfaces (one verb, three thin adapters): GraphQL `createService` and sibling create mutations (databases, keyvalues) gain an optional `ownerId` argument feeding the same t002 override; MCP create tools (`create_web_service`, the postgres/keyvalue creates) resolve through the session's `select_workspace` selection — the same precedence `list_services` already uses (`apps/mcp.go:595` `resolveOwnerID`: explicit > session selection > implicit default).

## Context

- `lego/backend/internal/apps/graphql.go` (createService), postgres/keyvalue graphql fragments; `dashboard/src/graphql/definitions.ts` sync follows in t005.
- `core.WorkspaceSelections` (core/workspaceselection.go) — the MCP read side already exists; creates just need to consult it.
- Render's own MCP scopes tool calls by the stateful selection (RESEARCH-workspaces.md, MCP paragraph).

## Steps

1. Add `ownerId` args to the create mutations; set the context override when present.
2. In MCP create handlers, apply `resolveOwnerID`-style precedence before invoking the verb (hoist the helper if it needs sharing across features via `core`).
3. Surface-parity tests: same inputs ⇒ same workspace stamping and same error shapes across REST/GraphQL/MCP.

## Files

- lego/backend/internal/apps/graphql.go, apps/mcp.go
- lego/backend/internal/postgres/graphql.go + mcp.go, keyvalue/graphql.go + mcp.go
- lego/backend/internal/core/ (shared precedence helper, if hoisted)

## Acceptance criteria

- [ ] GraphQL create with member `ownerId` lands in that workspace; non-member errors (tests).
- [ ] MCP create after `select_workspace(B)` lands in B (test).

## Out of scope

- New MCP tools — only existing creates learn the selection.
