---
id: w6/m14/t006
title: "Fix the /settings audit-log stale-workspace bug (m11 t004 residual)"
worker: worker6
status: done
estimate: 30m
depends_on: []
---

## Objective

Root-cause and fix the bug m11 observed live: the Settings → Security & Compliance → Audit Log table showed the account's *original* auto-provisioned workspace in every row even while switched into another workspace (held after a hard reload). `use-audit-log.ts:69` does pass a `workspaceId` — find where that value comes from and align it with the switcher's `currentWorkspaceId` source.

## Context

- `dashboard/src/features/audit/hooks/use-audit-log.ts` (`ownerId: workspaceId ?? ""`), `audit/api/audit.graphql`.
- `w6/done/m11/README.md` t004 evidence — the live observation, workspace ids included.
- Compare with `use-services.ts`'s working pattern (skip until the switcher's id resolves).

## Steps

1. Trace `workspaceId`'s provenance in the audit hook/route; identify why it pins to the default workspace (stale cache, wrong provider, or account-level query).
2. Fix to read the switcher's current workspace; ensure switching refetches.
3. Regression test: hook re-queries with the new `ownerId` after a workspace switch.

## Files

- dashboard/src/features/audit/hooks/use-audit-log.ts (+ its callers/tests)

## Acceptance criteria

- [x] Switching workspaces changes the audit query's `ownerId` (test).
- [x] Root cause written down in the fix's commit-adjacent notes (milestone README).

## Out of scope

- Backend audit changes — `auditLogs(ownerId…)` already scopes correctly server-side.
