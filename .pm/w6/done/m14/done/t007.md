---
id: w6/m14/t007
title: "Two-workspace end-to-end check: creates land where named; no spurious owner 403s; audit follows switcher"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m14/t003, w6/m14/t005, w6/m14/t006]
---

## Objective

Prove the milestone's behavior end-to-end with one identity holding two workspaces: a service created while targeting workspace B lands in B and stays fully accessible (the pre-fix failure mode was `GetApp`'s tenant gate 403ing the owner when the join picked A); the audit-log card follows the switcher.

## Context

- Live cluster if available (local CAPD stack per `w6/done/m11/README.md` t001, or real prod per the m13 precedent); otherwise the offline dev stub (`dashboard/scripts/local-bex.mjs`) for the dashboard legs + real-Postgres backend tests for the resolution legs — record which, per the m10/m11 documented-not-silent convention.

## Steps

1. Seed one identity with two workspaces (A older, B newer).
2. REST: `POST /v1/services` with `ownerId: B` ⇒ App labeled B; read logs/env-vars for it while implicit resolution would pick A ⇒ no 403.
3. Dashboard: switched into B, run the wizard ⇒ service appears in B's list, absent from A's.
4. Switch A↔B on `/settings` ⇒ audit rows change workspace.
5. Record evidence (transcripts/screenshots) in the milestone README; clean up scratch resources.

## Files

- .pm/w6/m14/README.md (evidence)

## Acceptance criteria

- [ ] All four legs recorded with evidence; any leg not live-verifiable is explicitly noted with its offline substitute.

## Out of scope

- Re-verifying single-workspace behavior (covered by existing suites).
