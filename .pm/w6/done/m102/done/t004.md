---
id: w6/m102/t004
title: Render parity — confirm no REST/GraphQL/MCP wire-shape or UI-copy change, rendering-only fix
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m102/t003]
---

## Objective

Confirm this milestone is a pure client/SSR-rendering-timing fix with no effect on any wire contract or visible copy, so it needs no ADR/parity-ledger update.

## Steps

1. Diff the rendered visible text before/after (should be byte-identical — "now"/"2m"/"3h"/etc. unchanged) across the touched components.
2. Confirm no REST/GraphQL/MCP schema or field changed.

## Files

- No code changes; verification only.

## Acceptance criteria

- [x] Visible copy unchanged.
- [x] No wire-contract change.

## Out of scope

- n/a

## Result

**Confirmed: rendering-only, no wire-shape or copy change. No ADR or parity-ledger update needed.**

- **Wire shape:** 22 changed files, every one under `dashboard/src`. Filtering the changed set for `i18n|graphql|\.gql|schema|codegen|openapi|resources` returns nothing, and no `lego/` file is touched. No REST/GraphQL/MCP field was added, removed, or retyped.
- **Copy:** identical. Every migrated site feeds the same `formatRelativeAge`/`formatRelativeUntil` the same value, and every literal fallback (`"—"`, `t("apiKeys.neverUsed")`, `<UnknownValue />`) is preserved verbatim as the wrapper's `fallback`. No i18n resource file changed. The one edge difference is an unparseable-but-non-empty timestamp routing to the fallback rather than the formatter's own `"—"` — same text, except `resource-table`'s updated-at column, which upgrades from `"—"` to `<UnknownValue />`.
- **Markup:** the only structural change is that some bare text and a few `<span>`s became `<time dateTime="…">`. `<time>` is inline like `<span>`, and every `className` was carried across unchanged, so layout and styling are untouched; the change is strictly additive semantically (a machine-readable instant where there was none).
