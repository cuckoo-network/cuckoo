---
id: w6/m115/t005
title: Render parity
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m115/t004]
---

## Objective

Confirm the change is consistent across every surface it exposes — REST, GraphQL and MCP in `lego/backend/`, plus the `dashboard/` UI — and compare against render.com's equivalent behavior, flagging any drift as follow-up rather than diverging silently.

## Context

- This milestone exists because a previous parity task (`w6/m101/t004`) asked "did these surfaces change?" and not "do these surfaces agree?". Ask the second question here.
- Render's contract for comparison: `service.name` is mutable; `slug` is stable; `GET /v1/services/{serviceId}` accepts the id only; there is no mutable/immutable name split, so any field t003 added is a bex extension.
- The webhook surface (`store/webhooks.go:679`) and the push-notification surface (`notifications/push_worker.go:610`, which tails the same feed per `m101/t004`) both read the display label already — they are the fourth and fifth surfaces and must not drift back.

## Steps

1. For one renamed service, read `name` from REST, GraphQL, MCP, a live webhook delivery, and the dashboard, and record all five values side by side.
2. Do the same for the field t003 added.
3. Compare the resulting service object against Render's published service schema; record any field bex now has that Render does not in `docs/ADR018-render-parity.md`.
4. Confirm the dashboard renders identically to before at the heading, breadcrumb, page title, home list and the notification per-service override dropdown — the five places the 39th hunt observed.

## Files

- `docs/ADR018-render-parity.md`
- verification only otherwise

## Acceptance criteria

- [ ] All five surfaces' `name` values recorded side by side for a renamed service, and identical.
- [ ] Any bex-only field is recorded in the parity ledger.
- [ ] Dashboard output confirmed unchanged at the five named locations.

## Out of scope

- New parity work unrelated to service naming.
