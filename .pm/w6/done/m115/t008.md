---
id: w6/m115/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m115/t006, w6/m115/t007]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet in `../README.md` against the live product, re-running the two probes the milestone was filed from. Confirm the deployed build contains the fix before reading the result as a pass.
2. Set every remaining task's `status: done`.
3. Move each `tNNN.md` to `w6/m115/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
5. Move the whole `w6/m115/` directory to `w6/done/m115/`.
6. Check `- [x]` for `m115` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD in `../README.md` verified live, not assumed.
- [ ] The milestone's four "Unverified this run" items are each either verified or restated as still-unverified in the closeout evidence.
- [ ] Milestone moved to `w6/done/m115/` with all statuses in sync.

## Out of scope

- Any new work surfaced during verification — file it separately.
