---
id: w6/m119/t007
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m119/t005]
---

## Objective

Cover every service type against the auto-sleep path and the dashboard eligibility rule.

## Context

- The existing tests could not see this: they exercise auto-sleep on the type that works (`web_service`) and the type that is excluded (`background_worker`). `private_service` sits between them and was never asserted either way — which is exactly how a predicate that excludes one of two Ingress-less types passed review.
- The five types split three ways at `app_controller.go:1700-1710`: `cron_job` and `static_site` return early, `background_worker` is excluded by the flag, and `web_service`/`private_service` both reach `desiredReplicas`. A table test over all five makes the split explicit instead of leaving the middle case unstated.

## Steps

1. Operator envtest, one case per type: web (sleeps), private (per t001), worker (never), cron and static (never reach the path). Red before t002 for the type that moves.
2. Assert the "wakes on next request" message is only stamped on a type that can be woken.
3. Assert the dashboard renders the idle control only for eligible types and never labels an ineligible Hibernated state as Sleeping.
4. `make test` (operator), `cd lego/backend && go test ./...`, `dashboard/yarn test`.

## Files

- `lego/operator/internal/controller/*_test.go`
- `lego/backend/internal/apps/*_test.go`
- `dashboard/src/features/services/components/__tests__/`

## Acceptance criteria

- [x] All five types asserted against the auto-sleep path in one table.
- [x] A test fails if the exclusion reverts to keying on the worker type alone.
- [x] Operator, backend, and dashboard suites are green.

## Out of scope

- Wake-path race tests, which `w6/m94` covers.

## Completion evidence

- Operator unit coverage asserts legacy/web eligible and private/worker/cron/
  static ineligible. Desired replicas explicitly covers private and worker.
- Envtest creates an expired free private service with an activator configured
  and proves its Deployment remains at one replica, its ClusterIP remains, and
  no Ingress is created.
- Dashboard component, policy, status, and route tests cover all ineligible
  types and the paid-web boundary.
- `make test`: green. `dashboard/yarn test`: 377 files, 2,738 tests, green.
- `cd lego/backend && go test ./...`: green. An initial run overlapped unfinished
  concurrent w6/m118 edits and failed four of their assertions; the rerun after
  that changeset settled passes in full. M119 itself changes no backend behavior.
