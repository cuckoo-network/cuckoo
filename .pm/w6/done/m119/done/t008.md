---
id: w6/m119/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m119/t006, w6/m119/t007]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet live, re-running the paired probe (free private service and free worker, both at `idleTTLSeconds: 60`, watched past three minutes). Confirm the deployed build contains the fix first.
2. Confirm the Settings idle control renders only where its sentence is true.
3. Set every remaining task's `status: done`.
4. Move each `tNNN.md` to `w6/m119/done/`.
5. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
6. Move the whole `w6/m119/` directory to `w6/done/m119/`.
7. Check `- [x]` for `m119` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live, not assumed — including the full idle window, which cannot be shortcut.
- [ ] The README's post-triage verification boundary is resolved or restated precisely in the closeout evidence.
- [ ] Milestone moved to `w6/done/m119/` with all statuses in sync.

## Superseded — see the triage update below

## Current closeout boundary (2026-08-28)

- The deploy precondition is satisfied: `9917191f` is an ancestor of `0ca167a8`, the production image pinned by `71fe9660`.
- The remaining paired free-private/free-worker probe and dashboard Settings check require production QA credentials, which are absent from this session. Local unit/envtest coverage is complete but is not substituted for this live acceptance criterion.

## Out of scope

- New work surfaced during verification — file it separately.

## Triage update (2026-08-27, later) — the "uncommitted" premise is stale

The m119 implementation is **committed**: `9917191f` ("fix(services): enforce free-tier lifecycle limits") carries both this milestone's private-service exclusion and `w6/m118`'s instance cap, and it is an ancestor of `HEAD`. `autoSleepEligible` (`lego/operator/internal/controller/app_controller.go:1635-1638`) now reads `web := app.Spec.Type == "" || app.Spec.Type == appv1alpha1.TypeWebService`, so the fix is in the tree and in history. Do not re-argue "uncommitted changes".

The later deployment supersedes that earlier boundary: `9917191f` is an ancestor of the production image pinned by `71fe9660`. The live paired probe (free private service + free worker at `idleTTLSeconds: 60`, watched past three minutes) is now blocked only on production QA credentials, not on writing or deploying code.

## Closed 2026-08-29 — live idle-window probes passed

On production (synthetic QA workspace):

- **Free private service stays running:** `qa-sweep-priv2` (`srv-da96hlj9ui0s73db2b0g`, image-backed, port 80) with `idleTTLSeconds: 60` — Running with 1 replica / 1 instance at 3m38s past Running (05:07:40→05:11:18Z), well past the 60s window.
- **`background_worker` exclusion re-verified:** `qa-sweep-worker` (`srv-da96c64039sc739tkjug`) with `idleTTLSeconds: 60` set 04:56:10Z — still Running with 1 replica / 1 instance at 05:11:18Z (14+ minutes past, exceeding the original 4m48s measurement).
- **Settings control placement:** the idle-timeout control is absent on the private service's Settings page (zero "idle" matches) and present with its sleep/wake sentence on the free web service (the type whose sentence is true).
- Resume-idempotence and the per-type operator envtest bullets were already CI-proven (recorded in the milestone).
