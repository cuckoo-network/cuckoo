---
id: w6/m13/t004
title: "Re-verify workspace delete live: no keyvalues RBAC error; literal OpenBao black-box re-read"
worker: worker6
status: done
estimate: 35m
depends_on: [w6/m13/t001]
---

## Objective

With the `keyvalues` RBAC grant and `apps.WorkspacePurger` deployed, a workspace delete on real prod must complete cleanly (no `keyvalues.app.bex.co is forbidden` alert), and the OpenBao purge must pass the literal black-box test m11 was blocked from running: recreate a same-named service in a second workspace and confirm the deleted workspace's env var does not leak through.

## Context

- `w6/done/m11/README.md` t002/t003 evidence — the RBAC error observed live, and the indirect-evidence PASS for the OpenBao purge with the blocked re-read documented.
- Two scratch identities over `https://api.bex.co` (curl; Kratos session via `X-Session-Token`, not `Bearer`), mirroring m11 t003's method.

## Steps

1. Scratch identity A: create service `m13-purge-test` (`private_service`, `nginx:alpine`), set env var `PURGE_CHECK=<random>`, read it back.
2. Delete workspace A (`sudo delete workspace …`) — expect success with **no** RBAC error and no orphaned App afterwards (the new purger).
3. Scratch identity B: create the same-named service; `GET .../env-vars` must not contain `PURGE_CHECK`.
4. Delete workspace B / clean up all scratch resources; record transcripts in the milestone README.

## Files

- .pm/w6/m13/README.md (evidence)

## Acceptance criteria

- [ ] Workspace delete returns success first try (no `keyvalues` 403 surfaced).
- [ ] Recreated same-named service in workspace B reads back zero env vars from A (transcript recorded).
- [ ] No scratch resource left on prod afterwards.

## Out of scope

- Fixing anything new the run surfaces — file it as follow-up (the m11 pattern), unless it is a one-line regression in this milestone's own scope.
