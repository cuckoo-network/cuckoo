---
id: w6/m13/t005
title: "Live-verify m12 on prod: upgrade unlocks invites; downgrade guard copy matches the capture"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m13/t001]
---

## Objective

m12's change-plan flow was verified against the local dev stub only. Re-drive it on real prod: a Hobby workspace upgraded to Pro must actually unlock member invites, and a guarded downgrade must refuse with the exact copy recorded in `docs/render-artifacts/workspace-plan-change.md`.

## Context

- `w6/done/m12/README.md` — DoD and guard inventory (members / services / per-user-workspace-cap / out-of-plan-roles).
- Playwright MCP, scratch account, workspace settings → plan section → change-plan dialog.

## Steps

1. Scratch account, Hobby workspace: confirm invite is gated ("member limit reached").
2. Change plan Hobby → Pro via the dialog; confirm the settings page reflects Pro and the invite dialog now sends (invite a throwaway email, then revoke it).
3. Attempt Pro → Hobby with the pending/accepted second member state that trips the member guard; confirm the guard error copy matches the capture verbatim.
4. Clean up (delete the scratch workspace); screenshots + evidence into the milestone README.

## Files

- .pm/w6/m13/README.md (evidence)

## Acceptance criteria

- [ ] Post-upgrade invite succeeds on prod (screenshot).
- [ ] Downgrade guard error shown live matches `docs/render-artifacts/workspace-plan-change.md` (screenshot).

## Out of scope

- Billing/payment steps — bex ships none, per w6's "Not in w6".
