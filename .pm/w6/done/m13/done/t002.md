---
id: w6/m13/t002
title: "Manual prod cleanup: delete the orphaned m11-purge-test App"
worker: worker6
status: done
estimate: 20m
depends_on: []
---

## Objective

Remove the orphaned `m11-purge-test` App that m11's t003 verification left running on real prod — it is live, billable, and unreachable through every API path (its `LabelTenant` names a deleted tenant, so `core.Base`'s tenant gate 403s everyone including its creator). The shipped `apps.WorkspacePurger` only prevents *future* orphans; it does not retroactively clean this one up.

## Context

- `w6/done/m11/README.md` t003 evidence — "**Third bug found (serious)**" + §Residual: needs `kubectl delete app m11-purge-test -n default` (or whichever namespace `BEX_API_NAMESPACE` points at in prod).
- Cluster access per the `deploy-app-from-local` skill's runbook (Hetzner API → kubeconfig).

## Steps

1. Reach the production cluster (deploy-app-from-local runbook access path).
2. `kubectl get app m11-purge-test -A` to locate the namespace; confirm it is the orphan (LabelTenant names a nonexistent tenant).
3. `kubectl delete app m11-purge-test -n <ns>`; confirm the ownerRef cascade removed its Deployment/Service/pod.

## Files

- none (live-cluster operation)

## Acceptance criteria

- [ ] `kubectl get app m11-purge-test -A` returns nothing.
- [ ] No Deployment/pod named after it remains (recorded command output).

## Out of scope

- Sweeping prod for other orphans (none are known; the purger prevents new ones once t001 deploys).
