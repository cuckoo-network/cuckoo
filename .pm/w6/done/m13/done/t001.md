---
id: w6/m13/t001
title: "Ship gate: land 42e139f + the uncommitted m12 diff on prod and confirm rollout"
worker: worker6
status: done
estimate: 30m
depends_on: []
---

## Objective

Get the already-committed m11 fixes (`42e139f`: Kratos-admin port, `keyvalues` RBAC, `apps.WorkspacePurger`) and the still-uncommitted m12 plan-change diff onto the real production cluster. This is a coordination gate: the repo rule forbids committing/pushing outside `/ship`, so the user must run `/ship`; this task's work is preparing the tree (nothing stale/unintended in the diff) and then confirming the CI deploy actually rolls prod onto the new images.

## Context

- `42e139f` is committed but prod's images were pinned earlier (`bb81086` pins to `6ef9de218dfe`, which predates the fixes) — deployment is not confirmed.
- The m12 diff (backend `changeWorkspacePlan` + dashboard plan section/dialog) is uncommitted in the working tree per `git status`.
- `w6/done/m11/README.md` §Residual lists exactly what the deploy is expected to fix.

## Steps

1. Review `git status`/`git diff` — confirm the pending tree is exactly the m12 work (plus this board change) and nothing unintended.
2. Ask the user to run `/ship`.
3. Watch the CI deploy path; confirm the prod `bex-api`/operator/dashboard pods roll to images built from the new commit (image tag/digest check via the deploy pipeline or the `deploy-app-from-local` runbook's cluster access).

## Files

- none (coordination + verification; the shipped diff is m11/m12's)

## Acceptance criteria

- [ ] `main` on origin contains `42e139f` and the m12 work.
- [ ] Prod `bex-api` pod runs an image built from (or after) that commit — recorded evidence (image digest or deploy log).

## Out of scope

- Any new code changes — this task ships what m11/m12 already wrote.
