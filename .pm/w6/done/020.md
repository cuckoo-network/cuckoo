Close the official Render CLI `ssh [serviceID|serviceName|instanceID]` checklist row with the already-shipped production gateway and verifier; do not build another SSH path.

- Pin the unmodified current CLI used by the audit (`render-oss/cli` v2.21.0, commit `c398207`) and run [`scripts/ssh-verify.sh`](../../../scripts/ssh-verify.sh) in a real PTY with `BEX_RENDER_CLI_VERIFY=1` and `BEX_RENDER_CLI_BIN` set to that checksum-verified binary. Supply the existing redacted verifier inputs out of band; never print the bearer token, private key, kubeconfig, or terminal contents.
- Reuse the self-cleaning paid two-replica fixture, isolated CLI config, `ssh-agent`, pinned host fingerprint, and OpenSSH shim already in the verifier. The 2026-07-17 production acceptance at [`.pm/w2/done/m39/evidence/2026-07-17-production-acceptance.md`](../../w2/done/m39/evidence/2026-07-17-production-acceptance.md) already proves service-name and complete-instance-id execution through `ssh.bex.co`; a fresh run is needed only if the current production revision or pinned CLI changes.
- Add/execute the missing direct-service-id leg: pass the service id, choose **Any instance** in the CLI's interactive instance picker, execute the runtime sentinel, and assert the shim recorded `<serviceID>@ssh.bex.co`. Keep the direct full-instance-id leg as the exact-replica proof.
- Do not claim the picker's exact-instance option works. In v2.21.0 both upstream callbacks assign `input.ServiceIDOrName` instead of their `instanceID` argument (`cmd/ssh.go`), so the selected replica is discarded; record this as an upstream CLI defect, not a bex gap, and do not fork/vendor/patch the CLI.
- Preserve the current pending edits in `docs/cli-compatibility-checklist.md`, add a production-SSH supplement under **How this pass was verified**, and mark the main running-instance `ssh` row `[x]` only after service id, service name, and complete instance id all reach their asserted destinations. Keep `--ephemeral` and `--plan` `[-]` with the running-instance carveout in `.pm/DO_NOT_DO.md`; they remain deliberate non-goals.

Done when the checklist cites the pinned CLI/version, public production acceptance, selector coverage, and upstream picker defect; `bash scripts/ssh-verify.test.sh`, shell syntax checks, pinned Markdown Prettier, and `git diff --check` pass; all disposable keys/services are absent afterward.

Completed 2026-07-18. The checksum-verified official v2.21.0 release passed the public production verifier by service name, service id with **Any instance**, and complete instance id; every selector reached the runtime sentinel through `ssh.bex.co`. The default public matrix also passed host-key, raw OpenSSH, PTY, exit-status, restart/redeploy, stale/suspended/free/shell-less/unknown/deleted-key behavior. The disposable key, service, and Scale workspace were deleted. The checklist and ADR035 now cite the run and retain the upstream exact-instance-picker defect plus the `--ephemeral`/`--plan` non-goals.
