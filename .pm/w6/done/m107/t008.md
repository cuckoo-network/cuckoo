---
id: w6/m107/t008
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m107/t006]
---

## Objective

Add meaningful tests that assert real behavior and failure modes — never trivial/tautological/snapshot-everything tests.

## Steps

1. Unit test t001's primitive directly (may already exist from t001 — extend rather than duplicate): fix `TZ=UTC` for a simulated SSR pass and a non-UTC zone for the simulated client pass, assert the SSR/pre-mount output is identical (no divergence possible) and the post-mount output is the client-timezone-correct text.
2. Component tests for at least the 3 previously-unguarded call sites (`recovery-panel.tsx`, `webhook-row.tsx`, `webhook-deliveries-card.tsx`): render with a mocked non-UTC client timezone and assert the displayed text matches the expected local conversion, not the raw UTC digits.
3. A regression test for the `formatDateLong` boundary-crossing case: an instant that falls on different calendar days in UTC vs. a non-UTC zone, asserting the viewer-local day is shown.
4. Do NOT test that `suppressHydrationWarning` is present/absent as a proxy for correctness — test the actual displayed value, which is the real defect this milestone fixes.

## Files

- New/updated `__tests__` files adjacent to each changed component, per `dashboard/CLAUDE.md`'s testing convention.

## Acceptance criteria

- [ ] Tests fail against the pre-fix code (verify by temporarily reverting the fix locally) and pass against the fixed code.
- [ ] `yarn test` green.

## Out of scope

- Testing every one of the 13 call sites exhaustively — cover the primitive thoroughly plus a representative sample across the severity tiers.
