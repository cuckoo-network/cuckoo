---
id: w6/m107/t006
title: Render parity
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m107/t005]
---

## Objective

Confirm this milestone's fix introduces no REST/GraphQL/MCP wire-shape or dashboard-copy change — it is a pure client-rendering-timing fix (the raw ISO instant sent over the wire is already correct and unchanged; only the human-readable rendering was wrong).

## Steps

1. Diff the GraphQL/REST responses for a repaired resource (e.g. an Environment Group) before/after the fix — confirm `createdAt`/`updatedAt` etc. are byte-identical (they were never the bug; only the client's rendering of them was).
2. Confirm no locale string changed (no new `t()` keys needed unless t001's primitive introduces a loading-placeholder copy string, in which case add it to `en`/`zh` locales per `dashboard/CLAUDE.md`'s Internationalization section).
3. Compare against render.com's own dashboard behavior if easily observable — not required to block this task if unavailable; note as n/a if skipped.

## Files

- No source changes expected beyond an optional locale addition for a placeholder string.

## Acceptance criteria

- [ ] REST/GraphQL/MCP payloads for a repaired resource type are unchanged by this milestone's diff.
- [ ] Any new user-visible string (e.g. a loading placeholder) is routed through `useTranslations()` per `dashboard/CLAUDE.md`, with both `en` and `zh` entries.

## Out of scope

- Any backend change — this milestone is dashboard-only.
