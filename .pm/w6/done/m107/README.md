# w6 · m107 — `formatDateTime`/`formatDateLong` display the SSR container's clock, not the viewer's, on every fresh page load

**Worker:** worker6 **Goal:** every dashboard surface that renders an absolute date/time via `formatDateTime`/`formatDateLong` shows the exact text a client-only render in the viewer's own browser timezone would produce — on the very first paint the user can observe, whether the page arrived via full navigation, a bookmark, or a client-side transition — never the SSR container's own (effectively UTC) clock digits mislabeled as local. **Status:** done (2026-08-26)

## Resolution (2026-08-26)

Confirmed real, then fixed. A throwaway React-19 hydration probe (`hydrateAcrossBoundary` shape) reproduced the exact mechanism: a `<time suppressHydrationWarning>` whose text differs between the SSR and client passes keeps the **server's** value permanently (`afterHydrate === "8:28 PM"`, 0 recovered errors); the identical node **without** the guard self-corrects to the client value (`"1:28 PM"`) but logs one React #418. So the `suppressHydrationWarning` added for `w6/030` was itself what froze the wrong UTC clock on screen.

Fix — a shared client-safe primitive plus a full call-site migration:

- **`common/hooks/use-is-hydrated.ts`** — `useSyncExternalStore(subscribe, () => true, () => false)`: `false` on the SSR pass and the hydrating client render (so both agree byte-for-byte — no #418), `true` after hydration commits. The officially-supported way to defer inherently client-only output.
- **`common/components/local-time.tsx`** — `LocalDateTime` / `LocalDate`: render the machine-readable instant in `dateTime` always, a reserved-width `Skeleton` until hydrated, then the viewer-local text. No `suppressHydrationWarning` needed (both server passes emit the placeholder). In a pure client-only render (RTL tests, client-only-data routes) the value shows immediately with no placeholder — the deferral engages only on real SSR→hydration.
- **`common/hooks/use-local-date.ts`** — `useLocalDateTime` / `useLocalDate`: the string form for `title=` attributes, sentence interpolation, and per-row `.map()` bodies where a component's own hook can't be called.

Migrated all 13 documented call sites + 3 additional `formatDeployTimestamp` render consumers the original grep missed (`deploy-timeline.tsx`, `deploys-list-page.tsx` — which carried the same `suppressHydrationWarning` anti-pattern — and `deploy-header.tsx`). Every render-body `suppressHydrationWarning` on a timestamp is gone; the only remaining direct `formatDateTime` call is the `formatDeployTimestamp` pure helper, now called only behind a hydration gate. Corrected the false-invariant doc comment on `format-date-time.ts`.

**Verification:** `common/components/__tests__/local-time.test.tsx` asserts, for both `formatDateTime` and the day-boundary `formatDateLong` case, that the SSR markup carries no AM/PM clock (only the placeholder + exact `dateTime`), that the settled post-hydration text equals the local formatter's output, and that **zero** #418s are recovered — a deterministic reproduction of the exact defect, run under `TZ=America/Los_Angeles`. `yarn typecheck && yarn lint && yarn test` (2697 tests) all green. Live `page.goto` spot-check against production (t005's original method) was not run this session (no prod browser access here); the hydration unit test is a stronger, deterministic substitute for this class of bug and remains re-checkable via `/qa-find-bugs`.

## Background (found live, 2026-08-26 `/qa-find-bugs` hunt, 17th run of the day)

While live-verifying `w6/m97` this run, a fresh Environment Group (`qa-20260826-envgroup`, `evg-da7kok7kh5kc73fbqvlg`, deleted after this hunt) exposed a bug in the exact mechanism `w6/030` and `w6/m102` already discuss, but neither actually covers:

1. Right after creation (client-side render, no page reload), the detail page's "Group Metadata" card showed `Created: August 26, 2026 at 1:28 PM` — correct: the account's browser timezone is `America/Los_Angeles` (confirmed via `Intl.DateTimeFormat().resolvedOptions().timeZone` inside the page), and the raw GraphQL `createdAt` is `2026-08-26T20:28:01.124475018Z` — 20:28 UTC − 7h = 13:28 = 1:28 PM. Correct.
2. A **fresh full-page load** (`page.goto` to the same URL — what an actual bookmark, shared link, or browser refresh does) of the identical page showed `Created: August 26, 2026 at 8:28 PM` for the **same instant** — the raw UTC clock digits (20:28) relabeled with a 12-hour "PM" suffix as if it had already been converted to local time. This is not a formatting-style difference; it is a factually wrong time, off by exactly the viewer's UTC offset.
3. Repeated the fresh-load check with `browser_console_messages(level: "error")` scoped to just that navigation (not the shared browser's `all: true` history, which mixes in unrelated noise from other resources/sessions) — **zero console errors**. This confirms the page's `suppressHydrationWarning` guard (added for `w6/030`) is doing exactly what it says: suppressing the warning, while leaving the wrong value on screen indefinitely (until some unrelated re-render happens to occur during the visit).

Evidence: `.playwright-mcp/qa-m97-verify-1-still-failing.png` is unrelated (m97 evidence); this bug's live artifacts are the GraphQL/console captures quoted above and reproduced inline in this filing (no separate screenshot was taken — the accessibility-tree `browser_find` output captured the exact displayed string both times, which is the load-bearing evidence for a text-content bug).

**Root cause.** `dashboard/src/common/lib/format/format-date-time.ts:10-16` (`formatDateTime`) and `:25-31` (`formatDateLong`) call `date-fns`'s `format()`, which reads the JS `Date` object through the **executing runtime's own local-timezone getters** — the Node SSR process's zone (effectively UTC in this deployment) during server rendering, the browser's zone during client rendering — with no timezone pinning and no gate on when it is safe to render. The function's own doc comment asserts a false invariant: "Deliberately a fixed en-US-style pattern (not the viewer's locale) so every surface renders timestamps identically" — true for the date format (month names, comma placement) but not for the clock reading, which silently follows whichever runtime executed the call.

**Control case, live-verified as hard as the failing one.** `/services/srv-d9nqg9dcavls73fp8m2g/deploys/dep-da4c61pahdqc73f1j9n0` (a real production deploy detail page) on a fresh full-page load correctly showed `Created: August 21, 2026 at 2:28 PM` for `createdAt: 2026-08-21T21:28:39Z` (21:28 UTC − 7h = 14:28 PM ✓) — using the exact same `formatDateTime` function via `dashboard/src/features/deploys/lib/deploy-presentation.ts`'s `formatDeployTimestamp`. This is **not** because that call site is fixed: `dashboard/src/features/deploys/components/deploy-header.tsx:44-60` returns a loading `Skeleton` whenever its `deploy` prop is nullish ("Nullish while the header `deploy` query is still in flight (w9/m62 t002)"), so the SSR pass never emits the real timestamp text node at all — the field is populated entirely client-side, after the client's own query resolves, so there is nothing to diverge from. This is exactly the trap the hunt procedure warns about: "a route-level or caller-level workaround is indistinguishable from a correct backend when you are looking through a browser." Any future change that makes the deploy query SSR-blocking (e.g. a prefetched loader, matching the pattern several other detail routes already use per `dashboard/CLAUDE.md`'s "Navigation pending states" section) would silently reintroduce this exact bug on the deploy page too.

**Exhaustive blast radius** (`grep -rn "formatDateTime(\|formatDateLong(" dashboard/src --include="*.tsx" --include="*.ts" | grep -v "__tests__\|format-date-time.ts"`, 13 call sites across 10 files):

| tier | call sites | live status |
| --- | --- | --- |
| **Unguarded — live #418 console error AND wrong value on every fresh load** | `features/databases/components/recovery-panel.tsx:96,102` (via the unguarded `Field` component, `:317-324`); `features/webhooks/components/webhook-row.tsx:50,81` (`<span>{exactLatest}</span>`); `features/webhooks/components/webhook-deliveries-card.tsx:247,319` (`<span>{exactSentAt}</span>`) | not individually re-navigated this run (see Unverified) — guard absence confirmed by direct code read |
| **Guarded with `suppressHydrationWarning` — console error suppressed, wrong value still shown until an unrelated re-render** | `features/env-groups/components/env-group-metadata.tsx:57-58` (**live-reproduced this run**); `features/audit/components/audit-event-row.tsx:22-26`; `routes/env-groups.tsx:236-240` (the list page's "Updated" column) | env-group-metadata confirmed live; the other two share byte-identical code shape, not individually re-navigated |
| **Masked by an unrelated client-only-render workaround (not fixed)** | `features/deploys/lib/deploy-presentation.ts`'s `formatDeployTimestamp`, consumed by `features/deploys/components/deploy-header.tsx:62-83` | confirmed live + by code read (control case above) |
| **Attribute-only (`title=`) — lower severity, self-corrects** | `routes/services.$serviceId.events.tsx:341,390` (`<time dateTime={timestamp} title={exactTimestamp}>`) | React patches attribute mismatches during hydration (unlike text nodes), so these are not part of the urgent-fix list but share the same root function |
| **`formatDateLong` siblings — same root cause, narrower manifestation** (only wrong when the instant falls within the ~7h UTC/local gap around a calendar-day boundary) | `features/workspaces/components/workspace-details-card.tsx:138`; `features/webhooks/components/webhook-detail-header.tsx:25`; `features/registry-credentials/components/registry-credential-row.tsx:88` | **Unverified this run** — not live-reproduced, flagged for the fix's regression tests |

The `recovery-panel.tsx` instance is the highest-stakes: its own code comment says "Exact restore-point boundaries, not relative ages — the user picks a point-in-time against these" — a Postgres point-in-time-recovery UI showing a restore boundary 7 hours off from reality is not merely confusing, it is the kind of wrong information a user could act on directly.

`webhook-deliveries-card.tsx:247,319` sits on the exact same `<time>` element as the sibling bug already filed as `w6/m102` (`formatRelativeAge(delivery.sentAt)` at line 317) — two independently-wrong values on one element, two different root causes (this milestone's timezone-of-runtime divergence vs. `m102`'s elapsed-time-since-render divergence), confirmed by reading both lines side by side.

**Adjacent classes / dedupe.** This is a **third**, independent root cause under the generic React #418 code (after `w1/done/m81/done/t002.md`'s `navigator.platform` read and `w6/m102`'s `formatRelativeAge`/`formatRelativeUntil`), and it **corrects the standing understanding of `w6/030`**: that item's own `suppressHydrationWarning` treatment (confirmed live in `env-group-metadata.tsx`, which carries an explicit `w6/030` comment) does stop the console error `w6/030` was literally filed against, so it is not being reopened or re-filed — but it was never a fix for the underlying wrong-value display, which `w6/030`'s own filing never asserted as in-scope. This milestone is the first to name and fix that broader, higher-severity defect: the value itself, not just the console noise, and across every call site of the shared formatter rather than the one page `w6/030` happened to be filed against.

## Tasks (in order)

All tasks **t001–t009 DONE** (2026-08-26) — see Resolution above.

| id   | title                                                                                                              | est | depends_on |
| ---- | ------------------------------------------------------------------------------------------------------------------ | --- | ---------- |
| t001 | Build a shared client-safe timestamp primitive: SSR and the pre-hydration client render byte-identical placeholder markup; the real, viewer-local text appears only once it is guaranteed correct | 45m | —          |
| t002 | Apply it to the 3 unguarded high-severity call sites (`recovery-panel.tsx`, `webhook-row.tsx`, `webhook-deliveries-card.tsx`) — removes both the live #418 error and the wrong-value bug | 30m | t001       |
| t003 | Apply it to the 3 `suppressHydrationWarning`-guarded call sites (`env-group-metadata.tsx`, `audit-event-row.tsx`, `routes/env-groups.tsx`) and the `formatDateLong` siblings (`workspace-details-card.tsx`, `webhook-detail-header.tsx`, `registry-credential-row.tsx`) — removes the wrong-value bug where the console error was already masked | 30m | t001       |
| t004 | Apply the same primitive to the attribute-only `title=` sites (`services.$serviceId.events.tsx:341,390`) for consistency, and to `deploy-presentation.ts`/`deploy-header.tsx` so a future SSR-blocking change to the deploy query can't silently reintroduce this bug | 20m | t001       |
| t005 | Live verification: fresh `page.goto` (not client-side nav) against at least 4 of the repaired routes, confirming the displayed text matches the viewer's actual local-timezone conversion of the raw instant, and zero `browser_console_messages(level:"error")` | 30m | t002, t003, t004 |
| t006 | Render parity — confirm no REST/GraphQL/MCP wire-shape or copy change; this is a pure client-rendering-timing fix | 15m | t005       |
| t007 | Simplify                                                                                                            | 20m | t006       |
| t008 | Test coverage — assert SSR output and post-mount client output for a fixed instant + fixed non-UTC `TZ`, both for the boundary-crossing `formatDateLong` case and a mid-day `formatDateTime` case | 30m | t006       |
| t009 | Closeout                                                                                                            | 10m | t007, t008 |

## Definition of done

- A fresh `page.goto()` (full navigation, not a client-side transition) to `/env-groups/<id>`, `/databases/<id>` (with a recovery-enabled Postgres), `/webhook/<id>`, and the env-groups list page each show a `Created`/`Updated`/exact-timestamp value that matches `new Date(rawInstant).toLocaleString()` computed in the same browser session — verified by comparing the displayed string against a `page.evaluate`d client-side computation of the same raw GraphQL instant, not merely "no console error."
- `browser_console_messages({level:"error"})` scoped to a single fresh navigation of each of the 3 currently-unguarded routes (`recovery-panel.tsx`'s route, the webhooks list, a webhook's delivery card) shows zero React error #418 — not merely suppressed, actually absent because the mismatch can no longer occur.
- `grep -rn "formatDateTime(\|formatDateLong(" dashboard/src --include="*.tsx" --include="*.ts" | grep -v "__tests__\|format-date-time.ts"` — every result routes through the new shared primitive (directly, or via a component that wraps it) — re-running this milestone's audit against the fixed tree finds zero call sites still calling `formatDateTime`/`formatDateLong` directly from a render body without the primitive.
- A regression test fixes `TZ=UTC` for the SSR-side assertion and a non-UTC zone (e.g. `America/Los_Angeles`) for the client-side assertion of the same instant, and asserts both produce the SAME final displayed text (the client-local one) — not merely that no exception/warning fires.
- `yarn typecheck && yarn lint && yarn test` (dashboard) green.

## Source + Goal linkage

- **Source:** live `/qa-find-bugs` hunt of `dashboard.bex.co`, 17th run of the day, 2026-08-26 — found while live-verifying `w6/m97`. Evidence: live GraphQL `createdAt: "2026-08-26T20:28:01.124475018Z"` for `evg-da7kok7kh5kc73fbqvlg` vs. displayed `"August 26, 2026 at 8:28 PM"` on a fresh load / `"August 26, 2026 at 1:28 PM"` immediately post-create; control case `srv-d9nqg9dcavls73fp8m2g`'s deploy `dep-da4c61pahdqc73f1j9n0` (`createdAt: "2026-08-21T21:28:39Z"`, correctly displayed `"August 21, 2026 at 2:28 PM"` on fresh load, explained by `deploy-header.tsx`'s skeleton-gate, not a fix). Both resources are read-only production data (the QA env group was created, verified, and deleted this run; the deploy page was read-only).
- **Goal linkage:** dashboard SSR correctness — same class of concern as `dashboard/CLAUDE.md`'s "Navigation pending states" and "SSR gotcha" sections, and the exact precedent `dashboard/CLAUDE.md`'s Internationalization section already documents solving for language detection (client timezone/cookie hint feeding the SSR pass) — this milestone can reuse that precedent's shape for date/time.
- **Expected outcome:** every absolute-timestamp display in the dashboard shows the same text a purely-client render would, from the first paint the user can observe, with no wrong value ever visible even briefly — closing the timezone half of the recurring #418 pattern the same way `w6/m102` closed the elapsed-time half.
- **Why now:** this is a live, currently-reproducing defect affecting real displayed data (not just a console warning) on production, including a Postgres point-in-time-recovery surface where a wrong timestamp is the kind of information a user could act on directly. It also corrects a standing misunderstanding on the board (`w6/030` treated as fully resolved) before that misunderstanding causes the underlying defect to be missed in a future audit.
- **Render parity task included:** yes (t006), as a confirmation only — this is a pure client-rendering-timing fix with no REST/GraphQL/MCP payload or dashboard-copy change.
