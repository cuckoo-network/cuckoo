---
id: w6/m109/t006
title: Test coverage
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m109/t004]
---

## Objective

Beyond t003's targeted regression tests, sweep for any other test gaps this bug class exposes — e.g. a table-driven test across both resource types (Postgres, KeyValue) and both surfaces (REST, MCP) asserting every `required`-per-Render-schema array field is always key-present, so a future new required array field added to either resource doesn't silently reintroduce the same class of bug.

## Acceptance criteria

- [x] A shared/table-driven test (or equivalent) exists that would catch a future re-introduction of "required array field marshals with omitempty" for Postgres or KeyValue. (The equivalent is the composed-server conformance suite: `internal/api/conformance_test.go` validates live REST responses against the pinned Render spec's ACTUAL `required` list — stronger than a hand-maintained table — and this milestone removed the stale `ipAllowList`/`readReplicas` suppression entries from `conformance_allowlist_test.go`, so any future omitempty on a required field fails `TestConformanceAllowlistEntries`/`TestRenderConformance` immediately. MCP-side key presence is guarded by the two `required_fields_test.go` empty-case tests.)
- [x] `go test ./...` (backend) and `make lint` (all four modules) green.
