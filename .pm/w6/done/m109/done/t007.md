---
id: w6/m109/t007
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m109/t005, w6/m109/t006]
---

## Objective

Once t001-t006 are done and this milestone's Definition of Done is actually verified live (not merely code-complete): set every task's `status: done`, move each `tNNN.md` to `w6/m109/done/`, mark every row `— **DONE**` and `**Status:** done` in `w6/m109/README.md`, move the whole `w6/m109/` directory to `w6/done/m109/`, and check `- [x]` for m109 in `w6/README.md`.

> **Historical state 2026-08-26:** t001–t006 were done (code + tests + ledger + conformance-allowlist cleanup; backend `go test ./...` and `make lint` ×4 green; empty-case tests proven red against the pre-fix code), but production deployment was still pending. The blocker update below supersedes that state.

## Acceptance criteria

- [ ] Definition of done in `w6/m109/README.md` re-verified live against production (or the equivalent dev environment) before closing, not assumed from code review alone.
- [ ] All board bookkeeping (task frontmatter, milestone README, workstream README) in sync.
- [ ] `w6/m109/` moved to `w6/done/m109/`.

## Blocker update (2026-08-27/28) — the deployment premise is stale; read this first

**The fix is live.** `732d0991` (w6/m109) is a confirmed ancestor of `0ca167a8`, the production image pinned by `71fe9660`. Everything written above about waiting for a deploy is **discharged** — do not re-argue it.

**The remaining blocker is credentials, not code and not the pipeline.** This session could not run the live leg because the QA sign-in is not configured:

- `scripts/qa-login.sh` exits **2** — `QA_EMAIL`/`QA_PASSWORD` are absent from `.env`. They are present as empty names in `.env.example:423-424`, so `.env` has simply drifted behind it.
- `BEX_API_TOKEN` is set but returns **401** against `https://api.bex.co/v1/services` (and `/v1/owners`), so it is not a usable API credential for this surface either — the unauthenticated control returns the same 401.

**To unblock:** fill `QA_EMAIL`/`QA_PASSWORD` in `.env` (never paste them into chat or a tool call — `scripts/qa-login.sh` reads them inside its own process and hands back cookies only), then run this task's steps unchanged. Nothing else about the task needs revisiting.

## Closed 2026-08-29 — live re-probe passed

Run with a dedicated synthetic QA identity (`qa-sweep-20260829@example.com`, workspace `tea-da96ad4039sc739tkjqg`, billing-excluded via the CP internal API) against `api.bex.co`, on fresh free-tier fixtures `dpg-da96b239ui0s73db2amg` / `red-da96b2c039sc739tkjs0`:

- REST `GET /v1/postgres/{id}`: `'ipAllowList' in body` **true** (`[]`), `'readReplicas' in body` **true** (`[]`).
- REST `GET /v1/key-value/{id}`: `'ipAllowList' in body` **true** (`[]`).
- MCP `tools/call get_postgres`: `ipAllowList: []` and `readReplicas: []` both present; `get_key_value`: `ipAllowList: []` present.
- Non-empty control: `PATCH` one CIDR entry → read back `[{cidrBlock: 203.0.113.0/24, …}]` → cleared back to `[]`.
- GraphQL-unchanged + regression bullets were already CI-proven by t003/t006's suites (re-run green at `8ab1e6db` in this session's full-backend pass).
