---
id: w6/m101/t007
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m101/t005, w6/m101/t006]
---

## Objective

Close the milestone once its Definition of Done is actually met (observable, not just "the code is written").

## Steps

1. Confirm every DoD bullet in `README.md` is live-verified (the rename → webhook-payload probe, the store query fix, the tests).
2. Set every task's `status: done`; move each `tNNN.md` to `w6/m101/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w6/m101/README.md`.
4. Move `w6/m101/` to `w6/done/m101/`.
5. Check `- [x]` for **m101** in `w6/README.md`.
6. `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [ ] DoD verified live, not assumed.
- [ ] Board state (workstream README, milestone README, task frontmatter) agrees everywhere.

## Result

Definition of done re-verified against the real-Postgres suite rather than assumed — the live probe in DoD bullet 1 needs the fix deployed, which `/ship` + the deploy pipeline do, not this milestone (same shape as w6/m100's closeout).

- **Renaming a service changes the next delivery's `data.serviceName`** — proven at both layers it passes through. In SQL: `TestPGWebhookFeedReportsDisplayNameAfterRename` runs the production `webhookEventsQuery` against real Postgres, renames the app mid-test, and asserts all four apps-joined arms switch from the immutable name to the display label (and back when cleared). In the projection: `TestProjectRenamedServiceKeepsLabelAndIDApart` pins that the label reaches `data.serviceName` while `data.serviceId` stays the immutable id. The test is verified **RED** against the pre-fix `a.name` spelling, failing on all four arms by name.
- **`go test ./lego/backend/internal/webhooks/... ./lego/backend/internal/store/...`** includes the renamed-app case (`display_name` set and differing from `name`) and asserts the queried row's `ServiceName` is the display label. Green; full `go test ./lego/backend/...` green on a pristine PG 17 cluster with `-count=1`, `make lint-backend` 0 issues.
- **`grep -c "AS service_name" lego/backend/internal/store/webhooks.go` = 1**, and that count is not the check: only the first arm carries the alias, the other three select positionally. Re-read the query — all four apps-joined arms resolve through the shared `appDisplayLabel` const; the datastore arm keeps its own `target_name` fallback.
- **`apps.name` is untouched.** No `UPDATE apps SET name` exists anywhere; `AppView.Name` still returns the immutable id on direct reads, the feed's `service_id` is still derived from it, and the audit arm still matches targets on it. REST/GraphQL/MCP round-trip tests were not modified and pass unchanged.

**Carried forward (not blockers, recorded so they aren't mistaken for done):**

- **Not deployed.** The production probe against `dashboard.bex.co` — create a webhook to a real sink, rename a service, trigger a deploy, verify the signed body — can only run once this ships. `eden-dash-v3` (`srv-d9ndt8hmcglc739fkp50`) keeps emitting `block-eden-mono` until then, and, per the no-backfill decision, until it is renamed once more after the migration lands.
- **No backfill** (t001, as specified): services renamed before migration 0100 report their immutable name until their next rename. A one-time `UPDATE apps SET display_name = …` from live CRs would close that if it proves to matter.
- **Push notifications got the same fix for free** — `notifications/push_worker.go:610` tails the same feed, so its `"<service> deploy started."` titles were stale for renamed services too. Not separately probed live.
- **Per-service Events tab** — still not independently reproduced against a renamed service, as the milestone noted. `store/events.go` carries no `ServiceName` at all (the caller already knows the service), so there is nothing to drift.
