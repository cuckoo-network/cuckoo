---
id: w6/m101/t006
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m101/t004]
---

## Objective

Add any meaningful test coverage gaps beyond t003's targeted regression test, for the behavior this milestone shipped.

## Context

- t003 already covers the core regression (renamed service → correct webhook `serviceName`). This task covers anything t003 didn't: e.g. the new store setter's own unit test, a migration-applies-cleanly check, and the empty-store (`BEX_CP_DB_URI` unset) path for `SetDisplayName`.

## Steps

1. Review t001-t003's diff for untested branches (store-unavailable path, migration idempotency, `NULLIF` edge case with a whitespace-only display name).
2. Add tests for any real gaps found; skip tautological/snapshot-everything tests.

## Files

- `lego/backend/internal/store/`, `lego/backend/internal/apps/service.go` test files.

## Acceptance criteria

- [ ] `go test ./lego/backend/internal/...` green.
- [ ] No coverage added purely to move a number — each new test asserts real behavior or a real failure mode.

## Out of scope

- Coverage for code this milestone didn't touch.

## Result

Reviewed t001-t003's diff for untested branches and covered the real ones:

- **Store-unavailable / bare-CR paths** — `TestSetDisplayNameWithoutStorePatchesCR` (`s.Store == nil`, i.e. `BEX_CP_DB_URI` unset) and `TestSetDisplayNameUnmanagedAppSkipsStore` (managed-by label absent, `LabelAppID` present so the gate itself is pinned).
- **Row-write failure** — `TestSetDisplayNameRowWriteFailureLeavesCRUntouched`: the verb surfaces the error and the CR keeps its old label, so a rename is never half-applied.
- **Whitespace-only display name** — the `NULLIF` edge case can't reach the column: `SetDisplayName` trims before either write, so `"   "` is a clear. Asserted on both halves (row write is `""`, CR spec is cleared) in `TestSetDisplayNameManagedAppWritesRowThenCR`.
- **Empty-`display_name` fallback** — covered twice in the PG test (never-renamed, and after a clear).
- **Missing app** — `TestPGSetAppDisplayNameMissingApp`.
- **Migration idempotency** — not re-tested: `TestPGStore` already runs `Migrate` twice and asserts the second is a no-op, and the new PG tests exercise the column end-to-end. A dedicated test would only re-assert that `Migrate` runs.

`go test ./lego/backend/...` green on a pristine PG 17 cluster with `-count=1`; `make lint-backend` reports 0 issues.
