---
id: w6/m101/t005
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m101/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Touches: `lego/backend/internal/store/` (migration + setter), `lego/backend/internal/apps/service.go`, `lego/backend/internal/store/webhooks.go`.

## Steps

1. Invoke `/simplify` scoped to this milestone's diff.
2. Apply any behavior-preserving cleanups it finds; skip anything that would change behavior or add scope.

## Files

- Whatever `/simplify` flags within this milestone's diff.

## Acceptance criteria

- [ ] `/simplify` run recorded (applied fixes or "none found") in the closeout evidence.

## Out of scope

- Anything outside this milestone's own diff.

## Result

`/simplify` run over this milestone's diff — three parallel reviews (reuse, quality, efficiency).

**Efficiency: no findings.** Query plans identical old vs new (target-list-only expression), migration metadata-only, and `SetDisplayName` costs exactly one extra SQL `UPDATE` on a rare user-initiated verb with no extra Kubernetes call (`writeThroughStoreFetched` reuses the already-fetched App).

**Applied:**

- Dropped `memStore.displayNames` — write-only dead state; the fake doesn't implement the feed, so the method now only rejects an unknown app the way `PGStore` does.
- Reused the sibling PG-test preamble instead of re-rolling skip/migrate/pool/`NewPGStore`; renamed it `webhookFairnessStore` → `webhookPGStore` now that two files share it.
- Extracted `manage(app, appID)` out of `managedApp` so the two new tests stop hand-rolling the managed-by label map (also drops a `store` import from `displayname_test.go`).
- Comment trims: the impl doc no longer duplicates the interface doc; `appDisplayLabel`'s 8-line doc cut to the two non-obvious facts; the migration header cut to the bug plus the fallback rule; the three new test headers cut to one line each (the file's existing tests carry none).
- **Factual fix:** the "the webhook feed is its only reader" claim was wrong — `notifications/push_worker.go` reads the same rows. Now "the workspace-wide event feed, webhook and push dispatch alike", in all three places it was stated.
- Test strengthening: the unmanaged-app case now carries `core.LabelAppID` so it actually pins `LabelManagedBy` as the gate rather than the trivial no-labels path; the tests were renamed and split onto the package's sibling spellings (`…ManagedAppWritesRowThenCR` / `…UnmanagedAppSkipsStore` / `…WithoutStorePatchesCR` / `…RowWriteFailureLeavesCRUntouched`); the service-id invariant was hoisted out of a thrice-called closure; the missing-app case became its own test; the dispatch test was replaced as recorded in t003.

**Declined:**

- Rewording `writeThroughStoreFetched`'s `"update source of truth: %w"` error — shared by suspend/plan/scale/idle-TTL, outside this milestone's blast radius. Its doc gained a clause naming `SetDisplayName` as the one caller with the opposite ownership instead.
- A sibling `datastoreDisplayLabel` const for symmetry — would require `webhookEventsQuery` to stop being a `const`.
