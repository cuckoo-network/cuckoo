---
id: w6/m24/t011
title: Environments linkage: `envGroupIds` + `set_environment_env_groups`
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m24/t002]
---

## Objective

Resolve t005's `environmentId` hedge into real work: Render's `environment.envGroupIds` means env groups are assignable to Environments, and Environments ARE modeled (w1/m32 + w6/m19/m20 — services via `apps.environment_id`, datastores via `core.LabelEnvironment`). Add env-group membership: `envGroupIds` on the environment object, a `set_environment_env_groups` verb (REST/GraphQL/MCP, the `set_environment_services` grammar), and `environmentId` accepted on env-group create per Render's POST.

## Context

- `lego/backend/internal/environments/` (w1/m32) + `internal/envgroups` — groups gain workspace attribution in this milestone (t002), so environment membership must respect the same workspace boundary (an environment and a group in different workspaces never link).
- Spec: `environment.envGroupIds` + envGroup POST `environmentId` (pinned copy, 2026-07-14); found by the round-7 field diff.

## Files

- `lego/backend/internal/environments/`, `lego/backend/internal/envgroups/`

## Acceptance criteria

- [x] An env group joins/leaves an environment via all three surfaces; `envGroupIds` reads back on the environment; cross-workspace link refused; ADR032's "Environments cover" list updated.

## Out of scope

- Dashboard environments-panel UX for groups (follow-up note if Render's UI has it).
