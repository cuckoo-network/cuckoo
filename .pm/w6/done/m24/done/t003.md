---
id: w6/m24/t003
title: Scope list/get/reveal to the caller's workspace
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m24/t002]
---

## Objective

`ListEnvGroups` returns only the resolved workspace's groups; get/reveal on a group in another workspace is 403 (not absent-leak vs 403 inconsistency — match the m17/m18 convention). Thread `ownerId`/switcher selection the way usage/api-keys do (`core.WithWorkspace`).

## Files

- `lego/backend/internal/envgroups/{service,rest,graphql,mcp}.go`

## Acceptance criteria

- [x] The t001 list + reveal leaks now fail closed; a member's own groups still fully work (regression test).
