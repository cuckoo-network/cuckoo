---
id: w6/m116/t005
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m116/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- The specific target: `app.Spec.IdleTTLSeconds` is read directly in four places (`:1638`, `:1649`, `:1654`, `:2098`). If t002 introduced an effective-TTL helper, every one of those should go through it — a second direct read is exactly how `autoSleepEligible` and `idleRequeueAfter` would drift apart on the default.
- `autoSleepEligible` and `shouldAutoHibernate` are deliberately split (`w6/m94`) so the routing and scheduling questions can differ while an App wakes. Do not merge them; the comment above them explains why.

## Steps

1. Run `/simplify` over the touched files.
2. Confirm there is exactly one place that answers "what is this App's effective idle window?", and paste the grep count.

## Files

- files touched by t002/t003

## Acceptance criteria

- [ ] `/simplify` run; behavior-preserving changes applied.
- [ ] One effective-TTL resolution point, verified by grep.
- [ ] `autoSleepEligible`/`shouldAutoHibernate` remain separate.

## Out of scope

- Behavior changes.
