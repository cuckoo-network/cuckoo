---
id: w6/m116/t007
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m116/t005, w6/m116/t006]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet live, re-running this milestone's own sequence: create a free service, read its Settings label, leave it idle past 300s, then exercise the nonzero path and the wake. Confirm the deployed build contains the fix before reading the result as a pass.
2. Set every remaining task's `status: done`.
3. Move each `tNNN.md` to `w6/m116/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
5. Move the whole `w6/m116/` directory to `w6/done/m116/`.
6. Check `- [x]` for `m116` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live, not assumed — including the idle observation, which takes longer than the shortest preset and cannot be shortcut.
- [ ] The milestone's four "Unverified this run" items are each verified or restated as still-unverified in the closeout evidence.
- [ ] If t001 chose B, the effect on pre-existing free services is confirmed as intended and observed on at least one.
- [ ] Milestone moved to `w6/done/m116/` with all statuses in sync.

## Out of scope

- New work surfaced during verification — file it separately.

## Outcome

Decision (B, 15 min) implemented, all eight+one surfaces corrected, docs written, tests added, simplified to one resolution point. Gates green: operator `make test` (envtest), backend `go test ./...`, `make lint` (four modules), dashboard typecheck + tests. Milestone moved to `w6/done/m116/` with statuses in sync and the workstream box checked.

**Live DoD not run this session (honest):** no cluster/QA access and the local mock cluster is down, so the create → idle-past-window → hibernate → wake sequence and the observation on a pre-existing free service (`eden-dash-v3`) were **not** executed — the idle observation also cannot be shortcut. Behavior is pinned deterministically by the operator envtest + unit tests (which force the idle/terminating state rather than waiting for it). The four "Unverified this run" items are restated as still-unverified in the README's Resolution section; none was resolved live here. The effect on already-running free `idleTTLSeconds: 0` services (they begin sleeping on the 15-min default) is the intended, deliberate change from the DoD, exercised in envtest but not observed on a live tenant this session.
