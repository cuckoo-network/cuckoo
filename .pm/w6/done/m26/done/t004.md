---
id: w6/m26/t004
title: Verify the unmodified CLI live and add the harness guard
worker: worker6
status: done
estimate: 35m
depends_on: [w6/m26/t003]
---

## Objective

Prove the fix through the repository's unmodified `./cli/render` binary against w6's isolated dev environment, then make that path part of the repeatable compatibility verification harness and update the evidence ledger.

## Context

- Current live evidence is `failed to list instances: 404 Not Found` in `docs/cli-compatibility-checklist.md`.
- `.pm/w6/dev-6/` already hosts an isolated authenticated API/dashboard stack and a running hello-world service.
- `scripts/cli-compat.sh verify` currently calls out service instances as manually tested but unguarded.

## Steps

1. Rebuild/restart dev-6 with the route and authenticate the official CLI without printing credentials.
2. Run `services instances` against a running service and a zero-instance/suspended service; exercise a foreign or nonexistent service id.
3. Add a self-cleaning harness assertion that checks `id` and `createdAt`, not merely exit code.
4. Mark the checklist row ✅ with the exact live evidence and update/retire the old w2 ownership references.

## Files

- `scripts/cli-compat.sh`
- `docs/cli-compatibility-checklist.md`
- `.pm/w2/done/008.md`

## Acceptance criteria

- [x] The unmodified CLI exits zero and renders real instances in JSON and interactive/text mode.
- [x] Empty and denied/not-found behavior is exercised live.
- [x] The verification script fails if the endpoint regresses to 404, `null`, or a malformed instance object.

## Out of scope

- Patching or forking `render-oss/cli`.
