---
id: w6/m50/t005
title: Render parity
worker: worker1
status: done
estimate: 20m
depends_on: [w6/m50/t002, w6/m50/t003, w6/m50/t004]
---

## Objective

Confirm the blueprint-sync-error fields and error-handling behavior are consistent across REST, GraphQL, MCP, and the dashboard UI, and check for drift against render.com's equivalent blueprint-sync-status surface.

## Steps

1. Confirm REST, GraphQL, and MCP (t002) all expose the same error-message field with the same semantics for the same failed sync.
2. Confirm the dashboard (t003) renders exactly what the API returns — no silent normalization dropping it.
3. Compare against render.com's blueprint/IaC sync-failure UX (does Render show a sync-failure reason in its dashboard?) and record any deliberate divergence in `docs/ADR018-render-parity.md` rather than leaving it undocumented.
4. t004 (global search) has no REST/GraphQL/MCP surface — dashboard-only, no parity check needed there.

## Acceptance criteria

- [x] REST/GraphQL/MCP agree field-for-field on the new error data. All three wire off the identical `BlueprintSyncView.ErrorMessage`/`errorMessage` field; round-trip parity proven by `TestRESTListBlueprintSyncsIncludesErrorMessage`, `TestGraphQLBlueprintSyncsIncludesErrorMessage`, `TestMCPListBlueprintSyncsIncludesErrorMessage`.
- [x] Any Render divergence is documented, not silent. Recorded in `docs/ADR018-render-parity.md`: Render's own docs (blueprint-spec, infrastructure-as-code) document validation errors shown at Blueprint-creation time but don't specify what a later failed-sync history entry shows, and no Render account was available to check the live dashboard — logged as an honest "no confirmed divergence either way," not asserted as parity.
