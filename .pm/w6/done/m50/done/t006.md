---
id: w6/m50/t006
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w6/m50/t005]
---

## Objective

Run `/simplify` over everything this milestone touched (migration, store, apps service/graphql/rest/mcp, dashboard route + search dialog) — reuse, simplification, efficiency; behavior-preserving only.

## Outcome

Extracted `errMsgPtr(err error) *string` in `lego/backend/internal/apps/blueprint.go` to dedupe the identical "nil on success, err.Error() on failure" conversion that both `CreateBlueprint` and `runSync` needed before calling `UpdateBlueprintSync`. No other duplication found across the migration, store, GraphQL/REST/MCP fragments, or the dashboard route/search-dialog changes — each surface's existing pattern (e.g. `combobox.tsx`'s `shouldFilter={false}` precedent) was reused rather than reinvented.
