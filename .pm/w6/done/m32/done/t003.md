---
id: w6/m32/t003
title: Extend the workspace-delete purger to env groups
worker: worker6
status: done
estimate: 60m
depends_on: []
---

## Objective

The workspace-delete purge sweep deliberately excluded env groups because they had "no tenant attribution" — stale since w6/m24 shipped workspace attribution. Extend the purger so deleting a workspace deletes its env groups (store rows + any projected state), leaving no orphans.

## Context

- Exclusion recorded at `.pm/w7/done/m12/t001.md:46`, already annotated "the workspace-purger's own exclusion should be re-examined".
- w6/m24 gave env groups `ownerId` workspace attribution (incl. the lazy on-read migration).
- Purger location: the workspace-delete path shipped by w7/m12 ("delete really deletes") in `lego/backend` — find via the m12 task files / grep for the purge sweep.

## Steps

1. Locate the purge sweep; add env groups keyed on the attributed `ownerId`.
2. Handle groups still lacking attribution (pre-migration rows): decide delete-by-legacy-key vs skip-with-log, mirroring how the purger treats other partially-attributed resources.
3. `go build ./... && go test ./...`.

## Files

- workspace purger in `lego/backend/internal/` (locate from w7/m12)
- `lego/backend/internal/envgroups/`

## Acceptance criteria

- [x] Deleting a workspace with env groups leaves zero env-group rows for that owner.
- [x] Unattributed legacy rows handled per the documented decision, never silently skipped.

## Out of scope

- Single-service delete semantics (unchanged).
