---
id: w6/m32/t007
title: Test coverage
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m32/t005]
---

## Objective

Add meaningful tests: (a) env-groups pagination walk (terminates, full coverage, envelope shape asserted); (b) workspace-delete orphan regression (workspace with groups → delete → zero rows remain); (c) migration audit event fires once. Each must assert the real failure mode, not snapshots.

## Files

- `lego/backend/internal/envgroups/` tests + purger tests

## Acceptance criteria

- [x] All three behaviors locked; `go test ./...` passes.
