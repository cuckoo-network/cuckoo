---
id: w6/m38/t009
title: Test coverage
worker: worker6
status: done
estimate: 1h
depends_on: [w6/m38/t007]
---

## Objective

Add meaningful regression coverage for the CRD-to-CNPG behavior, every adapter, failure modes, legacy compatibility, and credential redaction introduced by this milestone.

## Context

- This feature crosses `types`, backend, operator, generated CRD schema, dashboard, and a live CLI verifier.
- The costly failures are false success, API/data-plane identity mismatch, accidental post-bootstrap mutation, broken legacy reconciliation, and Datadog API-key disclosure.

## Steps

1. Cover both-custom, one-custom/one-default, both-default, invalid, dry-run, legacy, and recovery cases.
2. Assert exact CNPG bootstrap database/owner projection and unchanged legacy spec output.
3. Add cross-adapter REST/GraphQL/MCP/Blueprint tests plus dashboard create-form behavior.
4. Cover create/update Datadog rejections and prove the supplied secret never appears in response/audit/log capture.
5. Run `make test` from `lego/operator`, `go test ./...` from `lego/backend`, relevant dashboard tests/typecheck, and lint in proportion to all touched modules.

## Files

- relevant `lego/types`, `lego/operator`, `lego/backend`, and `dashboard` test files

## Acceptance criteria

- [x] Tests assert real persistence/projection/connectivity semantics and named failure modes, not snapshots alone.
- [x] Operator/types and backend required suites pass.
- [x] Relevant dashboard tests/typecheck and lint pass.
- [x] Generated files have no drift after the test/codegen gate.

## Out of scope

- Inflating coverage with tautological tests.
