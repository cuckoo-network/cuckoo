---
id: w6/m38/t006
title: "Unmodified official CLI + live CNPG proof; update checklist and Postgres ADR"
worker: worker6
status: done
estimate: 1h
depends_on: [w6/m38/t004, w6/m38/t005]
---

## Objective

Prove the user-visible result with the unmodified official Render CLI and a real CNPG cluster, then replace the checklist's gap statements with reproducible evidence and document the new physical-name contract.

## Context

- The source checklist is a compatibility evidence ledger, not a unit-test wish list; a green flag requires exercising the actual client wire shape.
- A response echo is insufficient. The test must connect to the initialized database using the returned connection information and verify the current database/current user.
- Existing official-CLI verifiers in `scripts/` and completed w6 milestones are the pattern: self-cleaning resources, no fork/vendor of the CLI, and no credential output.
- Repo rules forbid printing kubeconfig or secret contents.

## Steps

1. Add/extend a self-cleaning official-CLI verifier that creates with both flags, waits for Ready, and checks REST readback.
2. Connect without printing the password/URL and assert `current_database()` and `current_user` match the requested values.
3. Exercise independent omission/default cases and the Datadog 400 path.
4. Update `docs/cli-compatibility-checklist.md`, `docs/ADR009-postgresql-management.md`, `docs/ADR006-bex-api.md`, and any parity-ledger row with exact evidence and legacy/default semantics.

## Files

- `scripts/` official CLI Postgres verifier
- `docs/cli-compatibility-checklist.md`
- `docs/ADR009-postgresql-management.md`
- `docs/ADR006-bex-api.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [x] Unmodified CLI create with both custom fields reaches Ready and a real SQL query proves both identities.
- [x] The verifier always deletes its Database resource and never prints connection credentials or kubeconfig contents.
- [x] Each checked checklist box links its status to captured command/behavior evidence; Datadog is marked as a deliberate non-goal with explicit 400 behavior, not silent success.
- [x] Markdown is formatted with the pinned Prettier version.

## Out of scope

- Forking or patching `render-oss/cli`.
