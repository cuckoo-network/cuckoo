# Invite-gate error links to the plan-change section (m12 CTA)

The Hobby member-limit rejection in the invite dialog ("Your plan's member limit is reached — upgrade to invite more.", observed live in `done/m11/README.md` t004) is a dead end: no link to the upgrade path. m12 shipped the plan section + change-plan dialog on workspace settings — add a CTA from the invite-gate error to it (link or button that routes to `/workspace/settings` plan section, ideally opening the dialog).

Sub-hour: one dialog component edit (`dashboard/src/features/team/`, the invite dialog's error state) + a test asserting the CTA renders on the limit error. Not a milestone.
