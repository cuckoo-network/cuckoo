---
id: w6/m93/t006
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m93/t005]
---

## Objective

Close the milestone once its definition of done is actually met (not merely once the code is written): set every task's `status: done`, move each `tNNN.md` to `w6/m93/done/`, mark every row `— **DONE**` and `**Status:** done` in this `README.md`, move the whole `w6/m93/` directory to `w6/done/m93/`, and check `- [x]` in `w6/README.md`.

## Acceptance criteria

- [ ] The milestone's Definition of done (both fixes live-verified against `dashboard.bex.co`, control cases unaffected) holds, not just that code was written.
- [ ] All prior tasks moved to `done/`, statuses synced across task frontmatter, milestone README, and workstream README.
- [ ] `w6/m93/` moved to `w6/done/m93/`.

## Note

Per the `w6/m11` → `w6/m13` / `w6/m50-52` → `w6/040` precedent: if the production deploy pipeline (`deploy.yml`, tracked broken in `w6/040.md`) is still failing when the code is complete and gated green, close the milestone anyway and carry the live-verification residual into `w6/040.md` rather than holding this open on an external infrastructure blocker. Check `w6/040.md`'s current status before closing.

## Closeout (2026-08-25)

Closed with its live leg carried to `w6/040`, the same shared blocker as `m50`/`m51`/`m52`: the definition of done ends in a live check on `dashboard.bex.co`, and `deploy.yml` has failed on every commit since 2026-08-24 (a `fatal error: fault` in the opensandbox image build on the self-hosted runner — infrastructure, not code; every test job on those commits is green). Both fixes are implemented, gated, and regression-tested.

One clause of the definition of done is retired rather than carried, and that is the milestone's main finding: **"the live-tail output for each run matches the historical query, with no duplicate lines" was never established as a defect.** t002's triage shows the mechanism it blamed cannot produce a duplicate — an exact replay yields a byte-identical key, which is precisely what the existing dedupe drops — and the live evidence (live-on vs live-off) compares an accumulating session buffer against a bounded time window, so it does not isolate one. Re-verifying it would only re-confirm behavior that already holds. What `w6/040` should check for this milestone is the Postgres connection-string clause, plus the resume behavior (a reconnect no longer re-reads the pod's whole log).
