---
id: w6/m128/t004
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m128/t003]
---

## Objective

Run `/simplify` over this milestone's diff — the cancel path and whatever seam `t001` opened into the fact helpers — behaviour-preserving.

## Context

- `lego/backend/internal/deploys/service.go` and `lego/backend/internal/store/reconciler.go`.
- If `t001` exported or moved `buildLifecycleFacts`/`buildEndedStatus` to reach them from the deploys package, the tidy-up question is whether that seam is the right shape or whether the fact assembly belongs in one place both callers use.
- Keep `buildEndedStatus`'s two cancel branches distinct and commented — the `isBuildPhase` split is the whole point, and collapsing it would silently change what a post-build cancel reports.

## Steps

1. Run `/simplify` over the diff.
2. Apply behaviour-preserving improvements only.
3. Re-run `cd lego/backend && go test ./...`.

## Files

- The files `t001` touched

## Acceptance criteria

- [ ] No behaviour change; `t005`'s tests still pass.
- [ ] The two cancel branches remain distinct.
- [ ] `source_key` idempotency is intact.

## Out of scope

- Restructuring the reconciler's fact pipeline.
