---
id: w6/m121/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m121/t006, w6/m121/t007]
---

## Objective

Close the milestone once every task is complete **and its definition of done actually holds** — including the live browser checks, which CI cannot run.

## Context

- The DoD is deliberately live-verifiable: it requires a real `fetch(..., {method:'DELETE'})` from `https://dashboard.bex.co` completing on **two** route families. Do not close on green unit tests alone.
- `.pm/w6/060.md` is this milestone's originating note; confirm it sits at `w6/done/060.md` and is not duplicated back into the open inbox.

## Closeout evidence (2026-08-31)

- `797b6a4829e6fc801c7e08f4b2d860a6b77ca16b` shipped the implementation; descendant production run [33465032732](https://github.com/bex-co/bex/actions/runs/33465032732) rolled bex and the dashboard successfully after every test and blocking critical-vulnerability gate passed. The run concluded `cancelled` only in the later `Post CVE scan — OpenSandbox server HIGH+CRITICAL (report)` cleanup; all deploy work and the final credential scrub were already green.
- Raw production preflights advertised `DELETE, OPTIONS`, `PATCH, OPTIONS`, and `PUT, OPTIONS` on routed endpoints. The header allowlist was exactly `Authorization, Content-Type, Idempotency-Key, X-Session-Token`, max-age was 7200, the dynamic Vary headers were present, and a non-allowlisted origin received no CORS grant.
- Signed-in Chrome 152 established an authenticated REST GET control at 200, then completed two DELETE route families plus PATCH, PUT, and webhook resend requests. The nonexistent probes resolved as handler 404s rather than browser CORS failures, and captured resend traffic included `idempotency-key`.
- t003's live EventSource result remains recorded in the milestone: reconnect sent browser-owned `Last-Event-ID` without an OPTIONS preflight, so the header stays excluded.
- The temporary authenticated browser storage state was deleted after the run.

## Steps

1. Re-run the live DoD checks from a real browser session on production, on two route families, for DELETE plus a PATCH and a PUT route.
2. Confirm `t003`'s `Last-Event-ID` answer is written down and `t002` acted on it.
3. Set every remaining task `status: done`, move each `tNNN.md` to `w6/m121/done/`, mark rows `— **DONE**`, set `**Status:** done`.
4. Move `w6/m121/` to `w6/done/m121/` (leave no stub) and check `- [x]` in `.pm/w6/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m121/`, `.pm/w6/README.md`

## Acceptance criteria

- [x] Live DoD checks re-run and passing on production, on two route families — recorded with the captured preflight, not asserted.
- [x] Milestone moved to `w6/done/m121/` with no tombstone at the old path.
- [x] Status is consistent across the workstream README, the milestone README and every task's frontmatter.

## Out of scope

- Any follow-up surfaced by `t003` (e.g. a genuine SSE resume defect) — file it separately rather than holding this milestone open.
