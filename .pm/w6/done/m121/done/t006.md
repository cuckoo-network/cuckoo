---
id: w6/m121/t006
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m121/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — `withCORS` and the router-enumeration seam — for reuse, simplification and efficiency, behavior-preserving.

## Context

- `lego/backend/internal/api/auth.go` (`withCORS`, `:708-734`) is the whole footprint, plus whatever seam `t001` added.
- Watch specifically for: the verb set being recomputed per request when it could be computed once at construction, and the `Vary` `Add`-not-`Set` subtlety from `e2394e52` surviving any tidy-up.

## Steps

1. Run `/simplify` over the changed code.
2. Apply behavior-preserving improvements only.
3. Re-run `cd lego/backend && go test ./internal/api/...`.

## Files

- `lego/backend/internal/api/auth.go` and the files `t001` touched

## Acceptance criteria

- [x] No behavior change; `t004`'s drift guard and `t007`'s tests still pass.
- [x] Per-request work that can be hoisted to setup has been hoisted, or a note says why not.

## Completion evidence

- `/simplify` ran its reuse, quality and efficiency reviews. Applied: one authoritative router topology (matcher and served handler cannot diverge), named `serverMuxes` instead of positional mux returns, one mount-pattern constant, the existing all-surfaces test fixture, shared comma-token parsing, removal of dead/redundant code, and a shallow request copy for the route probe.
- Origin parsing, the delegated topology and reviewed constants are setup-time work. The remaining per-preflight operation is one or two `ServeMux.Handler` lookups because the answer is deliberately route- and requested-method-specific; `Max-Age: 7200` keeps that path off ordinary requests and amortizes browser preflights.
- After cleanup: `go test ./internal/api/...`, full `go test ./...`, and `make lint-backend` all pass.

## Out of scope

- Refactoring CORS handling into a third-party middleware library — out of scope for this milestone.
