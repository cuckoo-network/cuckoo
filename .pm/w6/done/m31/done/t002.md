---
id: w6/m31/t002
title: REST create: accept + bind `registryCredentialId`
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m31/t001]
---

## Objective

`POST /v1/services` accepts `registryCredentialId`, membership-checks the credential against the target workspace, and binds it so the created App pulls with it.

## Context

- w2/m14's binding mechanism (credential → materialized Secret → App reference).
- Cross-tenant rules: foreign/nonexistent credential id → the w6/m22 403/404 split.

## Steps

1. Remove the field from the unsupported list on create; decode + validate.
2. Bind through the same core verb the native create path uses.

## Files

- lego/backend/internal/apps/rest.go
- lego/backend/internal/apps/service.go (if the core create needs the field threaded)

## Acceptance criteria

- [x] Create with a valid credential id binds it; foreign id refuses correctly.

Valid ids persist to Postgres/CR intent and materialize the exact Secret; unknown/foreign ids are 404/403 before the App write.

## Out of scope

- Patch + other surfaces (t003).
