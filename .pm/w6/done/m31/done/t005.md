---
id: w6/m31/t005
title: Render parity
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m31/t004]
---

## Objective

Check the field is consistent across REST, GraphQL, MCP, and the dashboard picker (same semantics/errors) and against Render's behavior; flag drift as follow-up.

## Acceptance criteria

- [x] Four surfaces compared against t001's contract; drift filed, not silent.

The Existing Image flow is covered on all four surfaces, REST reads include Render's `{id,name}` summary, and the dashboard's “None” maps to explicit no-auth rather than omission/legacy auto-match. Render's separate Docker source-build placement needs safe BuildKit auth and is filed as `w6/017` rather than accepted as a no-op.
