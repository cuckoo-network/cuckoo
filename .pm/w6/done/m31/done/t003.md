---
id: w6/m31/t003
title: REST patch + GraphQL/MCP symmetry
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m31/t002]
---

## Objective

`PATCH` accepts `registryCredentialId` (set/change/clear per Render's semantics from t001), and GraphQL/MCP create+update expose the same capability with the same semantics.

## Files

- lego/backend/internal/apps/rest.go
- lego/backend/internal/apps/graphql.go
- lego/backend/internal/apps/mcp.go

## Acceptance criteria

- [x] All three surfaces accept the field with identical semantics and error shapes.

REST/GraphQL/MCP expose create plus set/change/empty-clear through the same Core verb; the dashboard create picker uses the GraphQL path.

## Out of scope

- Dashboard changes (w2/m14 already shipped the UI picker; confirm-only in t005).
