---
id: w6/m43/t004
title: Render parity
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m43/t001, w6/m43/t002, w6/m43/t003]
---

## Objective

Check that t001-t003's fixes are consistent across every surface bex exposes
this behavior on, and that no equivalent copy/status bug exists elsewhere the
live QA pass didn't happen to click through.

## Context

- These three findings are dashboard-UI-only (no REST/GraphQL/MCP field or
  semantics changed) — but the New Service wizard and status badge each have
  more than one entry point worth re-checking after the fix.
- Full QA report with the original live-Render comparison:
  https://claude.ai/code/artifact/29934e6e-7bd8-4fa0-8a86-26327bb72932

## Steps

1. Re-check the New Service wizard for Private Service and Static Site too
   (not just Background Worker/Web/Cron) — confirm t001/t003 didn't leave
   either of those two showing mismatched copy.
2. Check the services list page and the service-detail overview panel (both
   render the same status badge component per `service-status-badge.tsx`'s
   own comment about "list, detail header, and the overview panel") for the
   t002 fix — confirm "Sleeping" doesn't leak into any of the three
   surfaces for a resuming worker.
3. Confirm `cron_job` (which also can't auto-sleep or bind a port) is
   correctly covered by both t001 and t002's gates, not just
   `background_worker`.

## Files

- `dashboard/src/features/services/components/service-status-badge.tsx`
- `dashboard/src/routes/index.tsx` (services list)
- `dashboard/src/routes/services.$serviceId.index.tsx` (overview panel)

## Acceptance criteria

- [ ] Every surface that renders the New Service copy or the status badge
      shows correct, type-appropriate text for all five service types.
- [ ] Any additional drift found is filed as a follow-up note rather than
      silently left.

## Out of scope

- New REST/GraphQL/MCP endpoints or fields — none of this milestone's fixes
  touch the wire contract.
