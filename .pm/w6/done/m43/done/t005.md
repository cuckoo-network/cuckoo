---
id: w6/m43/t005
title: Simplify
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m43/t004]
---

## Objective

Run `/simplify` over the files t001-t004 touched — reuse, simplification,
efficiency, and altitude cleanups only, no behavior change.

## Context

- Likely candidates: whether `showNoUrlNote`'s type set should now be the
  single shared source of truth for "does this type have an HTTP port/URL"
  across t001 and t002 (avoid two independent type-set checks drifting apart
  later), and whether the new per-type title/description locale keys from
  t003 can share a lookup table instead of a growing ternary/switch chain.

## Files

- Whatever t001-t003 touched: `service-source-picker.tsx`, `services.new.tsx`,
  `create-service-input.ts`, `status.ts`, `types.ts`, `locales/en.ts`.

## Acceptance criteria

- [ ] `/simplify` run and its findings applied or explicitly noted as
      declined with reasoning.

## Out of scope

- Bug hunting — that's what t001-t004 were for.
