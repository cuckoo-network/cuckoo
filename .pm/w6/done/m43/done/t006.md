---
id: w6/m43/t006
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m43/t004]
---

## Objective

Add tests that assert the real, previously-broken behavior — each should
fail against the pre-fix code and pass after.

## Context

- `dashboard/src/routes/__tests__/services.new.test.tsx` already exists and
  is the natural home for t001/t003's assertions (it already tests the
  create-wizard's cron-type branching, per the file the earlier research
  pass found: `"Background Worker" radio + "no-public-url note"` cases are
  already covered there — extend that pattern rather than duplicating it).
- `status.ts`/`types.ts` likely have (or should get) a `__tests__` sibling
  for `deriveStatus()`/`isSleeping()` — check what exists before adding a new
  file.

## Steps

1. Test: Background Worker + Existing Image tab does not render the `$PORT`
   hint; Web Service + Existing Image tab still does.
2. Test: `deriveStatus()` given `{ type: "background_worker", suspended:
   false, phase: "Hibernated" }` does not return key `"sleeping"`; given
   `{ type: "web_service", suspended: false, phase: "Hibernated" }` it still
   does.
3. Test: New Service page title/subtitle for each of the five service types
   matches that type (no type shows the `web_service` strings unless it is
   `web_service`).

## Files

- `dashboard/src/routes/__tests__/services.new.test.tsx`
- `dashboard/src/features/services/lib/__tests__/status.test.ts` (create if
  it doesn't exist)

## Acceptance criteria

- [ ] All three new/extended test cases fail on a pre-t001/t002/t003
      checkout and pass after.
- [ ] `yarn test` green in `dashboard/`.

## Out of scope

- Broader test-suite refactors unrelated to this milestone's behavior.
