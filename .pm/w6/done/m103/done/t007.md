---
id: w6/m103/t007
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m103/t006]
---

## Objective

Close the milestone once its Definition of done actually holds — not merely once the code is written.

## Status (2026-09-03) — implementation done + locally verified; only prod live-verify remains

t001–t006 are done. Bug B's fix was reproduced and verified against a **local production SSR build** (concurrency race 95/192 → 0/480; 5 zh-cookie reloads with zero #418; 2885 tests + lint + build green — see the milestone README's implementation addendum). What is left before this milestone can move to `w6/done/m103/`:

1. Ship + deploy the dashboard fix.
2. On `dashboard.bex.co`, signed in with a non-English session: reload 5× and confirm zero React #418 (`browser_console_messages`).
3. Fire two concurrent requests to the same route with different `i18nextLng` cookies and confirm each response's `<title>`/body matches its own language (the local prod-build check already passes; this confirms it under real production concurrency).
4. Re-run the **`w6/030`** repro (`/env-groups/<id>` under a non-English session) and record the outcome in `w6/030.md` + here — auth-gated, so it could not run in the offline stub.
5. Then perform the mechanical close (steps below).

## Steps

1. Confirm every item in the milestone README's Definition of done is independently verified (live reload test showing zero #418 across 5 consecutive reloads under a non-English session; concurrent-request test showing no cross-request language contamination; `w6/030` re-check outcome recorded either direction).
2. Set every task's frontmatter `status: done`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w6/m103/README.md`.
4. Move each `tNNN.md` to `w6/m103/done/`.
5. Move the whole `w6/m103/` directory to `w6/done/m103/`.
6. Check `- [x]` for **m103** in `w6/README.md`.
7. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m103/*` (moved), `.pm/w6/README.md`

## Acceptance criteria

- [ ] Definition of done independently re-verified, not assumed from task completion alone.
- [ ] Board state (task frontmatter, milestone README, workstream README) all agree.
- [ ] `w6/m103/` no longer exists at its original path; `w6/done/m103/` does.

## 2026-09-05 — triage: fix confirmed in the in-flight deploy; live-verify gated on it landing

The fix commit `f6c4506e` is on `main` but NOT yet in the deployed image pin (`4bd6eb2a3129`). The `deploy.yml` run for `88e05205` (which contains `f6c4506e`) passed every test gate this session and is in `build-and-deploy` (image push) as of 23:xxZ — so m103's live-verify is gated purely on that run landing un-superseded, not on any code or credential gap. Authenticated browser access is available this session (synthetic QA identity via the Kratos session), so the 5×-zh-reload / concurrent-cookie / `w6/030` re-check legs can run the moment the pin advances off `4bd6eb2a3129`. No code change owed; still deploy-gated.

## 2026-09-05 — DEPLOYED and FULLY LIVE-VERIFIED; closing

`88e05205` deployed (image pin `88e0520539f5`, which contains the fix commit `f6c4506e`). Ran every remaining DoD leg against `dashboard.bex.co` with an authenticated zh session:

- **Bug B, SSR isolation — no cross-request contamination.** 8 rounds of two *concurrent* SSR requests to `/` with different `i18nextLng` cookies (zh vs en, same session): all 16 responses matched their own cookie — every zh request `<html lang=zh>` + Chinese body, every en request `<html lang=en>` + no Chinese. 0/8 contaminated (pre-fix this race produced ~49% cross-contamination).
- **Bug B, reload stability — zero #418.** 5 consecutive zh-cookie reloads of `/`: all `<html lang=zh>`, Chinese rendered, **0 React #418** and 0 console errors each (English was already the clean control).
- **Bug A, immediate switch.** Starting in English ("Overview"), the account-menu **Language → 中文** click switched the whole UI to Chinese *without a reload* — tab title `概览`, nav 项目/蓝图/智能体/环境变量组/通知/账单, `<html lang=zh>`, cookie `i18nextLng=zh`. Pre-fix it stayed English until a reload.
- **`w6/030` re-check (auth-gated, couldn't run offline).** Created env group `evg-daear4ld4jps73fdn450`, loaded `/env-groups/<id>` under the zh session 3× fresh: Chinese rendered, group name shown, **0 React #418** every load. The `formatDateTime` timezone-of-instant mismatch that `w6/030` covers does not reproduce. Env group deleted after.

Every DoD bullet has direct live evidence. Closing; recorded the `w6/030` outcome here (its own note can be closed on this evidence).
