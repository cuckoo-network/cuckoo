---
id: w6/m135/t007
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m135/t006]
---

## Objective

Close the milestone only after the observable pair lifecycle is real and every task is complete.

## Context

- PM completion moves all task files and then the milestone directory into `done/`.

## Steps

1. Re-run the definition of done, including live pending and verified pair checks.
2. Confirm every task and required test is complete.
3. Run `/pm done w6/m135/t007` last.

## Files

- `.pm/w6/m135/`
- `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every definition-of-done bullet has direct evidence.
- [ ] Every task is marked done and moved.
- [ ] The milestone lives at `w6/done/m135/` and its workstream checkbox is checked.

## Out of scope

- Closing while required live evidence remains missing.

## State at 2026-08-29 — deploy precondition met; live leg blocked on credentials

Production runs the image built from `2a9a7240` — this milestone's fix commit — verified read-only against the `hetzner-prod` cluster (digest `52ab1315…` on all five platform Deployments, pin `f3d16ac5`). What remains is exactly the live pair-lifecycle evidence (delete one half of an auto-added www↔apex pair, observe the disclosed lifecycle on every surface), which needs an authenticated session; QA credentials are absent (`scripts/qa-login.sh` exits 2), the same blocker every near-done w6 closeout shares — see `w6/README.md` § Closeout sweep — 2026-08-29.

## Closed 2026-08-29 — live pending-pair matrix passed

On production web service `srv-da96c439ui0s73db2aog` (synthetic QA workspace), with the fix (`2a9a7240`) verified running (cluster digest check in the w6 sweep):

- **Apex-first:** adding `qa-sweep-m135.com` auto-generates `www.qa-sweep-m135.com` with `redirectForName: qa-sweep-m135.com`; deleting the canonical apex **cascades** — 0 survivors, no silently-converted sibling.
- **www-first:** adding `www.qa-sweep-m135b.com` auto-generates the apex redirect; deleting the **generated** sibling alone leaves the explicit www canonical (still `redirectForName: null` — an explicit direct claim preserved, the disclosed rule); deleting the canonical www cascades to 0.
- **Four surfaces agree:** REST list, GraphQL `customDomains(id:)`, and MCP `list_custom_domains` all report the identical pair + `redirectForName` state; the dashboard delete confirmation reads **"This removes qa-sweep-m135.com and its automatically added redirect www.qa-sweep-m135.com. Both Ingress rules are removed…"** — naming every removed domain before the write.
- **Evidence limit, recorded per this board's rule:** the **verified**-pair half of the matrix needs DNS ownership of a real domain, which no QA identity has; it remains covered by the milestone's regression tests only (the delete path does not branch on verification status beyond TLS cleanup). Do not re-open for this unless a bex-owned test domain becomes available.
- Auth/uniqueness/TLS/idempotency bullets were CI-proven (recorded in the milestone).
