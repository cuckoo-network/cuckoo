---
id: w6/m37/t005
title: "GraphQL: confirm arg on deleteDatabase/suspendDatabase/deleteKeyValue/suspendKeyValue"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m37/t002, w6/m37/t003]
---

## Objective

Expose t002/t003's `confirm` gate on the GraphQL surface, matching the Apps mutations' existing `confirm` arg.

## Context

- Find the Apps GraphQL `confirm` arg wiring (grep `confirm` in `lego/backend/internal/apps/graphql.go`) as the reference shape.
- `deleteDatabase`/`suspendDatabase` live in `lego/backend/internal/postgres/graphql.go`; `deleteKeyValue`/`suspendKeyValue` in `lego/backend/internal/keyvalue/graphql.go`.

## Steps

1. Add an optional `confirm: String` arg to the four mutations.
2. Thread it into the service call the same way t004's REST handlers do.

## Files

- `lego/backend/internal/postgres/graphql.go`
- `lego/backend/internal/keyvalue/graphql.go`

## Acceptance criteria

- [ ] All four mutations accept `confirm` and thread it through.
- [ ] Omitting `confirm` on an unprotected resource is unaffected (no regression).

## Out of scope

- MCP wiring — t006. REST — t004 (already done, this task can run concurrently once t002/t003 land).
