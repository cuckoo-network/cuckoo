---
id: w6/m7/t004
title: Simplify — /simplify over the code this milestone changed
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m7/t003]
---

## Objective

Run `/simplify` over the code this milestone touched (reuse / simplification /
efficiency; behavior-preserving). One resolver for subject⇄own-id — not three
copies across render/graphql/mcp.

## Context

- Changed code: `internal/id/id.go`, `internal/store/` (owner-id table + queries), `internal/workspaces/{service,render,graphql,mcp}.go`.
- Watch for: the subject→own-id mapping invoked per-item in a members loop — batch or memoize if it shows up; a single resolver, not duplicated logic per surface.

## Steps

1. `/simplify` over the milestone diff.
2. Apply behavior-preserving cleanups; re-run tests.

## Files

- The files changed by t001–t002.

## Acceptance criteria

- [ ] No behavior change; tests still green
- [ ] Own-id mapping has a single source of truth

## Out of scope

- New behavior or features.
