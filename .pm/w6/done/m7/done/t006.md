---
id: w6/m7/t006
title: Closeout — move milestone to done when DoD holds
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m7/t005]
---

## Objective

When every other task is complete **and the DoD actually holds**, close the
milestone: mark all tasks done, move each `tNNN.md` to `w6/m7/done/`, set the
README rows `— **DONE**` + `**Status:** done`, move `w6/m7/` → `w6/done/m7/`, and
check `- [x] **m6**` in the w6 workstream README.

## Context

- Do not run until the DoD is real: `own-` ids minted+persisted+stable, member `userId`s are `own-` ids across REST/GraphQL/MCP, and `docs/render-artifacts/owners-api.md` + `w6/done/001.md` updated (parity resolved; `type:user`/peer-resolve recorded as non-goals).
- Convention: `.pm/CLAUDE.md` (done items move to `done/`; keep status synced across all three places).

## Steps

1. Verify the DoD end state is observable (not just code-written).
2. `/pm done w6/m7/t006` last → triggers the milestone move.

## Files

- `.pm/w6/m7/*`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] DoD verified real before moving
- [ ] Milestone in `w6/done/m7/`; w6 README box checked; statuses synced

## Out of scope

- Any implementation (that's t001–t002).
