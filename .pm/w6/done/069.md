# Restore the production Stripe publishable key so bex-api can roll forward

Why: production is pinned to the 2026-09-01 bex image but its new API pods fail closed before startup because the out-of-band Stripe Secret lacks the live-mode publishable key required by the enabled payment-method gate.

Deployment run `33546048149` built and pinned image digest `sha256:e2874bfe4f8ad5ce65eb6ec96dd24b24de217efd478df4a23d6c17cfcc6a28d5`, then timed out with only one new `bex-api` replica because that pod repeatedly exited with `BEX_REQUIRE_PAYMENT_METHOD=all: BEX_STRIPE_PUBLISHABLE_KEY is required for Stripe Payment Element`. The existing `bex-system/bex-stripe` Secret contains a live restricted key and the payment gate, but no `BEX_STRIPE_PUBLISHABLE_KEY`; neither the repository's GitHub Actions secret inventory nor the local operator `.env` contains one. Two old API replicas remain Ready, so the service is available but the rollout is incomplete.

The Stripe account owner must retrieve the matching `pk_live_*` key, restore the full operator-side Stripe configuration (the local `.env` currently has neither the publishable key nor the existing restricted key), explicitly select and verify the `hetzner-prod` kubectl context, then apply it with `BEX_STRIPE_ALLOW_LIVE=1 scripts/stripe-billing-secret.sh`. Do not invent a value, reuse a test key, print it, or commit it. Verify both `bex-api` replicas run the pinned digest and the deployment is fully rolled out afterward.

Resolved 2026-09-03: the account owner supplied the matching live publishable key, and its value exactly matches `bex-system/bex-stripe` without being printed or committed. Production now has 2/2 Ready `bex-api` replicas at digest `sha256:5423ab0b8fb6f6b16f53f23af2e1fdcda5ee018589d601561aa46fe9f755f1de`; the deployment is fully rolled out and healthy. The m117 authenticated live probe subsequently passed for all three databases.
