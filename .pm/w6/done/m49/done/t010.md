---
id: w6/m49/t010
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m49/t009]
---

## Objective

Close the milestone once its Definition of done actually holds, not merely once the code is written.

## Steps

1. Re-verify every bullet in the milestone `README.md`'s Definition of done live, one by one — including the two environments/api-keys/registry-credentials bullets, which may resolve as "not applicable" per t005/t006 rather than "fixed," and that is an acceptable closeout state as long as it's the evidenced outcome, not an assumed one.
2. Set every task's `status: done`, move each `tNNN.md` to `w6/m49/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in this milestone's `README.md`.
4. Move the whole `w6/m49/` directory to `w6/done/m49/`.
5. Check `- [x]` for `m49` in `w6/README.md`.
6. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Definition-of-done re-verification (2026-08-24)

Every bullet re-checked against live evidence, not assumption:

1. Key Value duplicate-name conflict shows the specific reason via the dashboard UI — **live-verified** (t002).
2. Same for Postgres — **live-verified** (t003).
3. Same for Project, decision on adding the name recorded — **live-verified + decision recorded** (t004).
4. Environments confirmed live and fixed (real conflict, not a different-shaped finding) — **live-verified** (t005).
5. API keys/registry credentials confirmed not-applicable with evidence — **already done** (t006, prior session).
6. The mechanism (`core.NewConflictError`/`isNameConflictError`/`conflictOrGenericMessage`) applied consistently across every touched hook, not ad-hoc — **verified**: all 5 create hooks (services, keyvalue, postgres, project, environment) key off the same `extensions.code: "CONFLICT"` signal; services' `setNameConflict` inline-state shape is a deliberate, documented UX difference (t001), not a different detection mechanism.
7. Regression tests for `use-create-service.ts` and `use-create-workspace.ts` — **added/re-verified green** (t009).
8. `docs/ADR018-render-parity.md` updated — **done** (t007).
9. Backend `go test ./...` + `make lint`, dashboard `yarn typecheck && yarn lint && yarn test` — **all green**, re-confirmed at closeout time below.

## Acceptance criteria

- [x] Definition of done verified live, bullet by bullet, with evidence (not assumption) for every "not applicable" outcome.
- [x] All board-sync steps above completed.

## Out of scope

- Re-opening scope decided in earlier tasks (e.g. re-litigating t001's mechanism choice) — closeout confirms the shipped state, it doesn't redesign it.
