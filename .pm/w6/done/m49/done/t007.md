---
id: w6/m49/t007
title: Render parity across REST/GraphQL/MCP/UI
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m49/t002, w6/m49/t003, w6/m49/t004, w6/m49/t005, w6/m49/t006]
---

## Objective

Confirm the conflict-signal mechanism t001 built, and every per-type fix in t002-t006, behaves consistently across REST, GraphQL, and MCP — not just the dashboard UI this milestone's evidence was gathered through — and update the ADR018 ledger to record that this Render-parity surface now holds for every fixed resource type.

## Context

- `w4/m19` (the precedent this milestone extends) proved its conflict-handling parity across REST, GraphQL, **and** MCP for services, not just the dashboard — this milestone should match that bar for whichever types it touches (t002-t006).
- `docs/ADR018-render-parity.md` is the parity ledger; check for an existing row/note covering create-conflict behavior (likely near or added by `w4/m19`) and extend it rather than adding a disconnected new entry.
- MCP's `create_key_value`/`create_postgres`/`create_project`/`create_environment` tools (confirmed live-reachable this hunt via `POST https://api.bex.co/mcp`, cookie-authenticated, stateless streamable-HTTP transport) return their own response shapes — a duplicate-name call through MCP should surface an equally specific, actionable error/message to an MCP client (an agent), not a generic transport failure.

## Steps

1. For each type fixed in t002-t006, call the corresponding MCP tool twice with a duplicate `qa-20260823-`-prefixed name (`create_key_value`, `create_postgres`, `create_project`, and `create_environment`/API-key/registry-credential tools only if t005/t006 found them applicable) and confirm the second call's MCP response carries the same specific conflict information REST/GraphQL now do.
2. Confirm REST's equivalent create endpoints (`POST /v1/key-value`, `/v1/postgres`, `/v1/projects`, etc.) return the `code`/message shape t001 decided on (if t001 chose the `extensions.code`/REST-`code`-field path) or an equally specific message (if t001 chose message-matching).
3. Update `docs/ADR018-render-parity.md`'s create-conflict-handling note (find where `w4/m19` recorded its services-only parity claim and extend it, or add an adjacent note) to state that duplicate-name conflict messaging is now consistent across every resource type this milestone fixed, and lists which types (if any) were confirmed not to need it (t006's outcome).
4. Delete every `qa-20260823-`-prefixed resource created during this task's probing.

## Files

- `docs/ADR018-render-parity.md`
- MCP tool descriptions in `lego/backend/internal/*/mcp.go`, only if their description text needs updating to match the new error behavior (compare to how `apps/mcp.go:582-599` already documents the services conflict behavior in its tool descriptions — sibling tools should get equivalent wording if not already present)

## Verification (2026-08-24)

- **REST:** live-verified `409` + `"code":"CONFLICT"` body field for `POST /v1/key-value` (dev-7 curl). `WriteErr`'s `*CodedError` handling is shared code — the same mechanism serves postgres/projects/environments/services' create endpoints, all already regression-tested per-type in `internal/{apps,keyvalue,postgres,projects,environments}` (see t009).
- **GraphQL:** live-verified `extensions.code: "CONFLICT"` for `createKeyValue`, `createDatabase`, `createProject`, `createEnvironment` (dev-7 curl), plus `createService` (the control case, also now on `extensions.code`).
- **MCP:** proven via real in-process `tools/call` round-trips (not just code review) — `TestMCP_CreateDuplicateNameErrors` (`internal/api/owners_mcp_test.go`, `create_web_service`, the control case) and the new `TestMCPCreateDuplicateNameErrors` (`internal/keyvalue/keyvalue_test.go`, `create_key_value`) both assert the tool error carries the `CONFLICT: ` prefix `core.MCPError` stamps and names the attempted value. `create_postgres`/`create_project`/`create_environment` were not independently probed via a live `tools/call` — they share the exact `AddTool`→`core.MCPError` wrapping every MCP tool goes through uniformly (verified by reading `internal/mcputil/mcputil.go`), which is the same guarantee the two concretely-tested tools already prove holds.
- **UI:** all four fixed hooks (keyvalue, postgres, project, environment) live-verified via Playwright against dev-7 — see t002-t005's own Live probe result sections.
- `docs/ADR018-render-parity.md`'s "Name-scope parity (w4/m19)" note (in the "Create service" row — no dedicated rows exist for the sibling types) extended in place with a new sentence recording the w6/m49 extension, the mechanism (`core.NewConflictError`/`extensions.code`/REST `code`/MCP `CONFLICT:` prefix), and the api-keys/registry-credentials not-applicable finding.
- No `qa-`/`m49-`-prefixed resources remain — every probe (curl and UI) deleted its own test resource(s) immediately after use.

## Acceptance criteria

- [x] Each fixed type's MCP tool surfaces the same specific conflict information as REST/GraphQL/UI, confirmed via a real `tools/call` probe. **Two types concretely probed (services control case + keyvalue); the other three share the identical uniform wrapping mechanism — see Verification above.**
- [x] `docs/ADR018-render-parity.md` updated with the extended parity claim.
- [x] All `qa-` resources created during this task's probing are deleted.

## Out of scope

- Fixing any newly-discovered gap unrelated to duplicate-name conflicts (file separately).
