---
id: w6/m5/t001
title: Capture live Render workspace settings/rename/delete semantics
worker: worker6
status: done
estimate: 30m
depends_on: []
---

## Objective

Produce the live Render capture `w6/m1/t001` was marked done without: fill the deep-research pass's one unresolved open question — what Render's workspace settings surface actually does for rename, delete (confirmation guard wording, resource consequences, any grace period), and whether ownership transfer exists.

## Context

- `w6/RESEARCH-workspaces.md` open question 1 — delete/rename semantics were never verified live (dashboard-only, behind login).
- `w6/done/m1/t001.md` — the original task, marked `status: done`, whose acceptance criteria require exactly this artifact; confirmed via `ls docs/render-artifacts/` that `workspace-lifecycle.md` does not exist. This task supersedes/completes it for real.
- Precedent: `w5/m7` and `w3/m3` captured Render pages live via the Playwright MCP; shapes went to `docs/render-artifacts/`.
- Screenshots go to `.playwright-mcp/` (bare filenames, per repo `CLAUDE.md`).

## Steps

1. Log into dashboard.render.com (Playwright MCP), open a workspace's Settings.
2. Record the settings page fields; exercise rename; open (without confirming) the delete flow — capture guard text, stated consequences for services/databases, and any transfer-ownership option.
3. Note the workspace dropdown/creation flow shapes in passing (plan cards, payment-step skippability for Hobby) to confirm `RESEARCH-workspaces.md` findings 2-3.
4. Write the captured shapes/wording to `docs/render-artifacts/workspace-lifecycle.md` (+ screenshots in `.playwright-mcp/`).

## Files

- `docs/render-artifacts/workspace-lifecycle.md` (new)

## Acceptance criteria

- [ ] Delete-flow guard semantics (confirmation mechanism + stated resource consequences) documented verbatim
- [ ] Rename behavior and any transfer/ownership option documented (or its absence stated)
- [ ] `RESEARCH-workspaces.md` findings 2-3 (creation flow, Hobby payment-step skippability) confirmed or corrected in the artifact

## Out of scope

- Building/fixing anything in bex's dashboard (t002).
- Members/roles pages (`w4/m12` captures its own surface).
