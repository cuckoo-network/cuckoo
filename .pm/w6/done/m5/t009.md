---
id: w6/m5/t009
title: "Closeout: verify DoD, mark done, move to done/"
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m5/t008]
---

## Objective

Verify the milestone's Definition of Done actually holds, then close it out: mark every task done, move task files to `done/`, update the milestone `README.md` status, move the whole milestone directory to `w6/done/m5/`, and check its box in `w6/README.md`.

## Context

- DoD (from the milestone README): `docs/render-artifacts/workspace-lifecycle.md` exists with verbatim Render semantics; bex's UX matches it or documents deliberate drift; the full lifecycle flow is verified live against real infrastructure; the dashboard's hand-authored workspace GraphQL types are confirmed against a live schema.

## Steps

1. Re-confirm each DoD clause: the doc artifact exists, the live-cluster rerun (t003) actually happened this milestone (not reused from a stale prior run), and the codegen diff (t004) is resolved.
2. If it holds: set every task's `status: done`, move each `tNNN.md` to `w6/m5/done/`, mark every row `— DONE` and `**Status:** done` in the milestone README, move `w6/m5/` to `w6/done/m5/`, check `- [x]` in `w6/README.md`.
3. If the DoD does not hold, do not close — file what's missing as a new task instead.

## Files

- `.pm/w6/m5/README.md`
- `.pm/w6/README.md`
- All `.pm/w6/m5/tNNN.md` files

## Acceptance criteria

- [ ] DoD reconfirmed live before closing
- [ ] Milestone moved to `w6/done/m5/` only after confirmation

## Out of scope

- Any new implementation work.
