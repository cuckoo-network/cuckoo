---
id: w6/m133/t007
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m133/t005]
---

## Objective

Add tests asserting the behaviour this milestone shipped and the failure modes it closes — never tautological or snapshot-everything tests.

## Context

Behaviours worth pinning:

- A fresh database's declared-override read is **empty**, and the editor renders empty. This is the assertion whose absence defined the milestone (48 seeded rows).
- Setting one parameter and re-reading returns exactly one.
- Operator-owned names (`restore_command`, `archive_command`, `ssl_key_file`, `port`, and the WAL/replication group) do not reach `spec.parameters` through **any** surface.
- A `client`/`session`-sourced value is never persisted.
- `shared_preload_libraries` stays filtered — correct today, must not regress.
- A legitimate tuning parameter round-trips on REST, GraphQL and MCP.
- The ADR024 `can_create` gate still fires for statement-logging parameters.
- The `pg_settings` read still returns the effective non-default set — it is a correct diagnostic and must survive.

## Steps

1. Add backend tests beside the existing suites in `lego/backend/internal/postgres/`.
2. Add a dashboard test that a fresh database renders an empty editor.

## Files

- `lego/backend/internal/postgres/*_test.go`
- `dashboard/src/features/databases/components/__tests__/`

## Acceptance criteria

- [ ] Each bullet above has a test that fails against the pre-fix behaviour.
- [ ] `cd lego/backend && go test ./...` and `dashboard/yarn test` pass.

## Out of scope

- Live production verification — that belongs to the DoD at closeout.
