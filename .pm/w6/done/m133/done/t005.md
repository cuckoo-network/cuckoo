---
id: w6/m133/t005
title: Render parity sweep (REST/GraphQL/MCP/dashboard)
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m133/t002, w6/m133/t003]
---

## Objective

A new read is added across three surfaces and the write policy changes, so check consistency across REST, GraphQL, MCP and the dashboard, and against render.com's equivalent.

## Context

- bex-api is Render-compatible per `docs/ADR006-bex-api.md`; the three fragments share one service (`internal/api/CLAUDE.md`).
- Render exposes tenant-settable Postgres parameters **without** exposing the provider's internal archival commands — that is the shape to match, and the concrete difference this milestone closes.
- `docs/ADR009-postgresql-management.md` is the governing ADR; `docs/ADR018-render-parity.md`'s Postgres rows are the ledger to reconcile.

## Steps

1. Confirm the declared-overrides read and the write policy behave identically on all three API surfaces.
2. Confirm the error shape for a refused parameter is consistent (REST `code`, GraphQL `extensions.code`, MCP message prefix).
3. Update the ADR018 Postgres row to describe what actually holds.
4. File residual drift separately.

## Files

- `lego/backend/internal/postgres/{rest,graphql,mcp}.go`
- `docs/ADR018-render-parity.md`, `docs/ADR009-postgresql-management.md`

## Acceptance criteria

- [ ] The three API surfaces agree on the read and on the refusal.
- [ ] ADR018's Postgres row matches shipped behaviour.
- [ ] Any remaining divergence is filed, not absorbed.

## Out of scope

- Parity work unrelated to parameters.
