---
id: w6/m133/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m133/t004, w6/m133/t007]
---

## Objective

When every other task is complete **and the definition of done actually holds**, close the milestone.

## Context

- The DoD bullets are live-verifiable on a disposable database: a fresh one shows an empty editor, one set parameter reads back as exactly one, `restore_command` and `ssl_key_file` are refused through the API directly, and the `pg_settings` insight still returns its rows.
- The `Unverified` list must be verified here or explicitly re-carried, not dropped. It includes the one item that decides severity: **what CloudNativePG actually does when `spec.parameters` contains an operator-owned key** — reject, silently win, or fight it in a reconcile loop. That was never determined.
- Also still open: whether Key Value shares the same conflation, and which three rows carried `client`/`session` source.

## Steps

1. Re-run every DoD probe against production on a disposable database and record the responses.
2. Set remaining tasks `status: done`, move each `tNNN.md` to `w6/m133/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w6/m133/` to `w6/done/m133/` leaving no stub, and check `- [x]` in `.pm/w6/README.md`.

## Files

- `.pm/w6/m133/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified live and recorded.
- [ ] The CloudNativePG-conflict question is answered or explicitly re-carried.
- [ ] Milestone moved to `w6/done/m133/` with status synced in all three places.

## Out of scope

- New findings surfaced during verification — file them separately.

## State at 2026-08-28 — t001–t007 are done and deployed; this task is blocked on credentials

Everything CI can verify is verified and green: dashboard 379 files / 2773 tests, backend `go test ./...` across 60 packages, `make lint` ×4. See the milestone README's "Implementation record" for what landed and for the two judgement calls (the `pg_stat_statements.track` addition, and `shared_preload_libraries` keeping its silent drop).

The deploy precondition is satisfied: `35655226` is an ancestor of `0ca167a8`, the production image pinned by `71fe9660`.

**Blocked on:** QA credentials (`scripts/qa-login.sh` exits **2** — `QA_EMAIL`/`QA_PASSWORD` absent from `.env`). Several other w6 closeouts share this blocker.

**Carry these into the closeout — do not let it imply they were settled:**

- **What CNPG does with a captured operator key is still unverified**, exactly as the Background said. The guard makes it unreachable through the API from now on, but it says nothing about databases whose `spec.parameters` may **already** hold captured values from before the fix. Check the three production databases for operator-owned keys in `spec.parameters`; if any carries them, that is a data-repair item to file separately, not something to fix quietly here.
  - **Settled clean 2026-08-29 (closeout sweep):** read every production Database CR (`kubectl --context hetzner-prod get databases.app.bex.co -A`, read-only) — now **four** databases (3 in `tea-d98210cb…`, 1 in `tea-da2isiml…`), and **every one has an empty `spec.parameters`**. No pre-fix captured values exist, so there is no data-repair item to file. The CNPG behavioral question itself (reject vs silently-win vs reconcile-fight) is hereby **explicitly re-carried as academic**: the guard makes the state unreachable through the API and no production CR carries it.
- **Key Value was never checked** for the same declared-vs-observed conflation. It has its own insights surface; if the shape repeats there it deserves its own note.
  - **Settled clean 2026-08-29 (closeout sweep), by code-read:** Key Value has no parameter surface to conflate — `lego/backend/internal/keyvalue/` defines no `ParameterOverrides`/`GetParameterSpec` (its only tunables are the typed create-time `maxmemoryPolicy`/`persistenceMode` options), and `dashboard/src/features/keyvalue/` contains no parameter editor or `pg_settings`-style read. The m133 shape cannot repeat there.
- The cosmetic nit the Background recorded (an "External host" term rendering an empty definition on a non-public database) is `w6/052.md`'s, not this milestone's — do not fold it in at closeout.

## Closed 2026-08-29 — live disposable-database DoD probes passed

Run with the synthetic QA identity (`qa-sweep-20260829@example.com`, workspace `tea-da96ad4039sc739tkjqg`) against production, on disposable free-tier `dpg-da96b239ui0s73db2amg` (PG 16):

- **Fresh database → empty declared set:** `GET /v1/postgres/{id}/parameters` → `[]`.
- **One set parameter reads back as exactly one:** `PUT /parameter-overrides {"parameters":{"work_mem":"8MB"}}` → 200; `GET /parameters` → `[{"name":"work_mem","value":"8MB"}]`.
- **Operator-owned keys refused through the API directly:** `restore_command`, `ssl_key_file`, `archive_command` each → 400 "`<name>` is managed by the platform and cannot be set as a database parameter".
- **The pg_settings insight is intact:** `GET /parameter-overrides` → 47 rows, sources `{configuration file: 44, client: 2, session: 1}` — the operator's `archive_command`/`restore_command`/`ssl_key_file` correctly present in the OBSERVED view only.
- **Editor binding (dashboard Insights):** exactly **1** editable textbox + 1 Remove button (the declared `work_mem`) against 47 read-only observed rows — versus the filed bug's 48 editable pairs.
- **Carried items:** production CRs clean (see the 2026-08-29 sweep note above — no data-repair item); Key Value has no parameter surface (code-read); CNPG-conflict question re-carried as academic; the client/session rows are the API's own connection settings — measured live: `default_transaction_read_only` (client), `statement_timeout` (client), `transaction_read_only` (session).
