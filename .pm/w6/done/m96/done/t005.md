---
id: w6/m96/t005
title: Simplify
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m96/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving only).

## Context

- Scope: `lego/backend/internal/api/render_openapi.go` and its test file, plus any docs touched in t003.
- In particular check whether the new path-parameter-pattern override map (t001) can share structure with the existing `renderRequiredCompatibility`/`renderOptionalParameterCompatibility`/`renderQueryExtensions` maps' lookup pattern rather than introducing a fourth, slightly-different shape.

## Steps

1. Run `/simplify` over the diff from t001-t004.
2. Apply any clearly-safe, conflict-free wins; defer anything touching code outside this milestone's scope (note why, per the `/simplify` skill's own conventions).

## Files

- `lego/backend/internal/api/render_openapi.go`
- `lego/backend/internal/api/render_openapi_test.go` (or wherever t002 landed)

## Acceptance criteria

- [ ] `/simplify` run recorded (what was applied, what was deferred and why).
- [ ] `go build ./... && go test ./...` (backend) still green after any changes.

## Out of scope

- Anything outside the files this milestone touched.

## Outcome (2026-08-26)

Simplify pass over the milestone's diff. Applied:

- The new override map shares the existing lookup shape exactly, as this task's Context asked: keyed by operationId, resolved once per operation, applied inside `applyRenderCompatibility`'s existing parameter loop next to `renderOptionalParameterCompatibility`. The loop's per-ref nil check was hoisted to a single `continue` rather than repeated per concession.
- `renderIDPathParameters` maps to `ids.Kind` rather than a prefix string, so the table test mints real ids with `ids.New(kind)` instead of splicing a prefix onto a workspace id's suffix — shorter and honest about what it is testing.
- The cross-kind guard derives its route set from `NewServer(...).restHandler()` — the same real mux `TestRenderRouteIntersectionInventory` uses — replacing the hand-listed routes of the first draft, which had `jobId`'s path wrong and would have drifted from reality on the next route change.
- `blueprintRequest` builds its request once instead of twice.

Declined: nothing. The diff is one map plus four query-extension lines in `render_openapi.go`, six `store.MapError` wraps in `blueprint.go`, and tests.

`go build ./... && go test ./...` (backend) green; `make lint` 0 issues.
