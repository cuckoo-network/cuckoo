# 022 — `.pm/w1/045.md` is stale: DNS ownership verification shipped

> **Resolved 2026-08-18: already done.** `w1/045.md` was completed in `w4/m84` (migration 0086 + verified-only serving) and already sits at `.pm/w1/done/045.md` with a completion header; the `VerificationStatus` residue is covered there (ownership state is exposed separately from certificate/serving status). Nothing left to retire — this note is obsolete.

Filed by `/pm-brainstorm for w6` 2026-08-17. Board hygiene in **w1**, scheduled here because workers are general-purpose. ~10m.

`.pm/w1/045.md` ("Custom domains are served before DNS ownership is proven", scan F4) says a host claim is persisted after only `AuthorizeApp(can_operate)` + canonicalization + `reservedHost` + `hostClaimedElsewhere`, "with no DNS challenge bound to workspace + app + hostname". **That is no longer true at HEAD:**

- `lego/backend/internal/apps/domains.go:40-59` — `DomainOwnershipVerifier` / `systemDomainOwnershipVerifier.VerifyTXT` resolves TXT records and requires an exact value.
- `:62-79` — `domainOwnershipChallenge` derives an app-bound challenge: `_bex-challenge.<registrableDomain>` with value `bex-domain-verification=<sha256(appID + root)[:16]>`.
- `:81-91` — `requireDomainOwnership` returns `core.ErrConflict` naming the exact record to create when verification fails.
- Wired into **both** add paths: `:517` and `:589`.

The note also carries a "Milestone-sized; kept as a note deliberately — promote it when it is scheduled" header, so it will otherwise sit in w1's inbox indefinitely looking like open product work.

**Do:** confirm the above against HEAD, then retire the note to `.pm/w1/done/045.md` with a line recording that the gap was closed in code (identify the commit/milestone that landed it if it is attributable). If any sub-claim of F4 survives — e.g. `VerificationStatus` still deriving from cert-manager issuance at `domains.go:203-233` rather than from the challenge — split that residue into a fresh note rather than retiring it silently.
