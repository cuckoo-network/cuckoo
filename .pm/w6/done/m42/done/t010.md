---
id: w6/m42/t010
title: "Test coverage: gate-mode, admission, and auth-flow regressions"
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m42/t008]
---

## Objective

Lock the milestone's behavior in with meaningful regression tests — asserting real refusals, mode boundaries, and flow continuations, never tautologies.

## Context

- Existing patterns to extend: `internal/*/payment_gate_test.go` (apps/postgres/keyvalue/sandbox/billing), the agentsessions service tests, dashboard auth-page `__tests__`.
- The three mode boundaries matter most: `1` stays byte-identical (free create passes unbound), `all` refuses free creates and agent-session admission synchronously, unknown value fails startup.

## Steps

1. Backend: table-drive the mode matrix across the gated features; add the agent-session admission refusal test (no row/turn/dispatch on 402); assert the coded error shape once per surface.
2. Dashboard: register→verification continuation with `next` preserved and an unguarded `next` dropped; login verification-required rendering; verification resend affordance.
3. Confirm t006's recovery-invariant guard is executable/repeatable, not prose-only.

## Files

- `lego/backend/internal/{core,agentsessions,apps,postgres,keyvalue}/…_test.go`
- `dashboard/src/features/auth/**/__tests__/`

## Acceptance criteria

- [x] Each acceptance criterion in t001–t006 is pinned by at least one test that fails when the behavior regresses.
- [x] All three suites green (`make test` where operator-adjacent — not expected here; `cd lego/backend && go test ./...`; dashboard `yarn test`).

## Out of scope

- E2E browser automation of the email round-trip (manual on dev-6 in t004/t005 is the record).

## Coverage assessment + additions (2026-08-21)

Already pinned during implementation: billing_config mode matrix (1/all/
invalid/missing-deps), apps all-mode free-create refuse + bound pass,
agentsessions create/steer/rehydrate synchronous-402 admission (no row/no
dispatch on refusal), auth-next relay (round-trip, tamper, unsafe-write),
OtpCodeInput (auto-submit boundary, dedupe, forwarding, OS semantics) +
page-wide paste (capture, sanitize, no-hijack, no-code), extractOtp, Kratos
courier templates drift pin + shape guard, KratosCourierNotReady promtool
unit test, and the pre-existing payment_gate family across postgres/keyvalue/
sandbox/blueprints.

Added here: `internal/core/payment_test.go` TestRequirePlanBillingModeMatrix —
the seam itself, four quadrants (plan × mode) plus the nil-gate compatibility
path, so a future call site cannot regress the widening without a core-level
failure. t006's recovery probe remains the documented manual re-probe (its
guard is the pinned comment in kratos.values.yaml). Full backend suite +
lint and the 2387-test dashboard suite green after the t009 refactors.
