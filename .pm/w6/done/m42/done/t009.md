---
id: w6/m42/t009
title: "Simplify: /simplify over the changed code"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m42/t008]
---

## Objective

Run `/simplify` over the code this milestone changed (backend gate mode + agent-session admission, dashboard auth pages, scripts) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Standing closing task. Watch for: the mode enum leaking as stringly-typed checks at call sites; duplicated `next`-guard logic across the three auth pages; `auth-secrets.sh` fragment drift against the values-file comments.

## Steps

1. `/simplify` the milestone's diff; apply accepted fixes.

## Files

- whatever the milestone's diff touched

## Acceptance criteria

- [x] `/simplify` run recorded; accepted findings applied; all suites still green.

## Out of scope

- Bug-hunting (that's `/code-review`); refactors beyond the milestone's diff.

## Applied (2026-08-21, three-agent review over the full m42 diff)

Fixed: (1) register-page onSuccess dropped its awaited clearStore/
invalidateSessionCache/router.invalidate — Elements awaits onSuccess BEFORE
the full-page continue_with redirect, so that work only delayed the signup
hot path (dead imports removed); (2) PaymentRequiredProvider memoizes its
context value — a fresh object per render re-rendered every gate consumer on
each 2s readiness poll, amplified by the new agent-session deps; (3) the
agentsessions admission trio (payment/billing/cap) extracted to one
`admitDispatch` helper in a fixed order across Create/Steer/rehydrate — the
copy-paste had already drifted into three orderings; (4) verification page
consumes the auth-next relay unconditionally (no stale stash) and compares
`VerificationFlowState.PassedChallenge` instead of a raw string; (5) paste
listener short-circuits on other-editable targets before running extractOtp;
(6) stray blank line + redundant script case-arm comment removed; (7) the
paymentGate.run wrap indentation formatted. A `yarn format` sweep reformatted
64 unrelated drifted files — reverted; only the nine touched files kept their
formatting. Reviews otherwise judged the diff clean (agent findings recorded
verbatim in this session). All suites re-green: dashboard 2387, backend
packages, typecheck/lint.
