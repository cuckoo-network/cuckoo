---
id: w6/m47/t006
title: Test coverage — hibernate/wake cycle, event-fact labeling, logs empty-state title
worker: worker1
status: done
estimate: 45m
depends_on: [w6/m47/t004]
---

## Objective

Add meaningful tests for the behavior this milestone ships, asserting real behavior and failure modes rather than gaming coverage.

## Steps

1. Operator-level (envtest): a service auto-hibernates via idle timeout, a simulated external request reaches the activator, and the App leaves `Hibernated` within a bounded time — this is the regression test t001 itself requires; make sure it actually exercises the fixed code path (Ingress backend / activator reachability), not just the annotation patch.
2. Backend-level: `observedStateFacts` emits the new distinct hibernate/wake fact types for a phase transition with `Spec.Suspended == false`, and still emits the original `service_suspended`/`service_resumed` facts when `Spec.Suspended == true` — covering both branches added in t002, plus a regression test that a genuine user-driven Suspend/Resume is unaffected.
3. Dashboard: a component test for `log-viewer.tsx` asserting the empty-state title (not just the body) differs between the filtered and unfiltered zero-result cases — the exact gap t003's fix closes.

## Acceptance criteria

- [x] All three tests exist, are red against the pre-fix code (verified by temporarily reverting each fix), and green after.
- [x] No snapshot-everything or tautological assertions.

## Outcome

| layer | test | red-before proof |
| --- | --- | --- |
| operator | `TestAutoHibernateIngressBackendResolvesInTenantNamespace` + two siblings (`lego/operator/internal/controller/auto_hibernate_wake_test.go`) | reverted `ingressBackend` → `hibernated backend = bex-activator, want in-namespace alias "bex-activator-web":8888` |
| backend | `TestAutoHibernateFactsStayApartFromSuspend` (`lego/backend/internal/store/event_facts_test.go`) | reverted the fact branching → `want exactly one service_hibernated`, got `service_suspended` |
| dashboard | two cases in `LogViewer zero-result empty state (w6/m47)` (`log-viewer.test.tsx`) | reverted the title branch → filtered case fails |

The operator test asserts the backend Service **actually exists in the Ingress's namespace**, not merely that the name changed — a name Traefik cannot resolve was the original bug, so asserting the name alone would have re-admitted it. It also pins the alias labels/owner to the exact identity `operator-alias-admission.yaml` requires, so drifting from the admission shape fails here instead of only in production. The two siblings cover the same-namespace short-circuit (no alias created) and the wake path restoring the App's own Service as the backend.

Suites green: `make test` (operator), `go test ./...` (backend), `yarn test` (dashboard, 2546 tests), `make lint` (0 issues, all four modules), `scripts/gitops-validate.sh`.
