---
id: w6/m127/t005
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m127/t004]
---

## Objective

Run `/simplify` over this milestone's diff — the create form, the patch path, and the locales — behaviour-preserving.

## Context

- `dashboard/src/routes/keyvalue.new.tsx`, `dashboard/src/features/keyvalue/locales/{en,zh}.ts`, `lego/backend/internal/keyvalue/{service.go,rest.go,graphql.go,mcp.go}`.
- After `t002`, `maxmemoryPolicy` and `persistenceMode` follow an identical patch shape — validation, normalization, threading through three adapters. That symmetry is the obvious consolidation, and it is worth doing precisely because the asymmetry is what allowed one to be forgotten.
- Do not collapse `validate()`'s per-field messages into a generic one; naming the offending value is a deliberate courtesy (`service.go:169-172`).

## Steps

1. Run `/simplify` over the diff.
2. Apply behaviour-preserving improvements only.
3. Re-run `cd lego/backend && go test ./...` and `cd dashboard && yarn test`.

## Files

- The files `t001`–`t003` touched

## Acceptance criteria

- [ ] No behaviour change; `t006`'s tests still pass.
- [ ] The two settings share their patch shape rather than diverging again.
- [ ] Per-field validation messages still name the offending value.

## Out of scope

- Restructuring the Key Value feature.

## Outcome

Behavior-preserving cleanups folded in as the change landed: create-time persistence validation now uses the shared `persistenceModeKnown` (matching `maxmemoryPolicyKnown`) instead of an inline `slices.Contains`, so create and update use one check; the GraphQL/MCP/REST additions reuse the existing `PatchMutation`/patch-decode shapes rather than new plumbing. `gofmt`/`go vet` clean; suites green.
