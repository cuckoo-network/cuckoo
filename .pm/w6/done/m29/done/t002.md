---
id: w6/m29/t002
title: "Undo the interim drift: remove the hand-patched default-SA imagePullSecrets"
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m29/t001]
---

## Objective

Once t001 is deployed to prod, remove the live hand patch (`bex-registry-pull` on the `default` namespace's default ServiceAccount) so prod state is fully declared, and sweep for any other tenant namespace that would need the same treatment (per the note's "confirm no other tenant namespaces" instruction).

## Context

- `.pm/w7/001.md` — the interim fix's exact shape and why it was applied (recovering a live outage during the m36 roll).
- Deliberate choice recorded in the milestone README: the design-aligned fix is the operator attachment (option 1), NOT declaring the SA patch in gitops (option 2) — remove, don't declare.

## Steps

1. Verify prod runs the t001 operator (image digest) and a spot-check Deployment carries the secret.
2. `kubectl patch` the SA to drop the entry; confirm running pods keep working and a forced re-pull succeeds.
3. Sweep other namespaces with tenant workloads for equivalent hand patches.

## Files

- prod cluster state only (no repo files; record evidence in the milestone)

## Acceptance criteria

- [x] `kubectl get sa default -o yaml` shows no `imagePullSecrets` hand patch; a post-removal pull still authenticates via the workload-level secret.

## Out of scope

- The fresh-node end-to-end proof (t003).
