---
id: w6/m40/t007
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m40/t006]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

- Do not run this until the DoD is real: zero duplicate numbers, every ADR indexed, no dangling ADR link, and the guard green in CI with a passing self-test.
- Conventions: [`.pm/CLAUDE.md`](../../CLAUDE.md) — move the whole milestone, leave no tombstone.

## Steps

1. Verify every clause of the definition of done, running the guard as the mechanical check.
2. Set every remaining task's frontmatter `status: done`; move each `tNNN.md` to `.pm/w6/m40/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `.pm/w6/m40/README.md`.
4. Move the whole `.pm/w6/m40/` directory to `.pm/w6/done/m40/` and `rmdir` the original.
5. Check `- [x]` for m40 in `.pm/w6/README.md`.
6. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m40/**`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Definition of done verified clause by clause, with the guard run as evidence.
- [ ] Milestone directory lives at `.pm/w6/done/m40/`; nothing remains at the old path.
- [ ] `.pm/w6/README.md` shows `- [x] **m40**`.

## Out of scope

- Any further renumbering — if a new collision appears, the guard should have caught it; fix the guard.
