---
id: w6/m34/t006
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m34/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (backend accept paths, operator build-Job builder) — reuse / simplification / efficiency, behavior-preserving.

## Acceptance criteria

- [x] `/simplify` run; worthwhile findings applied or filed.

## Evidence

The simplification pass reused m31's single materialization and binding paths rather than adding source-specific persistence, extracted one `isDockerfileBuild` predicate, reused one dashboard picker renderer, and kept the unset operator path unchanged. It also tightened static-site rejection and consolidated the derived build config instead of mounting separate competing Secrets. No follow-up remained.
