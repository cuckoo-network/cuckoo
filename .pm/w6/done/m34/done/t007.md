---
id: w6/m34/t007
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m34/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped beyond t003's isolation set: create/patch/echo round-trips per surface, the docker-config merge with both auths present, and the unset-binding byte-identical Job spec. Tests must assert real behavior and failure modes — no tautological coverage.

## Acceptance criteria

- [x] `cd lego/backend && go test ./...` and `make test` (from `lego/operator/`) green with the new tests.

## Evidence

Final closeout passed `go build ./... && go test ./...` for the backend; operator `make test` (including envtest) and `make lint` with zero issues; and dashboard `yarn typecheck`, `yarn lint`, plus 202 test files / 1,190 tests on rebased main. The operator suite was rerun after the lint cleanup and stayed green.
