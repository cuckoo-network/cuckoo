---
id: w6/m11/t006
title: "Test coverage: meaningful tests for the behavior this milestone shipped"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m11/t002, w6/m11/t003, w6/m11/t004]
---

## Objective

Add meaningful tests for what this milestone shipped in code — the stub's `workspaceMembers` path (a populated Team page offline) and any verification-surfaced fix. Tests must assert real behavior and failure modes, not game coverage.

## Context

- The bulk of this milestone is live verification (its "test" is the recorded evidence); code changes are limited to `dashboard/scripts/local-bex.mjs` + any fixes.
- Existing Team page tests: `dashboard/src/features/team/components/__tests__/team-panel.test.tsx`.

## Steps

1. Cover the stub's `workspaceMembers` response shape (enriched `userId`/`email`) so drift from the real schema fails a test.
2. Add regression tests for any bug the live verifications surfaced.

## Files

- `dashboard/scripts/local-bex.mjs` (or a test alongside the stub's consumers)

## Acceptance criteria

- [ ] Stub `workspaceMembers` shape is asserted by a test; `yarn vitest run` green

## Out of scope

- Re-testing behavior m4/m5/m10 already covered
