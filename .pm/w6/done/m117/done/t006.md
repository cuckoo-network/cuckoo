---
id: w6/m117/t006
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m117/t004]
---

## Objective

Cover the case the existing suite structurally cannot see: a CNPG read that fails.

## Context

- `listBackups`' doc comment names the blind spot outright — "Best-effort: an unavailable CNPG CRD (e.g. envtest) yields an empty list." Every existing test runs in exactly that condition, so an empty list is the expected value everywhere and the production failure is invisible.
- The four states to pin: backups readable and present; readable and genuinely empty; unreadable; plan has backups disabled.

## Steps

1. Add a test where the Cluster `Get` fails and assert the response is distinguishable from the healthy-empty one.
2. Same for a failing Backup `List`.
3. Assert `latestRecoveryTime` is absent in the unreadable case and present in the healthy one.
4. Assert `Recover` refuses before any billing gate or resource creation when the window is unsubstantiated.
5. Keep a healthy-empty control that still passes unchanged.
6. Confirm the new tests fail with t002 reverted.

## Files

- `lego/backend/internal/postgres/*_test.go`
- `dashboard/src/features/databases/components/__tests__/recovery-panel.test.tsx`

## Acceptance criteria

- [x] All four states asserted.
- [x] A test fails when t002 is reverted.
- [x] `cd lego/backend && go test ./internal/postgres/...` and `dashboard/yarn test` green.

## Completion (corrected 2026-09-01)

All four states are pinned in `internal/postgres/advanced_test.go`: an ObjectStore-keyed readable window with exact first/last values and a completed Backup; readable empty and partial windows; forbidden and missing ObjectStore sources; an unreadable Backup list; and a backup-disabled plan. `TestRecoverRefusesAnUnsubstantiatedWindow` asserts refusal before billing/resource creation and preserves source-read failures as non-400 errors. Dashboard tests distinguish read error, plan-disabled, unestablished, and established-window behavior, including the disabled Restore gate and stopped hidden export polling. Full verification is green: `go test ./...` and focused `-race` in `lego/backend`; repository-wide `make lint` (0 issues in all four Go modules); dashboard typecheck, lint, and all 390 files / 2,869 tests; and `scripts/gitops-validate.sh`.

## Out of scope

- Testing the restore path itself, which `w7/039` covers.
