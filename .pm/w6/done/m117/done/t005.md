---
id: w6/m117/t005
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m117/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Watch for the pattern that caused this: `recovery.go` has two different spellings of the same swallow (an `err == nil` guard at `:128` and an error-to-empty-slice return at `:145`). After t002 there should be one way of expressing "this CNPG read failed", not two.
- `unstructured.Nested*` calls discard their `ok`/`err` returns in several places (`:129`, `:151`). Those are lower-stakes than the two this milestone fixes, but decide deliberately whether each is fine rather than leaving the same shape unreviewed next door to a fixed one.

## Steps

1. Run `/simplify` over the touched files.
2. Confirm there is one representation of a failed CNPG read.

## Files

- files touched by t002/t003

## Acceptance criteria

- [x] Simplification review run; behavior-preserving changes applied.
- [x] One spelling of "recovery-window read failed"; any remaining discarded `ok`/`err` is deliberate and noted.

## Completion (corrected 2026-09-01)

The required Simplify review ran as three parallel reuse, quality, and efficiency passes over the complete diff. It produced four applied cleanups: one `effectiveBackupServerName` helper now serves recovery lookup, restore pinning, and upgrade-backup validation; a typed `recoveryWindowBounds` owns the two-bound invariant; independent ObjectStore/Backup reads run concurrently; and the dashboard stops hidden export polling while recovery metadata is unreadable. Test fixtures lost an unused ObjectStore-list registration and now use resource-accurate names; postmortem comments were reduced to durable invariants.

The suggested cross-module Barman constants were deliberately not moved into `lego/types`: each unstructured consumer currently owns its external GVK constants (including CNPG Backup), and introducing a new shared package for one pair would widen the CRD-contract module without removing the surrounding duplication. The validator's two small `yq` reads remain separate because the explicit per-rule assertions are clearer and run only in CI, not a product hot path.

## Out of scope

- Behavior changes.
