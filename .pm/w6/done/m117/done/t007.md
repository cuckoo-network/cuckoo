---
id: w6/m117/t007
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m117/t005, w6/m117/t006]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet live against the three production databases, re-running this milestone's probe. Confirm the deployed build contains the fix first.
2. Confirm the reported state matches what `t001` established the cluster actually holds.
3. Set every remaining task's `status: done`.
4. Move each `tNNN.md` to `w6/m117/done/`.
5. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
6. Move the whole `w6/m117/` directory to `w6/done/m117/`.
7. Check `- [x]` for `m117` in `w6/README.md`.

## Acceptance criteria

- [x] DoD verified live, not assumed.
- [x] The reported recovery state matches cluster ground truth.
- [x] If `t001` found the backups genuinely gone, the separate ops item is linked and its status stated.
- [x] Milestone moved to `w6/done/m117/` with all statuses in sync.

## Progress (2026-09-01)

The pre-deploy production check is explicit. `bex-system/bex-api` still runs image digest `sha256:f5f5a9c9cc04bfcc5a13c8952fe636c426d8f1bab330cd3975be4c76652dabc6`, and its ServiceAccount still receives `no` for both `get objectstores.barmancloud.cnpg.io/bex-tenant-postgres` and `list backups.postgresql.cnpg.io` in `tea-d98210cbbpdc73dcrkvg`. Meanwhile, each Database's exact `status.backupServerName` still resolves to a complete ObjectStore recovery-window pair. The live API probe would therefore exercise the old denied path, not the worktree fix; ship/deploy is the next required step before this task can honestly close.

Commit `e75b08ea` shipped and deployment run `33546048149` built/pinned digest `sha256:e2874bfe4f8ad5ce65eb6ec96dd24b24de217efd478df4a23d6c17cfcc6a28d5`. The m117 RBAC is live: the `bex-api` ServiceAccount now receives `yes` for both exact reads above. The API rollout itself is blocked by separate production configuration, filed as `w6/069`: the new pod exits before serving because `bex-system/bex-stripe` lacks `BEX_STRIPE_PUBLISHABLE_KEY` while `BEX_REQUIRE_PAYMENT_METHOD=all`; neither GitHub Actions nor the local operator configuration contains a value to install safely. Two old API replicas remain Ready, so no m117 live API claim can be made until the Stripe account owner supplies the matching live publishable key and the rollout completes.

## Completion (2026-09-03)

The live Stripe publishable key supplied by the account owner exactly matches `bex-system/bex-stripe`; no key value was printed or committed. Production now runs 2/2 Ready `bex-api` replicas at digest `sha256:5423ab0b8fb6f6b16f53f23af2e1fdcda5ee018589d601561aa46fe9f755f1de`, pinned by `b532ab05` from source commit `c3a0a122`; `e75b08ea` is an ancestor of that deployed build. The m117 ServiceAccount receives both required reads.

An authenticated `POST /v1/postgres/{id}/recovery-info` returned HTTP 200 for all three production databases. An automated comparison joined each Database's `status.backupServerName` to `ObjectStore/bex-tenant-postgres.status.serverRecoveryWindow` and compared the returned backup ids to completed CNPG Backup objects:

```text
dpg-d9nqg95cavls73fp8m20  window_exact=true  backups_exact=true  api=13  cluster=13
dpg-d9rrkoc4h4mc73edurp0  window_exact=true  backups_exact=true  api=13  cluster=13
dpg-d9rs3ee0ccis738kc7c0  window_exact=true  backups_exact=true  api=13  cluster=13
```

The exact live recovery windows are `2026-08-21T22:42:06Z`–`2026-09-03T03:01:04Z`, `2026-08-21T22:42:56Z`–`2026-09-03T03:01:43Z`, and `2026-08-21T22:42:21Z`–`2026-09-03T03:01:08Z`, respectively. The Recovery card loaded against the first database with Restore enabled for this established window. The simulated denied-ObjectStore, unreadable-Backup-list, honest-empty, complete-window, and refusal-before-billing/resource-creation cases remain pinned by tests; the current-main focused suites pass (`go test ./internal/postgres/...`, dashboard recovery-panel 7/7). No backup-operations incident exists, so `w6/069` was only a rollout-configuration blocker and is resolved alongside this closeout.

## Out of scope

- New work surfaced during verification — file it separately.
