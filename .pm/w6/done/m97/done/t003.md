---
id: w6/m97/t003
title: Render parity
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m97/t002]
---

## Objective

Confirm the fix keeps REST, GraphQL, MCP, and the dashboard UI byte-consistent on this surface, and that no drift was introduced.

## Context

- This hunt already confirmed REST (`GET /v1/services/{id}/deploys/{id}`) and GraphQL (`deploy(...).failureReason`) return the byte-identical `failureReason` string pre-fix. That agreement must not regress.
- MCP's deploy-status tooling (`lego/backend/internal/apps/mcp.go` or wherever deploy status is exposed to MCP) reads the same underlying store row — confirm it too.
- The dashboard's Deploy detail page and Events feed (`dashboard/src/features/deploys/` or `dashboard/src/routes/services.$id.deploys...`) render `failureReason`/`status` directly from these APIs — confirm a successful post-fix deploy clears the "Failed" badge and stale Events entries do not persist incorrectly (a pre-fix failed deploy in a service's history should still correctly show as Failed; this is about the *next* deploy succeeding, not rewriting history).
- render.com has no equivalent surfaced failure mode (this guard is bex-specific tenant-isolation hardening) — n/a for direct behavioral parity, but the REST/GraphQL/MCP/UI internal-parity check still applies per `docs/ADR006-bex-api.md`.

## Steps

1. Re-run the exact GraphQL and REST probes from this milestone's README against a real post-fix deploy; confirm both report `status: "build_in_progress"` → `"live"` identically as the deploy progresses.
2. Check the MCP deploy/service status tool returns the same `status`/no spurious `failureReason`.
3. Load the dashboard Deploy detail and Events pages for the same deploy; confirm no leftover "codex F1" text or stuck "Failed" badge.

## Files

- `lego/backend/internal/apps/mcp.go` (or wherever MCP deploy status lives — locate via grep)
- `dashboard/src/features/deploys/` (or equivalent route)

## Acceptance criteria

- [ ] REST/GraphQL/MCP report identical `status` for the same deploy at each stage.
- [ ] Dashboard Deploy detail + Events reflect the successful deploy, no stale F1 text.

## Out of scope

- Any new REST/GraphQL/MCP fields — this task is confirmation, not feature work.
