---
id: w6/m108/t003
title: Regression tests for the stale-link case, without regressing genuine failures
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m108/t002]
---

## Objective

Add regression coverage in `lego/backend/internal/envgroups/patch_test.go` for the stale-linked-service case fixed by t001/t002, while proving the existing genuine-failure test is untouched.

## Context

- `lego/backend/internal/envgroups/patch_test.go:216-247` (`TestPatchEnvironmentReportsPartialRebuildForRolloutOnlyRetry`) is the existing control case: two real apps (`sampleApp("web")`, `sampleApp("worker")`), an injected `errors.New("rebuild unavailable")` for "worker", asserting `RolledOut=false`/`FailedServiceIDs=["worker"]` then a genuine retry succeeding. This must keep passing byte-for-byte.
- No existing test constructs the "linked service was deleted from the store" case — confirmed this hunt by reading the whole file. The new test should link a group to a service, remove that service from the fake store/`GetApp` lookup (however the test fixtures model "since-deleted" — check `sampleApp`/`newFakeStore`'s app-removal affordances, add one if none exists), and call `PatchEnvironment`.

## Steps

1. Add a test (e.g. `TestPatchEnvironmentToleratesDeletedLinkedService`) that: creates a group linked to two services, deletes one from the fake store, calls `PatchEnvironment` with `SaveMode: SaveModeDeploy` — assert `RolledOut: true`, `FailedServiceIDs` empty, and the still-existing service's action was actually invoked (so the fix isn't silently skipping everything).
2. Repeat for `SaveMode: SaveModeRebuild`.
3. Assert the stale id is gone from a subsequent `GetEnvGroup(...).ServiceLinks` (t002's prune).
4. Run `TestPatchEnvironmentReportsPartialRebuildForRolloutOnlyRetry` unmodified and confirm it still passes — do not weaken its assertions to accommodate the fix.

## Files

- `lego/backend/internal/envgroups/patch_test.go`

## Acceptance criteria

- [ ] New test(s) pass and fail without t001/t002's fix (verify by temporarily reverting, or reasoning through why the old code would fail them).
- [ ] `TestPatchEnvironmentReportsPartialRebuildForRolloutOnlyRetry` passes unmodified.
- [ ] `go test ./lego/backend/internal/envgroups/...` is green.

## Out of scope

- Dashboard-side tests (covered by t004/t006 as needed).
