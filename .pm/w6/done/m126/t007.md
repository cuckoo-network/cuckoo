---
id: w6/m126/t007
title: Closeout
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m126/t005, w6/m126/t006]
---

## Objective

Close the milestone once every task is done **and** the DoD holds against production.

## Context

- The live check is cheap and was run this way: `POST /v1/projects` → compare its key set with `GET /v1/projects/{id}` for the same id. They must match.
- **`PATCH` and the links `PUT`s cannot be checked from a browser** while `w6/m121` is open — the CORS method list blocks them. Use a non-browser client, or verify them in tests and say so plainly rather than claiming a live check that did not happen.
- Clean up every fixture. This run created a project, an environment and a service and removed all three (`deleteService` → true; `deleteEnvironment` and `deleteProject` returned their ids; the workspace's three pre-existing projects were left untouched).

## Steps

1. Create a project on production; compare the create and read key sets.
2. Verify the remaining write paths by whatever means is honest, and record which means.
3. Confirm `t002`'s environments decision is visible in the created project.
4. Delete the fixture project (and anything created under it); confirm it is gone.
5. Set remaining tasks `status: done`, move each to `w6/m126/done/`, mark rows `— **DONE**`, set `**Status:** done`.
6. Move `w6/m126/` to `w6/done/m126/` (no stub) and check `- [x]` in `.pm/w6/README.md`; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m126/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Create/read key sets compared live and matching, with the capture recorded.
- [ ] The write paths' verification method is stated, not implied.
- [ ] Fixtures deleted and confirmed gone.
- [ ] Milestone moved to `w6/done/m126/` with no tombstone; status consistent in all three places.

## Out of scope

- Sibling-resource write shapes surfaced by `t003` — those are their own filings.
