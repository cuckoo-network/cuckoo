---
id: w6/m126/t004
title: Render parity — project shape across REST, GraphQL, MCP and the dashboard
worker: worker6
status: done
estimate: 25m
depends_on: [w6/m126/t001, w6/m126/t002, w6/m126/t003]
---

## Objective

The project wire shape changes on five REST handlers. Confirm the other surfaces agree and that the result matches Render.

## Context

- **GraphQL and MCP project reads were never probed this run** — establish what they return before assuming they are consistent.
- **The dashboard's project page was never opened** — check it renders correctly against the corrected shape, particularly if anything consumed the internal-only `serviceIds`/`databaseIds`/`keyValueIds` that the REST create response used to carry.
- Render's `project` object is the target: `['id','createdAt','updatedAt','name','owner','environmentIds']`, all required.

## Steps

1. Read a project through REST (create + get), GraphQL and MCP; compare field sets.
2. Grep the dashboard for consumers of the fields the create response used to return, and confirm nothing depended on them.
3. Open the dashboard project page and confirm it still renders.
4. Decide whether `ADR018` needs a row (t002 may already have added one) and record the reasoning.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR006-bex-api.md` — only if a line is warranted

## Acceptance criteria

- [ ] REST/GraphQL/MCP project shapes established by probe and reconciled.
- [ ] No dashboard consumer depended on the removed internal fields — checked by grep, not assumed.
- [ ] The dashboard project page renders.
- [ ] A recorded decision on the ledger row.

## Out of scope

- Unrelated parity gaps surfaced while reading the ledger.
