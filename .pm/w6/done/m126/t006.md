---
id: w6/m126/t006
title: Test coverage
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m126/t004]
---

## Objective

Cover the shape guarantee per verb, and cover the paths that are correct today and could regress.

## Context

- `t003` is the schema-level conformance guard. This task is the behavioural half: each handler returns what it should, including the four write paths that were never observed live this run.
- The reads and the LIST cursor nesting are correct today and are what a careless refactor breaks — cover them too, not only the fixed writes.
- Assert on field **sets**, not a golden blob: a snapshot test over the whole body will fail on every unrelated field addition and teaches nothing about the invariant.

## Steps

1. Assert `POST` returns exactly Render's six project fields.
2. Assert `PATCH` and the three links `PUT`s return the same set.
3. Assert `GET` single and list are unchanged, with the list still nesting `{project, cursor}`.
4. Assert an unauthorized caller still gets the authz error rather than a shaped empty object.
5. Assert environments' create/read shapes are unchanged.

## Files

- `lego/backend/internal/projects/*_test.go`
- `lego/backend/internal/api/*_test.go`

## Acceptance criteria

- [ ] `cd lego/backend && go test ./...` passes.
- [ ] A test fails if any project handler reverts to the internal view.
- [ ] The LIST cursor nesting is pinned.
- [ ] No snapshot-everything assertions.

## Out of scope

- Live production verification; that is the DoD.
