Complete `GET /v1/env-groups` list filtering against Render's public OpenAPI: repeated/comma-separated `name`, `ownerId`, and `environmentId`, plus `createdBefore`/`createdAfter`/`updatedBefore`/`updatedAfter`. Paging shipped in m32; these accepted filters must be implemented or rejected explicitly, never silently ignored.
