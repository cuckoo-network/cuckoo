---
id: w6/m110/t008
title: Closeout
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m110/t007]
---

## Objective

Close the milestone once its Definition of done is actually met — not merely once the code is written.

## Closeout evidence (2026-09-09)

Synthetic QA workspace `tea-dagc1ke7dt4c73f45lbg` (billing-excluded). Fixtures:

- long (≥22 chars): `qa-200243-longname-metrics` / `srv-dagcoku7dt4c73f45lig`
- short control: `qa-200243-short` / `srv-dagcol1c7cos73dm342g`

DoD live results:

1. **App compute metered** — after the 03:00 UTC hour rollup (`usage: rolled up window 2026-09-09T03:00:00Z`), REST `/v1/usage` and GraphQL `usage.estimatedCost` both show `instance_seconds` on `resourceKind: "service"` for long and short, quantity `1.00` hr (3600s), tier `starter`.
2. **Application Metrics** — REST `/v1/metrics/{memory,cpu,instance-count}` returned non-empty series for the long-name service while Running.
3. **Dashboard /billing charge tree** — same `estimatedCost` payload powers `/billing`; GraphQL confirmed compute lines per service.
4. **Fresh ≥22-char name** — fixture created this session; MEMORY/CPU/INSTANCES populated within the scrape window after Ready.
5. **Short-name non-regression** — short fixture likewise returned MEMORY/CPU/INSTANCES series (no cross-App labels observed in the probe).
6. **Cross-surface** — REST + GraphQL usage/metrics agree on the fix funnel; MCP not separately exercised this closeout (single backend funnel).

Historical `srv-da7o6ovvqdcc73bpn9hg` sequencing: m122/m123 already closed; that fixture is not in this workspace. Disposable long/short fixtures deleted after verification.

## Acceptance criteria

- [x] DoD verified live, not assumed, against a deployed build.
- [x] Disposable closeout fixtures deleted after verification.
- [x] Milestone moved to `w6/done/m110/` with all statuses in sync.
