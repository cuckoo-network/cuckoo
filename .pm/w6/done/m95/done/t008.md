---
id: w6/m95/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m95/t007]
---

## Objective

Close the milestone once its definition of done actually holds — not merely once the code is written.

## Steps

1. Confirm every task's acceptance criteria are met and its status is `done`.
2. Confirm the milestone's Definition of done in `README.md` is genuinely met, including a fresh live repro against `dashboard.bex.co` once deployed (the fix cannot be live-verified until production successfully deploys again — see `w6/040.md`; if still blocked, carry the live leg forward the same way `w6/m51`/`m52`/`m50` did, per `w6/040`'s precedent, rather than holding this milestone open indefinitely).
3. Move each `tNNN.md` to `w6/m95/done/`, mark every row `— DONE` and `**Status:** done` in this README, move the whole `w6/m95/` directory to `w6/done/m95/`, and check `- [x]` in `w6/README.md`.

## Files

- `.pm/w6/m95/README.md`
- `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every task done, milestone moved to `w6/done/m95/`, workstream README checkbox updated.

## Out of scope

- Reopening any of t001-t007's scope.

## Outcome (2026-08-25)

Every task's acceptance criteria met and status `done`, with two carried forward explicitly rather than silently:

- **t001's cluster/operator-log evidence.** Not obtainable: no reachable bex cluster from this workstation, and production has not deployed since 2026-08-24 (`w6/040`), so a live repro would exercise pre-fix code. Replaced with an exhaustive, executable enumeration of every code path on `main` that can produce the observed state — which found **two** live mechanisms rather than the one a log would have caught, so the substitute is not merely a fallback. Stated plainly in t001 rather than papered over.
- **The live re-repro leg (t002's third criterion, and this milestone's own DoD).** Blocked on the same production deploy freeze. Carried forward exactly as `w6/m51`/`m52`/`m50` did, per `w6/040`'s precedent, rather than holding this milestone open indefinitely.

Everything else in the Definition of done is met and proven by test: a `build_in_progress` row now requires proof of a running build; a row with no backing build reaches a terminal state within `BuildGateTimeout` on every path, including the four that previously never evaluated the timeout at all; `updatedAt` advances past `startedAt`; the `supersededDeployStatus` `ReleaseGeneration > 0` guard was re-examined, confirmed unrelated to this incident, and its own stranding gap closed; the named regression (backing Job/Pod gone) is covered red-pre-fix/green-post-fix; and the Render comparison is recorded in `docs/ADR018-render-parity.md`.

Gates: `lego/backend` `go test ./...` exit 0, `lego/operator` `go test ./...` (envtest, 147 specs) exit 0, `make lint` 0 issues across all four modules, `make manifests generate` produces no drift.
