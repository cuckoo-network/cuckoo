---
id: w6/m95/t003
title: Close (or explicitly rule out) the supersededDeployStatus orphan-timeout gap guarded on ReleaseGeneration > 0
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m95/t001]
---

## Objective

`lego/backend/internal/store/reconciler.go`'s `supersededDeployStatus` (`:924-969`) only closes an orphaned `build_in_progress`/queued row as `Canceled` past its gate timeout when `app.Status.ReleaseGeneration > 0` (`:965-968`). That guard was added by `2da39122` and `2f9db5d4` specifically so a legacy operator's metadata-generation churn (e.g. manual scale) wouldn't wrongly cancel a live rollout. Determine whether this guard can also strand a row when `ReleaseGeneration` is legitimately `0` or the App CR is briefly/permanently absent from the reconciler's `List` pass (`reconciler.go:511-517`), and close that gap without reopening the legacy-operator false-cancel risk the guard exists to prevent.

## Context

- This hunt's 3 repros all had matching release generations (the normal `PhaseBuilding` switch branch fired, not this orphan branch) — so t001 may confirm this guard is unrelated to the discovered incident. If so, this task still stands as a hardening pass against the documented, currently-live gap; do not skip it on that basis, but do not overstate it as "the" root cause if t001 found a different mechanism.
- `appReleaseGeneration` (`reconciler.go:984-989`) already falls back to `app.Generation` (the k8s metadata generation) when `ReleaseGeneration == 0` — a real k8s object's metadata generation starts at 1, not 0, so the literal `generation == 0` branch at `:941` is close to unreachable in practice; the practical gap is the `ReleaseGeneration > 0` guards at `:965`, `:985`, `:992` specifically privileging the release-generation-aware signal over the always-available metadata-generation fallback for the *timeout* decision, while using the fallback freely elsewhere.
- Also check `reconciler.go:511-517` (the `!ok → Create; continue` branch): if the App CR is missing from one `List` pass entirely, `recordObservations`/`recordDeploy` — and therefore the whole timeout check — does not run for that App that pass at all.

## Steps

1. Write (or locate) a test that puts a `build_in_progress` row on an App with `ReleaseGeneration == 0` past `BuildGateTimeout`, and confirm today it is NOT closed (red).
2. Change the guard so the gate-timeout closure fires even when release-generation evidence is entirely absent, while preserving the original fix's guarantee: a legacy operator's operational metadata-generation churn (manual scale) must still NOT be treated as a supersede/orphan.
3. Confirm `2da39122`'s and `2f9db5d4`'s own regression tests (`TestObservedDeployStatusCancelsOrphanedRowAfterTimeout` and siblings) still pass unchanged.

## Files

- `lego/backend/internal/store/reconciler.go`
- Its existing test file (find via `grep -rl TestObservedDeployStatusCancelsOrphanedRowAfterTimeout lego/backend`)

## Acceptance criteria

- [ ] A new test proves the `ReleaseGeneration == 0` (or App-CR-briefly-missing) case now resolves within the gate timeout — red pre-fix, green post-fix.
- [ ] Every existing reconciler/deploy-status test still passes, including the two prior orphan-fix regression tests by name.
- [ ] The task's closing note states explicitly whether this guard was or was not the mechanism behind this hunt's discovered incident (per t001).

## Out of scope

- The specific mechanism t001 confirms as the actual cause of this hunt's incident, if it is not this guard (that is t002's job).

## Outcome (2026-08-25)

**This guard was NOT the mechanism behind this hunt's incident** — t001 confirms it: all three repros had matching release generations, so `supersededDeployStatus` returned `settled=false` and the normal `PhaseBuilding` switch ran. The two mechanisms that were live are named in t001 and fixed in t002. This task stands as the hardening pass the filing asked for, and the gap it closes is real.

**The gap.** Past the gate timeout with a generation mismatch and no `status.releaseGeneration`, the branch returned `("", true)` — settled, with no status. Settled means the caller does not fall through to the phase switch, and no status means nothing closes the row: it stays open forever. That is precisely the stranding the whole branch was added to prevent, reintroduced in its own fallback.

**The fix separates two questions the original guard conflated.** `ReleaseGeneration > 0` decides *which terminal* to pick, not *whether* to pick one. Metadata generation is untrustworthy evidence that a row was SUPERSEDED — it also moves for operational churn like a manual scale — so `canceled` remains gated on release-generation evidence exactly as before. But it is not evidence of a healthy build either, so past the timeout the row now fails out in its last observed phase via `timedOutDeployStatus`, which is what an unmatched row with no generation mismatch at all already did. The legacy operator's false-cancel risk is untouched; only "never resolves" becomes "resolves honestly".

**Prior regression tests.** `TestObservedDeployStatusCancelsSupersededRow` passes unchanged. `TestObservedDeployStatusCancelsOrphanedRowAfterTimeout` keeps every assertion of the fix it guards (`2da39122`/`2f9db5d4`) — including that a legacy operator is never reported `canceled`, now asserted explicitly rather than implied — and its one changed line is the assertion this task was chartered to change: the legacy timed-out row goes from `""` (open forever) to `build_failed`. Red pre-fix, green post-fix.

`reconciler.go:511-517`'s `!ok → Create; continue` branch, flagged in this task's Context, is covered by t002's `settleAbandonedDeploys`: it self-heals (the same pass recreates the CR and the next observes it), but it no longer *depends* on healing to evaluate the timeout.
