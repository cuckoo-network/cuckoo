---
id: w6/m95/t007
title: Test coverage for the fixed behavior
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m95/t005]
---

## Objective

Add meaningful tests for the behavior this milestone ships, beyond the targeted regression tests already required by t002/t003/t004 — cover the full journey (create → stuck build → resolves within gate timeout) and the control case (a genuinely healthy build must still reach `live` without being falsely timed out).

## Context

- Tests must assert real behavior and failure modes; never game coverage with trivial, tautological, or snapshot-everything tests.
- Control case matters as much as the failing case: a slow-but-genuinely-progressing build (like the hunt's own `eden-dash-v3` control observation) must not be caught by whatever new timeout/self-heal logic ships here.

## Steps

1. Add an end-to-end store/reconciler test for the full stuck→resolved journey.
2. Add a control test: a build that IS progressing (real Job/Pod exists, making real progress) must not be prematurely failed by the new logic, even near the gate timeout boundary.

## Files

- `lego/backend/internal/store/reconciler_test.go` (or sibling)
- Operator envtest suite, if t001/t002 land there

## Acceptance criteria

- [ ] New tests pass; existing suite (`make test` from `lego/operator/`, `go test ./...` from `lego/backend/`) stays green.
- [ ] The control case (healthy in-progress build near the timeout boundary) has its own explicit test, not just the failing case.

## Out of scope

- The specific regression tests already required as acceptance criteria on t002/t003/t004 (this task adds coverage beyond those, not a duplicate of them).

## Outcome (2026-08-25)

Coverage beyond t002/t003/t004's targeted regressions:

- **Full journey** — `TestFirstDeployWithNoBackingBuildReachesATerminalStateWithinTheGateTimeout` drives a fresh repo-backed service through real `ReconcileOnce` passes: create → parked with no build → `queued` (and no `startedAt`) → past the gate → `build_failed` with `finished_at`, an advanced `updatedAt`, and the operator's own explanation carried into `failureReason`.
- **Control case, checked as hard as the failing case** — `TestHealthyBuildIsNotFailedAtTheGateBoundary` runs a build that is genuinely progressing to one second inside `BuildGateTimeout` and asserts it is still `build_in_progress`, then crosses into rollout (proving the phase transition restarts the budget rather than inheriting 34 minutes of build time — the 2026-08-11 misattribution) and on to `live` with an empty `failureReason`. This test is green both pre- and post-fix by design: its whole job is to prove nothing added here can cut a real build short.
- **Non-closure inside the budget** — both `TestDeployIsSettledWhenTheAppCRCannotBeObserved` cases assert the row is untouched while still inside its budget before asserting it closes past it, so `settleAbandonedDeploys` cannot pass by closing rows eagerly.
- **The notification leg** — the abandoned close is asserted to notify exactly once, with the row's own status and reason.

Suites: `lego/backend` `go test ./...` and `lego/operator` `go test ./...` (envtest, 147 specs) both exit 0; `make lint` 0 issues across all four modules.
