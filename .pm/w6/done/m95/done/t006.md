---
id: w6/m95/t006
title: Simplify the touched code
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m95/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse, simplification, efficiency — behavior-preserving only).

## Context

- Touches `lego/backend/internal/store/reconciler.go`, `lego/backend/internal/core/poll.go`, and whichever operator files t001/t002 land in.

## Steps

1. Invoke `/simplify` scoped to this milestone's diff.
2. Apply any behavior-preserving cleanups it finds.

## Files

- Whatever t001-t004 touched.

## Acceptance criteria

- [ ] `/simplify` run and its findings addressed or explicitly declined with reason.

## Out of scope

- New behavior changes (this task is quality-only).

## Outcome (2026-08-25)

Simplify pass run over this milestone's diff; three cleanups applied, all behaviour-preserving:

- `ReconcileOnce` looked up `openByApp[d.ID]` at four points in one loop body; hoisted to a single `open` binding used by all four (three settle sites plus `recordObservations`).
- The two string literals `"BuildQueued"`/`"Building"`, previously duplicated across the module boundary, collapse into one contract in `lego/types` — the reuse cleanup that makes the fix possible rather than a cosmetic one.
- A doc-comment paragraph break in `failureReasonFor` so the added rationale does not read as a continuation of the quota paragraph.

Declined: merging `timedOutDeployReason` and `abandonedDeployReason`. They differ by one clause, but composing them produces "check the build logs — and this service's App record could not be read", which advises reading logs in exactly the case where the control plane cannot see the service. Two short functions beat one misleading sentence.

`make lint` (all four modules) reports 0 issues.
