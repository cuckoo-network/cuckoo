---
id: w6/m25/t001
title: CRD field + operator → `terminationGracePeriodSeconds`
worker: worker6
status: done
estimate: 45m
depends_on: []
---

## Objective

Add `spec.maxShutdownDelaySeconds` (int32, kubebuilder-validated 1–300) to the App CRD and have the reconciler set the pod template's `terminationGracePeriodSeconds` from it for web/private/background-worker Deployments; unset ⇒ leave k8s's default (30) untouched — byte-identical reconcile for existing Apps.

## Context

- `lego/types/v1alpha1/app_types.go` + codegen from `lego/operator/` (`make test` regenerates).
- `lego/operator/internal/controller/app_controller.go` `reconcileKubernetes` pod template; cron Jobs/static sites are out (spec places the field on the three long-running types only).
- Render schema: integer, min 1, max 300, default 30 (pinned spec, 2026-07-14).

## Files

- `lego/types/v1alpha1/app_types.go`, `lego/operator/internal/controller/app_controller.go`, CRD manifests

## Acceptance criteria

- [x] Envtest: set field ⇒ pod template grace period matches; unset ⇒ field absent (k8s default); out-of-range rejected by CRD validation.

## Out of scope

- API surfaces (t002/t003).
