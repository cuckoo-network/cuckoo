---
id: w6/m131/t008
title: Closeout
worker: worker6
status: done
estimate: 10m
depends_on: [w6/m131/t004, w6/m131/t007]
---

## Objective

When every other task is complete **and the definition of done actually holds**, close the milestone.

## Context

- Do not run this until the DoD is real. Its bullets are live-verifiable: `type=request` returning rows for a service with traffic, non-empty `label=method`/`label=statusCode`, a dashboard Method filter that narrows, and `scripts/logs-verify.sh` run against prod with its output recorded.
- This milestone exists because `w3/m8` closed with exactly that production check owed. Closing this one the same way would repeat the mistake — the guarantee is the live run, not the code.
- The `Unverified` section lists what the filing hunt never observed: the root cause, the metrics consequence (`ADR018:190`), platform-wide scope, MCP, and the individual Status-code/Request-path filters. Each must be verified here or explicitly re-carried, not dropped.

## Steps

1. Re-run every DoD probe against production and record the responses.
2. Set remaining tasks `status: done`, move each `tNNN.md` to `w6/m131/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w6/m131/` to `w6/done/m131/` leaving no stub, and check `- [x]` in `.pm/w6/README.md`.

## Files

- `.pm/w6/m131/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified live against production and recorded.
- [ ] Milestone moved to `w6/done/m131/` with status synced in all three places.

## Out of scope

- New findings surfaced during verification — file them separately.

## 2026-09-05 — DoD verified live end to end; closing

`c58814d7` is deployed (ancestor of image pin `4bd6eb2a3129`). Every DoD bullet re-run against production and recorded:

- `type=request` returns rows on a traffic service (`beancount-forum`, 1h) — was 0 on all five at filing.
- `label=method` / `label=statusCode` non-empty from Loki's index — were `[]`.
- Dashboard Method=GET narrows to GET-only rows (14/14) — was static-fallback → zero.
- `host` discovery value used as a `host` filter returns rows.
- **`scripts/logs-verify.sh` run against production (the check `w3/m8` left owed): PASS** — durable-across-restart, real bounded ranges, honest 503 fallback, the app/request split clean both ways, `level=error` truthful, scoped discovery, and the live deployed ServiceName regex attributes `tea-<xid>` AND `default` while rejecting `kube-system`. Full log in the closeout evidence.
- App/build logs + level normalization still work (regression controls green).
- t009 metrics host-narrowing: `http-requests` host-filtered returns the same 61 points as unfiltered — correct, because `beancount-forum` serves exactly one host, so there is nothing to narrow away (the original bug was 61→0).

All tasks done. Closing.
