---
id: w6/m131/t007
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m131/t005]
---

## Objective

Add tests asserting the behaviour this milestone shipped — real behaviour and failure modes, never tautological or snapshot-everything tests.

## Context

Behaviours worth pinning, each either a live defect or a live-correct case a change could break:

- A `type=request` query against a store with request streams returns those rows.
- Whatever `t002` decided for "store up, stream not produced" — including the deliberate no-change outcome, if that is what was chosen.
- The store-absent 503 path still holds for `type=request`, `type=build` and store-only filters.
- A genuinely quiet service still returns an honest empty.
- `t003`'s decision about `host` discovery vs filterability, and the dashboard's empty-discovery fallback behaviour.
- App, build and predeploy reads are unchanged — they work today and share the query path.
- `type=all` resolves to whatever `t004` settled, with the `type=build` combination rule (`400 log type "build" must be requested on its own`) still enforced.

## Steps

1. Add the tests beside the existing suites in `lego/backend/internal/logs/`.
2. Cover the dashboard fallback behaviour in `dashboard/` if `t003` changed it.

## Files

- `lego/backend/internal/logs/*_test.go`
- `dashboard/src/features/logs/**/__tests__/` if applicable

## Acceptance criteria

- [ ] Each bullet above has a test that fails against the pre-fix behaviour.
- [ ] `cd lego/backend && go test ./...` and `dashboard/yarn test` pass.

## Out of scope

- The live production assertion — that is `t004`'s guard.

## Outcome (2026-08-28)

Tests added for the behaviours this milestone actually shipped:

- `TestRequestStreamStatesAgreeAcrossRESTGraphQLAndMCP` — the three states (store absent => 503/error; store present + stream not produced => honest empty; store present + produced => rows) asserted to agree across all three adapters, with **MCP exercised over the real protocol** rather than assumed.
- `TestQuietServiceAndDarkPipelineAreTheSameResponse` — makes t002's deliberate no-change outcome checkable: the two cases are byte-identical, which is *why* the guard is a platform probe and not a response field.
- `TestBuildTypeIsNeverCombined` — the `type=build` alone rule, replacing three tests of unreachable branches.
- `log-filter-bar.test.tsx` ×3 — t003's fallback behaviour in all three discovery states.
- `TestShipperRegex*` (landed earlier in `4c66473b`) continues to guard the regex against the deployed config.

The store-absent 503 path, the honest-empty path, and app/build/predeploy reads are all still covered by the existing suites, which stay green.
