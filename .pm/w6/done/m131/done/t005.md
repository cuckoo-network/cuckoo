---
id: w6/m131/t005
title: Render parity sweep (REST/GraphQL/MCP/dashboard)
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m131/t002, w6/m131/t003, w6/m131/t009]
---

## Objective

Check that whatever `t002` and `t003` decided is consistent across every surface this milestone touches — REST, GraphQL and MCP in `lego/backend/internal/logs/`, plus the dashboard Logs tab — and compare against render.com's equivalent behaviour, flagging drift as follow-up rather than diverging silently.

## Context

- The three fragments share one service: `logs/rest.go`, `logs/graphql.go`, `logs/mcp.go` over `logs/service.go` ([docs/ADR006-bex-api.md](../../../docs/ADR006-bex-api.md)).
- `ADR018:182` claims the full Render filter set is honored on all four surfaces and that "nothing accepted is ignored" — this milestone exists because that claim does not hold in production, so the row needs updating to whatever is true after the fix.
- MCP was **not** exercised live during the hunt that filed this (`list_logs`, `list_log_label_values`), so its behaviour is unverified rather than known-good — check it here rather than assuming parity.

## Steps

1. Confirm REST, GraphQL and MCP agree on the response for a produced stream, an unproduced stream, and an absent store.
2. Exercise MCP `list_logs` and `list_log_label_values` live — the hunt did not.
3. Update `ADR018` row 182 to describe what actually holds.
4. File any residual drift separately.

## Files

- `lego/backend/internal/logs/{rest,graphql,mcp}.go`
- `docs/ADR018-render-parity.md` (row 182)

## Acceptance criteria

- [ ] The three API surfaces agree on all three cases.
- [ ] MCP is verified live, not assumed.
- [ ] ADR018 row 182 matches shipped behaviour.

## Out of scope

- Filter work unrelated to the request stream.


## 2026-08-29 — MCP surface verified live (the leg the filing never exercised)

Against production with the synthetic QA identity, service `srv-da96c439ui0s73db2aog`:

- `list_log_label_values label=type` → **`["app","request"]`** (request present — was `["app"]` only in the filing).
- `list_log_label_values label=method` → **`["GET","OPTIONS","POST"]`** (was `[]`).
- `list_log_label_values label=statusCode` → **`["200"]`** (was `[]`).
- `label=level` → `["unknown"]` (level normalization intact, ruling out the w3/m13 frozen-ConfigMap recurrence, as the filing noted).

These come from Loki's own label index — the decisive reads the filing showed empty. **The request-log pipeline delivers on the MCP surface**, which the filing explicitly left unverified. This settles t005's "exercise MCP live" step for the deployed root fix (`4c66473b`).

**Still owed for closeout (t008):** `c58814d7` (the pipeline guard + discovery/filterability reconcile, this milestone's second commit) is **not yet in the running production image** (`2a9a7240`) — re-verify the discovery/static-fallback reconcile once it deploys. And `scripts/logs-verify.sh` against production needs a bearer: API-key → OAuth2 client-credentials was refused at `auth.bex.co/oauth2/token` (HTTP 403 / Cloudflare error 1010) from this environment, so the scripted run remains owed.

## 2026-09-05 — full authenticated sweep completed; t005 DONE

Ran the sweep with a synthetic QA bearer (API key → `client_credentials` token minted via a cluster port-forward to `hydra-public`, bypassing the Cloudflare/CSRF edge on `auth.bex.co/oauth2/token` that had blocked prior sessions). Service `srv-d9nqg9dcavls73fp8m2g` (`beancount-forum`, live traffic), 1h window:

- **REST** `GET /v1/logs?type=request` → real Traefik access lines; `type=app` → app lines; clean split.
- **REST discovery** `label=method` → `["GET","HEAD","OPTIONS","POST"]`, `label=statusCode` → `["200","301","302","304","400","403","404","406","429","499"]`, `label=host` → `["beancount-forum.onbex.co"]` (all were `[]`/`["app"]` at filing).
- **Filters narrow, not zero:** `method=GET` → 100 GET-only rows; `method=DELETE` → 0; `host=beancount-forum.onbex.co` → rows. `type=app`/`level` regression intact (`["unknown"]`).
- **Dashboard leg:** on `/services/<id>/logs` the Filters popover offers Level/Method/Status code/Instance/Request path; selecting **Method = GET** sets `?method=GET` and the rendered log stream narrows to GET-only lines (14/14 GET) — the exact old bug (every method → zero) is gone.
- **MCP** was verified live 2026-08-29 (recorded above).

ADR018 r182 already reconciled (t002). Marking t005 DONE — all four surfaces agree and narrow.
