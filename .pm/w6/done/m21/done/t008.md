---
id: w6/m21/t008
title: Closeout
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m21/t007]
---

## Objective

Close the milestone once the definition of done actually holds: set all tasks `status: done`, move them to `done/`, update the milestone README status, move `w6/m21/` to `w6/done/m21/`, and check its box in the `w6` workstream README.

## Steps

1. Verify the definition of done in `README.md` is actually met.
2. Run `/pm done w6/m21/t008`.

## Files

- `w6/m21/README.md`, `w6/README.md`, all task files in `w6/m21/`.

## Acceptance criteria

- [x] Milestone moved to `w6/done/m21/`, all statuses in sync.

## Out of scope

- Any new implementation work.

## Resolution

DoD verified: an App can specify `dockerfilePath` and `startCommand` via bex.yml, REST,
GraphQL, and MCP, and both take effect on the build (`--opt filename=`,
`TestBuildJobCustomDockerfilePath`) and the Deployment (container `Command` override,
`startcommand_test.go`'s envtest table, now correctly gated on build strategy rather than
`Runtime == "docker"` alone). `make test` (operator) and `go test ./...` (backend) pass;
no new lint issues. Milestone moved to `w6/done/m21/`, all task statuses synced.
