---
id: w6/m21/t007
title: Test coverage
worker: worker6
status: done
estimate: 1h
depends_on: [w6/m21/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: Dockerfile-path override in builds, start-command override in Deployments, and end-to-end field round-trip.

## Steps

1. `build_test.go` case: custom `DockerfilePath` produces the expected `--opt filename=` arg; path-traversal rejected.
2. `app_controller` envtest: `StartCommand` set vs. unset produces the expected container `Command`/`Args`.
3. Backend integration test: create App via REST/GraphQL/MCP with both fields, assert CR round-trip.

## Files

- Test files alongside each package touched in t001-t004.

## Acceptance criteria

- [x] Tests assert real behavior and failure modes (e.g. path traversal rejection), not tautological checks.
- [x] `make test` (operator) and `go test ./...` (backend) pass.

## Out of scope

- Load/performance testing.

## Resolution

`build_test.go`'s `TestBuildJobCustomDockerfilePath` and `service.go`'s
`store.ValidRootDir`-backed traversal rejection already existed (shipped earlier). Added:
`lego/backend/internal/apps/builder_test.go`'s `TestDockerfilePathAcrossCreateSurfaces`
(GraphQL + MCP round-trip — REST was already covered by
`TestRenderDockerDetailsMapToDockerfileBuild`); `deploy_test.go`'s
`TestDeployMapsDockerfilePathAndStartCommand` (bex.yml round-trip); and a new
`lego/operator/internal/controller/startcommand_test.go` envtest table covering
runtime:docker with/without `startCommand`, the legacy `builder:dockerfile` path, and the
native-runtime negative case (baked into the image's own CMD, no Deployment override).
`make test` (operator, 42/42 specs) and `go test ./...` (backend, all packages) both pass;
`make lint` shows no new issues (verified against the pre-existing `main` baseline).
