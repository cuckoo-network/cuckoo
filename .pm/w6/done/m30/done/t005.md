---
id: w6/m30/t005
title: "Simplify — /simplify over the code this milestone changed"
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m30/t004]
---

## Objective

Run `/simplify` over the operator + backend + dashboard diff; the middleware-rendering plumbing shared with w7/m5/m32 is the likely reuse spot.

## Acceptance criteria

- [x] `/simplify` run; shared host filtering/naming/upsert helpers retained, stale redirect artifacts are centrally garbage-collected, and the live-discovered Traefik annotation mismatch was corrected for both redirect and IP-allowlist middleware paths.
