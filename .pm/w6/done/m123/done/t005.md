---
id: w6/m123/t005
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m123/t004]
---

## Objective

Run `/simplify` over this milestone's diff — the failure-message composition, the deploy timestamp path, and the build log wiring — behaviour-preserving.

## Context

- `lego/operator/internal/controller/{app_controller.go,build_failure.go}`, `lego/operator/internal/build/build.go`, `lego/backend/internal/store/{deploy_lifecycle.go,reconciler.go}`, `lego/backend/internal/logs/progress.go`.
- `buildFaultViews` exists as one table precisely so the three projections cannot disagree (`build_failure.go:23-29`) — keep that property; do not split it back into switches while tidying.

## Steps

1. Run `/simplify` over the diff.
2. Apply behaviour-preserving improvements only.
3. Re-run the operator and backend suites.

## Files

- The files `t001`–`t003` touched

## Acceptance criteria

- [ ] No behaviour change; `t006`'s tests still pass.
- [ ] The single-table invariant in `build_failure.go` survives.

## Out of scope

- Restructuring the build pipeline beyond this milestone's diff.
