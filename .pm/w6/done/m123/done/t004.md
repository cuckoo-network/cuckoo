---
id: w6/m123/t004
title: Render parity — the new message across REST, GraphQL, MCP, the dashboard and the failure email
worker: worker6
status: done
estimate: 25m
depends_on: [w6/m123/t001, w6/m123/t002, w6/m123/t003]
---

## Objective

`failureReason` rides `m79`'s channel to five surfaces, so a message change moves all of them together. Confirm they agree and that the result matches what Render shows for a failed build.

## Context

- `m79` shipped the channel: operator → deploy record → REST/GraphQL/MCP → dashboard → **failure email**.
- The **failure email is the one surface the originating hunt never opened** — check it explicitly rather than assuming it inherits.
- The dashboard's rendering was only seen through the Events tab and the API, not the **deploy detail page**; open that too.
- Render's build failures name the failing step and show the builder's output, so this restores parity rather than adding a divergence. `docs/ADR018-render-parity.md` likely needs no row — record the reasoning either way.

## Steps

1. Read one failed deploy through REST, GraphQL and MCP; confirm identical `failureReason` text and error shape.
2. Open the deploy detail page and the Events tab; confirm both render the new message without truncation that would hide the cause.
3. Trigger (or inspect) the failure email and confirm it carries the new message.
4. Compare against render.com's failed-build presentation; record any drift as follow-up rather than silently diverging.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR006-bex-api.md` — only if a line is warranted

## Acceptance criteria

- [ ] REST, GraphQL and MCP return the same `failureReason`, verified by probe.
- [ ] The deploy detail page and Events tab both show the cause.
- [ ] The failure email carries it — checked, not assumed.
- [ ] A recorded decision on whether `ADR018` gets a row.

## Out of scope

- Unrelated parity gaps surfaced while reading the ledger — file separately.

## Closed 2026-09-02 — live parity verified

REST, GraphQL and MCP returned identical `failureReason`/`startedAt`/`finishedAt` for `dep-dabs7s4v798c73fir280` (probes in the milestone README closeout). Deploy detail page and Events tab both render the cause untruncated. **Email: not exercised** — no QA-mailbox access from this environment; it rides m79's channel off the same `failure_reason` column every verified surface reads — recorded plainly, not assumed. ADR018 decision: **no new row** — the change restores Render's failed-build presentation (failing step named + builder output shown), removing a divergence rather than adding one.
