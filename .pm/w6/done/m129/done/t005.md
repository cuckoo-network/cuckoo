---
id: w6/m129/t005
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m129/t004]
---

## Objective

Run `/simplify` over this milestone's diff — the counter, whatever list/affordance change `t001` chose, and the tests — behaviour-preserving.

## Context

- `lego/backend/internal/workspaces/service.go`, possibly `lego/backend/internal/apps/service.go`, possibly `dashboard/src/features/usage/`.
- After `t002` the three counters follow one rule; expressing that rule once rather than three times is the obvious consolidation — but keep the three resource kinds' caps distinct, since Hobby differs per kind (`plans.go:107`).
- Do not merge the display counter with the ResourceQuota builder just because both call `QuotaCapsForPlan`; they are deliberately separate layers, and coupling them would make the enforcement path depend on a display concern.

## Steps

1. Run `/simplify` over the diff.
2. Apply behaviour-preserving improvements only.
3. Re-run `cd lego/backend && go test ./...` and, if the dashboard changed, `cd dashboard && yarn test`.

## Files

- The files `t001`–`t002` touched

## Acceptance criteria

- [ ] No behaviour change; `t006`'s tests still pass.
- [ ] Display and enforcement remain separate layers reading one cap source.

## Out of scope

- Refactoring the quota mechanism itself.
