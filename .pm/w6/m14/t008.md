---
id: w6/m14/t008
title: "Render parity: sweep ownerId semantics across REST/GraphQL/MCP/UI; update ADR018 row 19"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m14/t007]
---

## Objective

Standing closing task: confirm the `ownerId` write contract is consistent across all four surfaces (same field names, same 403 semantics, same absent-⇒-default behavior) and compare against Render's OpenAPI — noting the one deliberate softening (Render requires `ownerId` on create; bex keeps it optional with a deterministic default). Update `docs/ADR018-render-parity.md`'s "Create service" row, whose current `ownerId` omission note ("single workspace") this milestone obsoletes.

## Context

- `docs/ADR018-render-parity.md` row "Create service (web / private)" + the gaps table.
- `docs/ADR006-bex-api.md` — one core, thin adapters.

## Steps

1. Sweep the three backend fragments + dashboard for field/semantics/error-shape consistency.
2. Diff against Render's documented create contracts; file any drift as follow-up notes rather than silently diverging.
3. Update ADR018 (row text + evidence); `npx prettier@3.4.2 --write` the touched docs.

## Files

- docs/ADR018-render-parity.md

## Acceptance criteria

- [ ] ADR018 row reflects the shipped `ownerId` behavior with the optional-vs-required deviation documented.
- [ ] Any found drift is filed (inbox note or task), not silently accepted.

## Out of scope

- Building toward Render capabilities marked `—` in the ledger.
