---
id: w6/m14/t005
title: "Dashboard: thread currentWorkspaceId into create/write mutations"
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m14/t004]
---

## Objective

The dashboard's list queries already pass the switcher's workspace (`use-services.ts:29` → `ownerId: currentWorkspaceId`) but its **mutations** don't — a multi-workspace user creating a service while switched into workspace B gets it in an arbitrary workspace. Thread `currentWorkspaceId` into the create/write mutations: the services wizard (`createService`), database and keyvalue creates, and any env-var/write path whose backend verb learned `ownerId` in t003/t004.

## Context

- `dashboard/src/features/services/` (wizard + hooks), `databases/`, `keyvalue/` — mirror the `use-services.ts` pattern (skip/pause until `currentWorkspaceId` resolves).
- Regenerate/sync `dashboard/src/graphql/definitions.ts` after the m14 schema change (the m5-established source-diff check).
- Scope prettier/format to touched files only (repo memory: never bare `yarn format`).

## Steps

1. Add `ownerId` to the mutation documents in `*/api/*.graphql`; sync `definitions.ts`.
2. Pass `currentWorkspaceId` from the workspace context in each mutation hook.
3. Update affected hook/component tests; `yarn typecheck` + `yarn vitest run` green.

## Files

- dashboard/src/features/services/api/*.graphql + hooks
- dashboard/src/features/databases/, keyvalue/ equivalents
- dashboard/src/graphql/definitions.ts

## Acceptance criteria

- [ ] Every create mutation document carries `ownerId` wired from the switcher (tests assert the variables).
- [ ] `yarn typecheck` + full vitest suite green.

## Out of scope

- The audit-log workspace source — t006.
