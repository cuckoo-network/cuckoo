---
id: w6/m48/t006
title: Test coverage for the fixed behaviors
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m48/t004]
---

## Objective

Add meaningful tests for the behaviors this milestone fixed — real assertions on the null-vs-empty-string wire contract and the dashboard consumer logic, not tautological or snapshot-everything tests.

## Context

- Backend: a regression test asserting `envGroup(id:<workspace-scoped>){environmentId}` (and the migrated `service`/`postgres`/`keyValue` fields) resolve to `null` over GraphQL for an unset optional FK, mirroring what REST already asserts via its `omitempty` contract. Follow `w6/m44`'s test shape (`lego/backend/internal/api/deadid_notfound_test.go`) for the "GraphQL vs REST agreement" pattern.
- Dashboard: unit tests for `isCompatible` (both the "both sides unset → compatible" and "genuine mismatch → incompatible" cases), the `available` list gating the Link Service control, and `moveTarget`'s initial value for a workspace-scoped group.

## Steps

1. Backend test(s) pinning the null/omitted agreement for each migrated field, proven red against the pre-fix code.
2. Dashboard test(s) for `linked-services-card.tsx` and `env-group-actions.tsx` covering the workspace-scoped-group cases, proven red against the pre-fix code.

## Files

- `lego/backend/internal/envgroups/`, `apps/`, `postgres/`, `keyvalue/` (new or extended `*_test.go`)
- `dashboard/src/features/env-groups/components/__tests__/`

## Acceptance criteria

- [ ] New/updated backend tests fail on the pre-fix code and pass on the fix.
- [ ] New/updated dashboard tests fail on the pre-fix code and pass on the fix.
- [ ] `cd lego/backend && go test ./...` and `dashboard/`: `yarn test` green.

## Out of scope

- Test coverage for t003's copy-only change beyond a render-the-right-string assertion (no new behavior to pin).
