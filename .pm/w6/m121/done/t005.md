---
id: w6/m121/t005
title: Render parity — confirm the widened preflight changes no wire contract, and record the divergence class
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m121/t004]
---

## Objective

The milestone changes an advertised REST surface, so the standing parity check applies. Confirm the change is a transport-layer correction with no field, semantic or error-shape drift on REST/GraphQL/MCP, and record how it sits against Render.

## Context

- Render documents its REST API as **server-to-server** and does not invite browser clients, so bex is not restoring a Render behavior here — it is making its own allowlisted origin coherent with its own router. Say that explicitly rather than filing it as a parity gap closed.
- `docs/ADR006-bex-api.md` is the compatibility contract; `docs/ADR018-render-parity.md` is the ledger.
- Nothing about request/response bodies changes — only which requests a browser is permitted to send.

## Steps

1. Confirm no REST response body, status code or error shape changed.
2. Confirm GraphQL and MCP are untouched (they are: `withCORS` fronts the HTTP layer, and the GraphQL path already worked).
3. Decide whether `ADR018` needs a row. Expected: no row, since the advertised *surface* is unchanged — only its browser reachability. Record the reasoning either way.
4. Note in `ADR006` (or the CORS code comment) that the preflight is derived from the router, so future route additions are covered automatically.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR006-bex-api.md` — only if a line is warranted

## Acceptance criteria

- [x] Written confirmation that no REST/GraphQL/MCP wire shape changed.
- [x] An explicit decision on whether `ADR018` gets a row, with reasoning.
- [x] The "Render is server-to-server, this is bex's own coherence" framing is recorded so a later reader does not mistake it for a parity gap.

## Completion evidence

- No request body, REST response body, status code or error mapper changed. GraphQL and MCP registrations/handlers are untouched; only browser preflight metadata around the existing HTTP router changed.
- `docs/ADR018-render-parity.md` gets no row: Render documents its public REST API for server-to-server use and promises no browser CORS contract, so this is not a Render parity gap closed. It makes bex's own credentialed dashboard origin coherent with the Render-compatible routes bex already exposes.
- The code records the durable invariant where it matters: preflight probes the composed `ServeMux` topology, so a future registered method is admitted without a second policy edit.

## Out of scope

- Any new Render-parity work surfaced while reading the ledger — file separately.
