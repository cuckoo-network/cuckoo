---
id: w6/m131/t006
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m131/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency — strictly behavior-preserving.

## Context

- Likely touched: `lego/backend/internal/logs/` (`service.go`, `loki.go`, and whichever adapters `t002`/`t003` moved), the dashboard Logs Filters popover, and `scripts/logs-verify.sh`.
- `t004` already removes `lokiTypeMatcher`'s unreachable branches; check whether the surrounding type-handling collapses further once they are gone, without losing the comments that document Render's vocabulary.

## Steps

1. Run `/simplify` over this milestone's diff.
2. Keep behaviour identical; existing suites plus `t007`'s tests stay green.

## Files

- The files this milestone changed.

## Acceptance criteria

- [ ] `/simplify` run over the diff; changes behavior-preserving.
- [ ] `make lint` and `cd lego/backend && go test ./...` pass.

## Out of scope

- Refactors beyond this milestone's code.
