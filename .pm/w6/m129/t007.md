---
id: w6/m129/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m129/t005, w6/m129/t006]
---

## Objective

Close the milestone once every task is done **and** the DoD holds against production.

## Context

- The live check is cheap and read-only: compare GraphQL `workspaceLimits` against `GET /v1/services?limit=100` for the same workspace. During the hunt these read 11 and 6.
- The full-cycle check needs a create and a delete: read the counter, create a service, read it again, delete, wait for teardown to finish, read again. It must return to the starting value.
- **`t003`'s measured result must be recorded in the milestone README**, replacing the inferred claim about ResourceQuota behaviour. Do not close with an inference standing where a measurement now exists.
- `w3/m46/t009`'s stuck static site (`srv-da7tf87krsvc73c3mcng`) may still be terminating. If it is, it is a live fixture for this DoD — and its continued presence is worth noting on that task, since it is the thing holding the quota.

## Steps

1. Compare the counter with the list on production; confirm they reconcile.
2. Run the create → read → delete → read cycle and confirm the counter returns.
3. Confirm the README's ResourceQuota claim is the measured one.
4. Note the state of the long-running terminating static site for `w3/m46/t009`.
5. Set remaining tasks `status: done`, move each to `w6/m129/done/`, mark rows `— **DONE**`, set `**Status:** done`.
6. Move `w6/m129/` to `w6/done/m129/` (no stub) and check `- [x]` in `.pm/w6/README.md`; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m129/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Counter and list reconcile on production, with the capture recorded.
- [ ] The create/delete cycle returns the counter to its starting value.
- [ ] The inferred ResourceQuota claim has been replaced by `t003`'s measurement.
- [ ] Milestone moved to `w6/done/m129/` with no tombstone; status consistent in all three places.

## Out of scope

- Fixing the stuck teardown itself — that is `w3/m46/t009`.
