---
id: w6/m29/t005
title: "Test coverage — envtest: secret attached when set, omitted when unset"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m29/t003]
---

## Objective

Meaningful envtest coverage in `lego/operator`: with `BEX_REGISTRY_PULL_SECRET` set, every generated workload kind that pulls tenant images carries it; with it unset, the generated specs are unchanged (the byte-identical dev default). Assert the actual pod specs, not just "no error".

## Acceptance criteria

- [ ] Envtests cover set/unset for each workload kind touched; `make test` green.
