---
id: w6/m105/t005
title: Render parity
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m105/t004]
---

## Objective

Confirm the disk-snapshot surface stays consistent across REST, GraphQL, MCP, and the dashboard after
t002-t004 land, and that the existing not-configured/real-outage distinction in the dashboard
(`disk-section.tsx`'s `SnapshotsCard`) still holds with the new `t003` error code in play.

## Context

- The disk-snapshot verb triple: `GET /v1/disks/{diskId}/snapshots` (REST), `diskSnapshots` (GraphQL),
  `list_disk_snapshots` (MCP) — all three call the same `Service.ListDiskSnapshots`
  (`lego/backend/internal/apps/disk_snapshots.go`).
- Render's own disk-snapshot API shape (`docs/ADR082-persistent-disks.md` D5) is the parity reference.

## Steps

1. Confirm all three surfaces return the same shape and the same error code (both
   `DISK_SNAPSHOTS_NOT_CONFIGURED` and t003's new code) for the same underlying condition.
2. Confirm the dashboard's `SnapshotsCard` branches correctly on both codes post-fix (no regression to the
   `notConfigured` branch introduced by t003's changes).
3. Note any deliberate divergence from render.com's own disk-snapshot behavior; there should be none new
   from this milestone.

## Files

- `lego/backend/internal/apps/disk_snapshots.go`
- `dashboard/src/features/services/components/disk-section.tsx`

## Acceptance criteria

- [ ] REST/GraphQL/MCP agree on shape and error codes for both the not-configured and the new
      backend-failure cases.
- [ ] Dashboard branches correctly on both, verified against a real (or faked) response of each kind.

## Out of scope

- New Render-parity gaps unrelated to disk snapshots.
