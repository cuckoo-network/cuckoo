---
id: w6/m105/t007
title: Test coverage
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m105/t005]
---

## Objective

Add meaningful regression tests for the behavior this milestone fixed: a backend list-snapshots failure
must not silently regress to the generic "internal error" state again.

## Context

- The exact regression this milestone closes has no permanent test today — `w1/m87/t009` tested the
  not-configured case and the cross-surface identity of that one code, but nothing pins the behavior of a
  configured-but-failing `DiskSnapshotLister`.

## Steps

1. Add a fake `DiskSnapshotLister` that returns a raw (non-coded) error and assert `ListDiskSnapshots`
   returns the new coded error from t003, not a bare wrapped error.
2. Add a REST + GraphQL + MCP-level test (mirroring `w1/m87`'s existing coverage pattern) proving the new
   code survives each surface's own error-handling/sanitization path, the same way `w1/m87/t007`'s guard
   proved it for `DISK_SNAPSHOTS_NOT_CONFIGURED`.
3. `go test ./...` (backend) and `yarn test` (dashboard, if `disk-section.tsx` changed) both green.

## Files

- `lego/backend/internal/apps/disk_snapshots_test.go`
- `dashboard/src/features/services/components/__tests__/disk-section.test.tsx` (if UI changed)

## Acceptance criteria

- [ ] A test exists that fails on the pre-fix code (generic "internal error" for a configured-but-failing
      lister) and passes on the post-fix code.
- [ ] `go test ./...` and relevant dashboard tests are green.

## Out of scope

- Coverage for unrelated disk verbs.
