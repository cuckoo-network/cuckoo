---
id: w6/m115/t006
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m115/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- The specific reuse target: the display-label fallback now exists in Go (`apps/render.go:511-515`), SQL (`store/webhooks.go:679`), and TypeScript (`dashboard/src/features/services/lib/status.ts:35-39`). Three implementations of one rule is what let them drift. The Go and SQL copies are unavoidable (different runtimes); the TypeScript one becomes redundant once t002 lands and is the candidate to retire.
- Retiring the dashboard copy is only safe if GraphQL's `name` is the label for **every** query the dashboard runs, including ones that do not select `displayName`. Check before deleting.

## Steps

1. Run `/simplify` over the touched files.
2. Evaluate whether `status.ts`'s `displayName ?? immutableName` can collapse to `s.name` — and if it cannot, leave a comment saying why, so the next reader does not treat it as dead code.

## Files

- files touched by t002/t003/t004
- `dashboard/src/features/services/lib/status.ts`

## Acceptance criteria

- [ ] `/simplify` run; behavior-preserving changes applied.
- [ ] The dashboard-side fallback is either removed or annotated with the reason it stays.

## Out of scope

- Behavior changes.
