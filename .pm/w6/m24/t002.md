---
id: w6/m24/t002
title: Attribute groups to a workspace (label/store + migration)
worker: worker6
status: done
estimate: 60m
depends_on: [w6/m24/t001]
---

## Objective

Give each env group an owning workspace: a store column or CR/Secret label (`core.LabelProject`-style), set at create from the caller's resolved workspace, plus a migration assigning existing global groups a deterministic owner (the oldest-membership / documented rule — decide + record; do NOT silently strand them).

## Context

- Env groups materialize to `<evg-id>-env`/`<evg-id>-files` Secrets in `s.Namespace`; the `apps.project_id` / m18 `core.WithWorkspace` attribution seams are the pattern.

## Files

- `lego/backend/internal/envgroups/service.go` (+ store/migration if groups gain a row)

## Acceptance criteria

- [ ] New groups carry their workspace; a migration assigns existing groups a recorded owner; round-trip asserted.

## Out of scope

- Enforcement (t003/t004) — this task only records ownership.
