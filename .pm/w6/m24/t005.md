---
id: w6/m24/t005
title: `ownerId` + timestamps on the group object (Render shape)
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m24/t002]
---

## Objective

Expose `ownerId` and created/updated timestamps on the env-group object across REST/GraphQL/MCP, closing most of the w5/m26-documented shape divergence vs Render's OpenAPI (`ownerId`, timestamps, `environmentId`). Verify field names against `https://api-docs.render.com/openapi/render-public-api-1.json`; leave `environmentId` out only if genuinely unmodeled (note it).

## Files

- `lego/backend/internal/envgroups/{service,rest,graphql,mcp}.go`

## Acceptance criteria

- [ ] Group object carries `ownerId` + timestamps identically on all three surfaces; ADR018 shape divergence narrowed with evidence.
