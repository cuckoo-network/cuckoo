---
id: w6/m24/t001
title: Reproduce the cross-tenant list + reveal (severity record)
worker: worker6
status: todo
estimate: 45m
depends_on: []
---

## Objective

Before changing anything, prove the exposure: two workspaces on the mock cluster (or a real-Postgres+OpenFGA test), workspace A creates an env group with a sensitive value; confirm whether a workspace-B caller can (a) see it in `ListEnvGroups`, (b) get it, (c) reveal its value via the `RelCanViewSensitive` path, (d) link it into a B service. Record which of the four actually leak — the fix scope follows the finding, not the assumption.

## Context

- `lego/backend/internal/envgroups/service.go` — `s.Authorize(ctx, core.RelCanView/...)` checks the caller's own-workspace relation; the group store is namespace-flat (`service.go:608,618`).
- Mirror the m18 repro harness (`TestReadSideOwnerIDTargetingE2E`).

## Acceptance criteria

- [ ] A failing test (or documented manual repro) demonstrates each real leak among list/get/reveal/link; severity recorded on the milestone README.

## Out of scope

- The fix (t002–t005).
