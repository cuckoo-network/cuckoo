---
id: w6/m41/t005
title: Simplify the code this milestone changed
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m41/t004]
---

## Objective

Run `/simplify` over the code this milestone touched — behavior-preserving only.

## Context

- Changed surface: `lego/backend/internal/store/reconciler.go` (the ordering guard, the checkpoint reference, the rejection counter) and the metric registration site.
- Watch specifically for: three now-adjacent filters on the unhealthy path (`RolloutSettling` exclusion, `debounceUnhealthy`, the ordering guard) that may read more clearly as one explicit pipeline — but only if that does not obscure the distinct reason each exists. Each was added by a different incident; preserving that legibility is worth more than brevity.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the clearly-correct findings; record anything declined and why — especially any collapse that would blur why each filter exists.

## Files

- the diff produced by `t001`–`t004`

## Acceptance criteria

- [ ] `/simplify` run; findings applied or explicitly declined with a reason.
- [ ] Behavior unchanged — `t006` and the m78 tests stay green.
- [ ] `make lint-backend` (from `lego/operator/`) clean.

## Out of scope

- Bug hunting (`/code-review`) and any behavior change.
