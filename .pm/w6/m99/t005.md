---
id: w6/m99/t005
title: Simplify
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m99/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse, simplification, efficiency — behavior-preserving).

## Context

- Changed surface is small and concentrated: one backend field computation + mapper wiring (t001), one frontend boolean derivation (t002), and their tests (t003). Watch specifically for: the t001 grant-check being duplicated instead of factored into one helper `mintCloneSecret` and the new read-path both call; and the t002 frontend logic accidentally re-deriving from `useRepos()` when the server-provided field already answers the question more reliably.

## Steps

1. Run `/simplify` scoped to the diff from t001-t003.
2. Apply any reuse/simplification/efficiency findings that don't change behavior.

## Files

- Whatever t001-t003 touched.

## Acceptance criteria

- [ ] `/simplify` run recorded; findings applied or explicitly noted as declined with reason.
- [ ] No behavior change from t001-t003's verified acceptance criteria.

## Out of scope

- New functional changes — this task is cleanup only.
