---
id: w6/m99/t007
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m99/t006]
---

## Objective

Verify the milestone's Definition of done actually holds live, then close it out: mark every task done, move `tNNN.md` files to `w6/m99/done/`, mark the README's table rows `— **DONE**` and `**Status:** done`, move `w6/m99/` to `w6/done/m99/`, and check the box in `w6/README.md`.

## Context

- Do not run this task until the DoD in `w6/m99/README.md` is verified against a live service in both the granted and ungranted cases — a milestone lands in `done/` when its observable end state is real, not merely when the code is written.

## Blocked on

t001–t006 are done and green in the working tree; this task's live-verification steps require the change to be **deployed to production** first (create an ungranted-repo service on `dashboard.bex.co`, read the hint, and re-run this hunt's MCP `tools/call get_service` against it). Until then the milestone stays open — its observable end state is not real yet.

## Steps

1. Live-verify each Definition of done bullet (create an ungranted-repo service, check the hint + REST/GraphQL/MCP field; check the `w2/m9` control case is unchanged; confirm a real push to the ungranted repo still doesn't auto-deploy).
2. Run `npx prettier@3.4.2 --write "**/*.md"`.
3. Perform the standard closeout move (frontmatter, README status lines, directory move, workstream checkbox).

## Files

- `.pm/w6/m99/*` → `.pm/w6/done/m99/*`
- `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every DoD bullet in `w6/m99/README.md` verified live, not just reasoned about.
- [ ] All tasks `status: done`, moved to `done/`.
- [ ] `w6/README.md` checkbox checked, milestone directory moved to `w6/done/m99/`.

## Out of scope

- Anything not already scoped by t001-t006.

## Blocker update (2026-08-27/28) — the deployment premise is stale; read this first

**The fix is live.** `bfd3a062` (w6/m99) is a confirmed ancestor of `050d40e4`, the commit `deploy.yml` last shipped successfully (`git merge-base --is-ancestor bfd3a062 050d40e4`). Everything written above about waiting for a deploy is **discharged** — do not re-argue it.

**The remaining blocker is credentials, not code and not the pipeline.** This session could not run the live leg because the QA sign-in is not configured:

- `scripts/qa-login.sh` exits **2** — `QA_EMAIL`/`QA_PASSWORD` are absent from `.env`. They are present as empty names in `.env.example:423-424`, so `.env` has simply drifted behind it.
- `BEX_API_TOKEN` is set but returns **401** against `https://api.bex.co/v1/services` (and `/v1/owners`), so it is not a usable API credential for this surface either — the unauthenticated control returns the same 401.

**To unblock:** fill `QA_EMAIL`/`QA_PASSWORD` in `.env` (never paste them into chat or a tool call — `scripts/qa-login.sh` reads them inside its own process and hands back cookies only), then run this task's steps unchanged. Nothing else about the task needs revisiting.
