---
id: w6/m96/t004
title: Render parity
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m96/t002, w6/m96/t003]
---

## Objective

Confirm REST, GraphQL, and MCP all agree on Blueprint-by-id behavior after t001-t003, and that this fix introduced no drift between them — GraphQL and MCP were already correct (never wrapped by the REST validator), so this task's job is to reconfirm that with evidence, not assume it, and to check render.com's actual behavior for the equivalent calls where practical.

## Context

- REST: `internal/apps/rest.go:1172-1198` (blueprint by-id + syncs routes).
- GraphQL: `internal/apps/graphql.go` (`blueprint`, `blueprintSyncs`, `syncBlueprint`, `updateBlueprint`, `disconnectBlueprint` resolvers).
- MCP: `internal/apps/mcp.go:344-407` (`get_blueprint`, `sync_blueprint`, `update_blueprint`, `disconnect_blueprint`, `list_blueprint_syncs`).
- All three must accept a `blp-…` id, apply the same authz (own workspace → 200/data, other workspace → denied, nonexistent → not-found), and return the same shape for the same underlying row.

## Steps

1. Re-run (or write, if missing) one test per surface that calls the by-id verb with the same real id and workspace-membership scenarios as t002's REST tests, and diff the returned fields/errors across REST/GraphQL/MCP for parity.
2. Compare against Render's actual documented behavior for `GET /v1/blueprints/{id}` etc. (the embedded OpenAPI spec's response schema is the reference; note any deliberate bex divergence found, don't silently fix or silently ignore it).
3. Record findings (parity confirmed, or any drift found + filed as follow-up) in this task's own notes before marking done.

## Files

- `lego/backend/internal/apps/graphql.go`, `mcp.go`, `rest.go` (read/verify; only touch if a real drift is found)

## Acceptance criteria

- [ ] REST/GraphQL/MCP verified to agree on the by-id Blueprint verbs' success/403/404 behavior and response shape, with a test or explicit evidence for each, not an assumption.
- [ ] Any drift found is either fixed here (if trivial) or filed as a new, separately-scoped follow-up (if not) — not silently left.

## Out of scope

- Re-litigating t001's fix design.
