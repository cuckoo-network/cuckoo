---
id: w6/m96/t003
title: Correct ADR020 (record the deviation) and ADR018's Blueprint parity row
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m96/t001]
---

## Objective

Make the documentation match reality: ADR020 currently neither lists Blueprint in its aligned-prefix table nor records it as a deliberate, documented deviation — it's simply a gap. After t001 closes the functional bug, document why `blp-…` differs from Render's `exs-…` and that a compatibility override exists for it, so the next person who greps ADR020 for "is this id REST-safe" gets a real answer instead of silence.

## Context

- `docs/ADR020-identifiers.md:16-24` — the aligned-prefix table lists `tea-/srv-/dpg-/red-/cdm-/evt-/crr-` as "Render's public-API spellings, so bex ids are drop-in for Render-shaped clients." Blueprint isn't in it (correctly, since `blp-` isn't Render's spelling) — but it's also missing from...
- `docs/ADR020-identifiers.md:65-70` — "Known deviations (deliberate, documented)" — four entries, none about Blueprint.
- `docs/ADR018-render-parity.md` — find the Blueprint row/section (added by `w2/m62`) and correct any claim that REST is fully Render-OpenAPI-compliant for Blueprint id routes; note the fix (this milestone) and that it's now backed by a real through-the-validator test (t002), not a bare-mux one.
- Also record, as **unverified** (per this hunt's own root-cause-review checklist — don't let an inferred claim ride in dressed as a checked one): whether `blueprintsync`'s `bsr-…` id (Render's own sync id shape is `exe-…` per `w2/m62/done/t001.md:18`) has the same class of gap. This hunt did not find a `pattern` constraint on any sync-id path parameter in the currently-registered intersected operations (the sync id doesn't appear as its own path parameter anywhere in the embedded spec — it's returned in response bodies, not addressed by path), so it's very likely a non-issue, but say that explicitly rather than silently assuming it.

## Steps

1. Add a fifth "Known deviation" entry to ADR020 for Blueprint (`blp-` vs Render's `exs-`), noting the compatibility override in `render_openapi.go` that makes REST accept it (link t001's change).
2. Note in the same entry (or a follow-up sentence) the `bsr-`/`exe-` sync-id observation as unverified-but-likely-fine, with the reasoning above, so it's a recorded fact instead of a re-litigated question next time someone audits this file.
3. Update ADR018's Blueprint row to reflect that REST-by-id now actually passes the OpenAPI gate, with the real test (t002) as evidence instead of the previous unverified claim.
4. Run `npx prettier@3.4.2 --write "docs/ADR020-identifiers.md" "docs/ADR018-render-parity.md"`.

## Files

- `docs/ADR020-identifiers.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] ADR020's "Known deviations" section includes Blueprint's id-shape divergence and the compatibility mechanism that resolves it for REST.
- [ ] ADR018's Blueprint row cites the real fix + real test, not the previously-unverified DoD language.
- [ ] The `bsr-`/`exe-` sync-id question is recorded as explicitly unverified, not silently resolved either way.

## Out of scope

- Actually verifying the sync-id question beyond what t001/t002's exhaustive spec grep already covered (no pattern constraint found) — record it, don't re-derive it from scratch.
