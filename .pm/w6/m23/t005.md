---
id: w6/m23/t005
title: DNS instructions for both records
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m23/t003]
---

## Objective

The per-domain `dnsRecord` surface (CNAME for subdomain, ALIAS `@` for apex — w5/m10) covers both halves of a pair so the user sees the two records they must create, matching the capture's presentation.

## Files

- `lego/backend/internal/apps/` (dnsRecord derivation)

## Acceptance criteria

- [ ] A paired add returns instructions for both hosts on all three surfaces.
