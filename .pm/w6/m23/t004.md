---
id: w6/m23/t004
title: Collision guard covers the sibling
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m23/t003]
---

## Objective

Close w7/m6's documented blind spot: registering a host reserves its sibling too — `www.foo.com` on app A blocks `foo.com` on app B with the same `409 conflict` (and the reserved-platform-host guard is unaffected). Cover the race the `domains.host` UNIQUE index closes for exact hosts.

## Files

- `lego/backend/internal/apps/domains.go` (`hostClaimedElsewhere`), store uniqueness treatment for the pair

## Acceptance criteria

- [ ] Cross-app sibling add is 409 on every surface; same-app sibling add remains the t003 idempotent path.
