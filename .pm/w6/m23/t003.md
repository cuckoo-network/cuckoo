---
id: w6/m23/t003
title: Auto-pair sibling on add/delete per the capture
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m23/t002]
---

## Objective

Implement exactly the captured behavior in the Add/Delete domain verbs (all three adapters inherit via the one core verb): sibling auto-add (or redirect flag) on add, and the captured delete semantics — never invented behavior.

## Context

- `App.spec.hosts[]` drives Ingress/cert (docs/ADR005-custom-domain.md); an auto-added sibling mints certs/DNS-instructions like any host.
- Idempotency: adding the second half of an existing pair must not duplicate or 409 against its own sibling.

## Files

- `lego/backend/internal/apps/{domains.go,service.go}`

## Acceptance criteria

- [ ] Add-apex, add-www, delete-half, re-add cases behave per the capture, asserted by tests.
