---
id: w6/m10/t005
title: "Docs: update ADR024's divergence note + refresh the ADR018 members row"
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m10/t004]
---

## Objective

The docs stop describing the divergence this milestone removed: ADR024's members design notes and ADR018's "Workspace members & roles" row record that the members surface now returns `userId`/`email` (evidence pointers to the m10 code/tests), keeping only the honest residuals — no `name` trait in the Kratos schema, and the t001 mutation-keying verdict if it flagged drift.

## Context

- `docs/ADR018-render-parity.md` — row "Workspace members & roles": "**Divergence (◐ shape):** members are keyed by identity subject, not `user{email,name}` (no per-member profile store yet)…" — rewrite with evidence, keep the row honest about residuals.
- `docs/ADR024-members.md` — the members ADR this milestone's shape change belongs in.
- Repo rule: `npx prettier@3.4.2 --write "**/*.md"` before finishing.

## Steps

1. Update the ADR018 row cell text + evidence pointers.
2. Update ADR024 (member object shape, enrichment path, degradation contract, residual divergences).
3. Run prettier.

## Files

- `docs/ADR018-render-parity.md`
- `docs/ADR024-members.md`

## Acceptance criteria

- [ ] ADR018's members row no longer lists the closed shape divergence as open; residuals (no `name` trait; mutation keying if drifted) are explicitly recorded.
- [ ] ADR024 describes the enriched shape + the omit-on-unset contract.

## Out of scope

- CLAUDE.md env-table changes (no new env vars — `BEX_KRATOS_ADMIN_URL` already documents the members lookup).
