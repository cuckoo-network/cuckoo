---
id: w6/m10/t007
title: "Simplify: /simplify over the code this milestone changed"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m10/t006]
---

## Objective

Standing closing task. Run `/simplify` over the m10 diff (members service/fragments, api wiring, team feature) for reuse / simplification / efficiency — behavior-preserving only. Prime candidate: the enrichment loop now exists in both `workspaces.ListMembers` and `members.List` — evaluate whether a shared helper (or one service delegating to the other's projection) removes the duplication without crossing feature-package boundaries.

## Context

- `lego/backend/internal/members/`, `lego/backend/internal/api/server.go`, `dashboard/src/features/team/`.

## Steps

1. `/simplify` scoped to the milestone's changed files.
2. Apply behavior-preserving findings; re-run backend + dashboard tests.

## Files

- Whatever the m10 diff touched.

## Acceptance criteria

- [ ] Findings applied or explicitly declined with a reason.
- [ ] All tests green after.

## Out of scope

- Behavior changes, new features.
