---
id: w6/m98/t006
title: "Test coverage"
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m98/t004]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real behavior and failure modes, not tautological or snapshot-everything tests.

## Context

- `lego/backend/internal/billing/stripe_test.go` has credit-related tests (`TestStripeBillingForIncludesCreditBalance`, etc.) but none combine a nonzero-usage fixture with an applied-credit-grant fixture — the exact production scenario that caused this bug went untested.
- `lego/backend/internal/usage/billing_test.go:268` (`TestBillingCrossSurfaceParity`) only checks REST/GraphQL/MCP agree with each other — it cannot catch a value that's consistently wrong across all three, which is exactly what happened here. Consider whether a value-correctness assertion (not just cross-surface agreement) belongs alongside it.
- `dashboard/src/features/usage/components/__tests__/charges-card.test.tsx:205-250` tests the categories-sum fallback and the "prefers Stripe's real amount" path, but not `invoicedUsd="0.00"` alongside nonzero categories.

## Steps

1. Add a Stripe billing test stubbing a preview-invoice response shaped like the real bug (credit grant zeroing the net figure while metered line items are nonzero) and assert the resolved gross amount is correct.
2. Add the equivalent test for `finalizedInvoices()` once a finalized-invoice-with-credit fixture is constructed.
3. Add a dashboard test for `ChargesCard`/`CreditBalanceCard` covering the exact production scenario (nonzero categories + credit-consumed backend value).
4. Consider whether `TestBillingCrossSurfaceParity` needs a companion value-correctness test.

## Files

- `lego/backend/internal/billing/stripe_test.go`
- `lego/backend/internal/usage/billing_test.go`
- `dashboard/src/features/usage/components/__tests__/charges-card.test.tsx`
- `dashboard/src/features/usage/components/__tests__/billing-summary-cards.test.tsx` (create if it doesn't exist)

## Acceptance criteria

- [ ] New backend test(s) fail on the pre-fix code and pass on the post-fix code for both `currentInvoice()` and `finalizedInvoices()`.
- [ ] New dashboard test(s) cover the exact production scenario observed in this hunt.

## Out of scope

- Broad test-suite refactors unrelated to this milestone's change.

## Outcome

Backend (`internal/billing/stripe_test.go`):

- `TestStripeBillingForReportsGrossChargeWhenCreditAbsorbsTheInvoice` — the production shape (subtotal 7478 / total 0 / `total_pretax_credit_amounts` 7478 of type `credit_balance_transaction`), asserted for **both** `currentInvoice()` and `finalizedInvoices()`, closing the "not yet observed live" gap on the second call site. Fails on the pre-fix code (`74.78` vs `0.00`).
- `TestStripeBillingForDoesNotReportCompDiscountAsCredit` — a Mode B comp lands in the same Stripe list and must not be counted as credit.
- Existing preview/invoice fixtures gained the `subtotal`/`amount_due` fields a real Stripe invoice always carries.

Backend (`internal/usage/billing_test.go`): `sampleBilling()` now carries a part-credited period, and `TestBillingCrossSurfaceParity` gained **value**-correctness assertions beside its agreement checks — three surfaces reporting the same wrong number is how the netted total went unnoticed.

Dashboard: `charges-card.test.tsx` covers gross-total-plus-due-line, no due line when nothing was credited, and the estimate fallback when Stripe has rated nothing; new `billing-summary-cards.test.tsx` covers `CreditBalanceCard` reporting Stripe's applied/due verbatim (full and partial coverage) and `InvoiceHistoryCard` showing what a credited invoice collected; `usage-page.test.tsx`'s credit tests now drive the backend figures instead of the removed client-side arithmetic; `use-usage.test.ts` covers the `"0.00"` default for a server that predates the fields.

