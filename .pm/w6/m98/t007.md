---
id: w6/m98/t007
title: "Closeout"
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m98/t005, w6/m98/t006]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet in `../README.md` against the live product (or the deployed state, whichever the deploy pipeline allows at close time).
2. Set every remaining task's `status: done`.
3. Move each `tNNN.md` to `w6/m98/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
5. Move the whole `w6/m98/` directory to `w6/done/m98/`.
6. Check `- [x]` for `m98` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD in `../README.md` verified live, not assumed.
- [ ] Milestone moved to `w6/done/m98/` with all statuses in sync.

## Blocked on deploy (2026-08-26)

t001–t006 are done and moved to `done/`; this task stays open because its first acceptance criterion is a **live** check and the fix is not deployed yet. What remains, in order:

1. `/ship` + deploy, then re-run the hunt's own in-page probe on `dashboard.bex.co/billing` as the QA user (workspace `tea-d98210cbbpdc73dcrkvg`): `fetch('https://api.bex.co/graphql', {credentials:'include'})` for the `Usage` query, and compare `billing.currentCost.amountUsd` / `creditsAppliedUsd` / `amountDueUsd` against `estimatedCost.totalUsd`.
2. That probe also settles which of t001's three candidate causes the live `$0.00` actually was — read `creditsAppliedUsd` (nonzero ⇒ cause (a)), then `amountUsd` (nonzero with `creditsAppliedUsd` zero ⇒ cause (b) comp; zero ⇒ cause (c), Stripe rated nothing, and the page now shows the rate-sheet estimate). Record the answer here.
3. Confirm the "Total month to date" headline equals the category-tree sum and that "Credits applied" is nonzero if and only if credit was actually consumed.
4. `finalizedInvoices()` still has no live finalized invoice to observe on this workspace; it is covered by the same code path and by a test fixture shaped like the bug, which is what the DoD allows in place of a live check — note that explicitly at close rather than claiming a live verification that did not happen.

