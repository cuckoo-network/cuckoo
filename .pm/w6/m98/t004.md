---
id: w6/m98/t004
title: "Render parity"
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m98/t003]
---

## Objective

Confirm the fixed billing-total behavior is consistent across every surface it touches, and that no REST/MCP surface was missed.

## Context

- This hunt's grep found `AmountUSD`/`amountUsd` has no REST or MCP wiring in `internal/usage/rest.go` or `internal/usage/mcp.go` — GraphQL-only. Re-confirm this is still true after t002/t003's changes (a schema addition could accidentally need REST/MCP mirroring if the field becomes part of a broader response those surfaces already expose).
- render.com has no direct equivalent (bex's Stripe-based usage billing + ADR071 credits are a bex-only extension) — record this explicitly rather than leaving the comparison blank.

## Steps

1. Re-run the grep for `amountUsd`/`AmountUSD`/`currentCost` across `internal/usage/rest.go` and `internal/usage/mcp.go` post-fix; confirm still no REST/MCP exposure, or add parity wiring if t002 changed the shape in a way that now needs it.
2. Confirm the dashboard is the only consumer and double-check no other GraphQL query (besides `Usage`) re-exposes `currentCost` under a different name.
3. Record the render.com comparison as n/a with a one-line justification in this task's notes.

## Files

- `lego/backend/internal/usage/rest.go`
- `lego/backend/internal/usage/mcp.go`
- `lego/backend/internal/usage/graphql.go`

## Acceptance criteria

- [ ] Confirmed (not assumed) that REST/MCP have no `currentCost`/invoice-amount surface needing a matching fix, or such a surface is added if one exists.
- [ ] render.com comparison recorded as n/a with justification.

## Out of scope

- New REST/MCP features unrelated to this fix.
