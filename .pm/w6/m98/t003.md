---
id: w6/m98/t003
title: "Dashboard: reconcile ChargesCard total + CreditBalanceCard applied/due math with the corrected backend shape"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m98/t002]
---

## Objective

Update the dashboard's billing components to consume t002's corrected backend fields so the "Total month to date" and "Credits applied → amount due" lines are both accurate and internally consistent with the category tree above them.

## Context

- `dashboard/src/features/usage/components/charges-card.tsx:189-219` — `total = invoiced ?? categories.reduce(...)`, where `invoiced = usd(invoicedUsd)` and `invoicedUsd` is wired from `billing?.currentCost?.amountUsd` (`usage-page.tsx:133`). Once `currentCost.amountUsd` is gross (per t002), this logic should already produce the correct total — verify, don't assume.
- `dashboard/src/features/usage/components/billing-summary-cards.tsx:59-88` (`CreditBalanceCard`) — computes `applied: min(available, cost)` / `due: max(0, cost - available)` from `currentCost.amountUsd` treated as pre-credit gross cost. This math is only correct once `currentCost.amountUsd` genuinely is gross; re-verify the arithmetic against t002's actual schema (especially if t002 added a distinct "credit consumed" field instead of leaving this derived client-side).
- `dashboard/src/features/usage/hooks/use-usage.ts:88-97` — the `Billing`/`BillingAmount` TypeScript interfaces; update if t002 changed the GraphQL schema shape.
- `dashboard/src/features/usage/api/usage.graphql` — regenerate codegen (`dashboard/CLAUDE.md`'s offline codegen recipe) if the schema changed.

## Steps

1. If t002 kept `currentCost.amountUsd` as the only field (now gross) and left credit reporting entirely in the existing separate `credits` block: verify `ChargesCard`'s total is now correct, and decide whether `CreditBalanceCard`'s client-side `applied = min(available, cost)` computation is still the right math (it should now show a real nonzero "applied" figure whenever credit was consumed).
2. If t002 added a new explicit "credit consumed this period" field: wire it directly into `CreditBalanceCard` instead of re-deriving it from `cost - due`, and update `use-usage.ts`'s `BillingAmount`/`Billing` interfaces + `usage.graphql` accordingly.
3. Regenerate/reconcile `dashboard/src/graphql/definitions.ts` per the offline codegen recipe (`dashboard/CLAUDE.md` § Offline codegen) — splice only the touched query, don't wholesale-overwrite.
4. Update `charges-card.tsx`'s and `billing-summary-cards.tsx`'s comments if the "invoiced amount is Stripe's rating, shown verbatim" framing needs adjusting now that gross/net is explicit.

## Files

- `dashboard/src/features/usage/components/charges-card.tsx`
- `dashboard/src/features/usage/components/billing-summary-cards.tsx`
- `dashboard/src/features/usage/hooks/use-usage.ts`
- `dashboard/src/features/usage/api/usage.graphql`
- `dashboard/src/graphql/definitions.ts` (codegen output, spliced not overwritten)

## Acceptance criteria

- [ ] Live on `dashboard.bex.co/billing` (once deployed) for a workspace with an active credit grant and nonzero usage: "Total month to date" matches the category tree sum.
- [ ] "Credits applied → amount due" shows a nonzero "applied" figure whenever credit was actually consumed this period.
- [ ] `charges-card.test.tsx` and `billing-summary-cards` tests (add if missing) cover the exact production scenario: nonzero categories + nonzero credit grant + the corrected backend total.

## Out of scope

- Backend field changes (t002).
