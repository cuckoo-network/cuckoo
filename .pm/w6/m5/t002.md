---
id: w6/m5/t002
title: Reconcile bex's danger-zone/rename UX against the capture
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m5/t001]
---

## Objective

Compare bex's shipped workspace settings/delete UX (`w6/m3`) against t001's live Render capture, and fix any drift — or explicitly document why bex deliberately diverges.

## Context

- `w6/done/m3/README.md`'s own admission: *"delete/rename confirmation UX (exact copy, resource-count enumeration) was never live-captured ... this milestone's danger-zone wording is an original, reasonable design rather than a verified clone."*
- The components to check are `w6/m3`'s settings page (rename, plan display) and danger-zone delete flow (type-to-confirm guard) in `dashboard/src/`.
- Known deliberate drift already documented (keep, don't "fix"): workspace names are DNS-label constrained vs. Render's freeform names (`w6/done/m3/README.md`'s Source + Goal linkage section).

## Steps

1. Diff t001's captured confirmation wording, resource-consequence copy, and any transfer-ownership affordance against bex's current implementation.
2. Fix genuine drift (copy, guard mechanism, resource enumeration) where it doesn't conflict with a documented deliberate deviation.
3. For anything left different on purpose, add/update the deliberate-drift note in `w6/m3`'s (now `done/`) README or a follow-up doc, so it doesn't get re-flagged as an unverified gap again.

## Files

- `dashboard/src/routes/` (workspace settings/delete UI, exact path per `w6/m3`'s shipped code)
- `.pm/w6/done/m3/README.md` (drift notes, if any)

## Acceptance criteria

- [ ] Every clause in t001's capture is either matched in bex's UI or has a recorded deliberate-drift reason
- [ ] No unexplained copy/flow mismatch remains between bex and Render's delete/rename UX

## Out of scope

- Live-cluster acceptance testing of the reconciled UI (t003).
