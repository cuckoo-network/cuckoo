---
id: w6/m5/t004
title: Regenerate codegen against live schema, diff hand-authored types
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m5/t003]
---

## Objective

Re-run `yarn codegen` against the live bex-api GraphQL schema (same cluster as t003) and diff it against the hand-authored `Workspace`/`createWorkspace`/`renameWorkspace`/`deleteWorkspace` additions in `dashboard/src/graphql/definitions.ts`, fixing any drift.

## Context

- `w6/done/m3/README.md`'s Follow-ups: *"Re-run `yarn codegen` against a live bex-api once one is reachable, and diff against the hand-authored ... additions in `src/graphql/definitions.ts` (no live schema/session token was available this session to run codegen for real)."*
- The mock cluster from t003 is already up and reachable — reuse the same session rather than standing up a second one.

## Steps

1. With the t003 mock cluster still running and a valid session token, run `yarn codegen` (or the dashboard's equivalent script) against the live bex-api GraphQL endpoint.
2. Diff the regenerated types against the hand-authored `Workspace`/`createWorkspace`/`renameWorkspace`/`deleteWorkspace` entries in `dashboard/src/graphql/definitions.ts`.
3. Fix any field/type mismatch found; if none, note that the hand-authored definitions were confirmed accurate.

## Files

- `dashboard/src/graphql/definitions.ts`
- Codegen output (wherever the dashboard's generated GraphQL types normally land)

## Acceptance criteria

- [ ] `yarn codegen` run successfully against a live bex-api schema
- [ ] Hand-authored workspace GraphQL definitions confirmed accurate or corrected to match
- [ ] Dashboard build/typecheck stays green after any fix

## Out of scope

- Codegen for non-workspace GraphQL surfaces (out of this milestone's scope).
