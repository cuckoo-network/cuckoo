---
id: w6/m5/t003
title: Live-cluster acceptance rerun
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m5/t002]
---

## Objective

Re-run `w6/m3/t005`'s acceptance pass for real: drive the full workspace lifecycle in a real browser against a real cluster (real Postgres, real OpenFGA, real Kratos), not the offline dev stub it was originally verified against.

## Context

- `w6/done/m3/README.md`: *"Verified against: ... `scripts/local-bex.mjs` ... rather than a live `scripts/mock-cluster.sh` cluster — none was available in this session. ... Residual gap: a real-cluster rerun ... has not happened."*
- `w6/m1`'s own real-infra e2e test (`workspaces_e2e_test.go`) already covers the backend half — this task closes the frontend-wiring half of the risk.
- `scripts/mock-cluster.sh` stands up the local CAPD mock cluster + operator, per repo `CLAUDE.md`.

## Steps

1. Stand up `scripts/mock-cluster.sh` (or reuse an already-running one).
2. In a real browser (Playwright), drive: open switcher → **+ New Workspace** → name + Hobby plan → create → land in new workspace → every page (services, databases, env vars, metrics) scoped to it → rename via settings → propagates to switcher → delete (type-to-confirm) → workspace and its resources gone → lands in remaining workspace.
3. Also verify the 6th-Hobby-workspace limit error shows inline, per `w6/m3`'s original DoD.
4. Record results (pass/fail per clause) in this milestone's README or a completion note.

## Files

- None (verification task; fixes go back into `dashboard/src/` if something fails).

## Acceptance criteria

- [ ] Full create → switch → rename → delete flow verified live against real Postgres + OpenFGA + Kratos
- [ ] 6th-Hobby-workspace limit error verified inline
- [ ] Any discrepancy from the offline-stub pass is fixed or filed as a follow-up, not silently ignored

## Out of scope

- GraphQL codegen verification (t004) — this task is UI/flow behavior only.
