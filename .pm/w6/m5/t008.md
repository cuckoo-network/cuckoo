---
id: w6/m5/t008
title: Test coverage for reconciled UX
worker: worker6
status: todo
estimate: 25m
depends_on: [w6/m5/t006]
---

## Objective

Add meaningful tests for any UI behavior changed by t002's reconciliation fixes — not trivial/tautological tests. The live-cluster/codegen verification itself (t003/t004) is a one-time acceptance pass, not something to encode as an automated test.

## Context

- Only add tests where t002 actually changed behavior (copy-only fixes don't need new test coverage; guard-mechanism or flow changes do).

## Steps

1. Review what t002 changed; for any behavioral (not copy-only) change, add or update a test asserting the new behavior.
2. Confirm existing `w6/m3` tests still pass unmodified where behavior didn't change.

## Files

- Test files colocated with whatever `dashboard/src/` components t002 touched.

## Acceptance criteria

- [ ] Behavioral changes from t002 have test coverage
- [ ] Dashboard test suite green

## Out of scope

- Re-testing unchanged `w6/m3` behavior.
