---
id: w6/m46/t008
title: Test coverage for the fixed behaviors
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m46/t006]
---

## Objective

Add meaningful tests for the behaviors this milestone shipped, asserting real behavior and failure modes — not trivial or tautological coverage. Most of this should already exist from `t001`-`t005`'s own acceptance criteria; this task closes any gaps and confirms every regression test is proven red pre-fix / green post-fix.

## Context

- `t001`, `t002`, `t004` each specify a proven-red-pre-fix regression test as part of their own acceptance criteria — verify those exist and actually fail against a pre-fix checkout (e.g. `git stash` the fix, run the test, confirm red, restore).
- `t003` specifies one test per create/update entrypoint — verify the count matches what was enumerated.
- `t005` specifies a client-side test with no local mutation involved — verify it doesn't accidentally exercise the button-triggered path instead.

## Steps

1. Enumerate every test added across `t001`-`t005`.
2. For each, confirm it fails against the pre-fix code and passes post-fix.
3. Fill any gap found — e.g. if `t004`'s fix accidentally also covers `t001`'s scenario or vice versa, make sure both have their OWN independent regression test, not one test doing double duty by coincidence.

## Files

- Test files added by `t001`-`t005`.

## Acceptance criteria

- [ ] Every regression test from `t001`-`t005` is confirmed red-pre-fix / green-post-fix.
- [ ] No behavior this milestone claims to fix is covered by fewer than one dedicated test.

## Out of scope

- Adding coverage for pre-existing, unrelated code this milestone didn't touch.
