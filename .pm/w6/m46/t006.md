---
id: w6/m46/t006
title: Render parity across REST/GraphQL/MCP/UI
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m46/t001, w6/m46/t002, w6/m46/t003, w6/m46/t004, w6/m46/t005]
---

## Objective

Check that this milestone's fixes are consistent across every surface bex-api exposes — REST, GraphQL, MCP (`docs/ADR006-bex-api.md`) — and the `dashboard/` UI (`dashboard/CLAUDE.md`). Compare against the equivalent render.com behavior and flag any drift as follow-up work rather than silently diverging.

## Context

- `Expose`/host semantics (`t001`-`t003`) are read identically by `AppView.URL` (`service.go:796`, `a.Status.URL`) regardless of which surface created the App — confirm REST's service response, GraphQL's `server.url`, and MCP's equivalent field all agree a private_service never carries a public URL, post-fix.
- Deploy-status vocabulary and the Service header (`t004`-`t005`) are the same surfaces `w6/m45` t003/t005 already touched — confirm this milestone's fix doesn't regress that work and that REST/GraphQL/MCP deploy-status reads (`DeployCanceled` and friends) stay in the existing eleven-value vocabulary (`w2/done/018.md`).

## Steps

1. For `t001`-`t003`: create a `private_service` through REST, GraphQL, and MCP; confirm each surface's read response never shows a public URL/host, post-fix.
2. For `t004`-`t005`: confirm the deploy-status vocabulary is unchanged (no new bex-only status value introduced) and that REST/GraphQL/MCP all report the same terminal status for a given deploy.
3. Compare against render.com's documented behavior for private-service network isolation and first-deploy status reporting where a public reference exists; record any deliberate bex divergence in `docs/ADR018-render-parity.md` rather than leaving it undocumented.

## Files

- `docs/ADR018-render-parity.md` (update if any divergence is newly documented)

## Acceptance criteria

- [ ] REST, GraphQL, and MCP all agree a `private_service` never exposes a public URL after `t001`-`t003`.
- [ ] REST, GraphQL, and MCP all agree on a service's deploy/phase status after `t004`-`t005`.
- [ ] Any intentional divergence from render.com found in the process is recorded in `docs/ADR018-render-parity.md`, not left implicit.

## Out of scope

- Re-implementing any of `t001`-`t005`'s fixes — this task only verifies cross-surface consistency.
