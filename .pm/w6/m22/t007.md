---
id: w6/m22/t007
title: Test coverage - real kpack dispatch
worker: worker6
status: todo
estimate: 1.5h
depends_on: [w6/m22/t005]
---

## Objective

Replace `build_test.go`'s "buildpack rejected" assertion with real dispatch coverage, and add tests for the new build-time env passthrough.

## Context

- `build_test.go:417-418` currently asserts the error contains `"buildpack"` — this assertion must be replaced, not just left alongside the new code path.

## Steps

1. Remove/replace the old "buildpack rejected" test.
2. Add coverage (fake kpack client or envtest, matching the existing BuildKit test harness style) asserting: `Image` CR created with correct repo/branch/rootDir, build-time env vars threaded through, success/failure status propagation.
3. Verify `TestBuild...` suite still passes for the unaffected Dockerfile/auto paths.

## Files

- `lego/operator/internal/build/build_test.go`

## Acceptance criteria

- [ ] No stale "buildpack unsupported" assertions remain.
- [ ] New tests assert real dispatch behavior and failure modes, not just CR existence.
- [ ] `make test` (operator) passes.

## Out of scope

- End-to-end cluster verification (covered by the milestone's overall DoD, not a unit test).
