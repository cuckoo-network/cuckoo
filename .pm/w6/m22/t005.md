---
id: w6/m22/t005
title: Render parity check - spec.builder buildpack across surfaces
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m22/t002, w6/m22/t003]
---

## Objective

Verify that `spec.builder: buildpack` is creatable/visible consistently across REST, GraphQL, MCP, and the dashboard UI, now that it actually works instead of erroring.

## Steps

1. Confirm the `builder` field's `buildpack` value is accepted and surfaced identically across all three API surfaces.
2. Check whether the dashboard's create-service wizard needs to stop hiding/disabling the buildpack option (if it currently does) — file a follow-up note if UI work is out of scope for this milestone.
3. Compare against render.com's runtime auto-detect UX for drift.

## Files

- `lego/backend/internal/apps/*.go`
- `dashboard/` (create-service wizard, if touched)

## Acceptance criteria

- [ ] `spec.builder: buildpack` works identically via REST/GraphQL/MCP.
- [ ] Dashboard gap, if any, filed as a follow-up note.

## Out of scope

- Building new dashboard UI beyond removing an artificial block, if one exists.
