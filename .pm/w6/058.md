# Live-verification status of the `d247d210` inbox fixes — seven notes still awaiting their production re-probe

Why: the board's own closure bar is "fixed, deployed, **and** re-probed live", so these seven are correctly still open — but nothing records which probe each one needs, and every QA hunt re-derives that from scratch (this note exists because the 29th run nearly re-investigated `035` before finding its fix in the git log).

## The situation

`d247d210` ("fix: triage w6 inbox — cron concurrency, project moves, build_started, publish errors (w6/027-041)") states **"Nine verified w6 inbox notes, each still reproducing at HEAD"** and describes a fix for each of `027 028 029 030 031 033 035 036 037 038 039 041`, with gates run (dashboard typecheck + lint + 2629 tests; backend `go test ./...`; operator `make test` + `make lint`).

Eleven have since been closed against the board's bar — `029`/`030`/`041` (2026-08-26, "confirmed live on production (19th `/qa-find-bugs` run, ancestor-of-deployed-commit + live re-probe for `030`)"), `038` (2026-08-25), `035` (2026-08-27, 29th run — see `done/035.md`), `031` (2026-08-27, 32nd run — both halves, including the decisive failed-create case; see `done/031.md`), and `028` (2026-08-27, 33rd run — all three pluralization branches; its second bullet was never a defect by its own text; see `done/028.md`).

**One remains open:** `039`. (`031` closed 2026-08-27 by the 32nd run, `028` by the 33rd, `027` by the 34th, `037` by the 35th, `033` by the 36th, `036` by the 37th — see the matching files under `done/`.) That is not board rot — it is the correct state until each is re-probed. What is missing is a record of *which* probe closes each one.

## What each still needs

| note | claimed fix in `d247d210` | live probe that would close it |
| --- | --- | --- |
| `039` | `reconcileCronJob` pauses the CronJob's schedule for a manual run's duration | create a cron job on a tight schedule, hit Trigger Run, and confirm no second concurrent execution starts across at least one schedule tick |

## Notes

- Only `039` remains, and it is the one that cannot be shortcut: it needs a cron tick to land across a manual Trigger Run, so it wants a service on a tight schedule and patience rather than cleverness.
- Worth carrying forward from `036`'s closure: when a fix **removes** the failing step rather than handling it, the probe in this table can become the wrong test. `036`'s row said "use route interception to fail the second mutation" — but the fix deleted the second mutation, so the right verification was to count the operations and then prove the server-side premise that makes one call sufficient. Re-read what the fix actually did before running the probe as written.
- `036` needs route interception to fail the second call, the same technique the original `036` filing used; it is the most involved of the seven.
- Nothing here asserts any of the seven is still broken. Each is "fix claimed and gated in CI, production re-probe outstanding" — the same state `035` was in until this run.
