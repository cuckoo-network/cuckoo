# Live-verification status of the `d247d210` inbox fixes — seven notes still awaiting their production re-probe

Why: the board's own closure bar is "fixed, deployed, **and** re-probed live", so these seven are correctly still open — but nothing records which probe each one needs, and every QA hunt re-derives that from scratch (this note exists because the 29th run nearly re-investigated `035` before finding its fix in the git log).

## The situation

`d247d210` ("fix: triage w6 inbox — cron concurrency, project moves, build_started, publish errors (w6/027-041)") states **"Nine verified w6 inbox notes, each still reproducing at HEAD"** and describes a fix for each of `027 028 029 030 031 033 035 036 037 038 039 041`, with gates run (dashboard typecheck + lint + 2629 tests; backend `go test ./...`; operator `make test` + `make lint`).

Six have since been closed against the board's bar — `029`/`030`/`041` (2026-08-26, "confirmed live on production (19th `/qa-find-bugs` run, ancestor-of-deployed-commit + live re-probe for `030`)"), `038` (2026-08-25), `035` (2026-08-27, 29th run — see `done/035.md`), and `031` (2026-08-27, 32nd run — both halves, including the decisive failed-create case; see `done/031.md`).

**Six remain open:** `027 028 033 036 037 039`. (`031` closed 2026-08-27 by the 32nd run — see `done/031.md`.) That is not board rot — it is the correct state until each is re-probed. What is missing is a record of *which* probe closes each one.

## What each still needs

| note | claimed fix in `d247d210` | live probe that would close it |
| --- | --- | --- |
| `027` | health-check section gate converted off an open-coded `servesHttp`, keeping render-while-loading | open a web service's Settings → Health Checks and a non-HTTP service's (worker/cron); confirm the section shows/hides correctly and does not flash during load |
| `028` | `"1 requests"` singular fixed on the Metrics page; unbatched query count addressed | load a service's Metrics page over a sub-hour range with exactly 1 request in it; confirm the copy reads "1 request" and count the Metrics GraphQL calls in the Network panel |
| `033` | project-delete now discloses that Environments cascade | open a Project's delete confirmation and read the copy; confirm it names the Environment cascade instead of promising "nothing is deleted" |
| `036` | "Move to project" collapsed to one all-or-nothing mutation | move a resource between two `qa-` projects with the second call intercepted to fail; confirm the resource stays in its original project rather than landing in neither |
| `037` | `serverRefusalReason` surfaces the backend's words across all 14 `useFieldMutation` callers | save a Health Check Path without its leading slash; confirm the toast quotes the server's reason, not the generic "Couldn't update…" |
| `039` | `reconcileCronJob` pauses the CronJob's schedule for a manual run's duration | create a cron job on a tight schedule, hit Trigger Run, and confirm no second concurrent execution starts across at least one schedule tick |

## Notes

- `027` and `028` are the two whose fixes are dashboard-only and therefore cheapest to verify — worth doing first in whichever hunt picks this up.
- `036` needs route interception to fail the second call, the same technique the original `036` filing used; it is the most involved of the seven.
- Nothing here asserts any of the seven is still broken. Each is "fix claimed and gated in CI, production re-probe outstanding" — the same state `035` was in until this run.
