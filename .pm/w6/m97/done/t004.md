---
id: w6/m97/t004
title: Simplify
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m97/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Scope: `lego/operator/internal/controller/app_controller.go` (the guard change) and any test files touched in t001/t002.
- In particular check whether the two deterministic name-builder functions (clone/pull) should live in one shared place instead of being duplicated across `lego/backend` and `lego/operator` (they currently can't share a package directly per the module boundary — see if `lego/types/v1alpha1` is the right shared home, or if duplication with a cross-referencing comment is the pragmatic answer given the module split documented in `lego/CLAUDE.md`).

## Steps

1. `/simplify` over the diff.
2. Confirm no behavior change from the simplification pass alone (tests from t002 still green).

## Files

- Whatever t001/t002 touched.

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly declined with reason.
- [ ] All tests still green.
