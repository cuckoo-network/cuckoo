---
id: w6/m97/t005
title: Test coverage
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m97/t004]
---

## Objective

Add any meaningful test coverage gaps this milestone's own investigation surfaced beyond t002's direct regression tests — real behavior and failure modes, not tautological coverage padding.

## Context

- t002 already covers the direct self-reference/cross-reference cases. This task is for anything found while implementing t001/t002 that deserves its own assertion — e.g. a table test enumerating all six other protected-name fields (`envFromSecret`, `envFromSecrets`, `filesFromSecrets`, per-var `secretKeyRef`, both pending annotations) still correctly reject the App's own clone-secret name if referenced through *those* fields instead of `spec.CloneSecret` (the running container must never mount its own transient clone token, even though `spec.CloneSecret` itself is now allowlisted for the operator's internal build-time use).

## Steps

1. Add the cross-field table test described above if not already covered by t002.
2. Review whether `lego/backend/internal/deploys/service_test.go` and `lego/backend/internal/apps/service_test.go` (the CreateApp/redeploy/trigger paths) have any existing test that mocks a GitHub-connected repo and asserts the resulting App CR's `spec.CloneSecret` — extend if the assertion stops at "field is set" without ever exercising the operator-side guard end-to-end (even via a lightweight fake, if one exists in the test harness).

## Files

- `lego/operator/internal/controller/protected_secret_test.go`
- `lego/backend/internal/apps/service_test.go`, `lego/backend/internal/deploys/service_test.go` as applicable

## Acceptance criteria

- [ ] Cross-field self-name-as-mount-target case is asserted rejected.
- [ ] No trivial/snapshot-everything tests added — each new test asserts a real behavior or failure mode.
