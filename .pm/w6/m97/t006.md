---
id: w6/m97/t006
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m97/t005]
---

## Objective

Close the milestone once its Definition of done is actually, observably met — not merely once the code is written.

## Steps

1. Confirm every task t001-t005 is done and its acceptance criteria hold.
2. Live-verify the milestone README's Definition of done against `dashboard.bex.co` once the fix is deployed: create (or redeploy) a service from a GitHub-connection-covered repo and confirm it reaches `build_in_progress`/`live` instead of an instant `update_failed` naming its own clone secret.
3. Run `/pm done w6/m97/t006` to perform the actual close: mark every task `status: done`, move each `tNNN.md` to `w6/m97/done/`, mark every row `— DONE` and set `**Status:** done` in the milestone README, move the whole `w6/m97/` directory to `w6/done/m97/`, and check `- [x]` in the workstream README.

## Acceptance criteria

- [ ] Definition of done live-verified against production, not just code review.
- [ ] Milestone moved to `w6/done/m97/` with all status markers in sync.
