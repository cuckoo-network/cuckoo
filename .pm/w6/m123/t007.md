---
id: w6/m123/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m123/t005, w6/m123/t006]
---

## Objective

Close the milestone once every task is done **and** the DoD holds against a real failed build on production.

## Context

- The DoD is a live fixture CI cannot run: a free `web_service` on the public repo `github.com/render-examples/express-hello-world` with `dockerfilePath: ./NoSuchDockerfile`. Create it, let it fail, read all three surfaces, then delete it (the originating run's fixture deleted cleanly — `deleteService` → `true`, `GET` → 404).
- Several things were never observed live and must not be closed as if they were: the failure **email**, and the `FaultInfra` / `FaultTimeout` message paths. Either exercise them or record plainly that they remain code-verified only.

## Steps

1. Recreate the fixture; confirm the failure reason names the missing Dockerfile with no internal markers, the duration is real, and the log carries buildkit's error.
2. Confirm the tenant/infra class distinction still differs (`w7/m82/t003`).
3. Record the verification status of the email and the infra/timeout paths.
4. Delete the fixture and confirm 404.
5. Set remaining tasks `status: done`, move each to `w6/m123/done/`, mark rows `— **DONE**`, set `**Status:** done`.
6. Move `w6/m123/` to `w6/done/m123/` (no stub) and check `- [x]` in `.pm/w6/README.md`; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m123/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Live fixture re-run on production with its captures recorded.
- [ ] The email and infra/timeout verification status is stated plainly rather than implied.
- [ ] Fixture deleted (404 confirmed).
- [ ] Milestone moved to `w6/done/m123/` with no tombstone; status consistent in all three places.

## Out of scope

- The clone-auth question on `qa-20260826-webhook-renamed` — not this milestone's, and `w6/m97` is the likelier owner.
