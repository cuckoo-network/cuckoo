---
id: w6/m94/t006
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m94/t005]
---

## Objective

Close the milestone once its definition of done is actually met (not merely once the code is written): set every task's `status: done`, move each `tNNN.md` to `w6/m94/done/`, mark every row `— **DONE**` and `**Status:** done` in this `README.md`, move the whole `w6/m94/` directory to `w6/done/m94/`, and check `- [x]` in `w6/README.md`.

## Acceptance criteria

- [ ] The milestone's Definition of done (repeated live trials of the hibernate-then-immediately-request repro no longer produce Traefik's raw error, or the residual rate is explicitly documented as accepted) holds, not just that code was written.
- [ ] All prior tasks moved to `done/`, statuses synced across task frontmatter, milestone README, and workstream README.
- [ ] `w6/m94/` moved to `w6/done/m94/`.

## Note

Per the `w6/m11` → `w6/m13` / `w6/m50-52` → `w6/040` precedent: if the production deploy pipeline (`deploy.yml`, tracked in `w6/040.md`) is still failing when the code is complete and gated green, close the milestone anyway and carry the live-verification residual into `w6/040.md` rather than holding this open on an external infrastructure blocker. Check `w6/040.md`'s current status before closing.
