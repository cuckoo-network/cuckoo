---
id: w6/m93/t005
title: Test coverage
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m93/t004]
---

## Objective

Add meaningful regression tests for both fixes — must assert real behavior and failure modes, not trivial/tautological/snapshot-everything tests.

## Context

- Postgres fix: the control case is Key Value's already-correct single-source pattern — a test should assert Postgres now matches that shape, and that the internal dial-out path (`query.go`/`insights.go`) is unaffected.
- Log fix: the critical control case is that the ms-precision `logLineKey` dedupe from `w9/053` still works as defense in depth even when the resume cursor is absent/unsupported by a caller — don't let this fix remove test coverage for that.

## Steps

1. Postgres: a test proving `InternalConnectionString`'s host equals `PSQLCommand`'s host, red pre-fix (asserting today's mismatched value) / green post-fix.
2. Postgres (control case): a test proving `query.go`/`insights.go`'s internal dial-out path is unchanged.
3. Logs: a test proving a resubscribe carrying `Last-Event-ID` does not replay lines at or before that cursor.
4. Logs (control case): a test proving a resubscribe with no `Last-Event-ID` still works (falls back to full replay + client-side ms-dedupe, matching today's behavior for old/non-resuming clients).
5. Run the full suite for both modules (`cd lego/backend && go test ./...`, `dashboard/yarn test`) to confirm nothing else broke.

## Files

- `lego/backend/internal/postgres/service_test.go`
- `lego/backend/internal/logs/*_test.go`
- `dashboard/src/features/logs/hooks/__tests__/` (or wherever the existing suite lives)

## Acceptance criteria

- [ ] A test proving the Postgres host-mismatch fix, red pre-fix / green post-fix.
- [ ] A test proving the internal dial-out control case is unaffected.
- [ ] A test proving the log resume-cursor eliminates a boundary-second replay duplicate.
- [ ] A test proving the no-`Last-Event-ID` fallback path still works.
- [ ] Full test suites pass for every touched module.

## Out of scope

- Broad new test infrastructure — reuse existing test patterns in each touched file.
