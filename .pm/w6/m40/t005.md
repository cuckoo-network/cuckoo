---
id: w6/m40/t005
title: Simplify the code this milestone changed
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m40/t004]
---

## Objective

Run `/simplify` over the guard script and any CI wiring this milestone added — behavior-preserving only.

## Context

- The only real code in this milestone is `t004`'s guard plus its CI wiring; the rest is documentation moves and text.
- Watch specifically for: logic duplicated from the sibling validators in `scripts/` that should be shared or, conversely, a shared helper that is not worth the coupling — the existing validators are deliberately standalone, so match that convention rather than fighting it.

## Steps

1. Run `/simplify` over the milestone's diff, scoped to the guard and CI wiring.
2. Apply the clearly-correct findings; record anything declined and why.

## Files

- the guard script and CI wiring from `t004`

## Acceptance criteria

- [ ] `/simplify` run; findings applied or explicitly declined with a reason.
- [ ] Guard behavior unchanged — `t006`'s self-test stays green.

## Out of scope

- Bug hunting (`/code-review`) and any behavior change.
