---
id: w6/m40/t001
title: Settle the renumbering for the three collided ADRs
worker: worker6
status: todo
estimate: 25m
depends_on: []
---

## Objective

Decide which of each colliding pair keeps its number and which moves, assign the movers unused numbers, and record the old→new mapping in each moved file's header so citations written before this milestone stay traceable.

## Context

- The three collisions, measured at HEAD 2026-08-17:
  - ADR040 — `ADR040-billing-metronome.md` vs `ADR040-openchoreo-evaluation.md`
  - ADR049 — `ADR049-render-yaml-parity.md` vs `ADR049-tenant-billing-credits.md`
  - ADR060 — `ADR060-build-worker-reliability-and-performance.md` vs `ADR060-security-review-round7.md`
- 71 ADRs exist; the next free numbers are above the current maximum. Confirm the actual free set before assigning — do not assume contiguity.
- **Which one moves matters.** Prefer keeping the number on the more heavily cited file, so `t002` rewrites fewer references and fewer already-published citations rot. Count citations before deciding (`grep -rn "ADR040" --include=*.md --include=*.go .`).
- Precedent for the header note: `ADR060-security-review-round7.md` and `ADR063`/`ADR065` already carry "Renamed from ADRnnn to resolve the number collision with …" lines. Follow that exact style.
- Anti-pattern to avoid: the previous renames each *created* the next collision by picking a number that was already spoken for elsewhere. Verify the target number is free against the whole `docs/` listing, not against memory.

## Steps

1. Enumerate every ADR number in use and derive the genuinely free set.
2. For each collided pair, count citations of the number across `docs/`, `.pm/`, `CLAUDE.md`, `scripts/`, and Go comments; the more-cited file keeps the number.
3. Assign each mover a verified-free number and `git mv` the file.
4. Add the old→new note to each moved file's header, matching the existing precedent's wording.
5. Record the three mappings in this task's completion note so `t002` and `t003` work from one authoritative list.

## Files

- `docs/ADR040-openchoreo-evaluation.md` / `docs/ADR040-billing-metronome.md`
- `docs/ADR049-render-yaml-parity.md` / `docs/ADR049-tenant-billing-credits.md`
- `docs/ADR060-build-worker-reliability-and-performance.md` / `docs/ADR060-security-review-round7.md`

## Acceptance criteria

- [ ] `ls docs/ADR*.md | sed 's/.*ADR\([0-9]*\).*/\1/' | sort | uniq -d` returns nothing.
- [ ] Each assigned number was verified free against the full listing before use.
- [ ] Each moved file's header records its old→new number in the established style.
- [ ] The three old→new mappings are written down for `t002`/`t003` to consume.
- [ ] Files were moved with `git mv` so history follows.

## Out of scope

- Rewriting references (`t002`) and index entries (`t003`).
- Editing any ADR's content, status, or decisions.
