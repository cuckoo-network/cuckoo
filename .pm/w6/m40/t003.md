---
id: w6/m40/t003
title: Index the three unindexed ADRs in CLAUDE.md
worker: worker6
status: todo
estimate: 25m
depends_on: [w6/m40/t001]
---

## Objective

Add the three ADRs currently absent from `CLAUDE.md`'s docs index so every architectural decision is discoverable from the one index that is loaded into context each session.

## Context

- Missing at HEAD: `ADR030-pricing.md`, `ADR033-workflows.md`, `ADR040-openchoreo-evaluation.md` (the last will carry its `t001` number).
- The index lives in `CLAUDE.md` under `## Docs index`. Its established entry style is a one-line bullet: a relative link, an em dash, then a dense one-line summary of what the document decides — often leading with the ADR's status when it is `Proposed`/`Accepted`. Match the surrounding entries; they are terse and specific, not generic.
- Read each of the three ADRs before writing its line — the summary must state what the document actually decides, and a wrong summary is worse than a missing one.
- `ADR040-openchoreo-evaluation.md` is adjacent to `docs/ADR039-operator-audit-and-platform-reuse.md` and to `.pm/DO_NOT_DO.md`'s "Do not materialize or start an OpenChoreo/Korifi substrate spike now" item — its summary should not read as roadmap authorization.

## Steps

1. Read each of the three ADRs, including its status line.
2. Write one index bullet per ADR in the established style, placed in the index's existing ordering.
3. Confirm each new link resolves.

## Files

- `CLAUDE.md`

## Acceptance criteria

- [ ] All three ADRs appear in `CLAUDE.md`'s docs index with resolving links.
- [ ] Each summary reflects what that ADR actually decides, written after reading it.
- [ ] The OpenChoreo entry does not read as authorization to start the spike `DO_NOT_DO.md` forbids.
- [ ] Every `docs/ADR*.md` is now indexed (the `t004` guard will assert this mechanically).

## Out of scope

- Editing ADR content or status.
- The guard itself (`t004`).
