---
id: w6/m16/t002
title: "Store + migration: projects, environments, service_environment"
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m16/t001]
---

## Objective

Implement t001's design as a new control-plane migration and store layer: `projects`, `environments`, `service_environment` tables, tenant-scoped like every other control-plane table.

## Context

- `lego/backend/internal/store/` — migration convention (numbered `NNNN_description.sql`, mirror the most recent migration's style).
- Follow `docs/ADR020-identifiers.md`'s id convention — mint `Project`/`Environment` ids through `lego/backend/internal/id` with a new `id.Kind` registered.

## Steps

1. Write the migration for `projects`, `environments`, `service_environment`.
2. Add store methods (Create/Rename/Delete/List for both resources; Assign/Unassign for the join).
3. Register the new id kinds in `lego/backend/internal/id`.

## Files

- `lego/backend/internal/store/migrations/` (new migration file)
- `lego/backend/internal/store/` (store methods)
- `lego/backend/internal/id/` (new id kinds)

## Acceptance criteria

- [ ] Migration applies cleanly; store methods have unit tests against a real Postgres (per repo convention).

## Out of scope

- The verb layer (Core-level authorization etc.) — t003.
