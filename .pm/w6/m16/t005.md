---
id: w6/m16/t005
title: "MCP: project/environment read/list tools"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m16/t003]
---

## Objective

Expose read/list tools for projects and environments over MCP, mirroring the workspace tools' read-only-on-MCP precedent (`w6/m2` — Render's own MCP ships no workspace-mutation tools either, only reads).

## Context

- `w6/m2` (`.pm/w6/done/m2/`) — the `list_workspaces`/`select_workspace`/`get_selected_workspace` precedent (read-only MCP, mutations are dashboard/GraphQL-only for workspaces).
- Decide: does Render's own MCP server expose project/environment tools at all? If not, this is a bex superset like the workspace read tools already are.

## Steps

1. Add `list_projects`/`get_project`/`list_environments`/`get_environment` MCP tools, thin adapters over the same Core verbs.

## Files

- `lego/backend/internal/projects/mcp.go`

## Acceptance criteria

- [ ] The four read tools work and return data consistent with REST/GraphQL.

## Out of scope

- MCP mutation tools — matching the workspace precedent, mutations stay off MCP for this milestone.
