---
id: w6/m16/t003
title: "Core verbs: project/environment lifecycle + service assignment"
worker: worker6
status: todo
estimate: 1h
depends_on: [w6/m16/t002]
---

## Objective

Implement the Core-layer verbs: create/rename/delete a project; create/rename/delete an environment; assign/unassign a service to an environment — authorized and tenant-scoped like every other write verb.

## Context

- `lego/backend/internal/core/base.go` — the `Core.Base` pattern every resource's verbs go through (tenant/workspace authorization).
- `lego/backend/internal/workspaces/service.go` — the workspace-lifecycle verb precedent to mirror in shape.

## Steps

1. Implement the six verbs (create/rename/delete ×2 resources, assign/unassign).
2. Ensure a service can only be assigned to an environment within the same workspace it belongs to.
3. Ensure deleting a project cascades to its environments (and un-assigns their services, doesn't delete the services themselves).

## Files

- `lego/backend/internal/projects/service.go` (new package)

## Acceptance criteria

- [ ] All six verbs work correctly, tenant-scoped, with cascade-on-delete behavior verified.

## Out of scope

- REST/GraphQL/MCP surfaces — t004/t005.
