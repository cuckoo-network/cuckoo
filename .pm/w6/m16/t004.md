---
id: w6/m16/t004
title: "REST/GraphQL: projects + environments CRUD"
worker: worker6
status: todo
estimate: 1h
depends_on: [w6/m16/t003]
---

## Objective

Expose t003's verbs over REST and GraphQL, mirroring Render's `/projects`/`/environments` shape.

## Context

- Render's `/projects` + `/environments` REST API (api-docs.render.com) — field names/shapes to mirror where sensible.
- `lego/backend/internal/projects/` — the new package from t003.

## Steps

1. REST: `/v1/projects` (CRUD) + `/v1/environments` (CRUD) + assign/unassign endpoints.
2. GraphQL: `projects`/`project`/`environments`/`environment` queries + `createProject`/`renameProject`/`deleteProject` and the environment equivalents + `assignServiceToEnvironment`/`unassignServiceFromEnvironment` mutations.

## Files

- `lego/backend/internal/projects/rest.go`
- `lego/backend/internal/projects/graphql.go`

## Acceptance criteria

- [ ] Both surfaces expose full CRUD + assignment, verified against a running backend.

## Out of scope

- MCP — t005.
