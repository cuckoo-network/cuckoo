---
id: w6/m11/t002
title: "m5 residual: real-browser rerun of the full workspace lifecycle live"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m11/t001]
---

## Objective

Close m5's documented residual: re-drive the whole workspace lifecycle — create → switcher → rename → delete with the `sudo delete workspace <name>` guard — in a real browser against the live cluster (m5 verified it against the offline stub + real-infra backend tests only).

## Context

- `w6/done/m5/README.md` "Residual (documented, external)": the live-cluster _browser_ rerun was blocked by the local-cluster outage.
- Reference capture: `docs/render-artifacts/workspace-lifecycle.md` (the `sudo` phrase, Hobby-only delete).
- Use Playwright MCP; screenshots with bare filenames land in `.playwright-mcp/`.

## Steps

1. Log in to the dashboard against the live stack.
2. Create a workspace, switch to it, rename it, then delete it typing the exact `sudo delete workspace <name>` phrase; verify the guard rejects a wrong phrase first.
3. Record evidence (screenshots + a paragraph) in the milestone README.

## Files

- `.pm/w6/m11/README.md` (evidence)

## Acceptance criteria

- [ ] Full lifecycle re-driven in a real browser on the live cluster, guard behavior matching `docs/render-artifacts/workspace-lifecycle.md`
- [ ] Evidence recorded in the milestone README

## Out of scope

- New lifecycle features (plan change is w6/m12)
