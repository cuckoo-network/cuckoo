---
id: w6/m15/t002
title: "Dedupe the purger List+delete loops"
worker: worker6
status: todo
estimate: 25m
depends_on: []
---

## Objective

`postgres/purger.go`, `keyvalue/purger.go`, and `apps/purger.go` each carry a verbatim ~12-line List-by-`core.LabelTenant`-then-delete-each loop. Extract one shared helper (or switch to a single `client.DeleteAllOf(...)` call with the label selector) and point all three purgers at it.

## Context

- `.pm/w6/010.md` — the source note; flagged by `w6/m11`'s own `/simplify` pass as correctly out of that milestone's diff.
- `lego/backend/internal/postgres/purger.go`, `lego/backend/internal/keyvalue/purger.go` (or equivalent path), `lego/backend/internal/apps/purger.go`.

## Steps

1. Confirm the three loops are behaviorally identical modulo the resource type and label selector.
2. Extract a shared helper (generic over the k8s object type) or replace with `client.DeleteAllOf`.
3. Point all three purgers at it.

## Files

- the three purger files above

## Acceptance criteria

- [ ] All three purgers share one implementation.
- [ ] Existing purger tests pass unchanged (behavior-preserving).

## Out of scope

- Any purger behavior change beyond the dedup itself.
