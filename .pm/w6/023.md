# 023 — `.pm/w8/README.md` lists m25 and m26 as open, but neither milestone exists

Filed by `/pm-brainstorm for w6` 2026-08-17. Board hygiene in **w8**, scheduled here because workers are general-purpose. ~10m.

`.pm/w8/README.md:40-41` carries two unchecked milestone lines:

```
- [ ] **m25** — Webhook immutable attempt history + manual Resend (13 tasks) ← from authenticated Bex↔Render webhook parity audit 2026-08-17, needs m24
- [ ] **m26** — Webhook management UX + drift-proof dashboard parity (10 tasks) ← from authenticated Bex↔Render webhook parity audit 2026-08-17, needs m25
```

But `.pm/w8/m25/` and `.pm/w8/m26/` **do not exist** — no directory, no `README.md`, no task files anywhere in the tree. Twenty-three tasks are advertised as scheduled work with nothing behind them, and `/pm status` will report two milestones it cannot find a next actionable task for.

Both cite the same source: an "authenticated Bex↔Render webhook parity audit 2026-08-17" (same day as this note). Most likely a brainstorm's README lines were written without the milestone bodies being materialized.

**Do:** find out which — check whether the audit produced a working document (the `builder-issues.md` / `RESEARCH-workspaces.md` pattern) that still holds the intended task breakdown. Then either materialize both milestones properly via `/pm` (README + task files + closing tasks, IDs matching the path) or remove the two lines until they are real. Do not leave them half-declared.

Related: `.pm/w8/done/m24/` (webhook event-ID hydration) shipped 2026-08-17 and is the declared dependency of m25 — so the sequencing intent is real even if the bodies are missing.
