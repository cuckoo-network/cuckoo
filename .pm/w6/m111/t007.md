---
id: w6/m111/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m111/t006]
---

## Objective

Close the milestone once its Definition of done actually holds — not once the code is written.

## Context

- Every DoD bullet is a live click on `dashboard.bex.co`, so the fix must be **deployed** before any of them can be claimed. Confirm the fix commit is an ancestor of the commit `deploy.yml` last shipped and that the production image pin points at it, the way `w6/m98`'s closeout did.
- The reproduction fixture is `srv-da7o6ovvqdcc73bpn9hg` (`qa-20260826-webhook-svc`), whose pod logged exactly once at `2026-08-27T00:33:06Z`. It is also `w6/m110`'s fixture, and `w6/m110/t008` owns deleting it — do not delete it here; coordinate so whichever milestone closes second removes it.

## Steps

1. Verify every DoD bullet live against a deployed build.
2. Record t002's decision about `logs.emptyBody` in the milestone README, per DoD bullet 5.
3. Set every remaining task's `status: done`.
4. Move each `tNNN.md` to `w6/m111/done/`.
5. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
6. Move the whole `w6/m111/` directory to `w6/done/m111/`.
7. Check `- [x]` for `m111` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live against a deployed build, not assumed.
- [ ] t002's `logs.emptyBody` decision is written into the README.
- [ ] Fixture-deletion ownership with `w6/m110/t008` is settled, not duplicated.
- [ ] Milestone moved to `w6/done/m111/` with statuses in sync.

## Out of scope

- Remaining work becomes new board items.
