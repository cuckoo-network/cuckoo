---
id: w6/m111/t006
title: Test coverage
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m111/t004]
---

## Objective

Cover both defects with tests that assert the real failure modes, including the call sites that are correct today only by accident.

## Context

- `log-viewer.test.tsx` already covers the `w6/m47/t003` filtered/unfiltered branch pair and asserts the contradictory title/body can never co-render. Extend that contract to the range dimension rather than replacing it.
- `use-live-logs.test.ts` exists and injects an `EventSourceFactory`, so the subscribe URL is assertable without a network.
- The bug's trigger is a **relationship** — lines exist, but outside the window — so a fixture with no lines at all proves nothing. Tests need history present and outside the range.

## Steps

1. Assert `subscribeUrl` carries `startTime`, that it tracks a range change, and that it is clamped at the `BEX_MAX_QUERY_HOURS` boundary.
2. Assert the empty state for each of the four states, including precedence when a filter and a narrow range are both active.
3. Assert the never-logged copy is not rendered for a service with out-of-window history — the exact live repro, as a test.
4. Add a regression test for `deploy-log-panel`'s empty state per t003's outcome.
5. `cd dashboard && yarn test`.

## Files

- `dashboard/src/features/logs/components/__tests__/log-viewer.test.tsx`
- `dashboard/src/features/logs/hooks/__tests__/use-live-logs.test.ts`
- `dashboard/src/features/deploys/` tests

## Acceptance criteria

- [ ] Subscribe URL carries and tracks the window, and is clamped.
- [ ] All four empty states and their precedence are covered.
- [ ] The out-of-window-history case is a test, not just a manual repro.
- [ ] The deploy log panel has a regression test.
- [ ] `yarn test` passes.

## Out of scope

- Coverage of code this milestone did not change.
