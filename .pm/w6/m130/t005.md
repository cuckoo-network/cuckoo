---
id: w6/m130/t005
title: Render parity sweep (REST/GraphQL/MCP/dashboard)
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m130/t003, w6/m130/t004]
---

## Objective

Check that this milestone's change is consistent across every surface it exposes — REST, GraphQL and MCP in `lego/backend/` (same fields, semantics, error shapes) and the `dashboard/` UI — and compare against render.com's equivalent behaviour, flagging any remaining drift as follow-up work rather than silently diverging.

## Context

- bex-api is meant to be Render-compatible: `docs/ADR006-bex-api.md`; the parity ledger is `docs/ADR018-render-parity.md` (row 117 covers this field).
- The three fragments live together: `lego/backend/internal/apps/{rest,graphql,mcp}.go`.
- This milestone changes a REST request shape and a REST response shape, so the parity task is required rather than optional.

## Steps

1. Confirm REST, GraphQL and MCP agree on the field's semantics and per-type behaviour after `t002`/`t003`.
2. Confirm the error shape for the refused mutation is consistent across surfaces (REST body `code`, GraphQL `extensions.code`, MCP `CONFLICT:`-style prefix conventions as applicable).
3. Confirm ADR018 row 117 reflects the shipped behaviour, including any deliberate divergence.
4. Record anything still divergent as a new board item rather than fixing it here.

## Files

- `lego/backend/internal/apps/rest.go`, `graphql.go`, `mcp.go`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] The three API surfaces agree on semantics and per-type behaviour.
- [ ] ADR018 row 117 matches what shipped.
- [ ] Any residual drift is filed, not absorbed silently.

## Out of scope

- New parity work unrelated to this field.
