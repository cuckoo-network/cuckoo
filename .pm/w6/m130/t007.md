---
id: w6/m130/t007
title: Test coverage
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m130/t005]
---

## Objective

Add meaningful tests for the behaviour this milestone shipped, asserting real behaviour and failure modes — never trivial, tautological, or snapshot-everything tests.

## Context

The behaviours worth pinning, each of which was a live defect or a live-correct case that a global change could break:

- `serviceDetails.renderSubdomainPolicy` decodes on create and patch for `web_service` and `static_site`.
- Top-level still decodes, and wins when both are sent.
- The response reports the field where and only where `t002` decided, per type.
- `setSubdomainPolicy` is refused for each of `private_service`, `background_worker`, `cron_job` with a type-specific reason.
- A routable service with custom hosts can still set `disabled`; one without still gets the existing 400.
- The `ipAllowList` control still decodes nested (it must not regress while its sibling is being moved).

## Steps

1. Add the tests at the layer that actually exercises the wire format, alongside the existing REST/GraphQL/MCP tests in `lego/backend/internal/apps/`.
2. Include the conformance-guard outcome `t004` settled on.

## Files

- `lego/backend/internal/apps/*_test.go`
- `lego/backend/internal/api/conformance_allowlist_test.go` if narrowed

## Acceptance criteria

- [ ] Each bullet above has a test that fails against the pre-fix behaviour.
- [ ] `cd lego/backend && go test ./...` passes.

## Out of scope

- Live production verification — that belongs to the DoD, exercised at closeout.
