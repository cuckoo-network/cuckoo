# w6 — project workstream (worker6)

**Worker:** worker6. This is a general-purpose bex workstream. It may take work anywhere in the project; the milestones below are scheduled work and historical records, not a permanent purpose, specialty, or ownership boundary.

## Local dev environment

Develop against `.pm/w6/dev-6/`, this worker's own isolated stack on the shared local kind/CAPD cluster — never the shared cluster's default `auth`/`bex-system` namespaces or standard ports (5173/4433/4445/8090/8091/5432), which any other worker's session may also be using. `dev-6` gets its own Kratos + Hydra + Mailpit (namespace `dev-6-auth`) and app namespace (`dev-6`), reusing the shared cluster's CNPG operator and bex operator, plus a locally-built `bex-api` on dedicated ports derived from N=6 (`dev-6/ports.env`) so it never collides with any other workstream's `dev-N`.

- `bash scripts/dev-env.sh 6 up` — bring it up (idempotent — safe to re-run)
- `bash scripts/dev-env.sh 6 status` — health check (processes, pods, HTTP) + a pass/fail verification inventory
- `bash scripts/dev-env.sh 6 down` — tear it down (leaves the shared cluster and every other workstream's `dev-N` untouched)
- `bash scripts/dev-env.sh 6 clean` — reclaim `logs/` and `bin/` (refuses while the environment is up)

`up` prints the dashboard command to point at it once bex-api is running. One shared implementation serves every workstream since `w1/m72`; `.pm/w6/dev-6/` keeps only `ports.env` (a generated record of the derivation), this README, and `.gitignore`.

## Milestones

- [x] **m1** — Workspace model & lifecycle verbs: create · rename · delete · plan limits (10 tasks) ← from RESEARCH-workspaces.md — done 2026-07-09 (backend shipped `b06e301`, verified vs real Postgres + OpenFGA), moved to `done/m1/`
- [x] **m2** — Render `owners` read API + MCP workspace tools (9 tasks) ← from RESEARCH-workspaces.md, needs m1; supersedes w2/002 — done, moved to `done/m2/`
- [x] **m3** — Dashboard workspace UX: `/new/workspace` flow · switcher · settings (9 tasks) ← from RESEARCH-workspaces.md, needs m1 — done 2026-07-09 (switcher/create/settings/delete shipped, verified end-to-end against the offline dev stub — no live cluster available this session, see `done/m3/README.md`), moved to `done/m3/`
- [x] **m4** — Workspace-scoped datastores: fix ownerId labeling + wire real delete purgers (10 tasks) ← from `/pm-brainstorm for w6` 2026-07-09, closing a gap m1 itself deferred ("OpenBao/Database purger concrete impls") plus a discovered Postgres label-mismatch bug and a KeyValue labeling gap — done 2026-07-10 (Postgres/KeyValue teardown + ownerId scoping live-verified against real Postgres+OpenFGA; OpenBao secrets purge implemented + unit-tested but not live-verified, see `done/m4/README.md`), moved to `done/m4/`
- [x] **m5** — Live-verify workspace dashboard UX against Render + real infrastructure (5 tasks) ← from `/pm-brainstorm for w6` ("more for w6") 2026-07-09, closing m1/t001's unmet live-capture acceptance criteria and m3's own "Follow-ups" (offline-stub-only verification, ungenerated codegen diff) — done 2026-07-11 (live Render capture → `docs/render-artifacts/workspace-lifecycle.md` revealed the `sudo delete workspace <name>` guard + Hobby-only delete; reconciled end-to-end with tests; codegen drift closed by source-diff; live-cluster _browser_ rerun blocked by a diagnosed local-cluster outage, see `done/m5/README.md`), moved to `done/m5/`
- [x] **m6** — Security review: RBAC, supply chain, injection surface, network isolation (10 tasks) ← from `/pm-brainstorm` 2026-07-11 (`GOAL.md` #7, "Security review"); placed here as the least-loaded worker (0 open milestones at brainstorm time), not for topical fit — done 2026-07-11 (`docs/ADR028-security-review.md` written; cosign + SBOM in `deploy.yml`; input validators + OAuth `iss` check added with tests; tenant-isolation matrix live-tested + CI structural guard; stale wildcard/unused RBAC deleted), six follow-up notes filed (`w6/002`–`w6/007`), moved to `done/m6/`
- [x] **m7** — User owners: opaque `own-` identity ids for member identifiers (6 tasks) ← from `/pm how to implement own- or owner feature` 2026-07-11 + design review that trimmed it — done 2026-07-11 (mint `id.Owner` + `owner_ids` table/migration 0009; members `userId` is now an opaque own- id instead of a raw Kratos subject, verified through the full REST stack + a real-Postgres round-trip; owner `type: user` and peer `own-` resolution recorded as deliberate non-goals — bex has only team workspaces, peer-resolve has no consumer), moved to `done/m7/`.
- [x] **m8** — Scope down bex-api's cluster-wide RBAC (7 tasks) ← promoted from `002` 2026-07-12 (`w6/m6`'s RBAC least-privilege audit top finding) — done 2026-07-12, shipped directly via `552130b` (ahead of its own task files); moved to `done/m8/`
- [x] **m9** — Security hygiene chores (9 tasks) ← from `/pm-brainstorm` 2026-07-12, groups `003`, `004`, `005`, `007`, `008` (each sub-hour; `008` is an urgent pre-existing test break) — done 2026-07-12, shipped directly via `552130b` (ahead of its own task files); moved to `done/m9/`
- [x] **m10** — Member identity: user{email} on the members surface (9 tasks) ← from `/pm-brainstorm for more w6` 2026-07-12 (ADR018 members-row ◐ shape divergence; reuses m7's `own-` ids + m2's Kratos-admin lookup, which the owners API already composes — the members CRUD surface + Team page never adopted it) — done 2026-07-12 (`userId`+`email` enrichment across REST/GraphQL/MCP/dashboard, verified via real-fragment tests + code-level wiring inspection — no live cluster available this session, see `done/m10/README.md`), moved to `done/m10/`
- [x] **m11** — Live-verification sweep: close the m4/m5/m10 residuals on the restored cluster (7 tasks) ← from `/pm-brainstorm more for w6` 2026-07-12 (the "not live-verified" residuals recorded in `done/m4`, `done/m5`, `done/m10`; unblocked by the local-cluster Calico/CNPG fix) — done 2026-07-12 (workspace lifecycle + OpenBao purge live-verified against real prod; Team-page email-primary rendering root-caused + fixed in source but not yet deployed; two more real bugs found+fixed — `keyvalues` RBAC gap, orphaned Apps from the public create path — plus a live orphaned resource left on real prod needing manual `kubectl` cleanup, see `done/m11/README.md`), moved to `done/m11/`
- [x] **m12** — Workspace plan change: upgrade · downgrade · plan-gated roles (9 tasks) ← from `/pm-brainstorm more for w6` 2026-07-12 (RESEARCH-workspaces.md findings 1/4/5; the last missing lifecycle verb — `workspaces` service has Create/Rename/Delete only) — done 2026-07-12 (`changeWorkspacePlan` GraphQL mutation + downgrade guards for members/services/per-user-cap/out-of-plan-roles; plan-gated role sets via `store.RoleAllowedOnPlan`; dashboard plan section + change-plan dialog; live Render capture `docs/render-artifacts/workspace-plan-change.md`), moved to `done/m12/`
- [x] **m13** — Ship & live-verify the m11/m12 residuals on prod; purge the orphan (8 tasks) ← from `/pm-brainstorm more for w6` 2026-07-12 (`done/m11/README.md` §Residual: three undeployed fixes + the live orphaned `m11-purge-test` App; m12's stub-only verification) — done 2026-07-13, live-verified **twice** by two agent sessions on the same checkout/cluster, reconciled into one record (`done/m13/README.md`): one premise was stale on arrival (the m11/m12 fixes were **already shipped and running** on prod — verified by image digest + live `BEX_KRATOS_ADMIN_URL`/`keyvalues` RBAC checks); the orphaned `m11-purge-test` App **was** still live and was deleted (`kubectl delete app`, confirmed `NotFound`). Every remaining DoD clause was verified live: clean workspace delete with **no `keyvalues` 403**, no orphaned App, the literal OpenBao black-box re-read (same-named service in a 2nd workspace reads back zero env vars), the Team page rendering **email-primary**, and the plan flow (hobby gates invites → pro unlocks → guarded downgrade shows the recorded copy). Found + fixed + regression-tested **three new bugs**: a plan-limit bypass via the pending-invite window (proved on prod: a Hobby workspace holding 2 members), a missing `jobs.batch` RBAC grant silently breaking usage build-seconds metering, and a stale positive-TTL identity→tenant cache that 403'd the workspace switcher for up to 30s after a self-delete. All three fixes await `/ship`. Moved to `done/m13/`
- [x] **m14** — Deterministic workspace targeting: multi-workspace caller-tenant resolution (11 tasks) ← from `/pm-brainstorm more for w6` 2026-07-12 (m11 t003/t004 residuals: `TenantForIdentity` has no deterministic pick, audit log mis-scoped; ADR018 "Create service" row's stale `ownerId` omission) — done 2026-07-13 (deterministic default workspace = oldest membership; `ownerId` honored on creates across REST/GraphQL/MCP/dashboard, membership-checked, 403 for a non-member; `core.Base.GetApp` now authorizes the VERB'S OWN relation against the APP'S OWN workspace — which is what lifts m11's owner-403 without letting an admin of A act as an admin in B; verified against a live Postgres + OpenFGA (`TestMultiWorkspaceTargetingE2E`) and a browser click-through of the audit card. Found+fixed **two real bugs beyond the DoD**: a pre-existing **cross-tenant credential read** — `GET /v1/postgres/{name}` / key-value fetched ANY workspace's CR by name, connection string included — and a wrong-workspace write this milestone nearly shipped (`ownerId: B` on a name taken in A silently redeployed A's service). Known limitation filed as `w6/013`: a verb still also authorizes against the caller's DEFAULT workspace, so an invited _viewer_ can't operate services in their own workspace), moved to `done/m14/`
- [x] **m15** — Workspace lifecycle polish: invite-gate CTA + purger dedup + ChangePlan pending-invites guard (8 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13, groups `009`, `010`, `011` (each sub-hour) — done 2026-07-13, moved to `done/m15/`
- [x] **m17** — Fix false-403: collapse the caller-default-workspace vs. resource-workspace authorization gates (9 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13, promotes `013` (reproduced during **m14**; fix designed there as `core.Base.AuthorizeApp`) — done 2026-07-13 (`core.Base.AuthorizeApp`/`AuthorizeDatabase`/`AuthorizeKeyValue`: authorize + fetch a named App/Database/KeyValue in ONE call, against the resource's OWN workspace, replacing the old `Authorize` + `GetApp`/`fetchDatabase`/`fetchKeyValue` pair at ~50 call sites across apps/postgres/keyvalue/secrets/logs/metrics/envgroups/deploys/events; `List(ownerID)` in all three resource types folded onto `core.WithWorkspace`'s membership-checked mechanism, closing a parallel OpenFGA-only gap; the exact `bob`/`tea-team`/`tea-mine` scenario from `013` verified live against a real Postgres + OpenFGA + the real REST router (`TestW6013_InvitedViewerRestartsOwnWorkspaceServiceE2E`), plus permanent fake-based regression tests in `apps`/`secrets`/`core`; both named regression suites (`TestAuthzGuardsEveryVerb`, `TestEveryTargetedVerbIsNamedOrExcused`) stay green throughout, the latter's AST sweep extended with one-hop constant propagation so it still tells read verbs from write verbs post-collapse. `/simplify` found and fixed a genuine efficiency bug (`envgroups.UnlinkService` double-fetching its own App) and dead code (`core.Base.GetDatabase`/`GetKeyValue`, `keyvalue.patchKeyValue`), and correctly declined a reorder that would have broken the deliberate "authorize before revealing feature availability" property (verified empirically against `TestAuthzGuardsEveryVerb`)), moved to `done/m17/`
- [x] **m18** — Deterministic ownerId threading: usage, API keys, GitHub connections (9 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13, promotes `012` (the read/bind-side residual **m14** left open) — done 2026-07-14 (`ownerId` threaded through usage/api-keys/GitHub-connections on REST/GraphQL/MCP, mirroring m14's `core.WithWorkspace` seam; dashboard Usage + API-keys pages follow the switcher; `use-current-workspace.ts`/`team-panel.tsx` were already fixed by m14's own `/simplify` pass, verified not re-broken. Found+fixed **two real bugs beyond adapter plumbing**: `ListAPIKeys` had **no tenant filter at all** — every workspace's keys were visible to any caller who could manage their own — and `RevokeAPIKey` had no ownership check — any key id was deletable cross-workspace; both closed via a new `KeyBinder.TenantForKey` seam. Verified against a live Postgres + OpenFGA (`TestReadSideOwnerIDTargetingE2E`, mirrors `TestMultiWorkspaceTargetingE2E`) plus fast unit tests), moved to `done/m18/`
- [x] **m19** — Protected-environment ACLs: `protectedStatus`/`networkIsolationEnabled`/`ipAllowList` on Environments (10 tasks) ← from `/pm-brainstorm more milestones` 2026-07-13 (`docs/ADR018-render-parity.md` Environments row; `docs/ADR032-environments.md`'s own "Known divergence" section names this gap). The Environments mechanism itself lives in `w1/m32` — materialized under `w6` **per user direction**, same cross-workstream-placement pattern as `014.md` → `w2/m28` — done 2026-07-14, reconciled with the concurrently-shipped **m20** (both sessions independently needed a `core.LabelEnvironment` App/Database/KeyValue label; m20's landed first as the DB/KV-membership label `"app.bex.co/environment-id"`, so this milestone's own App-only, isolation-signal label was renamed to `core.LabelNetworkIsolation` to avoid the collision, and t006's `ipAllowList` fan-out was upgraded from "environment's project members" to m20's precise `EnvironmentID`-based membership). Migration `0024` (renumbered from an initial `0023` — collided with the concurrently-shipped `w1/m33` pre-deploy migration's own `0023`) + `store.Environment`/`SetEnvironmentACL`; `SetACL`/`PATCH /v1/environments/{id}/acl`/`setEnvironmentACL`/`set_environment_acl` full-replace across REST/GraphQL/MCP; `core.ProtectedStatusProtected/Unprotected` shared vocabulary; `core.LabelNetworkIsolation` stamped/cleared on member App CRs by `environments.Service` itself — not the reconciler — whenever membership or the isolation flag changes; the operator's `reconcileNetworkPolicy` scopes ingress/egress to same-environment peers instead of same-workspace when the label is present (verified live via envtest); `apps.Service`'s new destructive-verb guard (`protection.go`) blocks Delete/Suspend(not Resume)/an existing-service deploy-override on a protected member unless the caller echoes back `ProtectedConfirmation`, mirroring `workspaces.DeleteConfirmation`; `ipAllowList` fans out to every Database/KeyValue whose `EnvironmentID` matches. Dashboard reads the three fields (`EnvironmentFields` fragment, codegen'd offline via `TestDumpGraphQLSchema`) but ships no editing UX yet — REST/GraphQL/MCP only. `/simplify`'s 4-agent pass found the diff unusually clean; applied fixes: moved `core.WithConfirm`/`ConfirmFrom` and the `ProtectedStatus*` constants out of a single-feature-import chain into `core` properly, collapsed `setSuspended`'s duplicated write-through branches and `environments`' two label-sync loops into one each. All three test suites green (`go test ./...` backend + operator envtest + dashboard `tsc`/vitest)), moved to `done/m19/`
- [x] **m20** — Environments coverage for Databases & Key Value (8 tasks) ← from `/pm-brainstorm more milestones` 2026-07-13, closes the services-only asymmetry (Environments cover Apps only; Projects already cover all 3 resource types) — done 2026-07-14 (`SetEnvironmentID` on `postgres`/`keyvalue.Service` writes the new `core.LabelEnvironment` CR label — added here as a shared prerequisite ahead of **m19**/t003, which still owns the separate App-CR projector; `environments.Service.SetDatabases`/`SetKeyValues` full-replace verbs mirror `internal/projects`; `EnvironmentView.databaseIds`/`keyValueIds` surfaced identically across REST/GraphQL/MCP; dashboard `manage-resources-dialog.tsx` replaces the services-only `assign-services-dialog.tsx`; `/simplify` fixed a real `Create`/`List` N+1 efficiency regression, both locked in with tests; all backend tests + `lint-backend` + dashboard typecheck/lint/930 tests pass), moved to `done/m20/`
- [x] **m21** — Build/start command override: `dockerfilePath`/`startCommand` on App create (8 tasks) ← from `/pm-brainstorm more milestones` 2026-07-13 (`docs/ADR018-render-parity.md` service-create-fields row; `lego/backend/internal/apps/mcp.go:106` already documents the gap). Materialized under `w6` per user direction — done 2026-07-14 (t001-t003 were found already shipped by an earlier, unrelated commit — `App.Spec.DockerfilePath`/`StartCommand` already existed in types/build.go/app_controller.go; this milestone closed the remaining gap — `dockerfilePath` threaded through GraphQL's `createService` + MCP's `create_web_service`/`create_cron_job` + bex.yml ingestion, matching `startCommand`'s existing coverage. `/simplify`'s altitude pass found and fixed a real correctness gap the milestone's own new envtest exposed: `StartCommand`'s Deployment/CronJob command override was gated on `Runtime == "docker"` only, silently dropping the override for a legacy `Builder: "dockerfile"` App with no explicit `Runtime` — broadened to reuse the existing `effectiveBuilder` mechanism. Dashboard UI gap (dockerfilePath unsurfaced anywhere; startCommand create-only, no Settings edit) filed as `w6/015.md`. All operator envtest + backend test suites pass, no new lint issues), moved to `done/m21/`
- [x] **m22** — CNB buildpack builds via kpack (8 tasks) ← from `/pm-brainstorm more milestones` 2026-07-13, implements the deferred `spec.builder: buildpack` contract left ready since `w1/m5`; done 2026-07-14 (kpack/Paketo, authenticated Zot publishing, digest reconciliation, build env, least-privilege RBAC, real Dockerfile-less E2E, and Render-native runtime/build/start compatibility), moved to `done/m22/`
- [x] **m23** — Custom domains: www↔apex sibling pairing (10 tasks) ← from `/pm-brainstorm more milestones for each worker` round 2, 2026-07-14 (ADR018 domains row's "omitted rather than faked — no public-suffix list" divergence, reopened WITH the missing mechanism: `golang.org/x/net/publicsuffix`; also closes `w7/m6`'s documented per-host collision-guard blind spot). Materialized under `w6` for capacity per the m19–m22 precedent — done 2026-07-14 (live-Render-docs capture (`docs/render-artifacts/custom-domain-pairing.md`) pins add/delete/redirect semantics; `registrableDomain`/`isApex`/`wwwSibling` on `golang.org/x/net/publicsuffix` replace the old dots-count heuristic; `AddDomain` auto-pairs the www↔apex sibling one hop deep — served directly rather than redirected, since bex has no per-host Ingress-redirect mechanism, an honest conscious divergence; `hostClaimedElsewhere` extended symmetrically so a sibling claim blocks cross-app squatting in both directions; both halves get independent `dnsRecord` DNS instructions on REST/GraphQL/MCP and the dashboard now shows the pairing + both DNS blocks in the add dialog. `/simplify`'s 4-agent pass found and fixed a real cross-language bug beyond its own remit: the dashboard's client-side pairing-display heuristic would silently miss a pair if a tenant typed mixed-case (`Foo.com`), traced to the backend never normalizing hostname casing — fixed by lowercasing at the `AddDomain` write boundary (single source of truth) plus a regression test; also collapsed a recursive-boolean-flag pairing implementation into two explicit calls and table-drove a duplicated conflict test. ADR005 + ADR018 updated with evidence of the closed divergence. All three test suites green, including new REST/GraphQL/MCP-level sibling-conflict + paired-DNS-instructions tests), moved to `done/m23/`
- [x] **m24** — Env groups: workspace attribution, Render object shape, and Environment linkage (11 tasks) ← from `/pm-brainstorm more milestones for each worker` rounds 4 and 7 — done; moved to `done/m24/`. Cross-tenant list/get/reveal/link closed with workspace-attributed metadata and deterministic legacy migration; `ownerId`/timestamps ship on REST/GraphQL/MCP; dashboard list/create follow the workspace switcher. The round-7 extension adds `environmentId` on env groups, `envGroupIds` on Environments, and same-workspace full-replace membership over REST/GraphQL/MCP. Verified by the real Postgres+OpenFGA isolation harness and browser switcher pass recorded in the milestone, plus package-level join/leave/cross-workspace/adapter regressions; backend test/build/lint green.
- [x] **m25** — `maxShutdownDelaySeconds`: graceful-shutdown window per service (8 tasks) ← from `/pm-brainstorm` round 6, 2026-07-14 — done 2026-07-14 (optional validated CRD field maps to pod `terminationGracePeriodSeconds`; Render-shaped REST plus GraphQL/MCP and dashboard editor cover web/private/worker with named validation; full suites, browser persistence, and a live mock-cluster pod at 137 seconds verified), moved to `done/m25/`
- [x] **m26** — Official CLI `services instances`: live service-instance listing (8 tasks) ← from user `$pm for w6`, 2026-07-15; transfers the last open slice of `w2/008` — done 2026-07-14 (authorized Kubernetes Pod projection; exact Render `id`/`createdAt` route under `/services` + `/apps`; explicit rollout/terminal/cron/static/zero-instance policy; unmodified CLI + self-cleaning harness live proof; REST-only surface decision documented; backend build/tests/lint green), moved to `done/m26/`
- [x] **m28** — Render resource metadata: owner · region · dashboard URL · updated timestamp (10 tasks) ← promoted from `016` 2026-07-15 after the official-CLI compatibility audit — done 2026-07-15 (shared tenant-safe owner resolver + explicit optional `BEX_REGION` + real dashboard detail routes + authoritative mutation/reconciliation timestamps across Service/Postgres/Key Value REST; configured/unconfigured/legacy/authz/timestamp integration coverage; unmodified official CLI self-cleaning verifier live-green in dev-9; backend build/tests/lint and docs/shell formatting green), moved to `done/m28/`
- [x] **m29** — Tenant image pulls by design: imagePullSecret on generated workloads (6 tasks) ← promotes `w7/001` 2026-07-15 — done 2026-07-15 (`556fa974`; stale-workload backfill + set/unset envtests; default-SA fallback removed; fresh-node authenticated pull and final four-workload production sweep green at Argo `93db47ea`), moved to `done/m29/`
- [x] **m30** — Custom domains: sibling redirect, the real 3xx (7 tasks) ← from `/pm-brainstorm` round 10, 2026-07-15 (closes the divergence `m23` consciously shipped, `docs/ADR005-custom-domain.md:60` — the auto-paired www↔apex sibling is served identically instead of 3xx-redirecting to the canonical host; the missing per-host mechanism landed in-tree via `w7/m32`'s Traefik middleware work)
- [x] **m31** — `registryCredentialId` on Render-shaped service create/patch (8 tasks) ← from `/pm-brainstorm` round 12, 2026-07-15 — done 2026-07-15 (durable explicit binding across REST/GraphQL/MCP/dashboard; canonical `/registrycredentials` lookup/readback; unmodified official CLI live-green against an auth-enabled private registry with anonymous negative control; Docker build-time auth drift filed as `017.md`), moved to `done/m31/`
- [x] **m32** — Env groups, first-class: Render list envelope + pagination, purge on workspace delete, migration audit trail (8 tasks) ← from `/pm-brainstorm` round 13, 2026-07-15 — done 2026-07-15 (live `{envGroup,cursor}` REST paging despite Render's stale endpoint schema; GraphQL/MCP paging; workspace-delete purge including deterministic ownerless legacy handling; exactly-once system audit for lazy ownership migration; full backend build/tests + lint green), moved to `done/m32/`
- [x] **m33** — Environments surface-parity chores: create ACL + list filters + field drift (8 tasks) ← from `/pm-brainstorm` round 14, 2026-07-15 — done 2026-07-15 (one shared ACL-aware create path across REST/GraphQL/MCP; Render-documented REST filters honored or named 400; exact REST field shape with GraphQL extensions documented; full backend build/tests/lint plus dashboard typecheck green), moved to `done/m33/`
- [x] **m34** — Docker-build `registryCredentialId`: private base images for repo-backed Docker services (8 tasks) ← promotes `017` (filed by m31's closeout parity check), `/pm-brainstorm` round 17, 2026-07-15 — done 2026-07-15 (REST nested Docker field plus GraphQL/MCP shared binding and dashboard Docker-repo picker; operator merges private-base pull + platform push auth into one buildkitd/cosign-only config with collision and isolation tests; live CAPD proof reached Running with the valid credential and failed `401 Unauthorized` with the wrong credential; full backend/operator/dashboard gates green), moved to `done/m34/`
- [x] **m35** — Env-groups list filters: implement or explicitly reject (7 tasks) ← promotes `018` (filed by m32's closeout) via `/pm-brainstorm` round 18, 2026-07-15 — done 2026-07-15 (all documented name/multi-owner/Environment/created+updated filters implemented with OR-within/AND-across semantics, strict named timestamp errors, and filter-before-page; shared query parsing now also backs Environments; GraphQL/MCP REST-only dialect decision documented; full backend build/tests/lint green), moved to `done/m35/`
- [x] **m36** — Read-path correctness chores: legacy-timestamp filter + registry-cred batch lookup (5 tasks) ← from `/pm-brainstorm` round 21, 2026-07-15, groups the round's shipped-diff finding (m35's `matchesTimeWindow` silently drops legacy groups with empty `createdAt`/`updatedAt` under any time filter — parse-error fallthrough) with `019` (the `restServices` per-item registry-credential name N+1, re-verified at HEAD), each sub-hour, per the sizing rule — **DONE 2026-07-16**
- [x] **m37** — Protected-environment guard for Postgres/KeyValue delete + suspend (11 tasks) ← from `/pm feature parity check and fix around delete a psql database against render` 2026-07-16, closes the divergence `m19`'s own closeout text flagged and left open ("a 'protected' environment should protect its database too") — Apps have the confirm-phrase destructive-verb guard, Postgres/KeyValue don't
- [x] **m38** — Official CLI `postgres create`: custom database/owner names + fail-closed Datadog flags (10 tasks) — done 2026-07-18; unmodified CLI + real CNPG SQL identity proof, all adapters, fail-closed Datadog, full gates green; moved to `done/m38/`
- [x] **m39** — Control-plane identity for the namespace pruner: stop dev-N stacks cross-pruning tenants (7 tasks) ← from `/pm-brainstorm for w6` 2026-08-17, promotes `w3/017` — done 2026-08-17. `BEX_CP_IDENTITY` (label `app.bex.co/control-plane`, default `production`) now scopes **both** cluster-scoped prunes. The `/simplify` review found the App-CR projector has the identical delete-by-absence bug — worse, in fact, since each control plane recreates its own App and the two flap tenant Deployments every 30s rather than deleting once — so t007 was added mid-milestone and the DoD widened; without it the milestone's own goal was unmet. Also fixed: a malformed `BEX_CP_IDENTITY` used to start cleanly, provision **zero** namespaces (every apply rejected, errors collected per-workspace and never fatal) and keep pruning — now fatal at startup. Adoption and pruning deliberately disagree on the unlabeled case (adoptable by anyone, prunable only by `production`), and converging another identity's object is refused so a harness raised from someone else's bex-db dump can't steal-then-reap. Every guard pinned by tests verified red pre-fix; ADR043 gains D9. Moved to `done/m39/`
- [x] **m40** — ADR registry integrity: renumber the three collisions, index every ADR, add a fail-closed guard (7 tasks) ← from `/pm-brainstorm for w6` 2026-08-17, measured at HEAD — done 2026-08-17. Three numbers were claimed by two files each; in every pair the number stayed with the more-cited document (ADR040→billing-metronome 47 refs, ADR049→render-yaml-parity 14, ADR060→build-worker 24), and the movers became **ADR070** (openchoreo-evaluation), **ADR071** (tenant-billing-credits), **ADR072** (security-review-round7), each stamped with its old→new mapping. Bare-number prose was resolved by reading context, not bulk-replaced — most of the 289 occurrences correctly meant the winners; `ADR060 §D7` in the KeyValue controller means the build-worker ADR and was left alone. `ADR030-pricing`, `ADR033-workflows`, and the OpenChoreo evaluation were unindexed and are now in `CLAUDE.md`. The durable half is `scripts/adr-registry-validate.sh` + its self-test (3 proven red cases), wired into `scripts.yml` with `docs/**` + `CLAUDE.md` path triggers and its explicit shellcheck list. The guard earned its keep on its first run, flagging a prose ellipsis (`docs/ADR047-...md`) that forced a tighter slug charset, then a fake ADR name in its own comment. **Moving round 7 to ADR072 places it numerically after round 13 — deliberate:** the security lineage was never numerically ordered (it already skips 062/065), each round names its predecessors explicitly, and keeping the number there would have cost ~3× the rewrites plus every `ADR060 D1`–`D8` citation the build-worker rollout depends on. Moved to `done/m40/`
- [x] **m41** — Causally-ordered availability edges: refuse time-traveled unhealthy conclusions (7 tasks) ← from `/pm-brainstorm for w6` 2026-08-17, promotes `w3/016` (ADR052 § Consequences item 2)
- [x] **m42** — Open-signup launch gates: seamless email verification (hybrid) + payment `all` mode (ADR075 D7+D8) (13 tasks) — done 2026-08-21 (D8 hybrid + branded courier emails + OTP UX live-verified on dev-6 and shipped to prod; t007 flipped `BEX_REQUIRE_PAYMENT_METHOD=all` on production with all 19 non-bound first-party workspaces Mode-A excluded, neither=0/20; agent-session 402s wired into the interception dialog), moved to `done/m42/` ← from `docs/ADR075-user-onboarding.md` (user decisions 2026-08-19, corrected + revised to the D8 hybrid 2026-08-20); materialized per user direction 2026-08-20 — the launch gates split from the D1–D6 dashboard work by risk profile
- [x] **m43** — New-Service and status-badge copy bugs for non-HTTP service types (7 tasks) ← from a live bex-vs-Render Background Worker lifecycle QA pass, 2026-08-21 (https://claude.ai/code/artifact/29934e6e-7bd8-4fa0-8a86-26327bb72932): the Existing-Image `$PORT` hint and the New Service title/subtitle both ignore the selected service type, and the status badge derives "Sleeping / wakes on the next request" for a Background Worker transiently resuming from Suspend — a status that structurally cannot apply to a type with no HTTP endpoint. Findings 3 (Free-tier availability for Background Worker) and 5 (Suspend confirmation friction) from the same pass need a product decision first, not a code fix — filed as `w6/025.md` and `w6/026.md`. — done 2026-08-21 (all three fixed, each pinned by a test proven red pre-fix and verified live on dev-6; the `/simplify` pass caught a drift bug the fix itself had introduced — a bare `/services/new` showed "New Service" in the tab and "New Web Service" in the heading — now closed by a shared `DEFAULT_SERVICE_TYPE`, and moved `servesHttp()` into `service-type.ts` where the predicate family already lived), moved to `done/m43/`. Health-check gate reuse deferred to `027` (not behavior-preserving while loading).
- [x] **m44** — Dead-id detail URLs regressed: single-resource GraphQL returns a zero-value object instead of null (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-22: `server`/`database`/`keyValue` answer a dead id with an all-empty object, which walks straight past the `!resource` predicate `w9/m55` built its redirect-home-on-dead-id guarantee on — so `/services/<dead>` renders a ghost service page with an enabled **Manual Deploy** button. Regression of `w9/m55` (done 2026-07-17). — done 2026-08-22. **Triage corrected the hunt's diagnosis on three points, two of which would have sent the fix to the wrong layer** (full record in `done/m44/README.md`): the reported `errors`-key split was not real (all three verbs already returned `app not found` beside the zero object — only the `data` half differed), the cause is graphql-go's executor returning a resolver's un-completed value from `resolveField`'s named result after recovering the error panic (**not** `gqlutil.KeyVerb`, so a per-verb patch would have left the hand-written `blueprint` and `project` literals still leaking), and the dashboard pages were never missing `useNotFoundRedirect` — their `!error` predicate was defeated, because a dead id is REPORTED as an error. Fixed at two seams: `gqlutil.NilOnError` on the composed schema (28 id-addressable read verbs sweep clean; a future one inherits it) and one shared `resourceNotFound`/`resourceFailed` pair replacing seven hand-rolled copies. REST 404 + MCP tool-error were already correct and are now pinned. Both new suites proven red with the fix reverted. **Not live-verified — closed unshipped, so the DoD's production re-probe of `dashboard.bex.co` is still owed** (see `done/m44/README.md`). Moved to `done/m44/`.
- [x] **m45** — Live QA hunt: native build PATH loss, invisible creation-time env vars, stale post-cancel status (8 tasks, **done 2026-08-22, closed unshipped** — `bash -lc` → `bash -c` proven against the real `golang:1.24-bookworm`/`rust:1-bookworm` images with Docker (Rust was only _reasoned_ broken when filed; now confirmed, same exit 127); creation-time literal env vars now seeded into the mutable env store before the App CR so REST/GraphQL/MCP and the Environment tab all see them, with the Blueprint path deliberately left manifest-owned; `"Server"` added to Cancel/Rollback's refetch and the shared triple hoisted to one `DEPLOY_REFETCH_QUERIES`. t004: live-tail duplication was already fixed by `6e35aa9a`, the Root-Directory "auto-detect" turned out not to exist at all (recorded as an ADR018 parity gap), commit/date spacing fixed. The live `dashboard.bex.co` probes are owed by whoever runs `/ship` — enumerated in the milestone README) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-22 (run from a `/loop 10m` session): a native Go build fails 100% of the time (`go: command not found`) because the generated Dockerfile's `bash -lc` login-shell wrapper resets `PATH`, discarding the toolchain image's own `PATH`; an env var set at service-creation time is baked into the App spec and works, but is never seeded into the `internal/secrets` store the Environment tab/`envVars` API read, so it's permanently invisible to the UI; and the service header's status pill goes stale for up to 30s after Cancel/Rollback because `deploy-actions.tsx`'s `refetchQueries` omits `"Server"`. A fourth, lower-confidence finding (live-tail reconnect log duplication, Root-Directory runtime auto-detect, commit/date text spacing) needs fresh root-causing. A candidate fourth bug (Free-tier service scaling past 1 instance) was investigated and rejected — `docs/ADR018-render-parity.md` line 66 already documents it as a deliberate, user-confirmed divergence (`w1/m81`).
- [x] **m46** — Private services publicly exposed via platform subdomain; first-time deploys wrongly marked Canceled with a stuck status pill (9 tasks, **done 2026-08-22, closed unshipped** — both bugs were ONE write: the control-plane projector's `projectSpec` hardcoded `Expose: true` for every App because the `apps` row carried no service type, and `applyOwnedSpec` re-stamped it on every resync over the type-aware value the create path had computed. That published private services at their platform subdomain, and — because the overwrite is a spec-mutating Update that bumps `metadata.generation` past the generation `CreateApp` opened the first deploy row at — closed brand-new deploys `Canceled` whenever it raced the operator's first reconcile. Which is exactly why only `private_service`/`background_worker`/`cron_job` were affected, matching the hunt's two findings. Fixed with migration 0095 (`apps.type`), `Expose`/`Type` removed from the projector's owned set, and the exposure rule collapsed into one allowlist on the CRD contract (`AppSpec.PubliclyRoutable`) that `EffectiveHosts` and the Ingress site both gate on. Custom domains aligned on the same predicate across create/AddDomain/Blueprint/UI; create now stamps the release-generation annotation every other deploy path already did; both header badges moved onto a shared converging poll. Every regression test proven red pre-fix. The live `curl` / first-deploy production probes are owed by whoever runs `/ship` — enumerated in the milestone README) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-22 (run from a `/loop 30m` session, iteration 4): a `private_service` — advertised as "Accessible only within the platform network" — was reproducibly reachable at its `.onbex.co` URL with a real, unauthenticated, live-serving Ingress route (`curl` confirmed 200 + real app body; the operator's own `Status.URL` field independently confirmed it); the one code path that decides `Expose` is correctly type-aware and long-merged, so the actual divergent write is unverified and left for research, with an operator-level type gate added as a backstop regardless of cause. Separately, a brand-new service's first-ever deploy was marked `Canceled` with no user action and no real supersede, traced to `ReleaseGeneration` being initialized from a possibly-already-bumped `app.Generation` (`lego/backend/internal/store/reconciler.go:917-929`, `lego/operator/internal/controller/release_identity.go:301-302`), leaving the Service header stuck reporting `Building` indefinitely since the client only refetches on a local button-triggered mutation, not a server-driven status change.
- [x] **m47** — Free-tier sleep/wake broken (blocker) + event/log copy conflates hibernate with suspend (7 tasks, **DONE 2026-08-23**, moved to `done/m47/`) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-22 (run from a `/loop 30m` session, iteration 5): a Free-tier web service, after auto-hibernating on its idle timeout (dashboard correctly showed "Sleeping to save resources — wakes on the next request"), never actually woke — every request to its `.onbex.co` URL returned a bare Traefik-default `404 page not found` instead of the activator's wake flow, and `phase` stayed `Hibernated` across repeated requests over 2+ minutes; the exact defect was left unverified at filing time and is now **confirmed statically (2026-08-23), and it was none of the three hypotheses**: an Ingress backend resolves only within the Ingress's own namespace, and `ingressBackend` named the platform Service `bex-activator` (namespace `bex-system`) as the public backend for an auto-hibernating App — but under ADR043 every tenant App's Ingress lives in its own `tea-<xid>` namespace, so the backend simply did not exist there and Traefik answered with its own default 404. The same function already handled this correctly for maintenance mode via an App-owned ExternalName alias; the hibernate branch never got one, and every hibernate test ran in namespace `default`, where the defect is invisible. Fixed with a `bex-activator-<app>` alias, with the fail-closed alias admission policy, live verifier, ADR029/ADR043 and the operator env table updated in lockstep. **Live-verified before/after on one real App in the tenant namespace `tea-…` on the shared CAPD cluster**: pre-fix the hibernated Ingress backend `bex-activator` does not exist in the App's own namespace (`services "bex-activator" not found`); post-fix the App-owned `bex-activator-<app>` ExternalName alias does, DNS resolves the full chain to the platform activator, a request while asleep returns the `Retry-After: 5` wake page instead of a 404 (`"waking app"` in the activator log), and ~6s later the App is `Running` with the Ingress flipped back to its own Service and serving a real `HTTP 200`. The live leg also caught that `TestPushWorkerEnqueuesObservedLifecycleFacts` is `BEX_TEST_DB_URI`-gated and had been silently skipping, so t002's store change had never run against real Postgres — pointed at a database it correctly failed (an auto-hibernate no longer enqueues a `service_suspended` push, which is the point), and its Hibernated leg is now marked a genuine user suspension. Migration 0097 applies cleanly; full backend suite green on a pristine database. Incidental unrelated defect found while raising the environment and filed as `w6/031`: `config/rbac` grants no `replicasets`, so any `make deploy` operator fails cache sync and reconciles nothing (production's gitops RBAC is unaffected). Separately, root-caused exactly: the Events feed (`lego/backend/internal/store/event_facts.go:387-390`) labels every transition into `Hibernated` phase as `"Service suspended"` — the identical event type used for a real user-driven Suspend — contradicting the codebase's own documented invariant that suspension is user-exclusive (`service.go:856-857`), and leaking into webhook/notification payloads too. Also root-caused: the Logs tab's empty state (`dashboard/src/features/logs/components/log-viewer.tsx:183`) shows a hardcoded "No logs yet" title alongside a correctly-branching "No logs match these filters." body whenever a search/filter yields zero results on a service with real log history — two lines asserting opposite things at once.
- [x] **m48** — GraphQL leaks empty-string zero-values as real optional-FK data; Project-only create-form selection is a silent no-op (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-22/23 (run from a `/loop 30m` session, iteration 6): a workspace-scoped Environment Group's `environmentId` resolves to `""` over GraphQL instead of `null` (REST already omits the same field via `omitempty` for the same group) — `EnvGroupView.EnvironmentID` is a bare `string`, and `gqlutil.StrField` resolves its zero value straight through. The dashboard's own `?? null` normalization never catches `""`, so every environment-less service linked to an environment-less group shows a false "Legacy link outside this group's Environment" warning, the "Link Service" control is hidden entirely (its `available` list computes empty for the single most common no-Environment configuration), and "Manage → Move group" renders the placeholder "Select an Environment" over a group that already reads "Workspace (no Environment)" one scroll up. The identical `gqlutil.StrField`-on-bare-string shape also leaks `service`/`postgres`/`keyValue`'s `projectId`/`environmentId` (confirmed live for `service`). Sibling bug class to the same day's `w6/m44` ("dead id → zero-value object") but a different mechanism: m44's `NilOnError` nulls a whole object on resolver _error_; this is one _field_ inside a successfully-resolved object whose own zero value is a legitimate unset state. Separately, and independently root-caused: creating a service from "+ New Service" launched from inside a Project's own page, with that Project pre-selected and Environment left unset, silently never joins the project (`projectId` comes back `""`, the resource never appears on the project's page) — confirmed intentional architecture (Project membership flows only through Environment assignment, per `w1/m32`/`w5/m25`), but the shared `ProjectEnvironmentSelector`'s hint text ("Selecting an environment also adds the resource to its project") wrongly implies picking a Project alone already did something; used by all three of New Service, New Key Value, and New Database's create dialog. — **done 2026-08-23** (`gqlutil.OptionalStrField` migrated onto all 4 call sites; live-verified for `service`/`postgres`/`keyValue` REST/GraphQL parity and the corrected create-form copy; `envGroup` itself and the env-groups UI click-through verified by automated tests proven red/green rather than live, since no reachable environment this session had OpenBao configured — see `done/m48/README.md`'s Closeout evidence for the residual live-verification debt), moved to `done/m48/`.
- [x] **m49** — Duplicate-name creation errors discard the backend's specific conflict reason (extend w4/m19's services-only fix) (10 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-23 (run from a `/loop 30m` session, adversarial-input testing angle): `w4/m19` (done 2026-07-14) built and proved clean 409-plus-inline-message-plus-suggested-name handling for duplicate service names, but scoped to services only. Live-confirmed the sibling create flows never got the same treatment: creating a Key Value, Postgres instance, or Project a second time with an identical name returns a specific backend conflict message over GraphQL (`"a key-value store named \"...\" already exists in this workspace"`, `"a Postgres database named \"...\" already exists in this workspace"`, `"project: already exists"`), but the dashboard discards it for a generic "Couldn't create X. Please try again." toast — `use-create-key-value.ts`/`use-create-database.ts` extract the message only to check for the unrelated cap-limit case, and `use-create-project.ts` uses a bare `catch {}` that never reads the message at all. Three more hooks (`use-create-environment.ts`, `use-create-api-key.ts`, `use-create-registry-credential.ts`) share the identical bare-catch shape but weren't independently live-tested this run. `use-create-service.ts` (the correct control case) and `use-create-workspace.ts` (a different but also-correct passthrough) are unaffected and are this milestone's regression-test targets. The backend itself uses three inconsistent message shapes under two different `ErrConflict` sentinels, so the fix's first task designs a stable, backend-copy-independent conflict signal (the codebase already has a documented precedent for this exact problem — `graphql-error.ts`'s `extensions.code`-based `hasGraphQLErrorCode`/`planLimitExtensions`, used for `PLAN_LIMIT`/`RATE_LIMITED`) before any per-type fix lands. — **done 2026-08-24** (found the exact-fit existing mechanism, `core.NewConflictError`/`CodedError`, already used for `PLAN_LIMIT`/`BLUEPRINT_GROUPING_LIMIT` — no new backend plumbing needed; migrated keyvalue/postgres/project/environment/services onto it, adding the attempted name to project/environment's previously name-less messages; all four fixed dashboard hooks + the services control case live-verified via Playwright against dev-7; MCP parity concretely proven via two in-process `tools/call` tests; api-keys/registry-credentials confirmed not-applicable (no uniqueness enforced); `/simplify` graduated the four hooks' identical conflict-branch into one shared `conflictOrGenericMessage` helper; ADR018 updated; full backend/dashboard gates green), moved to `done/m49/`.
- [x] **m50** — Blueprint sync failures vanish; global search fuzzy-matches noise from resource IDs (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-23 (run from a `/loop 30m` session, adversarial-testing angle: copy-to-clipboard accuracy checked clean across Key Value/Postgres connection strings and a masked deploy hook URL, then two real findings). Blueprint `blp-d9nqg95cavls73fp8m10` (`discourse_docker`) carries 9 unexplained `Error` sync rows spanning 2026-08-04 to 2026-08-21 in its Sync History table with zero detail anywhere in the UI; traced to `store.BlueprintSync` (`lego/backend/internal/store/blueprints.go:48-55`) having no error column at all and `UpdateBlueprintSync` taking no error parameter — both write call sites in `apps/blueprint.go` (`CreateBlueprint` ~line 452, `runSync` ~line 666, the latter shared by manual REST/GraphQL/MCP sync and webhook-triggered auto-sync via `triggerBlueprintSync`) compute the real `applyErr` and discard it before persisting. Separately: the workspace's Cmd+K global search (`global-search-dialog.tsx`) feeds cmdk's default fuzzy scorer a composite `${name} ${id} ${type}` string per resource: because each resource's ~20-char near-random id is in that string, short realistic queries subsequence-match unrelated resources' ids — live-measured `db` returning 9 items (3 correct) including two Projects with no `db` substring anywhere, `cms` returning 9 (2 correct), `eden` returning literally every resource in the workspace (6/6); a nonsense control query correctly returned zero, confirming the filter runs but is far too permissive. `combobox.tsx:244-71` already has the correct fix pattern in this same codebase (`shouldFilter={false}` + explicit substring `.includes()`); `w9/m92/t002` fixed the identical "search doesn't narrow" symptom for the Agents sidebar via a different mechanism (a hand-rolled fuzzy matcher, not cmdk) — confirmed via grep that cmdk's `Command` is used in exactly one other place in the dashboard, so this is a sibling instance of the same lesson, not a regression of that fix. — done 2026-08-25 — blueprint sync failures now persist a human-readable reason (migration `0098`) through REST/GraphQL/MCP into the dashboard's Sync History table, and Cmd+K global search filters by literal case-insensitive substring instead of cmdk's fuzzy scorer (which subsequence-matched `db`/`cms` against near-random resource ids and returned most of the workspace). Live leg carried to `040`, moved to `done/m50/`

- [x] **m51** — Settings-page spec changes (Start Command et al.) never create a deploy-history row, so a failed one strands the service in stuck Building (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-23/24 (run from a `/loop 30m` session, investigating whether a manual Restart action's zero-downtime claim holds — it does, deliberate full-rebuild design, not a bug — while setting up that test fixture): editing a service's Start Command via Settings persists correctly (`setStartCommand` confirmed via direct GraphQL query) but creates **no** `deploys` row — `SetCommands` (`lego/backend/internal/apps/service.go:3137-3160`) patches `App.Spec` via `patchFetched` and returns, never calling the `deploys` package's create-deploy path, unlike `triggerDeploy` (used by Manual Deploy and, since `w2/m30`, Restart). Live-confirmed consequence: a broken Start Command (`./app --qa-simple-test`, an unrecognized flag) caused a real rebuild+redeploy — Events showed `Instance failed` — but with zero tracked deploy, `service.phase` stuck reporting `Building` for 2+ minutes; a _second, valid_ Start Command correction, itself confirmed persisted, did **not** self-heal the stuck phase — only an explicit `triggerDeploy`-routed Restart recovered it. `w2/m30` (done 2026-07-14) deliberately fixed this exact tracking gap for the Restart action specifically but never extended it to Settings-page mutations. Candidate same-shape siblings by field type (not each independently live-verified — t001 confirms): `SetRootDir`, `SetDockerfilePath`, `SetPreDeployCommand`, `SetSourceAndRegistryCredential`. **A later same-run iteration (2026-08-24) confirmed the pattern isn't confined to `apps.Service.SetXxx` at all**: linking an Environment Group to a service (`envgroups/service.go:947-957`'s `linkFetched`) forces the identical untracked rebuild via a completely different package and mutation — filed as new task t008 with a 16-call-site grep of every direct App-CR patch outside the tracked `deploys` path, for t002's fix scope to cover. Separately investigated and correctly rejected: an env var value the app process deliberately echoed appeared unmasked in runtime logs (expected — no platform, including Render, redacts app-echoed secrets from raw log output; the dashboard's Environment tab reveal-on-demand masking, the only real guarantee here, was confirmed correct); env var precedence when the same key is set both directly and via a linked group (direct value correctly wins at runtime, matching Render's convention). — done 2026-08-25 — the operator's own artifact/release identity table moved into the CRD contract (`appv1alpha1.SpecRollsRelease`) and now gates deploy-history writes, so every config change that actually rolls a release — Settings fields, env vars, secret files, env-group link/unlink — opens a real `config_change` deploy row instead of rebuilding invisibly. Two premises in the filed report were wrong and both mattered: "any spec change re-enters the build path" is Restart-specific (half the `SetXxx` family is operational, and a blanket gate would have minted a phantom deploy per rename), and the "stuck Building" phase was unobservable rather than unbounded (900s operator deadline + 18m control-plane gate already cap it). Live leg carried to `040`, moved to `done/m51/`
- [x] **m52** — Canceling a first-ever deploy misreports the service as Failed, not Canceled (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-24 (run from a `/loop 30m` session, testing Cancel during the earliest possible deploy phase — clone/setup, before the build step starts): canceling a fresh service's very first deploy at the earliest observable phase (`Created`, `==> Build queued`) leaves the deploy detail page correct throughout (`Deploy status: Canceled`, timeline, logs all agree), but the service header's status pill settles on **Failed** and stays there. Confirmed not a UI-only staleness bug via a direct GraphQL query: `deploy(...).status` returns `"canceled"` while `server(...).phase` returns `"Failed"` — the backend itself disagrees with its own deploy record. Root cause: `lego/operator/internal/controller/app_controller.go:552-573`'s `settleCanceledRelease` (reached when a build is canceled and `app.Status.Image == ""`, i.e. no prior successful release exists) sets `app.Status.Phase = appv1alpha1.PhaseFailed` — the Condition it also sets is correctly `Reason: "BuildCanceled"`, but the coarse Phase enum has no distinct value for "user canceled, no release yet," so it reuses the same value a genuine build error produces; the dashboard's `ServiceView` projection (`dashboard/src/features/services/lib/status.ts`) never carries the Condition Reason at all, only the raw phase, so nothing downstream could disambiguate even if it tried. Single call site, reached uniformly by the generic App reconcile loop — affects all 5 App-typed service kinds (web, private, worker, cron, static), one mechanism not five. Postgres/Key Value backup restore was also investigated this iteration and correctly not filed — plan-gated (Free tier has no backups/recovery at all; testing it would require a paid upgrade, off-limits per production safety rules). — done 2026-08-25 — a canceled first-ever deploy reports the new `PhaseCanceled` instead of reusing `PhaseFailed`, so `server.phase` stops contradicting its own `deploy.status` and the header shows a neutral "Canceled" rather than a red "Failed" for something the user did on purpose. Render has no service-level status at all (its header renders the latest deploy), which is exactly why bex's `phase` — a documented bex-only extra — was free to drift; a genuine build failure still reports `Failed`. Live leg carried to `040`, moved to `done/m52/`
- [x] **m93** — Postgres internal connection-string host mismatch; log live-tail resume cursor (6 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 2026-08-25 — done 2026-08-25. **t001 real, fixed:** `PostgresConnectionInfo` echoed CNPG's raw `uri` (2-label host) while the psql command, pooler and per-replica strings on the same response all used the operator's canonical `.svc` `Status.Host`; it now derives from `Status.Host` too. The filed "both hosts must always match" criterion was corrected — that holds only for a private database, since a public one deliberately points psql at the external host. **t002 disproved, not implemented:** its premise (an SSE replay collides in the same millisecond and defeats the client dedupe) is backwards — same ms + same pod + same message is an _identical_ `logLineKey`, exactly what the dedupe drops, and a replay re-parses the same bytes through the same code so it cannot differ; the live-on/live-off control also compares an accumulating buffer against a bounded window. Triage did find a **different real defect** at the same call site: `NewPodLogStream` followed with no lower bound, so every subscribe re-read the pod's whole log from offset 0 and `LogQuery.Since` was honored only by discarding lines after kubelet had already shipped them. That bound now reaches `PodLogOptions.SinceTime`, SSE frames carry `id:`, and `Last-Event-ID` resumes the window. Two self-inflicted bugs were caught by test en route: an unindexed format verb that would have appended `%!(EXTRA …)` to every NDJSON line, and an empty `id:` on a stamp-less line, which per the SSE spec resets the client cursor and would have silently disabled resume. Live leg carried to `040`, moved to `done/m93/`
- [x] **m94** — Auto-hibernate/wake Ingress-backend race can leak a raw Traefik error to the first request (6 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 2026-08-25 — done 2026-08-25. The filed race is real and confirmed in code (the Deployment scale-to-0 write precedes the Ingress swap to the activator; non-atomic, ingested independently by Traefik). Triage also settled t001's open step 4 and found a **worse sibling on the resume path**: the activator answers `Retry-After: 5` and stamps `lastActive`, so the next reconcile hands the route back to a Deployment whose pods are still starting — a client retrying at exactly the advertised second hits an endpoint-less Service and gets the same raw `no available server`, close to deterministic for any app slower than ~5s to start. Both fixed: the activator now holds the route whenever the App has no ready endpoint (not merely once the idle TTL elapses), and the hibernate pass writes the route, keeps the pods, and requeues 10s before draining — reordering alone was rejected because Traefik's propagation is unobservable to the operator, so ordering can only narrow the window, never close it. Maintenance mode is exempt from the hold (caught by an existing test — holding would have stopped it hibernating forever); manual suspend and workers untouched. A pre-existing test that asserted the racy contract now requires a ready pod first. `gocyclo` caught the additions pushing `reconcileKubernetes` 30→36; folding them down plus extracting the disk/snapshot/restore sequence left it at 28. Live leg carried to `040`, moved to `done/m94/`
- [x] **m95** — A fresh service's first deploy can hang at `build_in_progress` forever with no backing build job (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co` hosting surfaces, 2026-08-25. Reproduced 3/3 on independent fresh web services (Docker runtime, `examples/hello-go` as `rootDir`/`dockerContext` against the public `bex-co/bex` repo, Free plan, across both GitHub-App and Public-Git-URL source methods): the deploy row sits at `build_in_progress` with `updatedAt` frozen byte-identical to `startedAt` for minutes, while `GET /v1/logs/subscribe?resource=<id>&type=build` returns `{"error":"no running build is available to follow"}` (`ErrBuildNotRunning`, `lego/backend/internal/logs/service.go:70`) — meaning no build pod exists at all, confirmed against a real, unrelated production service (`eden-dash-v3`) genuinely mid-build at the same time as the live control case. `lego/backend/internal/store/reconciler.go:867-922`'s `observedDeployStatus` is a faithful projector of the App CR's own `status.Phase == PhaseBuilding` — the deploy row is not lying, the operator-side App CR itself is stuck. Root cause needs cluster/operator-log access this hunt didn't have; candidate mechanisms and the plausible third-recurrence lineage (`2da39122`, `2f9db5d4`, both already deployed) are in the milestone README. Canceling a stuck deploy separately reproduced the already-fixed-but-undeployed `w6/m52` (`phase: "Failed"` instead of `"Canceled"`) — confirms `w6/040`'s diagnosis, not re-filed.
- [x] **m96** — Fix Render OpenAPI compat gate rejecting every REST Blueprint-id route (blueprintId pattern mismatch) (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 2026-08-25/26 (4th run of the day). `internal/api/render_openapi.go` validates every REST request against the embedded Render OpenAPI spec, which pins `blueprintId` to Render's own historical shape (`^exs-[0-9a-z]{20}$`); bex mints Blueprint ids as `blp-…` (`internal/id/id.go:79`) and always will, so `GET/PATCH/DELETE /v1/blueprints/{id}` and `GET /v1/blueprints/{id}/syncs` 400 unconditionally for every real id, live-confirmed against a real production blueprint (`blp-d9nqg95cavls73fp8m10`, `discourse_docker`). The dashboard is unaffected (GraphQL/MCP bypass this validator entirely and were confirmed still correct), but any REST-only Render-API-compatible client cannot ever call these four operations — silently false against `w2/m62`'s own closed Definition of done, which was verified only through a bare-mux test that never exercises the real validator. Exhaustive cross-check of every pattern-constrained id path parameter in the Render-intersected REST surface found this is the _only_ mismatch (disk/webhook/event/job ids all already match Render's pattern). **Done 2026-08-26 — three defects, not one:** the `blueprintId` pattern was real, and driving these routes through the real composed server (the coverage gap the milestone was filed to close) found two more that each alone would have kept them unusable — Render's four blueprint operations declare no `ownerId`, which every bex handler reads, so the strict-query gate 400'd any call naming a workspace; and every blueprint verb returned its raw store error, so a missing or other-workspace id came back `500 internal error` rather than 404 on REST, GraphQL and MCP alike. All three fixed; live REST verification carried to `w6/040`.
- [ ] **m97** — Stop the codex-security F7 protected-secret guard from self-rejecting every App's own clone/pull secret (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 5th run of the day, 2026-08-25/26. Creating a Web Service from `bex-co/bex` (the dashboard's default "GitHub" source tab) failed its first deploy in under a second with `"app references protected operator Secret \"tea-…-qa-20260825-web-clone\" which tenant workloads may not mount (codex F1)"` (REST and GraphQL agree byte-for-byte — not a parity bug). Root cause: `9b942c46`'s "codex-security 2026-08 F7" extended `rejectProtectedSecretRefs` (`lego/operator/internal/controller/app_controller.go:1279`) to also refuse `spec.CloneSecret`/`spec.ExternalRegistryPullSecret`, but the backend's own deploy pipeline mints and sets `spec.CloneSecret` to a Secret it creates and labels protected on purpose (`cloneSecretName(app) = app + "-clone"`, `lego/backend/internal/apps/clonesecret.go:52`) — the guard cannot tell "an App naming its own deterministic clone secret" from "an App naming a different protected Secret to exfiltrate it," so it now rejects both, unconditionally, for every repo the workspace's GitHub connection covers. Confirmed live (not deploy-lag): the failing deploy's own auto-pin commit message names `03226396`, a descendant of `9b942c46`, and `717143ae` (the last GH-Actions-green commit) provably predates the F7 addition — the pipeline pins platform images past a later commit even when the overall workflow run is marked cancelled/failed by an unrelated later stage (`w6/040`'s opensandbox-controller build). Control case live-verified: a service built from an unconnected public repo (`octocat/Hello-World`) reached `build_in_progress` with no rejection, isolating the defect to `CloneSecret`/`ExternalRegistryPullSecret` only. Exhaustive grep found 4 backend call sites setting `spec.CloneSecret` (create, webhook redeploy, blueprint-sync redeploy, manual/hook/rollback trigger) — every resource type that can carry `spec.Repo` (web/private/worker/cron/static) is affected, 100% of the time, self-inflicted. A sibling bug in `spec.ExternalRegistryPullSecret` (same guard, same self-reference pattern via `lego/backend/internal/registrycreds/pullsecret.go:48`) is reasoned from code but not live-reproduced (no registry credential configured in the QA workspace). **Independently re-confirmed live in the 14th run (2026-08-26):** the exact same failure reproduced on a fresh service, plus a second angle folded in as t007 — an already-Live standing service (`beancount-forum`, `srv-d9nqg9dcavls73fp8m2g`) is bounced to `phase: "Failed"` by an unrelated later reconcile (no new deploy), and the dashboard's Events feed mislabels the cause as a generic readiness-check failure (`lego/backend/internal/store/reconciler.go:1232-1281`'s `observedServiceStateFor` collapses any non-excluded Ready=False reason into `readiness_failed`).
- [x] **m98** — Fix billing "Total month to date" collapsing to $0.00 when Stripe credit grants absorb real usage charges (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 6th run of the day, 2026-08-25/26. On `/billing` for the QA workspace (pro plan, $1000 Stripe credit grant "superadmin's privilege" active), the Charges category tree summed to ~$74.74 (Services $23.01 / Postgres $26.57 / Key Value $21.41 / Sandboxes $3.75) but "Total month to date" read $0.00 USD — confirmed via an in-page authenticated `fetch` of the `Usage` GraphQL query: `estimatedCost.totalUsd="74.78"` vs. `billing.currentCost.amountUsd="0.00"` for the identical workspace/period, with `billing.credits.availableUsd="1000.00"`. Root cause: `lego/backend/internal/billing/read.go:265` (`currentInvoice()`) reads Stripe's `Invoices.CreatePreview` `Total` field, which nets out Billing Credit Grant consumption on the same preview call (`docs/ADR071-tenant-billing-credits.md`'s own mechanism) — exactly the "invoice total is unreliable under flexible/credit billing" caveat the codebase already documents and works around for the separate `Credits` block (`read.go:44-46`) but never applied to `currentCost`. The same value also drives `CreditBalanceCard`'s "Credits applied → amount due" line (`dashboard/src/features/usage/components/billing-summary-cards.tsx:59-88`), so it reads "$0.00 applied" in the exact period ~$74.78 of credit was actually consumed — two dashboard readouts broken by one backend value. Exhaustive blast radius: exactly 2 call sites share this pattern (`currentInvoice()` line 265, `finalizedInvoices()` line 288); GraphQL-only, no REST/MCP exposure (grep confirmed). Deliberately not a confident one-line `Total`→`Subtotal` swap — Stripe's own Billing Credit Grants semantics may apply credit before `Subtotal` is even computed, so the milestone opens with a spike task to settle the real Stripe field semantics via a live test-mode experiment before landing the fix. **Done 2026-08-26 — live-verified on production** (19th `/qa-find-bugs` run): the fix is a confirmed ancestor of the successfully-deployed `8bbc8763`; the headline now reads a real "$29.47" instead of "$0.00" against a $78.40 estimate, with "Credits applied" correctly nonzero. The DoD's exact-match bullet doesn't literally hold (Stripe's rated figure lags bex's real-time estimate by design — ADR040's seal-then-emit meter window) — recorded in `done/m98/README.md` as expected skew, not a reopened defect. Moved to `done/m98/`.
- [ ] **m99** — Auto-Deploy hint and reported state lie about push-deliverability for a repo the connected GitHub App doesn't grant (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 7th run of the day, 2026-08-25/26. Created a Web Service via the **Public Git URL** tab (the documented `w6/m97` workaround) from a personal repo the workspace's `bex-co` GitHub App installation does not cover; pushed a real second commit and waited over 90s — the Deploys tab never auto-triggered a new deploy (only a manual "Deploy latest commit" did), yet Settings → Build & Deploy showed "Auto-Deploy: On Commit … redeploys automatically via the GitHub app," and MCP's `get_service` (first exercised this run, over `POST /mcp` with the QA browser session) agreed: `"autoDeploy":"yes","autoDeployTrigger":"commit"` — REST/GraphQL/MCP all agree with the false claim. Root cause: `dashboard/src/features/services/components/build-deploy-section.tsx:135`'s `viaGitHub = !!connection?.connected && /github\.com[/:]/i.test(repo)` checks only that some GitHub connection exists and the repo string looks like a github.com URL — never whether THIS repo is actually in the connection's grant — while the backend's stored `autoDeploy` boolean (`service.go:2342`, correctly on-by-default per Render parity) is reported unconditionally over REST/GraphQL/MCP with no per-repo deliverability check at all, even though the exact grant-match signal (`GitHub.CloneToken`'s `ok`) already exists and runs on every deploy trigger via `clonesecret.go`. `w2/m9`'s own shipped verification tested only the granted-repo case, never this one. Intersects `w6/m97`: that milestone's workaround explicitly routes users toward exactly the repo shape (GitHub-connected workspace + an ungranted github.com URL) that mis-reports here. **BLOCKED 2026-08-28 — code done and deployed; only the live sweep is owed.** Two external preconditions, neither a code change: (1) QA credentials — `scripts/qa-login.sh` exits 2, `QA_EMAIL`/`QA_PASSWORD` are absent from `.env` (empty names live in `.env.example:423-424`) and `BEX_API_TOKEN` 401s against `api.bex.co`; (2) for m110 only, the fix must ship first. See `040.md` for the pipeline state.
- [x] **m100** — Fix App CR generation bookkeeping so a deploy queued behind a build-failed sibling is never permanently stranded at `queued` (6 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 9th run of the day, 2026-08-25/26. Triggering a second deploy while a service's first deploy is still `queued`/`build_in_progress`, then letting the first fail its build, leaves the second permanently stuck at `status:"queued"` with empty `startedAt`/`finishedAt`/`failureReason` and a frozen `updatedAt` — confirmed via direct GraphQL polling over 185+ seconds (well past the ~40-90s a solo deploy takes to leave `queued`), with zero `build_started`/`build_ended` events ever recorded for it. Only a manual Cancel unsticks it; nothing does so automatically. Root cause: `lego/operator/internal/controller/app_controller.go:3761-3765`'s `setNotReadyCondition` (the shared body behind `r.fail`) stamps the Ready condition's `ObservedGeneration` from the App's raw current `metadata.generation` instead of the release generation the failing build was actually pinned to — so when a second deploy's spec patch has already bumped the generation before the first's build fails (an ordinary race, not `PodFailurePolicy`-specific: `BackoffLimitExceeded`/`DeadlineExceeded` share the same exposure), `terminalBuildFailureRecorded` (line 881-886, gating re-dispatch at line 681) wrongly concludes the _newer_ generation already failed terminally and halts the reconcile before `app.Status.ReleaseGeneration` can ever persist forward — stranding the newer generation with no further reconcile ever scheduled. The backend's `supersededDeployStatus` (`lego/backend/internal/store/reconciler.go:1058-1060`) then correctly-by-design waits forever on the broken assumption that the operator will eventually advance. Blast radius: 8 backend call sites share the identical `patchApp`/`stampReleaseGeneration` shape (manual deploy/deploy hook, rollback, create, redeploy/webhook auto-deploy, blueprint sync, restart, env-var/secret-triggered restart, env-group batch attach/detach) — any two racing the same way reproduce it. Live-verified control case: the same race resolved correctly when the blocking deploy was explicitly _canceled_ by a fresh trigger rather than left to fail on its own, isolating the bug to the build-failure resolution path specifically.
- [x] **m101** — Webhook payload `serviceName` ignores `displayName` after a service rename (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 10th run of the day, 2026-08-25/26. A QA webhook subscribed to all events, pointed at a real HTTPS sink, captured a live `commit_ignored` delivery for the real production service `eden-dash-v3` (`srv-d9ndt8hmcglc739fkp50`) whose signed payload read `"serviceName":"block-eden-mono"` — the service's pre-rename, immutable name — while the dashboard/GraphQL (`service(id:...){name displayName}` → `{"name":"block-eden-mono","displayName":"eden-dash-v3"}`) show `eden-dash-v3` everywhere else. The webhook's HMAC signature was independently re-derived and matched exactly, so signing itself is correct; the bug is the payload's content. Root cause: `lego/backend/internal/apps/service.go:3588-3598`'s `SetDisplayName` patches only the k8s App CR ("displayName is not owned by the control-plane row projector"), while `lego/backend/internal/store/webhooks.go`'s `webhookEventsQuery` (4 of 5 `UNION` arms) selects the immutable `a.name` straight from Postgres — a column the store never updates on rename (exhaustive grep: zero `UPDATE apps SET name` anywhere, and no `display_name` column exists at all), so the drift is permanent and structural, not a caching lag. Affects every App-CR-backed resource type (web/private/worker/cron/static) identically; Postgres/Key Value are unaffected (no displayName/rename feature exists for them).
- [ ] **m102** — Hydration mismatch (#418) in `formatRelativeAge`/`formatRelativeUntil` across 15+ components (7 tasks) ← from the same hunt, 2026-08-25/26. Loading `/webhook/<id>` right after a delivery landed ~55s earlier logged a live `Minified React error #418` (saved: `.playwright-mcp/qa-webhook-react418-console.log`), traced to `dashboard/src/features/webhooks/components/webhook-deliveries-card.tsx:317-320` calling `dashboard/src/features/services/lib/format.ts:25-46`'s `formatRelativeAge` (defaults `now = Date.now()`, evaluated independently at SSR-render time vs. client-hydration time — any elapsed-time bucket boundary crossed in between mismatches). Not a duplicate of `w6/030` (open note; its own fix already landed, confirmed live via `env-group-metadata.tsx:50-59`'s `suppressHydrationWarning` + explicit `w6/030` comment) — that bug is `formatDateTime`'s timezone-of-instant divergence, a distinct mechanism from this one's elapsed-time-since-render divergence, same generic React #418 code. Exhaustive grep of all 27 `formatRelativeAge`/`formatRelativeUntil` call sites found only 2 (in 2 files) incidentally guarded as a byproduct of unrelated work; the other 22 across 15 files (full list in the milestone) are unguarded and share the identical live-reproducible defect. **BLOCKED 2026-08-28 — code done and deployed; only the live sweep is owed.** Two external preconditions, neither a code change: (1) QA credentials — `scripts/qa-login.sh` exits 2, `QA_EMAIL`/`QA_PASSWORD` are absent from `.env` (empty names live in `.env.example:423-424`) and `BEX_API_TOKEN` 401s against `api.bex.co`; (2) for m110 only, the fix must ship first. See `040.md` for the pipeline state.
- [ ] **m103** — Fix i18n language-switch regression and the SSR i18n singleton race causing hydration mismatches for non-English sessions (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 11th run of the day, 2026-08-25/26. Switching the dashboard's language via the account menu ("P" avatar → Language → 中文 — the only switcher reachable once signed in) left the page fully in English immediately after the click even though `i18n.language`/the persisted `i18nextLng` cookie were already `"zh"`; a full reload then rendered correctly in Chinese but logged a live `Minified React error #418` on every single subsequent reload (English, the default, reproduced zero errors — confirmed control case). Two distinct root causes: **Bug A** — `dashboard/src/common/components/user-nav.tsx:96-98`'s `handleLanguage` is the only one of 4 real `changeLanguage(` call sites in the app that skips `await ensureLanguage(lang)` first (the `w9/m60`-shipped lazy-load contract every other site follows), so it switches `i18n.language` to `"zh"` without the catalog loaded, silently falling back to English text — masked in tests because `dashboard/src/test/setup.ts:34-38` globally preloads the zh bundle for every test run. **Bug B** — `dashboard/src/i18n/init.ts:17-29` is one process-wide i18next singleton with zero per-request isolation (no `cloneInstance()`/`createInstance()` anywhere in the app; no custom SSR entry point), mutated per-request by `dashboard/src/routes/__root.tsx:19-36`'s `beforeLoad` — under real concurrent traffic a different request's `changeLanguage("en")` can interleave with this request's `changeLanguage("zh")` before HTML serializes, so the emitted markup can reflect whichever request won the race, independent of that request's own cookie — the exact "SSR renders English, hydration corrects to zh" signature reproduced live, and blast-radius-sitewide since every route's `head()`/title resolution reads the same ambient singleton. Distinct from `w6/m102` (elapsed-time-since-render race, unrelated mechanism) and possibly (unverified, to be re-checked) the same as the still-unrooted `w6/030`.
- [x] **m104** — Cancel on an image-backed service's deploy never reaches the App CR — rollout continues and phase never settles (10 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 12th run of the day, 2026-08-26. Canceling a fresh image-backed Background Worker's crash-looping first deploy (`docker.io/library/nginx:alpine`, `chown` failure under bex's non-root tenant sandbox) correctly closed the deploy row `"canceled"`, but the service's own `phase` (REST and GraphQL agree) stayed `"Deploying"` forever, and the underlying pod kept crash-looping in the background unaffected — proof Cancel had zero effect on the App CR. Root cause: `lego/backend/internal/deploys/service.go:661`'s `if a.Spec.Repo != ""` guard around the `AnnotationCanceledReleaseGeneration` write means an image-backed App (`spec.Image` set, `spec.Repo` empty) never gets the marker `w6/m52` (done, 2026-08-24) relies on to reach `settleCanceledRelease` — so m52's own fix (`PhaseFailed` → `PhaseCanceled` for a canceled release with no prior image) is unreachable for the entire image-backed dimension of the same "5 App-typed service kinds" blast radius m52 claimed to cover. Confirmed recoverable, not a permanent brick: a subsequent real deploy (`setImage` to a working image) converged normally to `Running`/`Live`. Also confirmed this run (deploy-lag, not filed as a bug): the new "source switching in settings" feature (`fa7b9993`, `.pm/w5/done/m76`) is not yet deployed to production — live GraphQL's `setRepo` mutation still has the pre-fix `(id, repo)` signature (no `branch` arg), and the live settings page still renders the pre-m76 "Change source type" UI.
- [x] **m106** — Service `ipAllowList` sits at the wrong REST nesting level: root, not `serviceDetails` (6 tasks) — done 2026-08-26 (triaged **real** against Render's pinned OpenAPI: `ipAllowList` is a `webServiceDetails`/`staticSiteDetails` property and is absent from the top-level `service` schema entirely. Dropped the top-level `renderService.IPAllowList` field outright and emit it from `renderServiceDetails()`, gated via `TypePubliclyRoutable(svcType)`, omitted when empty — mirroring the sibling `disk` block. One correction to the finding: MCP was **not** a separate code path — `get_service`/`create_*`/`list_services` share `toRenderService`, so the same change fixes MCP too, matching Render's REST-proxying MCP; GraphQL is genuinely separate and unaffected. Tests assert nested `serviceDetails.ipAllowList` on create-response + GET + all three surfaces, the root key is absent, and private/worker/cron omit it; ADR018 row corrected. `go test ./internal/api ./internal/apps ./internal/core ./internal/events` green, gofmt/vet clean; moved to `done/m106/`) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 16th run of the day, 2026-08-26. The dashboard's "Networking" section (inbound CIDR restriction) works end to end — a saved CIDR persists and real out-of-list traffic gets `403 Forbidden` from a genuine Traefik `Middleware` — but REST's response places `ipAllowList` at the top level of the service object instead of inside `serviceDetails`, where Render's own pinned OpenAPI schema (`lego/backend/internal/api/openapi/render-public-api-1.json`, `webServiceDetails`/`staticSiteDetails`) declares it. Live-confirmed via REST POST create + GET on fresh `qa-` fixtures (all deleted during cleanup) and cross-checked against GraphQL (`ipAllowListEntries`), which correctly nests it. Root cause: `lego/backend/internal/apps/render.go:150-157` declares `IPAllowList` as a top-level struct field (populated at line 286), while `renderServiceDetails()` (`render.go:293-360`) never adds an `ipAllowList` key — the exact same bug class already fixed once in the same function for `Disk` (`w1/m86`, comment at `render.go:346-353`). GraphQL and MCP are unaffected (separate code paths, confirmed by grep); the one existing test (`TestRESTCreateWithIPAllowListAndReadBack`) asserts the wrong (top-level) shape, encoding the bug rather than catching it. `docs/ADR018-render-parity.md:116`'s "REST: … proven through unmodified CLI v2.21.0" claim is inaccurate as written.
- [x] **m107** — `formatDateTime`/`formatDateLong` display the SSR container's clock, not the viewer's, on every fresh page load (9 tasks) — done 2026-08-26 (triaged real via a React-19 hydration probe: `suppressHydrationWarning` freezes the server's UTC value on screen — 0 recovered errors — while the un-guarded node self-corrects to local + logs #418, so the `w6/030` guard was itself the freeze. Fixed with `useIsHydrated` (`useSyncExternalStore`) + `LocalDateTime`/`LocalDate` components + `useLocalDateTime`/`useLocalDate` hooks; migrated all 13 documented sites plus 3 `formatDeployTimestamp` render consumers the grep missed — every render-body `suppressHydrationWarning` on a timestamp removed. Deterministic hydration regression test asserts no clock in SSR markup, correct local text post-hydration, zero #418, under `TZ=America/Los_Angeles`; typecheck+lint+2697 tests green. Live prod `page.goto` spot-check deferred — no prod browser this session; the hydration unit test is a stronger substitute. Moved to `done/m107/`), ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 17th run of the day, 2026-08-26, found while live-verifying `w6/m97`. A fresh Environment Group's detail page showed `Created: August 26, 2026 at 1:28 PM` right after creation (correct, client-rendered) but `Created: August 26, 2026 at 8:28 PM` on a fresh full-page load of the identical page for the same instant (`createdAt: 2026-08-26T20:28:01Z`, viewer timezone `America/Los_Angeles`) — the raw UTC clock digits mislabeled as local, not a formatting-style difference. Root cause: `dashboard/src/common/lib/format/format-date-time.ts:10-16,25-31` (`formatDateTime`/`formatDateLong`) call `date-fns`'s `format()`, which reads the runtime's own local timezone — Node's (effectively UTC) during SSR, the browser's during CSR — with no pinning and no render-time gate. `w6/030`'s `suppressHydrationWarning` treatment (confirmed live in `env-group-metadata.tsx`) only silences the console error; it does not correct the displayed value, which stays wrong until an unrelated re-render happens to occur — a materially different, more severe defect than `w6/030`'s filed scope ever claimed. Control case verified as hard as the failing one: a deploy detail page's identical `formatDateTime` usage showed the correct time on fresh load only because `deploy-header.tsx` renders a loading `Skeleton` until its query resolves client-side — an accidental workaround, not a fix. Exhaustive blast radius (13 call sites, 10 files): 3 completely unguarded (live #418 + wrong value — `recovery-panel.tsx`'s Postgres point-in-time-recovery boundaries are the highest-stakes instance), 3 guarded-but-still-wrong (`env-group-metadata.tsx`, `audit-event-row.tsx`, `routes/env-groups.tsx`), 1 masked-by-accident (`deploy-header.tsx`), 2 attribute-only/self-healing (`services.$serviceId.events.tsx`), and 3 `formatDateLong` siblings sharing the same cause (narrower day-boundary manifestation, unverified live this run). A third, independent root cause under the generic React #418 code, distinct from `w6/030` (console-error-only) and `w6/m102` (elapsed-time-since-render, not timezone).
- [x] **m108** — Env group Patch rollout permanently reports failure once a linked service is deleted (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 18th run of the day, 2026-08-26. — done 2026-08-26 (triaged real: `patch.go`'s rollout loop now tolerates `core.ErrNotFound` for a since-deleted linked service — excluded from `failedServiceIds`/`affectedServiceIds` so `rolledOut` is true again — and `pruneStaleLinks` self-heals the group's `meta.links`; dead `rollLinked` removed; regression tests cover both `deploy`+`rebuild` modes and preserve genuine-failure behavior; REST/GraphQL/MCP share the one `PatchEnvironment` so parity + the dashboard alert follow by construction. Verified via `go test ./internal/envgroups/...`; live-cluster repro not run this session — see `done/m108/README.md`), moved to `done/m108/`. Linked a fresh Environment Group to a fresh test service, deleted the service, then edited the group's variable and clicked "Save and deploy" — the dashboard rendered a persistent "Configuration saved; rollout incomplete" alert with a "Retry rollout" button that can never succeed. Live GraphQL probe: `patchEnvGroupEnvironment(..., saveMode:"deploy")` returned `{"rolledOut":false,"failedServiceIds":["srv-…"]}` citing the already-deleted service. Root cause: `lego/backend/internal/apps/service.go:2080`'s `Service.Delete` never removes a deleted service's id from any Environment Group's persisted `meta.links`, and `lego/backend/internal/envgroups/patch.go:273-293`'s rollout loop calls `rollOne`/`RebuildService` directly for each linked id without the `core.ErrNotFound` tolerance `rollLinked` (dead code, never called) already establishes — so a stale link permanently pollutes every future save-and-deploy/save-and-rebuild for that group, masking whether the group's other, real linked services actually rolled out. Breaks the documented guarantee in `docs/ADR006-bex-api.md:533` ("rollout-only failures are retryable") for this specific case, since the failure is permanent, not transient. REST/GraphQL/MCP share one implementation (`PatchEnvironment`), confirmed unaffected by divergence; blast radius is exactly one call site (`patch.go:287`), both `deploy` and `rebuild` `SaveMode` branches.
- [ ] **m109** — Postgres/KeyValue REST+MCP omit Render-required `ipAllowList`/`readReplicas` when empty (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 20th run of the day, 2026-08-26, while checking whether `w6/m106`'s nesting bug had siblings in other resource types (it doesn't — Postgres/KeyValue/Redis correctly place `ipAllowList` at the object root per Render's own schema). Instead found a different, real defect on the same field: Render's pinned OpenAPI spec marks `ipAllowList` (`postgres`/`keyValue`/`redis`) and `readReplicas` (`postgres`) as `required`, but bex's REST and MCP responses omit the key entirely whenever the slice is nil — the common case (no CIDR restriction, no read replicas) — while GraphQL correctly returns `[]`. Live-verified on real pre-existing production resources (`dpg-d9nqg95cavls73fp8m20`, `red-d9p49kdrtmes73c34ovg`, read-only probes, nothing created/modified/deleted): REST `GET` and MCP `get_postgres`/`get_key_value` both lack the keys; GraphQL's `ipAllowListEntries`/`readReplicas` fields return `[]`. Root cause: `lego/backend/internal/postgres/service.go:107,124` (`PostgresView.ReadReplicas`/`.IPAllowList`) and `lego/backend/internal/keyvalue/service.go:95` + `lego/backend/internal/keyvalue/rest.go:66` (`KeyValueView.IPAllowList`/`renderKeyValue.IPAllowList`) all carry `json:"...,omitempty"` on a Render-required array field. The correct fix pattern already ships in this codebase for the identical field on a sibling resource: `lego/backend/internal/environments/rest.go:72,80-84` (no `omitempty`, explicit nil-coalesce to `[]core.IPAllowListEntry{}`). Exhaustively checked every other `required` field on both schemas: the rest either already lack `omitempty` or are scalar/pointer fields that are always non-empty on any real provisioned resource (live-verified present on the same probed instance), so only these specific array fields manifest the bug; `role`/`suspenders` (also `required` per Render's `postgres` schema) are not implemented as fields at all — a distinct, unrelated, unimplemented-feature gap, explicitly out of scope here. Dashboard is unaffected (GraphQL-only for these fields, confirmed by grep). **BLOCKED 2026-08-28 — code done and deployed; only the live sweep is owed.** Two external preconditions, neither a code change: (1) QA credentials — `scripts/qa-login.sh` exits 2, `QA_EMAIL`/`QA_PASSWORD` are absent from `.env` (empty names live in `.env.example:423-424`) and `BEX_API_TOKEN` 401s against `api.bex.co`; (2) for m110 only, the fix must ship first. See `040.md` for the pipeline state.
- [ ] **m110** — App compute is never metered, and a service named ≥22 chars loses CPU/memory/instance metrics (8 tasks; **triaged 2026-08-27 — both defects confirmed real from the source and fixed: t001–t004 + t007 done, t005/t006/t008 open on a deploy for live verification. A third instance of Defect A fell out — `build_seconds` had the same wrong name AND the wrong namespace, so build compute was never metered either. t001's open question is settled: production wires only `NewMetricsServerReader`, so autoscaling was never exposed. t004's decision is (a) leave history — Prometheus retention is 3d, so the affected range is unrecoverable. See the milestone README's Resolution section**) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 21st run, 2026-08-27. Opened the Metrics tab of the one service in the QA workspace whose name exceeds 21 characters: its Memory, CPU and Total Instances cards all read "No data in range" while the Network Metrics cards on the same page, same window, had real data. Chasing it through the API instead of the UI surfaced **two independent defects in the same four lines of PromQL**. **Defect A (billing, silent since the meter shipped):** `lego/backend/internal/usage/service.go:808` meters App `instance_seconds` with `app.Name` from a `store.App` — the _workspace-scoped public_ name (`store/store.go:96-102`) — while the pods are named `core.CRName(tenantID, name)` (`core/base.go:1379`, applied `apps/service.go:1736`, used verbatim as the Deployment name at `operator/.../app_controller.go:1751`). PromQL regexes are anchored, so `pod=~"block-eden-mono-…"` matches zero pods named `tea-d98210cbbpdc73dcrkvg-block-eden-mono-…` — **for every App, at every name length**. Live: an in-page authenticated `Usage` probe shows charge kinds `service → ["egress_bytes"]` for all 11 service rows, vs `postgres`/`key_value` → `["instance_seconds","storage_gb_seconds"]`; `beancount-forum` (standard tier, running 24 days, rated $17.50/mo in `pricing.yaml:38-53`) carries **no compute line at all**. The control verifies the cause, not just the contrast: datastores pass a real CR name (`usage/service.go:573`) into the ordinal matcher and meter correctly. `queryInstanceSecondsByMatcher` returns `(0, true)` for a query that matched nothing and `processAppMeterWindowResult` persists it as `Quantity: 0, SourceHealth: healthy` — so nothing has ever flagged it. **Defect B (metrics):** the shared selector `pod=~"%s-[a-z0-9]+-[a-z0-9]{5}"` assumes a two-segment pod name Kubernetes does not guarantee. `k8s.io/apiserver@v0.35.0/pkg/storage/names/generate.go` truncates a `GenerateName` base longer than `MaxGeneratedNameLength = 63-5 = 58` and then appends 5 chars — eating the hash separator, so past the threshold the pod name has one segment, not two. With the 25-char tenant prefix and a 9–10-char pod-template hash that threshold is a **22-character service name**, out of the 30 `ValidAppName` allows (`store/api.go:588`) — and since the hash length varies (6–10 seen live), a service near the boundary can flip across deploys. Live pod names from Loki's `instance` label: `tea-…-qa-20260826-webhook-svc-55d855bcb7sxgd` (63 chars, ONE segment, 0 MEMORY/CPU/INSTANCES series over 24h) vs `tea-…-block-eden-mono-5f557c45fd-ll7x9` (57 chars, TWO segments, all series current). `HTTP_REQUESTS` returns data for the same service over the same window — it goes through `traefikServiceLabel`, so namespace and app identity are provably correct and only the _shape_ assumption fails. Complete population check: of the workspace's six services, the 23-char one is the only name ≥22 and the only one missing Application Metrics. Exhaustive blast radius — `grep -rn '\[a-z0-9\]{5}' lego/ --include='*.go' | grep -v _test` returns **exactly 4 lines in 3 files**: `metrics/source.go:256`, `usage/service.go:884`, `autoscale.go:101`+`:104`. Metrics reach REST (`rest.go:92`), GraphQL (`graphql.go:298`) and MCP (`mcp.go:88`) through one funnel, so one fix moves three surfaces; web/private/worker/static all take Deployment names from `app.Name`, cron takes a CronJob name the same way (reasoned from the k8s generator, not probed); Postgres/Key Value use the ordinal matcher and are unaffected (verified live). Autoscale's exposure is config-dependent — its Prometheus reader is the fallback, and which reader production runs is explicitly **unverified**, settled by t001. Fixing A alone would walk straight into B, so they land together. **BLOCKED 2026-08-28 — t005/t006 now DONE (t005 found and fixed a second defect: MCP returned `{"series":null}` where REST/GraphQL returned `[]`). Only t008's live sweep is owed, on BOTH preconditions:** the fix `23c323f9` is still not live (five deploys since `050d40e4` have failed, been cancelled by supersession, or sat unpicked — the self-hosted runner is reporting `total_count: 0`), and the sweep needs QA credentials `scripts/qa-login.sh` cannot find. t008 also inherits t005's `/billing` charge-tree bullet. See `040.md`.
- [x] **m111** — The Logs tab's range is a lie in both directions: the live tail ignores it, and the empty state denies logs it excluded (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 22nd run, 2026-08-27, journey 7 (Logs). Opened a service's Logs tab on its defaults — range **Last hour**, Live **on** — at `02:23Z` and got exactly one line, timestamped `00:33:06Z`, **1h50m outside the selected window**, directly beneath the page's own promise "The range limits history. Live mode appends new lines as they arrive." **Defect A:** `dashboard/src/features/logs/hooks/use-live-logs.ts:45-56`'s `subscribeUrl` builds `/v1/logs/subscribe?resource=…[&type][&text][&instance]` with **no `startTime`**, and `log-viewer.tsx:134-143` threads the resolved window to `useLogHistory` only. Server-side `logs/rest.go:152-156` then leaves `q.Since` zero — its only fallback is `Last-Event-ID`, which a browser `EventSource` sends on *re*connect and never on the first one — so `podlogs.go:56-66` omits `SinceTime` and kubelet replays the pod's log from offset 0. The discriminator is decisive: flipping Live off (`?live=0`, range unchanged) empties the pane, and the range-scoped history read is independently correct (`logs(startTime: now-1h)` → 0 rows; `now-6h` → 9 rows, `00:25:40Z`–`00:33:12Z`). The pod had produced exactly one line, which is exactly what appeared. Not a regression of `w6/m93` — that milestone's outcome scopes its fix to reconnects, while `podlogs.go:48-54` states the general cost verbatim; this is the first-connect half it never claimed, so every tab open still re-ships a pod's whole history from kubelet. **Defect B (worse):** with Live off and the same range, the pane reads **"No logs yet — This service hasn't produced any logs yet."** about a service that produced 9 lines two hours earlier. `log-viewer.tsx:151,180-183` picks the copy from `hasActiveLogFilters(queryFilters)`, and `logs/types.ts:125-131` checks only `type`/`text`/`STRUCTURED_FILTER_KEYS` — **the range is not in `LogFilters` at all**, it is a separate `RangeSelection` (`log-viewer.tsx:134-135`), so the chooser structurally cannot see the one input that excluded the rows. And the range is _always_ bounded: `RANGE_PRESETS` (`metrics/lib/range.ts:16-26`) is nine spans `30m`–`30d` plus custom absolute windows with no "all time", and the Logs tab defaults to the second-narrowest (`1h`, `logs/lib/log-search.ts:134`) — so `logs.emptyBody` makes a claim the page is never in a position to make. **This predicate has been widened twice already and missed the range both times** (`w6/m47/t003`, which added `logs.emptyFilteredTitle` after finding the title and body contradicting each other, and commit `717143ae` "treat instance filter as active for empty-state copy") — the shape of the type is the defect, not the predicate's contents, so a third widening would miss it again. Blast radius: `subscribeUrl` has 1 caller and `useLiveLogs` 2 consumers — the second, `deploys/components/deploy-log-panel.tsx:178-179`, renders `logs.emptyTitle`/`emptyBody` with **no** `filtered` branch at all and is correct today only by accident, so it needs a regression test rather than being left alone; `hasActiveLogFilters`' caller count is explicitly **not yet measured** (t003 owns it). Layer check done: `/v1/logs/subscribe` already parses `startTime` (`rest.go:442`) and documents that it wins, but `rest.go:61`'s `CheckQueryWindow` against `BEX_MAX_QUERY_HOURS` (default **720**, `cmd/api/main.go:523`) puts the widest `30d` preset exactly on the rejection boundary — the fix must clamp rather than forward raw. — **done 2026-08-26** (`/goal` triage): both defects reproduced at HEAD and were fixed + test-covered. **A** — the live tail now sends the selected window's start as `?startTime=` (held out of `subKey`/read via a ref, so the sliding window never reopens the SSE stream), and `rest.go`'s `Last-Event-ID` resume takes the _later_ of window-start and cursor so the browser sending `startTime` on its own reconnects keeps `w6/m93`'s resume instead of re-reading the window. No `BEX_MAX_QUERY_HOURS` clamp was needed — `logsSubscribe` never window-checks and every sendable range is ≤ 720h. **B** — since the range is always bounded, "never logged" is unassertable, so that copy was _deleted_: `logs.emptyTitle`/`emptyBody` (en+zh) are now window-relative, fixing the deploy panel by construction too (it shares the strings; `hasActiveLogFilters` has exactly 1 caller — `log-viewer`). No new state-machine or predicate. REST-only subscribe surface → no GraphQL/MCP mirror. Code-and-test verified (backend `internal/logs`, dashboard lint + 3 suites green); not re-run live this session. Moved to `done/m111/`.
- [x] **m114** — Generate Blueprint emits the tenant-prefixed CR name, so bex's own exporter produces a `render.yaml` bex's own validator rejects (7 tasks) — **done 2026-08-26**, moved to `done/m114/`: confirmed real (byte-for-byte reproduction), fixed at the single producer `blueprint_generate.go:177` (`a.Name` → `appServiceName(a)`, the existing LabelServiceName-with-legacy-fallback helper) which covers all three surfaces (REST/GraphQL/MCP) and all five App kinds, with a store-managed-App regression test proven to fail without the fix; datastore output byte-unchanged. ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 26th run, 2026-08-27, journey 13. Pressed **Generate Blueprint** → selected one service → **Generate**, in a dialog whose own copy says "export as a render.yaml you can commit to a repo and connect as a Blueprint". It emitted `name: tea-d98210cbbpdc73dcrkvg-qa-20260826-webhook-svc` — the Kubernetes object name `core.CRName(tenantID, name)` — instead of the public service name. **The loop is closed and it fails:** feeding that output verbatim to `validateBlueprint` returns `{"valid": false, "errors": ["service \"tea-…-qa-20260826-webhook-svc\": bad request: name must be a DNS label of 1-30 chars ([a-z0-9-])"]}`, and the identical YAML with **only** the name swapped for the public one returns `{"valid": true, "errors": []}`. The generated name is 48 chars against `ValidAppName`'s 30-char cap (`store/api.go:588`); the tenant prefix alone is 25, so **any service whose public name is ≥6 chars** exports an unappliable manifest — total, not an edge case. It also writes the workspace's internal tenant id into a file the user is told to commit, in the same dialog that reassures "Secret values are never exported". Root cause: `lego/backend/internal/apps/blueprint_generate.go:177` — `"name": a.Name`. The siblings in the same file are correct and are the control: databases emit `d.Spec.Name` (`:361`), key-values `kv.Spec.Name` (`:397`), cross-refs a resolved `display` (`:328`,`:347`) — verified live, generating from `beancount-forum-db` alone yields the bare `name: beancount-forum-db`. The fix has a documented source: `core.LabelServiceName` (`core/base.go:175-182`) "carries an App CR's **PUBLIC** name (w4/m19)", with `a.Name` as the fallback for a legacy bare-object-named App. **Known class, new surface — not a regression of `w4/m19`:** m19 found and fixed exactly this ("the create response echoing the tenant-prefixed CR name instead of the public service name … with a regression test", `TestCreate_ResponseNameIsThePublicNameNotTheCRName`), and that fix still holds (`GET /v1/services/<id>` returns the public `name`+`slug` pair). Generate Blueprint shipped afterwards in `w8/m22` (`487eab31`), so a per-call-site regression test could not cover it — hence t003 guards the class rather than adding a third one-line fix. Unverified: whether other fields are invalid behind the name (it fails first), whether REST/GraphQL/MCP expose the generate verb at all, and the four non-`web` kinds.
- [x] **m115** — Four read surfaces disagree about what a service's `name` is, and the one REST returns cannot address the service (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 39th run, 2026-08-27, found while checking the Notifications page's per-service override dropdown against the service list. The dropdown listed `eden-dash-v3` and `qa-20260826-webhook-renamed`; the GraphQL `services` query for the same workspace listed the same two ids as `block-eden-mono` and `qa-20260826-webhook-svc`. Probed all four read paths at one instant (`2026-08-27T11:04:11.747Z`, in-page authenticated `fetch`): for `srv-d9ndt8hmcglc739fkp50`, REST returns `{name:"eden-dash-v3", slug:"block-eden-mono", displayName:"eden-dash-v3"}` while GraphQL returns `{name:"block-eden-mono", slug:"block-eden-mono", displayName:"eden-dash-v3"}` — same for `srv-da7o6ovvqdcc73bpn9hg`. The control is load-bearing: a service with **no** `displayName` (`srv-d9bkcspg9s7c73d0n8ug`) reads identically on both, so the divergence is the display label specifically, not projection lag. **Root cause:** `lego/backend/internal/apps/graphql.go:299` reads `AppView.Name` raw, while REST at `render.go:254` reads `renderServiceName(a)`, whose own comment (`render.go:508-510`) states the intent — Render's contract, "where service.name itself is mutable". MCP is not a third implementation: it serializes the same `renderService` struct (`mcp.go:50-52`), so it follows REST; webhooks got the SQL spelling of the same rule in `w6/m101` (`store/webhooks.go:679`). GraphQL is the one surface that never calls the helper — and `mcp.go:36-39` asserts in so many words that "the three surfaces cannot drift", which is true of the service method and false of the serializer. **A second, sharper half:** REST's `name` does not round-trip. `GET /v1/services/eden-dash-v3` → `404 app not found` on the value REST itself just returned, while `GET /v1/services/block-eden-mono` → `200 name=eden-dash-v3`; GraphQL behaves identically for both strings. `core.Base.GetApp` (`core/base.go:1453-1495`) resolves `LabelAppID` then `LabelServiceName` and never `displayName`. So each surface is internally coherent and they disagree — GraphQL's `name` round-trips and is stale, REST's is current and 404s — which is exactly why "make GraphQL match REST" is not the spec: it would import REST's 404 into the only surface without it. `slug` cannot absorb the immutable name either, because `PlatformSubdomain` appends a 4-char suffix on cross-tenant collision (`apps/service.go:846`, doc at `:346-353`). **Consumer checked, not assumed:** the dashboard is correct _despite_ GraphQL — `dashboard/src/features/services/lib/status.ts:35-39` re-implements the fallback client-side (`displayName ?? immutableName`), which is why the heading, breadcrumb, page title, home list and override dropdown all show `eden-dash-v3`; `immutableName` has exactly 2 references, both in that file, so the fix leaves the UI byte-identical. **Why now:** `w6/m101` fixed this class on the webhook surface and its Goal line claims the label matches what "the dashboard/REST/GraphQL already show" — while its own Source section quotes the contradicting GraphQL response verbatim, and `m101/t004` calls GraphQL's `name` "(immutable id)" and passes it. That parity task asked "did this change?", not "do these agree?", so the drift survived a check written to catch it. **Blast radius:** one line (`graphql.go:299`); `renderServiceName` has exactly 2 non-test callers (`render.go:254`, `rest.go:663`); all five App-CR types share `AppView`; the GraphQL type is reachable as `server(id:)`, `service(id:)`, `services(ownerId:)` and `SyncBlueprintResult.services`, all sharing `serviceGQLType`. Postgres/Key Value unaffected — re-verified rather than inherited from m101: all 7 `displayName` hits under `internal/postgres/` + `internal/keyvalue/` are `pgTierDisplayName`/`kvTierDisplayName` plan-label helpers. **Unverified:** the collided-slug case (every QA-workspace service has `slug == name`; needs a second workspace), MCP read live (code path only), the webhook surface re-probe, and whether any non-dashboard GraphQL consumer exists.
- [ ] **m116** — A free service never auto-sleeps as created: `idleTTLSeconds: 0` disables auto-sleep while eight surfaces call it "the platform default" (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 40th run, 2026-08-27, journey 15 (free-tier sleep → wake). Every service in the QA workspace reads `idleTTLSeconds: 0`, **including a freshly created free one** — so this is the shipped default, not legacy data. Its Settings tab renders `combobox: Platform default` directly under the sentence "Free services sleep after this idle window, then wake on the next request." **Failing case:** `qa-20260827-sleep` (`srv-da81t88ueu1c7395ink0`, free, Go) served once at `11:28:14Z`, then untouched — `phase=Running, replicas=1` at every 2-minute sample through `11:40:44Z`, **12m30s idle, 2.5× the shortest preset the Settings select even offers (300s)**. **Control, same service, one field changed:** `setIdleTimeout(idleTTLSeconds:60)` at `11:40:58Z` → `phase=Hibernated` at `11:41:59Z`, **61 seconds**. **Wake (journey 15's own promise):** `t+1s/t+6s/t+10s` → `503 {"error":"service hibernated","retryAfter":5}`, `t+15s` → `200`. So the mechanism is entirely healthy and the only thing between a free service and the free tier is the value the product ships it with. **Root cause:** `lego/operator/internal/controller/app_controller.go:1638` — `autoSleepEligible` requires `IdleTTLSeconds > 0`, and no controller default exists anywhere (the only `defaultIdleTimeout` constants in `lego/` are the pg-/kv-sni-proxy TCP timeouts; the CRD field is `+optional` with **no** `+kubebuilder:default`). The same predicate also gates `:2098` (whether `app.bex.co/last-active` is ever stamped) and `:2031` (whether the activator is the Ingress backend), so a `0` App is not merely never asleep — it is never on the wake path. **Eight sites assert the phantom default**, two of them machine-read contracts: `types/v1alpha1/app_types.go:613` → the generated CRD schema `config/crd/bases/app.bex.co_apps.yaml:525-526` (so `kubectl explain` repeats it), `apps/service.go:435-436`, `:3042`, `:3047`, `apps/mcp.go:96` (agent-facing jsonschema, "0 restores the controller default"), `apps/render.go:116`, and the dashboard's `lib/idle-timeout.ts:6-8` + `idle-timeout-row.tsx:21,24` + the user-visible label `services.idleTimeoutDefault` = "Platform default" in both `en` and `zh`. `w7/m80` is the accepted precedent for treating the MCP half as a live defect rather than a typo. **Why it matters:** ADR003:80 makes free-tier sleep load-bearing — "a sleeping pod occupies nothing, so the cluster **overcommits well beyond Σ** and Free approaches \$0" — which does not hold when every free App stays resident. **Render parity:** Render's free spin-down is fixed and always on; bex adds a knob (deliberate, w1/m4.5) but ships it defaulted to "never" (not a recorded decision). **Blast radius:** `autoSleepEligible` has exactly 3 callers, all in `app_controller.go`, and `:2031` is correct today only because `0` never reaches it. Only App CRs carry an idle window; Postgres/Key Value have `suspended` but no TTL. **Checked and not a sibling defect:** a free worker could never be woken by a request, and `desiredReplicas:1984` already excludes it (`autoHibernating = !worker && …`) with the reason in its own comment — verified at the call site. **Unverified:** whether `eden-dash-v3` (the workspace's other free service, `Running` 25 days at `0`) has ever slept (not touched — non-QA production); whether any bare-CR `Tier == ""` App exists in prod; the `zh` string was not rendered; and the deployed `BEX_ACTIVATOR_SERVICE` env var was not read (though the 61s hibernate and 15s wake prove the activator is live).
- [ ] **m117** — The Recovery card reports "No backups yet" for databases with proven backups, because two failed Kubernetes reads degrade silently to "nothing" (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 43rd run, 2026-08-27, journey 11 (Postgres), read-only throughout. All three pre-existing databases (`beancount-forum-db`, `tianpan-forum-db`, `blockeden-forum-db`; all `available`, `backupsEnabled:true`) return the same thing from `POST /v1/postgres/{id}/recovery-info`: `{"enabled":true,"latestRecoveryTime":"2026-08-27T12:53:32Z","backups":[]}` — no `earliestRecoveryTime` key at all, and a `latestRecoveryTime` that tracks the wall clock exactly (a call one minute earlier returned `12:52:41Z`). The dashboard renders that as a self-contradicting card: **`Earliest restore point: No backup yet`** and **`Backups: No backups yet.`** directly beside **`Latest restore point: August 27, 2026 at 5:54 AM`**, with `Restore to new instance` enabled. **It is a defect, not a true report, and the platform's own verified evidence proves it:** `.pm/w7/039.md` recorded these exact three ids with completed daily CNPG Backups on 2026-08-21 (`dpg-…-backup-20260821030000 completed` ×3) and *executed* a restore of one, matching row counts against the source (18,324 posts / 4,168 topics / 190 users). That note fixed the **restore** path (`serverName` projection) and never touched the **reporting** path — it is the evidence for this filing, not a duplicate. **Root cause:** `lego/backend/internal/postgres/recovery.go` builds three fields from three sources and only the one that cannot fail survives — `:128` discards a failed CNPG `Cluster` Get behind `err == nil` (so `EarliestRecoveryTime` is never set), `:145-147` returns `[]BackupView{}` on a failed `Backup` List (indistinguishable from empty), and `:133` sets `LatestRecoveryTime = s.Now()` with no cluster read at all. `enabled:true` comes off the Database CR's `Status.BackupsEnabled`, which also needs no CNPG access — so the entire observed response is reproducible with every CNPG read failing. **Consumer checked:** `recovery-panel.tsx:95-105` already handles the missing earliest (`fallback={t("databases.recoveryNoBackupYet")}` — that fallback is what prints "No backup yet"), but the latest field has no fallback because the backend never omits it; the fix must come from the backend for the UI to be able to express it. **The expensive half:** `Recover` (`recovery.go:199`) gates only on `!Status.BackupsEnabled`, so with backups enabled-but-unreadable it passes validation and `RequirePlanBilling` and provisions a **new paid database** that cannot bootstrap — reasoned from the code, deliberately **not executed** (it would have created a billable resource on a production workspace). **Blast radius:** `listBackups` has exactly 1 caller and the Cluster Get is inline, but `RecoveryInfoView` is a wire type on REST+GraphQL+MCP so the shape change moves all three plus the dashboard. Resource-family enumerated: only Postgres has a recovery/backup reporting surface — web/static/cron/worker/private have none, and Key Value has operator-side nightly RDB backups with no API to list them (a separate gap, explicitly out of scope). **Ruled out live:** a by-name/by-id lookup mismatch — `POST /v1/postgres/beancount-forum-db/recovery-info` → `404 app not found`, so only the `dpg-` id ever reaches the Cluster Get. **Unverified, and `t001` exists to settle it:** whether the Backup objects still exist (bex-api cannot read them — RBAC/namespace/CRD — versus genuinely pruned since 2026-08-21). No cluster access this run. If they are genuinely gone that is an ops incident *on top of* this reporting defect, and it would not make the silent degradation correct.
- [ ] **m118** — Nothing caps instance count by plan: a free service runs up to 100 pods at `$0.00/second`, under UI copy promising it is "billed accordingly" (8 tasks; **triaged + implemented 2026-08-27 — every claim confirmed real from the source, fixed not deleted. Render parity gathered (free instance types have no horizontal scaling → cap 1, `docs/render-artifacts/free-tier-scaling.md`), settling t001 and reversing ADR018 row 68's w1/m81 "free scales like any other" divergence (which stated no cost reasoning and predates m110's unmetered-compute finding). Cap lives in the tier catalog (`tiers.yaml free: maxInstances: 1`, `tiers.Compute.InstanceCap`); enforced in backend `Scale`/create/autoscaling + `SetPlan` downgrade-refusal (`checkInstanceCap`/`planDowngradeError`, refusal names plan+limit), operator `clampReplicas(app,n)` defense-in-depth, dashboard copy+slider, ADR018/ADR030. Tests red-before-green on tiers/backend/operator; `make test` envtest + `golangci-lint` (0 issues) + dashboard typecheck/test all green. Left open only for the deploy-gated t005 live over-cap re-run across REST/GraphQL/MCP + `/instances`, same shape as m110. See the milestone README Resolution section**) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 47th run, 2026-08-27, journey 6 (Scaling). `scaleService(numInstances:3)` on a **free** web service returned `{"plan":"free","replicas":3}`; REST confirmed `numInstances: 3`; and `GET /instances` at `14:55:59Z` returned **three real pods** (`…6sbehds2`, `…8ttcqua7`, `…a3jlvf0o`). Journey 6's own promise — "the applied number is the number that runs" — therefore **passes**: the API is not lying. The defect is that nothing bounds the number by plan. The dashboard offers the same service a **1–100 slider** under the sentence "All instances use the same instance type and are **billed accordingly**", while the free compute rate is literally `usdPerSecond: 0.0` (`pricing/pricing.yaml:40-41`). **Three layers, no gate in any:** `apps/service.go:3010-3038` (`Scale`) validates only `1..MaxReplicas` and never reads the plan; `operator/.../app_controller.go:3560-3568` (`effectiveReplicas`) passes `spec.Replicas` through and `clampReplicas` (`:3574-3582`) bounds only `[0,100]`; and `types/tiers/tiers.go:45-62` (`ComputeTier`) has **no instance-count field at all**, so there is no cap to enforce even if a caller wanted one. `MaxReplicas = 100` (`app_types.go:756`). **Deliberately deferred, not missed** — `.pm/w2/done/m56/README.md:30` lists under *Explicitly excluded*: "…or silently changing free-plan replica policy **without separate parity evidence**." So this is a known open policy question with a stated bar; `t001` supplies the evidence and settles it, and the rest implements whatever it decides. **Why now:** it compounds `w6/m110` (open), which established App compute is never metered at all — so instance count is currently both unbilled and unbounded on every plan, and fixing m110 alone would start billing paid plans while leaving free uncapped. **Blast radius:** `Scale` is one verb behind REST/GraphQL/MCP, but two writers reach around it — create, and autoscaling `minInstances`/`maxInstances` — so a cap on `Scale` alone is bypassable; `t004` adds operator defense-in-depth on the `w6/m46/t002` precedent, whose post-mortem showed a **projector** write re-stamping a field the API had set correctly. **Unverified:** Render's actual free-tier instance policy was **not** checked this run (reasoned from the "billed accordingly" copy and the paid-scaling convention only — `t001` owns it, and the filing does not treat it as evidence); what the cap should be; whether worker/cron/private/static behave identically (only `web_service` was scaled live); and autoscaling on free was never exercised. Fixture scaled back to 1 within ~90s and deleted (404 confirmed).
- [ ] **m119** — Keep private services out of public-edge auto-sleep (8 tasks; t001/t002/t004/t005/t006/t007/t009 done, t008 deployed closeout pending) ← Live QA proved a free `private_service` hibernated at zero replicas with no public activator wake route, while the worker control stayed Running. Triage chose the safe product boundary: only free public `web_service` Apps auto-sleep; private traffic bypasses both the activator wake path and its activity signal. Operator envtest keeps an expired private service at one replica/no Ingress, the dashboard hides idle controls and Sleeping copy for ineligible types, and ADR003/ADR007/ADR018 record the decision. The proposed generic Resume task was deleted because ADR007 defines Resume as the idempotent inverse of explicit suspension; overriding any zero-replica cause would fight auto-sleep/autoscaling ownership. Operator, backend, and dashboard suites are green. Remaining: ship/deploy, then repeat the private/worker 60-second probe and close the milestone.
- [ ] **m121** — CORS preflight advertises a stale method + header list, so 53 mutating REST routes and two real request headers are unreachable from the origin bex itself allowlists (8 tasks) ← promoted from `060.md` (41st `/qa-find-bugs` run, journey 5) after the **55th** run independently reproduced it on a second route family (2026-08-27, journey 11 / Postgres). One page, one session: `GET /v1/postgres/{id}` → **200**, `DELETE /v1/postgres/{id}` → **threw `Failed to fetch`**. The preflight read directly returns **204** with `access-control-allow-origin: https://dashboard.bex.co` and `access-control-allow-credentials: true` — correct — but `access-control-allow-methods: GET, POST, OPTIONS`; re-running it with `Access-Control-Request-Method: POST` returns the **byte-identical** string, proving a static constant rather than a per-route computation. So the server accepts the preflight and then omits the method, and the browser never sends the request. **Root cause:** two literals in `withCORS`, `lego/backend/internal/api/auth.go:724-725`; the router disagrees on **53 distinct patterns** (DELETE 26, PATCH 17, PUT 10). **The security rationale is already dead:** `060`'s control deleted the same resources from the same browser, origin and cookie via `POST /graphql`, so the method list blocks nothing an attacker on that origin could not already do — it only removes REST parity. **Why it survived:** the dashboard never uses the broken path — `grep -rn "method: *['\"]DELETE" dashboard/src` returns **zero** hits, and a database delete goes through `use-delete-database.ts` → `databases.graphql`. **The 55th run also settled `060`'s one Unverified line by measuring `Allow-Headers`** (`Authorization, Content-Type, X-Session-Token`, no `Max-Age` at all): `Idempotency-Key` is a **confirmed gap** (read at `webhooks/rest.go:213`, its POST verb allowed but the header not, so browser clients silently lose idempotency); `Last-Event-ID` (`logs/rest.go:159`, SSE resume) is absent and whether `EventSource` reconnects preflight it is **untested** — `t003` exists to run it; and `X-Api-Key` is **ruled out** — it appears only at `modelproxy.go:380,384` on an **outbound** upstream request, so the raw grep count that suggests otherwise is a trap the fix must not fall into. **Unverified:** whether a third-party browser REST client exists in production; whether `BEX_API_CORS_ORIGIN` names anything beyond the dashboard (never read); the `Last-Event-ID` question above.
- [ ] **m122** — The Events tab silently hides five event types the API emits, because "all types" means the dashboard's catalog rather than what the feed returns (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **56th run**, 2026-08-27, journey 3/5. REST returned a `custom_domain_verified` event at `2026-08-27T11:54:07Z`; **its immediate neighbours both render** on the Events tab (`custom_domain_added` at `11:54:08Z` and `11:52:55Z`, shown as "7h"), so it is inside the displayed window and still never appears — which is what rules out "the UI queried a narrower window". The live "Filter events" control offers 62 checkboxes whose domain entries are exactly "Custom domain added", "Custom domain removed", "Platform subdomain updated" — no "Custom domain verified", and **zero disk options** — so the type is not merely unrendered but **unselectable**, with no user action able to reveal it. **Root cause, two lines:** `services.$serviceId.events.tsx:82-84` seeds the default selection as `new Set(SERVICE_EVENT_TYPES)` and `:106-108` renders `events.filter(e => selectedTypes.has(e.type ?? ""))` — the feed is intersected with the dashboard's catalog, fail-**closed**. **Exhaustive diff:** the backend declares 60 event-type constants; **5** are absent from the catalog groups and from `LABEL_KEYS` — `custom_domain_verified` (verified live) plus `disk_attached/_detached/_restored/_updated`. All five are reachable, not dead constants: `eventTypes` (`events/service.go:245`) maps `apps.VerifyDomain`, `apps.AddDisk`, `apps.DeleteDisk`, `apps.UpdateDisk`, `apps.RestoreDiskSnapshot` onto them and `:655` assigns `ev.Type = eventTypes[r.Verb]`. **The correct behaviour already exists one file over** — `events/lib/timeline.ts:20-41` is fail-**open** (`if (filter === "all") return true`), and `serviceEventLabelKey` already falls back to a generic label — so the Events tab is the outlier, which settles which side is correct rather than leaving "make them consistent". **Why three guards missed it:** `scripts/events-verify.sh` and `TestEventSurfaceParity` are backend-only, and `service-event-filter.test.tsx:9` seeds from `SERVICE_EVENT_TYPES` itself — self-referential, structurally unable to notice the catalog is short. `git log -6 -- dashboard/src/features/events/` shows the catalog untouched since `afe614f1` (w7/m66), which is how later additions drifted in. **Not a regression of m66:** its DoD said "every _new_ type", scoping the guarantee to its own additions. **Unverified:** the four `disk_*` types are code-confirmed reachable but none was seen live (no persistent-disk service in the QA workspace); the count-badge undercount is code-derived only; `zh` locale not checked.
- [ ] **m123** — A failed build tells the tenant nothing true: the failure reason is Kubernetes internals, the duration is 0s, and buildkit's error never reaches the log (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **57th run**, 2026-08-27, journey 14 (deliberately break a build). **Evidence: 3 failed deploys, 2 services, 2 fault phases, 2 repos, 2 causes** — the second fixture (`qa-20260827-badbuild`, **public** repo, **buildkit** phase, `dockerfilePath: ./NoSuchDockerfile`) was purpose-built this run and deleted after (`deleteService` → true, `GET` → 404), and is what proves this is neither clone- nor auth-specific. **(A)** `failureReason` reads `build failed: PodFailurePolicy: Container buildkit for pod bex-build/bld-tea-d98210cbbpdc73dcrkvg-qa-20260827-badbuild-gen-2-hhtxp failed with exit code 90 matching FailJob rule at index 1` — the internal namespace, the Job naming scheme with the workspace id, a bare exit code, and a sentence about bex's own PodFailurePolicy config. Root cause `app_controller.go:849`, which concatenates the curated class sentence from `buildFaultViews` (`build_failure.go:36-51`) with Kubernetes' Job condition text (`build/build.go:563`). **The good mechanism is built and unused:** `captureFailureTail` (`build/build.go:1249-1259`) sets `FallbackToLogsOnError` on every build container so the failing container's output is retrievable — and a grep for consumers finds only setters plus an unrelated CrashLoopBackOff reader. **The house style is in the same file:** the runtime path at `app_controller.go:3635-3645` writes a genuinely actionable tenant-facing message. **(B)** that deploy's `startedAt` (`19:57:15.720548Z`) and `finishedAt` (`.720549Z`) are **one microsecond** apart on a build that ran **68 seconds** — `DeployStatusStartsExecution` (`deploy_lifecycle.go:61-63`) excludes only queued/canceled/deactivated, so `build_failed` counts as executing and `TransitionDeploy` stamps `started_at` on the terminal skip. **Control:** every live/deactivated deploy on two services has a distinct start, and `canceled` — which the predicate does exclude — has none at all, so the predicate explains both. The true duration exists as `RunSeconds` (`build/build.go:431`) and goes only to a Prometheus histogram. Symptoms: Events renders `0s`, and `progress.go:101` stamps the `==> Building from` banner with `StartedAt` so it sorts **after** the failure it introduces. **(C)** buildkit's output reaches the build log **not at all** — 11 lines, zero buildkit lines, while the clone's own output ships fine; **cause not traced, `t003` owns it**. **Not a regression of `w7/m82`:** its t003 DoD (tenant vs infra produce different `failure_reason` values) is met; nobody covered the raw Job text glued alongside. **Unverified:** defect C's cause, the failure **email** surface, the FaultInfra/FaultTimeout strings (code-read only), and the deploy detail page's rendering.
- [ ] **m124** — A service that is serving traffic reports "Failed" because its latest build failed — the coarse phase contradicts the running instance, on every surface (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **58th run**, 2026-08-27, journeys 2 + 14. `qa-20260826-webhook-renamed` (`srv-da7o6ovvqdcc73bpn9hg`) answers `curl` with **HTTP/2 200** and the body `OK — pushed v3` from **1 running instance**, while REST _and_ GraphQL both report `phase: "Failed"` and the dashboard header renders **"Service Failed"** directly above a correct "Latest deploy: Build Failed". **Control, built and deleted in the same run:** `qa-20260827-env` — same workspace, same free plan, same type, also serving 200 from one instance — reports `Running`. The only difference is whether the LATEST BUILD succeeded, not whether the service works. **Root cause:** `app_controller.go:3913` stamps `app.Status.Phase = PhaseFailed` unconditionally, and the reconcile then quiesces (`:832-839`), so it stays Failed. **The rule is already written down in this repo and already implemented one function away:** `app_types.go:1104-1111` says "Reusing `PhaseFailed` here made a service contradict its own deploy history … that release keeps serving and the phase stays Running", and the cancel path enforces it at `app_controller.go:577-579` with `if app.Status.Image != "" { return r.dispatchRuntime(...) }` — `w6/m52`'s fix for the identical contradiction. The failure path never got that guard. **Consumer trap the fix must clear first:** `store/reconciler.go:1027-1031` derives the DEPLOY ROW's `build_failed` FROM `PhaseFailed`, so simply keeping the phase Running would stop the deploy row reaching `build_failed` — the verdict must be re-sourced from the Build condition `r.fail` already writes at `:3920-3926`. **Blast radius, counted:** 1 writer (`app_controller.go:3913`), 2 backend readers (`reconciler.go:1027`, `:1209`), and 1 dashboard component (`service-status-badge.tsx`, which its own doc says serves list rows + detail header + overview panel so they cannot drift). **Not a regression of m52** — m52's scope was cancel. **Unverified:** `Status.Image` was never read directly (the GraphQL `image` field does not exist), so "a prior release is present" is inferred from serving 200 on 1 instance; MCP's phase was never probed; the first-deploy-failure case (where Failed IS correct) was not exercised.
- [ ] **m125** — The Blueprint plan says "create" for services that already exist, because the planner keys them by tenant-prefixed CR name (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **59th run**, 2026-08-27, journey 13. **Measured plan-vs-apply divergence:** planned a manifest (`POST /v1/blueprints/validate`, multipart) → `create qa-20260827-bp`; applied it (`POST /v1/blueprints/deploy`) → `srv-da8a82fm2e9c73ft65fg`; **re-planned the same manifest with the service now existing, and a modified one (`buildCommand: npm install` → `npm ci`) — BOTH returned the byte-identical plan, still `operation: "create"`, `totalActions: 1`** — while `mode` claims `current_state`. Then applied the changed manifest: **200, SAME service id, exactly one service, `buildCommand` now `npm ci`** — an in-place UPDATE. So the plan says create for what apply does as an update. **Root cause:** `blueprint_state_plan.go:83` keys `resolver.services[app.Name]` — the Kubernetes object name, which for a store-managed App is `CRName(tenant, name)` — while `:119` looks up the MANIFEST's public name, so every lookup misses and `:150-152` turns every miss into `BlueprintPlanCreate`. **The control is 11 lines below in the same function:** `:97` keys databases by `database.Spec.Name` and `:111` key-values by `keyValue.Spec.Name`, both with a duplicate-name guard services lack entirely. **The repo already diagnosed this exact mistake:** `blueprint_generate.go:177-185` says "The manifest-facing PUBLIC name, never the tenant-prefixed CR object name … `appServiceName` reads `LabelServiceName` … Datastore entries already emit `Spec.Name`; this aligns services with them. **(w6/m114)**" — m114 fixed the EXPORTER and shipped `appServiceName` (`blueprint_ownership.go:165-170`); the PLANNER never got it. Not a regression of m114 — its scope was the exporter. Apps have no `Spec.Name` (`app_types.go:260-264`), so the fix is `appServiceName(app)`. **Blast radius:** `blueprintActionPlan` has **4** call sites — `deploy.go:581`, `blueprint.go:419`, `blueprint.go:572` all DISCARD the plan as an error gate; only `blueprint.go:277` (`blueprintValidationFor`) uses it, and it serves BOTH `validate` and `preview`. Fixing the key also makes the update branch run for services for the first time, which `t002` owns. **Unverified:** whether `estimatedPricing` is wrong for the same reason (only the free tier was exercised, where it is 0.00 either way — `t003`); whether GraphQL/MCP expose the plan at all; whether datastores plan `update` correctly in a live run (read from code, not measured); and the dashboard's Blueprint page, never opened.
- [ ] **m126** — Creating a project returns the internal view, not Render's project shape — 5 of 7 project handlers skip the mapper, and conformance only checks reads (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **60th run**, 2026-08-27, journey 1. `POST /v1/projects` returned keys `createdAt, databaseIds, id, keyValueIds, name, ownerId, serviceIds`; `GET /v1/projects/{id}` on the same project returned `id, name, owner, environmentIds, createdAt, updatedAt`. **Two shapes for one resource.** Render's pinned `project` schema has exactly six properties and requires ALL of them — `id, createdAt, updatedAt, name, owner, environmentIds` — and defines no `serviceIds`/`databaseIds`/`keyValueIds`/`ownerId` at all, so the GET is correct and the POST body is **missing three required fields and carrying four undefined ones**. **Root cause:** `projects/rest.go` applies `renderProject` by hand and only on the reads — `:101-110` POST returns `s.Create(...)` and `:120-129` PATCH returns `s.Rename(...)`, both raw `ProjectView`, while `:138-140`'s three links-PUTs pass the explicit `identity` helper defined at `:77`. **2 of 7 handlers emit Render's shape; the 5 that don't are all writes.** **The control is the sibling resource:** `environments/rest.go:169-179` wires the mapper into `core.HandleMapped` so no handler can forget it — and the live environment CREATE response duly carried the full Render shape. **The obvious fix does not compile:** `HandleMapped`'s mapper is `func(T) V` with no ctx and no error, while `renderProject` needs both (it calls `environmentIDs` to fill Render's required `environmentIds`) — which is probably why the write paths skipped it, and `t001` must decide the mechanism. **Why nothing caught it:** `conformance_test.go`'s `operationPath` covers exactly **10** operationIds, every one a READ, no project operation among them, and **no create/update response of any resource is validated** — and there is no allowlist entry for projects, so this is unexamined rather than waived. **Second, smaller divergence (`t002`):** Render's `projectPOSTInput` requires `environments`; bex decodes only `{name, ownerId}` and the created project had **zero** environments until one was made by hand. **Placement itself is CORRECT** — the environment's `serviceIds`, the service's own `environmentId`/`projectId`, and GraphQL all agreed. **Unverified:** the PATCH and three links-PUT shapes (browser PATCH/PUT blocked by `w6/m121`'s CORS list — code-read only); GraphQL/MCP project shapes; the dashboard project page; and whether other resources share the write-path mistake (`t003` should reveal it).
- [x] **m127** — A free Key Value created in the dashboard is silently non-durable, on a plan that provisions a 1 GB disk — and `persistenceMode` can never be changed afterwards (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **61st run**, 2026-08-27, journey 12. **Measured:** a free Key Value created via `POST /v1/key-value` came back `options: {maxmemoryPolicy: allkeys_lru, persistenceMode: "journal_snapshot"}`. **The dashboard does the opposite:** `keyvalue.new.tsx:104-108` sets `const effectivePersistence = isFree ? "off" : persistenceMode`, `:122` sends it, `:200`/`:228` disable both Selects, and `:243` tells the user **"The Free plan has no persistent disk, so persistence is off."** — with the code comment citing "Render's exact behavior". **The premise is false for bex, and its own catalog says so:** `lego/types/tiers/tiers.yaml` gives the valkey free tier `storageGB: 1` and notes verbatim "Render's KV free tier is 25MB / no persistence; **bex's is more generous** at 128Mi". **What `off` costs:** `keyvalue_controller.go:173-177` maps it to `--appendonly no --save ""` — a pure in-memory cache — so two creation paths give the same plan opposite durability, with a 1 GB disk provisioned either way. **And it is permanent:** `persistenceMode` is create-only on every surface — `KeyValuePatch` (`service.go:683-694`) has no such field, `rest.go:336-341` does not decode it, `mcp.go:163` does not pass it — while Render's `keyValuePATCHInput` lists all five (`name, plan, maxmemoryPolicy, persistenceMode, ipAllowList`), so adding it CLOSES a divergence. **The ledger repeats the wrong rationale:** `ADR018:150` gives "no persistent disk" as bex's reason and documents post-create updatability for `maxmemoryPolicy` only, while `:17`'s re-baseline lists `persistenceMode` as a covered surface — true at create, not at update. **Precedent:** `w7/m45` did exactly this for `maxmemoryPolicy`; `t002` copies its shape. Not a regression — the lock was deliberate, from a Render premise that does not hold here. **Unverified:** I did NOT create a KV through the dashboard form (the `off` outcome is code-read, so the first DoD bullet is a verification step); whether any existing free store is actually on `off`; whether GraphQL `createKeyValue` forces the same; and what a mode change does to live data.
- [ ] **m128** — A deploy canceled mid-build leaves an unclosed build in the activity feed: `build_started` with no `build_ended` (6 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **65th run**, 2026-08-27, journey 3. Created `qa-20260827-dep`, let deploy #1 reach `live`, triggered #2 and cancelled it while `build_in_progress` (confirmed by reading the deploy first). The **complete** 30-minute feed (limit=50) holds 7 events: the canceled deploy has `deploy_started` + `build_started` + `deploy_ended(canceled)` and **no `build_ended`**, while the successful deploy in the same service minutes earlier has `build_started` **and** `build_ended`. Re-probed after a further 15s to rule out a late fact — unchanged. **Root cause:** `deploys/service.go:706` closes the row directly (`CloseDeploy(ctx, deployID, DeployCanceled, "")`) and emits no fact; the facts come from `store/reconciler.go:630-665` (`recordDeploy`), which only processes **open** deploys, so once `FinishedAt` is set the row is never revisited. **The intent is documented AND implemented, just unreachable:** `reconciler.go:751-759` states "a cancel while still building means it was canceled", and `buildEndedStatus` (`:832-851`) returns `EventStatusCanceled` when `isBuildPhase(current)` — which `build_in_progress` satisfies. The only route that calls it is the **supersede** path (`:654-659`); an explicit user cancel never reaches it. So the target behaviour needs no designing. **Blast radius counted:** `CloseDeploy` has exactly **1** caller (`deploys/service.go:706`); the other 3 grep hits are two interface declarations and the PGStore implementation. **Not an m66 regression** — `w7/m66/t001`'s own title scopes it to "(reconciler-emitted facts)", which the cancel verb is not. **Cancel is otherwise correct and must not be disturbed:** 200, `status: canceled`, detail/list/events agree, and a real 17s duration (`23:55:15.693` → `23:55:32.427`) — which incidentally corroborates `m123` (the collapse there needs a terminal SKIP; this deploy was observed mid-build). **Also verified, not filed:** rollback works end to end (201, `trigger: "rollback"`, live, real 16s duration). **Unverified:** webhook delivery (none subscribed); the supersede path's emission; the queued-cancel case; and the dashboard Events tab, never opened.
- [x] **m129** — A resource whose deletion has not finalized is hidden from the list but still counted against the plan quota: "Services 11 / 100" over 6 visible services (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **66th run**, 2026-08-28, journey 16 ("disappears from every list, project page, **and usage view**"). Read-only. GraphQL `workspaceLimits` returns `services{used:11,limit:100}` while `GET /v1/services?limit=100` returns **6** — and `postgres{used:3}` / `keyValues{used:2}` match reality exactly, which is what makes the services gap real rather than a misread. **Root cause:** `workspaces/service.go:953-955` counts `len(apps.Items)` / `len(dbs.Items)` / `len(kvs.Items)` with **no deletion filter**, while `apps/service.go:1131` drops any App with a `DeletionTimestamp` **deliberately** (w3/m46/t002, whose own comment concedes "a delete stuck on a failing finalizer becomes invisible here"). Same objects, opposite treatment, nothing reconciling them. **The counter is NOT lying — and that decides the fix:** enforcement is a per-namespace Kubernetes **ResourceQuota** built from the same caps (`store/namespaces.go:487`, rejection mapped by `apps/service.go:2050-2061`), and k8s counts objects until they are actually removed — so the displayed 11 honestly reports what will gate a create. Filtering the counter would make the number look right and turn it into a lie. **Stakes:** Hobby caps are `Postgres: 1, KeyValues: 1` (`plans.go:107`), so ONE terminating datastore consumes the entire quota while invisible in the list. **Not covered by w3/m46** — its DoD is four static-site items (suspended cert, list row, clearCache, SPA fallback) and names no quota/cap/usage surface; `w7/m9` shipped this counter and is the precedent to extend. **Blast radius:** `ResourceLimits` has 1 caller + 1 dashboard consumer; `QuotaCapsForPlan` has 2 (display + enforcement), which must not drift. **Unverified:** the composition of the 11-vs-6 gap — exactly ONE terminating App is known (`qa-20260827-static`, frozen at `2026-08-27T06:26:24Z`, ~17.5h), demonstrating the mechanism; the other four are consistent with earlier hunts' deleted fixtures but were not confirmed (no cluster access). Also unverified: that the ResourceQuota counts terminating objects — **`t003` gates the whole milestone on measuring it**, because if it does not, the framing inverts.
- [x] **m130** — `renderSubdomainPolicy` is the third field in its family at the wrong REST nesting level: Render-shaped clients get 400 sending it where the spec declares it, and it is reported for service types that have no subdomain (8 tasks) ← promoted from `w6/063` (46th run, 2026-08-27) and extended by a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **68th run**, 2026-08-28, journey 10 (cron job / background worker / private service). **Defect 1, request side, the interop break:** Render declares the field inside `webServiceDetailsPOST`/`staticSiteDetailsPOST`/`…PATCH`, and bex accepts it **only** at the top level, so `POST /v1/services {"type":"web_service","name":"","serviceDetails":{"env":"go","renderSubdomainPolicy":"disabled"}}` returns `400 bad request: request body contains unknown field "renderSubdomainPolicy"` — **for `static_site` too**, so it is not type-gating. The top-level form decodes fine (fails later on name), and the **control settles it**: the same request carrying `serviceDetails.ipAllowList` decodes, because `w6/m106` moved that field to Render's nesting. `serviceDetailsReq` (`apps/rest.go`) holds `ipAllowList` (m106) and `disk` (`w1/m86`) — the two prior fixes of this exact pattern — and not `renderSubdomainPolicy`; four adjacent fields there are already dual-accept ("accepted here or at the top level, top level wins"), so the fix is the file's own established shape. **Defect 2, response side:** emitted **both** top-level (where Render's `service` schema has no such property at all) **and** in `serviceDetails` for all five types — captured live across `web_service`/`private_service`/`background_worker`/`cron_job`, every one reporting `"enabled"` while the same payload omits `url`, and GraphQL agreeing. **The routing itself is correct** — all three non-routable hostnames 404 on `:80` and `:443` with no cert issued, against a public control returning 200 — so what is wrong is what the API says *about* it. **The repo already wrote down the rule:** `render.go:159-163`, immediately below the offending declaration, says `ipAllowList` is web/static-only "so it is emitted from renderServiceDetails(), not here — see the ipAllowList block there (w6/m106)"; the untyped emission sits at `render.go:318` in a field table whose next two entries (`url`, `buildCommand`) **do** carry type conditions. **Why no guard caught it:** the conformance allowlist has a blanket `serviceDetails: does not match the documented union` entry for `retrieve-service`/`list-services`, and the pinned `service` schema leaves `additionalProperties` unset, so the extra top-level property cannot fail at all. Also absorbs `063`'s original defect — `setSubdomainPolicy` tells a private service to add a custom domain, which the custom-domain guard then refuses, sending the caller in a circle (`apps/service.go:3560-3577`, no type gate). **Unverified:** the PATCH nesting rejection (browser PATCH is CORS-blocked by `w6/m121`; established by code inspection only), the MCP surface, and that a real Render client sends the nested form (reasoned from the spec, not observed).
- [ ] **m131** — The request-log half of `w3/m8` delivers nothing in production: `type=request` is empty on every service, and the Method/Status/Path filters can never match (9 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **69th run**, 2026-08-28, journey 7 (Logs + Metrics — "filters narrow, ranges shift, empty states are honest"). **Exhaustive across all five web services in the workspace, 24h window:** `GET /v1/logs?resource=<id>&type=request` returns **200 with 0 logs** for every one, and the decisive reads — `GET /v1/logs/values?label=method` and `?label=statusCode`, which come from **Loki's own label index** — return **`[]`** for every one, while `label=type` returns only `["app"]`. Nine HTTP 200s were driven against a fresh fixture (3× GET /qa-marker-alpha, 2× POST /qa-marker-beta, 2× GET /, 1× DELETE /qa-marker-gamma) and 30s later **zero** request lines existed. **Controls isolate it to the request pipeline, not Loki and not the shipper as a whole:** app logs work (100 lines), build logs work (100 lines, same store), `host` discovery works — and critically **level normalization works** (`label=level` → `["error","unknown"]`), which **rules out** a recurrence of the `w3/m13` frozen-ConfigMap incident that `log-shipper.yaml:66-89` documents (that frozen state lacks level normalization too) — the first thing a diagnosis would otherwise chase. **The UI completes it:** the Filters popover offers Level/**Method**/**Status code**/Instance/**Request path**, three of which are request-only, and the Method dropdown lists `GET·POST·PUT·PATCH·DELETE` from a **static fallback** (discovery returns `[]`) — five selectable values that return zero rows on every service. **Contract gap:** `ADR018:182` marks this ✅ on all four surfaces and says "nothing accepted is ignored", but its honest-failure path keys on **store presence only** (503 when `BEX_LOKI_URL` is unset); the store is up here, so the query succeeds into `200 {"logs":[]}` — indistinguishable from a quiet service. **NOT a regression:** `w3/m8` shipped this but its own status line defers the live check — "needs a Loki-synced cluster (prod), which the CAPD mock is not" — so `scripts/logs-verify.sh` was never run in production; this milestone is that owed verification. **Unverified:** THE ROOT CAUSE IS NOT ESTABLISHED (no cluster access; t001 owns it, and the three candidate stop points are read from config, not measured); the metrics consequence (`ADR018:190` — host/path-filtered `http_requests`/`http_latency` are served from this same request-log store) was not probed; platform-wide vs single-workspace scope unknown; MCP not exercised live.
- [ ] **m132** — REGRESSION of `w2/m39`: the SSH gateway never sends KEXINIT, so native `ssh` into a running instance cannot connect at all (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **71st run**, 2026-08-28, journey 8 (Shell / SSH — "attaches, **or fails with a real reason**"). **`ssh.bex.co:22` accepts TCP, writes its `SSH-2.0-bex` banner, and then never sends `SSH_MSG_KEXINIT`.** Raw-socket probe (no OpenSSH involved) with two independent controls in the same run: `github.com` → 800 bytes, first msg type **20 (KEXINIT)**; `gitlab.com` → 800 bytes, type **20**; `ssh.bex.co` → **0 bytes, no KEXINIT**. RFC 4253 requires it immediately after the version exchange, so the instrument is sound. **Consistent** (5/5 trials, `kexinit_received=0`), **not target-dependent** (identical for `agentmarketcap-1` on the PAID `starter` plan, a free fixture, and a nonexistent service — so not the eligibility gate), and **not algorithm negotiation** (four forced KEX/cipher sets all identical; `ssh -vvv` never logs a `peer server KEXINIT proposal`, and a server finding no common algorithm would send its own proposal or a DISCONNECT first). Reachability is fine: `:22` answers, `:2222` is correctly refused. **This is a REGRESSION with the prior verification on the record** — `ADR035:3` records live verification **2026-07-14** and `:157` a production CLI pass **2026-07-18**; `w2/m39` (done 2026-07-17) owns the broken guarantee, and t002 walks its DoD item by item since the handshake failure masks every one. Meanwhile the product still advertises `sshAddress` (`"srv-…@ssh.bex.co"` on REST **and** GraphQL) on every eligible paid service, and the Shell page says "Open a shell … from your terminal". **The guard exists and does not run:** `scripts/ssh-verify.sh` (17,976 bytes) is the written public-edge acceptance for ADR035 — the SECOND such script found idle, after `scripts/logs-verify.sh` in `w6/m131`; t004 cross-references m131/t004 to solve it once. **Verified working and must not regress:** the free-plan gate is correct end to end (409 on `shell-ticket`, `sshAddress` absent, dashboard explains "requires a running paid … service"). **Unverified:** THE ROOT CAUSE (no cluster access; the PROXY-protocol hypothesis is unmeasured and its config location was not even found), the Browser Web Shell and `sandbox exec` (different transports, and testing them needs a paid service that is a customer workload), all post-KEXINIT authentication behaviour (no key registered), and second-client coverage.
- [ ] **m133** — The Postgres parameter editor is seeded from the live `pg_settings` read, so one edit writes 48 operator-managed values — including the WAL archive/restore commands and TLS paths — into the tenant's declared config (8 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, **72nd run**, 2026-08-28, journey 11 (Postgres). Created `qa-20260828-pg` (free, PG 18), set **no** parameters, and the Insights "Non-default parameters" table renders **48 editable rows** — every one a textbox pair with a Remove button. `{ databaseParameterOverrides(id:) { name setting source } }` → 48 rows, `{"configuration file": 45, "client": 2, "session": 1}`. **What is in the list is the point:** `archive_command` = `/controller/manager wal-archive …` and `restore_command` = `/controller/manager wal-restore …` — the CloudNativePG operator's own binary path, and the mechanism continuous backups and PITR run on — plus `ssl_ca_file`/`ssl_cert_file`/`ssl_key_file`, `wal_level`, `max_replication_slots`, `hot_standby`, `recovery_target_timeline`, `port`, `listen_addresses`. Three rows are sourced `client`/`session` — not configuration at all, they exist only because of the API's **own** connection. **Root cause: the backend keeps two reads and the UI is bound to the wrong one.** `insights.go:388-391` states it outright — "GetParameterSpec returns the currently stored parameter overrides from the Database CR (spec.parameters), **not from pg_settings**. Use ParameterOverrides for the live database view." The editor is fed `databaseParameterOverrides` (the pg_settings read) via `databases.graphql:364-370` → `use-database-insights.ts:74` → `insights-panel.tsx:299-307` → `parameter-overrides-editor.tsx:70`. **And Save is a FULL REPLACEMENT** — the component's own comment says so, `save()` submits `rows.map(...)` not a diff, and `dirty` compares against an identically-seeded baseline, so **removing one row enables Save and writes the other ~47**. The backend filter is one name wide (`normalizeParameterOverrides`, `service.go:934-948`, drops only `shared_preload_libraries`). **The correct read is exposed nowhere** — `GetParameterSpec` appears in no `{graphql,rest,mcp}.go`, and `parameters` is absent from the Postgres view, so `spec.parameters` is effectively **write-only** and the editor had nothing right to bind to. **Not a regression:** `w5/m35` shipped the editor onto the read the panel already had. **Verified working, must not break:** free-tier `backupsEnabled:false` with `recovery-info` → `{"enabled":false,"backups":[]}` and `export` → `[]` (honest empties), connection info gated behind an explicit Reveal button, and `externalConnectionString` correctly omitted for a non-public database. **Unverified:** Save was never clicked (full-replacement is code-established, not observed); what CloudNativePG does with an operator-owned key in `spec.parameters` (decides severity); REST/MCP write paths; whether Key Value shares it.

### Triage pass — 2026-08-27 (all 24 open milestones verified against the tree)

Every open milestone was re-checked one by one against the current source, not taken on trust from its write-up. **No milestone was deleted: all 24 describe defects that are still present.** Each root cause named in a milestone README was located at (or within a few lines of) the file:line it claims. What changed is bookkeeping, plus four findings that only appear when the board is read as a whole.

**Board state corrected**

- **m103** — Bug A is fixed in the tree (`user-nav.tsx`'s `handleLanguage` awaits `ensureLanguage`) with its non-tautological regression test present; `t001` frontmatter said `todo`. Marked done and moved to `m103/done/`. Bug B (`t002`) re-confirmed open: still zero `createInstance`/`cloneInstance` anywhere in `dashboard/src`.
- **m117** — `94c0a185` shipped `t001` (root cause: bex-api held no `postgresql.cnpg.io` RBAC) and `t002` (both reads now error instead of degrading to a false empty). Both marked done and moved. **`t003` is only half done**: the dashboard fallback landed, but its third acceptance bullet — refusing an unsubstantiated restore *in the API* — is unmet, because `Service.Recover` (`postgres/recovery.go:197`) still gates only on `Status.BackupsEnabled` and never on `firstRecoverabilityPoint`. Annotated in place.
- **m118** — implementation verified real (`tiers.MaxInstances` + `checkInstanceCap` at `apps/service.go:4202`); six tasks already carried `status: done` but had never been moved. Moved to `m118/done/` and their README rows marked.
- **m119** — the closeout note claimed the changes were uncommitted. They are not: `9917191f` carries them and is an ancestor of `HEAD`. Corrected; the real remaining blocker is the deploy.

**Cross-milestone findings**

- **A fixture one milestone plans to delete is another's only evidence.** `m110/t008` step 3 deletes `srv-da7o6ovvqdcc73bpn9hg`. That service is `m122`'s first DoD bullet (the hidden `custom_domain_verified` event), one of `m115`'s two renamed-service samples, and the source of two of `m123`'s three failed deploys. Cross-references added to `m110/t008` and `m122`; whichever of the four closes last owns the deletion.
- **`m97/t007` and `m124` overlap and need sequencing.** `m124/t002` (don't set `PhaseFailed` while a prior release serves) removes the occasion for the misleading event in the common case; `m97/t007` step 2 still owns the reason-code mapping (`observedServiceStateFor` at `store/reconciler.go:1298-1315` still labels every non-excluded Ready=False reason `readiness_failed`). Land `m124/t002` first. Cross-referenced in both.
- **`m97/t007`'s first leg is already fixed**, by `m97/t001`'s carve-out in `rejectProtectedSecretRefs`; what remains there is a regression test, not a fix. Scope narrowed in place.
- **`m132` is the most severe open item and was reproduced live during this pass.** A raw socket to `ssh.bex.co:22` gets `SSH-2.0-bex` and then EOF — no KEXINIT — so the advertised SSH surface is completely dead right now, for every client.

**One systemic blocker, six milestones**

`m99/t007`, `m102/t003`+`t007`, `m109/t007`, `m110/t008` and `m119/t008` are all live-verification closeouts, and all six are parked on the same two preconditions rather than on any unwritten code:

1. **QA credentials.** Re-tested this pass: `scripts/qa-login.sh` still exits **2** — `QA_EMAIL`/`QA_PASSWORD` are absent from `.env` while present as empty names in `.env.example`. Nothing in these six can be verified live until those are filled.
2. **A green deploy.** `HEAD` is **54 commits** past `050d40e4`, the last commit `deploy.yml` shipped successfully. `9917191f` (m118+m119), `23c323f9` (m110) and `94c0a185` (m117) are all **absent** from the deployed build. The `deploy.yml` run on `94c0a185` (`33143365056`) was still `pending` at triage time; the runs before it were push-cancelled, and `344ead76` failed.

These six are correctly filed and correctly blocked — they are not stale and should not be deleted, but none of them can advance from a keyboard alone.


## Inbox

- `024.md` — open-signup funnel observability counters (ADR075 launch precondition; sub-hour)
- `025.md` — decide: should Background Worker have a Free instance tier at all? (product decision, from the 2026-08-21 live bex-vs-Render worker QA pass)
- `026.md` — decide: should Suspend require typed confirmation, matching Delete? (product decision, same QA pass)
- `040.md` — **live-verify the m50/m51/m52 fixes on production, and unbreak the `deploy.yml` pipeline blocking it** (every deploy run since 2026-08-24 is red on a `fatal error: fault` in the opensandbox image build on the self-hosted runner; all test jobs are green, so this is runner infrastructure, not code) — the pipeline has since recovered; see its own file for the running log of what's been live-verified since
- `042.md` — shared Project/Environment create-form hint says "pick or create an Environment" but the selector offers no create affordance (only "No environment" + existing ones), so following it means abandoning the create form and losing every field already filled in; distinct gap in the same string `w6/m48` t003 already rewrote once for a different inaccuracy; sub-hour, verified live 2026-08-25
- `043.md` — Blueprint detail's Sync History card flashes "No syncs yet" during its own query's loading window even when real history exists (`blueprints.$blueprintId.tsx:190,424` discards `useBlueprintSyncs`'s `loading` flag) — distinct from `w6/done/m50` (which added the `errorMessage` column content to the same table, not this loading-vs-empty gap); sub-hour, verified live 2026-08-25/26
- `044.md` — the per-row Copy button on an env-var/secret-file reveal row (`service-environment-editor.tsx:957`, shared `SensitiveViewItem`) hardcodes the bulk "Copy env vars" aria-label and fires the bulk "Environment copied" toast for a single-value copy — doubly wrong for a secret file, which isn't an env var; sub-hour, verified live 2026-08-25
- `045.md` — New Service page's browser tab title never updates when the Service Type radio changes after load (`services.new.tsx:63-67`'s `head()` reads only the URL search param, not the live radio state the `<h1>` reads); not a regression of `w6/m43` (different scope: initial load vs. post-load client-state change); sub-hour, verified live 2026-08-25/26
- `046.md` — global search's "Navigation" section header renders even when zero navigation items match the query (`global-search-dialog.tsx:188-199`'s `CommandGroup` has no length guard, unlike the project-level search's honest empty state); sub-hour, verified live 2026-08-25/26
- `047.md` — static site: a request path over ~1000 chars returns `502 "origin error"` instead of `404` (`lego/operator/internal/staticserver/staticserver.go:208-209` — an S3 key-too-long error isn't classified as not-found); sub-hour, verified live 2026-08-26
- `048.md` — cron schedule's human-readable description doesn't handle named weekday/month fields (`MON`/`JAN`) even though they're valid and accepted (`dashboard/src/features/services/lib/cron.ts:99-129`'s `describeCron` re-parses with digit-only regexes instead of reusing `resolveValue`); sub-hour, verified live 2026-08-26
- `049.md` — Billing page's "Projected total for `<month>`" mismatches between SSR and hydration (React #418) — `charges-card.tsx:204`'s `now = new Date()` default parameter feeds `projectMonthEnd`, a third distinct hydration-mismatch mechanism beyond `w6/m102`/`w6/m107`; sub-hour, verified live 2026-08-26 (19th run)
- `050.md` — Billing's "Projected total for `<month>`" extrapolates Stripe's **subscription-period** total (`currentCost.periodStart` 2026-08-16 → `periodEnd` 2026-09-16) using the **calendar-month** elapsed fraction (`charges.ts:82-89`'s `projectMonthEnd`, fed from `charges-card.tsx:228,237`), understating it ~2.4× — live: $30.01 covering ~11 days divided by ~26 days elapsed rendered "$36.07" beneath a charge tree summing to $79.10. Explicitly **not** the estimate-vs-rated seal-window skew `w6/m98/t007` accepted and asked hunts not to re-file: the arithmetic is wrong regardless of that skew, and t007 attributed the whole gap to meter lag without noticing the two figures cover different windows. `periodStart`/`periodEnd` are already in the `Usage` query but never reach `ChargesCard`; sub-hour, verified live 2026-08-27 (21st run)
- `051.md` — Deploy rows label every deploy's `createdAt` as "Deployed {timestamp}" regardless of status (`deploys-list-page.tsx:174-175,216` + `locales/en.ts:90-91` / `zh.ts:90-91`), so a **Canceled** or **Build Failed** deploy reads "Deployed August 8, 2026 at 7:16 PM" — the label string's own `description` already admits it ("created/deployed timestamp"). Related to but distinct from `w6/035` (Events feed's `build_started` fact, a backend `file:line`); sub-hour, verified live 2026-08-27 (21st run) on `srv-d9bkcspg9s7c73d0n8ug`
- `052.md` — "External host" renders a **blank** value on Postgres and Key Value detail pages: `databases.$databaseId.tsx:371` and `keyvalue.$keyValueId.tsx:276` both do `externalHost ?? "—"`, but the backend returns an empty string (live: all 3 databases and both key-values report `{"public": false, "externalHost": ""}`), so `??` never fires and the `<dd>` is literally empty while every sibling row has text. The fix pattern ships two lines away — the adjacent `region` row uses a conditional spread to omit itself, and `ha-panel.tsx:136` uses a truthiness check on the same field for replicas. Blast radius is exactly 2 (`grep -rn 'externalHost ?? ' dashboard/src`); sub-hour, verified live 2026-08-27 (22nd run)
- `053.md` — every service type's Build Command hint says it builds "your site's **static** output" (`services/locales/en.ts:1894-1898`; zh `1858-1861`), rendered unconditionally on both surfaces that expose the field — `build-deploy-section.tsx:238` (every type's Settings tab) and `services.new.tsx:333` (the New Service form). Its sibling key's own description gives the origin away: `services.buildCommandLabel` is documented "(static_site settings)". Live on `srv-d9bkcspg9s7c73d0n8ug`, a Go/Docker `web_service`; sub-hour, verified live 2026-08-27 (22nd run)
- `054.md` — a service's Events feed never records a project/environment reassignment: assigned `srv-da7o6ovvqdcc73bpn9hg` to a fresh environment at `02:55:04Z` and `GET /v1/services/<id>/events?startTime=<now-24h>` still returns the same 14 events (newest `00:33:15Z`), re-checked at +8s and +2min, with the dashboard Events tab agreeing ("You've reached the beginning of this service's history"). The move **is** audited (`environments.SetServices` with `metadata.service`), and the feed is not deploys-only — it already carries `display_name_changed`. Naming trap for the fixer: the existing `service_environment_changed` type is the **env-var** config change (`w1/m81` t003, a documented divergence), not this. Not a duplicate of `w6/036`, which covers the move's partial-failure orphaning rather than the success path recording nothing; sub-hour, verified live 2026-08-27 (23rd run)
- `055.md` — custom-domain DNS instructions say "create **this exact** TXT record" once per domain, at one shared host, with a different value each time: `_bex-challenge.<registrable-domain>` holds a per-claim random value (`newDomainChallenge()`, `store/store.go:957-963`, called from `AddDomainClaim`) while the record **name** is per-registrable-domain (`domains.go:83-85`). Live: three claims under one apex demanded `GjyWKaFV…`, `a2_VKEVFs…` and `6ptrPZoR…` at the same host, all three panels stacked on the settings page. The design is deliberate (`ADR005:16,53` — "each row has its own durable challenge and must verify independently"; `VerifyTXT` uses `slices.Contains` so values coexist) — the defect is that no instruction says to keep the existing records, and bex auto-adds the `www` sibling so a first-time user hits two conflicting panels immediately. Copy/grouping fix, not mechanism; sub-hour, verified live 2026-08-27 (24th run)
- `056.md` — deleting an apex custom domain leaves its **auto-added** `www` sibling behind with `redirectForName` silently stripped, converting a redirect into a direct-serving domain the user never added; the delete confirmation mentions no sibling in either direction. Live: after deleting `qa-20260827.invalid`, `GET /v1/services/<id>/custom-domains` returned `[{ "name": "www.qa-20260827.invalid" }]` with the `redirectForName` key gone. Sits beside `w6/m30`, whose DoD ("deleting the pairing removes the redirect cleanly") covers the redirect mechanism — which does go away — but not the paired claim row's own lifecycle; filed as a gap, not a regression, with a note to re-read m30's DoD before deciding. Unverified: the reverse direction (add `www` first) and whether a **verified** pair behaves the same; sub-hour, verified live 2026-08-27 (24th run)
- `057.md` — two gaps in the Secret Files panel, both on `service-environment-editor.tsx` (the file `w6/044` also cites): its **empty state** says "Add a file to mount secret contents into this service" while the panel contains no button at all — the `Add secret file` / `Upload files` controls only appear after pressing **Edit** in the differently-titled Environment Variables panel above it; and the new row's `File name` input renders its validation hint ("Use letters, digits, dot, dash and underscore; not '.' or '..'") on an **untouched empty** field, telling the user they got it wrong before typing. Also records what the same journey verified working (add/delete both persist over REST) and two things it did **not** verify: that the file actually lands at `/etc/secrets` (the mounting deploy failed at clone) and whether REST omitting secret-file `content` is a deliberate Render divergence; sub-hour, verified live 2026-08-27 (25th run)
- `059.md` — the static-site **Redirects/Rewrites** and **Headers** editors have six row controls between them and none has an accessible name (`static-site-section.tsx:189-216` and `:301-320`): the route-type `<Select>` has none at all, and the five `<Input>`s are named only by hardcoded English placeholders (`/old/*`, `/index.html`, `/*`, `X-Frame-Options`, `DENY`) that go stale the moment a field holds a value. Confirmed with `ariaSnapshot()` per this skill's own accessibility trap, not a DOM scan — `combobox: Redirect` (a value, not a name) beside a correctly-named `button "Remove rule"` in the same row. The file is otherwise translated (29 `t("services.*")` calls), so the placeholders doubling as names also means a non-English screen reader announces untranslated labels. Journey 9's core promise was verified passing in the same run and is recorded in the note so it is not re-probed; sub-hour, verified live 2026-08-27 (30th run)
- `061.md` — a rollback emits `build_started`/`build_ended: succeeded` although it runs no build
- `062.md` — no pluralization convention: four ad-hoc schemes, and ~9 strings still render "1 variable operations"
- `064.md` — a blueprint apply creates env groups and reports that it did not; GraphQL’s sync result also drops key values
- `065.md` — the Metrics time range (and both datastore log viewers') is lost on reload and cannot be shared; the service Logs page persists its own
- `066.md` — `w6/m111`'s owed live verification is discharged: both Logs defects confirmed fixed on production
- `067.md` — the Environment page lists a key twice — as a service variable and under a linked env group — with no sign the group's value is shadowed; runtime precedence is correct (`envFromSources`, `app_controller.go:3466-3487`), only the display is silent

> `060.md` promoted to **m121** 2026-08-27 after the 55th `/qa-find-bugs` run reproduced it on a second route family and measured the header list it had left unverified — moved to `done/060.md`.

_(`039.md` + `058.md` closed 2026-08-27 — the 38th `/qa-find-bugs` run verified `039` live on a purpose-built once-a-minute cron running `sleep 200`: with a manual run active, **three** schedule ticks (10:29, 10:30, 10:31) passed with `maxConcurrent = 1`, where the filed bug started a second concurrent execution at the first tick. Two controls beyond that — the schedule **resumed** afterwards (a new scheduled `crr-` appeared unprompted, so the fix is not suspend-only), and the note's own `Unverified` reverse-ordering case resolved by chance: a manual trigger arriving during an active *scheduled* run **canceled** it (`"pending"` → `"canceled"` in 3s), exactly as the note predicted. All three orderings now hold the single-concurrent-execution guarantee. `058` closes with it — every note it tracked (`035 031 028 027 037 033 036 039`) is now closed, and it carries one lesson forward: a recorded probe is written from the **bug's** shape, and a fix that removes the failing step makes that probe the wrong test. Captures in `done/039.md`, `done/058.md`. Moved to `done/`.)_

_(`036.md` closed 2026-08-27 — the `d247d210` fix re-probed live during the 37th `/qa-find-bugs` run. Deliberately **not** by route interception: the fix removes the second mutation, so there is no longer a second call to fail and the orphaning window is gone by construction. Verified instead that exactly one write fires — moving a Key Value between two purpose-built projects through the UI issued `["SetProjectKeyValues", "Projects"]`, one `SetProject*` where the bug had two — and that the premise making one write sufficient is true: afterwards the source project's `keyValueIds` is `[]`, the target's holds the id, and the resource's own `projectId` is the target. Key-value exercised live; services and databases inherit the single-call shape through the same `moveTo`/`idsOf`/`runSet`, recorded as inherited rather than observed. Capture in `done/036.md`. Moved to `done/`.)_

_(`033.md` closed 2026-08-27 — the `d247d210` disclosure re-probed live during the 36th `/qa-find-bugs` run on a purpose-built project holding one environment. **Both** copy surfaces now lead with the cascade — the Danger Zone card ("Its environments and their configuration are deleted with it…") and the confirmation dialog ("Its environments and their configuration are deleted…") — replacing the filed "…nothing is deleted" that never mentioned Environments, and `zh` moved with `en` (`projects/locales/zh.ts:275-278,289-292`). The **disclosed behavior was re-confirmed rather than assumed**: after confirming, this note's original probe returns byte-identically `{"environment": null}` with `"app not found: environment: not found"`, so the CASCADE is intact and the copy is now true to it. Capture in `done/033.md`. Moved to `done/`.)_

_(`037.md` closed 2026-08-27 — the `d247d210` `serverRefusalReason` fix re-probed live during the 35th `/qa-find-bugs` run, **both branches**: a server refusal now toasts the backend's own sentence (`Health check path must start with /`, prefix stripped and capitalized) where the filed bug showed the generic copy, and a forced **transport** failure (`page.route` aborting only `setHealthCheckPath`) still toasts `Couldn't update the health check path.` — the arm a fix that always showed the server message would have broken. One line in the shared helper, so all 14 `useFieldMutation` hooks inherit it; only `use-health-check-path.ts` was exercised live, recorded as inherited rather than separately observed. Nothing persisted — the invalid path is refused server-side and the fixture is unchanged at `rev-4`. Capture in `done/037.md`. Moved to `done/`.)_

_(`027.md` closed 2026-08-27 — the `d247d210` conversion re-probed live during the 34th `/qa-find-bugs` run: one predicate at `settings.tsx:78` (`service ? servesHttp(service.type) : true`) now feeds both former sites (`:101` nav, `:346` section). `web_service` → `#health-checks` present; a purpose-built `cron_job` → absent, with the rendered section list confirming it. `worker`/`static_site` follow from the same single boolean, read not separately probed. The note's "what renders while loading?" question turns out to be **unobservable on this route** — `service` is defined at first paint from SSR or the Apollo cache, so the `: true` arm never evaluates in production; recorded as a limit of the verification, not as "no flash occurs". Capture in `done/027.md`. Moved to `done/`.)_

_(`028.md` closed 2026-08-27 — the `d247d210` pluralization fix re-probed live on production during the 33rd `/qa-find-bugs` run across **all three** branches: one generated request → `1 request`, `beancount-forum` over 12h → `162 requests` (the control, so the singular fix could not have broken the common case), and a zero-request window → summary deliberately omitted per `network-metrics-card.tsx:269`'s `requestCount > 0` gate. Real i18n key pair, not a hand-rolled `+ "s"`. The note's **second** bullet (10 unbatched `Metrics` operations; re-measured at 17 GraphQL POSTs per load) is unchanged and no successor was filed — the note's own text already classifies it as "efficiency, not correctness". Capture in `done/028.md`. Moved to `done/`.)_

_(`031.md` closed 2026-08-27 — the `d247d210` fix (CreateKeyValue's audit deferred past the CR write) re-probed live on production during the 32nd `/qa-find-bugs` run, **both halves**: a successful free-tier create wrote exactly one `CreateRedisEvent` with `targetName: "qa-20260827-kv"`, and re-submitting the same name (`409 CONFLICT`) wrote **no** audit row at all — the decisive half, since the filed bug recorded a success row for creates that then failed validation. Capture in `done/031.md`; it also confirms datastore effects populate `targetName`, a data point `w6/m112/t001` needs. Moved to `done/`.)_

_(`035.md` closed 2026-08-27 — the `d247d210` fix (`build_started` rides the deploy row's `started_at` instead of `created_at`, and is withheld while queued) re-probed live on production during the 29th `/qa-find-bugs` run: a fresh private service's first deploy sat queued ~20 min (`createdAt 05:23:23Z` → `startedAt 05:43:15Z`) and `build_started` fired at `05:43:15Z`, matching `startedAt`, with no `build_started` event existing at all across 10 polls while the row was still `queued`. Capture in `done/035.md`. Moved to `done/`.)_

_(`029.md` + `030.md` + `041.md` closed 2026-08-26 — all three were fixed by the same `d247d210` commit that closed `038.md` below (Danger Zone/Suspend URL copy gated on `publiclyRoutable`; the env-group-detail hydration mismatch guarded), now confirmed live on production (19th `/qa-find-bugs` run, ancestor-of-deployed-commit + live re-probe for `030`). Moved to `done/`.)_

_(`038.md` closed 2026-08-25 — the extract half had already landed in `d247d210`; the direct-publish/clone half the note also named was still open and is now fixed: `clone` writes its own operator-authored termination message per failing step, including the missing publish directory, and an unrecorded container failure names its step instead of quoting Kubernetes' bare `Error`. Moved to `done/`.)_

_(`032.md` + `034.md` closed 2026-08-25 during a board triage — both were already fixed in the tree: `032` by `80bae887` (the ReplicaSet RBAC grant, now marker-generated), `034` by `ca14cb59` (`parseNewServiceSearch` drops an unknown `?type=` instead of crashing); moved to `done/`.)_

_(`021.md` implemented 2026-08-18 (inert `usage_threshold` event dropped from the notification vocabulary); `022.md` + `023.md` retired 2026-08-18 as already-resolved board hygiene — all three moved to `done/`)_

_(`012.md` promoted to **m18** 2026-07-13; `018.md` promoted to **m35** 2026-07-15; `019.md` absorbed into **m36** (t002) 2026-07-15 — note moved to `done/`)_

_(`009.md`, `010.md`, `011.md` grouped into **m15** 2026-07-13; `013.md` promoted to **m17** 2026-07-13; `014.md` promoted to **w2/m28** 2026-07-13 — materialized under `w2`, not `w6`, per user direction; `016.md` promoted to **m28** 2026-07-15)_

> **m16 (Projects & environments: basic grouping) removed 2026-07-13** — duplicate of `w1/m31` ("Projects: group services within a workspace"), materialized by a concurrent `/pm-brainstorm` session the same day with near-identical scope (a `projects` table + REST/GraphQL/MCP/dashboard CRUD + assign/unassign). `w1/m31` is the tighter MVP (Projects only, no Environments) and is kept; a stub at `w6/m16/README.md` records this history (see below) rather than being deleted outright, since `/goal` tooling referenced the exact path. **Update 2026-07-13:** `w6/m16`'s deferred Environment half was NOT left unbuilt — it shipped as `w1/m32` ("Environments: named subsets of a Project's services"), layered on top of `w1/m31` instead of duplicating it, closing the grouping row fully (REST/GraphQL/MCP ✅ for both Projects and Environments; see `docs/ADR032-environments.md`).

> `002.md` promoted to **m8**; `003.md`, `004.md`, `005.md`, `007.md`, `008.md` grouped into **m9**; `006.md` (sign CNB tenant images) also implemented directly in `552130b` as an opt-in `BEX_TENANT_SIGNING_KEY_SECRET` path — all six notes moved to `done/` 2026-07-12 (`006` earlier than its own "park until required" note anticipated; superseded by direct implementation). `009.md`, `010.md`, `011.md` grouped into **m15** 2026-07-13 — moved to `done/`.

## Not in w6 (deliberate)

- **Members & roles** (invite/change-role/remove, Team page) — that's `w4/m12`; w6/m1 only enforces the Hobby single-member guard w4/m12 must respect.
- **Billing/payments** — bex has no billing system; the plan field ships without a payment step (Render's flat-subscription + inactivity-waiver model is documented in RESEARCH-workspaces.md for whenever billing becomes roadmap-worthy).
- **Workspace-mutation REST endpoints** — parity means their _absence_ (research finding 9: Render's REST owners surface is read-only; lifecycle is dashboard-only).
