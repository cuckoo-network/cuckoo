---
id: w6/m102/t007
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m102/t005, w6/m102/t006]
---

## Objective

Close the milestone once its Definition of Done is actually met (observable, not just "the code is written").

## Steps

1. Confirm every DoD bullet in `README.md` is live-verified (t003's live probes, the proximity-check script showing zero `guarded=NO`, dashboard test suites green).
2. Set every task's `status: done`; move each `tNNN.md` to `w6/m102/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w6/m102/README.md`.
4. Move `w6/m102/` to `w6/done/m102/`.
5. Check `- [x]` for **m102** in `w6/README.md`.
6. `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [ ] DoD verified live, not assumed.
- [ ] Board state (workstream README, milestone README, task frontmatter) agrees everywhere.

## Status — open, gated on t003

Everything closeout can verify without a deploy is verified; what remains is the live half.

**Verified now:**

- Proximity-check script returns zero lines — no `formatRelativeAge(`/`formatRelativeUntil(` call survives outside the wrapper (t002).
- `yarn lint` (typecheck + eslint) and `yarn test` (372 files, 2692 tests) green (t002, t006).
- No wire-shape or copy change (t004).

**Blocked:** DoD bullets 1 and 2 are live probes against `dashboard.bex.co`, which still serves the pre-fix bundle — see t003. Run them after `/ship` lands this milestone and the deploy completes, then close out (task frontmatter → `done`, files → `m102/done/`, milestone → `w6/done/m102/`, box checked in `w6/README.md`, `npx prettier@3.4.2 --write "**/*.md"`).

**Do not close this milestone on the code alone.** The whole point of the DoD's live bullets is that a hydration mismatch is a runtime SSR-vs-client timing phenomenon; the forced-boundary tests are strong evidence, not a substitute for having looked.

## Blocker update (2026-08-27/28) — the deployment premise is stale; read this first

**The fix is live.** `20b68db5` (w6/m102) is a confirmed ancestor of `050d40e4`, the commit `deploy.yml` last shipped successfully (`git merge-base --is-ancestor 20b68db5 050d40e4`). Everything written above about waiting for a deploy is **discharged** — do not re-argue it.

**The remaining blocker is credentials, not code and not the pipeline.** This session could not run the live leg because the QA sign-in is not configured:

- `scripts/qa-login.sh` exits **2** — `QA_EMAIL`/`QA_PASSWORD` are absent from `.env`. They are present as empty names in `.env.example:423-424`, so `.env` has simply drifted behind it.
- `BEX_API_TOKEN` is set but returns **401** against `https://api.bex.co/v1/services` (and `/v1/owners`), so it is not a usable API credential for this surface either — the unauthenticated control returns the same 401.

**To unblock:** fill `QA_EMAIL`/`QA_PASSWORD` in `.env` (never paste them into chat or a tool call — `scripts/qa-login.sh` reads them inside its own process and hands back cookies only), then run this task's steps unchanged. Nothing else about the task needs revisiting.
