---
id: w6/m102/t005
title: Simplify
worker: worker6
status: done
estimate: 20m
depends_on: [w6/m102/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Steps

1. Invoke `/simplify` scoped to this milestone's diff (the new shared component + its 22 call-site migrations).
2. Apply any behavior-preserving cleanups it finds.

## Acceptance criteria

- [x] `/simplify` run recorded (applied fixes or "none found") in the closeout evidence.

## Result

**Done — manual pass, `/simplify` deliberately not invoked as a skill.** Another worker has uncommitted in-flight changes under `lego/backend/` in this worktree (apps/store/webhooks + a new migration). `/simplify` reviews "the changed code", so running it would have swept a concurrent worker's unfinished work into this milestone's review and edits. Ran the equivalent reuse/simplification/efficiency pass over this milestone's diff by hand instead.

**Applied:**

1. **Extracted a duplicated test harness.** The `renderToString`-at-T → `hydrateRoot`-at-T+Δ probe had been written out three times (`relative-time.test.tsx`, `webhook-deliveries-card.test.tsx`, `api-key-row.test.tsx`), each with its own `Date.now` spy, container setup, `console.error` silencing and teardown. Now one helper: `dashboard/src/test/hydration.ts` → `hydrateAcrossBoundary(node, { serverNow, clientNow })`, returning `{ html, recovered }`. Callers are 3–5 lines each, and the `html` half doubles as the SSR-reachability assertion. (Sits beside the existing `src/test/virtual-geometry.ts` shared-helper precedent.)
2. **Removed 3 now-redundant `suppressHydrationWarning` props** — `resource-table.tsx`'s two `<TableCell>`s and `HeaderFacts`' `<dd>` — since the guard moved inward with the value.
3. **Collapsed 8 ternaries** of the shape `x ? formatRelativeAge(x) : "—"` into a bare `<RelativeAge value={x} />`.

**Considered and rejected:** merging `RelativeAge`/`RelativeUntil` into one component behind a `direction` prop — two named exports read better at the call site and match how the two formatters are already named. Also rejected: a shared component for the guarded `formatDateTime` spans, which belongs to w6/m107, not here.
