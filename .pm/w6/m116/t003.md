---
id: w6/m116/t003
title: Correct every surface that states the contract — 8 sites, one of them the CRD schema
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m116/t001]
---

## Objective

Make every place that describes `idleTTLSeconds: 0` say what t001 decided. Two of these are contracts a machine reads, not prose: the CRD schema and the MCP tool description.

## Context

The full list, from this milestone's grep:

1. `lego/types/v1alpha1/app_types.go:613` — CRD field doc
2. `lego/operator/config/crd/bases/app.bex.co_apps.yaml:525-526` — **generated**; regenerate with `make manifests generate` from `lego/operator/`, never hand-edit
3. `lego/backend/internal/apps/service.go:435-436` — `AppView.IdleTTLSeconds`
4. `lego/backend/internal/apps/service.go:3042` — `MaxIdleTTLSeconds`
5. `lego/backend/internal/apps/service.go:3047` — `SetIdleTTL`
6. `lego/backend/internal/apps/mcp.go:96` — MCP jsonschema, agent-facing
7. `lego/backend/internal/apps/render.go:116` — REST field comment
8. `dashboard/src/features/services/lib/idle-timeout.ts:6-8`, `components/idle-timeout-row.tsx:21,24`, and the label `services.idleTimeoutDefault` in `locales/en.ts` **and** `locales/zh.ts`

- `w7/m80` is the precedent for treating 6 as load-bearing: "an MCP description promising 'restore the platform default /' is the contract an agent reads, so a stale one is a live defect, not a typo." It fixed the service verb, the dashboard row, the MCP description and both locale strings together — the same set this task touches.
- The hint `services.settingsIdleTimeoutHint` ("Free services sleep after this idle window, then wake on the next request.") renders unconditionally beside the select. Under option A it becomes false whenever the select reads "off", so it needs a conditional variant — this is a copy change with a state machine behind it, not a string swap.

## Steps

1. Update sites 1 and 3–8 to t001's wording; regenerate site 2 via `make manifests generate` and keep the output.
2. If t001 chose A, rename the `0` label away from "Platform default" (it currently claims a default that does not exist) and add the conditional hint.
3. If t001 chose B, the existing labels are already correct — confirm each still reads true rather than editing for its own sake, and say so in the completion notes.
4. Keep `en` and `zh` in step; the `zh` catalog carries the same keys.

## Files

- the 8 sites above
- `lego/operator/config/crd/bases/app.bex.co_apps.yaml` (generated)

## Acceptance criteria

- [ ] `grep -rn "controller default\|platform default\|Platform default" lego/ dashboard/src --include="*.go" --include="*.ts" --include="*.tsx" --include="*.yaml"` returns only statements consistent with t001, with the count pasted into the completion notes.
- [ ] `kubectl explain app.spec.idleTTLSeconds` (or the generated YAML) matches the operator.
- [ ] The Settings hint cannot render as an unconditional claim beside a control set to "never".
- [ ] `en` and `zh` agree.

## Out of scope

- Operator behavior (t002).
