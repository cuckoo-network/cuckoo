---
id: w6/m31/t001
title: Verify the current rejection + capture Render's contract
worker: worker6
status: todo
estimate: 30m
depends_on: []
---

## Objective

Reproduce the `registryCredentialId` unsupported-field rejection on create and patch, and pin Render's exact wire contract (where the field sits in the create/patch body, validation, error on a foreign credential id) from the pinned OpenAPI.

## Context

- `lego/backend/internal/apps/rest.go:274` (`unsupportedField`), `:598`, `:855`.
- w2/m14 shipped `internal/registrycreds` (verbs + Secret materialization); GraphQL/MCP create paths may already accept it — check before assuming.

## Steps

1. Repro against dev-6: create + patch with `registryCredentialId` → capture the 400s.
2. Extract the field's schema/semantics from the pinned Render OpenAPI.
3. Record which surfaces already accept it vs reject it.

## Files

- (evidence only — note findings in the task/milestone)

## Acceptance criteria

- [ ] Rejection reproduced; Render contract + current per-surface coverage recorded.

## Out of scope

- The fix.
