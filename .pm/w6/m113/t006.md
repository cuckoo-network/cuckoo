---
id: w6/m113/t006
title: Test coverage
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m113/t004]
---

## Objective

Cover the quadrant `w6/m52` left untested, and keep the three quadrants it did test.

## Context

The matrix is two axes — did the newest attempt fail or was it canceled, and is an earlier release serving — and only one cell was ever wrong:

| | no prior release | prior release serving |
| --- | --- | --- |
| canceled | `Canceled` (m52 fixed) | `Running` (m52 asserted in prose, `app_types.go:1104-1112`) |
| failed | `Failed` (m52's control case) | **this milestone** |

`w6/m52`'s DoD asserted the bottom-left cell live as its explicit control. Its top-right cell is asserted only in a comment — check whether a test actually covers it, and add one if not.

## Steps

1. Add a test for the failing cell: build failure with `Status.Image` set and a serving release ⇒ the value t001 chose.
2. Keep the other three cells covered, including the prose-only cancel-with-prior-release case.
3. Cover `Hibernated` — a sleeping free-tier service must not report the new value.
4. Cover at least one non-web App kind, since the reconcile path is shared across web · private · worker · cron · static and the filing exercised only web.
5. Assert `Ready` / `ConditionBuild` are unchanged, per t003.
6. `cd lego/operator && make test`.

## Files

- `lego/operator/internal/controller/` tests
- `dashboard/src/features/services/` tests if the projection changed

## Acceptance criteria

- [ ] All four matrix cells covered, not just the new one.
- [ ] `Hibernated` covered.
- [ ] At least one non-web App kind covered.
- [ ] `Ready`/`ConditionBuild` regression assertions present.
- [ ] `make test` passes.

## Out of scope

- Coverage of code this milestone did not change.
