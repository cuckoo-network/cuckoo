---
id: w6/m113/t003
title: Apply the decision, guarding only the classes t002 identified
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m113/t002]
---

## Objective

Stop a failed rollout marking a serving service failed, for exactly the call sites t002 says can reach that state, and no others.

## Context

- The change lands in or around `lego/operator/internal/controller/app_controller.go:3912-3913` (`r.fail`), or per-caller if t002's classification argues for that — decide which and say why, since a change inside `r.fail` reaches all 47 sites at once.
- `r.fail` also sets the `Ready` condition and, for build failures, `ConditionBuild` attributed to the release generation (`w6/m100`). Those must keep working: the condition is where the failure detail lives once `phase` stops carrying it.
- The header must keep showing `Latest deploy: Build Failed` — DoD bullet 3. The fix hides nothing; it only stops attributing the failure to the service.

## Steps

1. Implement the guard for the classified sites, using t002's predicate.
2. Preserve the `Ready` / `ConditionBuild` behavior exactly — verify `w6/m100`'s generation attribution still holds.
3. Confirm the recovery path t001 specified: a subsequent successful deploy returns the service to `Running`.
4. Re-run the live repro from the filing: with `curl` returning 200, `GET /v1/services/<id>` must report the new value.

## Files

- `lego/operator/internal/controller/app_controller.go`
- `lego/types/v1alpha1/app_types.go` (if t001 chose a new enum value)

## Acceptance criteria

- [ ] Only t002's classified sites change behavior.
- [ ] `Ready` and `ConditionBuild` are unchanged, including `w6/m100`'s generation attribution.
- [ ] The live repro reports the new value while the service returns 200.
- [ ] A first-deploy build error with no prior release still reports `Failed`.

## Out of scope

- The clone failure that triggered the repro — a separate, cause-unverified claim (see the README's Unverified section).
