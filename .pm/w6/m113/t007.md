---
id: w6/m113/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m113/t006]
---

## Objective

Close the milestone once its Definition of done actually holds — not once the code is written.

## Context

- Every DoD bullet is a live probe against a deployed build. Confirm the fix commit is an ancestor of the commit `deploy.yml` last shipped and that the production image pin points at it, the way `w6/m98`'s closeout did.
- **The filing's fixture may not be usable.** `srv-da7o6ovvqdcc73bpn9hg` was left with two consecutive `build_failed` config-change deploys (`Container clone … exit code 90`) whose cause is unverified; a fresh service may be needed to reproduce DoD bullet 1. That service is also `w6/m110` and `w6/m111`'s fixture — coordinate before deleting or recreating it.
- t001's semantics decision and t002's 47-site classification must both be written into `../README.md` before closing (DoD bullet 7).

## Steps

1. Verify every DoD bullet live against a deployed build.
2. Confirm t001's decision and t002's table are recorded in `../README.md`.
3. Delete any `qa-` resources the DoD run created.
4. Set every remaining task's `status: done`.
5. Move each `tNNN.md` to `w6/m113/done/`.
6. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
7. Move the whole `w6/m113/` directory to `w6/done/m113/`.
8. Check `- [x]` for `m113` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live against a deployed build, not assumed.
- [ ] t001's decision and t002's table are in the README.
- [ ] Fixture coordination with `w6/m110` / `w6/m111` is settled, not duplicated.
- [ ] Milestone moved to `w6/done/m113/` with statuses in sync.

## Out of scope

- Remaining work becomes new board items.
