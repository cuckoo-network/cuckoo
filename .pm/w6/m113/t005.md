---
id: w6/m113/t005
title: Simplify — /simplify over the code this milestone changed
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m113/t004]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency — behavior-preserving only.

## Context

- Candidate: after t003 there may be two "is a release serving?" predicates — the new one and the existing `app_controller.go:577` check inside the cancel path. They should be one helper if they mean the same thing, and visibly different if they do not.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; record any skipped with the reason.

## Files

- Whatever this milestone changed.

## Acceptance criteria

- [ ] `/simplify` run; applied and skipped findings listed.
- [ ] No behavior change — t006's tests and the DoD probes still pass.

## Out of scope

- Refactors beyond this milestone's diff.
