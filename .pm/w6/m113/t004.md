---
id: w6/m113/t004
title: Render parity — phase across REST/GraphQL/MCP and the dashboard header
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m113/t003]
---

## Objective

Move every surface that exposes `phase` together, and record the divergence deliberately rather than resolving it by lookup.

## Context

- The filing captured all three surfaces agreeing on `Failed` (REST `phase`, GraphQL `service.phase`, dashboard header badge). They must agree on the new value too — a fix that moves only one is worse than the bug.
- `w6/m52` recorded the parity fact: **Render has no service-level status at all**; its header renders the latest deploy. So `phase` is a documented bex-only extra and this is a divergence to describe, not close.
- Whether MCP exposes `phase` was not checked during the hunt — establish it here.

## Steps

1. Probe REST, GraphQL and MCP for the same service in the new state and confirm one value.
2. Confirm the dashboard header renders it, with a colour/label that does not read as an outage, at desktop and narrow-mobile widths.
3. Update `docs/ADR018-render-parity.md` with what `phase` now means and why it has no Render counterpart.
4. Update `docs/ADR004-app-deployment.md` if it states the old phase lifecycle.

## Files

- `lego/backend/internal/apps/{rest,graphql,mcp}.go`
- `dashboard/src/features/services/lib/status.ts` and the header badge
- `docs/ADR018-render-parity.md`, `docs/ADR004-app-deployment.md`

## Acceptance criteria

- [ ] REST, GraphQL and MCP report one value; MCP's exposure is established either way.
- [ ] The header renders it correctly at both widths.
- [ ] ADR018 records the divergence; ADR004 matches the new lifecycle.

## Out of scope

- Changing Render's behavior expectations — there is none to match.
