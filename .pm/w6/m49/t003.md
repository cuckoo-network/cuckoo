---
id: w6/m49/t003
title: "Postgres: surface the conflict reason instead of the generic toast"
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m49/t001]
---

## Objective

`use-create-database.ts` has the identical shape and bug as Key Value's hook (t002) — fix it the same way, with the same t001 mechanism.

## Context

- Live-confirmed today: creating `qa-20260823-dbdup` twice → second call's GraphQL response: `{"data":{"createDatabase":null},"errors":[{"message":"conflict: a Postgres database named \"qa-20260823-dbdup\" already exists in this workspace","path":["createDatabase"]}]}`.
- `dashboard/src/features/databases/hooks/use-create-database.ts:84-92`:
  ```
  } catch (err) {
    if (isPaymentOnboardingCancelled(err)) return null;
    const msg = graphQLErrorMessage(err) ?? "";
    if (msg.toLowerCase().includes("workspace is limited")) {
      setCapLimit(msg);
    } else {
      toast.error(t("databases.createError", { name: input.name }));
    }
    return null;
  }
  ```
- Backend message source: `lego/backend/internal/postgres/service.go:451`.
- Apply the same decision t002 made (inline state vs. specific-message toast) — keep the two resource types' fixes consistent in shape since they started from identical code.

## Steps

1. Add a branch using t001's shared conflict-detection helper, alongside the existing cap-limit branch.
2. Surface the specific message inline or via a specific toast, mirroring t002's choice.
3. Reproduce live: create a Postgres instance, then attempt the same name again from the UI, confirm the specific reason is now shown.
4. Clean up any `qa-`-prefixed resources created for this check (Postgres provisioning is not instant — wait for the first instance to reach a stable state before the duplicate attempt if the conflict check isn't purely name-based at request time; the live probe this run showed it rejects synchronously, without waiting for provisioning, so this is likely a non-issue, but confirm).

## Files

- `dashboard/src/features/databases/hooks/use-create-database.ts`
- `dashboard/src/features/databases/locales/en.ts` (if a new inline message is added)
- The New Database / Create Database dialog component, if inline UI is added

## Acceptance criteria

- [ ] Live UI reproduction: duplicate Postgres name shows the specific conflict reason, not the generic toast.
- [ ] `graphQLErrorMessage`'s cap-limit branch is untouched and still works.
- [ ] `dashboard/`: `yarn typecheck && yarn lint` green.

## Out of scope

- Debounced pre-submit availability checking / suggested-name UX — same call as t002.
