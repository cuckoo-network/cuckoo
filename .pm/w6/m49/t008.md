---
id: w6/m49/t008
title: Simplify the touched code
worker: worker6
status: todo
estimate: 25m
depends_on: [w6/m49/t007]
---

## Objective

Run `/simplify` over everything this milestone changed (t001-t007) — reuse, simplification, efficiency, behavior-preserving only.

## Context

- t001-t006 deliberately introduce a shared helper/mechanism specifically to avoid five near-duplicate per-hook conflict-detection implementations — check that the actual result reads that way (one shared function called five times), not five copies that happened to be written in the same session.
- Compare the final shape of each fixed `use-create-*.ts` hook against `use-create-service.ts`'s existing, working pattern — look for an opportunity to extract a shared `useCreateResourceErrorHandling`-style hook if the five fixed call sites end up structurally identical, but only if that doesn't force an awkward abstraction over the two DoD-permitted difference (inline state vs. specific-message toast per t002's decision).

## Steps

1. Run `/simplify` scoped to the files this milestone touched.
2. Apply its findings.

## Files

- Every file touched by t001-t007.

## Acceptance criteria

- [ ] `/simplify` run and findings applied.
- [ ] `dashboard/`: `yarn typecheck && yarn lint` green; `cd lego/backend && go test ./...` + `make lint` green (if backend touched).

## Out of scope

- Behavior changes — this task is cleanup only.
