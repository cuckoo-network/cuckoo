---
id: w6/m103/t005
title: Simplify
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m103/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (`user-nav.tsx`, `i18n/init.ts`, `__root.tsx`, `document-head/index.ts`, and t002's new per-request isolation mechanism) — reuse, simplification, and efficiency cleanups only, behavior-preserving.

## Context

- Bug A's fix and the existing `language-switcher.tsx`/`use-language-hydration-sync.ts` sites now share the identical `ensureLanguage` → `changeLanguage` shape — check whether that shape is worth extracting into one shared helper (e.g. `switchLanguage(lang)` in `dashboard/src/i18n/`) so a fifth call site can't regress the same way a sixth time.
- t002's per-request isolation mechanism is new — check it doesn't duplicate logic already present in how `language`/`workspaceId`/`session` are already threaded per-request through `beforeLoad`/loader/context in `__root.tsx`.

## Steps

1. Run `/simplify` scoped to this milestone's diff.
2. If a shared `switchLanguage()` helper is warranted, extract it and update all call sites (`user-nav.tsx`, `language-switcher.tsx`, `use-language-hydration-sync.ts`) to use it — this directly forecloses a repeat of Bug A.
3. Apply any other findings from `/simplify` that are behavior-preserving.

## Files

- Whatever `/simplify` and step 2 touch — expected: `dashboard/src/i18n/` (possible new shared helper), `user-nav.tsx`, `language-switcher.tsx`, `use-language-hydration-sync.ts`.

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly dismissed with reasoning.
- [ ] If a shared switch-language helper was extracted, all 3 client-side call sites use it and no behavior changed.

## Out of scope

- Any change to t002's core per-request isolation approach — only cleanup of it, not re-architecture.
