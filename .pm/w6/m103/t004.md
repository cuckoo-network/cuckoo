---
id: w6/m103/t004
title: Render parity
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m103/t003]
---

## Objective

Confirm the fix doesn't introduce or leave any REST/GraphQL/MCP/UI drift, and record the parity finding for a feature Render itself doesn't have.

## Context

- This hunt already confirmed language/locale preference is purely client-persisted (a cookie + localStorage key, `i18nextLng`) with no backend GraphQL/REST/MCP field representing it — there is no server-side "language" mutation to keep in sync across surfaces.
- Render's own dashboard has no equivalent multi-language feature (English-only) — there is no render.com behavior to diverge from or match.

## Steps

1. Re-confirm (grep `lego/backend/internal` for any `language`/`locale` field on the user/member/session GraphQL or REST types) that no backend surface represents this state — if one is found, note it and check it doesn't disagree with the client's persisted value.
2. Record in this task (and, if warranted, in `docs/ADR018-render-parity.md`) that dashboard i18n is a bex-original enhancement with no Render equivalent — the "parity" finding is "n/a, not a divergence," not a gap to close.
3. Confirm the dashboard UI itself is internally consistent post-fix: the account-menu switcher (`user-nav.tsx`) and the pre-login `LanguageSwitcher` component both now follow the identical `ensureLanguage` → `changeLanguage` contract (t001 closes the one place they disagreed).

## Files

- No production code changes expected — this task is verification + a documentation note if `docs/ADR018-render-parity.md` needs one.

## Acceptance criteria

- [ ] Confirmed (via grep, not assumption) no REST/GraphQL/MCP field represents language/locale state.
- [ ] `user-nav.tsx` and `language-switcher.tsx` now follow the identical `ensureLanguage`-before-`changeLanguage` contract.
- [ ] Parity finding recorded (n/a / bex-original enhancement) rather than silently skipped.

## Out of scope

- Building a backend-persisted language preference — out of scope for this bug-fix milestone; a product decision if ever wanted.
