---
id: w6/m33/t002
title: ACL triple on GraphQL createEnvironment + MCP create_environment via the REST path
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m33/t001]
---

## Objective

GraphQL and MCP callers can create a protected/isolated/ip-gated environment atomically, through the same `validateACL`-before-create + `SetACL`-after pattern the REST handler uses (bad CIDR ⇒ clean 400, never an orphaned environment).

## Context

- REST reference implementation: `lego/backend/internal/environments/rest.go:157-193`.
- GraphQL args: `graphql.go:77-80`; MCP args: `mcp.go:37-40`.
- Consider lifting the shared create-with-ACL flow into the service layer so three adapters call one path.

## Steps

1. Add `protectedStatus`/`networkIsolationEnabled`/`ipAllowList` args to both surfaces.
2. Route through a shared Core helper; keep error shapes identical.

## Files

- `lego/backend/internal/environments/{service,graphql,mcp}.go` (+ `rest.go` if the flow is lifted)

## Acceptance criteria

- [ ] Cross-surface test: the same protected create succeeds identically on all three; invalid CIDR fails identically with no orphan

## Out of scope

- Enforcement semantics (w4/m28)
