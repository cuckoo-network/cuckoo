---
id: w6/m33/t003
title: Honor the documented list filters or 400 (inherit the absent-limit convention)
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m33/t001]
---

## Objective

`GET /v1/environments` honors each documented Render filter param per t001's feasibility map — filtering where the store supports it, returning an honest 400 for any accepted-but-unimplementable param — and inherits the absent-`limit` convention `w1/m42/t001` decides.

## Context

- Handler: `lego/backend/internal/environments/rest.go:125-148` — currently reads only `projectIds` (+ cursor/limit); `name`/`environmentId`/`createdBefore|After`/`updatedBefore|After`/`ownerId` are silently ignored.
- House rule: honored-or-400, never silent (w4/m23's audit `direction` precedent).
- Check `w1/m42/t001`'s outcome before choosing the no-params behavior; if it hasn't landed, coordinate rather than deciding independently.

## Steps

1. Implement the feasible filters in the list path.
2. 400 the infeasible ones with a message naming the unsupported param.
3. Apply the same filter set to GraphQL/MCP list args only if w2/m43 hasn't already restructured them — coordinate, don't collide.

## Files

- `lego/backend/internal/environments/{rest,service}.go`

## Acceptance criteria

- [ ] Table test: every documented param either filters (asserted results) or 400s; nothing is silently ignored

## Out of scope

- List paging mechanics (w2/m43)
