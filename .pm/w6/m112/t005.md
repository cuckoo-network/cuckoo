---
id: w6/m112/t005
title: Simplify — /simplify over the code this milestone changed
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m112/t004]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency — behavior-preserving only.

## Context

- Likely candidate: after t001 and t003, the target/targetName pair is set in several places (`core/audit.go`'s per-kind `Record*Effect` helpers, plus whatever t003 adds). If a common helper falls out, take it.
- t002's fix may leave a redundant audit write behind rather than removing it; check.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; record any skipped with the reason.

## Files

- Whatever this milestone changed.

## Acceptance criteria

- [ ] `/simplify` run; applied and skipped findings both listed.
- [ ] No behavior change — t006's tests and the DoD probes still pass.

## Out of scope

- Refactors beyond this milestone's diff.
