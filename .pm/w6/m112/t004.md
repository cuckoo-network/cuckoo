---
id: w6/m112/t004
title: Render parity — audit log across REST/GraphQL/MCP and the dashboard table
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m112/t001, w6/m112/t002, w6/m112/t003]
---

## Objective

Confirm this milestone stayed inside the divergence `w4/m26` deliberately chose, and that every surface exposing the audit log moved consistently.

## Context

- `w4/m26` (done, 2026-07-15) aligned REST to Render exactly and **deliberately** kept GraphQL on bex's flat dialect for the dashboard. This milestone adds a field to the flat dialect; it must not drag GraphQL toward Render's envelope, and it must not change REST.
- Whether MCP exposes the audit log at all was **not checked** during the hunt — establish it here rather than assuming.
- `docs/ADR018-render-parity.md`'s audit row was moved ◐→✅ by `w4/m26`; if this milestone changes what that row claims, update it.

## Steps

1. Establish whether `lego/backend/internal/audit/` has an MCP surface. If it does, check whether it carries the target and move it with the others; if it does not, record that as the answer.
2. Diff REST's response for the same rows before and after this milestone — it must be byte-identical apart from t003's new metadata key.
3. Confirm the dashboard's audit table renders the new field at desktop and narrow-mobile widths.
4. Compare against render.com's audit-log surface for the target concept and record any drift in `docs/ADR018-render-parity.md` as divergence with a reason.

## Files

- `lego/backend/internal/audit/{rest,graphql}.go`, plus `mcp.go` if it exists
- `dashboard/src/features/audit/`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] MCP's audit exposure is established as a fact, either way.
- [ ] REST is unchanged apart from t003's added metadata key.
- [ ] GraphQL stayed flat — no Render envelope introduced.
- [ ] The dashboard table renders correctly at both widths.
- [ ] Any render.com divergence is recorded with a reason.

## Out of scope

- Reopening `w4/m26`'s dialect decision.
