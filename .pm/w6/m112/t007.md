---
id: w6/m112/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m112/t006]
---

## Objective

Close the milestone once its Definition of done actually holds — not once the code is written.

## Context

- Every DoD bullet is a live probe or click on `dashboard.bex.co`, so the fix must be **deployed** first. Confirm the fix commit is an ancestor of the commit `deploy.yml` last shipped and that the production image pin points at it, the way `w6/m98`'s closeout did.
- The DoD's journey creates a project, an environment and an assignment. Delete all three afterwards and return any borrowed service to unassigned, exactly as the filing run did.
- Record t001's verb enumeration and t002's traced cause in this README before closing — both are DoD bullets, not incidental notes.

## Steps

1. Verify every DoD bullet live against a deployed build.
2. Write t001's `Target`-setting verb count and t002's traced cause into `../README.md`.
3. Delete the DoD run's `qa-` project, environment, and assignment; confirm the service is unassigned and still Running.
4. Set every remaining task's `status: done`.
5. Move each `tNNN.md` to `w6/m112/done/`.
6. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
7. Move the whole `w6/m112/` directory to `w6/done/m112/`.
8. Check `- [x]` for `m112` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live against a deployed build, not assumed.
- [ ] The verb count and the traced duplicate-row cause are recorded in the README.
- [ ] The DoD run's `qa-` resources are deleted and the borrowed service is unassigned.
- [ ] Milestone moved to `w6/done/m112/` with statuses in sync.

## Out of scope

- Remaining work becomes new board items.
