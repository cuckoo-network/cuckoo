---
id: w6/m108/t005
title: Simplify
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m108/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

`rollLinked` (`lego/backend/internal/envgroups/service.go:1037-1046`) is currently dead code that already implements the tolerance pattern t001 needs — decide during t001 whether to fold the loop into `rollLinked`/a shared helper (removing the dead code) or keep them separate with a documented reason. This task is the checkpoint to make sure that decision didn't leave orphaned code behind.

## Steps

1. Run `/simplify` scoped to the diff from t001-t004.
2. Confirm no dead code remains (`rollLinked` either reused or deliberately removed, not left orphaned).

## Files

- Whatever t001-t004 touched.

## Acceptance criteria

- [ ] `/simplify` run with no unresolved findings, or findings triaged and closed.

## Out of scope

- Unrelated simplification opportunities elsewhere in `envgroups`.
