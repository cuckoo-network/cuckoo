---
id: w6/m108/t006
title: Test coverage
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m108/t005]
---

## Objective

Add any meaningful test coverage gaps left after t003's regression tests — real behavior and failure modes, not tautological or snapshot-everything tests.

## Steps

1. Review t001-t004's diff for any behavior not yet covered by t003's tests (e.g. the `RebuildService`/`Trigger` not-found normalization from t001 Step 1, or the read-path prune from t002 if implemented separately from the write path).
2. Add tests only where they assert real behavior/failure modes.

## Files

- `lego/backend/internal/envgroups/patch_test.go`
- `lego/backend/internal/envgroups/service_test.go` (if the read-path prune needs its own coverage)

## Acceptance criteria

- [ ] `go test ./lego/backend/internal/envgroups/...` green.
- [ ] No trivial/tautological tests added.

## Out of scope

- Dashboard component tests unless t004 found a real dashboard-side gap.
