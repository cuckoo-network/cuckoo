---
id: w6/m108/t004
title: Render parity
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m108/t003]
---

## Objective

Confirm the fix preserves REST/GraphQL/MCP/dashboard agreement — they already share one implementation, and this task must prove the fix doesn't introduce a divergence.

## Context

- `lego/backend/internal/envgroups/{rest.go:115,graphql.go:302,mcp.go:203}` all call the same `s.PatchEnvironment` — confirmed this hunt. The dashboard's `useEnvGroupEnvironmentPatch` (`dashboard/src/features/env-groups/hooks/use-env-groups.ts:400-435`) consumes the GraphQL mutation's response fields directly.
- `EnvGroupView.ServiceLinks` is read the same way by REST `GET /v1/env-groups/{id}`, GraphQL `envGroup(id).serviceLinks`, and MCP.

## Steps

1. After t001-t003 land, exercise all three surfaces against the same stale-link scenario (a fake/test harness call, or a live QA repro if convenient) and confirm identical `rolledOut`/`failedServiceIds`/`serviceLinks` values.
2. Confirm the dashboard's "Configuration saved; rollout incomplete" alert no longer renders for this scenario (manual or component-test check against `service-environment-editor.tsx`).
3. Note any residual divergence as follow-up, don't silently accept it.

## Files

- `lego/backend/internal/envgroups/{rest.go,graphql.go,mcp.go}` (read-only verification)
- `dashboard/src/features/services/components/service-environment-editor.tsx`, `dashboard/src/features/env-groups/components/env-group-editors.tsx`

## Acceptance criteria

- [ ] REST, GraphQL, and MCP return identical `rolledOut`/`failedServiceIds` for the same stale-link scenario.
- [ ] REST, GraphQL, and MCP return identical `serviceLinks` post-prune.
- [ ] Dashboard no longer shows the "rollout incomplete" alert for a save whose only issue is a stale link.

## Out of scope

- Any Render-parity divergence found here that is unrelated to this milestone's bug — file it separately.
