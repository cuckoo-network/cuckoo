---
id: w6/m118/t006
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m118/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- The specific target: after t003 and t004, the replica bound exists in both the backend and the operator. They cannot share code (separate modules — `backend` never imports `operator`), so they will be two implementations of one rule. Make sure they read the limit from the same declared source (`lego/types/`, which both import) rather than each hardcoding it — that shared-types seam is exactly what `lego/CLAUDE.md`'s dependency rule exists for.
- `clampReplicas` may now have a plan-aware sibling; one of them should not survive.

## Steps

1. Run `/simplify` over the touched files.
2. Confirm the cap has one declared source that both modules read.

## Files

- files touched by t002/t003/t004

## Acceptance criteria

- [ ] `/simplify` run; behavior-preserving changes applied.
- [ ] One declared source for the limit, read by both modules.

## Out of scope

- Behavior changes.
