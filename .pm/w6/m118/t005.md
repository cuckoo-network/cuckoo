---
id: w6/m118/t005
title: Render parity
worker: worker6
status: in_progress # docs (ADR018/030 + render-artifacts) done; live over-cap re-run across REST/GraphQL/MCP + /instances pending a deploy
estimate: 20m
depends_on: [w6/m118/t003, w6/m118/t004]
---

## Objective

Confirm the cap behaves identically across REST, GraphQL, MCP and the dashboard, and record the decision in the parity ledger.

## Context

- ADR018 row 68 (manual scale) and row 69 (autoscaling config) both currently record parity without mentioning plan gating in either direction. Whatever t001 decided belongs in both rows.
- If bex's policy differs from Render's, that is a deliberate divergence and the ledger's job is to say so — `w2/m56` asked for exactly this evidence.

## Steps

1. Attempt an over-cap scale through REST, GraphQL and MCP; confirm the same status, code and message.
2. Confirm the dashboard's slider bound and its refusal path match.
3. Update ADR018 rows 68 and 69.
4. Re-run this milestone's live sequence on a fresh free service: scale, read `replicas`, read `/instances`, and confirm they agree.

## Files

- `docs/ADR018-render-parity.md`
- verification only otherwise

## Acceptance criteria

- [ ] All three API surfaces refuse identically.
- [ ] The dashboard agrees.
- [ ] ADR018 rows 68 and 69 state the policy and any divergence.
- [ ] The live sequence re-run and pasted.

## Out of scope

- Parity work unrelated to scaling.
