---
id: w6/m21/t001
title: App.Spec DockerfilePath + StartCommand fields
worker: worker6
status: todo
estimate: 45m
depends_on: []
---

## Objective

Add `DockerfilePath` and `StartCommand` fields to the `App` CRD spec.

## Context

- `lego/types/v1alpha1/app_types.go` — `App.Spec` currently has `Repo`, `RootDir`, `Branch`, `Builder` (`auto|dockerfile|buildpack`), no `DockerfilePath`/`StartCommand`.
- `lego/backend/internal/apps/mcp.go:106` already documents this gap in a comment.

## Steps

1. Add `DockerfilePath string` (optional, relative to `RootDir`, defaults to `Dockerfile`) and `StartCommand string` (optional, overrides image `ENTRYPOINT`/`CMD` when set) to `App.Spec` with kubebuilder markers.
2. Regenerate deepcopy (`make generate` from `lego/operator/`) and CRD yaml (`make manifests`).

## Files

- `lego/types/v1alpha1/app_types.go`
- generated: `zz_generated.deepcopy.go`, CRD yaml under `lego/operator/config/crd/`

## Acceptance criteria

- [ ] New fields present in the CRD, optional, zero-value backward-compatible with existing Apps.
- [ ] `make manifests generate` runs clean.

## Out of scope

- Consuming the fields (t002/t003/t004).
