---
id: w6/m35/t001
title: Pin Render's env-groups list-filter semantics; decide implement-vs-reject per param
worker: worker6
status: todo
estimate: 30m
depends_on: []
---

## Objective

From the pinned Render OpenAPI snapshot, extract the exact `GET /v1/env-groups` filter contract — param names, repeated/comma-separated forms, value types — and decide per param whether bex implements it or explicitly rejects it with a 400, recording the decision in the milestone.

## Context

- Render OpenAPI: `api-docs.render.com/openapi/render-public-api-1.json` (a fresh copy was fetched 2026-07-15; counts unchanged at 130 paths/207 ops).
- Precedent for both halves: the environments list shipped in w6/m33 (`lego/backend/internal/environments/rest.go`) implements name/ownerId/environmentId/createdBefore/After and 400s `updatedBefore`/`updatedAfter` as named-unsupported.
- Current state: `envgroups/rest.go:51-57` reads only single `ownerId` + paging.

## Steps

1. Extract the env-groups list params from the spec (forms + semantics).
2. Decide implement vs explicit-reject per param (implement unless the underlying data genuinely lacks the field — e.g. check whether env-group rows carry `updatedAt`).
3. Record the per-param decision table in this task or the milestone README.

## Files

- `docs/render-artifacts/` (if a capture note is warranted)
- `.pm/w6/m35/` (decision record)

## Acceptance criteria

- [ ] Per-param decision table exists with rationale
- [ ] Repeated/comma-separated form semantics pinned from the spec, not guessed

## Out of scope

- Implementation (t002)
