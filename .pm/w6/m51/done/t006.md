---
id: w6/m51/t006
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m51/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — must assert real behavior and failure modes, not trivial/tautological/snapshot-everything tests.

## Steps

1. Test: each t001-confirmed `SetXxx` verb creates a `deploys` row on call.
2. Test: a rebuild that fails reaches a terminal status, not stuck `building`.
3. Test: REST/GraphQL/MCP parity for the new tracking behavior.
4. Test: a verb t001 determined does NOT rebuild still does not spuriously create a deploy row (regression guard for the other direction).

## Acceptance criteria

- [ ] New tests fail red against the pre-fix behavior and pass after t002/t003's changes.
- [ ] Full backend suite green.

## Coverage added (2026-08-24)

| test | asserts |
| --- | --- |
| `apps/config_rollout_test.go` · `TestSettingsVerbsOpenDeployHistory` | eight rollout-forcing verbs each open exactly one `config_change` row, filed under the post-patch release generation |
| · `TestSettingsVerbStampsReleaseGeneration` | the row and the CR's release-generation annotation name the same release |
| · `TestOperationalVerbsOpenNoDeploy` | scale / idle TTL / display name / autoDeploy / notifyOnFail / suspend open **none** (the other-direction regression guard) |
| · `TestUnchangedSettingsValueOpensNoDeploy` | re-saving the current value is not a rollout — Settings pages resubmit whole forms |
| · `TestUnmanagedAppOpensNoDeploy` | a hand-applied App CR has no row to keep; tracking degrades, the verb still succeeds |
| · `TestDeployRowFailureDoesNotFailTheEdit` | a store failure is logged, not surfaced — the edit persisted and the rebuild is already rolling |
| · `TestSurfacesAgreeOnDeployHistory` | GraphQL and REST/MCP produce identical single rows |
| · `TestMultiFieldPatchIsOneDeploy` / `TestOperationalOnlyPatchIsNoDeploy` | batching: four fields → one row; operational-only → none |
| `envgroups/config_rollout_test.go` · `TestEnvGroupWritesOpenDeployHistory` | link, group-value change, and unlink each open a row; an idempotent re-link opens none |
| · `TestEnvGroupWritesWithoutTrackerStillApply` | CR-only mode degrades, never panics |
| `secrets/config_rollout_test.go` · `TestEnvWritesOpenDeployHistory` | env-var and secret-file writes open rows |
| · `TestSaveOnlyEnvPatchOpensNoDeploy` | `save_only` opens none; `deploy` opens one |
| `store/config_rollout_journey_test.go` · `TestConfigChangeFailsTerminallyThenSelfHeals` | the full failure → terminal → correction → live journey (t003) |
| `operator/.../release_identity_test.go` · `TestSpecRollsReleaseMatchesReleaseFingerprint` | the backend gate and the operator's release fingerprint agree field by field — drift is a red test |
| · `TestSpecRollsReleaseIgnoresNoOpChanges` | unchanged spec, nil→empty list, and an online disk grow all roll nothing |

Red-verified: reverting `patchFetched` to the untracked path fails the new `apps` tests (20 assertions).

Gates green: `cd lego/backend && go test ./...`, `make test` (operator), `make lint` (all four modules), `dashboard/yarn test` (363 files / 2605 tests), `cd lego/cli && go test ./...`.
