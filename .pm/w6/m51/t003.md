---
id: w6/m51/t003
title: "Verify stuck-phase recovery: a corrected Settings value must self-heal, not just retry"
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m51/t002]
---

## Objective

Confirm t002's fix actually closes the specific failure mode this milestone was filed from: a service stuck reporting `Building` after a broken Settings-driven rebuild, where even a subsequent *valid* correction to the same field did not unstick it.

## Context

- Live-reproduced this run: after `SetCommands` set an intentionally-broken start command and the resulting untracked rebuild failed readiness, `service.phase` stayed `Building` for 2+ minutes. Setting a second, *valid* start command afterward — itself confirmed persisted via a direct GraphQL read — did **not** change `phase` from `Building`. Only an explicit `triggerDeploy`-routed action (Restart) recovered it.
- If t002 makes every Settings edit open its own tracked deploy, this failure mode should disappear structurally (each edit is now its own deploy attempt with its own terminal status) — this task is the live check that it actually does, not an assumption.

## Steps

1. On a `qa-<yyyymmdd>-` test service, repeat the exact repro from `w6/m51`'s README: set a broken Start Command, confirm the resulting deploy reaches a terminal failed state (per t002), then set a valid Start Command and confirm — with no other action — a new deploy opens and reaches `live`.
2. Check the service header's phase/status badge converges to match the true state at each step (no UI lag beyond normal polling).
3. If any part of this doesn't self-heal, that's a residual bug for this task to fix or hand off, not to file as new.

## Files

- Whatever t002 touched.

## Acceptance criteria

- [ ] Live repro: broken → terminal failed status (not stuck `Building`).
- [ ] Live repro: valid correction afterward → new deploy → `Live`, with zero manual "Deploy"/"Restart" clicks.
- [ ] Service header badge matches backend state throughout, verified via a fresh page load (not just client cache).

## Out of scope

- Root-causing further if t002 already resolves this cleanly — this task is verification, not new design.
