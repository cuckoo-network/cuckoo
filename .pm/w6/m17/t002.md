---
id: w6/m17/t002
title: "Migrate ~45 call sites onto AuthorizeApp"
worker: worker6
status: todo
estimate: 1.5h
depends_on: [w6/m17/t001]
---

## Objective

Migrate every resource-scoped verb currently doing separate `Authorize` + `GetApp`/`fetchDatabase`/`fetchKeyValue` calls onto the single `AuthorizeApp` seam from t001.

## Context

- `w6/013` counts roughly 45 call sites across `apps/service.go`, `postgres/service.go`, `keyvalue/service.go`, and any other resource-scoped service package following the same pattern.
- Each site currently names its relation in two places (the `Authorize` call and, implicitly, whatever verb calls `GetApp` next) — after migration it's named once, so it can't drift.

## Steps

1. Sweep every resource-scoped verb (restart/suspend/resume/delete/scale/set-env-vars/etc. across apps, postgres, keyvalue) and replace `Authorize` + fetch with `AuthorizeApp`.
2. Confirm each site's audit-event vocabulary is unchanged post-migration.

## Files

- `lego/backend/internal/apps/service.go`
- `lego/backend/internal/postgres/service.go`
- `lego/backend/internal/keyvalue/service.go`
- any sibling resource-scoped service package with the same two-gate pattern

## Acceptance criteria

- [ ] All identified call sites use `AuthorizeApp`; no site still does the separate two-gate pattern for a resource-scoped verb.

## Out of scope

- `List(ownerID)`'s separate gating issue — t003.
