---
id: w6/m110/t006
title: Simplify — /simplify over the code this milestone changed
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m110/t005]
---

## Objective

Run `/simplify` over the code this milestone touched — reuse, simplification, efficiency — behavior-preserving only.

## Context

- The obvious candidate is the selector itself: before this milestone the same PromQL fragment existed in three files, which is how `metrics/source.go` and `usage/service.go` came to disagree about which name to interpolate. t001 decides shared-helper-vs-duplicated; this task is where the consequence gets tidied.
- `queryInstanceSeconds` and `queryInstanceSecondsStateful` already share `queryInstanceSecondsByMatcher`; check whether the App/stateful split still earns its keep after t002.

## Steps

1. Run `/simplify` over the diff this milestone produced.
2. Apply findings that are genuinely behavior-preserving; record any skipped with the reason.

## Files

- Whatever this milestone changed.

## Acceptance criteria

- [ ] `/simplify` run; applied findings listed, skipped findings listed with reasons.
- [ ] No behavior change — the tests from t007 and the DoD probes still pass.

## Out of scope

- Refactors beyond the code this milestone touched.
