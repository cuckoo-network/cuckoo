---
id: w6/m110/t008
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m110/t007]
---

## Objective

Close the milestone once its Definition of done is actually met — not merely once the code is written.

## Context

- The DoD's bullets are live probes against production, and several of them (bullets 1–5) need the fix **deployed**, not just merged. Confirm the fix commit is an ancestor of the commit `deploy.yml` last shipped and that the production image pin points at it, the way `w6/m98`'s closeout did.
- `srv-da7o6ovvqdcc73bpn9hg` (`qa-20260826-webhook-svc`) is the live reproduction fixture for bullets 2 and 3 and was deliberately **kept** rather than cleaned up, because it is the only service in the workspace with a name long enough to exhibit Defect B. Delete it as part of this closeout, once the DoD is verified.

## Steps

1. Verify every DoD bullet in `../README.md` against the live product, not against the diff.
2. Confirm the fix is actually deployed before claiming any live bullet.
3. Delete the `qa-20260826-webhook-svc` fixture service once bullets 2–4 are verified.
4. Set every remaining task's `status: done`.
5. Move each `tNNN.md` to `w6/m110/done/`.
6. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
7. Move the whole `w6/m110/` directory to `w6/done/m110/`.
8. Check `- [x]` for `m110` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live, not assumed, against a deployed build.
- [ ] The `qa-20260826-webhook-svc` fixture is deleted and confirmed gone from every list.
- [ ] Milestone moved to `w6/done/m110/` with all statuses in sync.

## Out of scope

- Any remaining work — that becomes new board items.

## Blocker update (2026-08-27/28)

The deploy precondition is now satisfied: `23c323f9` is an ancestor of `0ca167a8`, the production image pinned by `71fe9660`. Two non-code blockers remain:

1. **Credentials.** The live sweep needs a signed-in QA session, and this session has none: `scripts/qa-login.sh` exits **2** (`QA_EMAIL`/`QA_PASSWORD` absent from `.env`, though present as empty names in `.env.example:423-424`), and `BEX_API_TOKEN` returns **401** against `api.bex.co`. Fill those two names in `.env` to unblock — never through chat or a tool call.
2. **Fixture sequencing.** Keep `srv-da7o6ovvqdcc73bpn9hg` until m122 and m123 have closed or captured replacement evidence, as detailed below. m115 has already closed with its evidence recorded.

**One extra bullet is owed here beyond the DoD**, carried from `t005`: `/billing`'s charge tree must show compute per service with the Services subtotal including it. `t005` closed its other two criteria (cross-surface parity, ADR018 record) but could not open `/billing`, so that check belongs to this sweep.

## Triage finding (updated 2026-08-28) — do NOT delete the fixture until m122/m123 have taken what they need

Step 3 ("Delete the `qa-20260826-webhook-svc` fixture service") conflicts with two other **open** milestones that use this exact service (`srv-da7o6ovvqdcc73bpn9hg`, since renamed to `qa-20260826-webhook-renamed`) as their live evidence base. m115 also used it, but has since closed with its evidence recorded:

- **`w6/m122`** — its first Definition-of-done bullet is literally "on `https://dashboard.bex.co/services/srv-da7o6ovvqdcc73bpn9hg/events`, the `custom_domain_verified` event stamped `2026-08-27T11:54:07Z` renders in the Activity feed". Deleting the service destroys the only observed instance of the hidden event, and m122 becomes unverifiable without rebuilding a domain-verified fixture from scratch.
- **`w6/m115` (done)** — used it as one of the two renamed services proving REST/GraphQL `name` divergence (`{name:"qa-20260826-webhook-renamed", slug:"qa-20260826-webhook-svc"}`); its evidence is now preserved in the completed milestone.
- **`w6/m123`** — two of its three failed-deploy samples (`dep-da7rag7vqdcc73bpnin0`, `dep-da7r98nkrsvc73c3m5mg`) belong to it, and they are the private-repo/clone-phase half of the evidence that proves the defect is not buildkit-specific.

**Sequencing:** keep the fixture until m122 and m123 have either closed or captured their own replacements. Whichever of m110, m122 and m123 closes last owns the deletion. Do not treat this step as unconditional.
