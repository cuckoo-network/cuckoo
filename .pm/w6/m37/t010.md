---
id: w6/m37/t010
title: Test coverage
worker: worker6
status: todo
estimate: 1h
depends_on: [w6/m37/t008]
---

## Objective

Add meaningful tests proving the protected-Environment guard actually blocks and unblocks Postgres/KeyValue delete + suspend, matching the rigor of the existing Apps guard's test coverage.

## Context

- Reference: whatever test proves `apps.requireUnprotected`'s behavior today (grep for `requireUnprotected` or `ProtectedConfirmation` in `_test.go` files under `internal/apps`).
- Real behavior to assert, not tautologies: an unprotected resource's delete/suspend is unaffected; a protected resource's delete/suspend without `confirm` 400s with the exact phrase; the same call with the correct `confirm` succeeds; resume is never blocked; a DB-less (`s.Store == nil`) or environment-less resource is unaffected.

## Steps

1. Add unit/integration tests in `postgres` and `keyvalue` packages covering the above scenarios.
2. Add a REST/GraphQL/MCP-level test proving the `confirm` parameter reaches the guard on all three surfaces (can follow the shape of an existing cross-adapter parity test elsewhere in the codebase, e.g. `TestPostgresListPaginationAcrossRESTAndGraphQL`).
3. Add a dashboard test for the new confirm-dialog wiring (t007) if not already covered by the component's own test file.

## Files

- `lego/backend/internal/postgres/*_test.go`
- `lego/backend/internal/keyvalue/*_test.go`
- `dashboard/src/features/{databases,keyvalue}/components/__tests__/*`

## Acceptance criteria

- [ ] `cd lego/backend && go test ./...` green with new tests covering the scenarios above.
- [ ] Dashboard `yarn test` green with new/updated tests for the confirm-dialog flow.

## Out of scope

- Broad regression sweeps unrelated to this guard.
