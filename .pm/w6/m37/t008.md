---
id: w6/m37/t008
title: "Render parity: confirm-phrase semantics consistent across REST/GraphQL/MCP/UI"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m37/t006, w6/m37/t007]
---

## Objective

Check that the new protected-Environment guard on Postgres/KeyValue delete/suspend is consistent across every surface it touches, and consistent with how Apps already implement the same guard and with Render's documented protected-environment semantics.

## Context

- `docs/ADR018-render-parity.md` — Postgres CRUD row (currently ✅ across REST/GraphQL/MCP/UI) and the Environments/protected-status row.
- `docs/render-artifacts/protected-environments.md:15` — Render's captured contract that protection's restricted-action set covers datastore deletion.
- Compare field/error shape/semantics against `apps.Service`'s existing guard (`lego/backend/internal/apps/protection.go`).

## Steps

1. Confirm the error shape (`{id, message}`, named 400, exact phrase) is identical in spirit across postgres/keyvalue/apps.
2. Confirm REST/GraphQL/MCP (suspend) all reach the same underlying guard with no drift.
3. Update `docs/ADR018-render-parity.md`'s Postgres/KeyValue/Environments rows with a short note recording this milestone closed the divergence flagged by `w6/m19`.
4. Note the deliberate MCP-delete omission explicitly in the ledger (t006's finding) so it reads as a documented decision, not an oversight.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] ADR018 reflects the closed gap with a dated note.
- [ ] No REST/GraphQL/MCP/UI drift found, or any found drift is filed as a follow-up note.

## Out of scope

- Re-auditing unrelated parity rows.
