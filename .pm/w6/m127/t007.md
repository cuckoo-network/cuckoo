---
id: w6/m127/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m127/t005, w6/m127/t006]
---

## Objective

Close the milestone once every task is done **and** the DoD holds on production — including the one thing this hunt never did.

## Context

- **Create a free Key Value through the dashboard form**, not only through the API. That path was never exercised during the hunt; its `off` behaviour is a code reading. The whole milestone turns on it, so it must be observed at closeout.
- Compare it against an API-created free store from the same run, then `PATCH` one of them to a different `persistenceMode` and confirm the change lands.
- Delete both fixtures. This run's fixture deleted cleanly: `deleteKeyValue` → `true`, `GET` → 404.
- `t003`'s population count and decision must be recorded, not implied.

## Steps

1. Create one free Key Value through the dashboard form and one through `POST /v1/key-value`; compare `options.persistenceMode`.
2. `PATCH` one to a different mode; confirm on `GET`, and through GraphQL and MCP.
3. Confirm no en/zh string claims the free plan has no disk.
4. Delete both fixtures; confirm 404.
5. Set remaining tasks `status: done`, move each to `w6/m127/done/`, mark rows `— **DONE**`, set `**Status:** done`.
6. Move `w6/m127/` to `w6/done/m127/` (no stub) and check `- [x]` in `.pm/w6/README.md`; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m127/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Both creation paths exercised live and agreeing, with captures recorded.
- [ ] A live `PATCH` of `persistenceMode` confirmed on all three API surfaces.
- [ ] `t003`'s decision and population count recorded.
- [ ] Fixtures deleted (404 confirmed).
- [ ] Milestone moved to `w6/done/m127/` with no tombstone; status consistent in all three places.

## Out of scope

- Post-create eviction-policy editing in the dashboard, which ADR018 already tracks as its own follow-up.
