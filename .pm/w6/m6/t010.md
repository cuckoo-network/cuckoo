---
id: w6/m6/t010
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m6/t008, w6/m6/t009]
---

## Objective

Close the milestone: verify the definition of done actually holds, then move `w6/m6/` to `w6/done/m6/`.

## Context

- Per `.claude/commands/pm.md`: this task's completion **is** the move — set every remaining task's `status: done`, move each `tNNN.md` to `w6/m6/done/`, mark every row `— DONE` and `**Status:** done` in this milestone's `README.md`, move the whole `w6/m6/` directory to `w6/done/m6/`, and check `- [x]` in `w6/README.md`.

## Steps

1. Verify the DoD in this milestone's `README.md` is actually met (not just "code written").
2. Set every task's frontmatter `status: done`.
3. Move each `tNNN.md` to `w6/m6/done/`.
4. Update this `README.md`'s task table (`— DONE`) and `**Status:** done`.
5. Move `w6/m6/` → `w6/done/m6/`.
6. Check the `- [x]` box for m6 in `w6/README.md`.

## Files

- `.pm/w6/m6/**`
- `.pm/w6/README.md`

## Acceptance criteria

- [ ] DoD verified true, not assumed.
- [ ] Milestone moved to `w6/done/m6/` with no tombstone left at the old path.

## Out of scope

- N/A — this task is the close-out mechanism itself.
