# Batch the per-item registry-credential name lookup in `restServices`

`lego/backend/internal/apps/render.go:352-359` enriches each bound image-backed service with a `registryCredential {id, name}` summary by calling `RegistryCredentialName` (one `GetRegistryCredential` DB query) per item — inside the same loop where owners are batch-resolved via `resourcemeta.ResolveOwners` one line above (line 345). A page of bound services = N queries.

Fix: batch-resolve credential names for the page (one `IN (...)` lookup keyed by id, mirroring the owner batching) or memoize within the page. Sub-hour; low blast radius (only bound image-backed services trigger the lookup).

Source: `/pm-brainstorm` round 17, 2026-07-15 — shipped-diff consistency mine over `fd3bfad4..1daf9a3a` (the only unowned finding in an otherwise-clean sweep of the five fresh feature commits).
