---
id: w6/m135/t002
title: Implement coherent pair deletion in the authoritative claim store
worker: worker6
status: todo
estimate: 50m
depends_on: [w6/m135/t001]
---

## Objective

Apply the chosen lifecycle atomically in the domain-claim write path so no API adapter can produce a different pair result.

## Context

- Domain claims and `redirectForName` are stored under `lego/backend/internal/store/`.
- Direct add, pairing, delete, and dependent redirect cleanup must remain one coherent transaction boundary.

## Steps

1. Resolve pair/provenance state before deletion.
2. Delete or preserve the sibling according to `t001` in the authoritative store transaction.
3. Keep retries idempotent and clean dependent redirects without weakening ownership or uniqueness gates.

## Files

- `lego/backend/internal/store/store.go`
- `lego/backend/internal/apps/domains.go`

## Acceptance criteria

- [ ] Both delete directions implement the recorded rule atomically.
- [ ] No surviving sibling silently changes routing meaning.
- [ ] Authorization, ownership, uniqueness, TLS, and idempotency controls remain intact.

## Out of scope

- Redesigning custom-domain verification or certificate issuance.
