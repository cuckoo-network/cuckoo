---
id: w6/m114/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m114/t006]
---

## Objective

Close the milestone once its Definition of done actually holds — not once the code is written.

## Context

- DoD bullets 1–5 are live probes on `dashboard.bex.co`, so the fix must be **deployed** first. Confirm the fix commit is an ancestor of the commit `deploy.yml` last shipped and that the production image pin points at it, the way `w6/m98`'s closeout did.
- t001's caller count, t002's per-kind results and t003's emitter classification must all be written into `../README.md` before closing — they are DoD bullets, not incidental notes.
- t002 may have created `qa-` fixtures for the kinds the workspace lacks; confirm they are gone.

## Steps

1. Verify every DoD bullet live against a deployed build.
2. Confirm t001's caller count, t002's per-kind table and t003's classification are in `../README.md`.
3. Confirm every `qa-` fixture from t002 is deleted.
4. Set every remaining task's `status: done`.
5. Move each `tNNN.md` to `w6/m114/done/`.
6. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
7. Move the whole `w6/m114/` directory to `w6/done/m114/`.
8. Check `- [x]` for `m114` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live against a deployed build, not assumed.
- [ ] All three recorded artifacts (caller count, per-kind results, emitter classification) are in the README.
- [ ] No `qa-` fixtures remain.
- [ ] Milestone moved to `w6/done/m114/` with statuses in sync.

## Out of scope

- Remaining work becomes new board items.
