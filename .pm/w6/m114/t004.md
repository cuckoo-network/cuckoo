---
id: w6/m114/t004
title: Render parity — the generate verb and the manifest's name field across surfaces
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m114/t002, w6/m114/t003]
---

## Objective

Move every surface exposing the generate verb together, and check the manifest against the `render.yaml` contract it claims to speak.

## Context

- t001 establishes the caller count. If REST/GraphQL/MCP each expose generate, all three must emit the corrected name — one fix in the shared producer should cover them, but that must be verified rather than assumed.
- `docs/ADR049-render-yaml-parity.md` owns the `render.yaml` contract. The exported file claims to be a `render.yaml` ("export as a render.yaml you can commit to a repo"), so the name field's semantics should match what render.com's own Blueprint expects for `services[].name`.
- `docs/ADR018-render-parity.md`'s blueprint row records where bex diverges.

## Steps

1. Exercise generate on every entrypoint t001 found; confirm identical output.
2. Compare `services[].name` semantics against render.com's Blueprint spec — Render's `name` is the service name a user sees; confirm bex now matches and record any deliberate difference.
3. Update `docs/ADR049-render-yaml-parity.md` / `docs/ADR018-render-parity.md` if the exporter's contract changed.
4. Confirm the dashboard dialog's copy still matches what it hands over — it promises a file you can "commit to a repo and connect as a Blueprint", which only becomes true with this milestone.

## Files

- `lego/backend/internal/apps/blueprint_generate.go` and its adapters
- `dashboard/src/features/blueprints/`
- `docs/ADR049-render-yaml-parity.md`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Every entrypoint emits identical, valid output.
- [ ] `services[].name` semantics are compared against Render's spec and any divergence is recorded.
- [ ] The ADRs match the shipped contract.

## Out of scope

- New blueprint fields.
