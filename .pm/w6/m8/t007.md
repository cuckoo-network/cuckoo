---
id: w6/m8/t007
title: "Closeout — verify DoD met, then move the milestone to done/"
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m8/t005, w6/m8/t006]
---

## Objective

Verify the milestone's definition of done actually holds, then close it out per `.pm/CLAUDE.md` conventions.

## Steps

1. Confirm the bex-api SA cannot read Secrets/pods/logs outside its served namespaces, `app.bex.co` verbs remain cluster-wide, and existing logs/env-vars/managed-DB tests are green.
2. Set every task's `status: done`, move each `tNNN.md` to `w6/m8/done/`.
3. Mark every row `— DONE` and `**Status:** done` in this README.
4. Move the whole `w6/m8/` directory to `w6/done/m8/`.
5. Check `- [x]` for m8 in `w6/README.md`.

## Acceptance criteria

- [ ] Milestone's observable end state is real, not just code-complete.
- [ ] Board moved and status synced in all three places.
