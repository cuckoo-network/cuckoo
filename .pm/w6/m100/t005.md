---
id: w6/m100/t005
title: Test coverage
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m100/t003]
---

## Objective

Confirm the regression tests added in t002 actually assert real behavior and failure modes (not tautological/snapshot tests), and fill any remaining gap in coverage of the fixed mechanism before closeout.

## Context

- t002 already adds the core regression tests (the race repro, the `supersededDeployStatus` consumer test, the Rollback call site). This task is the coverage-quality pass: confirm each test would fail against the pre-fix code (re-check against a stashed pre-fix diff if convenient), and that no test merely asserts a mock was called rather than the actual generation/status outcome.

## Steps

1. Review t002's tests against pre-fix behavior (temporarily revert t001's fix locally, confirm the new tests go red, restore the fix).
2. Check for any of the milestone's "Unverified" call sites (webhook auto-deploy, Blueprint sync, env-var/secret restart) that can be cheaply covered by an additional table-driven case reusing t002's fixture, without scope-creeping into a full live-hunt-style verification of each.
3. Run `make test` (operator, from `lego/operator/`), `cd lego/backend && go test ./...`, and `make lint` (all four modules) — all green.

## Files

- Test files added/extended in t001/t002.

## Acceptance criteria

- [ ] Each new regression test confirmed red against pre-fix code, green after.
- [ ] `make test`, backend `go test ./...`, and `make lint` all green.

## Out of scope

- Full live-hunt reproduction of every unverified call site — that's future work, not this milestone's closeout gate.
