---
id: w6/m100/t003
title: Render parity check across REST/GraphQL/MCP/dashboard for the deploy-status projection
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m100/t002]
---

## Objective

Confirm the fix keeps REST, GraphQL, and MCP deploy-status surfaces agreeing (they already agreed byte-for-byte pre-fix per this hunt's own cross-check), and that the dashboard's Deploys list/detail page and Events feed correctly reflect a formerly-stuck deploy now reaching a real terminal state — no separate UI fix needed, but confirm the symptom (the Events feed's "Deploy started / In Progress" label for a queued row) disappears on its own once the backend actually progresses the row.

## Context

- `lego/backend/internal/deploys/` — REST + GraphQL + MCP fragments over the same `Service` root (`docs/ADR006-bex-api.md`).
- `dashboard/src/features/deploys/` — `deploy-detail-page.tsx`, `deploys-list-page.tsx`, `deploy-header.tsx` read the same projected fields this milestone touches.
- Compare against render.com's own behavior for a deploy queued behind a failed sibling (if reproducible on a real Render account) — note any deliberate divergence in `docs/ADR018-render-parity.md` if this changes wire semantics; otherwise note "no wire-shape change, only a liveness fix."

## Steps

1. Query REST (`GET /v1/services/{id}/deploys/{id}`) and GraphQL (`deploy(...)`) for a deploy that goes through the fixed race and confirm the two responses still agree field-for-field.
2. Confirm MCP's deploy-status tool returns the same shape.
3. Manually or via test drive the dashboard's Deploys list, detail page, and Events feed for the same deploy and confirm the "Deploy started / In Progress" label now corresponds to an actually-progressing deploy (not a stuck one) — no code change expected here unless something new surfaces.
4. Record any drift found in `docs/ADR018-render-parity.md`.

## Files

- `lego/backend/internal/deploys/rest.go`, `graphql.go`, `mcp.go`
- `dashboard/src/features/deploys/`
- `docs/ADR018-render-parity.md` (only if drift found)

## Acceptance criteria

- [ ] REST/GraphQL/MCP deploy-status responses agree post-fix (spot-checked against a real race, not just unit-level).
- [ ] Dashboard Deploys list/detail/Events views show a consistent, progressing state for a deploy that goes through the fixed race.
- [ ] Any Render-parity drift found is recorded in `docs/ADR018-render-parity.md`; if none, this task notes that explicitly.

## Out of scope

- New UI features. This is a verification pass, not a redesign.
