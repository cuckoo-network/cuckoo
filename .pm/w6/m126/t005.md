---
id: w6/m126/t005
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m126/t004]
---

## Objective

Run `/simplify` over this milestone's diff — the project handlers, any new mapper helper, and the conformance additions — behaviour-preserving.

## Context

- `lego/backend/internal/projects/rest.go`, possibly `lego/backend/internal/core/http.go`, and `lego/backend/internal/api/conformance_test.go`.
- If `t001` added a ctx+error mapper variant, the obvious tidy-up is folding it together with `HandleMapped`. Weigh that against churn on every existing caller — environments included — and say why.
- The `identity` pass-through at `rest.go:77` should be gone or clearly justified after `t001`; leaving it lying around invites the same mistake again.

## Steps

1. Run `/simplify` over the diff.
2. Apply behaviour-preserving improvements only.
3. Re-run `cd lego/backend && go test ./...`.

## Files

- The files `t001`–`t003` touched

## Acceptance criteria

- [ ] No behaviour change; `t006`'s tests still pass.
- [ ] `identity` is removed or justified.
- [ ] Existing `HandleMapped` callers are unaffected, or covered by regression tests if the helper changed.

## Out of scope

- Restructuring the projects feature.
