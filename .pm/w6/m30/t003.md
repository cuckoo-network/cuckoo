---
id: w6/m30/t003
title: "Surface the redirect: domain views + dashboard copy + ADR005"
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m30/t002]
---

## Objective

Make the behavior legible: the custom-domain views (REST/GraphQL/MCP) mark the auto-paired sibling as redirecting to its canonical (mirroring however Render's domain object expresses it — from t001/the pinned spec), the dashboard add-dialog's pairing copy says "redirects to" instead of "served identically", and ADR005's divergence paragraph (line ~60) is rewritten to describe the shipped behavior.

## Files

- `lego/backend/internal/` (domains feature views), `dashboard/src/features/` (domains dialog), `docs/ADR005-custom-domain.md`

## Acceptance criteria

- [ ] All three API surfaces + the dialog reflect the redirect; ADR005 no longer documents the divergence.

## Out of scope

- Letting tenants choose the redirect direction beyond Render's own semantics.
