---
id: w6/m125/t005
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m125/t004]
---

## Objective

Run `/simplify` over this milestone's diff — the resolver's three indexing loops and the plan branch — behaviour-preserving.

## Context

- `lego/backend/internal/apps/blueprint_state_plan.go` is the whole footprint.
- After `t001` the three loops (services, databases, key-values) differ only in type and accessor, and all three now carry the same duplicate guard. That is the obvious candidate for a shared helper — but keep the accessor difference explicit and commented, since it is exactly what was wrong before and a future reader must not "simplify" services back onto `a.Name`.

## Steps

1. Run `/simplify` over the diff.
2. Apply behaviour-preserving improvements only.
3. Re-run `cd lego/backend && go test ./...`.

## Files

- `lego/backend/internal/apps/blueprint_state_plan.go`

## Acceptance criteria

- [ ] No behaviour change; `t006`'s tests still pass.
- [ ] The public-name-versus-object-name distinction stays explicit in the code, not buried in a generic helper.

## Out of scope

- Refactoring the Blueprint planner's architecture.
