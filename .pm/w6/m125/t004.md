---
id: w6/m125/t004
title: Render parity — the plan across REST validate/preview, GraphQL, MCP and the dashboard
worker: worker6
status: todo
estimate: 25m
depends_on: [w6/m125/t002, w6/m125/t003]
---

## Objective

The plan changes shape in practice (creates become updates), so confirm every surface that exposes it moves together and that the result matches Render's Blueprint preview.

## Context

- REST reaches the plan two ways, both through `blueprintValidationFor`: `POST /v1/blueprints/validate` (multipart) and `POST /v1/blueprints/preview` (repo-based, `blueprint.go:370`). One fix serves both — verify rather than assume.
- **Whether GraphQL and MCP expose the plan at all was never checked this run.** That is this task's first question, not a given.
- **The dashboard's Blueprint page was never opened this run** either; check what it renders for a plan and whether it distinguishes create from update.
- Render's Blueprint preview distinguishes creating a resource from updating one, so this restores parity rather than adding a divergence. `docs/ADR018-render-parity.md` likely needs no row — record the reasoning.

## Steps

1. Confirm `validate` and `preview` return the same plan for the same manifest.
2. Establish whether GraphQL and MCP expose a plan; if they do, confirm it matches.
3. Open the dashboard's Blueprint page and check how it renders create versus update.
4. Decide on an `ADR018` row and record the reasoning either way.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR006-bex-api.md` — only if a line is warranted

## Acceptance criteria

- [ ] `validate` and `preview` agree, verified by probe.
- [ ] GraphQL/MCP plan exposure is established as a fact, not assumed.
- [ ] The dashboard's rendering of create vs update is checked.
- [ ] A recorded decision on the ledger row.

## Out of scope

- Unrelated Blueprint parity gaps surfaced while reading the ledger — file separately.
