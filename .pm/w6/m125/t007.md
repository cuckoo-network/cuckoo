---
id: w6/m125/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m125/t005, w6/m125/t006]
---

## Objective

Close the milestone once every task is done **and** the DoD holds against production.

## Context

- The DoD is a two-apply live fixture CI cannot run. Recipe, verified this run: `POST /v1/blueprints/validate` (multipart, `file=render.yaml`) → `POST /v1/blueprints/deploy` → re-plan → re-apply the modified manifest.
- **Clean up both objects.** `deploy` creates a **Blueprint record** named after the repo in addition to the service — this run's left `express-hello-world` behind until it was removed. REST `DELETE /v1/blueprints/{id}` is currently unreachable from a browser (`w6/m121`), so use the GraphQL `disconnectBlueprint(id:)` mutation; `deleteBlueprint` does not exist.
- Two things were never observed this run: GraphQL/MCP plan exposure and the dashboard's Blueprint page. Record their status rather than implying they were checked.

## Steps

1. Run the two-apply fixture on production; confirm the second plan is an update, that a changed manifest plans differently, and that apply matches.
2. Confirm a first-time service still plans `create`.
3. Delete the fixture service **and** disconnect the Blueprint record; confirm both are gone.
4. Record the GraphQL/MCP/dashboard verification status plainly.
5. Set remaining tasks `status: done`, move each to `w6/m125/done/`, mark rows `— **DONE**`, set `**Status:** done`.
6. Move `w6/m125/` to `w6/done/m125/` (no stub) and check `- [x]` in `.pm/w6/README.md`; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m125/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Live two-apply DoD re-run with captures recorded.
- [ ] Fixture service deleted and Blueprint record disconnected — both confirmed gone.
- [ ] GraphQL/MCP/dashboard status stated plainly.
- [ ] Milestone moved to `w6/done/m125/` with no tombstone; status consistent in all three places.

## Out of scope

- `w6/m121`'s CORS defect, which merely made REST cleanup awkward here.
