---
id: w6/m12/t001
title: "Capture Render's change-plan flow → docs/render-artifacts/workspace-plan-change.md"
worker: worker6
status: todo
estimate: 30m
depends_on: []
---

## Objective

Capture how Render's dashboard changes a workspace plan (entry point, plan cards, downgrade guards and their exact copy, what's blocked mid-flow) into a reference artifact, and decide bex's surface shape from it — before any code.

## Context

- `RESEARCH-workspaces.md` findings 1/4/5: post-2026-04-23 lineup Hobby ($0) · Pro · Scale · Enterprise; Hobby = 1 member / 25 services / 5 per user; roles plan-gated (Pro: Admin+Developer; Scale+: Contributor/Viewer/Billing).
- Precedent artifact + method: `docs/render-artifacts/workspace-lifecycle.md` (live authenticated capture, w6/m5).
- Expected decision: GraphQL mutation + dashboard only — Render's REST owners surface is read-only (research finding 9) and its MCP has no workspace mutations, so REST/MCP absence is the parity-correct shape.

## Steps

1. Capture the change-plan flow from a live authenticated dashboard.render.com session (fall back to render.com/docs pages if a paid flow can't be entered without a card — note what's inferred vs seen).
2. Record entry point, dialog/page layout, guard conditions + exact error copy, and whether downgrade is blocked or destructive.
3. Write `docs/render-artifacts/workspace-plan-change.md`; state bex's decided shape (mutation name, args, guard copy) at the top.

## Files

- `docs/render-artifacts/workspace-plan-change.md` (new)

## Acceptance criteria

- [ ] Artifact exists with capture evidence and an explicit seen-vs-inferred note
- [ ] bex surface shape (mutation + guards + copy) decided and written down

## Out of scope

- Billing/payment capture beyond noting where Render inserts it (bex ships no payment step)
