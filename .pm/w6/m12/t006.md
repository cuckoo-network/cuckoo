---
id: w6/m12/t006
title: "Render parity: sweep GraphQL/UI vs the capture; confirm REST/MCP absence is correct"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m12/t005]
---

## Objective

Standing parity check: the shipped change-plan surface matches `docs/render-artifacts/workspace-plan-change.md` (flow, guards, copy), the role gating matches Render's per-plan sets, and REST/MCP deliberately expose nothing (matching Render's read-only REST owners surface and mutation-less workspace MCP). Flag any drift as follow-up work rather than silently diverging.

## Context

- docs/ADR006-bex-api.md (one core, thin adapters); docs/ADR018-render-parity.md — update the workspace-related rows with the shipped state + evidence pointers.

## Steps

1. Compare dialog flow + guard copy against the t001 capture; compare role sets against RESEARCH finding 5.
2. Confirm no REST/MCP additions crept in; confirm GraphQL error shapes match the lifecycle mutations'.
3. Update ADR018's relevant rows/backlog; file follow-up notes for any drift.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] ADR018 updated with evidence; any drift filed as follow-up, none silently accepted

## Out of scope

- Fixing drift beyond copy-level tweaks (file notes instead)
