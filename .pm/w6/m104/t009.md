---
id: w6/m104/t009
title: Test coverage
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m104/t007]
---

## Objective

Add meaningful regression tests for the behavior this milestone shipped, beyond what t003-t006 already wrote inline, closing any remaining gaps.

## Context

- t003/t004 already add the two core behavior tests (no-prior-release, prior-release-exists). This task is the sweep for anything those left uncovered — e.g. the annotation-write unit test in isolation, the doc-comment/type-level change, and the adjacent-class regression (a genuine build failure must still report `PhaseFailed`, and an already-terminal deploy's cancel must still 409).

## Steps

1. Review t003-t006's tests for gaps.
2. Add tests for the adjacent classes explicitly (genuine failure still `PhaseFailed`; already-terminal cancel still 409) if not already covered.
3. Ensure tests assert real behavior and failure modes — no tautological or snapshot-everything tests.

## Files

- `lego/backend/internal/deploys/*_test.go`
- `lego/operator/internal/controller/*_test.go`

## Acceptance criteria

- [ ] `go test ./...` (operator + backend) green.
- [ ] `make lint` (all four modules) green.
- [ ] Adjacent-class regressions (genuine failure, already-terminal cancel) have explicit control-case tests.

## Out of scope

- New behavior (this task is coverage-only).
