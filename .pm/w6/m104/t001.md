---
id: w6/m104/t001
title: Decide and name the target behavior for both branches of canceling an image-backed deploy, checking Render parity first
worker: worker6
status: todo
estimate: 20m
depends_on: []
---

## Objective

Before touching code, write down the exact target behavior for canceling an image-backed service's deploy in both branches (no prior successful release; prior successful release exists), and check what render.com actually does for the same action so the fix is a stated decision, not an inferred "make it consistent."

## Context

- `lego/backend/internal/deploys/service.go:624-638` — the existing doc comment on `Cancel` already asserts "matches Render: an image-backed deploy has no build to interrupt in the first place." That claim is about the BUILD only; it says nothing about what phase/state the service should settle to afterward, or whether a mid-rollout image change can be reverted.
- `w6/m52` (`.pm/w6/done/m52/README.md`) already made this exact decision for repo-backed Apps: no-prior-release cancel → `PhaseCanceled`; prior-release cancel → keep serving the previous release, stay `Running`. The question here is whether the same two target states apply unchanged to image-backed Apps, or whether the image case needs its own distinct value/reasoning.
- Render has no live account available this run — reason from Render's public docs/behavior description if reachable, otherwise state explicitly that the decision is being made on bex's own product logic (consistency with `m52`) rather than confirmed Render parity, and record that as a known gap for the Render parity closing task (t007) to close later if a Render account becomes available.

## Steps

1. Write down, in this task's Implementation section, the exact target `phase`/status value for: (a) canceling an image-backed App's very first deploy with no prior successful release, (b) canceling a later deploy of an image-backed App that has a prior successful release.
2. State explicitly whether these are the same values `m52` established for repo-backed Apps (recommended: yes, for consistency of the `phase` field's meaning) or if there's a reason to diverge.
3. Note the Render-parity confidence level (checked live / reasoned from docs / assumed) for the record.

## Files

- None (decision-only task) — record the decision in this file's Implementation section for t002-t006 to reference.

## Acceptance criteria

- [ ] Both target values are written down with reasoning, not just asserted.
- [ ] The Render-parity confidence level is stated honestly.

## Out of scope

- Implementation (t002+).
