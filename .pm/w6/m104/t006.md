---
id: w6/m104/t006
title: Confirm REST/GraphQL/MCP show no divergence and the Cancel-dialog copy is accurate for the prior-release branch
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m104/t004]
---

## Objective

Confirm the fix is visible identically over every wire surface, and that the dashboard's existing Cancel confirmation copy is now truthful for image-backed services too.

## Context

- `lego/backend/internal/deploys/rest.go`, `graphql.go`, and `mcp.go` all call the single `Service.Cancel` method (`lego/backend/internal/deploys/service.go:639`) — confirmed by grep this run. No adapter-level divergence is expected, but confirm rather than assume, per the milestone's own standard.
- The dashboard's Cancel confirmation dialog already reads "The in-progress deploy will be stopped. The last successful deploy remains live." — this sentence was FALSE for an image-backed service before this fix (nothing was stopped, nothing stayed live in a meaningful sense for the no-prior-release case). Confirm it's now true for both branches; no dashboard copy change should be needed if t002-t004 land correctly, but verify rather than assume.

## Steps

1. Call `POST /v1/services/{id}/deploys/{deployId}/cancel` (REST), the `cancelDeploy` GraphQL mutation, and MCP's `cancel_deploy` tool against the same image-backed scenario and confirm identical resulting `phase`/deploy `status` across all three.
2. Re-run the dashboard UI flow (Cancel button + confirmation dialog) against an image-backed service in both branches and confirm the dialog's promise now holds.
3. If any surface disagrees, that's a new, separately-tracked finding — do not silently patch it into this task's scope without recording it.

## Files

- `lego/backend/internal/deploys/rest.go`, `graphql.go`, `mcp.go` (read-only verification, no expected changes)
- `dashboard/src/features/services/` (Cancel dialog — read-only verification, no expected changes)

## Acceptance criteria

- [ ] REST/GraphQL/MCP cancel calls produce identical `phase`/`status` results for the same image-backed scenario.
- [ ] The dashboard's Cancel dialog copy is verified accurate for both branches (no code change expected, but verified live).

## Out of scope

- Any dashboard code change (none expected).
