---
id: w6/m36/t001
title: matchesTimeWindow — stop silently dropping legacy groups with missing timestamps
worker: worker6
status: todo
estimate: 30m
depends_on: []
---

## Objective

Decide and implement the legacy-row semantics for env-group time filters: a group whose `createdAt`/`updatedAt` meta is empty (pre-metadata rows) must not be excluded by a parse-error fallthrough. Default direction: treat a missing timestamp as "unknown — don't exclude on that axis"; if explicit exclusion is chosen instead, document it at the filter and in m35's dialect notes.

## Context

- `lego/backend/internal/envgroups/service.go:215-224` — `matchesTimeWindow` does `time.Parse(RFC3339, raw)` and returns `false` on error; empty string ⇒ always excluded under any time filter.
- w6/m35 shipped the filters yesterday with strict named 400s for *caller* timestamp errors — that stays; this is about *stored* legacy values.

## Steps

1. Distinguish empty/missing from malformed stored values; implement the chosen semantics.
2. Unit test: legacy group + each of the four time filters.

## Files

- `lego/backend/internal/envgroups/service.go` + tests

## Acceptance criteria

- [ ] A group with empty timestamps appears (or is documented-and-tested excluded) under every time filter; no parse-error fallthrough decides visibility.

## Out of scope

- Backfilling timestamps onto legacy rows (separate concern; note if attractive).
