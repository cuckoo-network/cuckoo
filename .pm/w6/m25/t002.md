---
id: w6/m25/t002
title: REST: create + `PATCH` + read in `serviceDetails`
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m25/t001]
---

## Objective

Accept `maxShutdownDelaySeconds` on `POST /v1/services` and `PATCH /v1/services/{id}` and return it on the service object — in Render's exact placement (`serviceDetails`, per the pinned spec's webServiceDetails/privateServiceDetails/backgroundWorkerDetails POST/PATCH schemas). Out-of-range ⇒ named 400 (don't rely on CRD validation alone for the API error shape). Reject on cron/static types with a named error.

## Files

- `lego/backend/internal/apps/{rest.go,service.go}` (+ `AppView`)

## Acceptance criteria

- [ ] Create/PATCH/read round-trip for the three types; 400 for range/type violations; named rejection on cron/static.
