---
id: w6/m25/t003
title: GraphQL + MCP mirrors
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m25/t002]
---

## Objective

Thin adapters over the same verb: GraphQL `createService(maxShutdownDelaySeconds:)` + setter mutation (the `setHealthCheckPath` grammar), MCP create args + `set_max_shutdown_delay` tool (bex extension if Render's MCP lacks one — check and note).

## Files

- `lego/backend/internal/apps/{graphql.go,mcp.go}`

## Acceptance criteria

- [ ] Adapter-parity test: identical field/semantics/errors on all three surfaces.
