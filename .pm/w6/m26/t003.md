---
id: w6/m26/t003
title: Add authorization, lifecycle, and wire-shape regressions
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m26/t002]
---

## Objective

Lock the instance endpoint's real behavior with fake Kubernetes client and HTTP-router tests covering populated, empty, lifecycle-transition, missing-service, and foreign-workspace cases.

## Context

- A 200 with `null`, a response containing extra internal fields, or an authorization check against the caller's default workspace would all appear superficially implemented while still breaking parity or isolation.
- Use timestamp values that prove `createdAt` comes from Pod metadata and verify the response with a Render-compatible struct, not only generic JSON.

## Steps

1. Exercise the service method with labeled and unrelated Pods.
2. Exercise the exact REST path through `RegisterREST`.
3. Assert empty-array and cross-workspace/nonexistent failure modes.

## Files

- `lego/backend/internal/apps/*_test.go`

## Acceptance criteria

- [ ] Tests fail if the route disappears, returns `null`, changes field names/types, includes unrelated Pods, or bypasses authorization.
- [ ] Tests cover the lifecycle inclusion policy selected in t001.

## Out of scope

- Tautological snapshots or tests against a hand-built route that bypass the real router.
