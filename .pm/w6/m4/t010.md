---
id: w6/m4/t010
title: "Closeout: verify DoD, mark done, move to done/"
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m4/t009]
---

## Objective

Verify the milestone's Definition of Done actually holds, then close it out: mark every task done, move task files to `done/`, update the milestone `README.md` status, move the whole milestone directory to `w6/done/m4/`, and check its box in `w6/README.md`.

## Context

- DoD (from the milestone README): deleting a workspace tears down its managed Postgres databases, managed KeyValue stores, and OpenBao secrets; `ownerId` filtering/listing on Postgres and KeyValue works correctly and matches the Apps contract; a tenant's own App can reach its own managed Valkey instance over the network.

## Steps

1. Re-run t006's integration test (or a live equivalent against a real/mock cluster) to reconfirm the DoD holds.
2. If it holds: set every task's `status: done`, move each `tNNN.md` to `w6/m4/done/`, mark every row `— DONE` and `**Status:** done` in the milestone README, move `w6/m4/` to `w6/done/m4/`, check `- [x]` in `w6/README.md`.
3. If the DoD does not hold, do not close — file what's missing as a new task instead.

## Files

- `.pm/w6/m4/README.md`
- `.pm/w6/README.md`
- All `.pm/w6/m4/tNNN.md` files

## Acceptance criteria

- [ ] DoD reconfirmed before closing
- [ ] Milestone moved to `w6/done/m4/` only after confirmation

## Out of scope

- Any new implementation work.
