---
id: w6/m109/t004
title: Render parity — confirm GraphQL unaffected, correct any doc claim
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m109/t003]
---

## Objective

Confirm the fix is REST/MCP-only (GraphQL was already correct and must stay byte-identical), and check whether `docs/ADR018-render-parity.md` makes any claim about this shape that needs correcting the way `w6/m106` had to correct its line 116.

## Steps

1. Re-run this hunt's live GraphQL probes (`database(id:){ipAllowListEntries, readReplicas{name}}`, `keyValue(id:){ipAllowListEntries}`) against the same or an equivalent fixture post-fix; confirm byte-identical `[]` output to before.
2. Grep `docs/ADR018-render-parity.md` for any line describing Postgres/KeyValue `ipAllowList`/`readReplicas` REST or MCP shape; if one asserts something the pre-fix code contradicted, correct it to describe the fixed (accurate) shape without dropping surrounding correct content — same discipline `m106`'s t003 applied to its own ledger line.
3. Confirm the dashboard needs no change (already GraphQL-only for these fields — re-verify with `grep -rn "v1/postgres\|v1/key-value" dashboard/src --include="*.ts" --include="*.tsx"` returning no direct REST fetch call sites, matching this milestone's own background finding).

## Acceptance criteria

- [ ] GraphQL output for both fields, both resource types, is confirmed unchanged pre/post fix.
- [ ] `docs/ADR018-render-parity.md` accurately describes the fixed shape (edited if it previously claimed something false; left alone if it already didn't cover this).
- [ ] Dashboard confirmed to need no change.
