---
id: w6/m109/t005
title: Simplify
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m109/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (`lego/backend/internal/postgres/service.go`, `lego/backend/internal/keyvalue/service.go`, `lego/backend/internal/keyvalue/rest.go`, and any test files touched). Behavior-preserving only — look for a shared helper opportunity (t001/t002 apply the identical two-line nil-coalesce pattern in three places; consider whether a small shared helper in `core` — mirroring `core.AllowListFromCIDRs`'s existing style — reduces duplication without adding indirection that obscures the fix).

## Acceptance criteria

- [x] `/simplify` run over the diff; findings applied or explicitly declined with reason. (Applied: the three inline nil-coalesces collapsed into one `core.AllowListOrEmpty` helper beside its `AllowListFromSpec` sibling in `core/cidr.go` — the exact helper opportunity this task flagged; `environments/rest.go`'s inline pattern left alone as pre-existing code outside this milestone's diff.)
- [x] No behavior change from t001-t003's verified fix. (All `required_fields_test.go` + conformance tests green after the refactor; full backend suite + `make lint` ×4 green.)
