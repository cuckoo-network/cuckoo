---
id: w6/m124/t007
title: Closeout
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m124/t005, w6/m124/t006]
---

## Objective

Close the milestone once every task is done **and** the DoD holds against a real service on production.

## Context

- The deploy precondition is satisfied: `6f29c189` is an ancestor of `0ca167a8`, the production image pinned by `71fe9660`. The remaining fixture work needs production QA credentials, which are absent from this session.
- The DoD needs a live two-state fixture CI cannot build: a service that deployed successfully and is serving, then a failing build on top. The 57th run's recipe works — create a free `web_service` on the public repo `github.com/render-examples/express-hello-world` (runtime `node`, `buildCommand: npm install`, `startCommand: npm start`), let it reach `live`, then set `dockerfilePath` to a nonexistent file (or otherwise break the build) and redeploy.
- Two things were never observed this run and must not be closed as if they were: **MCP's** phase, and the **first-deploy-failure** case where `Failed` is correct.
- Delete the fixture afterwards; this run's control deleted cleanly (`deleteService` → true, `GET` → 404).

## Steps

1. Build the two-state fixture; confirm a serving service with a failed latest build reports running on REST, GraphQL and MCP, and that the dashboard header no longer says "Service Failed".
2. Confirm the deploy row still reads `build_failed`.
3. Exercise the first-deploy-failure case and confirm it still reports `Failed`.
4. Delete the fixture and confirm 404.
5. Set remaining tasks `status: done`, move each to `w6/m124/done/`, mark rows `— **DONE**`, set `**Status:** done`.
6. Move `w6/m124/` to `w6/done/m124/` (no stub) and check `- [x]` in `.pm/w6/README.md`; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m124/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Live DoD re-run on production with captures recorded, including the deploy-row check.
- [ ] The first-deploy-failure case exercised, not inferred.
- [ ] Fixture deleted (404 confirmed).
- [ ] Milestone moved to `w6/done/m124/` with no tombstone; status consistent in all three places.

## Out of scope

- `w6/m123`'s failure-reason work, which shares the fixture but is its own milestone.
