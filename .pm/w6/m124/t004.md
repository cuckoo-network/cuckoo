---
id: w6/m124/t004
title: Render parity — phase across REST, GraphQL, MCP and the dashboard
worker: worker6
status: todo
estimate: 25m
depends_on: [w6/m124/t003]
---

## Objective

`phase` is user-facing on REST, GraphQL and the dashboard, and this milestone changes what it reports. Confirm every surface moves together and that the result matches how Render presents a failed deploy over a live release.

## Context

- **MCP was never probed for phase this run** — that is the gap this task must close, not assume.
- Render distinguishes service state from deploy state: a failed deploy leaves the previously deployed version serving, and the service is not presented as failed. So this restores parity rather than adding a divergence; record that reasoning.
- `docs/ADR018-render-parity.md` is the ledger; `docs/ADR006-bex-api.md` the contract.

## Steps

1. Read one such service through REST, GraphQL and MCP; confirm all three report the same phase.
2. Confirm the deploy status is consistently `build_failed` on all three.
3. Compare with render.com's presentation of a failed deploy over a live release.
4. Decide whether `ADR018` needs a row (expected: no — parity is restored, not diverged) and record the reasoning.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR006-bex-api.md` — only if a line is warranted

## Acceptance criteria

- [ ] REST, GraphQL and MCP agree on phase, verified by probe rather than inspection.
- [ ] The deploy status agrees across all three.
- [ ] A recorded decision on the ledger row.

## Out of scope

- Unrelated parity gaps surfaced while reading the ledger.
