---
id: w6/m124/t005
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m124/t004]
---

## Objective

Run `/simplify` over this milestone's diff — the phase guard, the deploy-status derivation, and the badge — behaviour-preserving.

## Context

- `lego/operator/internal/controller/app_controller.go`, `lego/backend/internal/store/reconciler.go`, `dashboard/src/features/services/`.
- The cancel path and the failure path now share a predicate ("is a prior release serving?"). Extracting it is the obvious simplification — but keep the two paths' distinct terminal states (`Canceled` vs `Failed`), which exist for a documented reason.

## Steps

1. Run `/simplify` over the diff.
2. Apply behaviour-preserving improvements only.
3. Re-run the operator, backend and dashboard suites.

## Files

- The files `t001`–`t003` touched

## Acceptance criteria

- [ ] No behaviour change; `t006`'s tests still pass.
- [ ] Cancel and failure keep their distinct terminal states.

## Out of scope

- Reworking the phase vocabulary itself.
