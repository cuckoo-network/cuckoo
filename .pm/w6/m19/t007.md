---
id: w6/m19/t007
title: Render parity check - protection/isolation/allowlist fields
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m19/t006]
---

## Objective

Verify the new Environment fields (`protectedStatus`, `networkIsolationEnabled`, `ipAllowList`) are consistent across REST, GraphQL, MCP, and the dashboard UI, and match render.com's semantics/error shapes.

## Context

- `docs/ADR006-bex-api.md` — one core, thin adapters, Render compatibility.
- `dashboard/CLAUDE.md` — dashboard conventions.

## Steps

1. Compare field names/types/semantics against render.com's Environment API.
2. Confirm REST/GraphQL/MCP return identical shapes for the same Environment.
3. Confirm dashboard UI (if present after this milestone) reflects the same fields, or file a follow-up if the UI is out of scope here.
4. Flag any drift as follow-up work rather than silently diverging.

## Files

- `lego/backend/internal/environments/*.go`
- `dashboard/` (Environments UI, if touched)

## Acceptance criteria

- [ ] REST/GraphQL/MCP field shapes verified identical.
- [ ] Any drift from render.com documented as a follow-up note, not silently shipped.

## Out of scope

- Building new dashboard UI beyond what's needed to expose these fields (separate from `w5/m25`).
