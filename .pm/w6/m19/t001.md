---
id: w6/m19/t001
title: Store - protection/isolation/allowlist columns on Environment
worker: worker6
status: todo
estimate: 45m
depends_on: []
---

## Objective

Add `protectedStatus` (enum: unprotected/protected), `networkIsolationEnabled` (bool), and `ipAllowList` ([]string CIDR) to the Environment persistence model, mirroring Render's Environment API fields.

## Context

- `lego/backend/internal/store/environments.go:29-36` — current `Environment` struct is `{ID, ProjectID, TenantID, Name, CreatedAt}`, no protection/isolation fields.
- Migrations live in `lego/backend/internal/store/migrations/`; the environments table was added in `0019_environments.up.sql`.
- Render's Environment object exposes `protectedStatus`, `networkIsolationEnabled`, `ipAllowList` (per `docs/ADR018-render-parity.md`).

## Steps

1. Add a new migration `NNNN_environment_acls.up.sql`/`.down.sql` adding the three columns (default `protectedStatus='unprotected'`, `networkIsolationEnabled=false`, `ipAllowList='{}'`).
2. Extend the `Environment` struct in `environments.go` with the new fields.
3. Update the store's create/get/list SQL to read/write the new columns.

## Files

- `lego/backend/internal/store/environments.go`
- `lego/backend/internal/store/migrations/`

## Acceptance criteria

- [ ] New Environment rows default to unprotected/isolation-off/empty allowlist.
- [ ] Existing environments migrate cleanly (no data loss, no null-constraint failures).

## Out of scope

- Enforcement (t004/t005/t006) and API surface (t002).
