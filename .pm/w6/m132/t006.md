---
id: w6/m132/t006
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m132/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency — strictly behavior-preserving.

## Context

- Likely touched: `lego/backend/internal/sshgateway/` (pre-auth admission path and whatever `t003` altered), `scripts/ssh-verify.sh`, and the guard added by `t004`.
- If `t004` shared a mechanism with `w6/m131/t004`, check the two did not grow parallel implementations of the same synthetic.

## Steps

1. Run `/simplify` over this milestone's diff.
2. Keep behaviour identical; existing suites plus `t007`'s tests stay green.

## Files

- The files this milestone changed.

## Acceptance criteria

- [ ] `/simplify` run over the diff; changes behavior-preserving.
- [ ] `make lint` and `cd lego/backend && go test ./...` pass.

## Out of scope

- Refactors beyond this milestone's code.
