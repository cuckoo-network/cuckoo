---
id: w6/m132/t008
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m132/t004, w6/m132/t007]
---

## Objective

When every other task is complete **and the definition of done actually holds**, close the milestone.

## Context

- Do not run this until the DoD is real. Its bullets are live-verifiable: the raw KEXINIT probe against `ssh.bex.co:22`, a real `ssh` session on an eligible paid service, `scripts/ssh-verify.sh` run against production, and the plan-gate path still refusing correctly.
- **This milestone exists because `w2/m39` was verified live once, in July, and nothing re-checked it afterwards.** Closing this one without a running guard would repeat exactly that — which is what `t004` is for.
- The `Unverified` section lists what the filing hunt never observed: the root cause, the Browser Web Shell, `sandbox exec` and `ags-…` targets, all post-KEXINIT authentication behaviour, and second-client coverage. Each must be verified here or explicitly re-carried, not dropped.

## Steps

1. Re-run every DoD probe against production and record the responses.
2. Set remaining tasks `status: done`, move each `tNNN.md` to `w6/m132/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w6/m132/` to `w6/done/m132/` leaving no stub, and check `- [x]` in `.pm/w6/README.md`.

## Files

- `.pm/w6/m132/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified live and recorded.
- [ ] The guard from `t004` is running, not merely written.
- [ ] Milestone moved to `w6/done/m132/` with status synced in all three places.

## Out of scope

- New findings surfaced during verification — file them separately.

## Status (w6/m132, 2026-08-28) — deployed; authenticated closeout and scheduled-run evidence owed

The raw KEXINIT bullet now passes against production: `bash scripts/ssh-kexinit-probe.sh` completed successfully against `ssh.bex.co:22` on 2026-08-28. The fix is deployed. The real authenticated `ssh` session, full `scripts/ssh-verify.sh` matrix and official CLI leg remain blocked on an authorized disposable key/eligible fixture. The new `ssh-edge-liveness.yml` workflow is present, but `gh run list --workflow ssh-edge-liveness.yml` currently returns no runs, so its first autonomous execution is also still owed. Do not move to `w6/done/m132/` until those boundaries are recorded.

## 2026-09-05 — 3 of 4 DoD bullets live-verified; guard bug fixed; residual is the human-token scripted run

Verified live this session (see t002 for full evidence):
- **raw KEXINIT probe** against `ssh.bex.co:22` — PASS.
- **a real ssh session on an eligible paid service** — raw exec + runtime env + host-key pin + **interactive PTY/resize** all PASS on a paid `starter` fixture (the PTY anomaly is resolved: it was a non-TTY client artifact, not a regression).
- **plan-gate refusing** — free + non-Running denied (2026-08-29 walk).
- **`scripts/ssh-verify.sh` run against production** — STILL OWED end to end: the script now reaches its fixture (its `ownerId` payload bug is fixed this session) and passes host-fingerprint, but its own `POST /v1/ssh-keys` self-registration needs a **human device-flow token** (a `client_credentials` machine token gets 403 — correct), so a single unmodified full run + the official CLI leg remain credential-gated.

**The t004 guard now runs** (its `gh: command not found` failure on one `bex-production` runner is fixed this session — pinned+checksummed `Ensure gh CLI` step). Do not close until the one scripted `ssh-verify.sh` pass + official CLI leg complete under a human token; everything else the DoD asks for is verified.
