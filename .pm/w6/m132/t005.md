---
id: w6/m132/t005
title: Render parity sweep (REST/GraphQL/MCP/dashboard + official Render CLI)
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m132/t002, w6/m132/t003]
---

## Objective

Confirm the restored behaviour is consistent across every surface that exposes it, and against render.com's equivalent.

## Context

- `sshAddress` is a Render-compatible field on `serviceDetails`, served by REST, GraphQL and MCP from the shared view (`lego/backend/internal/apps/render.go`).
- The **official Render CLI** drives this exact path: `ADR035:157` records driving the real `render-oss/cli` v2.21.0 binary against production, including the known upstream instance-picker defect that makes exact-replica acceptance use the full-instance-id argument instead.
- `docs/ADR018-render-parity.md`'s SSH row is the ledger entry to reconcile.

## Steps

1. Confirm REST, GraphQL and MCP agree on `sshAddress` and on the eligibility semantics.
2. Re-run the official CLI leg from `ADR035:157`.
3. Update the ADR018 SSH row to reflect what actually holds after the fix.
4. File residual drift separately rather than absorbing it.

## Files

- `lego/backend/internal/apps/{render,rest,graphql,mcp}.go`
- `docs/ADR018-render-parity.md`, `docs/ADR035-ssh.md`

## Acceptance criteria

- [ ] The three API surfaces agree, and the dashboard matches.
- [ ] The unmodified official CLI completes the shell path.
- [ ] ADR018's SSH row matches shipped behaviour.

## Out of scope

- Parity work unrelated to SSH.
