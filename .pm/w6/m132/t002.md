---
id: w6/m132/t002
title: Walk `w2/m39`'s definition of done item by item against production
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m132/t001]
---

## Objective

The handshake failure masks **every** guarantee `w2/m39` shipped, so none of them is currently verified. Restoring the handshake proves only the handshake. Walk m39's definition of done item by item and record, per item, whether it is live-verified or still owed — because a survey that covers part of the original guarantee yields a fix that restores part of it.

## Context

- `w2/m39` — "SSH into running service instances", done 2026-07-17 — shipped key registration, `ssh <service-id>@ssh.bex.co`, instance selection (any and specific), the official Render CLI path, and the authorization/audit trail (`ssh_sessions` rows, `AuthorizeApp(can_view_sensitive)`, per `ADR035:100`).
- `ADR035:157` documents the acceptance matrix `scripts/ssh-verify.sh` runs, which is the natural checklist: host-key pinning, runtime environment, PTY resize, any-instance and specific-instance raw SSH, exit status, stale-instance denial, restart- and redeploy-driven stream closure, suspended/free/shell-less behaviour, unknown/deleted keys.
- `ADR035:3` records live verification on 2026-07-14 and `:157` a production CLI pass on 2026-07-18 — so there is a known-good baseline to compare against.
- Related milestones that share this gateway and were **not** tested when this was filed: `w2/m55` (Browser Web Shell, different transport), `w2/m65` (Open in Zed / `ags-…` targets), `w2/m66` (`RequiresSshKey` onboarding gate).

## Steps

1. Register a disposable SSH key and confirm the full native path end to end.
2. Walk the `ADR035:157` matrix, recording each item as live-verified or owed.
3. Explicitly check the three related milestones above, since they were never exercised.
4. Record anything still owed rather than assuming it survived.

## Files

- `scripts/ssh-verify.sh`, `docs/ADR035-ssh.md`
- `.pm/w2/done/m39/` (the DoD being walked)

## Acceptance criteria

- [ ] Every `w2/m39` DoD item is marked live-verified or still owed, with evidence.
- [ ] The `ADR035:157` matrix has been run, not just read.
- [ ] `w2/m55`, `w2/m65` and `w2/m66` are each checked or explicitly recorded as still untested.

## Out of scope

- Fixing anything the walk uncovers beyond the handshake — file separately.
