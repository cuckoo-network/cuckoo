---
id: w6/m132/t002
title: Walk `w2/m39`'s definition of done item by item against production
worker: worker6
status: todo
estimate: 40m
depends_on: [w6/m132/t001]
---

## Objective

The handshake failure masks **every** guarantee `w2/m39` shipped, so none of them is currently verified. Restoring the handshake proves only the handshake. Walk m39's definition of done item by item and record, per item, whether it is live-verified or still owed — because a survey that covers part of the original guarantee yields a fix that restores part of it.

## Context

- `w2/m39` — "SSH into running service instances", done 2026-07-17 — shipped key registration, `ssh <service-id>@ssh.bex.co`, instance selection (any and specific), the official Render CLI path, and the authorization/audit trail (`ssh_sessions` rows, `AuthorizeApp(can_view_sensitive)`, per `ADR035:100`).
- `ADR035:157` documents the acceptance matrix `scripts/ssh-verify.sh` runs, which is the natural checklist: host-key pinning, runtime environment, PTY resize, any-instance and specific-instance raw SSH, exit status, stale-instance denial, restart- and redeploy-driven stream closure, suspended/free/shell-less behaviour, unknown/deleted keys.
- `ADR035:3` records live verification on 2026-07-14 and `:157` a production CLI pass on 2026-07-18 — so there is a known-good baseline to compare against.
- Related milestones that share this gateway and were **not** tested when this was filed: `w2/m55` (Browser Web Shell, different transport), `w2/m65` (Open in Zed / `ags-…` targets), `w2/m66` (`RequiresSshKey` onboarding gate).

## Steps

1. Register a disposable SSH key and confirm the full native path end to end.
2. Walk the `ADR035:157` matrix, recording each item as live-verified or owed.
3. Explicitly check the three related milestones above, since they were never exercised.
4. Record anything still owed rather than assuming it survived.

## Files

- `scripts/ssh-verify.sh`, `docs/ADR035-ssh.md`
- `.pm/w2/done/m39/` (the DoD being walked)

## Acceptance criteria

- [ ] Every `w2/m39` DoD item is marked live-verified or still owed, with evidence.
- [ ] The `ADR035:157` matrix has been run, not just read.
- [ ] `w2/m55`, `w2/m65` and `w2/m66` are each checked or explicitly recorded as still untested.

## Out of scope

- Fixing anything the walk uncovers beyond the handshake — file separately.

## Status (w6/m132, 2026-08-28) — deployed; authenticated matrix owed

The handshake fix is deployed, and `bash scripts/ssh-kexinit-probe.sh` passes against `ssh.bex.co:22` on 2026-08-28. Code-verifiable pieces remain green: resolver/eligibility/audit logic (`internal/apps/ssh_test.go`, `sshgateway/nativessh`). Still owed live with an authorized disposable key and eligible fixture: any/specific-instance raw SSH, PTY resize, exit status, restart/redeploy closure, stale-instance denial, and the related-but-untested `w2/m55` (Web Shell), `w2/m65` (`ags-…`), `w2/m66` (`RequiresSshKey`) surfaces.


## 2026-08-29 — item-by-item walk against production (authenticated matrix)

Restored full authenticated access this session (synthetic QA identity, a comped paid `starter` web service, a registered disposable Ed25519 key). Walking w2/m39's DoD:

- **KEXINIT handshake (the regression):** FIXED — credential-free probe passed 2026-08-28; this session completed full auth on top.
- **Authorized member, added key, raw OpenSSH exec, host key, runtime env:** **LIVE-VERIFIED.** `ssh <srv>@ssh.bex.co "id && hostname"` into a running paid `nginxinc/nginx-unprivileged` service returned real in-pod output (`uid=101(nginx) … tea-…-qa-sweep-paid3-… SSH_MATRIX_OK`); host key pinned `SHA256:cwBcvu8ou7s53NcHrFMxqd3955BuoJRhDx9mMMGFpNE`.
- **Fail-closed gates:** **LIVE-VERIFIED.** A **free** web service -> `Permission denied (publickey)`; a **non-Running** (Deploying) service -> `Permission denied`. (An image without a shell also failed usefully: `traefik/whoami` -> "unable to start /bin/sh in this image", stock `nginx` -> CrashLoopBackOff from unprivileged chown — both surfaced as real errors, not silent exec.)
- **Interactive PTY:** **ANOMALY, OWED.** `pty-req` was refused in two independent attempts — raw `ssh -tt` and a real forked PTY — with `PTY allocation request failed on channel 0`, while the exec (non-PTY) channel works. The gateway code (`nativessh/session.go`) does grant `pty-req` for a valid terminal size, so this is either a genuine PTY regression or an environment artifact; it needs the scripted `ssh-verify.sh` (which drives a proper PTY + resize) to settle — do NOT credit interactive-TTY as verified.
- **Full scripted matrix + official CLI leg:** **OWED.** `scripts/ssh-verify.sh` needs a bearer; API-key -> OAuth2 client-credentials was refused at `auth.bex.co/oauth2/token` (403 / Cloudflare 1010) from this environment, and full-matrix mode additionally needs out-of-band viewer/foreign/static/cron identities. The `ssh-edge-liveness.yml` workflow's first autonomous run is also still owed.

Net: the regression and the core exec + eligibility guarantees are live-verified; PTY is a recorded anomaly and the scripted matrix/CLI legs remain credential-gated.
