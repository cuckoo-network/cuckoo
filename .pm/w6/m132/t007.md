---
id: w6/m132/t007
title: Test coverage
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m132/t005]
---

## Objective

Add tests asserting the behaviour this milestone restored, including the failure modes — never tautological or snapshot-everything tests.

## Context

Behaviours worth pinning:

- The gateway sends `SSH_MSG_KEXINIT` after the version exchange. This is the assertion whose absence defined the whole milestone, and it is testable at the protocol level without credentials.
- Whatever `t003` decided for a pre-authentication refusal.
- `ADR035:106`'s post-authentication non-disclosure is unchanged — an unknown key, a foreign service and an ineligible service must remain mutually indistinguishable at that stage.
- The plan gate still holds: `sshEligible` refuses free plans, `POST /shell-ticket` returns 409, `sshAddress` is absent from a free service's `serviceDetails`. All three verified live when this was filed and must not regress.
- Whatever `t002`'s walk found still owed.

## Steps

1. Add the protocol-level KEXINIT assertion where the gateway's handshake is exercised.
2. Cover the pre-auth refusal decision and the unchanged post-auth opacity.
3. Cover the plan-gate assertions.

## Files

- `lego/backend/internal/sshgateway/**/*_test.go`
- `lego/backend/internal/apps/*_test.go` (eligibility / shell-ticket)

## Acceptance criteria

- [ ] Each bullet above has a test that fails against the pre-fix behaviour.
- [ ] `cd lego/backend && go test ./...` passes.

## Out of scope

- The production liveness guard — that is `t004`.
