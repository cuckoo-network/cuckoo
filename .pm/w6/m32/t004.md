---
id: w6/m32/t004
title: Emit an audit event when the lazy on-read ownership migration fires
worker: worker6
status: todo
estimate: 25m
depends_on: []
---

## Objective

w6/m24's lazy on-read migration silently rewrites an env group's ownership the first time it's read. Emit an `audit_events` row (actor = system, action = ownership-migration, target = the group) when it fires, so the migration is observable and debuggable after the fact.

## Context

- Flagged in `.pm/w6/done/m24/README.md:43` as an out-of-scope follow-up.
- Audit-event write path: `lego/backend/internal/` audit package (w4/m10; retention per `BEX_AUDIT_RETENTION_DAYS`).

## Steps

1. Find the migration branch in `internal/envgroups`; add the audit write.
2. Confirm the event renders sanely in the audit-log surfaces.

## Files

- `lego/backend/internal/envgroups/`

## Acceptance criteria

- [ ] Reading a pre-attribution group produces exactly one audit event; subsequent reads produce none.

## Out of scope

- Backfilling audit rows for migrations that already ran.
