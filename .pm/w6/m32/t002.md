---
id: w6/m32/t002
title: Implement the {envGroup, cursor} envelope + cursor/limit; check GraphQL/MCP
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m32/t001]
---

## Objective

Rework `GET /v1/env-groups` to return Render's per-item envelope and honor `cursor`/`limit` (convention from w1/m42 t001 — default 20 via `core.PageParams` + `StablePage`), keeping the existing `ownerId` filter. Align the GraphQL/MCP env-group list surfaces where they exist.

## Context

- `lego/backend/internal/envgroups/rest.go:33-40` — bare `[]EnvGroupView` via `core.WriteJSON`; zero `cursor`/`limit` references in the whole package.
- Envelope pattern to copy: environments (w4/m22) / datastores (89c42936).

## Steps

1. Add the envelope struct + paging to the REST handler.
2. Check `internal/envgroups` GraphQL/MCP list surfaces for the same drift; align.
3. Audit dashboard usage of the list response shape (`dashboard/`) — it consumes GraphQL, but confirm nothing parses the REST array.
4. `cd lego/backend && go build ./... && go test ./...`.

## Files

- `lego/backend/internal/envgroups/rest.go` (+ graphql/mcp siblings if drifted)

## Acceptance criteria

- [ ] Response items are `{envGroup: {...}, cursor: "..."}`; paging walk terminates with full coverage.
- [ ] `ownerId` filtering still works.

## Out of scope

- Env-group *content* (env var) pagination — w9/m10 shipped it; secret-files are w9/m5.
