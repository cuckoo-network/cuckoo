---
id: w6/m28/t008
title: Simplify — run $simplify over metadata projection, owner lookup, and server wiring
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m28/t007]
---

## Objective

Run the repository simplification pass over the completed milestone and apply behavior-preserving improvements to shared metadata projection, owner lookup, and dependency wiring.

## Context

- Three similar resource adapters make duplicated owner/URL/timestamp logic especially likely.
- The REST wire types must stay package-local enough to preserve module boundaries without repeating business logic.
- Optimization must preserve authorization order, omission rules, and list-query bounds.

## Steps

1. Run `$simplify` over the milestone diff.
2. Consolidate repeated source/omission logic behind narrow helpers where ownership is clear.
3. Remove dead placeholders and redundant mappings left by the partial Service/Key Value implementations.
4. Check list-path allocations/queries and server-construction clarity.
5. Rerun focused tests after every accepted simplification.

## Files

- `lego/backend/internal/apps/`
- `lego/backend/internal/postgres/`
- `lego/backend/internal/keyvalue/`
- `lego/backend/internal/workspaces/`
- `lego/backend/internal/api/server.go`

## Acceptance criteria

- [ ] `$simplify` findings are applied or explicitly declined with a correctness reason.
- [ ] No three-way copy of owner/region/dashboard/timestamp business rules remains.
- [ ] Authorization, omission, and official-CLI behavior are unchanged.
- [ ] Focused backend tests remain green.

## Out of scope

- Broad refactors unrelated to the metadata path.
