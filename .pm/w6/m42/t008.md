---
id: w6/m42/t008
title: "Render parity: widened 402 + auth flows consistent across REST/GraphQL/MCP/dashboard"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m42/t007]
---

## Objective

Verify the milestone's surface changes are consistent everywhere they show: the widened 402/`PAYMENT_REQUIRED` is identical in shape and semantics across REST, GraphQL, and MCP (agent sessions included), and the restructured auth flows leave no dashboard surface assuming a registration-time session.

## Context

- Both gates are deliberate, documented divergences from Render (Render's free tier is card-less; Render's verification wall is inside the product, not at the door) — parity here means **internal** three-surface consistency plus the divergence staying recorded (ADR075 Context/D8), per the milestone README's Source note.
- The official Render CLI is bex's fifth surface (`docs/cli-compatibility-checklist.md`): check a cardless create through the CLI gets a comprehensible error, not a wire-format break.
- ADR018 has two stale rows in this area already swept by w2/m79 — don't duplicate that; flag only new drift.

## Steps

1. Exercise a cardless create + agent-session create on REST, GraphQL, MCP, and the official CLI; compare error code/status/shape.
2. Sweep dashboard surfaces for registration-time-session assumptions (redirects, cached whoami) beyond the pages t004 touched.
3. File any drift as follow-up inbox notes rather than silently diverging.

## Files

- verification only; follow-up notes under `.pm/w6/` if drift is found

## Acceptance criteria

- [ ] 402 shape identical across the three surfaces + CLI behaves sanely; evidence recorded.
- [ ] No dashboard surface breaks on the no-session-after-signup restructure.

## Out of scope

- Fixing unrelated parity gaps found along the way (file notes).
