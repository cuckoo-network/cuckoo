---
id: w6/m42/t009
title: "Simplify: /simplify over the changed code"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m42/t008]
---

## Objective

Run `/simplify` over the code this milestone changed (backend gate mode + agent-session admission, dashboard auth pages, scripts) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Standing closing task. Watch for: the mode enum leaking as stringly-typed checks at call sites; duplicated `next`-guard logic across the three auth pages; `auth-secrets.sh` fragment drift against the values-file comments.

## Steps

1. `/simplify` the milestone's diff; apply accepted fixes.

## Files

- whatever the milestone's diff touched

## Acceptance criteria

- [ ] `/simplify` run recorded; accepted findings applied; all suites still green.

## Out of scope

- Bug-hunting (that's `/code-review`); refactors beyond the milestone's diff.
