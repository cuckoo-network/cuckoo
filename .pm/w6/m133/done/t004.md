---
id: w6/m133/t004
title: Blast radius — every parameter writer bound by the policy, correct callers regressed
worker: worker6
status: done
estimate: 40m
depends_on: [w6/m133/t002, w6/m133/t003]
---

## Objective

The write funnels through shared code, so the guard must be verified against a counted list of entrypoints rather than the one the dashboard happens to use — and the callers that behave correctly today need regression tests, since they are correct *because of* current behaviour.

## Context

Enumerate and confirm, do not estimate:

- `SetParameterOverrides` (`lego/backend/internal/postgres/insights.go:385`) → `UpdatePostgres(ctx, dbID, PostgresPatch{ParameterOverrides: &params})`.
- `service.go:867` is where the patch reaches `d.Spec.Parameters`, via `normalizeParameterOverrides`.
- Dashboard writer: `SetDatabaseParameterOverridesDocument` through `use-database-insights.ts:85`.
- Whatever REST and MCP equivalents exist — **neither was exercised** when this was filed, so they are unverified rather than known-good.
- ADR024's `can_operate` / `can_create` split is asserted in both the backend and `parameter-overrides-editor.tsx`; confirm the rebind and the guard did not move it.

## Steps

1. Re-run the greps and paste the counts for every path that can write parameters.
2. Confirm each is bound by `t003`'s policy.
3. Add regression tests for the callers that are correct today — a tenant with a legitimate tuning parameter must keep working, and must not be swept up by the guard.
4. State whether the policy is global or has any allowlisted exception, and why.

## Files

- `lego/backend/internal/postgres/{service,insights,rest,graphql,mcp}.go`
- `dashboard/src/features/databases/hooks/use-database-insights.ts`

## Acceptance criteria

- [ ] Every parameter-write entrypoint is listed with a re-run count and shown to be bound by the policy.
- [ ] REST and MCP write paths are exercised live, not assumed.
- [ ] Regression tests cover the legitimate-tuning-parameter case.
- [ ] The ADR024 permission split is confirmed unchanged.

## Out of scope

- Key Value's equivalent surface — noted as unchecked in the milestone; file separately if it shares the conflation.
