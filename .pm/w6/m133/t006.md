---
id: w6/m133/t006
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m133/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency — strictly behavior-preserving.

## Context

- Likely touched: `lego/backend/internal/postgres/{service,insights,rest,graphql,mcp}.go` and `dashboard/src/features/databases/`.
- Specific reuse target: `t003`'s name-classification policy sits next to the existing `sensitiveLoggingParameters` map (`service.go:890`), which classifies names for a different decision. Check whether the two collapse into one classification pass without conflating their two distinct purposes.

## Steps

1. Run `/simplify` over this milestone's diff.
2. Keep behaviour identical; existing suites plus `t007`'s tests stay green.

## Files

- The files this milestone changed.

## Acceptance criteria

- [ ] `/simplify` run over the diff; changes behavior-preserving.
- [ ] `make lint`, `cd lego/backend && go test ./...`, and `dashboard/yarn test` pass.

## Out of scope

- Refactors beyond this milestone's code.
