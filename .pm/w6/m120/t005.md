---
id: w6/m120/t005
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m120/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- If t001 found the parked state depends on a mutable annotation that something else clears, the simplification worth considering is whether "is this App parked, and since when" should be derived from durable state rather than reconstructed from an annotation each pass.
- Do not merge `autoSleepEligible` and `shouldAutoHibernate` — `w6/m94` split them deliberately so the routing and scheduling questions can differ mid-wake.

## Steps

1. Run `/simplify` over the touched files.
2. Note whether the parked-state representation is now single-sourced.

## Files

- files touched by t002/t003

## Acceptance criteria

- [ ] `/simplify` run; behavior-preserving changes applied.
- [ ] `autoSleepEligible`/`shouldAutoHibernate` remain separate.

## Out of scope

- Behavior changes.
