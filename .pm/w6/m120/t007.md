---
id: w6/m120/t007
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m120/t005, w6/m120/t006]
---

## Objective

Close the milestone once its Definition of done is actually met (not merely once the code is written).

## Steps

1. Verify every DoD bullet live: free web service, `idleTTLSeconds: 60`, untouched URL, event feed after 10 minutes — one `service_hibernated`, no `service_woken`. Confirm the deployed build contains the fix first.
2. Verify the genuine wake in the same session.
3. Set every remaining task's `status: done`.
4. Move each `tNNN.md` to `w6/m120/done/`.
5. Mark every row `— **DONE**` and set `**Status:** done` in `../README.md`.
6. Move the whole `w6/m120/` directory to `w6/done/m120/`.
7. Check `- [x]` for `m120` in `w6/README.md`.

## Acceptance criteria

- [ ] DoD verified live via the event feed, not polling.
- [ ] The four "Unverified this run" items are each verified or restated in the closeout evidence.
- [ ] `w6/m119/t009`'s outcome is cross-referenced — the two milestones share a mechanism question.
- [ ] Milestone moved to `w6/done/m120/` with all statuses in sync.

## Out of scope

- New work surfaced during verification — file it separately.
