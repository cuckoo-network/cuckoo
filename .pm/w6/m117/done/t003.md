---
id: w6/m117/t003
title: Make the Recovery card honest, and stop offering a restore it cannot substantiate
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m117/t002]
---

## Objective

Render t002's new state, and stop enabling a Restore button that would provision a billable database destined to fail.

## Context

- The card today (live capture, `2026-08-27`): `Earliest restore point: No backup yet` beside `Latest restore point: August 27, 2026 at 5:54 AM`, with `Backups: No backups yet.` below and `Restore to new instance` enabled.
- `recovery-panel.tsx:95-105`: the earliest field already has `fallback={t("databases.recoveryNoBackupYet")}`; the latest has no fallback because the backend never omits it. Once t002 can omit it, the panel needs the matching fallback and a copy string for the unreadable case (`en` and `zh` together).
- **Restore is the expensive half.** `Recover` (`recovery.go:199`) gates only on `!src.Status.BackupsEnabled`. With backups enabled but unreadable it passes name validation, passes `ensureDatabaseNameAvailable`, passes `RequirePlanBilling`, and creates a new paid Database that cannot bootstrap. Not executed during the hunt — doing so would have created a paid resource on a production workspace — so confirm this reading before relying on it.
- The panel's own copy ("Restore always creates a new database — this one is never modified") reasonably encourages a user to just try it, which makes the billable dead end more likely, not less.

## Steps

1. Render the unreadable state distinctly from "no backups yet" — a user must be able to tell "you have no backups" from "we could not check".
2. Give the latest field a fallback for the omitted case.
3. Gate `Restore to new instance` on there being a substantiated recovery window; when there is not, disable it with the reason rather than hiding it (the panel's existing convention: "recovery is unavailable, not broken").
4. Add the backend refusal too, so an API caller hits it as well — a disabled button is not a gate.
5. `en` and `zh` move together.

## Files

- `dashboard/src/features/databases/components/recovery-panel.tsx`
- `dashboard/src/features/databases/hooks/use-recovery.ts`
- `dashboard/src/features/databases/locales/{en,zh}.ts`
- `lego/backend/internal/postgres/recovery.go` (`Recover` precondition)

## Acceptance criteria

- [ ] The three recovery facts on the card never contradict each other.
- [ ] "Could not read" is visually distinct from "none exist".
- [ ] Restore is refused — in the API, not only the UI — when the window is unsubstantiated, before anything billable is created.
- [ ] `en` and `zh` in step; `dashboard/yarn test` green.

## Out of scope

- Redesigning the restore flow itself.


## Completion (2026-08-31)

The API-side refusal landed: `Service.Recover` now requires an established recovery window via the shared `recoveryWindowStart` read (CNPG `firstRecoverabilityPoint`) and refuses with 400 **before** the plan/billing gates and before any resource creation; an unreadable window propagates as the read's error, never as absent or present. Covered by `TestRecoverRefusesAnUnsubstantiatedWindow` and `TestRecoveryInfoUnreadableClusterIsAnErrorNotEmpty`. The dashboard leg had already shipped in `94c0a185`; the recover flow's error toast surfaces the new refusal verbatim.

## Triage update (2026-08-27) — half of this task already landed

`94c0a185` shipped the dashboard leg: `recovery-panel.tsx` now gives the Latest restore-point field the same "No backup yet" fallback as Earliest, so the two boundaries can no longer contradict each other, and `recovery.go` only sets `latestRecoveryTime` once `firstRecoverabilityPoint` is present.

**Still open — the API-side refusal.** `Service.Recover` (`lego/backend/internal/postgres/recovery.go:197`) gates only on `src.Status.BackupsEnabled` ("has no backups to recover from"). It never checks whether a recovery window is actually established, so a caller can still POST a recover for a database whose `firstRecoverabilityPoint` is absent and get a new, billable instance provisioned against a window that does not exist. That is this task's third acceptance bullet ("Restore is refused — in the API, not only the UI") and it is unmet. Scope the remaining work to that check plus its test; do not redo the dashboard leg.
