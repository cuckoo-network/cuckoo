---
id: w6/m122/t004
title: Render parity — confirm the fix is dashboard-only and the API surfaces already agree
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m122/t003]
---

## Objective

The milestone changes a user-facing surface over a Render-compatible feed, so the standing parity check applies. Confirm the three API surfaces need no change and that the fix restores Render's behaviour rather than diverging from it.

## Context

- The hunt's evidence says REST already returns all five types correctly — `custom_domain_verified` was read straight out of `GET /v1/services/{id}/events`. The defect is purely dashboard-side. **Confirm that rather than inheriting the assumption**, including GraphQL `serviceEvents` and MCP `list_service_events`.
- Render's Events page shows the types its API emits, so making the tab fail-open restores parity; it does not add a divergence row.
- `docs/ADR006-bex-api.md` is the contract; `docs/ADR018-render-parity.md` is the ledger.

## Steps

1. Read the same service's events through GraphQL and MCP and confirm all types present in REST appear there too.
2. Confirm no REST/GraphQL/MCP change is needed; if one is, that is a new finding, not a silent fix here.
3. Decide whether `ADR018` needs a row. Expected: no — the API was already correct.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR006-bex-api.md` — only if a line is warranted

## Acceptance criteria

- [ ] GraphQL and MCP verified to carry the same types as REST, by probe rather than inspection.
- [ ] A recorded decision on whether `ADR018` gets a row, with reasoning.

## Out of scope

- Any unrelated parity gap surfaced while reading the ledger — file separately.
