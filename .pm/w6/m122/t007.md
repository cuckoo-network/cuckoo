---
id: w6/m122/t007
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m122/t005, w6/m122/t006]
---

## Objective

Close the milestone once every task is done **and** the definition of done actually holds on production.

## Context

- The DoD is live-verifiable and CI cannot run it: it needs the Events tab loaded against `dashboard.bex.co` with the `custom_domain_verified` row visible and the filter listing it.
- The `disk_*` half was never observed live (no service with a persistent disk in the QA workspace). Either provision one to verify, or record explicitly that those four remain code-verified only — do not let the closeout imply they were seen.

## Steps

1. Re-run the live DoD checks on production: the hidden event renders; "Filter events" lists all five; a synthetic unknown type still renders.
2. Decide and record the `disk_*` verification status.
3. Set remaining tasks `status: done`, move each `tNNN.md` to `w6/m122/done/`, mark rows `— **DONE**`, set `**Status:** done`.
4. Move `w6/m122/` to `w6/done/m122/` (no stub) and check `- [x]` in `.pm/w6/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w6/m122/`, `.pm/w6/README.md`

## Acceptance criteria

- [ ] Live DoD checks re-run on production and recorded with their captures.
- [ ] The `disk_*` verification status is stated plainly.
- [ ] Milestone moved to `w6/done/m122/` with no tombstone; status consistent in all three places.

## Out of scope

- Follow-ups surfaced by `t003`'s split-vocabulary note — file separately rather than holding this open.

## State at 2026-08-28 — t001–t006 are done and deployed; this task is blocked on credentials

Everything CI can verify is verified and green (dashboard 379 files / 2769 tests; backend `internal/api` + `internal/events`; `make lint` ×4). See the milestone README's "Implementation record" for what landed.

The deploy precondition is satisfied: `11c0df98` is an ancestor of `0ca167a8`, the production image pinned by `71fe9660`.

**Blocked on QA credentials.** Every remaining DoD bullet is a live probe against `dashboard.bex.co`. `scripts/qa-login.sh` exits **2** — `QA_EMAIL`/`QA_PASSWORD` are absent from `.env` (present as empty names in `.env.example`). Other w6 closeouts share this blocker.

**Two things this closeout must not quietly drop:**

- **The `disk_*` half was never observed live.** The QA workspace has no service with a persistent disk, so all four types are code- and test-verified only. Step 2 asks for this status plainly — state it, do not let the closeout imply they were seen. Provisioning a disk-backed service to verify is the alternative.
- **`w6/m110/t008` plans to delete this milestone's fixture** (`srv-da7o6ovvqdcc73bpn9hg`), which carries the `custom_domain_verified` row stamped `2026-08-27T11:54:07Z` that DoD bullet 1 names. Take the capture before that milestone closes, or rebuild a fixture with a verified custom domain.

**One extra bullet is owed beyond the DoD**, from t004: `w6/068.md` was filed for the `disk_attached`/`disk_detached` vs Render `disk_created`/`disk_deleted` spelling divergence. It is a separate decision and does **not** gate this closeout, but link it from the closeout evidence so the two are not later confused.
