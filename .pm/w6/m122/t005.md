---
id: w6/m122/t005
title: Simplify
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m122/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — the events route's filter state, the filter component, the catalog, and the new guard — behaviour-preserving.

## Context

- `dashboard/src/routes/services.$serviceId.events.tsx`, `dashboard/src/features/events/`.
- Specifically worth a look: whether the selection state and `filterTimelineEvents` can share a helper **without** collapsing their deliberately different semantics — one is fail-open by design, and merging them carelessly would reintroduce this bug from the other direction.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behaviour-preserving improvements only.
3. Re-run `cd dashboard && yarn test`.

## Files

- The files `t001`–`t003` touched

## Acceptance criteria

- [ ] No behaviour change; `t003`'s guard and `t006`'s tests still pass.
- [ ] The two filters remain semantically distinct, or their unification is justified and tested.

## Out of scope

- Restructuring the events feature beyond this milestone's diff.
