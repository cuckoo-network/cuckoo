---
id: w6/m34/t005
title: Render parity
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m34/t004]
---

## Objective

Check the shipped behavior is consistent across every surface it exposes — REST, GraphQL, MCP (same field placement, semantics, error shapes as Render's `envSpecificDetails.registryCredentialId`) — and that the dashboard's create-from-repo Docker flow offers the credential selector the image flow grew in `w6/m31` (`dashboard/src/routes/services.new.tsx`). Flag any drift as follow-up work rather than silently diverging.

## Context

- Contract capture: `docs/render-artifacts/registry-credential-service-binding.md` — extend it with the repo-backed context evidence.
- `docs/ADR018-render-parity.md` registry-credentials row + `docs/cli-compatibility-checklist.md` — update markers with evidence.

## Steps

1. Diff each surface's accept/echo shape against Render's spec for the nested field.
2. Verify or file the dashboard create-from-repo selector (coordinate with `w5/m37` — don't duplicate its settings-editor scope).
3. Update ledger rows with evidence.

## Files

- `docs/render-artifacts/registry-credential-service-binding.md`, `docs/ADR018-render-parity.md`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] All surfaces verified consistent or drift filed as owned follow-up; ledgers updated.

## Out of scope

- `w5/m37`'s settings attach/detach editor.
