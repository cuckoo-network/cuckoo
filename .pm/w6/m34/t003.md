---
id: w6/m34/t003
title: "Isolation tests: foreign/unknown credential rejected; credential invisible to tenant build steps"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m34/t002]
---

## Objective

Prove the two security properties the note demands: (1) a tenant can only bind credentials their own workspace owns, and (2) the bound credential never becomes tenant-readable during the build.

## Context

- `w6/m31` shipped the workspace-scoping tests for the image context (`lego/backend/internal/apps/registry_credential_binding_test.go`) — extend for the repo-backed path.
- The tenant-visibility property is about the Job spec: no build-arg, no BuildKit secret, no mount into any container a tenant Dockerfile step executes in.

## Steps

1. Backend: table-driven tests — foreign-workspace credential id, unknown id, non-Docker runtime — each rejected with the Render error shape.
2. Operator: Job-spec assertions — the docker config lands only in the buildkitd container's mounts/env; assert absence from build args and from the BuildKit secret list.

## Files

- `lego/backend/internal/apps/registry_credential_binding_test.go`
- operator build-Job builder tests (alongside t002's code)

## Acceptance criteria

- [ ] All rejection cases covered and asserting the error shape, not just the status code.
- [ ] A Job-spec test fails if the credential ever appears as a build arg, BuildKit secret, or non-buildkitd mount.

## Out of scope

- Live-cluster verification (t004).
