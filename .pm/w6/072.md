# Refresh mobile session details opened directly

Why: an agent-session detail opened by deep link can remain at its initial lifecycle state when the Sessions list was never mounted, hiding completion or a ready pull request until manual refresh.

**Estimate:** 45m; keep as an inbox note. **Priority:** 5 in the 2026-09-07 mobile-review handoff. **Depends on:** `w6/m139/t002` and `w6/m139/t003` for recovery/access-generation behavior.

**Source:** mobile review at `e609e9322`, $pm-brainstorm proposal 5, user handoff to w6. Goal linkage: ADR048 phase-one agent supervision and ADR008 machine-readable operational state. This repairs `w11/m6` snapshot behavior; live transcript attach and steering remain in `w11/m7`.

## Evidence

`mobile/src/features/agent-sessions/detail/session-detail-screen.tsx:39` uses cache-and-network with a manual refetch but no polling or foreground recovery. The Sessions list polls and may update normalized cache when it is mounted; the reproduction must open the detail directly with that list unvisited, so it cannot accidentally supply the missing refresh.

## Work and acceptance

- Reuse `mobile/src/common/hooks/use-recovery.ts`, `recovery-coordinator.ts`, and freshness utilities for access-gated foreground/online refresh of the exact session.
- Refresh active sessions on the existing bounded cadence and expose stale/refresh-error state with English/Chinese strings. Use existing lifecycle helpers to avoid inventing terminal-state semantics.
- Stop background/offline work and reject responses from an old session/workspace/access generation.
- In an isolated direct-link test, advance a running session to completed/PR-ready while the list is unmounted; the detail must update without manual navigation. Cover foreground return, transient error, terminal state, and access loss.
- Run focused behavioral checks and mobile typecheck.

**Done when:** direct-entry details show current lifecycle/PR state on their own; background/offline requests stop, cached failures remain visibly stale, and stale responses cannot repopulate protected content.

**Files:** `mobile/src/features/agent-sessions/detail/session-detail-screen.tsx`, its focused tests, existing recovery/freshness helpers, and `mobile/src/translations/{en,zh}.ts`.

**Out of scope:** transcript streaming, new prompt turns, agent runtime changes, new push event types, or closing the existing physical-device/live-agent qualification gates.
