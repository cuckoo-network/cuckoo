# Dedupe the purger List+delete loops (shared helper / DeleteAllOf)

`postgres/purger.go`, `keyvalue/purger.go`, and `apps/purger.go` now carry three verbatim copies of the same ~12-line List-by-`core.LabelTenant`-then-delete-each loop. m11's own `/simplify` pass (t005, see `done/m11/README.md`) flagged both the duplication and that a single `client.DeleteAllOf(...)` call could replace the loop — but correctly skipped it as out of that milestone's diff.

Sub-hour, behavior-preserving: extract one shared helper (or switch to `DeleteAllOf` with the label selector) and point all three purgers at it; existing purger tests must stay green unchanged. Not a milestone.
