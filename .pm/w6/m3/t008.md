---
id: w6/m3/t008
title: Test coverage — meaningful tests for the behavior this milestone shipped
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m3/t006]
---

## Objective

Automated tests for the UI behavior that matters: workspace-context scoping (queries re-issued with the new workspace on switch), creation-flow validation and limit-error rendering, role-gated rename, and the delete guard's disabled-until-typed logic. Real assertions, not snapshots.

## Context

- Dashboard test setup: `dashboard/src/test/` + `__tests__/` conventions (Vitest/RTL per `dashboard/CLAUDE.md`).
- Mock Apollo with typed errors matching m1/t006's shapes so error-rendering tests exercise the real mapping.

## Steps

1. Context/provider tests: switch → variables change, persistence, default fallback.
2. `/new/workspace`: plan default, submit wiring, inline limit error.
3. Settings/delete: role gating, confirm-string gating, refusal rendering.

## Files

- `dashboard/src/__tests__/` (or co-located tests)

## Acceptance criteria

- [ ] Switch/scoping, limit error, role gating, and delete guard each have behavior-asserting tests
- [ ] Tests fail if the guard or scoping regresses (verified by mutation of the code under test at least once)
- [ ] Dashboard test suite green

## Out of scope

- Browser e2e (done manually in t005).
