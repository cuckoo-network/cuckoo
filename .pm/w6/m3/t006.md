---
id: w6/m3/t006
title: Render parity — side-by-side vs live Render dashboard; flag drift
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m3/t005]
---

## Objective

Standing parity gate: compare the shipped switcher, `/new/workspace` flow, settings, and delete guard side-by-side against the live Render dashboard (per m1/t001's artifact plus a fresh look), and against the backend surfaces (GraphQL semantics identical to what REST/MCP expose per w6/m2). Drift → w6 inbox notes.

## Context

- Precedent: w3/m4.5 and w5/m7 ran the same side-by-side method.
- Include copy-level checks (plan card wording honestly diverges on billing — that's recorded drift, not silent).

## Steps

1. Side-by-side pass with screenshots of both dashboards.
2. Cross-surface consistency: what the UI shows must match `GET /v1/owners` / MCP for the same user.
3. File drift notes.

## Files

- `.playwright-mcp/` (comparison screenshots)
- `.pm/w6/` (drift notes if any)

## Acceptance criteria

- [ ] Side-by-side comparison recorded for all four surfaces (switcher, create, settings, delete)
- [ ] UI/REST/MCP agree for the same identity
- [ ] All drift either fixed or filed

## Out of scope

- Pixel-perfect cloning; bex keeps its own visual identity.
