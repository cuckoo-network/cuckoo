---
id: w6/m134/t003
title: Carry and render the event across REST, GraphQL, MCP, and dashboard
worker: worker6
status: todo
estimate: 45m
depends_on: [w6/m134/t002]
---

## Objective

Expose the new fact consistently through every service-event consumer and render it with useful translated copy in the dashboard.

## Context

- `lego/backend/internal/events/service.go` maps facts to public event views.
- `dashboard/src/features/events/service-event-catalog.ts` owns dashboard grouping and labels.
- Webhook delivery consumes the same event vocabulary and must not silently omit the new type.

## Steps

1. Thread the type and details through the shared REST/GraphQL/MCP view and webhook vocabulary.
2. Add dashboard catalog grouping, icon, label, and detail rendering in English and Chinese.
3. Verify unknown-event fail-open behavior remains intact.

## Files

- `lego/backend/internal/events/`
- `lego/backend/internal/webhooks/`
- `dashboard/src/features/events/`
- `dashboard/src/features/services/locales/en.ts`
- `dashboard/src/features/services/locales/zh.ts`

## Acceptance criteria

- [ ] REST, GraphQL, MCP, webhook payloads, and dashboard rendering agree on the type and details.
- [ ] English and Chinese copy distinguishes project moves from environment moves.
- [ ] Existing event filters and unknown-type fail-open behavior still work.

## Out of scope

- Redesigning the Events tab or workspace audit-log UI.
