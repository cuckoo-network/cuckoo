---
id: w6/m134/t007
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m134/t006]
---

## Objective

Close the milestone only after every task is done and the observable service-history outcome is verified.

## Context

- PM completion moves task files into `done/`, then moves the milestone to `w6/done/m134/` and checks the workstream entry.

## Steps

1. Re-run the definition of done, including a live project/environment move when production access is available.
2. Confirm every prior task is complete and all required tests pass.
3. Run `/pm done w6/m134/t007` last so the milestone moves atomically to `done/`.

## Files

- `.pm/w6/m134/`
- `.pm/w6/README.md`

## Acceptance criteria

- [ ] Every definition-of-done bullet has direct evidence.
- [ ] Every task is marked done and moved under `done/`.
- [ ] The milestone directory is moved to `w6/done/m134/` with the workstream checkbox checked.

## Out of scope

- Closing on code-complete status while required live evidence remains missing.

## State at 2026-08-29 — shipped; deploy in flight; live leg blocked on credentials

t001–t006 are done and shipped in `8ab1e6db` (see the milestone README and `w6/README.md` implementation note). Deploy state, verified against the cluster: production currently runs the image built from `2a9a7240` (digest `52ab1315…`, pin `f3d16ac5`) — `8ab1e6db`'s own deploy run built and tested green but was superseded before pinning by the next push, so its code reaches production with the first deploy that completes un-superseded (several runs were still leapfrogging each other at sweep time — verify with `git log --grep "pin platform images"` and the cluster digest before running the live leg).

**Blocked on:** the live DoD leg needs an authenticated project/environment move on `dashboard.bex.co` after that roll-out, and QA credentials are absent (`scripts/qa-login.sh` exits 2; the `.env` `BEX_API_TOKEN` returns 401 — same blocker as every other w6 closeout in the 2026-08-29 sweep). Once credentials exist: repeat `.pm/w6/done/054.md`'s repro (assign a service to a fresh environment), then confirm one `service_moved` event with the placement pair in `GET /v1/services/{id}/events`, the same `evt-…` id in the workspace audit log's row, and the dashboard Events tab rendering the two from→to lines.


## 2026-08-29 — credential blocker removed; now deploy-blocked only

The QA-credential blocker is solved (a synthetic QA identity now drives `api.bex.co`), and the move itself was exercised live: `PUT /v1/environments/{env}/service-links` on the synthetic workspace returned 200 and the environment's `serviceIds` now lists the moved service. **But no `service_moved` event appeared in `GET /v1/services/{id}/events`** — because the running production image is `2a9a7240` (digest `52ab1315…`), which **predates this milestone's `8ab1e6db`**. So m134's code is verified shipped but not yet deployed.

The remaining deploys have been failing on transient CI/infra (a flaky vendored-OpenSandbox watch test; a ghcr OAuth-token blip during image push) and leapfrogging via supersession; re-runs were kicked. **Once `8ab1e6db` reaches production** (verify: `git log --grep "pin platform images"` shows a pin whose commit contains `8ab1e6db`, and the cluster digest changes off `52ab1315…`), repeat the move above and confirm exactly one `service_moved` event carries the before/after placement pair, the same `evt-…` id appears in the workspace audit log, and the dashboard Events tab renders the two from→to lines. All other DoD bullets are code-complete + CI-green (see the milestone record).
