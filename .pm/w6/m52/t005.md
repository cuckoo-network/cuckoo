---
id: w6/m52/t005
title: "Render parity: REST/GraphQL/MCP/UI agree on the new status; render.com behavior confirmed"
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m52/t003, w6/m52/t004]
---

## Objective

Confirm the new signal from t002/t003 is visible identically across every surface that exposes service phase/status, and matches whatever render.com behavior t001 found.

## Context

- `docs/ADR006-bex-api.md` — bex-api is meant to be Render-compatible across REST/GraphQL/MCP; the `server`/`service` phase field is shared by all three fragments.
- t001's Render-parity research is the baseline this task confirms bex now matches (or records as a deliberate, noted divergence).

## Steps

1. Query the affected phase/status field via REST, GraphQL, and MCP for a canceled-first-deploy service; confirm all three agree with each other and with the dashboard.
2. Re-confirm the dashboard UI (t004) renders correctly end to end, not just via a raw query.
3. Record the render.com comparison from t001 in `docs/ADR018-render-parity.md` if it reveals a tracked divergence either way.

## Files

- `lego/backend/internal/apps/{rest.go,graphql.go,mcp.go}`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] REST, GraphQL, and MCP all report the same distinguishable value for a canceled-first-deploy service.
- [ ] Any divergence from render.com's actual behavior is recorded, not silently shipped.

## Out of scope

- Building new surface support beyond wiring through the existing three fragments.
