---
id: w6/m52/t006
title: Simplify
worker: worker6
status: done
estimate: 15m
depends_on: [w6/m52/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Touches `lego/operator/internal/controller/app_controller.go`, `lego/types/`, `lego/backend/internal/apps/graphql.go`, and `dashboard/src/features/services/lib/{status,labels}.ts` + `types.ts` + locale files.

## Steps

1. Run `/simplify` over the diff from t002–t005.
2. Apply any behavior-preserving cleanups it finds.

## Files

- Whatever t002–t005 touched.

## Acceptance criteria

- [ ] `/simplify` has run over the milestone's diff and its applicable findings are applied.

## Out of scope

- Any change that alters behavior — this task is cleanup only.

## Findings (2026-08-24)

Reviewed the milestone's diff — it is 5 production lines across 4 files plus locales, so there was little to collapse. One fix applied: the rationale for "canceled is not failed" was written out twice at similar length (`types.ts`'s union member and `status.ts`'s `PHASE_STATUS` entry). The `types.ts` copy now points at the mapping table instead of restating it.

Explicitly considered and rejected as changes that would have added code without adding decisions:

- A `deriveStatus` special case for `Canceled` — the existing `PHASE_STATUS` lookup already handles a plain phase; the special cases there exist only for states derived from several fields at once.
- An `observedDeployStatus` case for `PhaseCanceled` in the backend reconciler — see t003; the Cancel verb already closes the row, so a case could only affect an unrelated open row.
