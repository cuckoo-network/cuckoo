---
id: w6/m52/t008
title: Closeout
worker: worker6
status: todo
estimate: 10m
depends_on: [w6/m52/t007]
---

## Objective

Close the milestone once its definition of done is actually met (not merely once the code is written): set every task's `status: done`, move each `tNNN.md` to `w6/m52/done/`, mark every row `— **DONE**` and `**Status:** done` in this `README.md`, move the whole `w6/m52/` directory to `w6/done/m52/`, and check `- [x]` in `w6/README.md`.

## Acceptance criteria

- [ ] The milestone's Definition of done (service-level status agrees with deploy-level status for a canceled first-ever deploy, genuine-failure control case unaffected, live-verified across at least 2 App types) holds, not just that code was written.
- [ ] All prior tasks moved to `done/`, statuses synced across task frontmatter, milestone README, and workstream README.
- [ ] `w6/m52/` moved to `w6/done/m52/`.
