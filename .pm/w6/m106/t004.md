---
id: w6/m106/t004
title: Simplify
worker: worker6
status: todo
estimate: 15m
depends_on: [w6/m106/t003]
---

## Objective

Run `/simplify` over this milestone's diff (`render.go`'s response-shaping change and its tests) — reuse, simplification, efficiency, behavior-preserving only.

## Steps

1. Run `/simplify` scoped to the files this milestone touched.
2. Apply any behavior-preserving cleanups found (e.g. shared gating helper if the `svcType == web_service || svcType == static_site` check now appears more than once).

## Acceptance criteria

- [ ] `/simplify` run recorded with what was applied and what was declined and why.
