---
id: w6/m106/t003
title: Render parity
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m106/t002]
---

## Objective

Confirm the fix actually closes the Render-parity gap this milestone exists for, and correct the documentation that currently misstates the shape as already-exact.

## Steps

1. Re-verify GraphQL (`ipAllowList`/`ipAllowListEntries`) and MCP (`get_service`/`update_service`) still agree with REST's corrected values on a live/test service — they should be unaffected by t001, but confirm rather than assume.
2. Correct `docs/ADR018-render-parity.md:116` to describe the actual (now-fixed) nested shape. Keep the useful content already there (web + static scope, `{cidrBlock,description}` shape, Traefik enforcement mechanism, `w7/m32`/`w9/m56` lineage) — only the "REST: … proven through unmodified CLI v2.21.0" claim needs correcting to reflect what was actually verified.
3. Check `docs/render-artifacts/cli-services-create-update.md`'s `--ip-allow-list` rows (create + update) for the same claim and correct if they assert the same inaccurate placement.
4. If `t001` decided to keep a transitional root-level alias, document that divergence explicitly in the ledger rather than silently shipping it.

## Files

- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/cli-services-create-update.md`

## Acceptance criteria

- [ ] GraphQL/MCP re-confirmed correct post-fix (test or live check, not assumption).
- [ ] ADR018:116 and the CLI capture doc both describe the actual, current, correct behavior.
