---
id: w6/m18/t006
title: "Render parity — full-surface consistency check"
worker: worker6
status: todo
estimate: 20m
depends_on: [w6/m18/t005]
---

## Objective

Confirm `ownerId`/`owner_id` targeting is consistent across REST, GraphQL, MCP, and the dashboard for usage, API keys, and GitHub connections — same param name/semantics as the `w6/m14` create-path precedent — and note the result in `docs/ADR018-render-parity.md` where relevant (usage and API keys are bex extensions per that ledger; GitHub connections already ✅).

## Context

- `docs/ADR018-render-parity.md` — "API-key management" and "bex ahead of Render" (usage) rows.

## Steps

1. Diff the `ownerId` param name/behavior across all four surfaces for the three features.
2. Update ADR018 evidence pointers if the row text references workspace-targeting behavior that changed.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] No surface uses a different param name or semantics for the same targeting concept.
- [ ] ADR018 updated where its existing text would otherwise go stale.

## Out of scope

- New backend surface changes.
