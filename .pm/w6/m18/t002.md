---
id: w6/m18/t002
title: "GraphQL: ownerId arg on usage, apiKeys, GitHub connection operations"
worker: worker6
status: todo
estimate: 35m
depends_on: [w6/m18/t001]
---

## Objective

Add the matching `ownerId` argument to the GraphQL `usage`, `apiKeys`/`createApiKey`, and GitHub-connection queries/mutations, following the `w6/m14` `createService(ownerId:)` precedent.

## Context

- `lego/backend/internal/usage/graphql.go`, `lego/backend/internal/apikeys/graphql.go`, `lego/backend/internal/github/graphql.go`.
- Same `core.WithWorkspace` plumbing as t001 — this is schema + resolver wiring, not new authz.

## Steps

1. Add `ownerId: ID` arg to the `usage` query.
2. Add `ownerId: ID` arg to `apiKeys` query and `createApiKey` mutation.
3. Add `ownerId: ID` arg to the GitHub connection query.
4. Regenerate GraphQL schema/types (codegen) and update the dashboard's generated client types.

## Files

- `lego/backend/internal/usage/graphql.go`
- `lego/backend/internal/apikeys/graphql.go`
- `lego/backend/internal/github/graphql.go`
- Generated schema/codegen artifacts (backend + dashboard)

## Acceptance criteria

- [ ] `usage(ownerId: "tea-B")` returns B's data for a multi-workspace caller.
- [ ] `createApiKey(ownerId: "tea-B")` binds the new key to B, not the caller's default workspace.
- [ ] Codegen is clean (no drift between schema and generated types).

## Out of scope

- MCP (t003), dashboard threading (t004).
