---
id: w6/m18/t008
title: "Test coverage — meaningful tests for the behavior this milestone shipped"
worker: worker6
status: todo
estimate: 30m
depends_on: [w6/m18/t006]
---

## Objective

Ensure the `ownerId`/`owner_id` targeting behavior across REST/GraphQL/MCP and the dashboard fix are covered by tests that assert real behavior and failure modes, beyond t005's cross-surface integration test.

## Context

- Unit-level coverage per adapter (REST/GraphQL/MCP param parsing + 403 on non-membership) complements t005's end-to-end integration test.
- Dashboard: `use-current-workspace.ts` fix needs a unit test asserting it reads the switcher's selection, not `workspaces[0]`.

## Steps

1. Add REST/GraphQL/MCP unit tests for `ownerId` parsing and the 403-on-non-membership path for each of the three features, where not already covered by t005.
2. Add a `use-current-workspace.test.ts` asserting the switcher-selection fix.

## Files

- `lego/backend/internal/{usage,apikeys,github}/*_test.go`
- `dashboard/src/features/team/hooks/__tests__/use-current-workspace.test.ts`

## Acceptance criteria

- [ ] Tests fail if `ownerId` parsing regresses to ignoring the param.
- [ ] Tests fail if `use-current-workspace.ts` reverts to `workspaces[0]`.
- [ ] Full test suite green.

## Out of scope

- E2E test already covered by t005.
