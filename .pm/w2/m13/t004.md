---
id: w2/m13/t004
title: "Dashboard: Health & Alerts field in service Settings"
worker: worker2
status: todo
estimate: 40m
depends_on: [w2/m13/t002]
---

## Objective

Add a Health Check Path field to the service Settings page, wired to the GraphQL `updateService` mutation from t002 — this folds in inbox note `w5/009`, which deferred exactly this UI pending the backend update surface.

## Context

- `w5/009` (`.pm/w5/009.md`): "Do not build UI yet: `w1/m23/t001` decides wire-into-a-ReadinessProbe or drop the field. If t001 keeps the field: the update surfaces need a backend home first — file that as a w2 inbox note then, and promote this alongside it." `w1/m23/t001` kept the field; t001–t003 of this milestone are that backend home.
- Render's Settings → Health & Alerts section is the reference UI — capture it live via Playwright into `docs/render-artifacts/` for field-level/copy parity (per `w5/009`'s instruction).
- `dashboard/src/features/services/` (or wherever the Settings form lives — check `w5/m7`'s instance-type-picker precedent for the surrounding form).

## Steps

1. Playwright-capture Render's Health & Alerts settings section (field label, help text, validation) into `docs/render-artifacts/`.
2. Add the Health Check Path field to the service Settings form, wired to `updateService`.
3. Match Render's copy/placement where reasonable; note any deliberate divergence.

## Files

- `dashboard/src/features/services/` (service Settings form + its GraphQL mutation hook)

## Acceptance criteria

- [ ] The Settings page has a Health Check Path field that reads and writes via `updateService`.
- [ ] A Playwright capture of Render's equivalent section exists under `docs/render-artifacts/`.

## Out of scope

- Any other Health & Alerts fields Render exposes beyond health-check path (e.g. alert notification settings) — out of this milestone's scope, file separately if found.
