---
id: w2/m86/t007
title: Test coverage
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m86/t005]
---

## Objective

Pin the flip's invariants with meaningful tests: the operator provisions disk PVCs with the configured storage class when `BEX_DISK_STORAGE_CLASS` is set and cluster-default when unset (existing envtests may already cover half — extend, don't duplicate), and structural validation fails closed on a broken LUKS wiring (storage-class Secret parameters ↔ minted Secret name drift).

## Context

- Existing coverage: `lego/operator/internal/controller/disk_envtest_test.go`, `disk_test.go`.
- `scripts/gitops-validate.sh` is the structural fail-closed home for manifest invariants (see the `#PSL` guard precedent).

## Steps

1. Audit existing disk envtests for storage-class assertions; add the set/unset class propagation cases that are missing.
2. Add a gitops-validate structural check: `disk-storageclass.yaml`'s node-secret parameter names/namespace template must match the operator's minted-Secret convention.
3. `make test` (operator) green.

## Files

- `lego/operator/internal/controller/disk_envtest_test.go`
- `scripts/gitops-validate.sh`

## Acceptance criteria

- [ ] New assertions fail if the class propagation or the class↔Secret wiring regresses; full operator suite green.

## Out of scope

- Live-cluster assertions (t004 carried those).
