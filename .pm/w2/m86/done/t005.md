---
id: w2/m86/t005
title: Record the existing-disk stance and encryption claim in ADR082 + ADR018
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m86/t004]
---

## Objective

Make the docs tell the truth after the flip: ADR082 §D3 stops saying encryption-at-rest is unverified/off, states that disks created after the flip date are LUKS-encrypted, that pre-flip disks remain on the default class, and documents the snapshot-restore migration path for them. Update the ADR018 disks row's encryption caveat to match.

## Context

- ADR082 currently withholds the encryption-at-rest parity claim explicitly.
- `docs/ADR018-render-parity.md` disks row carries the corresponding caveat.
- Migration path for pre-flip disks: snapshot → restore into a fresh disk on the LUKS class (the restore Job from w1/m85) — document it as an operator-driven, per-disk procedure, not an automatic migration.

## Steps

1. Amend ADR082 §D3 (+ §Verification if the flip evidence belongs there) with the flip date, scope, and migration path.
2. Update the ADR018 disks row caveat.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR082-persistent-disks.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Both docs state the post-flip encryption guarantee, the pre-flip plaintext residual, and the migration path — no doc still claims encryption-at-rest is unshipped.

## Out of scope

- Announcing/claiming encryption for pre-flip disks.
