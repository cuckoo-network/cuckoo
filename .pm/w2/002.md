# Workspace MCP tools — blocked on w1/m9

Render's official MCP server ships `list_workspaces` / `select_workspace` / `get_selected_workspace` (agents pick which workspace subsequent tools operate in). bex can't serve these until real workspaces exist — that's w1/m9 (tenant onboarding: `workspace:tea-<id>` replaces `workspace:default`).

When w1/m9 lands: add the three tools to the MCP adapter under Render's names, backed by the store's tenants table and the caller's OpenFGA memberships; "selected workspace" is per-MCP-session state. Likely sub-hour once the tenancy verbs exist — promote only if it grows.

**2026-07-08:** superseded — workspace lifecycle materialized as workstream `w6`; these three tools are `w6/m2/t005` (per-session selection, Render schemas pinned in `w6/m2/t001`). Retire this note to `w2/done/` when w6/m2 closes.
