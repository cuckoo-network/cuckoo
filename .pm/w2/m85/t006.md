---
id: w2/m85/t006
title: Test coverage for wildcard/default-certificate invariants
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m85/t003]
---

## Objective

Prevent the fallback certificate, namespace trust boundary, or secret supply path from disappearing silently.

## Steps

1. Extend `scripts/gitops-validate.sh` with structural checks for the DNS-01 Issuer, wildcard DNS name, default TLSStore, Secret reference, and prod-only wiring.
2. Assert Traefik's default TLS resources namespace is explicit and trusted.
3. Assert workflow/env/secret-uploader names agree and no literal credential is present.
4. Keep live `openssl`/HTTP probes in the closeout checklist rather than pretending static checks prove issuance.

## Acceptance criteria

- [ ] Tests fail if wildcard DNS-01 or TLSStore wiring is removed/misnamed.
- [ ] Tests fail if namespace scoping or credential inventory drifts.
- [ ] Existing GitOps validation passes.
- [ ] Tests distinguish structural configuration from live issuance evidence.

## Out of scope

- Committing a real ACME or Cloudflare credential for tests.
