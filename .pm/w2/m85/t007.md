---
id: w2/m85/t007
title: Closeout
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m85/t005, w2/m85/t006]
---

## Objective

Close only after the public edge presents trusted TLS for an actually unknown host.

## Steps

1. Re-run the external unknown/active/suspended probes from t004 and archive sanitized evidence.
2. Confirm the TLS Secret metadata, issuer, SAN, expiry, live trust, TLSStore reference, and intentional HTTP 404.
3. Mark tasks done, move them to `done/`, move `w2/m85/` to `w2/done/m85/`, and check the w2 row.

## Acceptance criteria

- [ ] `notfound.onbex.co` no longer presents `TRAEFIK DEFAULT CERT`.
- [ ] Unknown host still returns 404 and control hosts remain correct.
- [ ] Milestone is moved to `w2/done/m85/` with no stub.

## Out of scope

- Any code change after the milestone is complete.
