---
id: w2/m31/t002
title: "REST: GET .../deploys reads status/createdBefore/createdAfter/cursor/limit"
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m31/t001]
---

## Objective

Wire the REST list endpoint to the new store-layer params.

## Context

- `lego/backend/internal/deploys/rest.go` — `GET .../deploys` handler, currently calls `s.List(r.Context(), r.PathValue("id"))` with nothing else.
- Query param parsing convention: match however other list endpoints in this codebase already parse `status`/date/`cursor`/`limit` query strings (e.g. events, audit logs) rather than inventing a new parsing idiom.

## Steps

1. Parse `status` (repeatable query param), `createdBefore`/`createdAfter` (RFC3339), `cursor`, `limit` from the request.
2. Pass through to `deploys.Service.List` (which now accepts the params from t001).
3. Bad/unparseable values → 400 with a clear message, not a silently-ignored filter.

## Files

- `lego/backend/internal/deploys/rest.go`

## Acceptance criteria

- [ ] `GET .../deploys?status=live&limit=5` returns at most 5 live deploys.
- [ ] `GET .../deploys` (no params) is unchanged from today.
- [ ] Malformed `createdBefore` returns 400, not a silently-dropped filter.

## Out of scope

- GraphQL/MCP (t003/t004).
