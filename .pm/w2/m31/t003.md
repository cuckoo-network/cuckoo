---
id: w2/m31/t003
title: "GraphQL: deploys(serviceId, status, createdBefore, createdAfter, cursor, limit)"
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m31/t001]
---

## Objective

Mirror t002's REST filters on the `deploys` GraphQL query.

## Context

- `lego/backend/internal/deploys/graphql.go` — `deploys(serviceId)` field, `graphql-go/graphql` field-config args.

## Steps

1. Add `status: [String]`, `createdBefore: String`, `createdAfter: String`, `cursor: String`, `limit: Int` args.
2. Same validation/parsing as REST (t002) — delegate to the same `Service.List`, don't duplicate parsing logic if it can live once in `service.go`.

## Files

- `lego/backend/internal/deploys/graphql.go`

## Acceptance criteria

- [ ] `deploys(serviceId, status: ["live"], limit: 5)` matches REST's equivalent query result set exactly.

## Out of scope

- MCP (t004).
