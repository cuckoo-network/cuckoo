---
id: w2/m31/t005
title: Render parity — cross-surface consistency + refresh docs/ADR018-render-parity.md's List/get deploy row
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m31/t004]
---

## Objective

Confirm REST/GraphQL/MCP produce identical result sets for equivalent filter/pagination inputs, and update the parity ledger.

## Steps

1. Run the same filter/pagination query through all three surfaces, confirm identical results.
2. Update `docs/ADR018-render-parity.md`'s "List / get deploy objects" row to note pagination/filtering support, including the MCP `limit`/`cursor` gap this closes (t004's finding on whether it changes MCP's default result size).

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] The three surfaces agree on results for the same filter inputs.
- [ ] The parity ledger row is accurate, not aspirational.

## Out of scope

- New capability — verification + documentation only.
