---
id: w2/m32/t005
title: Render parity — cross-surface consistency + refresh docs/ADR018-render-parity.md's Deploys rows
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m32/t004]
---

## Objective

Confirm REST/GraphQL/MCP report the new statuses/updatedAt identically, and update the parity ledger to state bex's real (still partial, now larger) status-enum coverage explicitly.

## Steps

1. Cross-check REST/GraphQL/MCP for the same deploy report identical status/updatedAt values.
2. Update `docs/ADR018-render-parity.md`'s Deploys section: list bex's now-6-of-11 status values, and name the remaining 5 (`created`/`deactivated`/`queued`/`pre_deploy_in_progress`/`pre_deploy_failed`) as deliberately out of reach with the specific missing bex feature each depends on.
3. Fold in `w2/m30`'s and `w2/m31`'s parity-row updates if they haven't landed yet (check `.pm/w2/done/m30` and `m31` — if still open, coordinate rather than overwrite their edits to the same rows).

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] The Deploys status-enum coverage is stated as a specific count/list, not a bare checkmark.
- [ ] Each of the 5 remaining unreachable status values names the missing bex feature it depends on.

## Out of scope

- Building any of the 5 remaining statuses' backing features.
