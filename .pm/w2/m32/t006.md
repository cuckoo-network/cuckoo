---
id: w2/m32/t006
title: Simplify — /simplify over the code this milestone changed
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m32/t005]
---

## Objective

Run a simplification pass over everything t001–t004 touched — behavior-preserving only.

## Context

- Likely candidate: the reconciler's build-Job-lookup logic (t001) may duplicate `Cancel`'s existing `buildJobName` derivation — factor to one place if so.

## Steps

1. Run `/simplify` over `lego/backend/internal/store/reconciler.go`, `store.go`, `lego/backend/internal/deploys/`, and the dashboard status-badge additions.
2. Apply only non-behavior-changing simplifications.

## Files

- Whatever t001–t004 touched.

## Acceptance criteria

- [ ] No duplicated build-Job-lookup logic between `Cancel` and the reconciler's new status-detection path.
- [ ] Full test suite still green.

## Out of scope

- Any behavior change.
