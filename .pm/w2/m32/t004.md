---
id: w2/m32/t004
title: "REST/GraphQL/MCP surface the two new statuses + updatedAt; dashboard status badges"
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m32/t001, w2/m32/t002]
---

## Objective

Confirm the new `build_in_progress`/`build_failed` statuses and `updatedAt` field flow cleanly through every adapter, and extend the dashboard's Events tab status-badge mapping so the two new statuses render sensibly instead of falling through to the `default` case.

## Context

- `dashboard/src/routes/services.$serviceId.events.tsx` — `statusVariant`/`statusKey` switch statements currently handle `live`/`update_in_progress`/`update_failed`/`canceled`; add `build_in_progress`/`build_failed` cases.
- `dashboard/src/features/services/locales/{en,zh}.ts` — new locale keys for the two statuses' badge text.

## Steps

1. Confirm REST/GraphQL/MCP already return the new values correctly (should be automatic from t001/t002/t003's plumbing — this task is mostly verification + the dashboard piece).
2. Add `build_in_progress`/`build_failed` cases to `statusVariant`/`statusKey`.
3. Add the two locale keys (en + zh).

## Files

- `dashboard/src/routes/services.$serviceId.events.tsx`
- `dashboard/src/features/services/locales/en.ts`
- `dashboard/src/features/services/locales/zh.ts`

## Acceptance criteria

- [ ] A deploy in `build_in_progress`/`build_failed` renders a distinct, correctly-labeled badge in the Events tab instead of falling through to a generic default.

## Out of scope

- Any new dashboard surface beyond the existing Events tab's badge rendering.
