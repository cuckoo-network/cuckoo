---
id: w2/m32/t007
title: Test coverage — build-vs-rollout status transitions, updatedAt writes, trigger-value correctness
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m32/t005]
---

## Objective

Meaningful tests for every new status transition and field this milestone introduced.

## Steps

1. Reconciler/envtest: a repo-backed deploy whose build Job is running/failed reports `build_in_progress`/`build_failed`, not the old collapsed `update_in_progress`/`update_failed`.
2. Reconciler: an image-backed deploy's status progression is unaffected (regression guard).
3. Store: `updated_at` is bumped on every transition, including the new build-phase ones.
4. `store_test.go` (or equivalent): the trigger-value doc/behavior decided in t003 is asserted, not just commented.

## Files

- `lego/backend/internal/store/reconciler_test.go`
- `lego/backend/internal/store/store_test.go`
- `lego/backend/internal/deploys/service_test.go`

## Acceptance criteria

- [ ] A test proves a build failure and a rollout failure produce different, correct status strings.
- [ ] `go test ./...` green, including the operator's envtest suite (`make test` from `lego/operator/`).

## Out of scope

- Live-cluster acceptance — envtest coverage is the bar, matching this workstream's existing precedent.
