---
id: w2/m67/t005
title: Closeout
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m67/t004]
---

## Objective

Close the milestone once the definition of done actually holds: sync statuses, move tasks to `done/`, move `w2/m67/` to `w2/done/m67/`, check the box in `w2/README.md`.

## Context

- The move IS the completion signal — run `/pm done w2/m67/t005` last, only when the DoD's observable behavior is real (shipped, suites green).

## Steps

1. Verify the DoD against the shipped behavior.
2. `/pm done` this task last; it triggers the milestone move.

## Files

- `.pm/w2/m67/` → `.pm/w2/done/m67/`; `.pm/w2/README.md`

## Acceptance criteria

- [ ] Milestone directory lives only under `w2/done/`; all three status locations agree.

## Out of scope

- New work — file follow-ups as inbox notes.
