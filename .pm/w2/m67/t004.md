---
id: w2/m67/t004
title: Test coverage for idle math, connected-never-expires, and cap behavior
worker: worker2
status: todo
estimate: 40m
depends_on: [w2/m67/t002]
---

## Objective

Standing closing task: meaningful tests for the shipped behavior and its failure modes — the idle computation, the connected-pins-the-clock rule, TTL boundaries, and the cap's refusal/limit semantics.

## Context

- Highest-value seams: `idleSince` = max(turn end, last SSH end); open session ⇒ never reaped; disconnect restarts the clock; TTL boundary exact; `0` ⇒ legacy immediate; stale ssh-row guard still works; Cancel bypasses grace; cap counts only live-phase+sandbox rows; cap refusal named and cross-surface consistent; cap `0` disables.
- All against the fake store/clock fixtures from `completion_test.go` / `service_test.go` — no sleeps.

## Steps

1. Sweep the milestone diff for uncovered branches; add cases where a failure is user-visible (a sandbox reaped under an editor, or a workspace wrongly locked out, are the worst cases — cover both directions).
2. Full backend suite green.

## Files

- `lego/backend/internal/agentsessions/completion_test.go`, `service_test.go`
- `lego/backend/internal/store/` tests as needed

## Acceptance criteria

- [ ] Every rule in the DoD has a direct test; suite green.

## Out of scope

- Coverage-gaming; live-cluster acceptance (rides the next prod use of Open in Zed).
