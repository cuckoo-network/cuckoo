---
id: w2/m93/t003
title: Add a read-only migration readiness report
worker: worker2
status: todo
estimate: 50m
depends_on: [w2/m93/t002]
---

## Objective

Combine continuous read evidence and the migrated-resource inventory into a reproducible fail-closed readiness assessment.

## Context

- w2/m92 completed phases 1–3; w2/035 retains destructive Phase 4.
- Current repository evidence does not establish continuous legacy-read coverage; live telemetry sufficiency must be checked first.

## Steps

1. Extend the inventory verifier or add a focused sibling script consuming the chosen evidence source.
2. Report clean, legacy reads detected, or insufficient evidence with exact interval, source coverage and resource scope.
3. Check tombstones and scoped workload references; preserve exceptions for unlabeled siblings. Never mutate grants, flags or artifacts.

## Files

- `scripts/verify-workspace-scoped-identity.sh`
- `scripts/registry-migration-readiness.sh`

## Acceptance criteria

- [ ] Clean requires a complete 14-day interval with zero migrated legacy reads and a valid current inventory.
- [ ] Missing/truncated history, inaccessible sources and collection gaps cannot pass.
- [ ] Detected legacy reads identify the affected resource and prevent readiness.

## Out of scope

- Executing Phase 4, deleting tenant artifacts, disabling dual-read, changing pricing, or adding tenant-facing API/UI surfaces.
