---
id: w2/m84/t004
title: Expose a human-only GraphQL delete-account mutation
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m84/t002, w2/m84/t003]
---

## Objective

Give the dashboard one narrow, typed entry point into the account-lifecycle service.

## Steps

1. Add a GraphQL mutation with the t001 confirmation/recent-auth contract and typed blocker details.
2. Require a direct Kratos human session; refuse machine API keys and delegated OAuth callers before any side effect.
3. Route only to the core account service and preserve consistent error/audit envelopes.
4. Document why REST and MCP do not expose this destructive human-account operation unless t006 finds a real parity requirement.

## Files

- `lego/backend/internal/accounts/{graphql,service}.go`
- `lego/backend/internal/api/{server,scope_matrix_ops}.go`
- `dashboard/src/features/auth/api/`

## Acceptance criteria

- [ ] A direct human can invoke the mutation with the exact confirmation/proof.
- [ ] API keys and delegated OAuth tokens are refused with zero side effects.
- [ ] Blockers are structured enough for the UI to name the affected workspaces and remedy.
- [ ] The mutation is covered by the schema/scope inventory guards.

## Out of scope

- A generic admin delete-user endpoint.
