---
id: w2/m65/t003
title: "DB: GRANT SELECT ON agent_sessions to bex_ssh_gateway"
worker: worker2
status: done
estimate: 30m
depends_on: []
---

## Objective

Give the gateway's scoped Postgres role the one new grant ADR054 D7 enumerates, so an SSH-time resolver can read the session row.

## Context

- `lego/backend/internal/sshgateway/dbrole/dbrole.sql` — current grants: `ssh_keys`, `tenants`, `tenant_members`, `ssh_sessions`, `shell_ticket_nonces`, `audit_events`, `agent_session_transcripts`. No `agent_sessions`.
- `TestGatewayScopedRoleAllowsOwnSurfaceDeniesTheRest` (`dbrole_integration_test.go`) asserts the exact grant surface and must move `agent_sessions` from the deny list to the allow list.
- Applied out-of-band via `scripts/ssh-gateway-db-role.sh`.

## Steps

1. Add `GRANT SELECT ON agent_sessions TO bex_ssh_gateway;` to `dbrole.sql`.
2. Update the scoped-role integration test's allow/deny sets.
3. Note the re-apply step (`scripts/ssh-gateway-db-role.sh`) in the milestone's rollout notes for the live-acceptance task.

## Files

- `lego/backend/internal/sshgateway/dbrole/dbrole.sql`
- `lego/backend/internal/sshgateway/dbrole/dbrole_integration_test.go`

## Acceptance criteria

- [ ] Integration test proves the role reads `agent_sessions` and still cannot touch anything outside its enumerated surface.

## Out of scope

- Any other grant; the role stays otherwise identical.
