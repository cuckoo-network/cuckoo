---
id: w2/m65/t002
title: "FGA: add can_view_sensitive to the agent_session type"
worker: worker2
status: done
estimate: 30m
depends_on: []
---

## Objective

Add `can_view_sensitive: can_view_sensitive from workspace` to the `agent_session` FGA type so the SSH resolver can gate shell access on the sink-appropriate relation (ADR054 D2).

## Context

- `deploy/gitops/authz/model.fga` — `type agent_session` currently has `workspace`, `can_view`, `can_operate`, `can_create`; all inherit from the parent workspace tuple.
- The applied artifact `model.json` in the same directory must be regenerated to match.
- Workspace `can_view_sensitive` is developer/admin (`model.fga` workspace relations) — the same roles the App SSH/Web Shell sink requires (`apps/service.go` `ResolveSSHSession`).

## Steps

1. Add the relation to `type agent_session` in `model.fga`; regenerate `model.json`.
2. Add/extend an authz test asserting developer/admin allow and contributor deny on `can_view_sensitive` for an `agent_session` object.
3. Confirm existing tuple writes (`GrantAgentSessionWorkspace`) need no change (inheritance-only relation).

## Files

- `deploy/gitops/authz/model.fga`
- `deploy/gitops/authz/model.json`
- `lego/backend/internal/authz/` (tests)

## Acceptance criteria

- [ ] `can_view_sensitive` check on `agent_session:<id>` allows developer/admin, denies contributor, via the workspace tuple only.
- [ ] Backend tests green.

## Out of scope

- The resolver that consumes the relation (t004).
