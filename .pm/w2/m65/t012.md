---
id: w2/m65/t012
title: Simplify pass over the milestone's changes
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m65/t011]
---

## Objective

Standing closing task: run `/simplify` over the code this milestone changed (gateway, agentsessions, store, dashboard) — reuse, simplification, efficiency; behavior-preserving.

## Context

- Likely candidates: the composite resolver vs `parseSSHUsername` duplication; `serveConn`'s branched channel loop; shared liveness-gate helper between the resolver (t004) and the presenter (t008).

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted fixes; keep the suites green.

## Files

- As surfaced by the pass.

## Acceptance criteria

- [ ] Pass run, fixes applied or explicitly declined with reasons; all suites green.

## Out of scope

- Behavior changes.
