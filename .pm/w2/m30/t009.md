---
id: w2/m30/t009
title: Test coverage — commitId checkout override, deployMode reject/accept paths, restart-opens-deploy-row
worker: worker2
status: todo
estimate: 35m
depends_on: [w2/m30/t007]
---

## Objective

Meaningful tests for every real behavior/failure mode this milestone introduced — no tautological or snapshot-everything tests.

## Context

- `lego/backend/internal/deploys/service_test.go` (or wherever `Trigger`'s existing tests live) — extend for `commitId`/`deployMode`.
- `lego/backend/internal/apps/` — restart-now-opens-a-deploy-row needs a test that actually asserts a `deploys` row exists after calling `Restart`, not just that the CR patch happened.
- Dashboard: `service-detail-header.test.tsx`'s updated restart assertion (already covered by t006, verify it's real and not vacuous).

## Steps

1. Backend: `commitId` on a repo-backed service checks out the right ref (or as close to verifiable as the existing build-Job test harness allows); `commitId` on a cron job is rejected.
2. Backend: `deployMode: deploy_only` on a repo-backed service is rejected with the specific typed error; on an image-backed service, succeeds and behaves like `build_and_deploy`.
3. Backend: `deploy_only` + `commitId` together is rejected.
4. Backend: `apps.Restart` produces a `deploys` row retrievable via `List`.
5. Frontend: the dropdown's two items call `useTriggerDeploy` with the correct, distinct arguments.

## Files

- `lego/backend/internal/deploys/service_test.go` (or equivalent)
- `lego/backend/internal/apps/service_test.go` (or equivalent, for the restart-opens-a-row assertion)
- `dashboard/src/features/services/components/__tests__/service-detail-header.test.tsx`

## Acceptance criteria

- [ ] Every acceptance criterion listed in t002/t003/t004/t005/t006 has a corresponding automated test, not just a manual verification note.
- [ ] `go test ./...` (backend) and `yarn test` (dashboard) both green.

## Out of scope

- Live-cluster acceptance scripting — unit/integration coverage is the bar here, matching this workstream's existing `w2/m10` precedent (live acceptance is a stretch goal, not a blocker).
