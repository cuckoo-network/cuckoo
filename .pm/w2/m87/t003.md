---
id: w2/m87/t003
title: Expose detection as a dashboard-facing GraphQL query
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m87/t002]
---

## Objective

Expose the probe+heuristic as one GraphQL query the wizard can call — e.g. `repoRuntimeDetection(connectionId, repo, branch, rootDir)` returning `{runtime, matchedManifest}` with `runtime: null` for unknown — beside the existing repo-listing GraphQL surface, guarded by the same authorization.

## Context

- Existing GitHub GraphQL surface: `lego/backend/internal/github/graphql.go` (+ `graphql_test.go`) — follow its resolver/auth idioms.
- The dashboard is the GraphQL client (`dashboard/CLAUDE.md`); codegen consumes the schema.
- Deliberately GraphQL-only for now: Render exposes no public REST/MCP for language inference — t006 (Render parity) confirms or revisits that stance.

## Steps

1. Add the query type/resolver; wire auth exactly like the repo-listing query.
2. Errors from the probe map to `runtime: null` (unknown), never a GraphQL error for the expected cases.
3. Extend `graphql_test.go` with an auth-denied case and a happy path.

## Files

- `lego/backend/internal/github/graphql.go`
- `lego/backend/internal/github/graphql_test.go`

## Acceptance criteria

- [ ] Query returns the detected runtime for a fixture repo tree, `null` on unknown, and denies an unauthorized caller identically to the repo-listing query.

## Out of scope

- REST/MCP exposure (t006 decides), dashboard wiring (t004).
