---
id: w2/m87/t006
title: Render parity
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m87/t005]
---

## Objective

Standing closing task: check the shipped detection against Render's actual behavior and across bex's surfaces. Confirm the precedence matches the t002 evidence; decide and record whether GraphQL-only is the right surface stance (Render has no public REST/MCP language-inference endpoint — if that holds, document the deliberate scoping; if not, file the gap as follow-up). Update the ADR018 gap-backlog row to done/owned by this milestone.

## Context

- `docs/ADR006-bex-api.md` (one core, thin adapters), `docs/ADR018-render-parity.md` gap backlog row "New Service wizard runtime auto-detection".

## Steps

1. Re-check Render's wizard behavior against the shipped one (precedence, re-infer trigger, override semantics).
2. Record the surface decision + any drift as follow-ups; never silently diverge.
3. Update ADR018 (row owner/status) and run prettier.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] ADR018 row closed with owner `w2/m87`; surface stance and any residual drift explicitly recorded.

## Out of scope

- Building REST/MCP exposure in this milestone (file follow-up if warranted).
