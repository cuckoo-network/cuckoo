---
id: w2/m87/t002
title: Detection heuristic: manifest → runtime mapping mirroring Render's precedence
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m87/t001]
---

## Objective

Turn a probed file listing into a runtime verdict. A pure, table-driven function mapping manifest files to bex's runtime vocabulary, with a documented precedence (Dockerfile wins, then language manifests), returning "unknown" when nothing matches or the signal is ambiguous beyond the precedence rule.

## Context

- Mapping: `Dockerfile` → docker; `go.mod` → go; `package.json` → node; `requirements.txt`/`pyproject.toml` → python; `Gemfile` → ruby; `mix.exs` → elixir; `Cargo.toml` → rust.
- Precedence must mirror Render's observed behavior — capture Render's actual inference (its docs + a live check on render.com with a multi-manifest repo) and record the evidence in the task notes; where Render's behavior is unobservable, pick Dockerfile-first and document the choice.
- bex's runtime vocabulary: whatever `CreateRequest`/the wizard's runtime field accepts today (`lego/backend/internal/apps/`) — do not invent new runtime values.

## Steps

1. Capture Render's precedence evidence (docs + live probe).
2. Implement the pure mapping function + table in the github (or apps) package.
3. Table-driven tests: each single-manifest case, the multi-manifest precedence cases, empty/unknown.

## Files

- `lego/backend/internal/github/` (detection function beside the probe)
- its `_test.go`

## Acceptance criteria

- [ ] Table-driven tests pin every mapping and the precedence order; the function is pure and returns "unknown" rather than guessing.

## Out of scope

- Buildpack-grade detection (framework, version); Root Directory UI behavior.
