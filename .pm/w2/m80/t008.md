---
id: w2/m80/t008
title: "Closeout: verify DoD, mark done, move milestone to done/"
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m80/t007]
---

## Objective

Verify the milestone's Definition of done end to end, sync status everywhere it lives, and move the milestone to `done/` per `.pm/CLAUDE.md`.

## Context

Standing closing task. DoD (README): workspace-prefixed writes + prefix-confined reads test-asserted; migration command with verification + tombstones proven on the dev stack; quota + REST/GraphQL/MCP behavior unchanged; global-sweep snapshot cache retired or narrowed; prod runbook with an explicit authorization gate; ADR066 residual line annotated.

## Steps

1. Walk the DoD line by line against the shipped diff + t004's dev-stack record; fix or file follow-ups for any gap before closing.
2. Confirm CI: backend-test, go-lint green on the final state.
3. Sync status: each task's frontmatter `status: done` and file moved to `w2/m80/done/tNNN.md`; milestone README `**Status:**` line + task rows marked `— **DONE**`; w2 workstream README checkbox checked.
4. Move the whole milestone directory to `.pm/w2/done/m80/` (`mv` then `rmdir`-check — no tombstone, stub, or redirect left at `.pm/w2/m80/`).
5. Ship per repo rules (only via /ship when the user invokes it — never auto-commit).

## Files

- `.pm/w2/m80/` → `.pm/w2/done/m80/`
- `.pm/w2/README.md` (checkbox)

## Acceptance criteria

- Every DoD clause verified or a follow-up item filed with a reason.
- Status synced in all three places; the old milestone path no longer exists.
- Nothing committed outside the user-invoked ship flow.

## Out of scope

- New feature work; the prod migration itself (stays authorization-gated per t005).
