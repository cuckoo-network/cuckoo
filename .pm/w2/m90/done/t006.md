---
id: w2/m90/t006
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m90/t003, w2/m90/t004]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — chiefly that the WS-edge probe's classification is real (refusal-shape = healthy, dead = failure), not a tautology.

## Context

- w6/m132's precedent: protocol-level tests (`nativessh/kexinit_probe_test.go`) plus a `scripts/gitops-validate.sh` manifest-lockstep check. A candidate here: assert the `ssh-shell` IngressRoute and the gateway's shell-listener config stay in lockstep (route exists ⇒ Secret/env wiring present), so a future manifest edit can't silently strand the edge again.
- Never game coverage: a probe test that passes against both a live and a dead target is worthless.

## Steps

1. Test the probe's discrimination against a local stand-in (refusing listener vs closed port vs TLS failure).
2. Evaluate and, if cheap, add the manifest-lockstep validation check.

## Files

- Test files beside the probe script / validator

## Acceptance criteria

- [ ] Probe classification proven by tests that fail when the discrimination is broken
- [ ] All touched suites green

## Out of scope

- Re-testing m55's gateway WebSocket unit coverage (already exists)
