---
id: w2/m90/t007
title: Closeout
worker: worker2
status: todo
estimate: 10m
depends_on: [w2/m90/t006]
---

## Objective

When every other task is complete **and the definition of done is actually met** (recorded production session, fail-closed matrix, green liveness run including the WS probe, docs updated), close the milestone: mark all tasks done, move task files to `w2/m90/done/`, set `**Status:** done`, move `w2/m90/` to `w2/done/m90/`, and check the box in `w2/README.md`.

## Context

- Do not run until the DoD holds — activation is real when the production evidence exists, not when the code is written.

## Steps

1. Verify the DoD bullet by bullet against evidence (`.pm/w2/m90/evidence/2026-09-08-production-activation.md`).
2. **Gate:** `scripts/shell-ws-probe.sh` must be on `origin/main`, then a green `ssh-edge-liveness.yml` run must include step `Probe the Web Shell WS edge for refusal-shape` (dispatch with `gh workflow run "Production edge liveness"` after `/ship`, or wait for schedule). Record the run URL in evidence.
3. Execute the closeout moves per `.pm/CLAUDE.md`: mark t007 done → `done/t007.md`; set README `**Status:** done`; `mv .pm/w2/m90 .pm/w2/done/m90`; check `- [x] **m90**` in `.pm/w2/README.md`; update ADR evidence links to `done/m90/evidence/` if still pointing at the pre-move path.

## Files

- `.pm/w2/m90/`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] DoD verified; board state synced in all three places

## Out of scope

- —
