---
id: w2/m81/t007
title: Closeout — verify DoD, mark done, move milestone to done/
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m81/t006]
---

## Objective

Standing closing task: confirm every Definition-of-done clause in the milestone README holds, sync statuses, and move the milestone into `done/` per `.pm` conventions.

## Context

- DoD (README): metrics-server runs with `--kubelet-certificate-authority` and without `--kubelet-insecure-tls` on mock + prod; a newly scaled-up node gets an auto-approved serving cert (approver logs + CSR state); `kubectl top nodes/pods` and bex-api's resource-metrics fallback work; the validate-script guards fail on regression; ADR072 #5 / ADR061 #11 / ADR063 #9 annotated closed.
- `.pm` convention: a done milestone moves **whole** to `w2/done/m81/` (`mv` then `rmdir` nothing left behind — no tombstone, stub, or redirect at `.pm/w2/m81/`), with status synced in all three places (task frontmatter, milestone README `**Status:**` + `— **DONE**` row, workstream README checkbox).

## Steps

1. Walk the DoD clause by clause with fresh evidence (mock-cluster commands, prod `kubectl top`, guard-script runs, doc diffs).
2. Set every task's frontmatter `status: done`; update the milestone README `**Status:**` line and the w2 workstream README checkbox/row.
3. `mv .pm/w2/m81 .pm/w2/done/m81` — verify the old path no longer exists.

## Files

- `.pm/w2/m81/` → `.pm/w2/done/m81/` (the whole directory)
- `.pm/w2/README.md`

## Acceptance criteria

- All DoD clauses verified with evidence; statuses synced in all three places; milestone directory lives only at `.pm/w2/done/m81/`.

## Out of scope

- Any new implementation work — a failed DoD clause reopens the owning task instead.
