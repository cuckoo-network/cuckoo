---
id: w2/m41/t010
title: "Blueprint by-id read parity: REST GET /v1/blueprints/{id} + MCP get_blueprint (folds w2/014)"
worker: worker2
status: todo
estimate: 45m
depends_on: []
---

## Objective

Render's public API has `GET /blueprints/{blueprintId}` (and a `/blueprints/{blueprintId}/syncs` list), but bex's by-id blueprint read is GraphQL-only. Add the REST route and an MCP `get_blueprint` tool with the same shape, and verify the syncs-list shape against the pinned spec while there.

## Context

- GraphQL by-id read: `lego/backend/internal/apps/graphql.go:779` — reuse its core read, thin adapters only (ADR006).
- REST today has only validate/list/sync (`apps/rest.go:1079-1114`); MCP has no `get_blueprint`.
- Pinned Render spec (round-14 copy) documents both routes — check the syncs-list envelope/cursor shape before implementing.
- Independent of this milestone's `keyValue` chain — can run any time before t006 (Render parity), which now covers it.

## Steps

1. REST `GET /v1/blueprints/{id}` from the existing core read; Render error shape on not-found/foreign-workspace.
2. MCP `get_blueprint` tool, same fields.
3. Verify (or file) the `/syncs` list shape against the pinned spec.

## Files

- `lego/backend/internal/apps/rest.go`, `mcp.go` (+ `graphql.go` only if the core read needs extraction)
- `docs/ADR018-render-parity.md` / `docs/cli-compatibility-checklist.md` row evidence if markers change

## Acceptance criteria

- [ ] By-id read consistent across REST/GraphQL/MCP (same fields, error shapes).
- [ ] Syncs-list shape verified with evidence, or drift filed.

## Out of scope

- The `keyValue` parser work (this milestone's t001–t004).
