---
id: w2/m41/t002
title: Shared parser: `keyValue` entries
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m41/t001]
---

## Objective

The shared bex.yml/render.yaml parser accepts `keyValue` entries per t001's pinned schema instead of omitting the field "for wire compatibility".

## Context

- `lego/backend/internal/apps/blueprint.go:73` — the placeholder comment.
- The parser already handles services / Postgres / env groups (w1/m24, w2/m15).

## Steps

1. Add the keyValue struct + validation (name, plan, maxmemoryPolicy, ipAllowList) to the shared parser.
2. Named validation errors matching the existing style (never silent drops).

## Files

- lego/backend/internal/apps/blueprint.go
- lego/backend/internal/apps/deploy.go (shared bex.yml types if they live there)

## Acceptance criteria

- [ ] A Blueprint with a keyValue entry parses; malformed entries produce named errors.

## Out of scope

- Provisioning (t003).
