---
id: w2/m41/t004
title: `fromService` → keyvalue connection env refs
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m41/t003]
---

## Objective

An env var `fromService` referencing a key-value store's connection resolves to the internal connection string at deploy, removing the named rejection.

## Context

- `lego/backend/internal/apps/deploy.go:1200` — `fromService to a keyvalue connection is not supported`.
- Postgres `fromDatabase` resolution nearby is the pattern; internal URL comes from the KeyValue status.

## Steps

1. Resolve keyvalue targets in `resolveRef` (connection string property per t001's contract).
2. Same-workspace guard identical to the existing fromService checks.

## Files

- lego/backend/internal/apps/deploy.go

## Acceptance criteria

- [ ] A service env var fed by a Blueprint keyValue connection materializes correctly; cross-workspace refs still refuse.

## Out of scope

- Copying not-yet-minted values across services (recorded residual).
