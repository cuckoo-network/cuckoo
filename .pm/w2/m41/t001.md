---
id: w2/m41/t001
title: Pin Render's Blueprint `keyValue` schema → render-artifacts
worker: worker2
status: todo
estimate: 30m
depends_on: []
---

## Objective

Capture Render's render.yaml `keyValue` resource schema (fields, plans, ipAllowList, `fromService` linkage semantics) so the parser mirrors evidence, not guesses.

## Context

- render.com/docs/blueprint-spec (live) + the pinned OpenAPI where applicable.
- Existing artifact style: docs/render-artifacts/.

## Steps

1. Extract the `keyValue` block schema + an example from Render's Blueprint docs.
2. Note how `fromService`/`fromDatabase`-style refs point at key-value connection strings.
3. Write `docs/render-artifacts/blueprint-keyvalue.md`.

## Files

- docs/render-artifacts/blueprint-keyvalue.md (new)

## Acceptance criteria

- [ ] Artifact pins the schema with sources; ambiguities called out explicitly.

## Out of scope

- Parser changes.
