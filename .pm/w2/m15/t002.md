---
id: w2/m15/t002
title: "Implement validate: reuse w1/m24's all-or-nothing validation, no apply"
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m15/t001]
---

## Objective

Implement `POST /v1/blueprints/validate`: run a `bex.yml` through `w1/m24`'s all-or-nothing validation path and return per-entry errors, without applying anything.

## Context

- `w1/m24`'s validation path (one invalid entry rejects the whole apply with per-entry errors, nothing partially created) — this task reuses that exact logic, factored out so it can run standalone.

## Steps

1. Factor `w1/m24`'s validation logic into a function callable without triggering the apply step, if it isn't already.
2. Wire `POST /v1/blueprints/validate` to call it and return the per-entry error list (or success).

## Files

- wherever `w1/m24`'s stack-deploy validation lives (likely `lego/backend/internal/apps/` or a new `blueprints` package)

## Acceptance criteria

- [ ] A bad `bex.yml` returns per-entry errors with zero side effects (nothing created/modified).
- [ ] A valid `bex.yml` returns success with zero side effects.

## Out of scope

- list/sync — t003.
