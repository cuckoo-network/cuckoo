---
id: w2/m28/t002
title: "Implement core.ErrPlanLimit sentinel"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m28/t001]
---

## Objective

Implement `core.ErrPlanLimit`, wrapping `ErrBadRequest`, carrying `{code: "PLAN_LIMIT", plan, limit}` — the first concrete consumer of t001's envelope design.

## Context

- `lego/backend/internal/core/errors.go` — add the new sentinel alongside the existing ones, following the same doc-comment convention.

## Files

- `lego/backend/internal/core/errors.go`

## Acceptance criteria

- [ ] `core.ErrPlanLimit` exists, wraps `ErrBadRequest` (so existing `errors.Is(err, core.ErrBadRequest)` checks still work), and carries the plan+limit params.

## Out of scope

- Wiring it into the members guards — t003.
