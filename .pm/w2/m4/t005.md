---
id: w2/m4/t005
title: Simplify — /simplify over the code this milestone changed
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m4/t004]
---

## Objective

Run `/simplify` over the code w2/m4 touched (apps feature service + REST/GraphQL/MCP fragments, store helpers) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Standing closing task; scope is strictly the milestone's diff.

## Steps

1. `/simplify` over the m4 diff; apply the accepted findings.

## Files

- Whatever m4 changed (see t001–t004).

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; `make test` (operator) + `go test ./...` (backend) still green.

## Out of scope

- Bug hunting (that's `/code-review`); new behavior.
