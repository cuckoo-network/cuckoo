---
id: w2/m4/t006
title: Test coverage — meaningful tests for create/delete behavior
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m4/t004]
---

## Objective

Add meaningful tests for what m4 shipped: validation failure modes (bad name/plan/body), store-mode vs store-less write paths, delete of unknown ids, and three-adapter delegation. Tests must assert real behavior and failure modes — no trivial, tautological, or snapshot-everything tests.

## Context

- Standing closing task; the existing apps feature tests and the w2/m1 MCP round-trip harness are the patterns to extend.

## Steps

1. Table-driven validation tests for `Create` (name/plan/image-vs-repo/env).
2. Store-mode test: create/delete survive a projector resync.
3. Adapter tests: REST status codes (201/204/400/404); MCP/GraphQL delegate to the same methods.

## Files

- `lego/backend/internal/apps/*_test.go`
- `lego/backend/internal/store/*_test.go` (if helpers were added)

## Acceptance criteria

- [ ] Every acceptance criterion in t001–t003 has a test that fails if the behavior regresses.
- [ ] `go test ./...` green in `lego/backend`.

## Out of scope

- Coverage-percentage gaming; e2e cluster tests beyond t004's.
