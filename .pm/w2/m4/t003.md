---
id: w2/m4/t003
title: GraphQL (createService/deleteService) + MCP (create_web_service, delete_service) fragments
worker: worker2
status: todo
estimate: 25m
depends_on: [w2/m4/t002]
---

## Objective

Add the remaining two adapters over the same verbs: GraphQL mutations `createService`/`deleteService` for the dashboard, and MCP tools `create_web_service` (Render's official tool name/args) plus `delete_service` (bex extension, named after the REST verb pattern like `restart_service`).

## Context

- MCP `create_web_service` exists in `render-oss/render-mcp-server` — match its name and argument names so Render-trained agents transfer; Render's MCP has no delete, so `delete_service` follows the established bex-extension naming rule (w2/m1 README).
- Fragments are pure presentation beside the service: `lego/backend/internal/apps/graphql.go` / `mcp.go`, composed in `internal/api/server.go`.

## Steps

1. GraphQL: `createService(input)` / `deleteService(id)` mutations returning the `Service` type / success boolean, matching the existing mutation style.
2. MCP: register `create_web_service` (Render args) and `delete_service` (`{serviceId}`), both returning the `service` object shape.
3. Update `docs/bex-api.md` MCP tool table.

## Files

- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/apps/mcp.go`
- `docs/bex-api.md`

## Acceptance criteria

- [ ] All three surfaces create/delete through the identical `Service` methods (no second implementation).
- [ ] MCP tool name/args match Render's `create_web_service`; `delete_service` keys on `serviceId`.

## Out of scope

- Dashboard UI for creation (w5); end-to-end acceptance (t004).
