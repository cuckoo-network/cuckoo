# Managed OIDC for AI / cloud providers (Render product)

From **w2/m79**. Render ships Managed OIDC (Anthropic, OpenAI, AWS GA) so services authenticate without long-lived API keys — dashboard/product surface, not in the public OpenAPI. bex agent sessions use BYO keys in OpenBao + mandatory SSH-gateway model proxy (ADR062/ADR064). Decide whether to build a Render-shaped Managed OIDC product or keep documenting the deliberate alternative; do not silently claim parity.
