---
id: w2/m77/t005
title: Evidence + docs — file the walk under evidence/, update ADR059's enablement trail and the m68 note
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m77/t004]
---

## Objective

The hibernate→rehydrate walk is durably evidenced under `.pm/w2/m77/evidence/`, ADR059 records the enablement (and its Status/Performance sections reflect measured reality), and the m68 DONE note's "enablement step" pointer is closed out.

## Context

w2/m68's README explicitly deferred the live acceptance to "the enablement step when the S3 contract is provisioned" — this milestone is that step, so the trail must be closed in all three places: evidence file, ADR, and the m68 note. Evidence follows the existing pattern (`.pm/w2/done/m68/evidence/2026-08-15-d7-opensandbox-snapshot-spike.md`); at closeout the whole m77 directory moves to `done/`, evidence included.

## Steps

1. Write `.pm/w2/m77/evidence/<date>-hibernate-rehydrate-walk.md`: bucket + SSE proof (t001), arm log line (t002), hibernate observations (t003), restore proof + latency sample vs SLOs (t004), and the rollback statement (unset env ⇒ reclaim reverts to Terminate). Redact bucket credentials and any tenant identifiers; object keys/prefixes are fine.
2. Update `docs/ADR059-agent-sandbox-hibernation.md`: note production enablement date, the measured resume-latency sample against the D4 SLOs, and any observed deviation (SLO miss, fallback events) in the Performance section.
3. Append one line to `.pm/w2/done/m68/README.md`'s status note: the deferred live walk was executed in w2/m77 on <date> (link the evidence file).
4. If t001 ended in a user hand-off, ensure the recorded provisioning steps also land in the evidence file for future re-provisioning.

## Files

- `.pm/w2/m77/evidence/<date>-hibernate-rehydrate-walk.md` (new)
- `docs/ADR059-agent-sandbox-hibernation.md`
- `.pm/w2/done/m68/README.md` (one-line closure note)

## Acceptance criteria

- Evidence file exists with all four walk stages + rollback documented; no credentials or `.env` contents included.
- ADR059 carries the enablement + measured latency; m68's note links here.
- Markdown formatted per repo rules (prettier is run at milestone shipping time, not by this task in isolation if the board says otherwise).

## Out of scope

ADR018 parity-ledger changes (no new surface); dashboard docs; retention-sweep 7-day observation (note it as a diary follow-up if desired).
