---
id: w2/m35/t004
title: Authenticated download (`can_view_sensitive`)
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m35/t003]
---

## Objective

Deliver the artifact per t001's captured mechanism (expiring presigned URL in the export record, or a streamed authenticated endpoint) behind the `can_view_sensitive` relation — a dump is the whole database, gate it like connection strings.

## Context

- Presigned URLs leak-by-forwarding; if presigned, keep TTL short and mint per-request, never store the URL. Decide + document.

## Files

- `lego/backend/internal/postgres/` (rest + service)

## Acceptance criteria

- [ ] Authorized caller retrieves and restores the dump; viewer without `can_view_sensitive` gets 403; the URL/endpoint expires per the recorded TTL.
