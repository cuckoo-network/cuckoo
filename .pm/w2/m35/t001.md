---
id: w2/m35/t001
title: Verify Render's export endpoints + artifact shape (capture)
worker: worker2
status: todo
estimate: 30m
depends_on: []
---

## Objective

Pin down what Render actually ships before building: the exports endpoints' request/response shapes (OpenAPI), the artifact format (`pg_dump` custom vs plain, compression), how the download is delivered (URL in the record? expiring link?), and retention. Record it in `docs/render-artifacts/`.

## Context

- Render OpenAPI: `https://api-docs.render.com/openapi/render-public-api-1.json`; docs render.com/docs/postgresql-backups.
- bex today: `lego/backend/internal/postgres/recovery.go` — exports are CNPG on-demand `Backup` snapshots (physical).

## Files

- `docs/render-artifacts/postgres-exports.md` (new capture)

## Acceptance criteria

- [ ] Capture answers: endpoint shapes, artifact format, download mechanism, retention. Divergences we intend to keep are named up front.

## Out of scope

- Any implementation.
