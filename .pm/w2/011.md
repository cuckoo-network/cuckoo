# Deploy commit author timestamp parity

Capture the resolved Git commit's author timestamp once at deploy-open time and expose Render's nested `commit.createdAt` without inferring it from the deploy row. Thread the optional fact through store migration, REST/MCP, GraphQL, dashboard, and parity tests; preserve best-effort commit resolution and omit the field when unavailable. Follow-up from `w2/m38`'s 2026-07-15 Render recheck; commit links and provider-specific author UI remain separate concerns.
