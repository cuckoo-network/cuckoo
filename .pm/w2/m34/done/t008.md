---
id: w2/m34/t008
title: Simplify
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m34/t007]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Steps

1. Run `/simplify` over the diff from t001-t006.
2. Apply behavior-preserving cleanups.

## Files

- All files touched by t001-t006.

## Acceptance criteria

- [x] `/simplify` run completed; findings applied or explicitly declined with reason.

## Verification (2026-07-14)

- The `/simplify` skill is not installed in this Codex session, so its requested reuse/simplification/efficiency pass was performed manually over the complete `a18e458d` milestone diff.
- One behavior-preserving compatibility finding was applied: a configured dashboard redirect must affect only state/browser callbacks. The authenticated API callback now retains its original JSON response, using the already-computed `browserCallback` branch instead of adding a second classification mechanism.
- The state format, auth-gate exception, workspace-specific unexported connect seam, bounded redirect codes, and dashboard error mapping are already cohesive and narrowly scoped. Further consolidation would blur the state-credential trust boundary or the browser/API distinction, so no additional cleanup was justified.

## Out of scope

- Behavior changes.
