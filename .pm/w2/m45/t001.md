---
id: w2/m45/t001
title: Pin Render semantics; parse + persist the field with a ran-once marker
worker: worker2
status: todo
estimate: 30m
depends_on: []
---

## Objective

Capture Render's documented `initialDeployHook` semantics (first *successful* deploy; behavior on failed first deploys; whether blueprint re-sync updates the command) into a render-artifacts note, then parse the field in the shared blueprint parser and persist it plus a ran-once marker on the App/store.

## Context

- Blueprint parser: `lego/backend/internal/apps/blueprint.go` (the shared render.yaml/bex.yml path w2/m37/m40/m41 all touch).
- The marker needs to survive re-syncs: a store column or App CR status field, decided here (compare how `w1/m33` records pre-deploy Job generations).
- ADR018:49 currently records the field as unsupported.

## Steps

1. Capture semantics → `docs/render-artifacts/initial-deploy-hook.md` (evidence-first, per board convention).
2. Parse + validate the field; persist command + ran-once marker.

## Files

- `lego/backend/internal/apps/blueprint.go`, store migration if a column is chosen, `lego/types/v1alpha1/app_types.go` if a CR field is chosen
- `docs/render-artifacts/initial-deploy-hook.md` (new)

## Acceptance criteria

- [ ] Parser accepts the field; the persisted command + marker round-trip.
- [ ] Semantics note records the first-successful-deploy rule with citations.

## Out of scope

- Execution (t002).
