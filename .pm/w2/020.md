# Live-verification sweep: execute the blocked closeout tasks in place

From `/pm-brainstorm for w2` 2026-08-18 (round 2, item 1). The board's dominant drag: ~8 items across workstreams are code-complete and blocked only on a live verification pass. Do **not** create new tasks — execute the existing ones **in place**, crediting each toward its own milestone's closeout (worker2 is general-purpose; this note is scheduling, not ownership transfer).

The list (verify each is still open before starting):

- `w1/m64` t008 — logged-in prod Playwright walk of the agent-sessions dashboard UI
- `w3/m41` t004 — `scripts/agent-session-verify.sh` live prod E2E (also unblocks w3/m77 and w7/023's HELD state)
- `w3/m77` t006 — transcript-recorder closeout (shares the m41 gate)
- `w5/m70` t008 — Billing page post-ship live verification
- `w5/m71` t006 — Key Value metrics fix, needs a Prometheus-wired stack
- `w5/028` — m58 Host/Path metrics-filter live browser walk
- `w5/029` — m60 dashboard dead-end live browser walk
- `w5/031` — m59 remaining live Render-side drift checks (Team · Usage/Billing · Notifications · Webhooks · Blueprints)

Ground rules: use the `hetzner-prod` context + Playwright MCP where prod access suffices. Anything that turns out to need **user-held** credentials (a real logged-in account we don't control, authorization to touch tenant data) gets reported back with the exact blocker, not forced. Explicitly out of scope: `w7/m77` t007 (tenant-data migration — needs explicit user authorization) and all physical-device-gated w11 items.
