---
id: w2/m40/t003
title: "Fixture e2e: environment-scoped groups round-trip"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m40/t002]
---

## Objective

A committed `bex.yml` fixture with a project → environment → envVarGroups block goes validate → apply against the mock cluster; afterwards the environment reads back with the group in `envGroupIds` (REST) and the dashboard environment card shows it.

## Files

- fixture under the blueprints test data; evidence in the milestone

## Acceptance criteria

- [ ] Live round-trip evidence captured; re-apply is idempotent (no duplicate group).
