---
id: w2/m38/t001
title: "Store schema: updated-at + transition facts"
worker: worker2
status: todo
estimate: 40m
depends_on: []
---

## Objective

Extend the deploy-row source of truth with a real `updated_at` and the minimal phase facts needed to persist evidence-backed status transitions.

## Context

- `lego/backend/internal/store/store.go` owns `store.Deploy` and deploy SQL.
- `lego/backend/internal/store/migrations/` owns additive control-plane schema.

## Steps

1. Add an idempotent migration and thread the fields through store reads/writes.
2. Advance `updated_at` only on actual deploy transitions and preserve old rows.

## Files

- `lego/backend/internal/store/store.go`
- `lego/backend/internal/store/migrations/`

## Acceptance criteria

- [ ] Old rows remain readable; store tests prove transition timestamps update only when state changes.

## Out of scope

- Commit metadata, which w9/001 already shipped, and adapter projection.
