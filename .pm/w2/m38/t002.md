---
id: w2/m38/t002
title: Define the evidence-backed deploy transition model
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m38/t001]
---

## Objective

Define valid transitions through Render's deploy vocabulary without claiming a phase the control plane cannot observe.

## Context

- `lego/backend/internal/store/reconciler.go` currently closes one coarse open row.
- The operator remains mechanism-only and DB-free.

## Steps

1. Map observable App/Job facts to created, queued, build, pre-deploy, update, live, failed, canceled, and deactivated states.
2. Define legal/race-safe transitions and timestamp semantics.

## Files

- `lego/backend/internal/deploys/`
- `lego/backend/internal/store/`

## Acceptance criteria

- [ ] A reviewed transition table names the evidence for every emitted state and rejects invalid regressions.

## Out of scope

- Adding an operator-to-database write path or changing commit provenance.
