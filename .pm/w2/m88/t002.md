---
id: w2/m88/t002
title: Update every workflow's `runs-on` to its trust pool
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m88/t001]
---

## Objective

Apply the t001 classification: every `runs-on` line across `.github/workflows/` and `lego/operator/.github/workflows/` gains its pool label (`[self-hosted, Linux, ARM64, ci]` or `[..., production]`). No workflow is left on the bare three-label set once the split lands.

## Context

- 24 workflows in `.github/workflows/`, 3 in `lego/operator/.github/workflows/` (some have multiple jobs — classify per job where a workflow mixes concerns, e.g. a test job + a publish job in `cli-release.yml`).
- `DO_NOT_DO.md #CI-RUNNERS`: any `runs-on: ubuntu-*` or line missing `self-hosted` stays forbidden.

## Steps

1. Mechanical edit per the classification table; per-job where needed.
2. Re-check reusable/called workflows and matrix jobs for inherited `runs-on`.
3. Push behind the normal PR/ship flow; jobs queue until the fleet re-label (expected, fail-safe).

## Files

- `.github/workflows/*.yml`
- `lego/operator/.github/workflows/*.yml`

## Acceptance criteria

- [ ] `grep -rn "runs-on" .github lego/operator/.github` shows every line carrying exactly one pool label consistent with the t001 table.

## Out of scope

- The validator enforcement (t003).
