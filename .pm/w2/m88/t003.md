---
id: w2/m88/t003
title: Enforce pool-per-trust in `scripts/github-actions-validate.sh`
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m88/t002]
---

## Objective

Make the split self-defending: extend `scripts/github-actions-validate.sh` so a credentialed workflow targeting the `ci` pool (or any `runs-on` missing a pool label) fails validation, alongside the existing GitHub-hosted rejection.

## Context

- The script already fails closed on `runs-on: ubuntu-*` / missing `self-hosted` (cited by `DO_NOT_DO.md #CI-RUNNERS`).
- Credentialed detection should key on the same evidence t001 used (environment gates / secret names), kept as an explicit allowlist in the script so drift is loud: a new workflow must be classified before it validates.

## Steps

1. Add the pool assertions + the classified-workflow list to the script.
2. Negative-test locally: a deliberately mislabeled copy must fail; the real tree must pass.
3. Confirm the script runs where it already runs in CI (`scripts.yml` or equivalent gate).

## Files

- `scripts/github-actions-validate.sh`

## Acceptance criteria

- [ ] Validation fails on: a credentialed workflow with `ci` label, any workflow with no pool label, any GitHub-hosted label; passes on the real tree.

## Out of scope

- Runtime (GitHub-side) enforcement — labels + validation are the mechanism.
