---
id: w2/m55/t010
title: Closeout
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m55/t009]
---

## Objective

Close the milestone once its definition of done actually holds — including live acceptance of the browser Web Shell on a `dev-N` stack. Do **not** move to `done/` on code-written-alone.

## Context

- Standing Closeout closing task (`.pm/CLAUDE.md`): completing this task performs the milestone move.
- Live acceptance needs a running gateway edge. The `dev-2` stack (`.pm/w2/dev-2/`) can exercise the WS exec path locally; a **production** Web Shell additionally depends on the m39 gateway being production-activated (m39 t008/t009 — public DNS/TCP-22 + live matrix), which is a separate track. Record prod status honestly rather than claiming it.

## Steps

1. Verify the DoD end-to-end on `dev-2`: open `/services/{id}/shell`, pick a Ready instance, get a live TTY in the app container via the browser; confirm the copy-ready `ssh` command coexists; confirm free/suspended/foreign/non-Ready fail closed; confirm redeploy/restart/timeout closes the session; confirm the `ssh_sessions` audit row has no terminal content; confirm bex-api still has no `pods/exec`.
2. Capture sanitized evidence under `w2/m55/evidence/`.
3. Note production status: if the m39 gateway isn't production-activated yet, record that prod Web Shell is gated on it (do not mark prod-verified falsely).
4. Run the `done` closeout: set every remaining task `status: done`, move each `tNNN.md` to `w2/m55/done/`, mark all rows `— **DONE**` and `**Status:** done` in the milestone README, move the whole `w2/m55/` to `w2/done/m55/`, and check `- [x]` in `w2/README.md`.

## Files

- `w2/m55/evidence/` (new)
- `.pm/` bookkeeping per the `done` subcommand.

## Acceptance criteria

- [ ] DoD verified live on `dev-2` with sanitized evidence recorded.
- [ ] Production status recorded honestly (prod Web Shell gated on m39 gateway activation if not yet live).
- [ ] Milestone moved to `w2/done/m55/`; `w2/README.md` box checked; statuses synced across all three places.

## Out of scope

- Any remaining implementation (must be done in t001–t009 before closeout).
