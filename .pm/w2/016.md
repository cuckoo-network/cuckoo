# Render-parity deploy-history table columns

Refine `dashboard/src/features/deploys/components/deploys-list-page.tsx` to match the current Render deploy-list presentation observed at `https://dashboard.render.com/web/srv-d2rnr3jipnbc73deuvgg`: render the history as an explicit responsive table/grid with `Deploy`, `Trigger`, `Duration`, and an action column. Keep the deploy/status/commit/timestamp content in the first column; humanize every stored trigger (`create`, `api`, `deploy_hook`, `rollback`, and `blueprint`); show elapsed `startedAt` → `finishedAt` time (and a running elapsed value for an active deploy); preserve Cancel for an active deploy; and show Rollback only for an older successful/deactivated deploy that can be a rollback target. Rows and their non-action content remain links to deploy detail, while action buttons must not trigger row navigation.

No backend work is expected: `deploys.graphql` already selects `trigger`, `startedAt`, `finishedAt`, and `rollbackOf`; `formatDeployDuration` and `DeployActions` already provide the core presentation and confirmed rollback/cancel mutations. Reuse the common table primitives, add English/Chinese column and trigger labels, and extend `deploys-list-page.test.tsx` plus the duration/action tests for desktop headings, responsive/accessibility behavior, running/terminal durations, trigger labels, eligible rollback, ineligible/failed/current-live rows, and action-click isolation. Render's rollback documentation confirms that rollback targets are previous successful deploys with retained artifacts; bex's server remains the final availability check.

This is a sub-hour UI refactor because the data, mutation, confirmation flow, and tests' fixtures already exist; keep it as an inbox note rather than creating a milestone.
