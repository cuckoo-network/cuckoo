---
id: w2/m57/t005
title: Simplify the PTY and verifier implementation
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m57/t004]
---

## Objective

Run `/simplify` over the PTY probe, live orchestration, and CLI-compat dispatch added by this milestone, preserving the proven behavior while removing duplicated process control, parsing, redaction, and cleanup logic.

## Context

- PTY lifecycle code is easy to make subtly unsafe: timeout, EOF, signal, and child-exit paths must converge on one bounded cleanup path.
- The shell verifier must not grow a second implementation of token exchange or Render CLI environment setup already owned by `scripts/cli-compat.sh`.
- Simplification must preserve secret redaction and exact-target cleanup even if that means retaining a small amount of explicit defensive code.

## Steps

1. Run `/simplify` over the milestone's changed Go and shell files.
2. Consolidate repeated timeout/process-finalization, sanitized reporting, API polling, and exact-resource cleanup helpers where doing so makes failure behavior easier to audit.
3. Remove redundant configuration knobs and reuse `scripts/cli-compat.sh`'s existing auth/version/config setup.
4. Re-run the focused non-live tests and document any suggested simplification deliberately rejected for safety.

## Files

- `lego/backend/internal/keyvalue/kvcli_acceptance_test.go`
- `scripts/keyvalue-cli-verify.sh`
- `scripts/cli-compat.sh`

## Acceptance criteria

- [ ] `/simplify` has reviewed every code path introduced by t001–t003, and accepted improvements are applied.
- [ ] There is one obvious owner for PTY teardown, transcript sanitization, fixture cleanup, and CLI auth bootstrap.
- [ ] Focused tests remain green and the live acceptance contract is unchanged.

## Out of scope

- General refactors of the Key Value service, CLI compatibility suite, or SSH verifier unrelated to this milestone.
