---
id: w2/m64/t007
title: "Simplify: /simplify over the changed code"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m64/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (backend `agentsessions` service/adapters, dashboard agent-session components) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Likely candidates: the sync/async split may leave duplicated lifecycle bookkeeping between `Create`/`Steer`/`Resume`; the dispatch-runner abstraction should be one mechanism, not three.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply the accepted cleanups; keep tests green.

## Files

- `lego/backend/internal/agentsessions/`
- `dashboard/src/features/agent-sessions/`

## Acceptance criteria

- [ ] `/simplify` findings triaged and applied or explicitly declined.
- [ ] Full backend + dashboard suites still green.

## Out of scope

- Bug hunting (that's `/code-review`).
