---
id: w2/m9/t007
title: Simplify — /simplify over the milestone's diff
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m9/t006]
---

## Objective

Standing closing task. Run `/simplify` over the code this milestone changed (types, operator build path, `internal/apps` token/webhook code, dashboard Build & Deploy section) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Keep `make test` / `make lint` (from `lego/operator/`) and dashboard checks green after applying findings.

## Steps

1. `/simplify` over the milestone diff; apply accepted findings.
2. Re-run tests/lint.

## Files

- Whatever the review flags

## Acceptance criteria

- [ ] Review run; accepted findings applied; tests/lint green

## Out of scope

- Bug hunting; new behavior
