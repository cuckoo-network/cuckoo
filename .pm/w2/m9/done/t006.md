---
id: w2/m9/t006
title: Render parity — cross-surface consistency, ledger row "Git connections" → ✅
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m9/t005]
---

## Objective

Standing closing task. Verify m9's changes are consistent across REST/GraphQL/MCP + UI (`autoDeploy` semantics everywhere it appears, webhook behavior documented), compare against Render's auto-deploy behavior, and update [docs/ADR018-render-parity.md](../../../docs/ADR018-render-parity.md): "Git connections (GitHub / GitLab app)" row to ✅ (GitHub; GitLab/Bitbucket noted as the remaining ◐ scope), evidence pointers refreshed, gap backlog row closed.

## Context

- Render comparable: auto-deploy on push to the linked branch, toggleable (`render.com/docs/github`); bex adds nothing Render lacks here except the still-supported manual HMAC path (keep it listed under "bex ahead").
- Also refresh [docs/ADR017-deploy-from-chat.md](../../../docs/ADR017-deploy-from-chat.md) consequences (two-key webhook) and the PR-previews row's "ties to git integration" note (now unblocked, still untracked).

## Steps

1. Cross-surface sweep; fix or file drift.
2. Ledger + ADR updates; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR017-deploy-from-chat.md`, `docs/ADR026-github-integration.md`

## Acceptance criteria

- [ ] Ledger row updated with evidence; drift (if any) filed as follow-up

## Out of scope

- GitLab/Bitbucket work itself
