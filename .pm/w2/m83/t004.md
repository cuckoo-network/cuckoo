---
id: w2/m83/t004
title: Render parity — /agents surface consistency
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m83/t001, w2/m83/t002, w2/m83/t003]
---

## Objective

Confirm merging controls did not make any API capability unreachable from the UI.

## Context

- Render has no coding-agent product (`docs/ADR018-render-parity.md`), so there is no render.com behavior to compare against; parity is bex's own cross-surface discipline.
- This milestone is UI-only by intent — no REST/GraphQL/MCP change should be needed. The risk is the opposite direction: a capability the API exposes losing its last UI entry point.

## Steps

1. List what create/list accept (repo, branch, session references, archived/phase filters) and confirm each is still reachable in the UI.
2. Confirm no backend change was needed; if one was, it belongs in its own task.
3. Flag any capability now unreachable as follow-up rather than silently dropping it.

## Files

- `dashboard/src/routes/agents.tsx`, `dashboard/src/features/agent-sessions/`
- `lego/backend/internal/agentsessions/` (read-only)

## Acceptance criteria

- [ ] Every create/list capability still has a UI path.
- [ ] No backend surface changed.
- [ ] Anything dropped is filed as follow-up.

## Out of scope

- Adding UI for capabilities that never had it.
