---
id: w2/m83/t007
title: Closeout
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m83/t006]
---

## Objective

Close the milestone once `/agents` actually shows one of each in a browser.

## Context

- Per `.pm/CLAUDE.md`, completing this task **is** the move.
- The DoD is visual, so verify in a running dashboard at both breakpoints — not from tests alone.

## Steps

1. Open `/agents` in a running dev-N and confirm one repo-attach control and one Recents surface, at desktop and narrow-mobile.
2. Confirm archived/all still work and a session can still be started with and without a repo.
3. Set every task `status: done`, move each to `done/`, mark rows and `**Status:** done`.
4. Move `w2/m83/` to `w2/done/m83/` and tick the box in `.pm/w2/README.md`.

## Files

- `.pm/w2/m83/**`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] Verified in a browser at both breakpoints.
- [ ] Milestone moved to `w2/done/m83/` with no stub left behind.
- [ ] `.pm/w2/README.md` shows `- [x] **m83**`.

## Out of scope

- Any code change.
