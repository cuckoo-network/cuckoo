---
id: w2/m83/t003
title: Re-check the /agents skeletons against the simplified ready state
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m83/t001, w2/m83/t002]
---

## Objective

Keep the preloading skeletons honest after the layout changes.

## Context

- The repo rule (`CLAUDE.md`, `dashboard/CLAUDE.md#navigation-pending-states-the-white-flash-fix`) is explicit: a skeleton is a structural preview of the exact ready state at the same breakpoint — outer bounds, padding, max-width, columns, headings, tabs, major regions with stable heights.
- `AgentsPageSkeleton` is the route's `pendingComponent`, and there is an existing `agents-page-skeleton.test.tsx` plus a `conversation-skeleton.tsx`.
- Removing a whole list region (t002) and a control (t001) changes that geometry, so the skeletons will over-promise unless updated.

## Steps

1. Update `AgentsPageSkeleton` to the post-t001/t002 geometry.
2. Update the conversation skeleton if the detail layout shifted.
3. Compare pending and ready side by side at desktop and narrow-mobile, as the rule requires.
4. Update the existing skeleton test.

## Files

- `dashboard/src/common/components/detail-skeletons/`
- `dashboard/src/features/agent-sessions/components/conversation-skeleton.tsx`
- `dashboard/src/common/components/__tests__/agents-page-skeleton.test.tsx`

## Acceptance criteria

- [ ] Skeleton matches the ready state at desktop and narrow-mobile.
- [ ] No skeleton region without a ready-state counterpart.
- [ ] Skeleton test updated and passing.

## Out of scope

- Skeletons for routes this milestone did not touch.
