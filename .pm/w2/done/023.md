# Service outbound IPs (`GET /services/{id}/outbound-ips`)

From **w2/m79** upstream drift re-baseline. Render's live OpenAPI (2026-08-19) adds `GET /services/{serviceId}/outbound-ips` (`retrieve-service-outbound-ips` → `outboundIps`). bex has no REST/GraphQL/MCP/UI counterpart (ADR018 row ✖). Distinct from workspace **Dedicated outbound IPs** (DO_NOT_DO `—`).

Implement Render-shaped read across the three adapters + optional dashboard Connect affordance once product confirms shared-egress vs dedicated semantics for bex.

**DONE 2026-08-23** — Render-shaped read shipped across REST (`GET /v1/services/{id}/outbound-ips`), GraphQL (`Service.outboundIps`), MCP (`get_service_outbound_ips`). Wire shape pinned from Render's live OpenAPI (fetched 2026-08-23): 200 body is the `outboundIps` schema directly — `{type, ips[], dedicatedIpId?}`. bex truthfully returns `type:"shared"` with the sorted/deduped ExternalIPs of `bex.co/pool=tenant` Nodes (no NAT gateway ⇒ pod egress sources from its node IP; empty pool ⇒ `ips: []`); `dedicatedIpId` never present (dedicated IPs stay a DO_NOT_DO non-goal). Authz = `can_view` on the service, 403-before-404. RBAC: bex-api ClusterRole gained `nodes get,list` (`lego/operator/config/api/rbac.yaml`); scope matrix regenerated; ADR018 row flipped ✅/✅/✅/✖ (UI deferred per the note's product gate). Tests + full `internal/api` suite + `make lint` + `gitops-validate.sh` green. Not done: live-cluster node read; Render spec pin re-baseline (stays with m79's next pass).
