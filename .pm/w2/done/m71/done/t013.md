---
id: w2/m71/t013
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m71/t012]
---

## Objective

Close the milestone once its definition of done actually holds: every DoD clause of `w2/m71/README.md` is real and test-pinned, not merely code-written.

## Context

- Follow `.pm/CLAUDE.md` + the `/pm done` conventions: set remaining tasks `status: done`, move each `tNNN.md` to `w2/m71/done/`, mark rows `— **DONE**` + `**Status:** done` in the milestone README, move the whole `w2/m71/` to `w2/done/m70/`, check `- [x]` in `w2/README.md`. Never leave a tombstone at the old path.

## Steps

1. Verify the milestone DoD clause by clause (record evidence pointers).
2. Execute the closeout moves; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m71/**`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] `w2/done/m70/` exists with all tasks in `done/`; `w2/README.md` box checked; status synced in all three places.

## Out of scope

- Any new work discovered — inbox-note it.
