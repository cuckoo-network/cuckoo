---
id: w2/m71/t004
title: "Replay-only attach tickets: mint on terminal/hibernated, gateway empty-pod-claim replay"
worker: worker2
status: done
estimate: 45m
depends_on: []
---

## Objective

ADR065 D2.1 — make "archived sessions can still be viewed" true: `attach-ticket` on a terminal/`hibernated` session with empty `sandbox_id` mints a ticket with an empty pod claim instead of `AGENT_SESSION_NOT_ATTACHABLE`; the gateway attach listener, on an empty pod claim, serves the durable-transcript replay + `[DONE]` and never dials; the stream `POST` on such a ticket refuses.

## Context

- Mint refusal today: `agentsessions/service.go:1089-1092`; the Completer blanks `sandbox_id` at terminate and after hibernate (`completion.go:452-457`, `:501-504`).
- Gateway: `lego/backend/internal/sshgateway/agentattach/agentattach.go` already replays-then-`[DONE]` for a gone pod — the change is admitting the ticket and short-circuiting the dial on an empty pod claim.
- `AGENT_SESSION_NOT_ATTACHABLE` narrows to the genuinely unattachable window (provisioning/pre-dispatch).

## Steps

1. Mint: allow terminal/`hibernated` + empty `sandbox_id` ⇒ empty pod claim; keep the refusal for `creating`/pre-dispatch.
2. Gateway: empty pod claim ⇒ DB replay + `[DONE]`, no dial, `POST` refused with a coded error.
3. Ticket-claim validation: ensure nonce/claims verification tolerates the empty pod field everywhere it is checked.

## Files

- `lego/backend/internal/agentsessions/service.go`
- `lego/backend/internal/sshgateway/agentattach/agentattach.go` (+ its tests)
- shared ticket claims type (`internal/sandboxexec/ticket.go` lineage — locate the agent-session ticket struct)

## Acceptance criteria

- [ ] Tests: terminal session mints; gateway serves full replay + `[DONE]` without dialing (no pod lookup); `POST` refused; provisioning-phase session still gets `AGENT_SESSION_NOT_ATTACHABLE`.
- [ ] Live-phase attach behavior byte-identical to today.

## Out of scope

- Dashboard gate removal (t008); JSON transcript read (t005).
