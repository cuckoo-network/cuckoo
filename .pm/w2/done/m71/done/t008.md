---
id: w2/m71/t008
title: "Dashboard: drop the sandboxId conversation gate (terminal-session replay)"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m71/t004]
---

## Objective

With t004's replay-only tickets, remove the dashboard's `sandboxId` gate so every terminal/archived session's conversation renders through the existing `useChat` replay — replacing the "no conversation" fallback for reaped sessions.

## Context

- Gate: `dashboard/src/features/agent-sessions/components/session-chat-column.tsx:77-97` — renders `SessionConversation` only when `session.sandboxId` is set; otherwise `ConversationFallback`.
- The mint verb now succeeds for terminal sessions; the stream replays then `[DONE]`; `useChat` with `resume: true` already consumes replay-only streams (ADR051's zero-client-change precedent).
- Keep the fallback for genuinely unattachable states (provisioning, `AGENT_SESSION_NOT_ATTACHABLE`).

## Steps

1. Gate on attachability (phase/mint result), not `sandboxId`; terminal sessions attempt the replay attach.
2. Composer stays disabled/hidden for terminal + archived sessions (stream `POST` refuses anyway — don't render a dead input).
3. Update affected component tests.

## Files

- `dashboard/src/features/agent-sessions/components/session-chat-column.tsx` (+ sibling components/tests as touched)

## Acceptance criteria

- [ ] Component tests: terminal session with transcript renders the conversation; provisioning session shows the fallback; composer absent on terminal/archived.
- [ ] Dashboard suite + lint + typecheck green.

## Out of scope

- Archive UI/filters (t009).
