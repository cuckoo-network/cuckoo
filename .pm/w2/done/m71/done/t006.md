---
id: w2/m71/t006
title: "List filters + pagination wired through REST/GraphQL/MCP"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m71/t001]
---

## Objective

ADR065 D3 on the wire: the session list defaults to unarchived, filters on `archived`/`phase`/`repo`/`createdBefore`/`createdAfter`, and keyset-paginates (`limit` default 50, max 200, opaque cursor) — identically across REST `GET /v1/agent-sessions`, GraphQL `agentSessions`, and MCP `list_agent_sessions`.

## Context

- Today's surfaces take only `ownerId` (`rest.go:28-35`, `graphql.go:145-148`, `mcp.go:39-88`).
- t001 provides the store filter struct + cursor; this task is the three-surface contract + validation (invalid phase/cursor ⇒ coded 400).
- The unpaginated read disappears — audit in-repo callers (dashboard adapts in t009).

## Steps

1. REST: query params + a paged response envelope (items + `nextCursor`), matching the house pagination shape used elsewhere in bex-api (check deploys/history pagination for the precedent — round-6 #7 "store-bounded history pagination").
2. GraphQL: filter args + connection-style/cursor result consistent with existing paginated queries.
3. MCP: same params on `list_agent_sessions`.

## Files

- `lego/backend/internal/agentsessions/{rest,graphql,mcp,models,service}.go`
- `dashboard/src/graphql/` generated ops (regenerate as needed; consumer change is t009)

## Acceptance criteria

- [ ] Tests: default excludes archived on all three surfaces; each filter round-trips; limit clamp at 200; invalid cursor/phase ⇒ coded 400; page walk is stable under concurrent inserts (keyset property).

## Out of scope

- Dashboard controls (t009); full-text search.
