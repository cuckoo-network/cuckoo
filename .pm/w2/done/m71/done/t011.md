---
id: w2/m71/t011
title: Simplify — /simplify over the changed code
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m71/t010]
---

## Objective

Standing closing task: run `/simplify` over the code this milestone changed (store, agentsessions service + surfaces, agentattach gateway, dashboard feature) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Watch for: duplicated phase-guard predicates across archive/delete/resume gates; the ticket empty-pod-claim branch vs the existing gone-pod branch in `agentattach.go`; filter-struct plumbing repeated across three surfaces.

## Steps

1. `/simplify` over the milestone diff; apply the fixes.
2. Re-run backend + dashboard suites and lint.

## Files

- Whatever the milestone touched (see t001–t009).

## Acceptance criteria

- [ ] Simplify pass applied; all suites + lint green; behavior unchanged (no test assertions weakened).

## Out of scope

- Bug hunting (that's /code-review territory).

## Applied (2026-08-16)

Three parallel review agents (reuse/quality/efficiency) over the m70 diff; fixes applied, all suites re-green (backend `go test ./...` + real-PG store/agentsessions on a fresh DB + `make lint-backend`; dashboard 308 files / 2124 tests + lint):

- Merged the byte-identical `deletablePhase`/`replayablePhase` into one `finishedPhase`; added the dashboard twin `isFinished` on the View (used by the chat-column attach gate and the header's delete gate).
- Extracted `normalizeLimit(limit, def, max)` (was written twice with different constants); GraphQL resolvers now use `gqlutil.PositiveLimit` so an explicit `limit: 0` 400s like REST instead of silently serving the default page.
- REST archive/unarchive collapsed onto `core.HandleByID`; the transcript `afterSeq` parse now rides `core.QueryLimit64` + `core.WriteErr` (coded envelope) instead of a hand-rolled `strconv` + `WriteErrStatus`.
- Transcript read: the row limit is pushed into SQL (`LIMIT`, new store param; the gateway replay passes 0 = byte-budget-only) and the session-row existence pre-fetch moved behind the empty-page case — one query on the common polled read instead of two + up-to-20x over-fetch.
- Migration 0078 now also drops the dead `agent_session_transcripts_created_idx` (its only consumer was the removed prune) — write amplification gone; down migration recreates it.
- Dropped `ListRequest`'s misleading JSON tags (never wire-decoded).
- Dashboard: one `useArchiveToggle` hook replaces the two duplicated toggle handlers AND fixes their raw `err.message` toasts (now `agentSessionErrorMessage` i18n copy); rows are presentational (one mutations-hook instance per list, not per row); the five `id → View` mutation wrappers collapsed onto a local `useByIdViewMutation` factory (the c75a6811 move); delete confirm uses `buttonVariants({variant:"destructive"})`; header prop renamed `onCanceled` → `onChanged`; `AGENT_SESSION_PHASES` const in types.ts is now the single phase list (route filter derives from it, typed select state).

Skipped (deliberate): swapping the coded `AGENT_SESSION_FILTER_INVALID` for `core.ParseEnum`'s plain 400 (the coded error is the cross-surface contract and test-pinned); the partial index for the archived-heavy working set (low severity at current cardinality, revisit if archives accumulate); consolidating the repo's four hand-rolled Apollo `fetchMore` pagers (crosses feature boundaries — future sweep).
