---
id: w2/m71/t010
title: Render parity — surface consistency check (REST/GraphQL/MCP/dashboard)
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m71/t007, w2/m71/t008, w2/m71/t009]
---

## Objective

Standing closing task: verify the milestone's changes are consistent across every surface they expose — same fields, semantics, and error shapes on REST, GraphQL, and MCP (`docs/ADR006-bex-api.md`), and the dashboard renders the same model. Render has no agent-session product, so the exemplar comparison is Devin's documented archive semantics (viewable-not-modifiable while archived, reversible, terminate-is-destructive) rather than render.com; flag any drift as follow-up work, never silently diverge.

## Context

- New/changed surface: archive/unarchive/delete verbs, `archivedAt` on the View, `AGENT_SESSION_ARCHIVED` 409, list filters + pagination, the transcript read, replay-only attach.
- Check `docs/ADR018-render-parity.md` — agent sessions are a bex extension; add/adjust a ledger note only if the ledger already tracks the agent-session surface.

## Steps

1. Diff the three surfaces' request/response/error shapes for each new verb + the list contract; exercise each with a scripted smoke (REST curl, GraphQL query, MCP tool call).
2. Confirm dashboard behavior matches the wire semantics (e.g. archived working-set exclusion mirrors `archived=false`).
3. File follow-up inbox notes for any drift found.

## Files

- `lego/backend/internal/agentsessions/**` (verification target; fixes in place if trivial)
- `docs/ADR018-render-parity.md` (note only if applicable)

## Acceptance criteria

- [ ] Written check record in this task (or milestone README) listing each surface × verb with ✅/drift disposition.

## Out of scope

- New capability work discovered here — inbox-note it.

## Check record (2026-08-16)

| surface | archive | unarchive | delete | transcript | list filters+cursor | archivedAt on View | disposition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REST | ✅ `POST /{id}/archive` | ✅ `POST /{id}/unarchive` | ✅ `DELETE /{id}` (204) | ✅ `GET /{id}/transcript` | ✅ query params | ✅ | one service impl behind thin fragments |
| GraphQL | ✅ `archiveAgentSession` | ✅ `unarchiveAgentSession` | ✅ `deleteAgentSession: Boolean` | ✅ `agentSessionTranscript` | ✅ args | ✅ | same |
| MCP | ✅ `archive_agent_session` | ✅ `unarchive_agent_session` | ✅ `delete_agent_session` | ✅ `get_agent_session_transcript` | ✅ args | ✅ | same |
| dashboard | ✅ row + header | ✅ row + header | ✅ detail confirm dialog | ✅ stream replay (replay-only ticket) | ✅ tabs + phase select + load-more | ✅ badges | mirrors `archived=false` default |

Error shapes are structurally identical across surfaces (single service implementation; `core.WriteErr`/`gqlutil`/`core.MCPError` adapters). Recorded divergences, deliberate:

- GraphQL transcript `part` is `String!` (verbatim JSON text) while REST/MCP emit raw JSON — GraphQL has no raw-JSON scalar in this schema; clients parse.
- bex `delete` refuses live phases (cancel first) instead of Devin's `DELETE?archive=true` escape hatch — ADR065 D4 decision, no escape hatch on a destructive verb.
- No dashboard date-range filter controls (createdBefore/After are wire-only); repo filter likewise wire-only — controls shipped: archive membership tabs + phase select + load-more. Follow-up only if demand appears.
- Devin exemplar semantics held: archive reversible + viewable-not-modifiable (`AGENT_SESSION_ARCHIVED` on resume/steer/pin/turn; reads + cancel allowed), archived excluded from the default list, Archived home in the sidebar.
- `docs/ADR018-render-parity.md` untouched: agent sessions are a bex extension with no Render row.
