---
id: w2/m71/t003
title: "Service: fresh-authorized hard delete verb (blob-before-row, audit event)"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m71/t001]
---

## Objective

ADR065 D4: `DELETE /v1/agent-sessions/{id}` (+ GraphQL `deleteAgentSession`, MCP `delete_agent_session`) — hard-delete a terminal/`hibernated` session under fresh authorization; snapshot blob deleted before the row; transcripts cascade; audit event written; refused on live phases.

## Context

- Destructive sink ⇒ `AuthorizeFresh` per the ADR061 #8 / ADR063 #7 seam (see `AuthorizeFresh*` call sites in `lego/backend/internal/` for the pattern — e.g. Postgres/KeyValue delete).
- Blob-before-row ordering mirrors the expiry sweep (`sweepExpiredHibernations`, `completion.go:234-254`); Cancel's best-effort blob delete (`service.go:743-783`) shows the snapshot-store call.
- Allowed phases: `completed|failed|canceled|hibernated` with no live sandbox; live phases get a coded 409 (cancel first — no `archive=true` escape hatch).

## Steps

1. Service `Delete`: fresh authz, phase guard, snapshot blob delete first (when `snapshot_ref` set), then row delete, audit event.
2. Wire the three surfaces; REST is `DELETE /v1/agent-sessions/{id}`.

## Files

- `lego/backend/internal/agentsessions/{service,rest,graphql,mcp}.go`
- `lego/backend/internal/audit/` (event kind if a new one is needed)

## Acceptance criteria

- [ ] Tests: live-phase refusal; blob deleted before row (order observable via fake snapshot store); transcripts gone after delete; fresh-authz path exercised (cached-allow / fresh-deny refuses); audit event recorded.

## Out of scope

- Dashboard confirmation dialog (t009); workspace-level retention policy.
