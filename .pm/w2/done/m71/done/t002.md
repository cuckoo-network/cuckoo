---
id: w2/m71/t002
title: "Service: archive/unarchive verbs + AGENT_SESSION_ARCHIVED 409 gate + idle-grace zeroing"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m71/t001]
---

## Objective

ADR065 D1: `archive`/`unarchive` as idempotent `can_operate` verbs on all three surfaces; while archived, mutation verbs refuse with the coded `AGENT_SESSION_ARCHIVED` 409; archiving a live-sandbox session zeroes the idle grace so the Completer reclaims at its next tick without canceling an in-flight turn.

## Context

- Service: `lego/backend/internal/agentsessions/service.go` (cancel at :743-783 is the shape/authz template); surfaces: `rest.go`, `graphql.go`, `mcp.go`; error codes in `models.go`.
- Idle grace: Completer `teardown`/`reclaim` (`completion.go:398-449`), `BEX_AGENT_SANDBOX_IDLE_TTL` (ADR059 D2 / w2/m67).
- Gated verbs: `resume`, `steer`, `pin`, `unpin`, and the stream-`POST` path (gate at ticket mint / gateway prompt-turn). `cancel` + reads stay allowed.

## Steps

1. Service `Archive`/`Unarchive` (authorize `can_operate`, idempotent; archive marks the session so the Completer treats idle grace as elapsed — e.g. a flag the reclaim check reads alongside `archived_at`).
2. Add `AGENT_SESSION_ARCHIVED` (409) and gate resume/steer/pin/unpin + the live-turn ticket path; identical error shape ×3 surfaces.
3. Wire REST `POST /v1/agent-sessions/{id}/archive|unarchive`, GraphQL `archiveAgentSession`/`unarchiveAgentSession`, MCP `archive_agent_session`/`unarchive_agent_session`; add `archivedAt` to the shared `View` (`models.go:153-189`).

## Files

- `lego/backend/internal/agentsessions/{service,rest,graphql,mcp,models}.go`
- `lego/backend/internal/agentsessions/completion.go` (reclaim honors archive as zero grace)

## Acceptance criteria

- [ ] Tests: archive idempotent; gated verbs 409 with the code while archived; unarchive restores them; archive on a live sandbox triggers next-tick reclaim without canceling the turn; `cancel` works while archived.
- [ ] Error shape identical across REST/GraphQL/MCP (test-pinned).

## Out of scope

- Delete (t003), list filters (t006), dashboard (t009).
