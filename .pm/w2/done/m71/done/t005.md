---
id: w2/m71/t005
title: "Poll-shaped transcript read ×3 surfaces + remove dead PruneAgentSessionTranscripts"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m71/t001]
---

## Objective

ADR065 D2.2: the ADR047-D9 "optional" transcript read, built — `GET /v1/agent-sessions/{id}/transcript` (seq-paginated, parts verbatim), GraphQL `agentSessionTranscript`, MCP `get_agent_session_transcript`. Plus the D2 retention decision: transcripts are product data — remove the dead `PruneAgentSessionTranscripts`.

## Context

- Store reads exist: `AgentSessionTranscript` (replay), `AgentSessionTranscriptMaxSeq` (`store/agentsessions.go`); 64 MiB/session cap bounds reads.
- Dead prune: `store/agentsessions.go:602-613`, called only by `store_pg_test.go:292-295`; the `created_at` index comment claims a sweep that never existed.
- Parts are verbatim `text` (never jsonb) — return them untouched; paginate by `seq` cursor + limit.

## Steps

1. Store: seq-keyset paginated transcript read (cursor = last seq, limit).
2. Wire REST/GraphQL/MCP with authz matching `get` (`can_view`-level, whatever `get` uses today); identical shapes.
3. Remove `PruneAgentSessionTranscripts` + its test; fix the index comment to record cascade-only deletion.

## Files

- `lego/backend/internal/store/agentsessions.go`, `store_pg_test.go`
- `lego/backend/internal/agentsessions/{service,rest,graphql,mcp}.go`
- `lego/backend/internal/store/migrations/0067_agent_session_transcripts.up.sql` (comment only if migrations are immutable — then fix in code comment instead; do not edit an applied migration)

## Acceptance criteria

- [ ] Tests: pagination boundaries, verbatim part bytes round-trip, archived + terminal sessions readable, authz enforced.
- [ ] `PruneAgentSessionTranscripts` gone; no caller remains; `go test ./...` green.

## Out of scope

- Full-text search; per-workspace transcript-storage bounds.
