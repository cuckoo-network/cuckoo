---
id: w2/m71/t009
title: "Dashboard: per-row archive/unarchive, Archived section, filter/pagination controls, delete dialog"
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m71/t002, w2/m71/t003, w2/m71/t006]
---

## Objective

ADR065 D6: the dashboard surface — per-row archive/unarchive (list + session header), an Archived section reachable from the sidebar (Devin's Folder → Archived analog) excluded from the working-set rail and its fuzzy search, filter + pagination controls on `/agents?view=list`, and a confirm-dialog delete on the session detail page only.

## Context

- Feature dir: `dashboard/src/features/agent-sessions/` — `session-list.tsx` (table), `agent-sessions-nav-section.tsx` (sidebar working set + fuzzy search), `session-detail-header.tsx` (pin/cancel buttons precedent), `use-agent-sessions.ts` (5s/30s adaptive polling — now fetches one filtered page).
- One-rail convention (ADR047 D9a): the Archived section is a branch inside `DashboardSidebar`, never a second `<aside>`; guarded by `one-rail-invariant.test.ts`.
- Destructive verbs are desktop-only (ADR048); delete gets a typed confirm dialog, off the list rows.
- en/zh label parity per the house i18n pattern (see deploys-list precedent).

## Steps

1. Mutations + hooks for archive/unarchive/delete (typed `AGENT_SESSION_*` error mapping, `no-cache` per house pattern).
2. Working-set rail + default list request `archived=false`; Archived entry point → filtered list view.
3. `view=list` filter controls (archived/phase/repo/date) + cursor pagination ("load more" or pager per existing table precedent).
4. Per-row + header archive/unarchive buttons; delete dialog on detail page.

## Files

- `dashboard/src/features/agent-sessions/**` (components, hooks, graphql ops, tests)
- `dashboard/src/components/dashboard-sidebar/**` (Archived section branch)

## Acceptance criteria

- [ ] Component tests: archive removes the row from the working set without a reload; Archived view lists it; unarchive restores; delete requires confirmation and navigates away; filters/pagination drive the query variables.
- [ ] One-rail invariant test still green; dashboard suite + lint + typecheck green.

## Out of scope

- Bulk archive-all/undo, folders, drag-and-drop, tags (deferred per ADR065 D6).
