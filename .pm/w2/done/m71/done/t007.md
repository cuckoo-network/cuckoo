---
id: w2/m71/t007
title: Retention-expiry auto-archive + CLAUDE.md retention-wording fix
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m71/t002]
---

## Objective

ADR065 D5: `ExpireHibernatedAgentSession` additionally stamps `archived_at` so a session whose snapshot aged out leaves the working set by itself; and fix the standing doc drift — CLAUDE.md's `BEX_AGENT_SNAPSHOT_RETENTION` entry says the sweep deletes "snapshot + row" but the code keeps the row (and now archives it).

## Context

- `ExpireHibernatedAgentSession`: `lego/backend/internal/store/agentsessions.go:400-418`; sweep: `sweepExpiredHibernations`, `completion.go:234-254`.
- Also stale: the `ListHibernatedForRetention` comment (`store/agentsessions.go:372-375`) repeats the "+ row" claim.
- No new phase — expiry stays `canceled` + named `failure_reason`; distinguishability comes from `archived_at` + the reason.

## Steps

1. Stamp `archived_at` inside `ExpireHibernatedAgentSession` (same UPDATE).
2. Correct the `ListHibernatedForRetention` comment and CLAUDE.md's env-table entry (snapshot only; row + transcript kept, session auto-archived).

## Files

- `lego/backend/internal/store/agentsessions.go`
- `lego/backend/internal/agentsessions/completion.go` (only if the sweep needs to pass anything through)
- `CLAUDE.md`

## Acceptance criteria

- [ ] Test: expired session ends `canceled` + archived + snapshot fields cleared, transcript intact.
- [ ] Both comment and CLAUDE.md wording match the code; `npx prettier@3.4.2 --write "**/*.md"` clean.

## Out of scope

- Any other automatic archive edge (no idle/age auto-archive).
