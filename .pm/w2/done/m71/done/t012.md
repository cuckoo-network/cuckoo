---
id: w2/m71/t012
title: Test coverage — behavior + failure-mode tests for archive/read/delete/pagination
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m71/t010]
---

## Objective

Standing closing task: close the gaps between per-task tests and the milestone's definition of done — every DoD clause pinned by a meaningful test asserting real behavior and failure modes (never tautological/snapshot-everything).

## Context

- High-value seams: archive×phase matrix (archive during `running` → next-tick reclaim, turn not canceled); `AGENT_SESSION_ARCHIVED` shape identical ×3 surfaces; replay-only ticket cannot reach the dial path (assert no pod lookup); delete's fresh-authz refusal + blob-before-row ordering; keyset pagination under concurrent inserts; expiry auto-archive leaves transcript readable; dashboard archived-exclusion + composer-absence.
- Real-Postgres integration tests run in CI (`BEX_TEST_DB_URI` — not skipped).

## Steps

1. Audit t001–t009 coverage against the milestone DoD; write the missing tests.
2. Full backend `go test ./...` + dashboard suite + `make lint-backend`.

## Files

- `lego/backend/internal/{store,agentsessions,sshgateway/agentattach}/**_test.go`
- `dashboard/src/features/agent-sessions/**/__tests__/**`

## Acceptance criteria

- [ ] Each milestone-DoD clause maps to at least one named test; all suites green.

## Out of scope

- Live-cluster E2E (rides the standing operator-run gate like m41/m43; note it in the closeout if deferred).

## Coverage record (2026-08-16)

DoD clause → pinning tests:

- Archive verbs idempotent + phase-untouched + `AGENT_SESSION_ARCHIVED` gate on resume/steer/pin/unpin (+ create-resume routing), reads + cancel allowed: `TestArchiveUnarchiveLifecycle`, `TestCancelAllowedWhileArchived`, `TestCreateResumeOfArchivedRefused` (archive_test.go); mint + redemption turn gates: `TestArchivedRefusesTurnTicketsMintAndRedemption`.
- Idle-grace zeroing at next tick: `TestCompleterReclaimsArchivedImmediately` (the in-flight-turn non-cancellation is structural — archive never touches the sandbox; the Completer only reclaims after finalize, pinned by the existing completion suite).
- Replay-only tickets: `TestAttachTicketReplayOnlyForReapedSessions` (service), `TestPodTripleAllOrNothing` (ticket), `TestAgentAttachReplayOnlyTicketNeverDials` (gateway: no pod lookup, replay + [DONE], POST 403); dashboard render: `session-chat-column.test.tsx` terminal/hibernated/archived cases.
- Transcript read: `TestTranscriptPagesVerbatimParts` (paging, verbatim bytes, archived readable, 404/cross-workspace); REST `afterSeq` 400 in `TestArchiveRESTAdapters`; SQL row limit: store PG row-limit assertion in `assertAgentSessionTranscripts`.
- List contract: real-PG filters/cursor/idempotency (`assertAgentSessionArchive`), service validation (`TestListDefaultsToWorkingSetAndValidatesFilters`, `TestListLimitNormalization`), REST param plumbing + coded 400s (`TestArchiveRESTAdapters`), GraphQL surface (`TestArchiveGraphQLSurface`).
- Delete: phase guard, blob-before-row, idle-grace sandbox teardown-first, fresh-authz refusal (`TestDeleteGuardsAndOrdering`, `TestDeleteTearsDownIdleGraceSandboxFirst`, `TestDeleteRefusedByFreshRecheck`), REST 204/409 (`TestArchiveRESTAdapters`), GraphQL delete (`TestArchiveGraphQLSurface`).
- Expiry auto-archive + snapshot-only sweep + transcript survival: `assertAgentSessionArchive` expire assertions (real PG).
- Dashboard: archived row badge + per-row toggle (`session-list.test.tsx`), header archive/unarchive menu + delete-behind-confirmation (`session-detail-header.test.tsx`), composer hidden while archived (`session-chat-column.test.tsx`).
- Suites: backend `go test ./...` green incl. real-PG store+agentsessions on a fresh DB (migration 0078 applied from scratch); `make lint-backend` 0 issues; dashboard 308 files / 2124 tests + lint + typecheck green.

Deferred (noted per task out-of-scope): live-cluster E2E rides the standing operator-run gate (as m41/m43 did).
