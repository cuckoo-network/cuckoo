# 034 — Decide the disk $/GB-month rate: measured margin is 56.2%, ADR082 assumed 65–71%

Why: the authenticated Hetzner pricing read (`w2/done/030.md`, 2026-09-08) measured the real volume cost at **$0.0767/GB-month USD**, so the shipped **$0.175/GB-month** disk rate earns ~**56.2%** gross margin — below the ~65–71% range ADR082 §Pricing assumed — and the keep-or-reprice decision currently has no recorded home.

- **The decision (user's):** keep $0.175 (still 30% off Render's $0.25; margin ~56.2%) or re-price.
- Either way, record the outcome in `docs/ADR082-persistent-disks.md` §Pricing — its margin assumption is now known-wrong on the record.
- If a re-price is directed, the change is sub-hour: `lego/backend/internal/pricing/pricing.yaml` disk rate + estimate tests + the ADR082 note + ADR018's disks-row price mention ($0.175 appears in the ledger text).

← from `/pm-brainstorm for w2` 2026-09-07 #5; residual of `w2/done/030.md` ("pricing.yaml deliberately untouched — re-pricing is a user decision").

---

**Resolved 2026-09-08** — **keep $0.175** (Render $0.25 × 0.70) for consistency with the ADR030 Render×0.70 sheet. ADR082 D8 unit economics + §Verification and ADR030's disk paragraph updated to the measured $0.0767 cost / ~56.2% margin. `pricing.yaml` unchanged.
