# Cron `lastSuccessfulRunAt` — fold into m36 when picked up

Render's `cronJobDetails.lastSuccessfulRunAt` (pinned OpenAPI, 2026-07-14) is missing from bex's cron service object; derivable from `status.runs` (last run with status Succeeded). Sub-hour on its own, but it belongs inside **open `w2/m36`** (cron runs API) — whoever picks m36 up should `add-task` this there rather than doing it standalone, so the run-object capture (m36/t001) pins the timestamp format once.

Source: `/pm-brainstorm more milestones for each worker` round 7, 2026-07-14 (systematic field-diff).
