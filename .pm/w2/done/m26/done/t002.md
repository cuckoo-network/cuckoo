---
id: w1/m26/t002
title: Ship the node-image-pull CiliumNetworkPolicy through Argo CD
worker: worker1
status: done
estimate: 20m
depends_on: []
---

## Objective

Move the `CiliumNetworkPolicy allow-node-image-pulls` fix — currently applied live and unmanaged — into the GitOps-tracked `deploy/gitops/base/network-policies.yaml` so it survives the next Argo CD sync instead of getting reverted.

## Context

- Root cause: kubelet image pulls arrive from the **node** identity; a plain k8s `ipBlock` NetworkPolicy can never match node-originated traffic under Cilium. The additive policy uses `fromEntities: [host, remote-node]` in the `bex-registry` namespace.
- The policy is reportedly already added to `deploy/gitops/base/network-policies.yaml` "pending ship" per the note — confirm its current state before re-adding.

## Steps

1. Check whether `deploy/gitops/base/network-policies.yaml` already has the policy committed; if not, add it.
2. Confirm Argo CD's live state matches the committed manifest (no drift) after the next sync.
3. Remove the unmanaged live-applied copy once GitOps owns it (avoid dual ownership).

## Files

- `deploy/gitops/base/network-policies.yaml`

## Acceptance criteria

- [ ] Policy is committed and Argo CD reports it in sync (not `OutOfSync`/unmanaged).
- [ ] A forced Argo CD sync does not revert or duplicate the policy.

## Out of scope

- The node-template DNS/TLS fix (t001) — different failure mode on the same symptom.
