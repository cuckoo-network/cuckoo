---
id: w2/m84/t009
title: Simplify the code this milestone changed
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m84/t008]
---

## Objective

Run `/simplify` over the account-deletion diff without weakening lifecycle guarantees.

## Context

- The largest duplication risks are workspace/member teardown, Ory HTTP clients, store retry machinery, confirmation UI, and subject-data inventory logic.
- Deletion ordering, direct-session checks, onboarding suppression, and explicit disposition rules are behavior, not cleanup opportunities.

## Steps

1. Run `/simplify` over all changed backend/dashboard/docs files.
2. Reuse existing purger, Ory transport, typed-error, confirmation-dialog, settings-section, and worker patterns where their semantics match.
3. Remove dead helpers, duplicate state enums, orphaned locale keys, and parallel cleanup paths.
4. Re-run backend and dashboard gates relevant to the diff.

## Files

- All files changed by t001–t008.

## Acceptance criteria

- [ ] One orchestrator owns deletion order and one policy table owns data disposition.
- [ ] Existing workspace/member/Ory/UI primitives are reused where semantically valid.
- [ ] No dead code, duplicate locale keys, or bypass cleanup path remains.
- [ ] Simplification preserves all destructive and retry invariants.

## Out of scope

- Unrelated auth, workspace, or settings refactors.
