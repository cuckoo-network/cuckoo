---
id: w2/m87/t005
title: Live verification against real repos (one per runtime + an ambiguous case)
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m87/t004]
---

## Objective

Drive the wizard against a real GitHub connection on the dev-2 stack (or prod with a test workspace) and verify detection end to end: one repo per supported manifest, one nested Root Directory case, one multi-manifest precedence case, one unrecognizable repo.

## Context

- dev-2 stack: `bash scripts/dev-env.sh 2 up`; a GitHub App connection is required (the same developer-supplied credential gate w3/m79 t012 records — if unavailable locally, run against prod like w1/m86's drill).
- `examples/hello-go/` and the bex-co org's existing repos cover several runtimes; create tiny fixture repos for the rest if needed.

## Steps

1. Connect GitHub, open the New Service wizard, and walk the matrix; screenshot/record each detection outcome.
2. Verify the manual-override and silent-failure behaviors live.
3. File any divergence found as fixes in this milestone (or follow-up notes if out of scope).

## Files

- evidence notes under `.pm/w2/m87/`

## Acceptance criteria

- [ ] Recorded matrix: every supported runtime detected on a real repo, Root Directory re-infer observed, ambiguous case follows the documented precedence, unknown repo leaves manual selection untouched.

## Out of scope

- Non-GitHub sources.
