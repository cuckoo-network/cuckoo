---
id: w2/m87/t008
title: Test coverage
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m87/t006]
---

## Objective

Standing closing task: ensure the shipped behavior and its failure modes are meaningfully tested end to end — probe outcomes (nested path, 404, rate limit), every heuristic mapping + precedence, GraphQL auth/unknown, and the wizard's clobber-protection and silent degradation. Extend what t001–t004 added; close any gap between "tested in units" and "asserted as a behavior".

## Context

- Backend: `lego/backend/internal/github/*_test.go` (CI runs real Postgres + OpenFGA).
- Dashboard: feature tests beside the hook/route; never snapshot-everything.

## Steps

1. Audit coverage against the DoD clauses; add missing failure-mode tests.
2. Full gates: `cd lego/backend && go test ./...`, `dashboard yarn test`, lint.

## Files

- `lego/backend/internal/github/` tests, `dashboard/src/features/services/` tests

## Acceptance criteria

- [ ] Every DoD clause maps to at least one asserting test; all suites green.

## Out of scope

- Coverage-gaming trivial tests.
