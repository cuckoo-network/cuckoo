---
id: w2/m79/t001
title: "Diff current Render OpenAPI + public docs against ADR018 rows; list drifted/new capabilities"
worker: worker2
status: done
estimate: 60m
depends_on: []
---

## Objective

Produce a working drift list: every ADR018 row whose Render-side evidence has changed since the last audit window (passes from 2026-07-08, re-verified through 2026-07-27), plus every genuinely new Render capability absent from the ledger — without touching the doc yet.

## Context

- `docs/ADR018-render-parity.md` is a 164-row table (`| Render capability | REST | GraphQL | MCP | UI | Evidence / divergence |`, cells ✅/◐/✖/—) whose Method section records the audit dates: four API passes from 2026-07-08 re-verified through 2026-07-27, a dashboard walk 2026-07-15, and the w1/m55 canonical-surface re-verification 2026-07-27.
- Sources to diff against: Render's live OpenAPI spec (api-docs.render.com), render.com/docs changelog/feature pages, and the current `render-oss/render-mcp-server` tool list. Captured prior evidence lives under `docs/render-artifacts/`.
- `.pm/DO_NOT_DO.md` hard constraint: the `—` non-goal rows (persistent disks, one-off jobs/ephemeral shells beyond the recorded SSH/web-shell/agent-sandbox carve-outs, static CDN cache purge, log/metric drains, SSO/SAML/SCIM/maintenance runs/dedicated IPs, workflows & tasks) stay `—` even if Render extended them — note upstream changes as ledger-note candidates only, never as reopened work.

## Steps

1. Re-read ADR018's Method + row inventory; note each row's Render-side evidence pointer and date.
2. Fetch Render's current OpenAPI spec; diff operations/fields against the rows' recorded REST evidence (new endpoints, new fields on existing endpoints, changed enums/defaults).
3. Sweep render.com/docs changelog + the official MCP server repo for capabilities shipped since ~2026-07-27.
4. Write the drift list (scratch file or PR-description notes, not the ADR yet): per item — affected row (or "new row"), what changed upstream, proposed cell/evidence change, and whether it maps to a DO_NOT_DO `—` row (⇒ note-only).

## Files

- Read-only: docs/ADR018-render-parity.md, docs/render-artifacts/, .pm/DO_NOT_DO.md
- Output: drift list handed to t002 (working notes, not committed as a report doc)

## Acceptance criteria

- [ ] Every ADR018 row dispositioned: unchanged / drifted (with what changed) / superseded
- [ ] New upstream capabilities since 2026-07-27 enumerated with a proposed row or an explicit skip rationale
- [ ] Zero DO_NOT_DO `—` rows proposed for reopening

## Out of scope

- Editing ADR018 (t002); fixing any bex-side gap; CLI work (t003/t004).
