---
id: w2/m79/t008
title: "Closeout: verify DoD, mark done, move milestone to done/"
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m79/t007]
---

## Objective

Standing closing task: verify the milestone's definition of done end to end, sync status everywhere, and move the whole milestone directory to `done/` per `.pm/CLAUDE.md` conventions.

## Context

DoD checklist (from this milestone's README):

- `docs/ADR018-render-parity.md` carries a dated 2026-08 re-baseline note with every row re-verified or updated.
- `lego/cli` pins the current upstream release; `cd lego/cli && go test ./...` green.
- `docs/cli-compatibility-checklist.md` re-run against a live dev stack with the bumped CLI, evidence updated.
- Every newly found gap exists as a filed inbox note or milestone candidate — none silently fixed (beyond t007's tested trivial fixes) or dropped.
- DO_NOT_DO `—` rows untouched.
- Markdown CI: `npx prettier@3.4.2 --write "**/*.md"` over the changed docs before finishing.

## Steps

1. Walk the DoD list against the actual diffs; spot-check three ADR018 rows and three checklist rows for evidence pointers.
2. Set every task's frontmatter `status: done`; update the README `**Status:**` line and the w2 workstream README checkbox/`— **DONE**` row.
3. `mv .pm/w2/m79 .pm/w2/done/m79` — move the entire directory, leave no tombstone/stub at the old path.

## Files

- .pm/w2/m79/ → .pm/w2/done/m79/ (whole directory)
- .pm/w2/README.md — workstream status sync

## Acceptance criteria

- [ ] All DoD items verified with evidence
- [ ] Status synced in task frontmatter, milestone README, and workstream README
- [ ] Milestone directory lives only at `.pm/w2/done/m79/`

## Out of scope

- Shipping/committing (that's /ship, user-invoked); any remaining feature work.
