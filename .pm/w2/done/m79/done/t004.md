---
id: w2/m79/t004
title: "Re-run the CLI compatibility checklist against a live dev stack with the bumped CLI; update rows + evidence"
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m79/t002, w2/m79/t003]
---

## Objective

Re-grade `docs/cli-compatibility-checklist.md` against a live dev stack using the newly pinned upstream CLI, updating the checklist's stated version, the "How this pass was verified" record, and every row whose grade or evidence changed.

## Context

- The checklist's current server oracle is the unmodified **v2.21.0** client (dev-9 baseline 2026-07-18 + named supplements); the launcher section separately records the `lego/cli` v2.22.0 import. This pass promotes the t003-pinned version to the graded client and stamps a new dated verification entry.
- Row legend: `[x]` / `[~]` / `[ ]` / `[-]`. The `[-]` non-goal rows (workflows, ephemeral SSH, `ea` objects, previews, Datadog, `--runtime` switch) mirror DO_NOT_DO and stay `[-]`.
- Harness: `scripts/cli-compat.sh verify` (Hydra `client_credentials` per call, `RENDER_HOST` → local bex-api) plus the command-tree sweep with `-o json` + raw REST read-back. Environment limits of the base dev stack (no kpack/OpenBao/Loki/OpenFGA/…) are recorded as such, distinguishing bex gaps from environment limits — keep that discipline.
- t003's upstream-change list drives targeted attention: new/changed commands and flags get fresh rows or re-grades rather than inherited grades.

## Steps

1. Stand up (or reuse) an isolated dev stack per the checklist's dev-environment convention (`.pm/w2/dev-2` exists for w2).
2. Run `scripts/cli-compat.sh verify` with the bumped CLI; sweep new/changed commands from t003's list individually.
3. Update the checklist: header version + pin, a new dated "verified" entry describing this pass's scope, per-row grade/evidence updates, and new rows for new upstream commands/flags (graded honestly — `[ ]` if bex lacks them, `[-]` only with a recorded non-goal rationale).
4. Append newly found bex-side gaps to the "Real gaps found this pass" section AND to the drift list for t005 filing — none fixed inline (except trivially small wire-shape fixes explicitly deferred to t007's judgment).

## Files

- docs/cli-compatibility-checklist.md — version, verification record, rows, gaps section
- Read-only: scripts/cli-compat.sh, .pm/DO_NOT_DO.md

## Acceptance criteria

- [ ] Checklist header names the newly pinned version with a dated 2026-08 verification entry
- [ ] Every new/changed upstream command or flag from t003 has a graded row
- [ ] `[-]` rows unchanged; environment-limit grades still distinguished from real gaps
- [ ] All newly found gaps recorded (checklist gaps section + t005 hand-off), none silently fixed

## Out of scope

- Fixing discovered gaps; production-edge supplements (SSH/psql/KV verifiers) unless a grade depends on one; releasing the CLI.
