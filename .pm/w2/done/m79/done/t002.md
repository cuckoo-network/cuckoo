---
id: w2/m79/t002
title: "Update ADR018: adjust/add rows, stamp a dated re-baseline note"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m79/t001]
---

## Objective

Apply t001's drift list to `docs/ADR018-render-parity.md` so the ledger is truthful as of 2026-08, and stamp a dated re-baseline note in the Method section following the existing convention (e.g. the "Canonical-surface re-verification (w1/m55, 2026-07-27)" paragraph).

## Context

- Row format is `| Render capability | REST | GraphQL | MCP | UI | Evidence / divergence |` with ✅/◐/✖/— cells, each backed by a code pointer (bex side) or a Render spec/docs pointer (Render side). Keep that evidence discipline: no cell changes without a pointer.
- DO_NOT_DO `—` rows stay `—`; upstream extensions to those capabilities land only as updated evidence text on the existing row.
- The Method section is the audit provenance record — the new note must name this milestone (w2/m79), the date, the sources diffed (live OpenAPI, docs changelog, MCP server), and the scope (rows re-verified vs updated vs added).

## Steps

1. For each drifted row from t001: update the evidence/divergence cell (and status cells only where bex's actual state warrants it — a Render-side addition bex lacks typically moves ✅→◐ or adds a recorded divergence, per existing row style).
2. Add rows for genuinely new Render capabilities (status cells reflect bex's real state today, usually ✖ or —; a new `—` needs a deliberate-non-goal rationale and, if it's a standing anti-goal, a DO_NOT_DO cross-reference — but do not edit DO_NOT_DO in this task).
3. Add the dated re-baseline Method paragraph: "Upstream drift re-baseline (w2/m79, 2026-08-…)".
4. Gaps discovered here are recorded in the drift list for t005 to file — none fixed inline.

## Files

- docs/ADR018-render-parity.md — row updates + Method note (the only file this task edits)

## Acceptance criteria

- [ ] Dated w2/m79 re-baseline note present in the Method section
- [ ] Every t001 drift item reflected in a row edit/addition or an explicit skip note
- [ ] All `—` non-goal rows unchanged in status; evidence text updated only where upstream moved
- [ ] Markdown formatted (prettier is run at milestone shipping time, not by the scaffold)

## Out of scope

- Fixing bex-side gaps; the CLI checklist (t004); filing board items (t005).
