---
id: w2/m65/t001
title: Accept ADR054 + amend ADR035 (multi-channel, ags-* branch, SFTP carveout, stale relation)
worker: worker2
status: done
estimate: 30m
depends_on: []
---

## Objective

Land the policy half: flip ADR054 to Accepted (citing this milestone), apply its "Amendments to ADR035" section to `docs/ADR035-ssh.md`, and sync the root docs index.

## Context

- ADR054 §"Amendments to ADR035" enumerates the exact sentences: single-channel invariant (§Decision step 6, §SSH protocol), the `ags-…` resolver branch, the `sftp`-bridge exception for sandbox targets, RBAC/DB-grant enumeration, non-goals narrowing ("hosted sandboxes" → anonymous/ephemeral execution).
- ADR035 §Decision step 4 and §Audit still say `can_operate`; the code moved to `can_view_sensitive` (codex round-4 #8, commit `40b75be9`) — fix the stale prose in the same pass.
- DO_NOT_DO #22's carveout sentence was added when this milestone was materialized; verify it reads correctly against the final ADR wording.

## Steps

1. Edit `docs/ADR035-ssh.md` per the five amendment items in ADR054.
2. Set ADR054 `**Status:**` to Accepted (date + `w2/m65`).
3. Update the ADR054 line in the root `CLAUDE.md` docs index (Proposed → Accepted).
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR035-ssh.md`
- `docs/ADR054-open-in-zed.md`
- `CLAUDE.md`
- `.pm/DO_NOT_DO.md` (verify only)

## Acceptance criteria

- [ ] ADR035 contains the sandbox multi-channel exception, the `ags-…` branch, the sandbox-only `sftp` bridge, the `agent_sessions` SELECT grant, and no longer claims `can_operate` gates native SSH.
- [ ] ADR054 status is Accepted and references `w2/m65`.
- [ ] Prettier clean.

## Out of scope

- Any code change.
