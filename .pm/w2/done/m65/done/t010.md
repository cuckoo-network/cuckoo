---
id: w2/m65/t010
title: "Live acceptance: real Zed client → live sandbox, full flow evidence"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m65/t005, w2/m65/t006, w2/m65/t007, w2/m65/t009]
---

## Objective

Prove the whole chain with a real Zed client against a live agent session: hotlink → gateway auth → multi-channel → server upload → editing `/workspace` with a working terminal; record evidence in the milestone.

## Context

- Environment: dev-2 stack or the mock cluster with the gateway + a live agent session; prod only after `scripts/ssh-activate.sh --check` and `scripts/ssh-gateway-db-role.sh` re-apply (t003 grant).
- Scenarios from the milestone DoD: happy path (Zed opens, edits a file, integrated terminal runs `ls /workspace`); contributor key refused at handshake; `srv-…` second channel still rejected; teardown deferral (let the turn finish while Zed is open → sandbox survives; Cancel → connection drops); terminal-phase session shows no button.
- Zed requires the exact-version server — expect the upload path (egress closed) and note upload size/time in evidence.

## Steps

1. Stand up a live session; register a test SSH key via the existing key surface.
2. Walk each scenario; capture gateway logs, `ssh_sessions` rows, and screenshots into `evidence/`.
3. File follow-up inbox notes for anything degraded (e.g. Zed version-churn friction, channel-cap tuning).

## Files

- `.pm/w2/m65/evidence/` (new)

## Acceptance criteria

- [ ] All five scenarios recorded with evidence; any deviation filed as follow-up, not silently accepted.

## Out of scope

- Production DNS/activation changes beyond what ADR035 already gates.
