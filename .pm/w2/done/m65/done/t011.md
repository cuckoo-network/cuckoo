---
id: w2/m65/t011
title: Render parity sweep (bex extension row + surface consistency)
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m65/t010]
---

## Objective

Standing closing task: check cross-surface consistency and record the parity position. Render has no agent-session or editor-remoting surface — this is a bex extension, so the check is internal consistency plus an honest ledger entry, not a Render comparison.

## Context

- `docs/ADR018-render-parity.md` — add/extend the agent-sessions extension row noting `sshAddress` + Open in Zed (bex-only, like the usage API precedent).
- Surfaces: REST `GET /v1/agent-sessions*`, GraphQL `AgentSession.sshAddress`, MCP `get_agent_session`/`list_agent_sessions`, dashboard header — same field name, same gating semantics, same absence behavior.

## Steps

1. Verify the four surfaces agree (field, gating, omission).
2. Update ADR018's ledger with evidence pointers.
3. Prettier.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Ledger updated; any drift between surfaces filed as a task before Closeout, not shipped silently.

## Out of scope

- Building any Render-side equivalent comparison — none exists.
