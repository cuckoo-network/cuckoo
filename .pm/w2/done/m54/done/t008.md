---
id: w2/m54/t008
title: Simplify — one title/data/metadata path
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m54/t007]
---

## Objective

Run `$simplify` over m54's dashboard changes so title formatting, head metadata, dynamic route data, fallbacks, and route classification each have one clear source of truth.

## Context

- The primary risks are one loader per route that duplicates Apollo requests, title helpers that merely rename string duplication, and effects left behind after SSR migration.
- Static generic social metadata and dynamic private document titles intentionally have different data boundaries; simplification must preserve that separation.

## Steps

1. Run `$simplify` over every file changed by t001–t007.
2. Remove direct `document.title` writes, duplicate suffix/separator strings, redundant loaders, and unused compatibility branches.
3. Consolidate resource-head data access only where ownership is genuinely shared; keep resource-specific error semantics explicit.
4. Re-run focused head/route tests after each behavior-preserving change.

## Files

- `dashboard/src/common/lib/`
- `dashboard/src/routes/`
- `dashboard/src/features/*/`

## Acceptance criteria

- [ ] Title formatting, global metadata, and route classification each have one canonical implementation.
- [ ] Dynamic head resolution shares the page's data/cache rather than creating title-only duplicate requests.
- [ ] No direct `document.title` write or manually concatenated brand suffix remains.
- [ ] Simplification does not move private names into generic social metadata.

## Out of scope

- Unrelated dashboard-wide refactors.
