# Stale legacy `default/beancount-forum` App: doomed clone re-runs each operator roll; delete or repair

Why: a Failed legacy App in `default` re-ran an unbuildable clone on every operator roll, spamming the controller log and wasting build quota.

From prod k9s/log inspection, 2026-08-20. (The inbox file was never written — only the README line landed; reconstructed at closeout.)

**DONE 2026-08-23** — both halves resolved:

- The re-run mechanism was fixed by **w2/m82** (terminal Failed builds quiesce — no re-dispatch after Job TTL reap). Verified live: zero `default/beancount-forum` ERROR/clone activity in the last 24h of `bex-controller-manager` logs.
- The stale App itself: **deleted from prod** with user approval (conditional on a working non-legacy replacement — satisfied: `tea-d98210cbbpdc73dcrkvg-beancount-forum` Running, pod 1/1 for 31h, `https://beancount-forum.onbex.co` routing). `kubectl delete app beancount-forum -n default` completed; no leftover Deployment/Service/Ingress/Job in `default`.
