---
id: w2/m92/t007
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m92/t005]
---

## Objective

Add meaningful tests for anything this milestone changed in code: any `registry-migrate` or identity/inventory fix found during the rehearsal or live run gets a regression test proven against the failure it fixed (m75's existing fake-backed tool tests are the harness to extend). If the milestone shipped zero code changes, verify the existing m75 suites still pin the behaviors this run relied on (dry-run default, digest-verify-before-tombstone, no-blob-delete) and record that check.

## Context

- Existing coverage: `lego/operator/cmd/registry-migrate` + `internal/identity` tests from w2/m75 (copy+verify+tombstone against fakes; dual-read envtests).
- Never game coverage: an operational milestone does not need invented tests — it needs any *new* code path pinned and the relied-upon invariants confirmed still guarded.

## Steps

1. Diff the milestone's code changes; write regression tests for each.
2. Confirm the m75 invariants above are still test-pinned; record where.

## Files

- Tests beside whatever changed (operator `make test` scope)

## Acceptance criteria

- [ ] Every code change this milestone made is regression-tested (or no-code-change + invariant-check recorded)
- [ ] Operator `make test` + `make lint` green

## Out of scope

- New coverage for untouched m75 code

## Outcome

Regression tests: `TestSkopeoCopyArgsPassesExplicitCreds`, `TestSkopeoCopyArgsOmitsCredsWhenEmpty`. Confirmed `internal/migrate` invariants still green. `go test ./cmd/registry-migrate/... ./internal/migrate/...` + `make lint` green.
