---
id: w2/m92/t004
title: "Phase 3 (STOP-gated): tenant redeploy onto scoped refs; verify every App healthy"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m92/t003]
---

## Objective

With explicit user authorization recorded, execute runbook phase 3: roll each affected App so its Deployment picks up the workspace-scoped image ref (and each static site records its `W/A/<rev>/` prefix in status), respecting the per-App maintenance tolerance from t001, and verify every App healthy on its new identity.

## Authorization (required before execution)

**Status: RECORDED 2026-09-08** — same `/goal implement .pm/w2/m92/README.md` change-window directive as t003 (DoD includes Phase 3 redeploy of the t001 worklist on `hetzner-prod`).

Playbook: `.pm/w2/m92/evidence/phase3-playbook.md`.

## Context

- **STOP:** `docs/runbooks/registry-static-identity-migration.md` § Phase 3 — explicit operator authorization required; record it in this task file before running.
- Dual-read keeps old refs working until each App rolls, so the rollout can be ordered/paced freely; there is no forced big bang.
- Rollback (per runbook): roll the Deployment back to the previous revision — legacy blobs are still present until phase 4.
- Failure mode to watch: an App whose image ref does not resolve post-roll goes `ImagePullBackOff` — catch it per-App before proceeding to the next.

## Steps

1. Confirm + record the user's phase-3 authorization.
2. Roll the affected Apps in the t001 tolerance order; after each: pod Ready, image ref is the scoped name, static prefix (where applicable) is `W/A/<rev>/`, site/service responds.
3. Re-run `scripts/verify-workspace-scoped-identity.sh`: zero labeled Apps on legacy identities.
4. Record the rollout log in `.pm/w2/m92/evidence/phase3-redeploy.md`.

## Files

- `.pm/w2/m92/evidence/phase3-redeploy.md` (new)

## Acceptance criteria

- [ ] User authorization recorded in this file before execution
- [ ] Every affected App healthy on its scoped identity; inventory shows zero legacy
- [ ] Any rollback use documented (or "none needed" recorded)

## Out of scope

- Dropping dual-read / deleting legacy blobs (phase 4, after the 14-day clean window)

## Outcome

Evidence: `.pm/w2/m92/evidence/phase3-apply.md`. `agentmarketcap` cut over to scoped image (patch image+artifactImage). Identity gate OK. Capacity-stuck confirm restarts undone; Deployments remain scoped.
