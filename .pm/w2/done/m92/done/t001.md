---
id: w2/m92/t001
title: Phase 1 — read-only inventory of legacy-named identities across prod
worker: worker2
status: done
estimate: 40m
depends_on: []
---

## Objective

Run the runbook's read-only phase 1 against production: enumerate every labeled App still on a legacy-named Zot repo/user/pull Secret or legacy static prefix, and record the migration set (which Apps, which artifacts, estimated copy volume) so phases 2–3 have an exact worklist and a before-picture to diff against.

## Outcome

Evidence: `.pm/w2/m92/evidence/phase1-inventory.md`. Seven labeled Apps on the worklist (23 tag copies + 7 tombstones); `hello-static` unlabeled/out of scope; dual-read flag currently off (Phase-2 precondition).

## Context

- Runbook: `docs/runbooks/registry-static-identity-migration.md` § Inventory (Phase 1 — read-only; no authorization needed).
- Inventory procedure: `scripts/verify-workspace-scoped-identity.sh` (the live procedure `w2/done/m75` designated).
- Identity scheme: `lego/operator/internal/identity` (ADR074) — labeled Apps should be on `W/A` repos / `app-W-A` users / `reg-pull-W-A` Secrets / `W/A/<rev>/` prefixes; unlabeled Apps are out of scope (byte-identical by design).
- `registry-migrate` dry-run (`lego/operator/cmd/registry-migrate`, dry-run is the default) is also a legitimate inventory instrument.
- Note each affected App's maintenance tolerance (runbook "Before you start") — phase 3 needs a rollout per App.

## Steps

1. Run the inventory against prod; capture the legacy set (Apps × artifacts) in `.pm/w2/m92/evidence/phase1-inventory.md` (no credentials, no tenant content).
2. Run `registry-migrate` in dry-run mode; confirm its plan matches the inventory.
3. Record per-App maintenance tolerance for the phase-3 rollout ordering.

## Files

- `.pm/w2/m92/evidence/phase1-inventory.md` (new)

## Acceptance criteria

- [ ] Complete legacy-identity worklist recorded, cross-checked against the dry-run plan
- [ ] Zero mutations (read-only proven — no `--apply` anywhere)
- [ ] Maintenance tolerance noted per affected App

## Out of scope

- Any mutation; any unlabeled-App work

