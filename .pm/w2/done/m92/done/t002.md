---
id: w2/m92/t002
title: Rehearse phase 2 --apply on a scratch App (runbook precondition)
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m92/t001]
---

## Objective

Satisfy the runbook's precondition — "You have rehearsed Phase 2 `--apply` on a scratch App, not a tenant" — by creating a throwaway labeled App with a built image (and, if cheap, a static publish), running `registry-migrate --apply` against it alone, and proving copy + digest-verify + tombstone + dual-read serving end to end before any tenant artifact is touched.

## Outcome

Evidence: `.pm/w2/m92/evidence/phase2-rehearsal.md`. Scratch App migrated with digest match + tombstone + legacy blob retained; serve/redeploy OK; App deleted.

## Context

- Runbook: `docs/runbooks/registry-static-identity-migration.md` § Before you start + § Phase 2.
- The scratch App is first-party and disposable — the STOP does not apply to it, but scope the `--apply` invocation to it explicitly (never a bare all-Apps apply here).
- m75's tool tests prove the mechanism against fakes; this rehearsal proves it against the real prod Zot/S3.

## Steps

1. Create the scratch labeled App; deploy so a real image (and optionally a static publish) exists under legacy-shaped naming if reproducible, else use the oldest pre-ADR074 artifact shape available.
2. Run phase 2 `--apply` scoped to the scratch App; verify digests match, tombstones written, no blob deleted.
3. Verify dual-read: the App still serves/pulls correctly both before and after its redeploy (mini phase 3).
4. Tear down the scratch App; record the rehearsal in `.pm/w2/m92/evidence/`.

## Files

- `.pm/w2/m92/evidence/phase2-rehearsal.md` (new)

## Acceptance criteria

- [ ] Scoped `--apply` succeeded: copy + digest-verify + tombstone, zero blob deletes
- [ ] Dual-read serving proven pre- and post-redeploy on the scratch App
- [ ] Scratch App removed; no tenant artifact touched

## Out of scope

- Tenant Apps (t003/t004, behind the STOP)
