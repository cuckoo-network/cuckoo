# bex-api rollout scheduling noise: surge pod Pending on required anti-affinity (3 platform nodes)

From the 2026-08-20 `hetzner-prod` inspection. Deploy-per-push produced 8 bex-api ReplicaSets in ~80 minutes; each rollout's surge pod goes Pending with `FailedScheduling` (`3 node(s) didn't match pod anti-affinity rules`) until an old-RS pod terminates, because 2 replicas + required anti-affinity + stacked rollouts saturate the 3 platform nodes. `auth/kratos` shows the same. Self-heals every time, but it spams Warning events, slows each rollout, and trains people to ignore FailedScheduling.

Options (pick one, small change): `maxSurge: 0` + `maxUnavailable: 1` for bex-api (capacity-neutral rollouts), or soften to `preferredDuringSchedulingIgnoredDuringExecution`, or accept and document. Check kratos' deployment strategy at the same time. Sub-hour.

**DONE 2026-08-23** — switched bex-api (`lego/operator/config/prod/kustomization.yaml`), kratos + hydra (`deploy/gitops/overlays/prod/values/{kratos,hydra}.values.yaml`) from required to preferred hostname pod anti-affinity (weight 100). This kills the surge-pod FailedScheduling Pending on the 3-node platform pool while preserving maxUnavailable:0 (the w1/m52 never-kill-before-ready invariant) and steady-state spreading; PDB minAvailable:1 unchanged. `scripts/gitops-validate.sh` `check_request_path_ha` now accepts required OR preferred(>=1, hostname) and the full tree renders PASS. Lands in prod on the next ship + Argo sync.
