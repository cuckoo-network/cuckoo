---
id: w2/m77/t006
title: Simplify pass over whatever code/scripts this milestone touched
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m77/t005]
---

## Objective

Run /simplify over the milestone's changed files and apply the reuse/simplification/altitude cleanups it surfaces. Standing closing task.

## Context

m77 is mostly operational, so the expected diff is small: `lego/operator/config/api/deployment.yaml` env wiring, a new `scripts/agent-snapshot-secret.sh`, possibly small fixes from the live walks, and docs. The secret script should mirror the established `scripts/stripe-billing-secret.sh` / `scripts/snapshot-registry-secrets.sh` posture rather than inventing a new shape — that convergence is exactly what this pass checks.

## Steps

1. Enumerate the milestone's working-tree changes (`git status` / `git diff`).
2. Run /simplify over them; apply accepted findings.
3. Re-run any suites the touched files are covered by (`cd lego/backend && go test ./...` if backend code changed; `bash scripts/gitops-validate.sh` if manifests changed and it covers them).

## Files

- Whatever t001–t005 touched (deployment manifest, secret script, docs).

## Acceptance criteria

- /simplify run; findings applied or explicitly declined with a reason.
- Touched-area suites still green.

## Out of scope

Bug hunting (/code-review's job); refactors beyond the milestone's diff.

## Done (2026-08-19)

Reuse: live-walk `ls-remote` 403 now shares `withGitAuthRetry` with `cloneWithRetry` (same 10×3s mint-race budget) rather than a second loop. `scripts/agent-snapshot-secret.sh` already follows the stripe/static secret-script posture (`.env` source, temp env file, `optional: true` Secret, no values in argv). No further altitude edits. Driver tests 56/56.
