---
id: w2/m77/t008
title: Closeout — verify DoD, mark done, move milestone to done/
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m77/t007]
---

## Objective

The milestone's Definition of done is verified point by point and the board reflects completion. Standing closing task.

## Context

Per `.pm/CLAUDE.md`: a done milestone moves **whole** to `wN/done/mN/` (`mv` the directory then `rmdir` nothing extra — the original path must simply not exist; never a tombstone). Status syncs in three places: each task's frontmatter, this README's `**Status:**` + `— **DONE**` rows, and the w2 workstream README checkbox. The evidence directory travels with the move (ends at `.pm/w2/done/m77/evidence/`).

## Steps

1. Walk the DoD: prod session hibernated (phase, snapshot object under workspace prefix in the SSE bucket, pod reclaimed); rehydrated with restored `/workspace`; latency recorded vs p50<~5s / p95<~15s; evidence filed; rollback documented.
2. Set every task's frontmatter `status: done`; add `— **DONE**` to the README task rows and flip `**Status:**` to DONE with date.
3. Update the w2 workstream README checkbox for m77.
4. `mv .pm/w2/m77 .pm/w2/done/m77` — leave nothing at the old path.
5. Run `npx prettier@3.4.2 --write` over the touched markdown (repo CI rule) as part of shipping.

## Files

- `.pm/w2/m77/**` → `.pm/w2/done/m77/**`
- `.pm/w2/README.md` (checkbox)

## Acceptance criteria

- Every DoD clause verified with a pointer to its evidence.
- All three status locations in sync; old path `.pm/w2/m77/` no longer exists.
- No tombstone/stub left behind.

## Out of scope

Shipping/committing (only via /ship per repo rules); starting follow-up milestones (file inbox notes instead if needed).
