---
id: w2/m28/t003
title: "Wire members guards to return ErrPlanLimit"
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m28/t002]
---

## Objective

Wire both `members` service guards — the seat-cap refusal and the role-gate refusal — to return `core.ErrPlanLimit` (with real plan/limit values) instead of a plain error string.

## Context

- `lego/backend/internal/members/service.go` — the two plan-limit guards (invite seat cap, invited-role-not-on-plan).
- `.pm/w6/done/014.md` — confirms these are exactly the two refusals `TestInvitePlanRefusalsNameThePlan` currently pins by string.

## Steps

1. Replace both guards' plain error returns with `core.ErrPlanLimit`, populated with the real plan and limit values.

## Files

- `lego/backend/internal/members/service.go`

## Acceptance criteria

- [ ] Both guards return `core.ErrPlanLimit` with correct params.

## Out of scope

- REST/GraphQL surfacing — t004.
