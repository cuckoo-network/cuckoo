---
id: w2/m28/t006
title: "Regression tests: code+params shape, not string matching"
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m28/t005]
---

## Objective

Replace/extend the existing string-pinning test with assertions on the real code+params shape, and add a dashboard test proving the CTA renders off the code.

## Context

- `TestInvitePlanRefusalsNameThePlan` (`lego/backend/internal/members/`) — currently pins the exact English substring; should assert the structured code+params instead (or in addition, if the message text itself still matters for some other reason).

## Steps

1. Update/extend `TestInvitePlanRefusalsNameThePlan` to assert `code == "PLAN_LIMIT"` and correct params, not just substring presence.
2. Add a dashboard test: given a `PLAN_LIMIT`-coded error, the CTA renders; given a differently-worded but same-coded error, the CTA still renders (proving decoupling from prose).

## Files

- `lego/backend/internal/members/service_test.go` (or wherever the existing test lives)
- `dashboard/src/features/team/hooks/use-invite-member.test.ts` (or equivalent)

## Acceptance criteria

- [ ] Backend test asserts the structured shape.
- [ ] Dashboard test proves the CTA is decoupled from the exact error string.
