---
id: w2/m28/t009
title: "Test coverage: final gap sweep"
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m28/t007]
---

## Objective

t006 already added the primary regression tests (structured-shape assertion + CTA decoupling). This is a final sweep for any gap: confirm the general `CodedError` envelope itself (t001/t004) has a direct unit test independent of the plan-limit case, so future consumers can rely on it.

## Acceptance criteria

- [ ] `CodedError`'s REST and GraphQL surfacing has a direct test, not only exercised indirectly via the plan-limit case.
