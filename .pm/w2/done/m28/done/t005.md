---
id: w2/m28/t005
title: "Dashboard: key the invite-plan CTA on the code, not a substring"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m28/t003, w2/m28/t004]
---

## Objective

Replace `use-invite-member.ts`'s `.includes("plan")` substring heuristic with a check on the `PLAN_LIMIT` code, and render localized copy from the error's params instead of the raw backend string.

## Context

- `dashboard/src/features/team/hooks/use-invite-member.ts` — the current substring-matching hook.
- `w6/m15`'s CTA (link/button to the plan-change section) — this task doesn't change the CTA's destination, only how it decides to show.

## Steps

1. Parse the GraphQL error's `extensions.code` (or REST error body's `code`) in the hook.
2. Branch on `code === "PLAN_LIMIT"` instead of the string match.
3. Render localized copy (en/zh) using the error's `plan`/`limit` params instead of the raw backend message.

## Files

- `dashboard/src/features/team/hooks/use-invite-member.ts`
- i18n copy files (en/zh) for the new localized plan-limit message

## Acceptance criteria

- [ ] The CTA renders based on the code, not a string match.
- [ ] The message is localized, not raw backend English, for a `zh` user.

## Out of scope

- Any other invite-dialog error states.
