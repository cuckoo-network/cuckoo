---
id: w2/m38/t003
title: Persist build-phase progress and failure
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m38/t002]
---

## Objective

Project observable BuildKit/kpack work onto created, queued, build_in_progress, and build_failed without mistaking rollout failure for build failure.

## Context

- `lego/backend/internal/store/reconciler.go` currently closes a coarse open row.
- The operator stays DB-free; status projection remains backend-owned.

## Steps

1. Observe build lifecycle through the backend's existing Kubernetes seams.
2. Persist build transitions through the transition model from t002.

## Files

- `lego/backend/internal/store/reconciler.go`
- `lego/backend/internal/store/store.go`
- `lego/backend/internal/deploys/`

## Acceptance criteria

- [x] Tests distinguish queued/running/succeeded/failed build work and preserve newest-wins/cancel behavior.

## Out of scope

- Pre-deploy and rollout transitions (t004).
