---
id: w2/m35/t007
title: Export-artifact retention
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m35/t003]
---

## Objective

Exports don't accumulate forever: expire artifacts (and mark records `expired`) per a recorded rule — match Render's captured retention if t001 found one, else pick and document one (the usage/audit retention-sweep precedent).

## Files

- `lego/backend/internal/postgres/` (+ the daily-sweep pattern from usage/audit)

## Acceptance criteria

- [ ] An artifact past the window is deleted from the store and its record marked, asserted by test with a shortened window.
