---
id: w2/m7/t008
title: Simplify — `/simplify` over the m7 diff
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m7/t007]
---

## Objective

Run `/simplify` over the milestone's diff (`lego/backend/internal/keyvalue/`, `internal/id`, the operator suspend change, server registrations) and apply reuse/simplification/efficiency findings. Behavior-preserving only. Watch especially for copy-paste from `internal/postgres/` that should be shared helpers instead of parallel code.

## Context

- The postgres package was the template — genuine sharing candidates (list envelopes, error mapping, Secret reads) may have emerged in duplicate.

## Steps

1. `/simplify` scoped to the m7 diff; apply or consciously decline findings.
2. Re-run backend + operator tests.

## Files

- Whatever the m7 diff touched.

## Acceptance criteria

- [ ] Findings applied or declined explicitly; tests still green.

## Out of scope

- Bug hunting; new tests (t009).
