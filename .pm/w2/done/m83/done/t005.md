---
id: w2/m83/t005
title: Simplify the code this milestone changed
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m83/t004]
---

## Objective

Run `/simplify` over this milestone's diff — behavior-preserving.

## Context

- Removing a control and a list region should delete code, not just hide it: check for now-unused props, handlers, translations and list components.
- `new-session-composer.tsx` (~388 lines) and `session-list.tsx` (~333) are the main files; both should shrink.

## Steps

1. Run `/simplify` over the changed files.
2. Delete now-dead components, props, handlers and locale strings in **both** `en.ts` and `zh.ts`.
3. Re-run the dashboard suite.

## Files

- The files touched by t001–t003.

## Acceptance criteria

- [ ] No dead code or orphaned translation keys left behind.
- [ ] `yarn typecheck && yarn lint && yarn test` pass.
- [ ] No behavior change.

## Out of scope

- Refactoring agent-session components this milestone did not touch.
