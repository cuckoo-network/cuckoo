---
id: w2/m83/t006
title: Test coverage for the consolidated controls
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m83/t004]
---

## Objective

Lock the consolidation so the removed affordances cannot quietly return, and so the capabilities they carried do not regress.

## Context

- The failure mode is a _re-addition_: a later change puts a second repo button back. So assert the count of repo-attach controls, not merely that one exists.
- The capability risk is `w4/m36`'s session-attach and the archived/all filters — both must be asserted directly.
- `agents-search.test.ts` already covers `validateSearch`; extend rather than duplicate it.

## Steps

1. Assert exactly one repo-attach control renders.
2. Assert repo attach and session attach both still work.
3. Assert one Recents surface, and that archived/all remain reachable.
4. Extend the existing search test for the URL forms t002 must preserve.

## Files

- `dashboard/src/features/agent-sessions/components/__tests__/`
- `dashboard/src/routes/__tests__/agents-search.test.ts`

## Acceptance criteria

- [ ] A test fails if a second repo-attach control is added.
- [ ] A test fails if a second Recents list is added.
- [ ] Session attach and archived/all filtering are asserted.
- [ ] `yarn test` passes.

## Out of scope

- Visual regression testing.
