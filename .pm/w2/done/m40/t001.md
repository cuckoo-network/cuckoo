---
id: w2/m40/t001
title: "Thread environments[].envVarGroups through Blueprint ingest"
worker: worker2
status: todo
estimate: 45m
depends_on: []
---

## Objective

In the Blueprint/bex.yml ingestion path, accept `projects[].environments[].envVarGroups`: create (or match) the env group and record its Environment membership via the w6/m24 verbs, inside the existing all-or-nothing preflight (nothing is created if any part of the stack fails validation).

## Context

- `.pm/w2/done/010.md` (the note); the ingest path from `w1/m24` (multi-service bex.yml) + `w1/m35` (envVarGroups/fromGroup fields) — grep `envVarGroups` in `lego/backend/internal/` for the named unsupported error branch.
- Membership mechanism: `internal/envgroups` + `internal/environments` (`envGroupIds`, w6/m24 full-replace verbs — use the service-layer verbs, not raw store writes).

## Files

- `lego/backend/internal/` (blueprint/bex.yml ingest + envgroups linkage)

## Acceptance criteria

- [ ] Environment-scoped groups create with membership; root + ungrouped behavior byte-identical; preflight still all-or-nothing.

## Out of scope

- validate/sync verb acceptance (t002).
