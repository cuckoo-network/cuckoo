---
id: w2/m40/t002
title: "validate/sync verbs accept the nesting; delete the unsupported error"
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m40/t001]
---

## Objective

`w2/m15`'s `/blueprints` validate · sync verbs accept the environment-scoped nesting (validate reports it as applying cleanly, sync applies it), and the named unsupported error branch is deleted — grep-proof that no path still refuses the official nesting.

## Files

- `lego/backend/internal/` (blueprints feature)

## Acceptance criteria

- [ ] `validate_bex_yml` on the t003 fixture returns no unsupported findings; the error string no longer exists in the tree.
