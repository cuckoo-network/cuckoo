---
id: w2/m56/t005
title: Render parity — revalidate manual scale on every surface
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m56/t003, w2/m56/t004]
---

## Objective

Revalidate Render's manual-scale behavior and prove that bex's REST, GraphQL, MCP, and dashboard surfaces share the corrected asynchronous semantics.

## Context

- `docs/ADR018-render-parity.md` marks manual scale complete across all surfaces, but the source-backed production path failed below the API layer.
- The public request shapes need not change; the parity defect is the deploy side effect and premature completion claim.
- The incident service uses the free plan. Any Render plan-policy difference must be evidenced and filed explicitly rather than silently folded into this mechanism fix.

## Steps

1. Capture current official Render behavior for an accepted scale request, convergence reporting, build/deploy history, and applicable plan restrictions.
2. Exercise the corrected bex behavior through REST, GraphQL, MCP, and the dashboard against one source-backed service.
3. Confirm all adapters produce the same desired replica state and none requires a valid clone credential or creates a deploy record.
4. Update the parity ledger and a sanitized evidence artifact; file any policy/UX divergence that is real but outside this milestone.

## Files

- `docs/ADR018-render-parity.md`
- `docs/ADR006-bex-api.md`
- `docs/render-artifacts/`
- `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] Render's asynchronous scale semantics and plan constraints are backed by current primary evidence.
- [ ] REST, GraphQL, MCP, and dashboard acceptance all converge to the same replica target without a build or deploy-history entry.
- [ ] The parity ledger no longer treats API acceptance alone as proof of operational success.
- [ ] Every discovered divergence is either closed with evidence or filed with an owner and scope.

## Out of scope

- Autoscaling UX, billing implementation, or unrelated lifecycle parity.
