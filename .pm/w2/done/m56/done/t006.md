---
id: w2/m56/t006
title: Simplify — one deploy-decision path
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m56/t005]
---

## Objective

Run `$simplify` over m56's implementation so artifact identity, release decisions, backfill, and operational reconciliation have one legible source of truth.

## Context

- The highest regression risk is parallel condition trees that disagree about whether to build, run pre-deploy, advance a revision, or reuse the active image.
- Backward compatibility is required, but permanent legacy branches should not obscure the steady-state controller.

## Steps

1. Run `$simplify` over every code file changed by t001–t005.
2. Consolidate deploy classification and identity comparison without erasing explicit workload-type differences.
3. Remove redundant generation checks, stale comments, duplicate hashing, and compatibility branches that can terminate after backfill.
4. Re-run focused tests after each behavior-preserving improvement.

## Files

- `lego/types/v1alpha1/`
- `lego/operator/internal/controller/`
- `lego/backend/internal/deploys/`
- `dashboard/src/features/services/`

## Acceptance criteria

- [ ] One named decision path determines build, pre-deploy, revision advancement, and active-image reuse.
- [ ] Kubernetes generation remains a reconciliation acknowledgement, not a hidden second release-identity implementation.
- [ ] Backfill is isolated from the normal steady-state path and remains testable.
- [ ] Simplification preserves all m56 acceptance behavior.

## Out of scope

- Unrelated operator, backend, or dashboard refactors.
