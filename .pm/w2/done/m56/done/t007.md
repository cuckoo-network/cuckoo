---
id: w2/m56/t007
title: Test coverage — stale credential, legacy status, and true deploys
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m56/t005]
---

## Objective

Add regression coverage that reproduces the production failure conditions and distinguishes operational reconciliation from genuine deploys.

## Context

- w2/m12 verified the scale API but did not cover a source-backed App with an existing release and unusable clone credential.
- Unit coverage must inspect absence of Jobs, stable workload templates, status recovery, and deploy history—not merely the final `spec.replicas` value.
- A live cluster is still needed to prove two ready pods and retention of the original serving pod.

## Steps

1. Add exhaustive field-classification tests and controller tests for a completed generation-1 source build followed by replicas 1→2 with an unusable clone Secret.
2. Assert no generation-2 build/pre-deploy Job, unchanged image/revision/template hash, updated desired replicas, and no deploy-history insertion.
3. Add the legacy failed-status/backfill case from production and a control case proving a real source/build change still builds, pre-deploys once, and advances revision.
4. Cover the dashboard's accepted/pending/error copy and adapter consistency where existing backend tests do not already provide it.
5. Run focused suites, full affected Go suites, dashboard checks, and one live CAPD acceptance; record sanitized commands and outcomes.

## Files

- `lego/operator/internal/controller/*_test.go`
- `lego/backend/internal/apps/*_test.go`
- `lego/backend/internal/deploys/*_test.go`
- `dashboard/src/**/*.test.tsx`
- `.pm/w2/m56/evidence/`

## Acceptance criteria

- [ ] An automated test fails under the old generation gate and passes only when scale reuses the active artifact.
- [ ] Tests prove the absence of build, pre-deploy, rollout, and deploy-history side effects—not only the replica mutation.
- [ ] Legacy backfill and genuine deploy success/failure paths are covered.
- [ ] `make test`, backend tests, and dashboard typecheck/lint/test/build pass for the final change.
- [ ] Live evidence shows two ready pods, the original pod retained, and no build Job despite an unusable clone credential.

## Out of scope

- Tautological snapshots, unrelated end-to-end expansion, or printing Secret/kubeconfig contents in evidence.
