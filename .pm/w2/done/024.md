# Postgres REST `connectionPool` enum vs bex `pooler` bool

From **w2/m79**. Render OpenAPI create/PATCH/read expose `connectionPool: pgbouncer | none` (changelog 2026-07-01). bex already provisions PgBouncer via CRD/`CreateRequest` `pooler` bool + Blueprint `connectionPool`, and returns pooled connection strings when enabled (ADR018 Access-control/pooler row stays ✅ on mechanism). Gaps: REST responses omit the `connectionPool` string (conformance allowlist), and create/PATCH do not accept Render's enum. Align wire shape without changing the CNPG Pooler mechanism.

**DONE 2026-08-23** — wire shape aligned without touching the CNPG Pooler mechanism. `PostgresView.connectionPool` (`"pgbouncer"|"none"`, required like Render) populated from the pooler bool on REST read/list/create/PATCH; create + PATCH accept Render's enum as an alias folded through one `resolvePooler` (`lego/backend/internal/postgres/service.go`). Conflict rule: identical dual values pass, contradictory → named 400 `POSTGRES_CONNECTION_POOL_CONFLICT`, unknown enum → `POSTGRES_CONNECTION_POOL_UNKNOWN` (matches the `secrets` contradiction idiom). PATCH gained `Pooler *bool` (operator already reconciles flips). GraphQL read field added beside `poolerEnabled`; GraphQL/MCP writes deliberately untouched (no Render contract). Conformance allowlist's two stale `connectionPool is missing` entries removed. New `connection_pool_rest_test.go` + `internal/postgres` + `internal/api` suites green; ADR018 pooler row updated.
