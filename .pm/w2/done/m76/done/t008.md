---
id: w2/m76/t008
title: "Simplify: run /simplify over the changed code"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m76/t007]
---

## Objective

Run the repository's `/simplify` skill over everything this milestone touched — the webpush transport, worker fan-out, subscription store, REST/GraphQL additions, and the dashboard hook/panel/SW — hunting reuse, altitude, and duplication cleanups (quality only; bug-hunting is /code-review's job).

## Context

- Likely simplification pressure points found while scoping: the webpush transport vs. `lego/backend/internal/notifications/push/expo.go` (shared envelope validation/caps in `push/transport.go` should not be duplicated); `classifyPushError` in `push_worker.go` growing a second provider's vocabulary (consider a per-transport classifier behind one contract); `lego/backend/internal/store/webpush_subscriptions.go` vs. `device_subscriptions.go` (near-twin CRUD — extract shared helpers only if it genuinely reduces code); dashboard `use-web-push-subscription.ts` vs. existing `use-update-push-notification-settings.ts` patterns.
- Standing closing task per the milestone template; applies to the diff, not the whole tree.

## Steps

1. Collect the milestone's changed-file set (`git status` / diff against the pre-milestone base).
2. Invoke `/simplify` over that set.
3. Apply accepted cleanups; re-run the touched test suites (`cd lego/backend && go test ./...`; dashboard `yarn test`).
4. Note in this task any suggested-but-rejected simplifications and why.

## Files

- Whatever t001–t006 touched under `lego/backend/internal/notifications/`, `lego/backend/internal/store/`, `lego/backend/cmd/api/`, `dashboard/src/features/notifications/`, `dashboard/public/`, `scripts/`, `docs/`

## Acceptance criteria

- [x] /simplify ran over the full milestone diff.
- [x] Accepted cleanups applied with green backend + dashboard tests.
- [x] Rejected suggestions recorded with rationale.

## Simplify notes

Accepted: reuse Expo's `parseRetryAfter` for webpush HTTP 429; configurable `fakePushSender.Supports` for the four-cell matrix; Prometheus label-order assertion.

Rejected: merging `webpush_subscriptions` into `device_push_subscriptions` (Expo CHECKs on provider/platform must stay); a shared CRUD helper with device subscriptions (the sibling table's keys/caps/digests differ enough that extraction would hide the isolation); a third-party webpush SDK.

## Out of scope

Behavior changes, new features, refactors outside the milestone diff.
