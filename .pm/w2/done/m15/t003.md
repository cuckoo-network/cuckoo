---
id: w2/m15/t003
title: "Implement list/sync over the control-plane store"
worker: worker2
status: todo
estimate: 1h
depends_on: [w2/m15/t001]
---

## Objective

Implement `GET /v1/blueprints` (list known blueprint sources) and `POST /v1/blueprints/{id}/sync` (re-apply), backed by the control-plane store.

## Context

- A blueprint source is a repo+path+branch tuple already tracked via git-connect (`w2/m8`/`m9`) — no new tracking mechanism needed, just a read/list view over existing data plus the sync trigger.

## Steps

1. Implement `list`: query the control-plane store for known git-connected sources that carry a `bex.yml`.
2. Implement `sync`: re-trigger `w1/m24`'s idempotent apply for the identified source.

## Files

- wherever the new `blueprints` package/handlers live

## Acceptance criteria

- [ ] `list` returns known sources; `sync` re-applies idempotently (a second sync with no changes is a no-op).

## Out of scope

- validate — t002.
