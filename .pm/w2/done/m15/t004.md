---
id: w2/m15/t004
title: "GraphQL + MCP mirrors of validate/list/sync"
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m15/t002, w2/m15/t003]
---

## Objective

Mirror the three REST verbs on GraphQL and MCP — the MCP `validate_bex_yml` tool is the concrete agent-safety payoff: a standalone, side-effect-free dry-run an agent can call before `deploy`.

## Context

- Existing MCP tool precedent: thin adapters over the same Core verbs (`docs/ADR006-bex-api.md`).
- `lego/backend/internal/deploys/mcp.go` or a new `blueprints/mcp.go` — follow the existing package-per-feature convention.

## Steps

1. GraphQL: `blueprints`/`blueprint` queries + `validateBlueprint`/`syncBlueprint` mutations.
2. MCP: `list_blueprints`, `validate_bex_yml` (or `validate_blueprint`), `sync_blueprint` tools, thin adapters over the same verbs t002/t003 implemented.

## Files

- `lego/backend/internal/blueprints/graphql.go`
- `lego/backend/internal/blueprints/mcp.go`

## Acceptance criteria

- [ ] All three verbs work identically on GraphQL and MCP as they do on REST.
- [ ] The MCP `validate` tool has zero side effects when called standalone.
