---
id: w2/m11/t005
title: "Live acceptance: create a private-repo service via each of REST/GraphQL/MCP on the mock cluster; confirm build succeeds and deploy history populates"
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m11/t002, w2/m11/t003, w2/m11/t004]
---

## Objective

Prove the fix end-to-end against a real GitHub App connection on the mock cluster, matching the conditions that surfaced the original bug (`w2/m8`+`m9`'s live acceptance).

## Context

- `scripts/mock-cluster.sh` — local mock cluster.
- The original failure was observed live: `bld-tea-…-hello-live-sm-gen-1` Failed with `could not read Username for 'https://github.com'`.

## Steps

1. Create a private-repo service via REST (`POST /v1/services`), confirm the build succeeds (clone secret present) and the deploy appears in `GET .../deploys`.
2. Repeat via GraphQL `createService`.
3. Repeat via MCP `create_web_service`/`deploy`.

## Files

- none (verification task)

## Acceptance criteria

- [ ] All three surfaces produce a successfully-built private-repo service with populated deploy history.

## Out of scope

- Public-repo/image-based creates — those already worked; this only re-verifies the private-repo + history fix.
