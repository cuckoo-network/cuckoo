---
id: w2/m82/t001
title: Snapshot-pull protected label on create + non-fatal backfill + ClusterRole patch
worker: worker2
status: done
estimate: 35m
depends_on: []
---

## Objective

Make `SandboxNamespaceRegistryReconciler` / `EnsureSnapshotCreds` succeed without Forbidden ERROR while stamping the protected-from-tenant-mount label on `bex-snapshot-pull`.

## Context

- Live ERROR: `backfill protected label on pull secret: secrets "bex-snapshot-pull" is forbidden … cannot patch` from `sandboxnamespaceregistry`.
- Root cause: create path only labeled `app.bex.co/component=snapshot-pull`; `ensurePullSecret` then patched the protected label; ClusterRole `bex-operator-snapshot-pull` was `get`+`create` only.
- Comment on the backfill already said "Failure is not fatal" but the code returned the error.
- Prod stopgap already applied 2026-08-20: ClusterRole gained `patch`; existing Secrets labeled. Gitops must match or Argo will revert.
- Draft already in tree: `lego/operator/internal/registry/snapshot_creds.go`, `creds.go`, `deploy/gitops/base/tenant-namespace-clusterroles.yaml`, `lego/backend/internal/store/namespaces.go` comment.

## Steps

1. Stamp `app.bex.co/protected-from-tenant-mount=true` in `EnsureSnapshotCreds` create labels (mirror per-App `reg-pull-*`).
2. On backfill patch failure: log + continue (do not fail Ensure).
3. Add `patch` to `bex-operator-snapshot-pull` in gitops; update the get+create comments.
4. Keep rotation/delete out of scope (still get+create+patch only).

## Files

- `lego/operator/internal/registry/snapshot_creds.go`
- `lego/operator/internal/registry/creds.go`
- `deploy/gitops/base/tenant-namespace-clusterroles.yaml`
- `lego/backend/internal/store/namespaces.go` (comment only)

## Acceptance criteria

- [x] New snapshot-pull Secrets created with the protected label
- [x] Forbidden/backfill failure does not fail `EnsureSnapshotCreds`
- [x] Gitops ClusterRole verbs are `get`, `create`, `patch`
- [x] Annotating a sandbox namespace no longer produces `sandboxnamespaceregistry` Reconciler ERROR for this reason

## Out of scope

- Credential rotation / delete verbs
- Tenant App build failures themselves
