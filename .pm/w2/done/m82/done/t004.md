---
id: w2/m82/t004
title: Test coverage (standing)
worker: worker2
status: done
estimate: 35m
depends_on: [w2/m82/t001, w2/m82/t002]
---

## Objective

Add meaningful tests for both failure modes this milestone closes.

## Context

- `TestEnsureSnapshotCredsEndToEnd` already asserts (or should assert) the protected label on create.
- Need coverage that backfill Forbidden does **not** fail Ensure (fake client denying Patch).
- Need coverage that a second Failed observation after Ready is recorded returns nil / no error (unit or envtest on App reconciler).

## Steps

1. Assert protected label on create in snapshot creds tests.
2. Assert backfill patch failure is non-fatal.
3. Assert `buildOutcomeAlreadyRecorded` path does not return an error / does not re-call fail semantics.
4. `cd lego/operator && go test` for touched packages.

## Acceptance criteria

- [x] Tests fail if either bug regresses
- [x] Touched package tests green
