---
id: w2/m75/t007
title: "Prod migration runbook with explicitly authorization-gated cutover + rollback"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m75/t006]
---

## Objective

Write the production cutover runbook executing ADR055's coordinated rollout — (1) new scheme + dual-read (ships with this milestone's operator), (2) migration job over existing repos/prefixes, (3) tenant redeploys, (4) drop the fallback + delete legacy artifacts — with the live-tenant-data steps behind an EXPLICIT "do not proceed without operator authorization" gate, and a rollback section for each phase. The runbook is a document; nothing in this task touches the prod cluster.

## Context

- The four-phase rollout is already scoped in docs/ADR055-security-review-round4.md:44 (F2/F3 "migration-gated" disposition) — the runbook operationalizes it.
- Outage modes to guard: re-keying the registry repo invalidates image refs baked into running Deployments (ImagePullBackOff until rebuild+re-push); re-keying S3 prefixes orphans published static sites until redeploy (both named in ADR055:44). Dual-read (t002/t003) + tombstone-not-delete (t004) are the mitigations; legacy deletion happens only in phase 4.
- Prod access facts: kubectl context `hetzner-prod` (memory note "Prod cluster access"); name-keyed teardown hazard — duplicate/legacy App CRs must be handled finalizer-aware with the operator paused (same memory note). Registry credential custody: `scripts/registry-secrets.sh` + `.env` (docs/ADR019-infra-credentials.md).
- Runbook precedent/format: docs/ADR036-ca-rotation-runbook.md, docs/ADR037-openbao-rekey-runbook.md, docs/runbooks/stripe-billing-setup.md.
- ADR067:33 — store-managed CR names are already `<ws>-<name>`-prefixed; the prod legacy inventory is the never-renamed/hand-applied CR set (also flagged as an open question in docs/ADR069-security-review-round14.md:118) — the runbook's phase 2 starts with that inventory (t004 tool's enumerate/dry-run).

## Steps

1. Draft the runbook (suggest `docs/runbooks/registry-static-identity-migration.md`) with the four phases, per-phase preconditions, exact commands (t004 tool invocations, `kubectl` verification), verification checkpoints, and rollback.
2. Mark phase 2 onward with an explicit authorization gate: "STOP — requires explicit operator (user) authorization before touching live tenant data".
3. Include the legacy-App inventory procedure and the phase-4 fallback-drop criteria (zero dual-read hits over a stated window).
4. `npx prettier@3.4.2 --write` on the new doc only.

## Files

- docs/runbooks/registry-static-identity-migration.md (new)

## Acceptance criteria

- [x] Four phases with commands, verification, and rollback each
- [x] Live tenant-data steps sit behind an explicit authorization-gated STOP
- [x] Legacy-CR inventory procedure included; phase-4 drop criteria stated

## Out of scope

- Executing any phase against prod — no live prod tenant-data migration without explicit operator authorization; ADR-register annotation (t008); w7/m85 digest-pinning and w7/m86 cache implementation stay in w7.

## Completion evidence (2026-08-18)

`docs/runbooks/registry-static-identity-migration.md`: four ADR055 phases, inventory, rollback, phase-4 drop after 14-day zero dual-read hits. Explicit **STOP — requires explicit operator authorization** before phases 2–4. No prod tenant data was touched.
