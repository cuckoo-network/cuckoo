---
id: w2/m75/t009
title: "Simplify: run /simplify over the code this milestone changed"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m75/t008]
---

## Objective

Standing closing task: run the repository's `/simplify` skill over the code m75 changed — reuse, simplification, efficiency, and altitude cleanups only (no bug hunting; that is /code-review's job) — and apply the resulting fixes so the identity-scheme plumbing lands at its simplest defensible shape.

## Context

- Expected changed surface: `lego/operator/internal/registry/` (creds.go, zotconfig.go, snapshot_creds.go), `lego/operator/internal/build/build.go` + kpack.go, `lego/operator/internal/publish/publish.go`, `lego/operator/internal/staticserver/staticserver.go`, `lego/operator/internal/controller/app_controller.go` + build_registry_secret.go, `lego/operator/cmd/registry-migrate/` (new in t004), plus their tests.
- Likely candidates: the scheme helper introduced in t002 should be the ONE derivation every call site (build, creds, ACL, retention, purge, publish, serve, migrate) consumes — hunt for stragglers still concatenating `app.Name`; dual-read branching should live at one seam per sink, not be re-implemented per call site.
- Gates after edits: `make test` + `make lint` from `lego/operator/` (lint covers all four modules).

## Steps

1. Invoke `/simplify` scoped to the m75 diff.
2. Apply accepted cleanups; keep behavior identical (tests unmodified except mechanical renames).
3. Re-run `make test` and `make lint`.

## Files

- Whatever m75 changed under lego/operator/ (see Context)

## Acceptance criteria

- [x] /simplify run over the milestone's diff and accepted fixes applied
- [x] No call site derives a registry/prefix identity outside the shared scheme helper
- [x] `make test` and `make lint` green after cleanups

## Out of scope

- Behavior changes or new features; bug hunting (/code-review's job); w7/m85 digest-pinning and w7/m86 cache implementation stay in w7; no live prod tenant-data migration without explicit operator authorization (t007's gate).

## Completion evidence (2026-08-18)

Simplify pass: `identity` package is the only repo/user/Secret/prefix concatenator; call sites use `appIdentity` / `identity.ForApp`. Renamed locals that shadowed the import (`owned`). Deleted unused `readPassword`/`probe` wrappers. Collapsed `buildJobPullSecret` branches. Serve fallback uses `LegacyStaticPrefix`. `make test` + `make lint` green after cleanups.
