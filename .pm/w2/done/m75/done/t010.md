---
id: w2/m75/t010
title: "Test coverage: meaningful tests for scheme minting, dual-read, migration verification, collision prevention"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m75/t008]
---

## Objective

Standing closing task: audit the coverage t002–t005 landed and fill the gaps with meaningful tests — the four behaviors the DoD hangs on are scheme minting, dual-read fallback, migration verification, and cross-workspace collision prevention. Prefer table-driven unit tests plus one envtest exercising the reconcile path.

## Context

- Existing registry test surface to extend, not duplicate: `lego/operator/internal/registry/creds_test.go`, `snapshot_creds_test.go`, `verify_test.go`; controller-side `registry_auth_test.go`, `registry_cleanup_test.go`, `per_app_registry_envtest_test.go`, `predeploy_pull_secret_test.go`, `publish_pull_secret_test.go`, `m71_build_secret_ownership_test.go`, `app_deletion_test.go`.
- Static side: `lego/operator/internal/publish/publish_test.go`, `lego/operator/internal/staticserver/staticserver_test.go`, `lego/operator/internal/controller/app_controller_staticsite_test.go`.
- Migration tool: t004's verification helpers (digest mismatch → abort, idempotent resume) need fakes-based tests under `lego/operator/internal/registry/` and `internal/publish/`.
- Collision-prevention cases that MUST exist: same App name in two workspaces → disjoint repo/user/`reg-pull-*`/prefix; a new-scheme App's finalizer purge cannot touch a legacy same-named sibling's repo or prefix (the ADR055 F1-class cross-tenant effect this milestone exists to kill); `checkOwnedArtifact` (`lego/operator/internal/controller/build_registry_secret.go:129`) refuses cross-App overwrites of new-scheme names.
- Gates: `make test` from `lego/operator/` (unit + envtest; codegen/fmt/vet first).

## Steps

1. Diff-driven gap audit: list each new/changed exported behavior vs the tests t002–t005 added.
2. Add the missing table-driven unit tests (scheme formats incl. length/charset edges, unlabeled-App legacy byte-identity, dual-read precedence).
3. Add/extend one envtest: labeled App reconcile mints new-scheme artifacts end to end; deletion purges only its own.
4. Add migration-verification failure/resume tests against fakes.
5. `make test` green; note coverage summary in this file.

## Files

- lego/operator/internal/registry/*_test.go, lego/operator/internal/publish/publish_test.go, lego/operator/internal/staticserver/staticserver_test.go
- lego/operator/internal/controller/per_app_registry_envtest_test.go (or a new m75 envtest file), lego/operator/internal/controller/registry_cleanup_test.go

## Acceptance criteria

- [x] All four DoD behaviors have direct tests (minting, dual-read, migration verification, collision prevention)
- [x] Cross-workspace purge-isolation test exists and fails on a name-keyed regression
- [x] `make test` green

## Out of scope

- e2e/kind suites (`make test-e2e` stays CI territory); dashboard/backend tests (no surface changed); w7/m85 and w7/m86 stay in w7; no live prod tenant-data migration without explicit operator authorization (t007's gate).

## Completion evidence (2026-08-18)

Minting: `identity_test.go`, `creds_test.go` disjoint workspaces, envtest same-named Apps. Dual-read: creds dual-read ACL tests, `resolver_test.go` legacy fallback. Migration: `migrate_test.go` dry-run / mismatch abort / resume / sibling skip-tombstone. Collision/purge isolation: `TestPurgeJobHitsOnlyOwningPrefixes`, `TestRegistryCleanupLabeledAppDoesNotDeleteLegacyRepo`, scoped-revoke-leaves-sibling. `make test` from `lego/operator/` green (identity 84.5%, migrate 86.1%, registry 85.5%, publish 91.0%).
