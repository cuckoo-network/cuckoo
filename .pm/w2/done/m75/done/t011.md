---
id: w2/m75/t011
title: "Closeout: verify DoD, mark done, move milestone to done/"
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m75/t010, w2/m75/t012]
---

## Objective

Standing closing task: verify every Definition-of-done clause in `.pm/w2/m75/README.md` against the shipped state, then execute the board's done procedure so the milestone leaves the active set with no tombstone.

## Context

- DoD was amended after review Finding 2: envtest disjoint mint + the t012 upgrade-transition test replace a shared-cluster operator-image walk (other workers share that cluster). Live prod cutover stays runbook-gated.
- Prior closeout (2026-08-18) was unstaged because it closed before Finding 1's transition coverage existed.
- Done procedure (`.pm/CLAUDE.md`): a done milestone moves WHOLE — never leave a tombstone at the old path.

## Steps

1. Walk the (amended) DoD clause by clause; t012 must be done first.
2. Set remaining task frontmatter `status: done` and the milestone README `**Status:** done`.
3. Move task files into `.pm/w2/m75/done/`, then `mv .pm/w2/m75 .pm/w2/done/m75`.
4. Check the m75 box in `.pm/w2/README.md`.

## Files

- .pm/w2/m75/ (whole directory → .pm/w2/done/m75/)
- .pm/w2/README.md (milestone checkbox row)

## Acceptance criteria

- [x] Every amended DoD clause verified with pointed evidence
- [x] Task frontmatter, milestone README status, and w2 README checkbox all synced (three places)
- [x] `.pm/w2/m75` absent; `.pm/w2/done/m75/` contains the whole milestone

## Out of scope

- Shipping/committing (only via /ship on user request); any remaining prod migration phases (t007 runbook); w7/m85 and w7/m86 stay in w7.

## Evidence (2026-08-18, second closeout)

Amended DoD walk:

- Same-named Apps mint disjoint workspace-scoped Zot repos/users/Secrets: envtest `collide` × two `tea-…` namespaces (`per_app_registry_envtest_test.go`).
- Already-published labeled static site keeps empty `status.staticPrefix`: envtest `keeps an already-published labeled site on the empty/legacy prefix across reconcile`.
- Completed labeled publish records `W/A/<rev>/`: envtest `records a workspace-scoped staticPrefix after a completed labeled publish`.
- Unlabeled App stays on the legacy column: identity tests + skip-publish unlabeled `StaticPrefix` empty; unlabeled direct-publish records `site-direct/rev-1/`.
- Migration copy+verify+tombstone against fakes: `internal/migrate` + `cmd/registry-migrate`; no blob delete.
- Prod runbook with authorization-gated STOP: `docs/runbooks/registry-static-identity-migration.md`.
- ADR055 F2/F3 residual-register annotated; F2/F3 stay open until runbook phase 4.
- Live operator-image upgrade on the shared mock cluster remains out of scope; `scripts/verify-workspace-scoped-identity.sh` is the live inventory procedure.

`go test ./internal/controller/ ./internal/migrate/ ./internal/identity/ ./internal/staticserver/` + `make lint` green. Finding 1 fixed before this closeout.
