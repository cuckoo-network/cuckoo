---
id: w2/m75/t008
title: "Docs: annotate ADR055/ADR067/ADR069 residual-register lines and any env/table changes in CLAUDE.md"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m75/t007]
---

## Objective

Update the security lineage's residual registers to reflect m75's new state — F2/F3 move from "Deferred — migration-gated" to "code landed (dual-read + migration tool); prod cutover runbook-gated" — and sync every operator-facing doc/table the implementation touched (CLAUDE.md env table if any new env var was added, ADR022's registry sections, ADR029's prefix description, `.env.example` if applicable).

## Context

- docs/ADR055-security-review-round4.md — F2/F3 disposition rows (lines 21-22) + the migration-gated paragraph (line 44) + the open question (line 52).
- docs/ADR067-security-review-round12.md — finding 1's split (lines 13, 26-33), the "residual within the residual" note on `RevokeCreds`/purge (line 44), and the standing-deferral table row "OCI/Zot + static-prefix tenant identity migration | ADR055 F2/F3 | re-confirmed (finding 1)" (line 85).
- docs/ADR069-security-review-round14.md:118 — the open-question repeat (legacy bare-named App CR inventory).
- Intermediate re-confirmations to annotate if they carry register lines: docs/ADR056-security-review-round5.md (findings 1/3), docs/ADR057-security-review-round6.md (8/10), docs/ADR061-security-review-round8.md.
- docs/ADR022-tenant-isolation.md §Registry access control (line 183) + §Per-App pull credentials (line 226) — name formats change for labeled Apps.
- docs/ADR029-static-sites.md — the `<appID>/<revision>/` prefix contract.
- CLAUDE.md operator env-var table + `.env.example` — only if t002–t005 introduced env vars (mirror name + comment, never values).
- Markdown is CI-checked: run `npx prettier@3.4.2 --write` on the touched `.md` files.

## Steps

1. Annotate each register/disposition line with the m75 state and a pointer to this milestone + the t007 runbook (do not rewrite history — append the new state).
2. Update ADR022/ADR029 name-format text for the workspace-scoped scheme (legacy format retained as the documented fallback).
3. Sync CLAUDE.md env table + `.env.example` for any new variables.
4. Prettier over the touched markdown files.

## Files

- docs/ADR055-security-review-round4.md, docs/ADR067-security-review-round12.md, docs/ADR069-security-review-round14.md
- docs/ADR056-security-review-round5.md, docs/ADR057-security-review-round6.md, docs/ADR061-security-review-round8.md (register lines only, where present)
- docs/ADR022-tenant-isolation.md, docs/ADR029-static-sites.md
- CLAUDE.md, .env.example (only if env vars changed)

## Acceptance criteria

- [x] ADR055 F2/F3 + ADR067 finding-1 register lines carry the m75 annotation with milestone/runbook pointers
- [x] ADR022/ADR029 describe the workspace-scoped formats with the legacy fallback noted
- [x] CLAUDE.md/.env.example in sync with any new env vars
- [x] Prettier clean on touched files

## Out of scope

- Declaring F2/F3 fully closed (that happens only after the runbook's phase 4 in prod); no live prod tenant-data migration without explicit operator authorization (t007's gate); w7/m85 digest-pinning and w7/m86 cache implementation stay in w7.

## Completion evidence (2026-08-18)

Append-only annotations on ADR055 F2/F3, ADR056 1/3, ADR057 8/10, ADR067 finding 1 + register row, ADR069:118. ADR022 registry formats and ADR029 prefix updated. ADR061 had no F2/F3 line (skipped). CLAUDE.md / `.env.example` unchanged (no new env vars). F2/F3 remain open until runbook phase 4.
