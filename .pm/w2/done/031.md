# Repair ADR018 stale board cross-references

Why: the parity ledger is the evidence map every parity round reuses by rule; two of its pointers now reference `.pm` paths that no longer exist, sending future brainstorms re-deriving settled state.

- The Workspace-lifecycle row's "Known drift" note cites `.pm/w9/050.md` (Enterprise plan directly assignable) — that path exists nowhere under `.pm/` (not open, not in any `done/`). Find where the item actually went, or restate the drift inline, and fix the pointer.
- The Managed-OIDC row says "Filed `.pm/w2/025.md`" — that note was deferred to `.pm/FUTURE-MAYBE.md` by user direction 2026-08-23 (trigger: tenant demand for key-less AI/cloud auth, or parity pressure). Point the row at the FUTURE-MAYBE entry and record the deferred status.
- Distinct from open `w4/035` (ADR018's stale shared-hosting security update) — coordinate if both land together, the edits are in different rows.

← from `/pm-brainstorm for w2` 2026-09-01 #6.

---

**Resolved 2026-09-08** — both pointers fixed in `docs/ADR018-render-parity.md`: the Workspace-lifecycle row now states the Enterprise-directly-assignable drift inline (no board item exists; the deliberate non-goal lives in `w9/done/m88`'s parity notes + `w4/done/m90/done/t004.md`'s out-of-scope list — the cited `.pm/w9/050.md` never existed), and the Managed-OIDC row points at the `.pm/FUTURE-MAYBE.md` entry with its deferred status and trigger. `w4/035` untouched (different rows).
