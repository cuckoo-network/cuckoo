---
id: w2/m73/t012
title: Simplify
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m73/t011]
---

## Objective

Run `/simplify` over the complete m73 diff and apply worthwhile reuse, clarity, and efficiency improvements without changing the accepted Environment Group behavior.

## Context

- The milestone deliberately reuses service-environment drafts, dotenv handling, generated values, save modes, Project/Environment selectors, and Apollo cache helpers.
- The main cleanup risk is near-duplicate service/group UI or Core code that drifts immediately after launch; the main over-abstraction risk is erasing distinct authorization and rollout boundaries.

## Steps

1. Review the full changed diff with the `simplify` skill.
2. Consolidate genuinely identical parsers, draft operations, validators, error mapping, and cache-update helpers.
3. Remove dead immediate-editor paths made obsolete by the staged UI while retaining documented compatibility endpoints.
4. Check for avoidable metadata sweeps, duplicate secret reveals, N+1 scope queries, and repeated rollout orchestration.
5. Apply accepted improvements and rerun focused tests touched by the cleanup.

## Files

- `lego/backend/internal/envgroups/`
- `dashboard/src/features/env-groups/`
- shared service-environment/project helpers changed by m73

## Acceptance criteria

- [ ] `/simplify` has reviewed the full milestone diff and its accepted changes are applied.
- [ ] Service/group reuse has one owner where semantics are truly identical; authorization, secret custody, and fan-out rollout differences remain explicit.
- [ ] No dead staged/immediate dashboard path, duplicate parser, or redundant scope query remains.
- [ ] Focused tests remain green and no acceptance criterion from t001–t011 changes.

## Out of scope

- Bug hunting beyond issues exposed by the simplification review.
- Broad redesign outside files changed by m73.
