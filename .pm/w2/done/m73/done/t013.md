---
id: w2/m73/t013
title: Test coverage and repository gates
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m73/t011, w2/m73/t012]
---

## Objective

Close all meaningful automated coverage gaps for m73's correctness, concurrency, secret-safety, adapter, cache, staged-editor, and responsive behaviors, then run every repository gate affected by the final diff.

## Context

- Individual tasks require focused tests; this gate reviews the final integrated behavior and fills missing cross-feature/race coverage.
- Highest-risk cases are generated-value intent loss, concurrent name claims, legacy ambiguity, CAS compensation, zero/one rollout fan-out, bulk reveal failure, cache eviction, and workspace/scope switching.

## Steps

1. Audit task acceptance criteria against permanent tests and add missing Core, adapter, real-store/fake-store, hook, component, router, and browser-level regressions.
2. Run backend tests/build plus dashboard test/lint/typecheck; run operator/types gates if the final diff touches their modules.
3. Run focused race/repetition tests for name-claim and group-patch concurrency and record commands/results in the milestone closeout notes.
4. Regenerate GraphQL/code artifacts and manifests only where their source changed; verify generated files are clean.
5. Run Prettier for Markdown and verify `git diff --check`.

## Files

- `lego/backend/internal/envgroups/**/*_test.go`
- `dashboard/src/features/env-groups/**/__tests__/`
- `dashboard/src/routes/**/__tests__/`
- generated GraphQL/types touched by m73

## Acceptance criteria

- [ ] Permanent tests cover every P0/P1/P2 reproduction and the complete staged/scope/import/export/clone/search flows.
- [ ] At least one concurrency test proves one unique-name winner; CAS compensation and rollout-idempotency tests are deterministic under repetition/race.
- [ ] Backend `go test ./...` and `go build ./...` pass from `lego/backend`.
- [ ] Dashboard `yarn test`, `yarn lint`, and `yarn typecheck` pass; production build runs if routing/codegen changes require it.
- [ ] Any touched operator/types module passes its documented make/test/lint gates.
- [ ] Markdown is Prettier-clean and `git diff --check` reports no error.

## Out of scope

- Unrelated flaky-test cleanup unless m73 caused or exposed the failure.
- Claiming production/live success beyond t011's recorded evidence.
