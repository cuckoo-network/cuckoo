---
id: w2/m81/t005
title: Simplify — run /simplify over the changed manifests/scripts
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m81/t004]
---

## Objective

Standing closing task: run `/simplify` over everything this milestone touched and apply the reuse/simplification/altitude cleanups it finds.

## Context

- Scope of the diff: the new approver Application + values, the CAPD template edits, the metrics-server base/overlay changes, the register-line doc annotations, and any validate-script additions made early.
- `/simplify` is quality-only (reuse, duplication, altitude) — it does not hunt for bugs; anything correctness-shaped goes back to the owning task.

## Steps

1. Collect the milestone's changed files (`git status` / `git diff`).
2. Run `/simplify` and review its findings.
3. Apply accepted cleanups; re-run `bash scripts/gitops-validate.sh` and `bash scripts/clusterapi-validate.sh` to confirm nothing regressed.

## Files

- Whatever t001–t004 changed (see their Files sections); no new files expected.

## Acceptance criteria

- /simplify run recorded; accepted findings applied; both validate scripts still pass.

## Out of scope

- New functionality; guard-script coverage (t006); closeout (t007).
