---
id: w2/m45/t006
title: Test coverage
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m45/t004]
---

## Objective

Add meaningful tests for the shipped behavior: parse/persist round-trip, first-success-only gating (including the failed-first-deploy retry), re-sync no-rerun, and the echo shape per surface. Tests must assert real behavior and failure modes — no tautological coverage.

## Acceptance criteria

- [ ] `cd lego/backend && go test ./...` and `make test` (from `lego/operator/`) green with the new tests.
