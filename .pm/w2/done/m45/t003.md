---
id: w2/m45/t003
title: Surface echo + no-rerun verification + ledger updates
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m45/t002]
---

## Objective

Echo the field on blueprint reads (validate/list/by-id across the surfaces bex exposes), verify end-to-end that re-syncs and re-deploys don't re-run the hook, and flip the ledgers.

## Context

- `docs/ADR018-render-parity.md:49` ("recorded as unsupported") and any ADR006 "ignored" list mention.
- Blueprint read surfaces: `apps/rest.go` validate/list/sync, `graphql.go` by-id, MCP tools.

## Steps

1. Add the field to blueprint read/echo shapes.
2. Live or envtest verification of the no-rerun property across a re-sync + a redeploy.
3. Update ADR018 + ADR006 with evidence.

## Files

- `lego/backend/internal/apps/rest.go`, `graphql.go`, `mcp.go`; `docs/ADR018-render-parity.md`, `docs/ADR006-bex-api.md`

## Acceptance criteria

- [ ] Field visible on reads; ledgers updated with evidence; no-rerun observed.

## Out of scope

- The standing parity comparison (t004).
