---
id: w2/m91/t005
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m91/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — in particular, the alias/merge plumbing t001's policy introduced across the three tools should end up as one shared idiom, not three hand-rolled variants.

## Outcome

Unified into `lego/backend/internal/mcputil/alias.go`: `PreferString`, `PreferPtrOrZero`, `RequireAliasString`, `ResourceIDs`. Domain validators (`applyCPUAggregation`, `applyHTTPRequestAggregate`) stay in metrics. Static-site wiring needed no alias merge.

## Acceptance criteria

- [x] `/simplify` findings applied or explicitly dismissed with reasons
