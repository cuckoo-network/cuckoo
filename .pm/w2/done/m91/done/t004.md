---
id: w2/m91/t004
title: Flip the parity-inventory classes; update ADR018 + the m70 evidence note
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m91/t001, w2/m91/t002, w2/m91/t003]
---

## Objective

Reclassify the three repaired tools out of `Divergent` in the MCP parity inventory, verify cross-surface consistency for the touched fields, and bring the written record in line: ADR018's "filed for repair" sentence becomes a done record, and the m70 evidence rows are annotated.

## Measured reclassification (w2/m91)

| tool | before | after | note |
| --- | --- | --- | --- |
| `get_metrics` | Divergent | **Superset** | all upstream args present + bex extras |
| `create_postgres` | Divergent | Divergent | only `region` remains (accepted; diskSizeGb repaired) |
| `create_static_site` | Divergent | Divergent | only required `publishPath` remains (accepted; autoDeploy/buildCommand repaired) |

Inventory: Divergent 7→6, Superset 2→3. `get_metrics` removed from `mcpAcceptedDivergences`; postgres/static reasons updated.

## Cross-surface spot-check

- MCP `autoDeploy`/`buildCommand` match REST/GraphQL create fields (same `CreateRequest`).
- Metrics MCP Render names alias onto the same `MetricQuery` REST/GraphQL already populate (`host`/`path`/`groupBy`/`quantile`/`resolution`).
- **Follow-up (not blocking):** REST/GraphQL postgres create still spell storage as `diskSizeGB` (capital B), while Render MCP uses `diskSizeGb`. MCP now accepts both; unifying REST/GraphQL casing is a separate surface decision.

## Acceptance criteria

- [x] Unintended divergences repaired; inventory/guard green and fail-closed (`get_metrics` left Divergent; create tools stay only for accepted genuine diffs)
- [x] ADR018 + inventory records updated
- [x] Cross-surface consistency verified; REST/GraphQL `diskSizeGB` vs MCP `diskSizeGb` filed as follow-up above

## Out of scope

- The four accepted genuine differences and the three upstream-only tools
