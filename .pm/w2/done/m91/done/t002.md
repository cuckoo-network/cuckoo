---
id: w2/m91/t002
title: create_static_site — wire autoDeploy + buildCommand to the existing CreateRequest fields
worker: worker2
status: done
estimate: 30m
depends_on: []
---

## Objective

Add the two arguments Render's `create_static_site` has and bex's omits — `autoDeploy` and `buildCommand` — wiring them to the `CreateRequest` fields that already exist and already serve the other create tools. Pure wiring, no new capability.

## Acceptance criteria

- [x] `create_static_site` accepts `autoDeploy` + `buildCommand` and the created App spec reflects both
- [x] `publishPath` requirement unchanged

## Out of scope

- Any other `create_static_site` argument
