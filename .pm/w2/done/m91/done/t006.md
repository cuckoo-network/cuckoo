---
id: w2/m91/t006
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m91/t004]
---

## Objective

Add meaningful tests for the repaired contracts: every accepted Render spelling proven end-to-end (arg → spec/query), both-set precedence proven per the alias policy, and the inventory guard proven still capable of failing (a deliberate misclassification trips it).

## Mutation spot-checks

1. Swapped `PreferPtrOrZero` primary/legacy order → `TestPreferPtrOrZero` + `TestCreatePostgresDiskSizeGbAlias/render_wins_when_both_set` + `TestMCPCreatePostgresDiskSizeGb` failed; reverted.
2. Set `TestMCPParityInventory` want Superset=2 → failed with `got 3 tools, want 2`; reverted.

## Acceptance criteria

- [x] Render-spelling round-trips + precedence covered for all three tools
- [x] Mutation spot-checks recorded (which mutation fails which test)
- [x] Backend `go test ./...` + `make lint` green
