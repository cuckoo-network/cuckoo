---
id: w2/m91/t003
title: get_metrics — adopt Render's arg names; triage the 7 missing upstream args
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m91/t001]
---

## Objective

Make `get_metrics` accept Render's argument names — applying t001's alias policy — and triage the 7 upstream arguments bex's tool was missing.

## Triage of the 7 missing upstream args (pin)

| upstream arg | disposition |
| --- | --- |
| `resourceId` | **wired** — primary; legacy `resource[]` kept; Render wins when both set |
| `resolution` | **wired** — alias of `resolutionSeconds` |
| `httpLatencyQuantile` | **wired** — alias of `quantile` |
| `httpHost` | **wired** — alias of `host` |
| `httpPath` | **wired** — alias of `path` |
| `cpuUsageAggregationMethod` | **thin wire** — empty/`AVG` accepted (metrics-server snapshot); `MAX`/`MIN` rejected |
| `aggregateHttpRequestCountsBy` | **partial** — `statusCode` → `GroupBy=status`; `host` rejected (no Traefik/Loki host group-by axis) |

Datastore sibling `get_datastore_metrics` got the same `resourceId`/`resolution` aliases for consistency (still an Extension).

## Context

- `lego/backend/internal/metrics/mcp.go` — alias fields + triage helpers.
- Upstream contract: the m70 pin, `.pm/w1/done/m70/evidence/inventory.md` row 48.

## Acceptance criteria

- [x] `resourceId` / `httpLatencyQuantile` / `resolution` accepted and honored
- [x] Each of the 7 missing args either wired (tested) or rejected with a recorded reason
- [x] Bex extensions (`quantiles` overlay) unchanged

## Out of scope

- Metrics service capability additions beyond argument plumbing
