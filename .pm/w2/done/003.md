# Key Value surfaces (REST/GraphQL/MCP) — blocked on w1/007 mechanism

Render's official MCP server ships `create_key_value` / `list_key_value_instances` / `get_key_value`; Render's REST has the matching key-value endpoints. bex has no mechanism yet — the `KeyValue` CR + reconciler (Valkey/Redis) is itself only an inbox note (`w1/007`). Surfaces without the mechanism would be fake, so this stays parked.

When w1/007 lands: expose the surfaces in `lego/backend/internal/` as a feature package (one service, three thin adapters), Render tool/endpoint names, shapes verified against the OpenAPI spec — same pattern as postgres (w5/m8). Likely milestone-sized then; promote.

Source: second `/pm-brainstorm for w2` 2026-07-08; `render-oss/render-mcp-server` tool list; `w1/007`.
