---
id: w2/m14/t003
title: REST — registry-credentials CRUD
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m14/t001]
---

## Objective

Expose `registry_credentials` CRUD over bex-api's REST surface, mirroring Render's `/registrycredentials` shape and following the existing resource-package conventions.

## Context

- Render's real endpoint is "Update Registry Credential" per its docs (confirmed live via search 2026-07-13) — check the actual field names/shape via Render's OpenAPI spec if reachable, matching the verification bar other parity milestones hold (`docs/ADR018-render-parity.md`'s "Method" section).
- Follow the REST envelope/error-shape conventions in `docs/ADR006-bex-api.md`.

## Steps

1. `GET/POST /v1/registry-credentials`, `GET/PATCH/DELETE /v1/registry-credentials/{id}` (secret returned only on create, matching the webhook-endpoint pattern; `PATCH` can update the secret/expiry without returning it).
2. Wire into the existing OpenFGA authz check (workspace-scoped).

## Files

- new `lego/backend/internal/registrycreds/rest.go`
- `lego/backend/internal/api/server.go` (route registration)

## Acceptance criteria

- [ ] Create returns the secret once; subsequent reads never include it.
- [ ] List/get/delete are workspace-scoped (cross-workspace access denied).
- [ ] Error shapes match the existing REST convention.

## Out of scope

- GraphQL/MCP — t004/t005.
