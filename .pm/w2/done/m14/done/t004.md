---
id: w2/m14/t004
title: GraphQL — registry-credentials CRUD
worker: worker2
status: done
estimate: 25m
depends_on: [w2/m14/t003]
---

## Objective

Mirror the REST surface from t003 on GraphQL, matching the dashboard-shaped query/mutation naming convention used by every other resource.

## Context

- Follow `docs/ADR006-bex-api.md`'s "one Core, three adapters" pattern — resolvers call the same Core verbs REST does.

## Steps

1. `registryCredentials`/`registryCredential(id)` queries, `createRegistryCredential`/`updateRegistryCredential`/`deleteRegistryCredential` mutations.
2. Regenerate GraphQL codegen artifacts consumed by the dashboard.

## Files

- `lego/backend/internal/registrycreds/graphql.go`
- dashboard codegen outputs

## Acceptance criteria

- [ ] Same authz scoping as the REST surface; codegen committed with no drift.

## Out of scope

- REST (prerequisite, t003) and MCP (t005).
