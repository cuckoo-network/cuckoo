---
id: w2/m63/t006
title: Test coverage — collapse/resize/persistence behavior tests
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m63/t004]
---

## Objective

Standing closing task. Add meaningful tests for the behavior this milestone shipped — assertions on real behavior and failure modes, not snapshots.

## Context

- Dashboard test setup: vitest + testing-library (see existing component tests, e.g. the deploys-list-page suite).
- Behaviors worth pinning:
  - `collapsible="icon"`: collapsed state renders the rail (not `w-0`), labels hidden, `SidebarMenuButton` tooltips present.
  - Drag model: pointer sequence on `SidebarRail` updates `--sidebar-width` clamped to [min, max]; below-threshold release collapses; outward drag from collapsed re-expands; dblclick toggles.
  - ARIA: separator exposes `role`, `aria-valuemin/max/now`, and `aria-valuenow` tracks resize.
  - Cookie codec: signed-width encode/decode round-trips; legacy `true`/`false` values parse without crashing; collapse encodes negative, re-expand restores magnitude.
  - SSR hydration: provider initialized from `{open, width}` renders matching first-paint markup.
- jsdom has no real layout — drive pointer events with synthetic `clientX` and assert on the CSS var/state, not pixels.

## Steps

1. Unit-test the cookie codec (pure function — extract it if it isn't one).
2. Component-test the provider + rail drag/snap/dblclick/ARIA behaviors.
3. Component-test icon-collapse rendering for the main sidebar variant.
4. Run the full dashboard suite.

## Files

- `dashboard/src/common/components/ui/sidebar.test.tsx` (or colocated per repo convention)
- codec module if extracted

## Acceptance criteria

- [ ] Each behavior above has at least one failing-if-broken assertion.
- [ ] No tautological/snapshot-everything tests.
- [ ] Full `yarn test` green.

## Out of scope

- Playwright end-to-end (manual DoD walk covers the live check).
