---
id: w2/m63/t005
title: Simplify — run /simplify over the changed sidebar code
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m63/t004]
---

## Objective

Standing closing task. Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Primary target: `dashboard/src/common/components/ui/sidebar.tsx` (drag logic, cookie encoding) and the three sidebar consumers + `dashboard-layout/index.tsx`.
- Watch for: duplicated pointer-math, dead `sidebar_state` boolean paths left behind, drag state that could live in a ref instead of state, repeated `group-data-` class strings that belong in the primitive.

## Steps

1. Run `/simplify` scoped to the files t001–t003 touched.
2. Apply behavior-preserving cleanups; re-run dashboard tests/typecheck/lint.

## Files

- `dashboard/src/common/components/ui/sidebar.tsx`
- `dashboard/src/common/components/dashboard-layout/*`

## Acceptance criteria

- [ ] `/simplify` pass completed; accepted findings applied or explicitly declined with reasons.
- [ ] `yarn test`, typecheck, lint green after cleanup.

## Out of scope

- New behavior.
