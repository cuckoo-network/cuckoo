---
id: w2/m63/t007
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m63/t006]
---

## Objective

Standing closing task, run last. When t001–t006 are complete **and the definition of done actually holds** (live walk: icon-rail collapse on all three variants, drag-resize with snap, dblclick, reload in both states/widths with no flash, mobile Sheet intact), close the milestone.

## Context

- Do not run until the DoD is verified against a running dashboard, not merely code-complete.

## Steps

1. Verify the milestone DoD live.
2. Set every remaining task `status: done`; move each `tNNN.md` to `w2/m63/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w2/m63/README.md`.
4. Move the whole `w2/m63/` directory to `w2/done/m63/` and check `- [x]` in `w2/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m63/**`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] DoD verified live before any moves.
- [ ] Status synced in all three places; no tombstone left at `w2/m63/`.

## Out of scope

- New work (file follow-ups as inbox notes instead).
