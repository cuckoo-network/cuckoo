---
id: w2/m63/t004
title: Render parity — verify dashboard-surface consistency, document deliberate divergence
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m63/t003]
---

## Objective

Standing closing task. This milestone is dashboard-chrome-only (no REST/GraphQL/MCP change), so the parity check is scoped to the UI surface: confirm the sidebar changes regress no existing dashboard flows, check render.com's own dashboard sidebar behavior once, and record the deliberate Devin-style divergence instead of silently diverging.

## Context

- The parity contract (`docs/ADR018-render-parity.md`) tracks capabilities across REST/GraphQL/MCP/UI; sidebar chrome is not a capability row, but the standing rule is "compare against the equivalent render.com behavior and flag drift rather than silently diverging."
- Render's dashboard (as of the last audits) has a fixed left nav; bex is intentionally adopting Devin's richer pattern (icon rail + drag resize + persistence). That is a deliberate divergence, not accidental drift.

## Steps

1. Walk render.com's dashboard sidebar once (or use existing evidence under `docs/render-artifacts/`) and note its collapse/resize behavior.
2. Spot-check bex dashboard pages that mount each sidebar variant (main, project, service) plus the w3/m44 agent chat page — nav, header toggle, and content layout intact in both states.
3. If any behavior diverges from Render in a way a Render user would notice negatively, file it as follow-up; otherwise record the deliberate-divergence note (milestone README or the relevant dashboard doc).

## Files

- `docs/ADR018-render-parity.md` (only if a note/row is warranted)
- `.pm/w2/m63/README.md` (divergence note)

## Acceptance criteria

- [ ] All three sidebar variants + the agent chat page verified in expanded and collapsed states.
- [ ] Divergence-vs-Render explicitly recorded; any negative drift filed as follow-up work.

## Out of scope

- Any REST/GraphQL/MCP checks (no API surface changed).
