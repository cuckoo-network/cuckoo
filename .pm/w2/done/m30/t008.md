---
id: w2/m30/t008
title: Simplify — /simplify over the code this milestone changed
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m30/t007]
---

## Objective

Run a simplification pass over everything t001–t006 touched — behavior-preserving only.

## Context

- Likely candidates: the `commitId`/`deployMode`/`clearCache` validation logic in `rest.go`/`graphql.go` (check for duplication between the two adapters that should instead live once in `service.go`); the `apps.Restart`→`deploys.Trigger` call-through (t004) for any redundant authorization double-checks now that one verb calls the other.

## Steps

1. Run `/simplify` (or the equivalent manual review) over `lego/backend/internal/deploys/`, `lego/backend/internal/apps/service.go`'s `Restart`, and the dashboard files t006 touched.
2. Apply only reuse/simplification/efficiency changes that don't alter observable behavior.

## Files

- Whatever t001–t006 touched.

## Acceptance criteria

- [ ] No duplicated validation logic between REST and GraphQL adapters for the new fields.
- [ ] Full test suite still green after simplification.

## Out of scope

- Any behavior change — this task is pure cleanup.
