---
id: w2/m30/t007
title: Render parity — cross-surface consistency + refresh docs/ADR018-render-parity.md's Deploys rows
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m30/t006]
---

## Objective

Check that `commitId`/`clearCache`/`deployMode` behave identically across REST and GraphQL (same validation, same errors, same effect), and that the restart consolidation (t004/t006) didn't leave the dashboard's list-row Restart and header Restart producing observably different results. Refresh the parity ledger to reflect the real, now-narrower-but-honest field set, replacing the current overstated ✅/✅/✅ claim.

## Context

- `docs/ADR018-render-parity.md` §Deploys — the "Trigger a deploy" row currently reads `✅ | ✅ | ✅ | ✖` with no mention of the missing body fields; this task corrects it.
- Compare bex's final `commitId`/`deployMode` behavior against Render's documented `CreateDeployJSONBody` semantics one more time (cron rejection, `deploy_only` + `commitId` combination rejection) to confirm nothing drifted during implementation.

## Steps

1. Re-verify REST and GraphQL reject/accept identically for every case t002/t003/t005 defined.
2. Update the Deploys §"Trigger a deploy" row: what's now supported (`commitId`, `deployMode` with its honest constraint), what's deliberately not (`imageUrl`, true no-rebuild `deploy_only` for repo-backed services), and why.
3. Note the restart-consolidation fact (Restart now opens a deploy-history row) somewhere it'll be found — either this row or a note near the events-feed row.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] The Deploys "Trigger a deploy" row accurately states the supported field set and the deliberate gaps, not a blanket ✅.
- [ ] REST and GraphQL produce identical accept/reject decisions for every case this milestone added.

## Out of scope

- New capability — this task is verification + documentation only.
