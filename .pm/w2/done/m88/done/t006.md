---
id: w2/m88/t006
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m88/t004]
---

## Objective

Standing closing task: give the validator's new pool assertions real tests — fixture workflow snippets that must fail (credentialed-on-ci, unlabeled, GitHub-hosted) and pass (correctly labeled), runnable in CI beside the script's existing checks, asserting failure modes rather than just the happy path.

## Context

- Shell-script testing follows whatever harness `scripts.yml` already runs for `scripts/`; keep fixtures tiny and self-contained.

## Steps

1. Add fixtures + a test entry exercising each assertion added in t003.
2. Wire into the existing scripts CI gate; all green.

## Files

- `scripts/github-actions-validate.sh` test fixtures / harness
- `.github/workflows/scripts.yml` (only if the gate needs the new test)

## Acceptance criteria

- [x] Each t003 assertion has a failing-fixture test; suite green in CI.

> Done 2026-09-07 (verified; the coverage itself landed with the earlier m88 sessions) — `scripts/github-actions-validate.test.sh` proves red fixtures for every t003 assertion: GitHub-hosted `ubuntu-latest`, bare shared-pool label set, unapproved pool label, noncanonical runner-group syntax, secret/write-token/`environment:` on `bex-ci` (4 shapes), unguarded fork-PR job, PR-reachable `bex-production` — plus the green counterparts and a real-tree end-to-end pass. Wired in `.github/workflows/scripts.yml` (runs both the validator and its self-test); green in CI today (`scripts (test)` run 34160723241) and locally after the t005 edits.

## Out of scope

- Testing GitHub's own label routing.
