---
id: w2/m88/t004
title: Verify green runs on both pools; record the split in ADR083 + ADR019
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m88/t003]
---

## Objective

After the operator executes the fleet re-label runbook, prove the split live: at least one green CI-pool run (e.g. `backend-test.yml`) and one green production-pool run (a real deploy), with `gh run view` evidence showing which runner took each. Then record the split as done in ADR083 (follow-up 1) and ADR019 (runner custody section).

## Context

- The host re-label is operator-executed from the t001 runbook; this task coordinates and verifies, mirroring how w2/m85 handled its operator-supplied certificate step.
- `gh run view --json` exposes the runner name/labels per job.

## Steps

1. Confirm fleet re-label complete (runner list shows both pools populated).
2. Trigger/observe one green run per pool; capture runner evidence.
3. Update `docs/ADR083-security-review-round20.md` (follow-up 1 → done, pointer here) and `docs/ADR019-infra-credentials.md`; prettier.

## Files

- `docs/ADR083-security-review-round20.md`
- `docs/ADR019-infra-credentials.md`
- evidence notes under `.pm/w2/m88/`

## Acceptance criteria

- [x] Recorded evidence of a green run on each pool (criterion narrowed 2026-09-07: "on the intended hosts" → on the intended **pools** — host separation rejected by user decision, `.pm/DO_NOT_DO.md` `#RUNNER-HOSTS`); both ADRs updated.

> Done 2026-09-07 — fleet found fully offline and recovered first (Docker Desktop restarted, 16 stale OrbStack zombie runners stopped, 13 stale unlabeled registrations deleted; 7/7 `bex-ci` + 3/3 `bex-production` online). Green evidence: CI pool `scripts (test)` 34160723241 on `c57df3d66c97` (`bex-ci`); production pool deploy 33994179845 `build-and-deploy` success on `57789c8a59a8` (`bex-production`). ADR083 (status header, finding 2/3/4, obligations, scanner guidance, follow-ups) and ADR019 §Decision 5 record the split final at label level with host separation + rotation rejected (`#RUNNER-HOSTS`). Full record: `evidence/2026-09-07-fleet-recovery.md`.

## Out of scope

- Splitting further (per-repo pools), ephemeral runners.
