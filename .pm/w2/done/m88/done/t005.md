---
id: w2/m88/t005
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m88/t004]
---

## Objective

Run `/simplify` over the changed workflows, validator, and runbook — reuse, simplification, efficiency; behavior-preserving (e.g. de-duplicating repeated `runs-on` arrays via YAML anchors only if the repo already uses that idiom).

## Context

- Standing closing task.

## Steps

1. `/simplify` scoped to the milestone diff; apply accepted cleanups.
2. `scripts/github-actions-validate.sh` stays green.

## Files

- whatever t001–t004 touched

## Acceptance criteria

- [x] Simplify pass run and findings applied or explicitly declined.

> Done 2026-09-07 — three parallel review agents (reuse/consistency, quality, concision) over the m88 closeout diff. Applied: 3 stale "host separation pending" lines inside ADR083 (status header, finding 2, scanner guidance), finding-4 control list corrected (label routing does not bound host compromise), obligations 1–2 rewritten without strikethrough, follow-ups bullet collapsed to a pointer, runbook de-duplicated to one full decision statement + pointers and re-worded for the single-host topology, ADR019 sentence tightened, validator comment updated, evidence-file HEAD mis-attribution fixed (`0276d0ab1e`, not `cf11d724d`), dangling "ship note" reference replaced. Declined: none. `scripts/github-actions-validate.sh` + self-test re-run green after the edits.

## Out of scope

- Bug hunting; new behavior.
