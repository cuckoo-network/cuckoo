---
id: w2/m55/t007
title: Render parity
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m55/t004, w2/m55/t005, w2/m55/t006]
---

## Objective

Confirm the Web Shell is consistent across every surface it exposes and matches render.com's behavior, and update the parity ledger. The exec ticket + gateway WS are new surfaces; the dashboard terminal + instance picker are the user-facing change. Flag any drift as follow-up rather than silently diverging.

## Context

- Standing Render-parity closing task (`.pm/CLAUDE.md`). This milestone is feature dev touching the dashboard UI plus a new bex-api ticket endpoint and a new gateway WS endpoint, so parity applies.
- Ledger: `docs/ADR018-render-parity.md` — the Shell/SSH row currently records the browser terminal as a deliberate `—`/`◐` divergence (from m39). It must move to reflect that bex now ships an in-dashboard Web Shell.
- CLI: `docs/cli-compatibility-checklist.md` — the browser shell has no CLI counterpart (Render's shell in the CLI is `render ssh`, already covered by m39); note that this milestone doesn't change the CLI surface, or update if anything shifted.
- Surface consistency: this feature is UI + backend transport, not a REST/GraphQL/MCP resource CRUD, so "same fields across REST/GraphQL/MCP" mainly concerns the ticket-mint verb — verify it's exposed where it should be (at minimum session-authenticated REST/GraphQL for the dashboard; MCP is not required for a browser-only terminal — note the reasoning).

## Steps

1. Compare the shipped Web Shell against Render's live `/shell` page (terminal + instance picker + copy-ready `ssh` command coexisting) and note any UX drift.
2. Update `docs/ADR018-render-parity.md`: move the Shell/Web-Shell row to reflect the in-dashboard terminal; keep SFTP/forwarding/ephemeral/jobs as divergences.
3. Confirm the ticket-mint verb's surface placement is intentional and documented (why MCP is or isn't included for a browser-only terminal).
4. Update `docs/cli-compatibility-checklist.md` only if the CLI surface is affected; otherwise record that it isn't.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- `docs/cli-compatibility-checklist.md` (only if affected)

## Acceptance criteria

- [ ] ADR018 Shell row updated to show the in-dashboard Web Shell; remaining divergences stay accurate.
- [ ] Ticket-mint surface placement documented (REST/GraphQL present; MCP inclusion/exclusion justified).
- [ ] Any parity drift from Render's page is captured as explicit follow-up, not silently diverged.
- [ ] Markdown formatted.

## Out of scope

- New feature code (done in t001–t006).
