---
id: w2/m55/t008
title: Simplify
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m55/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — the exec-ticket mint, the gateway WebSocket handler + bridge, and the dashboard terminal/picker/reconnect components — for reuse, simplification, and efficiency, behavior-preserving.

## Context

- Standing Simplify closing task (`.pm/CLAUDE.md`). Quality only — no behavior change, no new features.
- Watch for duplication against existing mechanisms: the gateway WS bridge should reuse `KubeExecutor.Execute`, `ResolveSSHSession`, session caps, and the audit seam rather than re-implementing any of them; the ticket verify helper should be the single shared path used by bex-api and the gateway; the dashboard terminal should reuse the logs live-tail WS patterns where sensible.

## Steps

1. Run `/simplify` over the m55 diff (backend ticket + gateway WS + dashboard terminal).
2. Apply behavior-preserving cleanups; collapse any accidental duplication with m39/logs WS code.
3. Re-run the relevant suites (`cd lego/backend && go test ./...`, dashboard `yarn test`/`yarn lint`) to confirm no behavior change.

## Files

- Files touched by t002–t006.

## Acceptance criteria

- [ ] `/simplify` findings applied or consciously deferred with a note.
- [ ] No behavior change; backend + dashboard suites still green.
- [ ] No duplicated exec/targeting/audit/WS logic that m39 or the logs tail already provides.

## Out of scope

- Bug hunting (`/code-review`) and new tests (t009).
