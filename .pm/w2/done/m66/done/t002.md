---
id: w2/m66/t002
title: "Settings deep-link: /settings#addSshPublicKey anchor + auto-focus form + returnTo"
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m66/t001]
---

## Objective

Make the CTA's destination land on the *form*, not just the page: `/settings#addSshPublicKey` scrolls to and auto-expands/focuses the add-key input in the SSH Public Keys panel, and a `returnTo` param round-trips the user back to where they started once a key is saved.

## Context

- Panel: `dashboard/src/features/ssh-keys/components/ssh-keys-panel.tsx`, rendered in the Access section of `dashboard/src/features/auth/pages/settings-page/index.tsx:91` (route `/settings`).
- The `AddSshKeyCta` (t001) links here; encode the origin as `?returnTo=<path>` (e.g. `/agents/ags-…` or `/services/srv-…`). Validate `returnTo` is a same-origin internal path before navigating back (reuse the dashboard's existing safe-nav guard; never honor an external URL).
- On successful key create, if `returnTo` is present, navigate back to it; the gate on that page (t001, cache updates after the create mutation) then shows the real affordance live — no manual refresh.
- Anchor behavior: `#addSshPublicKey` scrolls the panel into view and focuses the key textarea / expands the add form. Keep it idempotent and SSR-safe (hash handling client-side only).

## Steps

1. Give the SSH Public Keys panel a stable id/anchor and a "focus the add form when the hash matches" effect.
2. Thread `returnTo` through the CTA link → settings page → post-create navigation, with same-origin validation.
3. Confirm the create mutation updates the shared cache so the origin page's gate flips without a reload.
4. Tests: hash focuses the form; a valid `returnTo` navigates back after save; an external/invalid `returnTo` is ignored.

## Files

- `dashboard/src/features/ssh-keys/components/ssh-keys-panel.tsx`
- `dashboard/src/features/ssh-keys/components/add-ssh-key-cta.tsx`
- `dashboard/src/features/auth/pages/settings-page/index.tsx`
- `dashboard/src/routes/settings.tsx` (search-param typing if needed)

## Acceptance criteria

- [ ] `/settings#addSshPublicKey` lands with the add-key form focused/expanded.
- [ ] Saving a key with `?returnTo=<internal path>` returns there and the affordance is live; external/invalid `returnTo` is refused.

## Out of scope

- The per-surface CTA copy/placement (t003/t004).
