---
id: w2/m66/t008
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m66/t007]
---

## Objective

Close the milestone once its definition of done actually holds: sync statuses, move tasks to `done/`, move `w2/m66/` to `w2/done/m66/`, check the box in `w2/README.md`.

## Context

- The move IS the completion signal — run `/pm done w2/m66/t008` last. DoD requires the gate live on all three surfaces with fail-open proven, not merely code written.

## Steps

1. Verify the DoD in `README.md` against the shipped behavior (ideally a live dashboard walk of the no-key → add-key → return round-trip).
2. `/pm done` this task last; it triggers the milestone move.

## Files

- `.pm/w2/m66/` → `.pm/w2/done/m66/`; `.pm/w2/README.md`

## Acceptance criteria

- [ ] Milestone directory lives only under `w2/done/`; all three status locations agree.

## Out of scope

- New work — file follow-ups as inbox notes.
