---
id: w2/m66/t006
title: Simplify pass over the milestone's changes
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m66/t005]
---

## Objective

Standing closing task: run `/simplify` over the milestone's dashboard changes — reuse, simplification, efficiency; behavior-preserving.

## Context

- Likely candidates: the two-door fallback shared between agent-session and service surfaces (extract one `SshAffordanceFallback` rather than duplicating), the `returnTo` param plumbing, and any duplicated CTA copy between en/zh.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted fixes; keep dashboard test + lint + typecheck green.

## Files

- As surfaced by the pass.

## Acceptance criteria

- [ ] Pass run; fixes applied or explicitly declined with reasons; suites green.

## Out of scope

- Behavior changes.
