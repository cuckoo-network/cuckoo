---
id: w2/m66/t007
title: Test coverage for gate states + deep-link round-trip + fail-open
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m66/t005]
---

## Objective

Standing closing task: meaningful tests for the behavior this milestone shipped — the gate's three states, the deep-link/return round-trip, and the fail-open guarantee. Assert real behavior and failure modes; no snapshot-everything.

## Context

- Highest-value assertions: fail-open (query error/loading → real affordance, NEVER the CTA); confirmed zero-key → CTA on every gated surface (agent session, service Connect, service Shell); has-key → unchanged affordance; two-door only on Web-Shell-eligible services; `returnTo` same-origin validation (external URL refused); post-save cache flip shows the live affordance without reload.

## Steps

1. Sweep the milestone diff for uncovered branches; add component/hook tests where a failure would be user-visible (a mis-gated affordance that hides a working feature is the worst case — cover it explicitly).
2. Full dashboard suite + lint + typecheck green.

## Files

- `dashboard/src/features/ssh-keys/**/__tests__/`
- `dashboard/src/features/agent-sessions/components/__tests__/`
- `dashboard/src/features/services/components/__tests__/`

## Acceptance criteria

- [ ] Fail-open is explicitly tested on at least one gated surface; all three gate states covered per surface; `returnTo` external-URL refusal covered.

## Out of scope

- Tautological/coverage-gaming tests.
