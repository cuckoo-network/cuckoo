---
id: w2/m29/t009
title: "Closeout — DoD met → move milestone to done/"
worker: worker2
status: todo
estimate: 10m
depends_on: [w2/m29/t008]
---

## Objective

Once every task above is done and the milestone's Definition of done is actually met, close the milestone.

## Context

- `.claude/skills/pm/SKILL.md` § `done` subcommand — this task's completion **is** the move.

## Steps

1. Confirm all tasks `status: done` and the DoD holds.
2. Set every task's frontmatter to `done`, mark rows `— DONE` in this README, set `**Status:** done`.
3. Move `w2/m29/*.md` to `w2/m29/done/`.
4. Move the whole `w2/m29/` directory to `w2/done/m29/`.
5. Check `- [x]` for m29 in `w2/README.md`.

## Files

- `.pm/w2/m29/**`
- `.pm/w2/README.md`

## Acceptance criteria

- [ ] Milestone directory relocated to `w2/done/m29/`.
- [ ] `w2/README.md` checkbox checked with a done note.

## Out of scope

- Anything not already covered by t001–t008.
