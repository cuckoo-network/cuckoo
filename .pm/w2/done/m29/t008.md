---
id: w2/m29/t008
title: "Test coverage — meaningful tests for the behavior this milestone shipped"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m29/t006]
---

## Objective

Ensure the dry-run mechanism and its three adapter surfaces are covered by tests that assert real behavior and failure modes, beyond t005's zero-side-effect matrix.

## Context

- t005 covers the "no side effects" property; this task covers param parsing, error paths (e.g. a dry-run call with an invalid plan still 400s), and the preview envelope's field-level correctness.

## Steps

1. Add unit tests per adapter (REST/GraphQL/MCP) for `dryRun`/`dry_run` param parsing and the preview envelope shape.
2. Add a test asserting a dry-run call with invalid input (bad plan, name conflict, unauthorized ownerId) still errors exactly as a real call would.

## Files

- `lego/backend/internal/apps/*_test.go`
- `lego/backend/internal/postgres/*_test.go`
- `lego/backend/internal/keyvalue/*_test.go`

## Acceptance criteria

- [ ] Tests fail if dry-run silently swallows a validation error instead of surfacing it.
- [ ] Full test suite green.

## Out of scope

- E2E already covered by t005.
