---
id: w2/m78/t006
title: "Render parity: refusal error shapes identical + Render-dialect across the three surfaces; bex-extension status documented"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m78/t004, w2/m78/t005]
---

## Objective

Standing closing task (narrow parity scope): verify the scope-refusal error is byte-consistent and Render-dialect across REST/GraphQL/MCP, and document scopes as a bex extension in the parity ledger.

## Context

Scopes are a documented bex extension — Render API keys are all-powerful, so Render has no scope surface to be compatible with. The parity obligation is therefore inverted: bex must not leak a non-Render error dialect. The REST refusal must match the Render error shape the dialect tests enforce (`lego/backend/internal/api/error_dialect_test.go`); GraphQL and MCP refusals must carry the same code (`INSUFFICIENT_SCOPE` or as decided in t003) per the cross-surface consistency precedent (`AGENT_SESSION_LIVE_LIMIT`, `BLUEPRINT_GROUPING_LIMIT` style). `docs/ADR018-render-parity.md` is the ledger; the official Render CLI (`docs/cli-compatibility-checklist.md`) must be unaffected because its device-flow tokens are exempt.

## Steps

1. Exercise the refusal on all three surfaces with a read-scoped token; capture the wire shapes and assert the REST one passes the existing dialect conventions (extend `error_dialect_test.go` if the suite enumerates codes).
2. Assert the same coded error appears on GraphQL and MCP (cross-surface test, t008 hosts the full matrix — here just the shape check).
3. Spot-check the official Render CLI path stays green: a device-flow (audience-less, platform-client) token hits no scope refusal on any command it could before.
4. Add/annotate the parity-ledger row: scopes as a bex extension, ✅ with evidence, noting Render has no counterpart.
5. Update `docs/ADR018-render-parity.md` (and `docs/cli-compatibility-checklist.md` only if a CLI-visible note is warranted).

## Files

- `docs/ADR018-render-parity.md`
- `lego/backend/internal/api/error_dialect_test.go` (extend if needed)
- Read-only: `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- One coded refusal shape, Render-dialect on REST, consistent on GraphQL/MCP, with test evidence.
- Parity ledger documents the bex-extension status with evidence links.
- Official Render CLI behavior demonstrably unchanged.

## Out of scope

- The full negative-path test matrix (t008); any new parity rows unrelated to scopes.
