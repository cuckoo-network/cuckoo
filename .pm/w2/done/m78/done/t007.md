---
id: w2/m78/t007
title: "Simplify: run /simplify over the changed code"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m78/t006]
---

## Objective

Standing closing task: run the `/simplify` skill over everything this milestone changed and apply the reuse/simplification/altitude cleanups it finds.

## Context

The milestone touches the auth gate (`lego/backend/internal/api/auth.go`), a new classification table + guard, three surface seams, the audit vocabulary, and the dashboard consent bridge — exactly the kind of cross-cutting change that accretes duplicate scope-string handling, parallel class enums, or three slightly different refusal renderers. `/simplify` is quality-only (no bug hunting; that is `/code-review`).

## Steps

1. Diff the milestone's changes (`git diff` scoped to the files listed across t002–t005 plus the dashboard consent files).
2. Invoke `/simplify` over that changed set.
3. Apply the accepted findings — likely candidates: one scope-normalization helper shared by introspection, consent lockstep docs, and audit; one refusal renderer consumed by the three surface adapters; the matrix as the single source for both guard test and enforcement.
4. Re-run the touched suites: `cd lego/backend && go test ./internal/api/... ./internal/audit/...`, `cd dashboard && yarn test`, and `make lint` from `lego/operator/`.

## Files

- Whatever t002–t005 changed under `lego/backend/internal/api/`, `lego/backend/internal/audit/`, `dashboard/src/common/server-fn/`

## Acceptance criteria

- `/simplify` has been run over the milestone's changed code and its accepted findings applied.
- All touched test suites and lint pass after the cleanups.
- No behavior change (byte-identical wire shapes before/after).

## Out of scope

- New features or scope-vocabulary changes; bug hunting (`/code-review` territory); simplifying code the milestone did not touch.
