---
id: w2/m44/t004
title: Deploy-hook imgURL param through the same validator
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m44/t002]
---

## Objective

The deploy hook accepts `imgURL` per Render, validated by the (possibly stricter) hook-side rule from t001, replacing today's named 400.

## Context

- Rejection site: `lego/backend/internal/deploys/deployhook.go:256,292`.
- The hook keeps its per-token rate limit (six/min, burst two) — no change there.

## Steps

1. Wire the param through the t002 verb with the hook trust level.
2. Remove the named-rejection branch; keep a clear deny message for out-of-rule origins.

## Files

- `lego/backend/internal/deploys/deployhook.go`

## Acceptance criteria

- [ ] A hook trigger with an in-rule `imgURL` deploys that image; out-of-rule gets the rule-naming error; no `imgURL` behaves exactly as before

## Out of scope

- New hook params beyond `imgURL`
