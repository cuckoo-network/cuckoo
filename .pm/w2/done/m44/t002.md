---
id: w2/m44/t002
title: Core verb — validated image-origin override on deploy creation
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m44/t001]
---

## Objective

Deploy creation in Core accepts an optional image-origin override, validated by t001's predicate, and projects it onto the App's image for that deploy.

## Context

- Deploy creation path: `lego/backend/internal/deploys/` (Core.Create / trigger verbs from w2/m5/m30).
- Only image-backed services qualify (repo-backed services build; Render 400s `imageUrl` on them — verify in t001).

## Steps

1. Add the override to the create/trigger verb; validate with the shared predicate; reject repo-backed services per the pinned behavior.
2. Ensure the deploy record captures the effective image ref (the existing `image{ref}` field).

## Files

- `lego/backend/internal/deploys/service.go` (+ predicate location per t001)

## Acceptance criteria

- [ ] Unit tests: allowed origin applies, disallowed origin errors with the rule-naming message, repo-backed service rejected

## Out of scope

- Surface wiring (t003/t004)
