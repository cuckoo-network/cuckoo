---
id: w2/m72/t007
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m72/t006]
---

## Objective

Standing closing task. Run `/simplify` over the code this milestone changed (operator build package, App controller, types status additions, backend deploy projection) — reuse, simplification, efficiency, altitude; behavior-preserving only. The D1 refactor retires a poll loop; make sure no dead wait-loop scaffolding, redundant status fields, or duplicated terminal-state handling survives it.

## Context

- Prime candidates: leftover synchronous-wait helpers in lego/operator/internal/build after t003, and any dispatch/observe duplication between native and kpack paths.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply the accepted findings; keep `make test` + backend tests green.

## Files

- lego/operator/internal/build/, lego/operator/internal/controller/, lego/types/, lego/backend/ (as changed by t002–t006)

## Acceptance criteria

- [ ] No dead code from the retired poll path remains.
- [ ] All suites still green after applied simplifications.

## Out of scope

- Bug hunting (that's /code-review) or new behavior.
