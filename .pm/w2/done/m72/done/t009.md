---
id: w2/m72/t009
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m72/t008]
---

## Objective

Standing closing task, run last. When t001–t008 are complete **and the definition of done actually holds** (starvation regression green, no supersede path cancels a running Job, SLIs distinguish superseded from canceled, surfaces consistent), close the milestone per `.pm/CLAUDE.md`: mark every task done, move task files to `w2/m72/done/`, set the README rows `— **DONE**` + `**Status:** done`, move `w2/m72/` to `w2/done/m72/`, and check the box in `w2/README.md`.

## Context

- Completing this task _is_ the move — `/pm done w2/m72/t009` last triggers the milestone move.
- DoD is behavioral, not "code written": verify the envtest scenario passes and production deploys complete under push load before closing.

## Steps

1. Re-verify the DoD checklist in the milestone README.
2. Execute the done-move (tasks → `done/`, milestone → `w2/done/m72/`, statuses synced in all three places).
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- .pm/w2/m72/ (whole directory), .pm/w2/README.md

## Acceptance criteria

- [ ] `w2/m72` exists only under `w2/done/`; no tombstone at the old path; statuses synced in task frontmatter, milestone README, and workstream README.

## Out of scope

- Any remaining ADR060 decisions (D2 classification, D3 cache, D6 tightening, …) — separate future milestones.
