---
id: w2/m72/t006
title: "Render parity: deploy lifecycle states across REST/GraphQL/MCP/dashboard"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m72/t005]
---

## Objective

Standing closing task. The supersede change alters user-visible deploy lifecycle: a coalesced generation must surface the way Render surfaces a replaced queued deploy (not as a spurious "canceled" failure), and an in-flight build must report as building/queued consistently. Verify the deploy status vocabulary and semantics are identical across REST (`/v1/services/{id}/deploys`), GraphQL, and MCP in lego/backend, and that the dashboard's deploy list/activity renders the superseded state honestly; compare against Render's documented deploy statuses and flag any drift as follow-up rather than silently diverging.

## Context

- docs/ADR006-bex-api.md (one core, thin adapters — same fields/semantics/error shapes ×3 surfaces); docs/ADR018-render-parity.md is the ledger to update if a deliberate divergence is recorded.
- The Cancel verb's wire behavior (w2/m10) must be unchanged.
- dashboard/CLAUDE.md before touching the UI.

## Steps

1. Map each new terminal/pending state to the Render deploy-status vocabulary; decide the wire representation for superseded (Render reference behavior first, divergence documented second).
2. Diff the three backend surfaces for identical projection; fix any drift.
3. Check the dashboard deploy views render the new states without "unknown status" fallbacks.
4. Update docs/ADR018-render-parity.md if any cell's evidence changed.

## Files

- lego/backend/ (deploys projection across REST/GraphQL/MCP)
- dashboard/ (deploy list/activity states)
- docs/ADR018-render-parity.md

## Acceptance criteria

- [ ] Same status vocabulary and semantics on all three API surfaces (tested at the core, spot-checked per adapter).
- [ ] Superseded generations never render as failures; dashboard shows them coherently.
- [ ] Any Render divergence is recorded in the parity ledger, none silent.

## Out of scope

- New dashboard UX beyond honest state rendering.
