---
id: w2/m64/t008
title: "Test coverage: async-dispatch lifecycle, race, and failure-surfacing tests"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m64/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — the async lifecycle's real failure modes and races, not line coverage: cancel-vs-dispatch, double-steer, dispatch failure surfacing, lazy-ticket not-ready, and the dashboard's optimistic/rollback flows.

## Context

- Backend: `lego/backend/internal/agentsessions/service_test.go` has `fakeStore`/`fakeLifecycle`; extend with deterministic synchronization (t001's injectable runner) — no sleeps.
- Race pass: run the package with `-race`.
- Dashboard: `components/__tests__/` for creating/failed/retry states, optimistic steer append + rollback + dedup.
- Tests must assert observable behavior (phases, reasons, error codes, sandbox teardown calls) — never tautological assertions on internals.

## Steps

1. Backend: cancel-during-dispatch converges (sandbox canceled, phase stays canceled); dispatch failure → `failed` + reason; steer conflict while background re-dispatch in flight; resume failure → `failed`; attach on `creating` → named retryable error.
2. `go test -race ./internal/agentsessions/...` green.
3. Dashboard: the t004/t005 component tests exist and assert user-visible outcomes.

## Files

- `lego/backend/internal/agentsessions/service_test.go`
- `dashboard/src/features/agent-sessions/components/__tests__/`

## Acceptance criteria

- [ ] Every acceptance criterion in t001/t003 has a test that fails if the behavior regresses.
- [ ] `-race` pass green on the backend package.
- [ ] Dashboard suite green.

## Out of scope

- Live-cluster E2E (note for a future acceptance run if a cluster cycle allows).
