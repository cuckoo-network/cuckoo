---
id: w2/m64/t009
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m64/t007, w2/m64/t008]
---

## Objective

Close the milestone once its definition of done actually holds: fast create/steer measured on the dev stack, provisioning/failure states rendering on the chat page, all suites green.

## Steps

1. Verify the milestone README's definition of done against reality (dev stack walk: create → instant navigate → creating → running; induced dispatch failure → failed + reason + retry; steer → optimistic append + redispatching).
2. Set every remaining task's `status: done`, move each `tNNN.md` to `w2/m64/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
4. Move `w2/m64/` to `w2/done/m64/`; check `- [x]` in `w2/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [ ] Definition of done verified, not assumed.
- [ ] Board state synced in all three places; milestone directory lives only under `w2/done/`.
