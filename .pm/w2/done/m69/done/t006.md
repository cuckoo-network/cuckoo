---
id: w2/m69/t006
title: "Test coverage — proxy auth, injection, narrowing, and failure modes"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m69/t005]
---

## Objective

Close the test gaps across the whole phase-1 chain with behavior-asserting tests — the confinement claims in ADR062's DoD must each be pinned by a test, not by inspection.

## Context

- The security-critical assertions: no real credential in sandbox-visible env (fake lifecycle), source-pod refusal, non-live-phase refusal at request time (not just mint time), header stripping, pinned-endpoint-only upstream, gated-off byte-identity, credential absent from logs/audit/errors.
- House style: table-driven Go tests with fakes (`gatewaytest`, `fakeLifecycle`, `fakeModelKeys` already exist); driver tests in the agent-image test suite.

## Steps

1. Sweep t001–t004 acceptance criteria for untested claims; add missing cases.
2. Add one integration-shaped test walking placeholder-dispatch → proxy → injected upstream against a stub provider server.
3. Assert the negative space: foreign pod, canceling session, mismatched upstream host, oversized/streaming bodies.

## Files

- `lego/backend/internal/sshgateway/modelproxy/*_test.go`
- `lego/backend/internal/agentsessions/*_test.go`, `internal/sessionegress/*_test.go`, `internal/sandbox/*_test.go`
- `lego/agent-image/driver/` tests

## Acceptance criteria

- [ ] Every DoD clause in the milestone README has at least one asserting test.
- [ ] No trivial/tautological coverage padding; failure modes dominate the new cases.
- [ ] `cd lego/backend && go test ./...` and the driver suite green.

## Out of scope

- Live-cluster acceptance (enablement step, post-ship); e2e kind runs.
