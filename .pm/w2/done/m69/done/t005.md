---
id: w2/m69/t005
title: "Simplify — /simplify over the milestone's changed code"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m69/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (modelproxy package, agentsessions/sandbox dispatch, sessionegress, driver) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Standing closing task. Likely reuse targets: the shared gateway kernel (source-pod resolution, limiter), `internal/hmacticket` conventions, `core` HTTP helpers.

## Steps

1. `/simplify` scoped to the milestone diff.
2. Apply accepted cleanups; keep behavior identical (suites green).

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run recorded; accepted findings applied; all suites green.

## Out of scope

- Bug hunting (that's `/code-review`), new behavior.
