---
id: w2/m69/t007
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m69/t006]
---

## Objective

Close the milestone once every other task is done **and the DoD holds**: set remaining task statuses to done, move task files to `w2/m69/done/`, mark all README rows `— **DONE**` and `**Status:** done`, move `w2/m69/` whole to `w2/done/m69/`, and check the `- [x]` box in `w2/README.md`.

## Context

- Standing closing task; completing it _is_ the move (`/pm done w2/m69/t007` last).
- DoD gate: the milestone's confinement claims must be test-pinned (t006) and the gated-off path byte-identical — do not close on "code written".

## Steps

1. Verify the DoD clause by clause against merged code + green suites.
2. Execute the closeout moves; leave no tombstone at the old path.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m69/` → `.pm/w2/done/m69/`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] Milestone directory lives only at `w2/done/m69/`; statuses in sync in all three places.

## Out of scope

- Production enablement (env rollout) — an operational step after ship.
