---
id: w2/m86/t003
title: Flip `BEX_DISK_STORAGE_CLASS=hcloud-volumes-luks` through the protected env chain
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m86/t001, w2/m86/t002]
---

## Objective

Point production disk provisioning at the LUKS class: set `BEX_DISK_STORAGE_CLASS=hcloud-volumes-luks` for the operator through the repository's protected env pattern, uncommenting/wiring the manager env plumbing so the deployed operator receives it.

## Context

- The operator reads it at `lego/operator/cmd/manager/main.go:357`; the manager manifest carries it commented out (`lego/operator/config/manager/manager.yaml:258`).
- Env custody: `.env.example` mirrors `.env` names (no values); `scripts/gh-secrets.sh` pushes `.env` → GitHub secrets; deploy workflows inject into the rendered manifests. This is not a secret value, so it may belong directly in the gitops/deploy manifest rather than a GH secret — follow whichever pattern the neighboring `BEX_DISK_SNAPSHOT_*` vars used (w1/m87).
- Gate-off must remain byte-identical: unset ⇒ cluster-default class, exactly as today.

## Steps

1. Trace how `BEX_DISK_SNAPSHOT_*` reach the prod operator (deploy workflow / gitops overlay) and mirror that for `BEX_DISK_STORAGE_CLASS`.
2. Set the value in the prod path; add the name to `.env.example` if the GH-secret chain is used.
3. Confirm the local/mock operator picks it up (`dev` env or envtest with the env set).
4. Do not roll prod out of band — the flip lands with the next `/ship`-driven deploy; note that in the milestone.

## Files

- `lego/operator/config/manager/manager.yaml`
- `deploy/gitops/` overlay or deploy workflow env (whichever the snapshot vars used)
- `.env.example` (names only, if applicable)

## Acceptance criteria

- [ ] The rendered production operator manifest carries `BEX_DISK_STORAGE_CLASS=hcloud-volumes-luks`; unset environments are byte-identical to today; `scripts/gitops-validate.sh` passes.

## Out of scope

- Live verification (t004); migrating pre-flip disks.
