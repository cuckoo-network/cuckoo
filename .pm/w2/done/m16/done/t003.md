---
id: w2/m16/t003
title: "Key Value: Service.SetPlan verb"
worker: worker2
status: done
estimate: 30m
depends_on: []
---

## Objective

Add the same `SetPlan` shape as t001 to the Key Value service layer, targeting the `KeyValue` CR instead of `Database`.

## Context

- `lego/types/v1alpha1/keyvalue_types.go` — `Plan string` is already a mutable spec field.
- `lego/backend/internal/keyvalue/` — service layer package.
- Reuse t001's validated pattern (row-then-CR ordering, Render-plan-spelling mapping) rather than re-deriving it.

## Steps

1. Add `Service.SetPlan(ctx, id, plan string)` to `lego/backend/internal/keyvalue`, validating against the Key Value plan catalog (`free`/`starter`/`standard`).
2. Same row-then-CR ordering guarantee as t001.

## Files

- `lego/backend/internal/keyvalue/*.go`
- `lego/backend/internal/keyvalue/*_test.go`

## Acceptance criteria

- [ ] `SetPlan` on a store-managed instance writes the CP row, then the CR, in that order.
- [ ] An unknown plan name returns `core.ErrBadRequest` and touches neither the row nor the CR.

## Out of scope

- REST/GraphQL/MCP wiring (t004).
