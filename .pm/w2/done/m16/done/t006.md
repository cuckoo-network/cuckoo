---
id: w2/m16/t006
title: "Dashboard: Postgres + Key Value detail-page plan section"
worker: worker5
status: done
estimate: 45m
depends_on: [w2/m16/t002, w2/m16/t004]
---

## Objective

Add a plan-change control to the Postgres and Key Value detail pages, reusing the service Settings plan-picker card component rather than building a new one.

## Context

- `dashboard/src/features/` — locate the existing service plan-picker card (shipped for App services, `w5/m7`) and its tier-card grid pattern (also used by the Key Value create form's tier cards, `w5/m12`).
- Postgres/Key Value detail pages already exist (`w5/m8`, `w5/m12`) — this adds a section, not a new page.

## Steps

1. Extract or reuse the App plan-picker card component so it can target either `updateServicePlan`, `updateDatabasePlan`, or `updateKeyValuePlan` via a passed-in mutation.
2. Add the section to the Postgres detail page.
3. Add the section to the Key Value detail page.
4. Confirm the Apollo cache updates the instance's plan immediately on success (no stale read until refetch).

## Files

- `dashboard/src/features/postgres/` (or equivalent detail-page route)
- `dashboard/src/features/keyvalue/`
- shared plan-picker component (wherever `w5/m7`'s lives)

## Acceptance criteria

- [ ] Changing plan on the Postgres detail page updates the displayed plan without a manual page refresh.
- [ ] Same for Key Value.
- [ ] The plan-picker component is shared, not duplicated three ways.

## Out of scope

- Storage-size editing UI (not in this milestone's scope, see t005).
