---
id: w2/m16/t010
title: "Test coverage"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m16/t008]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — asserting real behavior and failure modes, not tautological or snapshot-everything tests.

## Context

- Mirror `apps.SetPlan`'s existing test coverage shape (`TestSetPlanValidatesAndMapsRenderSpelling`, `TestSetPlanManagedAppWritesRowThenCR`, `TestSetPlanUnmanagedAppSkipsStore`, `TestSetPlanRowWriteFailureLeavesCRUntouched`) for both Postgres and Key Value.

## Steps

1. Unit tests for `postgres.Service.SetPlan` and `keyvalue.Service.SetPlan`: valid plan change, unknown plan rejection, row-then-CR ordering, row-write-failure leaves CR untouched.
2. HTTP/GraphQL/MCP handler tests asserting consistent request/response shapes and error codes across the three surfaces for both resources.
3. The envtest from t005 counts toward this milestone's coverage — confirm it's present and passing.

## Files

- `lego/backend/internal/postgres/*_test.go`
- `lego/backend/internal/keyvalue/*_test.go`
- `lego/operator/internal/controller/*_controller_test.go`

## Acceptance criteria

- [ ] Tests cover the failure modes (unknown plan, row-write failure), not just the happy path.
- [ ] All new tests pass in CI.

## Out of scope

- None.
