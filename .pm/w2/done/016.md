# Render-parity deploy-history table columns

Refine `dashboard/src/features/deploys/components/deploys-list-page.tsx` to match the current Render deploy-list presentation. Render the history as an explicit responsive table/grid with `Deploy`, `Trigger`, `Duration`, and an action column. Keep the deploy/status/commit/timestamp content in the first `Deploy` column; humanize every stored trigger (`create`, `api`, `deploy_hook`, `rollback`, and `blueprint`); show elapsed `startedAt` → `finishedAt` time (and a running elapsed value for an active deploy); preserve Cancel for an active deploy; and show Rollback only for an older successful/deactivated deploy that can be a rollback target. Rows and their non-action content remain links to deploy detail, while action buttons must not trigger row navigation.

## Concrete references (user-directed, 2026-07-18)

- **Polish target (bex):** `https://dashboard.bex.co/services/srv-d9bj8s3eg85c7390eb9g/deploys`
- **Design reference to match (Render, live):** `https://dashboard.render.com/web/srv-d5mk47ogjchc738phl00/deploys`
- **Named columns:** `Deploy` · `Trigger` · `Duration` · action column. The user also called out a **`30`** — reconcile against the live Render page whether this is a deploy sequence number shown in the `Deploy` cell or the total-deploy count chip near the header (bex already renders an `N deploys` / `N deploys loaded` count — `deploys.listCount*`). Capture whichever Render actually shows; don't invent a number the API doesn't provide.
- (Earlier `w2/m38` / `w5/m40` walks used `srv-d2rnr3jipnbc73deuvgg` as the reference; the URL above supersedes it for this pass.)

## Why no backend work

`deploys.graphql` already selects `trigger`, `startedAt`, `finishedAt`, and `rollbackOf`; `formatDeployDuration` and `DeployActions` already provide the core presentation and confirmed rollback/cancel mutations. Reuse the common table primitives, add English/Chinese column and trigger labels (extending the existing `deploys.trigger*` keys in `features/deploys/locales/{en,zh}.ts`), and extend `deploys-list-page.test.tsx` plus the duration/action tests for desktop headings, responsive/accessibility behavior, running/terminal durations, trigger labels, eligible rollback, ineligible/failed/current-live rows, and action-click isolation. Render's rollback documentation confirms that rollback targets are previous successful deploys with retained artifacts; bex's server remains the final availability check.

## Sizing

Sub-hour UI refactor — the data, mutation, confirmation flow, and test fixtures already exist; only the presentational restructure of one component + i18n labels + test extension remain. Keep it as an inbox note rather than creating a milestone (per the sizing rule + `DO_NOT_DO.md`). If the user later wants the full milestone apparatus (testable DoD + Render-parity / simplify / test-coverage closing tasks + a live acceptance run against the two services above), `/pm promote w2/016` will materialize it.

## Implemented — 2026-07-18

Shipped as an uncommitted working-tree change:

- `deploys-list-page.tsx` rewritten from the stacked `divide-y` card list into the shared `Table` primitive (the `resource-table.tsx` idiom): explicit `Deploy` / `Trigger` / `Duration` column headers + an `sr-only` **Actions** header. `Trigger`/`Duration` are `hidden sm:table-cell` and fold under the deploy identity on mobile (`sm:hidden`); the `Deploy` cell keeps the status badge, opaque id, commit, deployed-at, and pre-deploy line as the one detail-page `<Link>`, with `DeployActions` a sibling cell (row-navigation vs. action stay separate targets).
- `deploy-status.ts` `deployTriggerKey` extended to humanize `deploy_hook` → **deploy hook** and `blueprint` → **blueprint sync** (also improves the deploy-detail header, which shares the helper); `rollback` still renders via `deploys.triggerRollback` in the caller.
- New i18n keys (en + zh): `deploys.column{Deploy,Trigger,Duration,Actions}`, `deploys.trigger{DeployHook,Blueprint}`, `deploys.durationActive`.
- `deploys-list-page.test.tsx` extended to 9 tests: column headers, all-five trigger humanization, running/terminal/unstarted durations, rollback-only-on-historical eligibility, and action-click isolation (link href + button not inside the link).
- **The "30":** confirmed it maps to bex's existing `N deploys` / `N deploys loaded` count chip in the card header — bex deploys carry opaque `dep-…` ids, not sequence numbers, so no per-row number was invented.

Verification: `yarn vitest run src/features/deploys` (101 pass), full `yarn vitest run` (252 files / 1572 pass), `yarn lint` (typecheck + eslint, exit 0), Prettier applied. **Not** done: a live browser walk against the two services above (blocked on a dev-N cluster / prod CORS + real deploy data); the note's original "no live acceptance for an inbox note" sizing holds. Uncommitted pending `/ship`.
