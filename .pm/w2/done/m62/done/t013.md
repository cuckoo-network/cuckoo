---
id: w2/m62/t013
title: "Closeout"
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m62/t012]
---

## Objective

Standing closing task — run last, only when the milestone's definition of done actually holds (dashboard create-from-repo, auto-sync on push, sync history, PATCH/disconnect, resources[], detail-page parity — all real and tested, suites green, ADR018 updated).

## Context

- Completing this task **is** the move: set every remaining task `status: done`, move each `tNNN.md` to `w2/m62/done/`, mark all rows `— **DONE**` and `**Status:** done` in the milestone README, move the whole `w2/m62/` to `w2/done/m62/`, and check the `- [x]` box in `.pm/w2/README.md`.

## Steps

1. Verify the DoD against reality (not just merged code).
2. Execute the done-moves per `.pm/CLAUDE.md`; leave no tombstone at the old path.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m62/**`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] Milestone directory lives at `.pm/w2/done/m62/` with all tasks in `done/`; workstream checkbox checked; statuses in sync in all three places.

## Out of scope

- Any new feature work — file follow-ups as inbox notes.
