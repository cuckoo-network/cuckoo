---
id: w2/m62/t012
title: "Test coverage — meaningful tests for the shipped behavior"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m62/t010]
---

## Objective

Standing closing task. Close the coverage gaps the implementation tasks left: tests must assert real behavior and failure modes — never trivial, tautological, or snapshot-everything tests.

## Context

- High-value scenarios to confirm exist (add any missing): webhook push → recorded sync end-to-end through the intake (both HMAC keys); create with `sync:false` values → env lands exactly once and never reapplies on the next sync; disconnect → resources still reconcile; legacy `'active'` row reads as `in_sync` and can be PATCHed/synced; sync failure → `error` status + errored run + stored manifest untouched; syncs pagination boundary; dashboard stepper gating + disconnect confirm.
- Backend: `cd lego/backend && go test ./...` (integration tests run in CI against real Postgres + OpenFGA). Dashboard: `yarn test`.

## Steps

1. Audit coverage of the scenarios above across `blueprint_test.go`, webhook tests, store tests, dashboard tests.
2. Add the missing ones; keep them behavior-asserting.
3. Full suites + `make lint` green.

## Files

- `lego/backend/internal/apps/blueprint_test.go`, webhook + store test files
- `dashboard/src/routes/__tests__/`, feature component tests

## Acceptance criteria

- [ ] Every scenario listed in Context has a test that fails if the behavior regresses.
- [ ] All suites green.

## Out of scope

- Live-cluster acceptance (record as evidence in Closeout if a cluster is available; not a gate for this task).
