---
id: w2/m62/t010
title: "Render parity — cross-surface consistency check vs render.com"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m62/t005, w2/m62/t008, w2/m62/t009]
---

## Objective

Standing closing task. Verify the milestone's changes are consistent across every surface they expose — REST, GraphQL, MCP (`lego/backend/`: same fields, semantics, error shapes) and the dashboard — and compare against Render's actual behavior/API, flagging drift as follow-up rather than silently diverging.

## Context

- Render evidence gathered 2026-08-01 (milestone README Background): blueprint object fields + status enum, PATCH field set, DELETE disconnect semantics, syncs schema + state enum, dashboard create flow, auto-sync default-on, sync-never-deletes.
- `docs/ADR018-render-parity.md` Blueprint row (line ~126) must be updated: dashboard-create + lifecycle + syncs evidence; remove the stale "sync-delete unsupported" divergence phrasing (it is parity — Render never deletes on sync) and record deliberate bex extensions (explicit-body sync, create verb surface).
- `docs/cli-compatibility-checklist.md`: recheck the blueprint validate row still holds; the official CLI has no blueprint list/sync commands — note if that changed upstream.
- `docs/ADR006-bex-api.md` § bex.yml field ledger: add `path`/`autoSync` handling notes.

## Steps

1. Field-by-field diff of REST/GraphQL/MCP blueprint + sync shapes against the Render OpenAPI excerpts; fix or file drift.
2. Walk the dashboard flows against the Render dashboard's flow (live or captured) and note UX divergences.
3. Update ADR018 row + ADR006 ledger + checklist; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR006-bex-api.md`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] All three API adapters agree on field names, status/state vocabulary, and error shapes.
- [ ] ADR018 Blueprint row reflects the shipped surface with dated evidence; remaining divergences are named, not silent.

## Out of scope

- Fixing drift larger than ~30m — file it as an inbox note instead.
