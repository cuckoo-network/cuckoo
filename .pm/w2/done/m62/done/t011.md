---
id: w2/m62/t011
title: "Simplify — /simplify over the changed code"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m62/t010]
---

## Objective

Standing closing task. Run `/simplify` over the code this milestone changed (backend blueprint/store/webhook packages + dashboard blueprints feature) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Likely candidates: the sync run-recording wrapper vs create's initial apply (one path, not two), fetch-helper reuse between create/sync/webhook, dashboard hook duplication across the new operations.

## Steps

1. `/simplify` at default effort over the milestone diff; apply safe findings.
2. Re-run backend + dashboard suites and `make lint`.

## Files

- `lego/backend/internal/apps/`, `lego/backend/internal/store/`, `lego/backend/internal/webhooks/`, `dashboard/src/features/blueprints/`

## Acceptance criteria

- [ ] Findings applied or explicitly declined with reasons; suites + lint green after.

## Out of scope

- Bug-hunting (that's /code-review, not this task).
