---
id: w2/m41/t003
title: Core wiring: create/update via keyvalue service; validate/list/sync coverage
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m41/t002]
---

## Objective

Blueprint apply/sync provisions or updates Key Value stores through the existing `keyvalue` service verbs (w2/m7), and `/blueprints` validate/list/sync (w2/m15) report keyValue resources instead of omitting them.

## Context

- `lego/backend/internal/keyvalue` create/update verbs; `internal/apps` blueprint sync path; w2/m15's blueprint verbs.
- Stack deploys ride `Core.Create` (docs/ADR017) — no bespoke endpoint.

## Steps

1. Map parsed keyValue entries onto keyvalue service create/update (idempotent on re-sync).
2. validate/list/sync include keyValue resources in their reports.
3. Respect per-workspace `BEX_MAX_KEYVALUES` caps with the standard typed error.

## Files

- lego/backend/internal/apps/ (blueprint sync)
- lego/backend/internal/keyvalue/

## Acceptance criteria

- [ ] Applying a Blueprint with a keyValue entry creates the store once and re-sync is a no-op; validate reports it.

## Out of scope

- Env refs (t004).
