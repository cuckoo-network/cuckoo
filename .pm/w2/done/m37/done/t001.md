---
id: w2/m37/t001
title: `secretFiles` at service create (three adapters)
worker: worker2
status: done
estimate: 45m
depends_on: []
---

## Objective

Accept `secretFiles` on `POST /v1/services`, GraphQL `createService`, and the MCP create tools, delegating to the existing secret-files verb (w1/m16's `<name>-files` Secret + `/etc/secrets` mount) so files exist before the first pod starts — the w5/m20 `envVars`-at-create pattern, applied to its sibling.

## Context

- Verify Render's item shape on `servicePOST.secretFiles` (pinned spec) — match names exactly.
- Ordering: files must be materialized before/with the CR so the first rollout mounts them (check how envVars-at-create sequenced this; mirror it).

## Files

- `lego/backend/internal/apps/{service,rest,graphql,mcp}.go`, `internal/secrets` caller

## Acceptance criteria

- [x] A create with secretFiles boots pods that read the file at `/etc/secrets/<name>` on first start; omitted ⇒ byte-identical behavior.
