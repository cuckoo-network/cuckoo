---
id: w2/m37/t003
title: bex.yml acceptance for both fields
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m37/t001, w2/m37/t002]
---

## Objective

`bex.yml` service entries accept `secretFiles` and the environment assignment riding the same create verbs (check what render.yaml's actual vocabulary is for each — mirror it, and reject-with-named-error anything Render's blueprint spec doesn't have rather than inventing fields). Update ADR006's §bex.yml field ledger.

## Files

- `lego/backend/internal/blueprints/` (or the bex.yml apply path), `docs/ADR006-bex-api.md`

## Acceptance criteria

- [x] A bex.yml using canonical `projects[].environments[]` deploys with Environment membership; direct create-body-only fields are rejected by name; the field ledger reflects it.
