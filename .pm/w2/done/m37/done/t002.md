---
id: w2/m37/t002
title: `environmentId` at create: service + Postgres + Key Value
worker: worker2
status: done
estimate: 45m
depends_on: []
---

## Objective

Accept `environmentId` on the three creates (per the spec: servicePOST, postgresPOSTInput, keyValuePOSTInput), assigning the newborn resource to that Environment with the project auto-join ADR032 defines for `set_environment_services` — one shared helper, not three copies. Unknown/foreign-workspace environment ⇒ the 403/404 convention `core.Base.resolveWorkspace` established (no existence probing).

## Context

- Environments: `internal/environments` (w1/m32); Databases/KV joined via `core.LabelEnvironment` (w6/m20 — services-only asymmetry already closed).
- Note: Environments cover services via `apps.environment_id`, datastores via CR label — the create paths differ accordingly.

## Files

- `lego/backend/internal/{apps,postgres,keyvalue}/` create paths + `internal/environments` helper

## Acceptance criteria

- [x] All three creates land the resource in the environment + project (asserted via labels/forward pointers); foreign environment refused per convention; omitted ⇒ unchanged.
