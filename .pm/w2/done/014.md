# Blueprint by-id read parity: REST GET /v1/blueprints/{id} + MCP get_blueprint

Render's public API has `GET /blueprints/{blueprintId}` (and a `/blueprints/{blueprintId}/syncs` list — verify its shape in the fresh spec). bex's by-id blueprint read is GraphQL-only (`lego/backend/internal/apps/graphql.go:779`); REST has only validate/list/sync (`apps/rest.go:1079-1114`) and MCP has no `get_blueprint` tool. Add the REST route + MCP tool (and check the syncs-list shape) — ~1h; promote to a milestone or fold into `w2/m41` (add-task) at pickup.

Source: `/pm-brainstorm` round 14, 2026-07-15 (consistency miner, finding B1).
