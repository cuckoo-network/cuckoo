---
id: w2/m74/t007
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m74/t005]
---

## Objective

Pin the contributor/allowed/loading relation matrix and run the dashboard gates that protect this milestone.

## Context

- Visual disabled state is insufficient: tests must prove denied handlers do not open dialogs or call mutations and that `can_operate` is not accidentally replaced by `can_create`.

## Steps

1. Extend environment, PostgreSQL, Key Value, and menu component tests with capability fixtures for contributor, viewer, allowed, and loading/unknown states.
2. Assert tooltip copy, keyboard/pointer behavior, no denied callbacks, and the independent sensitive-reveal boundary.
3. Run focused tests, the relevant dashboard suite, lint, and typecheck.

## Files

- `dashboard/src/features/{services,env-groups,databases,keyvalue}/components/__tests__/**`
- `dashboard/src/test/mocks/capabilities.ts`

## Acceptance criteria

- [x] Tests fail on either over-gating or under-gating, and dashboard test/lint/typecheck gates pass.

## Out of scope

- Backend authorization tests already covering the authoritative relations and build-filter coverage.
