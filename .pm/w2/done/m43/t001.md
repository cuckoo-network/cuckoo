---
id: w2/m43/t001
title: Add cursor/limit to GraphQL services + environments (and MCP list_environments); record whether MCP list_services arg-free is by-design
worker: worker2
status: todo
estimate: 60m
depends_on: []
---

## Objective

Give the GraphQL `services` and `environments` queries `cursor`/`limit` args with REST-identical semantics (the 89c42936 `StablePage` pattern from `postgres/graphql.go:356-360`), and the same for MCP `list_environments` (a bex extension, free of Render-MCP parity constraints). For MCP `list_services`, first check whether arg-free mirrors Render's official MCP server (the ledger records that rationale for `list_postgres_instances`) — if so, record parity-by-mirror and leave it; if not, paginate it too.

## Context

- `lego/backend/internal/apps/graphql.go:688-695` (services: `ownerId` only), `apps/mcp.go:47-51`.
- `lego/backend/internal/environments/graphql.go:51-59`, `environments/mcp.go:29-30,81-83`.
- REST semantics to match: `apps/rest.go:549-551` (default 20, cursor = name), `environments/rest.go:142-143`.

## Steps

1. Copy the datastore GraphQL pagination pattern to services + environments.
2. Add `cursor`/`limit` to MCP `list_environments` args.
3. Resolve the `list_services` by-design question against docs/ADR018-render-parity.md's MCP evidence; record the decision in the ledger row or implement.
4. `cd lego/backend && go build ./... && go test ./...`.

## Files

- `lego/backend/internal/apps/graphql.go`, `apps/mcp.go`
- `lego/backend/internal/environments/graphql.go`, `environments/mcp.go`
- docs/ADR018-render-parity.md (decision record)

## Acceptance criteria

- [ ] GraphQL walks terminate with full coverage and match REST page composition.
- [ ] The `list_services` decision is written down with evidence, whichever way it goes.

## Out of scope

- REST routes (already paginated) and dashboard query updates beyond compatibility.
