---
id: w2/m60/t007
title: Simplify the correctness fixes
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m60/t006]
---

## Objective

Run `$simplify` over m60 and consolidate predicate, monotonic-size, readiness, and validation logic where doing so preserves behavior.

## Context

- App already has a deletion-aware predicate that Database can reuse.
- Postgres and Valkey need parallel grow-only semantics but different mechanisms.

## Steps

1. Run the simplification workflow over changed code.
2. Remove duplicate event/size/condition helpers without coupling unrelated controllers.
3. Re-run all behavioral tests.

## Files

- `lego/operator/internal/controller/`
- `lego/backend/internal/apps/`
- `lego/backend/internal/postgres/`
- `lego/types/`

## Acceptance criteria

- [x] Shared semantics have one tested implementation where appropriate.
- [x] No lifecycle or parity behavior changes during simplification.

## Out of scope

- A general operator framework or data-plane engine adapter.
