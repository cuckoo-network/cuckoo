---
id: w1/m25/t009
title: Test coverage — meaningful tests for each endpoint's real-data behavior + failure modes
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m25/t007]
---

## Objective

Add meaningful tests for the five new observability endpoints, asserting real behavior and failure modes (e.g. instance unreachable, unauthorized caller), not tautological coverage.

## Steps

1. Add unit/integration tests for each new verb (processes, top-queries, sizes, table-scans, parameter-overrides).
2. Cover access-denied and instance-unreachable failure paths.

## Acceptance criteria

- [ ] Tests assert real data shapes and fail on regression, not just "returns 200".
- [ ] Failure-mode paths (auth denial, unreachable instance) are covered.
