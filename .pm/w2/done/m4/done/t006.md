---
id: w2/m4/t006
title: Simplify — /simplify over the code this milestone changed
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m4/t005]
---

## Objective

Standing closing task. Run `/simplify` over what this milestone touched (delete verb + fragments, create-verification fixes, store delete helper) — reuse, simplification, efficiency; behavior-preserving only.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff; accepted findings applied; `go test ./...` green in `lego/backend`.
