---
id: w2/m4/t008
title: Closeout — DoD met → move milestone to done/
worker: worker2
status: done
estimate: 10m
depends_on: [w2/m4/t007]
---

## Objective

Standing closing task, run last and only when the definition of done actually holds (live create→URL→delete loop proven, create verified against the spec, all surfaces consistent). Close the milestone: set remaining tasks `status: done`, move each `tNNN.md` to `w2/m4/done/`, mark every row `— **DONE**` + `**Status:** done` in the milestone README, move `w2/m4/` whole to `w2/done/m4/`, and check the box in `w2/README.md`.

## Acceptance criteria

- [ ] DoD verified before the move; board state synced in all three places; `w2/m4/` no longer exists at its old path.
