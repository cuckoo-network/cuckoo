---
id: w2/m13/t006
title: "Render parity: healthCheckPath consistency across REST/GraphQL/MCP/UI"
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m13/t005]
---

## Objective

Verify `healthCheckPath` field name, semantics, and error shapes are consistent across REST, GraphQL, MCP, and the dashboard, and compare against Render's own Health Check Path behavior; update `docs/ADR018-render-parity.md`'s health-checks row.

## Context

- `docs/ADR018-render-parity.md` line 21 — the health-checks row this milestone closes.
- `docs/render-artifacts/` — t004's live capture of Render's Health & Alerts section.

## Steps

1. Diff field name/semantics/defaults across the four surfaces this milestone touched.
2. Compare against the Render capture from t004 — note any deliberate divergence.
3. Update the ADR018 row: move GraphQL/MCP/UI off ✖.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] ADR018's health-checks row reflects the shipped surfaces with evidence pointers.
- [ ] Any drift from Render's behavior is documented, not silent.

## Out of scope

- Building anything Render does that this milestone didn't scope (e.g. alert notification settings).
