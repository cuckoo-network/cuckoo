---
id: w2/m13/t002
title: "GraphQL: updateService mutation gains healthCheckPath"
worker: worker2
status: done
estimate: 25m
depends_on: [w2/m13/t001]
---

## Objective

Add `healthCheckPath` to the GraphQL `updateService` mutation (or equivalent), mirroring the REST update path t001 shipped, so GraphQL clients (including the dashboard) can read and write it.

## Context

- `lego/backend/internal/apps/graphql.go` — GraphQL schema + resolvers for services; `createService`/`updateService` mutations.
- t001's REST handler is the field-mapping reference — reuse the same underlying service-layer call rather than duplicating logic.

## Steps

1. Add `healthCheckPath` to the `updateService` mutation's input type and resolver, writing through the same internal call t001 uses.
2. Confirm the `service`/`server` query already returns `healthCheckPath` on read (it should, since create already threads it) — add it if missing.

## Files

- `lego/backend/internal/apps/graphql.go`
- generated GraphQL schema/codegen output (`make` codegen target if one exists for the backend)

## Acceptance criteria

- [ ] `updateService(id: ..., healthCheckPath: ...)` updates the field and is reflected on the next query.
- [ ] `dashboard`'s generated GraphQL client types pick up the new mutation field after codegen.

## Out of scope

- MCP/dashboard UI — t003/t004.
