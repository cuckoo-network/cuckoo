---
id: w2/m13/t003
title: "MCP: expose healthCheckPath on the create/update tool"
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m13/t001]
---

## Objective

Expose `healthCheckPath` as an argument on bex's MCP service create/update tool(s), so agent clients can set it without dropping to REST.

## Context

- `lego/backend/internal/apps/mcp.go` (or wherever the MCP tool definitions for services live) — `create_web_service` / `deploy` / any update tool.
- Reuse the same internal service-layer call t001/t002 use — MCP is a thin adapter per `docs/ADR006-bex-api.md`.

## Steps

1. Add `healthCheckPath` to the relevant MCP tool's input schema and wire it through to the same update call.
2. Confirm the tool's read-side (`get_service` or equivalent) already returns `healthCheckPath`.

## Files

- wherever `lego/backend/internal/apps/` defines its MCP tool schemas/handlers

## Acceptance criteria

- [ ] The MCP tool accepts `healthCheckPath` and the change is visible on a subsequent `get_service` call.

## Out of scope

- Dashboard UI — t004.
