---
id: w2/m80/t006
title: "Simplify: run /simplify over the changed code"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m80/t005]
---

## Objective

Run the standing /simplify pass over the milestone's changed code — reuse, simplification, efficiency, and altitude cleanups only (no bug hunting; that is /code-review's job).

## Context

Standing closing task. The main simplification pressure points expected from this milestone: duplicated dual-read fallback logic across the path helpers (t001), overlap between the new `migrate.go` walk and `nameclaims.go`'s `AuditNameClaims` sweep shape (t002), and dead code if t003 retired the `metaCache` snapshot (`envGroupMetaCacheTTL`, `sweepMeta`, `invalidateMetaCache` remnants).

## Steps

1. Run /simplify over the diff (`lego/backend/internal/envgroups/`, `lego/backend/internal/secrets/` if touched, `lego/backend/cmd/api/main.go`).
2. Apply accepted findings; in particular delete any now-unreferenced cache plumbing rather than leaving it behind a comment.
3. `cd lego/backend && go test ./...` green; `make lint-backend` (from `lego/operator/`) green.

## Files

- Whatever the diff touches — expected: `lego/backend/internal/envgroups/*.go`, `lego/backend/cmd/api/main.go`.

## Acceptance criteria

- /simplify findings triaged and applied or explicitly declined with a reason.
- No dead legacy-cache or duplicated fallback code left in the package.
- Backend tests + lint green.

## Out of scope

- Behavior changes; new features; refactors outside the milestone's diff.
