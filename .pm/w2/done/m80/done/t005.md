---
id: w2/m80/t005
title: "Prod migration runbook + explicitly authorization-gated cutover; annotate the ADR066 residual line"
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m80/t004]
---

## Objective

Write the production migration runbook (preconditions, dry-run, apply, verification, rollback) with an explicit operator-authorization gate before any prod mutation, and annotate ADR066's residual-register line to point at this milestone.

## Context

ADR066's residual register (docs/ADR066-security-review-round11.md:51) records "Env-group storage path migration (finding 3's structural fix) — explicit operator migration, same shape as w7/m70." The runbook is the deliverable that makes the migration operator-executable; actually running it against prod is NOT part of this milestone — it requires a fresh explicit user/operator authorization, and the runbook must say so in its first section. Precedent runbooks: docs/ADR036-ca-rotation-runbook.md, docs/ADR037-openbao-rekey-runbook.md, docs/runbooks/stripe-billing-setup.md.

## Steps

1. Write the runbook (suggested: `docs/runbooks/env-group-path-migration.md`, or a section in docs/ADR013-secrets.md if the docs owner prefers — pick one, don't do both): preconditions (t001–t003 build deployed, backup per ADR015/ADR031), dry-run invocation + expected report shape, apply, post-checks (the t004 verification sweep), rollback (legacy tombstones + OpenBao Raft snapshot restore), and the authorization gate up front.
2. Include the exact env-gated invocation from t002 and the `bao kv` inspection commands validated in t004.
3. Annotate docs/ADR066-security-review-round11.md:51 — append that the structural fix is implemented in w2/m80 with the cutover runbook reference (do not delete the line; the register is historical).
4. If ADR013's env-group storage description mentions the shared index, update it to describe the workspace-prefixed layout.
5. Markdown is CI-checked but this milestone's instructions say the closing prettier run happens at ship time per repo rules.

## Files

- `docs/runbooks/env-group-path-migration.md` (new) or `docs/ADR013-secrets.md`
- `docs/ADR066-security-review-round11.md` (residual-register annotation)

## Acceptance criteria

- Runbook covers dry-run/apply/verify/rollback and opens with the explicit authorization gate (no prod run without operator sign-off).
- ADR066:51 annotated with the w2/m80 reference.
- Docs consistent with the shipped path scheme.

## Out of scope

- Executing the prod migration; CLAUDE.md env-table edits beyond what t002 already justified.
