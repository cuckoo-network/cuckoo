---
id: w2/m61/t008
title: Complete restart residue rotation and timeout coverage
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m61/t007]
---

## Objective

Prove O-07–O-10 stay fixed under manager restart, transient cleanup failure, same-name recreation, Secret mutation, and stalled upstream endpoints.

## Context

- A happy-path delete test cannot prove retryability or artifact completeness.
- Credential and HTTP tests must not log real Secret material or rely on wall-clock hangs.

## Steps

1. Add inventory-based delete/recreate and removed-host residue tests.
2. Add persisted cleanup adoption across manager restart.
3. Add KeyValue rotation/mutation tests with authentication controls.
4. Add deterministic fake-server deadline/cancellation tests.
5. Run operator/backend test, lint, and targeted race suites plus delete audit.

## Files

- `lego/operator/internal/**/*_test.go`
- `lego/backend/**/*_test.go`
- `scripts/delete-audit.sh`
- `.github/workflows/operator-test.yml`

## Acceptance criteria

- [x] Every O-07–O-10 test fails when the corresponding fix is reverted.
- [x] Delete/recreate produces zero inventory residue after injected transient failures and restart.
- [x] Required test/lint/race gates pass without printing credentials; `delete-audit.sh` inventory and syntax are current, with live resource execution left to an explicitly named fixture.

## Out of scope

- Production deployment without a separate ship/deploy request.
