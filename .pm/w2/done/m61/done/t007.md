---
id: w2/m61/t007
title: Simplify cleanup and client plumbing
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m61/t006]
---

## Objective

Run `$simplify` over m61 and consolidate artifact selection, cleanup-result handling, Secret hashing, and bounded HTTP-client construction without changing behavior.

## Context

- Cleanup spans App, Database, build, publish, registry, and TLS helpers.
- Duplicated best-effort error handling caused O-07/O-08.

## Steps

1. Run the simplification workflow on all changed packages.
2. Centralize only semantics that truly share retry/absence rules.
3. Re-run residue, rotation, and timeout tests.

## Files

- `lego/operator/internal/controller/`
- `lego/operator/internal/build/`
- `lego/operator/internal/publish/`
- `lego/operator/internal/registry/`

## Acceptance criteria

- [x] Cleanup success/failure rules are explicit and not duplicated across call sites.
- [x] Simplification preserves every m61 failure-mode test.

## Out of scope

- A generic distributed transaction abstraction.
