---
status: done
---

# Persist MCP workspace selection in the shared Postgres store

- Add the schema migration and PGStore methods for session/subject/workspace selection.
- Wire the store through the API composition root without introducing a backend dependency into core feature packages.
- Cover upsert, read, identity isolation, and deletion/cascade behavior.
