---
status: done
---

# Add deterministic alternating-replica regression coverage

- Construct two independent MCP HTTP server instances that share only the persistence seam.
- Initialize on one replica, then alternate initialized notification, workspace selection, and a workspace-scoped tool call.
- Assert that no request returns `session not found` and that selection survives the replica switch.
