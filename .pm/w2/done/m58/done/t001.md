---
status: done
---

# Freeze the multi-replica session and identity contract

- Replace process affinity as a correctness requirement with replica-independent Streamable HTTP requests.
- Keep workspace selection keyed by both MCP session id and authenticated subject.
- Preserve a storeless in-memory fallback for local and stdio operation.
- Treat shared-store failures as errors, never as an implicit default-workspace selection.
