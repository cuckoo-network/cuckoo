---
status: done
---

# Make Streamable HTTP transport replica-independent

- Configure the MCP SDK's Streamable HTTP handler so follow-up requests do not require process-local session objects.
- Keep protocol initialization and session headers compatible with normal MCP clients.
- Ensure standalone transport behavior remains honest when a feature is unavailable in stateless mode.
