---
id: w1/m27/t009
title: Test coverage
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m27/t007]
---

## Objective

Standing closing task: add meaningful, CI-checkable coverage where feasible. Most of this milestone is infra/runbook work that isn't naturally unit-testable — favor a structural CI assertion over forcing a unit test that doesn't test anything real.

## Context

- Precedent for structural infra assertions: `lego/operator/internal/controller/tier_resources_test.go`'s pattern of asserting on rendered manifests rather than live infra.

## Steps

1. Add a structural test/check asserting `bex-postgres/cluster.yaml` declares `spec.backup.barmanObjectStore` (a manifest-parsing test, or a kustomize-build + assert check in CI) — so a future edit can't silently drop the backup config the way it was silently absent before.
2. Document the drill results from t003/t005/t006 as the "test evidence" for the restore paths themselves — these are one-time proofs, not automatable without standing infrastructure this milestone doesn't build.

## Files

- new test alongside `deploy/gitops/charts/bex-postgres/` (or wherever the repo's existing manifest-structure tests live)

## Acceptance criteria

- [ ] A CI-checkable test fails if `bex-db`'s backup config is ever removed/misconfigured.

## Out of scope

- Automating the restore drills themselves as recurring CI jobs.
