---
id: w2/m36/t010
title: Add cron `lastSuccessfulRunAt`
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m36/t005]
---

## Objective

Expose Render's `cronJobDetails.lastSuccessfulRunAt`, derived from the newest successful `status.runs` entry, alongside the first-class run API.

## Context

- Promoted from `.pm/w2/007.md` when m36 was picked up, as that inbox note requires.
- The timestamp format and successful status mapping are pinned in `docs/render-artifacts/cron-runs.md`.

## Files

- `lego/backend/internal/apps/{service.go,render.go,graphql.go}`
- dashboard GraphQL definitions

## Acceptance criteria

- [x] REST `serviceDetails.lastSuccessfulRunAt` and GraphQL `Service.lastSuccessfulRunAt` expose the newest successful run's RFC3339 finish time and omit it when none exists.

## Out of scope

- Persisting more than the operator's bounded recent run history.
