---
id: w2/m70/t001
title: Pin Render's current webhook contract and the supported/divergent event matrix
worker: worker2
status: done
estimate: 45m
depends_on: []
---

## Objective

Capture one dated, authoritative contract for Render webhook CRUD, delivery history, payloads, retries, and event names, then classify each difference as implement-now, existing bex extension, security divergence, or repository anti-goal so later tasks cannot silently choose incompatible semantics.

## Context

- Render sources: `https://render.com/docs/webhooks` and `https://api-docs.render.com/reference/{create,list,retrieve,update,delete}-webhook` plus `list-webhook-events`.
- Earlier evidence: `docs/render-artifacts/webhooks-ui.md`, `docs/render-artifacts/datastore-webhook-events.md`, and `w3/done/m27`.
- Current claims: `docs/ADR006-bex-api.md` § Outbound event webhooks and `docs/ADR018-render-parity.md` § Platform events & integrations / bex ahead.

## Steps

1. Record Render's current request/response schemas, empty-filter meaning, HTTPS requirement, attempt maximum, and complete 67-value API event enum with capture date.
2. Diff the contract against `internal/webhooks`, the served GraphQL vocabulary, MCP tools, and dashboard behavior.
3. Publish an exact disposition matrix, preserving mint-once secret and every `DO_NOT_DO` event-family boundary.

## Files

- `docs/render-artifacts/webhooks-api.md`
- `docs/render-artifacts/webhooks-ui.md`
- `docs/ADR006-bex-api.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [x] The artifact names all 67 current Render API event values and computes the bex overlap/missing/extension counts from code, not prose memory.
- [x] Every request, response, payload, retry, and dashboard gap in m70 has a source citation and explicit disposition.
- [x] No implement-now item conflicts with `.pm/DO_NOT_DO.md`; exclusions and the mint-once decision are explicit.

## Out of scope

- Implementing any contract change.
