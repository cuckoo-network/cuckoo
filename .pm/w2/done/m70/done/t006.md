---
id: w2/m70/t006
title: Reconcile delivery-attempt count and retry timing with Render's documented maximum
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m70/t001]
---

## Objective

Remove the likely initial-plus-eight off-by-one and pin the complete failure timeline so bex makes no more attempts than Render documents while retaining third-failure notification and final auto-disable behavior.

## Context

- Render currently documents a maximum of eight attempts, exponential backoff, and a final attempt around 33 hours after the first.
- `worker.go` has eight backoff entries after the initial attempt, yielding nine total attempts.
- `BEX_WEBHOOK_BACKOFF` must remain a deterministic accelerated verification seam.

## Steps

1. Derive and document the attempt/delay interpretation from the pinned current Render text.
2. Adjust the schedule/terminal comparison so total attempts, notification, and disable points agree.
3. Pin the entire clock-driven timeline in tests, including success on the last allowed attempt.

## Files

- `lego/backend/internal/webhooks/worker.go`
- `lego/backend/internal/webhooks/worker_test.go`
- `docs/ADR006-bex-api.md`

## Acceptance criteria

- [ ] Tests enumerate every allowed attempt and prove no ninth POST occurs under the current eight-attempt contract.
- [ ] The third failed attempt sends one outage notice; exhaustion records terminal failure, disables once, and sends the disable notice.
- [ ] The 15-second response budget and override parsing remain unchanged and tested.

## Out of scope

- User-configurable per-endpoint retry policies.
