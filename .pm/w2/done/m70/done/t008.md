---
id: w2/m70/t008
title: Render parity
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m70/t007]
---

## Objective

Re-run the dated Render comparison after implementation and prove that every supported webhook field and behavior is consistent across REST, GraphQL, MCP, and dashboard, with remaining differences recorded rather than hidden behind a ✅.

## Context

- Canonical expectations come from t001's artifact and `docs/ADR006-bex-api.md`'s one-core/thin-adapter rule.
- The official Render REST schema, GraphQL/MCP bex extensions, and dashboard must share core semantics even where their dialects differ.

## Steps

1. Execute the pinned REST compatibility fixture and compare dashboard flows to the current Render capture/docs.
2. Walk every changed core field/error through GraphQL and MCP.
3. Update ADR006/ADR018 with exact parity, superset, and deliberate-divergence statements and file any newly discovered gap separately.

## Files

- `docs/render-artifacts/webhooks-api.md`
- `docs/render-artifacts/webhooks-ui.md`
- `docs/ADR006-bex-api.md`
- `docs/ADR018-render-parity.md`
- `scripts/webhooks-verify.sh`

## Acceptance criteria

- [ ] Supported semantics and coded errors agree on REST, GraphQL, MCP, and dashboard.
- [ ] A real signed delivery verifies with a Standard Webhooks implementation and its history matches the REST event record.
- [ ] Documentation no longer claims Render lacks webhook APIs, a single-replica worker, or an unbounded endpoint count.

## Out of scope

- Implementing any new gap found during this review; file it with evidence instead.
