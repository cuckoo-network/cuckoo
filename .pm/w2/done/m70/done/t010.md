---
id: w2/m70/t010
title: Test coverage
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m70/t008]
---

## Objective

Close meaningful coverage gaps for event projection, cron observation, endpoint contracts, pagination/history, retries, authorization, and dashboard behavior, then run all suites affected by the milestone.

## Context

- Tests must assert failure modes and exact wire behavior, not snapshots or coverage-only branches.
- Changes span backend/store migrations and dashboard GraphQL consumers; integration gates may require the repository's established ephemeral Postgres/OpenFGA setup.

## Steps

1. Add table-driven source→payload and optional/empty request-contract tests, including replay and cross-workspace cases.
2. Add real-store pagination/time-filter/uniqueness tests and clock-driven retry exhaustion tests.
3. Add dashboard interaction tests for all-events, validation, labels, identity fallback, and paged activity filtering.
4. Run backend build/tests/lint and dashboard typecheck/lint/tests.

## Files

- `lego/backend/internal/webhooks/*_test.go`
- `lego/backend/internal/store/*_test.go`
- `lego/backend/internal/api/*_test.go`
- `dashboard/src/features/webhooks/**/__tests__/`
- `dashboard/src/routes/__tests__/`

## Acceptance criteria

- [x] Every Definition-of-done behavior has a non-tautological automated assertion, including failures and replay/idempotency.
- [x] `cd lego/backend && go build ./... && go test ./...` and the applicable lint target pass.
- [x] `cd dashboard && yarn typecheck && yarn lint && yarn test` passes.

## Outcome

Completed 2026-08-16. Coverage now pins source-to-event/status projection, observed cron replay, required/omitted/empty request semantics, case-insensitive name uniqueness, multi-owner and stable endpoint pagination, delivery time/status filters, bounded evidence, exact eight-attempt clocks, two-worker signature/dedup behavior, creator fallback, translated labels, all-events creation, and the dashboard's filtered paging behavior without automatic history crawling.

Verification: backend build and all package tests pass; `make lint-backend` reports zero issues; dashboard typecheck/lint pass (one unrelated pre-existing unused-disable warning), and all 308 files / 2,124 tests pass. Real-Postgres assertions are wired behind `BEX_TEST_DB_URI` for CI. A local disposable Postgres run was attempted, but this machine's Docker daemon hung during container start/exec; the exact test container was stopped and removed, so no local integration result is claimed.

## Out of scope

- Unrelated coverage cleanup.
