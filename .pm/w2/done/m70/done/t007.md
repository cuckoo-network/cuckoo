---
id: w2/m70/t007
title: Finish dashboard create/list/detail/activity parity over the corrected contract
worker: worker2
status: done
estimate: 75m
depends_on: [w2/m70/t002, w2/m70/t003, w2/m70/t004, w2/m70/t005, w2/m70/t006]
---

## Objective

Update the authenticated webhook pages to expose the corrected all-events, identity, endpoint, event-label, and delivery-history semantics without losing the existing grouped picker, mint-once secret step, or typed delete protection.

## Context

- `webhooks_.new.tsx` does not require a name and serializes All events as an explicit snapshot.
- List/detail chips show raw keys; the detail URL and enabled badge are not links; creator provenance is a subject UUID.
- Activity filters only loaded rows and cannot inspect Render's event timestamp/status code/response/error evidence.

## Steps

1. Make create/settings represent all-events distinctly, require name, support initially disabled creation where the contract exposes it, and render coded validation/quota conflicts.
2. Use catalog labels consistently in rows/chips, link the endpoint and Settings badge, and resolve creator email/avatar through the existing authorized identity lookup.
3. Render the corrected history fields with timestamp-range/status behavior and a bounded response/error detail affordance.
4. Preserve en/zh translations, SSR-safe route loading, and one-rail navigation invariants.

## Files

- `dashboard/src/routes/webhooks*.tsx`
- `dashboard/src/routes/webhook.$webhookId*.tsx`
- `dashboard/src/features/webhooks/`
- `dashboard/src/graphql/definitions.ts`
- `dashboard/src/features/webhooks/**/__tests__/`

## Acceptance criteria

- [ ] All-events remains future-inclusive after save/reload; blank/duplicate name and invalid URL errors are actionable.
- [ ] List/detail use human labels, clickable endpoint/settings controls, and creator identity rather than a raw subject when resolvable.
- [ ] Activity can distinguish/filter successful and failed history across pages and inspect bounded response/error evidence.
- [ ] Dashboard typecheck, lint, and focused webhook component/route tests pass in English and Chinese.

## Out of scope

- Replacing the global sidebar with Render's per-webhook sidebar or making the signing secret retrievable.
