---
id: w2/m70/t009
title: Simplify
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m70/t008]
---

## Objective

Run `/simplify` over the milestone's changed code and apply behavior-preserving reuse, clarity, and efficiency improvements before final coverage and closeout.

## Context

- Likely duplication seams include event-type/status projection, cursor envelopes, optional-versus-empty input handling, catalog labels, and response-evidence formatting.

## Steps

1. Run `/simplify` on the complete m70 diff.
2. Apply justified behavior-preserving improvements and re-run focused tests.
3. Record any intentionally retained complexity in the milestone closeout notes.

## Files

- `lego/backend/internal/webhooks/`
- `lego/backend/internal/store/`
- `dashboard/src/features/webhooks/`

## Acceptance criteria

- [x] `/simplify` has reviewed the full milestone diff and accepted improvements are applied.
- [x] No adapter reimplements core validation/projection semantics.
- [x] Focused backend and dashboard webhook tests remain green.

## Outcome

Completed 2026-08-16. Three parallel reviews covered reuse, clarity, and efficiency. The accepted pass moved REST list/time parsing onto shared `core` helpers, normalized and bounded distinct owner filters before authorization, consolidated delivery query parameters, made status/time filtering server-side (removing the dashboard's retained-history crawl), shared the auto-deploy audit constant, corrected stale contracts/comments, and batches replayed cron facts into one database round trip. Focused backend packages, the delivery component test, and dashboard typecheck pass.

Intentional retained complexity: `api/operations.ts` remains the repository's established handwritten typed-document fallback because the current GraphQL 17/codegen plugin crashes offline; the creator lookup continues to use the authorized cache-first workspace-members query because no narrower creator projection exists and the broader team hook would add invite reads and polling.

## Out of scope

- Behavior changes or unrelated refactors.
