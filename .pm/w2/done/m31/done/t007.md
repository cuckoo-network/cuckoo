---
id: w2/m31/t007
title: Test coverage — filter combinations, cursor pagination correctness, limit bounds
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m31/t005]
---

## Objective

Meaningful tests for the new filter/pagination behavior — no tautological tests.

## Steps

1. Store-layer: each filter individually and combined; cursor pagination actually advances through pages without gaps/dupes; `limit` is respected and clamped.
2. REST: malformed query params 400; valid ones match store-layer results.
3. GraphQL: same coverage as REST via the GraphQL args.
4. MCP: `limit` default/clamp behavior (1–100), cursor round-trips correctly.

## Files

- `lego/backend/internal/store/store_test.go` (or equivalent)
- `lego/backend/internal/deploys/service_test.go`/`rest_test.go`/`graphql_test.go`/`mcp_test.go` (whichever exist)

## Acceptance criteria

- [ ] Cursor pagination is proven gap-free and dup-free across a multi-page walk in a test.
- [ ] `go test ./...` green.

## Out of scope

- Live-cluster acceptance.
