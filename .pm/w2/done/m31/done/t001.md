---
id: w2/m31/t001
title: "store.ListDeploys: add status[]/createdBefore/createdAfter/cursor/limit"
worker: worker2
status: done
estimate: 40m
depends_on: []
---

## Objective

Extend the store-layer deploy query to support Render's real filter/pagination vocabulary — the subset bex can honestly back today. Render's `ListDeploysParams` also has `updatedBefore`/`updatedAfter`/`finishedBefore`/`finishedAfter` — **do not** add those here: bex has no `updated_at` column yet (that's `w2/m32`'s scope), and `finishedAt` already exists on the row but isn't in scope for this task's filter set unless it's a trivial addition once `createdBefore`/`createdAfter` are wired (use judgment: if adding `finishedBefore`/`finishedAfter` alongside `createdBefore`/`createdAfter` is nearly free given the same query-building code path, include it; if it meaningfully expands the task, leave it for a follow-up alongside `m32`'s `updatedAt` work).

## Context

- `lego/backend/internal/store/store.go` — wherever `ListDeploys`/the deploys query currently lives (likely a simple `SELECT ... WHERE app_id = $1 ORDER BY created_at DESC` with no other clauses today).
- `DeployStatus` values to filter by: bex's existing four (`update_in_progress`/`live`/`update_failed`/`canceled`) — `w2/m32` may add two more (`build_in_progress`/`build_failed`); this task's filter just needs to accept whatever status strings the store's own vocabulary recognizes, not hardcode today's four in a way that requires touching this file again when `m32` lands.
- Cursor: reuse whatever cursor convention `store.ListDeploys`'s sibling list queries (e.g. events, audit logs) already use — don't invent a new pagination idiom.

## Steps

1. Add a params struct (`ListDeploysParams` or similar) taking optional `Status []string`, `CreatedBefore`/`CreatedAfter *time.Time`, `Cursor *string`, `Limit *int`.
2. Build the `WHERE`/`ORDER BY`/`LIMIT` clause additively — an empty params struct must produce byte-identical output to today's unfiltered call (no regression for existing callers).
3. Reasonable default/max `limit` (mirror whatever convention other paginated store queries in this codebase already use).

## Files

- `lego/backend/internal/store/store.go`

## Acceptance criteria

- [ ] Calling `ListDeploys` with no params returns the same result as today.
- [ ] `Status`, `CreatedBefore`/`CreatedAfter`, `Cursor`, `Limit` each filter/paginate correctly, individually and combined.

## Out of scope

- `updatedBefore`/`updatedAfter` (needs `w2/m32`'s `updated_at` column first).
