---
id: w2/m34/t010
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m34/t009]
---

## Objective

Close the milestone once the definition of done actually holds: set all tasks `status: done`, move them to `done/`, update the milestone README status, move `w2/m34/` to `w2/done/m34/`, and check its box in the `w2` workstream README.

## Steps

1. Verify the definition of done in `README.md` is actually met — a real browser round trip through GitHub must be shown to work live, not just unit-tested.
2. Run `/pm done w2/m34/t010`.

## Files

- `w2/m34/README.md`, `w2/README.md`, all task files in `w2/m34/`.

## Acceptance criteria

- [x] Milestone moved to `w2/done/m34/`, all statuses in sync.

## Verification

- Production browser flow completed through the real GitHub App installation and returned to Settings with `bex-co` connected.
- Production Bearer callback returned HTTP `200` JSON with no redirect and preserved installation `90623475`.
- All task statuses, the milestone status, and the workstream checkbox are synchronized before moving the whole milestone directory to `w2/done/m34/`.

## Out of scope

- Any new implementation work.
