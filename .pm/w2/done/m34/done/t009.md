---
id: w2/m34/t009
title: Test coverage
worker: worker2
status: done
estimate: 1h
depends_on: [w2/m34/t007]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: state minting/verification (valid/missing/tampered/expired), the auth-gate exception's scope, and the workspace-scoped connect path.

## Steps

1. Unit tests for state minting/verification: valid, missing, tampered, expired cases.
2. Regression test that the auth-gate exception applies ONLY to the callback route, no other route is affected.
3. Integration test: full callback flow with a valid state records the connection against the correct workspace.

## Files

- `lego/backend/internal/github/rest_test.go`
- `lego/backend/internal/api/auth_test.go` (or equivalent)

## Acceptance criteria

- [x] Tests assert real failure modes (tampered/expired state rejected), not tautological checks.
- [x] `go test ./...` (backend) passes.

## Verification (2026-07-14)

- `state_test.go` covers valid round-trip, payload tampering, missing state, exact-expiry rejection, stateful install URLs, and explicit workspace binding.
- `auth_test.go` confines the anonymous exception to the exact callback method/path and exercises authenticated start → anonymous signed callback → correct persisted connection through the full handler stack with the production dashboard redirect configured. The same server then replays the credentialed Bearer callback and requires the legacy `200 {"status":"connected"}` JSON response with no redirect.
- `rest_test.go` covers missing/invalid/expired state, forged installations, bounded dashboard redirects, workspace-scoped persistence, and the authenticated callback's `200` JSON response with `DashboardURL` configured.
- Green gates: backend `go build ./...` and `go test ./...`; `make lint-backend` (0 issues); operator `make test`; dashboard `yarn typecheck`, `yarn lint`, `yarn test` (168 files / 1025 tests), and `yarn build`; `git diff --check`.

## Out of scope

- Load/performance testing.
