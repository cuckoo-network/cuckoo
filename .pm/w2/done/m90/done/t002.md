---
id: w2/m90/t002
title: Production browser acceptance — live terminal + fail-closed matrix
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m90/t001]
---

## Objective

Run the production Web Shell acceptance m55's closeout deliberately declined to claim: a real authenticated browser session on `dashboard.bex.co` gets a live terminal into a paid Running service, and every fail-closed leg refuses without exec.

## Context

- The dev-stack precedent and matrix shape: `.pm/w2/done/m55/evidence/2026-07-19-dev-2-live-acceptance.md` (happy path, pinned instance, bad/expired/replayed ticket, disabled, resolve-failure, per-identity cap).
- Dashboard page: `/services/{id}/shell` (xterm.js terminal + instance picker + copy-ready SSH command).
- ADR035 structural guarantees to re-assert: bex-api holds no `pods/exec`; `ssh_sessions` audits metadata only (no commands/terminal content).

## Steps

1. Happy path on a paid Running service: select a Ready instance, reach Connected, verify stdin/stdout echo, resize, and exit closure (kill the process or redeploy → terminal closes).
2. Fail-closed matrix: unauthorized identity, foreign-workspace member, free-plan service, suspended service, non-Ready target, replayed/expired ticket — each refused with a useful error and no exec.
3. Assert bex-api's ServiceAccount cannot create `pods/exec` and the new `ssh_sessions` rows carry metadata only.
4. Record sanitized evidence in `.pm/w2/m90/evidence/` (no tickets, no secret values, no tenant content).

## Files

- `.pm/w2/m90/evidence/` (new)

## Acceptance criteria

- [ ] Recorded production happy-path session (Connected → I/O → resize → exit closure)
- [ ] All six fail-closed legs refused with no exec
- [ ] `pods/exec` denial + metadata-only audit asserted in evidence

## Out of scope

- Any change to ticket semantics, session caps, or the gateway protocol

