---
id: w2/m68/t007
title: Render parity sweep (bex extension; cross-surface consistency)
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m68/t006]
---

## Objective

Standing closing task: Render has no agent-workspace/hibernation product, so record the bex-extension position in ADR018's ledger and enforce that the new fields/verbs (state, pinned, snapshot size, pin/unpin/delete) are semantically identical across REST, GraphQL, MCP, and the dashboard.

## Context

- Precedent: the usage API and agent-session rows already carry bex-extension entries in `docs/ADR018-render-parity.md`.
- Check error-code parity for the quota/pin refusals across surfaces; check the View field names match the ADR006 naming conventions.

## Steps

1. Cross-surface diff of the new fields/verbs.
2. ADR018 ledger row + gap-backlog note.
3. Prettier.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Ledger updated; any drift filed as a task before Closeout, not shipped silently.

## Out of scope

- Comparing to a Render behavior — none exists.
