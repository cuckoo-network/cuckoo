# Blueprint `initialDeployHook` one-time command

Render's Blueprint `initialDeployHook` runs a one-time command on a service's *first* successful deploy; bex records it as unsupported (`docs/ADR018-render-parity.md:49`; `docs/ADR006-bex-api.md:124`-era "ignored" list). Likely rides the `w1/m33` pre-deploy Job mechanism with a ran-once marker on the App/store. Small (~1-2h) — promote when picked up if it splits into multiple tasks.

Source: `/pm-brainstorm` round 12, 2026-07-15 (docs miner).
