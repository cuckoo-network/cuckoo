---
id: w2/m85/t006
title: Test coverage for wildcard/default-certificate invariants
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m85/t003]
---

## Objective

Prevent the fallback certificate, namespace trust boundary, or secret supply path from disappearing silently.

## Steps

1. Extend `scripts/gitops-validate.sh` with structural checks for the validated TLS Secret supply path, default TLSStore Secret reference, and prod-only wiring.
2. Assert Traefik's default TLS resources namespace is explicit and trusted.
3. Assert workflow/env/secret-uploader names agree and no certificate private key is rendered by GitOps.
4. Add a scheduled external `openssl`/HTTP synthetic for trust, wildcard SAN, expiry, and intentional 404; keep active/suspended control probes in closeout.

## Acceptance criteria

- [ ] Tests fail if TLS Secret validation or TLSStore wiring is removed/misnamed.
- [ ] Tests fail if namespace scoping, certificate inventory, or scheduled liveness drifts.
- [ ] Existing GitOps validation passes.
- [ ] Tests distinguish structural configuration from live issuance evidence.

## Out of scope

- Committing a real certificate or private key for tests.
