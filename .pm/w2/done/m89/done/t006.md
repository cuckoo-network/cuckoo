---
id: w2/m89/t006
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m89/t004]
---

## Objective

Standing closing task: fixture-test the validator's new assertion (build job referencing each deploy-secret class must fail; the real tree passes) and, if the digest handoff added scripting, unit-test its parsing/emission against malformed digests and multi-image outputs.

## Context

- Same shell-test harness as m88 t006 (the `scripts/` CI gate).

## Steps

1. Add failing fixtures per deploy-secret class + a passing real-tree check.
2. Test any digest-handling script edge cases.
3. CI gate green.

## Files

- `scripts/github-actions-validate.sh` fixtures/harness
- digest scripting tests (if any)

## Acceptance criteria

- [x] Each new assertion has a failing-fixture test; suite green in CI.

> Done 2026-09-07 — `scripts/github-actions-validate.test.sh` red fixtures per check-8 assertion: deploy secret in an image-build job, environment gate on an image-build job, workflow-header secret with an image build, bracket-form secret, annotated job key (fail-closed guard); greens: registry-token-only build job, deploy secret in a non-build job, plus the real-tree end-to-end pass. No digest scripting was added (handoff is native job outputs; the pin step's existing `^sha256:[0-9a-f]{64}$` validation covers malformed/empty outputs — empty strings iterate the quoted loop and fail loudly). Suite green locally and in CI (`scripts (test)` on `7c17e931e` and subsequent runs).

## Out of scope

- Live-pipeline assertions (t004 carried those).
