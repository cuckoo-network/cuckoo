---
id: w2/m89/t007
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m89/t005, w2/m89/t006]
---

## Objective

Close the milestone once every other task is done **and the definition of done actually holds** — a real production deploy shipped through the split pipeline, validator enforcing, ADRs closed.

## Context

- Standing closing task — completing it *is* the move.

## Steps

1. Verify the DoD clauses against evidence.
2. `/pm done w2/m89/t007` — statuses, task moves to `done/`, milestone to `w2/done/m89/`, README checkbox.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m89/`, `.pm/w2/README.md`

## Acceptance criteria

- [x] Milestone in `w2/done/m89/` with all three status locations in sync; DoD evidence recorded.

> Done 2026-09-08 (UTC) — DoD verified clause by clause: build steps read no deploy credential (YAML scope = `GITHUB_TOKEN` + OIDC only); deploy consumes the build's exact digests via job outputs with no mutable-tag re-resolution; validator check 8 fails closed on a deploy secret or environment gate in an image-build job (red fixtures prove it); one real production deploy shipped end to end (run `34171816919`, pin commit `5e0ff91ac`, in-cluster digest match asserted); ADR080 finding 2 + ADR083 follow-up 3 record closure with owner w2/m89.

## Out of scope

- Declaring done before the live split-pipeline deploy has run.
