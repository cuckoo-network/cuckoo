---
id: w2/m89/t005
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m89/t004]
---

## Objective

Run `/simplify` over the changed workflow, validator, and any pin scripting — reuse, simplification, efficiency; behavior-preserving.

## Context

- Standing closing task.

## Steps

1. `/simplify` scoped to the milestone diff; apply accepted cleanups.
2. `scripts/github-actions-validate.sh` stays green.

## Files

- whatever t001–t003 touched

## Acceptance criteria

- [x] Simplify pass run and findings applied or explicitly declined.

> Done 2026-09-07 — two review agents (reuse/consistency + correctness/quality) over the milestone diff. Applied: check 8 hardened against two verified bypasses (bracket-form `secrets['X']` refs; job keys with trailing comments now fail closed via an outside-job-header guard), stale `build-and-deploy` references updated in `lego/CLAUDE.md` + `dashboard/CLAUDE.md`. Declined with rationale: extracting a shared awk scaffold across checks 6/7/8 (three-copy inline pattern is this file's established idiom; extraction unwarranted until a fourth copy), comment-line false-positives (consistent with checks 6/7's existing fail-closed behavior), reordering fixture sections. The workflow split itself came back clean on all six correctness lenses (expression refs, job wiring, per-step tooling, write-back permissions, empty-output handling, Actions semantics).

## Out of scope

- Bug hunting; new behavior.
