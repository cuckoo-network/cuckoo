---
id: w2/m44/t003
title: REST imageUrl on the deploy body + GraphQL/MCP counterparts
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m44/t002]
---

## Objective

`POST /v1/services/{id}/deploys` accepts Render's `imageUrl` alongside the m30 body params (`commitId`/`clearCache`/`deployMode`), with GraphQL and MCP trigger verbs gaining the same argument.

## Context

- REST deploy-body decode from w2/m30; GraphQL/MCP trigger counterparts in `deploys/{graphql,mcp}.go` share `FilterOf`-style translators — keep the one-core-thin-adapters shape.

## Steps

1. Decode + pass through on REST; mirror on GraphQL mutation and MCP tool args (accurate descriptions).
2. Error mapping: the predicate's deny → Render-shaped 400 on all three surfaces.

## Files

- `lego/backend/internal/deploys/{rest,graphql,mcp}.go`

## Acceptance criteria

- [ ] The same imageUrl deploy round-trips through all three surfaces; deny case identical everywhere

## Out of scope

- Dashboard (the manual-deploy menu offering a tag prompt is w5 follow-up if the parity walk asks)
