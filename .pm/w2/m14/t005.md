---
id: w2/m14/t005
title: MCP — registry-credentials tools
worker: worker2
status: todo
estimate: 25m
depends_on: [w2/m14/t003]
---

## Objective

Expose registry-credential management as MCP tools, checking first whether Render's own official MCP server has equivalent tools before claiming this as a bex superset (don't over-assert parity direction without checking).

## Context

- Check `render-oss/render-mcp-server`'s tool list (the same reference `docs/ADR018-render-parity.md`'s Method section uses) for any registry-credential tool before writing the ADR note in the Render-parity closing task.
- Never expose the secret in a tool result after the initial create call.

## Steps

1. `list_registry_credentials`, `create_registry_credential`, `update_registry_credential`, `delete_registry_credential` tools, delegating to the same Core verbs as REST/GraphQL.
2. Note in this milestone's Render-parity closing task whether this is parity or a bex superset, based on the actual check.

## Files

- new `lego/backend/internal/registrycreds/mcp.go`

## Acceptance criteria

- [ ] Tools registered and callable; secret never appears in `list`/subsequent reads.

## Out of scope

- REST (t003) and GraphQL (t004).
