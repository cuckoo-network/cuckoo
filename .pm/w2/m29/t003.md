---
id: w2/m29/t003
title: "GraphQL: dryRun arg on the equivalent mutations"
worker: worker2
status: todo
estimate: 35m
depends_on: [w2/m29/t001]
---

## Objective

Add a matching `dryRun: Boolean` argument to the GraphQL `createService`, `updateServicePlan`, `createDatabase`, `createKeyValue` mutations (and their update equivalents), returning the same preview shape as the REST adapter.

## Context

- `lego/backend/internal/apps/graphql.go`, `lego/backend/internal/postgres/graphql.go`, `lego/backend/internal/keyvalue/graphql.go`.
- Reuse the t001 core dry-run mechanism — this is schema + resolver wiring, not new logic.

## Steps

1. Add `dryRun: Boolean` arg to each mutation listed above.
2. Wire the resolver to call `core.WithDryRun(ctx)` when set, and shape the mutation's return type to carry a preview payload distinguishable from a real create result.
3. Regenerate GraphQL schema/types (codegen) and update the dashboard's generated client types (no dashboard UI change required — codegen only).

## Files

- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/postgres/graphql.go`
- `lego/backend/internal/keyvalue/graphql.go`
- Generated schema/codegen artifacts (backend + dashboard)

## Acceptance criteria

- [ ] `createService(dryRun: true, ...)` returns a preview payload; a subsequent `services` query shows no new service.
- [ ] Codegen is clean (no drift between schema and generated types).

## Out of scope

- REST (t002), MCP (t004).
