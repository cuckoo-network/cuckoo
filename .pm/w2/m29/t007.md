---
id: w2/m29/t007
title: "Simplify — /simplify over the code this milestone changed"
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m29/t006]
---

## Objective

Run `/simplify` over every file this milestone touched and apply behavior-preserving cleanups (reuse, simplification, efficiency).

## Context

- Scope: `internal/core/dryrun.go`, and the dry-run additions to `internal/{apps,postgres,keyvalue}/{service,rest,graphql,mcp}.go`.

## Steps

1. Run `/simplify` over the diff.
2. Apply and verify fixes (`go test ./...`, `go vet`, `make lint`).

## Files

- Whatever `/simplify` flags.

## Acceptance criteria

- [ ] `/simplify` run completed; findings applied or explicitly declined with reason.
- [ ] Backend test suite + lint green after changes.

## Out of scope

- New feature work.
