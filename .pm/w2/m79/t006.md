---
id: w2/m79/t006
title: "Simplify: run /simplify over whatever code changed (CLI bump fallout)"
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m79/t005]
---

## Objective

Standing closing task: run the /simplify pass over the code this milestone actually changed — expected to be small (the `lego/cli` pin bump and any launcher-seam re-alignment from t003), possibly nothing beyond docs.

## Context

- /simplify reviews changed code for reuse, simplification, efficiency, and altitude cleanups — quality only, not bug-hunting.
- This milestone is documentation-heavy; the only code surface is `lego/cli` (go.mod/go.sum pin, possibly `main.go` version-intercept re-alignment). Doc-only diffs make this a fast no-op pass.

## Steps

1. `git diff` the milestone's working set; identify changed Go files (if any).
2. Run /simplify over them; apply accepted cleanups.
3. Re-run `cd lego/cli && go test ./...` if anything changed.

## Files

- lego/cli/ — whatever t003 touched

## Acceptance criteria

- [ ] /simplify run over all changed code (or recorded as no code changed)
- [ ] CLI tests still green after any cleanup

## Out of scope

- Simplifying code this milestone didn't touch; bug fixes (file them).
