---
id: w2/m79/t005
title: "File each newly found gap as an inbox note or milestone candidate; summarize the re-baseline in this README"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m79/t004]
---

## Objective

Turn every gap discovered in t001–t004 into a durable board item — an inbox note (`wN/NNN.md`) for sub-hour items, a milestone candidate note for larger ones — and write the re-baseline summary into this milestone's README so the outcome is auditable.

## Context

- The milestone's contract: drift becomes **filed work**, never silent fixes and never silently dropped findings. `.pm/CLAUDE.md` conventions govern note format and placement; sub-hour work stays an inbox note per DO_NOT_DO's minimum-bar rules.
- Gaps that map to DO_NOT_DO `—`/`[-]` non-goals are NOT filed as work — they are already recorded in the two ledgers as deliberate divergences; double-check none slipped into the filing list.
- Upstream-CLI bugs (like the recorded v2.21.0 instance-picker and `skills` panics) are noted in the checklist, not filed as bex work.

## Steps

1. Collect the consolidated gap list from t001/t002 (ADR018 drift) and t004 (checklist gaps).
2. For each: decide inbox note vs milestone candidate (size + dependency rationale); write the note under the owning workstream (`.pm/wN/NNN.md`), citing the ledger row as evidence.
3. Cross-link: each filed note references the ADR018 row / checklist item; the ledger entries already record the divergence itself.
4. Update this README: re-baseline summary (rows re-verified/changed/added, CLI old→new pin, count of filed items with their note ids), and mark t001–t005 status.

## Files

- .pm/w2/m79/README.md — summary
- .pm/wN/NNN.md — one per filed gap (workstream chosen per owning area)

## Acceptance criteria

- [ ] Every gap from the t001+t004 lists is either filed (with a note id) or mapped to an existing recorded non-goal/divergence — zero unaccounted
- [ ] README summary names the CLI pin transition and the filed note ids
- [ ] No filed item conflicts with DO_NOT_DO

## Out of scope

- Implementing any filed item; promoting candidates to milestones (that's a later /pm decision).
