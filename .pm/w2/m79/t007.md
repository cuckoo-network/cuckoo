---
id: w2/m79/t007
title: "Test coverage: CLI launcher suite green on the bump; regression tests for any trivially small wire-shape fix"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m79/t005]
---

## Objective

Standing closing task: prove the pin bump with the full launcher suite and validator, and — only where t004 surfaced a wire-shape divergence so small that fixing it is cheaper than filing it — land that fix WITH a regression test. Anything bigger stays filed (t005), not fixed.

## Context

- `cd lego/cli && go test ./...` is CI-enforced (`.github/workflows/cli-test.yml`); `scripts/bex-cli-validate.sh` cross-checks `UPSTREAM_RENDER_CLI.md` against `go.mod` so an unrecorded pin bump cannot pass CI.
- The launcher's hermetic tests cover Bex default mapping, process-level authenticated REST behavior, and Render-config isolation — the seams most exposed to an upstream bump (config schema, root command shape, version intercept).
- "Trivially small wire-shape fix" precedent: single-field accept/emit adjustments in the REST adapter with a focused regression test (e.g. the w5/m49 `image.ownerId:""` acceptance). If a fix needs schema/behavior design, it is not trivial — file it.

## Steps

1. Run `bash scripts/bex-cli-validate.sh` and `cd lego/cli && go test ./...`; fix any launcher-test fallout from the bump (that fallout is in scope — it IS the bump).
2. Review t004's gap list for at-most-one-or-two genuinely trivial wire-shape items; for each, land the fix in `lego/backend` with a focused regression test, and update the corresponding checklist row + t005 filing (withdraw the note or mark it fixed-here).
3. If backend code changed: `cd lego/backend && go test ./...` and `make lint-backend` (from `lego/operator/`).

## Files

- lego/cli/ — launcher tests
- lego/backend/ — only for accepted trivial fixes + their regression tests
- docs/cli-compatibility-checklist.md — row updates for anything fixed here

## Acceptance criteria

- [ ] `scripts/bex-cli-validate.sh` + CLI suite green on the new pin
- [ ] Every fix landed here has a regression test and an updated checklist row
- [ ] Everything non-trivial remains filed, not fixed

## Out of scope

- Feature work on filed gaps; operator changes; new e2e infrastructure.
