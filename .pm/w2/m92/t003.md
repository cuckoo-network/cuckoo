---
id: w2/m92/t003
title: "Phase 2 (STOP-gated): registry-migrate --apply — copy, verify, tombstone"
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m92/t002]
---

## Objective

With explicit user authorization recorded, execute runbook phase 2 against the t001 worklist: copy every legacy artifact to its workspace-scoped name, digest-verify each copy, and tombstone the legacy entry — **no blob deletion** (that is phase 4, after the clean window).

## Context

- **STOP:** `docs/runbooks/registry-static-identity-migration.md` § Phase 2 — "requires explicit operator authorization before touching live tenant data." Do not start until the user has authorized this phase in a change window; record the authorization (date + wording) in this task file before running.
- Tool: `lego/operator/cmd/registry-migrate` — `--apply` is the mutating switch; dry-run is the default and remains subordinate to the STOP.
- Rollback (per runbook): annotation off; legacy remains authoritative — dual-read means a failed/partial phase 2 leaves serving unchanged.

## Steps

1. Confirm + record the user's phase-2 authorization.
2. Re-run the dry-run immediately before `--apply`; diff against t001's worklist (catch drift since the inventory).
3. Run `--apply`; verify: every copy digest-matches, tombstones present, zero blob deletes, dual-read serving unchanged for every affected App.
4. Record results (counts, digests summary, anomalies) in `.pm/w2/m92/evidence/phase2-apply.md`.

## Files

- `.pm/w2/m92/evidence/phase2-apply.md` (new)

## Acceptance criteria

- [ ] User authorization recorded in this file before execution
- [ ] Every worklist artifact copied + digest-verified + tombstoned; zero blob deletes
- [ ] All affected Apps still serving/pulling correctly (dual-read) after phase 2

## Out of scope

- Redeploys (t004); blob deletion / dual-read removal (phase 4)
