---
id: w2/m92/t006
title: Simplify
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m92/t005]
---

## Objective

Run `/simplify` over whatever code this milestone changed — expected to be small (any `registry-migrate` / inventory-script fixes the rehearsal or live run surfaced); if the milestone shipped zero code changes, record that and close.

## Context

- Expected surface: `lego/operator/cmd/registry-migrate`, `scripts/verify-workspace-scoped-identity.sh`, docs.

## Steps

1. `/simplify` over the milestone's diff; apply safe wins (or record "no code changed").

## Files

- Whatever t001–t005 touched

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly dismissed with reasons (or no-code-change recorded)

## Out of scope

- Behavior changes
