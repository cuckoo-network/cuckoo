---
id: w2/m59/t007
title: Simplify the isolation implementation
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m59/t005, w2/m59/t006]
---

## Objective

Run the repository simplification workflow over m59's implementation and remove duplicated identity, Pod-hardening, and policy construction without changing behavior.

## Context

- BuildKit, kpack, publish, and pre-deploy have sibling Pod-template paths.
- Security fields drifting between variants caused O-01.

## Steps

1. Run `$simplify` over the changed operator and GitOps code.
2. Consolidate shared secure defaults where ownership and semantics are identical.
3. Re-run the m59 behavior checks after refactoring.

## Files

- `lego/operator/internal/build/`
- `lego/operator/internal/predeploy/`
- `lego/operator/internal/publish/`
- `deploy/gitops/base/`

## Acceptance criteria

- [ ] Shared hardening and identity defaults have one obvious source where practical.
- [ ] No security assertion or positive build path regresses after simplification.

## Out of scope

- Behavior changes beyond m59.
