---
id: w2/m78/t009
title: "Closeout: verify DoD, mark done, move milestone to done/"
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m78/t008]
---

## Objective

Standing closing task: verify every Definition-of-done clause with evidence, close the ADR069 follow-up line, sync all board status, and move the milestone to `done/`.

## Context

Per `.pm/CLAUDE.md`, a finished milestone moves whole to `w2/done/m78/` with no tombstone left behind, and status must be synced in three places: task frontmatter, milestone README (`**Status:**` + `— **DONE**` rows), and the w2 workstream README checkbox. The DoD also requires annotating `docs/ADR069-security-review-round14.md` line 114 (the "Board item" follow-up) as owned/closed by this milestone.

## Steps

1. Walk the milestone README's Definition of done clause by clause; record evidence (test names, file paths) for each: consent-obtainable read-only token, consistent 403 on all three surfaces, four exempt shapes unaffected, guard-tested classification table, audit fields, ADR line annotated.
2. Annotate `docs/ADR069-security-review-round14.md` line 114: the operation-level scope matrix + authorization-decision audit follow-up is implemented by w2/m78 (cite the ADR012 §7 amendment).
3. Confirm suites green: `cd lego/backend && go test ./...`, `cd dashboard && yarn test`, `make lint` from `lego/operator/`.
4. Set every task's frontmatter `status: done` and move task files to `w2/m78/done/`; mark task-table rows `— **DONE**` and set the README `**Status:**` line.
5. Move the whole milestone directory to `.pm/w2/done/m78/` (no stub or redirect at the old path) and check the m78 box in `.pm/w2/README.md`.

## Files

- `.pm/w2/m78/` (→ `.pm/w2/done/m78/`)
- `.pm/w2/README.md` (checkbox)
- `docs/ADR069-security-review-round14.md` (follow-up annotation)

## Acceptance criteria

- Every DoD clause has recorded evidence; all suites and lint green.
- ADR069's follow-up line is annotated owned/closed.
- Status synced in all three places; the old milestone path no longer exists.

## Out of scope

- Any code change (defects found here reopen the owning task instead); shipping/committing (user-invoked `/ship` only).
