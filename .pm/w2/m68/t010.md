---
id: w2/m68/t010
title: Closeout (requires a live hibernate→rehydrate acceptance with recorded resume latency)
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m68/t009]
---

## Objective

Close the milestone only when the DoD's observable end state is real on a live cluster: an idle workspace hibernated (pod gone, encrypted snapshot present), rehydrated with uncommitted edits intact, resume latency recorded against the p50<~5s / p95<~15s budget, retention + pin behavior demonstrated. Record the evidence, then perform the closeout move.

## Context

- The m65 lesson: skipping live acceptance let two out-of-band gaps ship dark. Do not close on code-written; the evidence (latency numbers, object listing, restored-tree diff) goes in `.pm/w2/m68/evidence/`.

## Steps

1. Live walk: idle → hibernate → verify object + pod-gone → resume → verify tree + latency → retention/pin checks.
2. Evidence note; then `/pm done w2/m68/t010` last — it triggers the milestone move.

## Files

- `.pm/w2/m68/evidence/` (new)
- `.pm/w2/m68/` → `.pm/w2/done/m68/`; `.pm/w2/README.md`

## Acceptance criteria

- [ ] Live evidence recorded; milestone directory lives only under `w2/done/`; all three status locations agree.

## Out of scope

- New work — follow-ups become inbox notes.
