---
id: w2/m68/t009
title: "Test coverage: state machine, hibernate/rehydrate failure modes, retention, pin/quota"
worker: worker2
status: todo
estimate: 60m
depends_on: [w2/m68/t007]
---

## Objective

Standing closing task: meaningful tests for the shipped behavior and its failure modes across the whole D2 state machine.

## Context

- Highest-value seams: every legal/illegal state transition; capture-failure ⇒ legacy reap (no stuck pod, no orphan object); corrupt-snapshot ⇒ explicit re-clone fallback; checksum mismatch; retention boundary + dirty extension + pinned immunity; pin quota boundary + named refusals cross-surface; usage-dimension rows appear/seal correctly; Resume/Steer preserve uncommitted work; Cancel deletes outright from both Active and Hibernated.
- Reuse the fake store/lifecycle/clock fixtures; object storage behind a seam fake; no sleeps.

## Steps

1. Sweep the milestone diff for uncovered branches; prioritize data-loss and cost-leak directions (a deleted pinned snapshot, or an orphaned never-deleted object, are the worst cases).
2. All suites green.

## Files

- `lego/backend/internal/agentsessions/` + `store/` + `sandbox/` tests
- `dashboard/src/features/agent-sessions/` tests

## Acceptance criteria

- [ ] Every DoD clause has a direct test; data-loss and cost-leak directions both covered; suites green.

## Out of scope

- Coverage-gaming; the live acceptance itself (t010).
