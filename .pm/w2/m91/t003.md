---
id: w2/m91/t003
title: get_metrics — adopt Render's arg names; triage the 7 missing upstream args
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m91/t001]
---

## Objective

Make `get_metrics` accept Render's argument names — `resourceId` (bex: `resource`), `httpLatencyQuantile` (bex: `quantile`), `resolution` (bex: `resolutionSeconds`) — applying t001's alias policy, and triage the 7 upstream arguments bex's tool is missing: wire each one that maps onto bex's metrics service, and record a reasoned rejection for each that doesn't.

## Context

- `lego/backend/internal/metrics/mcp.go:33-39` — `Resource`, `ResolutionSeconds`, `Quantile`/`Quantiles` (the `Quantiles` overlay is a bex extension — keep it; it's additive, not divergent).
- Upstream contract: the m70 pin, `.pm/w1/done/m70/evidence/inventory.md` row 48 ("missing 7 args; requires renamed `resource`"); consult the pinned upstream `tools/list` capture in `w1/done/m70/evidence/` for the exact 7 names/schemas.
- The database `get_metrics` sibling shape at `metrics/mcp.go:124-129` shares the naming — decide whether the aliasing extends there for consistency (record either way).

## Steps

1. Apply t001's alias policy to the three renames (Render spelling accepted; precedence tested).
2. Enumerate the 7 missing args from the pin; for each: wire it to the metrics service if the capability exists, else record the rejection + reason (in this task and t004's inventory annotation).
3. Tests using Render's exact spellings, including a both-set precedence case.

## Files

- `lego/backend/internal/metrics/mcp.go` (+ its tests)

## Acceptance criteria

- [ ] `resourceId` / `httpLatencyQuantile` / `resolution` accepted and honored
- [ ] Each of the 7 missing args either wired (tested) or rejected with a recorded reason
- [ ] Bex extensions (`quantiles` overlay) unchanged

## Out of scope

- Metrics service capability additions beyond argument plumbing
