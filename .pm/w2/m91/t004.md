---
id: w2/m91/t004
title: Flip the parity-inventory classes; update ADR018 + the m70 evidence note
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m91/t001, w2/m91/t002, w2/m91/t003]
---

## Objective

Reclassify the three repaired tools out of `Divergent` in the MCP parity inventory, verify cross-surface consistency for the touched fields, and bring the written record in line: ADR018's "filed for repair" sentence becomes a done record, and the m70 evidence rows are annotated.

## Context

- Classes + guard: `lego/backend/internal/api/mcp_parity.go`, `TestMCPParityInventory` (fail-closed against silent regrowth — the reclassification must go through it, not around it).
- `docs/ADR018-render-parity.md` line 11 — the "Three are unintended and are filed for repair" sentence (the w3/m46 `trigger_deploy` parenthetical is the format precedent).
- `.pm/w1/done/m70/evidence/inventory.md` rows 46–48.
- Cross-surface check (folded here from the omitted standing Render-parity task): REST/GraphQL already spell these facts per Render (`diskSizeGb` on REST postgres create, `autoDeploy`/`buildCommand` on REST/GraphQL service create, metrics params) — confirm the MCP change did not introduce drift among the three surfaces.

## Steps

1. Update the classifications; run `TestMCPParityInventory` and adjust expectations deliberately (each flip named, not bulk-accepted).
2. Cross-surface spot-check the touched fields on REST + GraphQL vs Render's shapes; flag any drift found as follow-up, never silently.
3. Update ADR018 line 11 + the inventory evidence rows; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `lego/backend/internal/api/mcp_parity.go` (+ test)
- `docs/ADR018-render-parity.md`
- `.pm/w1/done/m70/evidence/inventory.md`

## Acceptance criteria

- [ ] The three tools no longer classify `Divergent`; guard green and still fail-closed
- [ ] ADR018 + inventory records updated
- [ ] Cross-surface consistency verified or drift filed as follow-up

## Out of scope

- The four accepted genuine differences and the three upstream-only tools
