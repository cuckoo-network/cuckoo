---
id: w2/m91/t001
title: create_postgres — accept Render's diskSizeGb casing; decide the alias policy for all three tools
worker: worker2
status: todo
estimate: 30m
depends_on: []
---

## Objective

Make `create_postgres` accept Render's `diskSizeGb` spelling (bex currently only accepts `diskSizeGB`), and decide once — for all three tools in this milestone — how legacy bex spellings are handled (kept as documented compat aliases with a precedence rule, or removed), so t003 applies the same policy.

## Context

- `lego/backend/internal/postgres/mcp.go:62` — `DiskSizeGB int32 \`json:"diskSizeGB,omitempty"\`` (Render's tool: `diskSizeGb`); tool description at `:116` names the wrong casing too.
- A Go struct field can't carry two json names — the alias needs a second optional field merged in code (Render spelling wins when both are set) or a custom unmarshal. Pick the simplest shape that the jsonschema surface renders honestly.
- The m70 pin is the upstream contract source: `.pm/w1/done/m70/evidence/inventory.md` row 46; classes live in `lego/backend/internal/api/mcp_parity.go`.

## Steps

1. Decide + record the alias policy (in this task file or the milestone README): alias-with-precedence vs removal.
2. Implement `diskSizeGb` on `create_postgres` per that policy; update the tool description.
3. Test with Render's spelling, bex's spelling, and both-set precedence.

## Files

- `lego/backend/internal/postgres/mcp.go` (+ its tests)

## Acceptance criteria

- [ ] `create_postgres` accepts `diskSizeGb` and applies it to the created spec
- [ ] Alias policy recorded and applied; both-set precedence tested
- [ ] Existing callers of `diskSizeGB` behave per the recorded policy

## Out of scope

- The `region` omission on the create tools (accepted genuine difference, inventory rows 42–45)
