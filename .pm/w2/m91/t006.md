---
id: w2/m91/t006
title: Test coverage
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m91/t004]
---

## Objective

Add meaningful tests for the repaired contracts: every accepted Render spelling proven end-to-end (arg → spec/query), both-set precedence proven per the alias policy, and the inventory guard proven still capable of failing (a deliberate misclassification trips it).

## Context

- Never game coverage: a test that sends Render's spelling but doesn't assert it reached the spec/query is worthless.
- Mutation spot-check precedent: w5/m86 t009 (flip the behavior, watch named tests fail).

## Steps

1. Fill any gaps t001–t003's inline tests left (per-tool Render-spelling round-trips, precedence, rejection reasons for unwired `get_metrics` args).
2. Mutation spot-check: break one alias merge and one classification; confirm named tests fail; revert.

## Files

- Tests beside the three mcp.go files + `api/mcp_parity.go`

## Acceptance criteria

- [ ] Render-spelling round-trips + precedence covered for all three tools
- [ ] Mutation spot-checks recorded (which mutation fails which test)
- [ ] Backend `go test ./...` + `make lint` green

## Out of scope

- Coverage for untouched tools
