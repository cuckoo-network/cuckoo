---
id: w2/m91/t002
title: create_static_site — wire autoDeploy + buildCommand to the existing CreateRequest fields
worker: worker2
status: todo
estimate: 30m
depends_on: []
---

## Objective

Add the two arguments Render's `create_static_site` has and bex's omits — `autoDeploy` and `buildCommand` — wiring them to the `CreateRequest` fields that already exist and already serve the other create tools. Pure wiring, no new capability.

## Context

- `lego/backend/internal/apps/mcp.go:449-466` — `createStaticSiteArgs` has neither field.
- `CreateRequest` already carries both: `BuildCommand` (`lego/backend/internal/apps/service.go:393`) and `AutoDeploy` (`:486`).
- Sibling pattern to match: `create_web_service`'s args (`apps/mcp.go:184` `buildCommand`, `:191` `autoDeploy`) — but the wire contract to honor is the **upstream** tool schema from the m70 pin (`.pm/w1/done/m70/evidence/inventory.md` row 47), not the sibling's convenience shape; where they differ, follow upstream.
- `publishPath` stays required — the inventory marks that half genuine.

## Steps

1. Add both args to `createStaticSiteArgs` with upstream-faithful names/types; thread through `toCreateRequest()`.
2. Verify `buildCommand` reaches the static-site build (w7/m41 threaded it on the other surfaces) and `autoDeploy` gates push-to-deploy as on other types.
3. Tests: create a static site via MCP with both args; assert the spec carries them.

## Files

- `lego/backend/internal/apps/mcp.go` (+ its tests)

## Acceptance criteria

- [ ] `create_static_site` accepts `autoDeploy` + `buildCommand` and the created App spec reflects both
- [ ] `publishPath` requirement unchanged

## Out of scope

- Any other `create_static_site` argument
