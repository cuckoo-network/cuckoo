---
id: w2/m8/t008
title: Simplify — /simplify over the milestone's diff
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m8/t007]
---

## Objective

Standing closing task. Run `/simplify` over the code this milestone changed (`lego/backend/internal/github`, `internal/store`, `internal/api`, dashboard settings card) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Follow-up fixes must keep `make test` / `make lint` (from `lego/operator/`) and dashboard checks green.

## Steps

1. `/simplify` over the milestone diff; apply accepted findings.
2. Re-run tests/lint.

## Files

- Whatever the review flags

## Acceptance criteria

- [ ] Review run; accepted findings applied; tests/lint green

## Out of scope

- Bug hunting (that's `/code-review`), new behavior
