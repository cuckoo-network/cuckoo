---
id: w2/m3/t002
title: E2B-compatible API shapes so E2B SDK/tooling transfers
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m3/t001]
---

## Objective

Make the sandbox API speak E2B's shapes — the same "compatibility transfers habits" play bex-api runs for Render — so existing E2B SDKs/tooling work against bex unchanged.

## Context

- Pillar 5: "E2B-compatible sandboxes." Mirrors the Render-compat approach in `docs/bex-api.md` (verify shapes against the upstream spec; serve them as an adapter over the same Core).
- Adapter-only: no second sandbox implementation — E2B shapes are presentation over t001's service.

## Steps

1. Map E2B's create/connect/close request+response shapes onto the sandbox service.
2. Verify field names/enums against E2B's API; serve under E2B-compatible routes.

## Files

- `operator/internal/api/` (E2B adapter over the sandbox service)

## Acceptance criteria

- [ ] An E2B-shaped client can create and connect to a sandbox against bex without code changes.

## Out of scope

- Idle-hibernate (t003); full E2B feature parity (filesystem/proc APIs) — scope later if needed.
