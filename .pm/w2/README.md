# w2 — project workstream (worker2)

**Worker:** worker2. This is a general-purpose bex workstream. It may take work anywhere in the project; the milestones below are scheduled work and historical records, not a permanent purpose, specialty, or ownership boundary.

## Local dev environment

Develop against `.pm/w2/dev-2/`, this worker's own isolated stack on the shared local kind/CAPD cluster — never the shared cluster's default `auth`/`bex-system` namespaces or standard ports (5173/4433/4445/8090/8091/5432), which any other worker's session may also be using. `dev-2` gets its own Kratos + Hydra + Mailpit (namespace `dev-2-auth`) and app namespace (`dev-2`), reusing the shared cluster's CNPG operator and bex operator, plus a locally-built `bex-api` on dedicated ports derived from N=2 (`dev-2/ports.env`) so it never collides with any other workstream's `dev-N`.

- `bash scripts/dev-env.sh 2 up` — bring it up (idempotent — safe to re-run)
- `bash scripts/dev-env.sh 2 status` — health check (processes, pods, HTTP) + a pass/fail verification inventory
- `bash scripts/dev-env.sh 2 down` — tear it down (leaves the shared cluster and every other workstream's `dev-N` untouched)
- `bash scripts/dev-env.sh 2 clean` — reclaim `logs/` and `bin/` (refuses while the environment is up)

`up` prints the dashboard command to point at it once bex-api is running. One shared implementation serves every workstream since `w1/m72`; `.pm/w2/dev-2/` keeps only `ports.env` (a generated record of the derivation), this README, and `.gitignore`.

## Milestones

- [ ] **m92** — ADR055 F2/F3: execute the production identity migration (phases 1–3, arm phase 4) (8 tasks) ← from `/pm-brainstorm for w2` 2026-09-07 #2 (approved same day): w2/m75 shipped the workspace-scoped identity code, dual-read, `registry-migrate`, and the STOP-gated runbook on 2026-08-18, but prod phases 2–4 never ran, so the security register's two HIGH findings (registry repos/users and static prefixes keyed by App name alone) stay open and the legacy artifact set grows with every deploy; inventory → scratch-App rehearsal → STOP-gated copy/verify/tombstone → STOP-gated redeploy → arm the 14-day phase-4 window with a filed follow-up; every mutating phase waits for explicit user authorization; Render parity omitted (platform storage identity, no wire change)
- [x] **m91** — Repair the three Divergent MCP tools (7 tasks) ← from `/pm-brainstorm for w2` 2026-09-07 #4: w1/m70's inventory filed `create_postgres` (`diskSizeGb` casing), `create_static_site` (missing `autoDeploy`/`buildCommand`), and `get_metrics` (renamed `resourceId`/`httpLatencyQuantile`/`resolution` + 7 missing args) for repair, but the filing had no open board item and all three are still wrong in code; standing Render-parity task omitted as redundant (the milestone is the parity work — m79 precedent; cross-surface check folded into t004)
- [x] **m90** — Web Shell production activation + WS-edge liveness (7 tasks) ← from `/pm-brainstorm for w2` 2026-09-07 #3: m55 shipped the browser terminal with all manifests checked in but the production WebSocket edge never activated (ADR018 still says "awaits activation"); install `bex-shell-ticket`, verify `wss://ssh.bex.co/shell`, run the production acceptance matrix, and add a WS-edge probe to `ssh-edge-liveness.yml` so the edge can't die silently (the w6/m132 lesson); Render parity omitted (activation of an already-shipped surface, no wire change)
- [x] **m89** — Secretless image build, credentialed deploy (ADR080 F2) (7 tasks; **DONE 2026-09-08 UTC** — `deploy.yml` split into a secretless `build` job (secret scope = registry-push `GITHUB_TOKEN` + cosign OIDC; five digests as job outputs) and an `environment: production-deploy`-gated `deploy` job consuming `needs.build.outputs.*` with no mutable-tag re-resolution; validator check 8 fails closed on any image-build job referencing another secret or carrying an environment gate, with red-fixture self-tests incl. bracket-form/annotated-job-key bypass hardening; ADR080 finding 2 + ADR083 follow-up 3 recorded closed; proven live end to end by run 34171816919 — pin commit `5e0ff91ac`, in-cluster digest match asserted — after three green runs that also proved the w1/m59 supersede protection under rapid concurrent pushes; en route root-caused and fixed a concurrent commit's deterministic backend-gate red (`42d4532a3`, NULL-safe machine-subject exclusion)) ← from `/pm-brainstorm for w2` 2026-09-01 #4: ADR083 §Follow-ups records ADR080 finding 2 "still open" — `deploy.yml`'s build stage runs repo-controlled code with deploy credentials in scope; split into a secretless build job + environment-gated deploy job consuming the exact built digest; Render parity omitted (pure CI)
- [x] **m88** — Split self-hosted CI runner pools by trust level (7 tasks; **DONE 2026-09-07** — label-level pool split live and fail-closed-validated with red-fixture self-tests; fleet recovered from full outage (Docker Desktop down, OrbStack zombie fleet crash-looping, 13 stale registrations purged; 7/7 `bex-ci` + 3/3 `bex-production` back online); green runs verified on both pools (`scripts (test)` 34160723241 on `bex-ci`; deploy 33994179845 `build-and-deploy` on `bex-production` runner `57789c8a59a8`); host separation + shared-runner credential rotation **rejected by user decision** — single-Mac fleet is a hard constraint, residual accepted, recorded as `.pm/DO_NOT_DO.md` `#RUNNER-HOSTS` across ADR083/ADR019/runbook/validator) ← ADR083 finding 3; Render parity omitted (pure CI/platform infra)
- [x] **m87** — New Service wizard runtime auto-detection (9 tasks) ← from `/pm-brainstorm for w2` 2026-09-01 #2: ADR018's gap backlog's only open unowned row (surfaced by `w6/m45` t004) — Render infers a repo's runtime and re-infers when Root Directory changes; repo-tree probe + manifest heuristic + GraphQL query + wizard pre-select that never clobbers an explicit choice; Render parity included (backend query + dashboard UI)
- [x] **m86** — Disk encryption-at-rest: enable the LUKS storage class (ADR082 D3) (8 tasks; **DONE 2026-09-03** — prereqs verified live (cryptsetup bundled in the csi-driver image; kubelet, not the CSI SA, reads the passphrase Secret), prod mechanism drill PASSED (LUKS1 aes-xts mount + online grow on a real Hetzner volume), latent ≥54-char Secret-name divergence fixed pre-tenant, env flipped in the prod overlay + validator contract block; operator-path App proof + prod snapshot-store arming + full runbook drill on the encrypted class all passed live — running-app snapshot, restore-discards-later-writes, purge; four latent snapshot defects fixed pre-tenant; admission policy gained the validator-pinned `dskbak-`/`dskrst-` DAC-caps carve-out) ← from `/pm-brainstorm for w2` 2026-09-01 #1: the LUKS class + per-App passphrase minting shipped OFF in w1/m83 with two unverified live prereqs; verify `cryptsetup` on the CAPH node image + hcloud-csi Secret access, flip `BEX_DISK_STORAGE_CLASS`, live-verify resize/snapshot/restore on an encrypted disk — every pre-flip disk stays plaintext forever, so the unencrypted estate grows until this lands; Render parity omitted (platform storage mechanism)
- [x] **m85** — Valid TLS for unknown `*.onbex.co` hosts (7 tasks; **DONE 2026-09-01** — provider-independent wildcard certificate supply and fail-closed validation shipped; Traefik default TLSStore serves a trusted Let's Encrypt `*.onbex.co` certificate for unknown hosts with HTTP 404, while an active App retained its exact certificate and HTTP 200 route) ← live re-check 2026-08-29: `notfound.onbex.co` returns the intended HTTP 404 but presents `CN=TRAEFIK DEFAULT CERT`; add a DNS-01 wildcard fallback certificate and Traefik default TLSStore without weakening per-App certificates; Render parity omitted (platform edge, no product wire surface)
- [x] **m84** — Delete user account: safe identity and workspace offboarding (11 tasks; **DONE 2026-08-29** — durable retryable deletion intent, safe delete/leave/block workspace disposition, subject credential and OAuth cleanup, Kratos-last convergence, localized Settings danger zone with matched skeleton, and isolated-stack failure/retry proof) ← user-directed research handoff 2026-08-29 for `https://dashboard.bex.co/settings`; a durable, retryable account-deletion workflow that blocks unsafe last-admin exits, deletes sole-member workspaces through the existing teardown, leaves shared workspaces intact, revokes subject credentials, and deletes the Kratos identity last
- [x] **m83** — Simplify the agent composer and de-duplicate Recents (7 tasks; **DONE 2026-08-29** — one repo/session attachment control, rail-only Recents on the default page, rail Archived/All links, query-driven history preserved, breakpoint-matched skeletons, and 2,848 dashboard tests green) ← from `/pm-brainstorm` 2026-08-28 findings 5+6: `/agents` renders three repo-attach affordances and two Recents lists at once, left over from w1/m64 + w4/m36 + w3/m44 + w5/m64.
- [x] **m82** — Controller ERROR spam: snapshot-pull label/RBAC + terminal Failed-build requeue (7 tasks; **DONE 2026-08-20** — snapshot-pull Secrets carry the protected label at create + backfill is non-fatal + gitops ClusterRole `get/create/patch`; terminal Failed builds quiesce — no re-`fail`, no re-dispatch after Job TTL reap via `terminalBuildFailureRecorded`, no double metering; marker-bearing status writes retry on conflict instead of `_ =`-swallowing; envtest pins quiescence/reap/no-double-meter + a conflict-injection test pins marker durability; `make test` + `make lint` green) ← live prod diagnosis 2026-08-20; handoff after platform-service log scan; expanded same day by the k9s/log inspection session (t006 no re-dispatch after Job reap — Failed apps re-ran full builds on the 22:14Z operator roll; t007 durable Failed marker vs swallowed status writes); Render parity omitted (operator-internal)
- [x] **m75** — ADR055 F2/F3: workspace-scoped registry and static-prefix identity migration (12 tasks; **DONE 2026-08-18** — labeled Apps mint `W/A` Zot repos / `app-W-A` users / `reg-pull-W-A` Secrets / `W/A/<rev>/` prefixes via `internal/identity`; unlabeled Apps stay byte-identical; dual-read + dry-run-default `registry-migrate` + STOP-gated runbook; already-published labeled static site keeps empty `status.staticPrefix` across reconcile (Finding 1); DoD amended to envtest + that transition test (shared-cluster operator image not replaced); ADR055 F2/F3 annotated not fully closed until phase 4; `make test`/`make lint` green) ← from `/pm-brainstorm for w2` 2026-08-18 round 1; the lineage's oldest unowned code deferral (ADR055 F2/F3 → ADR067 #1), sequenced before w7/m86 mints more legacy-named `-cache` repos; Render parity omitted (no wire-surface change)
- [x] **m76** — Web push (VAPID): browser push channel for dashboard and self-hosters (10 tasks; **DONE 2026-08-19** — VAPID in-tree RFC 8291/8292 transport, sibling `webpush_subscriptions` + REST/GraphQL (no MCP), shared ADR052 policy, dashboard `/push-sw.js` + settings panel, runbook with zero Apple/Google deps; Expo CHECKs unchanged; env unset ⇒ channel absent) ← from `/pm-brainstorm for w2` 2026-08-18 round 2; makes the ADR052 push channel reach real users while w11/m5 native push stays release-gated on Apple/Google credentials; the self-hoster path (ADR048 §98)
- [x] **m77** — ADR059 hibernation: production enablement + resume-SLO evidence (8 tasks; **DONE 2026-08-19** — bucket `bex-agent-snapshots` + Secret `bex-system/bex-agent-snapshot` armed; session `ags-da33092c0fus738gr25g` hibernated then Steer-rehydrated with marker restored; warm-node resume 3.12–3.25s vs p50<~5s / p95<~15s) ← from `/pm-brainstorm for w2` 2026-08-18 round 2; the enablement step w2/m68's DONE note recorded but never filed — provision `BEX_AGENT_SNAPSHOT_S3_*`, live hibernate→rehydrate walk, latency vs ADR059 SLOs; Render parity omitted (operational enablement, m68 carried the surface)
- [x] **m78** — ADR069 follow-up: operation-level OAuth scope matrix + authorization-decision audit (9 tasks; **DONE 2026-08-19** — `bex.read`/`bex.write`/`bex.sensitive` operation classes at REST/GraphQL/MCP dispatch; mint rides `AuthorizeMintClass`; classification table + guard; `INSUFFICIENT_SCOPE` refusals audited as `api.ScopeClass`; w8/m27 vocabulary unchanged, `r14_scope_test.go` unmodified) ← from `/pm-brainstorm for w2` 2026-08-18 round 1; ADR069 finding 1's recorded "board item" (read/write/mint scope classes over `AuthorizeMintClass`, classification guard test, consent/discovery, decision audit)
- [x] **m79** — Upstream drift re-baseline: Render API + official CLI (8 tasks) ← from `/pm-brainstorm for w2` 2026-08-18 round 2; dated ADR018 re-baseline + pinned `render-oss/cli` verify-or-bump (already v2.22.0) + checklist re-run; gaps filed, never fixed inline; the standing Render-parity closing task is omitted as redundant (the milestone is the parity work)
- [x] **m80** — ADR066 #3: env-group OpenBao workspace-prefixed path migration (8 tasks) ← from `/pm-brainstorm for w2` 2026-08-18 round 1; the deferred structural fix behind the round-11 quota/cache mitigation, mirroring w7/m70's tenant keying; Render parity omitted (behavior-preserving storage layout)
- [x] **m81** — metrics-server kubelet TLS: CSR approver + `--kubelet-certificate-authority` (7 tasks) ← from `/pm-brainstorm for w2` 2026-08-18 round 1; closes ADR072 #5 / ADR061 #11 / ADR063 #9 (the insecure flag already left the prod render in `815e003b`; the approver + CA verification are the missing halves); Render parity omitted (pure platform infra)
- [x] **m74** — Complete role-aware environment and datastore controls (8 tasks; **DONE 2026-08-18** — service/environment-group writes now fail early on `can_create` while reveal/export stays independently `can_view_sensitive`; PostgreSQL/Key Value delete uses `can_create` and plan/lifecycle uses `can_operate` across detail + accessible row menus; shared plan cards + one table-level capability observer; ADR018 parity recorded; 2,279 dashboard tests, lint/typecheck/build green; Build Filters excluded as directed) ← promotes the non-build slice of `w9/048` after w9/m84 capability plumbing and w2/m73's shared environment editor
- [x] **m73** — Environment groups: correctness, scoped workflows, and live Render parity (14 tasks; **DONE 2026-08-17** — generated values preserved end to end; CAS-unique workspace names + fail-closed legacy ambiguity; one revision-aware sparse env/file patch with save-only/deploy/rebuild and compensation; Environment scope/Move/compatible links; masked staged editor, dotenv import, safe export, server clone, searchable table, immediate delete convergence; authenticated live acceptance and all backend/dashboard/lint/race/codegen gates green; synthetic resources removed) ← user handoff 2026-08-17 from an authenticated Bex/Render audit
- [x] **m72** — Build supersede semantics: run-to-completion + latest-pending slot, executing ADR060 D1 (9 tasks; **DONE 2026-08-16** — non-blocking build observation replaces the synchronous 30-min poll (`build.EnsureBuild`/`Observation` mirroring `internal/predeploy`, `RequeueAfter` cadence); `prepareAppReleaseDecision` pins the release identity while a build runs so a newer push coalesces into the implicit pending slot and the running build always completes + rolls out — the cancel-the-running-build `CancelActiveBuilds` newest-wins is removed (dead code deleted); new `status.releaseArtifactFingerprint`/`pendingReleaseGeneration` (CRD codegen); outcome SLIs `bex_build_outcomes_total{outcome}` splitting `superseded`/`canceled`/`failed`/`timeout`/`succeeded` + `bex_build_run_seconds`; starvation-regression envtest proves run-to-completion + coalescing + convergence, `make test`/build/types/backend-store all green; the investigation found the operator's per-object serialization already ran single-App builds to completion, so the real wins are non-blocking workers + explicit correct semantics + no wasted superseded builds; t006 parity: wire vocabulary unchanged, superseded→`canceled` matches Render, one deferred backend deploy-history refinement flagged as `w2/018`) ← from TPM discussion 2026-08-16 (production deploy starvation: push cadence < build duration ⇒ newest-wins cancels every in-flight build, livelock with zero completed deploys) + [docs/ADR060-build-worker-reliability-and-performance.md](../../docs/ADR060-build-worker-reliability-and-performance.md) D1/D5: a running build always runs to completion, a newer push overwrites a single per-App pending slot (intermediate commits coalesce), image-pushed is the point of no return, explicit Cancel (w2/m10) still preempts; build state moves into App status, outcome-labeled SLIs split `superseded` from `canceled`; amends ADR034's cancel-active line, aligns with Render/Vercel queued-supersede behavior
- [x] **m71** — ADR065: Devin-style agent-session archive (13 tasks; **DONE 2026-08-16** — `archived_at` migration 0078 + idempotent archive/unarchive + fresh-authorized hard delete + seq-paginated transcript read + default-unarchived filtered/keyset-paginated lists across REST/GraphQL/MCP; replay-only attach tickets (empty pod triple, gateway never dials) make every reaped session's conversation viewable again incl. the pre-existing backlog; retention expiry auto-archives; Completer skips the idle grace for archived sessions; dashboard Archived sidebar entry + membership tabs + phase filter + load-more + per-row archive + delete-behind-confirmation; latent `HibernateAgentSession`/`SetAgentSessionPinned` timestamptz-cast bugs surfaced and fixed by the first real-PG hibernation coverage; backend + real-PG + lint + dashboard 2124 tests all green) ← from `docs/ADR065-agent-session-archive.md` (Proposed 2026-08-16; user handoff "hand off to /pm"): `archived_at` as an orthogonal list-state flag (never a phase) + archive/unarchive/fresh-authorized-delete verbs ×3 surfaces with `AGENT_SESSION_ARCHIVED` 409; replay-only attach tickets + a seq-paginated transcript read so completed sessions' conversations are viewable again (today the sole read dies when the Completer blanks `sandbox_id`, ≤30m after completion); default-unarchived filtered + keyset-paginated session lists; retention-expiry auto-archive; dashboard Archived section + per-row archive + filter/pagination controls + delete dialog. Refines ADR047 D9a's recorded "not ported" gap; composes with ADR051/ADR059
- [x] **m70** — Outbound webhooks: truthful events + Render wire/dashboard parity (11 tasks; DONE 2026-08-16 — 32 truthful events, observed cron facts, Render non-secret REST CRUD/history semantics, exactly eight delivery attempts, corrected dashboard activity, and full backend/dashboard gates green; deliberate source/product/secret/cap divergences documented) ← user-directed webhook parity audit, 2026-08-16; closes residual contract and event-projection gaps left after w3/m27 without reopening deliberate platform non-goals
- [x] **m59** — P0 operator isolation: contain untrusted build, publish, and pre-deploy execution (9 tasks) ← from `docs/ADR039-operator-audit-and-platform-reuse.md` O-01/O-02; security release blocker before real multi-tenant source builds
- [x] **m60** — P1 operator correctness: deletion, grow-only storage, immutable type, and live child health (9 tasks; DONE 2026-07-19 — metadata-only Database finalization/retry, monotonic Postgres and real Valkey PVC growth, immutable type admission/Core guard, current-child health watches, parity docs, full tests/lint/race/GitOps green) ← from ADR039 O-03–O-06; sequenced after m59 so security blockers close first
- [x] **m61** — P2 operator cleanup and resilience: artifact reclamation, durable finalization, credential consistency, and HTTP deadlines (9 tasks; DONE 2026-07-19 — exact-UID inventory, restart-adopted terminal cleanup, immutable KeyValue credential revision, bounded HTTP, parity/delete audit docs, full tests/lint/race green) ← from ADR039 O-07–O-10; follows m60's lifecycle model
- [x] **m1** — MCP server over bex-api verbs (4 tasks) ← pillar 3
- [x] **m2** — Deploy-from-chat + HMAC git webhook (4 tasks; DONE — t001–t003 2026-07-08, t004 live acceptance PASSED 2026-07-09) ← pillar 4, needs w2/m4 + w1/m2 + w1/m5 (t001 amended 2026-07-08 to ride m4's `Core.Create`)
- [x] **m4** — Render-shaped service create & delete: verify shipped create + build delete (8 tasks; DONE 2026-07-09 — delete on all 3 surfaces, create verified vs Render OpenAPI, live delete-cascade acceptance PASSED) ← from `/pm-brainstorm for w2` 2026-07-08, re-scoped 2026-07-08 (create half shipped via m2/t001)
- [x] **m5** — Deploy history + trigger (`list_deploys` · `get_deploy` · `POST /deploys`) (7 tasks; DONE — t005 live acceptance PASSED 2026-07-09) ← from `/pm-brainstorm for w2` 2026-07-08, needs w1/m2
- [x] **m6** — MCP `query_render_postgres` — read-only SQL for agents ← reassigned to **w5/m9** (label collision with w5's done m6) and shipped there 2026-07-08 (`w5/done/m9/`)
- [x] **m7** — Key Value API surface (REST/GraphQL/MCP, Render-shaped) (10 tasks) ← promoted from `003` 2026-07-09, unblocked by w1/m14 (mechanism live in prod); dashboard half is `w5/m12` — done (in `done/m7/`; row synced 2026-07-12, was stale)
- [x] **m12** — Render scale API (`POST /v1/services/{id}/scale`) (5 tasks) ← relocated from w1 2026-07-08
- [x] **m8** — Connect GitHub: GitHub App connection + repo listing (REST/GraphQL/MCP/UI) (10 tasks; DONE 2026-07-12 — live DoD PASSED against the real `bex-co` GitHub App on the mock cluster; dashboard card re-landed after the codegen fix) ← from `/pm-brainstorm for w2` 2026-07-11 (parity row "Git connections" ◐)
- [x] **m9** — Private-repo deploys + zero-config GitHub push-to-deploy (9 tasks; DONE 2026-07-12 — t005 live acceptance PASSED: private repo → live URL, hands-free push → rev-2, `autoDeploy:false` suppresses, tampered signature 401; follow-up `005.md` filed on store-path composition) ← from `/pm-brainstorm for w2` 2026-07-11, needs w2/m8; unblocks `w5/m15`
- [x] **m10** — Deploy cancel + rollback (9 tasks; DONE 2026-07-12 — REST/GraphQL/MCP `Cancel`/`Rollback` on all surfaces, `0011_deploy_rollback_target` migration, parity matrix refreshed, full test suite + lint green; live acceptance scenarios coded but not run against the mock cluster this session — its DB credential had drifted) ← from `/pm-brainstorm more` 2026-07-12 (the two ✖ Deploys rows m5 left; rollback target recorded per deploy); UI buttons ride `w5/007`
- [x] **m25** — Managed Postgres observability: processes · top-queries · sizes · table-scans · parameter-overrides (10 tasks; DONE 2026-07-12 — REST/GraphQL/MCP + dashboard InsightsPanel; `pg_stat_statements` enabled in CNPG spec; parameter-overrides write path through operator; parity row closed) ← from `/pm-brainstorm more` 2026-07-12 (extends w1/m17); moved to `done/m25/`
- [x] **m11** — Unify service creation through the control-plane store (9 tasks; DONE 2026-07-12 — all creates write store rows + mint clone secrets; deploy history populates for all surfaces) ← promoted from `005` 2026-07-12 (found during the m8/m9 live acceptance: private-repo store-managed builds fail with no clone secret; public creates have no deploy history)
- [x] **m13** — Health check path: full-surface parity (PATCH · GraphQL · MCP · dashboard) (9 tasks; DONE 2026-07-13 — MCP `set_health_check_path` tool + `healthCheckPath` on `create_web_service`; GraphQL `createService(healthCheckPath:)` arg; fixed `GetApp` relation arg in `SetHealthCheckPath`; dedicated test suite; ADR018 row closed) ← from `/pm-brainstorm more milestones to work on` 2026-07-12 (docs/ADR018-render-parity.md line 21 gap; folds `w5/009`)
- [x] **m26** — Harden the build-image pull path: Zot NodePort + containerd hosts.toml baked into CAPH template; GC/retention policy; ZotRegistryFillingUp alert; autoscaler generation-churn fix (annotation vs spec.replicas); migration-ownership guard + migration 0013 (DONE 2026-07-13 — all code shipped, live cluster verification deferred to next cluster cycle)
- [x] **m27** — Close the control-plane disaster-recovery gaps (10 tasks; DONE 2026-07-12 — bex-db barmanObjectStore backup config + ScheduledBackup; BexDbBackupStale alert + CNPG scrape; ADR031 consolidated backup/restore policy; ADR011/ADR015 stale warnings updated; gitops-validate.sh structural guard; restore drill procedures documented — operational drills require live cluster execution post-deploy) ← from `/pm-brainstorm more` 2026-07-12 (highest-severity unaddressed gap: bex-db had zero backup)
- [x] **m14** — Registry credentials: private external image registries (8 tasks; DONE 2026-07-13 — REST/GraphQL/MCP/dashboard all shipped; live acceptance PASSED against a real, auth-enabled self-hosted registry: authenticated pull succeeded, no credential leak to an unrelated pull; a real ordering bug found live — Secret materialized before the App had a UID — fixed by dropping the owner reference and adding an explicit delete, mirroring the clone-secret pattern; ADR018 registry-credentials row moved to ✅ across the board) ← from `/pm-brainstorm more milestones to work on` 2026-07-13 (`docs/ADR018-render-parity.md` "Registry credentials" row, verified live that Render genuinely supports this — Docker Hub/GHCR/GitLab/Google Artifact Registry + a real Update Registry Credential API); mirrors the `w2/m8` GitHub-App-credential → materialized-Secret pattern
- [x] **m15** — `/blueprints` verbs: validate · list · sync (DONE 2026-07-13 — REST+GraphQL+MCP, `blueprint_test.go`, ADR018 updated) ← from `/pm-brainstorm more for w2` 2026-07-13, re-homed from an earlier `w1`-targeted proposal (the validate-before-apply shape is agent-safety work, not platform infra); needs `w1/m24`
- [x] **m16** — Managed datastore plan/instance-type updates: Postgres + Key Value (11 tasks; DONE 2026-07-13 — `SetPlan` verb for Postgres + Key Value on REST/GraphQL/MCP + dashboard plan-picker cards; operator envtests confirm CNPG/Valkey resources re-reconcile on plan change; ADR018 parity rows updated) ← from `/pm-brainstorm more milestones to work on` 2026-07-13 (`docs/ADR018-render-parity.md` Managed Postgres row, "No `PATCH` update yet (◐, low)"); mirrors the `apps.SetPlan` pattern already shipped for App services
- [x] **m28** — Typed error codes: general envelope + plan-limit as first consumer (10 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13, promotes `w6/014` (filed by `w6/m15`'s `/simplify` pass); materialized under `w2` per user direction (originally proposed for `w6`, where the plan-limit case originates)
- [x] **m29** — Dry-run/preview mode for service, Postgres, and Key Value create/update (9 tasks; DONE 2026-07-13 — `DryRun` field on `CreateRequest`/`CreatePostgresRequest`/`CreateKeyValueRequest`; `PreviewSetPlan` on all three services; REST body+query-param, GraphQL `dryRun: Boolean` arg, MCP `dryRun` field on all create/update tools; 21 tests assert zero k8s CRs created/modified; full test suite green) ← from `/pm-brainstorm more milestones to work on for each everyone of workers` 2026-07-13; extends `w2/m15`'s `validate_bex_yml` dry-run idiom from the `bex.yml` path to direct create/update calls
- [x] **m30** — Manual Deploy body: commitId, clearCache, deployMode, restart consolidation (10 tasks; DONE 2026-07-14 — REST body decode, `clearCache=true` → 400, `commitId` → `App.spec.BuildCommit`, `deployMode=deploy_only` accepted/rejected by source type, `restartServer` GraphQL consolidated into deploys, dashboard restart → `triggerDeploy`; `/simplify` pass cleaned 7 issues) ← from `/pm-brainstorm` 2026-07-14
- [x] **m31** — ListDeploys pagination + filtering (REST/GraphQL/MCP) (8 tasks) ← from `/pm-brainstorm` 2026-07-14 (same audit as m30; bex's `list_deploys` MCP tool is less complete than Render's own real MCP tool, not just the REST/GraphQL surface)
- [x] **m33** — Deploy Hook: secret-URL deploy trigger (9 tasks) ← from `/pm-brainstorm more` 2026-07-13 (third pass; `docs/ADR006-bex-api.md:124` `initialDeployHook` field, "ignored"); numbered m33 not m30 — a concurrent session claimed m30-m32 first
- [x] **m34** — Fix GitHub Connect: browser install-callback can't record the connection (10 tasks; DONE 2026-07-14 — normal dashboard browser install/callback and authenticated Bearer callback both verified live in production; Bearer preserved HTTP 200 JSON with no redirect and the existing `bex-co` installation) ← from `/pm-brainstorm more` 2026-07-13 (fifth pass; `docs/ADR026-github-integration.md` line 88 "Known limitation," discovered live during `m8`'s DoD run 2026-07-12, never given a follow-up milestone); numbered m34 not m31 — a concurrent session claimed m30-m32 first
- [x] **m35** — Postgres logical exports: `pg_dump` download parity (11 tasks) — **DONE 2026-07-14** (portable directory-format dump, private object-store upload, short-lived `can_view_sensitive` download, honest lifecycle + seven-day expiry, REST/GraphQL/MCP/UI parity, live restore evidence)
- [x] **m36** — Cron-job runs API: list · get · cancel (10 tasks; DONE 2026-07-14 — Render-compatible trigger/current-cancel plus bex list/get/per-run cancel across REST/GraphQL/MCP; stable `crr-…` ids, retained canceled history, `lastSuccessfulRunAt`, confirmed dashboard Cancel; live Job termination and all surfaces verified on the mock cluster) ← from `/pm-brainstorm more milestones for each worker` round 3, 2026-07-14
- [x] **m37** — Create-payload completeness: `secretFiles` + `environmentId` at create (8 tasks; DONE 2026-07-15 — three adapters, canonical Blueprint grouping, service wizard, full suites, live first-pod secret read + service/Postgres/Key Value Environment assignment) ← from `/pm-brainstorm` round 7, 2026-07-14
- [x] **m38** — Full deploy status lifecycle + transition timestamps (9 tasks; DONE 2026-07-15 — eleven-state evidence-backed persistence, transition timestamps, filter/adapter parity, and dashboard timeline/polling) ← from `w5/m29`'s 2026-07-14 Render-parity audit; narrowed after w9/001 independently shipped commit id/message provenance
- [x] **m39** — SSH into running service instances (14 tasks; DONE 2026-07-17 — stable public TCP/22/DNS + least-privilege gateway, raw OpenSSH and official CLI v2.21.0 any/exact-instance acceptance, PTY/lifecycle/denial matrix, key APIs and honest dashboard Shell guide) ← user explicitly reopened the narrow SSH surface from `.pm/DO_NOT_DO.md`, 2026-07-14; hosted sandboxes, ephemeral shells, jobs, and browser shell remain excluded
- [x] **m40** — Blueprint environment-scoped env groups (7 tasks; DONE 2026-07-16 — named unsupported error removed; groups created with environment membership; validate/sync accept the nesting; `envGroupIds` on the environment reflects the assignment; full test suite + lint green) ← promotes `010` 2026-07-15; `projects[].environments[].envVarGroups` (Render's official nesting) previously returned a named unsupported error — the membership mechanism it needed shipped in `w6/m24`
- [x] **m41** — Blueprint Key Value resources + keyvalue `fromService` refs (10 tasks; DONE 2026-07-16 — `fromService` type keyvalue/redis resolves to SecretKeyRef for connectionString/host/port/password; validation plan `keyValue` populated; stale `blueprint.go:73` comment removed; `GET /v1/blueprints/{id}` + MCP `get_blueprint` added; 3 new tests; CLI checklist + ADR018 updated) ← from `/pm-brainstorm` round 12, 2026-07-15 (`blueprint.go:73`'s wire-compat placeholder + `deploy.go:1200`'s named rejection + checklist line 84); makes all three managed resource types Blueprint-first-class; t010 folds `014` (blueprint by-id read parity: REST GET /v1/blueprints/{id} + MCP get_blueprint), `/pm-brainstorm` round 17
- [x] **m42** — Deploy commit author timestamp: `commit.createdAt` (7 tasks; DONE 2026-07-16 — `github.GetCommit` now parses `commit.author.date`; migration 0040 adds `commit_author_at` column; `renderCommit.CreatedAt` populated on REST+MCP; GraphQL `commitCreatedAt` field added; dashboard deploy-header shows author date when present; all tests pass) ← promotes `011` 2026-07-15
- [x] **m43** — Machine-surface list consistency: GraphQL/MCP pagination + service identity drift (7 tasks; DONE 2026-07-16 — GraphQL `services`/`environments` paged via `StablePage`; MCP `list_environments` paged; `Service.id` deviation documented in ADR020; `updatedAt` added to GraphQL `Service`; all ~26 "(bex App name)" MCP descriptions removed; MCP `list_services` arg-free confirmed by-design) ← from `/pm-brainstorm` round 13, 2026-07-15
- [x] **m44** — Image-origin override: manual-deploy `imageUrl` + deploy-hook `imgURL` (8 tasks; DONE 2026-07-16 — `TriggerParams.ImageURL` wired on REST/GraphQL/MCP + deploy hook `?imgURL=`; repo-backed services get named 400; `trigger_deploy` MCP tool added; 2 new tests; ADR018 updated) ← promotes `012` + the unowned m30 residual (`w2/done/m30/t005.md:41`), `/pm-brainstorm` round 14, 2026-07-15; one origin-safety design (t001) unlocks both entry points, retiring the deploy surface's last two named-rejection 400s
- [x] **m45** — Blueprint `initialDeployHook`: one-time first-deploy command (7 tasks; DONE 2026-07-16 — annotation-based ran-once tracking; `bex.co/initial-deploy-hook` stores command, `bex.co/initial-deploy-hook-ran` marks after `PreDeploySucceeded`; 4 new stack tests; ADR018 row updated) ← promotes `013`, `/pm-brainstorm` round 17, 2026-07-15; rides the w1/m33 pre-deploy Job mechanism with a ran-once marker, retiring ADR018:49's "recorded as unsupported" blueprint field
- [x] **m46** — Postgres + Key Value `updatedAt`/`region`/`dashboardUrl` on GraphQL and MCP (DONE 2026-07-16 — `PostgresView`/`KeyValueView` carry `Region`/`DashboardURL` populated by `Service.view()` wrappers; GraphQL adds `updatedAt`/`region`/`dashboardUrl` fields on both types; REST simplified; ADR018 gap-backlog row added) ← ADR018 w9/m41 audit found these fields absent from GraphQL/MCP despite REST already populating them; applies the `apps.Service.view()` enrichment pattern to the two managed datastore packages
- [x] **m47** — `POST /v1/services` returns `{service, deployId}` (DONE 2026-07-16 — `serviceAndDeploy` gains `DeployID omitempty`; create handler propagates `app.LatestDeployID` from the store's `FirstDeployID`; `recordingStore` updated; new test `TestREST_CreateResponseContainsDeployIDWhenStoreActive`; ADR018 Create-service row + gap-backlog updated) ← Render's `serviceAndDeploy` contract — the last documented divergence in the Create service row; omitted (never faked) when the store is inactive
- [x] **m48** — `deploy` object carries `serviceId` (DONE 2026-07-16 — `DeployView.ServiceID` from `store.Deploy.AppID`; `renderDeploy` gains `serviceId omitempty`; `toRenderDeploy` propagates it; GraphQL `Deploy` type gains `serviceId` field; stale `w2/011 inbox` ADR018 row corrected to `done (w2/m42)`) ← Render's deploy OpenAPI schema lists `serviceId`; bex had no pointer from a deploy back to its service
- [x] **m49** — Blueprint `scaling:` config wired into create (DONE 2026-07-16 — `bexScaling` struct + `bexService.Scaling` field; `scalingToAutoscalingRequest()` helper; `CreateRequest.Autoscaling`; `autoscalingSpec()` shared helper extracted from `SetAutoscaling`; `specFromCreate()` applies it; 2 new `stack_test.go` tests; ADR018 gap-backlog row added) ← `render.yaml scaling:` block was silently discarded by the Blueprint parser despite the REST/GraphQL/MCP autoscaling API being complete since w1/m20
- [x] **m50** — `databaseLogs` GraphQL type missing `instance`/`type` fields (DONE 2026-07-16 — two resolvers added to `databaseLogGQLType` in `postgres/graphql.go`; `TestGQLDatabaseLogEntryExposesInstanceAndType` added to `logs_test.go`; ADR018 "Postgres log history" row + gap-backlog updated) ← REST/MCP already returned `instance` and `type` from `DatabaseLogEntry.Labels`; GraphQL type was missing both fields despite the comment claiming they were exposed
- [x] **m51** — `matchesTimeWindow` silently drops legacy env groups with empty timestamps (DONE 2026-07-16 — `raw == ""` now short-circuits to `true` before `time.Parse`; `TestREST_EnvGroupLegacyEmptyTimestampPassesTimeFilter` seeds a timestampless group and confirms it passes time-filtered lists; ADR018 env-groups row + gap-backlog updated) ← `matchesTimeWindow` returned `false` on parse error, excluding pre-w6/m24 groups from every time-filtered list
- [x] **m52** — `GET /v1/services` list missing `?type=`, `?suspended=`, and four time-window filters (DONE 2026-07-16 — `?type=` OR-filter; `?suspended=true`/`false` with named 400 on unknown; `?createdBefore`/`?createdAfter`/`?updatedBefore`/`?updatedAfter` RFC3339 with named 400 on malformed and empty-timestamp pass; 3 new tests; ADR018 "List services" row + gap-backlog updated) ← Render CLI uses `?type=` to filter by service kind; bex list had only `?name=`/`?ownerId=`/`?environmentId=`
- [x] **m53** — `GET /v1/postgres` and `GET /v1/key-value` list filter parity (DONE 2026-07-16 — both handlers now support `?environmentId=` OR-filter, `?suspended=suspended`/`not_suspended` string enum (named 400 on unknown), and four RFC3339 time-window params (named 400 on malformed, empty timestamp passes); Key Value `?name=` also falls back to the opaque `red-` id; `"time"` import added to `keyvalue/rest.go`; 7 new tests across `postgres/filter_rest_test.go` and `keyvalue/filter_rest_test.go`; ADR018 rows + gap-backlog updated) ← same filters as m52 but for the two datastore list endpoints
- [x] **m54** — Dashboard document-head parity: Render-shaped titles + global social metadata (10 tasks; DONE 2026-07-17 — all 62 routes classified; one localized SSR/client title contract; dynamic names/types across projects, services, datastores, environment groups, Blueprints, and Webhooks; generic self-host-safe social metadata; 1,458 tests plus typecheck/lint/build green) ← user-directed all-page Render dashboard head research, 2026-07-17; evidence in `done/m54/evidence/2026-07-17-render-dashboard-head-audit.md`
- [x] **m55** — Browser-hosted Web Shell: in-dashboard xterm.js terminal for Render parity (10 tasks, done 2026-07-19; dev-2 Chrome + real-gateway acceptance recorded) ← user request 2026-07-17 ("learn from Render's `/shell`, make bex's `/services/{id}/shell` the same," transport "consistent with render.com"); explicitly reopens the browser-terminal anti-goal (`.pm/DO_NOT_DO.md` + ADR035 non-goal), mirroring the 2026-07-14 m39 SSH reopening; builds the browser transport atop m39's isolated ssh-gateway exec mechanism
- [x] **m56** — Separate deploy revisions from operational App reconciliation (8 tasks; DONE 2026-07-18 — source-built stale-credential 1→2 scale retained image/revision/template/original pod with no build, pre-deploy, or deploy-history side effect; genuine build + pre-deploy control and full suites passed) ← production incident 2026-07-18: scaling source-backed `srv-d9dd16roviqs738quds0` from 1→2 advanced the App generation, incorrectly launched a build with an expired clone credential, and left the active Deployment at one replica
- [x] **m57** — Official `kv-cli`: automated PTY + live Valkey acceptance (11 tasks; DONE 2026-07-18 — authoritative datastore A/AAAA reconciliation, trusted Hetzner PROXY client IP, explicit Redis SNI, unmodified v2.21.0 id/name production acceptance, cleanup evidence, and full validation green) ← user `$pm research and arrange tasks` 2026-07-18; closes `docs/cli-compatibility-checklist.md`'s headless-verification gap without patching or forking `render-oss/cli`
- [x] **m58** — Multi-replica Streamable HTTP MCP sessions (8 tasks; DONE 2026-07-19 — stateless Streamable HTTP POSTs, subject-scoped shared Postgres selection, alternating two-replica regression, migration integration) ← promoted from `015` 2026-07-19 after production retries proved stateful SDK sessions lose affinity across bex-api replicas
- [x] **m63** — Devin-style dashboard sidebar: icon-rail collapse + drag-to-resize + persisted state (7 tasks; DONE 2026-08-04 — three sidebars switched to `collapsible="icon"` with tooltips + an always-present header toggle; `SidebarRail` rewritten as a WAI-ARIA drag splitter (bounded [192,384], snap-to-rail, drag-out re-expand, dblclick/Enter/Arrow keys); signed-width cookie codec + isomorphic SSR read-back proven flash-free in server HTML; 18 new tests, full suite 1928 green; live DoD PASSED on the local dev stack) ← from user-directed live research of app.devin.ai's sidebar (Playwright drive, 2026-08-04)
- [x] **m64** — Fast agent-session create: accept fast, provision async (9 tasks; **DONE 2026-08-11** — implementation shipped + green since 2026-08-09; every DoD behavior asserted by backend `-race` + 15 dashboard tests, fix live in prod; t009 live-walk substituted by that automated coverage) ← from user report 2026-08-08 ("每次 chat 都要等半天"): `agentsessions.Create`/`Steer` block the HTTP response on sandbox provisioning (pod schedule + image pull, tens of seconds); split into sync accept (row + FGA tuple, `phase: creating`) + background dispatch, lazy attach ticket via the existing `AttachTicket` verb, dashboard navigates to the chat immediately and renders `creating`/`failed`+reason, steer appends optimistically with `redispatching`
- [x] **m67** — ADR059 Active tier: last-interaction idle grace + per-workspace live-sandbox cap (5 tasks; **DONE 2026-08-15** — shipped `7bf7dcde`, rolled to prod via the `81760b48` deploy: idle grace `BEX_AGENT_SANDBOX_IDLE_TTL` (default 30m, open editor pins at zero, `0` ⇒ ADR054 D6 immediate reap) generalizing the m65 deferral, + per-workspace live cap `BEX_AGENT_MAX_LIVE_SANDBOXES_PER_WORKSPACE` (default 5, `AGENT_SESSION_LIVE_LIMIT` 409 across REST/GraphQL/MCP); reaper terminal action kept a seam for m68's hibernate swap; idle math + cap semantics unit-tested on a fake clock) ← from `docs/ADR059-agent-sandbox-hibernation.md` (D2's Active state + D6's live-compute bound; extends w2/m65's ADR054 D6 deferral): reap only when `idle = now − max(last turn end, last SSH disconnect)` exceeds `BEX_AGENT_SANDBOX_IDLE_TTL` (never counts down while connected), + `BEX_AGENT_MAX_LIVE_SANDBOXES_PER_WORKSPACE`; the terminal action stays a seam so m68 swaps reap → hibernate at the same point
- [x] **m68** — ADR059 hibernation: spike → snapshot/rehydrate → pin, retention, billing (10 tasks; **DONE 2026-08-15** — t001–t009 shipped: D7 spike → **B**, migration 0073 + hibernate/rehydrate/pin/retention store methods, S3 `SnapshotStore` (presigned URLs, per-workspace prefix), Completer reclaim → hibernate with Terminate fallback, Resume/Steer rehydrate + latency instrumentation, driver `restoreWorkspace`, pin/unpin + quota + storage metering, full REST/GraphQL/MCP + dashboard surface, all suites green; **t010 live acceptance removed in the 2026-08-15 board cleanup** — infeasible pre-enablement (prod has no `BEX_AGENT_SNAPSHOT_S3_*` contract), tier ships env-gated OFF (unset ⇒ reclaim stays Terminate, byte-identical to m67); the live hibernate→rehydrate walk with recorded resume latency is the enablement step when the S3 contract is provisioned) ← from `docs/ADR059-agent-sandbox-hibernation.md`: t001 = the D7 OpenSandbox-snapshot spike (picks A-reuse-`Suspend` vs B-tar→object-storage, writes the verdict back into the ADR → Accepted); then Hibernated state + fs-snapshot-to-encrypted-object-storage (never a registry), rehydrate with p50<~5s/p95<~15s instrumentation, 7d retention + dirty-git extension, the **pinned** never-expire primitive (billed + quota'd), and the see/stop/unpin surface across REST/GraphQL/MCP + dashboard
- [x] **m69** — ADR062 phase 1: credential-injecting model proxy — BYO model key leaves the sandbox (7 tasks; **DONE 2026-08-15** — shipped env-gated OFF (`BEX_AGENT_MODEL_PROXY_URL` unset ⇒ byte-identical): gateway model-proxy listener (`internal/sshgateway/modelproxy`, source-pod auth + per-provider header injection + short-TTL `core.TTLCache` + streaming), gateway-only bex-api model-mint on internal :8091 (`agentsession.ModelMinter`/`ModelHandler`, OpenBao-backed, live-phase gated, shared HMAC envelope with the Git mint), placeholder-env + `sessionegress` narrowing (vendor host out, proxy carve-out), driver per-agent base-URL routing (claude/codex/gemini, fail-closed); every DoD clause test-pinned, backend + 30-test driver suite green; live enablement is the operational step once the prod env is provisioned) ← from `docs/ADR062-sandbox-credential-vault.md` (Proposed 2026-08-15; user handoff "hand off to /pm for its implementation for w2"): the reusable BYO provider key currently rides pod-spec env into the untrusted sandbox process tree (readable via inherited env + `/proc/1/environ`; literal-match scrubs miss encoded copies — the residual risk w3/m35/t003 recorded). OpenSandbox's Credential Vault is unusable on the gVisor pool (egress sidecar needs the iptables `nat` table runsc lacks), so build the bex-owned choke point ADR047 D6 phase 2 already reserves: gateway model-proxy listener (source-pod auth + session binding, upstream-hop header injection, agentcred pattern), gateway-only bex-api resolve endpoint (OpenBao custody unchanged), placeholder env + `sessionegress` narrowing (vendor host out, proxy carve-out beside `credentialGatewayHost`), per-agent base-URL routing in the driver (`ANTHROPIC_BASE_URL`/`OPENAI_BASE_URL`/Gemini equiv, fail-closed for adapters without one), scrub machinery retained; env-gated, unset ⇒ byte-identical. Token metering (`agent_token_units`) is explicitly a later milestone
- [x] **m66** — RequiresSshKey: SSH-key onboarding gate for SSH affordances (8 tasks; **DONE 2026-08-10** — reusable fail-open gate swaps the doomed ssh/`zed://` payload for an add-key CTA across the agent-session Open-in-Zed + service Connect/Shell surfaces, `?addKey` deep-link opens the form, `returnTo` round-trips back, browser-terminal second door on services; dashboard 300 test files + lint + typecheck green, live-verified) ← from w2/m65 live-testing failure 2026-08-10 (author hit `Permission denied (publickey)` in Zed; gateway metric `rejected_key=2`/`rejected_target=0` → no registered key) + PM discussion: a reusable dashboard gate that swaps the doomed ssh/`zed://` payload for an in-product add-key CTA (deep-link to `/settings#addSshPublicKey` + `returnTo` round-trip; two-door with the Web Shell on services), fail-open, across the agent-session (m65) + service SSH/Shell (m39/m55) surfaces
- [x] **m65** — Open in Zed: SSH remoting into agent-session sandboxes (14 tasks; **DONE 2026-08-11** — t010 live acceptance PASSED on the prod gateway, a real Zed client edited a sandbox `/workspace` (user-confirmed), after fixing two prod-only gaps: the out-of-band `agent_sessions` grant was never applied to the gateway role, and the sftp bridge ran in `/workspace` not `$HOME` so the server upload failed (fixed `session.go`, `babf63ba`); evidence in `done/m65/evidence/`; t012 reviewed, deferral seam left for m67) — **was: 11/14 done 2026-08-09** — t001–t009 + t011 + t013: backend `go test ./...` + dashboard 2009 tests + lint green, FGA in sync, dashboard→`zed://` handoff verified live; **t010 live real-cluster SSH acceptance BLOCKED** on ADR035 prod activation + a real OpenSandbox session, with t012/t014 gated behind it) ← from `docs/ADR054-open-in-zed.md` (Proposed 2026-08-09; user handoff "hand off to /pm for w2 for implement such function"): reuse the ADR035 gateway to serve `zed://ssh/ags-<xid>@ssh.bex.co/workspace` — composite `ags-*` target resolver gated on `can_view_sensitive`, sandbox-only multi-channel + SFTP-bridge carveouts, `GRANT SELECT ON agent_sessions`, editor-aware Completer teardown deferral, `sshAddress` on the session View + dashboard Open-in-Zed button; amends ADR035, complements ADR047 D8's ACP attach
- [x] **m62** — Git-connected Blueprints: Render dashboard + API parity (13 tasks; DONE 2026-08-01 — POST /v1/blueprints create, PATCH update, DELETE disconnect, GET syncs, auto-sync on push, status lifecycle, resources[], all surfaces + dashboard create dialog + detail parity) ← user-directed research 2026-08-01 (live `dashboard.render.com/blueprints` walk + Render OpenAPI vs code map): bex Blueprints are stored-manifest re-apply records, Render's are Git-connected instances — dashboard create-from-repo flow, auto-sync on push, sync history (`GET /v1/blueprints/{id}/syncs`), `PATCH`/`DELETE` disconnect, status lifecycle (`in_sync`/`syncing`/`error`/`paused`), and `resources[]` are all missing; rides m8/m9's GitHub App + the `/v1/webhooks/git` intake

## Inbox

- **034** — Decide the disk $/GB-month rate (measured 56.2% margin vs ADR082's assumed 65–71%): keep $0.175 or re-price ← from `/pm-brainstorm for w2` 2026-09-07 #5; residual of `030`

_(2026-09-08 triage: `033` fixed — operator now projects the disk-snapshot Secret pair from the apps namespace into each disk-bearing App's namespace at reconcile time, `SnapshotCredentialUnavailable` fail-closed condition, script seeds the source, envtest-pinned; `030` executed — authenticated Hetzner pricing read: volumes bill **$0.0767/GB-month USD** on this account ⇒ ~56.2% gross margin at the $0.175 rate, recorded in ADR082 §Verification, `pricing.yaml` untouched pending a user re-price decision; `031` fixed — ADR018's two stale `.pm` pointers corrected (Enterprise drift restated inline, Managed-OIDC row points at FUTURE-MAYBE). All three in `done/`.)_

_(`032` resolved 2026-09-03 — user supplied `BEX_STRIPE_PUBLISHABLE_KEY` into `.env`, patched into `bex-system/bex-stripe` (mode-checked), bex-api rolled 2/2 and Argo went Synced/Healthy; moved to `done/`)_

_(previous state: none open — 2026-08-23 triage: `020` sweep executed, blockers reported back; `022`/`023`/`024`/`026` implemented; `027` resolved + legacy App deleted; `028` investigated, decisions recorded; `029` shipped; `025` deferred to `.pm/FUTURE-MAYBE.md` by user direction — all in `done/`)_

_(previous state: none open — 2026-08-15 board cleanup: `017.md` resolved and moved to `done/` — the w3/m44 `session-sidebar.tsx` no longer exists after the agents-page restructure, nothing left to unify; `018.md` (bound the background-dispatch fan-out) deleted — mitigated by m67's `BEX_AGENT_MAX_LIVE_SANDBOXES_PER_WORKSPACE` dispatch-time cap, remaining global-semaphore hardening judged not worth carrying; `019.md` (deploy idempotently applies ssh-gateway `dbrole.sql` grants) deleted per cleanup — the grant surface still applies only via `scripts/ssh-gateway-db-role.sh`, so re-run it whenever `dbrole.sql` gains a grant)_

_(`015.md` promoted to **m58** 2026-07-19; moved to `done/`. `012.md` promoted to **m44** 2026-07-16; moved to `done/`. `013.md` promoted to **m45** 2026-07-16; moved to `done/`. `011.md` promoted to **m42** 2026-07-15; moved to `done/`. `010.md` promoted to **m40** 2026-07-15; moved to `done/`)_

> `016.md` implemented 2026-07-18 (deploys-history Render-parity table columns) — `deploys-list-page.tsx` rebuilt on the shared `Table` primitive with `Deploy`/`Trigger`/`Duration`/action columns, `deploy_hook`+`blueprint` trigger humanization, en/zh column labels, and 9 component tests; full dashboard suite + lint green; moved to `done/`. Live browser walk against `srv-d9bj8s3eg85c7390eb9g` vs Render `srv-d5mk47ogjchc738phl00` still pending a cluster.

> 006.md promoted to done 2026-07-12: `triggerDeploy` GraphQL mutation shipped in `internal/deploys/graphql.go`.

- (moved) agent OAuth 2.1 provider → promoted as `w4/m9` (auth workstream owns it; w2's MCP milestones consume it)

> `002.md` retired to `done/` after w6/m2 closed (line repaired 2026-07-12 — a stale "none open" bullet sat here while `005.md` was open). `003.md` promoted to `m7` (Key Value API surface) 2026-07-09 — moved to `done/`. `004.md` — Agent-connect recipe: done 2026-07-11 (`docs/ADR025-connect-an-agent.md`) — moved to `done/`. `005.md` promoted to **m11** (unify service creation) 2026-07-12 — moved to `done/`. `007.md` was promoted into m36 as t010 and moved to `done/` when m36 closed 2026-07-14. `008.md`'s two wire-shape bugs shipped; its remaining `services instances` route gap transferred by user direction to **w6/m26** 2026-07-15 — moved to `done/`. `009.md` — official Render CLI Blueprint validation multipart compatibility: fixed and live-verified 2026-07-15 — moved to `done/`.

> **m3 (E2B-compatible sandboxes) removed 2026-07-08** — hosted agent sandboxes (pillar 5) are off the roadmap by user decision; see [`.pm/DO_NOT_DO.md`](../DO_NOT_DO.md). The architecture record stays in docs/ADR014-sandboxes.md (ADR, status: proposed) for a future explicit re-open.
