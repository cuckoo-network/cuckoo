---
id: w2/m89/t003
title: Validator + docs: build jobs must reference no deploy secrets
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m89/t002]
---

## Objective

Make the separation self-defending and recorded: extend `scripts/github-actions-validate.sh` to fail closed when a build-classified job references a deploy-classified secret, and update ADR080 (finding 2 → closed) + ADR083 (follow-up 3 → owned here).

## Context

- Secret classification: reuse/extend the classification table from w2/m88 t001 if m88 landed first; otherwise declare the deploy-secret name list explicitly in the script (kubeconfig/infra/gitops secret names) so additions are a conscious act.
- The validator already parses workflows for the `#CI-RUNNERS` checks — follow its parsing idiom.

## Steps

1. Add the build-job/deploy-secret assertion + the secret class list.
2. Negative-test with a fixture that leaks a deploy secret into the build job.
3. Update `docs/ADR080-security-review-round19.md` + `docs/ADR083-security-review-round20.md`; prettier.

## Files

- `scripts/github-actions-validate.sh`
- `docs/ADR080-security-review-round19.md`, `docs/ADR083-security-review-round20.md`

## Acceptance criteria

- [ ] Validator fails on a deploy secret in the build job and passes the real tree; both ADRs record closure with owner `w2/m89`.

## Out of scope

- Pool-label enforcement (m88 t003).
