---
id: w2/m89/t004
title: Verify a full production deploy through the split pipeline
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m89/t003]
---

## Objective

Prove the split pipeline ships for real: one production deploy runs end to end through the secretless build job and the environment-gated deploy job, the deployed workloads run the digests that build emitted, and platform health is unchanged.

## Context

- Deploys ride `/ship` (never commit/push outside it); coordinate the landing so the next shipped commit exercises the split workflow.
- Evidence: `gh run view --json` (job separation + secret scopes visible in logs' env), `kubectl get deploy -n bex-system -o jsonpath` image references vs the run's emitted digests.

## Steps

1. Land the milestone's changes via the normal ship flow; watch the resulting `deploy.yml` run.
2. Capture: build job green with no deploy-credential mounts, deploy job green behind its environment gate, cluster images match emitted digests.
3. Record evidence in the milestone.

## Files

- evidence notes under `.pm/w2/m89/`

## Acceptance criteria

- [ ] Recorded evidence of one green production deploy through the split pipeline with digest match confirmed.

## Out of scope

- Rollback drills (unchanged mechanics).
