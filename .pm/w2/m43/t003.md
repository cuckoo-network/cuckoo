---
id: w2/m43/t003
title: Fix the stale "bex App name" MCP arg descriptions
worker: worker2
status: todo
estimate: 20m
depends_on: []
---

## Objective

~10 MCP arg structs describe `serviceId` as "the service id (bex App name)" (`lego/backend/internal/apps/mcp.go:39, 56, 64, 71, 78, 86, 91, …`) even though `list_services` returns minted `srv-…` ids — the description an agent reads actively misleads it into passing names. Update every such description to name the `srv-…` form (noting names are accepted as a legacy fallback).

## Files

- `lego/backend/internal/apps/mcp.go` (and any sibling packages with the same phrasing — grep `bex App name`)

## Acceptance criteria

- [ ] `grep -r "bex App name" lego/backend` returns nothing (or only deliberate legacy notes).

## Out of scope

- Resolver behavior (already accepts both).
