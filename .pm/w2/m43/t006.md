---
id: w2/m43/t006
title: Test coverage
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m43/t004]
---

## Objective

Add tests: GraphQL services/environments pagination walks (terminate, full coverage, REST-identical page composition) and a cross-surface id-consistency assertion (GraphQL `Service.id` matches the decided form; `updatedAt` present). Assert real failure modes, not snapshots.

## Files

- `lego/backend/internal/apps/` + `internal/environments/` tests

## Acceptance criteria

- [ ] Behaviors locked; `go test ./...` passes.
