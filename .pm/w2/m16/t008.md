---
id: w2/m16/t008
title: "Render parity: verify plan-update surface consistency"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m16/t007]
---

## Objective

Check that the new Postgres/Key Value plan-update verb is consistent across REST, GraphQL, MCP, and dashboard — same fields, semantics, and error shapes — and compare against Render's own documented Postgres/Key Value plan-change behavior, flagging any drift as follow-up rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` — bex-api's Render-compatibility design.
- `docs/ADR018-render-parity.md` § Managed Postgres row — the gap this milestone closes; update its evidence pointer once shipped.

## Steps

1. Compare bex's `PATCH /v1/postgres/{id}` / `PATCH /v1/key-value/{id}` field names and response shape against Render's documented update endpoints (if any exist for these resources — note if Render itself has no such endpoint, in which case this is a bex extension, not parity).
2. Confirm REST/GraphQL/MCP/dashboard all use the same plan-name vocabulary and reject the same invalid inputs identically.
3. Update `docs/ADR018-render-parity.md`'s Managed Postgres row (and add/update the Key Value row) reflecting the shipped state.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Parity ledger updated with the shipped evidence pointer.
- [ ] Any discovered drift is filed as a follow-up note, not silently left undocumented.

## Out of scope

- None.
