---
id: w2/m16/t004
title: "Key Value: PATCH /v1/key-value/{id} + GraphQL updateKeyValuePlan + MCP update_key_value_plan"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m16/t003]
---

## Objective

Wire t003's `SetPlan` verb onto all three API surfaces for managed Key Value, mirroring t002's Postgres shape.

## Context

- `lego/backend/internal/keyvalue/rest.go` — no `PATCH` route exists today.
- `lego/backend/internal/keyvalue/graphql.go`, `mcp.go`.

## Steps

1. REST: `PATCH /v1/key-value/{id}` accepting `{"plan": "..."}`.
2. GraphQL: `updateKeyValuePlan(id: ID!, plan: String!): KeyValue` mutation.
3. MCP: `update_key_value_plan` tool.

## Files

- `lego/backend/internal/keyvalue/rest.go`
- `lego/backend/internal/keyvalue/graphql.go`
- `lego/backend/internal/keyvalue/mcp.go`

## Acceptance criteria

- [ ] `PATCH /v1/key-value/{id}` with `{"plan":"standard"}` returns 200 and the updated plan.
- [ ] GraphQL `updateKeyValuePlan` and MCP `update_key_value_plan` both call the same `SetPlan` verb.
- [ ] An unknown plan is a 400/GraphQL-error/MCP-error on all three.

## Out of scope

- Dashboard UI (t006).
