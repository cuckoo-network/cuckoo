---
id: w2/m16/t009
title: "Simplify"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m16/t008]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Postgres and Key Value `SetPlan` verbs (t001, t003) are deliberately near-duplicates of each other and of `apps.SetPlan` — check whether the shared validation/ordering logic can be factored into one helper without forcing an awkward cross-package dependency.

## Steps

1. Run `/simplify` scoped to the files this milestone touched.
2. Apply any behavior-preserving cleanups it finds.

## Files

- All files touched by t001–t007.

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly dismissed with reasoning.

## Out of scope

- None.
