# Deploy-hook `imgURL` needs an image-origin override design first

Render's deploy hooks accept `imgURL` to deploy a specific image; bex rejects it — "bex has no safe image-origin matching/override verb yet" (`lego/backend/internal/deploys/deployhook.go:256`, 400 at `:292`). Before building: design what makes an image origin *safe* to accept from a secret-URL trigger (match against the service's configured registry/repo? require the w2/m14 credential's registry? never arbitrary origins). Write the design decision here, then promote to a milestone if it survives.

Source: `/pm-brainstorm` round 12, 2026-07-15 (code miner).
