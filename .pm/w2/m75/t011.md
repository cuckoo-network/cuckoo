---
id: w2/m75/t011
title: "Closeout: verify DoD, mark done, move milestone to done/"
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m75/t010]
---

## Objective

Standing closing task: verify every Definition-of-done clause in `.pm/w2/m75/README.md` against the shipped state, then execute the board's done procedure so the milestone leaves the active set with no tombstone.

## Context

- DoD (README): mock-cluster coexistence of same-named Apps in two workspaces end to end (t006 evidence), legacy App unchanged via dual-read, migration tool verified + tombstoning (t004/t006), authorization-gated prod runbook exists (t007), ADR055 F2/F3 register lines annotated (t008).
- Done procedure (`.pm/CLAUDE.md` + CLAUDE.md Rules): a done milestone moves WHOLE — never leave a tombstone, stub, or redirect README at the old path; the original path must simply not exist afterward.
- Workstream checklist: `.pm/w2/README.md` "## Milestones" — m75's checkbox + summary row.

## Steps

1. Walk the DoD clause by clause; link each to its evidence (t006 log, runbook path, ADR line numbers). Any unmet clause ⇒ stop, reopen the owning task.
2. Set every task's frontmatter `status: done` and the milestone README `**Status:** done` + a `— **DONE <date>**` summary row.
3. Move task files into `.pm/w2/m75/done/` per convention, then `mv .pm/w2/m75 .pm/w2/done/m75` (and confirm `.pm/w2/m75` no longer exists).
4. Check the m75 box in `.pm/w2/README.md` with a one-row summary (what shipped + gates green), matching the existing rows' style.

## Files

- .pm/w2/m75/ (whole directory → .pm/w2/done/m75/)
- .pm/w2/README.md (milestone checkbox row)

## Acceptance criteria

- [ ] Every DoD clause verified with pointed evidence
- [ ] Task frontmatter, milestone README status, and w2 README checkbox all synced (three places)
- [ ] `.pm/w2/m75` absent; `.pm/w2/done/m75/` contains the whole milestone

## Out of scope

- Shipping/committing (only via /ship on user request); any remaining prod migration phases (tracked by the t007 runbook, gated on explicit operator authorization); w7/m85 and w7/m86 stay in w7.
