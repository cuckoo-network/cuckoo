# Hetzner volume-price re-check (ADR082 verification)

Why: the disk meter bills $0.175/GB-month against a volume cost basis (€0.0440 vs €0.0572/GB-month) that was never confirmed against a real Hetzner project — the check needs an authenticated API read and no environment bex runs holds a Hetzner API token.

- ADR082 §Verification left exactly one item unrun: an authenticated `GET https://api.hetzner.cloud/v1/pricing` read of the volume price. Recorded still-open in the `w1/done/m86`/`m87` closeouts ("carry it to whoever first provisions a real Hetzner project").
- No code change either way — the rate lives in `lego/backend/internal/pricing/pricing.yaml`; the delta only moves gross margin between ~71% and ~65%.
- Run with an operator-supplied `HCLOUD_TOKEN` (Terraform custody; never commit it), record the observed €/GB-month in ADR082 §Verification, and touch `pricing.yaml` only if the user directs a re-price.

← from `/pm-brainstorm for w2` 2026-09-01 #5.
