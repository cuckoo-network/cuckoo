---
id: w2/m10/t009
title: Closeout — DoD met → move milestone to done/
worker: worker2
status: todo
estimate: 10m
depends_on: [w2/m10/t008]
---

## Objective

Standing closing task, run last and only when the DoD actually holds (live cancel + rollback proven, matrix rows refreshed). Close the milestone: remaining tasks `status: done`, files to `w2/m10/done/`, rows `— **DONE**` + `**Status:** done`, move `w2/m10/` whole to `w2/done/m10/`, check the box in `w2/README.md`.

## Acceptance criteria

- [ ] DoD verified before the move; board synced in all three places; `w2/m10/` gone from its old path.
