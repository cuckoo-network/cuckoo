# Agent-session `SessionSidebar` — unify with the sidebar primitives

Follow-up to `w2/m63` (Devin-style main sidebar). The w3/m44 agent chat page renders a second left rail, `dashboard/src/features/agent-sessions/components/session-sidebar.tsx:31` — a plain `<aside className="w-72 hidden lg:flex">`, not the shadcn sidebar kit. Once m63 lands, consider either rebuilding it on the primitives (gets collapse/resize/persistence for free) or at least letting it respond to the main sidebar's collapsed state so the chat page doesn't carry ~34rem of fixed rails on smaller desktop widths. Sub-hour scoping look first; promote if it turns out to be bigger.
