# Blueprint environment-scoped env-var groups

Thread `projects[].environments[].envVarGroups` through the existing env-group create/apply path so each group receives the declared Environment membership. Root and `ungrouped.envVarGroups` already work; today an environment-scoped block returns a named unsupported error. Keep the official Render nesting and the all-or-nothing Blueprint preflight.
