---
id: w2/m39/t011
title: Simplify
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m39/t010]
---

## Objective

Run `$simplify` over the SSH milestone's backend, deployment, dashboard, scripts, and documentation changes; remove duplication and accidental complexity without changing verified behavior or weakening the security boundary.

## Context

- Target selection must be shared by service projection, instances REST, and the gateway rather than reimplemented three ways.
- Key parsing/fingerprinting and authz/error mapping should each have one source of truth.
- Security-critical explicitness is preferable to clever abstraction where it makes review harder.

## Steps

1. Run `$simplify` across every changed file from t001–t010.
2. Consolidate duplicated key, target, stream, config, manifest, and UI logic where ownership is clear.
3. Remove dead compatibility branches and improve names/comments at trust boundaries.
4. Re-run focused tests after each behavior-preserving change.

## Files

- `lego/backend/internal/sshkeys/`
- `lego/backend/internal/sshgateway/`
- `lego/backend/internal/apps/`
- `dashboard/src/features/ssh-keys/`
- `lego/operator/config/ssh/`
- `deploy/gitops/`
- `scripts/ssh-verify.sh`

## Acceptance criteria

- [ ] Shared concepts have one implementation and dependency arrows remain `operator → types ← backend`.
- [ ] No simplification broadens RBAC, weakens authentication/authz, or records terminal content.
- [ ] Focused suites remain green.

## Out of scope

- Unrelated repository-wide refactors.
