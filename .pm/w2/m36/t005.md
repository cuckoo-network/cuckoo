---
id: w2/m36/t005
title: Dashboard: runs table on the cron service page
worker: worker2
status: todo
estimate: 40m
depends_on: [w2/m36/t004]
---

## Objective

The cron service page's runs display upgrades to the run-object API: status chips, started/duration, Cancel on a Running row (confirm dialog), cursor "Load more" — matching Render's cron-runs presentation. Read `dashboard/CLAUDE.md` first.

## Files

- `dashboard/src/features/services/` (cron section), generated GraphQL definitions

## Acceptance criteria

- [ ] Runs render from the new query; Cancel works in a real browser against the mock cluster; `yarn test` green.
