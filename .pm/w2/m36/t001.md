---
id: w2/m36/t001
title: Verify Render's run-object shape + cancel semantics
worker: worker2
status: todo
estimate: 30m
depends_on: []
---

## Objective

Capture Render's cron-run object (fields, statuses, envelope) and cancel behavior (which states are cancelable, response codes) from the OpenAPI spec + docs before building. Record in `docs/render-artifacts/cron-runs.md`.

## Context

- Render OpenAPI `https://api-docs.render.com/openapi/render-public-api-1.json` (list-cron-job-runs / retrieve-cron-job-run / cancel-cron-job-run).
- bex today: `lego/types/v1alpha1/app_types.go` `status.runs` (`CronRunView`: name, startedAt, finishedAt, status Running|Succeeded|Failed).

## Acceptance criteria

- [ ] Capture pins field names, status vocabulary, envelope, cancel constraints; names any bex divergence up front (e.g. run id derivation from the k8s Job name).
