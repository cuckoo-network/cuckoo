---
id: w2/m36/t004
title: GraphQL + MCP mirrors
worker: worker2
status: todo
estimate: 40m
depends_on: [w2/m36/t003]
---

## Objective

Thin adapters over the same verbs: GraphQL `cronJobRuns(serviceId,…)`/`cronJobRun(id)`/`cancelCronJobRun`; MCP `list_cron_job_runs`/`get_cron_job_run`/`cancel_cron_job_run` — mirroring Render's MCP tool names where they exist, marked bex extensions where they don't (check `render-oss/render-mcp-server`).

## Files

- `lego/backend/internal/apps/{graphql.go,mcp.go}`

## Acceptance criteria

- [ ] Adapter-parity test: identical run objects and error shapes on all three surfaces.
