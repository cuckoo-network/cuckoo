---
id: w2/m36/t002
title: Run list/get over `status.runs`, Render envelope
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m36/t001]
---

## Objective

`GET /v1/cron-jobs/{id}/runs` (cursor/limit, `{cronJobRun, cursor}` per t001's envelope) and `GET .../runs/{runId}` reading the App's `status.runs`, with stable run ids (derived from the Job name — the events feed's derived-id precedent). 404 for a non-cron service or unknown run id.

## Context

- `lego/backend/internal/apps/rest.go` (runCron + the services paging at :307-311 to reuse); authz via the resource's own workspace (`core.Base.AuthorizeApp`).

## Files

- `lego/backend/internal/apps/{rest.go,service.go}`

## Acceptance criteria

- [ ] List pages correctly; get fetches one run; non-cron 4xx per capture; ids stable across reads.
