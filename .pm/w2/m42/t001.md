---
id: w2/m42/t001
title: Author-timestamp capture at deploy-open + store migration
worker: worker2
status: todo
estimate: 45m
depends_on: []
---

## Objective

When a deploy opens and the commit resolves (the w2/m38 best-effort path), also capture the commit's author timestamp and persist it on the deploy row — a nullable column, backfilled never (historical rows stay empty/omitted).

## Context

- `lego/backend/internal/deploys/` commit-resolution seam from w2/m38.
- Next free store migration number — scan for concurrent claims first (the 0023/0024 collision precedent).

## Files

- lego/backend/internal/deploys/
- lego/backend/internal/store/migrations/

## Acceptance criteria

- [ ] New Git-backed deploys persist the author timestamp; unresolved commits leave it NULL; migration applies cleanly on an existing DB.

## Out of scope

- Surfaces (t002); provider-specific author UI.
