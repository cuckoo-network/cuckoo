---
id: w2/m42/t002
title: `commit.createdAt` on REST/MCP + GraphQL
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m42/t001]
---

## Objective

The deploy object's nested `commit` block gains `createdAt` on all three surfaces, omitted (not zero-valued) when unset — matching Render's wire shape from the pinned OpenAPI.

## Files

- lego/backend/internal/deploys/rest.go
- lego/backend/internal/deploys/graphql.go
- lego/backend/internal/deploys/mcp.go

## Acceptance criteria

- [ ] Field present + correct on all three surfaces for a resolved commit; absent (not null-stuffed) otherwise.
