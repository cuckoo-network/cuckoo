---
id: w2/m11/t009
title: "Closeout — verify DoD met, then move the milestone to done/"
worker: worker2
status: todo
estimate: 10m
depends_on: [w2/m11/t007, w2/m11/t008]
---

## Objective

Verify the milestone's definition of done actually holds, then close it out per `.pm/CLAUDE.md` conventions.

## Steps

1. Confirm creating a private-repo service via REST, GraphQL, or MCP builds successfully and its deploys appear in the deploys API, with no regression for public-image or existing creates.
2. Set every task's `status: done`, move each `tNNN.md` to `w2/m11/done/`.
3. Mark every row `— DONE` and `**Status:** done` in this README.
4. Move the whole `w2/m11/` directory to `w2/done/m11/`.
5. Check `- [x]` for m11 in `w2/README.md`.

## Acceptance criteria

- [ ] Milestone's observable end state is real, not just code-complete.
- [ ] Board moved and status synced in all three places.
