---
id: w2/m76/t007
title: "Render parity: subscription/settings surface consistent across exposed surfaces; no-push-at-Render divergence recorded"
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m76/t004, w2/m76/t006]
---

## Objective

Record the parity posture (standing closing task): Render has no push notifications at all, so web push — like native push — is a deliberate bex delivery-channel extension, never a "fix". Verify the subscription/settings surface is consistent across the surfaces bex chooses to expose it on (REST + GraphQL + dashboard; MCP deliberately excluded, matching the native-push decision), and update the parity ledger row.

## Context

- Parity ledger: `docs/ADR018-render-parity.md` line ~27 already carries the native-push extension paragraph ("Native push is a deliberate bex delivery-channel extension (w11/m5)… MCP deliberately has no device-registration or push-policy tools because an installation-bound human notification channel is not an agent capability") — extend that paragraph rather than adding a new row.
- Surfaces to cross-check: REST `GET/PATCH /v1/notification-settings/push`, `GET .../push/availability`, the subscription verbs (`lego/backend/internal/notifications/rest.go`), GraphQL `pushNotificationSettings` + t002's mutations (`graphql.go` ~line 202), dashboard panel (`dashboard/src/features/notifications/components/push-notification-settings-panel.tsx`). Existing cross-surface test precedent: `lego/backend/internal/notifications/subscription_surface_test.go` and `surface_test.go`.
- ADR052's stance that a browser-bound human notification channel is not an agent capability carries over: no MCP tool.

## Steps

1. Cross-surface audit: the same member sees the same policy + web-push availability + subscription list through REST and GraphQL; field names/shapes agree with the native-push conventions (no secrets/endpoints leaked in reads).
2. Extend `subscription_surface_test.go` (or sibling) with a REST↔GraphQL parity test for the web-push verbs.
3. Update `docs/ADR018-render-parity.md`'s push paragraph: web push (VAPID, browser) added in w2/m76 as a second transport of the same extension; MCP exclusion re-affirmed; no Render counterpart exists (their notification surface is email + Slack-via-integrations only).
4. Confirm no verb accidentally entered MCP (`lego/backend/internal/notifications/mcp.go` should be unchanged by this milestone).

## Files

- `docs/ADR018-render-parity.md`
- `lego/backend/internal/notifications/subscription_surface_test.go` (or new surface test)
- `lego/backend/internal/notifications/mcp.go` (verify-unchanged)

## Acceptance criteria

- [ ] REST and GraphQL return equivalent web-push settings/subscription data for the same caller (test-enforced).
- [ ] Parity ledger paragraph updated with the w2/m76 extension and the explicit no-Render-counterpart statement.
- [ ] `mcp.go` diff for this milestone is empty.

## Out of scope

"Fixing" the divergence (impossible — Render has nothing to converge to); CLI-surface coverage (`docs/cli-compatibility-checklist.md` — Render's CLI has no notification commands).
