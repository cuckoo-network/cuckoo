---
id: w2/m76/t010
title: "Closeout: verify DoD, mark done, move milestone to done/"
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m76/t009]
---

## Objective

Verify the milestone's Definition of done end to end on the dev stack, sync status everywhere, and move the milestone to `done/` per the `.pm` conventions.

## Context

- DoD (README): with `BEX_WEBPUSH_*` set, a member enables browser notifications in dashboard settings, selects events with the existing policy picker, an induced deploy-failure event arrives as a real OS notification; a browser-side unsubscribe leads to 404/410 pruning; env unset ⇒ channel absent, byte-identical; self-hoster runbook (`docs/runbooks/web-push.md`) is store-dependency-free.
- `.pm` rules (CLAUDE.md + `.pm/CLAUDE.md`): a done milestone moves **whole** to `.pm/w2/done/m76/` — `mv` the directory then `rmdir` nothing-left-behind; never leave a tombstone/stub at `.pm/w2/m76/`. Sync status in all three places: task frontmatter, milestone README `**Status:**` + `— **DONE**` row, `.pm/w2/README.md` workstream checkbox.
- Markdown is CI-checked: run `npx prettier@3.4.2 --write "**/*.md"` over the changed docs before finishing (allowed at closeout; the milestone-creation step deliberately skipped it).

## Steps

1. Walk the DoD on the dev stack: configure `BEX_WEBPUSH_*`, subscribe a real browser, induce a deploy failure (existing dev harness recipe), observe the OS notification; unsubscribe in the browser, push again, verify the 410 prune in logs/store.
2. Unset the env, restart bex-api, confirm the availability surface reports unconfigured and no webpush code path activates.
3. Confirm all suites green: `make test` (from `lego/operator/`) if operator files were touched (they should not be), `cd lego/backend && go test ./...`, dashboard `yarn test`, `make lint`.
4. Flip every t00N frontmatter `status: done`; update this milestone README `**Status:** done`; check the w2 workstream README box.
5. `mv .pm/w2/m76 .pm/w2/done/m76` — verify the old path no longer exists.
6. Run prettier over changed markdown.

## Files

- `.pm/w2/m76/` → `.pm/w2/done/m76/` (whole directory)
- `.pm/w2/README.md`

## Acceptance criteria

- [ ] Every DoD clause demonstrated (notification received, prune observed, unset ⇒ byte-identical, runbook store-free).
- [ ] All task/milestone/workstream statuses synced.
- [ ] Milestone directory lives only at `.pm/w2/done/m76/`; no stub remains.
- [ ] Backend + dashboard tests and `make lint` green; markdown prettier-clean.

## Out of scope

Shipping/committing (only via `/ship`); production rollout of `BEX_WEBPUSH_*` (operator action via the runbook); native push (w11/m5) and Live Activities/widgets (w11/001) untouched.
