---
id: w3/m12/t002
title: Remove dead silent host/path parsing
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m12/t001]
---

## Objective

Remove the now-unreachable code that parsed `host`/`path` into the metrics query without ever using them.

## Context

- Once t001 rejects requests carrying these filters before the query executes, the old silent-parse-and-drop code path is dead.

## Steps

1. Delete the dead parsing/threading of `host`/`path` into the Prometheus query construction in `lego/backend/internal/metrics/source.go`.
2. Confirm no other code path still relies on the dropped values.

## Files

- `lego/backend/internal/metrics/source.go`

## Acceptance criteria

- [ ] No dead code remains for the old silent-drop behavior.
- [ ] `go vet`/lint clean.

## Out of scope

- Behavior changes beyond dead-code removal.
