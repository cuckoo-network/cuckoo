---
id: w3/m18/t003
title: PromQL host/path filter wiring in the metrics service
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m18/t002]
---

## Objective

Replace the named 400 with real PromQL filtering on the router-label dimension t001 proved (host certainly; path only if the labels genuinely carry it — otherwise path keeps a now-documented 400).

## Context

- `lego/backend/internal/metrics/service.go` request-metrics PromQL builders; the label-matching idiom from the existing status/method filters.

## Files

- lego/backend/internal/metrics/service.go

## Acceptance criteria

- [ ] Host-filtered query returns only that host's traffic on all three surfaces; unsupported dimensions still 400 with an accurate message.

## Out of scope

- Dashboard (t005).
