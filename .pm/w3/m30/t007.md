---
id: w3/m30/t007
title: Render parity — cross-surface consistency vs Render's Key Value logs
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m30/t004, w3/m30/t005, w3/m30/t006]
---

## Objective

Check the shipped Key Value logs surface for consistency across REST, GraphQL, MCP, and the dashboard (same fields, semantics, error shapes) and against Render's equivalent behavior (t001's capture); flag any drift as follow-up work rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` (one core, thin adapters), `docs/ADR018-render-parity.md` (update the logs coverage row with evidence).

## Steps

1. Same query via all three adapters + the UI; diff fields/errors.
2. Diff against t001's captured Render contract; file follow-ups for divergences.
3. Update ADR018/ADR010 rows.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR010-observability.md`

## Acceptance criteria

- [ ] Parity row updated with evidence; any drift filed, none silent.

## Out of scope

- Fixing large drift inline (file it).
