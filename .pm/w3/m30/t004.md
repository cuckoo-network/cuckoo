---
id: w3/m30/t004
title: REST resource= support + GraphQL query + MCP tool for KeyValue logs
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m30/t003]
---

## Objective

Expose the t003 core on all three adapters: the Render-shaped REST `GET /v1/logs?resource=<keyvalue-id>` (plus a dedicated compatibility endpoint only if t001's capture shows Render has one), a GraphQL query, and an MCP tool — mirroring m28's Postgres set exactly.

## Context

- Postgres siblings: `lego/backend/internal/postgres/{rest,graphql,mcp}.go` log verbs; follow their shapes, error dialect (`core.WriteErr`), and honest store-off states.
- MCP tool descriptions must reflect durability truthfully (see t006 — don't repeat the stale-claim mistake).

## Steps

1. REST: accept KeyValue ids on the generic logs route per t001's captured contract.
2. GraphQL: add the query with the same entry shape as `databaseLogs` (including `instance`/`type` — coordinate with t006).
3. MCP: add the tool with an accurate durability description.

## Files

- `lego/backend/internal/keyvalue/rest.go` / `graphql.go` / `mcp.go` (or the shared logs adapters, matching where the Postgres verbs live)

## Acceptance criteria

- [ ] The same KeyValue log lines are returned via REST, GraphQL, and MCP with consistent fields and error shapes.

## Out of scope

- Live SSE tail for keyvalue (the SSE path stays App/build-only unless t001 proves Render tails datastores — record the decision either way).
