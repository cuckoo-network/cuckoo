---
id: w3/m30/t001
title: Capture Render's Key Value logs contract into docs/render-artifacts/
worker: worker3
status: todo
estimate: 30m
depends_on: []
---

## Objective

Pin down, with first-party evidence, exactly what Render's logs API offers for Key Value stores — params, filters, id shape (`red-…`), live-tail availability — so the bex surface is built against evidence rather than assumption (the m28 pattern: `docs/render-artifacts/postgres-logs.md`).

## Context

- `docs/render-artifacts/postgres-logs.md` already quotes Render's List-logs API as covering "servers, cron jobs, jobs, Key Value, and workflows" — this task fills in the Key Value specifics.
- Render's OpenAPI is pinned locally each brainstorm round; the `/logs` route's `resource` param accepts datastore ids.
- Follow the redaction practice of `docs/render-artifacts/` (no real ids/emails).

## Steps

1. Read Render's public docs + pinned OpenAPI for `/logs` with a Key Value resource; note filters (level applies?), paging, and whether the dashboard shows a KV Logs tab.
2. Record the findings as `docs/render-artifacts/keyvalue-logs.md` (or extend `postgres-logs.md` if the contract is literally identical — say so explicitly).
3. Note any divergence the bex implementation must consciously make.

## Files

- `docs/render-artifacts/keyvalue-logs.md` (new, or extend `postgres-logs.md`)

## Acceptance criteria

- [ ] The captured contract names the accepted params/filters and the expected entry shape for Key Value logs, with sources.

## Out of scope

- Any implementation.
