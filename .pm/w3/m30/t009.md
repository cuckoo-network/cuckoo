---
id: w3/m30/t009
title: Test coverage — meaningful tests for the shipped behavior
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m30/t007]
---

## Objective

Add meaningful tests for what this milestone shipped: keyvalue log query authorization and fallback behavior, adapter consistency, the `databaseLogs` label fields, and the dashboard viewer's states. Tests must assert real behavior and failure modes.

## Context

- Backend: mirror `lego/backend/internal/logs/logs_test.go` + `postgres/logs_test.go` patterns.
- Dashboard: mirror `postgres-log-viewer.test.tsx`.

## Steps

1. Backend tests: keyvalue routing/authz/fallback; GraphQL label fields.
2. Dashboard tests: viewer states.

## Files

- `lego/backend/internal/keyvalue/logs_test.go`, `postgres` test files
- `dashboard/src/features/keyvalue/**/__tests__/`

## Acceptance criteria

- [ ] New tests fail if keyvalue authz, fallback honesty, or the GraphQL label fields regress.

## Out of scope

- Coverage-gaming trivial tests.
