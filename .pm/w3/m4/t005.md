---
id: w3/m4/t005
title: "Simplify — run /simplify over the code this milestone changed"
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m4/t004]
---

## Objective

Run `/simplify` over the code touched by this milestone (metricsource.go, metrics.go, main.go, prometheus.yaml) — reuse, simplification, efficiency; behavior-preserving. The two Prometheus sources (request + resource) likely share query/HTTP plumbing worth factoring.

## Context

- Standing closing task (board convention).

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; `make test` + `make lint` green.

## Files

- `operator/internal/api/metricsource.go`, `operator/internal/api/metrics.go`, `operator/cmd/api/main.go`

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly declined.
- [ ] `make test` and `make lint` green afterward.

## Out of scope

- New behavior of any kind.
