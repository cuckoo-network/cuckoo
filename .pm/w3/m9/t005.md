---
id: w3/m9/t005
title: "Dashboard: notification preferences in Settings"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m9/t004]
---

## Objective

Add a notification-preferences control to the dashboard's Settings surface, wired to the t004 API.

## Context

- `dashboard/` — see `dashboard/CLAUDE.md`. Settings → Security & Compliance grouping shipped in `w4/m15`; notification preferences likely belongs alongside general workspace settings, not that security grouping — confirm placement against Render's live dashboard before building.

## Steps

1. Capture Render's live notification-settings page placement/shape (Playwright, same pattern as prior parity captures) to confirm dashboard IA.
2. Add a toggle/checkbox group for deploy started/succeeded/failed under the appropriate Settings section.
3. Wire to the t004 GraphQL query/mutation.

## Files

- `dashboard/` Settings pages + GraphQL queries

## Acceptance criteria

- [ ] Toggling a preference persists and reflects on reload.
- [ ] Placement matches (or deliberately documents divergence from) Render's live dashboard.

## Out of scope

- Per-service notification overrides — workspace-level only for v1.
