---
id: w3/m7/t004
title: Acceptance — suspend + deploy + scale → feed shows each, newest-first, paged; no secrets
worker: worker3
status: todo
estimate: 25m
depends_on: [w3/m7/t002]
---

## Objective

Prove the composition against a real cluster with the store on: run a scripted sequence — deploy, scale, suspend, resume, env-var write — then read the events feed and assert each action appears exactly once, newest-first, with truthful types/timestamps/actor identity; page through with the cursor and assert no duplicates or gaps at the page boundary; assert the env-var event carries the key change but **no value**.

## Steps

1. Script the action sequence against a test App (extend an existing verify script or add `events-verify.sh`).
2. Read the full feed; assert 1:1 correspondence + ordering.
3. Page with limit=2; assert boundary stability. Grep the raw responses for the planted secret value — must be absent.

## Files

- `scripts/` (verify script)

## Acceptance criteria

- [ ] Scripted pass (exit 0) covering correspondence, ordering, cursor stability, and value redaction.

## Out of scope

- Dashboard verification (w5/007).
