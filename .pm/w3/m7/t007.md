---
id: w3/m7/t007
title: Test coverage — composition correctness, cursor stability, redaction, store-less 503
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m7/t005]
---

## Objective

Standing closing task. Meaningful tests: each source type maps to the right event (table-driven over fixture rows), interleaved-timestamp ordering is total and stable, the cursor resumes without duplicates/gaps under inserts between pages, secret/env values never serialize into any event (a planted value must not appear), unknown service 404, non-member 403, store-less 503. Assert real behavior and failure modes — never tautological.

## Acceptance criteria

- [ ] Cursor-boundary and redaction cases have explicit tests; `go test ./...` green in `lego/backend`.
