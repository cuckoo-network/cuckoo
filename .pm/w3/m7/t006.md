---
id: w3/m7/t006
title: Simplify — run /simplify over the code this milestone changed
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m7/t005]
---

## Objective

Standing closing task. Run `/simplify` over the milestone's diff (events package, store view/query, fragments, verify script) — reuse / simplification / efficiency; behavior-preserving only. The two-source merge especially: if the SQL view made the Go-side merge redundant (or vice versa), keep one.

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; `go test ./...` green in `lego/backend`.
