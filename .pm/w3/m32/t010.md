---
id: w3/m32/t010
title: cli-compatibility-checklist rows 238-241 → [x] + cli-compat sandbox leg
worker: worker3
status: todo
estimate: 2h
depends_on: [w3/m32/t009]
---

## Objective

Prove `render ea sandbox create/exec/list/stop` works against bex unmodified and flip the checklist rows from `[-]` 404 to `[x]`.

## Context

- `docs/cli-compatibility-checklist.md:238-241` list `ea sandbox *` → `/v1/sandboxes` → 404 (`[-]` deliberate non-goal, now unblocked by pillar-5 re-open).
- Driven via `scripts/cli-compat.sh` (Hydra token exchange, `RENDER_HOST` → bex-api).

## Steps

1. Add a `cli-compat.sh` sandbox leg that drives the unmodified CLI through create/exec/list/stop.
2. Flip rows 238-241 to `[x]` with the evidence pointer.

## Files

- `scripts/cli-compat.sh`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] Unmodified `render ea sandbox create/exec/list/stop` passes against bex (clean exit, correct wire shape read back).
- [ ] Checklist rows 238-241 `[x]` with evidence.

## Out of scope

- The Render-parity sweep across surfaces (t011).
