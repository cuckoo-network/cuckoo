---
id: w3/m32/t013
title: Test coverage — sandbox client, lifecycle, authz/quota, snapshot round-trip
worker: worker3
status: todo
estimate: 3h
depends_on: [w3/m32/t011]
---

## Objective

Add meaningful tests for the sandbox surface — real behavior and failure modes, not coverage gaming.

## Steps

1. Unit: the in-package OpenSandbox client (Create/Pause/Resume/Delete/Endpoint), template registry, authz + owner stamping, per-tenant quota.
2. Integration (against the t007 k3s substrate): create/exec/pause/resume round-trip; cross-tenant denied; stolen-key scoped; snapshot rootfs-only.

## Files

- `lego/backend/internal/sandbox/` (+tests), `scripts/` (sandbox verify)

## Acceptance criteria

- [ ] Tests assert lifecycle, authz denial, quota, and rootfs-only snapshot semantics.
- [ ] Cross-tenant / stolen-key negative cases covered.
