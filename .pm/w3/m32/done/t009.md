---
id: w3/m32/t009
title: bex-api sandbox feature package + template registry + wiring
worker: worker3
status: done
estimate: 1d
depends_on: [w3/m32/t007, w3/m32/t008, w3/m31/t010]
---

## Objective

Add the `lego/backend/internal/sandbox` feature package — in-package OpenSandbox HTTP client, service verbs (authz + owner stamping + quota), REST `/v1/sandboxes*` (Render `ea sandbox` shapes), MCP `spawn_sandbox`/`sandbox_exec`, GraphQL, a template registry (drops `docker inspect`), wired at the five canonical insertion points (ADR014 D1/D2; ADR042 D2).

## Context

- The operator's `lego/operator/internal/runtime/opensandbox.go` client is unimportable from backend (Go `internal` + the `operator → types ← backend` arrow); backend carries its own copy over `core.DoJSON`/`core.OryTransport`.
- `Create` must be template-based (no `docker inspect`); the 5 insertion points: `cmd/api/main.go` env-read, `api.Deps`, `api.Server`, `NewServer`, `features()`.

## Steps

1. `internal/sandbox/{service,client,rest,mcp,graphql}.go`; port `Pause/Resume/Delete/Endpoint/WaitState`, rewrite `Create` template-based.
2. Template registry (register image+entrypoint).
3. Wire the 5 insertion points; env `BEX_OPENSANDBOX_URL`; the client sends the caller's workspace key minted by t006 (minting/lookup live in t006 — consume, don't re-implement).
4. Per-tenant `<ws>-sandbox` routing via the multi-tenant provider (t006).

## Files

- `lego/backend/internal/sandbox/`, `lego/backend/cmd/api/main.go`, `lego/backend/internal/api/server.go`

## Acceptance criteria

- [ ] `POST /v1/sandboxes` (Render shape) creates a sandbox in the caller's `<ws>-sandbox`; exec/list/stop work; MCP tools registered.
- [ ] Authz (`Authorize`) + owner stamping + per-tenant quota enforced at bex-api.

## Out of scope

- The cli-compat verification leg (t010), Render-parity sweep (t011).
