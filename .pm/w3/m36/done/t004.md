---
id: w3/m36/t004
title: Simplify the changed logs namespace-resolution code
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m36/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- The App and datastore log paths should resolve their namespace through one shared helper rather than repeating the `TenantNamespaces ? resolve : s.Namespace` branch at each call site.
- Reuse the existing `core.Base` resolvers (`AppNamespaceByName`) and mirror the metrics feature's shape rather than inventing a parallel mechanism.

## Steps

1. `/simplify` the diff from t001/t002.
2. Collapse duplicated namespace-resolution logic into the single helper; ensure the App-CR-in-hand path stays direct (`app.Namespace`).

## Files

- `lego/backend/internal/logs/service.go`

## Acceptance criteria

- [ ] No duplicated per-call-site namespace branching remains.
- [ ] Behavior unchanged; `go build ./... && go test ./...` pass.

## Out of scope

- New behavior or coverage (t005).
