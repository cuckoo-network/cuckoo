---
id: w3/m36/t006
title: Closeout
worker: worker3
status: todo
estimate: 10m
depends_on: [w3/m36/t005]
---

## Status: BLOCKED on /ship + deploy

t001–t005 are done (code + regression test landed in the working tree, backend build/test/vet/lint green). This closeout is deliberately held: the DoD's observable end state — logs actually returning for a tenant service on `dashboard.bex.co` — requires the fix to be shipped (`/ship`) and deployed to prod, then a read-only live check. Do not move the milestone to `done/` until that live check passes.

## Objective

Close the milestone once its definition of done is actually met (logs return for tenant services in prod), not merely when the code is written.

## Context

- DoD in the milestone README: tenant-service logs return across all surfaces; datastore logs resolved; regression test in place; shared-namespace mode byte-identical.
- Live check: `dashboard.bex.co/services/srv-d9bj8s3eg85c7390eb9g/logs` shows lines after the fix ships.

## Steps

1. Confirm the DoD holds live (post-`/ship` + deploy).
2. Set every remaining task `status: done`; move each `tNNN.md` to `w3/m36/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
4. Move the whole `w3/m36/` dir to `w3/done/m36/`; check `- [x] **m36**` in `w3/README.md`.
5. Update memory `project_logs_tenant_namespace_bug.md` (mark fixed + shipped commit).

## Files

- `.pm/w3/m36/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] Milestone moved to `w3/done/m36/`; workstream box checked.
- [ ] Live logs confirmed for a tenant service.

## Out of scope

- Retiring `BEX_TENANT_NAMESPACES` / `BEX_MAX_*` (that is m34).
