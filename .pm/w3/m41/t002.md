---
id: w3/m41/t002
title: "Evidence capture: command log / test-output tails into the session record"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m41/t001]
---

## Objective

Attach Codex-style verifiable evidence to the session (ADR047 D4): from the driver's captured stream (m37 t002 session log), extract the command log and test-output tails, store bounded evidence on the session record, and include a digest in the draft-PR body so reviewers can see what the agent ran and what passed.

## Context

- Wave-2's transcript store does not exist yet — evidence here is a bounded extract (size-capped columns/JSON on `agent_sessions`), not the full transcript; keep the shape forward-compatible with the transcript tee.
- Raw stream parts carry terminal output (`includeRawChunks: true`, D3).

## Steps

1. Driver: extract command invocations + trailing output windows from the session log at completion.
2. Store bounded evidence (cap sizes; truncation is explicit, never silent).
3. Render an evidence section into the PR body; expose evidence on the session get across surfaces.

## Files

- `lego/agent-image/driver/`, `lego/backend/internal/agentsession/`

## Acceptance criteria

- [ ] Session get returns command log + test tails; PR body carries the digest
- [ ] Truncation is marked when applied

## Out of scope

- Full transcript store + replay (wave 2, `.pm/w3/009.md`)
