---
id: w3/m11/t012
title: Closeout
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m11/t010, w3/m11/t011]
---

## Objective

Standing closing task: when every other task is complete and the milestone's Definition of Done actually holds (live-verified, not just code-complete), close the milestone.

## Context

- Per `.claude/commands/pm.md`'s `done` subcommand: this task's completion moves the whole milestone to `w3/done/m11/`.

## Steps

1. Confirm the DoD in `README.md` is genuinely met (t008's live verification passed).
2. Set every task's `status: done`, move each `tNNN.md` to `w3/m11/done/`.
3. Mark every row `— **DONE**` and `**Status:** done` in this milestone's `README.md`.
4. Move the whole `w3/m11/` directory to `w3/done/m11/`.
5. Check `- [x]` in `w3/README.md`.

## Files

- `.pm/w3/m11/` (moves to `.pm/w3/done/m11/`)
- `.pm/w3/README.md`

## Acceptance criteria

- [ ] Milestone directory moved, all three status locations in sync.

## Out of scope

- Re-opening scope not already in this milestone's task list.
