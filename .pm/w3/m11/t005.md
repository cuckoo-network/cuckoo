---
id: w3/m11/t005
title: GraphQL — webhookEndpoints/webhookDeliveries + CRUD mutations
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m11/t004]
---

## Objective

Mirror the REST surface from t004 on GraphQL, matching the dashboard-shaped query/mutation naming convention used by every other resource (env vars, custom domains, key-value, etc.).

## Context

- Follow `docs/ADR006-bex-api.md`'s "one Core, three adapters" pattern — the GraphQL resolvers should call the same Core verbs REST does, not reimplement logic.

## Steps

1. `webhookEndpoints`/`webhookEndpoint(id)` queries, `webhookDeliveries(endpointId)` query.
2. `createWebhookEndpoint`/`deleteWebhookEndpoint` mutations (update deferred — delete+recreate is fine for v1, matching several other resources' own omission of PATCH).
3. Regenerate GraphQL codegen artifacts consumed by the dashboard (`dashboard/src/graphql/definitions.ts` etc.).

## Files

- `lego/backend/internal/webhooks/graphql.go`
- dashboard codegen outputs

## Acceptance criteria

- [ ] Same authz scoping as the REST surface.
- [ ] Codegen is committed and matches the schema (no drift, per the pattern other milestones have hit).

## Out of scope

- REST (t004, prerequisite) and MCP (t006).
