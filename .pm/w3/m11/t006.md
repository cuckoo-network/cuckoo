---
id: w3/m11/t006
title: MCP — list_/create_/delete_webhook_endpoint tools
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m11/t004]
---

## Objective

Expose webhook-endpoint management as MCP tools so an agent can wire up integrations itself — a bex superset, since Render's own official MCP server (`render-oss/render-mcp-server` v0.3.0) ships no webhook tools at all.

## Context

- Follow the MCP tool-definition conventions used elsewhere in `lego/backend/internal/*/mcp.go` (e.g. `apikeys/mcp.go`).
- Never expose the signing secret in a tool result after the initial create call.

## Steps

1. `list_webhook_endpoints`, `create_webhook_endpoint`, `delete_webhook_endpoint` tools, delegating to the same Core verbs as REST/GraphQL.
2. Add this to the "bex ahead of Render" section of `docs/ADR018-render-parity.md` once shipped (or note it for the Render-parity closing task, t009, to record).

## Files

- new `lego/backend/internal/webhooks/mcp.go`

## Acceptance criteria

- [ ] Tools registered and callable via the MCP server; secret never appears in `list`/subsequent reads.

## Out of scope

- REST (t004, prerequisite) and GraphQL (t005).
