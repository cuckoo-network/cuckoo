---
id: w3/m17/t003
title: "Dashboard panel renders the series; docs note removed"
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m17/t002]
---

## Objective

The dashboard `DatastoreMetricsPanel` renders `replication_lag` for an HA Postgres (and simply doesn't show it for non-HA, matching the backend omission); remove the "still deferred: … observability" / replication-lag-gated notes in ADR006 (§ Lifecycle) and ADR009 now that it ships.

## Context

- `dashboard/src/features/` — the `DatastoreMetricsPanel` from w3/m10 (it already renders disk/connections; add the lag dimension).
- Docs: `docs/ADR006-bex-api.md:285` ("Still deferred: … observability"), `docs/ADR009-postgresql-management.md`.

## Files

- `dashboard/src/features/` (DatastoreMetricsPanel), `docs/ADR006-bex-api.md`, `docs/ADR009-postgresql-management.md`

## Acceptance criteria

- [ ] Panel shows lag for an HA DB, hides it for non-HA; the deferred notes are gone; dashboard tests updated.
