---
id: w3/m17/t001
title: "Implement ReplicationLagSource against cnpg_pg_replication_lag"
worker: worker3
status: todo
estimate: 45m
depends_on: []
---

## Objective

Implement the `ReplicationLagSource` func: query Prometheus for CNPG's `cnpg_pg_replication_lag` for a given Database's cluster over the requested window/resolution, returning `[]MetricSeries` — the same shape and injection pattern the other `DatastoreMetrics` source funcs (`disk`, `db_connections`, `kv_*`) already use.

## Context

- `lego/backend/internal/metrics/datastore.go` — `ReplicationLagSource` type + `ReplicationLagRequest`; the existing datastore source funcs are the template (grep for the disk/connections implementations wired off `BEX_PROM_URL`).
- Metric name: CNPG exposes `cnpg_pg_replication_lag` (seconds behind primary) per instance — aggregate to the cluster's max lag or per-replica series per how the panel consumes it (check the panel's expectation).

## Files

- `lego/backend/internal/metrics/` (the Prometheus-backed source implementation + its wiring in `main.go`/`server.go`)

## Acceptance criteria

- [ ] The source returns a real series from Prometheus for an HA cluster; unit-tested against a fake Prometheus response.

## Out of scope

- The gate flip in the verb (t002).
