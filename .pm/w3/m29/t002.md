---
id: w3/m29/t002
title: Map the three auto-deploy type values to verb+predicate in pushDown/FilterOf
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m29/t001]
---

## Objective

Wire the `type` filter values `auto_deploy_enabled`, `auto_deploy_disabled`, and `auto_deploy_changed` to the new store predicate from t001 so each resolves to `verbs=[apps.SetAutoDeploy]` **plus** the matching boolean/legacy predicate, instead of all three collapsing to the bare verb match.

## Context

- `lego/backend/internal/events/service.go:260` — `pushDown("auto_deploy_enabled")` / `("auto_deploy_disabled")` currently return verb-only filters; `auto_deploy_changed` falls through the default loop to the same verb.
- `List` (`service.go:427-433`) appends `view(r)` for every returned row with no post-filter — correct once the SQL predicate exists.
- All three surfaces (REST `type` query param, GraphQL arg, MCP `type` field) route through `FilterOf` → `pushDown`, so this one mapping fixes them all.

## Steps

1. Map each of the three type values to verb + predicate in `pushDown`.
2. Update the `pushDown` doc comment so its "nothing accepted is ignored" claim is true again.
3. Verify no other discriminated-at-view-time type has the same verb-only collapse (scan the `pushDown` table against the view-time switch).

## Files

- `lego/backend/internal/events/service.go`

## Acceptance criteria

- [ ] Each of the three type values produces a distinct store filter
- [ ] The doc comment matches the implementation
- [ ] A scan of the remaining type mappings found no sibling collapse (or filed what it found)

## Out of scope

- Surface-level tests (t005)
