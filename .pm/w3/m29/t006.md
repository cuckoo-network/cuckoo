---
id: w3/m29/t006
title: Closeout — verify DoD, sync status, move to done
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m29/t005]
---

## Objective

Close the milestone once the definition of done actually holds: each auto-deploy type filter returns only its own rows on all three surfaces with full-length pages.

## Context

- `.pm/CLAUDE.md` done-conventions; `/pm done` step 4 performs the milestone move.

## Steps

1. Verify the DoD against the shipped behavior (run the tests; exercise one surface live if a dev stack is up).
2. Sync status in task frontmatter, milestone README, workstream README.
3. Move tasks to `done/`, then the milestone to `w3/done/m29/`.

## Files

- `.pm/w3/m29/`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] DoD verified, not assumed
- [ ] Status synced in all three places; milestone moved whole to `w3/done/m29/`

## Out of scope

- New work discovered at closeout — file as notes
