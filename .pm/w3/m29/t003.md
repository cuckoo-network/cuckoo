---
id: w3/m29/t003
title: Render parity — filter semantics and error shapes consistent across REST/GraphQL/MCP
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m29/t002]
---

## Objective

Check the auto-deploy `type` filter behaves identically across REST, GraphQL, and MCP (same accepted values, same narrowing semantics, same error shape for unknown values) and compare against Render's events API behavior; flag any drift as follow-up rather than silently diverging.

## Context

- `lego/backend/internal/events/rest.go`, `graphql.go`, `mcp.go` — all route through `FilterOf`.
- `docs/ADR006-bex-api.md` — one core, thin adapters.
- Render's events `type` vocabulary in the pinned OpenAPI snapshot / `docs/render-artifacts/`.

## Steps

1. Exercise the three type values on each surface; compare results and error shapes for an unknown type.
2. Diff the accepted vocabulary against Render's documented event types.
3. File any drift as inbox notes.

## Files

- `lego/backend/internal/events/` (verification; fixes only if trivially in scope)

## Acceptance criteria

- [ ] The three surfaces agree on vocabulary, semantics, and error shapes
- [ ] Divergence from Render, if any, is filed not silent

## Out of scope

- New event types
