---
id: w3/m29/t005
title: Test coverage — each type returns only its rows on all three surfaces + short-page regression
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m29/t003]
---

## Objective

Add meaningful tests proving the filter contract: each auto-deploy type value returns only its own rows on REST, GraphQL, and MCP; and a short-page regression test proving that with mixed auto-deploy rows in the store, a filtered page is full-length (the filter is in SQL, not a post-query drop).

## Context

- Seed a mix of enabled / disabled / legacy `apps.SetAutoDeploy` audit rows.
- The pre-fix failure mode: `?type=auto_deploy_enabled` returned disabled and legacy rows too.

## Steps

1. Cross-surface tests for the three type values (the existing events cross-surface test pattern).
2. Short-page regression: N enabled + N disabled rows, page limit N, filtered request returns N rows and a correct cursor.

## Files

- `lego/backend/internal/events/*_test.go`
- `lego/backend/internal/store/events_test.go` (if store-level assertions live there)

## Acceptance criteria

- [ ] Tests fail on the pre-fix behavior (verified by reverting the mapping locally or asserting the exact contract)
- [ ] All three surfaces covered
- [ ] Short-page regression present

## Out of scope

- Coverage of unrelated event types
