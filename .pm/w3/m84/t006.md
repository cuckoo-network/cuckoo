---
id: w3/m84/t006
title: "Test coverage"
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m84/t004]
---

## Objective

Add meaningful tests for what this milestone shipped: label boundedness under adversarial paths, streaming passthrough through the wrapper, GraphQL/MCP label folding, and the rules' fire/no-fire cases.

## Context

- Adversarial inputs: ids in paths, unmatched paths with random segments, extremely long operation names, unknown tools — none may create a new series.
- Streaming: an SSE handler behind the middleware must flush per event (assert with a test server, not by inspection).
- Rule tests via promtool fixtures (t003).

## Steps

1. Extend `httpmetrics_test.go` with the adversarial matrix and the SSE flush test.
2. Mutation spot-check: remove the pattern fallback and confirm the cardinality test fails.

## Files

- `lego/backend/internal/api/httpmetrics_test.go`, GraphQL/MCP tests, promtool fixtures

## Acceptance criteria

- [ ] Each behaviour has a failing-when-broken test; suites green.

## Out of scope

- Coverage-number chasing.
