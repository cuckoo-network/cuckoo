---
id: w3/m84/t004
title: "Records: close the ADR088 §6 known gap, ADR010 note, origin-vs-edge correlation recipe on the FUTURE-MAYBE entry"
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m84/t003]
---

## Objective

Bring the records in line with the shipped telemetry so the next incident and the next brainstorm start from the truth.

## Context

- `docs/ADR088-platform-observability-ui.md` §6: replace the "Known gap … no first-party per-route request histogram" paragraph with the series names, label contract, and the two rules; add the api-gateways SLI rows.
- `docs/ADR010-observability.md`: one paragraph pointing platform-side API telemetry to ADR088 (tenant-facing metrics unchanged).
- `.pm/FUTURE-MAYBE.md` "Intermittent `ERR_CONNECTION_CLOSED` on api.bex.co mutations": append the correlation recipe — compare `bex_api_http_requests_total{surface="graphql",status=…}` against Traefik's counters for the same minute; an origin-side completed 2xx with an edge-side closed connection points at the edge/LB idle-timeout race, an origin-side gap points at bex-api.

## Steps

1. Edit the three documents; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR088-platform-observability-ui.md`, `docs/ADR010-observability.md`, `.pm/FUTURE-MAYBE.md`

## Acceptance criteria

- [ ] ADR088 §6 no longer states the known gap; the recipe is on the FUTURE-MAYBE entry; prettier clean.

## Out of scope

- Reproducing the connection-closed issue (its trigger is a recurrence).
