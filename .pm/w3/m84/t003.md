---
id: w3/m84/t003
title: "Scrape keep-list, api-gateways dashboard panels, two origin alert rules, coverage check green"
worker: worker3
status: todo
estimate: 60m
depends_on: [w3/m84/t002]
---

## Objective

Land the origin-side SLIs where operators look: extend the bex-api scrape keep-list so the new families survive ingestion (the m86 death-layer lesson), add origin latency/error panels to the api-gateways dashboard, and add two falsifiable alert rules with panels so `obs-coverage-check.sh` stays green.

## Context

- `deploy/gitops/base/prometheus.yaml`: bex-api scrape job keep-list (widened in `2761b94b2` for agent_session/webhooks — add `bex_api_.*`); rule groups + annotations conventions; `scripts/obs-coverage-check.sh` requires every alert's backing series on a dashboard with compatible matchers.
- `deploy/gitops/base/values/grafana.values.yaml`: api-gateways dashboard (ADR088 §6 SLI table row) — add origin p50/p95/p99 per `surface`, 5xx ratio by `route` with a traffic floor (distinguish zero-errors-with-traffic from no-data, per the falsifiable-green rule), top-N slow routes, GraphQL mutation error ratio, MCP tool latency.
- Rules: `BexApiOriginHighErrorRate` (5xx ratio over 5m above the Traefik threshold's sibling, with `sum(rate(_requests_total[5m])) > 0` guard) and `BexApiOriginLatencyHigh` (p95 by surface above an ADR088-recorded baseline for 10m). promtool rule tests for fire/no-fire.
- Validators: `scripts/gitops-validate.sh`, `helm template` base + prod, `scripts/obs-coverage-check.sh`.

## Steps

1. Keep-list; confirm on dev-3 or prod Prometheus that the families are ingested.
2. Panels; verify every PromQL parses and renders non-empty against a live scrape.
3. Rules + promtool tests; coverage check.

## Files

- `deploy/gitops/base/prometheus.yaml`, `deploy/gitops/base/values/grafana.values.yaml`, rule-test fixtures, `docs/ADR088-platform-observability-ui.md` §6 SLI table

## Acceptance criteria

- [ ] Families ingested (verified against a live Prometheus); panels render; both rules present with panels; `obs-coverage-check.sh` green, waiver list unchanged; promtool tests green.

## Out of scope

- Traefik-side changes; SLO burn-rate alerting beyond the two rules.
