---
id: w3/m44/t003
title: Message flow: user bubbles / agent prose + terminal status
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m44/t001]
---

## Objective

Adjust the conversation rendering so USER turns are right-aligned bubbles with an avatar and AGENT turns are plain full-width prose (markdown/inline-code/links, no agent avatar/bubble) — the Devin flow. Add a terminal status line ("… went to sleep" / session-ended dot) at the end of a settled transcript.

## Context

- `dashboard/src/features/agent-sessions/components/session-conversation-impl.tsx` (the m64 rendering — currently assistant-avatar + user-bubble; Devin drops the agent avatar and uses plain prose).
- Reference: `/Volumes/nvme4tbfish/projects/beancount-io/.../agent-message-list/agent-message-list.tsx`.

## Steps

1. User message row: right-aligned bubble + avatar (keep).
2. Agent message row: plain full-width prose, no avatar/bubble.
3. Replace/augment the current "Session ended." with a Devin-style terminus indicator (dot + label) keyed on terminal phase.

## Files

- `dashboard/src/features/agent-sessions/components/session-conversation-impl.tsx`
- `dashboard/src/features/agent-sessions/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] User turns are bubbles+avatar; agent turns are plain prose; a terminal status line ends a settled transcript.

## Out of scope

- Activity/Thought duration + timeline (t004).
