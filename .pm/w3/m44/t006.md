---
id: w3/m44/t006
title: Evidence behind a side-panel / menu toggle
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m44/t001]
---

## Objective

Move the Evidence panel out of a permanent right column into a side-panel toggle (top-right, Devin-style) or the "…" menu, so the conversation is the primary narrative and evidence is on demand.

## Context

- `dashboard/src/features/agent-sessions/components/evidence-panel.tsx` (bounded evidence: commandLog/testOutput/outputTail/changedFiles + truncation honesty).
- t001 reserves a header affordance (panel toggle / "…").

## Steps

1. Wrap the evidence panel in a toggleable side panel (or menu popover) opened from the header.
2. Ensure it still renders the bounded evidence + honest truncation; closed by default.

## Files

- `dashboard/src/features/agent-sessions/components/evidence-panel.tsx`
- `dashboard/src/routes/agents_.$agentSessionId.tsx`

## Acceptance criteria

- [ ] Evidence is reachable via a header toggle/menu, not a permanent column; still shows bounded evidence + truncation note.

## Out of scope

- Redesigning the evidence content itself.
