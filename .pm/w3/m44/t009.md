---
id: w3/m44/t009
title: Simplify
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m44/t008]
---

## Objective

Run `/simplify` over the code this milestone changed (the full-page layout, composer, message flow, activity/timeline, PR card, evidence panel, sidebar) for reuse/simplification/efficiency — behavior-preserving.

## Context

- `dashboard/src/features/agent-sessions/**` + `dashboard/src/routes/agents_.$agentSessionId.tsx`.

## Steps

1. Review for duplication / over-complex conditionals / dead code; reuse existing repo primitives.
2. Apply only safe, behavior-preserving changes; if nothing meaningful, say so.

## Files

- `dashboard/src/features/agent-sessions/**`, `dashboard/src/routes/agents_.$agentSessionId.tsx`

## Acceptance criteria

- [ ] Simplifications applied or explicitly none-needed; suites still green.

## Out of scope

- Behavior changes.
