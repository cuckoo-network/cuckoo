---
id: w3/m44/t004
title: Worked/Thought for Ns: duration + vertical-timeline steps
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m44/t003]
---

## Objective

Relabel the grouped-activity and reasoning collapsibles to Devin's "Worked for `<Ns>`" and "Thought for `<Ns>`" — collapsed by default with a duration — and render the expanded state as a VERTICAL TIMELINE of the individual steps (connector line + per-step rows: "Viewed CI status…", "ran command…", "chose to stop working").

## Context

- `dashboard/src/features/agent-sessions/components/activity-group.tsx` (the m64 grouped activity, currently "Edited N files" etc.); reasoning group in `session-conversation-impl.tsx`.
- Reference: `/Volumes/nvme4tbfish/projects/beancount-io/.../agent-message-list/tool-activity-group.tsx` (collapsed summary + expandable steps).
- **Durations decision (resolve here):** check whether the m43 v1 transcript carries per-part/step timestamps (`lego/agent-image/driver/src/session.ts`/`stream-hub.ts`). If not, derive elapsed from stream-arrival timing on the client first; only add per-part timing to the driver as a follow-up if the derived value is too coarse. Approximate is acceptable for v1.

## Steps

1. Summary label => "Worked for `<duration>`" / "Thought for `<duration>`"; collapsed by default.
2. Expanded => a vertical timeline (connector line) of steps.
3. Resolve the duration source (transcript timestamp vs derived); record the choice in the milestone open-decisions.

## Files

- `dashboard/src/features/agent-sessions/components/activity-group.tsx`
- `dashboard/src/features/agent-sessions/components/session-conversation-impl.tsx`
- `dashboard/src/features/agent-sessions/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] Groups read "Worked/Thought for Ns", collapsed by default, expand to a vertical-timeline of steps; a duration is shown (real or derived, documented).

## Out of scope

- Backend timestamp emission if derived timing is adequate (file as follow-up).
