---
id: w3/m44/t005
title: Inline PR card in the conversation flow
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m44/t001]
---

## Objective

Render the draft PR as a card WITHIN the conversation flow (Devin-style: PR title, `<repo>#<number> · +A −D · bot`, a review/analyze action) instead of a permanent right-column card.

## Context

- Current `dashboard/src/features/agent-sessions/components/pr-card.tsx` (right-column card); the session view already has `prUrl`/`prNumber`/`headSha` from `useAgentSession`.
- Devin places the PR card inline after the turn that opened it.

## Steps

1. Build/adapt an inline PR card component placed in the transcript when the session has a PR.
2. Include title, `<repo>#N`, diff-stat (+A −D if available), bot author, and a review/open action.

## Files

- `dashboard/src/features/agent-sessions/components/pr-card.tsx`
- `dashboard/src/routes/agents_.$agentSessionId.tsx`

## Acceptance criteria

- [ ] The PR renders as an inline conversation card (not a permanent side column) with title + ref + action.

## Out of scope

- Diff-stat if the API does not expose +/− counts — show what is available, file the gap.
