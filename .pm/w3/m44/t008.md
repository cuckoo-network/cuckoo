---
id: w3/m44/t008
title: Render parity
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m44/t002, w3/m44/t003, w3/m44/t004, w3/m44/t005, w3/m44/t006, w3/m44/t007]
---

## Objective

Compare the redesigned agent-session UI against Devin/Render's session UX and confirm consistency of the underlying data across surfaces. Agent sessions are a **bex extension** (Render has no equivalent) — parity here is a UX comparison + a check that no REST/GraphQL/MCP field/semantics drifted (the change is UI-only).

## Context

- `docs/ADR006-bex-api.md` (one core, thin adapters); `docs/ADR018-render-parity.md`; the Devin screenshot (target UX).
- The data (agentSessions/agentSession/create/steer/resume/cancel/attach + evidence/PR fields) is unchanged by this milestone — verify the UI still exposes it consistently and no adapter drifted.

## Steps

1. Walk the UX against the Devin screenshot; list any deliberate divergences.
2. Confirm REST/GraphQL/MCP for agent sessions are unchanged/consistent (no field/semantic/error drift from the UI work).

## Files

- (review only; file follow-ups as `w3/NNN.md` inbox notes)

## Acceptance criteria

- [ ] UX-parity gaps vs Devin recorded as follow-ups; no REST/GraphQL/MCP drift introduced.

## Out of scope

- New backend fields (file as follow-up).
