---
id: w3/m44/t007
title: Sessions sidebar (New session + Recent)
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m44/t001]
---

## Objective

Fold the `/agents` list into a left sidebar on the session view: a "New session" affordance + a "Recent" sessions list (each row: title + phase/status line + PR ref), so switching sessions stays in the chat view. `/agents` remains reachable as the standalone list.

## Context

- `dashboard/src/features/agent-sessions/hooks/use-agent-sessions.ts` (the list hook) + `components/session-list.tsx` (existing rows/chips).
- Reference: Devin's left sidebar (New session, Recent list with status lines).
- Respect the dashboard's existing chrome/sidebar patterns (`dashboard/src/common/components/dashboard-layout/`).

## Steps

1. Add a session-list sidebar to the session view (New session + Recent, keyed on `useAgentSessions`).
2. Each row: title, phase/status, PR ref; clicking navigates to that session, staying in the chat view.
3. Keep the standalone `/agents` route working.

## Files

- `dashboard/src/routes/agents_.$agentSessionId.tsx`
- `dashboard/src/features/agent-sessions/components/session-list.tsx` (or a new sidebar component)

## Acceptance criteria

- [ ] The session view shows a sidebar with New session + a Recent list; switching sessions stays in-chat; `/agents` still works.

## Out of scope

- Search/filter in the sidebar (file as follow-up if wanted).
