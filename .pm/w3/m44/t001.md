---
id: w3/m44/t001
title: Full-page chat layout + header
worker: worker3
status: done
estimate: 60m
depends_on: []
---

## Objective

Restructure `/agents/$agentSessionId` from the card-grid detail layout into a full-height chat: a scrollable transcript that fills the main pane, with a top header (session title, PR badge `#N`, a "…" menu / right-panel toggle) and space reserved for the bottom composer (t002). This is the structural change every other task builds on.

## Context

- `dashboard/src/routes/agents_.$agentSessionId.tsx` (the flat detail route; currently a card grid: header + conversation card + PR card + evidence panel + steering composer).
- Mirror the full-height chat shell from the beancount-io reference read this session (`/Volumes/nvme4tbfish/projects/beancount-io/dashboard/src/features/ai-agent/pages/agent/page.tsx`): `flex h-full min-h-0 flex-col`, a `flex-1 overflow-y-auto` transcript, a docked footer region.
- Keep `useAgentSession(id)` polling for metadata; the conversation column (`session-conversation.tsx`) stays but is no longer boxed in a fixed-height card.

## Steps

1. Replace the `grid lg:grid-cols-3` body with a full-height flex column: header (top) + scrollable transcript (fill) + composer slot (bottom, wired in t002).
2. Header: back-to-sessions, session title/phase chip, inline PR badge `#N`, and a "…"/panel-toggle affordance (evidence panel target for t006).
3. Remove the fixed `h-[32rem]` conversation card wrapper; the conversation scrolls within the page transcript region.

## Files

- `dashboard/src/routes/agents_.$agentSessionId.tsx`
- `dashboard/src/features/agent-sessions/components/session-detail-header.tsx`

## Acceptance criteria

- [ ] The detail route is a full-height chat (transcript fills the pane; composer docked at bottom; no card grid, no permanent right column).
- [ ] Header shows title + phase + PR badge + a panel/"…" affordance; `yarn typecheck` green.

## Out of scope

- Composer behavior (t002), message styling (t003), PR/evidence content (t005/t006), sidebar (t007).
