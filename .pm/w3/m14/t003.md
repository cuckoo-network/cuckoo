---
id: w3/m14/t003
title: "Deploy detail page: in-flight auto-refresh + live build pane"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m14/t002]
---

## Objective

While a deploy is in a non-terminal status, the deploy detail page polls/refetches the deploy (status + timeline) and switches the build-log pane from the Loki history query to the t002 live tail, falling back to the history view once the build ends (or when the tail refuses).

## Context

- `dashboard/src/features/deploys/` — `deploy-detail-page.tsx`, `deploy-log-panel.tsx`, `use-deploy.ts`, the eleven-status vocabulary from w5/m29/w2/m38.
- The logs feature already has an SSE consumer (`features/logs`, live-tail hook from w5/m6) — reuse it with the `type=build` param.

## Files

- `dashboard/src/features/deploys/`

## Acceptance criteria

- [ ] In-flight deploy: status/timeline update without manual refresh and build lines stream live; terminal deploy: unchanged history behavior; tests for the switchover.

## Out of scope

- The landing navigation (t004).
