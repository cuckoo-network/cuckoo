---
id: w3/m82/t006
title: "Live proof on dev-3 then prod: induced outage/recovery pairs, backup completion, webhook + push delivery"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m82/t004, w3/m82/t005]
---

## Objective

Prove the milestone's definition of done on a running cluster with a disposable fixture: a managed Postgres and a Key Value are taken down and brought back, a backup completes, and each transition arrives exactly once as an event-index row, a signed webhook delivery, and a push inbox row.

## Context

- dev-3: `bash scripts/dev-env.sh 3 up` (own auth stack + `dev-3` namespace; CNPG operator shared). The m78 runbook shape: baseline counts → induce → assert `+1` → restore → assert `+1`, held across ≥3 resyncs.
- Inducing: delete the CNPG primary pod (or scale the CNPG Cluster's instances to 0 and back); for Key Value, scale the Valkey StatefulSet to 0 and back. On-demand backup via the existing Postgres backup verb; assert `postgres_backup_completed` once CNPG reports `completed`.
- Webhook receiver: reuse the receiver pattern from `scripts/webhooks-verify.sh`; push: a registered device/webpush subscription as in m78's live leg.
- Production is off-limits without explicit authorization (the m46/m81 owed-live-probe precedent). Run dev-3 unconditionally; run prod only if authorized, otherwise record the owed run honestly in the milestone README and do not fabricate.
- Never print bearer tokens, kubeconfigs, or webhook secrets in evidence.

## Steps

1. Write `scripts/datastore-events-verify.sh` (turnkey: disposable workspace + datastore + endpoint + subscription; always tears down; exits non-zero on any missing or duplicated fact).
2. Run on dev-3; paste non-secret evidence (counts, ids, timestamps) into the milestone README.
3. If authorized, run on prod against a disposable fixture in a first-party workspace; otherwise record the deferral.
4. Fix anything the live run surfaces before t007.

## Files

- `scripts/datastore-events-verify.sh` (new), `.pm/w3/m82/README.md` (evidence), `docs/ADR052-notifications.md` (if the run exposes drift)

## Acceptance criteria

- [ ] dev-3 run: exactly one unavailable + one available fact per datastore, one backup-completed fact, matching webhook attempts and push inbox rows; suspended datastore produced nothing.
- [ ] Fixture fully torn down; evidence contains no secrets.
- [ ] Prod run recorded as done or as an explicitly owed run.

## Out of scope

- Scheduling this probe (a `w3/m83` concern).
