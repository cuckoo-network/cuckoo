---
id: w3/m82/t009
title: "Test coverage"
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m82/t007]
---

## Objective

Add meaningful tests for the behaviour this milestone shipped, asserting real outcomes and failure modes — never tautologies.

## Context

- Behaviours to pin: exactly-one pair per real transition across resyncs; suspended/provisioning never unavailable; stale-transition rejection for datastores; backup completion idempotent per backup name; restore/upgrade edges once; picker/vocabulary drift (advertised ⊆ producers); push CHECK widening; eligibility by datastore relation; `GET /v1/events/{id}` hydration of a datastore fact across REST/GraphQL/MCP.
- Existing harnesses: `store/*_pg_test.go` (real Postgres), `store/stale_conclusion_pg_test.go`, `webhooks/vocabulary_test.go`, `notifications/push_worker_test.go`, `api/events_surface_test.go`, dashboard `event-picker.test.tsx`.

## Steps

1. Add the tests next to the existing harnesses above; prefer one real-PG test per invariant.
2. Mutation spot-check: break the debounce or the stale guard and confirm the relevant test fails.

## Files

- `lego/backend/internal/store/*_pg_test.go`, `webhooks/*_test.go`, `notifications/*_test.go`, `api/*_test.go`, dashboard tests

## Acceptance criteria

- [ ] Each behaviour above has a failing-when-broken test (mutation spot-check recorded in the commit message).

## Out of scope

- Coverage-number chasing.
