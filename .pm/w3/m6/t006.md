---
id: w3/m6/t006
title: Simplify — run /simplify over what this milestone changed
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m6/t005]
---

## Objective

Standing closing task. Run `/simplify` over the milestone's diff (chart values, rule files, sealed secret manifest, verify script) — reuse / simplification / efficiency; behavior-preserving. Rule files especially: collapse near-duplicate expressions, keep the pack small.

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; `kustomize build` both overlays + `promtool check rules` green.
