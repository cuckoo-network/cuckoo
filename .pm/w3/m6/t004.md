---
id: w3/m6/t004
title: Rule pack — bex invariants (backup staleness, OpenBao sealed, bex-api down, 5xx spike)
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m6/t003]
---

## Objective

The rules that encode bex's own promises: `etcd-backup` and `openbao-backup` CronJob last-success age > 26h (the nightly jobs' staleness window — this is the "silent backup rot = data loss" alert), OpenBao sealed (its env-vars API 503s tenants; seal status via the openbao metrics endpoint or a blackbox probe of `/v1/sys/health`), bex-api health endpoint down >5m, and a Traefik 5xx-rate spike (>5% of requests for 10m — Traefik metrics are already scraped for the request-metrics feature).

## Context

- CronJob staleness: `kube_cronjob_status_last_successful_time` from t002's kube-state-metrics.
- OpenBao sealed-on-restart is a documented failure mode (docs/secrets.md); CI unseals on deploy, but a mid-cycle pod restart seals silently.
- Traefik metrics labels: reuse what `BEX_PROM_URL` request-metrics queries already select on (`lego/backend/internal/metrics/`).

## Steps

1. Author the four rules with actionable descriptions (e.g. sealed → "run scripts/bao-init.sh unseal path / see docs/secrets.md runbook").
2. `promtool check rules`; verify each series exists on the mock cluster (or note prod-only ones honestly).

## Files

- `deploy/gitops/base/values/` (rule files)

## Acceptance criteria

- [ ] All four rules load and reference live series; the backup-staleness alert fires when the CronJob is suspended past its window (verified in t005).

## Out of scope

- Auto-remediation (alerts page humans; CI-driven unseal stays the fix path).
