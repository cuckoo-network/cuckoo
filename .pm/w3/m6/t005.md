---
id: w3/m6/t005
title: Acceptance — synthetically break two rules; notifications arrive and resolve
worker: worker3
status: todo
estimate: 25m
depends_on: [w3/m6/t004]
---

## Objective

Prove the loop end-to-end on the mock cluster (receiver may be a capture webhook locally): break two monitored invariants — suspend the etcd-backup CronJob and fake its last-success age past 26h (or shorten the rule window for the test), and roll a bex-system Deployment to a bad image — then watch both alerts fire on the channel and resolve after the fix.

## Steps

1. Fire: bad-image rollout → not-Ready alert within one evaluation window; fix → resolved notification.
2. Fire: backup-staleness (window-shortened test rule or timestamp fixture) → alert; restore → resolved.
3. Record the observed end-to-end latency (eval interval + group_wait) in the milestone notes.

## Files

- `scripts/` (a small `alerts-verify.sh` if it earns its keep; else documented manual steps in the milestone README)

## Acceptance criteria

- [ ] Two distinct alerts observed firing and resolving on the real channel path; latency recorded.

## Out of scope

- Chaos testing every rule; prod game-days.
