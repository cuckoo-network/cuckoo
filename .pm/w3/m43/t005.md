---
id: w3/m43/t005
title: attach-ticket mint verb (REST/GraphQL/MCP)
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m43/t002]
---

## Objective

`POST /v1/agent-sessions/{id}/attach-ticket` (+ `attachAgentSession` GraphQL mutation + MCP tool): mint a reconnect ticket for a **running** session, closing the page-reload gap (today only create/resume/steer mint).

## Context

- ADR047 Consequences #8 / § D9 target shape. Same authz (`AuthorizeApp`-style `can_operate` via the session's workspace tuple), same claims + 90s TTL + nonce as `withTicket` (`internal/agentsessions/service.go:467-488`).
- Only meaningful for phases with a live sandbox (`running`, and post-t004 mid-turn states); terminal sessions don't need a ticket (replay is still ticket-gated — decide: mint for terminal too so history access stays uniformly ticketed; recommended yes, cheap and consistent).
- Response shape: the existing `{ticket, url, expiresAt}` trio; `url` becomes the same-origin stream path once t006 lands.

## Steps

1. Service method + REST route + GraphQL mutation + MCP tool, per the m39 adapter pattern (byte-identical `View` semantics).
2. 503 gating identical to the other mint verbs (`ticketEnabled()`); list/get remain exempt.
3. Audit-log the mint (web-shell precedent).

## Files

- `lego/backend/internal/agentsessions/{service.go,rest.go,graphql.go,mcp.go}` (+ tests)

## Acceptance criteria

- [ ] Running-session mint succeeds after authz; wrong-workspace caller fails closed; terminal-session behavior matches the recorded decision.
- [ ] Ticket verifies at the t002 listener end to end (integration test).
- [ ] All three surfaces return identical shapes (parity test per m39 precedent).

## Out of scope

- Dashboard consumption (`w1/m64`).
