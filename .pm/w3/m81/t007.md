---
id: w3/m81/t007
title: Test coverage for visibility, timeout, and failure modes
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m81/t005]
---

## Objective

Add meaningful automated coverage for the deleting-resource contract, including normal finalization, stuck cleanup, cross-surface consistency, and regression behavior for active services.

## Context

- The prior list test proved only that deleting Apps disappear from `List`; it did not cover by-id children, dead URL projection, or finalizer failure.
- Tests must assert real behavior and failure modes without tautological snapshots or long wall-clock waits.

## Steps

1. Add service-layer and adapter tests for deleting and active Apps.
2. Add operator tests for successful cleanup, timeout/failure, retry, and finalizer removal.
3. Add dashboard tests for the selected terminal/error behavior and return navigation.
4. Run the affected backend, operator, and dashboard suites.

## Files

- `lego/backend/internal/apps/service_deletion_test.go` (new)
- `lego/operator/internal/controller/app_controller_deletion_test.go` (new)
- `dashboard/src/features/services/components/__tests__/service-detail-dead-id.test.tsx`

## Acceptance criteria

- [ ] Tests fail against the pre-m81 behavior for the intended reason.
- [ ] Normal deletion, finalizer overrun, dead-URL omission, and active-service controls are covered.
- [ ] REST children, GraphQL, MCP, and dashboard contract drift is guarded.
- [ ] Affected backend, operator, and dashboard test suites pass.

## Out of scope

- Inflating coverage with snapshots that do not assert lifecycle semantics.
