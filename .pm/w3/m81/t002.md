---
id: w3/m81/t002
title: Apply deletion visibility and dead-URL semantics across all surfaces
worker: worker3
status: todo
estimate: 60m
depends_on: [w3/m81/t001]
---

## Objective

Implement t001's deleting-resource visibility contract consistently across the shared service projection and every by-id tenant surface, including removal of a URL whose route and certificate are already withdrawn.

## Context

- Live evidence showed 200 responses from service, routes, deploys, headers, and GraphQL after the list row and serving route were gone.
- MCP shares parts of the REST projection but must be verified explicitly; the dashboard currently turns the residual record into `Service Unknown`.
- Centralizing deletion visibility in the service layer is preferable to route-by-route drift where the architecture permits it.

## Steps

1. Implement the chosen deletion predicate/projection in the narrowest shared layer.
2. Apply it to REST service/routes/deploys/headers, GraphQL service, and MCP reads.
3. Make URL projection follow serving availability during deletion.
4. Update the dashboard detail behavior and error taxonomy to match the API contract.

## Files

- `lego/backend/internal/apps/service.go`
- `lego/backend/internal/apps/rest.go`
- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/apps/mcp.go`
- `dashboard/src/routes/static.$serviceId.tsx`
- `dashboard/src/routes/services.$serviceId.tsx`
- `dashboard/src/features/services/components/service-detail-layout.tsx`
- `dashboard/src/features/services/hooks/use-server.ts`

## Acceptance criteria

- [ ] List and every by-id surface agree for an App with a `DeletionTimestamp`.
- [ ] No response advertises a platform URL after its route/certificate have been withdrawn.
- [ ] REST service/routes/deploys/headers, GraphQL, MCP, and dashboard behavior are each tested or directly verified.
- [ ] Non-deleting services and datastore resource families do not regress.

## Out of scope

- Bounding the operator finalizer; t003 owns teardown timing and failure projection.
