---
id: w3/m27/t002
title: Serve Render's routes — /v1/webhooks CRUD with full-body PATCH (bex paths stay as aliases)
worker: worker3
status: todo
estimate: 60m
depends_on: [w3/m27/t001]
---

## Objective

bex answers Render's webhook management routes: `POST/GET /v1/webhooks`, `GET/PATCH/DELETE /v1/webhooks/{id}` — with the PATCH accepting Render's full update body (`name`/`url`/`enabled`/`eventFilter`), while the existing `/v1/webhooks/endpoints…` routes keep working as aliases.

## Context

- Current routes + enabled-only PATCH: `lego/backend/internal/webhooks/rest.go:121-183`.
- Core has create/list/get/setEnabled/delete verbs; a full-update Core verb is new (name/url/eventFilter mutation on an existing endpoint — decide whether changing `url` resets failure/auto-disable state).
- Field mapping per t001 (`eventTypes` ↔ `eventFilter`); w2/m36's cron precedent keeps historical aliases alive alongside the Render-shaped route (`docs/ADR006-bex-api.md:113`).
- Watch for a route conflict: `POST /v1/webhooks/git` (inbound push webhook) must keep working — `POST /v1/webhooks` and `/v1/webhooks/git` are distinct patterns under Go 1.22 mux, verify precedence.

## Steps

1. Add a Core `UpdateWebhookEndpoint` verb (validated URL — reuse the w7/m35 SSRF guard; eventFilter validated against the served vocabulary).
2. Register the Render-path handlers; route both path families through the same handlers.
3. Wire the full-body PATCH on both `/v1/webhooks/{id}` and `/v1/webhooks/endpoints/{id}`.
4. Confirm `POST /v1/webhooks/git` still matches its own handler.

## Files

- `lego/backend/internal/webhooks/rest.go`
- `lego/backend/internal/webhooks/service.go`

## Acceptance criteria

- [ ] `curl` against `/v1/webhooks` create/list/get/patch/delete round-trips with Render's field names
- [ ] Full PATCH updates name/url/enabled/eventFilter; old enabled-only bodies still work on the alias
- [ ] `/v1/webhooks/git` HMAC path unaffected (existing tests pass)

## Out of scope

- `/events` read (t003); GraphQL/MCP (t004)
