---
id: w3/m27/t005
title: Update ADR018 webhooks row + § bex-ahead and ADR006; check official CLI/MCP for webhook commands
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m27/t002, w3/m27/t003, w3/m27/t004]
---

## Objective

The ledger stops claiming Render manages webhooks dashboard-only: ADR018's Outbound-event-webhooks row (line ~149) and the § bex-ahead webhook-management entry (line ~169) are rewritten against the new spec, and ADR006 §Outbound event webhooks documents the Render-path surface.

## Context

- `docs/ADR018-render-parity.md:149` ("verified live render.com/docs/webhooks 2026-07-12") and `:169` ("Render's public docs manage webhooks from the dashboard only … its official MCP server ships no webhook tools") both predate the spec's `/webhooks` family.
- Re-check render-oss/render-mcp-server and the official CLI at current revisions: if either gained webhook commands, note it in the row and in `docs/cli-compatibility-checklist.md` (a new CLI row if applicable).

## Steps

1. Rewrite the two ADR018 locations with fresh evidence + capture dates.
2. Update ADR006:218's route/verb enumeration.
3. Re-check the official MCP server + CLI for webhook support; record findings.

## Files

- `docs/ADR018-render-parity.md`
- `docs/ADR006-bex-api.md`
- `docs/cli-compatibility-checklist.md` (only if the CLI gained webhook commands)

## Acceptance criteria

- [ ] No doc claims Render webhooks are dashboard-only; every changed claim carries evidence + date
- [ ] The bex-ahead entry accurately states what (if anything) remains a bex superset (e.g. `/deliveries`, the event-type vocabulary route)

## Out of scope

- Fixing unrelated stale ADR018 rows (w10/m3 owns those)
