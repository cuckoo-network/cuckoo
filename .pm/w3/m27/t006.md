---
id: w3/m27/t006
title: Render parity
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m27/t005]
---

## Objective

Check the shipped webhook surface is consistent across REST, GraphQL, and MCP (same fields, semantics, error shapes) and against Render's actual `/webhooks` behavior per the pinned spec; flag any drift as follow-up work rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` (one core, thin adapters), the t001 pinned artifact doc.
- Dashboard: the Settings → Webhooks panel — confirm nothing this milestone shipped breaks it and note (don't build) any UI follow-up.

## Steps

1. Drive the same operations through all three surfaces; diff field names/semantics/errors.
2. Compare against the pinned Render contracts; file divergences as notes.

## Files

- `docs/ADR018-render-parity.md` (row evidence if drift found)

## Acceptance criteria

- [ ] Cross-surface consistency verified with evidence; any drift filed, none silent

## Out of scope

- Fixes beyond trivial ones (file follow-ups)
