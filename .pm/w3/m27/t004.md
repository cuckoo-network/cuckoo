---
id: w3/m27/t004
title: GraphQL updateWebhookEndpoint + MCP update_webhook_endpoint (full-update parity with REST)
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m27/t002]
---

## Objective

The full-update verb t002 adds to Core is reachable from GraphQL and MCP, retiring the delete+recreate-only limitation on those surfaces.

## Context

- GraphQL mutations today: `createWebhookEndpoint`/`setWebhookEndpointEnabled`/`deleteWebhookEndpoint` (`lego/backend/internal/webhooks/graphql.go:132-158`).
- MCP tools today: `list_/create_/delete_webhook_endpoint` (`lego/backend/internal/webhooks/mcp.go:64-89`).
- Same admin-tier (`can_manage`) authz as the sibling write verbs.

## Steps

1. Add the GraphQL mutation (args mirroring the REST PATCH body).
2. Add the MCP tool with an accurate description (agents are a first-class consumer here).
3. Update ADR006:218's surface enumeration.

## Files

- `lego/backend/internal/webhooks/graphql.go`
- `lego/backend/internal/webhooks/mcp.go`
- `docs/ADR006-bex-api.md`

## Acceptance criteria

- [ ] The same update round-trips through REST, GraphQL, and MCP with identical semantics and error shapes
- [ ] `setWebhookEndpointEnabled` still works (kept or documented as subsumed)

## Out of scope

- Dashboard edit dialog (the panel's enable/disable + delete flows already exist; UI edit is follow-up if the parity walk w5/m32 asks for it)
