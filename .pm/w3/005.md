# "Deploy started" notification toggle

w3/m9 deliberately scoped deploy notifications to succeeded/failed; Render also offers a per-member "deploy started" toggle. Needs a notify hook where the deploy row OPENS (`deploys.Trigger` + the git-webhook path), not the reconciler's close-time `DeployNotifier` — small and well-located, keep the m9 pattern (best-effort, off the hot path, no field shipped inert). Sub-hour.

Filed rather than scheduled: w3's queue is full (m5, m11–m13).

Source: `/pm-brainstorm more milestones for each worker to work on until feature parity` 2026-07-14 (item 8); the ADR018 Notifications row's own "deploy started is not modeled" note.
