---
id: w3/m45/t001
title: Prompt-box create surface (drop the form fields)
worker: worker3
status: done
estimate: 60m
depends_on: []
---

## Objective

Rebuild the `/agents` create surface as the Devin org-home shape: the main pane is ONE prompt box — a large task textarea with a slim toolbar inside the input (`@` mention button placeholder for t002, Configuration popover trigger for t003, Send) — with no visible repo/branch/agent/Advanced form fields. The sessions list is fully carried by the m44 `SessionSidebar`.

## Context

- `dashboard/src/routes/agents.tsx` (currently: header + `NewSessionComposer` form + `SessionList` table).
- `dashboard/src/features/agent-sessions/components/new-session-composer.tsx` (the react-hook-form composer to reshape; keep `createAgentSession` + typed error mapping).
- The m44 sidebar: `components/session-sidebar.tsx` — reuse on `/agents` so the page = sidebar + composer pane (Devin org home).
- Branch auto-derivation: `bex-agent/<slug>` from the task text (slug helper exists — `src/common/lib/utils/slug.ts`); editable via Configuration (t003). Keep client-side `bex-agent/*` validation.
- Evidence: `.playwright-mcp/devin-home.png`.

## Steps

1. Restructure `routes/agents.tsx`: `SessionSidebar` + a centered composer pane (prompt box, in-input toolbar, Send disabled until task text present).
2. Reshape the composer: textarea + toolbar; repo comes from the t002 mention chip (temporary hidden field until t002 lands); branch auto-derives from the task with the `bex-agent/` prefix.
3. On success navigate to `/agents/$id` (unchanged).

## Files

- `dashboard/src/routes/agents.tsx`
- `dashboard/src/features/agent-sessions/components/new-session-composer.tsx`
- locales en/zh

## Acceptance criteria

- [ ] `/agents` renders sidebar + a single prompt box; no visible repo/branch/agent/Advanced fields; create still validates and navigates.

## Out of scope

- The `@` picker (t002); the Configuration popover (t003).
