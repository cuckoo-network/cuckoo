---
id: w3/m45/t006
title: Render parity
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m45/t002, w3/m45/t003, w3/m45/t005]
---

## Objective

UX-parity check vs Devin (bex extension — Render has no equivalent): walk the new composer/picker/sidebar against the evidence screenshots and list deliberate divergences; confirm no REST/GraphQL/MCP drift (the create input shape is unchanged — the UI just gathers `repo`/`branch`/`agentConfig`/`egressAllowlist` differently).

## Context

- Evidence: `.playwright-mcp/devin-{home,at-mention,at-repos,session-sidebar}.png`; `docs/ADR006-bex-api.md`.

## Steps

1. Side-by-side walk; record gaps as `w3/NNN` notes (e.g. more mention categories, mode selector).
2. Confirm the create mutation payload is byte-compatible with the pre-m45 form.

## Files

- (review only; follow-ups as inbox notes)

## Acceptance criteria

- [ ] Divergences recorded; zero API drift.

## Out of scope

- Building the recorded gaps.
