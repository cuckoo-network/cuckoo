---
id: w3/m38/t006
title: "Test coverage: mint scoping, refresh authz, helper behavior, scrub"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m38/t004]
---

## Objective

Meaningful tests for the credential path's real failure modes: mint refused for non-`bex-agent/*` targets and cross-workspace callers, refresh endpoint refuses a wrong-sandbox identity, helper never writes token material and fails loudly when the gateway is unreachable, and the scrub scan catches seeded credential material.

## Context

- Standing closing task. Backend tests live beside the packages (`go test ./...` from `lego/backend/`); gateway tests follow the sandbox-exec test precedent.

## Steps

1. Unit tests: mint policy (branch confinement, repo scoping, audit emit).
2. Gateway endpoint tests: identity binding, cross-workspace refusal, no-token-logging.
3. Helper tests: credential-protocol correctness, no-disk-write, loud failure.
4. Scrub test: seeded secret found by the scan pre-scrub, absent post-scrub.

## Files

- `lego/backend/internal/github/`, `lego/backend/internal/sshgateway/`, `lego/agent-image/` test files

## Acceptance criteria

- [ ] All four failure-mode groups covered and green in CI

## Out of scope

- Live E2E (w3/m41 t004 exercises the full path)
