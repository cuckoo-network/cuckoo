---
id: w3/m38/t005
title: Simplify pass over the credential path
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m38/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (github client extension, gateway endpoint, helper, scrub hooks) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Standing closing task. Render parity omitted for this milestone (internal credential plumbing — see milestone Source + Goal linkage).

## Steps

1. `/simplify` over the touched files; apply accepted findings.

## Files

- `lego/backend/internal/github/`, `lego/backend/internal/sshgateway/`, `lego/agent-image/`

## Acceptance criteria

- [x] Simplify findings triaged and applied or explicitly declined

## Verification

The pass kept one shared `agentsession` contract across bex-api, gateway, and helper; factored the HMAC implementation through one `signatureMAC`; retained the existing sandbox-exec lifecycle transport instead of adding another exec path; made the image non-root with only the enumerated scrub paths writable; and added the standard Git credential-cache socket to the scrub set. No remaining behavior-preserving consolidation justified expanding the trust boundary.

## Out of scope

- Bug hunting
