---
id: w3/m5/t005
title: Acceptance — pod restart loses nothing; bounded time-range queries over REST + MCP
worker: worker3
status: todo
estimate: 25m
depends_on: [w3/m5/t003, w3/m5/t004]
---

## Objective

Prove the milestone's point live: generate distinctive log lines from a running app, kill/restart its pod, and show the pre-restart lines still come back — over REST (`GET /v1/logs`-shaped query) and MCP `list_logs` — plus a bounded time-range query that excludes lines outside the range. _2026-07-11: retargeted from "the mock cluster" to prod — `scripts/mock-cluster.sh` installs no Argo CD/GitOps, so Loki can never sync there; prod runs Loki + log-shipper live on the platform pool with `BEX_LOKI_URL` wired._ _2026-07-13: attempted the live run — `scripts/logs-verify.sh`'s header/requirements fix (shipped, `649b5c6`) is done, but the run itself hit an unrelated active prod incident, not a milestone defect: `bex-controller-manager` is in `CrashLoopBackOff` (informer cache-sync timing out) because 3 `MachineHealthCheck`s are stuck retrying machine creation against an exhausted Hetzner server limit (5/5 servers, 8 desired), loading the API server. With the operator down, no App CR reconciles, so `whoami-logs` never left `Pending` — this blocks the run itself, not what it would prove. User is handling the incident directly; re-run once `bex-controller-manager` is healthy again (`kubectl -n bex-system get pods -l control-plane=controller-manager` should show `Running`/`1/1`, not `CrashLoopBackOff`). _Update same day: the operator has rolled through 3 ReplicaSets so far (`d679bbddd` → `74d4b8b895` → `7c5875b78f`), each reaching `1/1 Ready` for a couple minutes before crashing again — a 2-min stability window is NOT a reliable recovery signal. Two live-run retries both failed at the same `App/whoami-logs never reached Running` step because the operator was mid-crash during the run. The `MachineHealthCheck` remediation loop (`bex-control-plane-unhealthy-5m`/`bex-platform-unhealthy-5m`/`bex-tenant-0-unhealthy-5m`) is still actively retrying against the same exhausted Hetzner server limit — root cause still unresolved. Diagnostic `whoami-diag` App cleaned up manually; no test resources left in `default` (verified against the real customer Apps there: `agentmarketcap`/`beancount-cms`/`eden-cms-v2`/`hello-static`, untouched). Now waiting for 5 continuous minutes of `1/1 Ready` (raised from 2min) before retrying again.__

## Context

- Access path: `HCLOUD_TOKEN=… scripts/fetch-app-kubeconfig.sh <path>` (the canonical prod access path — post-pivot there is no mgmt cluster), then `KUBECONFIG=<path> scripts/logs-verify.sh` — the script is kubeconfig-agnostic.
- Prod safety: the test App `whoami-logs` pulls the public Docker Hub image `traefik/whoami` (a normal outbound pull — the node-local ctr-import constraint only affects bex-built images since zot is empty), lands on the untainted tenant pool, lives in ns `default` (outside the CP projector's namespace), and is deleted on cleanup.
- Operator: prod's in-cluster manager watches cluster-wide and reconciles the App CR — no host operator needed. On the mock the script would hang at the Running wait: its header claims it launches a host operator but it never does (`OP_PID` declared and killed, never assigned).
- Code prerequisite (outside .pm): fix `scripts/logs-verify.sh`'s header/requirements — drop the "bex-api AND the operator run on the host" + "scripts/mock-cluster.sh + GitOps" claims; requirement is "a cluster with Loki synced and an operator reconciling Apps (prod: the in-cluster manager; mock: run `make run` separately)".

## Steps

1. `scripts/fetch-app-kubeconfig.sh` → run `scripts/logs-verify.sh` against prod: it deploys `whoami-logs`, curls it to produce marked lines, and notes the timestamp window.
2. The script's restart phase: `kubectl delete pod` → wait for the replacement → query both surfaces for the pre-restart window; assert the marked lines return.
3. Its fallback phase reruns with `BEX_LOKI_URL` unset to confirm the truthful degraded answer (live logs only).
4. Confirm cleanup left nothing in `default` (the App and its Deployment/Service deleted).

## Files

- `scripts/logs-verify.sh` (header/requirements fix per Context; the script body already fits prod)

## Acceptance criteria

- [ ] Pre-restart lines retrievable after restart via REST and MCP; time-range bounds respected.
- [ ] Unset-fallback verified live.

## Out of scope

- Load testing ingest volume.
