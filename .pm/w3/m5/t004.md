---
id: w3/m5/t004
title: Retention to Render's window + env mirrors + docs caveat swap
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m5/t002]
---

## Objective

Make the durable window a deliberate number: set Loki retention to cover at least Render's searchable logs window (~7 days; verify the current figure against Render's docs and record it), size the PVC accordingly for the single-node reality, mirror any new env names into `.env.example`/`.env.template` (repo rule), and replace docs/observability.md's "v0 = live Kubernetes primitives, no store" caveat with the shipped architecture.

## Context

- Retention knob surfaced in t001's values file; Prometheus precedent keeps 3d for the dashboard's 24h presets — logs need the longer, user-facing window.
- docs/observability.md is the design record w3/001 wrote; it must not describe a superseded architecture (docs-vs-code audit discipline, 2026-07-08).

## Files

- `deploy/gitops/base/values/` (retention/PVC)
- `docs/observability.md`
- `.env.example`, `.env.template` (names only, if new vars)

## Acceptance criteria

- [ ] Retention + PVC sizing set and justified in the values comment; docs describe the Loki-backed reality including the unset-fallback.

## Out of scope

- Cross-host durability of the Loki volume (single-node reality, same caveat as etcd/OpenBao — noted, not solved). _2026-07-11: solved for free by the rebuild — prod's default StorageClass is `hcloud-volumes` (network-attached, re-attaches to a replacement node), so Loki history now survives node loss/replacement; the local-path/data-loss caveat is mock-only. The remaining caveat is single-replica read unavailability during volume re-attach (minutes), not data loss. Comment/docs refresh folded into t009._
