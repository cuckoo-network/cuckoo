---
id: w3/m31/t009
title: Test coverage — namespace lifecycle, per-tenant quota, NetworkPolicy, scale-0
worker: worker3
status: todo
estimate: 2h
depends_on: [w3/m31/t007]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real behavior and failure modes, not coverage gaming.

## Steps

1. Unit/envtest: namespace created/finalized with workspace; per-tenant `ResourceQuota` denies over-cap create; NetworkPolicy default-deny + allows emitted correctly.
2. Integration: a workspace at cap is denied; cross-namespace traffic denied; wake still works post-migration.

## Files

- `lego/backend/internal/store/` (+tests), `lego/operator/internal/controller/` (+envtest), `scripts/verify-*.sh`

## Acceptance criteria

- [ ] Tests assert namespace lifecycle, quota denial at the API server, and NetworkPolicy shape.
- [ ] Tests assert scale-0 still holds (pending pods → autoscale, empty → scale-down).

## Out of scope

- Sandbox tests (m32).
