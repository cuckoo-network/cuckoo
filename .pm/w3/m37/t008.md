---
id: w3/m37/t008
title: "Test coverage: driver spawn/one-turn/listeners + provider vendor tests in CI"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m37/t003, w3/m37/t004, w3/m37/t005, w3/m37/t006]
---

## Objective

Meaningful tests for the behavior this milestone shipped: driver spawn + headless one-turn against the stub ACP agent, listener behavior (SSE stream shape, WS round-trip, no-client headless run), key-injection scrub, and the t005 vendor tests wired into CI so provider bumps can't silently regress.

## Context

- Standing closing task. Tests assert real behavior and failure modes (agent crash → failed status; `loadSession` absent → no `session/load`), never snapshot-everything.
- Driver tests need a JS test runner in the image context; wire into CI alongside the backend workflows (a new job or an existing workflow's step).

## Steps

1. Driver unit/integration tests over the stub agent (turn, listeners, failure paths).
2. CI wiring for the driver test suite.
3. Backend tests for any sandbox-client/template-selection changes.

## Files

- `lego/agent-image/driver/` tests, `.github/workflows/`

## Acceptance criteria

- [ ] Driver suite green in CI; failure modes covered (crash, missing `loadSession`, no-client run)

## Out of scope

- Live-substrate E2E (w3/m41 t004)
