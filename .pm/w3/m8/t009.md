---
id: w3/m8/t009
title: Closeout — DoD met → move milestone to done/
worker: worker3
status: todo
estimate: 10m
depends_on: [w3/m8/t008]
---

## Objective

Standing closing task, run last and only when the DoD actually holds (live request/level/discovery assertions green, nothing silently ignored, matrix row updated). Close the milestone: remaining tasks `status: done`, files to `w3/m8/done/`, rows `— **DONE**` + `**Status:** done`, move `w3/m8/` whole to `w3/done/m8/`, check the box in `w3/README.md`.

## Acceptance criteria

- [ ] DoD verified before the move; board synced in all three places; `w3/m8/` gone from its old path.
