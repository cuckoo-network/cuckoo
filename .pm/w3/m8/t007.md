---
id: w3/m8/t007
title: Simplify — run /simplify over the code this milestone changed
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m8/t006]
---

## Objective

Standing closing task. Run `/simplify` over the milestone's diff (Traefik/Alloy config, LogQL builder extensions, discovery reads, verify script) — reuse / simplification / efficiency; behavior-preserving only.

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; `go test ./...` green in `lego/backend`; both overlay builds green.
