---
id: w3/m16/t002
title: "Projection: required from/to emitted; auto_deploy events discriminated"
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m16/t001]
---

## Objective

In `internal/events`' read-time projection: `plan_changed`/`instance_count_changed`/`autoscaling_config_changed` emit `details.from`/`details.to` from the recorded typed fields (spelled exactly as Render's OpenAPI defines them); the single `auto_deploy_changed` type splits into `auto_deploy_enabled`/`auto_deploy_disabled` by the recorded boolean. A legacy audit row with no recorded details keeps emitting the event **without** the fields — omitted, never fabricated.

## Files

- `lego/backend/internal/events/`

## Acceptance criteria

- [ ] New rows project the required fields; legacy rows omit them; the two auto-deploy names replace the bex-named collapse.

## Out of scope

- Divergences (3) and (4) in the ledger row (bex-named types; operator-driven omissions) — recorded rationale stands.
