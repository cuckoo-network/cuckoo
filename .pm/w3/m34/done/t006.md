---
id: w3/m34/t006
title: "Test coverage: caps via ResourceQuota; no shared-ns regressions"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m34/t004]
---

## Objective

Add/adjust tests so the retirement is safe and stays safe: the plan-tier caps are enforced (now via `ResourceQuota`), the cap-rejection error is Render-shaped (t004), and no code path silently depends on the deleted shared-namespace branches.

## Context

- The w3/m32 regression test `internal/apps/tenant_namespace_test.go` currently toggles `TenantNamespaces: true`; update it to the unconditional default (the toggle is gone after t002).
- `store/namespaces.go` `quotaForPlan` count caps should have a unit test asserting `count/apps.app.bex.co` etc. match the plan catalog.
- The cap-rejection mapping from t004 needs a test across the create verb.

## Steps

1. Rework `tenant_namespace_test.go` to assert the now-default per-tenant behavior (no flag).
2. Unit-test `quotaForPlan` count caps against the plan catalog (hobby/pro/scale/enterprise).
3. Test create-past-cap returns the Render-shaped error (t004) — at least at the service layer.

## Files

- `lego/backend/internal/apps/tenant_namespace_test.go`
- `lego/backend/internal/store/namespaces_test.go` (new or extend)
- `lego/backend/internal/apps/*_test.go` (cap-rejection)

## Acceptance criteria

- [ ] Tests assert real behavior (quota values, cap error shape), not tautologies.
- [ ] Full backend suite green.

## Out of scope

- E2E cluster tests of ResourceQuota admission (covered by w3/m31's verification).
