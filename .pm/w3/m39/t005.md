---
id: w3/m39/t005
title: "Render parity: cross-surface consistency (REST/GraphQL/MCP fields, errors, ea naming)"
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m39/t004]
---

## Objective

Standing parity check: Render has no coding-agent product, so parity here means bex's own cross-surface discipline — identical fields, semantics, and error shapes across REST, GraphQL, and MCP; Render's REST error dialect on the REST surface; `ea`-style early-access naming per the w3/m32 sandbox precedent. Flag any drift as follow-up work rather than silently diverging; record the bex-extension status in `docs/ADR018-render-parity.md`.

## Context

- docs/ADR006-bex-api.md (one core, thin adapters); ADR018 ledger gains an agent-sessions row marked bex-extension (the usage-API precedent).

## Steps

1. Field/error diff across the three surfaces; fix or file drift.
2. Add/extend the ADR018 row + cli-compat note if the Render CLI grows a relevant `ea` verb.

## Files

- `lego/backend/internal/agentsession/`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Three surfaces consistent; ledger row recorded

## Out of scope

- Dashboard UI (wave-2 w5 milestone)
