---
id: w3/m39/t007
title: "Test coverage: authz, lifecycle, ticket claims, cross-workspace isolation"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m39/t005]
---

## Objective

Meaningful tests for the session surface's real behavior and failure modes: FGA-denied cross-workspace access on every verb, lifecycle phase transitions (including cancel from each phase), ticket claim binding + nonce single-use + expiry, missing-BYO-key named error, and env-ungated 503s — across the store and all three adapters.

## Context

- Standing closing task. Integration tests gated on `BEX_TEST_DB_URI`/`BEX_TEST_OPENFGA_URL` run for real in CI (backend-test.yml) — use them for the FGA + store paths.

## Steps

1. Store + service tests (phases, cancel, id kind).
2. FGA isolation tests (two workspaces).
3. Ticket tests (claims, nonce reuse rejected, expiry).
4. Adapter-consistency tests (REST/GraphQL/MCP same shape, error dialects).

## Files

- `lego/backend/internal/agentsession/` test files

## Acceptance criteria

- [ ] All groups green in CI including the real-Postgres/OpenFGA integration path

## Out of scope

- Live-substrate E2E (w3/m41 t004)
