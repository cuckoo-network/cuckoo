---
id: w3/m42/t005
title: "Test coverage: pause/resume lifecycle + resume-path behavior"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m42/t003]
---

## Objective

Meaningful tests for the lifecycle behavior: suspend/resume state transitions in the client (including resume-of-never-suspended and suspend-of-terminated → named errors), session resume-mode selection (`loadSession` advertised vs not vs no snapshot), and the phase-signal accuracy the metering contract depends on.

## Context

- Standing closing task. Substrate-level verification is t002's live run; these tests lock the client/service logic hermetically.

## Steps

1. Lifecycle-client tests (transitions + error paths, stubbed OpenSandbox API).
2. Resume-mode selection tests (three modes).
3. Phase-signal test (hibernated window excluded per the w7/m76 contract shape).

## Files

- `lego/backend/internal/sandbox/`, `lego/backend/internal/agentsession/` test files

## Acceptance criteria

- [ ] All transition + mode-selection paths covered and green in CI

## Out of scope

- Live substrate checks (t002/t003)
