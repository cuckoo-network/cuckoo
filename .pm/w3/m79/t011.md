---
id: w3/m79/t011
title: "Closeout"
worker: worker3
status: todo
estimate: 10m
depends_on: [w3/m79/t009, w3/m79/t010, w3/m79/t012]
---

## Objective

Close the milestone once its definition of done actually holds — not merely when the code is written.

## Context

- DoD (see milestone README): `dev-env.sh N up --agents` flips all three gates, `status` asserts `capabilities.enabled=true` + component health, and a real-cred session runs to a draft PR against a local `<ws>-sandbox` sandbox; the runbook matches shipped behavior.

## Steps

1. Verify the DoD end to end on a live `dev-N` with real creds.
2. Set every remaining task `status: done`, mark rows `— **DONE**` and `**Status:** done` in the milestone README, move each `tNNN.md` to `done/`, move `w3/m79/` to `w3/done/m79/`, check `- [x]` in `.pm/w3/README.md`.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w3/README.md`, `.pm/w3/m79/**`.

## Acceptance criteria

- [ ] DoD verified live; milestone moved to `w3/done/m79/`; workstream README checked.

## Out of scope

- Any new work (file as follow-up inbox notes instead).
