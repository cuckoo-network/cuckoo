---
id: w3/m79/t008
title: "status/verify agent stack + end-to-end create-session smoke"
worker: worker1
status: todo
estimate: 45m
depends_on: [w3/m79/t007]
---

## Objective

Extend `dev-env.sh N status` with agent-stack assertions and add an end-to-end smoke that proves a session runs to a draft PR.

## Context

- The verification inventory already prints `[ok]`/`[FAIL]` and exits non-zero on failure (`scripts/dev-env.sh:548-594`).
- The gates surface through `Capabilities{Enabled: …}` (`internal/agentsessions/service.go:537`) — reachable via REST/GraphQL capabilities.
- `scripts/agent-session-verify.sh` is the prod E2E; adapt its shape for the local URLs (not wired into CI).

## Steps

1. Add assertions: OpenFGA reachable + store `bex` present; OpenBao unsealed; OpenSandbox `/health`; gateway pod Ready + aux `/healthz`; `capabilities.enabled == true`.
2. Add a smoke path (behind a flag / separate script): create a session against the developer's disposable repo, wait for the sandbox pod Ready in `<ws>-sandbox`, assert the attach stream emits `data-acp` parts and one turn completes, and confirm a draft PR is opened.
3. Make failures actionable (which gate/component is down).

## Files

- `scripts/dev-env.sh` (status assertions), a local `scripts/agent-session-verify-local.sh` (or reuse with env overrides).

## Acceptance criteria

- [ ] `dev-env.sh N status` reports `capabilities.enabled=true` and green gateway/OpenSandbox/OpenFGA when up; red + actionable when a component is down.
- [ ] The smoke creates a session that reaches a live turn and a draft PR (with real creds).

## Out of scope

- Wiring the smoke into CI (needs real creds); hibernation/archive coverage.
