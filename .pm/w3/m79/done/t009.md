---
id: w3/m79/t009
title: "Simplify"
worker: worker1
status: done
estimate: 20m
depends_on: [w3/m79/t008]
---

## Objective

Run `/simplify` over the code this milestone changed (the `dev-env.sh` agents overlay, its templates, and helper scripts): reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- Watch for duplication against the existing `forward`/`render`/`dsn`/pid-supervision helpers in `scripts/dev-env.sh` — new bring-up should reuse them, not fork variants (the m72 anti-pattern the single implementation replaced).

## Steps

1. `/simplify` the milestone diff; apply behavior-preserving cleanups.
2. Confirm default `up` remains byte-identical and `dev-env.test.sh` still passes.

## Files

- `scripts/dev-env.sh`, `scripts/dev-env/`, milestone helper scripts.

## Acceptance criteria

- [ ] No behavior change; reuse of existing helpers maximized; `dev-env.test.sh` green.

## Out of scope

- Bug hunting (that's `/code-review`); new features.
