---
id: w3/m79/t008
title: "status/verify agent stack + end-to-end repo-less session smoke"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m79/t007]
---

## Objective

Extend `dev-env.sh N status` with agent-stack assertions and prove a repo-less session runs end to end through a live sandbox, provider turn, transcript persistence, and attach replay.

## Context

- The verification inventory already prints `[ok]`/`[FAIL]` and exits non-zero on failure (`scripts/dev-env.sh:548-594`).
- The gates surface through `Capabilities{Enabled: …}` (`internal/agentsessions/service.go:537`) — reachable via REST/GraphQL capabilities.
- `scripts/agent-session-verify.sh` is the prod E2E; adapt its shape for the local URLs (not wired into CI).

## Steps

1. Add assertions: OpenFGA reachable + store `bex` present; OpenBao unsealed; OpenSandbox `/health`; gateway pod Ready + aux `/healthz`; `capabilities.enabled == true`.
2. Run a repo-less session, wait for the sandbox pod Ready in `<ws>-sandbox`, assert the attach stream emits `data-acp` parts, and confirm one provider turn completes and replays durably.
3. Make failures actionable (which gate/component is down).

## Files

- `scripts/dev-env.sh` (status assertions), a local `scripts/agent-session-verify-local.sh` (or reuse with env overrides).

## Acceptance criteria

- [x] `dev-env.sh N status` reports `capabilities.enabled=true` and green gateway/OpenSandbox/OpenFGA when up; red + actionable when a component is down.
- [x] A real-model repo-less smoke reached `completed`; its attach replay returned 15 parts including five `data-acp-*` parts and `[DONE]` from the correct `<ws>-sandbox` pod.

## Out of scope

- GitHub App delivery and draft-PR proof (t012); wiring live-provider smoke into CI; hibernation/archive coverage.
