---
id: w3/m79/t010
title: "Test coverage: dev-env.test.sh derivation for the new ports/env"
worker: worker1
status: done
estimate: 30m
depends_on: [w3/m79/t008]
---

## Objective

Extend `scripts/dev-env.test.sh` to cover the new per-N derivation (agent-stack ports, the reverse-hop address, and the agent env the overlay injects), asserting real behavior and cross-N isolation — not tautologies.

## Context

- `scripts/dev-env.test.sh` tests the pure derivation (ports/namespaces/identity) without a cluster; the derivation is the one thing testable offline.
- New per-N values (OpenFGA/OpenBao/gateway-forward ports) must stay collision-free across `dev-1..dev-10`, exactly like the existing port block.

## Steps

1. Add assertions that the new per-N ports derive from N and don't collide across N (mirror the existing offset checks).
2. Assert the agents-path env wiring is present/consistent (e.g. gateway URL vs forwarded port; `BEX_ALLOW_INSECURE_AUTHZ` dropped under `--agents`).

## Files

- `scripts/dev-env.test.sh`.

## Acceptance criteria

- [ ] New tests fail if a new per-N port collides or the agents env wiring drifts; green on the shipped code.

## Out of scope

- Live-cluster integration tests (covered by t008's smoke, not offline-testable).
