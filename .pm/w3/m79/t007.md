---
id: w3/m79/t007
title: "dev-env.sh N agents overlay: orchestrate the substrate + inject bex-api env"
worker: worker1
status: todo
estimate: 60m
depends_on: [w3/m79/t001, w3/m79/t003, w3/m79/t004, w3/m79/t006]
---

## Objective

Fold t001–t006 into one opt-in path on `scripts/dev-env.sh` (e.g. `dev-env.sh N up --agents` or an `agents` subcommand), and inject the full agent-session env into the locally-launched bex-api so all three gates flip. Default `up` stays byte-identical (opt-in only).

## Context

- bex-api launch block: `scripts/dev-env.sh:410-454`. Today it sets a stub GitHub App (id `1`) and `BEX_ALLOW_INSECURE_AUTHZ=1`.
- New vars to inject: `BEX_OPENSANDBOX_URL`, `BEX_SANDBOX_EXEC_SECRET`, `BEX_SANDBOX_EXEC_URL`, `BEX_SHELL_TICKET_SECRET`, `BEX_AGENT_SESSION_GATEWAY_URL`, `BEX_AGENT_MODEL_PROXY_URL`, `BEX_AGENT_GIT_PROXY_URL`, `BEX_AGENT_SESSION_IMAGE`, `BEX_OPENFGA_URL` (+ drop `BEX_ALLOW_INSECURE_AUTHZ`), `BEX_OPENBAO_URL`, and a **real** `BEX_GITHUB_APP_ID/_PRIVATE_KEY/_SLUG` (developer-supplied via override.env / env, never committed).
- Reuse the existing `forward`/pid/log supervision and `override.env` seam. Real creds come from `override.env` or the environment — the stub GitHub App path stays the default when `--agents` is off.

## Steps

1. Add the opt-in flag/subcommand; when set, run the t001–t006 bring-up (fga/bao → cilium CRD → controller → agent image → gateway → reverse hop → host OpenSandbox) after the base env.
2. Extend the bex-api launch with the new vars; source real GitHub App + provider key from `override.env`/env; fail clearly if they're missing under `--agents`.
3. Print the dashboard command and the agent-session URLs; keep default `up` unchanged.

## Files

- `scripts/dev-env.sh`, `scripts/dev-env/` templates, `.pm/wN/dev-N/override.env` documentation.

## Acceptance criteria

- [ ] `dev-env.sh N up` (no flag) is byte-identical to today.
- [ ] `dev-env.sh N up --agents` starts bex-api with all agent vars set and `BEX_ALLOW_INSECURE_AUTHZ` unset; process stays up.
- [ ] Missing real creds under `--agents` fail fast with an actionable message.

## Out of scope

- The status/verify assertions + smoke (t008).
