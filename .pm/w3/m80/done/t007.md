---
id: w3/m80/t007
title: "Simplify: run /simplify over the changed code"
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m80/t006]
---

## Objective

Standing closing task. Run `/simplify` over everything this milestone changed (error link, error-surface taxonomy, session state, draft helper, Kratos values) — reuse / simplification / efficiency only, behavior-preserving. Watch especially for duplicated 401-classification logic between the client link, SSR loaders, and hooks: it should live in exactly one module.

## Context

- Changed surface: `dashboard/src/common/apollo/`, `dashboard/src/common/hooks/`, `dashboard/src/common/server-fn/session.ts`, `dashboard/src/features/services/`, `deploy/gitops/base/values/kratos.values.yaml`.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted cleanups; re-run `dashboard/ yarn test`.

## Files

- (as flagged by the review)

## Acceptance criteria

- [ ] `/simplify` run over the milestone diff; accepted findings applied; tests still green.

## Out of scope

- Behavior changes; bug hunting (that's /code-review).
