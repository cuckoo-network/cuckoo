# Agent-session PR diff stats (+A −D) missing from the API

Found researching Devin (2026-08-05): its inline PR card shows `+1 −1` diff stats. Our agent-session surface exposes `prNumber`/`prUrl`/`headSha` but no additions/deletions, so the m44 inline PR card can't show them (gap noted at m44 t005).

Fix: when the Completer opens the draft PR (`lego/backend/internal/agentsessions/completion.go` → `OpenDraftPullRequest`), capture the PR's `additions`/`deletions` (the GitHub create-PR response carries them; else one GET) and persist on the session row; expose as `prAdditions`/`prDeletions` across REST/GraphQL/MCP + the dashboard splice; render in the m44 `pr-card.tsx` as `+A −D`.

Sub-hour-ish backend+surface change; promote if it grows.
