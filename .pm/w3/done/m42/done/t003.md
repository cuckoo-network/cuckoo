---
id: w3/m42/t003
title: "Session resume path: driver restart + session/load when the agent advertises it"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m42/t002]
---

## Objective

Make agent-session resume use real hibernation (ADR047 D1/D3): resume restores the rootfs (agent's own on-disk session state survives) and restarts the driver, which passes `existingSessionId` so the provider calls ACP `session/load` — **only** when the agent advertised `loadSession`; otherwise the honest fallback runs and the mode is recorded on the session. Credential scrub/re-fetch (m38 t004, m37 t004) must hold through the cycle.

## Context

- Provider contract vendor-tested in w3/m37 t005; steering currently re-dispatches (w3/m41 t003) — this task switches the preferred path to resume when a snapshot exists.
- Hibernated sessions stop accruing `sandbox_compute_seconds` (w7/m76 contract) — the phase signal must be accurate.

## Steps

1. Session lifecycle: idle/hibernate trigger + resume-on-steer preferring the snapshot path.
2. Driver restart flow with `existingSessionId`; mode recording (`resumed-loaded` / `resumed-fresh` / `redispatched`).
3. Live check: hibernate a session, steer it, confirm continuity (or honest fallback) + credential re-fetch.

## Files

- `lego/backend/internal/agentsession/`, `lego/agent-image/driver/`

## Acceptance criteria

- [ ] Resume-with-`loadSession` continuity demonstrated live with a supporting agent; fallback mode honest when unsupported
- [ ] No credential material survives the cycle; phase signal accurate for metering

## Out of scope

- Transcript-replay reattach (wave 2)
