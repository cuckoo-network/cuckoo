---
id: w3/m81/t006
title: Simplify the changed lifecycle code
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m81/t005]
---

## Objective

Run `/simplify` over the code changed by m81 and apply behavior-preserving reuse, clarity, and efficiency improvements.

## Context

- Deletion predicates and error mapping can drift when repeated across service children and API adapters.
- Operator finalizer paths are already complex; the pass must reduce duplication without weakening failure visibility.

## Steps

1. Run `/simplify` over the m81 diff.
2. Consolidate repeated deletion-state checks and projection helpers where ownership remains clear.
3. Re-run the affected tests after every material cleanup.

## Files

- `lego/backend/internal/apps/service.go`
- `lego/backend/internal/apps/rest.go`
- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/apps/mcp.go`
- `lego/operator/internal/controller/app_controller.go`
- `dashboard/src/features/services/components/service-detail-layout.tsx`

## Acceptance criteria

- [x] `/simplify` findings are applied or explicitly dispositioned.
- [x] Repeated lifecycle checks use the narrowest appropriate shared helper.
- [x] Behavior and error contracts remain unchanged after the pass.

## Out of scope

- Broad file splitting or unrelated controller refactors.
