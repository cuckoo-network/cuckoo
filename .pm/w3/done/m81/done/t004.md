---
id: w3/m81/t004
title: Live deletion timing + cross-surface E2E
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m81/t002, w3/m81/t003]
---

## Objective

Prove the implemented lifecycle contract with a disposable static site in a real cluster, measuring deletion timing and checking every tenant surface through the terminal outcome.

## Context

- The original fixture eventually finalized, so source inspection alone cannot prove the new timing or failure behavior.
- The prior production capture included REST, GraphQL, dashboard, TLS, and control resource types; reuse that evidence shape without retaining a fixture.

## Steps

1. Create and deploy a disposable static site with a route/header and at least one deploy.
2. Delete it and poll list, service/routes/deploys/headers, GraphQL, MCP, dashboard, and the public hostname.
3. Record the time to the documented terminal state and verify no dead URL survives in tenant-facing data.
4. Exercise the bounded cleanup-failure path in a controlled non-customer environment.
5. Delete all fixtures and retain only non-secret evidence.

## Files

- `scripts/`
- `.pm/w3/m81/README.md`
- `docs/ADR029-static-sites.md`

## Acceptance criteria

**Honest status:** the turnkey disposable probe and the cross-surface/timeout evidence are delivered; the **live static-S3 execution is deferred** (prod `hetzner-prod` is off-limits without explicit authorization, the local CAPD static plane is credential-less/degraded-503 by design, and this session's kubeconfig access is restricted). The original incident fixture already returns 404, so no active incident is claimed. The deterministic backend/operator/dashboard suites are the cluster-independent regression evidence for the same behaviors (the w2/m61 standard).

- [~] A disposable live static site reaches the documented terminal state within the stated window. — probe delivered (`scripts/static-delete-timing-verify.sh`, 5-minute deadline); live run deferred to a static-S3 environment.
- [~] List, REST children, GraphQL, MCP, dashboard, and public-host observations agree throughout the asserted contract. — the probe polls list + REST service/routes/headers/deploys + GraphQL + public host to convergence; MCP shares the one `core.Get` path (proven by tests). Live run deferred; cross-surface not-found proven deterministically (`service_deletion_test.go`, `service-detail-*.test.tsx`).
- [x] The bounded failure path is exercised without damaging customer resources — operator `TestStaticAppDeletionSurfacesStalledConditionOnOverrun` (overrun → `DeletionStalled`, finalizer retained) via the `FinalizerOverrunAfter` seam, no customer resources touched.
- [x] Fixture cleanup is confirmed and no credential or kubeconfig content is recorded — the probe deletes its fixture on every exit path and passes the bearer via a header file (never the process table); no kubeconfig is read or printed.

## Out of scope

- Production cleanup of unrelated legacy terminating resources.

