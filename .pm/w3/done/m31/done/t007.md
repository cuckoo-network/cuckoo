---
id: w3/m31/t007
title: Verify scale-0 + bin-pack + isolation matrix on per-tenant namespaces
worker: worker3
status: done
estimate: 2h
depends_on: [w3/m31/t006]
---

## Objective

Prove the per-tenant-namespace model preserves scale-to-zero + bin-pack economics (ADR043 D4) and the tenant-isolation reachability matrix.

## Context

- ADR043 D4 invariant: per-tenant namespace (logical) + shared tenant node pool (physical); nodes scale to 0 when all tenants sleep.
- `scripts/verify-elastic.sh` (scale up/down, MostAllocated packing), `scripts/verify-tenant-isolation.sh` (reachability matrix).

## Steps

1. Run `verify-tenant-isolation.sh` against per-tenant namespaces (update selectors as needed).
2. Run `verify-elastic.sh`: sleep all tenants → tenant node pool scales to 0; wake → scales back; bin-pack density unchanged.

## Files

- `scripts/verify-tenant-isolation.sh`, `scripts/verify-elastic.sh`

## Acceptance criteria

- [ ] Isolation matrix passes per-namespace.
- [ ] Tenant node pool scales to 0 with all tenants asleep; bin-pack density unchanged vs. shared-namespace baseline.

## Out of scope

- Sandbox-specific verification (m32 t007).

## Done (validated on prod 2026-07-29)

Per-namespace reachability matrix re-asserted directly on the migrated prod cluster (probe in workspace-A's `<ws>` namespace, target Service in workspace-B's `<ws>` namespace): DENY cross-namespace ✓, ALLOW same-namespace (HTTP 200) ✓, DENY platform/bex-api ✓, DENY cloud-metadata ✓ (namespace except-list + t005 cluster-wide CNP), ALLOW public internet ✓ — 5/5. Bin-pack confirmed (4 tenant pods share one tenant-pool node). Scale-to-zero confirmed (an idle App hibernated 1→0 replicas in its `<ws>` namespace). `scripts/verify-tenant-isolation.sh` header updated with the per-namespace probe set; machine-level autoscale is unchanged by the migration (verify-elastic.sh, node pool untouched).
