---
id: w3/m17/t006
title: "Test coverage — HA present, non-HA omitted, source-down error"
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m17/t004]
---

## Objective

Real behavior + failure modes: HA Database returns a lag series; non-HA Database omits it (assert absence, not zero); a source error surfaces `ErrMetricsUnavailable`; the Prometheus query builder produces the expected `cnpg_pg_replication_lag` selector. No tautological tests.

## Acceptance criteria

- [ ] The named cases covered; backend suite green.
