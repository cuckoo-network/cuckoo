---
id: w3/m30/t005
title: Dashboard Logs tab on keyvalue detail, cloned from postgres-log-viewer
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m30/t004]
---

## Objective

The keyvalue detail page gains a Logs tab with range/instance/search controls and honest empty/403/503/error states, cloning `postgres-log-viewer.tsx`/`use-postgres-logs.ts` (which m28 built to be reusable).

## Context

- `dashboard/src/features/databases/components/postgres-log-viewer.tsx` + `hooks/use-postgres-logs.ts` + the `databases.$databaseId.tsx` route wiring are the template.
- KeyValue detail: `dashboard/src/routes/keyvalue.$keyValueId.tsx`, `dashboard/src/features/keyvalue/`.
- Locale keys in both `en.ts` and `zh.ts` (the databases feature added a full `logs*` key set — mirror it).

## Steps

1. Extract-or-clone the viewer for keyvalue (prefer a shared component if the clone is byte-similar — flag in Simplify otherwise).
2. Wire the tab + route param, matching the Postgres tab's direct-link behavior.
3. Add locale keys (en + zh) and component tests mirroring `postgres-log-viewer.test.tsx`.

## Files

- `dashboard/src/features/keyvalue/…` (viewer, hook, tests, locales)
- `dashboard/src/routes/keyvalue.$keyValueId.tsx`

## Acceptance criteria

- [ ] A keyvalue store's logs render in its Logs tab with range/instance/search; unauthorized/store-off/error states show honest copy, not blank panels.

## Out of scope

- Live tail UI.
