---
id: w3/m43/t009
title: Test coverage
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m43/t007]
---

## Objective

Meaningful tests asserting the contract's failure modes, not just happy paths.

## Context

- House patterns: gateway tests (`sandbox_exec` + web-shell precedents), real-Postgres integration tests (`BEX_TEST_DB_URI`), driver Node tests, `scripts/agent-session-verify.sh` live legs.

## Steps

1. Byte-transparency: proxied output == driver output, v1 header intact (t002).
2. Ticket/nonce: expiry, reuse, wrong-pod/namespace claims, cookie-carrying requests — all fail closed with named errors.
3. Replay/splice: exactly-once across the boundary under concurrent writes; terminal replay + `[DONE]`; gateway-restart replay.
4. Turn: single-flight, concurrent-POST rejection, steer narrowing on live vs idle.
5. attach-ticket: authz, terminal-session decision, three-surface parity.
6. Store: ordered round-trip, cascade, retention (real Postgres).

## Files

- tests adjacent to each touched package; `scripts/agent-session-verify.sh`

## Acceptance criteria

- [ ] All suites green in CI; the live E2E legs pass on the real substrate (or are recorded as the closeout's pending operator run, m41-style).

## Out of scope

- Load/perf testing.
