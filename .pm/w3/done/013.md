# attach-ticket mint `url` returns the phase-2 wss origin, not the phase-1 SSE base

The `POST /v1/agent-sessions/{id}/attach-ticket` response `url` field is `BEX_AGENT_SESSION_GATEWAY_URL` = the **phase-2 raw-ACP WebSocket** origin (e.g. `wss://ssh.bex.co/agent-sessions`). It is **not** the phase-1 SSE stream base.

The phase-1 SSE conversation stream is served under the **primary API origin**, edge-routed to the gateway (m43 t006):

```
GET https://api.bex.co/v1/agent-sessions/{id}/stream   (header X-Bex-Agent-Ticket)
```

A client that naively does `mint.url + "/{id}/stream"` builds a `wss://…` URL and fails (this was a real bug in `scripts/agent-session-verify.sh` section 5, since fixed to use the API origin).

**Follow-up for w1/m64 (dashboard):**

- Dashboard/AI-SDK clients must derive the SSE stream URL from the API origin + session id, ignoring `url` for the phase-1 stream (`url` is only for the future WebSocket attach).
- Consider having the mint **also return the explicit phase-1 SSE stream URL** (e.g. `streamUrl`) so clients don't have to hardcode the path — verify the same shape across REST/GraphQL/MCP. Decide in m64.

---

**Done 2026-08-19 (worked from `w10/m9` t003).** Shipped the explicit `streamUrl` field: `agentsessions.Service` gained an `APIPublicURL` field (sourced from the existing `BEX_API_PUBLIC_URL`/`deps.DeployHookBaseURL`, no new env var), and `withTicket` populates `View.StreamURL = "<APIPublicURL>/v1/agent-sessions/<id>/stream"` whenever it's configured — empty otherwise, same graceful-degradation shape as the deploy-hook URLs that config already drives. Wired identically across REST (direct JSON serialization), GraphQL (`streamUrl` field added to `agentSessionGQLType`), and MCP (shares the `View` struct) — `TestViewFieldsAndCodedErrorsMatchEverySurface` enforces the three stay in lockstep. `url` is untouched and still documented as the phase-2 gateway origin. Dashboard's `AgentSessionTicket`/`MintedTicket` types gained the field (mapper + transport doc corrected to stop implying `url` was ever a stream endpoint); `scripts/agent-session-verify.sh` now cross-checks the minted `streamUrl` against its own local derivation when present. New tests: `TestAttachTicketBuildsStreamURLFromAPIPublicURL` (backend), `toAgentSessionTicket` mapper coverage (dashboard). `go test ./...` + `yarn typecheck && yarn lint && yarn test` green.
