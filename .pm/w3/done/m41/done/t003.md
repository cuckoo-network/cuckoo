---
id: w3/m41/t003
title: "Steering v1: new prompt turn resumes the session sandbox"
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m41/t001]
---

## Objective

Ship the phase-1 steering channel (ADR047 D8): a new prompt turn posted to an existing session (REST/GraphQL/MCP verb on the m39 surface) resumes the session's sandbox (or re-dispatches if terminated), runs another headless turn on the same branch, and updates the same draft PR — table-stakes steering without interactive attach.

## Context

- Agent-side conversation continuity: `existingSessionId` → `session/load` only when the agent advertises `loadSession` (m37 t005-tested); otherwise the new turn runs with the prompt + repo state as context (honest fallback, transcript replay is wave 2).
- Resume depends on w3/m42 for true hibernation; until then, re-dispatch on a fresh sandbox with a fresh clone of the `bex-agent/*` branch is the working path — record which mode ran.

## Steps

1. Steer verb on the session surface (validate session state; authz per m39).
2. Dispatch the turn (resume or re-dispatch), push, PR update.
3. Phase/status bookkeeping for multi-turn sessions.

## Files

- `lego/backend/internal/agentsession/`, `lego/agent-image/driver/`

## Acceptance criteria

- [ ] Steering turn produces a follow-up commit on the same branch + updated PR
- [ ] Works via re-dispatch when the original sandbox is gone; mode recorded

## Out of scope

- Live interactive attach (wave 2); PR-comment webhook loop (follow-on)
