---
id: w3/m41/t004
title: "Live E2E verification script on the prod substrate"
worker: worker3
status: done
estimate: 90m
depends_on: [w3/m41/t002, w3/m41/t003]
---

## Objective

Author `scripts/agent-session-verify.sh` and run it against the live substrate: create a session on a real (disposable) repo → agent commits → draft PR with evidence → steering turn → follow-up commit; plus failure-path assertions (agent crash → `failed`, non-`bex-agent/*` mint refused). The house E2E pattern: honest, evidence-capturing, repeatable.

## Context

- Script precedents: `scripts/secrets-verify.sh`, `scripts/webhooks-verify.sh`, `scripts/verify-sandbox-isolation-live.sh` — disposable fixtures, loud assertions, cleanup traps.
- Needs a test GitHub repo under the app installation (disposable fixture repo) + a BYO model key from the environment (never committed).
- Run under m40's egress policy if landed; record which policy profile was active.

## Steps

1. Script: fixture repo setup, session create (REST), poll to completion, PR + evidence assertions via the API and GitHub, steering turn, failure cases, teardown.
2. Run on prod (or dev-3 first, then prod); capture the evidence trail in the milestone log.

## Files

- new `scripts/agent-session-verify.sh`

## Acceptance criteria

- [x] Full happy path + steering green on the live substrate with evidence recorded
- [x] Failure paths assert named errors, no hangs, clean teardown

## Verification

Audited 2026-08-30 against the credential-free production record in `w5/done/m72/evidence/2026-08-18-git-delivery-recovery.md`. Workflow run `32194179340` deployed the tested build; `scripts/agent-session-verify.sh` completed create → draft PR #58 + evidence → steering follow-up commit, protected-branch and unknown-adapter refusal, transcript replay, and exact-fixture cleanup. The verifier reported `ALL AGENT-SESSION CHECKS PASSED (egress-profile=production)`.

## Out of scope

- CI automation (operator-run like the other live verifies)
