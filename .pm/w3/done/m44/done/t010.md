---
id: w3/m44/t010
title: Test coverage
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m44/t008]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: the full-page layout composition, user-bubble vs agent-prose rendering, "Worked/Thought for Ns" duration + timeline grouping (collapsed/expanded), the inline PR card, the composer state-routing (live chat-POST vs idle redispatch), evidence panel toggle, and sidebar session switching.

## Context

- Existing tests: `dashboard/src/features/agent-sessions/components/__tests__/*` + `lib/__tests__/*`; fixtures in `lib/mock-stream.ts`.
- Follow the repo testing-library + apollo-mock conventions.

## Steps

1. Cover each new/changed behavior with real assertions (not snapshots-of-everything).
2. Reuse mock-stream fixtures for the conversation-driven cases.

## Files

- `dashboard/src/features/agent-sessions/**/__tests__/*`

## Acceptance criteria

- [ ] New tests assert layout, bubble-vs-prose, Worked/Thought grouping + duration, inline PR card, composer routing, evidence toggle, sidebar switch; `yarn test` green.

## Out of scope

- Live-substrate walk (covered under Closeout / m64 precedent).
