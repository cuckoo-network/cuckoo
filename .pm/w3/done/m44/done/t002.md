---
id: w3/m44/t002
title: Docked chat composer (state-routed)
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m44/t001]
---

## Objective

Turn the existing `SteeringComposer` into the bottom-docked chat input of the full-page layout, preserving its state routing: a live/running session POSTs a chat turn via the conversation column's `useChat` `sendMessage`; an idle/terminal session redispatches via the steer/resume mutation. Disabled-with-reason states are preserved (canceling/canceled, turn-in-flight, stream unavailable).

## Context

- `dashboard/src/features/agent-sessions/components/steering-composer.tsx` (current logic + routing).
- The lifted `ConversationChatHandle` (`sendMessage`, `status`) from `session-conversation.tsx` is how a live turn is sent; t001 provides the docked slot.
- Match the reference composer affordances (auto-grow textarea, Enter-to-send, send button) — `/Volumes/nvme4tbfish/projects/beancount-io/dashboard/.../agent-chat-input.tsx`.

## Steps

1. Reposition/adapt the composer as the docked footer input (auto-grow textarea, send button, mode/placeholder).
2. Keep the routing decision (live => chat POST; idle/terminal => redispatch) and the disabled-reason messaging.

## Files

- `dashboard/src/features/agent-sessions/components/steering-composer.tsx`
- `dashboard/src/routes/agents_.$agentSessionId.tsx`

## Acceptance criteria

- [ ] The composer is docked at the bottom; a live session send goes through `useChat`, an idle/terminal send redispatches; disabled reasons intact (tested in t010).

## Out of scope

- Message rendering (t003); durations (t004).
