---
id: w3/m8/t006
title: Render parity — filter semantics vs Render's logs API/dashboard; matrix row updated
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m8/t005]
---

## Objective

Standing closing task. Verify the filter vocabulary and semantics match Render's logs API (names, value formats, combination behavior) across REST/GraphQL/MCP; check what the dashboard live-logs page (w5/m6) exposes against the newly-honored semantics and file UI drift (missing filter controls, stale empty-states) as a w5 inbox note rather than building UI here. Update docs/ADR018-render-parity.md's "Request / HTTP logs + structured filters" row (✖ → ✅/◐ with evidence, `host`/`direction`/`build` decisions documented) and the gap-backlog row.

## Acceptance criteria

- [ ] Three surfaces consistent; matrix + backlog rows updated with evidence; UI drift filed, not silent.
