---
id: w3/m11/t009
title: Render parity — check REST/GraphQL/MCP/UI consistency
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m11/t008]
---

## Objective

Standing closing task: verify the new webhooks surface is consistent across REST, GraphQL, MCP, and the dashboard UI (same fields, semantics, error shapes), and compare against Render's actual documented webhooks behavior (render.com/docs/webhooks), flagging any drift as follow-up rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` — "one Core, three adapters" is the standard this checks against.
- `docs/ADR018-render-parity.md` — update the "Outbound event webhooks" gap-backlog row from "untracked (low)" to point at this milestone, and note bex's deliberate divergences (no plan-tier gating; REST/GraphQL/MCP endpoint management where Render's public docs don't detail equivalents).

## Steps

1. Diff REST/GraphQL/MCP field names and error shapes for the webhooks resource.
2. Re-check render.com/docs/webhooks against what shipped; record any divergence.
3. Update `docs/ADR018-render-parity.md`'s gap-backlog table + the "Platform events & integrations" section row.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Parity ledger row updated with evidence pointers to the shipped code.
- [ ] Any divergence found is filed as a follow-up note, not left undocumented.

## Out of scope

- Fixing any drift found here beyond filing it — that's follow-up work.
