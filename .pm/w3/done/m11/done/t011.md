---
id: w3/m11/t011
title: Test coverage
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m11/t009]
---

## Objective

Standing closing task: add meaningful tests for the behavior this milestone shipped — real behavior and failure modes, not tautological or snapshot-everything tests.

## Context

- Precedent: `w4/m10`'s audit-log tests (unit + real-Postgres integration), `w2/m9`'s signature-verification tests for the git webhook.

## Steps

1. Signing: unit test that a known id/timestamp/body/secret produces the documented HMAC signature, and that an altered body fails verification.
2. Retry/backoff: test the delivery worker retries on non-2xx and stops/disables after the configured attempt count.
3. CRUD: REST/GraphQL/MCP tests that secret is never returned after creation and cross-workspace access is denied.
4. Dashboard: component test for the create-form secret-reveal-once behavior (matching `w4/m8`'s API-key mint test pattern).

## Files

- `lego/backend/internal/webhooks/*_test.go`
- `dashboard/src/features/webhooks/**/__tests__/`

## Acceptance criteria

- [ ] Tests assert real signing correctness and retry/disable behavior, not just "no error thrown."
- [ ] `go test ./...` (backend) and `yarn test` (dashboard) green.

## Out of scope

- Load/perf testing of the delivery worker.
