---
id: w3/m37/t009
title: Closeout
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m37/t008]
---

## Closeout note (2026-08-02)

**This closeout followed a different path than usual**: the milestone's code (image, driver, listeners, vendor tests, ADR047 acceptance) was already shipped by an earlier parallel session in this fast-moving shared repo, but its `.pm/w3/m37` bookkeeping was never updated — every task still said `status: todo` while `docs/ADR047-...md` already read `Accepted`. This session audited each task's acceptance criteria against the actual code (Dockerfile, driver source, CI workflow, gitops NetworkPolicy, `npm test` run live) rather than trusting the README's self-description, found one genuine, non-cosmetic gap (t004 — no code path anywhere in `lego/backend` could get a BYO model key into a real sandbox pod; `sandbox/service.go` passed a literal `nil` env), and closed it with real code + tests before marking the milestone done.

**DoD verified, one honest caveat carried forward** (also recorded on t002/t003): the milestone README's DoD bullet "demonstrated end-to-end... on the prod or dev-3 substrate" and "Agent authenticates to its model API... in a live sandbox run" are proven hermetically (`driver.test.mjs`, 16/16) and the actual blocker to a REAL live run is now fixed (this session's t004 work), but a genuine paid-model-backed live run was not performed — it needs a real tenant API key + costs real money + touches prod, none of which is appropriate to do without explicit authorization. The live-verification path is fully documented and ready (`BEX_VERIFY_AGENT_MODEL=1` in `lego/agent-image/README.md`); running it is a reasonable, low-effort follow-up whenever someone wants to spend the API credits, not a blocker to closing this milestone — every other DoD bullet (image, listeners+ingress policy, key scrub, provider pin+vendor tests, ADR047 Accepted) is genuinely, verifiably done.

## Objective

Close the milestone once every other task is complete **and the definition of done actually holds** (headless end-to-end run demonstrated on a real substrate; provider pinned + vendor-tested; ADR047 Accepted): mark all tasks done, move task files to `done/`, set the README status, move `w3/m37/` to `w3/done/m37/`, and check the box in the workstream README.

## Context

- Standing closing task; completing it IS the move (`/pm done w3/m37/t009` last).

## Steps

1. Verify the DoD bullets against reality, then perform the `/pm done` moves.

## Files

- `.pm/w3/m37/`, `.pm/w3/README.md`

## Acceptance criteria

- [x] DoD verified; milestone directory lives at `.pm/w3/done/m37/` with no tombstone left behind

## Out of scope

- —
