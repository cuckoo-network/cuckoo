---
id: w3/m37/t007
title: Simplify pass over the driver + image code
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m37/t003, w3/m37/t004, w3/m37/t005, w3/m37/t006]
---

## Closeout note (2026-08-02)

Driver/image code (shipped earlier): skimmed all 7 driver source files (845 lines) — each has one clear responsibility (`acp.mjs` spawn/probe, `config.mjs` env parsing, `credentials.mjs` injection/scrub, `session.mjs` turn execution, `server.mjs` listeners, `stream-hub.mjs` fan-out, `main.mjs` entrypoint); no dead code or duplication found.

New Go wiring (this session, t004): self-reviewed the diff and found + fixed one real issue — my inserted `ModelAPIKeyEnvVar` const + doc comment had been wedged between `CreateAgentSessionSandbox`'s existing doc comment and its function signature, breaking that comment's association with its function (godoc convention: a doc comment must sit immediately above what it documents). Moved the const above the pre-existing comment block instead. No other simplification found — `modelAPIKey`/`modelKeySecretPath` are each single-purpose, minimal functions with no duplication.

## Objective

Run `/simplify` over the code this milestone changed (agent image, driver, sandbox client touches) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Standing closing task. Render parity omitted for this milestone (no REST/GraphQL/MCP/UI surface — see milestone Source + Goal linkage).

## Steps

1. `/simplify` over `lego/agent-image/` + touched backend files; apply accepted findings.

## Files

- `lego/agent-image/`, `lego/backend/internal/sandbox/`

## Acceptance criteria

- [x] Simplify findings triaged and applied or explicitly declined

## Out of scope

- Bug hunting (separate review)
