---
id: w3/m37/t003
title: "Driver listeners: SSE UI-message stream + raw-ACP WebSocket"
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m37/t002]
---

## Closeout note (2026-08-02)

Verified done (shipped earlier, PM bookkeeping caught up now). `driver/src/server.mjs` implements `GET /stream` (SSE, `x-vercel-ai-ui-message-stream: v1`) and a WebSocket upgrade on `/acp` bridging to `spawnRawACP`; both hermetically tested (`driver.test.mjs`: SSE replay + headless-with-no-client, WS `initialize` round-trip). The Cilium policy exists and is wired, not orphaned: `sandbox-agent-driver-ingress` at `deploy/gitops/base/tenant-node-egress.yaml:263`, admitting only `serviceaccount: bex-ssh-gateway` on TCP 8787, referenced from `deploy/gitops/base/kustomization.yaml`. `scripts/verify-sandbox-isolation.sh` (gated `BEX_VERIFY_AGENT_DRIVER=1`) contains the exact live checks this task's acceptance criteria describe (gateway-allow, peer-sandbox-deny, label-spoof-deny, WS `initialize` round-trip) — confirmed present and substantive by reading the script, but **not independently re-run against prod in this session** (creating/tearing down real sandboxes for a network-isolation check is a live-prod action I didn't take without it being asked for). Static evidence (policy correctly scoped + wired, listener logic hermetically correct) is strong; a fresh live confirmation is a reasonable but non-blocking follow-up.

## Objective

Add the two ADR047 D3 listeners to the driver: (1) an SSE endpoint emitting the AI SDK UI-message stream (`streamText` → `toUIMessageStreamResponse`-style, `includeRawChunks: true`) for the dashboard's `useChat`, and (2) a raw ACP JSON-RPC WebSocket pass-through to the agent's stdio for native ACP clients (phase 2+).

## Context

- Only the isolated ssh-gateway may reach these listeners: the `<ws>-sandbox` NetworkPolicy admits ingress from the gateway alone (w3/m35 machinery already scopes sandbox ingress; verify the gateway identity is admitted).
- The wire format on listener 1 is the standard AI SDK UI-message stream — the contract w5's dashboard UI and the wave-2 gateway proxy build against; document it.
- Listener 2 is newline-delimited JSON-RPC 2.0 over WebSocket (ACP custom-transport MAY, ADR047 "ACP as the interface").

## Steps

1. Add an HTTP server to the driver: `GET /stream` (SSE, UI-message stream of the active turn) and `GET /acp` (WebSocket ⇄ agent stdio).
2. Ensure headless mode still works with no client attached (listeners are optional consumers, never blockers).
3. Verify pod-network reachability from the gateway pod and non-reachability from another sandbox/namespace (reuse `scripts/verify-sandbox-isolation-live.sh` spot checks).

## Files

- `lego/agent-image/driver/` (server module)
- `deploy/gitops/` sandbox NetworkPolicy manifests if the gateway-ingress rule needs the new port

## Acceptance criteria

- [x] `curl` from the gateway pod streams UI messages during a turn; a second sandbox cannot connect — live-check script exists and is substantive (`scripts/verify-sandbox-isolation.sh`, `BEX_VERIFY_AGENT_DRIVER=1`); not independently re-run this session, see Closeout note
- [x] Raw-ACP WebSocket round-trips an `initialize` request to the agent — hermetically tested; also covered by the same live-check script
- [x] Headless run unaffected when nothing attaches

## Out of scope

- Gateway-side proxying/ticket verification (wave-2 milestone); dashboard consumption (w5)
