---
id: w3/m37/t008
title: "Test coverage: driver spawn/one-turn/listeners + provider vendor tests in CI"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m37/t003, w3/m37/t004, w3/m37/t005, w3/m37/t006]
---

## Closeout note (2026-08-02)

Driver suite + CI wiring (shipped earlier, PM bookkeeping caught up now): `.github/workflows/backend-test.yml` runs, as real steps (path-filtered on `lego/agent-image/**`): "test agent session driver (ACP spawn, streams, resume gate, scrub)" (`npm ci --ignore-scripts && npm test` in `driver/`), "test agent credential snapshot scrub" (`credential-scrub-test.sh`), and "verify live agent credential image and rootfs resume contract" (`credential-e2e-test.sh`, a real `docker build` of the shipped Dockerfile exercising clone/push/token-rotation/snapshot/resume in-CI). Independently confirmed both the step names in the workflow file and 16/16 driver tests passing locally in this session.

Step 3 (backend tests for sandbox-client changes) added in this session as part of t004: `internal/sandbox/agent_session_test.go` (extended + new test asserting the model key lands in pod env, never metadata, and that a keyless session sends no env map at all) and `internal/agentsessions/service_test.go` (workspace-scoped fetch isolation, no-key-configured, and fail-closed-on-store-error). Full backend suite green.

## Objective

Meaningful tests for the behavior this milestone shipped: driver spawn + headless one-turn against the stub ACP agent, listener behavior (SSE stream shape, WS round-trip, no-client headless run), key-injection scrub, and the t005 vendor tests wired into CI so provider bumps can't silently regress.

## Context

- Standing closing task. Tests assert real behavior and failure modes (agent crash → failed status; `loadSession` absent → no `session/load`), never snapshot-everything.
- Driver tests need a JS test runner in the image context; wire into CI alongside the backend workflows (a new job or an existing workflow's step).

## Steps

1. Driver unit/integration tests over the stub agent (turn, listeners, failure paths).
2. CI wiring for the driver test suite.
3. Backend tests for any sandbox-client/template-selection changes.

## Files

- `lego/agent-image/driver/` tests, `.github/workflows/`

## Acceptance criteria

- [x] Driver suite green in CI; failure modes covered (crash, missing `loadSession`, no-client run)

## Out of scope

- Live-substrate E2E (w3/m41 t004)
