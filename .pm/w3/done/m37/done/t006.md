---
id: w3/m37/t006
title: "Flip ADR047 → Accepted; record the D3 amendment as adopted"
worker: worker3
status: done
estimate: 15m
depends_on: []
---

## Closeout note (2026-08-02)

Verified done (shipped earlier, PM bookkeeping caught up now). `docs/ADR047-cloud-coding-agent-sessions.md:3` reads `**Status:** Accepted; D3 agent-session control-plane API shipped in w3/m39 (2026-08-01)...`, and the D3 amendment is substantively recorded in the body (`:110` "D3 — ACP is the agent interface; a sandbox-internal AI SDK driver fronts it", `:172` records the supersession of the bespoke-gateway-bridge alternative). `CLAUDE.md:174` is synced (**Accepted**). `npx prettier@3.4.2 --check` on both files: clean.

## Objective

Materializing this milestone set is the ADR047 adoption decision (user-confirmed 2026-08-01): update `docs/ADR047-cloud-coding-agent-sessions.md` from `Proposed` to `Accepted`, note the scheduling (w3/m37–m42 + w7/m76 wave 1), and record that this re-opens the "Hosted Claude Code inside sandbox" deferral as a phased product per the ADR's own status line.

## Context

- ADR status line: `docs/ADR047-cloud-coding-agent-sessions.md:3`.
- CLAUDE.md docs index entry says "**Proposed**" — sync it.
- Markdown is CI-checked: run prettier after editing.

## Steps

1. Edit the ADR status line: Accepted, date, milestone pointers.
2. Sync the CLAUDE.md docs-index line.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR047-cloud-coding-agent-sessions.md`
- `CLAUDE.md` (docs index)

## Acceptance criteria

- [x] ADR047 reads `Status: Accepted` with the wave-1 milestone pointers
- [x] CLAUDE.md index synced; prettier clean

## Out of scope

- Any content changes to the ADR's decisions
