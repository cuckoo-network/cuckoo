---
id: w3/m16/t001
title: "Audit seam: typed per-verb non-secret detail fields"
worker: worker3
status: todo
estimate: 60m
depends_on: []
---

## Objective

Extend the audit interception point (`core.Base.AuthorizeTarget`'s event emission path) with **typed, named, non-secret** detail fields recorded by exactly the verbs that own them: plan from/to (SetPlan), replica from/to (Scale + autoscaling config), autoDeploy boolean (the auto-deploy toggle). The maintenance-mode typed-boolean precedent — a narrow struct per verb, never a generic arguments map, so no env-var value can ever ride along (the w4/m10 property).

## Context

- `lego/backend/internal/core/` (the audit emit path + target recording); the maintenance-mode detail field is the pattern to mirror (grep `maintenance` in `internal/events` + `internal/audit`).
- Store: whatever column/JSON shape the maintenance boolean used — extend it, don't invent a second channel; check whether a migration is needed.
- `from` values come from the verb's own pre-write read (the `AuthorizeApp`-fetched App), so no extra round-trip.

## Files

- `lego/backend/internal/core/`, `internal/audit/`, the verbs in `internal/apps/` (SetPlan/Scale/autoscaling/auto-deploy)

## Acceptance criteria

- [ ] The four verbs record their typed details; a grep-level guard (or the existing event-vocabulary test) proves no generic arguments object was introduced.

## Out of scope

- The projection to event shapes (t002); any other verb's details.
