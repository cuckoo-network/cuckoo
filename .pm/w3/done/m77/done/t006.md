---
id: w3/m77/t006
title: Closeout
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m77/t005]
---

## Objective

Close the milestone once its definition of done is actually met — a completed fire-and-forget session's conversation replays instead of showing "No conversation yet.", verified on the prod substrate.

## Context

- Do not close on code-complete alone: the DoD requires the live E2E headless-then-open leg green (shares the m41 operator-run gate).

## Steps

1. Confirm the DoD holds (real-PG green + `scripts/agent-session-verify.sh` headless-then-open leg green on prod; dashboard shows the conversation for a completed fire-and-forget session).
2. Set every remaining task `status: done`, mark all rows `— **DONE**`, set `**Status:** done` in the milestone README.
3. Move each `tNNN.md` to `w3/m77/done/`, then move the whole `w3/m77/` to `w3/done/m77/` and check `- [x]` in `.pm/w3/README.md`.
4. Update the ADR051 status line to Accepted/Shipped with the w3/m77 evidence; flip the ADR047 D10 + gap #5 notes from "unscheduled" to shipped.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [x] DoD verified on prod (not just code-complete).
- [x] Milestone moved to `w3/done/m77/`; workstream box checked.
- [x] ADR051 + ADR047 status notes updated to shipped.

## Out of scope

- Any follow-up (e.g. optional JSON transcript read for MCP/REST parity) — file as a new inbox note.
