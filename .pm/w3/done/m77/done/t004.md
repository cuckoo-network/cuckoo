---
id: w3/m77/t004
title: Simplify the changed code (/simplify)
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m77/t001, w3/m77/t002, w3/m77/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving). The recorder should share the tee code path with the live-attach splice rather than fork it.

## Context

- Changed surface: `internal/sshgateway/agentattach` (or `agentrecord`), `cmd/ssh-gateway/main.go`, `internal/agentsessions/completion.go`, `internal/sandbox/{service,exec}.go`.
- Watch for: duplicated dial/tee logic between the browser attach path and the recorder (should be one helper), duplicated HMAC-ticket minting between sandbox-exec and record, and any redundant seq bookkeeping.

## Steps

1. Invoke `/simplify` on the milestone's diff.
2. Apply behavior-preserving consolidations; keep the byte-transparent/idempotent guarantees intact.

## Files

- The t001/t002/t003 changed files.

## Acceptance criteria

- [ ] Recorder and live tee share the dial/append helper (no forked copy).
- [ ] `cd lego/backend && go build ./... && go test ./...` green; `make lint` clean.

## Out of scope

- New behavior; new tests (t005).
