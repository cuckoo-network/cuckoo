---
id: w3/m77/t003
title: Env + config wiring + .env.example/CLAUDE.md/ADR051 sync
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m77/t001]
---

## Objective

Wire and document any config the recorder needs (gateway record listen route/addr; bex-api's record endpoint URL), keeping `.env.example`, the CLAUDE.md env tables, and the ADR051 status line in sync per repo rules.

## Context

- Prefer **reusing** the existing sandbox-exec trust envelope: `BEX_SANDBOX_EXEC_SECRET` (HMAC) and the internal gateway listener, adding only a route — minimize new surface. Add a bex-api-side record URL only if the exec URL base can't be reused (`BEX_SANDBOX_EXEC_URL` lineage).
- Repo rules: `.env.example` mirrors `.env` variable names (name + comment, never values); the CLAUDE.md env table is the human index; new/renamed vars update both. Markdown is prettier-checked.

## Steps

1. Decide reuse-vs-new for the secret/URL/listen addr; if any new var is introduced, add it to `.env.example` (value-less) and the CLAUDE.md operator/bex-api/ssh-gateway env table with a one-line meaning + unset-behavior.
2. Flip the ADR051 `**Status:**` line from "Proposed (unscheduled)" to reference `w3/m77` as the implementing milestone; leave "Proposed" until the milestone's DoD holds.
3. Confirm `bash scripts/skill-layout-validate.sh` unaffected; run prettier on changed markdown.

## Files

- `.env.example`
- `CLAUDE.md` (env variables table)
- `docs/ADR051-agent-session-transcript.md` (status line)
- (if a new var) the relevant `lego/backend/cmd/*` env read

## Acceptance criteria

- [ ] Any new env var appears in both `.env.example` and the CLAUDE.md table with correct unset behavior; no drift.
- [ ] ADR051 references `w3/m77` as its implementing milestone.
- [ ] `npx prettier@3.4.2 --write "**/*.md"` clean.

## Out of scope

- Endpoint/logic (t001/t002); tests (t005).
