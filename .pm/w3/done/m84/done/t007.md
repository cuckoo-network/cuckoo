---
id: w3/m84/t007
title: "Closeout"
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m84/t006]
---

## Objective

Close the milestone once its definition of done actually holds — not merely when the code is written.

## Context

- DoD (milestone README): families live on `/metrics` with bounded labels, panels fed by them, two rules with panels, coverage check green, ADR088 §6 gap closed.

## Steps

1. Verify the DoD against a live `/metrics` scrape and the rendered dashboard.
2. Set every remaining task `status: done`, mark rows `— **DONE**` and `**Status:** done` in the milestone README, move each `tNNN.md` to `done/`, move `w3/m84/` to `w3/done/m84/`, check `- [x]` in `.pm/w3/README.md`.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w3/README.md`, `.pm/w3/m84/**`

## Acceptance criteria

- [ ] DoD verified; milestone moved to `w3/done/m84/`; workstream README checked.

## Out of scope

- Any new work (file as follow-up inbox notes instead).
