---
id: w3/m14/t004
title: "Create wizard + Manual Deploy land on the in-flight deploy's page"
worker: worker3
status: done
estimate: 30m
depends_on: []
---

## Objective

Close the landing gap: the create wizard (`services.new.tsx:503` currently navigates to `/services/$serviceId`) and the Manual Deploy action navigate to the just-created/triggered deploy's detail page (deploy id is available from `createService`'s deploy record / `triggerDeploy`'s response — w9/m1 established the exact-link pattern), matching t001's captured Render behavior.

## Context

- `dashboard/src/routes/services.new.tsx`; the Manual Deploy action in `features/deploys` (w5/m21) — w9/m1 already made some actions land on the page; verify which paths remain and close them all.
- If a create path genuinely mints no deploy record (e.g. image-sourced create without an immediate deploy), fall back to the service page — honest, not a fake pending page.

## Files

- `dashboard/src/routes/services.new.tsx`, `dashboard/src/features/deploys/`

## Acceptance criteria

- [ ] Git-sourced create and Manual Deploy both land on the in-flight deploy page; the no-deploy fallback documented + tested.

## Out of scope

- Deploy-page internals (t003).
