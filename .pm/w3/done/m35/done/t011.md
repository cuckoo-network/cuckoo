---
id: w3/m35/t011
title: Test coverage for sandbox policies, authorization, RBAC, and DNS
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m35/t009]
---

## Objective

Add durable regressions for the exact security failures fixed by m35 so a future policy, adapter, or deployment change cannot silently reopen them.

## Context

- Current tests require `allow-same-namespace` in sandbox namespaces and describe that shape as sealed.
- Sandbox service tests assert the owner only in the immediate create response, not durable resource authorization.
- GitOps validation does not reject lifecycle-server cluster-wide mutation RBAC or an unpinned server image.

## Steps

1. Add table-driven policy-shape tests for hosting versus sandbox regimes and Cilium DNS/FQDN rules.
2. Add service/client adapter tests for owner/admin/cross-owner/cross-tenant and `networkPolicy` handling.
3. Add GitOps/RBAC guards plus the repeatable negative reachability harness from t008.

## Files

- `lego/backend/internal/store/namespaces_test.go`
- `lego/backend/internal/sandbox/*_test.go`
- `scripts/gitops-validate.sh`
- `scripts/verify-sandbox-isolation.sh`

## Acceptance criteria

- [ ] Tests fail if same-namespace sandbox traffic, arbitrary DNS, ignored policy fields, cross-owner access, label-only lifecycle/execd ingress, runc runtime bypass, cluster-wide mutation RBAC, or mutable image versions return.
- [ ] Tests assert real behavior/failure modes rather than snapshotting whole manifests.

## Out of scope

- Generic coverage-percentage targets.
