---
id: w3/m19/t009
title: Render parity — surfaces, vocabulary, evidence, and deliberate omissions
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m19/t008]
---

## Objective

Close the parity loop across the current Render dashboard/OpenAPI, bex's three API adapters, both dashboard consumers, and outbound webhooks; record every unsupported type as an explicit product or source boundary.

## Context

- Render's public `list-events` default is one hour for API clients, but its dashboard supplies a broader paged history.
- The supplied UI inventory contains dashboard labels not all present under the same names in the public enum.
- `.pm/DO_NOT_DO.md` excludes preview environments, persistent disks, workflows/tasks, provider maintenance runs, billing-driven features, and static-cache purge.

## Steps

1. Re-capture the Render filter tree and re-fetch the current official OpenAPI/docs; map UI labels to wire types and required detail fields.
2. Run parity assertions over REST, GraphQL, and MCP output/error/window/cursor behavior; verify the dashboard and webhook vocabulary use the same facts without requiring identical names where Render itself differs.
3. Update ADR006, ADR018, package documentation, and any Render artifact with the before/after support matrix, retained-history behavior, and exact omission reasons.
4. File only genuinely new, allowed residual work; do not reopen board anti-goals.

## Files

- `docs/ADR006-bex-api.md`
- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/`
- `lego/backend/internal/events/`
- `lego/backend/internal/webhooks/`
- `dashboard/src/features/events/`

## Acceptance criteria

- [ ] The parity ledger no longer calls the dashboard ✅ while its default successful view hides retained history.
- [ ] Every emitted type has one durable source and required typed details, or a documented, truthful limitation.
- [ ] Unsupported anti-goal labels are absent from APIs, filters, and webhook subscriptions.
- [ ] Official source links and capture dates are recorded without long copied passages.

## Out of scope

- Implementing a residual discovered during the parity check unless it is already inside t001–t008.
