---
id: w3/m19/t008
title: Live acceptance — exercise and cross-check every sourceable event family
worker: worker3
status: done
estimate: 40m
depends_on: [w3/m19/t007]
---

## Objective

Prove the expanded feed against a real non-production cluster and Postgres store, including the observed edges that unit fixtures cannot establish.

## Context

- `scripts/events-verify.sh` already proves the two-source feed, cursor paging, and secret redaction.
- m19 adds a third source plus operator-status edges, so live verification must cover reconciliation retries and both dashboard consumers.
- Production is not the place to induce image-pull, crash, or autoscaling failures.

## Steps

1. Extend the verification harness to trigger deploy, image-pull failure, suspend/resume convergence, runtime failure/recovery, branch/filter decisions, manual scale/config changes, and an autoscaler transition.
2. Cross-check REST, GraphQL, MCP, outbound-webhook delivery, the Events page, and Metrics markers against the same event ids/types/timestamps.
3. Force extra reconciler passes and restart bex-api/operator where safe; assert no duplicate facts.
4. Plant secret/env sentinel values and assert they appear in none of the event surfaces or delivery payloads; remove every fixture.

## Files

- `scripts/events-verify.sh`
- `lego/backend/internal/events/`
- `dashboard/src/features/events/`
- milestone evidence in `.pm/w3/m19/`

## Acceptance criteria

- [ ] All 19 sourceable supplied labels are exercised or tied to an already-proven unchanged source; every new label is exercised live.
- [ ] Every client surface agrees on stable ids, ordering, and typed details.
- [ ] Reconcile retries/restarts produce no duplicate or missing edge.
- [ ] Verification leaves no Apps, Jobs, webhook endpoints, or sentinel secrets behind.

## Out of scope

- Destructive probes against the named production service.
