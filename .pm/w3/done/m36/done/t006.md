---
id: w3/m36/t006
title: Closeout
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m36/t005]
---

## Status: DONE — live-verified 2026-08-02

Shipped: commit `cd3b5f72` (fix(logs): query the App's tenant namespace, not the shared s.Namespace) is 74 commits behind current `origin/main`, with multiple confirmed successful `deploy (bex via Argo)` CI runs since — long since deployed to prod.

Live-verified directly against production Loki (read-only `kubectl --context hetzner-prod` port-forward-free query via a throwaway probe pod in `monitoring`, deleted immediately after): `{namespace="tea-d98210cbbpdc73dcrkvg", app="tea-d98210cbbpdc73dcrkvg-beancount-cms-v2"}` returns real, recent log lines (timestamps ~2026-08-02T06:38 UTC, minutes before the check) from both replica pods. This is the App's actual per-tenant `<ws>` namespace — confirmed via `kubectl get app.app.bex.co -A`, which shows the App CR itself live in `tea-d98210cbbpdc73dcrkvg`. The exact regression this milestone fixed (logs querying the shared `default` namespace, where this App no longer lives, instead of its real namespace) is confirmed resolved in production, not just in code.

Memory step (5) skipped: no `project_logs_tenant_namespace_bug.md` memory file exists (never created in a prior session) — the bug is now fully fixed/shipped/verified, so creating one now would be a stale record with no future value; this milestone's `done/` entry is the durable record.

## Objective

Close the milestone once its definition of done is actually met (logs return for tenant services in prod), not merely when the code is written.

## Context

- DoD in the milestone README: tenant-service logs return across all surfaces; datastore logs resolved; regression test in place; shared-namespace mode byte-identical.
- Live check: `dashboard.bex.co/services/srv-d9bj8s3eg85c7390eb9g/logs` shows lines after the fix ships.

## Steps

1. Confirm the DoD holds live (post-`/ship` + deploy).
2. Set every remaining task `status: done`; move each `tNNN.md` to `w3/m36/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
4. Move the whole `w3/m36/` dir to `w3/done/m36/`; check `- [x] **m36**` in `w3/README.md`.
5. Update memory `project_logs_tenant_namespace_bug.md` (mark fixed + shipped commit).

## Files

- `.pm/w3/m36/**`, `.pm/w3/README.md`

## Acceptance criteria

- [x] Milestone moved to `w3/done/m36/`; workstream box checked.
- [x] Live logs confirmed for a tenant service.

## Out of scope

- Retiring `BEX_TENANT_NAMESPACES` / `BEX_MAX_*` (that is m34).
