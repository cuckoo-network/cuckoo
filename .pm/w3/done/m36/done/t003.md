---
id: w3/m36/t003
title: Render parity — logs return across REST/GraphQL/MCP + dashboard
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m36/t001, w3/m36/t002]
---

## Objective

Confirm the fix restores logs consistently across every surface the logs feature exposes, with no drift in fields/semantics/error shapes, and that the same holds for datastore logs.

## Context

- Surfaces (one core, thin adapters — `docs/ADR006-bex-api.md`): REST `GET /v1/logs` (+ `GET /v1/postgres/{id}/logs`, `GET /v1/key-value/{id}/logs`), GraphQL `logs`/`databaseLogs`/`keyValueLogs`, MCP `list_logs`/`get_logs` (+ datastore variants), dashboard Logs tabs (`dashboard/src/routes/services.$serviceId.logs.tsx`, Postgres/KV logs tabs).
- Compare against render.com log behavior; the honest-negative contracts (store-only filters 503, unknown filter values 400, live SSE always pod logs) must be preserved — the fix must not turn a legitimate empty into a fake line or vice-versa.

## Steps

1. Verify each surface returns real lines for a tenant service after the fix (dev-3 tenant-namespace stack or prod read-only spot check on `srv-d9bj8s3eg85c7390eb9g`).
2. Confirm all three API surfaces agree on shape/semantics; confirm the dashboard Logs tab renders lines.
3. Flag any residual Render drift as a follow-up note rather than silently diverging.

## Files

- `lego/backend/internal/logs/rest.go`, `graphql.go`, `mcp.go` (read-only verification)
- `dashboard/src/routes/services.$serviceId.logs.tsx` (read-only verification)

## Acceptance criteria

- [ ] REST, GraphQL, and MCP all return App logs for a tenant service, with identical fields/semantics.
- [ ] Postgres + Key Value log endpoints return for a tenant datastore.
- [ ] Dashboard Logs tab renders lines; honest empty/403/503 states still correct.
- [ ] Any Render drift recorded as a follow-up.

## Out of scope

- Behavior changes beyond the namespace fix.
