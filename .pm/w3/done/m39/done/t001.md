---
id: w3/m39/t001
title: "Control-plane agent_sessions table + store"
worker: worker3
status: done
estimate: 45m
depends_on: []
---

## Objective

Add the `agent_sessions` control-plane table and store layer: session id (new `id.Kind` registered in `lego/backend/internal/id` per ADR020, hyphen separator), workspace, target repo + `bex-agent/*` branch, agent `command`/`args` config, sandbox id, phase (`pending/setup/running/hibernated/completed/failed/cancelled`), timestamps, and result pointers (branch, PR URL — filled by w3/m41).

## Context

- Control-plane store + migrations: `BEX_CP_DB_URI` gated (docs/ADR003-control-plane.md); follow the existing migration + store package pattern in `lego/backend/internal/store/`.
- Id convention is depguard-enforced (`lego/backend/.golangci.yml`) — mint through `id.New(kind)` only.

## Steps

1. Register the new id kind (e.g. `ags-`) in `internal/id` (guard test updates itself by registry).
2. Migration + store CRUD (create, get, list-by-workspace, update-phase, cancel).
3. Keep evidence/transcript columns out (wave-2 transcript store); leave result pointers nullable.

## Files

- `lego/backend/internal/id/`, `lego/backend/internal/store/` (migration + `agent_sessions` store)

## Acceptance criteria

- [ ] Migration applies cleanly on a fresh and existing CP DB
- [ ] Store round-trips a session with the id guard test green

## Out of scope

- Transcript storage (wave 2); REST surface (t003)
