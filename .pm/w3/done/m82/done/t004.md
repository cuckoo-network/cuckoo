---
id: w3/m82/t004
title: "Advertise the new types: webhook catalog + picker, pinned ledger, ADR018 row, data.id hydration"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m82/t001, w3/m82/t002, w3/m82/t003]
---

## Objective

Make every fact t001–t003 now produces reachable the way existing ones are: advertised in the outbound-webhook vocabulary and the dashboard picker (both locales), resolvable through `GET /v1/events/{eventId}` / GraphQL `serviceEvent` / MCP `get_service_event` by `data.id`, and recorded in the pinned ledger and ADR018 so the parity claim stays evidence-backed.

## Context

- Advertised set: `lego/backend/internal/webhooks/service.go` (reads `eventvocab`); `webhooks/vocabulary_test.go` loads the pinned Render fixture from `docs/render-artifacts/` and fails on any advertised name outside Render's enum or documented extensions — the new names are all in Render's 67-value enum.
- Ledger: `docs/render-artifacts/webhooks-api.md` (the "No truthful producer; remain unsupported" row and the advertised count, 35 after w8/m34) and `docs/render-artifacts/datastore-webhook-events.md`; ADR018 outbound-webhooks row (`docs/ADR018-render-parity.md` ~line 227) and its Gap backlog table.
- From t003: `postgres_read_replicas_changed` stays in "No truthful producer" — read replicas are create-time-only in bex (`PostgresPatch` has no replica field and the Blueprint apply path writes `spec.readReplicas` without a datastore audit effect), so there is no post-create mutation to source it from. `postgres_ha_status_changed`, `postgres_connection_pool_enabled_changed`, `postgres_disk_size_changed`, and `key_value_config_restart` all have producers now and were moved to `bexSupported` in the pinned vocabulary fixture; the prose ledger rows and the advertised count are still t004's to update.
- Dashboard: `dashboard/src/features/webhooks/event-catalog.ts` (grouped keys + labels), `locales/en.ts`, `locales/zh.ts`, `components/__tests__/event-picker.test.tsx`; the events feed catalog `dashboard/src/features/events/service-event-catalog.ts` + `event-icon.tsx` only if datastore facts are rendered in a feed (verify in t007 whether Render shows one).
- Hydration: w8/m24's owner-scoped index resolves `webhook-id` / `data.id` for deploy, audit, and typed-fact sources "including datastore audits with no service-list home" — extend the typed-fact source to the new datastore fact table so `GET /v1/events/{id}` returns Render's bare `{id,timestamp,serviceId?,type,details}` (confirm the pinned OpenAPI's event schema for datastore subjects before choosing `serviceId` vs a datastore id field).

## Steps

1. Add the names to the advertised vocabulary and the picker catalog (group under the existing Postgres / Key Value headings); add en + zh labels.
2. Extend the event index's typed-fact source to read the datastore facts; verify hydration end to end with the existing executable verifier in `docs/render-artifacts/webhooks-api.md`.
3. Move the names out of "No truthful producer" in `webhooks-api.md`, update the advertised count, and refresh the ADR018 row + backlog line and `datastore-webhook-events.md`.
4. Run the dashboard picker tests and the backend vocabulary/drift tests.

## Files

- `lego/backend/internal/webhooks/service.go`, `internal/events/` (index), `internal/eventvocab/datastore.go`
- `dashboard/src/features/webhooks/event-catalog.ts`, `locales/en.ts`, `locales/zh.ts`, `components/__tests__/event-picker.test.tsx`
- `docs/render-artifacts/webhooks-api.md`, `docs/render-artifacts/datastore-webhook-events.md`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Every new name appears in the REST `/v1/webhooks` event-type validation, the GraphQL/MCP twins, and the dashboard picker, and none appears without a producer.
- [ ] `GET /v1/events/{id}` resolves a datastore availability fact and a backup fact by the id a delivered webhook carried.
- [ ] `webhooks-api.md`, `datastore-webhook-events.md`, and ADR018 agree on the advertised count and list.

## Out of scope

- Push delivery (t005); the datastore-page activity list decision (t007).
