---
id: w3/m82/t010
title: "Closeout"
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m82/t009]
---

## Objective

Close the milestone once its definition of done actually holds — not merely when the code is written.

## Context

- DoD (milestone README): live-proven unavailable/available pairs, backup completion, webhook + push delivery, suspended produces nothing, ledger + picker updated, stale-transition test green for datastores, default push policy unchanged.

## Steps

1. Verify the DoD against t006's evidence; if the prod run is still owed, say so in the README rather than claiming it.
2. Set every remaining task `status: done`, mark rows `— **DONE**` and `**Status:** done` in the milestone README, move each `tNNN.md` to `done/`, move `w3/m82/` to `w3/done/m82/`, check `- [x]` in `.pm/w3/README.md`.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w3/README.md`, `.pm/w3/m82/**`

## Acceptance criteria

- [ ] DoD verified; milestone moved to `w3/done/m82/`; workstream README checked.

## Out of scope

- Any new work (file as follow-up inbox notes instead).
