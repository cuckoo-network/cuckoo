---
id: w3/m46/t008
title: Closeout
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m46/t007]
---

## Objective

Close the milestone once the definition of done actually holds — the observable end states are real, not merely coded.

## Status (2026-09-07) — closed on the owed-live-probe precedent

Implementation + local verification for t001–t007 landed in `167eecf5`: operator suspend keeps the Ingress+cert (envtest green), `apps.Service.List` excludes deleting Apps (unit test), `clearCache` is wired across REST/GraphQL/MCP/UI (unit + parity + dashboard tests), and the SPA-fallback divergence is documented. The later deleting-App by-id/finalizer defect was promoted to `w3/m81` (now DONE); it is broader lifecycle work, not a post-closing-task append to this milestone.

**Closed 2026-09-07 with the live probe explicitly owed**, at user direction and on the established w3 precedent — the sibling `m81` (promoted out of this milestone) and `w6/m46` (see `w6/063`) both closed on deterministic-suite evidence with the live production probe recorded as owed, because `hetzner-prod` is off-limits without explicit authorization. In this run the deterministic evidence was re-verified green at HEAD `164e4958b`:

- backend `internal/apps` — `TestListOmitsDeletingApp` PASS (delete-row projection, DoD 2);
- backend `internal/deploys` — `TestTriggerRejectsUnknownClearCache`, `TestTriggerAcceptsClearCacheEnum`, `TestRESTTriggerClearCacheEnum` PASS (`clearCache`, DoD 3);
- operator `internal/controller` envtest suite PASS (62s), containing `keeps the host Ingress + certificate on the static-server when suspended (w3/m46)` (suspend cert, DoD 1);
- dashboard `manual-deploy-button.test.tsx` 4/4 PASS (Manual Deploy "Clear build cache & deploy");
- DoD 4 (SPA fallback) is a documentation decision in `docs/ADR029-static-sites.md` + the ADR018 ledger — no live check applies.

The m46 code is shipped in `167eecf5` (an ancestor of HEAD) and deployed to prod (`bex-system` rollout `sha256:7619f294…`, built from `main` on 2026-09-07), so the owed probe should pass when run.

**Owed live probe (carried forward, not blocking closure):** re-verify the original four behaviors on a live `onbex.co` static site — suspend → `openssl s_client` shows the Let's Encrypt cert (not `TRAEFIK DEFAULT CERT`) and `GET /` is a 404 over it; delete → the row leaves the Overview list promptly; Manual Deploy → "Clear build cache & deploy" reaches Live. Discharge it in a future `/qa-find-bugs` static-site run or any session with QA credentials / explicit prod authorization, and record the result the way `w6/063` discharged `w6/m46`'s owed probe.

## Context

- DoD: suspended static site no longer breaks TLS / copy is honest; deleted site drops from the list; `clearCache` reachable from Manual Deploy menu + MCP; SPA-fallback divergence documented.
- Live re-verification checklist: suspend a static site → `openssl s_client` shows the Let's Encrypt cert (not `TRAEFIK DEFAULT CERT`) and `GET /` is a 404 over that cert; delete a static site → its row leaves the Overview list promptly; Manual Deploy → "Clear build cache & deploy" triggers a deploy that reaches Live.

## Steps

1. Confirm every implementation + closing task is complete and the DoD is met (re-verify live on `onbex.co` where applicable).
2. Set every remaining task `status: done`; move each `tNNN.md` to `w3/m46/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
4. Move the whole `w3/m46/` directory to `w3/done/m46/` and check `- [x]` in the w3 README.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w3/m46/` (moves), `.pm/w3/README.md`

## Acceptance criteria

- [x] DoD met on deterministic + shipped/deployed evidence; live `onbex.co` probe recorded as explicitly owed (m81 / `w6/063` precedent); milestone moved to `w3/done/m46/` and checked in the w3 README.

## Out of scope

- Any remaining follow-ups filed as new inbox notes rather than blocking closeout.
