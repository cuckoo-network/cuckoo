---
id: w3/m46/t007
title: Test coverage for the shipped behavior
worker: worker3
status: done
estimate: 40m
depends_on: [w3/m46/t005]
---

## Objective

Add meaningful tests asserting the real behavior and failure modes this milestone shipped — never trivial/tautological/snapshot-everything coverage.

## Context

- Behaviors to lock: suspended static site serves managed cert / correct copy + resume restores serving; deleted static site is absent from the list (no lingering `Unknown`); MCP `trigger_deploy` honors `clearCache`; static-server SPA-fallback disposition (behavior test if changed, else the documented default).

## Steps

1. Operator/static-server: regression test for suspend→(maintenance page or unchanged copy)→resume, and the SPA-fallback default if t004 changed it.
2. Backend: test that a deleted static site drops from the services list projection; test MCP `trigger_deploy` passes `clearCache` through.
3. Dashboard: test the Manual Deploy "Clear build cache & deploy" action and post-delete list removal.

## Files

- `lego/operator/internal/staticserver/staticserver_test.go` + operator controller tests
- `lego/backend/internal/deploys/service_test.go`, `lego/backend/internal/apps/*_test.go`
- `dashboard/src/features/services/components/__tests__/manual-deploy-button.test.tsx` + list tests

## Acceptance criteria

- [ ] Each shipped behavior has a test asserting the real outcome and a failure mode.
- [ ] `make test`, `cd lego/backend && go test ./...`, and `yarn test` pass.

## Out of scope

- Coverage for unchanged behavior.
