---
id: w3/m46/t006
title: Simplify the changed code
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m46/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency / altitude) — behavior-preserving only.

## Context

- Changed areas: operator suspend/maintenance path, static-server (if t004 changed behavior), backend deploys/MCP `clearCache` wiring, services list projection, dashboard Manual Deploy menu + list handling + locales.

## Steps

1. Invoke `/simplify` over the milestone diff.
2. Apply behavior-preserving cleanups; keep changes consistent with surrounding code.

## Files

- The files touched by t001–t004.

## Acceptance criteria

- [ ] `/simplify` run over the milestone diff; cleanups applied without behavior change.
- [ ] Tests still pass.

## Out of scope

- Bug hunting (that's `/code-review`) and unrelated refactors.
