---
id: w3/m83/t005
title: "Weekly isolation matrix on the production runner: tenant-isolation + sandbox-isolation scripts, issue on red"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m83/t001]
---

## Objective

Re-prove the security invariants that were verified once and never again — ADR043's namespace reachability matrix and ADR042/m35's 40-check sandbox boundary — every week on the real substrate, so a regression of the CEL-vs-native-selector admission class (found late in m35) is loud rather than latent.

## Context

- `scripts/verify-tenant-isolation.sh`: needs `kubectl` + a curl-capable probe image; creates probe pods, asserts tenant→tenant, tenant→bex-api internal (`:8091`), node/metadata egress denials, `automountServiceAccountToken=false`; cleans up.
- `scripts/verify-sandbox-isolation-live.sh`: needs `BEX_API_URL`, `KUBECONFIG`, `BEX_API_NAMESPACE`, the OpenFGA token secret coordinates, and `BEX_LIVE_AGENT_MODEL_API_KEY` for the agent leg; disposable-fixture audit built in. The model-key leg costs real tokens: either run weekly with the existing `ANTHROPIC_API_KEY` secret or add a `SKIP_MODEL_LEG=1` flag and run the rest; decide and document.
- Kubeconfig acquisition + scrub: copy the `request-logs-liveness` job's steps (pinned host key per validator check 4).
- New workflow `.github/workflows/isolation-matrix.yml`, weekly, `bex-production`, `workflow_dispatch` for manual runs, label `isolation-matrix-red`. Must pass `scripts/github-actions-validate.sh` (SHA-pinned actions, self-hosted labels, credential boundary).

## Steps

1. Make both scripts non-interactive and exit-code clean for CI (flags only where needed; no behaviour change to checks).
2. Write the workflow with fetch → run both → issue open/close → scrub; concurrency group so two runs never overlap.
3. Dispatch once; confirm fixtures are gone (`kubectl get ns | grep -c probe` = 0, no leftover sandboxes).

## Files

- `.github/workflows/isolation-matrix.yml` (new), `scripts/verify-tenant-isolation.sh`, `scripts/verify-sandbox-isolation-live.sh`, `scripts/github-actions-validate.sh` (only if a new allowance is needed), `docs/ADR043-tenant-namespace-isolation.md` + `docs/ADR042-sandbox-cluster-substrate.md` (verification-cadence note)

## Acceptance criteria

- [ ] One dispatched run green on prod for both matrices with zero leftover fixtures.
- [ ] Temporarily labelling a probe pod to expect an allowed path makes the run red and opens `isolation-matrix-red` (red-path, recorded in t007).
- [ ] `github-actions-validate.sh` passes.

## Out of scope

- Adding new isolation checks (file separately if the run suggests any).
