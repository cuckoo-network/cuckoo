---
id: w3/m83/t008
title: "Simplify"
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m83/t006, w3/m83/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — the probe scripts, the liveness/isolation workflows, the rules and panels — for reuse and simplification without behaviour change.

## Context

- Likely targets: four copies of the gh-issue open/close step (extract a composite action or a `scripts/lib/tracking-issue.sh`), duplicated kubeconfig fetch/scrub steps, shared curl/poll helpers between `tenant-view-liveness.sh` and `deploy-canary.sh`.

## Steps

1. `/simplify` on the diff.
2. Re-run `scripts/github-actions-validate.sh`, `scripts/gitops-validate.sh`, `scripts/obs-coverage-check.sh`, and the `scripts.yml` shell tests.

## Files

- The milestone's touched files.

## Acceptance criteria

- [ ] No behaviour change; validators green; duplicated workflow steps collapsed where introduced.

## Out of scope

- New probes.
