---
id: w3/m83/t009
title: "Test coverage"
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m83/t006, w3/m83/t007]
---

## Objective

Add meaningful tests for what this milestone shipped: the probe scripts' pass/fail logic against fixture responses, the workspace guard in the deploy canary, and the alert rules' fire/no-fire cases.

## Context

- Repo pattern for shell tests: `scripts/*.test.sh` run by `.github/workflows/scripts.yml` (see `ssh-verify.test.sh`, `keyvalue-cli-verify.test.sh`); rule tests via promtool fixtures (t006).
- Tests must fail when the logic breaks: e.g., the tenant-view probe must fail when the logs response lacks the unique marker; the canary must refuse a foreign workspace id.

## Steps

1. `scripts/tenant-view-liveness.test.sh`, `scripts/deploy-canary.test.sh`, `scripts/request-logs-liveness.test.sh` with stubbed `curl` responses.
2. Register them in `scripts.yml`; mutation spot-check one assertion per script.

## Files

- `scripts/*.test.sh` (new), `.github/workflows/scripts.yml`

## Acceptance criteria

- [ ] Each probe has a failing-when-broken test; `scripts.yml` green.

## Out of scope

- Coverage-number chasing.
