---
id: w3/m18/t004
title: `ownerId` honored-or-400 + `aggregateBy` instance decision
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m18/t001]
---

## Objective

Close the two adjacent accepted-but-ignored fields: `MetricsFiltersQueryInput.ownerId` (graphql.go:201) gets validated against the caller's workspace (mismatch → error) instead of being ignored; the instance-flavored `aggregateBy` values (graphql.go:434) get an explicit recorded decision — implement if the data supports it, otherwise reject-or-document, never silently drop.

## Files

- lego/backend/internal/metrics/graphql.go
- docs/ADR006-bex-api.md (decision record if reject/document)

## Acceptance criteria

- [ ] No metrics input field is silently ignored; behavior covered by tests or a recorded decision.
