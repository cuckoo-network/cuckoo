---
id: w3/m18/t001
title: Verify router-label feasibility + cardinality on the live Traefik
worker: worker3
status: todo
estimate: 45m
depends_on: []
---

## Objective

Establish with evidence whether Traefik router-level Prometheus metrics (`--metrics.prometheus.addRoutersLabels=true`) give a usable host/path signal for per-service request filtering, and what the series-cardinality cost is on the real cluster — before any wiring.

## Context

- `lego/backend/internal/metrics/service.go:244` — the honest 400 (w3/m12).
- Router names encode the Ingress host rule; path granularity may only exist per-router, not per-request-path — that finding decides t003's real scope.
- Prometheus scrape config in deploy/gitops (w3/m4's Traefik scrape).

## Steps

1. Enable router labels on a dev/mock Traefik; inspect the emitted series (labels, per-host mapping, path availability).
2. Estimate cardinality growth for N services × M hosts; compare against the current scrape budget.
3. Record findings + go/no-go in the milestone (an evidence-backed "no" re-scopes t002/t003 to documentation).

## Files

- (evidence; gitops change only on the dev overlay)

## Acceptance criteria

- [ ] A written go/no-go with real emitted-series evidence and a cardinality estimate.

## Out of scope

- Production config (t002).
