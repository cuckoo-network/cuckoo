---
id: w3/m45/t005
title: Navigation integrity: deep links + standalone list + m44 view intact
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m45/t001, w3/m45/t004]
---

## Objective

Guarantee the restructure breaks no navigation: `/agents` and `/agents/$id` deep links keep working, the standalone sessions list stays reachable (the sidebar More action — either a scrollable full sidebar list or a retained list view), and the m44 session view is untouched.

## Context

- Routes: `agents.tsx`, `agents_.$agentSessionId.tsx`; route-inventory pins filenames.
- If the table list view is retired from the main pane, More must land somewhere real (e.g. an expanded sidebar or a `/agents?list=1` state) — no dead affordances.

## Steps

1. Decide + wire the More target; verify both deep links and the sidebar switch flow.
2. Sweep for dangling links to removed UI (e.g. old list-table anchors).

## Files

- `dashboard/src/routes/agents.tsx` (+ possibly `agents_.$agentSessionId.tsx` sidebar wiring)

## Acceptance criteria

- [ ] Both deep links render correctly; More lands on a real full list; the m44 session view unchanged.

## Out of scope

- New routes beyond what More needs.
