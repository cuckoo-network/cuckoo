---
id: w3/m45/t003
title: Configuration popover (Advanced fields relocated + error anchoring)
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m45/t001]
---

## Objective

Move the old Advanced fields — agent select (claude/gemini/codex), model, modelEndpoint, egress allowlist — plus the auto-derived branch (editable) into a compact Configuration popover on the composer toolbar, preserving the typed create-error anchoring.

## Context

- Current fields + error anchoring live in `new-session-composer.tsx` (egress `{reason,entry}` and model-endpoint errors anchor to their fields and auto-expand Advanced; 503 = house callout).
- Devin's equivalent is the "Configuration" toolbar popover (`.playwright-mcp/devin-home.png`).

## Steps

1. Popover (shadcn) on the toolbar holding the relocated fields; state lives in the same form.
2. On create rejection, field-coded errors open the popover and anchor inline (the old auto-expand behavior); 503 keeps the house callout on the composer.

## Files

- `dashboard/src/features/agent-sessions/components/new-session-composer.tsx` (+ a popover subcomponent)
- locales en/zh

## Acceptance criteria

- [ ] All four advanced fields + branch round-trip through the popover; egress/model-endpoint errors open + anchor; 503 callout intact.

## Out of scope

- New config fields.
