---
id: w3/m45/t004
title: Sidebar polish: search, More, human status phrases, shortcut
worker: worker3
status: done
estimate: 60m
depends_on: []
---

## Objective

Bring the m44 `SessionSidebar` to Devin grade: a Search affordance + a More/view-all action on the Recent header; rows show a human status phrase derived from phase + PR presence ("PR is ready" / "Working…" / "Failed" / "Canceled") with the PR number linking DIRECTLY to GitHub; a keyboard shortcut focuses New session.

## Context

- `dashboard/src/features/agent-sessions/components/session-sidebar.tsx` (m44) + `lib/mapper.ts` (phase predicates).
- Status phrase mapping: completed+PR ⇒ "PR is ready"; running/creating/resuming/redispatching ⇒ "Working…"; failed ⇒ "Failed"; canceled ⇒ "Canceled"; completed w/o PR ⇒ "Completed".
- Devin evidence: `.playwright-mcp/devin-session-sidebar.png` ("PR is ready • 1" where 1 → github PR; Recent header has Search + More; "New session (O)").
- The sidebar stays mounted only on `/agents*` (already matches Devin's feature-scoping — verified absent on their Wiki).

## Steps

1. Recent header: search input (client-side filter over title/repo) + a More action → the standalone list (t005).
2. Rows: human status phrase + PR number as an external GitHub link (stopPropagation).
3. Keyboard shortcut (match the repo's existing shortcut conventions; avoid ⌘K conflicts) focusing the composer / navigating to `/agents`.

## Files

- `dashboard/src/features/agent-sessions/components/session-sidebar.tsx`
- `dashboard/src/features/agent-sessions/lib/mapper.ts` (status-phrase helper)
- locales en/zh

## Acceptance criteria

- [ ] Search filters the Recent list; More reaches the full list; rows show the human phrase + working GitHub PR link; the shortcut works.

## Out of scope

- Server-side search; pagination.
