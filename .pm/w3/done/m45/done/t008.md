---
id: w3/m45/t008
title: Test coverage
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m45/t006]
---

## Objective

Meaningful tests for what m45 shipped: the composer renders as prompt-box-only (no form fields); `@` opens the category listbox; token insert + fuzzy filter + arrow/Enter navigation; picking embeds a chip that sets the repo; the no-chip nudge blocks submit; the Configuration popover round-trips all fields and anchors egress/model-endpoint errors; sidebar human status phrases + search filter + More + the GitHub PR link; `bex-agent/*` branch validation still enforced; deep links render.

## Context

- Existing tests to update: `components/__tests__/new-session-composer.test.tsx` (form-shaped assertions will break — rewrite them against the new UX, keep the error-mapping rigor); sidebar tests if present.

## Files

- `dashboard/src/features/agent-sessions/**/__tests__/*`

## Acceptance criteria

- [ ] All listed behaviors asserted for real; `yarn test` green.

## Out of scope

- Live-substrate walk (Closeout).
