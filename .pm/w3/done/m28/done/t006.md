---
id: w3/m28/t006
title: Simplify
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m28/t005]
---

## Objective

Run the repository simplify workflow over the milestone's changes, removing
adapter duplication and keeping log-source/query behavior in one core path.

## Acceptance criteria

- [x] Simplify findings are applied or explicitly recorded as no-change.

## Completion

The `/simplify` skill is unavailable in this Codex session, so its
behavior-preserving review was performed manually. The implementation reuses
`LogQuery`, the Loki source, pod-log reader, line renderer, and dashboard log
list; upstream's dedicated Postgres adapters delegate typed ids to that same
production query engine. The review also removed the duplicate dashboard panel,
added an explicit database-SSE refusal, and added a GitOps attribution guard.
