---
id: w3/m28/t007
title: Test coverage
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m28/t005]
---

## Objective

Run focused backend/dashboard tests and the required module suites, emphasizing
authorization, tenant attribution, bounds, adapter parity, and UI degraded
states.

## Acceptance criteria

- [x] Focused and required suites pass; live verification covers one uniquely
      marked database log line without cross-database leakage.

## Completion

Green: backend `go build ./... && go test ./...`; operator `make test`
(including envtest); dashboard `yarn lint && yarn test` (1,212 tests); and
`scripts/gitops-validate.sh` for base/local/prod renders plus the new Alloy
attribution guard. Tests seed two CNPG database pod sets and forbid mixing; the
fresh browser capture shows uniquely Postgres-shaped checkpoint/query/vacuum
lines and instance attribution.
