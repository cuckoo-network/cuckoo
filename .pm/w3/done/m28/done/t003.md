---
id: w3/m28/t003
title: REST GraphQL and MCP adapters for Postgres logs
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m28/t002]
---

## Objective

Expose the single core query through Render-shaped REST, GraphQL, and MCP
adapters with identical bounds, errors, and authorization.

## Acceptance criteria

- [x] Adapter contract tests prove field/filter/error parity and reject
      cross-workspace reads on every surface.

## Completion

Render's existing generic REST, GraphQL, and MCP log adapters now accept
`dpg-…` resources. The dedicated Postgres compatibility adapters remain and
delegate typed ids to the same production core. End-to-end adapter tests pin
resource/type/instance labels, search/instance behavior, and fail-closed
cross-workspace authorization before any history-source call.
