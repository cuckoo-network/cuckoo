---
id: w3/m15/t001
title: Capture the Render contract and compatibility plan
worker: worker3
status: todo
estimate: 45m
depends_on: []
---

## Objective

Pin the exact Render service-notification contract shown in the supplied screenshot before changing the model: enum/wire values, endpoint placement, workspace-default composition, recipient semantics, and the relationship between `notificationsToSend` and the read-only service `notifyOnFail` field. Define how bex's member preferences and existing `notifyOnFail=default|notify|ignore` values migrate.

## Context

- `docs/render-artifacts/notify-on-fail.md` captured the narrow field but explicitly deferred Render's richer `/notification-settings/overrides/services/{id}` model.
- `w3/done/m9` owns member notification preferences; `w4/done/m21` owns the current failure-only override.
- The current worktree contains `0032_notification_failure_only`, a partial default change that must be reconciled rather than independently shipped.

## Steps

1. Verify the screenshot states and exact Render API contract.
2. Record precedence between workspace/member defaults and service overrides.
3. Specify a backward-compatible mapping and migration for existing rows and `App.spec.notifyOnFail`.

## Files

- `docs/render-artifacts/notify-on-fail.md`
- `docs/ADR006-bex-api.md`
- `docs/ADR018-render-parity.md`
- `lego/backend/internal/store/migrations/0032_notification_failure_only.*.sql`

## Acceptance criteria

- [ ] Contract evidence names all four UI states, exact wire values/endpoints, precedence, recipients, and migration behavior without guessing.
- [ ] The plan states whether `notifyOnFail` remains a compatibility projection, is deprecated, or is replaced.

## Out of scope

- Slack and preview-environment notifications.
