---
id: w3/m15/t008
title: Test coverage
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m15/t006]
---

## Objective

Add meaningful automated and live coverage for defaulting, migration, authorization, cross-surface parity, delivery decisions, and selector persistence. Tests must fail on the original noisy all-enabled behavior.

## Context

- Backend coverage must include a real-Postgres migration case for legacy all-enabled and customized rows.
- Dashboard coverage must exercise all four options without snapshot-only assertions.

## Steps

1. Add focused Go unit/integration tests and adapter parity coverage.
2. Add dashboard hook/component/route tests.
3. Run backend and dashboard gates plus a real-browser save/reload acceptance pass.

## Files

- `lego/backend/internal/**/*_test.go`
- `dashboard/src/**/__tests__/`
- `scripts/` if a reusable live verifier is warranted

## Acceptance criteria

- [ ] Tests prove inherited failure-only, all, failure-only, and none across all lifecycle events.
- [ ] Migration tests prove customized rows are preserved per t001.
- [ ] Browser evidence proves selection persists and the inherited label resolves correctly.

## Out of scope

- SMTP provider deliverability testing beyond the existing mailer boundary.
