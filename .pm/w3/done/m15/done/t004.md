---
id: w3/m15/t004
title: REST, GraphQL, and MCP notification-policy surfaces
worker: worker3
status: todo
estimate: 60m
depends_on: [w3/m15/t002]
---

## Objective

Expose the captured service-notification policy consistently across REST, GraphQL, and MCP, including Render's management endpoint where applicable. Retain an explicit compatibility path for existing `notifyOnFail` clients according to t001 rather than silently changing its meaning.

## Context

- Existing member settings use `/v1/notification-settings` plus GraphQL/MCP equivalents.
- Existing service override is threaded through generic service create/PATCH and `set_notify_on_fail`.
- The screenshot represents a richer service override than `notifyOnFail` alone.

## Steps

1. Implement shared core verbs and validation.
2. Add thin REST, GraphQL, and MCP adapters with identical enum/error behavior.
3. Regenerate or deliberately update dashboard GraphQL artifacts.

## Files

- `lego/backend/internal/notifications/`
- `lego/backend/internal/apps/`
- `lego/backend/internal/api/`
- `dashboard/src/graphql/`

## Acceptance criteria

- [ ] All three surfaces read/write the same four-state policy and reject unknown values consistently.
- [ ] Cross-workspace access follows existing service authorization rules.
- [ ] Existing `notifyOnFail` clients observe the documented compatibility behavior.

## Out of scope

- A new general-purpose notification-rule engine.
