---
id: w3/m15/t005
title: Service Settings four-state notification selector
worker: worker3
status: todo
estimate: 60m
depends_on: [w3/m15/t003, w3/m15/t004]
---

## Objective

Replace the narrow service `notifyOnFail` control with the four-state Service Notifications selector shown in the supplied Render screenshot: `Use workspace default (Only failure notifications)`, `All notifications`, `Only failure notifications`, and `None`.

## Context

- Preserve the dashboard's established visual language and read `dashboard/CLAUDE.md` before editing.
- The inherited option must render the resolved workspace default in its label, not a stale hard-coded description.
- Saving and error feedback must follow existing service-settings patterns.

## Steps

1. Build the selector and explanatory copy in the existing Notifications card.
2. Wire loading, mutation, optimistic/cache behavior, and errors.
3. Verify desktop/mobile layout, save, and reload against a live local backend.

## Files

- `dashboard/src/features/services/`
- `dashboard/src/routes/services.$serviceId.settings.tsx`
- `dashboard/src/features/services/locales/`
- `dashboard/src/graphql/`

## Acceptance criteria

- [ ] All four labels and selected states match the screenshot's information architecture.
- [ ] A new/default service displays `Use workspace default (Only failure notifications)`.
- [ ] Each choice persists through a full-page reload and changes actual delivery behavior.

## Out of scope

- Preview-environment notification controls shown elsewhere in Render's panel.
