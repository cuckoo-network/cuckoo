---
id: w3/m10/t009
title: "Closeout — verify DoD met, then move the milestone to done/"
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m10/t007, w3/m10/t008]
---

## Objective

Verify the milestone's definition of done actually holds, then close it out per `.pm/CLAUDE.md` conventions.

## Steps

1. Confirm the metrics page/API shows autoscale-target, disk usage, and DB active-connections series for real services/databases, and replication-lag is wired but inert (documented) pending `w1/m22`.
2. Set every task's `status: done`, move each `tNNN.md` to `w3/m10/done/`.
3. Mark every row `— DONE` and `**Status:** done` in this README.
4. Move the whole `w3/m10/` directory to `w3/done/m10/`.
5. Check `- [x]` for m10 in `w3/README.md`.

## Acceptance criteria

- [ ] Milestone's observable end state is real, not just code-complete.
- [ ] Board moved and status synced in all three places.
