---
id: w3/m40/t006
title: "Test coverage: phase transitions, allowlist rendering, deny defaults"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m40/t004]
---

## Objective

Meaningful tests for the egress feature's failure modes: policy rendering for both phases (golden CNP output per profile), transition never widens (no intermediate allow-all), invalid tenant entries → named 400, deny floors present in every rendered variant, and provider→FQDN derivation.

## Context

- Standing closing task. Rendering tests are unit-level (no cluster needed); the live matrix is t004's job.

## Steps

1. Golden-render tests per profile (setup, agent baseline, agent+widened).
2. Validation tests (bad FQDN, wildcard abuse, metadata-range attempt).
3. Transition-safety test (label swap sequence).

## Files

- `lego/backend/internal/sandbox/` test files

## Acceptance criteria

- [ ] All groups green; deny floor asserted in every golden output

## Out of scope

- Live-cluster checks (t004)
