---
id: w3/m40/t002
title: "Baseline agent-phase allowlist: GitHub + model endpoint + gateway credential hop"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m40/t001]
---

## Objective

Define and render the agent-phase baseline (ADR047 D5): GitHub (push via the m38 helper), the configured model API endpoint (per the session's agent/BYO-key provider), and the gateway credential endpoint (m38 t002 hop) — nothing else. Record the D5 forward note: when the metering LLM proxy ships (wave 2), the model-endpoint entry narrows to the proxy.

## Context

- Model endpoint varies by provider (e.g. `api.anthropic.com`, `generativelanguage.googleapis.com`) — derive from the session's agent config, not a global constant.
- The gateway hop is in-cluster (CiliumNetworkPolicy endpoint selector), the rest are FQDN/SNI entries (m35 machinery).

## Steps

1. Baseline profile rendering from session config (provider → FQDN set).
2. In-cluster gateway-endpoint egress rule scoped to the credential port only.
3. D5 proxy-narrowing note in ADR047/policy code comments.

## Files

- `lego/backend/internal/sandbox/` (policy rendering)

## Acceptance criteria

- [ ] Agent-phase sandbox reaches GitHub + its model endpoint + the gateway hop; a control destination is denied
- [ ] Baseline derived per-session from provider config

## Out of scope

- The metering proxy itself (wave 2)
