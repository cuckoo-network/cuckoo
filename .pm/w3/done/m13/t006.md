---
id: w3/m13/t006
title: Test coverage
worker: worker3
status: done
estimate: 1h
depends_on: [w3/m13/t004]
---

## Objective

Add meaningful tests/checks for the behavior this milestone shipped: node-scoped discovery correctness and the regression guard from t004.

## Steps

1. If the config supports any local validation/dry-run tooling, add a test exercising the node-scope selector logic.
2. Ensure t004's regression signal itself has a test proving it actually fails when the scoping is removed (not just present).

## Files

- Test/CI files alongside `deploy/gitops/base/log-shipper.yaml`.

## Acceptance criteria

- [x] Tests assert real behavior (scoping present and effective), not tautological checks.

## Out of scope

- Full integration test requiring a live cluster (covered by t003, a one-time verification, not a repeatable CI test).

## Resolution

Proved t004's guard actually fires on a regression, not just that it's present: mutated a scratch copy of `log-shipper.yaml` to drop the field-selector expression and confirmed the guard's check exits 1 with a clear `FAIL: ... lost its node-scope field selector` message; confirmed it exits 0 cleanly on the correct config. Manual mutate/confirm-red/restore, matching this repo's established convention for this guard style (e.g. `w7/m8/t005`'s registry-auth guard verification — this class of structural `gitops-validate.sh` check doesn't carry a permanent negative-test script in this repo, verified live instead).
