---
id: w3/m13/t005
title: Simplify
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m13/t004]
---

## Objective

Run `/simplify` over the code/config this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Steps

1. Run `/simplify` over the diff from t001-t004.
2. Apply behavior-preserving cleanups.

## Files

- All files touched by t001-t004.

## Acceptance criteria

- [x] `/simplify` run completed; findings applied or explicitly declined with reason.

## Out of scope

- Behavior changes.

## Resolution

Ran `/simplify`'s review over the diff (`deploy/gitops/base/log-shipper.yaml` + `scripts/gitops-validate.sh`). One finding applied: the node-scoping rationale was initially written out in full three separate times (file-level note, extraEnv comment, discovery-block comment) — collapsed to one authoritative top-of-file "NODE SCOPING" note with the two inline comments reduced to one-line pointers. `scripts/gitops-validate.sh`'s new guard was found already clean (reuses the existing `if [ -f ]; vals=$(yq ...)` shape, comment length proportionate to the guards it's modeled on).

A second, self-inflicted issue surfaced and was fixed during t003's live-render testing (after this task's first pass): an explanatory comment placed *inside* `configMap.content` (which is itself passed through Helm's `tpl`) used literal double-brace syntax to describe the tpl-escaping trick, and Helm's `tpl` tried to evaluate that too, breaking the render. Moved that whole explanation to the outer YAML file-level comment (never passed through `tpl`) and reduced the inline River comments to brace-free one-liners referencing it.
