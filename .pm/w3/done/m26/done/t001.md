---
id: w3/m26/t001
title: Capture Render's datastore webhook event-type vocabulary
worker: worker3
status: done
estimate: 25m
depends_on: []
---

## Objective

Enumerate, with evidence, the exact event-type strings Render emits for Postgres and Key Value lifecycle (create/available, suspend/resume, delete, plan changes, maintenance if any) from Render's webhook docs and the pinned OpenAPI's webhook-event schemas, so t002 emits Render-named types rather than invented ones.

## Context

- bex's existing registry: `lego/backend/internal/webhooks/service.go:86-94` (deploy/server/service/cron families).
- Sources: render.com webhook docs, the pinned `render-public-api-1.json` event-type enums, and `docs/render-artifacts/` captures if present.

## Steps

1. Extract the datastore-relevant enum values + payload shapes.
2. Record the list (type string → trigger condition → payload fields) in this task/milestone notes.

## Acceptance criteria

- [x] Vocabulary table recorded with source citations; no invented names.

## Out of scope

- Emission (t002).

## Completion

Captured 2026-07-15 in `docs/render-artifacts/datastore-webhook-events.md` from Render's current public webhook documentation. The capture also corrects the milestone premise: Render defines no datastore suspend/resume/delete types and no Key Value create type.
