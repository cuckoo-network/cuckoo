---
id: w3/m26/t006
title: Test coverage
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m26/t004]
---

## Objective

Extend the webhook tests: registry knows the new types; each datastore lifecycle transition enqueues exactly one correctly-typed event with the documented payload; an unrelated transition enqueues nothing. Assert behavior, not snapshots.

## Files

- `lego/backend/internal/webhooks/` + postgres/keyvalue tests

## Acceptance criteria

- [x] Behaviors locked; `go test ./...` passes.

## Completion

Postgres/Key Value service tests lock post-success effect counts and target identity, registry tests lock every verb mapping and exclude generic updates, the worker test locks the thin payload projection, and the Postgres-backed store test covers immutable id/display-name recovery from the audit feed. `cd lego/backend && go build ./... && go test ./...` passes.
