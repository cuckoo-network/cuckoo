---
id: w3/m33/t004
title: MCP sandbox_exec (+ GraphQL) — direct execd, no token dance
worker: worker3
status: done
estimate: 2h
depends_on: [w3/m33/t001]
---

## Objective

Expose exec to agents over MCP as `sandbox_exec` (and GraphQL if the surface warrants it): run a command via `Client.Exec` (t001) directly, without the browser two-step token handshake — agents drive exec through MCP (ADR014 D3, ea-sandbox.md §exec note).

## Context

- Three-adapter consistency (`internal/api/CLAUDE.md`): the exec verb lives once in `service.go`; MCP/GraphQL are thin fragments. The REST two-step (t002/t003) is a presentation shape the unmodified CLI needs; MCP surfaces the same underlying `Service` exec.
- Authz identical to t002 (`can_operate` on the sandbox's workspace); scoped by the per-workspace key.

## Steps

1. Add `RegisterMCP` `sandbox_exec` (args: sandbox id, command, optional workspaceId) → returns stdout/stderr + exit code.
2. Add the GraphQL mutation only if it maps cleanly (else note why omitted, per parity rule).

## Files

- `lego/backend/internal/sandbox/mcp.go`, `graphql.go`, `service.go`

## Acceptance criteria

- [ ] MCP `sandbox_exec` runs a command in a sandbox and returns output + exit code, authorized + key-scoped.
- [ ] Verb registers into the single MCP registry (TestSurfaceParityAndWiring stays green).

## Out of scope

- The REST two-step + SSE (t002/t003).

## Done

MCP `sandbox_exec` runs a command via `ExecBuffered` (same authorized gateway path, buffered result) — agents get stdout/stderr/exitCode without the SSE handshake. REST + MCP consistent; GraphQL omitted (a streaming exec does not map cleanly to a GraphQL field — documented divergence).
