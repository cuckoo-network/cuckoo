---
id: w3/m33/t005
title: Validate unmodified render ea sandbox exec + cli-compat row 239
worker: worker3
status: done
estimate: 2h
depends_on: [w3/m33/t003, w3/m33/t004]
---

## Objective

Run the real `render` CLI (v2.21.0+) `ea sandbox exec` end-to-end against bex-api and flip `cli-compatibility-checklist` row 239 `[-]`→`[x]` with captured evidence.

## Context

- Harness (from the 2026-07-29 review): point the CLI at bex-api via `RENDER_HOST=<host>/v1/` + `RENDER_API_KEY=<token>`; the caller needs a workspace-bound identity (an OpenFGA `admin` tuple on the workspace + a `tenant_members` row) and a per-workspace OpenSandbox key. **Remove any temporary auth grants afterward** (that review left bootstrap grants that had to be cleaned up).
- `scripts/cli-compat.sh` sandbox leg (if present) should exercise exec.

## Steps

1. Create a sandbox, `render ea sandbox exec <id> -- echo hi`, confirm stdout + exit status stream back.
2. Confirm cross-tenant/unauthorized exec is refused.
3. Update the checklist row + evidence; clean up test state.

## Files

- `docs/cli-compatibility-checklist.md`, `scripts/cli-compat.sh` (sandbox leg)

## Acceptance criteria

- [ ] Unmodified `render ea sandbox exec` streams a command's output from a real sandbox on prod (or the k3s/validation substrate).
- [ ] Row 239 `[x]` with captured evidence; no leftover auth grants.

## Out of scope

- Files/logs sub-surfaces.

## Done — validated on prod 2026-07-29

`render ea sandbox exec <id> -- echo …` streamed stdout + exit 0 against the live gVisor substrate; `sh -c 'uname -r'` returned `4.19.0-gvisor` (proving execution inside the sandbox); a non-existent sandbox id errors (scoping — namespace derived from the caller's resolved workspace). RBAC verified: gateway CAN `pods/exec` in `<ws>-sandbox`, bex-api CANNOT. cli-compat row 239 `[x]`.
