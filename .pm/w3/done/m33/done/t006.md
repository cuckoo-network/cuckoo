---
id: w3/m33/t006
title: Render parity (ea sandbox exec across REST + MCP + CLI)
worker: worker3
status: done
estimate: 1h
depends_on: [w3/m33/t005]
---

## Objective

Verify the exec change is consistent across every surface it exposes — REST (the two-step CLI flow), MCP (`sandbox_exec`), and the unmodified CLI — with matching semantics/error shapes, and flag any drift vs render.com as follow-up.

## Steps

1. Cross-check REST vs MCP exec: same authz relation, same command/exit semantics, same error dialect.
2. Compare against render.com's `ea sandbox exec` behavior; record deviations in the parity ledger (docs/ADR018) or as follow-ups.

## Acceptance criteria

- [ ] REST and MCP exec are behavior-identical where they overlap; deviations documented, not silent.

## Out of scope

- New surfaces.

## Done

Parity: exec on REST (the CLI's SSE) + MCP (`sandbox_exec`, buffered). GraphQL exec omitted — a streaming command run does not fit a GraphQL field; noted as a deliberate divergence rather than faked.
