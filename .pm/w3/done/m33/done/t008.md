---
id: w3/m33/t008
title: Test coverage (token mint/verify, stream bridge, authz, MCP exec)
worker: worker3
status: done
estimate: 2h
depends_on: [w3/m33/t006]
---

## Objective

Add meaningful tests for exec: token mint + verify (expiry/replay/mis-bind), the execd stream bridge, authz refusal (cross-tenant), and MCP `sandbox_exec`. Assert real behavior and failure modes.

## Acceptance criteria

- [ ] Client.Exec (stub server), token verify negative paths, and MCP exec are covered; authz-refused exec never reaches the server.

## Done

Tests: ticket Mint/Verify (tamper/expiry/missing), gateway SSE handler (streams + targets + bad-ticket/replay/disabled), bex-api relay (authz + ticket minting + SSE relay + unavailable), buffered SSE parse, and the per-`<ws>-sandbox` gateway RoleBinding. Full suite + lint green.
