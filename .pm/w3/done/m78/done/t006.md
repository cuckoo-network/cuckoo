---
id: w3/m78/t006
title: "Test coverage: projection, policy round-trip, edge dedup"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m78/t004]
---

## Objective

Ensure the milestone's behavior is locked by meaningful tests — real behavior and failure modes, not tautologies.

## Context

- Must cover: per-fact projection (event name, title, urgency, deep link); default-policy invariance (no new events auto-enabled); policy round-trip REST↔GraphQL with the new events; unknown-event rejection; checkpoint edge dedup under repeated observations (t003's integration test counts here — verify it landed and is gated correctly on `BEX_TEST_DB_URI`); dashboard validation accepting the new events; mobile vocabulary test updated deliberately.

## Steps

1. Audit coverage of the m78 diff against the list above; fill gaps.
2. Confirm CI runs them (`backend-test.yml` real-PG job; dashboard `yarn test`).

## Files

- `lego/backend/internal/notifications/*_test.go`
- `lego/backend/internal/store/event_facts_test.go`
- `dashboard/src/features/notifications/` tests
- `mobile/src/__tests__/`

## Acceptance criteria

- [ ] Every behavior in the Context list has a failing-mode-asserting test.
- [ ] All suites green locally and in CI configuration.

## Out of scope

- Coverage-percentage games; snapshot-everything tests.
