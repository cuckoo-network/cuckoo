---
id: w3/m27/t001
title: Pin the fresh spec's webhook contracts into a render-artifacts doc; decide alias-vs-move
worker: worker3
status: todo
estimate: 40m
depends_on: []
---

## Objective

Capture Render's public webhook API contract (new since bex's 2026-07-12 assessment) as pinned evidence, and decide whether bex moves to `/v1/webhooks` with `/v1/webhooks/endpoints` kept as aliases (recommended) or serves both as peers.

## Context

- Fresh spec: `https://api-docs.render.com/openapi/render-public-api-1.json` — as of 2026-07-15 it has `POST/GET /webhooks`, `GET/PATCH/DELETE /webhooks/{webhookId}`, `GET /webhooks/{webhookId}/events`. PATCH body: `name`/`url`/`enabled`/`eventFilter`. POST requires `ownerId`/`name`/`url`/`enabled`/`eventFilter`.
- bex today: `/v1/webhooks/endpoints…` routes (`lego/backend/internal/webhooks/rest.go:121-183`), `eventTypes` field, `/deliveries` history, enabled-only PATCH.
- The trimmed test fixture `lego/backend/internal/api/testdata/render-openapi.json` (11 paths) is NOT the pinned spec — record where the authoritative snapshot for this contract lives.

## Steps

1. Download the spec; extract the webhook path schemas, the webhook object shape (field names, `eventFilter` semantics — filter vs list), list envelope/paging, and the `/events` item schema.
2. Write `docs/render-artifacts/webhooks-api.md` with the pinned contracts + capture date; note every axis where bex's shipped wire shape differs.
3. Record the alias-vs-move decision and the `eventTypes`→`eventFilter` mapping plan in the doc.

## Files

- `docs/render-artifacts/webhooks-api.md` (new)

## Acceptance criteria

- [ ] The artifact doc pins every webhook route + body/response schema with capture date and lists each bex divergence with code pointers
- [ ] The alias-vs-move decision and field-mapping plan are written down

## Out of scope

- Any code change (t002–t004)
