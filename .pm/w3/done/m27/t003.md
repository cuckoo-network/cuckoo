---
id: w3/m27/t003
title: GET /v1/webhooks/{id}/events per the pinned schema; keep /deliveries as the bex superset
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m27/t001]
---

## Objective

Serve Render's `GET /webhooks/{webhookId}/events` read — mapping bex's delivery history onto the pinned event-item schema — while keeping `GET …/{id}/deliveries` (keyset cursor, richer per-attempt detail) as the documented bex superset.

## Context

- Deliveries store + route: `lego/backend/internal/webhooks/rest.go:183` and the `webhook_deliveries` table (ADR006:218).
- The pinned schema (t001) defines the event item shape and paging; map fields honestly — omit, never fake, anything bex doesn't record.

## Steps

1. Implement the route against the deliveries store with the pinned response shape + paging.
2. Document the events-vs-deliveries relationship in the t001 artifact doc.

## Files

- `lego/backend/internal/webhooks/rest.go`
- `docs/render-artifacts/webhooks-api.md`

## Acceptance criteria

- [ ] `GET /v1/webhooks/{id}/events` returns pinned-schema items for a webhook with delivery history; paging walk terminates
- [ ] Fields bex can't source are omitted with the omission recorded in the artifact doc

## Out of scope

- Changing the deliveries route/shape
