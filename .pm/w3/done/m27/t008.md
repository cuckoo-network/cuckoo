---
id: w3/m27/t008
title: Test coverage
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m27/t006]
---

## Objective

Meaningful tests for the behavior this milestone shipped: the Render-path wire contract, full PATCH semantics, the events read, and cross-surface update parity — asserting real behavior and failure modes.

## Context

- Existing webhook tests: `lego/backend/internal/webhooks/*_test.go` (incl. `signing_test.go`).
- The DoD's wire-level walk (create→…→events at Render paths with Render field names) belongs here if not already added in t002/t003.

## Steps

1. Wire-level test at `/v1/webhooks` paths with Render field names, including the alias equivalence.
2. Failure modes: invalid `eventFilter` value, SSRF-guarded URL on update, PATCH on a missing id.

## Files

- `lego/backend/internal/webhooks/rest_test.go` (or feature test files)

## Acceptance criteria

- [ ] The DoD walk passes as a test; failure modes asserted; no tautological tests

## Out of scope

- Live E2E script changes (scripts/webhooks-verify.sh) unless trivially extended
