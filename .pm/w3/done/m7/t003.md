---
id: w3/m7/t003
title: MCP — mirror the official server's events tool if it exists, else document the omission
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m7/t002]
---

## Objective

Check `render-oss/render-mcp-server`'s current tool list for a service-events tool. If one exists, mirror its name/args/shape over the t001 view (the compatibility promise). If not, decide: add `list_service_events` as a bex extension (agents already reason over `list_deploys`; events may be redundant for them) or omit — either way record the decision in docs/ADR006-bex-api.md's MCP table and the milestone README, so the parity task (t005) has a documented stance instead of an accident.

## Files

- `lego/backend/internal/events/mcp.go` (if added)
- `docs/ADR006-bex-api.md`

## Acceptance criteria

- [ ] Official-server comparison recorded; tool added under the matching/extension name, or the omission documented with the why.

## Out of scope

- Inventing agent workflows beyond the tool registration.
