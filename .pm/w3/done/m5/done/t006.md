---
id: w3/m5/t006
title: Render parity — log shapes/filters consistent across REST/GraphQL/MCP/UI; compare Render
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m5/t005]
---

## Objective

Standing closing task. The backend swap must be invisible at the surfaces: REST, GraphQL, MCP `list_logs`, and the dashboard live-logs page return the same fields, filter semantics, and error shapes as before, and the now-real time-range behavior matches Render's logs API semantics (`render-public-api-1.json` + docs/ADR006-bex-api.md's verified-parity notes). Flag any drift as follow-up inbox notes rather than silently diverging.

## Acceptance criteria

- [ ] All four surfaces verified consistent; divergences from Render's logs behavior documented or filed.
