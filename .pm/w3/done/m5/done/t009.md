---
id: w3/m5/t009
title: Closeout — DoD met → move milestone to done/
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m5/t008]
---

## Objective

Standing closing task, run last and only when the DoD actually holds (pre-restart logs retrievable live, retention covering the recorded window, fallback byte-identical). Close the milestone: remaining tasks `status: done`, task files to `w3/m5/done/`, rows `— **DONE**` + `**Status:** done`, move `w3/m5/` whole to `w3/done/m5/`, check the box in `w3/README.md`.

## Context

_2026-07-11 additions — stale-claim refresh riding the closeout (code prerequisites outside .pm):_

- Refresh the storage caveat to the hcloud-volumes reality: `deploy/gitops/base/loki.yaml`'s "local-path … on prod / a node loss loses the history it holds" comment, `deploy/gitops/base/prometheus.yaml`'s matching comment, and docs/ADR010-observability.md's retention paragraph ("same single-node reality as Prometheus's PV"). On prod the default StorageClass is `hcloud-volumes` (network-attached, re-attaches to a replacement node): history survives node loss; the remaining caveat is single-replica read unavailability during re-attach (minutes), not data loss. Keep the local-path/data-loss caveat scoped to the CAPD mock only.
- Reword surviving "single-node" phrasing to "single-replica" / "platform-pool scale": `deploy/gitops/base/log-shipper.yaml` ("acceptable at single-node platform scale") and `loki.yaml` ("keep it single-node") — the platform pool is min 2 nodes now and the cluster is multi-pool.
- Optionally add one docs/ADR010-observability.md line: durable history also covers tenant-node replacement (autoscaler churn) — the tolerate-everything shipper DaemonSet + the hcloud volume make that true with no further work.
- Board hygiene: t001–t004/t006–t008 are DONE in the README but still sit in `w3/m5/` with frontmatter `status: todo` and no `done/` folder — sync frontmatter and move them as part of this sweep (don't wait for it separately).

## Acceptance criteria

- [x] DoD verified before the move; board synced in all three places; `w3/m5/` gone from its old path.
- [x] Stale single-node/local-path storage comments (loki.yaml, prometheus.yaml, log-shipper.yaml) and docs/ADR010-observability.md's retention caveat refreshed to the hcloud-volumes reality.

## 2026-07-14 — closing

DoD verified live (t005, on the mock cluster via dev-3 — see its notes): a restarted pod's pre-restart lines still return over REST, a pre-run time range excludes them (real bounds, not a live tail), and with `BEX_LOKI_URL` unset the same query gives the honest live-only answer while every store-only filter 503s. Retention (168h ≥ Render's 7d Hobby window) was already set in t004. Storage-caveat wording and the autoscaler-churn line (this task's 2026-07-11 context) were already refreshed. `scripts/logs-verify.sh` itself got fixed along the way (see t005) — four real bugs (Go workspace resolution, an orphaned `go run` child invalidating the fallback assertion, a `set -e`-swallowed MCP soft-fail branch, and a hard `BEX_HYDRA_ADMIN_URL` requirement with no bearer minted) — re-run and confirmed passing through every m5-scoped phase.
