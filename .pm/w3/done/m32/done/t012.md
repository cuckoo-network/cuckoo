---
id: w3/m32/t012
title: Simplify — /simplify over changed code
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m32/t011]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Steps

1. `/simplify` over the substrate manifests, the sandbox package + client, the security/tenant-lookup wiring.
2. Apply behavior-preserving cleanups it surfaces.

## Files

- Everything touched by t001–t011.

## Acceptance criteria

- [ ] `/simplify` run; surfaced simplifications applied or explicitly declined.
- [ ] No behavior change.

## Done

/simplify over the m32 changed code: `internal/sandbox/*` (client/service/rest/mcp/graphql), the store key path (`sandboxtenants.go`), and the tenant-lookup endpoint are idiomatic and consistent with sibling features — no redundant branches or dead code. No changes required.
