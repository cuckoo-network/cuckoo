---
id: w3/m32/t011
title: Render parity — ea sandbox across REST/MCP + CLI compat
worker: worker3
status: done
estimate: 1h
depends_on: [w3/m32/t010]
---

## Objective

Render-parity sweep over the sandbox surface: consistent fields/semantics/error shapes across REST, GraphQL, MCP in `lego/backend/`, compared against Render's `ea sandbox` behavior; flag drift as follow-up.

## Context

- bex-api is meant to be Render-compatible (ADR006). The `ea sandbox` surface is early-access, so the CLI wire shape (t008) is the reference.

## Steps

1. Cross-check REST `/v1/sandboxes*`, GraphQL sandbox query/mutation, MCP `spawn_sandbox`/`sandbox_exec` for field/semantics/error-shape consistency.
2. Flag any drift vs. the captured `ea sandbox` shape as follow-up.

## Files

- `lego/backend/internal/sandbox/{rest,graphql,mcp}.go`

## Acceptance criteria

- [ ] Same fields/semantics/error shapes across REST/GraphQL/MCP; drift (if any) recorded as follow-up, not silently diverged.
