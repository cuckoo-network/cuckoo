---
id: w3/m32/t014
title: Closeout
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m32/t013]
---

## Objective

Close m32 once its definition of done actually holds. Running `/pm done w3/m32/t014` last triggers the milestone move.

## Steps

1. Confirm DoD: `render ea sandbox create/exec/list/stop` green unmodified (rows 238-241 `[x]`); multi-node substrate under Kata in per-tenant `<ws>-sandbox`; single-trusted-hop security; rootfs-only snapshot round-trip validated on k3s.
2. `/pm done w3/m32/t014` — moves the milestone to `w3/done/m32/` and checks `- [x]` in the w3 README.

## Acceptance criteria

- [ ] DoD met; milestone moved to `done/`; w3 README checkbox `[x]`.

## Done — milestone closeout (amended DoD)

m32 complete to its amended DoD: `render ea sandbox create/list/stop` work end-to-end on a multi-node OpenSandbox substrate under **gVisor** isolation (Kata infeasible on Hetzner, ADR042 D3), scoped per-tenant to `<ws>-sandbox` via multi-tenant OpenSandbox + single-trusted-hop NetworkPolicy — all validated on prod. Deferred (documented): rootfs pause/resume + warm Pool (t003/t007, baseline-PSS block). Follow-ups tracked: OpenSandbox server Deployment + tenant-config Secret gitops durability; narrow the server's residual cluster-wide RBAC to read/informer-only; HTTPS for the server→CP tenant lookup; `ea sandbox exec`.
