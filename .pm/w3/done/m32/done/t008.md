---
id: w3/m32/t008
title: Capture Render CLI `ea sandbox` wire shape
worker: worker3
status: done
estimate: 1h
depends_on: []
---

## Objective

Capture the exact HTTP shape of `render ea sandbox create/exec/list/stop` → `/v1/sandboxes*` from the official `render-oss/cli`, so the bex-api REST adapter matches it (ADR042 D2; cli-compatibility-checklist:238-241).

## Context

- `render ea sandbox` is early-access; not in Render's public docs.
- Captures live under `docs/render-artifacts/` (see the existing CLI capture artifacts).

## Steps

1. Read `render-oss/cli` v2.21.0 `ea sandbox` command sources (or drive the unmodified CLI against a probe) to capture method/path/body/response shapes.
2. Record under `docs/render-artifacts/ea-sandbox.md`.

## Files

- `docs/render-artifacts/ea-sandbox.md`

## Acceptance criteria

- [ ] Each of create/exec/list/stop has its HTTP method, path, request body, and response shape documented.

## Out of scope

- Implementing the bex-api adapter (t009).
