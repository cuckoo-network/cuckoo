---
id: w3/m6/t008
title: Closeout — DoD met → move milestone to done/
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m6/t007]
---

## Objective

Standing closing task, run last and only when the DoD actually holds (both synthetic breaks fired and resolved on the real channel; rules declarative in gitops; no secret in git). Close the milestone: remaining tasks `status: done`, files to `w3/m6/done/`, rows `— **DONE**` + `**Status:** done`, move `w3/m6/` whole to `w3/done/m6/`, check the box in `w3/README.md`.

## Acceptance criteria

- [ ] DoD verified before the move; board synced in all three places; `w3/m6/` gone from its old path.
