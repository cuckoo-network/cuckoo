---
id: w3/m80/t009
title: Closeout
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m80/t008]
---

## Objective

Standing closing task — run last, and only when the milestone's definition of done actually holds (verified live: revoke a real session, watch the mounted page redirect with `next=`, sign back in, land on the same page with drafts restored; confirm Kratos sliding config is live). Then close the milestone per board convention.

## Context

- DoD: `.pm/w3/m80/README.md` § Definition of done.
- Convention: `.pm/CLAUDE.md` — done items move to `done/`, status synced in all three places.
- **Implementation status (2026-08-26):** t001–t008 complete. Dashboard typecheck + lint clean; 2720 dashboard tests green (6 new/extended test files pin the 401 classifier, redirect + returnTo/aal, auth-vs-network taxonomy, `expires_at` memo eviction, and the reauth-draft round-trip); `scripts/gitops-validate.sh` green with the Kratos helm chart rendering the sliding-session config across base/local/prod overlays. t006 parity: **zero** backend/GraphQL/MCP/schema diff, so the bex-api 401 contract (`{"error":"unauthorized"}` + `WWW-Authenticate: Bearer`) is byte-identical; the redirect-with-`next` behavior matches Render's own cookie-session dashboard, so no drift was filed.
- **Remaining before closeout — live E2E only** (needs a real Kratos, not the `local-bex` stub whose whoami never expires): (1) on `dev-N` or prod, sign in, then revoke the session server-side (Kratos admin or the Settings devices panel) and confirm a mounted service page's next Apollo request redirects to `/auth/login?next=<page>` — never the "The request to bex-api failed" card — and returns to the page after sign-in; (2) type unsaved env-var edits, expire the session, sign back in, confirm the "Restored unsaved changes" banner + edits survive; (3) confirm the Kratos `lifespan`/`earliest_possible_extend` config is live (whoami `expires_at` slides forward on activity) after the gitops sync.

## Live verification (prod `dashboard.bex.co`, 2026-09-07) — DoD PASS

Ran the full destructive round-trip on production against a signed-in QA session (Playwright; the QA password never entered the browser — sign-in was performed by minting a fresh Kratos session through `scripts/qa-login.sh`, which reads the credential in its own process). Service left clean: only ever an unsaved draft, discarded at the end; nothing written server-side.

1. **Sliding lifespan live (DoD 2).** `GET auth.bex.co/sessions/whoami` → 200, `active:true`, issued→expiry window = **168h** (the explicit 7-day lifespan, not the old fixed 24h default).
2. **Unsaved draft mirrored (DoD 3 setup).** On `eden-dash-v3` (`srv-d9ndt8hmcglc739fkp50`) `/env`, added an unsaved var `QA_M80_PROBE=unsaved-draft-should-survive-reauth`; confirmed it persisted to `sessionStorage["bex:reauth-draft:env:srv-d9ndt8hmcglc739fkp50"]` (`valueChanged:true`), never saved.
3. **Server-side revoke (real expiry state).** Kratos self-service logout (flow 200 → logout 204) revoked the session, then the captured `ory_kratos_session` cookie was restored → whoami **401 with the cookie present** = the exact "live cookie, dead session" a mid-session expiry produces.
4. **401 → login redirect, not an outage card (DoD 1).** The still-mounted `/env` page's next request produced (console-captured) `api.bex.co/graphql` **401** → `auth.bex.co/sessions/whoami` **401** (the `handleUnauthenticated` re-check confirming the session is gone) → hard-navigate to **`/auth/login?next=%2Fservices%2Fsrv-d9ndt8hmcglc739fkp50%2Fenv`**. The "Couldn't load service / The request to bex-api failed" card never rendered. Evidence: `.playwright-mcp/qa-m80-2-login-redirect-with-next.png`.
5. **One sign-in returns to the exact page (DoD, returnTo).** Re-authenticated (fresh Kratos session), reloaded `/auth/login?next=…env`; the login route detected the live session and bounced to `/services/srv-d9ndt8hmcglc739fkp50/env`.
6. **Unsaved edits survive the round-trip (DoD 3).** The env page showed the **"Restored unsaved changes"** banner ("We brought back the environment edits you were making before signing in again…") with the exact draft `QA_M80_PROBE=unsaved-draft-should-survive-reauth` intact. Evidence: `.playwright-mcp/qa-m80-3-restored-unsaved-changes.png`. Discarded it → `sessionStorage` cleared, editor back to "No environment variables".

Evidence screenshots are gitignored (`.playwright-mcp/qa-m80-{1,2,3}-*.png`); the durable proof is the reproduction above (network statuses + the `next=` target + banner copy), re-runnable by any session with QA credentials.

## Steps

1. Verify the DoD end-to-end on a real environment (dev-3 or prod).
2. Set every remaining task `status: done`, move each `tNNN.md` to `w3/m80/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w3/m80/README.md`.
4. Move `w3/m80/` → `w3/done/m80/` and check `- [x]` **m80** in `w3/README.md`.
5. `npx prettier@3.4.2 --write` the touched markdown.

## Files

- `.pm/w3/m80/` → `.pm/w3/done/m80/`
- `.pm/w3/README.md`

## Acceptance criteria

- [x] DoD verified live, not just unit-tested (prod `dashboard.bex.co`, 2026-09-07 — see the live-verification section above).
- [x] Milestone directory lives at `w3/done/m80/` with no stub left behind; all three status locations agree.

## Out of scope

- Any new findings — file them as inbox notes instead of expanding this milestone.
