---
id: w3/m80/t006
title: "Render parity: compare render.com session-expiry behavior"
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m80/t003, w3/m80/t005]
---

## Objective

Standing closing task. This milestone touches the dashboard user surface, so check the shipped behavior against the equivalent render.com experience and flag drift as follow-up notes rather than silently diverging. Also confirm the API contract is untouched: bex-api's 401 shape (`{"error":"unauthorized"}` + `WWW-Authenticate`) must be byte-identical before/after across REST, GraphQL, and MCP — this milestone is client + Kratos config only.

## Context

- Compare on a real render.com account: what happens to an open dashboard page when the session expires (redirect? modal? error card?), whether login returns you to the original page, and roughly how long a Render dashboard session lives / whether activity extends it.
- bex-api auth gate (must be unchanged): `lego/backend/internal/api/auth.go:363-366`, `lego/backend/internal/core/http.go:318-320`.
- Parity ledger to update if a divergence is worth recording: `docs/ADR018-render-parity.md`.

## Steps

1. Live-observe Render's expired-session dashboard behavior (or their documented session policy) and note it with evidence.
2. Diff against bex's shipped m80 behavior; file any meaningful drift as w3 inbox notes.
3. Grep/`go test` confirm no REST/GraphQL/MCP response-shape change landed in this milestone.

## Files

- `docs/ADR018-render-parity.md` (only if a divergence is recorded)
- `.pm/w3/` (follow-up notes, if any)

## Acceptance criteria

- [ ] Render's expiry behavior observed and recorded with evidence.
- [ ] Any drift filed as inbox notes, not silently accepted.
- [ ] bex-api 401 contract confirmed unchanged across REST/GraphQL/MCP.

## Out of scope

- Fixing any discovered drift in this milestone.
