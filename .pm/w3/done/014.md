# Retired: agent-session PR diff stats (+A −D)

Why: This was filed to supply the inline PR card with `+A −D`, but that card no longer exists and no current product surface consumes the proposed fields.

> **Retired 2026-08-31 without implementation.** `w5/m65` deliberately removed the inline draft-PR card; the session header's `#N` link is now the sole PR affordance. Persisting and exposing `prAdditions`/`prDeletions` across REST, GraphQL, MCP, and the dashboard would add storage and compatibility surface with no observable consumer. Re-open only if a concrete PR-diff consumer returns.

Original source: Devin research on 2026-08-05 and the former `w3/m44` inline PR card.
