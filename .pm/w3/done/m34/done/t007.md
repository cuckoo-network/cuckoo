---
id: w3/m34/t007
title: Closeout
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m34/t006]
---

## Objective

Close the milestone once its definition of done actually holds — the transitional flags are gone from code and docs, per-tenant namespaces are the unconditional default, caps are enforced solely by `ResourceQuota` with a Render-shaped rejection, and the backend suite + lint are green on a fully-migrated fleet.

## Context

- Do NOT run this until the Gating conditions in the milestone README are met AND every other task is genuinely done (not merely coded): the shared-namespace mode must be officially dropped and ADR043 accepted.

## Steps

1. Verify the DoD: `git grep` clean for the five env vars across `lego/`, `.env.example`, `CLAUDE.md`; ADR043 `Status: accepted`; tests + lint green.
2. Set every task's `status: done`, move each `tNNN.md` to `w3/m34/done/`, mark rows `— **DONE**` and `**Status:** done` in this README.
3. Move `w3/m34/` to `w3/done/m34/` and check `- [x]` in the w3 workstream README.

## Files

- `.pm/w3/m34/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] Milestone DoD verified true (not just code-complete).
- [ ] Milestone moved to `w3/done/m34/`; workstream checkbox checked.

## Out of scope

- Any further code work (belongs in a new milestone/task).
