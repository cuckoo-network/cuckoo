---
id: w3/m34/t003
title: "Docs: ADR043 -> accepted, retire ADR022 shared-ns language, CLAUDE.md/.env.example"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m34/t002]
---

## Objective

Land the documentation half of the retirement: flip ADR043 to accepted, remove ADR022's now-dead shared-namespace tenant-boundary language, and drop the removed env vars from the CLAUDE.md env table and `.env.example`.

## Context

- `docs/ADR043-tenant-namespace-isolation.md` is `Status: proposed` — flip to `accepted` (record the migration-complete evidence: fleet 100% migrated 2026-07-28, gate retired in t002).
- `docs/ADR022-tenant-isolation.md` — its "Option B" (shared namespace + label-scoped per-App NetworkPolicy as the tenant boundary) is already marked superseded by ADR043; ensure no lingering language presents shared-namespace as a live mode.
- `CLAUDE.md` env-var table — remove the `BEX_MAX_SERVICES`/`BEX_MAX_POSTGRES`/`BEX_MAX_KEYVALUES`, `BEX_TENANT_NAMESPACES`, `BEX_TENANT_SANDBOX_NAMESPACES` rows.
- `.env.example` — remove the same names + comments (repo rule: keep it in sync with `.env`).

## Steps

1. Edit ADR043 status + add a short "Accepted / migration complete" note.
2. Scrub ADR022 for any "shared apps namespace is the boundary" phrasing that now reads as current.
3. Remove the five env rows from the CLAUDE.md table and the entries from `.env.example`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR043-tenant-namespace-isolation.md`, `docs/ADR022-tenant-isolation.md`
- `CLAUDE.md`, `.env.example`

## Acceptance criteria

- [ ] ADR043 reads `Status: accepted`.
- [ ] The five removed env vars appear nowhere in `CLAUDE.md` / `.env.example`.
- [ ] Markdown passes prettier.

## Out of scope

- Code removal (t001/t002).
