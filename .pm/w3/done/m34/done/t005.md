---
id: w3/m34/t005
title: Simplify pass over the deleted-branch code
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m34/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — the `core.Base` namespace helpers and every call site that dropped a `TenantNamespaces` branch — to collapse now-trivial conditionals, unused parameters, and dead fallbacks that removing the gate exposed. Behavior-preserving only.

## Context

- After t002, `AppNamespace(tenantID)` is effectively `return tenantID`-with-default-guard and `AppListScope()` is always cluster-wide; several call sites that branched on the flag are now straight-line.
- Watch for helpers that only existed to bridge the two modes (e.g. a wrapper that is now a one-liner) and inline them where it improves readability.

## Steps

1. `/simplify` scoped to the m34 diff.
2. Apply reuse/simplification/efficiency suggestions; re-run build + tests.

## Files

- `lego/backend/internal/core/base.go` and the touched feature packages.

## Acceptance criteria

- [ ] No behavior change; backend + lint green.
- [ ] No dead flag-era scaffolding left (helpers/params that only served the removed branch).

## Out of scope

- Bug hunting (that's `/code-review`); new behavior.
