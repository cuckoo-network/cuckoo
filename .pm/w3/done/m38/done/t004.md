---
id: w3/m38/t004
title: "Snapshot hygiene: scrub credentials pre-snapshot; re-fetch on resume"
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m38/t003]
---

## Objective

Guarantee no credential material survives into a rootfs snapshot (ADR047 D2 snapshot hygiene): before hibernate/snapshot, scrub any transient credential state (helper caches, env residue, the OpenSandbox tenant key) — composing with w3/m37 t004's model-key scrub — and verify a resumed sandbox transparently re-fetches through the helper.

## Context

- The helper design (t003) already keeps tokens off disk; this task is the belt-and-suspenders sweep + the resume-path verification, and covers the OpenSandbox tenant key the ADR names explicitly.
- Snapshot path today: rootfs-only (ADR042); the commit Job fix is w3/m42 — until it lands, verify against a locally-exported rootfs or the dev substrate.

## Steps

1. Enumerate credential-bearing locations (env files, tmpdirs, agent config, helper state) and add a pre-snapshot scrub hook.
2. Post-scrub assertion (scan) wired into the snapshot path.
3. Resume test: restored sandbox pushes again via a fresh helper fetch.

## Files

- `lego/agent-image/` (scrub hook), sandbox lifecycle touchpoints in `lego/backend/internal/sandbox/`

## Acceptance criteria

- [x] Exported rootfs contains no token/key material (automated scan)
- [x] Resumed dev-substrate rootfs performs a successful authenticated push with no manual step

## Verification

`lego/agent-image/credential-e2e-test.sh` runs the pre-snapshot hook, commits the stopped agent container rootfs, advances the credential fixture beyond the one-hour token TTL, resumes from the committed rootfs, and pushes/fetches again through a newly minted credential. It passed on 2026-08-01; the production OpenSandbox/PSS cycle remains the milestone closeout proof owned by w3/m42 and w3/m41.

## Out of scope

- The snapshot-commit-Job PSS fix itself (w3/m42)
