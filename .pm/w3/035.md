# 035 — ADR008 vision status refresh: pillars 4/5 and roadmap items 1–4 read "planned" but shipped

Why: `docs/ADR008-vision.md` is the first thing a new contributor or agent reads, and its pillar table and roadmap list currently contradict the shipped system.

Verified against the code and board 2026-09-08 (`/pm-brainstorm for w3` #4, `/goal` check the same day):

- **Pillar 4 "Deploy-from-chat" reads 🔜 planned** — [ADR017](../../docs/ADR017-deploy-from-chat.md) is `accepted — implemented 2026-07-08` (MCP `deploy` + `create_web_service` over `Core.Create`, HMAC push-to-deploy in `lego/backend/internal/apps/webhook.go`), and the in-cluster build prerequisite it named (`w1/m5`) shipped.
- **Pillar 5 "E2B-compatible sandboxes" reads 🔜 planned** — the substrate, CLI `ea sandbox` create/list/stop/exec, hibernate/resume, and the whole ADR047 agent-session stack shipped in `w3/m32`–`m45` and later; ADR014/ADR042/ADR047/ADR059 are Accepted.
- **Roadmap 1 "Postgres control plane — not yet the prod default"** — production runs it unconditionally (ADR043's `NamespaceReconciler` requires `BEX_CP_DB_URI`; `deploy/gitops/base/bex-api-namespace-rbac.yaml` documents it as wired; `w7/m77` cut every datastore over on it).
- **Roadmap 2–4** (wake activator + HMAC webhook; Cluster Autoscaler; in-cluster builds) all shipped: `lego/operator/cmd/activator`, `deploy/gitops/base/autoscaler.yaml` (w1/m19), BuildKit workers (w1/m5, ADR034/ADR060).

Fix (sub-hour): flip the two pillar statuses to ✅ with ADR pointers; rewrite the roadmap block as "shipped" bullets plus what is actually next (e.g. the open w3 milestones m82–m84, mobile w11, pillar-5 follow-ons), keeping the Non-goals section. Cross-check `docs/CLAUDE.md`'s one-liner for ADR008 still fits. Run `npx prettier@3.4.2 --write "**/*.md"`.

← from `/pm-brainstorm for w3` 2026-09-08 #4; too small for a milestone (sizing rule).
