---
id: w3/m77/t005
title: Test coverage — unit + real-PG/E2E headless-then-open
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m77/t004]
---

## Objective

Prove the DoD with tests that assert real behavior: a fire-and-forget session with **no attach** has a non-empty transcript after completion, the record path is idempotent with the live tee, and a record failure never strands the session.

## Context

- Unit homes: `internal/sshgateway/agentattach/*_test.go` (record endpoint: HMAC verify, seq-seed from `AgentSessionTranscriptMaxSeq`, terminal-driver replay drain), `internal/agentsessions/completion_test.go` (record-before-teardown ordering; error → still finalizes/tears down).
- Real-Postgres integration is CI-enforced (`.github/workflows/backend-test.yml`, `BEX_TEST_DB_URI`) — assert rows land in `agent_session_transcripts` and `AgentSessionTranscript` replays them.
- Live E2E: extend `scripts/agent-session-verify.sh` with a **headless-then-open** leg — create a session, let it complete with no browser attach, then `GET /stream` and assert a non-empty replay (shares the m41 operator-run gate).

## Steps

1. Unit tests for the record endpoint (idempotency, HMAC, seq seed, replay of a closed hub).
2. Completer test: `RecordTranscript` called before `teardown`; on error the session still finalizes (no `running` strand).
3. Real-PG test: after a simulated headless completion the transcript is non-empty and replays in order; a second record is a no-op.
4. Add the `scripts/agent-session-verify.sh` headless-then-open assertion.

## Files

- `lego/backend/internal/sshgateway/agentattach/*_test.go`
- `lego/backend/internal/agentsessions/completion_test.go`
- `lego/backend/internal/store/agentsessions_test.go` (if a store assertion helps)
- `scripts/agent-session-verify.sh`

## Acceptance criteria

- [ ] A no-attach fire-and-forget session has a non-empty `agent_session_transcripts` after completion (real-PG test).
- [ ] Second record is a no-op (no duplicate seqs).
- [ ] Record failure path: session still finalizes + tears down.
- [ ] `scripts/agent-session-verify.sh` headless-then-open leg asserts non-empty replay.
- [ ] `cd lego/backend && go test ./...` green.

## Out of scope

- Perf/load testing; the phase-2 live-attach path (already covered by m43).
