---
id: w3/m10/t005
title: "Dashboard: add the four series to the metrics page charts (Render-consistent)"
worker: worker3
status: todo
estimate: 1h
depends_on: [w3/m10/t001, w3/m10/t002, w3/m10/t003, w3/m10/t004]
---

## Objective

Surface the autoscale-target, disk, active-connections, and replication-lag series on the dashboard's metrics page, matching Render's chart treatment.

## Context

- `dashboard/` — see `dashboard/CLAUDE.md`; metrics page shipped in `w3/m3`/`m4`/`m4.5`.

## Steps

1. Add a target line overlay to the existing CPU/mem charts (t001).
2. Add a disk-usage chart to the Postgres/KV detail metrics view (t002).
3. Add an active-connections chart to the Postgres metrics view (t003).
4. Add a replication-lag chart slot that renders "N/A" or is hidden until `w1/m22` ships real data (t004).

## Files

- `dashboard/` metrics page + its GraphQL queries

## Acceptance criteria

- [ ] All three live series (target, disk, connections) render with real data.
- [ ] Replication-lag renders a clear empty/N/A state, not a broken chart.

## Out of scope

- New chart library/visualization primitives — reuse the existing metrics-page chart components.
