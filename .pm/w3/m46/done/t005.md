---
id: w3/m46/t005
title: Render parity sweep across REST/GraphQL/MCP/UI
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m46/t001, w3/m46/t002, w3/m46/t003, w3/m46/t004]
---

## Objective

Verify every change in this milestone is consistent across the surfaces it exposes — REST, GraphQL, and MCP in `lego/backend/` (same fields, semantics, error shapes; bex-api is Render-compatible per `docs/ADR006-bex-api.md`) and the `dashboard/` UI — and compare against equivalent render.com behavior. Flag any residual drift as follow-up rather than diverging silently.

## Context

- Surfaces touched: MCP `trigger_deploy` (`clearCache`), dashboard Manual Deploy menu + suspend copy + resource list, static-site suspend serving, static-server SPA-fallback docs.
- Reference: `docs/ADR018-render-parity.md`, `docs/ADR029-static-sites.md`, `docs/cli-compatibility-checklist.md`.

## Steps

1. Confirm `clearCache` field name + semantics match across REST/GraphQL/MCP and the dashboard.
2. Confirm the delete-from-list and suspend behaviors are consistent between the API-driven list and the dashboard.
3. Compare each against render.com; record any leftover divergence in the ADR018 ledger as tracked follow-up.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR029-static-sites.md`

## Acceptance criteria

- [ ] Each shipped change is consistent across REST/GraphQL/MCP/UI, or the gap is filed as follow-up.
- [ ] The ADR018 ledger reflects the current state for the affected rows.

## Out of scope

- Unrelated parity rows.
