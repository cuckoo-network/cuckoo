# 042 — Locate recoverable forum uploads and establish durable storage

Why: the retirement preflight found database upload references whose files are already absent from the current forum container; database backups cannot reconstruct missing file bytes.

## Verified 2026-09-06

The three sites share the healthy gen-112 forum Deployment. Its Pod has no volumes. Both `/var/www/discourse/public/uploads` and `/var/discourse/shared/uploads` contain zero files. Database records include 29 custom `/forum/uploads/...` URLs (11 beancount, 8 tianpan, 10 blockeden), apart from built-in image assets. Requests directly to the running application with each site's Host header returned 25 HTTP 500 and 4 HTTP 404 responses. Public requests returned HTTP 403, so they did not supply recoverable bytes. All three `/forum/site.json` requests returned 200.

The old gen-54 image was inspected across all 34 layers: no files under either upload directory. Keeping or rolling back to that image therefore does not recover these uploads. No application rollout or old-image deletion had happened when this baseline was captured.

## Next action

Locate an earlier Discourse upload archive, object-store copy, or original upload files and match recovered bytes against database SHA-1 values. Confirm historical storage routing before concluding the originals are irrecoverable. If restoration is possible, plan the durable-volume or object-store configuration and a controlled restore as a separate migration; do not deploy a fresh empty upload directory as a purported fix. This note covers the initial recovery-source investigation; promote a larger implementation only once a recovery source and target are concrete.

Private per-file evidence is in the user's restricted backup directory, `/Users/tianpan/.local/share/bex/backups/forum-retirement-20260906/`; do not commit file contents or tenant data. This is separate from 038's obsolete-credential image retirement.
