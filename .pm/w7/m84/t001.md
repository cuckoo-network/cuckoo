---
id: w7/m84/t001
title: "Reproduce: a two-reconcile envtest that asserts owned objects do not change"
worker: worker7
status: todo
estimate: 40m
depends_on: []
---

## Objective

Turn the reasoning in `028` into a failing test before changing any projection code, so the fix is proven rather than argued — and so the sweep in t003 has an oracle instead of a code review.

## Context

- The claim: the API server defaults embedded pod-template fields on the stored Deployment (container `terminationMessagePath`/`terminationMessagePolicy`, pod `dnsPolicy`/`restartPolicy`/`schedulerName`/`terminationGracePeriodSeconds`, probe `successThreshold`, readiness `periodSeconds`/`failureThreshold`), while `applyDeploymentSpec` rebuilds `Containers` wholesale and forces `TerminationGracePeriodSeconds` — so `DeepEqual` can never hold and `CreateOrUpdate` returns `Updated` on **every** reconcile.
- `lego/operator/internal/controller/deployment_projection.go:269` is `applyDeploymentSpec`; `:296` rebuilds `Containers`; `:300` sets `TerminationGracePeriodSeconds`.
- `lego/operator/internal/controller/app_controller.go:3398` — `Owns(&appsv1.Deployment{}, ResourceVersionChangedPredicate{})`, which is what closes the loop.
- `registry_pull_secret_envtest_test.go:193-198` is the in-repo shape for asserting a second reconcile is a no-op — mirror it.
- Do not fix anything in this task. A red test is the deliverable.

## Steps

1. Write an envtest that reconciles an unchanged App twice and asserts every owned object's `resourceVersion` is unchanged after the second pass.
2. Extend it to Database and KeyValue owners so the sweep's scope is measured rather than assumed.
3. Record which objects actually churn — the list may be longer or shorter than `028` predicted; report what is true, not what was expected.

## Files

- `lego/operator/internal/controller/` envtests

## Acceptance criteria

- [ ] The test is **red** against the current tree for the Deployment path.
- [ ] The churning-object list is measured across all three controllers and written down.
- [ ] No production code changed in this task.

## Out of scope

- Fixing anything — t002 and t003.
