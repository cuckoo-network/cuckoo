---
id: w7/m68/t004
title: Backup staleness alert for KeyValue backup CronJobs
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m68/t002]
---

## Objective

A paid instance whose backups silently stop pages within a day.

## Context

- Pattern: `BackupCronJobStale` at `deploy/gitops/base/prometheus.yaml:707` (fires when a named backup CronJob has no success >26h, via kube-state-metrics job metrics). The KV variant must match by name pattern (`kvbak-*`) rather than a fixed list, since instances come and go.

## Steps

1. Add the label/regex-driven staleness rule.
2. Verify the expression against a live paid instance's CronJob metrics.

## Files

- `deploy/gitops/base/prometheus.yaml`

## Acceptance criteria

- [ ] Rule fires for a stale `kvbak-*` CronJob and stays quiet for healthy ones; Free instances (no CronJob) produce no alert.

## Out of scope

- Per-tenant notification (platform-operator alert only).
