---
id: w7/m68/t001
title: Env contract `BEX_KV_BACKUP_*` + docs/`.env.example` sync
worker: worker7
status: todo
estimate: 30m
depends_on: []
---

## Objective

Define the operator's KeyValue-backup configuration surface, mirroring the managed-Postgres backup contract: any variable unset ⇒ the feature is off and the operator is byte-identical to today.

## Context

- Mirror: `BEX_DB_BACKUP_DESTINATION` / `BEX_DB_BACKUP_ENDPOINT` / `BEX_DB_BACKUP_S3_SECRET` (CLAUDE.md env table; used by the logical-export/purge Jobs). New: `BEX_KV_BACKUP_DESTINATION` (e.g. `s3://bex-tfstate/keyvalue`), `BEX_KV_BACKUP_ENDPOINT`, `BEX_KV_BACKUP_S3_SECRET` (Secret with `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` in the apps namespace, out-of-band).
- Repo rules: root `CLAUDE.md` env table row + `.env.example` name/comment mirror (never values); `scripts/gh-secrets.sh` picks it up from `.env` automatically.

## Steps

1. Add config parsing/validation in the operator (fail closed: partial config ⇒ feature disabled + logged, matching the `StaticCredentialUnavailable`-style posture where applicable).
2. Update root `CLAUDE.md` env table + `.env.example`.

## Files

- `lego/operator/` config loading (wherever `BEX_DB_BACKUP_*` is read)
- `CLAUDE.md`
- `.env.example`

## Acceptance criteria

- [ ] All three vars documented; unset ⇒ verified byte-identical reconcile output (no CronJob objects).
- [ ] `.env.example` mirrors `.env` names.

## Out of scope

- The CronJob itself (t002).
