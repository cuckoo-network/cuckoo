---
id: w7/m51/t004
title: Complete payment setup idempotently and bind the default payment method
worker: worker7
status: todo
estimate: 1h
depends_on: [w7/m51/t003]
---

## Objective

Consume trusted Checkout/SetupIntent completion and idempotently make the resulting test payment method the Customer and bex Subscription default.

## Context

- `internal/billing/webhook.go` currently accepts only `invoice.payment_failed`.
- Stripe retries and reorders webhooks, so completion cannot rely on a browser redirect or at-most-once delivery.

## Steps

1. Extend the version-pinned endpoint event inventory only for the completion events the flow needs.
2. Verify metadata ownership, retrieve authoritative Stripe objects, and bind the payment method.
3. Make duplicate delivery and redirect-before-webhook races converge to the same ready state.

## Files

- `lego/backend/internal/billing/webhook.go`
- `lego/backend/internal/billing/sessions.go`
- `scripts/stripe-billing-setup.py`

## Acceptance criteria

- [ ] A completed test Checkout makes the intended Customer and Subscription payment-ready.
- [ ] Duplicate or reordered completion events cause no duplicate object or unsafe reassignment.
- [ ] A forged or cross-workspace metadata binding is rejected.

## Out of scope

- Dunning enforcement, which belongs to m52.
