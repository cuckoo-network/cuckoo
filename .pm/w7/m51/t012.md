---
id: w7/m51/t012
title: Test coverage
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m51/t011]
---

## Objective

Add meaningful automated coverage for payment onboarding, tax gating, Portal access, authorization, retries, and failure modes.

## Context

- Tests must assert real behavior rather than snapshot opaque Stripe payloads.
- The opt-in sandbox test must continue to refuse live keys.

## Steps

1. Add Stripe stub-transport tests for session payloads, idempotency, and tax preconditions.
2. Add REST/GraphQL/MCP parity and cross-workspace denial tests.
3. Add dashboard interaction tests and extend the opt-in test-mode smoke.

## Files

- `lego/backend/internal/billing/*_test.go`
- `lego/backend/internal/api/*_test.go`
- `dashboard/src/**/*.test.tsx`

## Acceptance criteria

- [ ] Happy path, duplicate callbacks, bad metadata, missing registration, degraded Stripe, and authorization failures are covered.
- [ ] No test can run with an `rk_live_*` or `sk_live_*` key.
- [ ] Backend, operator, dashboard, and setup-script test suites pass.

## Out of scope

- Coverage-number gaming or live-mode tests.
