---
id: w7/m61/t005
title: "Test coverage: anti-tautology self-tests — each regression class turns CI red"
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m61/t003]
---

## Objective

The m55 anti-tautology pattern applied here: prove the matrix actually catches its regression classes rather than passing vacuously. Self-tests that simulate (in a test copy, never the real code) each class and assert the sweep goes red: a verb gated on a weaker relation than its class requires, a role grant added to the table but not the model (and vice versa via the t001 pin), a verb missing from the sweep inventory.

## Acceptance criteria

- [ ] Each regression class above has a self-test proving the matrix/pin/completeness guard fails on it.
- [ ] Backend `go test ./...` + lint green.
