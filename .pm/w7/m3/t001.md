---
id: w7/m3/t001
title: "Capture Render's documented rate-limit contract into docs/bex-api.md"
worker: worker7
status: todo
estimate: 30m
depends_on: []
---

## Objective

Record, with evidence pointers, exactly what contract bex must match: Render's documented API rate limits (per-endpoint-class request budgets), the 429 response shape, and its retry headers (`Retry-After`, any `ratelimit-*` headers) — into a new rate-limits section of docs/bex-api.md, so t002 implements against a captured contract instead of guesses.

## Context

- Render's API docs document rate limiting (api-docs.render.com — reference/rate-limiting page; the OpenAPI spec `render-public-api-1.json` marks 429 responses per endpoint).
- The parity ledger method (docs/render-parity.md §Method) is the model: cite the live doc/spec, note divergences deliberately.
- bex serves three adapters off one core (docs/bex-api.md) — the contract section should state how a 429 surfaces on GraphQL (top-level error with extension code) and MCP (tool error result), since Render's docs only cover REST.

## Steps

1. Fetch Render's rate-limiting reference + the 429 entries in its OpenAPI spec; extract limits, headers, and error body shape.
2. Write docs/bex-api.md §Rate limits: the captured contract, bex's chosen budgets (env-default), and the GraphQL/MCP mapping.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/bex-api.md` (new §Rate limits)

## Acceptance criteria

- [ ] Section cites Render's live doc/spec URLs and quotes the 429 + header shape
- [ ] bex's default budgets and the GraphQL/MCP error mapping are stated
- [ ] Divergences (if any) marked deliberately, parity-ledger style

## Out of scope

- Implementation (t002); updating docs/render-parity.md rows (t005 owns ledger sync)
