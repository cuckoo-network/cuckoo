---
id: w7/m34/t003
title: "Middleware tests: presence, monotonic remaining, reset; 429 body recheck"
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m34/t002]
---

## Objective

Extend `ratelimit_test.go`: consecutive requests show decrementing remaining; reset behaves per the captured semantics; at 0 remaining the next request 429s with `Retry-After` AND the headers agreeing; the 429 body carries the RC1 `{id,message}` envelope.

## Acceptance criteria

- [ ] The named assertions pass; suite green.
