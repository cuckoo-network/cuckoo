# 030 — kpack `ClusterBuilder` staleness monitoring + a re-resolve cadence for pinned base digests

Why: a stale ClusterBuilder silently ships old toolchains and CVEs to every buildpack tenant, and a pinned digest never picks up upstream security fixes — pinning without a refresh cadence rots instead of drifting.

ADR060 D7 residual, split out by `builder-issues.md` §3.11 (P10), 2026-08-17. Two halves of one problem — pinning stops silent drift, but pinning without a refresh cadence silently rots instead.

**Half 1 — ClusterBuilder staleness.** D7: the kpack `ClusterBuilder` should get staleness monitoring (Ready condition + builder image age) folded into D5's alerting, because "a stale builder silently ships old toolchains and CVEs to every buildpack tenant". Nothing monitors it today.

**Half 2 — re-resolve cadence.** The five native base images are already digest-pinned (`lego/operator/internal/build/native.go:42-46`), which D7 asked for and which is done. But a pinned digest never picks up upstream security fixes. D7 says the "pinning cadence (periodic re-resolve + commit) comes with the milestone" and it did not. The same applies to whatever `w7/m85` pins.

Do both together — they are the same "is our build toolchain current?" question from two directions. Sub-hour each; promote to a milestone only if the cadence turns out to need automation (a scheduled re-resolve PR) rather than a documented periodic chore.
