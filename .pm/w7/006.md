# Retire or repair the unrecoverable `default/biliblilitest` Postgres — needs user approval

Three closeouts have now stepped around this cluster without resolving it:

- `w7/done/m33/README.md:27` — "Out of scope (separate user approval, per the note): repairing or retiring the pre-existing unrecoverable `default/biliblilitest` cluster."
- `w7/done/004.md` item 4 — "already `Cluster is unrecoverable and needs manual intervention` … has no ready instance or completed backup."
- `w9/done/m3/README.md` — "this milestone did not delete or repair it."

**The question for the user:** retire (delete the CNPG Cluster + purge its artifacts/rows — sub-hour) or attempt repair/recovery (multi-hour, no completed backup exists so recovery prospects are poor)?

Blocked on that decision; once made, retirement is a sub-hour ops task (no milestone), repair would need its own scoping. Recommendation from the evidence: retire.
