---
id: w7/m75/t004
title: Argo CD Applications — pin exact chart versions
worker: worker7
status: todo
estimate: 30m
depends_on: []
---

## Objective

Stop Argo CD from auto-deploying newly published chart versions into privileged namespaces via wildcard `targetRevision` + self-heal (codex-security #20, low).

## Context

- `deploy/gitops/base/log-shipper.yaml:90` (`1.*`), `loki.yaml:33` (`6.*`), `metrics-server.yaml:19` (`3.12.*`), `prometheus.yaml:51` (`25.*`) — minor-pinned wildcards with automated prune + self-heal. Comments call them "minor pins" but they admit every minor/patch within a major.

## Steps

1. Replace each wildcard `targetRevision` with an exact reviewed chart version.
2. Stand up a reviewed bump process (renovate/dependabot opening PRs, not auto-sync) and document it.

## Files

- `deploy/gitops/base/log-shipper.yaml`
- `deploy/gitops/base/loki.yaml`
- `deploy/gitops/base/metrics-server.yaml`
- `deploy/gitops/base/prometheus.yaml`

## Acceptance criteria

- [ ] No wildcard `targetRevision` remains; bumps are PR-reviewed.

## Out of scope

- Disabling self-heal globally (per-app decision).
