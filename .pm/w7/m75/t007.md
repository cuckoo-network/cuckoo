---
id: w7/m75/t007
title: Test coverage
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m75/t006]
---

## Objective

Add the guards/assertions that are testable from CI.

## Steps

1. A workflow self-test (or repo guard script) that fails if a `workflow_dispatch` input is interpolated directly into a secret-bearing `run:` script (catches regressions of t001).
2. Assert (where a test harness exists) that pinned versions/checksums are present in the cited files.

## Files

- A guard script (e.g. under `scripts/`) or an existing CI lint step.

## Acceptance criteria

- [ ] The input-injection pattern is regression-guarded.

## Out of scope

- Live infra provisioning tests.
