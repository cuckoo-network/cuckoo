---
id: w7/m75/t003
title: Argo CD install manifest — pin to a tagged release
worker: worker7
status: todo
estimate: 20m
depends_on: []
---

## Objective

Stop the deploy workflow from applying Argo CD's mutable `stable/manifests/install.yaml` with an administrative kubeconfig and no integrity check (codex-security #19, low).

## Context

- `.github/workflows/deploy.yml:377/394-395` — `kubectl apply --server-side --force-conflicts -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml` with the app-cluster admin kubeconfig.

## Steps

1. Pin to a tagged release (e.g. `v3.0.11/manifests/install.yaml`).
2. Optionally verify the commit SHA / a published checksum before apply; record the pinned version in a comment.

## Files

- `.github/workflows/deploy.yml`

## Acceptance criteria

- [ ] `stable` ref replaced with a pinned tag; version recorded.

## Out of scope

- Migrating off Argo CD.
