---
id: w7/m9/t001
title: "Design: per-workspace resource creation caps keyed to plan"
worker: worker7
status: todo
estimate: 30m
depends_on: []
---

## Objective

Design the cap model: max services, max managed Postgres instances, and max Key Value instances per workspace, keyed to the workspace's plan — mirroring `w1/m8`'s instance-tier catalog pattern (one catalog, limits everywhere).

## Context

- `w1/m8` — "Instance tiers: one catalog, Render-shaped plan API, limits everywhere" (`lego/types/tiers/` — note its own invariant: operator-imported, must never depend on money).
- `w6/m1` / `w6/m12` — workspace plan model + `changeWorkspacePlan` mutation; plan-gated role sets already exist as precedent (`store.RoleAllowedOnPlan`).
- Render's own plan-limit shape (dashboard-driven quota messaging) — check what render.com actually surfaces for reference, if anything public.

## Steps

1. Decide where the cap table lives (new data file alongside `lego/types/tiers/`, or a workspace-plan-keyed map in `lego/backend/internal/workspaces/` or similar).
2. Define default caps per plan tier (Hobby/Pro/etc — match `w6`'s existing plan tiers).
3. Document the design decision (short note, doesn't need a full ADR).

## Files

- wherever the plan-tier catalog / workspace plan model lives today

## Acceptance criteria

- [ ] A documented cap table exists, keyed by plan tier, for services/Postgres/KeyValue counts.

## Out of scope

- Enforcement — t002/t003.
