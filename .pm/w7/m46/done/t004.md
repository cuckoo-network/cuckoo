---
id: w7/m46/t004
title: "Simplify: run /simplify over the psql-verify leg"
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m46/t002]
---

## Objective

Run `/simplify` over the code this milestone changed — the new `psql-verify` leg in `scripts/cli-compat.sh` — for reuse, simplification, and efficiency, behavior-preserving.

## Context

- The `psql-verify` leg (t002) mirrors `pgcli-verify`; the two share disposable-Postgres setup, self-IP resolution, and cleanup traps. `/simplify` should surface any shared helper the two legs can factor out rather than duplicate.

## Steps

1. Run `/simplify` scoped to the `scripts/cli-compat.sh` diff for this milestone.
2. Apply the behavior-preserving cleanups (e.g. factor shared disposable-Postgres/self-IP/cleanup helpers between `pgcli-verify` and `psql-verify`).
3. Re-run `scripts/cli-compat.sh psql-verify` to confirm behavior is unchanged.

## Files

- `scripts/cli-compat.sh`.

## Acceptance criteria

- [ ] `/simplify` findings applied or consciously declined (with reason).
- [ ] `psql-verify` still passes after the cleanup.

## Out of scope

- Behavior changes or new coverage (that is t005).
