---
id: w7/m30/t004
title: Divergence allowlist keyed to ADR018 rows
worker: worker7
status: todo
estimate: 40m
depends_on: [w7/m30/t003]
---

## Objective

A structured allowlist of the documented, deliberate divergences (e.g. events `details` omits `from`/`to`; audit envelope is bex's `{object,cursor}`; create returns bare service not `{service,deployId}`) — each entry keyed to the ADR018 row that justifies it, so the suite passes on known divergences and fails on new ones. An unexplained divergence with no allowlist entry is a red build.

## Files

- `lego/backend/internal/conformance/` (allowlist + its doc pointers)

## Acceptance criteria

- [ ] Every currently-documented shape divergence is either schema-conformant or carries an allowlist entry citing its ADR018 row; removing an entry reddens the suite.
