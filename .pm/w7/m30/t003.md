---
id: w7/m30/t003
title: Per-family conformance cases (services/deploys/pg/kv/env/domains/events)
worker: worker7
status: todo
estimate: 60m
depends_on: [w7/m30/t002]
---

## Objective

Conformance cases for the core endpoint families: services, deploys, postgres, key-value, env vars + env groups, custom domains, service events — each driving the resource to a realistic state and validating list/get response bodies against the pinned schemas.

## Context

- Scope to families with a Render schema counterpart; bex-superset endpoints (usage, api-keys-over-API) have no Render schema — exclude with a note, don't fabricate one.

## Files

- `lego/backend/internal/conformance/`

## Acceptance criteria

- [ ] Each listed family has a passing conformance case (modulo allowlisted divergences from t004).
