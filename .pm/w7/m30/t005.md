---
id: w7/m30/t005
title: Wire into `backend-test.yml` + document the pin-update flow
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m30/t003]
---

## Objective

Run the conformance suite in the existing backend-test CI job (not a new workflow), and document in ADR018 (or a sibling doc) how to bump the pinned spec: re-fetch, diff, re-triage the allowlist, commit.

## Files

- `.github/workflows/backend-test.yml`, `docs/ADR018-render-parity.md` (or a conformance sub-doc)

## Acceptance criteria

- [ ] CI runs the suite on `lego/backend/**` changes; the pin-update procedure is written down.
