---
id: w7/m30/t002
title: Conformance harness over the CI Postgres/OpenFGA setup
worker: worker7
status: todo
estimate: 60m
depends_on: [w7/m30/t001]
---

## Objective

A test harness that boots bex-api (or its router) against the existing CI ephemeral Postgres + OpenFGA, drives a resource to a known state, and asserts each response validates against its Render schema — reusing the backend integration-test bootstrap, not a new cluster.

## Context

- Follow the existing gated integration tests (`BEX_TEST_DB_URI`/`BEX_TEST_OPENFGA_URL`) that already run real in CI (not skipped) per CLAUDE.md.

## Files

- `lego/backend/internal/conformance/`

## Acceptance criteria

- [ ] The harness runs one family end-to-end (create → GET → validate) against real Postgres+OpenFGA in CI.
