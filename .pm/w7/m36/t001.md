---
id: w7/m36/t001
title: Design the per-App credential scheme (htpasswd + Zot ACLs)
worker: worker7
status: todo
estimate: 45m
depends_on: []
---

## Objective

Decide the credential-per-App shape: htpasswd entry naming, Zot accessControl policy mapping each identity to its App's repo path prefix, config-reload mechanics (Zot hot-reload vs rolling restart), and rotation/revocation semantics — recorded before any code.

## Context

- docs/ADR022-tenant-isolation.md § Registry access control (w7/m8's push/pull split); current single pull credential minted by `scripts/registry-secrets.sh`.
- Zot config lives in deploy/gitops (bex-registry).
- Repo path today: images per App under a predictable prefix — verify the actual layout first.

## Steps

1. Verify the image path layout per App in Zot.
2. Choose identity naming + ACL policy shape; decide how htpasswd/ACL updates reach Zot (Secret mount + reload?).
3. Write the design into ADR022 as a proposed amendment.

## Files

- docs/ADR022-tenant-isolation.md

## Acceptance criteria

- [ ] Recorded design covering mint, mount, revoke, and Zot reload, with the repo-layout evidence.

## Out of scope

- Implementation.
