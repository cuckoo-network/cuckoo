---
id: w7/m37/t001
title: Narrowed non-`system:masters` operator kubeconfig
worker: worker7
status: todo
estimate: 60m
depends_on: []
---

## Objective

Issue an RBAC-limited kubeconfig (CSR-signed user or ServiceAccount token per what fits the substrate) for day-to-day human ops, scoped to the verbs actually used, and document it as the default — the `system:masters` admin cert becomes break-glass only.

## Context

- docs/ADR019-infra-credentials.md:80 — "no scoped, non-admin operator credential".
- Inventory the actually-used verbs from the ops runbooks/scripts before writing the ClusterRole (least privilege, w6/m8 style).

## Files

- infra/ or scripts/ (issuance script)
- docs/ADR019-infra-credentials.md

## Acceptance criteria

- [ ] The scoped kubeconfig performs the documented routine ops; a forbidden verb is actually denied; ADR019 updated.

## Out of scope

- CI credentials (separate custody path).
