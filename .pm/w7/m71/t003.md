---
id: w7/m71/t003
title: Simplify
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m71/t001, w7/m71/t002]
---

## Objective

Run `/simplify` over the changed controller code; extract a single ownership-check helper used by both the mirror (t001) and the finalizer delete (t002) so the policy is expressed once.

## Steps

1. `/simplify` over `lego/operator/internal/controller/app_controller.go`.
2. Extract `secretOwnedBy(secret, app) bool` (or similar) shared by both paths.

## Acceptance criteria

- [ ] One ownership predicate, reused by mirror + delete.

## Out of scope

- New behavior beyond unifying the check.
