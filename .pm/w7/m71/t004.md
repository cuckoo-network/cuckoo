---
id: w7/m71/t004
title: Test coverage
worker: worker7
status: todo
estimate: 40m
depends_on: [w7/m71/t003]
---

## Objective

Add cross-tenant negative tests for both the overwrite and delete paths.

## Steps

1. Overwrite-negative: pre-create a `bex-build` Secret with foreign ownership labels; reconcile an App whose clone/registry secret name collides; assert the Secret's data is unchanged.
2. Delete-ownership-negative: pre-create a foreign-owned same-named Secret; delete the App; assert the Secret survives.
3. Positive: an App's own mirrored Secrets are still overwritten/deleted correctly.

## Files

- `lego/operator/internal/controller/*_test.go` (extend the clone-secret / finalizer suites).

## Acceptance criteria

- [ ] Both negative tests pass; positives still pass.
- [ ] Assertions check data bytes / existence, not just absence of error.

## Out of scope

- Live cluster verification (envtest covers it).
