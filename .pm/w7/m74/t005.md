---
id: w7/m74/t005
title: Simplify
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m74/t001, w7/m74/t002, w7/m74/t003, w7/m74/t004]
---

## Objective

Run `/simplify` over the changed dashboard server-fns/pages and the sandbox-exec path; share the nonce-claim helper between browser-shell and sandbox-exec.

## Steps

1. `/simplify` over `dashboard/src/common/server-fn/`, the auth pages, and `lego/backend/internal/sshgateway/`.

## Acceptance criteria

- [ ] Nonce-claim logic has one home shared by both exec paths.

## Out of scope

- New behavior.
