---
id: w7/m59/t005
title: "Test coverage: quota shape per plan, quota-rejected grow surfacing, rollout convergence"
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m59/t003]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: the per-plan quota shape (all dimensions, both plans), the operator's quota-rejected-grow handling (condition set, backoff, recovery), and existing-namespace quota convergence. Tests must assert real behavior and failure modes — no tautological snapshot tests.

## Acceptance criteria

- [ ] A dropped quota dimension, a hot-looping quota rejection, or a create-only quota reconcile each turn a test red.
- [ ] Backend `go test ./...` + operator `make test` green.
