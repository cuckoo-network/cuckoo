---
id: w7/m59/t003
title: Rollout to existing tea-* namespaces + document per-plan values + ADR045 disposition
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m59/t002]
---

## Objective

Make the NamespaceReconciler converge already-provisioned `tea-*` namespaces to the new quota shape without disrupting live workloads, and record the fix: per-plan values documented, ADR045 Finding 4's disposition updated.

## Context

- The NamespaceReconciler (`lego/backend/internal/store/namespaces.go`) reconciles the quota object each pass — verify the update path actually rewrites existing quotas to include the new keys (not create-only).
- Kubernetes semantics: a ResourceQuota below current usage does not evict anything — it only blocks new/updated requests. Confirm and state this in the rollout note so the change is provably non-disruptive.

## Steps

1. Verify (or fix) the reconciler's quota update path so existing namespaces gain the new dimensions.
2. Document the per-plan values + derivation in `docs/ADR043-tenant-namespace-isolation.md` (quota section) or the store package doc, whichever ADR043 already anchors.
3. Update `docs/ADR045-security-review-round3.md` Finding 4's disposition row (fixed-in-place pointer to this milestone).
4. `npx prettier@3.4.2 --write` over touched markdown.

## Files

- `lego/backend/internal/store/namespaces.go`
- `docs/ADR043-tenant-namespace-isolation.md`
- `docs/ADR045-security-review-round3.md`

## Acceptance criteria

- [ ] Reconciling a pre-existing namespace converges its quota to the new shape (test or live-verified).
- [ ] Non-disruption rationale recorded; per-plan values documented; ADR045 Finding 4 row updated.

## Out of scope

- w3/m34's flag retirement (blocked on its own gate); production ship (post-`/ship` acceptance).
