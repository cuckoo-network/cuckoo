---
id: w7/m59/t004
title: Simplify pass over the changed code
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m59/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (quota shape in `store/namespaces.go`, autoscaler collision handling in `database_autoscale.go`/`database_controller.go`) — reuse / simplification / efficiency, behavior-preserving.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff; findings applied or explicitly declined with reasons.
- [ ] Backend + operator suites still green.
