---
id: w7/m74/t006
title: Test coverage
worker: worker7
status: done
estimate: 1h
depends_on: [w7/m74/t005]
---

## Objective

Cover each auth/session boundary with a real test.

## Steps

1. Device grant: a headless link visit does not complete the grant; confirmation + session binding does.
2. Consent: oversized body rejected before buffering; session resolved first.
3. Apollo cache: after logout→login, prior account's cached objects are gone (unit test on the clear hook + transition).
4. Sandbox-exec nonce: a replayed ticket against a second "replica" (second Server instance sharing the store) is rejected.

## Files

- Dashboard Vitest suite + `lego/backend/internal/sshgateway/*_test.go`.

## Acceptance criteria

- [ ] Each boundary has a passing non-tautological test.

## Out of scope

- Cross-browser reproduction (cache semantics are explicit in source).
