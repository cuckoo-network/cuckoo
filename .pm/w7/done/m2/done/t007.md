---
id: w7/m2/t007
title: "Closeout — DoD verified, milestone moved to done/"
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m2/t006]
---

## Objective

Close the milestone once its definition of done actually holds — the t004 checks pass on a live cluster — not merely when the code is written. Completing this task **is** the move.

## Context

- Standing closing task (`.claude/skills/pm/SKILL.md` §Closeout): set remaining tasks `status: done`, move each `tNNN.md` to `w7/m2/done/`, mark rows `— **DONE**` + `**Status:** done` in the milestone README, move `w7/m2/` to `w7/done/m2/`, check `- [x]` in `w7/README.md`.

## Steps

1. Re-run the verification script's m2 section against the target cluster; confirm green.
2. Run `/pm done w7/m2/t007` last — its step 4 performs the milestone move.

## Files

- `.pm/w7/m2/` → `.pm/w7/done/m2/` (the move); `.pm/w7/README.md` (checkbox)

## Acceptance criteria

- [ ] DoD observably true before any move
- [ ] Status synced in all three places (task frontmatter, milestone README, workstream README)
- [ ] No tombstone left at `.pm/w7/m2/`

## Out of scope

- Any new implementation work (goes through `add-task` before this task instead)
