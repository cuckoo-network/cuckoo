---
id: w7/m2/t005
title: "Simplify — /simplify over the code this milestone changed"
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m2/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (operator Deployment builder, build Job spec, namespace/quota manifests, verification additions) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Standing closing task (`.claude/commands/pm.md`).
- Touched surface: `lego/operator/internal/controller/app_controller.go`, `lego/operator/internal/build/build.go`, `deploy/gitops/`, `scripts/verify-tenant-isolation.sh`.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; re-run `make lint` from `lego/operator/` and `scripts/gitops-validate.sh`.

## Files

- Whatever `/simplify` flags within the milestone's diff

## Acceptance criteria

- [ ] `/simplify` findings triaged (applied or explicitly rejected)
- [ ] `make lint` + `scripts/gitops-validate.sh` still pass

## Out of scope

- Bug hunting (that's `/code-review`, not this task)
