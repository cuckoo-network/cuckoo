---
id: w7/m12/t009
title: Render parity: delete semantics across REST/GraphQL/MCP/UI
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m12/t008]
---

## Objective

Check that this milestone's delete-behavior changes stayed consistent across every surface and against Render: same status codes/error shapes on REST (`DELETE /v1/services/{id}`, `/v1/postgres/{id}`, `/v1/key-value/{id}`), same semantics in GraphQL (`deleteService`/`deleteDatabase`/`deleteKeyValue`), MCP (`delete_service` only — Postgres/KV deliberately have no MCP delete, matching Render), and the dashboard delete flows.

## Context

- `docs/ADR018-render-parity.md` — the delete/CRUD rows for services (~line 27), Postgres (~65), Key Value (~77) are all ✅; re-verify them now that delete destroys data, and update the evidence notes.
- Render documents service/database deletion as irreversible data destruction — confirm bex's docs/dashboard confirm-dialog copy says the same (dashboard: `delete-service-card.tsx`, `database-row-actions.tsx`, `key-value-row-actions.tsx`).
- Teardown is async (finalizer) — verify the API still returns 204 immediately and a follow-up GET 404s, same as before; no surface should have grown new blocking behavior.
- Flag any drift as follow-up work (inbox note) rather than silently diverging.

## Steps

1. Exercise all delete verbs on all surfaces (t008's script covers REST; hit GraphQL/MCP/dashboard manually or via existing tests).
2. Compare against Render's documented behavior; update ADR018 evidence cells/notes.
3. File inbox notes for any drift found.

## Files

- `docs/ADR018-render-parity.md`
- dashboard delete components (copy only, if needed)

## Acceptance criteria

- [ ] All three types' delete rows in ADR018 re-verified with updated evidence.
- [ ] No surface drift (fields/status codes/error shapes) introduced by m12.
- [ ] Dashboard confirm copy reflects irreversible data destruction.

## Out of scope

- Adding MCP delete tools for Postgres/KV (deliberate Render match — do not add).
