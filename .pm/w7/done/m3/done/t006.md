---
id: w7/m3/t006
title: "Simplify — /simplify over the code this milestone changed"
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m3/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (limiter middleware, caps in logs/metrics, env plumbing) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Standing closing task (`.claude/commands/pm.md`).
- Touched surface: `lego/backend/internal/api/`, `lego/backend/internal/logs/`, `lego/backend/internal/metrics/`, `lego/backend/cmd/api/main.go`.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; re-run `make lint-backend` from `lego/operator/`.

## Files

- Whatever `/simplify` flags within the milestone's diff

## Acceptance criteria

- [ ] `/simplify` findings triaged (applied or explicitly rejected)
- [ ] `make lint-backend` + `go test ./...` (backend) still pass

## Out of scope

- Bug hunting (that's `/code-review`, not this task)
