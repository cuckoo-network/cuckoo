---
id: w7/m3/t008
title: "Closeout — DoD verified, milestone moved to done/"
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m3/t007]
---

## Objective

Close the milestone once its definition of done actually holds — 429s observable on all three adapters against a running bex-api, caps enforced, knobs documented and mirrored — not merely when the code is written. Completing this task **is** the move.

## Context

- Standing closing task (`.claude/skills/pm/SKILL.md` §Closeout): set remaining tasks `status: done`, move each `tNNN.md` to `w7/m3/done/`, mark rows `— **DONE**` + `**Status:** done` in the milestone README, move `w7/m3/` to `w7/done/m3/`, check `- [x]` in `w7/README.md`.

## Steps

1. Against a running bex-api (mock cluster or prod): trip the limit on each adapter, confirm the documented contract; spot-check one env override.
2. Run `/pm done w7/m3/t008` last — its step 4 performs the milestone move.

## Files

- `.pm/w7/m3/` → `.pm/w7/done/m3/` (the move); `.pm/w7/README.md` (checkbox)

## Acceptance criteria

- [ ] DoD observably true before any move
- [ ] Status synced in all three places (task frontmatter, milestone README, workstream README)
- [ ] No tombstone left at `.pm/w7/m3/`

## Out of scope

- Any new implementation work (goes through `add-task` before this task instead)
