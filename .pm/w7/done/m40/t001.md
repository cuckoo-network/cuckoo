---
id: w7/m40/t001
title: GraphQL `customDomains`: cursor/limit + verificationStatus/domainType args via core.StablePage
worker: worker7
status: todo
estimate: 45m
depends_on: []
---

## Objective

Add `cursor`, `limit`, `verificationStatus`, and `domainType` arguments to the GraphQL `customDomains` field so it filters and pages with the exact semantics the REST list gained in w7/m38.

## Context

- REST reference implementation: `lego/backend/internal/apps/rest.go:1002-1044` — filters applied before `core.StablePage` slices; invalid enum → 400.
- Current GraphQL field: `lego/backend/internal/apps/graphql.go:755-761` — takes only `gqlutil.IDArg()`, returns the full unpaged list.
- Reuse `core.PageLimit` / `core.StablePage` (stable cursor on domain name) — do not reimplement paging.
- Filtering/paging is presentation over the already-fetched slice; keep the logic shared with REST (extract a helper if the two fragments would otherwise duplicate it).

## Steps

1. Extract the REST fragment's filter+page logic into a shared helper in the `apps` package if not already shareable.
2. Add the four args to the GraphQL field; map invalid enum values to the same error the REST 400 carries.
3. Return the paged connection shape consistent with the other paged GraphQL lists (see w8/m13's datastore precedent).

## Files

- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/apps/rest.go` (extraction only, behavior-preserving)

## Acceptance criteria

- [ ] GraphQL `customDomains` accepts all four args and matches REST results for the same inputs.
- [ ] Invalid `verificationStatus`/`domainType` values produce the same error semantics as REST's 400.
- [ ] `go build ./... && go test ./...` green in `lego/backend`.

## Out of scope

- Dashboard filter UI (Render's own dashboard is a flat list — no UI change wanted).
- MCP half (t002).
