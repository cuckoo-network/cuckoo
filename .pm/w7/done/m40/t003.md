---
id: w7/m40/t003
title: Render parity
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m40/t002]
---

## Objective

Check the change is consistent across every surface it exposes: REST, GraphQL, MCP (same fields, semantics, error shapes — `docs/ADR006-bex-api.md`) and the dashboard. Compare against the equivalent render.com behavior and flag any drift as follow-up rather than silently diverging.

## Context

- REST already mirrors Render's documented custom-domains list params; this milestone brings GraphQL/MCP to the same shape.
- Dashboard deliberately stays fetch-all (Render's UI is a flat list) — confirm and record, don't change.

## Steps

1. Diff the three surfaces' arg names, enum values, and error shapes for the custom-domains list.
2. Spot-check against Render's OpenAPI custom-domains rows and the parity ledger (`docs/ADR018-render-parity.md`); update the ledger cell/evidence if it references the REST-only state.

## Files

- `docs/ADR018-render-parity.md` (evidence/cell update if needed)

## Acceptance criteria

- [ ] No unexplained cross-surface drift; any deliberate divergence is written down.

## Out of scope

- New capability work found during the check — file it, don't build it.
