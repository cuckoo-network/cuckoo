---
id: w7/m52/t011
title: Verify failure → grace → enforcement → payment → recovery in test mode
worker: worker7
status: done
estimate: 1h
depends_on: [w7/m52/t010]
---

## Objective

Use Stripe test clocks and the production-hosted test endpoint to prove the full dunning transition, including duplicate/dropped/reordered webhook cases.

## Context

- The run must use disposable workspaces and Stripe test objects only.
- Test clocks/setup credentials must remain operator-side; the runtime RAK should not gain broad test-clock permissions.

## Steps

1. Advance a test Subscription through renewal failure and the configured short test grace.
2. Verify notifications, reversible enforcement, polling repair, and cross-surface state.
3. Complete test payment, prove precise recovery, and clean all fixtures.

## Files

- `scripts/stripe-billing-lifecycle-test.sh`
- `docs/runbooks/stripe-billing-setup.md`
- `docs/drills/`

## Acceptance criteria

- [x] The prod test workspace traverses every expected state with `livemode=false`.
- [x] Duplicate/out-of-order delivery and one deliberately missed webhook converge correctly.
- [x] Recovery restores only billing-owned changes and fixture cleanup is verified.

## Out of scope

- Real payment failure or live tenant suspension.
