# Retire the unrecoverable `default/biliblilitest` Postgres — **DONE 2026-07-19**

**Decision (user, 2026-07-19):** **retire** (repair rejected — cluster reported
`Cluster is unrecoverable and needs manual intervention`, no ready instance, no
completed backup, so recovery prospects were poor). Three prior closeouts
stepped around it (`w7/done/m33/README.md:27`, `done/004.md` item 4,
`w9/done/m3/README.md:34`); this one executed it.

## Executed + verified 2026-07-19 (prod app cluster)

Pre-delete confirmation (read-only) — target matched its description exactly:
`Database biliblilitest` (uid `5c0cd971-…`, workspace `tea-d9b9a2p7olhs73bcoi5g`),
phase `Provisioning`, condition `Ready=False / waiting for CloudNativePG`;
underlying CNPG cluster `Cluster is unrecoverable and needs manual intervention`,
0 ready instances; `status.backupsEnabled` unset; owned PVC `biliblilitest-1`,
Secret `biliblilitest-app`, Services `-r`/`-ro`/`-rw`. It was the **only**
Database on the cluster (zero collateral), and never left `Provisioning`, so
there was no live data to lose.

Mechanism — a single control-plane-safe **CR delete**, not raw SQL: the
control-plane reconciler projects only App CRs from `apps` rows
(`internal/store/reconciler.go:122`); Databases have **no** control-plane row
(no `databases` table / no `DeleteDatabase`), so the `Database` CR is
authoritative and the projector can't resurrect it.

```
kubectl -n default delete database.app.bex.co biliblilitest --wait=false
```

The operator finalizer (`app.bex.co/db-finalizer`, `handleDBDeletion`) deleted
the CNPG Cluster, skipped the S3 purge (backups never enabled), and removed the
finalizer. Within ~5s both the Database CR and CNPG Cluster were `NotFound`;
CNPG owner-ref GC removed the PVC, Secret, and all three Services.

Verification: no `biliblilitest` PVC/Secret/Service/pod/Cluster/Database in
`default` or cluster-wide; no `purge-db-biliblilitest` Job (correct — no backups
to purge). `bex-controller-manager` stayed `1/1 Running` throughout.

No repo change — pure ops. Operator behavior (the finalizer teardown) is w7/m12
code, already shipped.
