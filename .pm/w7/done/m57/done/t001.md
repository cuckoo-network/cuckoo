---
id: w7/m57/t001
title: Freeze the audit window + exclusion map
worker: worker7
status: done
estimate: 45m
depends_on: []
---

## Objective

Enumerate every surface shipped in the audit window (2026-07-20 → HEAD) and freeze the scope map the whole audit runs against: which planes are in, which shipped items are excluded because another closed or open milestone already owns their security evidence, and why. This map becomes the register's §Scope and is the guard against double-covering verified work.

## Context

- Window start 2026-07-20 = the day after the last audit-adjacent sweep; the note that defined it is `w7/done/010.md`.
- In-window shipped surfaces (from the milestone ledger): Stripe billing lifecycle (`w7/m50`–`m53`), static-site trust boundaries + the 2026-07-28 prod repair (`w7/m54`), per-tenant namespaces (`w3/m31`), sandboxes + exec (`w3/m32`–`m33`), sandbox hardening (`w3/m35`), strict REST validation (`w7/m49`).
- Exclusions with owners: `w3/m35` (sandbox network/ownership/RBAC/admission — closed, adversarially verified), `w7/m54`'s own owned items incl. the explicitly waived PSL gate, open `w7/m55` (cross-workspace authz matrix) and `w7/m56` (gateway DB credential), trigger-gated `w3/007` (shared sandbox TCB) and `w3/008` (credential brokering / content-aware egress).

## Steps

1. `git log --oneline --since=2026-07-20` + the `wN/done/` ledger entries dated in-window; list every shipped surface with its owning milestone.
2. For each, assign **in-scope** (no security evidence owned elsewhere) or **excluded** (name the owner + the evidence that covers it).
3. Write the scope map to the scratchpad as the draft register §Scope; sanity-check the in-scope set matches t002–t005's plane split and flag anything that fits no plane (becomes an explicit t005 cross-cutting item).

## Files

- `.pm/w7/done/010.md` (source note), `wN/done/` milestone READMEs dated 2026-07-20..HEAD
- scratchpad draft of the register §Scope (lands in `docs/` via t006)

## Acceptance criteria

- [ ] Every in-window shipped milestone/commit range appears exactly once in the scope map with an in/out disposition.
- [ ] Every exclusion names its owning milestone/note and the evidence that makes re-auditing it double-coverage.
- [ ] Any in-window surface that fits none of t002–t004's planes is explicitly routed to t005 (not silently dropped).

## Out of scope

- Auditing anything — this task only fixes the perimeter.
