---
id: w7/m57/t005
title: Cross-cutting sweep: in-window secret custody/rotation, RBAC/NetworkPolicy/CI-guard drift
worker: worker7
status: done
estimate: 60m
depends_on: [w7/m57/t001]
---

## Objective

Sweep the cross-cutting concerns that don't belong to a single plane: custody and rotation of every secret introduced in the audit window, and drift in the platform's RBAC / NetworkPolicy / CI-guard posture since 2026-07-20. Catch the seams between the per-plane audits (t002–t004) that a plane-scoped review would miss.

## Context

- New-in-window secrets to inventory: Stripe (`BEX_STRIPE_SECRET_KEY`, `BEX_STRIPE_WEBHOOK_SECRET`), sandbox-exec HMAC (`BEX_SANDBOX_EXEC_SECRET`), any static-plane publish/read secrets touched by `w7/m54`. For each: out-of-band custody, presence in `.env.example` as a value-less mirror (repo rule), rotation story, and no leakage into logs/audit/DB.
- RBAC/NetworkPolicy drift: w7's charter includes CI structural guards (gitleaks/trivy/gitops-validate, the least-privilege RBAC guard from `w7/m7`). Check that the in-window milestones didn't broaden a Role/ClusterRole/RoleBinding or add a NetworkPolicy hole without a guard catching it; confirm the existing guards still cover the new resources.
- CI-guard coverage: did any in-window feature add a resource class (Stripe webhook route, sandbox-exec listener, namespace-reconciler RBAC) that no CI guard asserts on? A guard gap is itself a finding.
- This is also the catch-all for any in-window surface t001's scope map routed here because it fit no single plane.

## Steps

1. Inventory every secret/env-var added since 2026-07-20 (diff the CLAUDE.md env table + `.env.example` against the window start); check custody, `.env.example` mirror, rotation, and non-leakage for each.
2. Diff RBAC (`config/rbac`, operator + bex-api ClusterRoles/Roles) and NetworkPolicy/CiliumNetworkPolicy over the window; flag any broadening not covered by an existing CI guard.
3. Confirm the CI structural guards (gitleaks, trivy, gitops-validate, RBAC least-privilege guard) still assert on the new resource classes; a gap is a finding.
4. Fold any t001-routed cross-cutting item into this sweep; record all findings (severity + evidence) into the shared register draft for t006.

## Files

- `CLAUDE.md` env table, `.env.example`, `lego/operator/config/rbac/`, `config/`/`deploy/gitops/` NetworkPolicy + CiliumNetworkPolicy, `.github/workflows/` + `scripts/gitops-validate.sh` (CI guards)
- register draft (shared, written out in t006)

## Acceptance criteria

- [ ] Every in-window secret/env-var is inventoried with a custody + `.env.example`-mirror + rotation + non-leakage disposition.
- [ ] RBAC and NetworkPolicy diffs over the window are reviewed; any broadening is either guard-covered or a filed finding.
- [ ] Each new resource class (webhook route, sandbox-exec listener, namespace RBAC) is confirmed CI-guard-covered or a guard-gap finding is filed.
- [ ] Every t001-routed cross-cutting item has a recorded disposition.

## Out of scope

- Plane-specific findings owned by t002–t004 (this task is the seams + posture, not the planes).
