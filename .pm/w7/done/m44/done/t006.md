---
id: w7/m44/t006
title: Closeout — move to done/ when the DoD holds
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m44/t005]
---

## Objective

Close the milestone once every task is done and the definition of done is
actually met (not merely when the code is written).

## Steps

1. Verify all five DoD bullets hold: render a sample failure email (test
   fixture or `scripts/`-style dump) and confirm framing + commit block +
   View Logs link; confirm omit paths (no commit / no base URL); confirm no
   REST/GraphQL/MCP/dashboard diff.
2. Set every task's `status: done`, mark rows `— **DONE**` in the milestone
   README, set `**Status:** done`.
3. Move each `tNNN.md` to `w7/m44/done/`.
4. Move the whole `w7/m44/` directory to `w7/done/m44/`.
5. Check `- [x]` for m44 in `w7/README.md`.

## Acceptance criteria

- [ ] All DoD bullets verified
- [ ] `w7/done/m44/` exists; no `w7/m44/` remains
- [ ] `w7/README.md` shows `- [x] **m44**`
