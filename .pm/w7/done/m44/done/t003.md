---
id: w7/m44/t003
title: Render parity — verify enriched email vs Render's captured message; update notify-on-fail.md + ADR018
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m44/t002]
---

## Objective

Confirm the enriched email matches Render's captured deploy-failure message and
introduces no REST/GraphQL/MCP/dashboard drift (email content only), then record
the parity: the `notify-on-fail.md` artifact and the ADR018 Notifications row.

## Context

- Render reference (source quote, 2026-07-17): impact framing naming the
  service + "latest changes may not be live", the failing commit message, and a
  "View Logs" link.
- `docs/render-artifacts/notify-on-fail.md` documents the notification *policy*
  contract; it has no email-content section — add one capturing Render's email
  shape and bex's enriched parity + stated divergences (plain-text vs Render's
  HTML; the started-email inclusion bex already notes).
- `docs/ADR018-render-parity.md` Notifications row (line ~158) — the row is ✅
  on policy/surfaces; add a sentence that the deploy-lifecycle email content now
  carries commit message + View Logs link + impact framing (w7/m44), so the ✅
  reflects content depth, not just policy.

## Steps

1. Diff-check: confirm the milestone changed only `internal/notifications`,
   `internal/store/reconciler.go`, and `cmd/api/main.go` — no REST/GraphQL/MCP
   adapter or dashboard files (email is not a bex-api surface).
2. Compare the rendered failure body against the Render quote (framing, commit,
   link) — note the deliberate divergences (plain-text transport; link target
   is bex's deploy-detail route).
3. Add an "Email content" section to `docs/render-artifacts/notify-on-fail.md`.
4. Extend the ADR018 Notifications row note with the w7/m44 content enrichment.
5. `npx prettier@3.4.2 --write` over the touched markdown.

## Files

- `docs/render-artifacts/notify-on-fail.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Rendered failure email matches Render's captured shape (framing + commit + link), divergences stated
- [ ] Confirmed no REST/GraphQL/MCP/dashboard drift (email-only change)
- [ ] notify-on-fail.md gains an email-content section
- [ ] ADR018 Notifications row cites the w7/m44 content enrichment

## Out of scope

- New notification surfaces or policy
