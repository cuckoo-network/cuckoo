---
id: w7/m44/t005
title: Test coverage — subject/body assertions for commit + link + framing across started/succeeded/failed
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m44/t003]
---

## Objective

Add meaningful tests for the enriched email: the failure body's impact framing,
the commit-message block (present/omitted), the View Logs link
(present/omitted), and consistency across the three lifecycle kinds — plus that
the reconciler threads the commit + deploy id through.

## Context

- `internal/notifications/service_test.go` already asserts the exact subject/body
  strings (e.g. `service_test.go:175`); those assertions must be updated to the
  new bodies and extended.
- The composer is pure (`deployEmail` → subject, body) — the cleanest unit-test
  seam; test it directly with/without commit and with/without base URL.
- t001's reconciler threading can be covered where the reconciler's notify seam
  is already exercised (assert the `DeployNotification` carries commit + deploy id).

## Steps

1. Update the existing exact-string assertions to the enriched bodies.
2. Add composer cases: failure body contains the framing + the given commit
   message + the deploy-detail link; image-backed (empty commit) omits the
   commit block; empty base URL omits the View Logs line.
3. Add a case per kind (started/succeeded/failed) confirming the shared
   structure + link.
4. Cover the reconciler threading (commit message + deploy id reach the
   `DeployNotification`), reusing the reconciler test's existing notify seam if
   present.
5. `cd lego/backend && go test ./...` green.

## Files

- `lego/backend/internal/notifications/service_test.go`
- `lego/backend/internal/store/reconciler_test.go` (or the existing reconciler notify test)

## Acceptance criteria

- [ ] `go test ./...` passes with the updated + new assertions
- [ ] Tests assert real content (framing text, commit block present/absent, link present/absent) — not tautologies
- [ ] The commit-absent and base-URL-absent omit paths are both covered

## Out of scope

- Live SMTP/end-to-end mail delivery tests
