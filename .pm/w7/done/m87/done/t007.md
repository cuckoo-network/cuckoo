---
id: w7/m87/t007
title: "Test coverage"
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m87/t005]
---

## Objective

Fill meaningful behavioral coverage gaps left by implementation and prove the tests detect the original missing cache behavior.

## Context

- Reuse the reproduction and live drill rather than replacing them with string-only assertions of generated flags.
- The failure is an environment-only warm-cache transition; exact bytes, precedence, stability, and credential boundaries are the key regression cases.

## Steps

1. Add focused native/controller tests for changed and unchanged input, literal/group/own-source combinations, removals, missing required sources, restart stability, and non-secret cache metadata.
2. Prove representative tests fail when the original omission is restored, using a local mutation or equivalent controlled baseline.
3. Run make test and make lint from lego/operator; run backend checks as needed if the shared types contract changed. Preserve the live drill's artifact-level acceptance.

## Files

- `lego/operator/internal/build/`
- `lego/operator/internal/controller/`
- `lego/types/v1alpha1/ (if changed)`
- `lego/operator/internal/controller/build_cache_drill_test.go`
- `.pm/w7/m87/t007.md (validation record)`

## Acceptance criteria

- [ ] Tests assert actual transitions/results and relevant failure modes, with a representative pre-fix failure recorded.
- [ ] Required affected checks pass; skipped integration or live checks remain explicitly open where they are needed for the DoD.
- [ ] Fixtures are isolated and cleaned up, and no test exposes real credentials.

## Out of scope

- Coverage-percentage targets, tautological assertions, or unrelated broad test work.
