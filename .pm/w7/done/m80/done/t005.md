---
id: w7/m80/t005
title: Correct ADR004, the parity ledger, and the CRD field comment
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m80/t004]
---

## Objective

Three documents currently describe the old semantics as if they were the design. Bring them to what the code does after t001–t004, and record the two deliberate divergences so a later reader does not read them as bugs.

## Context

- `docs/ADR004-app-deployment.md:90` — "A pod joins the Service's endpoints … only once its `ReadinessProbe` passes — an HTTP `GET` of `spec.healthCheckPath` … Empty `healthCheckPath` defaults to `/`." Wrong on both counts after t004.
- `docs/ADR004-app-deployment.md:123` — the annotated `bex.yml` example says `healthCheckPath: / # … defaults to "/"`.
- `lego/types/v1alpha1/app_types.go:349-353` — "defaults to `/`" (t004 rewrites this; confirm it landed).
- `docs/ADR018-render-parity.md` — the health-check row needs new evidence.
- Repo rule: run `npx prettier@3.4.2 --write "**/*.md"` before finishing.

## Steps

1. Rewrite the ADR004 passage to state the two-mode contract (unset → TCP; set → HTTP 2xx/3xx), the 5s budget, the 15-minute boot budget, and that the three rollout timers are one number.
2. Update the annotated example at ADR004:123.
3. Update the ADR018 health-check row with the Render sources and the current evidence.
4. Document the migration explicitly: existing services carry a persisted `healthCheckPath: "/"`; clearing it is how an owner opts into the TCP default. Say plainly that this milestone does not backfill and why.
5. Record the deliberate divergences: rotation removal is 30s where Render is 15s (t001's reasoning), and no `livenessProbe` where Render restarts at 60s (pointer to `w7/027.md`).
6. Run prettier.

## Files

- `docs/ADR004-app-deployment.md`
- `docs/ADR018-render-parity.md`
- `lego/types/v1alpha1/app_types.go` (verify t004's comment)

## Acceptance criteria

- [ ] No document still claims `healthCheckPath` defaults to `/` or that the probe is always HTTP.
- [ ] The migration note names the persisted-default fact and the opt-in.
- [ ] Both divergences from Render are recorded with reasoning, not left implicit.
- [ ] `npx prettier@3.4.2 --check "**/*.md"` is clean.

## Out of scope

- Dashboard copy or help text — t006 decides whether the Health & Alerts section needs wording changes.
