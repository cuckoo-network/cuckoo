---
id: w7/m80/t009
title: Closeout
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m80/t007, w7/m80/t008]
---

## Objective

The standing closeout task. Run it only once m80's definition of done actually holds — a milestone lands in `done/` when its observable end state is real, not when the code is merely written.

## Context

Before closing, confirm all four DoD items are demonstrably true, not just implemented:

1. A service with an unset `healthCheckPath` and a 404 root reaches Ready.
2. Every probe carries a 5-second budget.
3. The three rollout timers all read 15 minutes and are asserted equal.
4. An explicit `healthCheckPath` still produces a strict HTTP check.

Also confirm t005's docs landed and t006's parity findings were filed rather than dropped.

## Steps

1. Verify the DoD above; if any item is not real, stop and reopen the owning task.
2. Set every remaining task's `status: done`.
3. Move each `tNNN.md` to `w7/m80/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in `w7/m80/README.md`.
5. Move the whole `w7/m80/` directory to `w7/done/m80/`, leaving nothing behind at the old path (no tombstone, stub, or redirect README).
6. Check `- [x]` for m80 in `.pm/w7/README.md` with a one-line outcome summary.
7. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m80/**`
- `.pm/w7/README.md`

## Acceptance criteria

- [ ] All four DoD items verified true before the move.
- [ ] `.pm/w7/m80/` no longer exists; `.pm/w7/done/m80/` holds the milestone with every task in `done/`.
- [ ] Status is consistent in all three places: task frontmatter, milestone README, workstream checkbox.
- [ ] Prettier clean.

## Out of scope

- Shipping the code — only `/ship` commits and pushes.
