---
id: w7/m80/t007
title: Simplify the code this milestone changed
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m80/t006]
---

## Objective

The standing simplify task. Run `/simplify` over what m80 touched — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- Main surface: the probe construction in `lego/operator/internal/controller/deployment_projection.go`, which by t004 handles two handler kinds across two probes. Watch for a helper that grew parameters instead of clarity.
- Second surface: the shared timing constant introduced in t003 and consumed by t002's `progressDeadlineSeconds` — confirm one source of truth rather than a constant plus a comment repeating it.
- Third: test files, where t001/t003/t004 each added probe assertions that may now overlap.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the behavior-preserving findings.
3. Re-run both suites.

## Files

- whatever the diff touches

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly declined with a reason.
- [ ] `make test` and `cd lego/backend && go test ./...` both pass.
- [ ] No behavior change — this task must not alter any probe value.

## Out of scope

- Bug hunting (`/code-review` is the tool for that).
