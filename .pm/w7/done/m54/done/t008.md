---
id: w7/m54/t008
title: Complete negative-path and production smoke coverage
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m54/t006, w7/m54/t007]
---

## Objective

Finish meaningful automated and production coverage for the accepted browser contract and every Kubernetes and object-store failure mode this milestone prevents.

## Context

- Tests must assert real deny behavior and successful static lifecycle behavior, not only manifest snapshots.
- Production evidence must avoid credentials, deploy-hook tokens, kubeconfigs, and secret payloads.

## Steps

1. Add browser tests for the disclosed sibling-cookie divergence and host/origin scoping.
2. Add impersonation/admission tests for forbidden Service, Ingress, and alias mutations plus positive operator reconciliation.
3. Add object-store positive/negative permission tests for both identities.
4. Run operator, GitOps, docs, browser, and production lifecycle checks; record non-secret evidence.

## Files

- `lego/operator/internal/controller/*_test.go`
- `scripts/gitops-validate.sh`
- `scripts/verify-tenant-isolation.sh`
- browser security tests
- `docs/drills/`

## Acceptance criteria

- [x] Each of the three threat classes has real positive and negative coverage for its accepted contract.
- [x] Browser origin-storage/worker drift, removing alias authorization, or weakening an S3 deny causes the corresponding test to fail non-vacuously.
- [x] Required repository suites and lint pass.
- [x] A production static-site lifecycle and all negative probes pass with sanitized evidence.

## Completion evidence

Real Chrome asserts the accepted parent-cookie difference plus origin-local storage and Service Workers. The live verifier exercised 400 Service/Ingress authorization decisions, positive and hostile API-server admission, public serving, and the complete reader/publisher S3 allow/deny matrix. Disposable production static and maintenance fixtures covered publish, redeploy, read, maintenance transition, delete, prefix purge, and post-revocation deploy. Evidence is sanitized in `docs/drills/2026-07-29-static-site-trust-baseline.md`; the shipped GitOps, scripts, docs, operator, backend, and dashboard gates were green.

## Out of scope

- Coverage-only assertions that do not exercise a real browser, API server authorization/admission path, or object-store policy.
