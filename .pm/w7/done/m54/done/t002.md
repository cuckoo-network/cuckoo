---
id: w7/m54/t002
title: Evaluate onbex.co Public Suffix List inclusion (skipped)
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m54/t001]
---

## Objective

Evaluate PSL inclusion and record the owner's final decision on whether it remains a milestone requirement.

## Context

- A site at `evil.onbex.co` can currently request `Domain=onbex.co`; host-only cookies remain unreadable, but sibling requests can receive an injected same-name cookie.
- Render's equivalent shared tenant domain, `onrender.com`, is present in the Public Suffix List.
- The dashboard and API remain on `bex.co`; this task must not move the first-party control plane onto the tenant content suffix.
- On 2026-07-30 the owner explicitly instructed the project to skip PSL inclusion and mark this task done.

## Steps

1. Capture upstream eligibility and the current canonical-list state.
2. Verify current browser behavior rather than inferring it from a PSL library.
3. Record the owner waiver without claiming that `onbex.co` is present or browser-isolated.
4. Keep host-only/`__Host-` guidance and control-plane domain separation explicit.

## Files

- `publicsuffix/list` upstream private-domain section
- `docs/ADR029-static-sites.md`
- `docs/ADR012-auth.md`
- `scripts/`

## Acceptance criteria

- [x] The owner explicitly waived PSL inclusion on 2026-07-30.
- [x] Real-browser evidence continues to disclose that `Domain=onbex.co` crosses sibling tenants.
- [x] No upstream entry, browser isolation, or Render-equivalence claim is made.
- [x] Platform authentication remains on `bex.co`, and host-only/`__Host-` guidance is documented.

## Completion evidence

The canonical-list check, current upstream eligibility requirements, and real-Chrome comparison are recorded in `docs/render-artifacts/static-site-browser-isolation.md` and `docs/ADR029-static-sites.md`. This task is done by explicit owner waiver, not by PSL membership.

## Out of scope

- Moving dashboard or API origins, or introducing OAuth tokens into browser-readable storage.
