---
id: w7/m54/t007
title: Simplify the static-site security implementation
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m54/t006]
---

## Objective

Perform the behavior-preserving simplification review over the milestone's code and configuration changes, removing duplicated policy, Secret, alias, and verification logic while preserving the proven security behavior.

## Context

- Static and maintenance ExternalName aliases should share bounded, reviewable safety primitives where their contracts match.
- Object-store Secret naming and namespace placement must have one source of truth across manager, static-server, GitOps, and runbooks.

## Steps

1. Review every changed implementation and manifest path (the repository has no registered `$simplify` skill, so the equivalent review was performed directly).
2. Consolidate duplicated helpers/contracts where doing so makes the trust boundary easier to audit.
3. Re-run focused security checks after each behavior-preserving simplification.

## Files

- `lego/operator/`
- `deploy/gitops/`
- `scripts/`
- `docs/ADR029-static-sites.md`

## Acceptance criteria

- [x] Every milestone-owned code/configuration change received the equivalent behavior-preserving simplification review.
- [x] Duplicate alias-policy and Secret-contract logic is removed or its intentional separation is documented.
- [x] All security behavior and negative probes remain green.

## Completion evidence

Static and maintenance aliases share `reconcilePlatformAlias`; publish and purge share `validateStaticCredentialSecret`; the read/publish Secret contracts have one runtime name per role. The two explicit CEL destination branches remain intentionally separate because their names, targets, and ports are different fail-closed contracts. The final review strengthened exhaustive namespace verification and failure-path S3 probe cleanup in `429bd9d2` without changing runtime behavior.

## Out of scope

- Broad refactors outside the static-site trust boundaries changed by this milestone.
