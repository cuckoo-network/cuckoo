---
id: w7/m76/t002
title: "pricing.yaml + Stripe catalog entry"
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m76/t001]
---

## Objective

Give the new meter a price: add `sandbox_compute_seconds` to `pricing.yaml` (rate anchored against AgentCore-style per-second compute pricing, ADR047 research: ~$0.0895/vCPU-hr + $0.00945/GB-hr as the reference point) and to the Stripe catalog via `scripts/stripe-billing-setup.py`, so the sealed-outbox emitter has a meter/price to ship events against.

## Context

- Catalog + runbook: `scripts/stripe-billing-setup.py`, `docs/runbooks/stripe-billing-setup.md` (ADR040/w7/m50–m51 custody).
- Estimate-only behavior when Stripe is unset must keep working (pricing.yaml is the estimate source).

## Steps

1. `pricing.yaml` entry (unit, rate, display name).
2. Stripe setup script: meter + metered Price creation, idempotent re-run safe.
3. Runbook note for the operator-run provisioning step.

## Files

- `pricing.yaml` (locate in repo), `scripts/stripe-billing-setup.py`, `docs/runbooks/stripe-billing-setup.md`

## Acceptance criteria

- [ ] Estimate path prices sandbox usage with Stripe unset
- [ ] Setup script provisions the meter/price idempotently (stub/test mode)

## Out of scope

- Plan-included quota packaging (Devin/Cursor pattern — follow-on once packaging is designed)
