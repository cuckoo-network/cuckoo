---
id: w7/m76/t003
title: "PaymentGate coverage for sandbox create (ADR046 paid intent)"
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m76/t001]
---

## Objective

Extend the ADR046 paid-intent gate to sandboxes: under `BEX_REQUIRE_PAYMENT_METHOD=1`, sandbox creation (a metered, non-free resource once t001 lands) requires the bound-payment-method marker — 402/`PAYMENT_REQUIRED` across REST/GraphQL/MCP for cardless callers; cardless accrued rows stay pending per ADR046's bounded write-off, never silently free.

## Context

- Gate: injected `PaymentGate` in the service layer (docs/ADR046, w1/m62); covered resources today: Service/Postgres/Key Value create + plan change.
- Decide + document whether a free sandbox tier exists (if so, gate only paid shapes; if not, all creates gate) — ADR047 D6 implies all agent-session compute is metered.
- Agent-session create (w3/m39) provisions sandboxes through the same client — ensure the gate applies at the service layer both surfaces share, not per-endpoint.

## Steps

1. Wire `PaymentGate` into the sandbox (and agent-session) create path at the shared layer.
2. Error dialect: Render-shaped 402 on REST, consistent GraphQL/MCP shapes (ADR046 precedent).
3. Emitter interplay: cardless rows pending (existing withhold logic gains the new kind).

## Files

- `lego/backend/internal/sandbox/`, `lego/backend/internal/agentsession/` (shared service layer), billing gate package

## Acceptance criteria

- [ ] Cardless create → 402 across all three surfaces with the gate on; unaffected with it off
- [ ] Cardless accrued rows withheld from Stripe export

## Out of scope

- Onboarding UX changes (existing Checkout flow suffices)
