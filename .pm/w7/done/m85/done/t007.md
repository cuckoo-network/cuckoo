---
id: w7/m85/t007
title: Test coverage for the shipped behavior
worker: worker7
status: done
estimate: 40m
depends_on: [w7/m85/t005]
---

## Objective

Prove the guard actually guards, and that pinning did not change runtime behavior.

## Context

- The guard's self-test is the centerpiece; a pinning guard whose red case was never exercised is decoration.
- The behavioral risk of this milestone is **regression by pinning**: a wrong digest, a single-platform digest, or a Valkey pin that restarts running instances. Those need real assertions, not a diff review.

## Steps

1. Prove the guard red on each of the three syntaxes (Dockerfile FROM, Go constant, manifest `image:`) and green on the fixed tree.
2. Assert each pinned digest resolves to the same tag the code claims (catches a copy-paste digest from the wrong image).
3. Test the KeyValue backup path end to end with the new `age` provisioning, including decrypt.
4. Confirm operator and dashboard suites are green.

## Files

- guard self-test in `scripts/`
- `lego/operator/internal/controller/` tests

## Acceptance criteria

- [x] The guard is proven red on all three syntaxes and green after the fix.
- [x] Each digest is asserted to match its claimed tag.
- [x] Backup encrypt→decrypt round-trips with the new `age` provisioning.
- [x] No trivial or tautological tests; suites green.

## Out of scope

- Verifying pins against production running images — desirable, not a merge gate.
