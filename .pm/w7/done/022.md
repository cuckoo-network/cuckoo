# Pin dev-tooling npx packages (mermaid, Playwright MCP)

**Type:** optional dev-tooling hardening. **Not a milestone** (developer-machine only; `@latest`/`npx -y` is the conventional UX).

codex-security scan #17 + #22 (both low):

- `.claude/skills/mermaid/SKILL.md:7/22` — mandatory verify step runs `npx -y @mermaid-js/mermaid-cli` (no version/lockfile) in the agent host context.
- `.mcp.json:2/8` — Playwright MCP configured as `npx @playwright/mcp@latest` (unpinned), executed with developer/browser authority on accept.

Both resolve the current registry `latest`/tag each run on the developer machine (not CI, not production). Risk requires an npm registry/compromise MITM against a developer host.

**Trigger to promote:** a dev-tool supply-chain policy is adopted, or either package is implicated in an advisory. Fix shape: pin each to an exact reviewed version (e.g. `@mermaid-js/mermaid-cli@11.4.2`, `@playwright/mcp@0.0.30`) and reference from a lockfile-backed install. Until then: accepted dev-only convenience.

Source: codex-security scan `codex-security-bex-azfRGv`, findings #17 and #22.

> **Retired 2026-08-07** (w7 inbox cleanup): DONE — pinned in place: `.mcp.json` → `@playwright/mcp@0.0.79`, mermaid skill → `@mermaid-js/mermaid-cli@11.16.0` (both the versions `latest` resolved to on 2026-08-07, so behavior-preserving). Findings #17/#22 closed.
