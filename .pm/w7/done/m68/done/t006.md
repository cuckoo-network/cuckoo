---
id: w7/m68/t006
title: Simplify
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m68/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving) — prime candidate: shared helpers between the Database and KeyValue S3-purge finalizers, and between the platform backup CronJob shapes.

## Context

- Changed surface: `lego/operator/internal/controller/keyvalue_controller.go`, operator config plumbing, `deploy/gitops/base/prometheus.yaml`.

## Steps

1. `/simplify` scoped to the milestone's diff; apply safe cleanups.

## Files

- (as flagged)

## Acceptance criteria

- [x] Simplify pass run; cleanups applied or explicitly declined with reason.

## Result

The `/simplify` skill was unavailable in this session, so the diff received a manual behavior-preserving simplify pass. It extracted the shared bounded cleanup-Job name helper, reused the existing durable cleanup state machine, and changed its Job deletion to background propagation because the helper already explicitly deletes and observes labeled Pods. The review also found and fixed the 63-byte purge-name edge case. No broader abstraction between database and KeyValue backup creation was introduced because their snapshot mechanisms and lifecycle ownership differ materially.

## Out of scope

- Behavior changes.
