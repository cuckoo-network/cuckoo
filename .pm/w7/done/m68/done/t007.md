---
id: w7/m68/t007
title: Test coverage — envtest gating/teardown + snapshot-job behavior
worker: worker7
status: done
estimate: 60m
depends_on: [w7/m68/t005]
---

## Objective

Meaningful tests for the shipped behavior and its failure modes — gating, byte-identical-when-off, and teardown — not snapshot-everything coverage.

## Context

- Operator envtest suite: `lego/operator/internal/controller/keyvalue_test.go` + the deletion-test precedents (`database_deletion_test.go`).
- Behaviors that must be pinned: (1) env unset ⇒ no CronJob for any plan (the byte-identical contract); (2) paid ⇒ CronJob with correct schedule/spec, Free ⇒ none; (3) plan upgrade/downgrade flips the CronJob on/off; (4) finalizer purge called with the right prefix, retried on failure, skipped when disabled; (5) job naming/prefix derivation from the typed id.

## Steps

1. Add the envtest/unit cases above (`make test` from `lego/operator/`).
2. Where the S3 purge is exercised, follow the existing fake/object-store test seam used by the Database purge tests.

## Files

- `lego/operator/internal/controller/keyvalue_test.go`
- (new test files as needed)

## Acceptance criteria

- [x] All five behavior classes covered with assertions on real reconcile output.
- [x] `make test` green; no tautological tests (each fails if its behavior regresses).

## Out of scope

- Live-cluster E2E (covered by the t002/t003 live verifications and the t005 drill).
