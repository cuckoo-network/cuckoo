---
id: w7/m42/t007
title: Closeout — move to done/ when the DoD holds
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m42/t006]
---

## Objective

Close the milestone once every task is done and the definition of done is
actually met (not merely when the code is written).

## Steps

1. Verify all five DoD bullets in `README.md` hold against the live dev-7 dashboard
   (URL round-trip, reload-stable, shareable, `t`/`r` prefill, simplified bar, no
   capability lost).
2. Set every task's `status: done`, mark rows `— **DONE**` in the milestone README,
   set `**Status:** done`.
3. Move each `tNNN.md` to `w7/m42/done/`.
4. Move the whole `w7/m42/` directory to `w7/done/m42/`.
5. Check `- [x]` for m42 in `w7/README.md`.

## Acceptance criteria

- [ ] All DoD bullets verified live
- [ ] `w7/done/m42/` exists; no `w7/m42/` remains
- [ ] `w7/README.md` shows `- [x] **m42**`
