---
id: w7/m38/t002
title: Implement cursor/limit + verificationStatus/domainType; check GraphQL/MCP
worker: worker7
status: done
estimate: 50m
depends_on: [w7/m38/t001]
---

## Objective

Wire `core.PageParams` + `StablePage` (cursor = domain name, matching the emitted per-item cursors; default per w1/m42 t001's convention) into `listDomains`, and apply `verificationStatus`/`domainType` filtering before paging. Check the GraphQL/MCP domain list surfaces for the same drift and align.

## Context

- `lego/backend/internal/apps/rest.go:949-956` — handler ignores the query string.
- `lego/backend/internal/apps/domains.go:455-482` — `customDomainWithCursor` already exists; only honoring is missing.
- `domainType` derivation: apex vs subdomain from the host shape; `verificationStatus` from the domain's verification state field.

## Steps

1. Parse + validate the four params (unknown enum value ⇒ Render-shaped 400 via `core.WriteErr`).
2. Filter, then page.
3. Align GraphQL/MCP domain lists if they exist with the same gap.
4. `cd lego/backend && go build ./... && go test ./...`.

## Files

- `lego/backend/internal/apps/rest.go`
- `lego/backend/internal/apps/domains.go`

## Acceptance criteria

- [ ] Paging walk terminates with full coverage; filters return only matching domains.
- [ ] Invalid enum values get a 400 with `message`.

## Out of scope

- Sibling-redirect behavior (open w6/m30) and DNS-instruction surfaces.
