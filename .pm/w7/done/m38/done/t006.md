---
id: w7/m38/t006
title: Test coverage
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m38/t004]
---

## Objective

Add tests: pagination walk over > limit domains (terminates, full coverage, no duplicates — assert against the old identical-pages failure mode) and filter behavior for each `verificationStatus`/`domainType` value including the invalid-enum 400.

## Files

- `lego/backend/internal/apps/` tests

## Acceptance criteria

- [ ] Both behaviors locked; `go test ./...` passes.
