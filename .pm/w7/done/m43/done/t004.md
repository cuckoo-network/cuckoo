---
id: w7/m43/t004
title: Render parity — verify Scaling page + API consistency vs Render's live page; update ADR018 + manual-scaling.md
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m43/t001, w7/m43/t002, w7/m43/t003]
---

## Objective

Verify the finished Scaling page against the live Render capture, confirm the
milestone introduced no REST/GraphQL/MCP drift (dashboard-only change), and
update the parity records: ADR018's manual-scale row still cites the Settings
stepper as the UI evidence, and `manual-scaling.md` still carries the
docs-fallback placement guess the live capture corrected.

## Context

- Live Render reference (captured 2026-07-16, this milestone's source):
  Autoscaling card (toggle + min/max 1–100 + CPU/memory targets 1–90%, default
  60, enable/disable confirm modals) ⊕ Manual Scaling card (slider 1–100 +
  input + Save, shown iff autoscaling off) + Recent Metrics (48h avg
  memory/CPU utilization, total instances, "View all metrics").
- `docs/ADR018-render-parity.md` row "Manual scale (instance count)" — UI cell
  says "Settings +/- stepper (w5/m16)"; update to the Scaling-page card.
- `docs/render-artifacts/manual-scaling.md` — header says "Captured:
  docs-fallback (render.com login required…)" and places the section under
  Settings; append the live-capture correction (Scaling tab, slider 1–100,
  mutual exclusion with autoscaling, Recent Metrics section) rather than
  rewriting history.
- API surface confirmed at parity pre-milestone (survey 2026-07-16): REST
  `POST /scale` + `GET/PUT/DELETE /autoscaling`, GraphQL
  `scaleService`/`setAutoscaling`/`disableAutoscaling`/`autoscalingConfig`,
  MCP `scale_service`/`get_autoscaling`/`set_autoscaling`/`disable_autoscaling`.
  This task just confirms the milestone didn't touch them.

## Steps

1. Walk the dev-7 Scaling page against the capture: manual card exclusivity
   both ways, save round-trips, metrics section, corrected copy/ranges.
2. Confirm `git diff` touches no `lego/backend` surface files.
3. Update the ADR018 manual-scale row's UI evidence (Scaling-page card,
   w7/m43) and note the Settings-stepper removal; touch the autoscaling row
   only if its UI note needs the same correction.
4. Append the live-capture findings to `docs/render-artifacts/manual-scaling.md`.
5. `npx prettier@3.4.2 --write` over the touched markdown.

## Files

- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/manual-scaling.md`

## Acceptance criteria

- [ ] Live walk matches the Render capture (exclusivity, ranges, metrics, copy)
- [ ] No REST/GraphQL/MCP files changed in this milestone's diff
- [ ] ADR018 manual-scale row cites the Scaling-page card (w7/m43)
- [ ] manual-scaling.md records the live-capture placement correction

## Out of scope

- New API surface (none needed — full parity pre-exists)
