---
id: w7/m78/t004
title: Simplify the code this milestone changed
worker: worker7
status: done
estimate: 25m
depends_on: [w7/m78/t003]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- The reconcile's long `r.fail()` sequence is the altitude candidate: after t002 the blocking/best-effort distinction should be legible at a glance rather than encoded in the order of a hundred lines.
- The legacy per-App NetworkPolicy cleanup may be a deletion candidate in its own right if every cluster has migrated — check before removing.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving fixes only.
3. Re-run `make test` and `make lint`.

## Files

- Whatever this milestone changed.

## Acceptance criteria

- [ ] The blocking/best-effort distinction is legible in the code.
- [ ] `make test` green, `make lint` clean.

## Out of scope

- Behavior changes.

### Disposition (2026-08-09)

**N/A 2026-08-09.** No production code changed in this milestone — only a test was added. Nothing to simplify.
