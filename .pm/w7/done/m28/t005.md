---
id: w7/m28/t005
title: Docs: close the ADR010/ADR018 build-logs divergence
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m28/t004]
---

## Objective

Update `docs/ADR010-observability.md` (log filters section) and the `docs/ADR018-render-parity.md` §Logs row: `type=build` is now store-backed, with the store-less 503 contract and the tenant-scoping note.

## Steps

1. Rewrite the "`type=build` stays empty by design" divergence in both docs.
2. `npx prettier@3.4.2 --write "docs/*.md"`.

## Files

- `docs/ADR010-observability.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Neither doc still claims `type=build` is empty by design; prettier clean.

## Out of scope

- CLAUDE.md env-table wording (only if `BEX_LOKI_URL`'s description changes).
