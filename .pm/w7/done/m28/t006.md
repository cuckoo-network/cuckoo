---
id: w7/m28/t006
title: Render parity
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m28/t005]
---

## Objective

Check the change is consistent across every surface it exposes: REST, GraphQL, and MCP (`lego/backend/`) — same fields, semantics, error shapes — and the dashboard Logs tab (`type` dropdown should offer/handle `build`). Compare against Render's build-log behavior (deploy page + `type=build` filter) and flag any drift as follow-up work rather than silently diverging.

## Acceptance criteria

- [ ] All three API surfaces + the Logs-tab type filter behave identically for `type=build`; drift filed, not hidden.
