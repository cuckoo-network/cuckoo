---
id: w7/m67/t005
title: Amend ADR031 (policy table + serverName convention) + ADR012 consequences
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m67/t004]
---

## Objective

Make the docs match the new reality: ADR031's consolidated policy covers the auth DBs, and ADR012 stops claiming they lack off-cluster backup.

## Context

- `docs/ADR031-platform-data-backup.md`: add the three auth DBs to the backup-policy table (mechanism, schedule, destination `auth-dbs/`, 7d retention, restore runbook pointer, drill date from t004); add the `auth/auth-dbs-backup-s3` Secret to the one-time-setup section; record the `<name>-pg<major>` serverName convention (and why `bex-db` predates it).
- `docs/ADR012-auth.md` §Consequences: replace the "auth DBs still lack an off-cluster backup schedule" line with the shipped state.

## Steps

1. Edit both ADRs.
2. `npx prettier@3.4.2 --write "docs/*.md"` (repo rule).

## Files

- `docs/ADR031-platform-data-backup.md`
- `docs/ADR012-auth.md`

## Acceptance criteria

- [x] ADR031 table row + setup section + convention note present; drill recorded with date.
- [x] ADR012 consequence corrected; no doc still claims the gap exists.

## Out of scope

- RPO/RTO objectives and re-drill cadence (inbox note `w7/016`).
