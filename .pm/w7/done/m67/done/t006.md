---
id: w7/m67/t006
title: Simplify
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m67/t005]
---

## Objective

Run `/simplify` over the manifests/config this milestone changed (reuse / simplification / efficiency; behavior-preserving) — e.g. whether the three plugin blocks and ScheduledBackups can share structure via the chart's kustomization instead of triplication.

## Context

- Changed surface: `deploy/gitops/charts/barman-cloud-objectstores/`, `deploy/gitops/charts/auth-dbs/`, `deploy/gitops/base/prometheus.yaml`.

## Steps

1. `/simplify` scoped to the milestone's diff; apply safe cleanups.

## Files

- (as flagged)

## Acceptance criteria

- [x] Simplify pass run; cleanups applied or explicitly declined with reason.

## Result

The `/simplify` skill was unavailable in this session, so the pass was performed manually. The final shape is already consolidated: one shared ObjectStore, one multi-document ScheduledBackup file, one label-driven alert, and one reusable guard with its own fixtures. Further deduplication would hide the three distinct archive identities or schedules without reducing behavior.

## Out of scope

- Behavior changes.
