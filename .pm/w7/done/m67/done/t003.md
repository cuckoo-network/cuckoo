---
id: w7/m67/t003
title: WAL-archiver staleness alerts for the three auth DBs
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m67/t002]
---

## Objective

A silent backup failure on any auth DB pages within a day, exactly like `bex-db`'s.

## Context

- Pattern: `BexDbBackupStale` at `deploy/gitops/base/prometheus.yaml:721` (fires when the CNPG WAL archiver reports >26h without a successfully archived segment; scrape wiring note at `prometheus.yaml:381`). The `cnpg-tenant-db` scrape job already collects CNPG metrics (:9187) — confirm the auth-namespace pods are in scope or extend the scrape config.

## Steps

1. Extend the alert (generalize the existing rule over the auth clusters, or add per-cluster rules — prefer one label-driven rule if the metric carries the cluster label).
2. Deploy and verify the alert evaluates against live series for all three clusters (query the expression in Prometheus).

## Files

- `deploy/gitops/base/prometheus.yaml`

## Acceptance criteria

- [x] The staleness expression returns a healthy (non-firing) result for all three auth DBs in prod.
- [x] Killing the expression's input in a test (or a `promtool` unit test, matching existing practice in the repo if present) shows the alert would fire.

## Out of scope

- Alertmanager routing changes (existing email receiver is fine).
