---
id: w7/m45/t006
title: Closeout
worker: worker7
status: done
estimate: 10m
depends_on: [w7/m45/t005]
---

## Objective

Close the milestone once its definition of done actually holds — the unmodified Render CLI can change a Key Value instance's memory-policy and IP allow-list (set/replace/clear) after creation, verified on read-back, consistently across REST/GraphQL/MCP/dashboard, with tests guarding the no-op regression.

## Context

- Only run this when the DoD is real (behavior verified against a live bex), not merely when the code is written.
- Completing this task **is** the milestone move (`/pm done w7/m45/t006` triggers it).

## Steps

1. Confirm the milestone DoD holds: memory-policy update, ip-allow-list replace, and clear all mutate on read-back via the unmodified `render` CLI; cross-surface parity verified (t003); tests pass (t005).
2. Set every remaining task's `status: done`; move each `tNNN.md` to `.pm/w7/m45/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `.pm/w7/m45/README.md`.
4. Move the whole `.pm/w7/m45/` directory to `.pm/w7/done/m45/` and check `- [x] **m45**` in `.pm/w7/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m45/` (moves to `.pm/w7/done/m45/`)
- `.pm/w7/README.md`

## Acceptance criteria

- [ ] Milestone DoD verified against a live bex before closing.
- [ ] All tasks `status: done`, files under `done/`, milestone moved to `.pm/w7/done/m45/`, workstream checkbox `- [x]`.

## Out of scope

- Any further KeyValue update work (file as new inbox notes/milestones).
