---
id: w7/m45/t004
title: Simplify the changed Key Value update path
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m45/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (the Key Value update PATCH decode + patch struct + shared memory-policy/ip-allow-list apply helpers) for reuse, simplification, and efficiency — behavior-preserving only.

## Context

- Changed surface: `lego/backend/internal/keyvalue/rest.go`, `service.go`, and the shared memory-policy normalization + ip-allow-list apply helpers touched in t001/t002.
- Watch for reuse: t002 deliberately routes the main-PATCH allow-list through the same helper as the `PUT …/ip-allow-list` route — confirm there is genuinely one apply path, not two that merely agree by coincidence.

## Steps

1. Invoke `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving cleanups; re-run `cd lego/backend && go build ./... && go test ./...`.

## Files

- `lego/backend/internal/keyvalue/rest.go`, `service.go` and shared helpers changed in t001/t002

## Acceptance criteria

- [ ] `/simplify` run over the milestone diff; behavior-preserving cleanups applied.
- [ ] `go build ./...` and `go test ./...` pass in `lego/backend`.

## Out of scope

- Behavior changes or new features.
