---
id: w7/m45/t003
title: "Render parity: KeyValue update memory-policy + IP allow-list across REST/GraphQL/MCP/dashboard"
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m45/t001, w7/m45/t002]
---

## Objective

Verify the memory-policy + IP-allow-list update behavior fixed in t001/t002 is consistent across every surface it exposes — REST, GraphQL, and MCP in `lego/backend/`, plus the `dashboard/` UI — with the same fields, semantics, and error shapes, and compare against render.com's Key Value update behavior. Flag any drift as follow-up rather than silently diverging.

## Context

- REST is the fixed path (t001/t002). The shared core is `UpdateKeyValue` (`lego/backend/internal/keyvalue/service.go`).
- GraphQL: `updateKeyValue` / `renameKeyValue` (and any `setKeyValueIpAllowList`) resolvers.
- MCP: `update_key_value` / `rename_key_value` (and any `set_key_value_ip_allow_list`) tools.
- Dashboard: the Key Value settings/networking panels (memory policy + IP allow-list editors).
- bex-api is meant to be Render-compatible ([docs/ADR006-bex-api.md](../../../docs/ADR006-bex-api.md)); parity ledger is [docs/ADR018-render-parity.md](../../../docs/ADR018-render-parity.md); KV design is [docs/ADR021-keyvalue-management.md](../../../docs/ADR021-keyvalue-management.md).

## Steps

1. Grep the GraphQL + MCP + dashboard KV-update surfaces; confirm memory-policy and IP allow-list (set/replace/clear) are settable on each and share `UpdateKeyValue`'s semantics after t001/t002 (no surface still dropping a field).
2. Compare field names / enum values / error shapes against Render's Key Value update API; note any drift.
3. Update the affected rows in `docs/ADR018-render-parity.md`, `docs/ADR021-keyvalue-management.md`, and `docs/cli-compatibility-checklist.md` (the `keyvalues update --memory-policy | --ip-allow-list | --clear-ip-allow-list` lines flip from silent-no-op to working); file any residual drift as a follow-up note.

## Files

- `lego/backend/` GraphQL + MCP KeyValue resolvers/tools
- `dashboard/` Key Value settings/networking components
- `docs/ADR018-render-parity.md`, `docs/ADR021-keyvalue-management.md`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] REST, GraphQL, MCP, and dashboard all expose KV memory-policy + IP allow-list update with consistent fields/semantics/error shapes.
- [ ] Comparison against Render documented; any drift flagged as follow-up, not silently diverged.
- [ ] Parity ledger + checklist rows updated to reflect the fix.

## Out of scope

- New tests (t005).
- Simplification passes (t004).
