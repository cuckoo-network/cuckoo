---
id: w7/m72/t005
title: activator http.Server timeouts + MaxHeaderBytes
worker: worker7
status: done
estimate: 20m
depends_on: []
---

## Objective

Replace the activator's zero-timeout `http.ListenAndServe` with a configured `http.Server` so slow-header clients cannot hold connections indefinitely (codex-security #21, low).

## Context

- `lego/operator/cmd/activator/main.go:66/81-87` — `http.ListenAndServe(addr, nil)`; no `ReadHeaderTimeout`/`ReadTimeout`/`WriteTimeout`/`IdleTimeout`.
- Compare pg-sni-proxy/kv-sni-proxy internal metrics servers which set `ReadHeaderTimeout`.

## Steps

1. Build `&http.Server{Addr, Handler, ReadHeaderTimeout, ReadTimeout, WriteTimeout, IdleTimeout, MaxHeaderBytes}` and `srv.ListenAndServe()`.

## Files

- `lego/operator/cmd/activator/main.go`

## Acceptance criteria

- [ ] All four timeouts + `MaxHeaderBytes` set; slow-header connections dropped.

## Out of scope

- t002 (the per-request List cost) — separate task.
