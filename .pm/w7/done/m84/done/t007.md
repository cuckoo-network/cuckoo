---
id: w7/m84/t007
title: Closeout
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m84/t005, w7/m84/t006]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

The DoD is fully repo-verifiable via envtest — no production access required. The one clause needing care is "no pod rollout on existing production Deployments", which is proven by template-hash stability (t002/t006), not by watching prod.

## Steps

1. Verify the DoD holds, clause by clause.
2. Set every remaining task `status: done`; move each `tNNN.md` to `.pm/w7/m84/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in this README.
4. Move the whole `.pm/w7/m84/` directory to `.pm/w7/done/m84/` (leave no tombstone at the old path).
5. Check `- [x]` for m84 in `.pm/w7/README.md` with a one-line outcome.
6. Confirm `.pm/w7/done/028.md` records that this milestone superseded it.
7. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m84/**`, `.pm/w7/README.md`, `.pm/w7/done/028.md`

## Acceptance criteria

- [x] DoD verified clause by clause, not assumed.
- [x] Milestone moved to `.pm/w7/done/m84/`; nothing left at the old path.
- [x] Status consistent in all three places.

## DoD, clause by clause

| clause | verdict |
| --- | --- |
| "A second reconcile … issues zero writes" | Asserted for eight shapes across all three controllers by `divergence()`, which counts every `Create`/`Update`/`Patch`/`Delete` the reconcile makes through a wrapped client. One named exemption: the owner's own `status` subresource ([`033`](../033.md)). |
| "every owned object's `resourceVersion` is stable" | Asserted as the second half of the same helper. Weaker than the write count — it was already true before the fix, which is the milestone's headline finding — so it rides along rather than standing alone. |
| "`controllerutil.CreateOrUpdate` returns `OperationResultNone`" | Asserted literally, through the reconciler's own Deployment call. |
| "an invariant covering all three controllers that turns red if a future projection drifts" | `reconcile_convergence_envtest_test.go`, in the CI-enforced `make test` path, with an anti-tautology spec that drops one server-default family at a time and proves the recorder sees the PUT each time — and a per-shape owned-kinds assertion so a shape cannot pass by owning nothing. |
| "No pod rollout is triggered on existing production Deployments" | Proven, not argued: a Deployment stored from the **pre-fix** projection has today's projection applied to it, and the stored pod template is `Equal` with an unmoved `resourceVersion`. The pod-template hash is a pure function of the stored template. |

`make test` and `make lint` (all four modules) are green; codegen produces no diff.

## Out of scope

- Closing m82, m83, m85, m86.
