---
id: w7/m32/t004
title: REST/GraphQL/MCP with the structured `{cidrBlock, description}` shape
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m32/t002]
---

## Objective

The field on create + PATCH + read across the three adapters in Render's exact placement (`serviceDetails.ipAllowList`) and structured item shape; CIDR-validated (junk ⇒ named 400); named rejection on private/cron types per the spec's placement.

## Files

- `lego/backend/internal/apps/{service,rest,graphql,mcp}.go` (+ `AppView`)

## Acceptance criteria

- [ ] Round-trip + validation + type rejection identical on all three surfaces (adapter-parity test).
