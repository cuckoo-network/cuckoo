---
id: w7/m32/t002
title: CRD field + Traefik `ipAllowList` middleware on the app Ingress
worker: worker7
status: done
estimate: 60m
depends_on: [w7/m32/t001]
---

## Objective

`spec.ipAllowList [{cidrBlock, description}]` on the App CRD; the reconciler mints/attaches a Traefik `ipAllowList` middleware to the service's Ingress (all hosts) when non-empty, removes it when cleared — the w7/m5 KV mechanism, HTTP flavor. Mind `X-Forwarded-For`/real-IP handling behind the LB (Traefik `ipStrategy` — get the client IP, not the LB's, or the allowlist is useless; verify against the prod Traefik/hcloud LB proxy-protocol setup).

## Context

- KV precedent: `lego/operator/internal/controller/` KeyValue ipAllowList middleware (w7/m5); managed-PG equivalent (w1/m17).
- The LB/real-IP question is the landmine — test with a real external vantage point, not just in-cluster curl.

## Files

- `lego/types/v1alpha1/app_types.go` (+ codegen), `lego/operator/internal/controller/app_controller.go`

## Acceptance criteria

- [ ] Envtest: non-empty list ⇒ middleware attached to every host rule; cleared ⇒ removed; real-IP strategy configured and justified in-code.
