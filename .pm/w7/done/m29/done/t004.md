---
id: w7/m29/t004
title: Record drill outcomes + re-drill cadence; fix runbook steps
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m29/t001, w7/m29/t002, w7/m29/t003]
---

## Objective

Fold the three drills back into the docs: a dated drill-record entry per chain (outcome, environment, deviations), every wrong runbook step corrected in place, and a re-drill cadence set (ADR031 already reserves the section).

## Files

- `docs/ADR031-platform-data-backup.md` (+ `docs/ADR011-etcd-backup-restore.md` / `docs/ADR015-openbao-backup-restore.md` where steps were wrong)

## Acceptance criteria

- [x] Three dated drill records exist; no known-wrong step remains in any runbook; prettier clean.

## Changes made

### ADR011 (etcd)
- Fixed `docker run` commands to use `--entrypoint /usr/local/bin/etcdutl` (image has no PATH-resolved `etcdutl`)
- Fixed `etcdctl` calls inside the throwaway container to use `/usr/local/bin/etcdctl`
- Added explanation that `etcdutl` requires ≥3.6.x image and the restore must use the cluster's actual image
- Updated "What was tested" with the 2026-07-14 drill record

### ADR015 (OpenBao)
- Added separate "Same-instance restore" and "Fresh-node restore" sections
- Fresh-node path now includes `--force` flag, pod restart step, and unseal-with-old-keys clarification
- Removed incorrect suggestion to run `bao-init.sh` before restoring
- Added tested confirmation note

### ADR031 (consolidated)
- Updated "last verified" column in the summary table for all three stores
- Fixed bex-db runbook step 0 to use `-U postgres` instead of `-U bex`
- Fixed S3 path in step 1 to `s3://$TF_STATE_BUCKET/bex-db/bex-db/` and changed `.catalog.json` to `backup.info`
- Fixed step 4 (setup) to use correct nested path
- Added CNPG 1.31+ migration warning
- Wrote three full dated drill records
- Updated re-drill cadence table with last-drilled dates and forward triggers
