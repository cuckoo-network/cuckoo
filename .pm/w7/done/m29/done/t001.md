---
id: w7/m29/t001
title: etcd snapshot → restore drill (mock cluster)
worker: worker7
status: done
estimate: 60m
depends_on: []
---

## Objective

Execute the ADR011/ADR031 etcd restore runbook end-to-end against a disposable environment: take (or fetch) a real nightly snapshot, restore it into a fresh control plane, and verify the cluster state (Apps, CRs) survives. Prod untouched.

## Context

- Runbooks: `docs/ADR011-etcd-backup-restore.md`, consolidated in `docs/ADR031-platform-data-backup.md`.
- Nightly snapshots land in Hetzner Object Storage; the mock cluster is `bash scripts/mock-cluster.sh` (CAPD).
- The mock cluster's etcd differs from prod's (kind vs kubeadm-on-Hetzner) — note any step that cannot be faithfully drilled locally and say so in the record rather than pretending.

## Steps

1. Walk the runbook literally, on the mock (or a throwaway kind) cluster — no improvised steps.
2. Log every deviation between the doc and reality.
3. Verify restored state: CRs present, operator reconciles.

## Files

- `docs/ADR011-etcd-backup-restore.md`, `docs/ADR031-platform-data-backup.md` (corrections land in t004)

## Acceptance criteria

- [x] A restore completed from a real snapshot; deviations list captured for t004.

## Outcome (2026-07-14)

Executed against the live CAPD 2-node cluster (bex-btmph-wzrbc control-plane, kindest/node:v1.36.1, etcd 3.6.8-0). Created `drill-test-app` App CR with label `bex.co/drill: w7-m29-2026-07-14`, took a live etcd snapshot (5.4 MB / 550 KB gzipped), restored via `etcdutl snapshot restore` + throwaway `etcd` container, verified App CR and jq pipeline. Key deviation: runbook pinned `3.5.15-0` but `etcdutl` requires `3.6.x+`; corrected in ADR011 + CronJob.

## Out of scope

- Prod execution; changing the backup CronJob itself.
