---
id: w7/m29/t002
title: OpenBao Raft snapshot → restore drill
worker: worker7
status: done
estimate: 45m
depends_on: []
---

## Objective

Execute the ADR015/ADR031 OpenBao restore runbook: restore a real Raft snapshot into a fresh OpenBao instance, unseal it, and verify tenant env-var secrets read back.

## Context

- Runbooks: `docs/ADR015-openbao-backup-restore.md`, `docs/ADR031-platform-data-backup.md`.
- Unseal keys live in `.env` (never print/commit); prod runs a 2-member raft until the quota raise (see `.pm/FUTURE-MAYBE.md`).

## Steps

1. Restore the snapshot into a disposable OpenBao (local container or mock cluster), unseal, read a known secret path.
2. Log doc-vs-reality deviations for t004.

## Acceptance criteria

- [x] A known `tenants/*` secret reads back from the restored instance; deviations captured.

## Outcome (2026-07-14)

Fresh-node path drilled using two local `openbao/openbao:2.5.5` Docker containers. Wrote `tenants/default/drill-test-app/DATABASE_URL` (drill_marker: w7-m29-2026-07-14), took Raft snapshot, force-restored to a fresh instance, restarted pod, unsealed with original keys → secret confirmed present. Key deviations: `--force` flag required (plain restore returns 403 with different master key); restart required after force restore; runbook corrected in ADR015.

## Out of scope

- Prod execution; k8s-auth re-wiring beyond what the runbook covers.
