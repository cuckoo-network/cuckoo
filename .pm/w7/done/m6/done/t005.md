---
id: w7/m6/t005
title: Simplify — /simplify over the code this milestone changed
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m6/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — the store host-uniqueness lookup/index, the `AddDomain` collision + reserved-host guards, the reserved-host predicate, and the three adapters' error surfacing — reuse / simplification / efficiency, behavior-preserving.

## Context

- Scope is exactly the files touched by t001–t003.
- `/simplify` is quality-only (no bug hunting — that is `/code-review`).
- Watch for the same-App duplicate check and the cross-App uniqueness check being foldable into one host-lookup path.

## Steps

1. Run `/simplify` over the milestone's changed files.
2. Apply behavior-preserving cleanups; keep tests green (`cd lego/backend && go test ./...`).

## Files

- The files changed by t001–t003 (store, `domains.go`, adapters, dashboard).

## Acceptance criteria

- [ ] `/simplify` run over the diff; behavior-preserving cleanups applied.
- [ ] Backend + dashboard build and existing tests still pass.

## Out of scope

- New behavior; new tests (t006).
