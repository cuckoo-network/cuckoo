---
id: w7/m6/t004
title: Render parity — collision + reserved rejection shape/semantics vs Render's "domain already in use"
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m6/t003]
---

## Objective

Confirm the collision + reserved-host rejection this milestone adds is consistent across REST, GraphQL, and MCP (same status/error shape/message) and matches Render's behavior — Render blocks a duplicate domain with "This domain already exists on another site." Flag any drift as follow-up rather than diverging silently.

## Context

- bex-api is meant to be Render-compatible (`docs/ADR006-bex-api.md`); update the custom-domain row in `docs/ADR018-render-parity.md` to record the duplicate-domain block now that it exists.
- Render performs **no** standalone ownership challenge — do not add one; parity here is specifically the duplicate/"in use" block (Render) plus bex's reserved-host guard (a bex-specific superset, note it as such).
- Surfaces to cross-check: `rest.go`, the GraphQL add-domain resolver, the MCP add-domain tool, and the dashboard form.
- Render's create-custom-domain API is `POST /v1/services/{serviceId}/custom-domains` with a `name` field; its documented error codes include 400 **and** 409 (the spec doesn't pin which the duplicate uses) — pin bex's status against Render's actual response and pick 400 or 409 to match.
- The existing `DomainView`/`renderCustomDomain` (`domains.go:32-55`, `276-286`) already mirror Render's `customDomain` schema (`verificationStatus`/`serverStatus`/`domainType`) — confirm t001–t003 left those fields byte-unchanged.

## Steps

1. Diff the rejection status + error shape across REST/GraphQL/MCP; assert consistency.
2. Pin the duplicate-domain HTTP status against Render's real response (400 vs 409) and align bex; compare the message against Render's "already exists on another site."
3. Confirm the success-path `DomainView` (`verificationStatus`/`serverStatus`/`domainType`/`dnsRecord`) is unchanged — no field added/renamed by this milestone.
4. Record the two known divergences explicitly in the parity ledger (not as silent drift): (a) reserved-host guard is a bex superset — Render reserves its own `*.onrender.com` similarly but has no dashboard/base-apex analog; (b) bex's collision is **per-host** because bex omits Render's www↔apex auto-pairing (`domains.go:270-275`), so Render's uniqueness covers the paired sibling and bex's does not.
5. Update the `docs/ADR018-render-parity.md` custom-domain row with the duplicate-domain-block evidence + the two divergence notes.

## Files

- `docs/ADR018-render-parity.md` (ledger row update)
- read-only cross-check of the three adapters + dashboard

## Acceptance criteria

- [ ] Rejection status + error shape are consistent across REST/GraphQL/MCP; any divergence documented or filed.
- [ ] Duplicate-domain behavior compared against Render; reserved-host guard recorded as an intentional bex superset; parity ledger updated.

## Out of scope

- Behavior changes (t001–t003); Simplify (t005); tests (t006).
