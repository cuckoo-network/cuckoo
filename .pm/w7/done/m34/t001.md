---
id: w7/m34/t001
title: "Capture Render's rate-limit headers (live + pinned spec) — or record absence"
worker: worker7
status: done
estimate: 30m
depends_on: []
---

## Objective

Pin the contract before writing code: inspect real api.render.com responses (an authenticated GET with a valid key — headers only, no data needed) and the pinned OpenAPI (`lego/backend/internal/api/testdata/render-openapi.json`) for rate-limit response headers (candidate spellings: `RateLimit-Limit`/`-Remaining`/`-Reset` or the draft-RFC combined `RateLimit` header). Record names, which responses carry them, and reset semantics (epoch vs delta). **If Render ships none**, record that with the captured evidence — matching Render's absence is also parity, and the milestone then closes at t007 without t002–t003.

## Files

- `docs/render-artifacts/rate-limit-headers.md` (the capture)

## Acceptance criteria

- [ ] Dated header dump from a real Render response + spec grep; either the exact contract to implement or a parity-by-absence finding.

## Out of scope

- Any bex change.
