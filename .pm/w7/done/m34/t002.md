---
id: w7/m34/t002
title: "Emit the captured header set from the token-bucket middleware"
worker: worker7
status: done
estimate: 40m
depends_on: [w7/m34/t001]
---

## Objective

Emit t001's exact header set on every authenticated response from the existing per-caller token bucket (`lego/backend/internal/api/ratelimit.go`): limit from `BEX_RATE_LIMIT`, remaining from the bucket's current tokens, reset per the captured semantics. `BEX_RATE_LIMIT=0` (disabled) ⇒ no headers (never fake an unlimited limit). Skip this task entirely if t001 recorded absence.

## Files

- `lego/backend/internal/api/ratelimit.go`

## Acceptance criteria

- [ ] Headers on 2xx/4xx authenticated responses with live bucket values; disabled ⇒ absent; 429 unchanged plus the headers.

## Out of scope

- Per-route differentiated limits (bex has one bucket per caller by design).
