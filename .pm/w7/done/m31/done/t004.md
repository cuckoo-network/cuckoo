---
id: w7/m31/t004
title: Custom Domains section toggle
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m31/t003]
---

## Objective

A platform-subdomain toggle in the service's Custom Domains section showing the `<slug>.onbex.co` host with its on/off state, disabled-with-reason when the guard applies (no custom domain), matching wherever Render surfaces this control per t001. Read `dashboard/CLAUDE.md` first.

## Files

- `dashboard/src/features/` (custom domains), generated definitions

## Acceptance criteria

- [x] Toggle round-trips in a real browser; guard state renders with its reason; `yarn test` green.
