---
id: w7/m31/t001
title: Capture Render's guard semantics
worker: worker7
status: done
estimate: 30m
depends_on: []
---

## Objective

Pin the edge behavior before building: on Render, what happens when you set `disabled` on a service with no custom domain (refused? allowed-but-warned? service unreachable)? Does the policy apply beyond web services? What does the service's `url` field show when disabled? Record in `docs/render-artifacts/render-subdomain-policy.md` (docs + OpenAPI; live dashboard capture if reachable).

## Acceptance criteria

- [x] Capture answers the no-custom-domain guard, type scope, and `url` behavior; names bex's planned mirror/divergence for each.
