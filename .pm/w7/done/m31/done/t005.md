---
id: w7/m31/t005
title: Activator + static-resolver honor the absent host
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m31/t002]
---

## Objective

Sweep the other host consumers for stale platform-host assumptions: the wake activator's host→App mapping, the static-server's host→site resolver, and the domains collision guard (a disabled platform host is still RESERVED — nobody else may claim it). Fix what breaks; record what was already fine.

## Files

- `lego/operator/internal/staticserver/resolver.go`, activator host mapping, `lego/backend/internal/apps/domains.go` (reserved-host check)

## Acceptance criteria

- [x] Sleeping-service wake and static serving both behave with the platform host disabled; the slug stays unclaimable by other tenants (test).
