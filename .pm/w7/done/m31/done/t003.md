---
id: w7/m31/t003
title: REST/GraphQL/MCP with Render's exact enum
worker: worker7
status: done
estimate: 40m
depends_on: [w7/m31/t002]
---

## Objective

`renderSubdomainPolicy` (exact name, enum `enabled|disabled`, unknown value ⇒ named 400) on create + PATCH + read across the three adapters, with the t001-captured guard for the no-custom-domain case enforced in the core verb (one place, all surfaces inherit).

## Files

- `lego/backend/internal/apps/{service,rest,graphql,mcp}.go` (+ `AppView`)

## Acceptance criteria

- [x] Round-trip + guard identical on all three surfaces (adapter-parity test); invalid enum 400s.
