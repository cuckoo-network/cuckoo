---
id: w7/m62/t001
title: Pinned govulncheck CI job over the Go workspace + baseline triage + gate policy (013)
worker: worker7
status: done
estimate: 45m
depends_on: []
---

## Objective

Wire `govulncheck` as a CI job over the Go workspace (`lego/types` + `lego/operator` + `lego/backend`) — the source-level, call-graph-aware complement to Trivy (images) and gitleaks (commits). Pin the tool by version + sha256, baseline-triage current findings before enabling the gate, and document the gate policy next to the job: fail when a reachable vuln has a fixed version available, warn otherwise.

## Context

- `w7/013` (retiring into this task) scoped this: `govulncheck` reports only vulns in **reachable** code, so it's low-false-positive and safe to gate.
- Pinning pattern: the gitleaks/trivy steps in `.github/workflows/deploy.yml` (version + sha256, never `@latest`).
- Home: own workflow or a step in the existing Go workflows (`operator-test.yml`/`backend-test.yml`) — pick by runtime cost; it must run on pushes/PRs touching `lego/**`.
- If the baseline surfaces vulns needing **breaking** dependency upgrades, do not absorb them here — file them (the `w1/006` Dependabot-triage handling pattern) and gate at warn for those until resolved.

## Files

- `.github/workflows/` (new or existing workflow)
- A short policy note next to the job (workflow comment or `docs/` pointer)

## Acceptance criteria

- [ ] CI runs pinned `govulncheck` over all three modules on relevant pushes/PRs.
- [ ] Gate policy documented; baseline triaged (findings listed + dispositioned, or clean).

## Out of scope

- Executing breaking dependency upgrades the baseline may surface (file, don't absorb).
