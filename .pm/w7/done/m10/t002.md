---
id: w7/m10/t002
title: "HTTP hardening headers on bex-api"
worker: worker7
status: done
estimate: 25m
depends_on: []
---

## Objective

Add a response-header middleware setting standard hardening headers on bex-api's raw `http.Server`.

## Context

- `.pm/w7/002.md` — the source note.
- `lego/backend/cmd/api/main.go` (raw `http.Server` setup, no header middleware today per the note) / `lego/backend/internal/api/server.go`.
- Headers: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Strict-Transport-Security` (HSTS, prod/TLS only), a conservative default `Content-Security-Policy`.

## Steps

1. Add a middleware setting the header set on every response.
2. Gate HSTS behind TLS/prod (don't set it on plain-HTTP local/dev).

## Files

- `lego/backend/cmd/api/main.go` or `lego/backend/internal/api/server.go`

## Acceptance criteria

- [ ] Every bex-api response carries the header set; HSTS only appears when serving over TLS/prod.

## Out of scope

- Dashboard SSR headers — t003.
- Health/webhook rate-limiter exemptions — unrelated, every response gets these headers regardless.
