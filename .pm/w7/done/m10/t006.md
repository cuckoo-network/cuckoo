---
id: w7/m10/t006
title: "Test coverage: header middleware + scan-gate config"
worker: worker7
status: done
estimate: 25m
depends_on: [w7/m10/t004]
---

## Objective

Add meaningful tests/checks for the header middleware on both surfaces and, where feasible, a CI-checkable assertion for the scan-gate configuration.

## Steps

1. A test asserting bex-api responses carry the expected headers (and that HSTS is absent over plain HTTP).
2. A test/check asserting dashboard SSR responses carry the expected headers.
3. Document the scan-gate threshold behavior if it isn't unit-testable (workflow-level, not code-level).

## Acceptance criteria

- [ ] Both header middlewares have a test asserting the real header set, not a trivial existence check.
