---
id: w7/m10/t001
title: "Trivy scan step in deploy.yml"
worker: worker7
status: done
estimate: 30m
depends_on: []
---

## Objective

Add a trivy scan step to `.github/workflows/deploy.yml` after the image build+push: scan the pushed bex image (and dashboard image) for CVEs — warn on HIGH, fail on CRITICAL.

## Context

- `.pm/w7/001.md` — the source note.
- `.github/workflows/deploy.yml` — the build+push workflow.
- Adjacent, not duplicated: `w1/006`/`w1/018` (Dependabot triage) covers the dependency graph; this covers the built artifact.

## Steps

1. Add a trivy scan action step after the image push, targeting the pushed image tag(s) for both bex and dashboard images.
2. Configure severity gating: fail on CRITICAL, warn (non-blocking) on HIGH.
3. Run once to get a baseline; tune the threshold if the baseline is noisy.

## Files

- `.github/workflows/deploy.yml`

## Acceptance criteria

- [ ] The workflow scans both images and fails the run on a CRITICAL finding.
- [ ] HIGH findings are reported but don't block.

## Out of scope

- Dependency-graph scanning (Dependabot's job, `w1/006`).
