---
id: w7/m10/t003
title: "HTTP hardening headers on dashboard SSR"
worker: worker7
status: done
estimate: 25m
depends_on: [w7/m10/t002]
---

## Objective

Add the equivalent hardening header set to dashboard SSR responses, tuned so the CSP doesn't break the dashboard's own asset loading — the real browser surface, so this needs more care than t002's bex-api middleware.

## Context

- `.pm/w7/002.md` — the source note.
- Dashboard is TanStack Start SSR; find its response/middleware layer (`dashboard/src/` server entry or SSR handler).

## Steps

1. Add the same header set (nosniff, frame-deny, HSTS behind TLS/prod, CSP) to SSR responses.
2. Tune the CSP against the dashboard's actual asset origins (self-hosted assets, any external font/script sources) — verify nothing breaks in a real browser load.

## Files

- dashboard SSR entry/middleware (TanStack Start server config)

## Acceptance criteria

- [ ] Dashboard SSR responses carry the header set.
- [ ] The dashboard loads correctly in a real browser under the new CSP (no console CSP violations breaking functionality).

## Out of scope

- bex-api headers — t002.
