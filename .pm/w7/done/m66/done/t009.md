---
id: w7/m66/t009
title: Test coverage — reconciler fact-emission + events vocabulary + dashboard catalog/rendering + parity guards
worker: worker7
status: done
estimate: 60m
depends_on: [w7/m66/t007]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — asserting real emission, ordering, idempotency, redaction, and cross-surface presence — never tautological or snapshot-everything coverage.

## Context

- Backend precedents: `internal/store/reconciler_test.go` + `internal/store/event_facts_test.go` (fact emission), `internal/events/*` parity tests, `internal/api/events_surface_test.go` (`TestEventSurfaceParity`), and the vocabulary guard `TestEveryTargetedVerbIsNamedOrExcused`.
- Acceptance harness: `scripts/events-verify.sh` (reconstructs the feed across cursored pages).
- Dashboard precedents: `dashboard/src/routes/__tests__/services.$serviceId.events.test.tsx`, `.../services.$serviceId.metrics.test.tsx`, `features/events/components/__tests__/event-timeline.test.tsx`.

## Steps

1. Reconciler tests: build/pre-deploy/job facts emitted once per lifecycle, correct `details.status`, idempotent across resync, none emitted when the phase is absent (image-backed / no pre-deploy).
2. Vocabulary/surface tests: new types present + identical across REST/GraphQL/MCP; redaction (no arbitrary value) still holds.
3. Dashboard tests: new types render with icon/label/badge and appear in the filter + as marker labels.
4. Extend `scripts/events-verify.sh` to cover a build-from-repo timeline including the new beats.

## Files

- `lego/backend/internal/store/reconciler_test.go`, `.../event_facts_test.go`
- `lego/backend/internal/api/events_surface_test.go`
- `dashboard/src/routes/__tests__/services.$serviceId.events.test.tsx` (+ metrics test)
- `scripts/events-verify.sh`

## Acceptance criteria

- [ ] Tests fail if any new type is dropped from a surface, mis-ordered, double-emitted, or leaks a non-typed value — i.e. they assert real failure modes.
- [ ] `cd lego/backend && go test ./...` + `yarn test` green in CI.

## Out of scope

- Closeout move (t010).
