---
id: w7/m66/t010
title: Closeout — move to done/ when the DoD holds
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m66/t009]
---

## Objective

Close the milestone once its definition of done is actually met (the new event types are emitted, rendered, cross-surface-consistent, documented, and tested), not merely when the code is written.

## Steps

1. Verify every DoD clause (t001–t009) is real — build/pre-deploy/job events observable on a real deploy, dashboard + REST/GraphQL/MCP consistent, docs reconciled, guards green.
2. Set every remaining task `status: done`; mark each row `— **DONE**` and `**Status:** done` in the milestone README.
3. Move each `tNNN.md` to `w7/m66/done/`; move the whole `w7/m66/` to `w7/done/m66/`; check `- [x] **m66**` in `.pm/w7/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [ ] Milestone lives at `.pm/w7/done/m66/` with all tasks done and the workstream checkbox ticked; DoD observably met.

## Out of scope

- Any residual divergence found in t007 (filed as its own inbox note / follow-up).
