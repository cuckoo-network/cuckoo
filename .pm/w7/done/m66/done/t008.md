---
id: w7/m66/t008
title: Simplify — /simplify over the milestone's diff
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m66/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — the reconciler fact-emission additions, the events vocabulary, and the dashboard catalog/rendering — for reuse, simplification, and efficiency, behavior-preserving.

## Context

- Likely simplification targets: the three near-identical fact-emission blocks in `reconciler.go` (build/pre-deploy/job all follow the `image_pull_failed` shape — factor a shared idempotent-emit helper if it reads cleanly), and the dashboard catalog entries (shared icon/label mapping for the ended-with-status types).

## Steps

1. `/simplify` over the milestone diff.
2. Apply behavior-preserving cleanups; re-run backend + dashboard tests.

## Files

- Whatever t001–t006 touched.

## Acceptance criteria

- [ ] No behavior change; `cd lego/backend && go test ./...` and `yarn test` still green; `make lint-backend` clean.

## Out of scope

- New functionality or new tests (t009).
