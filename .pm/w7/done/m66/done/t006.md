---
id: w7/m66/t006
title: Reconcile the stale omission vocabulary — service.go package doc, service-events.md, ADR018 non-goal rows
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m66/t001, w7/m66/t002, w7/m66/t003, w7/m66/t004]
---

## Objective

Bring bex's own documentation of the events vocabulary back in line with reality now that build/pre-deploy/job (and maybe branch_deleted) are supported, and restate the remaining true non-goals precisely. bex's honest-omission discipline is a feature; this task keeps it honest.

## Context

- `internal/events/service.go` package doc: the vocabulary table (`service.go:33-95`) and the `## Omissions` note (`service.go:86-94`) — the latter currently claims `build_started/build_ended` "have no accepted bex mechanism or durable source," which t001 falsifies.
- Render parity artifact: `docs/render-artifacts/service-events.md` (records the 19-of-31 truthful subset + the 12 documented omissions: preview/workflow, provider-maintenance, edge-cache, pipeline-minute, maintenance-deploy).
- Parity ledger: `docs/ADR018-render-parity.md` "Service events" row (currently ◐).
- Render's authoritative superset (list-events OpenAPI + webhooks reference) — the still-true non-goals after this milestone: `disk_created/updated/deleted` (no persistent-disk product), `artifact_fetch_failed`/`artifact_source_changed` (no artifact-source deploys), `server_hardware_failure` + `maintenance_started`/`_ended` (provider infra, no source), `pipeline_minutes_exhausted` (no pipeline-minutes billing), `zero_downtime_redeploy_started`/`_ended` (bex rolling deploys — map-to-deploy or omit, state which), `initial_deploy_hook_started`/`_ended` (no setup-hook distinct from pre-deploy), preview/workflow (product anti-goal).

## Steps

1. Update the `service.go` vocabulary table to list the new Render-named types and delete the falsified build clause from `## Omissions`; keep the honest list for the genuine non-goals above.
2. Update `service-events.md`: bump the supported/total count, move build/pre-deploy/job (+ branch_deleted per t004) into the supported table, and list the remaining non-goals with one-line reasons.
3. Update the ADR018 "Service events" row evidence (and flip ◐→✅ if the remaining gaps are all documented non-goals).

## Files

- `lego/backend/internal/events/service.go` (doc comment only)
- `docs/render-artifacts/service-events.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] No doc claims a source-less absence that the code now contradicts.
- [ ] The supported/non-goal split matches what t001–t004 actually shipped; each non-goal has a concrete reason.
- [ ] `npx prettier@3.4.2 --write "**/*.md"` clean.

## Out of scope

- The live-diff vs Render's rendered page + cross-surface consistency check (t007).
