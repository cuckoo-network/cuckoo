---
id: w7/m66/t004
title: branch_deleted event from the GitHub App branch-delete webhook (or document as a scoped non-goal)
worker: worker7
status: done
estimate: 45m
depends_on: []
---

## Objective

Add Render's `branch_deleted` event — fired when the tracked git branch is removed, disabling auto-deploys — **if** bex has a clean, durable signal for it. bex today has `branch_changed` (a bex extension) but no `branch_deleted`. This is the least-certain gap of the milestone: it is an investigate-then-decide task. If the GitHub App webhook (ADR026) delivers a branch-delete signal bex can key on against a service's tracked branch, emit the event; if it does not, record `branch_deleted` as a documented non-goal with the exact reason, rather than leaving it silently absent.

## Context

- GitHub App integration + webhook intake: ADR026, `internal/apps/webhook.go`, `POST /v1/webhooks/git` (`BEX_GITHUB_WEBHOOK_SECRET`). GitHub's `delete` webhook event fires on branch/tag deletion (`ref_type: branch`).
- Existing branch event: `internal/events/service.go` `TypeBranchChanged = "branch_changed"` (`service.go:137`) — an `apps.SetSource`-adjacent bex-named type, not Render's `branch_deleted`.
- Render semantics: `branch_deleted` = "Git branch was removed, disabling auto-deploys" (webhooks reference).
- Emission: an observed webhook fact (reconciler/webhook path), not an audit verb.

## Steps

1. Check whether the git webhook already receives/parses GitHub `delete` events and whether the service's tracked branch is known at intake.
2. If yes: emit a `branch_deleted` fact keyed on (service, deleted-branch == tracked-branch); add the vocabulary entry.
3. If no clean signal: do **not** invent one — record `branch_deleted` in `service-events.md` + ADR018 as a non-goal with the precise reason (mirrors bex's honest-omission discipline).

## Files

- `lego/backend/internal/apps/webhook.go` (+ `internal/store`/`internal/events`) — only on the emit branch
- `docs/render-artifacts/service-events.md`, `docs/ADR018-render-parity.md` — on the non-goal branch (t006 folds the final wording either way)

## Acceptance criteria

- [ ] Either: deleting a service's tracked branch produces one `branch_deleted` event; or: `branch_deleted` is documented as a non-goal with a concrete why (no silent absence). The chosen branch is stated in the milestone closeout notes.

## Out of scope

- Reworking `branch_changed` (kept as-is). Build/pre-deploy/job events (t001–t003), dashboard (t005).
