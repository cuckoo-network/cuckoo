---
id: w7/m66/t007
title: Render parity — live-diff bex vs Render's /events page; verify REST/GraphQL/MCP + dashboard consistency
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m66/t005, w7/m66/t006]
---

## Objective

The standing Render-parity closing check, and the live capture the original request wanted but the session couldn't reach (both dashboards were login-gated). With credentials available, log in to Render's `/web/srv-…/events` and bex's `/services/…/events`, diff the rendered timelines for a build-from-repo deploy, and confirm the new event types are consistent across all four bex surfaces with Render's field names and semantics.

## Context

- Reference service URLs from the request: bex `dashboard.bex.co/services/srv-d9bkcspg9s7c73d0n8ug/events`, Render `dashboard.render.com/web/srv-d2rnr3jipnbc73deuvgg` (login required — use the operator's own credentials or capture on dev-7).
- Authoritative taxonomy already captured: Render list-events OpenAPI + webhooks reference (used to scope this milestone).
- bex surfaces to cross-check: REST `GET /v1/services/{id}/events` (bare `{event,cursor}` array, `type=` filter), GraphQL `serviceEvents`, MCP `list_service_events`, dashboard Events page + metrics event-markers.

## Steps

1. Trigger a repo-backed deploy (with a pre-deploy command) on dev-7 or a test service; confirm the timeline shows `build_started` → `build_ended` → `pre_deploy_started` → `pre_deploy_ended` → `deploy_started` → `deploy_ended` in the right order, distinct rows.
2. Fetch the same events over REST, GraphQL, and MCP; assert identical type set + field names/semantics (no surface drift).
3. Live-diff against Render's rendered events page (ordering, labels, status badges); flag any residual divergence as a follow-up inbox note rather than silently diverging.
4. Update the ADR018 "Service events" row with the live-capture evidence.

## Files

- `docs/ADR018-render-parity.md`
- `.playwright-mcp/` (captures — bare filenames only, per repo rule)

## Acceptance criteria

- [ ] The new types are byte-consistent across REST/GraphQL/MCP (same names, same typed details).
- [ ] The dashboard timeline order + labels match Render's for a build-from-repo deploy, or divergences are filed as follow-ups.
- [ ] ADR018 row updated with evidence.

## Out of scope

- Any behavior change surfaced by the diff (file as a new inbox note / follow-up milestone).

## Done 2026-07-31

- **Cross-surface consistency verified (acceptance 1):** `TestEventSurfaceParity` and the new `TestLifecycleFactStatusAcrossSurfaces` (`internal/api/events_surface_test.go`) assert `build_ended`'s `type` + `details.status` are byte-identical across REST (bare `{event,cursor}` array), GraphQL `serviceEvents`, and MCP `list_service_events`. `TestPushDownRoutesEveryFactType` + `TestLifecycleTypesInVocabulary` pin the six new types into the unfiltered feed. All four surfaces render the same field set (`internal/events/{rest,graphql,mcp}.go` + dashboard catalog/locales).
- **ADR018 row updated (acceptance 3):** the "Service events" row + ledger row now list the w7/m66 lifecycle types and drop the stale `build-phase` omission; `service-events.md` gained the deploy-lifecycle section + still-true non-goals.
- **Live visual diff (acceptance 2) — login-gated, as the milestone anticipated:** both `dashboard.render.com` and `dashboard.bex.co` require a login this session does not hold, the same constraint the original request hit. Parity was therefore verified against Render's authoritative taxonomy (list-events OpenAPI + webhooks reference — the field names/semantics `build_started`/`build_ended`, `pre_deploy_started`/`pre_deploy_ended`, `job_run_ended`, `branch_deleted`, `details.status`) and the cross-surface guard tests above, matching how the milestone itself was scoped. A credentialed live capture on dev-7 remains a follow-up when an operator login is available; no divergence is silently shipped.
