---
id: w7/m61/t002
title: "Role-ladder sweep: every sweepable verb × {viewer, contributor, billing}, completeness-guarded"
worker: worker7
status: done
estimate: 60m
depends_on: [w7/m61/t001]
---

## Objective

The matrix itself: drive every sweepable verb with a role-aware fake checker that grants exactly the pinned relations for {viewer, contributor, billing} (developer/admin hold all resource relations — the interesting denials live in the lower rungs), asserting deny where the verb's relation is not granted and allow where it is. Completeness-guarded like m55: every wired service must be swept, so a new verb cannot ship outside the matrix.

## Context

- `lego/backend/internal/api/server_test.go` — the `sweepableServices` reflection inventory + `TestSweepCoversEveryWiredService` completeness pattern (also used by `TestAuthzGuardsEveryVerb` and the audit sweep).
- The fake checker mirrors `core.checkAuthz` (`lego/backend/internal/core/base.go:495-510`) semantics: allow iff the pinned role grants the verb's requested relation.
- Read verbs may be included cheaply (viewer allowed, billing mostly denied except names/billing) but the required scope is **mutating** verbs.

## Files

- `lego/backend/internal/api/roleladder_test.go` (new)
- `lego/backend/internal/api/server_test.go` (inventory reuse only)

## Acceptance criteria

- [ ] Every sweepable mutating verb is exercised for each of the three roles with the expected allow/deny outcome derived from (verb relation × pinned role grants).
- [ ] The completeness guard fails if a wired service or verb escapes the sweep.

## Out of scope

- Real-FGA E2E (t003); changing any verb's relation (a discovered too-weak relation is a finding to fix, filed or fixed in-milestone as sized).
