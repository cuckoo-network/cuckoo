---
id: w7/m61/t003
title: "Real-OpenFGA E2E: one verb per relation class per role"
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m61/t002]
---

## Objective

Prove the ladder against the real deployed model, not just the pinned table: extend the real-OpenFGA E2E harness so each relation class is exercised per role end-to-end — contributor restart-allowed but create/delete-denied; billing sees billing surfaces but is denied resource mutations and sensitive reads; viewer read-allowed, mutation- and sensitive-denied. One representative verb per (relation class × role) cell keeps runtime bounded while the t002 sweep carries breadth.

## Context

- `lego/backend/internal/api/multiworkspace_e2e_test.go:54-293` — the existing real-FGA harness (`TestMultiWorkspaceTargetingE2E`) already writes role tuples and asserts viewer allow/deny on a sample; extend rather than duplicate.
- CI runs these against real ephemeral OpenFGA (`BEX_TEST_OPENFGA_URL`, `.github/workflows/backend-test.yml`) — not skipped.

## Files

- `lego/backend/internal/api/multiworkspace_e2e_test.go` (or a sibling `roleladder_e2e_test.go`)

## Acceptance criteria

- [ ] Contributor: operate-class verb 2xx, create-class verb 403 — via real FGA.
- [ ] Billing: billing read 2xx, resource mutation 403, sensitive read 403 — via real FGA.
- [ ] Viewer: read 2xx, mutation 403 (already partially covered — keep, don't duplicate).

## Out of scope

- Developer/admin positive paths beyond what existing tests cover.
