---
id: w7/m61/t001
title: Pin the role→relation grant table from model.fga in a test helper
worker: worker7
status: done
estimate: 45m
depends_on: []
---

## Objective

Encode the five-role → relation grant table (viewer, contributor, developer, admin, billing → the exact `can_*` relations each holds) as a test helper, pinned against `deploy/gitops/authz/model.fga` so the table cannot silently diverge from the deployed model: a model edit that changes any role's derived grants without updating the table turns the pin test red.

## Context

- `deploy/gitops/authz/model.fga:14-76` — the role definitions and derived-permission rules (`can_view`, `can_view_logs`, `can_operate`, `can_create`, `can_view_sensitive`, `can_manage_keys`, `can_manage_ssh_keys`, `can_manage`, `can_manage_billing`).
- `lego/backend/internal/core/base.go:88-110` — the relation constants the verbs use.
- Pinning options: parse the model file from the repo path in the test, or embed a checksum + the derived table with a regeneration note — pick whichever keeps the failure message actionable.

## Files

- `lego/backend/internal/api/` (new test helper, e.g. `roleladder_test.go` shared table)
- `deploy/gitops/authz/model.fga` (read-only reference)

## Acceptance criteria

- [ ] The helper exposes role→relations for all five roles, matching `model.fga` exactly.
- [ ] Editing a role's grants in `model.fga` without updating the table fails the pin test with a message naming the drift.

## Out of scope

- The sweep itself (t002); any change to the model or verb relations.
