---
id: w7/m47/t008
title: "Simplify"
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m47/t006, w7/m47/t007]
---

## Objective

Run `/simplify` over the code m47 changed — reuse, simplification, efficiency, altitude — behavior-preserving. No new behavior; no bug-hunting (that is `/code-review`).

## Context

- New surface: `lego/backend/internal/billing/*` (client, emitter), the two migrations, `usage_hourly`/emitter wiring, `.env`/CLAUDE.md docs.
- Watch for reuse against existing patterns: the retry/backoff + per-source health shape already exists elsewhere (FUTURE-MAYBE notes a shared `egressquery` health helper); the loop-beside-`usage.Service` startup pattern; store accessor conventions.

## Steps

1. `/simplify` over the m47 diff.
2. Apply behavior-preserving cleanups; keep the env-gated/byte-identical-when-off contract intact.

## Files

- The m47 diff (`lego/backend/internal/billing/*`, migrations, wiring).

## Acceptance criteria

- [ ] `/simplify` run; cleanups applied or consciously declined with a note.
- [ ] All tests still green; no behavior change.

## Out of scope

- New features / bug fixes.
