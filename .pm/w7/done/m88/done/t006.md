---
id: w7/m88/t006
title: "Render parity"
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m88/t005]
---

## Objective

Verify the clean-rebuild guarantee across every exposed deploy surface against Render's documented enum and manual-deploy behavior.

## Context

- docs/ADR006-bex-api.md defines shared business logic with REST/GraphQL/MCP adapters; dashboard/CLAUDE.md governs its UI consumer.
- ADR018 Trigger a deploy claims parity using a cache-free/no-op premise; update the evidence to match effective cache reset behavior.

## Steps

1. Enumerate relevant REST, GraphQL, MCP, dashboard, and official-CLI calls and compare their fields, defaults, authorization, errors, and observable effect.
2. Check current primary Render documentation/spec and use existing captured evidence where available. Record intentional differences and unverified live comparisons accurately.
3. Apply in-scope adapter fixes if necessary and update the exact ADR018 rows; use /pm for a concrete follow-up if genuine adjacent drift remains.

## Files

- `lego/backend/internal/deploys/`
- `lego/backend/internal/store/ (if changed)`
- `lego/operator/internal/build/`
- `lego/operator/internal/controller/`
- `lego/types/v1alpha1/`
- `dashboard/src/features/services/ (if changed)`
- `docs/ADR018-render-parity.md`
- `docs/ADR060-build-worker-reliability-and-performance.md`

## Acceptance criteria

- [ ] Every exposed path uses the shared corrected contract and has evidence for its resulting build behavior.
- [ ] ADR018 accurately states the corrected capability, defaults, and any intentional divergence.
- [ ] No deliberate non-goal is added, and a successful decode alone is not called behavior parity.

## Out of scope

- Unrelated parity gaps or a new API/CLI/UI product surface.
