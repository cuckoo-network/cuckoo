---
id: w7/m30/t003
title: Per-family conformance cases (services/deploys/pg/kv/env/domains/events)
worker: worker7
status: done
estimate: 60m
depends_on: [w7/m30/t002]
---

## Objective

Conformance cases for the core endpoint families: services, deploys, postgres, key-value, env vars + env groups, custom domains, service events — each driving the resource to a realistic state and validating list/get response bodies against the pinned schemas.

## Files

- `lego/backend/internal/api/conformance_test.go`

## Acceptance criteria

- [x] Each listed family has a passing conformance case (modulo allowlisted divergences from t004).

## Resolution

`TestRenderConformance` covers 11 subtests:
- services/list (`list-services`), services/get (`retrieve-service`)
- deploys/list (`list-deploys`), deploys/get (`retrieve-deploy`)
- postgres/list (`list-postgres-databases`), postgres/get (`retrieve-postgres`)
- keyvalue/list (`list-redis`), keyvalue/get (`retrieve-redis`)
- env-vars/list (`retrieve-env-vars-for-service`)
- custom-domains/list (`list-custom-domains`)
- events/list (`list-events`)

All 11 subtests pass. Env groups excluded (bex maps them to the flat env-var list, no
separate endpoint). API-key and usage endpoints excluded (no Render schema counterpart).
