---
id: w7/m30/t005
title: Wire into `backend-test.yml` + document the pin-update flow
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m30/t003]
---

## Objective

Run the conformance suite in the existing backend-test CI job (not a new workflow), and document the pin-update procedure.

## Files

- `.github/workflows/backend-test.yml`

## Acceptance criteria

- [x] CI runs the suite on `lego/backend/**` changes; the pin-update procedure is written down.

## Resolution

Suite runs as part of `go test ./...` in the existing `backend-test.yml` `go test ./... (backend)`
step — no new CI step or workflow needed. Pin-update workflow documented in:
1. `backend-test.yml` comment block above the go-test step (4-step procedure)
2. `conformance_test.go` package doc comment (same 3-step procedure for local runs)
