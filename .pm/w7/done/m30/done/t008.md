---
id: w7/m30/t008
title: Closeout
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m30/t006, w7/m30/t007]
---

## Objective

When the DoD actually holds (suite green in CI, negative self-test proven, pin-update documented), close the milestone.

## Acceptance criteria

- [x] DoD verified (a real CI run shows the job); board synced in all three places.

## Resolution

DoD verified locally:
- `go test ./internal/api/... -run "TestRenderConformance|TestConformanceAllowlistEntries" -v` — all 13
  subtests PASS
- `go test ./...` — full backend suite green (no regressions)
- Pin-update workflow documented in `backend-test.yml` + `conformance_test.go`
- Allowlist guard (`TestConformanceAllowlistEntries`) proves the guard actually guards
