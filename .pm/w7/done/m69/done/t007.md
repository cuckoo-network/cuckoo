---
id: w7/m69/t007
title: Simplify
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m69/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving) — prime candidate: the four scripts' shared plumbing (S3 fetch + integrity check, throwaway lifecycle, confirmation gates, teardown verification) extracted into one sourced helper instead of quadruplication.

## Context

- Changed surface: `scripts/restore-*.sh`, docs pointer edits.

## Steps

1. `/simplify` scoped to the milestone's diff; apply safe cleanups.

## Files

- (as flagged)

## Acceptance criteria

- [x] Simplify pass run; cleanups applied or explicitly declined with reason.

## Result

`/simplify` was unavailable in this session, so the same scoped review ran manually. Shared S3/validation/confirmation/namespace lifecycle stayed in `scripts/lib/restore.sh`; duplicated store mechanics remained store-local. The pass made Postgres option failures explicit and split etcd extraction from validated reviewed-file apply, avoiding mixed-directory application without adding another abstraction.

## Out of scope

- Behavior changes.
