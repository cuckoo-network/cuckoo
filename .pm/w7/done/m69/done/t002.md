---
id: w7/m69/t002
title: scripts/restore-openbao.sh — scripted ADR015 throwaway restore
worker: worker7
status: done
estimate: 45m
depends_on: []
---

## Objective

Script the OpenBao Raft-snapshot fresh-node path (new init + `--force` + restart + unseal with OLD keys) into an isolated throwaway. The milestone-level safety contract supersedes the stale early note about automating a live same-instance restore.

## Context

- Runbook: `docs/ADR015-openbao-backup-restore.md` (both paths; fresh-node `--force` requirement discovered in the 2026-07-14 drill, w7/m29 — encode it so it's never re-discovered under pressure).
- Snapshots: `s3://bex-tfstate/openbao-snapshots/` (nightly, 7 kept, gzip).
- The fresh-node path drills against a throwaway namespace — the script's only target. `--live`/same-instance is rejected and remains incident theory in ADR015.
- Unseal keys come from `.env` (Shamir, ADR013) — the script prompts/reads them without ever echoing or logging them.

## Steps

1. Write the script (fetch → gunzip → throwaway target → force restore → original-key unseal → verification read of a known `tenants/` KV path).
2. Validate the fresh-node path fully against a fresh real prod snapshot and prove `--live` fails closed.

## Files

- `scripts/restore-openbao.sh` (new)

## Acceptance criteria

- [x] Fresh-node restore from the latest prod snapshot succeeds in a throwaway, verification read passes, throwaway torn down.
- [x] `--force` is applied exactly and only on the fresh-node path.
- [x] Unseal keys never appear in output, logs, or shell history (read via prompt/env, not argv).

## Out of scope

- Rekey/root-token rotation (ADR037's own runbook).
