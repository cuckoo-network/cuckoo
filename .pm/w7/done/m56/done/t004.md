---
id: w7/m56/t004
title: Simplify
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m56/t003]
---

## Objective

Run `/simplify` over the milestone's changes (the provisioning script, gitops wiring, and any store-layer touches) — reuse, simplification, efficiency; behavior-preserving.

## Context

- Likely targets: shared shell-script helpers across `scripts/ssh-host-key-secret.sh` / `scripts/registry-secrets.sh` / the new role script; duplicated Secret-handling boilerplate in the gitops manifests.

## Steps

1. `/simplify` over the milestone diff.
2. Apply accepted findings; re-verify the script's idempotence.

## Files

- `scripts/ssh-gateway-db-role.sh`, `deploy/gitops/` (milestone diff)

## Acceptance criteria

- [ ] Simplify pass run and findings applied or explicitly declined.
- [ ] Script re-run remains a no-op; gitops validation green.

## Out of scope

- New tests (t005).
