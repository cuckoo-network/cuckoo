---
id: w7/m36/t003
title: Zot htpasswd/ACL generation + revocation on App delete
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m36/t002]
---

## Objective

Zot's htpasswd + accessControl config gains/loses per-App identities as Apps come and go, scoped to each App's repo prefix, via the reload mechanism t001 chose.

## Files

- deploy/gitops/ (zot config/mechanism)
- lego/operator/ (whatever component owns the htpasswd materialization per t001)

## Acceptance criteria

- [ ] A new App's identity can pull only its prefix; a deleted App's identity stops authenticating.

## Out of scope

- Tenant workload attachment (t004).
