---
id: w7/m36/t004
title: Compose with w6/m29's imagePullSecret attach
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m36/t003]
---

## Objective

Tenant Deployments/CronJobs reference their App's own pull Secret through the attach mechanism w6/m29 ships (replacing the shared `BEX_REGISTRY_PULL_SECRET` for tenant workloads), with the shared-credential fallback preserved for the unset/dev path (byte-identical default).

## Context

- **Gate: `w6/m29` must be closed first** — its `imagePullSecrets` wiring is the seam this plugs into.

## Files

- lego/operator/internal/controller/

## Acceptance criteria

- [ ] Envtest: generated workloads carry the per-App secret when the feature is on; dev default unchanged.
