---
id: w7/m36/t002
title: Operator mints per-App pull credential + Secret
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m36/t001]
---

## Objective

The operator mints a per-App registry credential at App creation and materializes it as a docker-config Secret in the apps namespace, following the clone-secret/registry-secret lifecycle patterns (explicit delete, no owner-ref ordering trap — the w2/m14 lesson).

## Files

- lego/operator/internal/controller/ (app controller)
- lego/operator/internal/build/ (if the build push path splits creds too, per t001's design)

## Acceptance criteria

- [ ] Envtest: creating an App materializes its credential Secret; deleting the App removes it.

## Out of scope

- Zot-side ACLs (t003).
