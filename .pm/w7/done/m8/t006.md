---
id: w7/m8/t006
title: Simplify — /simplify over the code/manifests this milestone changed
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m8/t005]
---

## Objective

Run `/simplify` over everything m8 touched — the build Job credential plumbing, the Zot gitops manifests, and the script changes — applying reuse/simplification/efficiency cleanups without behavior change.

## Context

- Scope: `lego/operator/internal/build/build.go`, `lego/operator/internal/controller/` env plumbing, `deploy/gitops/base/zot.yaml` + secret manifests, `scripts/verify-tenant-isolation.sh`, `scripts/gitops-validate.sh`.

## Steps

1. `/simplify` over the milestone's diff; apply accepted findings.
2. Re-run the affected tests and `scripts/gitops-validate.sh` to confirm behavior is preserved.

## Files

- Whatever the milestone's diff touched (see Context).

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; tests + validate script still green.

## Out of scope

- New behavior; bug hunting (that's `/code-review`).
