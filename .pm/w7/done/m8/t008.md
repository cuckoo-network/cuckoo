---
id: w7/m8/t008
title: Closeout — DoD verified, milestone moved to done/
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m8/t007]
---

## Objective

Close m8 only when its definition of done actually holds: anonymous catalog/push refused live, a credentialed build-from-git App round-trips, the CI guard is proven red-on-regression, and the credential is verifiably unreadable from tenant `RUN` steps. Then perform the standard closeout moves.

## Context

- DoD: `.pm/w7/m8/README.md` § Definition of done.
- Closeout mechanics: every task `status: done` + moved to `w7/m8/done/`, all rows `— **DONE**` + `**Status:** done` in the milestone README, the whole `w7/m8/` directory moved to `w7/done/m8/`, and the `- [x]` checkbox in `w7/README.md`. Never leave a tombstone at the old path.

## Steps

1. Re-run the t005 verification against the local mock; confirm every DoD line.
2. Execute the closeout moves; sync status in all three places.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m8/` → `.pm/w7/done/m8/`; `.pm/w7/README.md`

## Acceptance criteria

- [ ] DoD verified live, not inferred from code.
- [ ] Board state consistent: no open m8 path remains; checkbox, README status, and task frontmatter all agree.

## Out of scope

- Prod verification if the stale-operator caveat still blocks it (record it in the closeout note if so, with the follow-up path).
