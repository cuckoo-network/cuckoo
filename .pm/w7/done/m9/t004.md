---
id: w7/m9/t004
title: Render parity — same caps/error shapes across REST/GraphQL/MCP + dashboard; compare Render
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m9/t002, w7/m9/t003]
---

## Objective

Verify the shipped limits behave identically across every surface and against the Render anchors: the cap error carries the same fields/semantics on REST, GraphQL, and MCP; the dashboard create flows (services/databases/key-value) surface the limit error legibly rather than a generic failure; the divergences recorded in t001 (workspace-wide build cap; bex-chosen error shape) land in `docs/ADR018-render-parity.md` as documented drift, not silence.

## Context

- Anchors (verified 2026-07-12): Hobby = 25 services/workspace; free tier = 1 Postgres + 1 Key Value active per workspace; one active build per service with newest-wins cancellation. Sources cited in the milestone README.
- Precedent: m3's rate-limits ledger row and m6's collision-guard row show the shape — one ledger row (or row update) with evidence and the divergence noted.
- Dashboard create flows: `dashboard/` service/database/key-value creation forms (see `dashboard/CLAUDE.md` before editing).

## Steps

1. Exercise the cap error on all three API surfaces; diff fields/semantics; fix any drift.
2. Drive the dashboard create flow into the cap and check the displayed error; fix display if it's swallowed.
3. Update `docs/ADR018-render-parity.md` with the limits row(s) + divergences; cross-reference ADR006's new §.

## Files

- `docs/ADR018-render-parity.md`
- `dashboard/` (error display fix if needed)

## Acceptance criteria

- [ ] Identical error semantics on REST/GraphQL/MCP, verified by exercising each.
- [ ] Dashboard shows the limit error meaningfully; parity ledger updated with anchors + recorded divergences.

## Out of scope

- New dashboard limit-management UI (no Render equivalent to mirror).
