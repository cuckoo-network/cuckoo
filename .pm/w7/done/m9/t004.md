---
id: w7/m9/t004
title: "REST/GraphQL/MCP: surface current usage vs. limit"
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m9/t002]
---

## Objective

Let a caller see how close a workspace is to its resource-creation limits (e.g. "3/5 services") on REST, GraphQL, and MCP — not just find out by hitting the cap.

## Context

- Precedent: `w1/m8`'s plan API already surfaces limits for instance tiers; `w8`'s usage API surfaces metered quantities. This is the same shape for creation counts.
- Likely lives alongside the workspace/plan read surface (`w6/m2`'s owners API, or `w1/m8`'s plan endpoints).

## Steps

1. Add a field or endpoint returning `{used, limit}` per resource kind (services/Postgres/KeyValue) for the caller's workspace.
2. Expose it on REST, GraphQL, and MCP.

## Files

- wherever the workspace/plan read surface lives (`w6`'s owners API or `w1/m8`'s plan endpoints)

## Acceptance criteria

- [ ] `{used, limit}` per resource kind is readable on REST, GraphQL, and MCP, and matches reality.

## Out of scope

- Dashboard rendering — t005.
