---
id: w7/m9/t006
title: Test coverage — meaningful tests for cap enforcement + build serialization
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m9/t004]
---

## Objective

Add tests asserting the real behavior and failure modes m9 shipped: the (N+1)th create refused with the documented error while another workspace still creates (per resource type, per surface — the `TestAuditSurfaceParity` cross-surface pattern); newest-wins build cancellation and the per-workspace cap holding under a burst; all knobs unset ⇒ byte-identical behavior. No tautological or snapshot tests.

## Context

- Backend surface-parity precedent: `TestAuditSurfaceParity` (one behavior, three surfaces asserted equal).
- Operator: envtest suites in `lego/operator/internal/controller/` (the isolation suite shows the spec-invariant style); build Job assertions live near `lego/operator/internal/build/`.
- Edge cases worth pinning: cap of exactly the current count (create refused, existing unaffected); delete-then-create passes again; superseded-Job deletion doesn't orphan the new revision's Job.

## Steps

1. Backend: cap-enforcement tests per resource type + cross-surface error parity + unset-knob no-op.
2. Operator: envtest for newest-wins (two revisions ⇒ one active Job) and the workspace cap (requeue at cap, resume on slot free).
3. `make test` (from `lego/operator/`) + `cd lego/backend && go test ./...` green.

## Files

- `lego/backend/internal/{apps,postgres,keyvalue,core}` tests
- `lego/operator/internal/{build,controller}` tests

## Acceptance criteria

- [ ] Tests fail if a cap stops being enforced, leaks across workspaces, drifts between surfaces, or if serialization/cap regress.
- [ ] Both module test suites green.

## Out of scope

- E2E kind-cluster runs (`make test-e2e` is CI territory).
