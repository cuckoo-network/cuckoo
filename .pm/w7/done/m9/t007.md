---
id: w7/m9/t007
title: Closeout — DoD verified, milestone moved to done/
worker: worker7
status: todo
estimate: 15m
depends_on: [w7/m9/t006]
---

## Objective

Close m9 only when its definition of done actually holds — cap refusals observed live on all three surfaces with a second workspace unaffected, newest-wins + workspace build cap observed via `kubectl get jobs`, unset knobs byte-identical, ADR006 § written and the parity ledger updated. Then perform the standard closeout moves.

## Context

- DoD: `.pm/w7/m9/README.md` § Definition of done.
- Closeout mechanics: every task `status: done` + moved to `w7/m9/done/`, all rows `— **DONE**` + `**Status:** done` in the milestone README, the whole `w7/m9/` directory moved to `w7/done/m9/`, and the `- [x]` checkbox in `w7/README.md`. Never leave a tombstone at the old path.

## Steps

1. Re-verify each DoD line live on the local mock.
2. Execute the closeout moves; sync status in all three places.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m9/` → `.pm/w7/done/m9/`; `.pm/w7/README.md`

## Acceptance criteria

- [ ] DoD verified live, not inferred from code.
- [ ] Board state consistent: no open m9 path remains; checkbox, README status, and task frontmatter all agree.

## Out of scope

- Anything the DoD doesn't require.
