---
id: w7/m9/t005
title: Simplify — /simplify over the code this milestone changed
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m9/t004]
---

## Objective

Run `/simplify` over everything m9 touched — the core cap checks, the three create-verb call sites, the operator build-serialization logic, and any dashboard error-display change — applying reuse/simplification/efficiency cleanups without behavior change.

## Context

- Scope: `lego/backend/internal/{core,apps,postgres,keyvalue}`, `lego/operator/internal/{build,controller}`, `dashboard/` (if t004 touched it).

## Steps

1. `/simplify` over the milestone's diff; apply accepted findings.
2. Re-run the affected tests to confirm behavior is preserved.

## Files

- Whatever the milestone's diff touched (see Context).

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; tests still green.

## Out of scope

- New behavior; bug hunting (that's `/code-review`).
