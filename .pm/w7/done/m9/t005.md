---
id: w7/m9/t005
title: "Dashboard: cap-hit messaging + upgrade nudge"
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m9/t004]
---

## Objective

Show the workspace's resource usage vs. limit in the dashboard, and give a clear, actionable message (with an upgrade-plan nudge) when a create action is rejected for hitting the cap.

## Context

- t004's `{used, limit}` read surface.
- `w6/m12`'s plan-change dialog — the upgrade nudge should link there.
- New-service create wizard (`w5/m15`) and the Postgres/KeyValue create flows are the surfaces that need cap-hit error handling.

## Steps

1. Surface `{used, limit}` somewhere sensible (workspace settings or plan section, alongside `w6/m12`'s plan UI).
2. Handle the cap-hit error from create flows with a clear message + a link to the plan-change dialog.

## Files

- `dashboard/src/features/workspaces/` (plan section)
- `dashboard/src/features/services/`, Postgres/KeyValue create flows

## Acceptance criteria

- [ ] The dashboard shows current usage vs. limit.
- [ ] A cap-hit create attempt shows a clear message with an upgrade path, not a raw error.

## Out of scope

- Build-concurrency cap-hit UI — build triggers are largely backend-driven (webhook/CI); a create-flow-style UI isn't in scope here, only the REST/GraphQL/MCP error shape from t003/t004.
