---
id: w7/m82/t007
title: Simplify the code this milestone changed
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m82/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- Likely hotspots: the exit-code table and its call sites (t001), the already-long Job spec construction in `build.go`, and the outcome-classification plumbing spanning operator and bex-api (t003).
- `m81`'s simplify pass is the precedent for filing what it finds rather than fixing out of scope: it surfaced the Deployment `CreateOrUpdate` PUT loop and filed it as `028`.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving cleanups.
3. File anything out of scope as an inbox note rather than expanding this milestone.

## Files

- Whatever t001–t006 touched.

## Acceptance criteria

- [ ] `/simplify` findings are applied or explicitly filed.
- [ ] No behavior change; the milestone's tests still pass.

## Out of scope

- Refactoring code this milestone did not touch.
