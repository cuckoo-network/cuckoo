---
id: w7/m41/t005
title: Render parity — verify create form + settings match Render's static site flow
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m41/t003, w7/m41/t004]
---

## Objective

Confirm bex's static site create form and settings now match Render's live
dashboard, and update ADR018 to close the gap.

## Context

- Render's static site create form (`https://dashboard.render.com/static/new`) fields (captured via Playwright 2026-07-16):
  1. Source Code
  2. Name
  3. Project (optional)
  4. Branch
  5. Root Directory (optional)
  6. **Build Command** ← now shipped (t001)
  7. **Publish Directory**
  8. Environment Variables
  9. [Advanced] Secret Files
  10. [Advanced] Auto-Deploy toggle
  11. [Advanced] **Build Filters** (Included/Ignored Paths) ← now shipped (t004)
- `docs/ADR018-render-parity.md`
  - Static site row (line ~39): current ✅ note only mentions publishPath + routes + headers — update to also cite Build Command + Build Filters
  - Build Filters row (line ~24): confirms ✅ for settings; now also ✅ for create wizard after t004

## Steps

1. Open bex's create wizard at `http://localhost:<dev-5 port>/services/new`, select Static Site + a git repo, and confirm:
   - Build Command field appears above Publish Directory
   - Advanced section shows Build Filters (Included/Ignored Paths)
2. Create a test static site with a build command and a build filter; verify in Settings → Build & Deploy that:
   - Build Command editor shows the created value (editable)
   - Build Filters editor shows the created filters
3. Update `docs/ADR018-render-parity.md`:
   - Static site row: extend the UI ✅ note to mention Build Command + Build Filters at create time
   - Build Filters row: note that create-wizard support for static sites now ships alongside existing settings support

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] bex create form field order matches Render's (Build Command above Publish Directory)
- [ ] ADR018 static site row updated with Build Command + Build Filters create-time evidence
- [ ] ADR018 Build Filters row updated to note create-wizard parity for static sites

## Out of scope

- Cron job Build Command parity (tracked in `w5/done/013.md`, deferred)
