# Audit round 3 over post-2026-07-20 surfaces — once w3/m35 closes

**Type:** trigger-gated audit follow-on. **Not schedulable yet** — do not promote before the trigger fires.

A third evidence-backed security audit pass (the ADR028 → `w1/m53` lineage; both registers verified empty 2026-07-30) over the surfaces shipped since the last audit (2026-07-20): per-tenant namespaces + the NamespaceReconciler's cluster authority (`w3/m31`), gVisor sandboxes + exec (`w3/m32`–`m33`), the Stripe billing lifecycle incl. the public webhook + enforcement paths (`w7/m50`–`m53`), and the static-site serving/publish plane (`w7/m54`).

**Why not now:** the largest new surface (sandboxes) is mid-hardening under `w3/m35` — auditing it today would double-cover open, owned work (m35 t013 already owns NamespaceReconciler confinement; its adversarial matrix owns sandbox isolation evidence).

**Trigger to promote:** `w3/m35` closes → promote to a w7 milestone scoped to what m35 did *not* cover, with a fresh follow-up register in the ADR028 format.

Source: `/pm-brainstorm for w7` 2026-07-30.
