---
id: w7/m55/t007
title: Closeout
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m55/t006]
---

## Objective

Close the milestone once the definition of done actually holds: the matrix is enumerated across REST/GraphQL/MCP, the completeness guards bite, the E2E covers every service against real FGA, and the seeded-regression evidence exists.

## Context

- Board conventions: `.pm/CLAUDE.md` + `.claude/skills/pm/SKILL.md` (`done` subcommand step 4 moves the whole milestone).

## Steps

1. Verify the DoD line by line (CI green on `backend-test.yml`, guards demonstrated).
2. Set remaining task frontmatter `status: done`, move tasks to `w7/m55/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w7/m55/` → `w7/done/m55/`; check `- [x]` in `w7/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m55/**`, `.pm/w7/README.md`

## Acceptance criteria

- [ ] DoD verified true before any move.
- [ ] Status in sync across task frontmatter, milestone README, and workstream README; no tombstone left at the old path.

## Out of scope

- Any new coverage (file follow-ups instead).
