---
id: w7/m55/t006
title: "Test coverage: seeded-regression (mutation) verification of the matrix itself"
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m55/t004]
---

## Objective

The milestone ships tests, so the meaningful coverage task is proving the guard actually guards: seed each authz-regression class the matrix claims to catch and verify the suite turns red. A matrix that passes on today's correct code but would also pass on broken code is tautological — this task is the anti-tautology check.

## Context

- Regression classes to seed (temporarily, one at a time, each reverted): (1) a verb whose `Authorize*` call is removed; (2) a verb authorizing the caller's own workspace object instead of the target's (confused deputy); (3) a REST route rebound to skip its verb's authz path; (4) an MCP tool with the workspace check dropped; (5) for the E2E: an FGA model relaxation making a relation public.
- Each seed is a local, uncommitted mutation; record the red-run evidence (test name + failure line) in this task file or the milestone README when done.

## Steps

1. Apply each seeded regression; run the matrix; confirm the specific failing assertion; revert.
2. Where a class is awkward to seed by hand, add a permanent in-suite self-test instead (e.g. the completeness guard tested against a fake unregistered route).
3. Fill any coverage hole the seeding reveals.

## Files

- `lego/backend/internal/api/` matrix tests (assertions added as needed)

## Acceptance criteria

- [x] All five regression classes demonstrably fail the suite (evidence recorded).
- [x] Any hole found during seeding is closed.

## Out of scope

- Closeout (t007).

## Evidence (2026-07-30)

Classes 1–3 are proven **permanently** by `crossworkspace_selftest_test.go` (a synthetic `leakyService` whose confused-deputy / unguarded verbs surface as SUCCESS while its correctly-guarded verb is denied) — `TestCrossWorkspaceMatrixCatchesVerbLeaks` (classes 1+2) and `TestCrossWorkspaceMatrixCatchesRESTLeaks` (class 3). Classes 4–5 need a real code/model change, so they were seeded once and reverted:

- **(1) missing guard / (2) confused deputy** — permanent: the two leaky verbs return nil (would be flagged); `CorrectGet` is denied. Seeded live too via `setSuspended` (t001): `apps.Service.Suspend`/`Resume` flagged, reverted.
- **(3) REST route skipping authz** — permanent: `driveNonMember` sees the leaky `/leak/{name}` route as 2xx and the safe route as non-2xx.
- **(4) MCP tool with the workspace check dropped** — seeded a confused deputy into `apps.Service.Get`; **both** `TestCrossWorkspaceServiceVerbMatrix` (`apps.Service.Get: returned success`) and `TestCrossWorkspaceMCPMatrix` (`MCP tool "get_service": succeeded`) went red; reverted.
- **(5) FGA model relaxation making a relation public** — pushed a model adding `viewer` to workspace `can_create` to the live OpenFGA store; `TestMultiWorkspaceTargetingE2E` went red on the viewer-DELETE assertions for **services, postgres, AND key-value** (`carl (viewer of bravo) DELETE … : 204, want 403`), proving the t004 per-object additions bite; original model re-pushed, E2E green.

No coverage hole found during seeding — every seeded class was caught by an existing assertion.
