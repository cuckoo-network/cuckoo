---
id: w7/m65/t006
title: Test coverage: guard self-tests assert real red/green behavior
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m65/t002, w7/m65/t003, w7/m65/t004]
---

## Objective

Verify the milestone's shipped behavior is meaningfully tested — the pinning guard's red/green self-test (t002) is the core; audit the whole delivery for untested failure modes and close any gaps. Tests must assert real behavior, never tautologies.

## Context

- t002's self-test should already cover: tag ref → red, 40-hex ref → green, `./` local ref → exempt. Check the less obvious modes: a ref with a SHA-*like* but 39/41-hex suffix, a `uses:` inside a comment line, an entry present in workflows but missing from `expected_actions` (inventory diff direction), and an inventory entry with no workflow occurrence.
- t004's audit gate was demonstrated once on a branch (known-vulnerable dep) — that evidence is procedural, not durable; if a cheap durable assertion exists (e.g. a step-order/flags grep in `scripts.yml`'s validator or a scripts-level check that `dashboard-test.yml` contains both audit passes with the right severities), add it so the gate can't be silently deleted — the m58 "universal fail-closed guard" spirit.
- t003 (dependabot.yml) is config; a structural presence check (file exists, both ecosystems declared) can ride the same validator if trivially cheap — don't overbuild.

## Steps

1. Enumerate the milestone's observable behaviors and map each to an existing assertion; list gaps.
2. Add the missing self-test cases / structural checks (validator or `.test.sh`).
3. Run the full `scripts.yml` suite locally (`bash scripts/*.test.sh` for the touched tests) — green.

## Files

- `scripts/github-actions-validate.sh`, `scripts/github-actions-validate.test.sh`
- possibly `.github/workflows/scripts.yml`

## Acceptance criteria

- [ ] Every shipped guard behavior has an assertion that demonstrably fails when the behavior regresses (spot-check by reverting one guard line locally).
- [ ] Deleting the dashboard audit gate or weakening its severity turns something red (durable check, not just the one-off branch demo).
- [ ] No tautological/snapshot-everything tests added.

## Out of scope

- Go/dashboard unit-test suites (untouched by this milestone).
