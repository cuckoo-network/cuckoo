---
id: w7/m65/t005
title: Simplify pass over the changed workflows/scripts
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m65/t002, w7/m65/t003, w7/m65/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — the 14 workflow files, `scripts/github-actions-validate.sh` + its new self-test, `.github/dependabot.yml`, and the `dashboard-test.yml` audit steps — applying reuse/simplification/efficiency cleanups while preserving behavior.

## Context

- Likely candidates: repeated pin-comment formatting across workflows, duplication between the validator's inventory diff and the new 40-hex check, fixture-setup duplication between `github-actions-validate.test.sh` and existing `.test.sh` siblings.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; re-run `bash scripts/github-actions-validate.sh` and the self-test.

## Files

- `.github/workflows/*.yml`, `scripts/github-actions-validate.sh`, `scripts/github-actions-validate.test.sh`, `.github/dependabot.yml`

## Acceptance criteria

- [ ] `/simplify` run over the milestone's changed files; accepted findings applied.
- [ ] Validator + self-test + `scripts.yml` job still green (behavior preserved).

## Out of scope

- New functionality; bug hunting (that's `/code-review`).
