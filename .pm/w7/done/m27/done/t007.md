---
id: w5/m27/t007
title: Render parity — full-surface check
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m27/t006]
---

## Objective

Confirm the new Blueprints UI is consistent with the blueprint verbs already shipped on REST/GraphQL/MCP (`w2/m15`) and with Render's own Blueprints surface, flagging any drift as follow-up. Re-confirm this is not the DO_NOT_DO picker.

## Context

- Backend contract + divergences: `docs/ADR018-render-parity.md` "Blueprint / render.yaml IaC" row (no sync-delete of removed entries, no PR previews; named errors for rejected fields).
- DO_NOT_DO §"Do not build a 'Link database to service' picker" — verify the UI is read/validate/sync only, no DB→service env injection, no blueprint authoring picker.

## Steps

1. Diff the UI's actions (list/manifest/validate/sync) against the REST/GraphQL/MCP shapes — same field names, same all-or-nothing sync, same named rejected-field errors.
2. Compare against Render's Blueprints page IA; note any conscious divergence.
3. Update `docs/ADR018-render-parity.md` "Blueprint" row UI cell ✖→✅ (list/manifest/validate/sync shipped; authoring picker still a non-goal).

## Files

- `docs/ADR018-render-parity.md` (flip the Blueprint UI cell + note)

## Acceptance criteria

- [ ] UI matches the REST/GraphQL/MCP contract; divergences documented.
- [ ] Parity ledger's Blueprint UI cell reflects the shipped surface; no DO_NOT_DO picker crept in.

## Out of scope

- New backend fields (UI-over-existing-contract).
