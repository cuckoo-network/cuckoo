---
id: w5/m27/t001
title: Data layer — blueprints feature + hooks
worker: worker5
status: done
estimate: 35m
depends_on: []
---

## Objective

Stand up a `features/blueprints/` feature with hooks over the shipped blueprint verbs: list registered blueprints, validate one (dry-run), and sync one (idempotent re-apply).

## Context

- Backend contract (GraphQL): `blueprints` query + `validateBlueprint` + `syncBlueprint` mutations (`lego/backend/internal/blueprints`? — verbs shipped `w2/m15`, docs/ADR018-render-parity.md "Blueprint / render.yaml IaC" row). Blueprint rows carry `id`/`name`/`repo`/`branch`/`manifest`/`status`.
- GraphQL documents/types live in `dashboard/src/graphql/definitions.ts` — see [[dashboard-offline-codegen-drift]] (splice per-feature).

## Steps

1. Create `dashboard/src/features/blueprints/` (hooks + types + locales dirs).
2. Add `useBlueprints()` list hook (rows: id/name/repo/branch/manifest/status).
3. Add `useSyncBlueprint()` (idempotent re-apply) + `useValidateBlueprint()` (dry-run, returns per-entry errors, no apply) hooks; splice any missing documents into `definitions.ts`.

## Files

- `dashboard/src/features/blueprints/**` (new)
- `dashboard/src/graphql/definitions.ts` (splice missing documents)

## Acceptance criteria

- [ ] `useBlueprints()` returns the workspace's registered blueprints with their fields.
- [ ] Sync + validate hooks compile and return busy/error + (for validate) per-entry errors.

## Out of scope

- The pages themselves (t002/t003), validate UI (t004).
