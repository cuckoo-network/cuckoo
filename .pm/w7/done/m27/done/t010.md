---
id: w5/m27/t010
title: Closeout — DoD met → move milestone to done/
worker: worker5
status: done
estimate: 10m
depends_on: [w5/m27/t009]
---

## Objective

When every other task is complete **and the DoD actually holds** (Blueprints list/manifest/validate/sync all work from the dashboard, verified against the mock cluster), close the milestone.

## Context

- Closeout procedure: `.claude/skills/pm/SKILL.md` `done` subcommand step 4 — this task's completion _is_ the milestone move.

## Steps

1. Confirm the DoD: `/blueprints` lists registered blueprints; detail shows manifest; validate is a dry-run with per-entry errors; sync is idempotent — all verified live.
2. Run `/pm done w5/m27/t010` last → set all tasks `status: done`, move each to `done/`, mark rows `— **DONE**`, set `**Status:** done`, move the whole `w5/m27/` dir to `w5/done/m27/`, check `- [x]` in the w5 README.

## Files

- all `w5/m27/*` (moved to `w5/done/m27/`)
- `.pm/w5/README.md` (check the box)

## Acceptance criteria

- [ ] DoD verified true (not just code-complete); milestone moved to `w5/done/m27/`; w5 README box checked.

## Out of scope

- Any remaining implementation (must be done before this runs).
