---
id: w5/m27/t005
title: Empty/loading/error states + en/zh locales
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m27/t002, w5/m27/t003]
---

## Objective

Give the list, detail, and validate surfaces proper empty/loading/error states and add all `en`/`zh` locale strings for the Blueprints surface.

## Context

- Locale pattern: `dashboard/src/features/<feature>/locales/{en,zh}.ts`.
- Empty state on the list already carries the auto-registration copy (t002) — ensure it's localized.

## Steps

1. Add loading + empty + error states to the list (t002), detail (t003), and validate (t004) surfaces.
2. Add `en` strings for every user-visible label/message; mirror in `zh`.

## Files

- `dashboard/src/features/blueprints/locales/en.ts` (new)
- `dashboard/src/features/blueprints/locales/zh.ts` (new)
- list/detail/validate components

## Acceptance criteria

- [ ] No raw/untranslated strings; `en` and `zh` in sync.
- [ ] Loading, empty, and error states render distinctly.

## Out of scope

- Live cluster verification (t006).
