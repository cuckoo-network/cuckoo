---
id: w7/m1/t006
title: "Simplify — /simplify over the code this milestone changed"
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m1/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (projector labels, operator label propagation + any policy builder, gitops manifests, verification script) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Standing closing task (`.claude/commands/pm.md`).
- Touched surface: `lego/backend/internal/store/reconciler.go`, `lego/operator/internal/controller/`, `deploy/gitops/`, `scripts/verify-tenant-isolation.sh`.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; re-run `make lint` from `lego/operator/` and `scripts/gitops-validate.sh`.

## Files

- Whatever `/simplify` flags within the milestone's diff

## Acceptance criteria

- [ ] `/simplify` findings triaged (applied or explicitly rejected)
- [ ] `make lint` + `scripts/gitops-validate.sh` still pass

## Out of scope

- Bug hunting (that's `/code-review`, not this task)
