---
id: w7/m70/t005
title: Test coverage
worker: worker7
status: done
estimate: 40m
depends_on: [w7/m70/t004]
---

## Objective

Add tests that prove the isolation and the purge fix, including the negative paths the scan called out as missing.

## Steps

1. Cross-workspace collision test: two store-managed Apps named `web` in different workspaces — assert writes by A are not visible to B, and B cannot overwrite A's values.
2. Purge-uses-public-name test: deleting a store-managed App whose `metadata.name` is tenant-prefixed purges the public-name path; a same-name recreation returns empty.
3. (If migration ships here) migration idempotency + old-path-removed assertion.

## Files

- `lego/backend/internal/secrets/*_test.go` (extend the OpenBao / fake-store suite).

## Acceptance criteria

- [ ] Collision-isolation test passes.
- [ ] Purge-resolves-public-name test passes.
- [ ] No tautological/snapshot-only assertions.

## Out of scope

- Live OpenBao integration (loopback-bound suite already exists).
