---
id: w7/m70/t006
title: Closeout
worker: worker7
status: done
estimate: 10m
depends_on: [w7/m70/t005]
---

## Objective

Close m70 once its definition of done actually holds (isolation real in code + tests, purge fixed, keys migrated).

## Steps

1. Confirm DoD: cross-workspace isolation test green; purge path verified; migration applied/verified.
2. Set every task `status: done`, move `tNNN.md` → `w7/m70/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w7/m70/` → `w7/done/m70/`, check `- [x]` in `w7/README.md`.

## Acceptance criteria

- [ ] DoD holds before the move; milestone lands in `done/` only when the observable end state is real.

## Out of scope

- Anything that re-opens the keying decision.
