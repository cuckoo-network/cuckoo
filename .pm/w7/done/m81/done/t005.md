---
id: w7/m81/t005
title: Simplify the code this milestone changed
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m81/t004]
---

## Objective

Run `/simplify` over the diff this milestone produced (reuse / simplification / efficiency; behavior-preserving only).

## Context

- Expected blast radius: `lego/operator/internal/controller/deployment_projection.go` (+ its test) and description/copy files from t004.
- The probe block now carries three probes from one handler — check the construction reads as one idea, not three near-copies.

## Steps

1. Invoke `/simplify` scoped to the milestone's changed files.
2. Apply the behavior-preserving fixes it finds; re-run `make test` from `lego/operator/`.

## Files

- the milestone's changed files

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly waived; `make test` green.

## Out of scope

- Bug hunting beyond the changed diff (that is `/code-review` territory).
