---
id: w7/m81/t002
title: "Operator: add the livenessProbe per the t001 contract (5s budget, 10s period, 6 failures)"
worker: worker7
status: done
estimate: 40m
depends_on: [w7/m81/t001]
---

## Objective

Add the `livenessProbe` to the Deployment projection so the kubelet restarts a container whose health check fails for 60 consecutive seconds — Render's steady-state stage 2 — scoped exactly as t001 decided.

## Context

- `lego/operator/internal/controller/deployment_projection.go:191-203` — the non-worker branch that sets `StartupProbe` and `ReadinessProbe` from the shared `healthCheckHandler`. Liveness joins them here.
- Timing constants live in the same file (`healthCheckTimeoutSeconds = 5`, `healthCheckPeriodSeconds = 10`); add `failureThreshold: 6` for liveness (10s × 6 = 60s, Render's restart threshold) as a named constant with the Render citation, in the style of the existing block.
- The handler must be the same `healthCheckHandler(app.Spec.HealthCheckPath, p.port)` result the other two probes use — they can never disagree about what "healthy" means.
- If t001 chose explicit-path opt-in, the TCP branch gets no liveness probe and that path stays byte-identical.
- Workers (`p.worker`) set no probes at all — unchanged.

## Steps

1. Add the liveness timing constant(s) with the Render doc citation.
2. Set `container.LivenessProbe` in the non-worker branch per the t001 contract; update the comment block (t001 rewrote the rationale; this task removes the "no livenessProbe" sentence and the `.pm/w7/027.md` pointer).
3. Extend `deployment_projection_test.go` alongside the change (full assertion suite lands in t006; this task keeps `make test` green).

## Files

- `lego/operator/internal/controller/deployment_projection.go`
- `lego/operator/internal/controller/deployment_projection_test.go`

## Acceptance criteria

- [ ] An in-scope App's Deployment carries a `livenessProbe` with the shared handler, `timeoutSeconds: 5`, `periodSeconds: 10`, `failureThreshold: 6`.
- [ ] Out-of-scope shapes (workers; TCP-mode services if opt-in won) are byte-identical to before.
- [ ] `make test` (from `lego/operator/`) passes.

## Out of scope

- Docs and ledger updates (t003).
- The full behavior test matrix (t006).
