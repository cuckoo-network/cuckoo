---
id: w7/m81/t006
title: Test coverage for the shipped behavior
worker: worker7
status: done
estimate: 40m
depends_on: [w7/m81/t004]
---

## Objective

Pin the milestone's behavior with tests that assert real outcomes and failure modes — not tautologies.

## Context

- `lego/operator/internal/controller/deployment_projection_test.go` — the existing projection tests (pure function, no cluster needed); m80's probe/timer tests are the pattern to extend.
- The m80 invariants (startup budget = `progressDeadlineSeconds` = 15m from one constant; 5s timeout on every probe) must keep holding with a third probe present.

## Steps

1. Assert the liveness shape for an in-scope App: shared handler, `timeoutSeconds: 5`, `periodSeconds: 10`, `failureThreshold: 6`.
2. Assert the coexistence invariant: startup + readiness + liveness all present for an HTTP-path service, and liveness absent wherever t001's contract excludes it (TCP mode under opt-in; workers always).
3. Assert the startup-suspends-liveness precondition structurally: startup probe present with the full 15m budget whenever liveness is present — so a future edit cannot reintroduce boot-kill.
4. Negative case: the byte-identical guarantee for out-of-scope shapes.
5. Run the full suite: `make test` from `lego/operator/`.

## Files

- `lego/operator/internal/controller/deployment_projection_test.go`

## Acceptance criteria

- [ ] Each Definition-of-done item in the milestone README has at least one asserting test.
- [ ] Deleting or mistuning the liveness probe turns a test red (anti-tautology check: break it, watch it fail, revert).

## Out of scope

- envtest/e2e cluster runs beyond what `make test` already does.
