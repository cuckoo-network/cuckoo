---
id: w7/m81/t003
title: "Guidance + docs: cheap health endpoint, ADR004 restart stage, retire the ledger divergence"
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m81/t002]
---

## Objective

Ship the user-facing guidance `w7/027.md` required alongside liveness — point `healthCheckPath` at a cheap endpoint — and bring the written record (ADR004, the parity ledger) in line with the behavior that now ships.

## Context

- `w7/027.md`: "Guidance for pointing `healthCheckPath` at a cheap endpoint. Probing an expensive SSR route every 10s and restarting on 6 failures is a different proposition from probing `/healthz`. Render's own docs warn about health-endpoint design; bex has no equivalent guidance today."
- `docs/ADR004-app-deployment.md` — t001 recorded the contract there; this task adds the restart stage to the deploy/health flow description and the endpoint-design guidance.
- `docs/ADR018-render-parity.md` — m80/t006 recorded the missing restart stage as a known divergence; update the row to shipped (or to the argued divergence if t001 chose opt-in).
- Render's wording to mirror: [Health Checks](https://render.com/docs/health-checks) — 15s → out of rotation, 60s → restart, and their health-endpoint advice.

## Steps

1. ADR004: document the three consequences of the one health check (deploy gate, rotation, restart) and the cheap-endpoint guidance.
2. ADR018: retire or rewrite the divergence entry to match the shipped contract.
3. Grep `docs/` for other health-check mentions that predate liveness (e.g. `docs/render-artifacts/`) and correct any that now misdescribe behavior.
4. Run `npx prettier@3.4.2 --write "**/*.md"` on the touched docs.

## Files

- `docs/ADR004-app-deployment.md`
- `docs/ADR018-render-parity.md`
- any stale `docs/render-artifacts/*` health-check notes found in step 3

## Acceptance criteria

- [ ] A user can learn from bex's own docs that a failing health path restarts the instance after 60s, and that the path should be cheap.
- [ ] The parity ledger no longer lists the restart stage as an open divergence.

## Out of scope

- Code changes (t002 shipped them).
- Cross-surface description sweep (t004).
