---
id: w7/m81/t004
title: "Render parity sweep: health-check descriptions across REST/GraphQL/MCP + dashboard"
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m81/t003]
---

## Objective

Verify every surface that *describes* health checks tells the truth about the new restart behavior, and compare the shipped semantics against render.com one more time. m80/t006's lesson: the field was exposed on all four surfaces and each had independently drifted — an MCP description promising old behavior is the contract an agent reads, so a stale one is a live defect, not a typo.

## Context

- The `healthCheckPath` surface: REST (`POST/PATCH /v1/services`, kin-openapi validated), GraphQL (`setHealthCheckPath` + service fields), MCP (`set_health_check_path` tool description), dashboard Health & Alerts section + `en`/`zh` locale strings.
- m80 already swept these once (its t006 found the `"/"` coercion on all four); this sweep is about the *restart* semantics, not the path default.
- Render reference: [Health Checks](https://render.com/docs/health-checks).

## Steps

1. Grep `lego/backend/` and `dashboard/` for health-check descriptions (field descriptions, MCP tool text, locale strings, help copy).
2. Any that enumerate consequences ("removed from rotation", "deploy fails") must now include the 60s restart, or stay silent about consequences — never state the pre-m81 behavior.
3. Diff the shipped end-to-end behavior against Render's documented stages; flag any remaining drift as follow-up inbox notes rather than silently diverging.

## Files

- whichever description/copy files step 1 finds stale (expected: MCP tool descriptions, dashboard locale strings)

## Acceptance criteria

- [ ] No surface describes health checks as rotation-only or deploy-gate-only.
- [ ] Any deliberate remaining divergence from Render is recorded in the parity ledger, not just in code.

## Out of scope

- Changing probe mechanics (settled in t001–t002).
