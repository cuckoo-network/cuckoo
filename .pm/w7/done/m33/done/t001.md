---
id: w7/m33/t001
title: "Reproduce: managed Database on a tenant node + API-service probe from the CNPG identity"
worker: worker7
status: todo
estimate: 30m
depends_on: []
---

## Objective

Reproduce the defect deterministically before changing policy: create a managed Database that schedules onto a tenant node and prove its CNPG init pod cannot reach `https://10.96.0.1:443`, with a direct probe from the same pod identity (labels/namespace) the policy actually selects.

## Context

- `.pm/w7/done/004.md` — the production evidence (init pod `wait-for-get-cluster` retries exhausted; all node agents Ready, so it's policy, not a broken node).
- The live policy: `default/deny-tenant-node-and-metadata-egress` (Cilium), selecting `app.bex.co/workspace`-labeled pods with `egressDeny` to `host`/`remote-node` entities.
- The operator propagates the workspace label onto CNPG pods for same-workspace isolation (w6/m4) — that propagation is desired; the deny scope is the bug.

## Steps

1. On the mock/dev cluster with the same policy applied, create a Database pinned/scheduled to a tenant-labeled node.
2. Capture the init failure; run a probe pod with the identical labels to confirm the API-service timeout.
3. Record the exact selector/identity facts the t002 split must honor.

## Files

- evidence notes in this milestone dir (feeds t002/t003)

## Acceptance criteria

- [ ] Reproduction captured: CNPG init blocked on the API service under the current policy, from the exact selected identity.

## Out of scope

- Any policy change (t002).
