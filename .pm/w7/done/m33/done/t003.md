---
id: w7/m33/t003
title: "Regression guard + live bootstrap test: Database Ready + SELECT 1 from a fresh tenant node"
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m33/t002]
---

## Objective

Lock the fix in twice: a policy-render/unit regression (structural guard over the rendered manifests, the `scripts/gitops-validate.sh` pattern) so the selector split can't silently regress, and a live bootstrap test proving a managed Database on a fresh tenant node reaches `Ready` and executes `SELECT 1`.

## Context

- `scripts/gitops-validate.sh` already hosts structural guards (w3/m13, w2/m27 precedents) — add the selector assertions there.
- The w7/m4 reachability matrix (docs/ADR022-tenant-isolation.md) is the isolation-side acceptance to re-run.

## Steps

1. Add the render-time guard: the deny policy must exclude CNPG identities; the CNPG allow policy must scope to kube-API only.
2. Live: fresh tenant node (scale up or roll one), create a Database, wait for `Ready`, run `SELECT 1` through its connection URL.
3. Re-run the tenant reachability checks; attach both results to the milestone.

## Files

- `scripts/gitops-validate.sh`
- milestone evidence notes

## Acceptance criteria

- [ ] Structural guard fails on a reverted selector; live Database `Ready` + `SELECT 1` evidence captured; reachability matrix green.

## Out of scope

- `default/biliblilitest` repair/retirement (separate approval).
