---
id: w7/m46/t005
title: "Test coverage: wire psql-verify into the cli-compat regression suite + assert the negative allowlist-deny case"
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m46/t002]
---

## Objective

Make the psql acceptance durable, not a one-shot: wire `psql-verify` into the maintained `scripts/cli-compat.sh verify` regression path (like the other graded legs) and add the negative case so the checklist claim asserts real failure behavior, not just the happy path.

## Context

- The checklist's method note (`docs/cli-compatibility-checklist.md:11`) is anchored on `scripts/cli-compat.sh verify` — a leg that isn't wired into `verify` (or a gated supplement with a documented invocation) rots.
- The genuinely interesting behavior beyond the probe row is the **gate itself**: with the caller's IP absent from the Database's `ipAllowList`, the unmodified CLI must refuse client-side with `"IP address … not in allow list"` before spawning psql. Asserting that negative path proves bex faithfully reproduces Render's client-side gate (and that the allowlist field is actually surfaced on the read shape from t001).

## Steps

1. Wire `psql-verify` into `scripts/cli-compat.sh verify` (or register it as a documented, gated supplement with a named invocation, mirroring `pgcli`).
2. Add a negative assertion: a public Postgres whose `ipAllowList` does **not** contain the caller ⇒ the CLI exits non-zero with the `"not in allow list"` message, and no psql connection is attempted.
3. Confirm the leg still skips cleanly (non-failing) when `BEX_DB_DOMAIN` / pg-sni-proxy are absent, so `verify` on base dev-9 stays green.

## Files

- `scripts/cli-compat.sh`.

## Acceptance criteria

- [ ] `psql-verify` is reachable from the maintained regression path (or a documented gated supplement).
- [ ] Negative case asserts the CLI's client-side `"not in allow list"` refusal (IP absent from the allowlist), with no psql connection attempted.
- [ ] `verify` stays green on base dev-9 (leg skips cleanly without external connectivity).

## Out of scope

- CI wiring that would require an always-on external Postgres endpoint in CI (the leg stays a gated supplement; note this in the checklist if so).
