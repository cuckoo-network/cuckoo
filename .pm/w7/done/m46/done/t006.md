---
id: w7/m46/t006
title: "Closeout"
worker: worker7
status: done
estimate: 10m
depends_on: [w7/m46/t005]
---

## Objective

Close the milestone once its definition of done actually holds — the `psql [id|name]` row and its `-c/--command` sub-item are live-proven `[x]` with a captured artifact and a durable regression leg.

## Context

- Do not run this until the DoD is real: both `psql <id>` and `psql <name>` `-c` runs returned the probe row against a real external SNI endpoint with the allowlist passed, `docs/render-artifacts/psql-cli.md` exists, the checklist row + sub-item read `[x]`, and the negative allowlist-deny case is asserted.

## Steps

1. Verify the DoD holds (artifact present, checklist flipped, regression leg wired, negative case asserted).
2. Set every remaining task's `status: done`; move each `tNNN.md` to `w7/m46/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w7/m46/README.md`.
4. Move the whole `w7/m46/` directory to `w7/done/m46/`; check `- [x]` for m46 in `.pm/w7/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m46/**` → `.pm/w7/done/m46/**`; `.pm/w7/README.md`.

## Acceptance criteria

- [x] DoD verified real (not merely code-written).
- [x] Milestone moved to `w7/done/m46/`; workstream checkbox `- [x]`.
- [x] All task frontmatter `status: done`; prettier clean.

## Out of scope

- Any further psql work (interactive/TTY beyond the note, CI always-on endpoint) — file as a follow-up inbox note if it surfaces.
