---
id: w7/m50/t003
title: Wire Stripe into the emitter and runtime configuration
worker: worker7
status: done
estimate: 1h
depends_on: [w7/m50/t002]
---

## Objective

Make the existing seal-then-emit loop use Stripe when `BEX_STRIPE_SECRET_KEY` is configured while preserving the disabled estimate-only path.

## Context

- The control-plane store and `usage_hourly.emitted_at` remain unchanged.
- Billing epoch and seal horizon must remain startup-validated configuration.

## Steps

1. Replace Metronome construction in `cmd/api` with the Stripe client.
2. Rename runtime environment variables and logs without changing defaults.
3. Register a signature-verifying Stripe webhook route when configured.

## Files

- `lego/backend/cmd/api/main.go`
- `lego/backend/internal/billing/emitter.go`
- `lego/backend/internal/api/server.go`

## Acceptance criteria

- [ ] Key unset means no emitter, billing reader, Stripe route side effects, or startup failure.
- [ ] Key set wires one client into emission and billing reads.
- [ ] Malformed epoch configuration fails startup loudly.

## Out of scope

- Non-payment enforcement actions beyond verified event intake.
