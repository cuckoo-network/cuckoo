---
id: w7/m50/t004
title: Read Stripe invoice previews and finalized invoices
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m50/t002]
---

## Objective

Back the existing m48 billing result with Stripe's current subscription invoice preview and finalized invoice history.

## Context

- Public REST, GraphQL, MCP, and dashboard shapes must remain unchanged.
- Stripe amounts are integer minor units and currencies are ISO codes.
- Read failures degrade to the existing estimate-only behavior.

## Steps

1. Resolve the workspace Customer and bex subscription without creating either on a read.
2. Normalize the current invoice preview and non-draft invoice history.
3. Preserve newest-first ordering and stable empty slices.

## Files

- `lego/backend/internal/billing/read.go`
- `lego/backend/internal/billing/stripe_test.go`

## Acceptance criteria

- [ ] Current and finalized amounts, statuses, currency, and periods normalize correctly.
- [ ] No Customer/subscription returns `(nil, nil)` for estimate-only fallback.
- [ ] Stripe failures surface to the usage service, which continues to degrade gracefully.

## Out of scope

- A new invoice API surface or dashboard redesign.
