---
id: w7/m50/t006
title: Retire Metronome code and synchronize configuration
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m50/t003, w7/m50/t004, w7/m50/t005]
---

## Objective

Remove the superseded Metronome SDK/client/tests and make repository configuration describe only the direct Stripe billing path.

## Context

- Generic outbox, emitter, billing result types, and tenant exclusion remain.
- No `BEX_METRONOME_*` runtime knob may survive after migration.

## Steps

1. Remove Metronome-specific code and dependency entries.
2. Rename comments, migrations documentation, environment examples, and deployment configuration.
3. Search the repository for stale runtime references.

## Files

- `lego/backend/go.mod`
- `lego/backend/go.sum`
- `.env.example`
- `CLAUDE.md`
- `deploy/gitops/base/bex.yaml`

## Acceptance criteria

- [ ] Metronome SDK is absent from the module graph.
- [ ] No active configuration or product code references `BEX_METRONOME_*`.
- [ ] Stripe remains fully disabled when its runtime key is absent.

## Out of scope

- Rewriting historical milestone records as if Metronome had never shipped.
