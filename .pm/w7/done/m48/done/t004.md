---
id: w7/m48/t004
title: "Dashboard: real invoiced / current spend beside the estimate-only card"
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m48/t003]
---

## Objective

Show the real Metronome cost in the dashboard Usage page, visually distinct from the existing "(estimate only — not an invoice)" card, so users can tell an estimate from an actual invoice.

## Context

- `dashboard/CLAUDE.md` (read before editing); the Usage page + "Estimated Cost" card shipped in w8/m3 + m7.
- Consumes the GraphQL fields added in t003.
- Comped/excluded workspaces (no contract) show estimate-only — the UI must render that state without looking broken.

## Steps

1. Query the new billing fields in the Usage page.
2. Add a "Current spend / Invoices" presentation distinct from the estimate card (labeling, not just numbers).
3. Handle estimate-only (comped/excluded/degraded) gracefully.
4. Match the dashboard's existing card patterns (shadcn/Apollo conventions).

## Files

- `dashboard/` Usage page + its GraphQL query/fragments

## Acceptance criteria

- [ ] Real invoiced/current spend renders beside the estimate, clearly labeled as an invoice vs estimate.
- [ ] Estimate-only workspaces render cleanly (no error state).
- [ ] Dashboard tests / typecheck green.

## Out of scope

- Payment/collection UI (Phase 3, deferred).
