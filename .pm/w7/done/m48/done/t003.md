---
id: w7/m48/t003
title: "Surface real billing on the usage API (REST + GraphQL + MCP)"
worker: worker7
status: done
estimate: 1h
depends_on: [w7/m48/t002]
---

## Objective

Expose the real Metronome cost/invoices on bex's usage surface — identically across REST, GraphQL, and MCP — beside the existing advisory `estimatedCost`.

## Context

- `docs/ADR040-billing-metronome.md` §8 Phase 2.
- Existing usage surface: `lego/backend/internal/usage/{rest,graphql,mcp}.go` (all return `estimatedCost` today, ADR030).
- Add `invoicedCost` / `billingPeriod` fields (or a sibling `/v1/invoices` verb + `invoices` query + `get_invoices` tool) — one core, thin adapters (ADR006), so the three surfaces share semantics and error shapes.
- Authorization: same `can_view` workspace check the usage surface already uses.
- When Metronome is off/degraded or the tenant is comped/excluded: return `estimatedCost` only, cleanly (no 500).

## Steps

1. Add the billing read to the usage core (single source), calling t002's client.
2. Wire REST, GraphQL, and MCP adapters with identical field names/semantics/error shapes.
3. Keep `estimatedCost` for the in-flight (pre-seal, <48h) window; add the real invoiced amount for closed periods.
4. Cross-surface tests asserting parity of shape.

## Files

- `lego/backend/internal/usage/{rest,graphql,mcp}.go`
- usage core (where `estimatedCost` is attached today)

## Acceptance criteria

- [ ] REST, GraphQL, and MCP return the same real billing fields with the same semantics/error shapes.
- [ ] `estimatedCost` still present for the in-flight window; real invoiced cost present for closed periods.
- [ ] Degraded/comped/excluded ⇒ estimate-only, no 500.

## Out of scope

- Dashboard rendering (t004).
