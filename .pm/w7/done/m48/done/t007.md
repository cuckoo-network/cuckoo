---
id: w7/m48/t007
title: "Test coverage"
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m48/t006]
---

## Objective

Add meaningful tests for the behavior m48 shipped — real behavior and failure modes, not tautologies.

## Context

- Backend: `cd lego/backend && go test ./...`; dashboard tests too.
- Cover: contract provisioning idempotency + comped Mode B ($0 net, non-collectible); read client normalization + degraded/no-contract paths; cross-surface parity of the billing fields (REST/GraphQL/MCP); estimate-only fallback for comped/excluded/degraded.

## Steps

1. Unit tests for contracts + read client (incl. degraded + comped).
2. Cross-surface tests asserting REST/GraphQL/MCP field parity.
3. Dashboard test for the invoice-vs-estimate rendering + estimate-only state.

## Files

- `lego/backend/internal/billing/*_test.go`, `internal/usage/*_test.go`
- dashboard test(s)

## Acceptance criteria

- [ ] Tests assert the contract/read/parity/fallback behaviors and fail on regression.
- [ ] Backend + dashboard test suites green.

## Out of scope

- Live end-to-end against a real Metronome org.
