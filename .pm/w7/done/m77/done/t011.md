---
id: w7/m77/t011
title: Closeout
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m77/t009, w7/m77/t010]
---

## Objective

Close the milestone once its definition of done is actually met — not merely when the code is written.

## Context

This milestone's DoD includes a **live production cutover** (t007). Do not close on green tests alone: the observable end state is that the affected hetzner-prod tenants serve with zero hand-made artifacts.

## Steps

1. Verify the DoD holds, including the live-cutover clause.
2. Set every remaining task `status: done`; move each `tNNN.md` to `.pm/w7/m77/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in this README.
4. Move the whole `.pm/w7/m77/` directory to `.pm/w7/done/m77/` (leave no tombstone at the old path).
5. Check `- [x]` for m77 in `.pm/w7/README.md` with a one-line outcome.
6. Mark `.pm/w3/010.md` superseded and move it to `.pm/w3/done/010.md`.
7. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m77/**`, `.pm/w7/README.md`, `.pm/w3/010.md`

## Acceptance criteria

- [x] DoD verified including the live cutover, not assumed.
- [x] Milestone moved to `.pm/w7/done/m77/`; nothing left at the old path.
- [x] `w3/010` retired as superseded.
- [x] Status is consistent in all three places.

## Out of scope

- Closing `w7/m78` / `w7/m79`.


---

## Closed 2026-08-21

The DoD was verified against hetzner-prod, not inferred from green tests:

- all five datastore CRs `Ready` in `tea-d98210cbbpdc73dcrkvg`, none left in `default` for this tenant's App to reach;
- the App's pods resolve and connect to all three Postgres instances and Valkey in-namespace (5 / 4 / 2 live client backends) with no `CreateContainerConfigError` and no hostname patch;
- `count/databases.app.bex.co = 3` and `count/keyvalues.app.bex.co = 2` are actually charged against the workspace `ResourceQuota` — **this is `w3/010`'s gap, closed by observation rather than by argument**;
- every hand-made artifact is gone (Secret copies, both hand-written CiliumNetworkPolicies, the hand-made egress NetworkPolicy), with the three forums serving real content throughout — the removal being the proof the platform path carries the traffic;
- the three Ingresses remain, deliberately, per the 2026-08-09 correction: they are the only routing while `BEX_BASE_DOMAIN` stays unset.

Two items are carried forward rather than closed here, both recorded in `done/t007.md`: retiring the old `default` instances (Step 9, held for the soak, and what releases the raised `bex-tenant-0` autoscaler ceiling) and the `repo`-binding regression from `038`.

Executing this milestone was also what found `036`, `039` and `040` — three silent production defects on the backup and restore path, none of which any test or dashboard could have surfaced.
