---
id: w7/m58/t006
title: Closeout
worker: worker7
status: done
estimate: 15m
depends_on: [w7/m58/t005]
---

## Objective

Close the milestone once its definition of done actually holds: static-server hardened and still serving, every platform workload at baseline or documented-exempt, and the universal CI guard live and proven fail-closed.

## Context

- Do not run this until the DoD is real — a milestone lands in `done/` when its observable end state exists, not when the code is merely written (per `.claude/skills/pm/SKILL.md`).
- Closeout **is** the move: set every task `status: done`, mark every row `— **DONE**` + `**Status:** done` in the milestone README, move `w7/m58/` → `w7/done/m58/`, and check `- [x] **m58**` in `.pm/w7/README.md`.

## Steps

1. Verify the DoD: static-server carries the full hardened set and serves + reads S3 under read-only rootfs; every platform workload is baseline-or-exempt with the exemption list documented; `gitops-validate.sh` is fail-closed, passes on the fixed tree, and goes red on a reverted fix; CI runs it.
2. Set all task frontmatter to `status: done`; mark README rows `— **DONE**` and `**Status:** done`.
3. Move `w7/m58/` → `w7/done/m58/`; check `- [x] **m58**` in `.pm/w7/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m58/**` → `.pm/w7/done/m58/**`, `.pm/w7/README.md`

## Acceptance criteria

- [ ] The DoD is verified true before the move (static-server hardened + serving, all workloads baseline-or-exempt, guard live + fail-closed + CI-enforced).
- [ ] The milestone directory is moved to `done/` and the workstream checkbox is `- [x]`.
- [ ] Status is in sync across task frontmatter, milestone README, and workstream README.

## Out of scope

- Any hardening or guard work (prior tasks own that).
