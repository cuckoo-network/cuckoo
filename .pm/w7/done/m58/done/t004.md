---
id: w7/m58/t004
title: Simplify
worker: worker7
status: done
estimate: 20m
depends_on: [w7/m58/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — the static-server/pg-sni-proxy manifest edits and the new `gitops-validate.sh` guard block — for reuse / clarity / behavior-preserving cleanups.

## Context

- Likely simplification: if bex-api / ssh-gateway / manager / activator repeat the same securityContext block inline, consider a shared kustomize component/patch the hardened workloads reference — but only if it does not obscure the per-workload manifest or fight the existing config layout. Decline if it reduces readability of the security-relevant fields.
- The guard block should reuse the script's existing `yq` helper idiom rather than introducing a parallel parsing style.

## Steps

1. Identify the diff from t001–t003.
2. Run `/simplify` over exactly that diff; apply behavior-preserving cleanups only.
3. Decline (and record why) any change that weakens or obscures a securityContext field or the guard's fail-closed behavior.

## Files

- whatever t001–t003 touched (`config/staticserver/deployment.yaml`, `config/pg-sni-proxy/daemonset.yaml`, `scripts/gitops-validate.sh`, any shared kustomize patch introduced)

## Acceptance criteria

- [ ] `/simplify` has been run over the milestone's diff; behavior-preserving cleanups applied.
- [ ] No change weakens, obscures, or makes non-fail-closed any securityContext field or the guard.

## Out of scope

- Any behavior change or new hardening beyond what t001–t003 shipped.
