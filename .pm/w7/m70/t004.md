---
id: w7/m70/t004
title: Simplify
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m70/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — collapse any duplicated tenant-resolution logic between the read/write path and the purge path, and ensure `storeServiceName` + tenant resolution is expressed once.

## Steps

1. `/simplify` over `lego/backend/internal/secrets/` and `lego/backend/internal/apps/service.go`.
2. Apply behavior-preserving cleanups.

## Acceptance criteria

- [ ] Tenant + public-name resolution has one clear home, reused by read/write/purge.

## Out of scope

- New behavior.
