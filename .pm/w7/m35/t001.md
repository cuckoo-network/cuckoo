---
id: w7/m35/t001
title: "Extract m37's dial-time IP guard into a shared helper"
worker: worker7
status: todo
estimate: 45m
depends_on: []
---

## Objective

Lift the SSRF guard `w1/m37` added to the activator into a shared, reusable helper: a `net.Dialer.Control` (or equivalent) that rejects a connection whose resolved IP is loopback, private (RFC 1918), link-local (incl. `169.254.169.254`), unspecified, or otherwise non-public, plus an `http.Client` factory that uses it and does not follow redirects. One implementation both the activator and the webhook worker import.

## Context

- Source guard: `lego/operator/cmd/activator/main.go` (m37's maintenance-page fetch — grep `Dialer`/`Control`/SSRF).
- Consumers live in different modules (operator `cmd/activator` vs backend `internal/webhooks`) — place the helper where both can import it without breaking the `operator ↛ backend` boundary. Likely a small leaf package in `lego/backend` that the webhook worker uses, with the activator either importing it (if the dependency direction allows) or keeping a thin copy that the test asserts stays in sync. Decide and document the placement.
- The block-list must be table-driven so t003 can assert each range.

## Files

- new shared helper package; `lego/operator/cmd/activator/main.go` (switch to it if import allows)

## Acceptance criteria

- [ ] One documented helper with the full block-list + no-redirect client; placement respects the module boundary; activator behavior unchanged (its own tests still green).

## Out of scope

- Wiring the webhook worker (t002).
