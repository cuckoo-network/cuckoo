---
id: w7/m35/t002
title: "Apply the guard to the webhook worker's defaultClient"
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m35/t001]
---

## Objective

Replace `webhooks/worker.go`'s stock `defaultClient` with one built from the t001 helper: private/loopback/link-local/unspecified destinations are refused at dial time (delivery fails and is logged as a delivery error, feeding the existing retry/auto-disable path), and redirects are not followed. The activator moves to the shared helper so there is a single implementation.

## Context

- `lego/backend/internal/webhooks/worker.go` — `defaultClient` (line ~182), `requestTimeout`, the `client()` seam (keep the injectable `w.Client` for tests).
- `parseDestination` (`service.go:406`) stays as the syntax gate; this adds the dial-time network gate behind it.
- A blocked destination should surface as a normal delivery failure (retryable/auto-disabling), not a panic or a silent drop.

## Files

- `lego/backend/internal/webhooks/worker.go`

## Acceptance criteria

- [ ] `defaultClient` refuses non-public destinations at dial and doesn't follow redirects; the injectable client seam still works for tests; a blocked delivery is logged and flows through the normal failure path.

## Out of scope

- The test battery (t003).
