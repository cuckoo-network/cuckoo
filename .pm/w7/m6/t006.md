---
id: w7/m6/t006
title: Render parity — verification state + challenge record consistent across REST · GraphQL · MCP + dashboard, vs Render
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m6/t005]
---

## Objective

Confirm the custom-domain verification flow this milestone adds is consistent across every surface it exposes — REST, GraphQL, and MCP in `lego/backend/` (same fields, semantics, error shapes) and the `dashboard/` UI — and compare against render.com's custom-domain verification behavior. Flag any drift as follow-up rather than silently diverging.

## Context

- bex-api is meant to be Render-compatible (`docs/ADR006-bex-api.md`); the parity ledger row this makes truthful is `docs/ADR018-render-parity.md:59` (Verify / DNS instructions).
- Render's custom-domain flow: add → `pending`/`unverified` with DNS instructions → verify → `verified`, and it does not serve/route until verified. Compare bex's `status` values, challenge-record shape, and "not served until verified" semantics against that.
- Surfaces to cross-check: `lego/backend/internal/apps/rest.go`, the GraphQL custom-domain schema/resolvers, the MCP custom-domain tools, and the dashboard DNS-instructions panel.

## Steps

1. Diff the DomainView fields (`status`, `challengeRecord`, `dnsRecord`) and error shapes across REST, GraphQL, MCP — assert identical names/semantics.
2. Compare the state vocabulary + verification semantics against Render's documented custom-domain flow; note any naming drift (e.g. `verified` vs Render's term) as a conscious match or a filed follow-up.
3. Record the parity assessment (and update the ledger row `docs/ADR018-render-parity.md:59` if its evidence text needs correcting now that verification actually gates routing).

## Files

- `docs/ADR018-render-parity.md` (ledger row update, if needed)
- read-only cross-check of the three adapters + dashboard

## Acceptance criteria

- [ ] `status` + `challengeRecord` + error shapes are identical across REST, GraphQL, MCP; any divergence is documented as intentional or filed as follow-up.
- [ ] Semantics compared against Render's custom-domain verification; the parity ledger row reflects reality (routing gated on ownership).

## Out of scope

- Behavior changes (those are t001–t005); Simplify (t007); tests (t008).
