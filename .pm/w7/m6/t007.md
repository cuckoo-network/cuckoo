---
id: w7/m6/t007
title: Simplify — /simplify over the code this milestone changed
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m6/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (store migration + Domain methods, `domains.go` add/verify/delete + reserved-host predicate, the three adapters, the dashboard panel) — reuse / simplification / efficiency, behavior-preserving. Fold the challenge-record/DNS-lookup helpers and the cross-App/reserved guards into the cleanest shape without changing behavior.

## Context

- Scope is exactly the files touched by t001–t005.
- `/simplify` is quality-only (no bug hunting — that is `/code-review`).
- Watch for duplicated DNS-record construction between `dnsRecordFor` (existing CNAME/ALIAS) and the new challenge-record helper.

## Steps

1. Run `/simplify` over the milestone's changed files.
2. Apply behavior-preserving cleanups; keep tests green (`cd lego/backend && go test ./...`).

## Files

- The files changed by t001–t005 (backend store, `domains.go`, adapters, dashboard).

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff; behavior-preserving cleanups applied.
- [ ] Backend + dashboard build and existing tests still pass.

## Out of scope

- New behavior; new tests (t008).
