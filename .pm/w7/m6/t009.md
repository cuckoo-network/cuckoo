---
id: w7/m6/t009
title: Closeout — DoD verified, milestone moved to done/
worker: worker7
status: todo
estimate: 15m
depends_on: [w7/m6/t008]
---

## Objective

Close the milestone once its definition of done actually holds — not merely when the code is written. Verify the observable end state, then perform the `/pm done` move.

## Context

- DoD (from the milestone README): adding a domain returns a challenge record + `pending`; the host is not routed and no cert is requested until verify confirms DNS ownership; a second App (any tenant) cannot verify a host verified elsewhere; platform/base-domain hosts are rejected; state + challenge record round-trip on REST/GraphQL/MCP and render in the dashboard.
- Closing move mechanics: `.claude/commands/pm.md` § `done` (step 4 moves the whole milestone to `w7/done/m6/` and checks the box in the w7 README).

## Steps

1. Verify the DoD end state (unit/integration evidence; a live-cluster check of pending⇒unrouted / verified⇒routed if a cluster is available — else record what was and wasn't exercised, mirroring the m5 closeout note).
2. Set every remaining task `status: done`, mark all rows `— **DONE**`, set `**Status:** done` in the milestone README.
3. Run `/pm done w7/m6/t009` to trigger the milestone move to `w7/done/m6/` and check `- [x]` in the w7 README.

## Files

- `.pm/w7/m6/**` (statuses), `.pm/w7/README.md` (checkbox)

## Acceptance criteria

- [ ] DoD verified (with a note on what was/wasn't exercised live).
- [ ] Milestone moved to `w7/done/m6/`; w7 README box checked; no tombstone left at `w7/m6`.

## Out of scope

- Any remaining implementation (must be done before this task runs).
