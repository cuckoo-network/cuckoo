---
id: w7/m56/t006
title: Closeout
worker: worker7
status: todo
estimate: 15m
depends_on: [w7/m56/t005]
---

## Objective

Close the milestone once the definition of done actually holds: production gateway on the scoped role, old access revoked, negative verification captured, ADR035 updated, CI-durable allow/deny test green.

## Context

- Board conventions: `.pm/CLAUDE.md` + `.claude/skills/pm/SKILL.md` (`done` subcommand step 4 moves the whole milestone).

## Steps

1. Verify the DoD line by line (production evidence + CI green).
2. Set remaining task frontmatter `status: done`, move tasks to `w7/m56/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w7/m56/` → `w7/done/m56/`; check `- [x]` in `w7/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m56/**`, `.pm/w7/README.md`

## Acceptance criteria

- [ ] DoD verified true before any move.
- [ ] Status in sync across all three places; no tombstone left at the old path.

## Out of scope

- Any scope expansion (file follow-ups instead).
