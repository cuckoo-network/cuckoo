---
id: w7/m85/t006
title: Simplify the code this milestone changed
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m85/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — behavior-preserving only.

## Context

- Main hotspot: the guard should reuse `scripts/`' existing structural-guard helpers (the `w7/m58` single-sourced `.yq` pattern) rather than inventing a parallel one.
- The pinned constants may also now share a shape worth a single declaration site.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving cleanups.
3. File anything out of scope as an inbox note.

## Files

- Whatever t001–t005 touched.

## Acceptance criteria

- [ ] `/simplify` findings are applied or explicitly filed.
- [ ] The guard and its self-test still pass.

## Out of scope

- Refactoring code this milestone did not touch.
