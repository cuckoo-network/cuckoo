---
id: w7/m31/t002
title: CRD field + `effectiveHosts` drops the platform host
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m31/t001]
---

## Objective

Add the policy to the App CRD and make the operator's `effectiveHosts` omit the platform `<subdomain>.<BEX_BASE_DOMAIN>` host when disabled — Ingress rule, cert SANs/TLS secret hosts, and `status.url` all follow (status URL falls to the first custom host per the t001 capture).

## Context

- `effectiveHosts` is the single host seam (w4/m19: Ingress + status host key off it, incl. the static-site resolver).
- Flipping back to `enabled` must restore cleanly (cert re-issue is cert-manager's job — just reconcile the hosts).

## Files

- `lego/types/v1alpha1/app_types.go` (+ codegen), `lego/operator/internal/controller/app_controller.go` (`effectiveHosts` + status)

## Acceptance criteria

- [ ] Envtest: disabled ⇒ platform host absent from Ingress/TLS/status, custom hosts intact; re-enabled ⇒ restored.
