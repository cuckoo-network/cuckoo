---
id: w7/m52/t009
title: Add audited admin override, exclusion, and comp recovery controls
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m52/t002, w7/m52/t007, w7/m52/t008]
---

## Objective

Give authorized operators a safe, audited escape hatch to extend grace, clear a stuck transition, structurally exclude a workspace, or apply the existing 100%-off comp flow.

## Context

- `billing_excluded` exists and `CompCustomer` exists, but the full lifecycle needs explicit recovery semantics.
- Tenant members must never grant themselves billing exceptions.

## Steps

1. Add core admin commands with reason, actor, and before/after state.
2. Reuse existing exclusion/comp logic and route recovery through the same enforcement owner markers.
3. Deny tenant-facing credentials and record every override in audit history.

## Files

- `lego/backend/internal/billing/`
- `lego/backend/internal/store/`
- `lego/backend/internal/audit/`
- `lego/backend/internal/api/`

## Acceptance criteria

- [ ] Only the intended superadmin/control-plane boundary can invoke overrides.
- [ ] Every override is idempotent, reasoned, and audited.
- [ ] Excluded/comped workspaces cannot accidentally enter collection enforcement.

## Out of scope

- Tenant self-service discounts.
