# Investigate cross-workspace authz negative-test coverage

**Type:** sub-hour investigation. **Not a milestone yet** — confirm a gap first.

`w7/m30` shipped a Render-**OpenAPI contract-conformance** suite in CI (shape/field parity). Unconfirmed: whether a systematic **negative-authorization** guard exists — i.e. a test that asserts, for **every** REST/GraphQL/MCP verb, that a **non-member** caller is denied (`can_view` / `can_operate`), so an FGA-model regression that silently opens cross-workspace reads/writes would fail CI.

**Do this first (sub-hour):** grep the backend tests + OpenFGA test suite and map coverage against the full verb list. Determine whether the deny-path is systematically asserted or only spot-checked.

**Outcome branches:**

- Coverage is already systematic ⇒ close this note (no gap).
- Real gap ⇒ **promote to a milestone** (a negative-authz test matrix across all surfaces — the security-regression guard analog of m30's contract suite).

Not asserting a gap — flagging it. Source: `/pm-brainstorm for w7` 2026-07-19.
