---
id: w7/m38/t001
title: Confirm Render's custom-domains list params in the pinned OpenAPI
worker: worker7
status: todo
estimate: 15m
depends_on: []
---

## Objective

Extract the exact `GET /services/{id}/custom-domains` query parameters and response schema from the pinned Render OpenAPI (`render-public-api-1.json`): confirm `cursor`/`limit`, and the enum values for `verificationStatus` and `domainType`, so t002 implements against evidence.

## Acceptance criteria

- [ ] Params + enums recorded with spec citations in the milestone notes.

## Out of scope

- Implementation (t002).
