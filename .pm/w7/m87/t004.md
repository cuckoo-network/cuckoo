---
id: w7/m87/t004
title: "Exercise native cache invalidation across environment sources"
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m87/t003]
---

## Objective

Extend the existing executable build drill to prove the artifact-level contract across the environment paths bex supports.

## Context

- w4/m92 fixed empty/padded/newline byte handling; w4/m93 fixed linked-group delivery and precedence. Both contracts must hold with imported cache.
- build_cache_drill_test.go already creates real build Jobs and records cache-related evidence; reuse it instead of constructing an unrelated benchmark.

## Steps

1. Run unchanged-commit A-to-B cases for literal values, own Secret values, group-only values, and a service override of a linked group.
2. Exercise removal, unlink, empty values, and trailing-newline values, comparing actual output bytes with the existing environment contract.
3. Record the final unchanged-input cache-hit control, cache-disabled control, tool/image versions, and fixture cleanup in dated evidence.

## Files

- `lego/operator/internal/controller/build_cache_drill_test.go`
- `lego/operator/internal/controller/native_env_secret_test.go`
- `lego/operator/internal/build/native_test.go`
- `docs/drills/ (new dated native-build cache evidence)`

## Acceptance criteria

- [ ] Actual artifacts contain the newly requested bytes for each supported case, and precedence matches the runtime environment.
- [ ] A native static-site build is represented in the evidence alongside a running-service build, or a precise shared-path justification and remaining live gap is recorded without claiming it verified.
- [ ] At least one unchanged-input warm hit is observed after invalidation.
- [ ] Disposable build resources and cache/image artifacts are cleaned up according to the drill's scoped ownership rules.

## Out of scope

- A requirement for identical digests across independent clean builds; m86 disproved that invariant.
- Estate-wide performance measurement or serving-App changes.
