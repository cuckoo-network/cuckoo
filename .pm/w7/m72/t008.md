---
id: w7/m72/t008
title: Test coverage
worker: worker7
status: todo
estimate: 1h
depends_on: [w7/m72/t007]
---

## Objective

Add targeted tests per surface proving the bound is enforced.

## Steps

1. pg-sni-proxy: a stalled handshake is dropped within the deadline.
2. activator: steady-state request serves from the index without a kube `List`; flood is bounded.
3. static-server: an oversize object is rejected without an unbounded read.
4. database_exports: a Database beyond the export cap does not create extra Jobs.
5. build/publish: generated Pod specs assert ephemeral-storage request+limit and emptyDir `SizeLimit`.

## Files

- `lego/operator/cmd/activator/*_test.go`, `lego/operator/internal/staticserver/*_test.go`, `lego/operator/internal/controller/database_exports_test.go`, build/publish tests.

## Acceptance criteria

- [ ] Each bound has a passing test asserting the negative path.

## Out of scope

- Load/soak testing.
