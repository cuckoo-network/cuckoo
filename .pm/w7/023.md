# LLM token-metering proxy + `agent_token_units` (ADR047 wave 2)

Why: ACP drivers report zero token usage, so without this proxy agent token consumption can never be billed — and the same choke point is the planned exfiltration control (ADR047 D5/D6).

> **Gate lifted 2026-08-18; audited 2026-08-30.** w3/m41 is done with a green production verifier, so the explicit phase-1 hold no longer applies. This note is ready for an independent w7 scheduling/promotion decision; m41 closeout does not auto-promote it.

ADR047 phase 2's token-billing leg. Note: `@mcpc/acp-ai-provider` reports **zero token usage** (ACP doesn't carry it), so this proxy is the only honest token-metering path — never assume usage can come from the driver.

Scope when promoted (~4h, a real milestone):

- A metering LLM proxy the session's model traffic routes through; the w3/m40 model-endpoint allowlist narrows to the proxy — one mechanism then provides token metering **and** the exfiltration choke point (ADR047 D5/D6), advancing `.pm/w3/008.md`'s credential-brokering/egress-control ask.
- New meter kind `agent_token_units`: model-token passthrough at per-model rates (AI-credits-style) through the same `usage_hourly` → sealed-outbox → Stripe pipeline as w7/m76.
- Packaging groundwork: plan-included quota + metered overage (Devin/Cursor pattern) via Stripe metered Subscriptions with included quantities. **Per-request billing stays rejected** (ADR047 alternatives).
- Unlocks revisiting bundled-token billing / default bundled agent (D7) — still gated on the Claude Code resale/ToS answer.

Companions: `w3/009` (gateway proxy + transcripts), `w5/035` (dashboard UI).
