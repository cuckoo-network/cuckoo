---
id: w7/m86/t007
title: Simplify the code this milestone changed
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m86/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — behavior-preserving only.

## Context

- `build.go`'s Job construction is already long and this milestone adds another conditional branch to it; the arg-building and credential-mounting paths are the likely candidates.
- The cache ref derivation should share one source with the image ref rather than being independently string-built.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving cleanups.
3. File anything out of scope as an inbox note.

## Files

- Whatever t001–t006 touched.

## Acceptance criteria

- [ ] `/simplify` findings are applied or explicitly filed.
- [ ] The gate-off path is still byte-identical; tests pass.

## Out of scope

- Refactoring code this milestone did not touch.
