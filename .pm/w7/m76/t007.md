---
id: w7/m76/t007
title: "Test coverage: accrual windows, hibernation exclusion, gate, seal/export"
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m76/t005]
---

## Objective

Meaningful tests for the meter's real behavior and failure modes: accrual over running windows (partial hours, restarts — no double-count via cursor), hibernated windows accrue zero, weighting math, cardless-gate 402 on each surface, cardless rows withheld from export, and sealed-row export producing a correctly-shaped meter event (stub Stripe via `BEX_STRIPE_API_URL`).

## Context

- Standing closing task. Integration path: real ephemeral Postgres via `BEX_TEST_DB_URI` in CI; Stripe stubbed (existing test pattern from w7/m50–m51).

## Steps

1. Accrual/cursor/weighting unit + integration tests.
2. Hibernation-exclusion test (phase-signal fixture).
3. Gate tests across surfaces; withhold test.
4. Seal/export test against the Stripe stub.

## Files

- `lego/backend/internal/` usage/billing test files

## Acceptance criteria

- [ ] All groups green in CI including the real-Postgres path; no-double-count proven

## Out of scope

- Live Stripe verification (operator runbook territory)
