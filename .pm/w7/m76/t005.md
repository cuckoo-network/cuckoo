---
id: w7/m76/t005
title: "Render parity: usage-surface consistency across REST/GraphQL/MCP"
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m76/t002, w7/m76/t003, w7/m76/t004]
---

## Objective

Standing parity check: the usage surface is a bex extension (Render has no usage API — ADR023 precedent), so parity means cross-surface consistency for the new kind (fields, filters, error shapes) plus the 402 dialect matching ADR046's established shape on all three surfaces. Flag drift as follow-up; update the ADR018 usage row's kind list if it enumerates kinds.

## Context

- docs/ADR006-bex-api.md; docs/ADR018-render-parity.md usage row; ADR046 402 dialect.

## Steps

1. Cross-surface diff for usage reads + gate errors; fix or file drift.
2. Ledger touch-up + prettier.

## Files

- `lego/backend/internal/` usage feature, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Surfaces consistent; ledger accurate

## Out of scope

- Dashboard (existing usage UI; flagged separately if kind-hardcoded)
