---
id: w7/m76/t004
title: "Usage surface: sandbox rows in GET /v1/usage + GraphQL/MCP"
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m76/t001]
---

## Objective

Surface the new meter to tenants: `GET /v1/usage`, the `usage` GraphQL query, and the `get_usage` MCP tool return `sandbox_compute_seconds` rows (workspace-scoped, hourly/daily rollups per the existing shape), consistent across the three surfaces.

## Context

- Usage surface: docs/ADR023-usage-metering.md (`GET /v1/usage`, `usage` query, `get_usage` tool) — add the kind to whatever kind-enum/filter the surface exposes; unknown-kind filters keep their existing 400 behavior.

## Steps

1. Add the kind to the usage read paths + any kind filters/enums.
2. Verify hot-window + compacted-month reads include the kind.

## Files

- `lego/backend/internal/` usage feature (REST/GraphQL/MCP adapters)

## Acceptance criteria

- [ ] Sandbox rows visible on all three surfaces with consistent fields
- [ ] Kind filter accepts the new kind; monthly compaction path covered

## Out of scope

- Dashboard usage UI changes (follows the existing usage UI's kind handling; flag if it hardcodes kinds)
