---
id: w7/m76/t001
title: "New meter kind + emitter: sandbox lifecycle → usage_hourly, weighted, hibernation-excluded"
worker: worker7
status: todo
estimate: 90m
depends_on: []
---

## Objective

Add the `sandbox_compute_seconds` meter kind (ADR047 D6): accrue per-second sandbox compute from the sandbox lifecycle into `usage_hourly`, weighted by the sandbox's vCPU/GB shape (AgentCore-style — CPU-seconds + GB-seconds folded into one weighted unit or two sub-meters, pick and document), excluding hibernated/suspended windows.

## Context

- Rollup mechanics: docs/ADR023-usage-metering.md (`usage_hourly`, per-meter cursors, hourly detail → monthly compaction); existing kinds: instance-seconds / egress / build-seconds — follow the instance-seconds accrual shape.
- Source of truth for running windows: sandbox lifecycle records (`lego/backend/internal/sandbox/`, w3/m32) + phase signal; hibernation exclusion contract is w3/m42's accurate phase signal — accrue only `running` phases.
- Attribution: workspace-keyed (per-workspace OpenSandbox tenant scoping already stamps ownership).

## Steps

1. Meter kind + accrual query/emitter over sandbox lifecycle state (locate the existing usage emitter package and add the kind beside its siblings).
2. Weighting: derive from the sandbox template/resource shape; document the unit.
3. Cursor + backfill semantics consistent with existing meters (no double-count on restart).

## Files

- `lego/backend/internal/` usage/metering package (beside existing meter kinds), `lego/backend/internal/sandbox/`

## Acceptance criteria

- [ ] Running sandbox accrues weighted rows hourly; suspended windows accrue zero
- [ ] Restart-safe (cursor) — no double-counting proven in tests

## Out of scope

- Token metering (`agent_token_units`, wave 2 — `.pm/w7/023.md`)
