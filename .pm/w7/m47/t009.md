---
id: w7/m47/t009
title: "Test coverage"
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m47/t008]
---

## Objective

Add meaningful tests for the behavior m47 shipped — asserting real behavior and failure modes, not tautologies.

## Context

- Backend tests: `cd lego/backend && go test ./...` (CI-enforced).
- Key behaviors to cover: deterministic `transaction_id`; seal + epoch floor selection (below-floor never emitted, one-time gap warning); exactly-once via `emitted_at` with crash-between-ingest-and-stamp safety; retry classification (429/5xx retry, non-429 4xx → DLQ); `billing_excluded` ⇒ no customer/no events but `estimatedCost` unchanged; `BEX_METRONOME_TOKEN` unset ⇒ emitter never starts.

## Steps

1. Unit tests for the client (batching, retry classification) and emitter (selection, floor, mapping, idempotency).
2. A store-level test for the `emitted_at` outbox stamping under simulated failure.
3. Confirm the byte-identical-when-off path with a disabled-client test.

## Files

- `lego/backend/internal/billing/*_test.go`
- store/migration test as needed

## Acceptance criteria

- [ ] Tests assert the seal/epoch/idempotency/retry/exclusion/disabled behaviors above and fail if any regresses.
- [ ] `go test ./...` green in the backend module.

## Out of scope

- Live end-to-end against a real Metronome org (that is the t007 harness, run manually).
