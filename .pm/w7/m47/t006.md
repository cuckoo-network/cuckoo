---
id: w7/m47/t006
title: ".env.example/.env.template + CLAUDE.md env-table sync"
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m47/t005]
---

## Objective

Mirror the four new emitter env vars into the checked-in, value-less env templates and the CLAUDE.md env table, per the repo's sync rule.

## Context

- Repo rule: `.env.example` (local runtime) and `.env.template` (CI secrets) are value-less mirrors of `.env` — whenever a var is added/renamed/removed, mirror name + comment (never the value).
- New vars (ADR040 §6): `BEX_METRONOME_TOKEN`, `BEX_METRONOME_URL`, `BEX_METRONOME_SEAL_HOURS`, `BEX_METRONOME_EPOCH`.
- CLAUDE.md has the canonical env-var table (component = bex-api) — add rows there too.

## Steps

1. Add the four vars (name + one-line comment, no value) to `.env.example` and `.env.template`, keeping them in sync.
2. Add matching rows to the CLAUDE.md bex-api env table (semantics + unset-behavior, byte-identical when `TOKEN` unset).
3. `git grep` the four names to confirm no stray/renamed occurrences.

## Files

- `.env.example`
- `.env.template`
- `CLAUDE.md`

## Acceptance criteria

- [ ] All four vars appear (value-less) in both `.env.example` and `.env.template`.
- [ ] CLAUDE.md env table documents each var + its unset behavior.
- [ ] `.env.example` and `.env.template` carry the identical set of new names.

## Out of scope

- Any runtime `.env` (never committed).
