---
id: w7/m47/t010
title: "Closeout"
worker: worker7
status: todo
estimate: 10m
depends_on: [w7/m47/t009]
---

## Objective

Close m47 once its definition of done actually holds — not merely when the code is written.

## Context

- DoD (milestone README): sealed rows emitted exactly once with deterministic `transaction_id`; customers keyed by ingest alias; reconciliation harness matches within rounding; retry/DLQ correct; epoch floor prevents pre-billing backfill; `billing_excluded` tenants out but still see `estimatedCost`; byte-identical when off.
- Verify the reconciliation harness (t007) actually passed against a real export before closing.

## Steps

1. Confirm the DoD is observably met (run t007; check the disabled-path byte-identity).
2. Run `/pm done w7/m47/t010` last — set every remaining task `status: done`, move each `tNNN.md` to `w7/m47/done/`, mark rows `— **DONE**` + `**Status:** done`, move the whole `w7/m47/` to `w8/done/m47/`, and check `- [x]` in the w8 README.

## Files

- `.pm/w7/m47/*` (moves), `.pm/w8/README.md`

## Acceptance criteria

- [ ] DoD verified real (reconciliation passed), not just code-complete.
- [ ] Milestone moved to `w8/done/m47/`; w8 README box checked.

## Out of scope

- m48 (billing surface) — a separate milestone.
