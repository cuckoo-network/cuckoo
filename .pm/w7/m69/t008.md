---
id: w7/m69/t008
title: Test coverage — shellcheck gate + DRY_RUN self-tests
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m69/t006]
---

## Objective

Keep the scripts from rotting between drills: static analysis in CI plus hermetic self-tests of the logic that can be tested without a cluster or prod credentials.

## Context

- CI can't restore prod, so split coverage: (1) `shellcheck` over `scripts/restore-*.sh` in CI (precedent: existing script guards in the repo's workflows); (2) hermetic tests for the pure logic — snapshot selection (latest-by-RFC3339-name), parameter validation failing closed, integrity-check rejection of a corrupt fixture, `DRY_RUN=1` producing zero side effects (assert no kubectl/aws mutations via a PATH-shimmed fake).
- Anti-tautology: each test must fail when its behavior is broken (e.g. corrupt-fixture test fails if the integrity check is removed).
- Live E2E correctness is t005's job, not CI's — say so in the test file header so nobody "improves" CI into needing prod.

## Steps

1. Add the shellcheck CI step (or extend the existing script-guard job).
2. Write the hermetic self-tests (PATH-shim harness; fixtures under the test dir, tiny and synthetic).

## Files

- `.github/workflows/` (shellcheck wiring)
- `scripts/` test harness + fixtures (follow the `test_stripe_billing_secret.py` precedent for script-adjacent tests)

## Acceptance criteria

- [ ] shellcheck green in CI over all four scripts; red on an introduced violation (self-test).
- [ ] DRY_RUN self-test proves zero mutations via the shim.
- [ ] Corrupt-fixture and bad-parameter paths fail closed.

## Out of scope

- Live restore in CI (never — t005 and future re-drills own live verification).
