---
id: w7/m69/t006
title: Amend ADR031 — script contract + runbooks point at scripts
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m69/t005]
---

## Objective

Make the scripts the executable truth: ADR031's restore sections reference them first, prose stays as the explainer, and the shared script contract is documented once.

## Context

- `docs/ADR031-platform-data-backup.md` restore sections (+ the pointers from ADR011/ADR015/ADR021 into it or vice versa — follow the existing cross-reference direction).
- Contract to document: parameters, `DRY_RUN=1` semantics, the safety rails (throwaway targets only, never in-place, confirmation gates on anything live-touching, credentials via prompt/env never argv), and the drill-evidence expectation (every script change re-earns its drill record).
- The user's ask included "and ADR": a *new* ADR was considered and declined — ADR031 owns the data-protection topic (one-file-per-topic rule); record that disposition in a sentence so the decision is traceable.

## Steps

1. Amend ADR031 (+ touch ADR011/ADR015/ADR021 pointers where they name manual steps the scripts now own).
2. `npx prettier@3.4.2 --write "docs/*.md"`.

## Files

- `docs/ADR031-platform-data-backup.md`
- `docs/ADR011-etcd-backup-restore.md`, `docs/ADR015-openbao-backup-restore.md`, `docs/ADR021-keyvalue-management.md` (pointer updates only)

## Acceptance criteria

- [ ] Each restore section leads with its script invocation; no doc still presents ad-hoc commands as the primary path.
- [ ] Script contract + safety rails documented once, referenced elsewhere.
- [ ] "All restores manual" consequence updated to the shipped state.

## Out of scope

- RPO/RTO objectives table (`w7/016` — it will cite t005's timings).
