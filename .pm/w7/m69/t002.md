---
id: w7/m69/t002
title: scripts/restore-openbao.sh — scripted ADR015 restore (both paths)
worker: worker7
status: todo
estimate: 45m
depends_on: []
---

## Objective

Script the OpenBao Raft-snapshot restore for both documented paths: same-instance (live unsealed OpenBao) and fresh-node (new init + `--force` + restart + unseal with OLD keys).

## Context

- Runbook: `docs/ADR015-openbao-backup-restore.md` (both paths; fresh-node `--force` requirement discovered in the 2026-07-14 drill, w7/m29 — encode it so it's never re-discovered under pressure).
- Snapshots: `s3://bex-tfstate/openbao-snapshots/` (nightly, 7 kept, gzip).
- The fresh-node path drills against a throwaway (Docker or scratch namespace) — the script's default target; same-instance mode requires an explicit `--live` flag with a confirmation gate.
- Unseal keys come from `.env` (Shamir, ADR013) — the script prompts/reads them without ever echoing or logging them.

## Steps

1. Write the script (fetch → gunzip → target selection → `bao operator raft snapshot restore` with per-path handling → unseal → verification read of a known `tenants/` KV path).
2. Validate the fresh-node path fully against a real prod snapshot in a throwaway; validate `--live` mode in `DRY_RUN=1` only.

## Files

- `scripts/restore-openbao.sh` (new)

## Acceptance criteria

- [ ] Fresh-node restore from the latest prod snapshot succeeds in a throwaway, verification read passes, throwaway torn down.
- [ ] `--force` is applied exactly and only on the fresh-node path.
- [ ] Unseal keys never appear in output, logs, or shell history (read via prompt/env, not argv).

## Out of scope

- Rekey/root-token rotation (ADR037's own runbook).
