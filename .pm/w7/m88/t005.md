---
id: w7/m88/t005
title: "Exercise the clean-rebuild journey and correct cache descriptions"
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m88/t004]
---

## Objective

Complete the existing user/agent journey and remove the obsolete claim that all bex builds are cache-free.

## Context

- The dashboard Manual Deploy menu, GraphQL triggerDeploy, REST deploy creation, MCP trigger_deploy, and the official CLI already expose clearCache.
- ADR018's Trigger a deploy row and several deploy service/MCP descriptions still justify no-op behavior using ephemeral cache-free builds.

## Steps

1. Run a disposable native and Dockerfile warm-to-clear-to-warm journey in dev-7; inspect actual cache execution and artifact results.
2. Exercise REST, GraphQL, MCP, the existing dashboard control, and the pinned unmodified CLI with the same enum semantics.
3. Update affected code comments, MCP descriptions, and ADR018/ADR060 evidence to describe enabled and disabled cache behavior accurately.
4. Record fixture cleanup and any genuinely unverified surface instead of claiming completion from an accepted HTTP response.

## Files

- `lego/backend/internal/deploys/service.go`
- `lego/backend/internal/deploys/rest.go`
- `lego/backend/internal/deploys/mcp.go`
- `dashboard/src/features/services/components/manual-deploy-button.tsx`
- `dashboard/src/features/services/hooks/use-trigger-deploy.ts`
- `lego/operator/internal/controller/build_cache_drill_test.go`
- `docs/ADR018-render-parity.md`
- `docs/ADR060-build-worker-reliability-and-performance.md`
- `docs/drills/ (new dated clear-cache evidence)`

## Acceptance criteria

- [ ] An ordinary deploy hits cache, clear recomputes, and a later ordinary deploy reuses the fresh cache on both affected builders.
- [ ] All existing public surfaces pass clear/do_not_clear correctly and retain the same named errors for invalid input.
- [ ] The no-op/cache-free rationale is removed from active descriptions where it is no longer true.
- [ ] Dated evidence identifies the revision, environment, and completed fixture cleanup.

## Out of scope

- A replacement dashboard control, new CLI implementation, or production cache rollout.
- Claiming unauthenticated documentation review as live Render dashboard evidence.
