---
id: w7/m48/t005
title: "Render parity"
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m48/t004]
---

## Objective

Confirm the new billing surface is consistent across every surface it exposes: REST, GraphQL, MCP (`lego/backend/`) and the dashboard.

## Context

- Standing closing task (pm.md). Render exposes **no** billing/usage API, so this is **bex-ahead internal consistency**, not render.com-matching — verify the three API surfaces + dashboard agree on fields, semantics, and error shapes (ADR006 one-core/thin-adapters).
- `docs/ADR018-render-parity.md` already marks the usage surface "bex ahead"; note the billing extension there rather than claiming a Render parity gap.

## Steps

1. Diff the billing fields across REST/GraphQL/MCP for identical names/semantics/error shapes.
2. Confirm the dashboard consumes exactly those fields.
3. Update the `docs/ADR018-render-parity.md` usage/billing row (bex-ahead extension); flag any cross-surface drift as follow-up.

## Files

- `lego/backend/internal/usage/{rest,graphql,mcp}.go` (review)
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] REST/GraphQL/MCP + dashboard agree on billing fields/semantics/error shapes (drift filed as follow-up if any).
- [ ] Parity ledger updated to record the bex-ahead billing extension.

## Out of scope

- New behavior (this is a consistency audit).
