---
id: w7/m32/t003
title: Static-site coverage (shared static-server host routes)
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m32/t002]
---

## Objective

Static sites get the same gate: the middleware attaches to the static-site host's Ingress (which backs onto the shared static-server, docs/ADR029), scoped per-host so one site's allowlist never gates another's traffic through the shared backend.

## Files

- `lego/operator/internal/controller/` (static-site Ingress path), possibly `lego/operator/internal/staticserver/`

## Acceptance criteria

- [ ] Site A allowlisted + site B open: A blocks out-of-list, B unaffected — asserted by test.
