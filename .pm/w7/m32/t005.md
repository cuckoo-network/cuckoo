---
id: w7/m32/t005
title: Reconcile the PG/KV flat `[]string` divergence (decide + flag)
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m32/t004]
---

## Objective

bex's Postgres/KV `ipAllowList` is a conscious flat `[]string` ("no per-entry description" — ADR018 KV row); services now ship Render's structured shape. Decide: backfill PG/KV to structured (additive read shape, accept-both write) or document the deliberate split in ADR018/ADR006. Either is acceptable — an undecided fork is not.

## Files

- decision recorded in `docs/ADR018-render-parity.md` (+ code if backfilling)

## Acceptance criteria

- [ ] One recorded decision; if split, both rows document it; if backfilled, PG/KV round-trip the structured shape without breaking flat writers.
