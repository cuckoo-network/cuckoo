---
id: w9/m44/t003
title: "Web-service samples on prod: hello-go, hello-node, hello-python, whoami (image)"
worker: worker9
status: todo
estimate: 45m
depends_on: [w9/m44/t002]
---

## Objective

Run the t002 harness against the four plain web-service samples on production and record the evidence; small breakages fixed inline, anything structural handed to t005.

## Context

- `examples/hello-go` (docker build, port 3000, `MESSAGE`), `examples/hello-node` (docker, port 3000), `examples/hello-python` (docker, port 3000, healthCheckPath `/healthz`), `examples/whoami-app.yaml` (prebuilt `traefik/whoami` image — CLI `--image`, no build).
- Repo-backed creates: `--repo https://github.com/bex-co/bex --root-directory examples/<name> --runtime docker --branch main`; the public repo is clonable by prod build Jobs (proven in m43).
- hello-node/hello-python have plausibly never built on the Hetzner cluster; watch build logs (`render logs --resources <srv> --type build` or REST `type=build`) on failure.
- Each sample's expected body comes from its `MESSAGE` env var — pass a unique value per run so verify-up proves the fresh deploy, not a cached response.

## Steps

1. Add the four sample specs to the harness table.
2. Run each lifecycle on prod sequentially (free-tier caps: delete each before the next if needed).
3. Record per-sample evidence in this task file; triage failures (inline fix vs t005).

## Files

- `scripts/samples-lifecycle.sh` (spec table)
- this file (evidence)

## Acceptance criteria

- [ ] Four green lifecycle runs on prod (or documented failures with owned follow-ups in t005)
- [ ] Suspend leg verified as "URL stops serving the app response" and resume restores the exact expected body
- [ ] Post-delete: no pods, no `reg-pull-<name>` Secret, no `app-<name>` htpasswd user

## Out of scope

- static-site/cron/stack (t004)
