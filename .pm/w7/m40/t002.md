---
id: w7/m40/t002
title: MCP `list_custom_domains`: same filters + pagination, descriptions matched to REST vocabulary
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m40/t001]
---

## Objective

Give the MCP `list_custom_domains` tool the same `cursor`/`limit`/`verificationStatus`/`domainType` arguments, sharing t001's helper, so agents see the identical filtered/paged view.

## Context

- Current tool: `lego/backend/internal/apps/mcp.go:968-981` — takes only `serviceArgs`, loops all domains.
- Arg descriptions must use the same vocabulary as the REST query params (`pending|verified`, `apex|subdomain`) so agents can transfer knowledge across surfaces; compare `render-oss/render-mcp-server` naming where applicable.
- Paged MCP list output precedent: the datastore tools from w8/m13.

## Steps

1. Add the four args to the tool schema with REST-consistent descriptions.
2. Route through the shared filter+page helper from t001.
3. Emit the cursor for the next page in the tool result like the other paged tools.

## Files

- `lego/backend/internal/apps/mcp.go`

## Acceptance criteria

- [ ] `list_custom_domains` with each filter returns only matching rows; cursor walk visits every domain exactly once.
- [ ] Invalid enum values return the same error text/shape as the other two surfaces.
- [ ] `go build ./... && go test ./...` green in `lego/backend`.

## Out of scope

- New MCP tools; only the existing list gains args.
