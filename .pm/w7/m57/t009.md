---
id: w7/m57/t009
title: Closeout
worker: worker7
status: todo
estimate: 15m
depends_on: [w7/m57/t008]
---

## Objective

Close the milestone once its definition of done actually holds: the register exists in `docs/` covering every in-scope plane with probe evidence, every finding has exactly one disposition, owned follow-ups are filed on the board, and fixed-in-place findings carry regression tests.

## Context

- Do not run this until the DoD is real — a milestone lands in `done/` when its observable end state exists, not when the tasks are merely written (per `.claude/skills/pm/SKILL.md`).
- Closeout **is** the move: set every task `status: done`, mark every row `— **DONE**` + `**Status:** done` in the milestone README, move `w7/m57/` → `w7/done/m57/`, and check `- [x] **m57**` in `.pm/w7/README.md`.

## Steps

1. Verify the DoD: register present with §Scope/§Findings/§Follow-up/§Accepted-risks, exclusion map proves no double-coverage, each plane probed, dispositions complete, follow-ups filed, regression tests green.
2. Set all task frontmatter to `status: done`; mark the README table rows `— **DONE**` and `**Status:** done`.
3. Move `w7/m57/` → `w7/done/m57/`; check `- [x] **m57**` in `.pm/w7/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m57/**` → `.pm/w7/done/m57/**`, `.pm/w7/README.md`

## Acceptance criteria

- [ ] The DoD is verified true before the move (register real, dispositions complete, tests green).
- [ ] The milestone directory is moved to `done/` and the workstream checkbox is `- [x]`.
- [ ] Status is in sync across task frontmatter, milestone README, and workstream README.

## Out of scope

- Any audit or fix work (all prior tasks own that).
