---
id: w7/m60/t003
title: Env-table / .env.example / internal/api/CLAUDE.md always-public inventory sync
worker: worker7
status: todo
estimate: 15m
depends_on: [w7/m60/t002]
---

## Objective

Sync the documentation surfaces the new knobs touch: `BEX_WEBHOOK_RATE_LIMIT`/`_BURST` rows in the root `CLAUDE.md` env table, value-less lines in `.env.example` (the repo mirror rule), and `internal/api/CLAUDE.md`'s always-public inventory updated to note that the inventory is now CI-enforced (t002) and each route's limiter classification.

## Files

- `CLAUDE.md` (env table)
- `.env.example`
- `lego/backend/internal/api/CLAUDE.md`

## Acceptance criteria

- [ ] All three files updated; `npx prettier@3.4.2 --write` run over touched markdown.
- [ ] `.env.example` names match the code's `os.Getenv` keys exactly.
