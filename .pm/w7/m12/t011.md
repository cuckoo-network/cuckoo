---
id: w7/m12/t011
title: Test coverage for delete-time teardown
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m12/t009]
---

## Objective

Add meaningful tests for the behavior m12 shipped, asserting real teardown outcomes and failure modes — not tautological "function was called" checks.

## Context

- Backend: `apps.Service.Delete` purges secrets (fake purger asserts app identity; nil purger and erroring purger paths still delete the CR).
- Operator envtest: finalizer teardown — build Jobs/kpack Images in a foreign ns removed; TLS Secrets removed; teardown error ⇒ finalizer retained + requeue; NotFound ⇒ success.
- Fake-registry test for zot manifest deletion (auth + anonymous); fake-S3 test for prefix purge scoping (sibling prefixes untouched, empty-prefix refused).
- KeyValue StatefulSet carries the PVC retention policy; Database backup disposition per t007's decision.
- Failure modes matter most: partial teardown must not silently drop the finalizer or fail the API delete verb inconsistently.

## Steps

1. Inventory which of the above t001–t007 already added; fill the gaps.
2. Run `make test` (operator, from `lego/operator/`) and `cd lego/backend && go test ./...` green.

## Files

- `lego/operator/internal/controller/*_test.go`, `lego/operator/internal/{build,publish}/*_test.go`
- `lego/backend/internal/{apps,secrets}/*_test.go`

## Acceptance criteria

- [ ] Every teardown class (secrets, build artifacts, registry, static prefix, TLS, PVC policy, backups) has at least one behavior-asserting test including a failure-mode case.
- [ ] Both Go suites green; no coverage-gaming tests.

## Out of scope

- E2E on a live cluster (t008 owns that).
