---
id: w7/m12/t012
title: Closeout
worker: worker7
status: todo
estimate: 15m
depends_on: [w7/m12/t010, w7/m12/t011]
---

## Objective

Close w7/m12 once its definition of done actually holds: the t008 audit runs green on the mock cluster (zero leftovers for all four resource deletions) — not merely when the code is written.

## Context

- `.pm` conventions (`.pm/CLAUDE.md`): done items move to `done/` folders, whole-directory moves, no tombstones.

## Steps

1. Verify the milestone DoD: t008's `scripts/delete-verify.sh` green, ADR018/ADR009/ADR031 updates landed.
2. Set every remaining task `status: done`, move each `tNNN.md` to `w7/m12/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w7/m12/README.md`.
4. Move `w7/m12/` to `w7/done/m12/`; check `- [x] **m12**` in `w7/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w7/m12/**`, `.pm/w7/README.md`

## Acceptance criteria

- [ ] DoD verified before any move.
- [ ] Status synced in all three places; `w7/m12/` no longer exists outside `w7/done/`.

## Out of scope

- Follow-up drift items t009 filed (they live as inbox notes).
