---
id: w7/m12/t010
title: Simplify the changed code
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m12/t009]
---

## Objective

Run `/simplify` over the code m12 changed (apps/secrets delete path, App finalizer teardown hook + build/zot/publish teardown funcs, keyvalue/database controllers, delete-verify script) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Likely candidates: shared teardown-func plumbing in `app_controller.go`, S3 client duplication between `publish` teardown and backup/static code, secret-name derivation helpers.

## Steps

1. `/simplify` scoped to the m12 diff.
2. Apply accepted findings; re-run `make test` (operator) + `go test ./...` (backend).

## Files

- whatever m12 touched

## Acceptance criteria

- [ ] Simplify pass run and findings applied or explicitly declined.
- [ ] All suites still green.

## Out of scope

- Bug hunting (that's /code-review), new behavior.
