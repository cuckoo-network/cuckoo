---
id: w4/m20/t002
title: Expose `slug`/`subdomain` on the service object
worker: worker4
status: todo
estimate: 45m
depends_on: []
---

## Objective

Add the `slug` field (Render's name for the public-host segment; bex's `App.spec.subdomain`/`apps.slug`, minted w4/m19) to the service object on REST, GraphQL, and MCP, and surface it in the dashboard service header facts.

## Context

- `w4/011`: only observable baked into the URL today; Render's service object carries `slug`. Small, additive, non-breaking — `AppView` + the three render fragments.
- Match Render's field name (`slug`) on the wire; keep bex's internal `subdomain` naming where it already exists.

## Files

- `lego/backend/internal/apps/` (`AppView` + rest/graphql/mcp render paths), `dashboard/src/` service header

## Acceptance criteria

- [ ] `slug` present and correct (incl. the random-suffix case) on all three API surfaces; ADR018's "still open" divergence note updated.
