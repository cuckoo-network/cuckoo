---
id: w4/m14/t008
title: Simplify — run /simplify over the code this milestone changed
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m14/t007]
---

## Objective

Run `/simplify` over everything this milestone touched (`dashboard/src/features/audit/**`, the settings-page mount, `src/i18n/index.ts`) — reuse, simplification, efficiency; behavior-preserving only.

## Steps

1. Run `/simplify`.
2. Verify `yarn typecheck` / `yarn test` still pass after any changes it applies.

## Files

- `dashboard/src/features/audit/**`
- `dashboard/src/features/auth/pages/settings-page/index.tsx`
- `dashboard/src/i18n/index.ts`

## Acceptance criteria

- [ ] `/simplify` run to completion; no behavior change.
- [ ] `yarn typecheck` and `yarn test` green after.

## Out of scope

- New behavior or bug fixes — that's t009/follow-up, not this task.
