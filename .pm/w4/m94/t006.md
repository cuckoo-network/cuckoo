---
id: w4/m94/t006
title: Test coverage
worker: worker4
status: todo
estimate: 35m
depends_on: [w4/m94/t004]
---

## Objective

Add behavioral tests that fail on both current defects. Seed an existing JS/CSS or text asset beside index.html and assert its exact body, content type and cache headers survive catch-all rewrite and matching redirect; a genuine missing route still rewrites/redirects. Assert request-path-only versus destination-only headers on explicit and implicit fallbacks, plus GET/HEAD and the passing wildcard header.

Extend existing staticserver tests (TestSPAFallbackRoute currently seeds only index.html and tests a missing route). Cover directory index, multiple ordered rules, cache hits and bounded origin calls, required origin errors from t003, oversized keys/objects, and safe wildcard redirects without running harmful production probes. Run the affected operator tests/lint and any required changed-contract gates; keep API tests meaningful if no writer code changes.

## Acceptance criteria

- [ ] Pre-fix implementation fails the new existing-object and header assertions; the fixed implementation preserves all named control cases.
- [ ] Live disposable acceptance and cleanup are recorded with exact requests/responses and limitations.
