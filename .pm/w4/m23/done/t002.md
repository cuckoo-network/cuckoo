---
id: w4/m23/t002
title: "suspenders array on the service object"
worker: worker4
status: done
estimate: 30m
depends_on: []
---

## Objective

Render's service object carries `suspenders: []` (enum incl. `user`); bex exposes only the bare `suspended` bool. Emit `["user"]` when suspended via the suspend verb (the only suspender bex has today), `[]` otherwise, across `AppView` + the render fragments — the `slug` (w4/m20/t002) pattern. Never fake values bex can't know (no billing/admin suspends exist).

## Context

- `.pm/w4/014.md` (pinned OpenAPI enum list); `lego/backend/internal/apps/render.go` + `models.go`.

## Files

- `lego/backend/internal/apps/`

## Acceptance criteria

- [ ] Suspended service shows `suspenders: ["user"]`, running shows `[]`, on REST/GraphQL/MCP; conformance suite clean on the field.

## Out of scope

- New suspender kinds (none exist in bex).

## Resolution (2026-07-15) — done

Shipped via the parallel inbox-note session, merged 2026-07-15: ["user"]/[] derived from the Suspended bool by one shared helper across REST/GraphQL/MCP (no AppView field — pure derivation); TestSuspendersPresentOnAllThreeSurfaces. Full write-up: ../done/014.md.
