---
id: w4/m23/t007
title: "Test coverage — meaningful tests for each chore"
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m23/t005]
---

## Objective

Real behavior + failure modes per chore: direction ordering/400 with cursor checks; suspenders across suspend→resume; the fan-out latency bound under a slow fake sink; ipAllowList object round-trip incl. rejecting malformed entries. No tautological tests.

## Acceptance criteria

- [ ] Each chore has failure-mode coverage; backend suite green.

## Resolution (2026-07-15) — done

Covered by the same session: direction ordering + named-400 + forward-cursor semantics (unit, surface, and real-Postgres store tests); suspenders both states on all three surfaces; fallback-loop cap (below-cap unchanged / past-cap 3+1 aggregate / allowed-past-cap served); ipAllowList object round-trip + bad-CIDR-no-orphan + per-field PATCH merge. The slow-sink latency measurement was not separately tested — the cap itself is asserted, which is the bound.
