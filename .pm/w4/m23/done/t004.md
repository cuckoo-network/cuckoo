---
id: w4/m23/t004
title: "Environment ipAllowList REST wire shape: {cidrBlock, description} objects"
worker: worker4
status: done
estimate: 30m
depends_on: []
---

## Objective

Render documents Environment `ipAllowList` as an array of `{cidrBlock, description}` objects on create/retrieve/update; `internal/environments` exposes bare string CIDRs and updates them via the bex-only `/acl` route. Align the standard POST/PATCH input/response wire shape (the RC12/RC14 `IPAllowListEntry` pattern from postgres/keyvalue — accept `description`, persist what bex persists, never fake it back) without changing GraphQL/MCP's product-neutral string-list core.

## Context

- `.pm/w4/017.md` (filed by m22's parity closeout); `lego/backend/internal/environments/rest.go`; the postgres `ipAllowListToWire`/`FromWire` precedent.

## Files

- `lego/backend/internal/environments/`

## Acceptance criteria

- [ ] POST/PATCH accept and return the object shape; conformance suite passes the Environments routes with no new allowlist entry; GraphQL/MCP unchanged.

## Out of scope

- The `/acl` route's existence (stays; it's the protected-status verb from w6/m19).

## Resolution (2026-07-15) — done

Shipped via the parallel inbox-note session, merged 2026-07-15: standard POST/PATCH accept Render's full body ({cidrBlock, description} objects + protectedStatus/networkIsolationEnabled; per-field-partial PATCH; validate-before-create, no orphan row). Full write-up: ../done/017.md.
