---
id: w4/m23/t001
title: "Honor the audit-log direction param or 400 it"
worker: worker4
status: todo
estimate: 30m
depends_on: []
---

## Objective

`GET /v1/owners/{ownerId}/audit-logs` + GraphQL `auditLogs` accept `direction` and silently ignore it (always newest-first). Implement oldest-first ordering (`asc`) or return a named 400 — per the "nothing accepted is ignored" principle (w3/m8: store-only filters 503, unknown values 400). Check cursor semantics stay coherent under the chosen ordering.

## Context

- `.pm/w4/013.md`; `docs/ADR018-render-parity.md` §Audit logs documents the drift.
- `lego/backend/internal/audit/` — `Service.List` + its REST/GraphQL fragments.

## Files

- `lego/backend/internal/audit/`

## Acceptance criteria

- [ ] `direction=asc` returns oldest-first with working cursors, or 400s with a named error; unknown values 400; both surfaces agree.

## Out of scope

- MCP surface changes beyond what the shared verb implies.
