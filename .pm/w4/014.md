# `suspenders` array on the service object

Render's service object carries `suspenders: []` (enum `admin | billing | user | parent_service | stuck_crashlooping | hipaa_enablement | unknown` — pinned OpenAPI, 2026-07-14); bex exposes only the bare `suspended` bool. Sub-hour: emit `["user"]` when suspended via the suspend verb (the only suspender bex has today), `[]` otherwise, across `AppView` + the three render fragments — the `slug` (m20/t002) pattern. Don't fake values bex can't know (no billing/admin suspends exist).

Source: `/pm-brainstorm more milestones for each worker` round 7, 2026-07-14 (systematic field-diff); w4's payload-parity thread (m20).
