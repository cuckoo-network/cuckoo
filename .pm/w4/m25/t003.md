---
id: w4/m25/t003
title: API-key caller → identity email/name resolution
worker: worker4
status: todo
estimate: 45m
depends_on: [w4/m25/t001]
---

## Objective

A machine (API-key/OAuth) caller's `GET /v1/users` resolves through the key→identity binding to the same Kratos identity lookup, returning the owning user's email/name instead of blanks — ADR018:217's left-open half.

## Context

- Keys bind to tenants + identities (w1/m9, w4/m13, w6/m18's `KeyBinder`); Hydra introspection yields the subject.
- `docs/ADR018-render-parity.md:217`: "a machine (API-key) caller's email/name resolution returns empty; leave open".

## Steps

1. Resolve the introspected subject/key binding to a Kratos identity id.
2. Reuse the t002 lookup; cache consistently with the existing identity→tenant cache TTL semantics (w6/m13's stale-cache lesson).

## Files

- lego/backend/internal/workspaces/
- lego/backend/internal/apikeys/ (binding seam if needed)

## Acceptance criteria

- [ ] `render whoami` with a bex API key shows the owner's real email/name against dev-4.

## Out of scope

- Service-account-style keys with no owning human (return the documented empty shape).
