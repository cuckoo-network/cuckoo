# Record read-verb (view) denials in the audit log

`core.Base.authorizeAndAudit` (`lego/backend/internal/core/base.go:288-297`) is already the single chokepoint every verb funnels through — read-relation denials (`can_view`/`can_view_logs`/`can_view_sensitive`) already reach `b.emit(...)` and are dropped one line earlier by the `writeRelations` filter (`lego/backend/internal/core/audit.go:97-106`), a deliberate volume-driven cut noted in `docs/ADR006-bex-api.md:369`.

Closing it is a ~1-2 line policy change in one file, no per-package work: either remove the three read relations from `writeRelations`, or emit whenever `authzErr != nil` regardless of relation. Decide volume/schema first — do read views also want a `Target` recorded, and should `AuthorizeOn`-style workspace-wide read checks also record — before flipping the filter.

Sub-hour — a single, self-contained task, not a milestone.

Source: `/pm-brainstorm more` 2026-07-13 (third pass), confirmed real and still unfiled during the fifth pass 2026-07-13 (`lego/backend/internal/core/audit.go`'s own comments document this as a deliberate, still-open gap).
