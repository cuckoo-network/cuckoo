---
id: w4/m11/t004
title: Prod values + docs — WebAuthn RP ID for dashboard.bex.co, auth.md updated
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m11/t003]
---

## Objective

Finish the prod half: production Kratos values carry the WebAuthn RP ID/origin for `dashboard.bex.co` (passkeys bind to the domain — getting this wrong strands enrollments), and `docs/auth.md` gains the MFA design (methods, AAL policy, recovery codes) replacing any "no MFA" consequence.

## Context

- Prod values overlay under `deploy/gitops/` (the same split t001 used for local); no secrets involved — RP ID/origins are public config.
- Passkey RP ID pitfalls: must be the registrable domain the dashboard is served on; changing it later invalidates existing passkeys — record that constraint in the docs.
- Markdown is CI-checked: run prettier on touched docs.

## Steps

1. Prod values: RP ID `dashboard.bex.co` (or the registrable parent, decided and documented), origins list.
2. `docs/auth.md`: MFA section (methods, AAL, recovery, RP-ID constraint); consequences updated.
3. Argo-diff sanity check that only Kratos config changes ship.

## Files

- `deploy/gitops/` prod values for Kratos
- `docs/auth.md`

## Acceptance criteria

- [ ] Prod values render a Kratos config with the correct RP ID/origins (kustomize/helm-template check).
- [ ] `docs/auth.md` documents the shipped design and the RP-ID-is-forever constraint.

## Out of scope

- Live prod verification with a real passkey (needs the prod dashboard; follow-up smoke after deploy).
