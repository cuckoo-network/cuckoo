---
id: w4/m31/t003
title: Render parity
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m31/t001, w4/m31/t002]
---

## Objective

Narrow parity check: no wire-shape change is intended by this milestone, so verify none leaked — the official Render CLI's device-flow login still completes against the rate-limited routes, and every new throttling response is OAuth-dialect (`writeOAuthError` / RFC 8628 `slow_down`), never Render-REST-dialect. Flag any drift as follow-up rather than silently diverging.

## Context

- These are OAuth protocol routes — the one deliberate error-dialect exception, per `w9/done/m39`'s dialect sweep.
- The gate change (t001) must be invisible on the wire: same 401 shapes, same latency class for cache hits.

## Acceptance criteria

- [ ] CLI login ceremony verified (dev-N stack) or the blocking reason recorded.
- [ ] No REST/GraphQL/MCP shape touched; confirmed by diff inspection.
