---
id: w4/m26/t001
title: Hunt Render's actual audit-log response schema
worker: worker4
status: todo
estimate: 40m
depends_on: []
---

## Objective

Find Render's real audit-log API response shape using every evidence channel that now exists: the pinned OpenAPI (`render-public-api-1.json` — check for an `/audit-logs`-family path or event schema), the `render-oss/cli` source (does any command read audit logs?), render.com docs/changelog, and — if the API surface exists — a live capture against the team's real Render account (the w5/m32 workflow). Record what was searched even on a miss.

## Context

- ADR018:134 records the 2026-06-era conclusion: schema unresolvable from public docs.
- bex's current shape: best-effort fields + `{object, cursor}` per-item envelope (w4/m10).

## Steps

1. Grep the pinned spec for audit paths/schemas.
2. Grep the CLI source for audit-log usage.
3. Check docs; attempt a live REST capture from the real account if a route exists.
4. Record findings (schema, or a where-I-looked ledger) in the milestone notes.

## Acceptance criteria

- [ ] Either a captured/cited Render schema, or a dated exhaustive-search record naming each channel checked.

## Out of scope

- Changing bex code (t002).
