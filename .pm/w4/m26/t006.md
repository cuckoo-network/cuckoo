---
id: w4/m26/t006
title: Test coverage
worker: worker4
status: todo
estimate: 25m
depends_on: [w4/m26/t004]
---

## Objective

If t002 changed the response shape: lock it with a test asserting the field set + envelope on a seeded audit event (REST and GraphQL). If docs-only: verify the existing audit tests still describe the canonical shape and reference the ADR018 rationale; add the shape assertion if none exists.

## Files

- `lego/backend/internal/` audit tests

## Acceptance criteria

- [ ] The canonical shape is test-asserted either way; `go test ./...` passes.
