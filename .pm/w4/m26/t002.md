---
id: w4/m26/t002
title: Diff vs bex's fields + envelope; align, or record parity-by-evidence
worker: worker4
status: todo
estimate: 60m
depends_on: [w4/m26/t001]
---

## Objective

If t001 produced a schema: field-by-field diff against bex's audit-log responses (REST `GET /v1/audit-logs`-family, GraphQL, dashboard consumption) and align names/envelope where it doesn't break the dashboard — or stage the alignment with both shapes if it would. If t001 came up empty: no code change; write the parity-by-evidence rationale (bex's shape is the documented canonical one, with the search record as justification).

## Context

- Audit surface: `lego/backend/internal/` audit package (w4/m10), GraphQL resolvers, dashboard audit page (w4/m15 IA placement).
- Retention/semantics (`BEX_AUDIT_RETENTION_DAYS`) are out of scope — shape only.

## Files

- `lego/backend/internal/` audit REST/GraphQL (only if aligning)
- `dashboard/src/` audit page (only if field names change)

## Acceptance criteria

- [ ] Diff table (or documented no-schema outcome) recorded; any code change keeps the dashboard green.

## Out of scope

- Audit event *coverage* (which actions get logged) — shape/envelope only.
