---
id: w4/m13/t006
title: Test coverage — meaningful tests for the metadata + TTL behavior
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m13/t004]
---

## Objective

Standing closing task. Meaningful tests: the throttle actually throttles (N requests in a minute → one write), async recording never blocks or fails the request (failing store → request still 200), created-by stamps the real subject (session vs machine caller), store-less degradation behaves as documented, and the list surfaces render null last-used as "never". Assert real behavior and failure modes — never tautological.

## Acceptance criteria

- [ ] Throttle, failure-isolation, and degradation paths all covered; `go test ./...` + dashboard suite green.
