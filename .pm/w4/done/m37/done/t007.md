---
id: w4/m37/t007
title: Closeout
worker: worker4
status: done
estimate: 10m
depends_on: [w4/m37/t006]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

- DoD: `bex upgrade` resolves the newest `bex-cli/v*` release, no-ops when current, and for a newer release downloads → verifies (`checksums.txt` + cosign) → atomically replaces with rollback, while deflecting package-manager installs to `brew upgrade`; all covered by hermetic tests.
- Note in the closeout whether a **live** upgrade against a real released asset was exercised (needs ≥2 `bex-cli/v*` releases to exist) or deferred.

## Steps

1. Confirm every t001–t006 acceptance criterion is met and `cli` build + tests are green.
2. Set each task `status: done`, mark rows `— **DONE**` and `**Status:** done` in the milestone README.
3. Move `t001`–`t007` to `w4/m37/done/`, move `w4/m37/` to `w4/done/m37/`, check `- [x]` in the w4 README.

## Files

- `.pm/w4/m37/**`, `.pm/w4/README.md`

## Acceptance criteria

- [ ] DoD verified real (not just code-complete); milestone moved to `w4/done/m37/` and checked off.

## Out of scope

- Anything beyond closing the milestone.
