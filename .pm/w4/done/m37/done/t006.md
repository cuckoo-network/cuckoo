---
id: w4/m37/t006
title: Test coverage
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m37/t003, w4/m37/t004]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — asserting real behavior and failure modes, not tautologies.

## Context

- Cover: per-OS/arch asset resolution (t001), version-current no-op vs newer-replace decision (via the existing `update` package), verification-failure abort leaving the binary untouched (t003), rollback on mid-replace failure (t002), and the brew-path short-circuit (t004).
- Network/GitHub and cosign should be faked/injected so tests are hermetic (no live release fetches).

## Steps

1. Add table-driven tests for asset resolution and the upgrade decision.
2. Add tests for checksum-mismatch abort, bad-signature abort, and successful verify→replace.
3. Add a rollback test (simulated replace failure) and a brew-path short-circuit test.

## Files

- `cli/internal/upgrade/*_test.go`

## Acceptance criteria

- [ ] Tests assert abort-on-bad-checksum, abort-on-bad-signature, rollback, brew short-circuit, and the current-vs-newer decision.
- [ ] `cd cli && go test ./...` green.

## Out of scope

- Live end-to-end upgrade against a real GitHub release (manual verification note in closeout).
