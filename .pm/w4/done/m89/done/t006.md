---
id: w4/m89/t006
title: Simplify the code this milestone changed
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m89/t005]
---

## Objective

Run `/simplify` over this milestone's diff — behavior-preserving.

## Context

- `session-conversation-impl.tsx` is ~795 lines and gains a state derivation in t003; that derivation should be a pure, testable function rather than more inline branching.
- t001–t002 introduce a code→copy mapping that could easily end up duplicated between the composer and the conversation view.

## Steps

1. Run `/simplify` over the changed files.
2. Extract the state derivation and the code→copy mapping to one place each.
3. Re-run backend and dashboard suites.

## Files

- The files touched by t001–t004.

## Acceptance criteria

- [ ] One definition of the conversation state derivation.
- [ ] One code→copy mapping.
- [ ] Backend `go test ./...` and dashboard `yarn typecheck && yarn lint && yarn test` pass.
- [ ] No behavior change.

## Out of scope

- Refactoring the conversation renderer beyond this milestone's diff.
