---
id: w4/m17/t007
title: Test coverage — meaningful tests for the behavior this milestone shipped
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m17/t005]
---

## Objective

Ensure the shipped behavior has real tests: the null-flow-with-challenge branch (t002), the `session_aal2_required` → `aal=aal2` propagation (t003) including the server-fn side, and failure modes (challenge present but session invalid; aal2 signal without a challenge). Tests must assert behavior, not snapshots.

## Context

- `dashboard/src/common/hooks/__tests__/use-ory-flow.test.tsx` — the fixture-driven suite (updated in t003; extend, don't duplicate).
- `dashboard/src/common/server-fn/__tests__/` — home for the `fetchSession` signal tests.

## Steps

1. Review what t002/t003 already added; fill gaps (especially `fetchSession`/`requireAuth` unit coverage of the new signal).
2. Add negative cases: null flow *without* a challenge still bootstraps normally; `aal=aal2` with no session redirects to plain login.

## Files

- `dashboard/src/common/hooks/__tests__/use-ory-flow.test.tsx`
- `dashboard/src/common/server-fn/__tests__/`

## Acceptance criteria

- [ ] Each new branch has a test that fails when the fix is reverted.
- [ ] Full dashboard suite green.

## Out of scope

- Coverage-number chasing.
