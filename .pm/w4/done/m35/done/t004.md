---
id: w4/m35/t004
title: First release bex-cli/v0.1.0: tag, watch, verify every channel end to end
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m35/t003]
---

## Objective

After the m35 changes are shipped and BEX_TAP_PUSH_TOKEN is set: push the bex-cli/v0.1.0 tag (user-approved), watch cli-release.yml to green (its first-ever real run), then verify: four archives + checksums.txt + sigstore bundle on the release; installer one-liner yields bex -v == v0.1.0; cosign verify-blob passes; formula landed in the tap and brew install bex-co/tap/bex works; the m34 update notice sees the release. If the token is not yet provided, everything except the formula push still completes — the tap step is rerun or picked up by the next release.

## Files

- git tag bex-cli/v0.1.0, GitHub release, bex-co/homebrew-tap

## Acceptance criteria

- [ ] See milestone Definition of done (this task's slice of it holds).

## Out of scope

- `bex upgrade` self-update (w4/030); homebrew-core submission; Windows targets.
