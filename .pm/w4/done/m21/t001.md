---
id: w4/m21/t001
title: Capture Render's `notifyOnFail` semantics
worker: worker4
status: done
estimate: 30m
depends_on: []
---

## Objective

Pin Render's exact contract before building: the field's enum values (default/notify/ignore or otherwise), where it lives on the service object, which surfaces can set it, and how it composes with the member-level workspace setting. Record in `docs/render-artifacts/notify-on-fail.md`.

## Context

- Render OpenAPI (service object + update-service body) + render.com/docs notifications page; bex's member-level model is `docs/ADR018-render-parity.md` §Notifications (w3/m9).

## Acceptance criteria

- [ ] Capture pins enum, placement, and default-composition semantics; names planned divergences (e.g. no "deploy started" tier — w3/005).
