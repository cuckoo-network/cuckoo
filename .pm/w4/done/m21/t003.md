---
id: w4/m21/t003
title: `DeployNotifier` honors the per-service override
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m21/t002]
---

## Objective

The reconciler's deploy-close notification hook (`internal/store` `Reconciler.DeployNotifier`, w3/m9) reads the App's `notifyOnFail` before mailing: ignore ⇒ skip failure emails for that service; default ⇒ current member-preference behavior; notify (if the capture has it) ⇒ per capture. Success emails follow the captured scope (does the field govern failures only? — t001 answers).

## Files

- `lego/backend/internal/store/` (DeployNotifier path), `lego/backend/internal/notifications/`

## Acceptance criteria

- [ ] Failure on an "ignore" service emails nobody; a sibling "default" service still emails opted-in members — asserted with a fake mailer.
