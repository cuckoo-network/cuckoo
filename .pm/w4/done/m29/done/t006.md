---
id: w4/m29/t006
title: Test coverage
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m29/t004]
---

## Objective

Add meaningful tests for the behavior this milestone shipped, beyond the per-task ones: the full migration story (legacy CR → normalized → survives schema tightening → admission rejects new legacy input) as one narrative envtest, and the negative cases (malformed `cidr`-less object rejected; normalization preserves descriptions).

## Context

- envtest infrastructure in lego/operator/; the types package's unit tests.

## Steps

1. Add the end-to-end migration-narrative test and negative cases.
2. `cd lego/operator && make test` green.

## Files

- lego/operator/internal/controller/ (envtest)
- lego/types/v1alpha1/ tests

## Acceptance criteria

- [ ] Migration narrative covered end-to-end; failure modes asserted.
- [ ] `make test` green.

## Out of scope

- Coverage games — every test asserts observable behavior.

## Resolution (2026-07-15)

Shape locked at three layers: types unit tests (legacy fixture fails, objects round-trip), envtest admission (string + cidr-less object rejected, structured round-trips), and the normalizer's clusterless 16-assertion harness. Operator `make test`, types, and backend suites green.
