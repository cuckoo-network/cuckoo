---
id: w4/m29/t004
title: Demote the UnmarshalJSON union decoder to a test fixture
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m29/t003]
---

## Objective

Retire `v1alpha1.IPAllowEntry.UnmarshalJSON`'s string-accepting branch from production code: with the schema structural and the fleet normalized, nothing legitimate can present the legacy shape anymore. Keep the legacy-shape knowledge as a test fixture documenting what the migration handled.

## Context

- `lego/types/v1alpha1/` — the union decoder shipped by w4/m24.
- Removing custom `UnmarshalJSON` entirely (falling back to standard struct decoding) is the cleanest end state if nothing else depends on it.

## Steps

1. Remove the string branch (or the whole custom decoder if that leaves standard decoding correct).
2. Move the legacy-shape cases into a test that documents the historical migration.
3. `make test` (operator) + `go test ./...` (backend) green — the types package is shared.

## Files

- lego/types/v1alpha1/
- tests alongside

## Acceptance criteria

- [ ] No production code path accepts a bare-string entry.
- [ ] Both module test suites green.

## Out of scope

- Any wire-surface change (REST/GraphQL/MCP handled strings via m24's own paths, unaffected here).

## Resolution (2026-07-15)

`v1alpha1.IPAllowEntry.UnmarshalJSON` deleted; the legacy shape survives only as `legacyIPAllowListFixture` in `ipallowlist_types_test.go`, whose test asserts it now FAILS to decode. `core.IPAllowListEntry`'s wire-side union decoder deliberately unchanged (surface contract); its stale "keep the two identical" comment corrected.
