---
id: w4/m29/t005
title: Simplify
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m29/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving) — the backfill site and the types package are the changed surface; the original `w4/019` was itself born from a simplify pass, so close the loop cleanly.

## Context

- lego/types/v1alpha1/, the operator backfill site.

## Steps

1. `/simplify` over the milestone's diff; apply accepted findings.

## Files

- as changed

## Acceptance criteria

- [ ] Simplify pass run; findings applied or filed.

## Out of scope

- Bug hunting.

## Resolution (2026-07-15)

3-agent review (reuse/simplification/stale-docs). Applied: falsified clauses in ADR009/ADR021 rewritten ("still decode" → normalized + rejected at admission); `core/cidr.go`'s dead CR-side-twin instruction corrected; the migration history detached from the type godoc so it no longer bakes into both CRD descriptions; misleading `legacy-acl-kv` fixture renamed; the envtest's description-blind middleware assertion dropped (duplicates `TestIPAllowListMiddlewareSpec`); two adjacent pre-existing stale claims fixed on the way (ADR018:71 "same divergence", ADR006 KV name-as-id + omitted-fields line). Declined: one-shot normalizer jq/pluralization cosmetics (already run clean against prod; harness green — churn > value); `scripts/lib/` extraction of the thrice-duplicated migrate-script scaffolding (refactors two other verified migration scripts; revisit if a fourth appears).
