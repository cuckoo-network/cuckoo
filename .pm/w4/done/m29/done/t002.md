---
id: w4/m29/t002
title: Verify + record zero string-shaped CRs remain (prod + local evidence)
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m29/t001]
---

## Objective

Prove the fleet is clean before the schema tightens: after t001's normalization has run everywhere (prod + the shared local cluster + any live `dev-N` stacks), enumerate every `Database`/`KeyValue` CR and record evidence that no string-shaped `ipAllowList` entry remains.

## Context

- The gate between "normalize" and "tighten": t003 must not start until this evidence exists.
- `kubectl get databases.app.bex.co,keyvalues.app.bex.co -A -o json` piped through a jq check for string-typed entries is sufficient evidence; record it in the milestone (evidence file or README note).

## Steps

1. Ensure the deployed operator includes t001; let it reconcile (or run the script) across environments.
2. Run the enumeration; attach output as `evidence/` in this milestone.

## Files

- .pm/w4/m29/evidence/ (new)

## Acceptance criteria

- [ ] Recorded evidence: zero string-shaped entries in prod and local environments, dated.

## Out of scope

- Fixing any CR the sweep finds broken for unrelated reasons — file a note.

## Resolution (2026-07-15)

Prod (all namespaces): both kinds already complete — zero string-shaped entries (every prod list was written through bex-api post-m24). Local CAPD cluster down at verification (ephemeral; recreates through the current write path). Evidence: evidence/normalize-2026-07-15.md.
