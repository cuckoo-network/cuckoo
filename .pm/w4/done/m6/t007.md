---
id: w4/m6/t007
title: "Docs: docs/ADR006-bex-api.md + docs/ADR013-secrets.md product section + env tables"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m6/t006]
---

## Objective

Document the product half: env-vars endpoints in `docs/ADR006-bex-api.md` (REST/GraphQL/MCP, Render-compat notes, 503 semantics), a product section in `docs/ADR013-secrets.md` (path layout, materialization flow + the etcd-projection trade-off, authorization), `BEX_OPENBAO_URL` in the CLAUDE.md env table, and `spec.env`/`envFromSecret` in the deployment docs.

## Context

- Repo rules: prettier on markdown; env tables in CLAUDE.md stay current.
- Cross-link: docs/ADR013-secrets.md (ADR, m5) ↔ docs/ADR006-bex-api.md ↔ docs/ADR004-deployment.md.

## Steps

1. Update the four docs; add a mermaid flow (API → Core → OpenBao → k8s Secret → Deployment) if it clarifies.
2. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR006-bex-api.md`, `docs/ADR013-secrets.md`, `docs/ADR004-deployment.md`, `CLAUDE.md`

## Acceptance criteria

- [ ] All new env vars, endpoints, and CRD fields documented; prettier-clean.

## Out of scope

- Marketing/vision edits.
