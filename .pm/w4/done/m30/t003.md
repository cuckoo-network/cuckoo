---
id: w4/m30/t003
title: Hoist acting-workspace resolution above AuthorizeApp's candidate loops; assess BatchCheck
worker: worker4
status: done
estimate: 40m
depends_on: []
---

## Objective

Stop `AuthorizeApp`'s fallback loops from re-running the loop-invariant `resolveWorkspace(ctx)` store query once per candidate: add a `resourceWorkspace` variant that takes the pre-resolved acting workspace (already computed at the top of `AuthorizeApp`) and use it in the three candidate loops. Verify-first whether OpenFGA BatchCheck is worth adopting for the per-candidate checks; adopt only if it simplifies, otherwise file the assessment.

## Context

- `lego/backend/internal/core/base.go:452` — `acting` resolved once at the top of `AuthorizeApp`.
- `base.go:470/483/513` — each loop candidate calls `resourceWorkspace(ctx, labels)`, which re-calls `resolveWorkspace(ctx)` (`base.go:625`) — ctx-dependent only, so N candidates ⇒ N identical `Workspace.Tenant` queries.
- Denial-path-only (latency, not correctness); the w4/015 audit cap already bounds the audit side. `AuthorizeDatabase`/`AuthorizeKeyValue` call sites (`base.go:569/592`) are single-shot — hoisting there is optional consistency, not required.

## Steps

1. Add the pre-resolved variant (keep `resourceWorkspace` for single-shot callers or make it a thin wrapper).
2. Use it in the three `AuthorizeApp` loops; preserve the exact error/audit semantics (the `actingErr` branch, per-candidate denial rows, the cap).
3. BatchCheck assessment: check the OpenFGA client version's support; note the finding either way.

## Files

- `lego/backend/internal/core/base.go`

## Acceptance criteria

- [x] N colliding candidates ⇒ exactly one workspace resolution (test asserts store call count)
- [x] Error + audit behavior byte-identical (existing authz tests pass)
- [x] BatchCheck decision recorded (adopted or filed with rationale)

## Out of scope

- Changing authorization semantics or audit capping
