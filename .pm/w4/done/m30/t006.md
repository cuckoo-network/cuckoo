---
id: w4/m30/t006
title: Test coverage — merge semantics in the service layer; store-call-count on the collision path
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m30/t003, w4/m30/t004]
---

## Objective

Add meaningful tests: identical patches through REST/GraphQL/MCP yield identical merged results (including the `""→unprotected` default and absent-vs-zero pointer semantics), and `AuthorizeApp`'s collision fallback resolves the acting workspace exactly once for N candidates.

## Context

- `environments/rest_test.go`'s cross-surface pattern (`TestCreateWithACLAcrossRESTGraphQLAndMCP`) is the template for the update triple.
- The authz test needs a counting store/FGA double around `resolveWorkspace`'s backing query.

## Steps

1. Cross-surface update tests: name-only, ACL-only, mixed, zero-value (`networkIsolationEnabled: false`), pre-migration empty-status row.
2. Collision-path test: N apps sharing `LabelServiceName`, assert one workspace resolution + unchanged denial auditing.

## Files

- `lego/backend/internal/environments/*_test.go`
- `lego/backend/internal/core/*_test.go`

## Acceptance criteria

- [x] Cross-surface merge equality asserted, including the pre-migration default
- [x] Store-call-count assertion on the collision path
- [x] Tests assert real failure modes, not snapshots

## Out of scope

- Coverage games
