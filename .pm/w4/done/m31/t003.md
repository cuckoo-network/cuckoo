---
id: w4/m31/t003
title: Render parity
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m31/t001, w4/m31/t002]
---

## Objective

Narrow parity check: no wire-shape change is intended by this milestone, so verify none leaked — the official Render CLI's device-flow login still completes against the rate-limited routes, and every new throttling response is OAuth-dialect (`writeOAuthError` / RFC 8628 `slow_down`), never Render-REST-dialect. Flag any drift as follow-up rather than silently diverging.

## Context

- These are OAuth protocol routes — the one deliberate error-dialect exception, per `w9/done/m39`'s dialect sweep.
- The gate change (t001) must be invisible on the wire: same 401 shapes, same latency class for cache hits.

## Acceptance criteria

- [x] CLI login ceremony verified (dev-N stack) or the blocking reason recorded.
- [x] No REST/GraphQL/MCP shape touched; confirmed by diff inspection.

## Resolution (2026-07-16)

`git status` on the milestone's diff touches only `internal/api/auth.go` (the auth-gate mechanics), `internal/cliauth/` (device-route rate limiting), `internal/api/server.go` (wiring), and `cmd/api/main.go` — no `rest.go`/`graphql.go`/`mcp.go` file in any feature package. Parity holds by construction. The rate-limited 429s reuse `writeOAuthError` (the existing OAuth-dialect helper), unchanged shape, just a new status code. **Live CLI login ceremony not run** — no cluster/dev-N stack access in this session (the same constraint w4/m28's t008 closeout hit); blocking reason recorded here rather than left silent.
