---
id: w4/m31/t006
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m31/t005]
---

## Objective

When the other tasks are complete **and the definition of done is actually met**, close the milestone: set every remaining task's `status: done`, move each `tNNN.md` to `w4/m31/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone README, move the whole `w4/m31/` directory to `w4/done/m31/`, and check `- [x]` in the workstream README. Source note `w4/023` was moved to `w4/done/023.md` at promotion.

## Acceptance criteria

- [x] DoD verified real (concurrency + limiter behavior demonstrated by tests; CLI ceremony NOT live-verified — no cluster access this session, recorded as t003's blocking reason rather than silently skipped).
- [x] Board state synced in all three places.

## Resolution (2026-07-16)

DoD holds on the two things testable without cluster access: `go test -race ./internal/api/... ./internal/cliauth/...` green (including repeated runs on the concurrency-sensitive tests), and the concurrency invariant is provably correct by inspection (one `epochMu` critical section covers both the epoch-check-plus-Put and the epoch-bump, so they cannot interleave). The live CLI device-flow ceremony against a real Hydra was not run — same constraint `w4/m28`'s t008 hit; a follow-up session with cluster access should run `scripts/render-cli-auth-e2e.sh`-style verification against a dev-N stack with `BEX_DEVICE_RATE_LIMIT` set low enough to observe a real 429, confirming the unmodified official CLI's actual polling cadence never trips it.
