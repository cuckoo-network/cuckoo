---
id: w4/m31/t005
title: Test coverage
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m31/t003]
---

## Objective

Meaningful tests for both behaviors: the revocation-epoch concurrency matrix (invalidate-during-flight does not block unrelated auth; no revoked-entry resurrection; epoch bump during RTT discards the stale Put) and the device-route limiter (per-IP shed before Hydra, second IP unaffected, ceremony-cadence admitted, OAuth-dialect 429). Race-enabled (`go test -race`). No tautological coverage.

## Files

- `lego/backend/internal/api/auth_test.go`, `lego/backend/internal/cliauth/*_test.go`

## Acceptance criteria

- [x] All scenarios above asserted and green under `-race`.

## Resolution (2026-07-16)

`internal/api/auth_test.go`: `TestInvalidateDoesNotBlockDuringInFlightUpstreamRTT` (liveness — invalidate returns without waiting on an in-flight RTT), `TestEpochBumpDuringRTTDiscardsStalePut` (non-resurrection, isolated from `invalidate`'s own cache mutations via an unrelated token/client/subject), plus the pre-existing `TestLogoutCannotBeUndoneByInflightIntrospection`. `internal/cliauth/service_test.go`: `TestDeviceRateLimiterShedsFloodPerIPNotGlobally` (per-IP shed, second IP unaffected, upstream call-count proves a shed request never reaches Hydra), `TestDeviceCeremonyAtPollingCadenceIsNotThrottled` (steady arrivals at the limiter's sustained rate never shed — a real-time test, ~1s wall clock, standing in for the RFC 8628 polling cadence without waiting out an actual multi-minute ceremony), `TestDeviceRateLimit429IsOAuthDialect` (all three routes, per-route subtest). `go test -race ./internal/api/... ./internal/cliauth/...` green, plus `-count=20`/`-count=5` repeats on the concurrency-sensitive tests specifically during development to build confidence beyond a single race-detector pass.
