---
id: w4/m82/t008
title: Simplify
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m82/t007]
---

## Resolution (2026-08-16)

Reviewed the diff directly (small/varied enough across five packages that a separate `/simplify` pass wasn't warranted). No findings:

- t003's App-binding check reuses `registryHost`/`pullSecretName`, already defined in `pullsecret.go`, rather than re-deriving host-matching logic a second time.
- t004's `proxyproto` package is a deliberate, documented duplicate of `lego/operator/internal/sniproxy`'s parser (the cross-module boundary forces it — `backend` cannot import `operator`) — the package doc comment says so explicitly, so it doesn't read as an accidental reuse miss to a future reader.
- t005's script + workflow follow the existing `scripts/render-schema-drift.sh` + `render-schema-drift.yml` shape (same checkout pin, same `set -euo pipefail` style) rather than inventing a new pattern.
- No new hand-rolled rate limiter, id-minting, or error-mapping logic was introduced anywhere — every fix rides an existing shared primitive (`core.NewConflictError`, `core.KeyedRateLimiter` via t003's neighbors, the shared error-mapping layer).

## Objective

Run a reuse/simplification/efficiency pass over the code this milestone changed.

## Acceptance criteria

- [x] Reviewed for duplication, dead code, and missed reuse opportunities across all five touched packages.
- [x] No findings requiring a follow-up filed.
