---
id: w4/m82/t005
title: "CI: weekly CLI release-staleness reminder (032 piece 1)"
worker: worker4
status: done
estimate: 30m
depends_on: []
---

## Resolution (2026-08-16)

New `scripts/cli-release-staleness.sh`: a pure `git log`/`git tag` diff (no network) that compares the latest `bex-cli/v*` tag against commits touching `cli/**`, `scripts/bex-cli-*`, `scripts/install-bex*`, or `.github/workflows/cli-release.yml`; exits 0 silently when caught up, exits 1 with the stale commit list on stdout otherwise. New `.github/workflows/cli-release-staleness.yml` (weekly cron `23 7 * * 1` + `workflow_dispatch`, `fetch-depth: 0` checkout for full tag history) runs it and opens/updates a `cli-release-stale`-labeled issue when stale (creating the label on first use), or closes that issue once a release catches the repo back up.

Verified locally against the real repo history: correctly reports 3 unreleased commits since `bex-cli/v0.1.0` right now (the fix that motivated `w4/m34`'s CLI version work landed after that tag).

## Objective

Close `032.md` piece 1: releasing the CLI should depend on a standing reminder, not memory.

## Files

- `scripts/cli-release-staleness.sh` (new)
- `.github/workflows/cli-release-staleness.yml` (new)

## Acceptance criteria

- [x] The script correctly identifies the current real staleness state against `HEAD`.
- [x] The workflow opens/updates a labeled tracking issue when stale.
- [x] The workflow closes that issue once the repo catches back up.
