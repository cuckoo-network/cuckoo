---
id: w4/m82/t007
title: Render parity
worker: worker4
status: done
estimate: 10m
depends_on: [w4/m82/t003, w4/m82/t004]
---

## Resolution (2026-08-16)

Narrow scope: only t003 (registry-credential `Delete`) touches a Render-facing verb. The new refusal lives inside the one shared `registrycreds.Service.Delete` that REST (`rest.go`), GraphQL (`graphql.go`), and MCP (`mcp.go`) all call — no adapter fragment was touched — and rides the existing `core.ErrConflict → 409` sentinel mapping already shared and tested across all three surfaces (`internal/api`'s error-mapping layer, exercised elsewhere via `postgres/service.go` and `secrets/batch.go`'s own `NewConflictError` uses). Cross-surface consistency holds by construction; no new per-adapter check was needed or added.

t001 (static-server timeout), t002 (mobile-only controller), t004 (an internal gateway audit field), t005/t006 (CI + GitHub repo settings) have no REST/GraphQL/MCP/dashboard surface — not applicable.

## Objective

Verify any user/tenant-facing surface this milestone touched stays REST/GraphQL/MCP/dashboard-consistent.

## Acceptance criteria

- [x] Confirmed the one touched surface (registry-credential delete) shares one Service implementation and the existing shared error-mapping contract across all three adapters.
