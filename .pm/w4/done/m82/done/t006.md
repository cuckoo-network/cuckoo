---
id: w4/m82/t006
title: "GitHub: bex-cli/v* tag protection ruleset (032 piece 2)"
worker: worker4
status: done
estimate: 15m
depends_on: []
---

## Resolution (2026-08-16)

Created live via `gh api --method POST repos/bex-co/bex/rulesets` after explicit user confirmation (a live GitHub repo security-settings mutation, not something `/ship` touches or can revert): ruleset **`bex-cli release tags`** (id `20906370`), `target: tag`, `enforcement: active`, condition `ref_name.include: ["refs/tags/bex-cli/v*"]`, rules `creation`/`update`/`deletion`, bypass actors `RepositoryRole` Admin (5) and Maintain (4) with `bypass_mode: always`. Response confirmed `current_user_can_bypass: "always"` — the acting maintainer is not locked out. Everything now flows through `/release`, matching `032.md`'s stated intent ("pushing a Class 2 tag is a public release").

The note's originally-planned `workflow_dispatch` one-click bump piece stays superseded by the `/release` skill, as `032.md` already recorded before this closeout.

## Objective

Close `032.md` piece 2: restrict `bex-cli/v*` tag creation to maintainers/CI.

## Files

- (live GitHub API mutation — no repo files; the outcome is recorded here and in `032.md`'s own closeout)

## Acceptance criteria

- [x] A GitHub ruleset exists restricting `bex-cli/v*` tag creation/update/deletion below Maintain/Admin role.
- [x] The acting maintainer confirmed as still able to bypass (not locked out of releasing).
