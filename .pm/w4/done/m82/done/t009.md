---
id: w4/m82/t009
title: Test coverage
worker: worker4
status: done
estimate: 10m
depends_on: [w4/m82/t007]
---

## Resolution (2026-08-16)

Every fix shipped with a new regression test exercising the actual failure mode, not just the happy path:

- **t001**: `TestServerTimeoutsConfigured` asserts `ReadTimeout > 0`.
- **t002**: rewrote the test that encoded the bug as intended behavior to assert the fix, plus a new success-path test.
- **t003**: three new tests — explicit-binding refusal, host-match refusal, explicit-elsewhere-doesn't-false-positive.
- **t004**: `proxyproto`'s full parser suite (ported) plus new `Wrap`/`Conn` tests over real `net.Pipe` connections; two new end-to-end `nativessh` gateway tests proving the audited address with and without a trusted peer configured.
- **t005**: verified locally against real repo history (not a synthetic fixture) — confirmed it correctly detects the CLI's actual current staleness.
- **t006**: verified via the ruleset creation response (`current_user_can_bypass`) rather than a unit test — a live GitHub API mutation has no local test surface.

Full suites green: `go build ./... && go vet ./...` for both `lego/backend` and `lego/operator`; targeted `go test` for `cmd/staticserver`, `internal/registrycreds`, `internal/proxyproto`, `internal/sshgateway/...` (`-race`), `cmd/ssh-gateway`; `yarn test:unit && yarn typecheck && yarn lint` for `mobile/`.

## Objective

Add meaningful tests for what this milestone shipped — real behavior and failure modes, not coverage padding.

## Acceptance criteria

- [x] Every fixed finding has at least one new test proving the specific failure mode it closes.
- [x] Full relevant test suites pass, not just the new tests in isolation.
