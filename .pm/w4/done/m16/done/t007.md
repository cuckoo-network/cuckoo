---
id: w4/m16/t007
title: "Simplify — run /simplify over the code this milestone changed"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m16/t006]
---

## Objective

Standing closing task: run `/simplify` over the m16 diff (consent route/page, `hydra-consent.ts` server functions, e2e script) for reuse / simplification / efficiency findings; apply the behavior-preserving ones, file the rest as follow-ups.

## Context

- Scope is the milestone's changed files only.
- Behavior-preserving: the t004 e2e script and existing consent tests must stay green.

## Steps

1. `/simplify` over the m16 diff.
2. Apply accepted findings; note skipped ones (with why) in the milestone README.

## Files

- `dashboard/src/routes/auth.consent.tsx`
- `dashboard/src/common/server-fn/hydra-consent.ts`
- `dashboard/src/features/auth/` (consent page component)
- `scripts/auth-oauth21-e2e.sh`

## Acceptance criteria

- [ ] `/simplify` run; findings applied or explicitly skipped with rationale.
- [ ] `yarn test` / `yarn lint` and the e2e script still green.

## Out of scope

- Bug-hunting (that's `/code-review`, not this task).
