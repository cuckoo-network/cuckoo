---
id: w4/m81/t008
title: Test coverage
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m81/t006]
---

## Objective

Meaningful tests for the behavior this milestone shipped — real assertions on failure modes, not coverage games.

## Context

Minimum set (some land inside their tasks; verify all exist and are honest):

- Store: mode-flip upsert clears the marker; same-mode upsert preserves it; cleared marker closes both the paid-intent gate and `SelectUnemittedUsage` (t001).
- Config: live key + dunning boots and wires `OnLifecycle`; live key alone unchanged; test mode unchanged (t002).
- Scripts: a DRY_RUN matrix for `stripe-billing-secret.sh` (live×portal-id×dunning×ALLOW_LIVE combinations) — shell-level table test or documented manual matrix with captured output; reconcile-lib gate logic (read-only vs mutating × mode × flag) (t003/t005).
- Setup script: webhook ensure idempotence + drift refusal, runnable against test mode (t004).

## Steps

1. Audit what t001–t005 already added; fill the gaps above.
2. `cd lego/backend && go test ./...` and any script-level checks green.

## Files

- `lego/backend/internal/store/billing_pg_test.go`, `lego/backend/cmd/api/` tests, script test harness as appropriate.

## Acceptance criteria

- [ ] Every bullet above has a test (or a recorded, justified exception).
- [ ] All suites green.

## Out of scope

- Live-mode integration tests (no live key exists in CI by design).
