---
id: w4/m81/t006
title: Rewrite runbook §6 into the executable go-live checklist
worker: worker4
status: done
estimate: 45m
depends_on:
  [w4/m81/t001, w4/m81/t002, w4/m81/t003, w4/m81/t004, w4/m81/t005]
---

## Objective

Make `docs/runbooks/stripe-billing-setup.md` §6 a checklist an operator can execute top-to-bottom in the staffed window, folding in every t001–t005 change, fixing the stale facts the research found, and recording the operator-decision inputs (tax classification, epoch, dunning on/off) that must be settled **before** the window.

## Context

- Stale facts: §4 item 2 (`:151`) says the live Subscription has "all **13** items" and `lego/backend/internal/billing/contracts.go:170`'s comment says "13 current dimensions" — the catalog is **14** (`sandbox_compute_seconds` added later; `pricing_test.go:46-61`). Fix both.
- Epoch semantics to document: emitter floor `max(Epoch, now−34d)` (`emitter.go:143`); `BEX_STRIPE_EPOCH` **must** be the cutover instant so test-period usage is permanently below the floor; the binary defaults to `now−34d` when unset (`billing_config.go:35`) — a raw-`kubectl` secret edit bypassing the installer would backfill 34 days of test usage onto live cards. Also `BackdateStartDate` (`contracts.go:70-71`) anchors **every** subscription's cycle to the global epoch, not per-workspace signup — document so invoice-period boundaries aren't misread.
- Tax activation sequence (from the Stripe-docs research; classification requires operator/tax-counsel confirmation, never auto-applied): record `txcd_10102000` (PaaS – business use) as the researched recommendation with `txcd_10101000` (IaaS) as the alternative, behavior `exclusive` (USD/US/B2B convention); both are effectively immutable once stamped (`ensure_price` refuses behavior changes, `stripe-billing-setup.py:204-208`). Sequence: confirm classification → create the **live** registration (status Collecting; test registrations don't count, `sessions.go:377`) → `--live --tax-code … --tax-behavior exclusive` → set the env pair in the live Secret → verify `readiness.tax.configured=true` with `reason=payment_setup_required` until first Checkout.
- New steps to fold in: t001's marker reset SQL (before the live secret rollout), t002's dunning decision at §6 step 4, t003's `--live --dashboard-url` + `bpc_*` requirement, t004's `--webhook-url` provisioning replacing the manual endpoint step, t005's post-cutover `report` verification.
- Keep: double-run catalog discipline, `BEX_STRIPE_ALLOW_LIVE=1`, second-operator prose, staffed-window alert list (the 8 `bex_billing_*` PrometheusRules in `deploy/gitops/base/prometheus.yaml:777-873`), §7 rollback unchanged.
- Out-of-band prerequisites to list explicitly at the top: Stripe account live activation (business verification + payout bank), tax counsel confirmation + live registration, the decided epoch instant.

## Steps

1. Fix the two "13"→14 occurrences (runbook `:151`, `contracts.go:170` comment).
2. Restructure §6 as: Prerequisites (out-of-band) → Decisions record (epoch / tax / dunning / portal) → Provisioning commands (catalog+portal+webhook, double-run) → Pre-deploy checklist → Cutover window (marker reset → installer → verify) → Post-cutover verification (`report`, readiness, alerts).
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/runbooks/stripe-billing-setup.md`
- `lego/backend/internal/billing/contracts.go` (comment only)

## Acceptance criteria

- [ ] §6 contains every step above in executable order, including the marker reset SQL and the tax sequence with the recorded recommendation + confirmation requirement.
- [ ] No "13 items"/"13 dimensions" text remains; both read 14.
- [ ] The epoch-omission footgun and the global cycle-anchor semantics are documented.
- [ ] Prettier clean.

## Out of scope

- Executing any part of the checklist against live Stripe.
- ADR-level policy changes (this is runbook + comment hygiene).
