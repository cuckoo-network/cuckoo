---
id: w4/m38/t003
title: Bump CLI-token lifespan to 7d + document the sticky-race mitigation
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m38/t002]
---

## Objective

Move the CLI-token lifespan from the interim 25h trial to Render's 7d posture, now that the t002 idempotency layer removes the "sticky lost race" hazard a lifespan-only bump would otherwise create.

## Context

- `scripts/auth-bootstrap-client.sh:159` `CLI_TOKEN_LIFESPAN=25h` — the comment says "deliberate trial value — bump toward 7d if it proves out." Both `device_authorization_grant_access_token_lifespan` and `refresh_token_grant_access_token_lifespan` use it; the script already asserts the value round-trips on the stored Hydra client (prefix match).
- `FUTURE-MAYBE.md:33` warns lifespan-only at 7d makes a lost race **sticky** (a revoked-but-persisted token isn't refreshed again for ~6 days). t002 removes that failure mode, so the bump is now safe.

## Steps

1. Set `CLI_TOKEN_LIFESPAN=7d` (or `168h`) and confirm the round-trip assertion still passes against Hydra's normalized duration.
2. Update the surrounding comment to reflect that 7d is now the standing value and why it is safe (the idempotency layer).
3. Retire the `FUTURE-MAYBE.md:33` entry (or annotate it done) as part of closeout.

## Files

- `scripts/auth-bootstrap-client.sh`
- `.pm/FUTURE-MAYBE.md` (annotate at closeout)

## Acceptance criteria

- [ ] Bootstrap mints 7d lifespans for both grants and the round-trip guard passes.
- [ ] The comment explains the 7d + idempotency-layer rationale.

## Out of scope

- Re-running the live bootstrap against production Hydra (an operator step; note it in closeout if not run).
