---
id: w4/m38/t005
title: Test coverage
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m38/t003]
---

## Objective

Add meaningful tests for the idempotency behavior, asserting real failure modes (the concurrent-refresh race) rather than tautologies. This task also carries the CLI-compat check in place of the omitted Render-parity task.

## Context

- The core assertion: two concurrent `POST /v1/token/refresh/` of the same token produce **one** upstream Hydra call and return an **identical** pair — the failure this milestone fixes. Fake/stub the Hydra upstream to count calls and to simulate the revoke-on-second-mint behavior.
- Run against a real control-plane Postgres so the advisory lock + durable store are actually exercised (the backend suite already spins ephemeral Postgres in CI, `BEX_TEST_DB_URI`).
- Cover: durable dedupe survives a simulated restart (second call after the first persisted returns the cached pair), distinct tokens don't collide, and an upstream error is surfaced-not-cached.

## Steps

1. Add a concurrency test issuing two simultaneous refreshes for one token; assert one Hydra call + identical response to both.
2. Add durability + distinct-token + error-not-cached cases.
3. Assert the response body/status stays byte-identical to the pre-change proxy shape (CLI-compat).

## Files

- `lego/backend/internal/cliauth/service_test.go`

## Acceptance criteria

- [ ] Concurrent same-token refresh: one upstream call, identical pair, no revocation race.
- [ ] Durable dedupe, distinct-token isolation, and error-not-cached all covered.
- [ ] `cd lego/backend && go test ./internal/cliauth` green (with the ephemeral-Postgres path).

## Out of scope

- Live production CLI ceremony (an operator/e2e step).
