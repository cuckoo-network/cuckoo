---
id: w4/m4/t007
title: "Simplify — run /simplify over the code this milestone changed"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m4/t006]
---

## Objective

Run `/simplify` over the m4 diff — authz.go + adapter guards, GitOps manifests/values, `authz-model.sh`/seeding, E2E additions — for reuse, simplification, efficiency, and altitude. Behavior-preserving only.

## Context

- Standing closing task. Watch particularly: the Checker cache vs the introspection cache (two TTL caches in one package — share or justify), and script overlap with the existing auth scripts.

## Steps

1. `/simplify` scoped to the milestone's changed files; apply accepted cleanups, note rejections with reasons.
2. `make test` + the t005 E2E must stay green afterwards.

## Files

- `operator/internal/api/`, `deploy/gitops/`, `scripts/`

## Acceptance criteria

- [ ] Cleanups applied or explicitly rejected with reasons; tests + E2E green.

## Out of scope

- New functionality or behavior changes.
