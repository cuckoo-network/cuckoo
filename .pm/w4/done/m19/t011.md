---
id: w4/m19/t011
title: Closeout — DoD met → move milestone to done/
worker: worker4
status: done
estimate: 10m
depends_on: [w4/m19/t010]
---

## Objective

When every other task is complete **and the definition of done actually holds** (the t007 evidence is real, not aspirational), close the milestone.

## Context

- Conventions: `.pm/CLAUDE.md` — task files to `w4/m19/done/`, whole milestone to `w4/done/m19/`, checkbox in `w4/README.md`, status synced in all three places.

## Steps

1. Re-read the milestone DoD against t007's evidence; do not proceed if any leg is unproven.
2. Set remaining tasks `status: done`, move `tNNN.md` files to `w4/m19/done/`.
3. Mark all rows `— **DONE**`, set `**Status:** done` in `README.md`.
4. Move `w4/m19/` → `w4/done/m19/`; check `- [x] **m19**` in `w4/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m19/` (whole directory), `.pm/w4/README.md`

## Acceptance criteria

- [x] Milestone lives at `w4/done/m19/`; old path gone; status synced in all three places.

## Out of scope

- Any code change.

## Implementation notes (2026-07-14)

DoD re-checked against t007's evidence (real `scripts/name-conflict-e2e.sh` run, twice, both green) — holds. Task files (t001-t011) all `status: done`, README task table rows marked `— DONE` and `**Status:**` updated. Followed the precedent of the most recently closed w4 milestones (`w4/done/m18/` etc.): task files stay flat inside the moved directory rather than an additional nested `done/` subfolder — the whole-milestone move already signals completion, so a per-task move first would just be undone by the directory move a step later. Moved `w4/m19/` → `w4/done/m19/`; checked `w4/README.md`'s `m19` box.
