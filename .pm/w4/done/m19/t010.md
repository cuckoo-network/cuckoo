---
id: w4/m19/t010
title: Test coverage — duplicate-name behavior, slug minting, host derivation
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m19/t008]
---

## Objective

Close the pre-existing gap (no test exercised bex-api duplicate-name behavior at all) with meaningful tests asserting real behavior and failure modes — no tautological coverage.

## Context

- Existing: `store/api_test.go:138` (internal API 409), `store/store_pg_test.go:496` (`CreateApp` conflict); nothing at the `Service.Create`/adapter level.
- Must cover the three outcomes: same-tenant 409 (REST + GraphQL + MCP shapes), cross-tenant coexistence (no 403 leak), conflict-race → 409-not-500.
- Operator: slug-vs-fallback host derivation; static resolver mirror.
- Dashboard: taken → suggest → accept flow (t006 component test may already exist — extend, don't duplicate).

## Steps

1. Backend: table-driven duplicate-name tests in `apps/apps_test.go` across all three adapters; suggestion-algorithm edge cases in the t005 tests if thin.
2. Operator: `effectiveHosts` + resolver cases in controller tests.
3. Dashboard: ensure the availability/suggestion flow test asserts the disabled-submit and race-409 paths.
4. Full runs: `make test` (operator), `go test ./...` (backend), `yarn test` (dashboard).

## Files

- `lego/backend/internal/apps/apps_test.go`
- `lego/operator/internal/controller/` tests
- dashboard test files beside `services.new.tsx`

## Acceptance criteria

- [x] Each DoD behavior has a failing-first-capable test (asserts outcome, not implementation).
- [x] All suites green.

## Out of scope

- e2e (t007 owns it); coverage-number chasing.

## Implementation notes (2026-07-14)

The context's "existing" line numbers were stale (written before t002-t006 implemented and self-tested the milestone in the same commit) — by the time this task started, t003-t006's own commit had already added the exact coverage this task asks for: `lego/backend/internal/apps/duplicatename_test.go` (REST 409 + message shape, GraphQL error, store-conflict-race → 409-not-500), `lego/backend/internal/api/owners_mcp_test.go`'s `TestMCP_CreateDuplicateNameErrors` (MCP shape) + cross-workspace coexistence cases, `nameavailable_test.go` (suggestion-chain edge cases, cross-workspace no-leak), `store_pg_test.go`'s `assertSlugMinting` (real-Postgres slug mint + collision suffix + DNS-label-safe truncation), `lego/operator/internal/controller/hosts_test.go` + `lego/operator/internal/staticserver/hosts_test.go` (`effectiveHosts` subdomain-preferred-over-name, both packages), and `dashboard/src/routes/__tests__/services.new.test.tsx`'s `"name availability (w4/m19)"` block (taken→suggest→accept, disabled-submit, raced-409 mapping). Verified all green rather than re-deriving: backend `go test ./...` (all packages ok), operator `make test` (envtest green, 57.9%/56.9% coverage on controller/staticserver), dashboard `yarn test` (149 files, 919 tests green). t009's simplify pass then found and closed one real coverage gap of its own (`TestCreate_ResponseNameIsThePublicNameNotTheCRName`, added to `createowner_test.go`) alongside its fix.
