---
id: w4/m19/t009
title: Simplify — run /simplify over the code this milestone changed
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m19/t008]
---

## Objective

Run `/simplify` over the milestone's diff (backend create/slug path, operator host derivation, dashboard form) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Likely candidates: the slug-mint retry loop vs the domains-conflict pattern; duplicated conflict-conversion code (`domains.go` vs `service.go`); dashboard availability debounce vs existing query hooks.

## Steps

1. `/simplify` scoped to the milestone's changed files.
2. Apply accepted findings; keep behavior identical (tests stay green).

## Files

- Milestone diff (backend, operator, dashboard)

## Acceptance criteria

- [x] /simplify run; accepted findings applied; all tests still green.

## Out of scope

- Bug hunting (that's /code-review); new features.

## Implementation notes (2026-07-14)

Ran the diff (`git diff f99e946^..f99e946`) through 4 parallel review agents (reuse/simplification/efficiency/altitude). Applied:

- **Correctness fix surfaced by the altitude pass** (worth calling out even though `/simplify` doesn't hunt bugs — it fell directly out of a duplication finding): `service.go`'s `view()` returned the raw k8s object name (`a.Name`) as the public `name`/`id` field on every surface — for a tenant-resolved create that's now `core.CRName(tenant, name)` (e.g. `"tea-xxxx-beancount-cms"`), not the name the caller typed, since this milestone introduced tenant-prefixed CR naming. Fixed to `publicName(a)` (the existing `LabelServiceName`-aware projection, already in-package from t005). No existing test caught it (`createowner_test.go`'s fixtures never asserted on `.Name`) — added `TestCreate_ResponseNameIsThePublicNameNotTheCRName`, which also proves the round-trip (`Get` on the returned id resolves, because it's now the real public name `GetApp`'s first candidate expects).
- **A second real drift found alongside it**: `create()`'s store-write error handling classifies `store.ErrConflict` → `core.ErrConflict` (409); the sibling `createNewApp()` (used by `applyCreate`/the blueprint stack path on a not-found) didn't, so an identical TOCTOU race on that path would have 500'd instead of 409'd. Added the same classification there.
- **Reuse + altitude, both flagged the same triplication**: the `spec.subdomain` → fallback-to-`.Name` snippet was hand-copied in `domains.go`, `app_controller.go`, and `staticserver/resolver.go`. Extracted to `AppSpec.PlatformSubdomain(name)` in `lego/types/v1alpha1/app_types.go` (all three call sites already imported `lego/types`), one shared rule instead of three hand-synced copies.
- **Skipped, with reason**: (1) the deeper `create()`/`createNewApp()` merge (~80 duplicated lines) — the acute drift bug is fixed; a full merge this late risks the cap-check/dry-run differences between the two. (2) The three-way `GetApp`/`AuthorizeApp`/`nameTaken` candidate-resolution duplication — authz-critical code, a shared-primitive extraction is real but riskier than this pass's budget. (3) `nameavailable.go`'s full-workspace `List` to answer a single-name check — real but not hot-path (debounced human typing, not a polled endpoint). (4) The dashboard's "adjust state during render" `lastAutoSwappedName` pattern flagged by one agent as unusual — reviewed and kept: it's the React-documented alternative to a `useEffect` here specifically to avoid a double-render/loop, already explained in an adjacent code comment; the suggested `useEffect` alternative would be a regression, not a simplification.
- All three suites re-run green after every fix: backend `go test ./...`, operator `make test`, dashboard `yarn vitest run services.new.test.tsx`; `scripts/name-conflict-e2e.sh` re-run end-to-end afterward (t007) confirmed no regression.
