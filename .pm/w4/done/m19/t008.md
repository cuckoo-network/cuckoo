---
id: w4/m19/t008
title: "Render parity — cross-surface consistency + ADR018 create-service row update"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m19/t007]
---

## Objective

Check the milestone's change is consistent across every surface it touches — REST, GraphQL, MCP (same fields, semantics, error shapes) and the dashboard — against Render's documented/observed behavior, and update the parity ledger where semantics changed.

## Context

- Render evidence: t001's `docs/render-artifacts/duplicate-service-names.md` (workspace-unique names; suffixed globally-unique slug; "Name is already in use" + suggestion UX).
- ADR018 create-service row (`docs/ADR018-render-parity.md:19`) currently documents `POST /v1/services` as an **upsert** — t003 changed that; the row and its evidence must be updated, and any consumers of the upsert semantics (docs/ADR017 deploy-from-chat rides `Core.Create`) re-checked.
- MCP tool descriptions were updated in t003 — verify they no longer promise "create again = redeploy".
- Render's API also exposes `slug` on the service object — check whether bex's service payloads should expose the slug/subdomain field for parity, and flag as follow-up if deferred.

## Steps

1. Diff the duplicate-name behavior across REST/GraphQL/MCP/dashboard: same 409 semantics, same message shape.
2. Update `docs/ADR018-render-parity.md` (create-service row + any naming-related cells) with evidence pointers.
3. Re-check docs/ADR017 (deploy-from-chat) and docs/ADR006 for stale upsert language; fix.
4. File follow-up inbox notes for any deliberate drift (e.g. slug not exposed in API payloads).
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- `docs/ADR017-deploy-from-chat.md`, `docs/ADR006-bex-api.md` (stale-language check)

## Acceptance criteria

- [x] All four surfaces agree on duplicate-name semantics; drift (if any) filed as follow-up, not silent.
- [x] ADR018 row reflects reject-on-duplicate with evidence.

## Out of scope

- Implementing any newly-found parity gap (file it).

## Implementation notes (2026-07-14)

- REST/GraphQL/MCP/dashboard confirmed to agree: same 409/error message shape (`conflict: name "..." is already in use`), same `serviceNameAvailable` scoping, MCP tool descriptions already said "rejected... rather than redeployed" since t003 (no stale "create again = redeploy" language left).
- `docs/ADR018-render-parity.md`'s "Create service" row rewritten: ◐/◐/◐/✅ → ✅/✅/✅/✅, replacing the stale "bex App name is a k8s object name, unique cluster-wide, 409 or 403" divergence text with the actual w4/m19 behavior (workspace-unique name reject-409, globally-unique slug with random suffix, `serviceNameAvailable` surface, dashboard inline-suggest UX) — evidence-linked to `docs/render-artifacts/duplicate-service-names.md`.
- `docs/ADR017-deploy-from-chat.md` and `docs/ADR006-bex-api.md` both described `POST /v1/services`/`createService`/`create_web_service` as an **upsert** — true before this milestone, now wrong: added `**Update (w4/m19):**` notes in both explaining the fork — the interactive create surface now rejects duplicates; `deploy`/the blueprint stack path keep the upsert behavior via a dedicated `applyCreate` (already existed pre-milestone, just newly the sole upsert path). Fixed the stale table row + prose in ADR006, the "one verb" Consequences bullet in ADR017.
- Render's service object exposes `slug`; bex's REST/GraphQL/MCP service payloads don't expose `subdomain`/`slug` (only baked into the served URL) — filed as follow-up `.pm/w4/011.md` (small, additive, deferred until a consumer needs it), not silently dropped.
- `npx prettier@3.4.2 --write` run over the touched docs.
