# w4/m27 local evidence — 2026-07-15

## Revisions

- Official unmodified Render CLI: `c23438e8bfc99e2903f611e9a27aef8239b27909`.
- bex base commit: `b3a1d08f7328b4e7fe6b8b69747cce156e2deda9`, plus the uncommitted w4/m27 worktree.
- Locally exercised bex-api binary SHA-256: `43185e7c3bbe0b44d70084342ab6150e901b2ffac6330ff791bb0bd5addd26a1`.

No credential, access token, refresh token, session cookie, kubeconfig, or test-user password is recorded here.

## Bootstrap

Both supported paths ran twice against dev-3 Hydra:

1. `HYDRA_ADMIN_URL=http://localhost:52030 scripts/auth-bootstrap-client.sh` (HTTP admin path, exercised by `dev-3/up.sh`).
2. `BEX_AUTH_NAMESPACE=dev-3-auth scripts/auth-bootstrap-client.sh` with `HYDRA_ADMIN_URL` unset (production-style `kubectl exec` path).

After each rerun, the fixed client existed once with id `429024F5E608930E2A65EF92591A25CC`, device-code + refresh-token grants, `openid offline_access`, `token_endpoint_auth_method=none`, `subject_type=public`, platform metadata, and a null client secret.

## Official CLI behavior

`scripts/render-cli-auth-e2e.sh` passed against dev-3 with `RENDER_API_KEY` unset and a fresh temporary `RENDER_CLI_CONFIG_PATH` for each user. The verifier used a real Chrome process and proved:

- user A completed device login through Hydra → Kratos → consent;
- `render workspaces` returned one real `tea-…` personal workspace and `render services` used it;
- forcing `api.expires_at` into the past made the next unmodified CLI command rotate both the access-token and refresh-token hashes;
- user B received a different personal workspace;
- `render logout` removed user A's local config, the previous access token returned 401, and the refresh chain returned `invalid_grant`;
- user B's CLI session still worked after user A logged out;
- the shared fixed client still existed, stayed public and secretless, and remained absent from `GET /v1/api-keys`.

The final verifier output was:

```text
✓ official CLI browser login, human tenancy, forced refresh, scoped logout,
  two-user isolation, shared secretless client, and API-key-list separation
```

## Regression suites

- `cd lego/backend && go test ./...` — passed.
- `cd dashboard && yarn test` — 191 files / 1145 tests passed.
- `cd dashboard && yarn typecheck && yarn lint` — passed.
- `bash scripts/gitops-validate.sh` — passed.
- Focused bootstrap, shell-syntax, browser-helper syntax, route-boundary, human-vs-machine identity, cache-invalidation race, API-key self-revoke, and dashboard device-flow checks passed.

## Production gate — not passed

Read-only production inspection on 2026-07-15 proved the milestone is not yet deploy-complete:

- `POST https://api.bex.co/v1/device-grant` returned 401, so the deployed API still places the route behind bearer authentication.
- Hydra reported `Unable to locate the resource` for fixed client `429024F5E608930E2A65EF92591A25CC`.
- The currently deployed bex image was `ghcr.io/bex-co/bex-operator@sha256:3cae733b50d6dbe147cdbedc82c3f5cf5580bea59edf0caf3868d6d9fb64fa6a`.

The milestone therefore remains `todo`. After an explicit `$ship`, repeat this same verifier against production with two disposable existing users and record the new deployed image digest before closing t001/t005/t009.
