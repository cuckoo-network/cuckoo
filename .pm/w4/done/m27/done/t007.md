---
id: w4/m27/t007
title: "Simplify — device-flow implementation pass"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m27/t006]
---

## Objective

Run `/simplify` over the milestone diff, concentrating Render-specific translation in one small adapter, removing duplicate Hydra/Kratos plumbing, and keeping API-key and human-CLI revocation paths explicit.

## Acceptance criteria

- [ ] One protocol adapter owns Render field/path translation; shared auth helpers stay shared; behavior and tests remain green.

## Resolution (2026-07-15)

`/simplify` (4 agents) over `441d6040`. Applied: deleted the dead `authMiddleware` wrapper; `introspectUpstream` returns only error (cache is the sole output channel); `proxyForm` failure paths collapsed into one `postForm` + single 503; revoke route moved off the bespoke root mount into a cliauth `RegisterREST` fragment inside the one REST router (invalidate hook injected via `New`); stray `t.Helper()` removed; internal/api/CLAUDE.md public-route inventory updated (the "one exception" sentence was falsified by the device routes). Declined → `w4/023.md`: the revocationMu-across-round-trip epoch refactor + IP-keyed limits for the public routes (auth-gate concurrency deserves focused review, not a closing-task drive-by); `Identity.Human` derivation + RevokeHandler conjunct collapse (defensive explicitness on a security path); script cosmetics (freshly captured prod evidence — rewrites would invalidate it).
