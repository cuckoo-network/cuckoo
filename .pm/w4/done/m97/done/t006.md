---
id: w4/m97/t006
title: "Simplify"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m97/t005]
---

## Objective

Run the repository's simplify workflow over this milestone's changes and apply behavior-preserving quality improvements.

## Context

- Review only the implementation and evidence changes made for this milestone.
- Keep conditional writes, secret custody, authorization, and recovery fences explicit.

## Steps

1. Run `/simplify` over the changed code and apply supported reuse, simplification, and efficiency fixes.
2. Prefer the existing feature-local helpers and workspace import boundaries; remove redundant paths rather than adding a general-purpose framework.
3. Re-run the affected checks after accepted edits and record the outcome.

## Files

- `lego/backend/internal/envgroups/`
- `lego/backend/internal/api/server.go (if changed)`
- `dashboard/src/features/env-groups/ (if changed)`

## Acceptance criteria

- [ ] Simplification preserves concurrency, lifecycle, secret-safety, and error semantics.
- [ ] No needless abstraction or duplicated surface-specific implementation remains in the changed paths.
- [ ] Relevant checks remain green after quality fixes.

## Out of scope

- Unrelated codebase cleanup or bug hunts.
