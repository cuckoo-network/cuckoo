---
id: w4/m92/t002
title: Fix literal preparation and verify the shared runtime consumers
worker: worker4
status: done
estimate: 35m
depends_on: [w4/m92/t001]
---

## Objective and steps

Change preparation and decoding together, preserving both sources' exact bytes. The Secret-file path already encodes bytes directly. Literal `printenv "$key"` at native.go:165 adds one terminator: remove exactly that byte before Base64 encoding, not all terminal newlines.

The actual pinned preparer is BusyBox 1.37.0 at `sha256:9db7b59979c38555a39def84a31fb98b5296952f9e3afd4f6f11f05b07adfab0`. Live local execution of that image confirmed `printenv --help` offers no -0 option; do not prescribe GNU-only printenv flags. It does support `head -c -1`: `printf "abc\n\n" | head -c -1 | od -An -tx1` returned `61 62 63 0a`. A sentinel-based alternative is also possible; execute the chosen path against the pinned image.

Exhaustive production-call search: one `nativeDockerfile` caller (native.go:141), one `nativeBuildPreparer` caller (build.go:1010). The generator selects six pinned toolchains: elixir, go, node, python, ruby, rust (native.go:47-54). This is a global codec fix for the native strategy, not a Go allowlist. App-kind family: web/private/worker/cron can use native runtimes; static uses Node by default and shares the generated RUN; Postgres and Key Value do not use this builder. Dockerfile and kpack strategies remain outside this codec.

Verify code routing and execute tests for literal/Secret values with no newline, one/two trailing newlines, embedded newline, empty value, and padding lengths 0/1/2. Keep duplicate-key precedence, PORT/BEX*NATIVE* filtering, and existing PATH behavior. Group-to-build projection is a separate lead, not assumed correct or included as a proven finding here. Files: native.go, native_test.go, BuildJob wiring and controller build-options/read paths as needed.

## Acceptance criteria

- [ ] Complete the observable checks described above and record results and remaining limits.
