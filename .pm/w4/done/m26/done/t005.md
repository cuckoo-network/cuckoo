---
id: w4/m26/t005
title: Simplify
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m26/t004]
---

## Objective

Run `/simplify` over any code this milestone changed; behavior-preserving. Skip with a note if t002 resolved as docs-only.

## Acceptance criteria

- [ ] `/simplify` run (or documented N/A); tests pass.

## Resolution (2026-07-15)

`/simplify` (4 agents) ran over the working tree. Applied: verb→event vocabulary + `denied()` moved to `service.go` (the events/webhooks vocabulary precedent — fragments stay thin); `core.SplitTarget` added beside the target constructors (first decoder of the format lives with its mint sites); consumer-less GraphQL `event` field dropped; `TestRenderEventVerbsExist` guards the mapping keys against silent method renames; gofmt. Skipped: exporting `Identity.Method` constants (touches auth/tenancy beyond this diff; repo-wide literal precedent), lazy empty-metadata map and the double outcome-compare (negligible per the efficiency review).
