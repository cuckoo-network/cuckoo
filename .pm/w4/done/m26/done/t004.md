---
id: w4/m26/t004
title: Render parity
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m26/t003]
---

## Objective

Whatever shape t002 shipped, verify it is identical across REST, GraphQL, and the dashboard's audit page (fields, envelope, error shapes); flag drift as follow-up. (MCP is a deliberate — for audit logs per the ledger.)

## Acceptance criteria

- [ ] Cross-surface check recorded; drift filed.

## Resolution (2026-07-15)

Cross-surface check: `TestAuditSurfaceParity` now asserts the documented REST↔GraphQL mapping (same id/timestamp/event-value/resource; actor object ↔ flat actor+actorMethod; error↔denied status). Dashboard audit page consumes the unchanged GraphQL dialect — its 16 feature tests pass untouched. MCP remains a deliberate — (Render's own MCP has no audit-log tool). No drift to file.
