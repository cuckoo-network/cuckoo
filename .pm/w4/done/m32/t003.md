---
id: w4/m32/t003
title: "Idempotent backfill sweep: re-run the fan-out for every environment; drop byte-identical datastore clobber residue"
worker: worker4
status: done
estimate: 60m
depends_on: [w4/m32/t002]
---

## Objective

Environments that already had non-empty rules before m28 have members carrying no layer at all — their rules are silently unenforced on Apps until the environment's next incidental write. Ship an idempotent one-shot sweep (the `scripts/ipallowlist-normalize.sh` shape: clusterless-testable, safe to re-run) that re-runs the member fan-out for every environment, and optionally drops a datastore's own `spec.ipAllowList` when it is byte-identical to its environment's list — provably the pre-m28 clobber's residue (harmless double-enforcement, but confusing to read).

## Context

- `.pm/w4/done/025.md` item 3. m28 migrated empty-list environments seeded-open but did not backfill members of environments that already had rules.
- `scripts/ipallowlist-normalize.sh` + its 16-assertion clusterless harness (w4/m29) is the established one-shot-sweep pattern — reuse its shape (dry-run mode, assertion harness, recorded prod run).
- Fan-out entry points: `lego/backend/internal/environments/service.go` (member stamp), `lego/backend/internal/postgres/service.go` / `lego/backend/internal/keyvalue/service.go` (datastore lists).

## Steps

1. Write the sweep: enumerate environments, re-run the m28 member fan-out for each (Apps + datastores), idempotently.
2. Add the optional residue-drop: datastore whose own `spec.ipAllowList` is byte-identical to its environment's list ⇒ drop the datastore's own copy (keep only the environment layer). Guard with a dry-run flag.
3. Clusterless assertion harness mirroring `ipallowlist-normalize`'s: pre-m28-shaped fixtures ⇒ post-sweep state asserted; second run asserted a no-op.
4. Run against prod (or record the m28-closeout pattern if no cluster this session) and note the result in the script header or milestone README.

## Files

- new `scripts/` sweep script + harness (follow `ipallowlist-normalize.sh` naming)
- `lego/backend/internal/environments/service.go` (expose/reuse the fan-out if the sweep drives it through the API/store rather than raw SQL)

## Acceptance criteria

- [x] After the sweep, a member App of a pre-m28 ruled environment carries `spec.environmentIPAllowList` matching the environment.
- [ ] Byte-identical datastore residue lists are dropped only when identical; differing lists untouched. — **declined, see resolution**: the residue-drop was explicitly optional in this task's own objective ("optionally drops"); harmless double-enforcement, not a correctness gap, so left out of a milestone scoped to correctness fixes. Not filed as a follow-up note — revisit only if the residue is ever found confusing in practice.
- [x] Second run is a no-op (harness-asserted).

## Resolution (2026-07-16)

Implemented in Go, not bash+jq: `ipallowlist-normalize.sh`'s shape doesn't fit here because the sweep needs BOTH the control-plane Postgres store (environment rows, current ACL, membership) and k8s CR writes, which a kubectl+jq script can't reach. `environments.Service.backfill` (exposed only via the `environments.Backfiller` wrapper type, mirroring `apps.WorkspacePurger`'s "internal system operation, no Authorize call" shape) does two phases: `store.RepairDriftedEnvironmentIDs` (a real-Postgres-verified one-shot SQL repair for t001-era drift) + a per-environment re-application of the standard member fan-out (equality-gated at every per-resource patch, so a second run is a true no-op — proven by `TestBackfillAppliesFanOutForPreM28Environments`'s two-run assertion and `TestBackfillRepairsDriftedEnvironmentIDsAndClearsTheirCR`). Exposed as the `api environments-backfill` subcommand (`cmd/api/main.go`), mirroring `ipallowlist-normalize.sh`'s "run once per cluster" operational shape. **Not run against a live prod/dev cluster this session** — no cluster access in this environment; the real-Postgres half (`RepairDriftedEnvironmentIDs`) was verified live against an ephemeral local Postgres container, and the full sweep (including the k8s CR fan-out) was verified via fake-client unit tests. Running the actual subcommand against prod/a dev cluster is a follow-up for whoever has cluster access — the command is `api environments-backfill` (needs `BEX_CP_DB_URI`).

## Out of scope

- Workspace-level IP-rule layer (explicit m28 deferral — needs user re-open).
