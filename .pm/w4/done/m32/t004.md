---
id: w4/m32/t004
title: Render parity — lifecycle semantics match Render's documented environment-rule behavior across surfaces
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m32/t003]
---

## Objective

Check the shipped lifecycle semantics against Render's documented behavior and confirm every surface observes identical post-lifecycle state. No new wire shapes are intended — this is a semantics check: service leaves environment ⇒ rules stop applying; project deleted ⇒ members unblocked; pre-existing rules enforce after backfill.

## Context

- `docs/render-artifacts/protected-environments.md` — the capture m28 aligned to; its "bex implementation decisions and drift" section is the comparison baseline.
- REST/GraphQL/MCP environments + services surfaces in `lego/backend/internal/environments/` and `lego/backend/internal/apps/`; dashboard environment editor (w5/m31).

## Steps

1. Compare each lifecycle edge's outcome with Render's documented behavior; flag drift as follow-up notes, never silently diverge.
2. Verify REST, GraphQL, MCP, and the dashboard all report the same membership/rules state after each lifecycle transition.
3. Update `docs/render-artifacts/protected-environments.md`'s drift paragraph if this milestone changes any recorded decision.

## Files

- `docs/render-artifacts/protected-environments.md`
- surface tests under `lego/backend/internal/environments/`

## Acceptance criteria

- [x] Each of the three lifecycle edges compared against Render's docs, result recorded (match or filed drift note).
- [x] Cross-surface consistency spot-checked for post-lifecycle state.

## Out of scope

- New REST/GraphQL/MCP fields or verbs.
