---
id: w4/m32/t005
title: Simplify
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m32/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving). Apply what's safe inline; file declined-with-reason findings as inbox notes.

## Context

- Expected touchpoints: `store/projects.go`, `projects/service.go`, `environments/service.go`, the new sweep script, projector changes.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving findings; re-run the affected tests.

## Files

- the milestone's changed files

## Acceptance criteria

- [x] `/simplify` run; applied findings keep the suite green; declined findings filed with reasons.

## Resolution (2026-07-16)

Ran 4 parallel review agents (reuse, simplification, efficiency, altitude) over the diff. Applied: (1) factored `Delete`'s and the first-cut `ClearMembersForProject`'s duplicated per-environment clear body into a shared `clearEnvironmentMembers` helper — this closed a real bug the duplication had hidden, missing env-group-membership clearing on the project-cascade path (now regression-tested); (2) fixed a double `rows.Close()` call in `SetProjectServices`; (3) dropped `ClearMembersForProject`'s redundant `tenantID` parameter (`store.Environment.TenantID` is always authoritative); (4) restructured the authorization shape entirely — the first cut had bolted a top-level `s.Authorize` onto `ClearServiceEnvironmentLayer`/`ClearMembersForProject`/`Backfill` purely to satisfy the reflection-based authz-completeness sweep, but the altitude review found the codebase's actual established pattern for this exact situation (`apps.WorkspacePurger.PurgeWorkspace`, "internal system operation, no caller identity, never call Authorize") and that the bolted-on check was even mis-scoped (checked the caller's ambient ctx-workspace, not the `tenantID` actually passed in) — restructured as unexported `Service` methods exposed only through two thin wrapper types (`ProjectMemberClearer`, `Backfiller`) that the reflection sweep never walks, which also eliminated the `base.Authz = nil` mutation the `environments-backfill` subcommand originally used (no longer needed — the wrapper never calls Authorize at all). Declined: the SELECT-then-UPDATE→single-CTE collapse in `SetProjectServices` (efficiency agent) — real but marginal on a non-hot-path verb, and the CTE form was judged less readable than the two-step version by the simplification agent's independent pass; the N+1 `ListPostgres`/`ListKeyValues`/`ListEnvironmentServices` calls across `Backfill`'s/`ClearMembersForProject`'s per-environment loops (efficiency agent) — a pre-existing gap in `propagateIPAllowList` predating this milestone, not something these new call sites introduced, and out of scope for a correctness milestone; both declined with reasons rather than fixed, per the review's own qualification that they're lower-priority.

## Out of scope

- Bug-hunting (that's /code-review, not this task).
