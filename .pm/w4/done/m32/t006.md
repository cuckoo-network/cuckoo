---
id: w4/m32/t006
title: Test coverage — lifecycle-edge and idempotency tests asserting real enforcement outcomes
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m32/t004]
---

## Objective

Consolidate meaningful tests for the behavior this milestone shipped: the three lifecycle edges and both sweeps' idempotency, asserting projected CR specs and enforcement-relevant state — never trivial or tautological coverage.

## Context

- t001–t003 each add their own tests; this task closes the gaps between them: cross-edge interactions (e.g. departure then project-delete), the real-Postgres integration path (`BEX_TEST_DB_URI` suite), and CR-spec assertions where earlier tasks only asserted rows.

## Steps

1. Table-driven test over the lifecycle matrix (join/leave/delete × ruled/deny-all/empty environment).
2. Real-Postgres integration test for the store cascade + sweep (`BEX_TEST_DB_URI`-gated; runs in CI).
3. Idempotency assertions for both one-shot sweeps.

## Files

- `lego/backend/internal/store/*_test.go`, `lego/backend/internal/environments/*_test.go`, `lego/backend/internal/projects/*_test.go`
- sweep harness under `scripts/`

## Acceptance criteria

- [x] Lifecycle matrix covered; failures assert observable enforcement state (CR spec), not implementation details.
- [x] `cd lego/backend && go test ./...` green.

## Out of scope

- Live-cluster e2e (belongs to closeout's DoD check if a cluster is available).
