---
id: w4/m1/t008
title: Simplify — run `/simplify` over the code this milestone changed
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m1/t007]
---

## Objective

Run `/simplify` over everything this milestone touched — GitOps manifests/values, `scripts/auth-secrets.sh`, `scripts/auth-verify.sh`, `docs/auth.md` — for reuse, simplification, and efficiency. Behavior-preserving only.

## Context

- Standing closing task; applies to the diff of w4/m1 t001–t007.

## Steps

1. `/simplify` scoped to the milestone's changed files; apply accepted cleanups.
2. Re-run `scripts/auth-verify.sh` to confirm behavior is unchanged.

## Files

- `deploy/gitops/base/` (kratos/hydra/db manifests + values)
- `scripts/auth-secrets.sh`, `scripts/auth-verify.sh`

## Acceptance criteria

- [ ] Cleanups applied or explicitly rejected with reasons; verify script still exits 0.

## Out of scope

- New functionality or behavior changes.
