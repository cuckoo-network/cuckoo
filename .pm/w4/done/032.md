# Release-staleness reminder cron + `bex-cli/v*` tag ruleset

**Closed 2026-08-16 by `w4/m82`.** Both pieces shipped: `scripts/cli-release-staleness.sh` + `.github/workflows/cli-release-staleness.yml` (weekly cron, opens/updates/closes a `cli-release-stale`-labeled issue); a live GitHub ruleset `bex-cli release tags` (id `20906370`) restricting `bex-cli/v*` tag creation/update/deletion to Admin/Maintain roles, created via `gh api` after explicit user confirmation.

From the 2026-08-15 release-engineering discussion (docs/ADR058-release-engineering.md); do after `w4/m35` ships. Originally this note also planned a `workflow_dispatch` one-click bump — superseded the same day by the `/release` skill (`.claude/skills/release/SKILL.md`), which computes/confirms the version, pushes the tag, watches `cli-release.yml`, and verifies every channel. Two pieces remain:

1. ~~**Weekly staleness cron:** open/update an issue when `cli/**`, `scripts/bex-cli-*`, `scripts/install-bex*`, or `.github/workflows/cli-release.yml` have commits newer than the latest `bex-cli/v*` tag — so releasing depends on the reminder, not on memory.~~ **DONE.**
2. ~~**Tag ruleset:** restrict `*/v*` (at least `bex-cli/v*`) tag creation to maintainers/CI — pushing a Class 2 tag is a public release; everything should flow through `/release`.~~ **DONE.**

Graduation (unchanged): release-please manifest mode when ≥2 versioned components or cadence justifies release PRs; verify slash tag-separator support first.
