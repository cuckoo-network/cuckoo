---
id: w4/m25/t001
title: Kratos identity schema: `name` trait + settings/registration surfacing
worker: worker4
status: done
estimate: 45m
depends_on: []
---

## Objective

Add a `name` trait to the Kratos identity schema so bex's identity model tracks it, collectable at registration and editable in settings — without breaking existing identities (the trait is optional).

## Context

- Kratos identity schema lives in the auth GitOps config (deploy/gitops/, kratos config) — w4/m11 touched it last (webauthn identifier).
- Existing identities have no name; schema change must be backward-compatible.
- Dashboard registration/settings pages render Kratos flows via Ory Elements.

## Steps

1. Add optional `name` trait (given/family or single display name — match Render's user object shape from the pinned OpenAPI).
2. Roll the schema through dev-4 and the gitops config; verify existing identities still authenticate.
3. Confirm the settings flow renders the new field for self-service edit.

## Files

- deploy/gitops/ (kratos identity schema)
- .pm/w4/dev-4/ (harness schema if separate)

## Acceptance criteria

- [ ] New registrations can set a name; existing users can add one via settings; old sessions unaffected.

## Out of scope

- bex-api surfaces (t002).
