---
id: w4/m5/t007
title: "Simplify — run /simplify over the code this milestone changed"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m5/t006]
---

## Objective

Run `/simplify` over everything this milestone touched (Argo app + values, overlays, `bao-init.sh`/`bao-verify.sh`, deploy.yml step) — reuse, simplification, efficiency; strictly behavior-preserving.

## Context

- Standing closing task; scope = this milestone's diff only.

## Steps

1. `/simplify` over the milestone's changed files.
2. Re-run `scripts/bao-verify.sh` to prove behavior unchanged.

## Files

- Whatever the milestone changed.

## Acceptance criteria

- [ ] Simplifications applied or explicitly none found; `bao-verify.sh` still exits 0.

## Out of scope

- New behavior.
