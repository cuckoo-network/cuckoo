---
id: w4/m24/t002
title: "Backend: persist + return descriptions on every verb, all three resources"
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m24/t001]
---

## Objective

Thread descriptions through `internal/postgres` and `internal/keyvalue` (the existing `IPAllowListEntry` wire types stop dropping the field — write it to the CR, read it back on every view) and through `internal/environments`' store entries (the `w4/m23/t004` wire shape gains persistence; coordinate so the two tasks don't collide). GraphQL keeps its product-neutral shape decision wherever one is recorded — extend, don't contradict.

## Files

- `lego/backend/internal/postgres/`, `internal/keyvalue/`, `internal/environments/`

## Acceptance criteria

- [ ] Create/get/update/list round-trip descriptions on all three resources across REST/GraphQL/MCP; empty description stays empty (never faked).

## Out of scope

- Dashboard + CLI legs (t003).
