---
id: w4/m24/t001
title: "CRD: structured ipAllowList entries with backward-compatible conversion"
worker: worker4
status: done
estimate: 60m
depends_on: []
---

## Objective

Move `Database.spec` and `KeyValue.spec` ipAllowList from `[]string` to a structured `[{cidr, description}]` (new field alongside the old, or an in-place type change with conversion — pick the least-churn shape kubebuilder supports cleanly), such that existing CRs keep working untouched and the operator's enforcement path (CNPG `pg_hba` / Valkey middleware) reads exactly the cidrs it read before.

## Context

- `lego/types/v1alpha1/database_types.go` + `keyvalue_types.go`; codegen via `make manifests generate` from `lego/operator/`.
- Byte-identical enforcement is the invariant: envtest-diff the rendered CNPG/Valkey objects before/after for a cidr-only spec.
- `w7/m32`'s service ipAllowList shipped post-RC12 — check whether it already stores descriptions and mirror its shape if so.

## Files

- `lego/types/v1alpha1/`, `lego/operator/internal/controller/` (readers), generated CRD bases

## Acceptance criteria

- [ ] New shape round-trips through the CRD; a legacy string-list CR still enforces identically (envtest diff); `make test` green.

## Out of scope

- Backend wire/store changes (t002).
