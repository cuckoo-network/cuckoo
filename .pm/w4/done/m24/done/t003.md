---
id: w4/m24/t003
title: "Dashboard editors + CLI round-trip legs"
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m24/t002]
---

## Objective

The dashboard allow-list editors (databases, key value, and — once w5/m31/t002 ships — environments) gain the description column/input; strengthen the `cli-compat` verify legs so the postgres/keyvalue `--ip-allow-list "cidr=…,description=…"` runs assert the description comes BACK (the exact single-field-check trap RC14 taught).

## Files

- `dashboard/src/features/databases/`, `features/keyvalue/`; `scripts/cli-compat.sh` verify legs

## Acceptance criteria

- [ ] Descriptions visible/editable in the dashboard; CLI verify asserts non-empty description round-trip; coordinated (not duplicated) with w9/m4's leg work.

## Out of scope

- The environments ACL editor itself (w5/m31/t002 owns it).
