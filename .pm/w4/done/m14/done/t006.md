---
id: w4/m14/t006
title: Mount `<AuditLogPanel />` on the Settings page
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m14/t005]
---

## Objective

Make the panel reachable: mount it on `/settings`, alongside `TeamPanel` and `ApiKeysPanel` — the same page, not `/workspace/settings` (that route is workspace metadata/danger-zone only; Team and API Keys already live on `/settings` in this codebase, even though `dashboard/CLAUDE.md`'s route table currently describes it as pure Kratos account settings).

## Context

- `dashboard/src/features/auth/pages/settings-page/index.tsx`: currently renders the Kratos `Settings` flow, then `<TeamPanel />`, then `<ApiKeysPanel />`, all inside the same `max-w-2xl` column.
- Render's own dashboard IA places Audit Logs next to Team Members (`docs/render-parity.md`'s dashboard-IA note) — mount `<AuditLogPanel />` after `<ApiKeysPanel />` to match that adjacency.

## Steps

1. Import `AuditLogPanel` into `settings-page/index.tsx` and render it after `<ApiKeysPanel />`.
2. Confirm layout spacing matches the existing `space-y-6` rhythm (no extra wrapper needed — the panels are siblings in that column already).

## Files

- `dashboard/src/features/auth/pages/settings-page/index.tsx`

## Acceptance criteria

- [ ] `/settings` renders the Audit Log card below API Keys for an admin; nothing renders there for a non-admin.

## Out of scope

- Any change to `/workspace/settings`.
