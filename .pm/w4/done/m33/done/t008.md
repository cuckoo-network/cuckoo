---
id: w4/m33/t008
title: Simplify
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m33/t007]
---

## Resolution (2026-08-15)

No simplification needed. The milestone's fixes shipped through later codex rounds (#8/#9/#11) that each ran their own `/simplify` pass; the resulting code in `postgres/access.go`, `hydra-device.ts`, `database_controller.go`, and the four workflows is already idiomatic and reuse-clean (e.g. the shared `hmacticket` envelope, the single `managedRoles` projector). The only code this closeout touched was two test files (added assertions), which need no simplification. Nothing declined.

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving). Apply what's safe inline; file declined-with-reason findings as inbox notes.

## Context

- Expected touchpoints: `apps/service.go`, `agentsessions/service.go`, `members/service.go`, `postgres/access.go`, `dashboard/src/common/server-fn/hydra-device.ts`, the four workflows, `deploy/gitops/authz/model.{fga,json}`.

## Acceptance criteria

- [ ] `/simplify` run; applied findings keep the suite green; declined findings filed with reasons.

## Out of scope

- Bug-hunting (that's /code-review, not this task).
