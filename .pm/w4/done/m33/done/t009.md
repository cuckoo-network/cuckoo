---
id: w4/m33/t009
title: Test coverage
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m33/t007]
---

## Resolution (2026-08-15)

Coverage in place across the milestone's findings:

- **t001/t002** role-level refusal: `lego/backend/internal/agentsessions/ssh_test.go` + `service_test.go` prove the `can_view_sensitive` sink relation; `representativeVerbRelations` pins both verbs into the sweep.
- **t003** exact-role convergence on partial-failure retry: `lego/backend/internal/members/m33_test.go`.
- **t004** device-flow stays pending until the confirmation POST: `dashboard/src/common/server-fn/__tests__/hydra-device.test.ts` (signed-out bounce without pairing + `handleDeviceConfirm` session/same-origin gating).
- **t005** tombstone revoke — **backfilled by this closeout**: `TestUsersCRUD` (`postgres/advanced_test.go`) now asserts `spec.deletedUsers` records the drop; `TestCnpgClusterSpecManagedRoles` (`operator/.../database_test.go`) now asserts a deleted user projects `ensure:absent` without `login`. Both pass.

The in-cluster live-credential integration proof (authenticate, delete, prove the session is refused before 204) named in t005 remains an envtest/e2e follow-up rather than a unit test — the unit + projection layers are pinned; noted here so it isn't lost.

## Objective

Add meaningful tests for what this milestone shipped — real behavior and failure modes, not coverage padding.

## Context

- t001/t002 are already pinned in `representativeVerbRelations`, which errors when a pinned verb leaves the sweep. The gap that remains is a **role-level** assertion: a contributor is refused shell/exec and steer while a developer succeeds.
- t003 needs a partial-failure test: inject an old-role revoke failure, retry the downgrade, assert every non-target role tuple is gone before success.
- t004 needs the signed-out sequence end to end (grant → signed-out link → login → still pending → confirm POST → authorized).
- t005's proof is the live-credential test named in that task.

## Acceptance criteria

- [ ] A role-matrix test proves a contributor can manage SSH keys but cannot obtain a shell or exec.
- [ ] A `ChangeRole` partial-failure test proves exact-role convergence on retry.
- [ ] The device-flow test asserts the grant stays pending until the confirmation POST.
