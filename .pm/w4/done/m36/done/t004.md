---
id: w4/m36/t004
title: "Reference parity: Devin/Cursor behavior benchmark + serialization contract unchanged"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m36/t003]
---

## Objective

The standing Render-parity closing task, reframed for this surface: Render has no agents composer or `@`-mention feature — the reference products are Devin and Cursor. Verify the shipped behavior against their documented model, and assert the backend contract did not move.

## Context

- Reference model (from the 2026-08-14 research): Devin — bare `@` shows categories (Repos/Files/Macros/Playbooks/Skills/Secrets/Sessions), `@<name>` matches entities directly, repo mentions render as icon badges ([docs.devin.ai/get-started/first-run](https://docs.devin.ai/get-started/first-run), release notes 2025-03-19 + 2025-10-10). Cursor — universal most-relevant list first, category rows filter within a type ([docs.cursor.com @-symbols](https://docs.cursor.com/en/context/@-symbols/overview)). Undocumented in both: exact ranking and fuzzy-vs-prefix matching — grouped sections is bex's deliberate choice, record it, don't chase it.
- Contract: `createAgentSession` receives bare `repo: "owner/name"` + `sessionIds[]`; the `repos:`/`session:` prefixes are editor-token conventions only (`composer-document.ts`, `use-agent-session-mutations.ts:72-101`).

## Steps

1. Walk the shipped dropdown against the reference checklist: universal match without prefix, optional category narrowing, badge rendering, keyboard nav.
2. Re-assert (test already exists per t003; confirm it) the mutation payload is byte-identical to pre-m36 — no prefix leakage, mention text absent from `task` prose.
3. Record any deliberate divergence from Devin/Cursor (e.g. grouped sections vs. unknown Devin layout) in a short note in the milestone README or `docs/ADR047-cloud-coding-agent-sessions.md` D9 vicinity, flagging follow-ups (more mention types) as inbox notes rather than silently diverging.

## Files

- `dashboard/src/features/agent-sessions/` (verification only)
- milestone README / ADR047 (divergence note)

## Acceptance criteria

- [ ] Checklist pass recorded (universal match, narrowing, badges, payload)
- [ ] No REST/GraphQL/MCP shape change confirmed
- [ ] Divergences + future mention-type candidates written down, not implied

## Out of scope

- Implementing additional mention types this pass
