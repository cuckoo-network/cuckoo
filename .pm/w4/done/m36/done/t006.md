---
id: w4/m36/t006
title: "Test coverage: pin `@be` → repo surfaced + universal-level edge cases"
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m36/t004]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real assertions on the universal level's semantics and failure modes, beyond what t001/t003 added incidentally.

## Context

- The pre-m36 suite deliberately had **no** test asserting a bare `@be` surfaces repos (the gate was by design); that absence is what let the friction ship. The regression test is the DoD's anchor.
- Existing suites: `dashboard/src/features/agent-sessions/lib/__tests__/mention.test.ts`, `components/__tests__/new-session-composer.test.tsx`.

## Steps

1. Lib: `@be` → `bex-co/bex` row (the headline regression); ranking order stable across type groups; category rows matched by their own labels (`@rep` → `Repositories` row); empty-source and repos-still-loading states; bounded entity preview on empty query; explicit `repos:`-prefix path unchanged.
2. Component: one-step keyboard selection from the universal level; single-repo invariant via universal selection; payload assertion (bare `repo`, `sessionIds`, no prefix in `task`).
3. No tautological/snapshot-everything tests — each test states an observable behavior a regression would break.

## Files

- `dashboard/src/features/agent-sessions/lib/__tests__/mention.test.ts`
- `dashboard/src/features/agent-sessions/components/__tests__/new-session-composer.test.tsx`

## Acceptance criteria

- [ ] A test fails if the category-only top-level gate is ever reintroduced
- [ ] Universal-level edge cases (loading, empty, ranking, invariants) covered
- [ ] `yarn test` green in `dashboard/`

## Out of scope

- E2E browser automation (manual check happens in t004's walkthrough)
