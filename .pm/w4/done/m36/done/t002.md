---
id: w4/m36/t002
title: "Picker UI: grouped sections with headers, top-level loading/empty states, locales"
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m36/t001]
---

## Objective

Render t001's merged option list in the dropdown: section headers per type ("Repositories", "Sessions"), category rows visually distinct from entity rows, and correct loading/empty presentation at the universal level.

## Context

- `dashboard/src/features/agent-sessions/components/mention-picker.tsx` renders the listbox rows today (icons at lines 27-30: `repos → BookMarked`, `sessions → Bot`) plus a repo footer preview.
- Reference behavior: Devin renders selected repo mentions as attachment badges with icons (release note 2025-10-10); Linear groups its universal `@` menu into sections. Cursor keeps category rows inline in the same list.
- Copy lives in `dashboard/src/features/agent-sessions/locales/{en,zh}.ts` (category labels at `en.ts:125-154`) — new section-header / hint strings need both locales.

## Steps

1. Render group section headers from the row structure t001 emits; keep category rows (drill-down affordance) styled distinctly from entity rows so the two-level shortcut remains discoverable.
2. Wire the extended `mentionEmptyText` states: repos still loading at top level shows loading copy, not "No matches".
3. Add locale strings (en + zh) for headers/hints; no hard-coded English.
4. Verify the repo footer preview still triggers on repo-row highlight at the universal level.

## Files

- `dashboard/src/features/agent-sessions/components/mention-picker.tsx`
- `dashboard/src/features/agent-sessions/locales/en.ts`
- `dashboard/src/features/agent-sessions/locales/zh.ts`

## Acceptance criteria

- [ ] Typing `@be` in the running app shows a "Repositories" section containing `bex-co/bex`
- [ ] Category rows remain visible/selectable on bare `@` and read as navigation, not entities
- [ ] Loading repos ⇒ loading copy at top level, not a false "No matches"
- [ ] Both locales render without missing-key fallbacks

## Out of scope

- Keyboard navigation semantics across sections (t003)
