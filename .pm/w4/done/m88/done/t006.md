---
id: w4/m88/t006
title: Closeout
worker: worker4
status: done
estimate: 10m
depends_on: [w4/m88/t004, w4/m88/t005]
---

## Objective

Close the milestone only when the definition of done is actually met: move tasks to `done/`, mark the milestone done, move `w4/m88/` to `w4/done/m88/`, and check the workstream README box.

## Context

- DoD lives in `w4/m88/README.md`. Completing this task _is_ the board move — do not run it early.

## Steps

1. Confirm DoD: k9s-visible JSON errors, Loki dashboard pod streams, Loki Traefik `dashboard.bex.co` access lines; tenant App log paths unchanged.
2. Set remaining tasks `status: done`, move each `tNNN.md` to `w4/m88/done/`, mark README rows/`**Status:**`, move the milestone to `w4/done/m88/`, check `- [x]` in `w4/README.md`.

## Files

- `.pm/w4/m88/**`
- `.pm/w4/README.md`

## Acceptance criteria

- [x] DoD observables verified (or explicitly blocked with evidence if prod query unavailable — then do not close).
- [x] Milestone directory lives only under `w4/done/m88/` with no tombstone at `w4/m88/`.

## Out of scope

- Expanding platform log coverage to other namespaces/hosts.

## Live verification (2026-08-20, after `f6b143cb` deploy)

- Deploy: https://github.com/bex-co/bex/actions/runs/32340841020 green; pin `7d458770`.
- Alloy CM includes `platform_pods` + `dashboard.bex.co` allowlist (Argo Synced).
- Loki `{type="platform",service="dashboard"}` — listen lines from ns `dashboard`.
- Loki `{type="request",service="dashboard"} |= "dashboard.bex.co"` — Traefik access; **no `host` label**.
- JSON error shape on live stdout + Loki `{type="platform",service="dashboard",level="error"}` for `m88_deliberate_route_probe` (bounded `path`/`status`/`msg`); unit tests cover `writeServerLog` / `withReportedRouteError` / ErrorPage+consent+Apollo wiring.
