---
id: w4/m88/t005
title: Test coverage
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m88/t003]
---

## Objective

Add meaningful tests for the observability behavior this milestone ships — logger output shape and shipper retention/drop rules — not tautological snapshots.

## Context

- Dashboard: assert JSON error lines and that happy-path probes stay quiet; keep DEV Apollo logging gated.
- Shipper: prefer a clusterless/config fixture or scripted assertion over the River config's keep/drop outcomes for dashboard ns + `dashboard.bex.co` host (mirror existing `scripts/*.test.sh` patterns if present for Alloy/gitops).

## Steps

1. Cover the dashboard logger / error-path wiring with unit tests (level, required fields, no secret headers).
2. Cover shipper rule intent with the lightest durable check the repo already uses for GitOps config (fixture parse, golden fragment, or validate script).
3. Ensure CI paths that already lint/test dashboard and gitops still pass.

## Files

- `dashboard/src/**/__tests__/`
- `scripts/` or `deploy/gitops/` test helpers if extended

## Acceptance criteria

- [x] Failure-mode tests fail if error logging regresses to silence or starts logging secrets/variables.
- [x] Shipper tests/fixtures fail if dashboard pod or `dashboard.bex.co` retention is accidentally removed.
- [x] No snapshot-everything or assert-true fluff.

## Out of scope

- Full live Loki e2e in CI (prod query remains a manual acceptance check on t002/t003).
