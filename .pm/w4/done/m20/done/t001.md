---
id: w4/m20/t001
title: Audit log records read-verb (view) denials
worker: worker4
status: done
estimate: 45m
depends_on: []
---

## Objective

Close the deliberate w4/m10 volume cut: denied read-relation checks (`can_view`/`can_view_logs`/`can_view_sensitive`) reach the audit log. Decide the two open semantics questions FIRST (from `w4/done/012.md`, filed as a duplicate `001.md`): do read denials record a `Target`, and do `AuthorizeOn`-style workspace-wide read checks also record — then flip the filter.

## Context

- `lego/backend/internal/core/base.go:288-297` (`authorizeAndAudit`) is the single chokepoint; `internal/core/audit.go:97-106` (`writeRelations`) drops read relations one line before `b.emit(...)`.
- Denials only — successful reads stay unrecorded (volume). `docs/ADR006-bex-api.md:369` documents the current cut; update it.

## Steps

1. Decide + write down the Target/AuthorizeOn semantics in the ADR006 audit section.
2. Emit when `authzErr != nil` for read relations (or remove them from `writeRelations` for the denial path).
3. Update ADR006 + the ADR018 audit-logs row ("read-verb denials are a documented future extension" → shipped).

## Files

- `lego/backend/internal/core/audit.go`, `docs/ADR006-bex-api.md`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] A viewer denied `can_view_sensitive` produces a `status: denied` audit row; successful reads produce none.

## Out of scope

- Recording successful reads (volume-prohibitive, unchanged).
