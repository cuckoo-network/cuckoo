---
id: w4/m28/t007
title: Test coverage
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m28/t005]
---

## Objective

Meaningful tests for the shipped behavior: the composition matrix, the migration no-lockout invariant, the deny-all read, and the operator's middleware reconcile — real behavior and failure modes, no tautologies.

## Context

- t002's table tests may already cover composition — extend, don't duplicate.
- Operator-side: envtest asserting the Ingress middleware appears/disappears with the CR field.

## Steps

1. Fill gaps across backend + operator + dashboard component tests (deny-all warning).

## Files

- `lego/backend/internal/environments/*_test.go`, operator envtest files, dashboard `__tests__`

## Acceptance criteria

- [ ] Composition matrix, migration invariant, deny-all semantics, and middleware reconcile all asserted

## Out of scope

- Live-cluster E2E beyond t003's acceptance check

## Resolution (2026-07-15)

Table-driven `TestEnvironmentIPAllowListChaining` (both layers chain, each alone, deny-all placeholder, none) + `TestDatastoreEnvironmentLayerChaining` (TCP route references both) + inverted `TestSetACL_PropagatesIPAllowListToEnvironmentMembers` (layer projected, own list untouched) + `TestSetACL_EmptyListProjectsDenyAll` + the pg-gated seeded-create no-lockout invariant. Backend, operator, dashboard (1189) suites + lint green.
