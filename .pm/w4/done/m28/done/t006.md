---
id: w4/m28/t006
title: Simplify
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m28/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (behavior-preserving reuse/simplification/efficiency).

## Context

- Expected touched code: `lego/backend/internal/environments/`, projector, operator ingress reconcile, dashboard environments feature.

## Steps

1. `/simplify` over the milestone's diff; apply safe cleanups.

## Files

- (the milestone's diff)

## Acceptance criteria

- [ ] Simplify pass run; tests still green

## Out of scope

- Bug hunting

## Resolution (2026-07-15)

Simplify (2 agents: quality + lifecycle-consistency): applied — duplicate HTTP middleware spec builder deleted (one `cidrMiddlewareSpec`), dead `SetIPAllowList` dropped from the environments interfaces+fakes, stale clobber-era doc comments rewritten, orphaned comments fixed, `applyAppAllowLists` + `stampEnvironmentMembership` extracted (the 3-copy create stamp), unchanged-layer short-circuits on the datastore verbs. The consistency review's five in-scope gaps fixed in t003; three cross-feature edges filed as `w4/025.md`.
