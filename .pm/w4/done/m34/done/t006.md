---
id: w4/m34/t006
title: Test coverage — version interception, notice gating, cache behavior
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m34/t005]
---

## Objective

Meaningful tests for the shipped behavior: version-request detection semantics, the bex version line, and every notice gate — asserting real behavior and failure modes, not snapshots.

## Context

- `cli/main_test.go` already builds the real binary and runs it against httptest servers — extend that harness.
- The notice check must be testable without GitHub: injected HTTP layer (t003) + env-pointed API base.

## Steps

1. Binary-level: `-v` prints the bex line and hits no render-oss URL; `services -v` passes through; global-flag value skipping.
2. Unit: release-list filtering by `bex-cli/v` prefix, semver compare, 24h cache read/write, each silence gate (CI, non-TTY, opt-out, dev version), silent failure on HTTP error.

## Files

- `cli/main_test.go`
- `cli/internal/` test files for the new package(s)

## Acceptance criteria

- [ ] Each gate has a test that fails if the gate is removed.
- [ ] A test proves no network call happens on cache hit.
- [ ] `cd cli && go test ./...` green.

## Out of scope

- Live GitHub API integration tests.
