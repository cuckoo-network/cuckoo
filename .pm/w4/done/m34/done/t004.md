---
id: w4/m34/t004
title: Upstream PR to make cfg.RepoURL overridable; record the login-view limitation
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m34/t003]
---

## Objective

Fix the non-interceptable second call site: `pkg/tui/views/login.go` also queries `cfg.RepoURL` (render-oss/cli) and shows a Render upgrade banner inside `bex login` once upstream ships a release newer than our pin. Open a small upstream PR turning `cfg.RepoURL` (and `cfg.InstallationInstructionsURL`) from consts into vars so embedders can repoint them via `-X` — the same embedder-friendliness as the already-exported `cmd.RootCmd`. Until merged, document pin-bumping as the mitigation.

## Context

- Both call sites (`cmd/root.go`, `pkg/tui/views/login.go`) read `cfg.RepoURL`; once it is a var, two `-X` flags in `scripts/bex-cli-build.sh` redirect everything and t001's interception becomes defense-in-depth.
- `cli/UPSTREAM_RENDER_CLI.md` already carries the pin-update checklist — add the login-view item there.

## Steps

1. Fork/branch render-oss/cli; change the two consts to vars; PR with the embedder rationale.
2. Add to `cli/UPSTREAM_RENDER_CLI.md`: the login-view call site, the pin-bump interim mitigation, and "check PR status; once merged, add the `-X` repoints to `scripts/bex-cli-build.sh`".
3. Update `docs/bex-cli.md`'s branding-limits section to mention the login-view banner as a known upstream-behavior remnant.

## Files

- upstream PR (render-oss/cli)
- `cli/UPSTREAM_RENDER_CLI.md`
- `docs/bex-cli.md`

## Acceptance criteria

- [ ] Upstream PR opened and linked from `cli/UPSTREAM_RENDER_CLI.md`.
- [ ] Pin checklist covers the login-view call site and the post-merge `-X` follow-up.
- [ ] `docs/bex-cli.md` documents the interim limitation honestly.

## Out of scope

- Blocking this milestone on the PR merging (mitigation documented instead).
