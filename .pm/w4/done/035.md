# Correct ADR018's stale shared-hosting security update

Sub-hour documentation correction from `/pm-brainstorm for w4`, 2026-08-18.

`docs/ADR018-render-parity.md` still says production no longer configures `onbex.co` as `BEX_BASE_DOMAIN`. That statement is stale and contradicts `.pm/DO_NOT_DO.md` `#PSL`: disabling the base domain caused the second production outage, the value was restored, and `scripts/gitops-validate.sh` now rejects an empty production hosting suffix.

Rewrite the shared-hosting security update and any downstream parity-row wording that depends on it so the ledger states the current posture:

- shared platform hosting remains enabled;
- the cross-tenant cookie risk is real and accepted before open signup;
- host-only / `__Host-` cookies are the interim tenant guidance;
- neither unsetting `BEX_BASE_DOMAIN` nor submitting `onbex.co` to the PSL is currently authorized;
- `.pm/DO_NOT_DO.md` `#PSL` is the canonical current decision and reopening trigger.

Done when ADR018 no longer recommends or describes the outage-causing configuration, contains no contradictory shared-hosting claim elsewhere, and links the accepted risk to the current guardrail. Do not change manifests or reopen the PSL submission.

## Resolution (2026-09-03)

Corrected ADR018's shared-hosting security update and every contradictory downstream claim found in ADR029, the static-site browser-isolation artifact, the datastore cutover runbook/drill, and the 2026-07-29 trust-baseline drill. Current posture matches `.pm/DO_NOT_DO.md` `#PSL`: shared hosting remains enabled (`BEX_BASE_DOMAIN=onbex.co`), cookie risk accepted before open signup, host-only/`__Host-` interim guidance, neither unsetting the base domain nor PSL submission authorized. No manifests changed.
