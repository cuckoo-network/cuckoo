---
id: w4/m9/t006
title: Simplify
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m9/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (dashboard challenge
passthrough + consent route + server-fn helper; bex-api metadata endpoint;
gitops values), behavior-preserving — reuse, simplification, efficiency.

## Context

- Files touched by m9: `dashboard/src/routes/auth.*`, `dashboard/src/common/{hooks/use-ory-flow.ts,server-fn/**}`, `lego/backend/internal/api/server.go`, `deploy/gitops/**/values/*.values.yaml`, `scripts/auth-oauth21-e2e.sh`.

## Steps

1. Run `/simplify` scoped to the m9 diff.
2. Apply behavior-preserving cleanups; re-run dashboard `yarn lint && yarn typecheck && yarn test` and backend `go build ./... && go test ./...`.

## Files

- Files changed in m9.

## Acceptance criteria

- [x] `/simplify` applied; no behavior change; checks green.

## Out of scope

- New behavior.
