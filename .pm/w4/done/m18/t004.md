---
id: w4/m18/t004
title: e2e — authorize → listed → revoke → the agent's token stops working
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m18/t002, w4/m18/t003]
---

## Objective

Extend `scripts/auth-oauth21-e2e.sh` with a revocation leg: authorize a DCR client and confirm its token works, list the subject's consent sessions and see the client, revoke it via the same admin verb the server-fn uses, then assert the token is dead (Hydra introspection `active: false` and/or 401 at `/mcp`). Add a session-revocation assertion (two Kratos sessions → revoke-others → the other session's cookie is rejected).

## Context

- The script's existing helpers and exit-0 contract; m17/t004 may have added a signed-in leg by now — compose, don't duplicate setup.
- Driving the admin verbs directly (curl) is the right altitude for e2e — the dashboard server-fns wrap the same calls; UI-level behavior is covered by t002/t003 component tests.

## Steps

1. Add the consent-revocation leg after an existing authorize leg.
2. Add the Kratos two-session revoke-others assertion.
3. Keep bash-3.2-compatible; script exits 0.

## Files

- `scripts/auth-oauth21-e2e.sh`

## Acceptance criteria

- [ ] Revoked client's token is rejected (introspection inactive or 401 at `/mcp`).
- [ ] Revoke-others kills the second Kratos session, not the first.
- [ ] Full script exit 0.

## Out of scope

- Browser-automation e2e.
