---
id: w4/m18/t002
title: Connected agents card — Settings → Security & Compliance
worker: worker4
status: done
estimate: 35m
depends_on: [w4/m18/t001]
---

## Objective

A **Connected agents** card in the dashboard's Settings → Security & Compliance section (the m15 grouping, beside the Audit Log card): each authorized OAuth client with name, granted scopes, and grant date, plus a revoke button with a confirm step. Empty state explains what appears here (agents connected via the ADR025 recipe).

## Context

- `dashboard/src/features/settings/` — the m15 Security & Compliance region; follow the Audit Log card's structure (section placement, 503 explanatory state).
- Data via the t001 server-fns; i18n in `dashboard/src/features/*/locales/{en,zh}.ts` (repo convention — both locales, always).
- Revoke is destructive-ish (the agent must re-authorize) — confirm dialog, then refetch the list.

## Steps

1. Build the card: list, empty state, 503 state (HYDRA_ADMIN_URL unset), revoke-with-confirm.
2. Wire i18n (en/zh) for all strings.
3. Component tests following the Audit Log card's test style.

## Files

- `dashboard/src/features/settings/` (new card component + tests)
- locale files (en/zh)

## Acceptance criteria

- [ ] Card renders inside Security & Compliance with grants listed; revoke confirms, then the grant disappears.
- [ ] Empty and 503 states render explanatorily, not as errors.
- [ ] `yarn test` / `yarn lint` green.

## Out of scope

- Per-scope partial revocation (all-or-nothing per client, matching Hydra's verb).
