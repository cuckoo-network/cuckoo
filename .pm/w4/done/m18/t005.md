---
id: w4/m18/t005
title: Render parity — superset check (no Render shape) + ADR018 revocation evidence
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m18/t004]
---

## Objective

The standing parity check in m16's superset form: Render has no OAuth provider (its only credential is a dashboard-minted API key) and session management is Kratos-owned, so the comparison target is ADR018's "bex ahead of Render" section — which claims "revocable, scoped token"; this milestone makes that claim fully true, so add the evidence — plus bex's own cross-surface consistency (deliberately no REST/GraphQL/MCP surface; note that this is account-scoped dashboard-SSR by design, like login/MFA).

## Context

- `docs/ADR018-render-parity.md` — bex-ahead OAuth entry + the Login·sessions row.
- `docs/ADR012-auth.md` §7 — document the revocation surface next to the consent design.
- Re-check render.com docs for any account "authorized applications"/session surface bex should acknowledge (none known as of 2026-07-12).

## Steps

1. Update ADR018 (bex-ahead evidence: the revocation e2e leg) and the sessions row note.
2. Add a short revocation subsection to ADR012 §7.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- `docs/ADR012-auth.md`

## Acceptance criteria

- [ ] ADR018's revocability claim cites the e2e leg.
- [ ] Any discovered Render drift filed as follow-up, not silently absorbed.
- [ ] prettier clean.

## Out of scope

- Building any REST/GraphQL/MCP grant surface (deliberate omission, recorded in the milestone README).
