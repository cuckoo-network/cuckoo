# MFA: TOTP + passkeys via Kratos

Kratos supports `totp` and `webauthn`/passkey credential methods — config-level enablement in `kratos.values.yaml` plus dashboard settings-flow rendering (Ory Elements handles most of it). Parked: no why-now until real tenants exist. Revisit after w1/m2 lands the tenants/accounts model.
