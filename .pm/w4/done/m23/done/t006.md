---
id: w4/m23/t006
title: "Simplify — /simplify over the code this milestone changed"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m23/t005]
---

## Objective

Run `/simplify` over the milestone's diff; apply behavior-preserving reuse/simplification/efficiency findings.

## Acceptance criteria

- [x] `/simplify` run; findings applied or declined with reasons.

## Resolution (2026-07-15)

Ran `/simplify` (4 parallel agents: reuse/simplification/efficiency/altitude) over `cc2156b8`. **Applied** (backend suite + lint green): collapsed the `pageNewestFirst`/`pageOldestFirst` wrapper pair into direct `pageKeyset(...)` calls with the direction/cursor doc on the one helper; hoisted Render's `direction` enum + validation to `core.ParseDirection` (pagination.go), consumed by both `logs.validate` and `audit.List` (constants aliased in each package, error text unchanged); extracted `core.auditsDenial(relation)` used by both `authorizeAndRecord` and `AuthorizeApp`'s aggregate denial row; the aggregate row now reuses the already-resolved `acting` workspace (`WorkspaceObject(acting)`) instead of `callerWorkspace`'s second store round-trip; environments PATCH seeds the ACL merge from Rename's returned full view instead of a redundant `Get` when both name+ACL are patched. **Declined**: environments core-verb refactor (create-with-ACL + `EnvironmentPatch`) and the fallback-loop loop-invariant `resolveWorkspace` hoist — real but beyond a behavior-preserving inline fix, filed as `w4/022.md`; batching the capped denial audit inserts — contradicts `015`'s deliberate shape-2 resolution (synchronous per-row, capped); `""→unprotected` normalization inside `SetACL` — would loosen the bex-native `/acl` route's named 400 on an empty status; `suspenders` slice-allocation micro-opt — negligible. The fourth `ipAllowList` wire-type copy flagged against the commit was already consolidated at HEAD by w4/m24 (`core.IPAllowListEntry`).
