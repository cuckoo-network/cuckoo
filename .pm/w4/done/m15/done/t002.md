---
id: w4/m15/t002
title: Move AuditLogPanel into the Security & Compliance section
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m15/t001]
---

## Objective

Relocate `<AuditLogPanel/>` from a bare top-level sibling in the flat Settings column into the Security & Compliance section grouping introduced in t001, preserving the panel's existing admin-only gating and store-less 503 states unchanged.

## Context

- `dashboard/src/features/auth/pages/settings-page/index.tsx` currently renders `<AuditLogPanel/>` last, as a sibling of `<TeamPanel/>` and `<ApiKeysPanel/>`.
- `AuditLogPanel` (`src/features/audit/components/audit-log-panel.tsx`) self-gates: a `can_manage` 403 hides the card, a store-less 503 shows an explanatory state (per `docs/ADR018-render-parity.md` line ~115). Moving it must not touch that logic — only its position in the tree.

## Steps

1. Move `<AuditLogPanel/>` inside the Security & Compliance section container from t001.
2. Confirm Team and API Keys remain where they are (not part of this milestone's regrouping).
3. Verify the panel's gating/empty/503 states still render correctly in the new position.

## Files

- `dashboard/src/features/auth/pages/settings-page/index.tsx`

## Acceptance criteria

- [ ] `<AuditLogPanel/>` renders inside the Security & Compliance section, not as a top-level sibling of Team/API Keys.
- [ ] Admin-only hide (403), store-less 503, and populated-table states are visually unchanged from before the move.
- [ ] `yarn typecheck` + `yarn lint` green.

## Out of scope

- Any change to `AuditLogPanel`'s internal gating, paging, or query logic.
