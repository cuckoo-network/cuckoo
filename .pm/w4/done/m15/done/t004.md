---
id: w4/m15/t004
title: Simplify the settings-page changes
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m15/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (the settings-page grouping + card move) for reuse / simplification / efficiency, behavior-preserving.

## Context

- Changed code is concentrated in `dashboard/src/features/auth/pages/settings-page/index.tsx` and the `auth` locale files.
- Watch for: an ad-hoc section wrapper that should reuse an existing shadcn `Card`/section primitive; duplicated heading markup that w4/006 + m11 will also need (consider whether a small reusable `SettingsSection` shell is warranted rather than inlined markup, without over-engineering).

## Steps

1. Invoke `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving cleanups; re-run `yarn lint` + `yarn test`.

## Files

- `dashboard/src/features/auth/pages/settings-page/index.tsx` (+ any helper extracted)

## Acceptance criteria

- [ ] `/simplify` findings addressed or consciously declined with reason.
- [ ] Behavior unchanged; `yarn lint` + `yarn test` green.

## Out of scope

- Bug hunting (that's `/code-review`, not this task).
