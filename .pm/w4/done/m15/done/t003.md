---
id: w4/m15/t003
title: "Render parity: Settings IA vs Render Workspace Settings→Compliance"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m15/t002]
---

## Objective

Confirm the new Settings IA is consistent across the surface it touches (dashboard UI only here) and reconcile it with Render's actual placement, updating `docs/ADR018-render-parity.md` so the ledger reflects the resolved IA rather than the drift note filed by m14.

## Context

- This milestone changes only the **dashboard UI** — there is no REST/GraphQL/MCP surface for audit-log placement (the audit-log API itself, w4/m10, is unchanged). So the parity check is UI-vs-Render-IA only.
- `docs/ADR018-render-parity.md` currently records (line ~115) a `◐, IA` divergence: "the milestone's own premise didn't hold … follow-up filed as `w4/007`", and (line ~145, § bex ahead of Render) the in-app viewer as a superset, and (line ~163) "IA-placement drift filed as `w4/007`".
- Render's actual IA: date-range CSV export under **Workspace Settings → Compliance**, no in-app table (render.com/docs/audit-logs, checked 2026-07-11).

## Steps

1. Verify the Audit Log card now sits under a Security & Compliance grouping that reads as Render-consistent (Compliance-adjacent), and that no other bex surface exposes audit-log placement inconsistently.
2. Update `docs/ADR018-render-parity.md`: change the `w4/007` references from "drift filed / follow-up filed" to "resolved — moved under Settings → Security & Compliance (w4/m15)"; keep the "bex ahead of Render" superset note (still true — bex has an in-app table, Render has CSV-only).
3. `npx prettier@3.4.2 --write "**/*.md"` after the doc edit.

## Files

- `docs/ADR018-render-parity.md`
- `dashboard/src/features/auth/pages/settings-page/index.tsx` (read-only verification)

## Acceptance criteria

- [ ] `docs/ADR018-render-parity.md` no longer describes the placement as unresolved drift; it records the resolution (moved under Security & Compliance, w4/m15).
- [ ] The "bex ahead of Render" superset entry (in-app paginated table vs Render CSV) is retained.
- [ ] No REST/GraphQL/MCP drift introduced (none of those surfaces changed) — noted in the parity entry.

## Out of scope

- Any code change to the audit-log API (w4/m10).
