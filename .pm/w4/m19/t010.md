---
id: w4/m19/t010
title: Test coverage — duplicate-name behavior, slug minting, host derivation
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m19/t008]
---

## Objective

Close the pre-existing gap (no test exercised bex-api duplicate-name behavior at all) with meaningful tests asserting real behavior and failure modes — no tautological coverage.

## Context

- Existing: `store/api_test.go:138` (internal API 409), `store/store_pg_test.go:496` (`CreateApp` conflict); nothing at the `Service.Create`/adapter level.
- Must cover the three outcomes: same-tenant 409 (REST + GraphQL + MCP shapes), cross-tenant coexistence (no 403 leak), conflict-race → 409-not-500.
- Operator: slug-vs-fallback host derivation; static resolver mirror.
- Dashboard: taken → suggest → accept flow (t006 component test may already exist — extend, don't duplicate).

## Steps

1. Backend: table-driven duplicate-name tests in `apps/apps_test.go` across all three adapters; suggestion-algorithm edge cases in the t005 tests if thin.
2. Operator: `effectiveHosts` + resolver cases in controller tests.
3. Dashboard: ensure the availability/suggestion flow test asserts the disabled-submit and race-409 paths.
4. Full runs: `make test` (operator), `go test ./...` (backend), `yarn test` (dashboard).

## Files

- `lego/backend/internal/apps/apps_test.go`
- `lego/operator/internal/controller/` tests
- dashboard test files beside `services.new.tsx`

## Acceptance criteria

- [ ] Each DoD behavior has a failing-first-capable test (asserts outcome, not implementation).
- [ ] All suites green.

## Out of scope

- e2e (t007 owns it); coverage-number chasing.
