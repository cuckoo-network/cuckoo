---
id: w4/m17/t006
title: Simplify — /simplify over the code this milestone changed
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m17/t005]
---

## Objective

Run `/simplify` over the m17 diff (`use-ory-flow.ts`, `session.ts`, `requireAuth`, login route, `auth-oauth21-e2e.sh`) — reuse / simplification / efficiency, behavior-preserving. The t003 altitude lift should leave the hook *simpler*; verify no probe-era scaffolding survives.

## Context

- m11's simplify pass is the precedent (it produced inbox `009`, which t003 executes — the loop should now close, not spawn another altitude note).

## Steps

1. `/simplify` over the milestone's changed files.
2. Apply behavior-preserving findings; file anything out-of-diff as an inbox note.

## Files

- Whatever m17 touched (see t002–t004).

## Acceptance criteria

- [ ] `/simplify` run; findings applied or filed.
- [ ] Dashboard suite still green.

## Out of scope

- Bug-hunting (that's `/code-review`, not this task).
