---
id: w4/m17/t005
title: Render parity — superset check (no Render shape) + ADR012/ADR025/ADR018 accuracy
worker: worker4
status: todo
estimate: 15m
depends_on: [w4/m17/t004]
---

## Objective

The standing parity check, in m16's superset form: Render has no OAuth provider, so verify bex's own documentation and cross-surface story instead of a Render shape — ADR012 §7's short-circuit description must match what live Kratos actually does (it currently documents `browser_location_change_required`, which v1.3.1 does not throw in the signed-in case), ADR025's recipe/troubleshooting must reflect the fixed signed-in path, and ADR018's "bex ahead of Render" OAuth entry gains the evidence.

## Context

- `docs/ADR012-auth.md` §7 — documents the OAuth2 short-circuit the hook relied on.
- `docs/ADR025-connect-an-agent.md` — the recipe whose most common path this milestone fixes.
- `docs/ADR018-render-parity.md` — "bex ahead of Render" OAuth provider entry (cites `auth-oauth21-e2e.sh` legs as evidence; add the signed-in leg).
- No REST/GraphQL/MCP change in this milestone — the surface is the Kratos-owned login page.

## Steps

1. Update ADR012 §7 to describe the observed `200 null` behavior and the HTML-endpoint navigation fix.
2. Re-read ADR025 end-to-end for accuracy against the fixed flow; update the troubleshooting section.
3. Add the signed-in e2e leg to ADR018's evidence.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR012-auth.md`
- `docs/ADR025-connect-an-agent.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] ADR012 §7 no longer documents a Kratos behavior that live Kratos doesn't exhibit.
- [ ] ADR018's bex-ahead OAuth entry cites the signed-in leg.
- [ ] prettier clean.

## Out of scope

- Any Render-shape comparison — there is none (documented in the milestone README).
