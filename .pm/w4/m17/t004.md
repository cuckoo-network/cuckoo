---
id: w4/m17/t004
title: e2e — reused-cookie-jar leg in auth-oauth21-e2e.sh proves signed-in authorize completes
worker: worker4
status: todo
estimate: 25m
depends_on: [w4/m17/t003]
---

## Objective

Close the gap that let `010` hide: add a leg to `scripts/auth-oauth21-e2e.sh` that drives a second authorization **reusing the cookie jar** of an already-logged-in browser session, asserting the flow completes (through remembered-consent or the consent page) without re-login and yields a working `/mcp` token.

## Context

- Every existing leg starts from a fresh cookie jar (a clean browser) — that's exactly why the 12 legs pass while the signed-in path was broken.
- The script already has `kratos()`/`jqf()`/`rel()` helpers and mints DCR clients; follow its conventions (curl cookie jars, exit-0 contract).
- The leg must exercise the t002 fix: the flow-init in the signed-in state answers `200 null`, and the script should follow the HTML-endpoint redirect chain the dashboard now performs.

## Steps

1. After an existing leg leaves a jar with a live Kratos session, start a new authorization for a fresh (or the remembered) client using that jar.
2. Follow the redirect chain; assert no login form is required and the flow reaches the client redirect with a code.
3. Exchange the code; assert the token works against `/mcp`.
4. Keep the script bash-3.2-compatible (repo precedent from `auth-mfa-e2e.sh`).

## Files

- `scripts/auth-oauth21-e2e.sh`

## Acceptance criteria

- [ ] New leg present and `scripts/auth-oauth21-e2e.sh` exits 0 end-to-end (now 13+ legs).
- [ ] The leg fails when t002's fix is reverted (verified once, locally).

## Out of scope

- Browser-automation e2e (the curl-level leg plus t001's manual browser check cover both altitudes).
