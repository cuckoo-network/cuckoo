---
id: w4/m22/t006
title: Simplify
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m22/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (t001-t005: `internal/projects` new tests, the `writeErr` wrapper, any authz-sweep additions) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Scope: `lego/backend/internal/projects/`, `lego/backend/internal/environments/` (test additions only).

## Steps

1. Invoke `/simplify` over the diff.
2. Apply any findings that are genuinely behavior-preserving cleanups.
3. File anything riskier as a follow-up note instead of applying it inline.

## Files

- Whatever `/simplify` touches within the milestone's diff.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff; findings applied or filed.

## Out of scope

- Any change outside this milestone's diff.
