---
id: w4/m22/t005
title: Render parity — REST/GraphQL/MCP error-mapping consistency
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m22/t004]
---

## Objective

Verify the 500→503 fix (t003) and the authz-guard closure (t002) land identically across `internal/projects`' REST, GraphQL, and MCP surfaces — same status/error shape per docs/ADR006-bex-api.md's "one core, thin adapters" contract — and compare against Render's own documented behavior for a misconfigured/unavailable resource, flagging any drift as follow-up rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` — one-core, thin-adapters contract.
- `docs/ADR018-render-parity.md` — parity ledger conventions (✅/◐/✖/— per surface).
- `internal/environments` — the sibling feature that already has this consistency; use it as the comparison baseline.

## Steps

1. Confirm REST 503, GraphQL error extension, and MCP tool-error all fire the same way for `ErrProjectsUnavailable`.
2. Confirm the cross-tenant 403-not-404 behavior (t001) is consistent across all three surfaces, not just REST.
3. Check `docs/ADR018-render-parity.md`'s Environments/Projects row(s) for anything that needs updating given this pass.
4. File any found drift as a follow-up inbox note rather than fixing it inline if it's out of this milestone's scope.

## Files

- `lego/backend/internal/projects/{rest,graphql,mcp}.go`
- `docs/ADR018-render-parity.md` (if a note is warranted)

## Acceptance criteria

- [ ] REST/GraphQL/MCP verified consistent for both fixes.
- [ ] Any drift found is filed as a follow-up note, not silently dropped.

## Out of scope

- Fixing drift found here beyond what t001-t003 already cover — file it, don't scope-creep.
