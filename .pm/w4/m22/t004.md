---
id: w4/m22/t004
title: Live-verify the environments success path end to end, close out the checklist row
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m22/t001, w4/m22/t002, w4/m22/t003]
---

## Objective

Close the literal gap `docs/cli-compatibility-checklist.md:56` names: _"The success path (a real, existing project) is unverified in either harness — no `projects create` command exists in the CLI and none was seeded server-side this pass."_ Seed a real project directly (REST/GraphQL/MCP — the CLI itself has no `projects create`), then run `render environments <that-real-id> -o json` and `render environments <unknown-id> -o json` against a live dev stack, and update the checklist row with both results.

## Context

- `docs/cli-compatibility-checklist.md:56` — the row to close out.
- `.pm/w4/dev-4/` — this workstream's isolated dev stack (Kratos/Hydra/bex-api), already used for the prior CLI-compat re-verification pass per the checklist's harness note.
- `scripts/cli-compat.sh` — the existing harness script; extend or drive manually if seeding a project isn't yet scripted.
- `RENDER_HOST` env override + the Hydra `client_credentials`-to-bearer exchange documented in the checklist's Harness section — reuse as-is, no CLI changes.

## Steps

1. `bash .pm/w4/dev-4/up.sh` (idempotent).
2. Seed a real project via REST or GraphQL directly against dev-4's bex-api (using the same auth exchange `cli-compat.sh` already does).
3. Run `render environments <real-project-id> -o json` — capture the response, confirm fields are correctly populated (not zero-valued).
4. Run `render environments <unknown-project-id> -o json` again to reconfirm the already-known-good 404 path hasn't regressed under t001-t003's changes.
5. Update `docs/cli-compatibility-checklist.md`'s `environments <id>` row: mark ✅ if both paths are clean, or keep ◐ with an updated, more specific note if something new surfaces.
6. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/cli-compatibility-checklist.md`
- `.pm/w4/dev-4/` (running stack, no file changes expected)

## Acceptance criteria

- [ ] A real, existing project's `render environments <id> -o json` output captured and correct.
- [ ] The unknown-id 404 path reconfirmed post-fix.
- [ ] Checklist row updated with evidence for both paths, reflecting the real current state (not upgraded to ✅ unless genuinely clean).

## Out of scope

- Building a `projects create` CLI command — that's forking/patching the CLI, which `.pm/DO_NOT_DO.md` rules out; seed the project out-of-band instead.
