---
id: w4/m22/t002
title: Bring internal/projects into the authz-guards-every-verb sweep
worker: worker4
status: todo
estimate: 30m
depends_on: []
---

## Objective

Close the gap `docs/ADR032-environments.md:9` names explicitly: _"`internal/projects` itself is not in that sweep, an existing gap this feature doesn't inherit."_ Every verb on `projects.Service` must provably require authorization — either by adding `internal/projects` to whatever reflection-based sweep `TestAuthzGuardsEveryVerb` already runs elsewhere (find its home package first), or, if the sweep is structurally coupled to another package, writing an equivalent explicit per-verb deny test for `projects.Service`.

## Context

- Find `TestAuthzGuardsEveryVerb` (likely `internal/core` or `internal/environments` — `docs/ADR032-environments.md:9` references it as an existing mechanism `environments` already satisfies). Read how it enumerates verbs (reflection over the `Service` struct's methods, most likely) before deciding whether `projects.Service` can be added to the same sweep or needs its own copy.
- `lego/backend/internal/projects/service.go` — every exported verb (`List`, `Get`, `Create`, `Rename`/`Update`, `Delete`, and whatever `Set*` verbs exist) must start with an authorize check.
- `lego/backend/internal/environments/environments_test.go:276-287` — `TestVerbsDenyWhenUnauthorized`, the closest existing analog if the reflection sweep turns out to be environments-specific rather than shared.

## Steps

1. Locate the sweep's actual mechanism and its current package scope.
2. Either register `projects.Service` with the shared sweep, or write a `projects`-local equivalent that reflects over its verbs and asserts each denies without authorization.
3. Run it — if any verb is found NOT to check authorization first, that's a real finding: fix the verb (add the missing `s.Authorize`/`AuthorizeOn` call) rather than skip it in the sweep.
4. Update `docs/ADR032-environments.md:9`'s parenthetical once the gap is closed — it should no longer describe `internal/projects` as excluded.

## Files

- `lego/backend/internal/projects/service.go`
- `lego/backend/internal/projects/projects_test.go` (from t001, or new if t001 not yet landed)
- wherever `TestAuthzGuardsEveryVerb` actually lives
- `docs/ADR032-environments.md`

## Acceptance criteria

- [ ] Every exported verb on `projects.Service` is covered by an authz-deny test (shared sweep or local equivalent).
- [ ] Any verb found missing an authorize-first check is fixed, with the fix covered by the new test.
- [ ] `docs/ADR032-environments.md`'s "not in that sweep" note updated to reflect the closed gap.

## Out of scope

- Environments' own sweep coverage — already complete per ADR032; this task is `internal/projects` only.
