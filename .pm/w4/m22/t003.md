---
id: w4/m22/t003
title: Fix internal/projects' ErrProjectsUnavailable REST/GraphQL/MCP 500→503 mapping
worker: worker4
status: todo
estimate: 40m
depends_on: []
---

## Objective

Close the second gap `docs/ADR032-environments.md:25` flags: with `BEX_CP_DB_URI` unset, `projects.ErrProjectsUnavailable` is a package-local sentinel that `core.WriteErr` doesn't recognize, so it "currently falls through to a bare 500 rather than the documented 503-for-unavailable convention." `internal/environments/rest.go` already has the fix pattern for its own sentinel (`ErrEnvironmentsUnavailable`) via a local `writeErr` wrapper that checks it first and maps to 503 before delegating to `core.WriteErr` — apply the same shape to `internal/projects`, across REST, GraphQL, and MCP.

## Context

- `lego/backend/internal/core/errors.go:52-56` — `ErrWorkspacesUnavailable` as the registered-in-core pattern (the alternative to a local wrapper).
- `lego/backend/internal/core/http.go:56` — where registered `Err*Unavailable` sentinels are checked and mapped to 503.
- `lego/backend/internal/environments/rest.go` — the local `writeErr` wrapper pattern to mirror (checks `ErrEnvironmentsUnavailable` first, then delegates).
- `lego/backend/internal/projects/service.go:78` — `ErrProjectsUnavailable` definition; used at lines 136, 164, 194, 206, 237, 253, 288, 291, 338, 341 (every verb).
- `lego/backend/internal/projects/rest.go`, `graphql.go`, `mcp.go` — the three surfaces that need the same mapping (check whether GraphQL/MCP already have their own error-translation layers that need the same fix, or delegate to REST's).

## Steps

1. Read how `internal/environments/rest.go` structures its local `writeErr` wrapper.
2. Add the equivalent to `internal/projects/rest.go`: check `errors.Is(err, projects.ErrProjectsUnavailable)` first, write 503, else delegate to `core.WriteErr`.
3. Check `internal/projects/graphql.go` and `mcp.go` for their own error-mapping paths — apply the same 503 mapping there (GraphQL errors typically carry an extension code; MCP tool errors carry a status-equivalent — match whatever `environments` already does for its own sentinel on those two surfaces).
4. Add a test: with a nil/unwired store, each surface (REST/GraphQL/MCP) returns 503-equivalent, not 500.

## Files

- `lego/backend/internal/projects/rest.go`
- `lego/backend/internal/projects/graphql.go`
- `lego/backend/internal/projects/mcp.go`
- `lego/backend/internal/projects/projects_test.go`

## Acceptance criteria

- [ ] REST: a projects call with `BEX_CP_DB_URI` unset returns 503, not 500.
- [ ] GraphQL and MCP surfaces for projects verbs return the equivalent unavailable signal (matching whatever `environments` already does), not a raw internal error.
- [ ] Regression test proves the 503 mapping and fails against the pre-fix fallthrough.

## Out of scope

- Registering `ErrProjectsUnavailable` in `core` itself — `docs/ADR032-environments.md:25` explains why local sentinels are the established shape (`core` can't import feature packages back); don't restructure that convention here.
