---
id: w4/m21/t002
title: Field: CRD/view + create/PATCH on REST/GraphQL/MCP
worker: worker4
status: todo
estimate: 45m
depends_on: [w4/m21/t001]
---

## Objective

Add `notifyOnFail` with Render's exact name/enum: App CRD field (validated), `AppView` + the three render fragments, settable at create and via `PATCH /v1/services/{id}` / GraphQL mutation / MCP setter (the `setAutoDeploy` grammar). Unknown value ⇒ named 400.

## Files

- `lego/types/v1alpha1/app_types.go` (+ codegen), `lego/backend/internal/apps/{service,rest,graphql,mcp}.go`

## Acceptance criteria

- [ ] Round-trips identically on all three surfaces (adapter-parity test); invalid enum 400s.
