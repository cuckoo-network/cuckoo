---
id: w4/m95/t003
title: Cover shared connection variants, authorization, and CA lifecycle
worker: worker4
status: todo
estimate: 50m
depends_on: [w4/m95/t001, w4/m95/t002]
---

## Objective

Enumerate and test the two existing service entrypoints (REST and GraphQL) and four public outputs (direct URI, PSQL, pooler URI, replica URI). Preserve internal variants. Verify the actual CA and SANs serving each endpoint before claiming one bundle covers it; add narrowly scoped per-endpoint material if the deployed CNPG topology requires it. Check configured/custom server CA references, renewal/rotation and refetch, unprovisioned CA, malformed PEM, foreign ownership, missing resource, expired session, forbidden and fresh revocation. No paid production mutation is authorized by the QA evidence; exercise paid shapes in isolated fixtures. Keep invalid CA/hostname rejection and certificate-only serialization under regression tests. This owns shared blast radius independently of the primary UI fix.

## Acceptance criteria

- [ ] The stated behavior is verified with evidence, and limits are recorded.
- [ ] Preserve the README trust, authorization and credential-handling contract.
