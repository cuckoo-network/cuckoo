---
id: w4/m35/t003
title: Homebrew tap: bex-co/homebrew-tap + formula auto-push on release
worker: worker4
status: done
estimate: 60m
depends_on: [w4/m35/t002]
---

## Objective

Create the public bex-co/homebrew-tap repo (user-authorized 2026-08-15) with README. Add a release-workflow step that renders Formula/bex.rb (version + per-platform sha256 from checksums.txt, on_macos/on_linux + arm/intel blocks, URL-encoding the bex-cli%2FvX.Y.Z tag in asset URLs) and pushes it to the tap with BEX_TAP_PUSH_TOKEN; the step skips cleanly while the secret is unset. Mirror BEX_TAP_PUSH_TOKEN in .env.example and register it in scripts/gh-secrets.sh.

## Files

- .github/workflows/cli-release.yml, .env.example, scripts/gh-secrets.sh, bex-co/homebrew-tap

## Acceptance criteria

- [ ] See milestone Definition of done (this task's slice of it holds).

## Out of scope

- `bex upgrade` self-update (w4/030); homebrew-core submission; Windows targets.
