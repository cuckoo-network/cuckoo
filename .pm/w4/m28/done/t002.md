---
id: w4/m28/t002
title: Core + store — composed rule evaluation across workspace/environment/service layers
worker: worker4
status: done
estimate: 60m
depends_on: [w4/m28/t001]
---

## Objective

Implement the t001 decision in the control plane: one evaluation path that composes the applicable layers' rules (pass-every-layer), the empty-means-deny semantic, and the migration that seeds `0.0.0.0/0` into existing environment rows.

## Context

- Environments store + `SetACL`: `lego/backend/internal/environments/service.go`.
- Control-plane migrations live with the store (`BEX_CP_DB_URI` gated) — the seed migration rides that mechanism.

## Steps

1. Migration: seed existing environments' empty lists per t001.
2. Composition evaluator (pure function, table-testable) + plumbing so projection (t003) consumes the composed result.
3. Keep the SetACL validation path consistent (IPv4 CIDR only, per Render).

## Files

- `lego/backend/internal/environments/service.go` (+ store/migration files)

## Acceptance criteria

- [ ] Table-driven test: layer composition (workspace×env×service presence/absence matrix) produces the decided outcomes
- [ ] Migration test: a pre-existing empty-list environment evaluates open (seeded), a new environment with rules deleted evaluates deny-all

## Out of scope

- The runtime enforcement point (t003)

## Resolution (2026-07-15)

Core+store shipped: `core.EnvironmentLayerCIDRs`/`DenyAllCIDR`/`DefaultEnvironmentAllowList`; seeded `CreateEnvironment` + migration 0039 (shipped as 0038, renumbered in `42e3289e` after colliding with the pre-existing `0038_audit_target_name`; w10/m5 hit the same collision concurrently) (existing empty rows → 0.0.0.0/0 + ::/0, no lockout — pg-gated create-seed invariant test added); `hasACL` distinguishes explicit deny-all `[]` from absent; the fan-out projects a flat CIDR layer (deny-all placeholder for empty) instead of clobbering member lists.
