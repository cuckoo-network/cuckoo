---
id: w4/m28/t004
title: Surfaces — REST/GraphQL/MCP semantics + dashboard copy for seeded default and deny-all
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m28/t002]
---

## Objective

The three machine surfaces and the dashboard tell the truth about the new semantics: creates seed `0.0.0.0/0` where the t001 design says so, deleting all rules reads as deny-all, and the w5/m31 editor's "empty means open" copy is replaced.

## Context

- Environment ACL surfaces: `lego/backend/internal/environments/{rest,graphql,mcp}.go`.
- Dashboard editor + copy: `dashboard/src/features/environments/components/environment-settings-dialog.tsx` + `locales/{en,zh}.ts`.

## Steps

1. Apply the seed-on-create / deny-all-read semantics uniformly across REST/GraphQL/MCP.
2. Update dashboard copy (en + zh) and any empty-state hint to match; keep the full-replace editor flow.
3. Update `docs/ADR032-environments.md` surface docs.

## Files

- `lego/backend/internal/environments/`
- `dashboard/src/features/environments/`
- `docs/ADR032-environments.md`

## Acceptance criteria

- [ ] All three surfaces return/accept identical semantics (cross-surface test)
- [ ] Dashboard shows the seeded default on a new environment and warns before saving an empty (deny-all) list

## Out of scope

- New dashboard flows beyond copy/hint changes

## Resolution (2026-07-15)

Surfaces: wire shapes unchanged (create-seeding is store-level, uniform across REST/GraphQL/MCP; explicit `[]` = deny-all honored everywhere); dashboard ACL editor copy rewritten for the new semantics (scope: services+static sites+datastores, layered composition, deny-all empty state with seeding guidance) in en+zh; environments feature tests green.
