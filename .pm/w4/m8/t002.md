---
id: w4/m8/t002
title: "Dashboard settings: API Keys section — list (created/last fields, never secrets) + revoke with confirmation"
worker: worker4
status: todo
estimate: 40m
depends_on: [w4/m8/t001]
---

## Objective

Add an API Keys section to the dashboard settings area: an Apollo-backed list of the caller's bex-minted keys (name/client id, created date — whatever m3's list shape exposes; secrets are never present in list responses by design) and a revoke action behind a confirmation dialog, with optimistic-update or refetch so a revoked key disappears immediately.

## Context

- `dashboard/CLAUDE.md` before editing; stack = TanStack Start + Apollo + shadcn.
- Settings today: `dashboard/src/features/auth/pages/settings-page/` renders only the Kratos settings flow — decide placement (a sibling section on `/settings` or a settings tab) consistent with the existing routing (`dashboard/src/routes/settings.tsx`).
- GraphQL operations from m3 (t001 confirmed/extended them for sessions).

## Steps

1. Add the GraphQL query/mutation documents + generated types per the dashboard's codegen setup.
2. Build the list + empty state + revoke confirmation (shadcn dialog) in a `features/` module.
3. Wire into the settings route; loading/error states consistent with existing pages.

## Files

- `dashboard/src/features/` (new api-keys module)
- `dashboard/src/routes/settings.tsx` (or the settings page composition)

## Acceptance criteria

- [ ] Logged-in user sees their keys (no secret material anywhere in the DOM or network tab beyond the list shape).
- [ ] Revoke asks for confirmation, then the key is gone from the list and its token stops working.

## Out of scope

- Mint flow (t003).
