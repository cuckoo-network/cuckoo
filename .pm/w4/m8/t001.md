---
id: w4/m8/t001
title: "Session-caller path: confirm/extend GraphQL api-key ops for Kratos-session users (interplay with m4's checker)"
worker: worker4
status: todo
estimate: 30m
depends_on: []
---

## Objective

Make sure a Kratos-session caller (the dashboard) can actually drive the m3 api-key GraphQL operations: verify the session-auth path reaches mint/list/revoke (m3 added session-cookie gating during /simplify — confirm which verbs it covers), extend where it does not, and settle how session identities are authorized once m4's `Checker` enforces — the session subject (`api.IdentityFrom`) needs tuples or an explicit design note so the dashboard surface will not 403 the day enforcement flips.

## Context

- m3 shipped: API-key verbs in Core + REST `/v1/api-keys` + GraphQL + MCP; list/revoke scoped to bex-minted keys via the `bex.co/api-key` metadata marker; session-cookie gating applied during m3 t006.
- m4 t004 (`Checker` seam) and t005 (seed tuples) are in flight — coordinate the session subject's tuple story there rather than duplicating.
- `operator/internal/api/CLAUDE.md` before editing; `docs/bex-api.md` for the adapter conventions.

## Steps

1. Trace the GraphQL api-key resolvers' auth requirements for a session (not bearer) caller; write a failing-or-passing check first.
2. Extend the session path where a verb is bearer-only; keep Core logic shared.
3. Record (code comment or docs/auth.md note) how session subjects map to checker tuples, aligned with m4's model.
4. `make test` green (GOTOOLCHAIN pin per memory).

## Files

- `operator/internal/api/` (GraphQL adapter, auth middleware as needed)
- `docs/auth.md` or `docs/bex-api.md` (session-subject authorization note)

## Acceptance criteria

- [ ] A Kratos session can mint, list, and revoke via GraphQL on the mock cluster (or httptest-level equivalent).
- [ ] The m4 tuple story for session subjects is written down; no contradiction with the checker design.

## Out of scope

- The dashboard UI itself (t002/t003); changing m4's model.
