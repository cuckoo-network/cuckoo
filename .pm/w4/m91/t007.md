---
id: w4/m91/t007
title: Closeout
worker: worker4
status: todo
estimate: 15m
depends_on: [w4/m91/t005, w4/m91/t006]
---

## Objective

Close the milestone once its definition of done actually holds live.

## Context

- DoD in `../README.md` — verify each bullet against the real product before moving the milestone to `done/`.

## Steps

1. Re-verify each DoD bullet live (1 GB → ~1 GiB capacity, 5 GB → ~5 GiB, three API surfaces agree, KeyValue matches StorageGB, used-series unchanged, ops alert still physical, regression test present).
2. Mark all tasks `status: done`, mark rows `— DONE`, set `**Status:** done` in the milestone README, move `w4/m91/` → `w4/done/m91/`, check `- [x]` in `w4/README.md`.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [ ] Every DoD bullet verified live before the move.
- [ ] Milestone moved to `done/`; workstream README checked.

## Out of scope

- Any remaining work — closeout only runs when the DoD is real.
