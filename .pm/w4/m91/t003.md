---
id: w4/m91/t003
title: KeyValue parity (+ scope compute-service disks)
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m91/t001]
---

## Objective

Confirm the t001 fix covers managed KeyValue (which has its own logical `StorageGB` and a `data-<name>` PVC subject to the same 10 GiB Hetzner floor), and determine whether compute-service disks (ADR082 persistent disks) share the logical-vs-physical split.

## Context

- KeyValue logical size: `lego/backend/internal/keyvalue/service.go:155` (`StorageGB`), `graphql.go:67`, `tiers.go:39`.
- `dashboard/src/features/metrics/types.ts:52` notes `disk`/`disk_capacity` also apply to services now (ADR082) — a service disk's capacity may or may not have the same split depending on how its PVC is provisioned.
- This was reasoned about but NOT exercised live in the source hunt — verify against a real KeyValue instance.

## Steps

1. Exercise a managed KeyValue instance's Metrics disk panel live; confirm "Capacity" equals its logical `StorageGB` after t001 (not the physical `data-<name>` PVC).
2. If KeyValue routes through a different code path than Postgres for `disk_capacity`, extend the t001 fix to cover it.
3. Determine whether a compute-service disk's `disk_capacity` reports logical or physical; if physical and it diverges, either fold the fix in or file a scoped follow-up note (don't silently leave it).

## Files

- `lego/backend/internal/keyvalue/*` (only if a KV-specific path exists)
- `lego/backend/internal/metrics/*`

## Acceptance criteria

- [ ] A live KeyValue instance's Metrics "Capacity" equals its logical `StorageGB`.
- [ ] The compute-service disk capacity behavior is stated (covered here or filed as a scoped follow-up).

## Out of scope

- Building anything new for compute-service disks beyond confirming/scoping their capacity metric.
