---
id: w4/m93/t005
title: Test coverage
worker: worker4
status: todo
estimate: 35m
depends_on: [w4/m93/t003]
---

## Objective

Add regression tests that fail on today's missing-group projection, including the already-correct own-value control.

Exercise the actual native preparer/decoder with group-only, no-env, own-over-group, ordered multi-group, literal-over-secret, empty and newline values (reuse m92 byte tests), and reserved keys. Assert values reach the command, not just that a volume name exists. Use controller tests for uncached namespace selection, optional absent groups versus required-source failure, App-UID ownership, two services sharing a group, cancel/retry and finalizer inventory. Guard native group-reference artifact semantics and ensure explicit kpack/Dockerfile behavior remains as documented. Cover static/default-Node and cron/worker/private wiring without requiring paid production fixtures.

## Acceptance criteria

- [ ] Tests detect the pre-fix group omission and preserve passing own-value behavior.
- [ ] Appropriate operator tests, codegen and lint pass; run other module gates if their contracts change.
- [ ] Live acceptance uses only disposable free QA resources and cleans them up.
