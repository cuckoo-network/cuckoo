---
id: w4/m30/t004
title: Render parity — PATCH semantics identical across surfaces vs Render's PATCH /environments/{id}
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m30/t002]
---

## Objective

Verify the partial-update contract matches Render's `PATCH /environments/{id}` (field names, optional-field semantics, error shapes) and that REST/GraphQL/MCP produce identical results for identical patches; flag drift as follow-up.

## Context

- Render OpenAPI snapshot (2026-07-15 copy); `docs/ADR032-environments.md`; `docs/render-artifacts/protected-environments.md`.

## Steps

1. Diff the PATCH body contract against the spec.
2. Exercise the same patch on all three surfaces; compare views and error shapes.
3. File any drift.

## Files

- `lego/backend/internal/environments/` (verification)

## Acceptance criteria

- [ ] Contract parity confirmed or drift filed
- [ ] Three surfaces agree

## Out of scope

- Dashboard environments UI changes
