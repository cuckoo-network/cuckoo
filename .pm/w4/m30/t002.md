---
id: w4/m30/t002
title: GraphQL updateEnvironment + MCP update_environment riding the core verb
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m30/t001]
---

## Objective

Expose the t001 core `Update` verb on the other two surfaces: a GraphQL `updateEnvironment` mutation and an MCP `update_environment` tool, with the same optional-field semantics as REST PATCH.

## Context

- `lego/backend/internal/environments/graphql.go` — currently only `renameEnvironment` + `setEnvironmentACL` (full replace).
- `lego/backend/internal/environments/mcp.go` — same shape (`rename_environment`, `set_environment_acl`).
- w6/m33's `CreateWithACL` wiring in both files is the pattern to mirror; keep the existing rename/setACL verbs (bex-native dialect) working.

## Steps

1. GraphQL mutation with nullable args mapping to `EnvironmentPatch` pointers.
2. MCP tool with optional fields + a truthful description.
3. Cross-check the three surfaces return the same view for the same patch.

## Files

- `lego/backend/internal/environments/graphql.go`, `mcp.go`

## Acceptance criteria

- [ ] Both new verbs ride `Update` — no merge logic in the adapters
- [ ] Absent-vs-zero distinction preserved (`networkIsolationEnabled: false` appliable)

## Out of scope

- Deprecating the existing rename/setACL verbs
