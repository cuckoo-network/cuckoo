# Sweep stale w4-owned evidence docs: two records falsified by later shipping

← filed by `/pm-brainstorm for w4 focusing on polishing existing features`, 2026-07-15. Sub-hour, doc-only.

1. **`docs/ADR018-render-parity.md:209`** — the "Official Render CLI browser login + refresh + per-user logout" row still reads `◐ — … production bootstrap/deploy/E2E evidence remains`, but `w4/m27` closed 2026-07-15 with exactly that evidence: `scripts/render-cli-auth-e2e.sh` exit 0 against production `api.bex.co` in a real Chrome, and `docs/cli-compatibility-checklist.md`'s login/logout rows already flipped ✅. Update the row ◐ → ✅ with the m27 evidence.

2. **`docs/render-artifacts/owners-api.md:156`** — still records "bex diverges here: `core.WriteErr` renders `{"error": "…"}`" as a documented divergence. Superseded by w9/m38: `WriteErr`/`WriteErrStatus` (`lego/backend/internal/core/http.go:37-90`) now emit the unified Render-shaped `{error, id, message}` envelope on every non-2xx path (live-verified via the official CLI's bad-key 401, checklist line 74). Rewrite the paragraph as shipped behavior (the `error` key is kept for bex-only callers; the divergence is gone).

Run `npx prettier@3.4.2 --write` on both docs when editing.
