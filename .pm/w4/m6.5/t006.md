---
id: w4/m6.5/t006
title: "Simplify — run /simplify over the dashboard code this milestone changed"
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m6.5/t005]
---

## Objective

Run `/simplify` over everything this milestone touched in the dashboard (GraphQL ops/hooks, the Environment route, the env-vars table + editor components) — reuse, simplification, efficiency; strictly behavior-preserving.

## Context

- Standing closing task; scope = this milestone's diff only. Reuse the dashboard's existing table/loading/empty-state and hook patterns rather than re-implementing them.

## Steps

1. `/simplify` over the milestone's changed dashboard files.
2. `dashboard` test + lint green to prove behavior unchanged.

## Files

- Whatever the milestone changed under `dashboard/`.

## Acceptance criteria

- [ ] Simplifications applied or explicitly none found; dashboard tests + lint still green.

## Out of scope

- New behavior.
