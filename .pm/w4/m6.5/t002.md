---
id: w4/m6.5/t002
title: "Environment route + service-nav item (Overview / Environment / Logs / Metrics)"
worker: worker4
status: done
estimate: 30m
depends_on: []
---

## Objective

Add the **Environment** tab to the service detail page: a new route `services.$serviceId.env.tsx` and an "Environment" item in the service nav, placed Render-style (Overview → Environment → Logs → Metrics). Empty shell for now; the table lands in t003.

## Context

- The service-detail IA already exists (w5/m5): layout `dashboard/src/routes/services.$serviceId.tsx`, tabs `services.$serviceId.index.tsx` (Overview), `.logs.tsx`, `.metrics.tsx`, and the nav component `dashboard/src/features/services/components/service-nav.tsx`.
- TanStack Router file-based routes; mirror the existing tab routes' structure and the nav's active-state handling.
- i18n: the dashboard is internationalized (w5/m3) — add the "Environment" label via the existing i18n mechanism, not a hardcoded string.

## Steps

1. Add `services.$serviceId.env.tsx` mirroring the logs/metrics tab routes.
2. Add the Environment entry to `service-nav.tsx` in Render's tab order.
3. Add the i18n label.

## Files

- `dashboard/src/routes/services.$serviceId.env.tsx` (new)
- `dashboard/src/features/services/components/service-nav.tsx`
- i18n locale files

## Acceptance criteria

- [ ] The Environment tab appears in the service nav and routes to the new page.
- [ ] Label is i18n'd; active-state styling matches the other tabs.

## Out of scope

- The env-var table + reveal + edit (t003/t004).
