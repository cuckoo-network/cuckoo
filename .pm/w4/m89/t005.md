---
id: w4/m89/t005
title: Render parity — coded errors identical across REST/GraphQL/MCP/UI
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m89/t002, w4/m89/t003, w4/m89/t004]
---

## Objective

Make sure the new codes reach all three API surfaces identically, in Render's error dialect.

## Context

- `docs/ADR018-render-parity.md` records that Render has no coding-agent product, so parity here is bex's own cross-surface discipline: identical fields and error shapes on REST, GraphQL and MCP.
- `TestViewFieldsAndCodedErrorsMatchEverySurface` already asserts this for the existing agent-session codes and will need the new ones.
- MCP coded errors are wrapped structurally at registration by `internal/mcputil.AddTool` (w1/m75) — no per-handler work should be needed, but confirm the new codes actually survive that path.

## Steps

1. Extend the cross-surface tests to the new codes.
2. Verify each code's HTTP status, GraphQL extension and MCP error text agree.
3. Confirm the messages carry no internal detail on any surface.
4. Update the ADR018 agent-sessions row if the taxonomy is worth recording.

## Files

- `lego/backend/internal/agentsessions/`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Every new code appears on all three surfaces with the same semantics.
- [ ] Cross-surface tests cover them and pass.
- [ ] No internal detail leaks on any surface.

## Out of scope

- Changing the pre-existing agent-session codes.
