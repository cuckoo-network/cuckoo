---
id: w4/m87/t006
title: Test coverage
worker: worker4
status: todo
estimate: 45m
depends_on: [w4/m87/t004]
---

## Objective

Prove source timestamps and duration rendering across live, replay, reconnect, legacy, and pathological sequences.

## Context

- Driver tests can assert one-time stamping at the publication boundary.
- Dashboard tests can feed identical parts through live and replay modes and compare rendered duration output.
- Tests must cover failure modes rather than snapshot an entire conversation.

## Steps

1. Add driver fixture coverage for all typed part families and original-timestamp preservation.
2. Add dashboard table cases for live/replay equivalence, reconnect dedupe, legacy/mixed histories, and invalid/out-of-order times.
3. Run driver tests plus dashboard typecheck, lint, and unit tests.

## Files

- `lego/agent-image/driver/test/`
- `lego/agent-image/driver/fixtures/acp-agent.mjs`
- `dashboard/src/features/agent-sessions/lib/__tests__/`
- `dashboard/src/features/agent-sessions/components/__tests__/`

## Acceptance criteria

- [ ] Tests fail against the pre-milestone arrival-time-only implementation.
- [ ] Identical transcripts render identical durations live and replayed.
- [ ] Reconnect, legacy, mixed, invalid, and out-of-order cases are behaviorally asserted.
- [ ] Driver and dashboard verification gates pass.

## Out of scope

- Snapshot-everything tests or live provider credentials in CI.
