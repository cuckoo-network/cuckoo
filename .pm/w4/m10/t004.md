---
id: w4/m10/t004
title: Acceptance — one event per write verb, denials recorded, no value leakage
worker: worker4
status: todo
estimate: 25m
depends_on: [w4/m10/t003]
---

## Objective

Prove coverage and hygiene: enumerate every write verb (the same inventory `TestAuthzGuardsEveryVerb` guards) and assert each emits exactly one audit event; a cross-tenant 403 produces a denial event; env-var and connection-info flows never leak values into events; the read surface shows it all.

## Context

- `TestAuthzGuardsEveryVerb` (w4/m4) already maintains the verb inventory — reuse it so a future verb that joins authz automatically joins the audit assertion, keeping the two lists from drifting.
- Live check on the mock cluster with `BEX_CP_DB_URI` set: suspend/resume/env-var write → query `/v1/audit-events`.

## Steps

1. Extend the verb-inventory test: every write verb → exactly one success event with correct caller/resource; denial path → one denied event.
2. Grep-style assertion over serialized events: no env-var values, no connection strings, no secrets.
3. Live smoke on the mock cluster end-to-end.

## Files

- `lego/backend/internal/api/*_test.go` (inventory extension)
- feature test files as needed

## Acceptance criteria

- [ ] Inventory test fails if a write verb emits zero or two events.
- [ ] Denials recorded; payload hygiene assertions pass; live smoke green.

## Out of scope

- Perf/volume testing.
