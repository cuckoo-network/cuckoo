---
id: w4/m2/t005
title: Simplify — run `/simplify` over the code this milestone changed
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m2/t004]
---

## Objective

Run `/simplify` over the m2 diff — `operator/internal/api/auth.go`, `server.go`, `graphql.go`, `cmd/api/main.go`, scripts and manifests — for reuse, simplification, and efficiency. Behavior-preserving only.

## Context

- Standing closing task; applies to the diff of w4/m2 t001–t004.

## Steps

1. `/simplify` scoped to the milestone's changed files; apply accepted cleanups.
2. `make test` and the t004 E2E script must stay green.

## Files

- `operator/internal/api/`, `operator/cmd/api/main.go`, `scripts/`

## Acceptance criteria

- [ ] Cleanups applied or explicitly rejected with reasons; `make test` + E2E green.

## Out of scope

- New functionality or behavior changes.
