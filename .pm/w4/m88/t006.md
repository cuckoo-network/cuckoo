---
id: w4/m88/t006
title: Closeout
worker: worker4
status: todo
estimate: 10m
depends_on: [w4/m88/t004, w4/m88/t005]
---

## Objective

Close the milestone only when the definition of done is actually met: move tasks to `done/`, mark the milestone done, move `w4/m88/` to `w4/done/m88/`, and check the workstream README box.

## Context

- DoD lives in `w4/m88/README.md`. Completing this task _is_ the board move — do not run it early.

## Steps

1. Confirm DoD: k9s-visible JSON errors, Loki dashboard pod streams, Loki Traefik `dashboard.bex.co` access lines; tenant App log paths unchanged.
2. Set remaining tasks `status: done`, move each `tNNN.md` to `w4/m88/done/`, mark README rows/`**Status:**`, move the milestone to `w4/done/m88/`, check `- [x]` in `w4/README.md`.

## Files

- `.pm/w4/m88/**`
- `.pm/w4/README.md`

## Acceptance criteria

- [ ] DoD observables verified (or explicitly blocked with evidence if prod query unavailable — then do not close).
- [ ] Milestone directory lives only under `w4/done/m88/` with no tombstone at `w4/m88/`.

## Out of scope

- Expanding platform log coverage to other namespaces/hosts.

## Live verification log (2026-08-20)

Shipper DoD proven against prod Loki after temporarily loading working-tree Alloy config (Argo `bex-platform-prod` self-heals `log-shipper` from `origin/main`, so durable enablement requires `/ship`):

- `{type="platform",service="dashboard"}` — streams present; sample listen/shutdown lines from ns `dashboard` pods; labels include `type=platform`, `service=dashboard`, `namespace=dashboard` (no fake `app`).
- `{type="request",service="dashboard"} |= "dashboard.bex.co"` — streams present; labels `method`/`status`/`service=dashboard`; **`host` not a Loki label** (line-only `RequestHost`).
- `label/type/values` includes `platform` alongside existing tenant types.
- Unit: `yarn vitest run src/common/lib/__tests__/server-log.test.ts` — 6/6 pass; `scripts/gitops-validate.sh` platform-dashboard retention guard passes.

**Still blocked for closeout:** live pod JSON `level=error` lines. Running image is still `ghcr.io/bex-co/bex-dashboard@sha256:1e451a7f…` without t001 code. Needs `/ship` → CI dashboard image → Argo digest roll, then deliberate SSR/route failure visible in `kubectl -n dashboard logs`.
