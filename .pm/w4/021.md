# Finish the orphaned w4/m23: run t006 (simplify) + t008 (closeout)

`w4/m23` sits under `.pm/w4/done/m23/` but two of its tasks were never done: `t006.md` (the `/simplify` pass over the milestone's diff — audit-log `direction` enum, `suspenders` array, denied-read fan-out cap, Environment `ipAllowList {cidrBlock, description}` wire) and `t008.md` (closeout) are both still `status: todo`, and the w4 README checkbox is unchecked. The code shipped (a parallel session implemented the source notes directly), but the simplify review was never run and the milestone was never formally closed.

Do it: run `/simplify` over that diff, apply findings, then complete the closeout properly (task statuses, README rows, checkbox).

Coordinate: `w10/m3/t008` owns the _bookkeeping drift_ half (the unchecked w4 README m23 checkbox) — this note is the actual unrun work; whoever gets there first should do both and tell the other.

~30m. Source: `/pm-brainstorm` round 16's closeout-residual sweep, 2026-07-15.
