---
id: w4/m18/t003
title: Active sessions card — Kratos /sessions list + sign out other sessions
worker: worker4
status: todo
estimate: 40m
depends_on: []
---

## Objective

Execute inbox `006`: an **Active sessions** card in Settings → Security & Compliance listing the user's active Kratos sessions (authenticated-at, current-session marker; device/IP metadata where Kratos provides it) with a "Sign out other sessions" action, and optionally per-session revoke. Kratos-native — no custom auth code.

## Context

- Kratos public API with the session cookie: `GET /sessions` (others' list), `DELETE /sessions` (revoke all others), `DELETE /sessions/{id}` (one). The dashboard already talks to Kratos through its session plumbing (`session.ts`, `use-ory-flow.ts`).
- `w4/006` (now in `done/`) has the original scoping, including the promote-conditions that would have made this its own milestone — none apply; keep it one card.

## Steps

1. Server-fn or direct client call (match how the settings flow talks to Kratos today) for list + revoke-others.
2. Card UI beside the Connected agents card: session rows, current-session badge, sign-out-others with confirm.
3. i18n (en/zh) + component tests.

## Files

- `dashboard/src/features/settings/` (card + tests)
- `dashboard/src/common/server-fn/session.ts` (if the calls live server-side)
- locale files (en/zh)

## Acceptance criteria

- [ ] Two browser sessions → the card lists both; sign-out-others kills the other one (it's logged out on next action) while the current survives.
- [ ] Current session is visibly marked and not revocable from the card.
- [ ] `yarn test` / `yarn lint` green.

## Out of scope

- Per-session geo enrichment, auto-revoke on password change, admin-initiated revoke of a member's sessions (the `006` promote-conditions — file a note if wanted later).
