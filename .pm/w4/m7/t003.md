---
id: w4/m7/t003
title: "Kratos values: enable courier from secret + verification flow; local overlay targets Mailpit"
worker: worker4
status: todo
estimate: 35m
depends_on: [w4/m7/t002]
---

## Objective

Flip the two switches the values file has been carrying TODOs for: `courier.enabled: true` with `courier.smtp.connection_uri` sourced from the `auth/kratos` Secret (t001), and `selfservice.flows.verification.enabled: true` (the identity schema already declares `verification.via: email` and recovery is already enabled-but-undeliverable). The local overlay overrides the connection URI to Mailpit's SMTP service.

## Context

- `deploy/gitops/base/values/kratos.values.yaml:76-101` — recovery enabled with the "can't deliver until courier.smtp lands" comment; `verification.enabled: false`; `courier.enabled: false`.
- Local overlay values layering: `deploy/gitops/overlays/local/values/` (per docs/auth.md §4).
- Recovery/verification `ui_url`s already point at the dashboard pages.

## Steps

1. Base values: enable courier (URI from Secret env/ref per chart convention) + verification flow; remove the stale TODO comments.
2. Local overlay: connection URI → `smtp://mailpit.<ns>.svc:1025` (no TLS locally).
3. Sync on the mock; Kratos healthy; courier logs show it is running.

## Files

- `deploy/gitops/base/values/kratos.values.yaml`
- `deploy/gitops/overlays/local/values/kratos.values.yaml` (or the overlay's inline override mechanism)

## Acceptance criteria

- [ ] Kratos pods healthy with courier enabled; no secret material in git.
- [ ] Triggering a recovery flow produces a message in Mailpit (manual check; scripted in t004).

## Out of scope

- E2E scripting (t004); SMTP provider account setup.
