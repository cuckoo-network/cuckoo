---
id: w1/m63/t015
title: Test coverage: schema exhaustiveness, conformance corpus, sync semantics, and cross-entrypoint invariance
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m63/t013, w1/m63/t014]
---

## Objective

Standing closing task: consolidate meaningful behavioral coverage for the contract m63 shipped, including mutation-sensitive guards that fail if a field is dropped, an entrypoint bypasses the compiler, or omission collapses into a zero value.

## Context

- Per-task tests cover individual paths; closeout requires a corpus spanning the whole pinned schema and all entrypoints.
- Tests must assert infrastructure/plan/error behavior, not only snapshots or parser round-trips.
- The relevant CI suites are backend, operator/types, dashboard, schema drift/exhaustiveness, and official CLI compatibility.

## Steps

1. Audit coverage against every capability-registry entry and add one meaningful accepted/unsupported/deprecated fixture per behavior class.
2. Add create→dashboard/API edit→sync sequences proving preserve/reset rules and no-op reapply across services/datastores/env.
3. Add cross-entrypoint and mutation tests for ignored-field prevention, compiler routing, secret redaction, no-write-on-error, and deterministic plans.
4. Run `cd lego/backend && go test ./...`, `make test` + `make lint` from `lego/operator/`, dashboard tests/typecheck/lint, schema checks, and CLI fixtures.

## Files

- `lego/backend/internal/apps/testdata/blueprints/**`
- `lego/backend/internal/apps/**/*_test.go`
- `lego/operator/internal/controller/**/*_test.go`
- `dashboard/src/**/__tests__/**`
- `scripts/*blueprint*verify*.sh`

## Acceptance criteria

- [ ] Registry exhaustiveness and mutation tests fail when a field handler/classification or compiler call is removed.
- [ ] Create/sync tests prove the full omission/explicit-empty matrix and zero writes for invalid/unsupported manifests.
- [ ] All required suites, lint, and official CLI fixtures pass; coverage is behavioral rather than tautological.

## Out of scope

- Tests for Render features deliberately classified unsupported beyond validating their refusal.
