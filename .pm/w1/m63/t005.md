---
id: w1/m63/t005
title: Postgres + Key Value create/sync semantics: defaults, omission, explicit empty, and plan spellings
worker: worker1
status: todo
estimate: 50m
depends_on: [w1/m63/t003]
---

## Objective

Apply the same presence-aware discipline to Postgres and Key Value resources, using Render boundary spellings/defaults for new resources while retaining current configuration for omitted existing-resource fields.

## Context

- `deploy.go:1995` and `applyKeyValueSpec` wholesale-copy Blueprint-owned datastore fields.
- Render's current Postgres default is `basic-256mb`, not bex's internal free default; legacy and current plan vocabularies need explicit translation/refusal.
- IP allow lists, disk size/autoscaling, connection pool, persistence mode, and version have distinct create/update constraints.

## Steps

1. Add field policies and exact Render-to-catalog plan translation for Postgres and Key Value.
2. Separate create defaults from sync preservation and validate immutable/grow-only transitions before mutation.
3. Preserve omitted IP rules and persistence/eviction settings; distinguish explicit empty lists.
4. Keep unchanged upserts no-op and return field-path conflicts for illegal downgrades/immutable changes.

## Files

- `lego/backend/internal/apps/blueprint_plan.go`
- `lego/backend/internal/apps/deploy.go`
- `lego/backend/internal/postgres/**`
- `lego/backend/internal/keyvalue/**`
- `lego/types/tiers/tiers.yaml`
- `lego/backend/internal/apps/stack_test.go`

## Acceptance criteria

- [ ] New-resource defaults and accepted plan spellings match the pinned Render contract or produce an explicit unsupported-plan error.
- [ ] Existing-resource omission preserves every documented datastore field; explicit empties and illegal transitions are tested.
- [ ] No unchanged datastore spec is rewritten/restarted.

## Out of scope

- Per-resource region placement and preview databases.
