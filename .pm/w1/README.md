# w1 — project workstream (worker1)

**Worker:** worker1. This is a general-purpose bex workstream. It may take work anywhere in the project; the milestones below are scheduled work and historical records, not a permanent purpose, specialty, or ownership boundary.

## Local dev environment

Develop against `.pm/w1/dev-1/`, this worker's own isolated stack on the shared local kind/CAPD cluster — never the shared cluster's default `auth`/`bex-system` namespaces or standard ports (5173/4433/4445/8090/8091/5432), which any other worker's session may also be using. `dev-1` gets its own Kratos + Hydra + Mailpit (namespace `dev-1-auth`) and app namespace (`dev-1`), reusing the shared cluster's CNPG operator and bex operator, plus a locally-built `bex-api` on dedicated ports derived from N=1 (`dev-1/ports.env`) so it never collides with any other workstream's `dev-N`.

- `bash scripts/dev-env.sh 1 up` — bring it up (idempotent — safe to re-run)
- `bash scripts/dev-env.sh 1 status` — health check (processes, pods, HTTP) + a pass/fail verification inventory
- `bash scripts/dev-env.sh 1 down` — tear it down (leaves the shared cluster and every other workstream's `dev-N` untouched)
- `bash scripts/dev-env.sh 1 clean` — reclaim `logs/` and `bin/` (refuses while the environment is up)

`up` prints the dashboard command to point at it once bex-api is running. One shared
implementation serves every workstream since `w1/m72`; `.pm/w1/dev-1/` keeps only
`ports.env` (a generated record of the derivation), this README, and `.gitignore`.

## Milestones

- [x] **m75** — Architecture-survey bug sweep: five cross-module drift fixes (9 tasks) ← promoted from `052.md`–`056.md`, the 2026-08-19 architecture-survey findings; the survey's pricing/tiers drift guard was deliberately excluded (owned elsewhere, user decision 2026-08-19). **Done 2026-08-19**, moved to `done/m75/`: all five fixed with regression tests **proven red on the pre-fix code**, and the sweep found three more instances of the same defect class than the notes did. The build-Job name derivation moved into the contract module both sides import (new `lego/types/k8sname` leaf + `appv1alpha1.RevisionJobName`/`BuildJobName`/`BuildRevision`) — which then absorbed **`predeploy.JobName`**, where the truncation bug was not theoretical: App CR names are tenant-prefixed (`tea-<xid>-<service>`), so any service name ≥23 chars collapsed every revision to ONE name and **silently skipped that revision's migration**. Fixing it opens a transition window (a migration already running under the old name would have run a second time, concurrently), closed by sweeping when the recorded Job name differs from the computed one. **`publish.JobName`** was a third copy, where a name hit reads as "already published" ⇒ stale content. The `gen-<n>` spelling was itself still hand-synced and is now shared. MCP coded errors are fixed structurally rather than per handler: `internal/mcputil.AddTool` wraps at registration — the last point the typed error exists, since the SDK converts it to text before any middleware — across all 175 registrations, with a **forbidigo lint rule** (type-resolved, so aliases and explicit instantiation are caught) forbidding the raw SDK call and the 49 now-redundant `core.MCPError` calls deleted. Deliberately NOT migrated: four other 63-char truncations that are length-safe today but whose rename would strand finalizer-awaited Jobs or gap live routing.
- [x] **m74** — The second MCP fold: the per-field `update_*` / `rename_*` tools (6 tasks) ← promoted from `051.md` during the 2026-08-18 w1 triage. **Done 2026-08-18**, moved to `done/m74/`: **187 → 175 tools** (−12, none created), measured off the live registry. The note's three open questions were answered from REST's own shape rather than fresh judgement — **fold what `PATCH` carries in the resource's body, keep what REST puts behind its own route** — which is why `scale_service`, the static routes/headers tools and the autoscaling verbs survive while plan/version/name/idleTTL/publishPath/schedule folded. `update_service` gained `dryRun` on REST's exact terms with one deliberate divergence: a dry run carrying a non-plan field is **refused and the field named**, where REST silently drops it. Plan changes stay billing events (the gates are in the Service layer; the fold removed a tool name, not a guard), and `update_cron_job` folded because upstream removed its own placeholder — the lost positioning is recorded in the ledger while ADR018 keeps its ✅. A cap-description invariant that folding would have dropped (the disk-autoscaling figure interpolated from the shared plan catalog) was carried onto the patch tool instead.
- [x] **m73** — Close the digest-pinning inventory for the images bex chooses (4 tasks) ← promoted from `046.md`'s R5-F6 supply-chain chunk during the 2026-08-18 w1 triage, after **seven consecutive security rounds** re-reported it. **Done 2026-08-18**, moved to `done/m73/`: pinned `valkey/valkey:7-alpine` and the `oliver006/redis_exporter` sidecar (which shares the tenant pod and its password), both digests resolved from the registry — `valkey:8-alpine` resolved to exactly the already-pinned default, cross-checking the method. The finding is why the residual survived seven reports: `valkeyImage`'s comment justified it with "bex cannot pre-resolve a digest for a major it has not seen", but `KeyValueSpec.Version` is a closed CRD enum (`"7"`/`"8"`) — the rationale was never re-checked against the CRD. The guard now derives its expectation from that enum marker, so adding a major without its digest fails CI; a previously-inverted test that asserted the explicit-version path **un**pinned was flipped. Still open and re-scoped honestly: `apk add age` at container start (three sites — needs a published image carrying `age`, ADR060 §D7) and the CI/provisioning download class (F13/F14/F15).
- [x] **m72** — Converge the ten `dev-N` harnesses onto one parameterized script (7 tasks) ← from the principal-engineer architecture review 2026-08-18 (session hand-off, no inbox note): 133 tracked files across `.pm/wN/dev-N/`, ~708 LOC copied ten times (7,078 tracked LOC), and **already diverged — `up.sh` has 8 distinct variants across the 10 copies** (only `w2`/`w7`/`w10` agree; `w5` is 274 lines against `w2`'s 175), `status.sh` has 5, `down.sh` alone is converged. The drift now changes what is testable where: `dev-1` wires `BEX_SMTP_ADDR` + `BEX_REQUIRE_VERIFIED_INVITE_EMAIL` and forwards a Mailpit SMTP port, `dev-2` does not, so invite-email behaviour can only be exercised on some workers' stacks. Independent of m70/m71 — can run in parallel. (Hand-off brief's gitignore item was already done; the real residual is bounding the 3.9 GB in `.pm/w5/dev-5/logs`.) **Done 2026-08-18**, moved to `done/m72/`: `scripts/dev-env.sh <N> {up|down|status|clean|env}` plus templated manifests replace the ten copies — tracked `dev-N` files **133 → 31**, tracked LOC **7,129 → 1,658** against 1,204 LOC of shared implementation (**net −4,267**), `up.sh` variants **8 → 1**, `du -sh .pm` **4.7 GB → 21 MB**. It implements the union, not the intersection, and reconciling the copies found three real defects: **five of the ten harnesses could not start `bex-api` at all** (missing `BEX_ALLOW_INSECURE_AUTHZ`, which m65's fail-closed authz requires), the Hydra issuer pointed at a dead port in eight of ten, and three settings claimed the same port family. The 3.9 GB turned out to be a retry loop with no backoff writing an error per second after the cluster died — fixed at the loop, not just truncated. t007's live two-workstream walk was **waived by the user** (the local CAPD cluster lost its control-plane PKI and needs a destructive rebuild); the milestone closes on CI-green derivation/isolation tests + shellcheck, with no live walk claimed.
- [x] **m71** — Collapse the bex-invented `set_*` MCP tool grammar (8 tasks) ← same review; **gated on m70**, whose inventory writes this milestone's exact scope. 34 `set_*` tools fold into the ~6 `update_*` tools that already exist (17 `update_*` exist today), for roughly −28 tools and zero ADR018 ✅ lost — the ledger's MCP column scores capability reachability, not tool-name identity. Every folded tool is one bex invented: `internal/apps/mcp.go:88` records "Render's official MCP has no update tools for any of them" and ADR018 repeats it in four rows. **Done 2026-08-18**, moved to `done/m71/`: measured **213 → 187 tools (−26)** and **157,582 → 146,018 schema+description bytes (−7.3%)**, both measured by driving the pre- and post-fold servers through `tools/list` rather than estimated. 30 setters folded into 5 patch tools — `update_service` (18), `update_postgres` (2), `update_key_value` (2), `update_environment` (5, extending the existing tool), `update_project` (3) — with the grammar taken from REST `PATCH` rather than invented: absent argument ⇒ untouched, present ⇒ written exactly (empty included), same apply order, proven by a test asserting MCP and REST produce identical CR specs field by field. 4 setters deliberately stayed standalone (`set_env_var` and friends are merge-one-key upserts against replace-shaped partners). Zero ADR018 ✅ lost; REST/GraphQL unchanged; retired names recorded in `docs/render-artifacts/mcp-setter-fold-2026-08-18.md` with a fail-closed guard. The ~15 remaining per-field `update_*`/`rename_*` tools are the same grammar under a different prefix and are filed as `w1/051.md`, not smuggled in here.
- [x] **m70** — MCP parity pin: classify all 213 tools and fail closed on drift (9 tasks) ← from the principal-engineer architecture review 2026-08-18 (session hand-off, no inbox note): the MCP surface is the only one of the four with no machine-checked contract. REST parity is pinned (`renderOpenAPISHA256` + `render-schema-drift.yml` + `TestRenderConformance` + 119-operation request validation); MCP parity is ten hand-written comments carrying manual check dates (`"checked live 2026-07-13"`, `"@ 2a00be1"`, `"v0.3.0"`), which is how the surface reached 213 tools without anyone deciding to. 86 of 212 parseable descriptions already self-declare `"bex extension"`, and whole fragments declare it once at the file header (`secrets` 10, `envgroups` 17, `jobs` 4). Non-destructive: pins, classifies, guards — renames nothing. **Done 2026-08-18**, moved to `done/m70/`: measured **upstream 22 vs bex 213** by building each server and reading its own `tools/list` — 10 `Parity1to1`, 1 `Superset`, 8 `Divergent`, 194 `Extension`. The pin paid for itself immediately: **four unintended argument bugs** (`create_postgres` `diskSizeGb`/`diskSizeGB` casing, `create_static_site` dropping `autoDeploy`/`buildCommand`, `get_metrics` renaming three args, `trigger_deploy` missing `clearCache` despite ADR018's w2/m30 row), a **stale comment** (`internal/secrets/mcp.go`'s "Render's official MCP has no env-var tools" — upstream ships `update_environment_variables`), and the finding that **upstream is shrinking** (it removed its three placeholder `update_*` tools in #89, 2026-07-23), which m71/t001 must weigh. Classes are derived from the pin rather than declared per call site (a 213-row table would rot the same way the comments did); `TestMCPParityInventory` holds the no-silent-regrowth property. Added a fourth class `Divergent` for the eight tools that share an upstream name while breaking its contract. Also flagged: `internal/jobs` ships 4 MCP tools + REST + GraphQL while one-off jobs are a declared non-goal — needs a decision. Unblocks m71.
- [x] **m69** — Blueprint auto-sync tenant binding: close the identity-less apply pipeline (5 tasks) ← from the round-15 whole-project security scan (Claude Code `/security-review the entire project`, 2026-08-18, HEAD `718a92e6`; one HIGH verified at 9/10, all other passes clean): the git-push Blueprint auto-sync goroutine runs the whole apply pipeline under `core.WithWorkspace(context.Background(), tenantID)` — workspace-named but identity-less — so `Tenant(ctx)` resolves empty and creates land **unlabeled in the shared namespace** (no store row ⇒ invisible to workspace lists/purge/quota/billing, attacker-imaged free-tier workloads outside ADR043 isolation from a mere `developer`-role push), display-name matching runs unscoped (can merge-patch legacy shared-ns CRs and bind a foreign database's connection Secret), and registry creds resolve against the bootstrap `default` workspace. The w9/001 fix covered only the store-row half. **Done 2026-08-18, same session** — `core.WithActingTenant` (server-derived tenant honored after the identity check; identity stays authoritative; acting/named mismatch fails closed) + `triggerBlueprintSync` binds it from the blueprint row + `runSync` fail-closed guard (`ErrBlueprintSyncWorkspaceUnresolved`); core + apps regression tests (labels/namespace, scoped matching, refusal) proven red on pre-fix code; full backend suite + backend lint green. Uncommitted pending `/ship`; moved to `done/m69/`.
- [x] **m68** — Security-scan round 5: executable-selection relations, deployed-config gaps, activation (11 tasks) ← from the 2026-08-10 round-5 codex-security scan (`~/.codex/state/plugins/codex-security/scans/bex/codex-security-bex-UAO3Zb/report.md`, revision `deedf4eb` — the `m66`+`m67` fix commit itself; 11 findings, 3 high + 8 medium, all high-confidence) + the same-day triage that verified all 11 against source. **Zero false positives, but five are already-known dispositions the scanner cannot see** (F1/F3 are m67/m66 controls deliberately shipped inert; F4/F6/F9/F11 are accepted or already-noted residuals → appended to `046.md`). Tier 1 is five real, cheap, prerequisite-free fixes: **F2** — the previous round's `codex #1` fix upgraded five executable-selection verbs to `can_create` and left **four** siblings on `can_operate` (pre-deploy command, cron command, one-off job command, deploy `imageUrl`/`commitId`), so a contributor still gets arbitrary execution with the service's secrets; **F7** — the same class in Postgres, where `log_statement=all` passes `normalizeParameterOverrides` under `can_operate` and `can_view_logs` includes contributor, manufacturing the disclosure `can_view_sensitive` gates; **F8** — a live regression against m67's own fix, whose IP-keyed failure bucket is deployed with no `BEX_TRUSTED_PROXY_CIDRS`, collapsing the internet into one 60/min bucket behind the Traefik pod IP; **F5** — `Recover` reaches `Client.Create` on a caller-selected plan with neither billing gate; **F10** — KeyValue backup `EmptyDir` unbounded while its `database_exports` sibling bounds at 20 GiB. Plus the two activations the previous round paid for and left inert: **F1** (re-stamp the platform clients, flip `BEX_OAUTH_REQUIRE_AUDIENCE=1`) and **F3** (`openbao-restore-drill.yml` — the one `admin.conf` fetcher m66 missed, and the one that then handles the unseal keys — plus the never-captured `BEX_SSH_KNOWN_HOSTS`). Fifth audit in the ADR028 → `m53` → `m65` → `m66`+`m67` lineage. **Code complete 2026-08-10** — all five Tier-1 findings closed at root cause with regression tests proven red on the pre-fix code (F2's guard enumerates the whole executable-selection _class_, since fixing five of nine verbs silently is what caused the recurrence); backend/operator/types/dashboard (303 files / 2077 tests) suites and backend lint green. Also caught in passing: `spec.deletedUsers` was added to the Database types on 2026-08-09 **without regenerating the CRD**, so the API server pruned it and the security fix it belonged to had been inert since the day it shipped — CRD regenerated and `operator-test.yml` now fails on any uncommitted codegen. **Three operator activations remain** (the milestone is deliberately NOT closed until they land): deploy for `BEX_TRUSTED_PROXY_CIDRS`, capture of `BEX_SSH_KNOWN_HOSTS`, and the `BEX_OAUTH_REQUIRE_AUDIENCE` flip after verifying the live Hydra client markers. Uncommitted pending `/ship`. **CLOSED 2026-08-13:** all three activations landed — CIDRs verified live in prod, host-key pin captured/verified via the cluster-private second path + mandatory mode asserted in all three workflows, and `BEX_OAUTH_REQUIRE_AUDIENCE=1` rolled out after confirming the live Hydra platform-client markers (CLI verified working post-flip, startup warning gone).
- [x] **m66** — Security-scan fast path: close the six cheap, confirmed findings (10 tasks) ← from the 2026-08-10 codex-security repository scan (`~/.codex/state/plugins/codex-security/scans/bex/codex-security-bex-qmBeaW/report.md`, revision `855b0ce7`; 18 reportable findings — 6 medium, 12 low, all high-confidence) + the same-day triage that verified all 18 against source (zero false positives) and re-rated **F10 upward**: `SetSecretFile` is the only one of seven call sites missing `storeServiceName` canonicalization, so a `PUT …/secret-files` addressed by `srv-…` id materializes into the pod but is invisible to every read/delete/purge path — a live data bug, not just stale-secret cleanup. Also F9 (webhook transport inherits `ProxyFromEnvironment`, defeating the dial-time SSRF guard), F8 (consent gate never checks `code_challenge_method=S256` despite its own doc comment), F12 (Packer hcloud plugin `>= 1.6.0` with no committed lock, resolved inside the `HCLOUD_TOKEN` job), F7 (CI SSH TOFU before reading `admin.conf` — the `BEX_SSH_KNOWN_HOSTS_FILE` seam exists and no caller uses it), F16-partial (`apk add age` at runtime in the pre-encryption backup stage). Fourth audit in the ADR028 → `w1/m53` → `w1/m65` lineage. **Done 2026-08-10** — all six closed at root cause with tests that fail on the pre-fix code (F10/F9/F8 verified by reverting each fix); F12 revised to a checksum-verified pinned install because Packer ≥1.14 no longer writes `.packer.lock.hcl`; F16 removed the runtime `apk add` and digest-pinned all five backup images with a new `gitops-validate.sh` guard. **F7 ships the mechanism but is inert until an operator captures `BEX_SSH_KNOWN_HOSTS`** (procedure in ADR019). Backend + dashboard (301 files / 2046 tests) + two new shell suites green. Uncommitted pending `/ship`; moved to `done/m66/`.
- [x] **m67** — Security-scan design fixes: admission, token class, connect binding, webhook bounds (9 tasks) ← the Tier-2 half of the same scan + triage: F1 (auth wraps the identity-keyed limiter and there is **no** negative cache, so every unique invalid bearer costs one Hydra/Kratos call — anonymous amplification into shared identity services), F6 (blanket empty-`aud` exception lets a consented DCR client's **human** token be replayed without bex resource binding, though `human` is already computed at the decision point), F5 (connect state is `{workspace, expiry}` only and the callback is anonymous, so the workspace proof and the installation-admin proof belong to different principals — installation login CSRF, made a lockout too by m65's uniqueness binding), F2 + F3 (no endpoint-count quota anywhere and no retention on `webhook_deliveries` — the only `DELETE` is migration `0055`'s one-shot dedup). These recur in the neighborhoods m65 just hardened because one primitive is missing: a durable server-side transaction/record for cross-provider bindings and cross-datastore projections. **Done 2026-08-10** — F1 meters _failures_ rather than requests (a per-request IP budget would have throttled the dashboard's one-pod-IP SSR traffic), on by default; F5 adds `github_connect_transactions` (migration 0071) so the state authorizes nothing and the callback must present the initiating subject, with 7 tests written as the attack; F2 caps endpoints at 25 transactionally; F3 sweeps terminal deliveries hourly in bounded batches. F6 is deliberately env-gated (`BEX_OAUTH_REQUIRE_AUDIENCE`) because enforcing before the platform-client marker is re-stamped would refuse the official Render CLI's logins — activation order in ADR012 §7. Full backend suite green. Uncommitted pending `/ship`; moved to `done/m67/`.
- [x] **m65** — Security-review remediation: close the 2026-08-07 codex-security scan findings (16 tasks) ← from the 2026-08-07 codex-security repository scan (`~/.codex/state/plugins/codex-security/scans/bex/codex-security-bex-hayvC8/report.md`, revision `137555ad`; 16 reportable findings — 3 high, 9 medium, 4 low), user `/pm` handoff same day. Third audit in the ADR028 → `w1/m53` (2026-07-19) lineage; overlaps/extends `docs/ADR045-security-review-round3.md`. **3 high** (F1 clone-token forwarding to attacker Git hosts, F2 cross-tenant GitHub-installation attach, F3 PR-triggered Terraform with prod credentials) are live tenant/contributor-reachable escalation paths; **9 medium** are mostly shared-service DoS + one OpenFGA tuple-convergence bug; **4 low** are fail-open/redirect/SSRF defense-in-depth. Each impl task closes 1–2 findings at root cause with a regression test that fails on the pre-fix code; t001–t012 are independent (parallelizable), high-severity first. **Done 2026-08-07** — all 16 findings remediated + regression-tested; backend suite + CI-gated backend lint + types green, operator touched packages `-race` green, dashboard/mobile green; F2 shipped unique binding + installation-admin verifier, F16 fail-closed startup, F13 secure-by-default invites. Uncommitted pending `/ship`; moved to `done/m65/`.
- [x] **m64** — Agent-sessions dashboard UI on the target API (ADR047 D9) (8 tasks) ← from `w5/035` via the ADR047 D9 deep-research pass; **re-materialized 2026-08-02** after the target-API-shape decision (user deleted the first cut, which pre-dated it): Devin-shaped `/agents` list + composer + `/agents/$id` detail whose conversation column runs on `useChat` over `w3/m43`'s same-origin stream endpoint (transcript replay + live tail, `data-acp` parts via vendored AI Elements, AI SDK v6 pin, ticket-header transport with attach re-mint) — no interim transcript synthesis or localStorage prompts; control-plane metadata stays on GraphQL polling. t001/t003 startable now, t002 mockable against the fixed v1 wire format, t004 hard-gated on `w3/m43`. **DONE 2026-08-18** — shipped + deployed to prod (dashboard image `b31301c8`); t008's logged-in prod live-substrate walk was **waived by the user**, the milestone closing on `w3/m43`'s green live E2E + the transport's unit tests against that exact wire format + local render verification (`done/m64/done/t008.md`).
- [x] **m63** — `render.yaml` parity: one strict, presence-aware Blueprint compiler (16 tasks) ← from [docs/ADR049-render-yaml-parity.md](../../docs/ADR049-render-yaml-parity.md) (Accepted 2026-08-02; user `$pm` handoff same session): **done 2026-08-03** — `render.yaml` is canonical with `bex.yml` as an identical-grammar warning alias; a pinned Render schema + exhaustive capability registry drives one fail-closed compiler/current-state planner across REST, GraphQL, MCP, dashboard, Git sync, and the API helper. Unsupported intent refuses pre-write with source locations; source-presence semantics, explicit plans, and no-write routing are regression-tested. Fresh production deploy `0173d4ed` and the unmodified Render CLI proved valid and `checksPass` refusal shapes; backend/operator/dashboard suites and lint are green. Moved to `done/m63/`.
- [x] **m62** — Payment-method onboarding + paid-tier gating (12 tasks) ← from `docs/ADR046-payment-onboarding-and-paid-gating.md` (Accepted 2026-07-31; user `/pm` handoff same day, from the signup-card investigation): signup stays card-free, but any paid intent (non-free tier create/plan change on service/Postgres/Key Value) requires a Stripe-bound payment method — webhook-stamped local marker + injected `PaymentGate` in the service layer (402/`PAYMENT_REQUIRED` across REST/GraphQL/MCP, dashboard just-in-time interception), env-gated by `BEX_REQUIRE_PAYMENT_METHOD`; the emitter stops auto-provisioning Stripe Customers/uncollectable invoices for card-less workspaces (ADR040 lineage: w7/m50–m53, w1/m60–m61). **Done 2026-07-31:** live local current-tree evidence covered REST/GraphQL/MCP/official CLI refusal, signed-webhook replay and identical-request success, emitter zero-call withholding/backfill, env-off invariance, plus dashboard interception; backend/operator/dashboard/real-Postgres/script/kustomize suites and lint are green. **Uncommitted pending `/ship`.**
- [x] **m61** — Workspace-delete teardown: close the three resource-deletion gaps (8 tasks) ← from the 2026-07-31 workspace-delete code review (Q&A session, no inbox note): purger retry unreachable once the tenant-row cascade lands (permanent orphans: live CNPG clusters/PVCs/backups, KeyValue CRs, OpenBao paths, env groups), sandboxes never stopped + `sandbox_tenant_keys` doesn't cascade (orphaned resolvable credential), Stripe never told (subscription stays active). ADR045 Finding 6 (w7/m62) fixed the adjacent namespace-prune bug; these were out of its window — **done 2026-07-31**: `workspaces.Service.Delete` split into pre-cascade purgers (secrets/envgroups/postgres/keyvalue/**sandbox**/**Stripe**, run while the row lives ⇒ a transient failure is retryable, and the sandbox-key/subscription-id reads still resolve) + post-cascade (App CRs, projector-resurrection-safe); migration 0056 adds `sandbox_tenant_keys→tenants ON DELETE CASCADE` (orphan-clearing DELETE first, validated against pre-existing orphans on a real Postgres); new `sandbox.WorkspacePurger` (lookup-only key → list+terminate) and `billing.WorkspacePurger`/`CancelContract` (immediate cancel w/ `invoice_now`, keeps the Customer); delete verb stays GraphQL-only (Render-consistent), contract unchanged; cross-cutting retry + phase-ordering tests + real-Postgres FK-cascade test; 40 backend packages + lint green; ADR040 §2 records the cancel-not-delete decision + accepted seal-window loss; pre-m61 prod Stripe-orphan sweep filed as `w1/044`; moved to `done/m61/`. **Uncommitted pending `/ship`.**
- [x] **m60** — Billing-role authz: make `can_manage_billing` real (8 tasks) ← promoted from `w7/014` (the w7/m61 role-ladder sweep's finding: `can_manage_billing` is modelled but no Go verb checks it — billing-role members 403 on billing management, diverging from Render's BILLING role) via `/pm-brainstorm more work for w1` 2026-07-31, user picked Option 1 (align to Render); w7 is drained, m57-precedent cross-workstream adoption — **done 2026-07-31**: `core.RelCanManageBilling` added + `billing.authorize()` flipped off admin-only `RelCanManage`, so the billing role manages billing on every surface while staying denied elsewhere; proven against real OpenFGA (`TestMultiWorkspaceTargetingE2E` §9) + unit/audit pins; ADR012/024/040/018 + dated comparison updated; `w7/014` closed; backend+dashboard suites/lint green; moved to `done/m60/`
- [x] **m59** — deploy-run supersession hygiene: neutral conclusion + pre-build staleness skip (6 tasks) ← from the 2026-07-30 w3/m35 ship (superseded deploy run `2fc94bd7` failed noisily at the write-back guard after building + signing three throwaway images); w1/m28 CI-signal lineage — done 2026-07-30, moved to `done/m59/`
- [x] **m58** — bex-api multi-replica correctness: durable shared state for replica-local buckets (8 tasks) ← from `.pm/FUTURE-MAYBE.md` "Durable/shared state for bex-api's replica-local in-memory buckets" (w1 brainstorm round 12, 2026-07-15); its trigger ("bex-api scales past one replica") fired when w1/m52 rolled bex-api to two replicas — the latent double-delivery / double-failure-email bug — done 2026-07-30, moved to `done/m58/`
- [x] **m57** — Codify static-site's manual prod fixes into gitops (7 tasks) ← promoted from `w9/014` (the w9/m44 prod-sweep residual: three hand-applied `kubectl` fixes — `bex-registry-pull` re-mint, static-server CRB, static-server Deployment drift — that regress on the next redeploy) via `/pm-brainstorm more work for w1` 2026-07-30; w9 is drained, platform infra is w1's lane — done 2026-07-30, moved to `done/m57/`
- [x] **m55** — Retire deprecated product compatibility surfaces (12 tasks) ← from user-requested deprecated-code audit, 2026-07-27 — done 2026-07-27, moved to `done/m55/`
- [x] **m56** — Exit deprecated platform versions and fleet migration scaffolding (15 tasks) ← from the same audit; operational companion to m55 — done 2026-07-28, moved to `done/m56/`
- [x] **m54** — Branded email module: dashboard-token codegen + HTML/text layout for all bex-sent mail (11 tasks) ← user request 2026-07-20 ("dedicated email template module… dashboard-consistent styles", "share code from dashboard", "do the right thing at once"); implementation started same session, uncommitted (codegen script + generated `brand_gen.go` + `layout.html.tmpl` in the working tree) — brand tokens generated from `dashboard/src/style.css` (oklch→hex, drift CI-gated by the dashboard suite), `internal/email` Message → text+HTML, multipart mailer with QP HTML + header hardening, all three senders rewired (members invites byte-parity, notifications deploy emails byte-parity, webhooks notices), dev-1 Mailpit visual verification
- [x] **m53** — Security-review remediation: close the 2026-07-19 audit findings (11 tasks) ← from the 2026-07-19 security review (six focused audits extending ADR028, covering the surface added since — SSH/web-shell, static sites, control plane, GitHub app): **M3** internal control-plane API (`:8091`) fails open / unauthenticated in prod (`bearer()` no-op on empty `BEX_CP_TOKEN`, set nowhere) · **M1** tenant-triggerable blind SSRF via the image-signature admission webhook (registry prefix match missing the `/` boundary + `httpclient.Shared` lacks `netutil.SafeDialContext` + unvalidated `Image`) · **M2** authz "every verb guarded" sweep omits 5 of 22 wired services (keyvalue/usage/events/notifications/webhooks; `keyvalue.KeyValueConnectionInfo` reveals the Valkey password) · **V1** verify Kratos enforces email verification for login-time invite redemption (else workspace/admin takeover) · **10 LOW** defense-in-depth items (fail-open authz mode, unbound-key delete, label-gated tenant isolation + metadata egress, opt-in admission, Valkey pod hardening, plaintext invite tokens, per-replica ticket nonce, ticket-in-URL, projects adapter, deploy-hook `imgURL`)
- [x] **m52** — Zero-error deploy rolls: bex-api drain + dashboard roll resilience (8 tasks) ← from the 2026-07-18 06:54 UTC prod roll collision (the user's move-to-project clicks and the `/project/prj-…` page failed while deploy run `2c9a7143` replaced both bex-api pods — all three `projects.SetServices` calls were in fact allowed+committed; audit-log + k8s-event evidence in the m52 README) + folds inbox `036` (post-roll dashboard SSR error page, filed at the m51 closeout): bex-api in-process drain window (`/readyz` + SIGTERM-then-serve, completing m30's arc) · rollout strategy/minReadySeconds · dashboard readiness gated on upstream reachability + stale error `<title>` fix · Apollo transient-read retry + hydration re-run of SSR-errored loaders · two clean controlled rolls under load as the DoD — **Done 2026-07-18**: shipped as `3cb335cf` and DoD verified on prod the same day (deploy roll + manual roll #2 of both Deployments under load: api 3523 req / dashboard 1454 req, zero non-2xx, zero error pages; live browser self-recovery incl. the stale `<title>` fix); closeout surfaced + fixed two latent prod bugs — the baked `VITE_SSR_API_URL` port never existed on the bex-api Service and `deny-tenant-ingress` never admitted the dashboard namespace, so prod SSR data fetches had been silently connection-refused all along (the deeper root of `036`) — Service `http-alt:8090` + scoped `allow-dashboard-ssr-to-bex-api` NetworkPolicy live on prod, **the two git files ride the next `/ship`**; operator CI flake that cost one deploy cycle filed as `038`; moved to `done/m52/`
- [x] **m51** — Egress correctness: count HTTPS bytes + record billable hours (8 tasks) ← promotes `034` + `035` (both filed 2026-07-18 from m50/t006's prod sanity check): the router resolver misses Traefik's `websecure-` TLS sibling so every bandwidth/billing consumer counts only 80→443 redirect bytes, and the usage rollup's all-or-nothing hour gate defers unhealthy hours forever so prod has zero `egress_bytes` rows — fix the one shared resolver + per-source hour gating (record healthy sources, defer only on transport) — **Done 2026-07-18**: resolver emits the `websecure-` TLS sibling (ground-truthed on Traefik v3.7.5; one fix point covers chart/month-to-date/health/billing) + per-source hour gating in the rollup (healthy sources record, unhealthy skip, transport defers); prod-verified — 1h read within 0.4% of a trusted Prometheus increase (~200× correction vs redirect-only), rollup cursor unfroze and wrote 44 non-zero hours (69.75 GB) for the reference service, history byte-identical; SSR-flake follow-up filed as `036`; moved to `done/m51/`
- [x] **m50** — Outbound Bandwidth: un-blank the panel (observability vs billing health gates) (10 tasks) ← user report 2026-07-18 ("Outbound Bandwidth No data in range" on the prod metrics page) + same-day live prod root-cause: `egressquery.Health()`'s window-wide all-or-nothing product (up-continuity × scrape density × zero resets × process-start × counter-loss) ERRORS the whole `Metrics(BANDWIDTH)`/`MonthToDateBandwidth` read when any term fails anywhere in the window — and on prod something always fails (egress-meter DS at template generation 67 in 45h rolls with every image pin + tenant-node churn left `up` at 26/2304 samples over 12h; Traefik router counters reset 32× in 12h from deploy Ingress reloads), so the panel is structurally blank; the dashboard then masks the GraphQL error as the empty-series "No data in range". Fix: split observability reads (best-effort + per-source health as data + honest degraded UI state) from the untouched billing-strict usage rollup, plus DS roll/scheduling mitigation — **Done 2026-07-18**: best-effort interactive reads (degraded-as-data via the `degraded_sources` label + `degradedSources` field), dashboard three-state panel, billing gate untouched (July usage rows byte-identical pre/post-deploy); prod-verified on `119800de` (12h/7d series render with "Partial data" degradation, month-to-date 83.1 MB as data); verification surfaced pre-existing `w1/034` (prod egress rollup records nothing) + `w1/035` (websecure router sibling uncounted — HTTPS bytes missing everywhere incl. billing); moved to `done/m50/`
- [x] **m49** — Webhook pages: `/webhooks/new` + `/webhook/$id` for Render UX parity (11 tasks) ← user request 2026-07-17 ("learn from dashboard.render.com/webhooks/new, use /webhooks/new instead of modal for feature parity") + same-day live walk of both dashboards and a backend audit: bex's create modal was a flat 17-raw-key list with a **live blocking bug** (the events list overflowed the dialog and hit-test-intercepted the Create button — `create-webhook-dialog.tsx:149` put `max-h-56` on the ScrollArea root, which never constrains the Radix viewport), `/webhooks/new` 404'd, there was no per-webhook page, and no edit UI even though `PATCH /v1/webhooks/{id}` / GraphQL `updateWebhookEndpoint` / MCP `update_webhook_endpoint` all shipped in w3/m11 — **Done 2026-07-17**: page-based create with the grouped/searchable/tri-state event picker (served vocabulary, catalog-side labels+groups with an "Other" fallback), `/webhook/$id` detail (header parity incl. `createdBy`, Activity deliveries with All/Successful/Failed filters, Settings edit finally wiring the w3/m11 update verb, Render's `delete webhook <name>` type-to-confirm), mint-once secret kept by user decision, the modal (and its bug) deleted plus a ScrollArea `viewportClassName` primitive fix + source-sweep regression test (`resource-checklist.tsx` had the same latent misuse); whole DoD walked live on dev-1 incl. a real `deploy_started` delivery row and a real click at the old bug's 800px viewport; 1400 dashboard tests + typecheck + lint green; `docs/render-artifacts/webhooks-ui.md` + ADR018 updated; simplify residuals `w1/033`; moved to `done/m49/`
- [x] **m48** — Deploy progress lines: a never-silent deploy log feed (8 tasks) ← from the 2026-07-17 deploy-page build-log incident (dead-tail/ingest-race half shipped as `52882f22`; evidence + Render recheck in `docs/render-artifacts/live-deploy-following.md` § 2026-07-17) — **Done 2026-07-17**: `internal/logs/progress.go` synthesizes the `==>` narration from deploy rows into explicit `type=build` reads (one core verb ⇒ REST/GraphQL/MCP identical) + the SSE tail (subscribe catch-up, wait-loop transitions, post-stream terminal line); live-proven on dev-1 incl. the deploy page mid-build and the unmodified official Render CLI; Loki-backed prod check filed as `032`; moved to `done/m48/`
- [x] **m47** — Events/Deploys consolidation for Render parity (9 tasks) ← user request 2026-07-16 (merge dashboard Events + Deploys pages into unified timeline; verify MCP has events tool; achieve REST/GraphQL/MCP surface consistency) — **Done 2026-07-16**: backend GraphQL enrichment (image, commit, timing), dashboard Events consolidation with deploy details display, Deploys tab removed, all surfaces consistent, tests green; moved to `done/m47/`
- [x] **m1** — Reliability: fix config drift + back up etcd (4 tasks) ← from `009`, `007` — done 2026-07-05, moved to `done/m1/`
- [x] **m2** — Control plane: Postgres source of truth in `lego/backend` (7 tasks; DONE 2026-07-09 — committed `aebbd43`, prod `BEX_CP_DB_URI` on since m9, live acceptance via `scripts/auth-tenant-e2e.sh` in m9/t004) ← from `005`, moved to `done/m2/`
- [x] **m2.5** — Refactor bex-api into feature packages (one package per feature) (9 tasks) ← from `/pm` architecture review 2026-07-06 — done (shipped `06f247e` 2026-07-06, verified + board synced 2026-07-08), in `done/m2.5/`
- [x] **m3** — Elastic substrate: bin-pack + autoscale (8 tasks) ← from `002`, `004` (retrofitted to current `/pm` canon 2026-07-08) — done 2026-07-11, moved to `done/m3/` (t008 unblocked by m19: 014 repaired, scheduler --config live, CI green; elastic behaviors proven on prod)
- [x] **m4** — Free tier = sleep: scale-to-zero + wake activator (5 tasks) ← from `003` — done 2026-07-08, moved to `done/m4/`
- [x] **m4.5** — Sleep in the dashboard: hibernated state, idle-timeout setting, wake UX (6 tasks; DONE 2026-07-09 — Sleeping badge + idle-timeout control, verified live) ← user request 2026-07-08, UI half of m4, moved to `done/m4.5/`
- [x] **m5** — Build & deploy from git, in-cluster (5 tasks; DONE 2026-07-09 — in-cluster BuildKit builds, verified live, simplified + tested; unblocks w2/m2 t004) ← from `008`, moved to `done/m5/`
- [x] **m7** — Prod hardening: network · secrets · images (6 tasks) ← from `010` (+ OpenBao backup, added 2026-07-08) — done 2026-07-08, moved to `done/m7/`
- [x] **m8** — Instance tiers: one catalog, Render-shaped plan API, limits everywhere (6 tasks) ← from architecture discussion 2026-07-08 (metrics page's "No limit configured" gap; Render compute-plans ladder) — done 2026-07-08, moved to `done/m8/`
- [x] **m9** — Tenant onboarding: real workspaces + OpenFGA enforced in prod (6 tasks; DONE 2026-07-09 — mint-on-first-login, key→tenant binding, workspace-scoped authz, prod OpenFGA flip, simplified + tested) ← from `/pm-brainstorm for w1` 2026-07-08 (m2 deferrals; prod authz is allow-all), moved to `done/m9/`
- [x] **m10** — OpenBao prod wiring: env-vars live in prod (6 tasks) ← from `/pm-brainstorm for w1` 2026-07-08 (docs/ADR013-secrets.md "Prod deploy path") — done 2026-07-08 (implementation shipped + locally validated), moved to `done/m10/`; prod activation (first init + live PUT) is the operator's runbook there
- [x] **m11** — Render custom-domains API over `App.spec.hosts[]` (5 tasks) ← promoted from `003` 2026-07-08
- [x] **m11.5** — Custom-domains dashboard section, UI half of m11 (8 tasks) ← promoted from `w5/003` 2026-07-09 — done 2026-07-09, moved to `done/m11.5/`
- [x] **m13** — Render parity audit: REST · GraphQL · MCP · UI matrix (6 tasks) ← user request 2026-07-08 — done 2026-07-08, moved to `done/m13/` (shipped `docs/ADR018-render-parity.md`; 5 gap notes filed: `008`–`011`, `w3/002`)
- [x] **m14** — Key Value (Valkey/Redis) managed store: `KeyValue` CRD + reconciler (6 tasks) ← promoted from `007` 2026-07-08 (mechanism; REST/GraphQL/MCP surfaces done in w2/m7, UI in w5/m12) — done 2026-07-09, moved to `done/m14/`
- [x] **m15** — Additional service types: background worker + cron job (8 tasks) ← promoted from `009` 2026-07-08 (static site split to `012`) — done 2026-07-09, moved to `done/m15/` (had been staged under `w7/m15/`)
- [x] **m16** — Config surfaces beyond env vars: environment groups + secret files (7 tasks) ← promoted from `010` 2026-07-08
- [x] **m17** — Managed Postgres advanced: data-protection + lifecycle + access (8 tasks) ← promoted from `011` 2026-07-08 (HA/replicas split to `013`) — done 2026-07-09, moved to `done/m17/` (HA/replicas remain in note `013`)
- [x] **m18** — Root Directory: CRD + build engine + webhook path-filter + API surface (10 tasks) ← from `/pm-brainstorm for w1` 2026-07-09 (Root Directory topic, monorepo support); UI half is `w5/m13` — done 2026-07-10, moved to `done/m18/`
- [x] **m19** — Rearchitecture: rebuild the Hetzner substrate right (12 tasks) ← from docs/rearchitecture.md 2026-07-10 (absorbed into [docs/ADR002-architecture.md](../../docs/ADR002-architecture.md) §The production substrate, 2026-07-11; original in git history), promoted from `014` (CAPH-owned network · 3×CP tainted · platform/tenant pools · self-managed pivot · port firewall + WireGuard) — **Done 2026-07-15**: 3/3 KCP, 3 fixed platform nodes, OpenBao 3/3, full substrate + elasticity proofs, and two consecutive green app-cluster runs; moved to `done/m19/`
- [x] **m19.1** — 5-server interim: rescue prod + early pivot (9 tasks) ← from m19 t006's Hetzner-quota blocker + user decisions 2026-07-10/11 (pivot pulled forward, `bex-infra` destroyed to free the slot for the first tenant node; tenant pool floor becomes min 1; m19 resumes with a two-number revert after the quota raise) — done 2026-07-11, moved to `done/m19.1/`; prod serving, self-managed, verify-substrate green
- [x] **m20** — Per-service autoscaling (Render `PUT …/autoscaling`) (8 tasks) ← promoted from `008` 2026-07-11 (unblocked by m3 + m19) — done 2026-07-11, moved to `done/m20/`
- [x] **m21** — Static sites (Render `static_site` type) (8 tasks) ← promoted from `012` 2026-07-11 (unlocks the CDN edge rules DO_NOT_DO parked until 012 lands); done 2026-07-12 (moved to `done/m21`)
- [x] **m22** — Managed Postgres HA (Render `enableHighAvailability` + failover + read replicas) (9 tasks) ← promoted from `013` 2026-07-11 (unblocked by m17 + m19) — done 2026-07-12, moved to `done/m22/`
- [x] **m23** — Misc: small parity + hardening/dev-infra chores (7 tasks) ← groups `005`, `006`, `015`, `016` 2026-07-11 (each sub-hour) — done 2026-07-12, moved to `done/m23/` (healthCheckPath→ReadinessProbe wired + envtested; Dependabot triaged + safe batch, residuals in `018.md`; mock workers labeled `bex.co/pool=platform` via CAPD template; stale single-node/data-loss comments + dead `10.0.0.0/16` swept)
- [x] **m24** — Multi-service `bex.yml`: Blueprint-shaped stack deploys (9 tasks) ← from `/pm-brainstorm more` 2026-07-12 (revives the 2026-07-09 proposal; DO_NOT_DO routes `fromDatabase` spec work to w1; all ingredients — types m15, env groups m16, Postgres m17, KV m14 — now shipped) — done 2026-07-13, moved to `done/m24/`
- [x] **m25** — Managed Postgres observability: processes · top-queries · sizes · table-scans · parameter-overrides (10 tasks) ← from `/pm-brainstorm more` 2026-07-12 (extends w1/m17, last open Postgres parity row) — done 2026-07-12, moved to `w2/done/m25/`
- [x] **m26** — Harden the build-image pull path (Zot node access, retention, drift guards) (10 tasks) ← promoted from `017` 2026-07-12 (found live during a routine backfill: no git-built image was reliably pullable on prod; autoscaler-minted nodes fail today without this) — done 2026-07-13, moved to `done/m26/` (t007 live-verified PASS on prod: a fresh autoscaled tenant node pulled a git-built image first-try, zero manual config)
- [x] **m27** — ~~Close the control-plane disaster-recovery gaps~~ **stale entry, fixed 2026-07-13**: this scope shipped as `w2/m27` (done 2026-07-12, `docs/ADR031-platform-data-backup.md`) — a concurrent `/pm-brainstorm` session materialized the identical proposal under `w2` before this README line was ever promoted to a real `w1/m27/` directory. No `w1/m27/` directory ever existed; nothing to move. See `w2/README.md`'s `m27` entry for the actual work.
- [x] **m28** — Gate deploys on real CI test runs (10 tasks) ← from a CI-workflow audit during `/pm-brainstorm more milestones to work on` 2026-07-13 (no `.github/workflows/*.yml` anywhere runs `go test`/`make test`/`yarn test`; `deploy.yml` builds+pushes+deploys on every push to `main` with zero test gate); not Render parity — pursued on reliability merits per user decision 2026-07-13 — done 2026-07-13, moved to `done/m28/`
- [x] **m29** — Managed Postgres external connectivity: SNI proxy for preamble-mode TLS clients (8 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13 (`docs/ADR009-postgresql-management.md:53` names the fix, never built) — done 2026-07-13, moved to `done/m29/`
- [x] **m30** — SIGTERM shutdown fix + Dependabot residual watch (6 tasks) ← groups `018`, `019` 2026-07-13 (each sub-hour), same pattern as `m23`/`w6/m15`/`w7/m10` — done 2026-07-13, moved to `done/m30/` (SIGTERM graceful-shutdown helper for both servers + regression test; 5/6 Dependabot residuals closed, `@tanstack/start-server-core` deferred; incidentally fixed a pre-existing m31 test regression that was reddening the dashboard suite)
- [x] **m31** — Projects: group services within a workspace (6 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13 (verified live via search that Render's Projects feature is real — render.com/docs/projects; scoped to grouping only, Environments deliberately deferred as a much larger follow-on)
- [x] **m32** — Environments: named subsets of a Project's services (11 tasks) ← follow-on to m31, closing the Environments half it deferred; rebuilt after an independent same-feature implementation on `w6/m16` collided with this milestone on `/ship` and was discarded in favor of composing with what had already shipped here — done 2026-07-13 (`internal/environments` layered on `internal/projects`; REST/GraphQL/MCP full CRUD; assignment auto-joins the project; live-verified against the CAPD mock cluster, `scripts/environments-verify.sh`; also fixed a 500-vs-503 gap `ErrEnvironmentsUnavailable` would have inherited from `projects.ErrProjectsUnavailable`'s equivalent unfixed gap), moved to `done/m32/`
- [x] **m33** — Pre-Deploy Command: gate rollout on a setup/migration step (9 tasks) ← from `/pm-brainstorm more` 2026-07-13 (`docs/ADR006-bex-api.md:124` lists `preDeployCommand` as an "ignored" bex.yml field — Render's Deploy section confirmed live via `w5/done/m13`; no overlap with `w6/m21`'s `dockerfilePath`/`startCommand` scope)
- [x] **m34** — Build filters: `buildFilter.paths` + `ignoredPaths` (9 tasks) ← from `/pm-brainstorm more milestones for each worker` 2026-07-14 (`docs/ADR018-render-parity.md` "buildFilter is not editable — ◐" row understates it: zero hits in `lego/`, the field is entirely unbuilt); generalizes w1/m18's `rootDirMatches` webhook path filter — done 2026-07-14, moved to `done/m34/`
- [x] **m35** — Blueprint field completeness: envVarGroups · fromGroup · sync:false · fromService (10 tasks) ← from `/pm-brainstorm more milestones for each worker` 2026-07-14 (ADR018 Blueprint row's named-error rejections; every ingredient shipped — m16 env groups, m24 stacks, w2/m15 verbs); `generateValue` acceptance rides `w8/m10`'s core verb — done 2026-07-14, moved to `done/m35/` (also landed `w8/m10/t001`, the shared `generateValue` core verb)
- [x] **m36** — Node bring-up efficiency: baked snapshot image + trimmed provisioning (8 tasks) ← promotes `.pm/FUTURE-MAYBE.md`'s node-bring-up entry 2026-07-14 — its trigger ("app images move into a pullable registry — zot wired end-to-end") fired when `w1/m26` closed 2026-07-13; the roll-safety condition that deferred it is gone for the same reason. **Done 2026-07-15**: baked `bex-worker` snapshot + tenant pool rolled live (54 s vs ~101–218 s, github/pkgs-independent); platform-pool roll spun out to `w1/022` (blocked on CNPG HA)
- [x] **m37** — Maintenance mode: Render-compatible public traffic interstitial (10 tasks) ← from `/pm-brainstorm` round 6, 2026-07-14; corrected against Render's live docs/OpenAPI/dashboard/event contract 2026-07-15 (paid web services; default/custom page; REST + Blueprint + UI + audit/webhook parity; NOT the ADR018 `/maintenance` managed-infra non-goal) — **done 2026-07-15**, executed and closed under w9's directory for capacity (`.pm/w9/done/m37/`; see `w9/README.md`)
- [x] **m38** — Platform-pool drainability: CNPG HA + staged baked-image roll (7 tasks) ← promotes `022` (renumbered `done/023.md` — number collision with the done error-body note) 2026-07-15; done 2026-07-15, moved to `done/m38/` (four HA CNPG clusters, rehearsed OpenBao unseal, three baked platform nodes, production request-path HA, sequential live drains)
- [x] **m39** — Dependency security: clear the alert spike (7 tasks) ← from `/pm-brainstorm` round 10, 2026-07-15 (Dependabot 4 → 18 alerts initially, then 31 as duplicate `x/crypto` advisories attached to the operator manifest; the m23/m30 triage pattern at milestone size) — **Done 2026-07-15**: all 31 alerts fixed and live Dependabot count verified at zero; moved to `done/m39/`
- [x] **m40** — Wake interstitial: the "sleeping, click to wake" page (7 tasks) ← from `/pm-brainstorm` round 11, 2026-07-15 — **Done 2026-07-15**: captured Render's browser/API wake split, shipped the content-negotiated polling interstitial with the bounded JSON contract preserved, and verified both wake paths plus the stable-origin transition on the isolated kind cluster; moved to `done/m40/`
- [x] **m41** — Stable Traefik LB origin IP across rebuilds (7 tasks) ← from `.pm/FUTURE-MAYBE.md` (trigger read as fired by `w7/m29`'s executed DR drills; user confirmed 2026-07-15, `/pm-brainstorm` round 12) — **Done 2026-07-15**: Terraform adopted and API-protected the existing production LB in place; two scratch clusters and a live Traefik Service UID rotation reattached the same ID/IP; moved to `done/m41/`
- [x] **m42** — Projects list: real cursor pagination (7 tasks) ← from `/pm-brainstorm` round 13, 2026-07-15 — **Done 2026-07-15**: stable default-20 REST pages, explicit GraphQL/MCP pages with compatibility defaults preserved, 41-record no-duplicate walk coverage, and an unmodified CLI v2.21.0 proof over 101 populated projects; moved to `done/m42/`
- [x] **m43** — Restore the `gitops (render)` CI guarantee (6 tasks) ← promoted from stale `020` after the implementation had already landed — **Done 2026-07-15**: `67da0499` migrated Traefik 41.0.0 values and synced the alert fixture; the pinned-promtool local guard and main Actions run 29402875626 pass; moved to `done/m43/`
- [x] **m46** — Service URLs speak `srv-` ids: close the GraphQL name-as-id deviation (8 tasks) ← user request 2026-07-16 straight after m45's route-parity walk (`/services/node-hello-world/metrics` vs Render's `/web/srv-…/deploys`): ADR020's last service name-as-id surface — GraphQL `Service.id` emits the App name (`apps/graphql.go:256`) while `AppView.ID` already carries the minted srv- id and `AuthorizeApp` already resolves srv- first with name fallback, so the flip is emit-side + a dashboard sweep + fallback pinning; the sibling `Domain.id` deviation is filed as `028`, not absorbed — **Done 2026-07-16**: one-resolver flip (`Service.id` → `AppView.ID`) with ZERO verb-layer changes (the `AuthorizeApp` LabelAppID-first seam already accepted both shapes); dashboard URLs became `/services/srv-…` automatically, the shell swaps param-templated titles for the resolved name, name URLs + legacy label-less CRs keep resolving (test-pinned, incl. a REST=GraphQL=core id-equality pin); walked live on dev-1 incl. Render's exact `/web/srv-…/metrics` shape; ADR020's deviation entry closed, `Domain.id` sibling filed as `028`; incidentally resolved the `0040` migration-number collision with the concurrent deploys milestone (mine renumbered to idempotent `0041`; dev-1 repaired 40→39→41) that had main's backend CI red — moved to `done/m46/`
- [x] **m45** — Dashboard route parity: workspace/user-scoped URLs, create-flow aliases + sidebar navigation (12 tasks) ← from live authenticated route probes 2026-07-16 (user-directed route-parity research, straight after m44's Team placement move raised the `/w/{tea-id}/settings` question): Render canonicalizes `/w/settings` → `/w/{tea-id}/settings` and `/settings` → `/u/{usr-id}/settings`, its New menu mints `/new/database`/`/new/redis`/`/new/project` plus `/{seg}/new`, and bex's existing `d.$` alias sends Render's own `/d/new` to a nonexistent `/databases/new` (404 today); w5/m39 covered resource deep links only. Adds `w.$`/`u.$`/`billing.$` aliases with URL-driven workspace-context switching (membership-refused for foreign ids), create-flow landings over the existing dialogs, first-class `/webhooks` + `/notifications` pages, `/login`/`/register` redirects, and the artifact capture; `/observability`, private-links/dedicated-ips, `/invites`, `/documents`, billing-proper, and workflows stay non-goals — **Done 2026-07-16**: `w.$`/`u.$`/`billing.$`/`login`/`register`/`new.*` aliases shipped with URL-driven workspace selection (foreign tea-ids refused via the 404 page, never a silent fallback), `/d/new` fixed via explicit per-segment create landings, `?new=database|project` URL-owned create dialogs on the overview, `/webhooks` + `/notifications` first-class pages, the grouped global sidebar (Integrations/Workspace; Networking omitted — non-goals), and the resource-scoped service sidebar replacing the tab strip (Monitor/Manage groups, `SERVICE_NAV_GROUPS` as the single item source); `requireAuth` now defaults `next` to the requested href so query params survive the SSR login bounce; whole DoD paste-walked live on dev-1 incl. a real image-backed service; 1279 dashboard tests + typecheck + lint green; simplify residuals filed as `027.md`; ADR018 + dashboard-routes.md updated — moved to `done/m45/`
- [x] **m44** — Team Members parity completion: seats, resend, token accept, security metadata, placement, audit (11 tasks) ← created as `w4/m33` from the Team Members investigation 2026-07-16 (live Render workspace-settings side-by-side on dev-1 + full members code map), moved into w1 as `m33` by user action the same day (`/goal implement .pm/w1/m33/README.md`), renumbered **m44** at closeout (number collided with done `w1/done/m33`, the pre-deploy-command milestone; code comments reference `w1/m33`, its number while open) — **Done 2026-07-16**: seat usage (`used`/`limit`, the invite cap's own formula) readable on REST/GraphQL/MCP and shown as "X of Y seats" with the invite entry disabled when full; `ResendInvite` verb (fresh mail, refreshed expiry, stable id+token) on all three surfaces + dashboard action; the emailed `?invite=<token>` link now actually redeems (`AcceptInvite` + `store.AcceptInviteByToken` sharing the login path's redemption core), joining cross-email with named already-accepted/expired/plan refusals, wired through the dashboard's auth-page stash → authenticated-layout redemption; `mfaEnabled` on MemberView from the owners API's existing Kratos credential inspection + a 2FA member-row badge; the Team panel moved to `/workspace/settings` (Render's placement, superseding w5/m36's IA-choice note); every member mutation audited with `member:`/`invite:` targets and typed `roleFrom`/`roleTo` (migration 0040, new `core.Base.AuthorizeOnTarget` seam, deferred success rows for Invite/ChangeRole, both acceptance paths recording the accepting identity). Whole DoD verified live on dev-1 incl. a real TOTP enrollment and second-factor login; real-Postgres integration tests green; `/simplify` pass applied (shared seat formula, single target parse, folded accept-event recording, one-request members+seats query, common-lib stash helper, unified test fake) with four residuals filed as `w1/026`; ADR018 members row, ADR024, and the dashboard-walk placement notes updated — moved to `done/m44/`
- [x] **m76** — Refactor-review hazard fixes (11 tasks) ← from the 2026-08-19 architectural refactor review §1, moved into w1 by user action 2026-08-19 (nine small correctness/observability gaps: keyvalue reveal gate + eager poll, modelproxy redirect guard, gateway shed metrics + session-gauge semantics, kv-sni-proxy health gauge, operator doc comments) — done 2026-08-19 (all nine gaps closed with regression tests: keyvalue reveal gate + eager poll mirroring the databases twins, modelproxy no-follow guard proven red pre-fix, per-scope listener shed metrics, session gauge now tracks all four shared-limiter holders with sandboxsse single-counted, kv-sni-proxy health gauge recovers on delete, reconcileStaticSite doc reattached; parity verified — no wire surface touched), moved to done/m76/
- [x] **m77** — Shared PROXY-protocol parser in lego/types (6 tasks) ← from the 2026-08-19 architectural refactor review §2.1, moved into w1 by user action 2026-08-19 (two byte-identical, never-synced copies of the security-critical parser across backend/operator; the "no shared module" justification is stale since `lego/types/netutil`) — done 2026-08-19 (parser moved verbatim to lego/types/proxyproto with the merged union suite — backend 8/8 + operator 5/5 cases accounted + 17 new — assignment re-exports with per-module delegation guards that fail on a re-fork; −671/+70 in the wrappers; all four modules test+lint green), moved to done/m77/
- [x] **m78** — Single service-PATCH op table across REST and MCP (7 tasks) ← from the 2026-08-19 architectural refactor review §2.2, moved into w1 by user action 2026-08-19 (the twice-maintained ordered op table behind `PATCH /v1/services/{id}` / `update_service` has drifted in four fields despite three comments asserting identity) — done 2026-08-19 (one 21-row table in apps/settings.go on core.PatchOps; REST/MCP reduced to fill-and-delegate; the four divergences preserved as explicit commented fields — resolution decisions filed as w1/073; order pin + reflection completeness guard + cross-surface equivalence test all mutation-verified; patch-pipeline/update-service-MCP/CLI-compat/authz-sweep suites pass unmodified), moved to done/m78/
- [x] **m79** — Dashboard GraphQL codegen convergence (6 tasks) ← from the 2026-08-19 architectural refactor review §2.3, moved into w1 by user action 2026-08-19 (the hand-written operations.ts shadow layer is stale — all 212 ops now in `graphql/definitions.ts` — and its types omit the `__typename` codegen emits non-optional; plus two prod deps resolving only as devDependency transitives) — done 2026-08-19 (five shadow files deleted — 1,221 lines, net +390/−1,337 — ~30 importers repointed with honest __typename/nullability fallout incl. new blueprints view normalizers; graphql-tag eliminated, typed-document-node declared; 654-file no-hand-written-documents guard verified red on a plant; 2,351 tests + typecheck + build green), moved to done/m79/
- [x] **m80** — Operator dedup pass (7 tasks) ← from the 2026-08-19 architectural refactor review §2.6 + §3, moved into w1 by user action 2026-08-19 (the host-bound git credential helper is cloned with its SECURITY comment on only one copy; build/publish/predeploy share an unfactored Job state machine; two smaller literal clones + narrow db/kv carve-outs) — done 2026-08-19 (GitHubCredentialHelper constant with SECURITY comment + CI guard and clone-script byte-identity proven by direct diff; EnsureOwnedJob shared across build/publish/predeploy — get-first after the simplify pass, zero-Creates steady state tested; deleteStaleChildren/podsReady/growOnlyIntent/secretClient/setNotReadyCondition consolidations; ptr → k8s.io/utils ptr.To; make test + lint green, no golden-manifest churn), moved to done/m80/

## Suggested execution order (2026-07-08 refinement, superseded 2026-07-09)

> All four items below are now done (m9, m5, m4.5 moved to `done/`; m3 remains open — see the Milestones list above for current status). Kept for provenance.

1. **m9** — tenant onboarding + enforced OpenFGA (m2/t007 marked done; prod authz is still allow-all — the standing security gap).
2. **m5** — build-from-git (t001/t002 are the last missing piece of the flow; the webhook half already shipped via w2).
3. **m3** — elastic substrate (replica-semantics contract settled by w2/m12; scale-down pays off most now that m4's sleep empties nodes).
4. **m4.5** — dashboard sleep UX (pairs with shipped m4; UI-half work).

> **Parity-backlog tier (promoted from the m13 audit, 2026-07-08):** **m14** Key Value · **m15** worker + cron service types · **m16** env-groups + secret-files · **m17** advanced Postgres. Sequence these after the V0 items above, ordered by the gap ranking in `docs/ADR018-render-parity.md`. **008** (per-service autoscaling) stays an inbox note until **m3** lands the node elasticity its replica scale-ups need (the metric→replica reconciler itself is new work in 008; m3 moves nodes, never `spec.replicas`).

> **m13 (parity audit) done 2026-07-08** — its output, `docs/ADR018-render-parity.md`, orders the remaining parity queue with evidence. Its gap notes were arranged into milestones on 2026-07-08: `007`→**m14**, `009`→**m15**, `010`→**m16**, `011`→**m17** (with `012` static-site + `013` Postgres-HA split off as notes); `008` (autoscaling) + `w3/002` (request logs) stay notes; the rest cross-reference existing owners (w2/m4 delete, w2/m5 deploys, w4/m8 keys-UI, w4/m12 members, w5/004 scaling-UI, w5/006 DNS-UI).

> **Board refinement (2026-07-08, from `/pm` + `/pm-brainstorm` conventions):** m3 and m5 retrofitted to the current canon — `## Source + Goal linkage` sections, the two standing closing tasks (Simplify, Test coverage), and pre-`lego/` paths fixed; m3/t001 (done) moved to `m3/done/`. m5/t003 reduced to verify-and-close (its webhook shipped in bex-api via w2's deploy-from-chat — where this refinement had independently concluded it belongs; operator stays mechanism-only). Planned m4/m7 retrofits and an m2/t007 rewrite were dropped — those milestones completed upstream first. No milestone was removed — all open ones map to a V0 roadmap item or Render parity and pass the DO_NOT_DO screen.

## Inbox

- `044` — one-off: cancel Stripe subscriptions orphaned by pre-m61 workspace deletes (conditional, operator-run; blocked until m61 ships to prod)
- `046` — residual Tier-3 findings from the 2026-08-10 scan (supply-chain pass F11/F13/F14/F15 + small residual F17 + scan coverage caveat). F18 re-owned by `w4/034`; the per-subject/per-workspace cap half has already shipped.
- `048` — security-scan round 6 (`codex-security-bex-rYWVXe`, revision `bfe1376c`): 6 new findings **fixed in place** with tests (#11 md links, #5 SSH pre-auth cap, #7 transcript replay memory, #3 webhook no-op/lease/age-out slices, #2 CI ref-guard, #9 client render budget) + pointers to the 5 already-tracked (#1→`w4/m84`, #4/#6→m68, #8/#10→046). Finding #3's deferred per-workspace delivery quota + fair scheduling is now `w4/m83`. Uncommitted pending `/ship`

> **Promoted 2026-08-17:** `045` → `w4/m84`; source note moved to `done/045.md`. Current main already has deterministic app-bound TXT pre-proof, and m84 completes it into a durable non-serving pending lifecycle.

> **Done 2026-07-31:** `043` (reprovision the shared CAPD mock cluster) — recreated, dev-5 verification inventory ALL GREEN, and every hand-applied piece of "walk-ready" codified into `scripts/mock-cluster.sh` / `dev-5/up.sh` / the mock-cluster skill; `w5/028`+`w5/029` walks unblocked locally. Note moved to `done/` with the full resolution.

> **Sync fix 2026-07-30:** `033` and `038` were still listed here as open but both live in `done/` — this section had lagged the moves. `033`'s trigger-gated simplify residuals are tracked in `.pm/FUTURE-MAYBE.md` (micro-abstraction residuals entry); `038` remains a fix-on-recurrence recipe recorded in `done/038.md` (poll the `kv-sni-proxy` meter delta if the flake returns).

> `037` closed 2026-07-18 same day it was filed — the post-ship confirmation ran immediately after `/ship` (evidence in `done/m52/`); note moved to `done/`.

> **Promoted 2026-07-18:** `036` (post-roll dashboard SSR error page) → **m52** (its t002); note moved to `done/`.

> `032` (m48 narration + clone-token remint prod verification) closed 2026-07-18 (`5aea657b`) — this line previously lagged the move to `done/`.

> **Done 2026-07-17:** `029` (deploy-page All/Application/Build type filter) and `030` (BuildKit pre-warm DaemonSet, `deploy/gitops/base/build-image-prewarm.yaml`) — both implemented in the m48 working set; notes moved to `done/` with resolutions. `031` resolved-superseded the same day: CLI-driven debugging showed both halves misdiagnosed — the GitHub App IS configured (secretKeyRef-wired) and the token is a 1h installation token bex must remint per deploy; the real bug (manual Trigger/deploy-hook never reminted, so private-repo manual builds ran on the previous deploy's expired token) is fixed in `internal/deploys` with the `store.CloneSecreter` bridge; verification folded into `032`.

_(`024.md` absorbed and closed by `w10/m4` 2026-07-15 — the prod SA patch was verified gone with the declared operator path load-bearing. `025.md` — Traefik LB `ignore_changes = [labels]` — absorbed into **w10/m6** (t001) 2026-07-15; note moved to `done/`.)_

_(`018`, `019` promoted to `m30` 2026-07-13)_

> `021.md` (`restart` CLI fix — bare-name fallback in `cli/pkg/resource/service.go`) fixed 2026-07-14, moved to `done/`.

> `022.md` (REST error-body shape swallowing every CLI-visible error message) fixed 2026-07-15 (`dfff3034`), re-verified live against the official Render CLI — note moved to `done/`.

> A **second** `022.md` (platform-pool CNPG HA blocker, filed by m36's pre-flight — a number reused in a concurrent-session collision) promoted to **m38** 2026-07-15 and renumbered to `done/023.md`.

> **Promoted 2026-07-13:** `018` (Dependabot residual triage) and `019` (bex-api SIGTERM shutdown bug) → **m30**; both notes moved to `done/`.

> **Promoted 2026-07-12:** `017` (Zot node-pull path: DNS/TLS, NetworkPolicy drift, retention, generation churn, migration-ownership drift) → **m26**; note moved to `done/`.

> **Promoted 2026-07-11 (`/pm group them into milestones`):** `008`→**m20** (per-service autoscaling), `012`→**m21** (static sites), `013`→**m22** (Postgres HA); the four sub-hour notes `005`+`006`+`015`+`016`→**m23** (misc chores milestone — each below milestone size individually, grouped per the sizing rule). All seven notes moved to `done/`. **Promoted 2026-07-08:** `007`→**m14**, `009`→**m15**, `010`→**m16**, `011`→**m17** (notes moved to `done/`); `008` kept as a note (gated on m3). See the m13 note above. **Promoted 2026-07-10:** `014` (prod KCP unmanageable, m7 aftermath) → **m19** via `docs/rearchitecture.md` (since absorbed into ADR002-architecture.md); note moved to `done/`.

> `003` (custom-domains API) promoted to **m11** and `004` (scale API) promoted to **m12** on 2026-07-08; notes moved to `done/`. m12 was subsequently relocated to **w2** (done: `w2/done/m12/`).

> **m6 (Multi-tenant isolation) removed 2026-07-07** — the plan leaned on vcluster-per-tenant, which is the wrong isolation model for bex (see [`.pm/DO_NOT_DO.md`](../DO_NOT_DO.md)). If tenant isolation is re-scoped later, it must be namespace-tier → microVM, not per-tenant virtual control planes.
