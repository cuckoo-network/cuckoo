---
id: w1/m22/t005
title: Dashboard HA UI (HA toggle, failover button, replica connection info)
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m22/t004]
---

## Objective

Surface HA in the dashboard's database page: a high-availability toggle, a failover/promote action, and read-replica connection info alongside the primary — mirroring Render's database HA UI.

## Context

- `dashboard/` — TanStack Start + Apollo + shadcn; read `dashboard/CLAUDE.md` first.
- The m8/m11.5 Databases page already reveals connection info with copy buttons — extend it (do not build a "link DB to service" picker; see `.pm/DO_NOT_DO.md`).
- Consumes the GraphQL surface from t004.

## Steps

1. Add an HA toggle to the database settings (gated to plans that allow it).
2. Add a failover/promote action with a confirmation.
3. Show read-replica connection strings (internal + external) with copy buttons, distinct from the primary.

## Files

- `dashboard/…` (database detail HA section + GraphQL operations)

## Acceptance criteria

- [ ] HA can be toggled, failover triggered, and replica URLs copied from the UI, all via GraphQL.
- [ ] Layout mirrors Render's database HA/connection UI.

## Out of scope

- Backend/mechanism (t001–t004).
