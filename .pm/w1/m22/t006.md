---
id: w1/m22/t006
title: Render parity — verify HA surfaces against Render
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m22/t004, w1/m22/t005]
---

## Objective

Confirm HA is consistent across REST/GraphQL/MCP/UI and matches render.com's Postgres HA behavior/API. Flag drift as follow-up.

## Context

- bex-api is Render-compatible (`docs/bex-api.md`); Render's `enableHighAvailability`, `failover`/`promote`, `replication`/read replicas.
- Update `docs/render-parity.md` (HA rows gap → ✅) and `docs/postgresql-management.md`.

## Steps

1. Diff fields/semantics/error shapes vs Render per surface.
2. Update the parity ledger + Postgres doc with evidence.
3. File follow-ups for intentional divergence.

## Files

- `docs/render-parity.md`, `docs/postgresql-management.md`

## Acceptance criteria

- [ ] All surfaces verified against Render; ledger + doc updated with evidence.

## Out of scope

- New feature work beyond reconciling drift.
