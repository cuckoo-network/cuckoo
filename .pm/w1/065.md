# Adopt core.HandleJSON in the 13 hold-out rest.go fragments

From the 2026-08-19 refactor review (§3 backend). `core.HandleJSON`/`HandleNoBody`/`HandleByID`/`HandleMapped`/`HandleLinks` (`core/http.go:288-375`) exist and are used by projects/secrets/envgroups/notifications/environments — but 13 fragments still hand-roll the 4-line write-error closure ~95 times: members (13 sites), webhooks (13), deploys (11), registrycreds (9), github (7), jobs (7), sandbox (6), metrics/billing/workspaces (5 each), apikeys/sshkeys (4 each), audit/usage/events. Proof of shape: `apikeys/rest.go:29-59` and `sshkeys/rest.go:26-56` are the identical create/list/delete triple that `projects/rest.go:79-132` writes via the helpers.

Mechanical migration, file-by-file; **skip the genuinely streaming handlers** (logs, agentsessions, sandbox SSE proxies — must stay raw). ~−300 lines, LOW risk; `render_openapi_test.go` + `conformance_test.go` pin the wire shapes. Sub-hour per file but >1h total — promote to a milestone with 2-3 batched tasks when scheduled.
