---
id: w1/m24/t007
title: Simplify — run /simplify over what this milestone changed
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m24/t006]
---

## Objective

Standing closing task. Run `/simplify` over the milestone's diff (schema/parse, secretRef resolution, multi-apply, MCP fragment, scripts, sample) — reuse / simplification / efficiency; behavior-preserving only.

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; `go test ./...` green in `lego/backend`; `make test` green if operator plumbing changed.
