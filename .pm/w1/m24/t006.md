---
id: w1/m24/t006
title: Render parity — bex.yml vs render.yaml Blueprint spec field-by-field; matrix row updated
worker: worker1
status: todo
estimate: 25m
depends_on: [w1/m24/t005]
---

## Objective

Standing closing task — here it is the milestone's core promise: walk render.yaml's Blueprint spec field-by-field against what bex.yml now honors and classify each (honored / documented omission / divergence — sync-delete, previews, envVarGroups, `fromService` per t002's decision, the absent `/blueprints` REST resource) in docs/ADR006-bex-api.md's bex.yml section and docs/ADR018-render-parity.md's "Blueprint / `render.yaml` IaC" row (◐ → updated with the multi-resource consume-side evidence). No dashboard surface — Playwright n/a, recorded in the README.

## Acceptance criteria

- [ ] Every render.yaml field classified with evidence; matrix Blueprint row updated; no silent drops.
