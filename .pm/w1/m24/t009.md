---
id: w1/m24/t009
title: Closeout — DoD met → move milestone to done/
worker: worker1
status: todo
estimate: 10m
depends_on: [w1/m24/t008]
---

## Objective

Standing closing task, run last and only when the DoD actually holds (live stack acceptance green, all-or-nothing + idempotency proven, Blueprint matrix row updated). Close the milestone: remaining tasks `status: done`, files to `w1/m24/done/`, rows `— **DONE**` + `**Status:** done`, move `w1/m24/` whole to `w1/done/m24/`, check the box in `w1/README.md`.

## Acceptance criteria

- [ ] DoD verified before the move; board synced in all three places; `w1/m24/` gone from its old path.
