---
id: w1/m24/t005
title: Acceptance — web + worker + postgres in one bex.yml; env injected; re-deploy no-op
worker: worker1
status: todo
estimate: 25m
depends_on: [w1/m24/t004]
---

## Objective

Prove the stack story on a real cluster: a `bex.yml` declaring a web service, a `background_worker`, and a postgres database deploys with one MCP `deploy` call; all three converge; the web service answers with data read through its `fromDatabase`-injected connection (a real query, not an env-presence check); re-running the deploy is a no-op (no restarts, no new deploy records).

## Steps

1. Author the stack sample in `examples/` (hello-go web extended with a DB read + a trivial worker).
2. One MCP deploy → poll all to Ready → curl the web URL asserting a DB-backed response.
3. Re-deploy; assert zero spec changes/restarts and no new deploy records.

## Files

- `examples/` (stack sample), `scripts/` (verify extension)

## Acceptance criteria

- [ ] Scripted end-to-end pass (exit 0) covering all three assertions on a live cluster.

## Out of scope

- Cron in the sample; prod run (mock/dev cluster suffices).
