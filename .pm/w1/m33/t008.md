---
id: w1/m33/t008
title: Test coverage
worker: worker1
status: todo
estimate: 1.5h
depends_on: [w1/m33/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: pre-deploy gating success/failure, rollback safety, status/log surfacing, and field round-trip.

## Steps

1. envtest for t002: successful pre-deploy gates rollout; failing pre-deploy leaves old revision serving.
2. Test for t003: pre-deploy failure distinct from health-check failure in deploy record.
3. Backend integration test: create/update App via REST/GraphQL/MCP with `preDeployCommand`, assert CR round-trip.
4. Dashboard test (if the repo's test setup covers UI): field persists, deploy-detail shows pre-deploy status.

## Files

- Test files alongside each package touched in t001-t005.

## Acceptance criteria

- [ ] Tests assert real behavior and failure modes (old revision keeps serving on migration failure), not tautological checks.
- [ ] `make test` (operator) and `go test ./...` (backend) pass.

## Out of scope

- Load/performance testing.
