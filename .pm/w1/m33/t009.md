---
id: w1/m33/t009
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m33/t008]
---

## Objective

Close the milestone once the definition of done actually holds: set all tasks `status: done`, move them to `done/`, update the milestone README status, move `w1/m33/` to `w1/done/m33/`, and check its box in the `w1` workstream README.

## Steps

1. Verify the definition of done in `README.md` is actually met — a real failing pre-deploy command must be shown to leave the previous revision serving, not just unit-tested.
2. Run `/pm done w1/m33/t009`.

## Files

- `w1/m33/README.md`, `w1/README.md`, all task files in `w1/m33/`.

## Acceptance criteria

- [ ] Milestone moved to `w1/done/m33/`, all statuses in sync.

## Out of scope

- Any new implementation work.
