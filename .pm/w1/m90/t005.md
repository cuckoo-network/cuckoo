---
id: w1/m90/t005
title: Render parity — agent-session identity across REST/GraphQL/MCP/UI
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m90/t002, w1/m90/t003, w1/m90/t004]
---

## Objective

Confirm this milestone changed only how a session is *displayed* and did not quietly introduce a new API field or diverge the three API surfaces.

## Context

- Render has no coding-agent product, so per `docs/ADR018-render-parity.md` there is no render.com behavior to compare against; parity here is bex's own cross-surface discipline.
- The session view shape (`id`/`ownerId`/`repo`/`branch`/`agentConfig`/`sandboxId`/`phase`/`status`/timestamps) is asserted identical across REST, GraphQL and MCP by `TestRESTGraphQLMCPCreateParity` and `TestViewFieldsAndCodedErrorsMatchEverySurface`.
- The display name is a presentation concern. Promoting it to an API field would add a field to three surfaces and to the mobile client's contract (ADR048) for no capability gain.

## Steps

1. Verify no new field was added to the agent-session view on any surface; the derivation stays in `dashboard/`.
2. Run the cross-surface parity tests in `lego/backend/` and confirm they still pass unchanged.
3. Check the mobile/`capabilities` projection was not touched.
4. If a strong case emerges for a server-side name, do **not** add it here — record it as a follow-up note instead.

## Files

- `lego/backend/internal/agentsessions/` (read-only verification)
- `docs/ADR018-render-parity.md` — only if a divergence is found worth recording

## Acceptance criteria

- [ ] No new agent-session API field on REST, GraphQL, or MCP.
- [ ] Backend parity tests pass unchanged.
- [ ] Any drift found is filed as follow-up rather than silently absorbed.

## Out of scope

- Adding a user-editable session name.
