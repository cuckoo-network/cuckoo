---
id: w1/m90/t006
title: Simplify the code this milestone changed
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m90/t005]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency; behavior-preserving.

## Context

- The likeliest duplication is the display name and the meta-line join: the header, the recents list, and the sidebar rail all render the same two things and previously each did it their own way.
- `session-detail-header.tsx` is ~490 lines and `session-list.tsx` ~333; both are candidates for extracting the shared row/meta rendering rather than leaving three near-copies.

## Steps

1. Run `/simplify` over the changed files.
2. Collapse any remaining duplicate display-name or separator logic to the single t001 helper.
3. Re-run the dashboard suite and confirm behavior is unchanged.

## Files

- The files touched by t001–t004.

## Acceptance criteria

- [ ] No duplicated display-name or meta-join logic remains.
- [ ] `yarn typecheck && yarn lint && yarn test` pass.
- [ ] No behavior change — the t007 assertions still hold.

## Out of scope

- Refactoring agent-session components this milestone did not touch.
