---
id: w1/m90/t008
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m90/t007]
---

## Objective

Close the milestone once its definition of done is actually true in the running dashboard.

## Context

- Per `.pm/CLAUDE.md`, completing this task **is** the move: tasks to `done/`, milestone to `w1/done/m90/`, checkbox ticked in the workstream README.
- Do not run it until the DoD holds against a real session, not just a green suite.

## Steps

1. Re-verify each DoD clause against a real repo-less session in a running dev-N (`dev-env.sh N agent-up`, then start a chat-only session).
2. Confirm a repo-backed session is unchanged.
3. Set every task `status: done`, move each to `done/`, mark the rows and `**Status:** done`.
4. Move `w1/m90/` to `w1/done/m90/` and tick the box in `.pm/w1/README.md`.

## Files

- `.pm/w1/m90/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Every DoD clause verified against a live session, not only tests.
- [ ] Milestone moved to `w1/done/m90/` with no stub left behind.
- [ ] `.pm/w1/README.md` shows `- [x] **m90**`.

## Out of scope

- Any code change — this task only closes the milestone.
