# sshgateway limiter + transport consolidation

From the 2026-08-19 refactor review (§3 network edge; ledger: https://claude.ai/code/artifact/fe4af1ce-211f-4109-a541-f0aabd273c73). Three in-binary merges, ~155 lines net, all LOW risk; promote to a milestone when scheduled (>1h together).

- **Limiter merge:** `sshgateway/limiter.go:25-76` (`SessionLimiter`) and `:79-136` (`ChannelLimiter`) are a literal clone differing only in defaults (100/5 vs 512/32) and metric label strings. One type parameterized by `(defaultGlobal, defaultPerKey, scopePrefix)`; keep both constructors so documented defaults + label vocabularies hold by construction (`limiter_test.go` pins them). Then give `SourceLimiter` (`:139-194`) a global dimension and have `cmd/ssh-gateway/main.go:420-464`'s `admissionListener` delegate to it (three copies of "global + per-source" today, incl. `nativessh/server.go:229`).
- **Transport extractions:** shared `TransportDefaults` (4 byte-identical `defaults()` cores: `nativessh/server.go:127`, `webshell/websocket.go:102`, `sandboxsse/sandboxsse.go:110`, `agentattach/agentattach.go:236`); `StartAuditedSession(store, target, subject, remoteAddr) (id, end, err)` for the 27-line verbatim audit block (`nativessh/server.go:323-349` ↔ `webshell/websocket.go:230-257` — return an explicit `end` closure, the defer-vs-`Limits.Release` ordering is load-bearing); `RevalidatedStream(parent, timeout, interval, check)` owning the revoked-vs-timeout discrimination restated in 3 places (`webshell:265-283`, `sandboxsse:192-227`, `nativessh:412-425`) — the one consolidation that *increases* safety (a fifth transport can't get the ADR061/ADR066 predicate wrong).

**Preserve, do not normalize:** sandboxsse acquires its slot before revalidating, agentattach after (both defensible — parameterize, don't pick); nonce-burn ordering comments (`agentattach:381-384`, `webshell:175-179`) are documented policy; webshell has no `Revalidator` (its resolve IS the authorize); only nativessh re-reads keys mid-connection (ADR061 round-8 #5); `ChannelLimiter` applies only to nativessh by design. Move every ADR provenance comment verbatim.
