# credproxy shared base for agentcred/modelproxy

From the 2026-08-19 refactor review (§3 network edge). Both gateway credential-injecting proxies share ~80 lines of scaffolding: broker fields (`Pods`/`API`/`Metrics`/`HTTP` seam/`Limits` + memoized limiter/client — `agentcred/agentcred.go:96-136,383-398` ↔ `modelproxy/modelproxy.go:58-94,213-219,483-493`) and the handler prologue (parse path → `RemoteIP` → `Acquire` + `LimitRejected` + 429 → `ResolveSessionPod` 403 → authorize 403 → mint).

Shape: `sshgateway/credproxy` leaf with an embeddable `Base` (`limiter(defGlobal, defPerPod)`, `httpClient(timeout)` — which should own the no-follow `CheckRedirect` guarantee w1/m76 t003 patches directly — and `Enabled()`), plus `credproxy.Admit(w, r, limiter, metrics)`. Each broker keeps its own `serve*`.

**Keep separate:** agentcred streams under `MaxBytesReader` (pkt-line validation is streaming), modelproxy buffers whole (nothing reaches the vendor before the cap); agentcred hides non-200 upstream bodies, modelproxy forwards them; agentcred audits, modelproxy doesn't; modelproxy's per-request duration ctx. Also name modelproxy's inline `NewSessionLimiter(32, 2)` defaults like agentcred does (`agentcred.go:54-57`). ~70 lines, MEDIUM risk. Do after w1/057.
