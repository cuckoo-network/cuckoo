---
id: w1/m85/t002
title: Snapshot list API + HMAC-signed 24 h snapshotKey
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m85/t001]
---

## Objective

`GET /v1/disks/{diskId}/snapshots` in Render's shape, with `snapshotKey` as an HMAC-signed object reference valid 24 h.

## Context

- Render shape: `diskSnapshot[] {createdAt, snapshotKey, instanceId}`; "Snapshot keys… expire after 24 hours" ([docs/render-artifacts/disks.md](../../../docs/render-artifacts/disks.md)).
- Signing: `lego/backend/internal/hmacticket` — new flavor with disk/object/expiry claims (each flavor owns only `Claims`).
- Listing reads the backup store's per-disk prefix; `instanceId` carries the single instance's id (the `/instances` naming).
- Surfaces: REST + GraphQL `diskSnapshots` + MCP `list_disk_snapshots`; scope matrix entries.

## Steps

1. Store-listing verb + key mint; three fragments; matrix + parity lists.

## Files

- `lego/backend/internal/apps/disks.go` (+ graphql/mcp fragments)
- `lego/backend/internal/hmacticket/`

## Acceptance criteria

- [ ] Shape test matches Render; a 25 h-old key is refused on restore (asserted in t006).

## Out of scope

- The restore execution (t003).
