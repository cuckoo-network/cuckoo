---
id: w1/m2/t007
title: GitOps-deploy the control plane + end-to-end acceptance
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m2/t002, w1/m2/t005, w1/m2/t006]
---

## Objective

Ship the service via GitOps and prove the whole chain works.

## Context

- Mirror `deploy/gitops/base/bex.yaml` (in-repo path, SSH deploy key) for the control-plane Deployment; register in `base/kustomization.yaml`.

## Steps

1. Add an Argo Application for the control-plane service.
2. Run the acceptance flow end-to-end.

## Files

- `deploy/gitops/base/` (control-plane Argo app + kustomization entry)

## Acceptance criteria

- [ ] Create a tenant + app via the API → an `App` CR appears → the operator deploys it → `status.url` flows back.

## Out of scope

- Auth/billing/UI polish.
