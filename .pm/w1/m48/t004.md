---
id: w1/m48/t004
title: Dashboard — verify the deploy page never renders the silent-window empty state
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m48/t003]
---

## Objective

With synthesis flowing through the existing build legs (GraphQL poll + SSE tail), confirm the deploy detail page shows a platform line within one poll interval of deploy creation, and adjust its empty/loading states only if something still renders as silence.

## Context

- `dashboard/src/features/deploys/components/deploy-log-panel.tsx` + `hooks/use-deploy-logs.ts` — no new legs expected; synthesized lines arrive as ordinary `type=build` entries. Check the `buildStoreUnavailable` banner still appears when applicable (synthesis must not mask it, per m48 DoD).
- Verify live on `dev-1` (`bash .pm/w1/dev-1/up.sh`) with a build whose pod is artificially slow (unschedulable node selector or a large never-pulled image).

## Steps

1. Drive a slow-start deploy on dev-1; watch the page from creation.
2. Fix any state that still shows empty/loader while synthesized lines exist.
3. Component-test any adjusted state.

## Files

- `dashboard/src/features/deploys/components/deploy-log-panel.tsx` (only if adjustment needed)

## Acceptance criteria

- [ ] Screen recording/screenshot evidence: first platform line visible ≤ ~5s after deploy creation on dev-1.
- [ ] `buildStoreUnavailable` banner behavior unchanged and pinned.

## Out of scope

- The deploy-page type filter (inbox `w1/029`).
