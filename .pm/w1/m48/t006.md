---
id: w1/m48/t006
title: Simplify — /simplify over the code this milestone changed
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m48/t005]
---

## Objective

Standing closing task: run `/simplify` over the m48 diff (logs synthesis, tail changes, any dashboard adjustment) — reuse/simplification/efficiency only, behavior-preserving.

## Context

- Candidate hotspots: the phase→line builder (one place, used by history + tail), the injected deploy-row source's shape vs `History`/`PodLogSource` symmetry.

## Steps

1. `/simplify` scoped to the milestone's changed files.
2. Apply safe findings; file residuals as an inbox note.

## Files

- `lego/backend/internal/logs/`, `dashboard/src/features/deploys/`

## Acceptance criteria

- [ ] Findings applied or filed; suites stay green.

## Out of scope

- Bug hunting (that's /code-review).
