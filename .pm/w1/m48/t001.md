---
id: w1/m48/t001
title: Capture Render's platform progress-line contract (vocabulary, log type, ids, CLI)
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

Pin down exactly how Render surfaces its platform progress lines so bex synthesizes a compatible shape, not a guess: the vocabulary (`==> Cloning from…`, `==> Checking out commit…`, `==> Running build command…`, `==> Your service is live`), which log `type` carries them, what labels/ids they have in the logs API, and whether the CLI shows them under `--type build`.

## Context

- `docs/render-artifacts/live-deploy-following.md` § 2026-07-17 records the incident and the deploy-page walk; render.com/docs/your-first-deploy names the `==>` lines.
- The Playwright profile holds a live render.com session (reference service `srv-d2rnr3jipnbc73deuvgg`), and the real Render logs API is reachable read-only — do NOT trigger deploys on the reference service (it serves production traffic); use docs + API reads of existing retained logs, or a scratch service only if one already exists.
- bex's own vocabulary must map to bex's real lifecycle (queued → building → pre-deploy → live/failed), anchored to the deploys row (`created_at`/`started_at`/`finished_at`/status) — where Render's exact wording assumes stages bex can't observe, choose the closest honest line.

## Steps

1. From Render docs + any retained real deploy logs, capture each `==>` line, its log type, and labels.
2. Decide bex's line vocabulary and the deterministic id scheme (deploy id + phase).
3. Record the mapping table in `docs/render-artifacts/live-deploy-following.md` (or a sibling artifact) for t002/t003 to implement against.

## Files

- `docs/render-artifacts/live-deploy-following.md`

## Acceptance criteria

- [ ] A written mapping: bex deploy phase → line text → log type/labels → deterministic id.
- [ ] CLI visibility of Render's lines (or documented uncertainty) noted.

## Out of scope

- Any implementation; any mutation of real Render services.
