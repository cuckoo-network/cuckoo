---
id: w1/m48/t005
title: Render parity — same lines on REST/GraphQL/MCP + official CLI render logs --type build
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m48/t004]
---

## Objective

Standing closing task: verify the synthesized lines are byte-consistent across every surface — REST `GET /v1/logs`, GraphQL `logs`, MCP `list_logs`, the SSE tail — and visible through the unmodified official Render CLI against bex-api; compare wording/shape against t001's captured Render contract and file any drift as follow-up rather than silently diverging.

## Context

- `scripts/cli-compat.sh` (dev-9 harness) or the prod-configured homebrew `render` CLI for the CLI leg.
- `docs/ADR006-bex-api.md` (one core, thin adapters); `docs/ADR018-render-parity.md` logs row.

## Steps

1. Same window, four surfaces, diff the line sets.
2. `render logs --type build --start … --end …` against a dev deploy.
3. Update the ADR018 evidence cell / artifact if the row's status changes.

## Files

- `docs/ADR018-render-parity.md` (evidence update if warranted)

## Acceptance criteria

- [ ] Four-surface consistency evidenced; CLI shows the lines.
- [ ] Any wording/shape drift vs Render filed as an inbox note, not shipped silently.

## Out of scope

- New surface features.
