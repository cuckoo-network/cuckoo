---
id: w1/m9/t005
title: Simplify — run /simplify over the code this milestone changed
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m9/t004]
---

## Objective

Run `/simplify` over the code touched by t001–t004 (store mint paths, apikeys binding, `core.Base` workspace resolver, list/get filtering) — reuse / simplification / efficiency; behavior-preserving.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff; accepted fixes applied; `go build ./... && go test ./...` green in `lego/backend`.
