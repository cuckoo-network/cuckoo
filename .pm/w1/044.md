# 044 — One-off: cancel Stripe subscriptions orphaned by pre-m61 workspace deletes

From w1/m61 (t004 out-of-scope). m61 makes workspace delete cancel the Stripe Subscription going forward, and migration 0056 auto-clears orphaned `sandbox_tenant_keys` rows on deploy. But any workspace deleted **before** m61 shipped, while Stripe billing was live, may still have an **active metered Subscription** at Stripe against a Customer whose workspace is gone (the local billing rows already cascaded away, so bex has no record to drive a cleanup from).

**Do (only if prod ran Stripe billing before m61 deploys — otherwise close as N/A):**

- In the Stripe dashboard (live mode), list active Subscriptions with metadata `bex_billing_contract=true` and cross-check each `bex_workspace=tea-…` against the live `tenants` table (prod DB, see `bex prod debugging access`). Any subscription whose workspace id no longer exists is an orphan.
- Cancel each orphan immediately (`invoice_now=true` to bill final exported usage); **keep** the Customer for invoice history (same stance as `CancelContract`).
- Sandbox keys need no action — migration 0056 already deleted their rows on deploy.

Sub-hour, conditional, operator-run. Blocked until m61 is shipped to prod.
