---
id: w1/m15/t006
title: Simplify — `/simplify` over what this milestone changed
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m15/t005]
---

## Objective

Standing simplify pass: run `/simplify` over the code this milestone changed (the type reconcile paths, the create-surface `type`/`schedule` plumbing, and the dashboard type-awareness) to remove duplication and keep the "one core, thin adapters" shape.

## Context

- Changed surface spans `lego/types/v1alpha1`, `lego/operator/internal/controller/` (t001/t002), `lego/backend/internal/apps/` (t003), and `dashboard/src/features/services/` (t004).
- The `/simplify` skill reviews for reuse/simplification/efficiency/altitude and applies fixes — quality only, not bug-hunting.
- Watch for the common drift: `type` handling duplicated across the three apps adapters instead of centralized in `Create`; worker/cron reconcile branches that could share the Deployment/CronJob build helpers.

## Steps

1. Run `/simplify` scoped to this milestone's diff.
2. Apply the safe reductions (dedupe adapter `type` plumbing, share reconcile helpers).
3. Re-run `make test` and dashboard tests to confirm green.

## Files

- Whatever t001–t004 touched (types, operator reconcile, apps adapters, dashboard services feature)

## Acceptance criteria

- [ ] `/simplify` run over the milestone diff; safe reductions applied.
- [ ] `make test` + dashboard tests still green.

## Out of scope

- Bug-hunting / correctness review (that's `/code-review`, not this pass).
- New features.
