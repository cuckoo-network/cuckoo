---
id: w1/m19.1/t007
title: Simplify — /simplify over what this milestone changed
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m19.1/t006]
---

## Objective

Run `/simplify` over the code this milestone touched — reuse / simplification / efficiency, behavior-preserving.

## Context

- Touched: infra/clusterapi/overlays/hetzner-caph/cluster.yaml (TEMP knobs + tenant floor), .github/workflows/app-cluster.yml + deploy.yml (SSH-to-CP access path — the two workflows likely now share duplicated kubeconfig-fetch logic worth extracting), docs/infra-credentials.md, docs/architecture.md.

## Steps

1. `/simplify` scoped to the files above; apply what it finds.

## Files

- as above

## Acceptance criteria

- [ ] /simplify run; findings applied or explicitly rejected with reasons.

## Out of scope

- Bug-hunting (that is /code-review's job).
