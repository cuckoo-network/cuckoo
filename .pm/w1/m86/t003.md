---
id: w1/m86/t003
title: Dashboard Disk tab (live-captured contract)
worker: worker1
status: todo
estimate: 1.5h
depends_on: []
---

## Objective

Add Render's **Disk** entry to the service sidebar's Manage group and build the tab to the 2026-08-23 live capture, with bex's rate.

## Context

- Contract: [docs/render-artifacts/disks.md](../../../docs/render-artifacts/disks.md) § Live dashboard walk — empty-state card ("…charged at $0.175/GB per month" for bex), inline add form (mount path with `/var/data` placeholder + denylist errors, 1/5/10/50/100 GB chips + free-text defaulting to 10, no name input), the five-bullet warning list verbatim, Cancel/Add Disk triggering a deploy.
- Post-create: usage-over-time graph (t004's series), grow (grow-only client bound), snapshot list + restore behind a confirmation phrase (m85 APIs), delete with the data-loss warning.
- Sidebar omission note in `dashboard` navigation cites the non-goal — remove it; eligible services only (web/private/worker, paid tier) — hide or explain otherwise.
- Consume m84's GraphQL surface; `dashboard/CLAUDE.md` conventions.

## Steps

1. Sidebar + route; empty/add states; attached state (graph, grow, snapshots, delete).

## Files

- `dashboard/src/features/` (new disks feature) + navigation

## Acceptance criteria

- [ ] Component tests for each state incl. refusal copy; `yarn test` green.
- [ ] Live dev-1 walkthrough matches the capture side-by-side.

## Out of scope

- Metrics backend wiring (t004).
