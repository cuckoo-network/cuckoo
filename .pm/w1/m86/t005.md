---
id: w1/m86/t005
title: "Records closeout: ADR018 cells, ADR030 row, CLI checklist, pre-GA price check"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m86/t001, w1/m86/t004]
---

## Objective

Close the reversal's paper trail: every record reflects shipped reality (ADR082 § Record changes, implementation half).

## Context

- `docs/ADR018-render-parity.md`: disk row cells `—` → shipped-surface marks with evidence; the Blueprint row's "no Disks group" divergence note removed; w1/m45 sidebar note ("Disk omitted") updated.
- `docs/ADR030-pricing.md`: discount table gains the disk row ($0.25 → $0.175).
- `docs/cli-compatibility-checklist.md`: disks n/a-upstream row.
- Pre-GA cost check (ADR082 § Verification): authenticated `GET https://api.hetzner.cloud/v1/pricing` (HCLOUD_TOKEN) for `volume.price_per_gb_month` — record the answer to the €0.0440-vs-€0.0572 question in ADR082 (and revisit the rate only if the user asks).

## Steps

1. Doc edits + the price check; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR030-pricing.md`, `docs/ADR082-persistent-disks.md`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] Grep for "non-goal" + "disk" finds no live contradiction outside historical records.
- [ ] Hetzner price recorded with date + source.

## Out of scope

- DO_NOT_DO.md (already carries the re-open record).
