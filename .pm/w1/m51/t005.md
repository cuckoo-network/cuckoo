---
id: w1/m51/t005
title: Render parity — cross-surface consistency + ADR018/ADR023 notes
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m51/t004]
---

## Objective

Standing parity gate: the bandwidth/usage numbers changed semantics (now include HTTPS) with unchanged shapes — confirm REST/GraphQL/MCP/dashboard consistency and record the semantic change in the ledgers.

## Context

- One resolver feeds all surfaces, so consistency should be automatic; the check is that no surface caches or pins the old router set, and the m50 `degraded_sources`/`degradedSources` extensions still behave with the wider matcher.
- ADR018's metrics row (Bandwidth availability/completeness notes) and ADR023 get the "bandwidth now includes the websecure sibling; history before 2026-07-18 undercounts" note.

## Steps

1. Spot-check REST `/v1/metrics/bandwidth`, GraphQL `metrics(BANDWIDTH)`/`monthToDateBandwidth`, MCP `get_metrics` return consistent magnitudes post-fix.
2. Update ADR018 + ADR023 notes; prettier.

## Files

- `docs/ADR018-render-parity.md`, `docs/ADR023-usage-metering.md`

## Acceptance criteria

- [ ] Three surfaces agree on magnitude for the same window.
- [ ] Ledgers record the semantic change + the historical-undercount note.

## Out of scope

- New fields.
