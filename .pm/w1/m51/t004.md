---
id: w1/m51/t004
title: Prod verification — HTTPS volume in chart/month-to-date + egress_bytes rows land
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m51/t002, w1/m51/t003]
---

## Objective

Prove both halves on production: the bandwidth surfaces now include HTTPS traffic, and the usage rollup writes `egress_bytes` rows within the next closed hours.

## Context

- Reference service `srv-d9bj8s3eg85c7390eb9g` (beancount-cms-v2). Pre-fix anchors from w1/m50/t006: month-to-date 83.1 MB (redirect-only), July usage table with zero `egress_bytes` rows.
- HTTPS cross-check must use a SHORT trusted window on the `websecure-` router (e.g. `increase(...[1h])` at a quiet moment) — long-window websecure increases are flap-reset inflated and not a truth source.
- Rollup check: the collector runs hourly; within ~2h of the deploy the usage GraphQL (period 2026-07) should show `egress_bytes` rows for serving apps. Historical rows must be unchanged (diff the pre-deploy snapshot pattern from m50/t006).

## Steps

1. After the deploy: load the metrics page; capture the chart magnitude change and `monthToDateBandwidth` (expect a jump above 83 MB as the month window now includes websecure bytes within retention).
2. Cross-check order-of-magnitude: API's next-hour delta vs a 1h websecure+default Prometheus increase.
3. Wait for a closed hour; query the usage surface — `egress_bytes` rows present and plausible; earlier-period rows unchanged.
4. Append evidence to ADR023's section; screenshots to `.playwright-mcp/`.

## Files

- `docs/ADR023-usage-metering.md`

## Acceptance criteria

- [ ] Month-to-date and the chart demonstrably include HTTPS volume.
- [ ] A fresh `egress_bytes` row exists for at least one serving app after the first closed hour.
- [ ] Pre-existing rows byte-identical.

## Out of scope

- Multi-day billing accuracy studies.
