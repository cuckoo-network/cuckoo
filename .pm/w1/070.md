# cmd/api/config.go: validate all env before irreversible side effects

From the 2026-08-19 refactor review (§1 + §4.5) — the review's only live operational hazard. `cmd/api/main.go` interleaves env parsing with irreversible startup: `store.Migrate` runs (:626 via :373), usage/audit loops + the :8091 control-plane server + the CR projector all start (:403-476) **before** later parses can `log.Fatalf` (`BEX_WEBHOOK_BACKOFF` :1156, `BEX_TRUSTED_PROXY_CIDRS` :1201, `BEX_AUTH_MAX_INFLIGHT` :1250, `BEX_RATE_LIMIT` :119-126, `BEX_HYDRA_ADMIN_URL` :517). A typo crashloops the pod repeating migrations + partial serving every restart. 32 fatal sites; 7 near-duplicate env parsers with silently different failure policies (fatal vs warn-default vs zero).

Shape: generalize the pattern `cmd/api/billing_config.go` already proved (pure funcs over `getenv`, returning errors, testable) into `Load(getenv, now) (Config, warnings, error)` called first in `main()`; `wire*` funcs take `cfg.X`; collapse the 7 parsers to 3 named policies. Two passes: (1) hoist all parsing with identical semantics — copy the fail-closed security messages verbatim (`:608` allow-insecure-authz, `:233`); (2) reorder validation ahead of the kube client / DB / any goroutine. Rider: fold the one escaped env read (`internal/secrets/store.go:128` `BEX_OPENBAO_JWT_PATH`) into Config so `cmd/` is the only reader. ~450 lines move out of main.go (→ ~700); 32 untestable fatals become table-testable. LOW-MED risk. Promote to a milestone when scheduled (>1h, natural 2-3 tasks).
