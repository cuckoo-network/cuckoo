# sniproxy Accept/Splice/Bootstrap: consolidate pg/kv proxy binaries

From the 2026-08-19 refactor review (§2/§3 network edge). ~140 duplicated lines in each of `operator/cmd/pg-sni-proxy/main.go` and `kv-sni-proxy/main.go` (42% of kv): tuning constants with identical values, ~85-line twin `main()` bootstraps, twin watcher `Reconcile`/router bookkeeping, and identical `handleConn` prologue (deadline → bufio → `ReadProxySource` → `AcquireSource` → `Reject("source")`, incl. a byte-identical anonymous logger interface) + epilogue (clear deadline → resolve → `AcquireRoute` → dial → `CopyBidirectional`).

Land in order: (1) `sniproxy.Accept(conn, trusted, limiter, meter, log)` + `sniproxy.Splice(...)` — pure extraction preserving exact ordering (deadline before any read; source-acquire after PROXY resolution, before any backend dial); (2) `sniproxy.Bootstrap(cfg)` for the manager/metrics-listener/TCP-listener sequence (also absorbs the 12-line prometheus-registry+ReadHeaderTimeout idiom the two proxies + egress-meter repeat); (3) optional generic `sniproxy.Table[R]` for `mu/table/invalid/healthy/delete` — which makes the w1/m76 t006 SetHealthy drift class structurally impossible.

**Never merge:** pg's SSLRequest preamble negotiation (`pg:427-448,491-505`) and pg's hostname-variant index (`pg:169-254`, codex round-12 #9 O(1) resolve) vs kv's plain map `resolve` (`kv:134-149`); kv's `Status.ExternalHost` equality gate. ~250 lines net, MEDIUM risk (unauthenticated pre-auth path of two public front doors — existing `main_test.go` suites, 340+353 lines, retarget onto the helpers). Depends on w1/m77 landing first.
