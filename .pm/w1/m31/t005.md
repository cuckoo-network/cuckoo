---
id: w1/m31/t005
title: Dashboard — services list groups by project
worker: worker1
status: todo
estimate: 1h
depends_on: [w1/m31/t003, w1/m31/t004]
---

## Objective

Group the dashboard's services list by project (Render's dashboard-organization shape), with a lightweight create/rename/delete flow for projects, and ungrouped services still shown flat.

## Context

- Follow `dashboard/CLAUDE.md` conventions.
- Reference: render.com/docs/projects' dashboard-organization description ("main page lists the services in your workspace, along with any projects you've organized them into").

## Steps

1. Update the services list (`dashboard/src/routes/index.tsx` or equivalent) to group by project, with ungrouped services in their own section.
2. Add a lightweight project-create/rename/delete flow (a simple form, not a full dedicated page unless the existing IA calls for one).
3. Add an assign-to-project action on a service (row action or in service Settings).

## Files

- `dashboard/src/features/` (new `projects` feature directory)
- `dashboard/src/routes/index.tsx` (or wherever the services list lives)

## Acceptance criteria

- [ ] Services list renders grouped-by-project correctly, with ungrouped services still visible.
- [ ] `yarn test`/`yarn lint` green.

## Out of scope

- Environments (Production/Staging deploy-target semantics) — explicitly deferred, not part of this milestone.
