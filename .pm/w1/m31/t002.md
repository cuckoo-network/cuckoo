---
id: w1/m31/t002
title: REST — projects CRUD + service assignment
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m31/t001]
---

## Objective

Expose `projects` CRUD and service-assignment over bex-api's REST surface.

## Context

- Follow the REST envelope/error-shape conventions in `docs/ADR006-bex-api.md`.

## Steps

1. `GET/POST /v1/projects`, `GET/PATCH/DELETE /v1/projects/{id}`.
2. `PATCH /v1/services/{id}` (or a dedicated assignment endpoint) to set/clear a service's `projectId`.
3. Wire into the existing OpenFGA authz check (workspace-scoped).

## Files

- new `lego/backend/internal/projects/rest.go`
- `lego/backend/internal/api/server.go` (route registration)

## Acceptance criteria

- [ ] CRUD + assignment work; workspace-scoped (cross-workspace access denied).
- [ ] Deleting a project un-assigns its services (doesn't delete the services).

## Out of scope

- GraphQL/MCP — t003/t004.
