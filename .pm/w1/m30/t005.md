---
id: w1/m30/t005
title: Test coverage
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m30/t004]
---

## Objective

Standing closing task: confirm t002's regression test is meaningful (asserts real shutdown behavior and would actually fail without the fix) — no additional tests are needed for the dependency-version bumps in t003, which are covered by the existing `yarn test`/`typecheck`/`build` suite.

## Context

- Precedent: this milestone's own t002 already builds the regression test; this task is the standing-closing-task confirmation pass, not new test-writing from scratch.

## Steps

1. Confirm t002's test genuinely fails without t001's fix (if not already verified as part of t002's own acceptance criteria, verify now).
2. Confirm no other untested behavior was introduced by t001-t003.

## Files

- `lego/backend/cmd/api/main_test.go`

## Acceptance criteria

- [ ] Regression test confirmed meaningful (fails pre-fix, passes post-fix).

## Out of scope

- New tests for the dependency bumps — the existing dashboard test suite already covers that surface.
