---
id: w1/m14/t006
title: Closeout
worker: worker1
status: todo
estimate: 10m
depends_on: [w1/m14/t005]
---

## Objective

Gate the milestone closeout: once the Definition of Done holds — a `KeyValue` CR reconciles to a running, tier-sized Valkey with an internal DNS Service + password Secret, optional public TCP/SNI route behind the domain env, and `make test` green including the reconciler envtest — record the milestone as done across the board. This task file documents the closeout gate; the whole-milestone move and workstream checkbox are performed by the human via `/pm done`.

## Context

- `.pm/CLAUDE.md` — done conventions: set each task's frontmatter `status: done`, move each `tNNN.md` to `wN/mN/done/`, mark rows `— **DONE**` and update the `**Status:**` line in the milestone README.
- The whole-milestone move (`w1/m14/` → `w1/done/m14/`) and the workstream README checkbox are done by the human via `/pm done` — do not do them here.
- DoD lives in `.pm/w1/m14/README.md`; verify it holds before flipping any status.

## Steps

1. Confirm the DoD in the milestone README holds (reconciler behavior + `make test` green).
2. Set `status: done` in each task's frontmatter (t001–t006) and move each `tNNN.md` to `.pm/w1/m14/done/`.
3. In `.pm/w1/m14/README.md`, mark every task row `— **DONE**` and set `**Status:** done`.
4. Hand off the whole-milestone move + workstream checkbox to the human via `/pm done`.

## Files

- `.pm/w1/m14/README.md` (status + row markers)
- `.pm/w1/m14/*.md` → `.pm/w1/m14/done/*.md`

## Acceptance criteria

- [ ] DoD verified as holding before any status flip.
- [ ] All tasks `status: done` and moved to `.pm/w1/m14/done/`; README rows `— **DONE**` and `**Status:** done`.
- [ ] Whole-milestone move + workstream checkbox left to the human `/pm done`.

## Out of scope

- The whole-milestone directory move and workstream README checkbox (human `/pm done`).
- Any implementation, simplification, or test work (t001–t005).
