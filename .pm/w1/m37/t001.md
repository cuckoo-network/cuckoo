---
id: w1/m37/t001
title: Capture Render's maintenance-mode serving semantics
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

Pin the behavior to mirror: what HTTP status does Render's maintenance page return (503? 200)? What does the default page look like? Is `uri` an external URL that's fetched/redirected or a path within the service? Does it apply to custom domains too? Which service types (spec says webServiceDetails only)? Record in `docs/render-artifacts/maintenance-mode.md` (render.com/docs/maintenance-mode + OpenAPI).

## Acceptance criteria

- [ ] Capture answers status code, default page, `uri` semantics, host + type scope; names planned divergences.
