---
id: w1/m37/t003
title: Operator: route hosts to a maintenance responder
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m37/t002]
---

## Objective

While `maintenanceMode.enabled`, every host the service serves answers with the default maintenance page at the captured status code — app pods untouched (no scale change, no deploy). Pick the mechanism deliberately: Traefik errors/redirect middleware vs pointing the Ingress at a shared responder (the static-server or activator pattern — the activator already serves an interstitial for sleeping services; prefer reusing one responder over minting a third). Record the choice + why on this task.

## Context

- Interactions to define, not discover: maintenance + auto-sleep (does an enabled service still sleep? waking into maintenance?), maintenance + suspend (suspend wins), maintenance + deploy (deploys proceed; page persists until disabled).

## Files

- `lego/operator/internal/controller/app_controller.go` (+ the chosen responder)

## Acceptance criteria

- [ ] Envtest: enabled ⇒ Ingress routes to the responder on all hosts; disabled ⇒ restored; pods' Deployment untouched throughout. Interaction rules written down.
