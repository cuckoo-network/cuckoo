---
id: w1/m52/t007
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m52/t005]
---

## Objective

Meaningful tests for the shipped behavior and its failure modes — no tautological or snapshot-everything tests.

## Context

- Backend: the drain sequence (SIGTERM → `/readyz` 503 while requests still succeed → shutdown after window; window respects the grace budget) — testable in-process against a real listener, like the m30 shutdown tests.
- Dashboard: retry link (retries transport errors, does NOT retry GraphQL errors or mutations); hydration re-run (fires once on dehydrated `state:"error"`, never on `ready`/`not-found`); stale-title fix (error title cleared on recovery).

## Steps

1. Backend tests beside the shutdown helper; `cd lego/backend && go test ./...` green.
2. Dashboard tests in the adjacent `__tests__` dirs; `yarn test` green.

## Files

- `lego/backend/cmd/api/` (or the helper's package) tests
- `dashboard/src/common/apollo/__tests__/`, `dashboard/src/common/hooks/__tests__/`, `dashboard/src/common/lib/document-head/__tests__/`

## Acceptance criteria

- [ ] Each new behavior has a test that fails without the fix.
- [ ] All three CI suites green.

## Out of scope

- E2E roll simulation in CI (t004 covers it live; automating a kind-cluster roll test is its own future note if wanted).
