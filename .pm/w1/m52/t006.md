---
id: w1/m52/t006
title: Simplify
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m52/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (bex-api shutdown/readiness helper, dashboard Apollo chain + loader-retry hook, health route) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Likely candidates: the t001 drain logic should live once in the shared m30 helper (api + ssh-gateway), not per-binary; the t003 hydration-retry hook should share shape with `useNotFoundRedirect`.

## Steps

1. `/simplify` over the milestone diff; apply what's clearly worth it, file residuals as an inbox note if any.

## Files

- The milestone's diff.

## Acceptance criteria

- [ ] Simplify pass run; applied or residuals filed.

## Out of scope

- Bug hunting (that's /code-review's job).
