---
id: w1/m52/t008
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m52/t007]
---

## Objective

Close the milestone once the definition of done actually holds (two clean rolls + self-recovery verified, evidence recorded) — not merely when the code is written.

## Steps

1. Confirm the DoD evidence from t004 is recorded in the milestone README.
2. Set every remaining task `status: done`, move each `tNNN.md` to `w1/m52/done/`.
3. Mark every row `— **DONE**` + `**Status:** done` in `w1/m52/README.md`.
4. Move `w1/m52/` → `w1/done/m52/` (whole directory, no tombstone) and check `- [x]` in `w1/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [ ] Board state synced in all three places; milestone lives only at `w1/done/m52/`.
