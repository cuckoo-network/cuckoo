---
id: w1/m36/t004
title: Cache the clusterctl download in `app-cluster.yml`
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

The FUTURE-MAYBE entry's minor sibling: cache the ~65MB clusterctl binary in `.github/workflows/app-cluster.yml` with `actions/cache` keyed on the clusterctl version, so CI runs stop re-downloading it.

## Files

- `.github/workflows/app-cluster.yml`

## Acceptance criteria

- [ ] A re-run with a warm cache skips the download (job log verified); a version bump busts the key.
