---
id: w1/m36/t003
title: Trim the apt package list
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

Drop the unused packages the m19.1 review flagged (`at jq unzip mtr apt-transport-https`) from the provisioning apt install; keep anything the remaining preKubeadmCommands actually use (verify by reading them, not by guessing).

## Files

- `infra/clusterapi/overlays/hetzner-caph/cluster.yaml`

## Acceptance criteria

- [ ] Provisioning succeeds with the trimmed list; no remaining command references a removed package.
