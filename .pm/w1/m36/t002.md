---
id: w1/m36/t002
title: Drop `kubeadm config images pull` from the worker templates
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

Remove the `kubeadm config images pull` line (~200MB of CP images workers never run) from the two worker `KubeadmConfigTemplate`s — the FUTURE-MAYBE entry's second item. Rolls with t001's roll; batch them.

## Files

- `infra/clusterapi/overlays/hetzner-caph/cluster.yaml`

## Acceptance criteria

- [ ] Worker provision log shows no CP image pre-pull; node still joins and runs tenant pods.
