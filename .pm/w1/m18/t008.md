---
id: w1/m18/t008
title: "Simplify: rootDir changes"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m18/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, and efficiency cleanups, behavior-preserving only.

## Context

- Touches `lego/types/v1alpha1/app_types.go`, `lego/operator/internal/build/build.go`, `lego/operator/internal/controller/app_controller.go`, `lego/backend/internal/apps/{webhook,service,rest,graphql,mcp}.go`.

## Steps

1. Run `/simplify` scoped to the files this milestone touched.
2. Apply any surfaced reuse/simplification/efficiency fixes.
3. Re-run `go build ./...` and `go test ./...` in `lego/operator/` and `lego/backend/` to confirm no behavior change.

## Files

- All files touched by t001-t007.

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly dismissed with reason
- [ ] No behavior change (tests still pass)

## Out of scope

- New functionality — this task is quality-only.
