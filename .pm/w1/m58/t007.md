---
id: w1/m58/t007
title: Test coverage
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m58/t006]
---

## Objective

Add meaningful tests asserting the behavior this milestone shipped — real behavior and failure modes, not coverage gaming.

## Context

- The load-bearing claims: two consumers take disjoint rows (`SKIP LOCKED`), an unacked lease requeues, the notified-at marker suppresses across restart/replica, the rate limit holds.

## Steps

1. Add/extend `internal/webhooks` tests for the claim + marker (two-consumer disjointness, lease requeue, restart no-re-notify).
2. Add a test for the deploy-hook limiter's chosen form.
3. Run the backend suite green.

## Files

- `lego/backend/internal/webhooks/*_test.go`
- `lego/backend/internal/deploys/*_test.go`

## Acceptance criteria

- [ ] Tests assert two-consumer disjoint delivery, lease requeue, no-re-notify-on-restart, and the rate-limit behavior.
- [ ] `cd lego/backend && go test ./...` green.

## Out of scope

- Live verification (t005).
