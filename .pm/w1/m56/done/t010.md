---
id: w1/m56/t010
title: Normalize build and release metadata; remove artifact-adoption and fingerprint backfills
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m56/t001]
---

## Objective

Bring all retained build artifacts and releases to the current labels/fingerprints, then remove controller branches that permanently adopt old objects.

## Context

- lego/operator/internal/execution/artifacts.go and internal/build/artifacts.go adopt artifacts missing current App UID ownership labels.
- lego/operator/internal/controller/release_identity.go backfills missing release fingerprints.
- The local cluster observed during research still had three unlabeled build artifacts, so maintained dev state must be migrated as well as production.

## Steps

1. Inventory and patch eligible retained artifacts/releases in maintained dev environments and production.
2. Verify current builds and releases always create the canonical metadata from birth.
3. Delete adoption/backfill branches and add absence guards.

## Files

- lego/operator/internal/execution/artifacts.go
- lego/operator/internal/build/artifacts.go
- lego/operator/internal/controller/release_identity.go
- lego/operator/internal/\**/*test.go
- scripts/

## Acceptance criteria

- [x] Retained build artifacts have current App UID ownership labels and releases have fingerprints.
- [x] Fresh and rebuilt Apps create canonical metadata without a later adoption pass.
- [x] Artifact-adoption and release-fingerprint fallback branches are removed.
- [x] New regression tests fail if canonical metadata is omitted at creation.

## Completion evidence

- Production and maintained-local pre-removal inventories found zero current cross-namespace artifacts missing `app.bex.co/app-uid` and zero active releases missing fingerprints. The 28 historical `bex-system` clone Secret copies were rechecked against Pod references, found unreferenced, deleted by exact namespace/name, and recounted at zero.
- Commit `a82f3a9e` removed name/timestamp adoption, UID-less reclaim, and release-fingerprint backfill branches. Build, pre-deploy, publish, and finalizer paths now fail closed without an App UID; deterministic existing objects must pass exact owner checks.
- Creation-shape and absence tests cover Build Jobs and pod templates, kpack Images, pre-deploy and publish Jobs, copied clone Secrets, build registry Secrets, finalizer cleanup, and missing-fingerprint reconciliation. The full operator suite passed locally and in deployment run [30353112208](https://github.com/bex-co/bex/actions/runs/30353112208), alongside backend/dashboard tests and the production rollout.
- The unmodified Render CLI v2.21.0 created Dockerfile-backed fixture `srv-d9k955v5ic4c73frfd8g`. Its initial build and manual rebuild reached `Running` at release generations 1 and 2 with non-empty artifact/release fingerprints; every retained execution artifact and both Job pod templates carried the exact App UID from creation. Exact-ID deletion removed the App and all scoped artifacts.
- A preceding native-runtime fixture also created only correctly UID-labeled artifacts and was deleted without residue after its unrelated existing runtime-toolchain failure. Final production/local recounts found zero artifact-specific missing-UID objects, zero old `bex-system` clone copies, and zero kpack artifacts missing UID; all non-failed production Apps retained fingerprints.

## Out of scope

- Preserving obsolete failed-build artifacts beyond the documented retention window.
