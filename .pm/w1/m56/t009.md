---
id: w1/m56/t009
title: Normalize legacy datastore names and IP allowlists; remove their migration fallbacks
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m56/t001]
---

## Objective

Normalize every remaining production and maintained-dev datastore object to current identity and structured CIDR shapes, then delete the one-time migration scripts and fallback parsers.

## Context

- scripts/postgres-name-migrate.sh and keyvalue-name-migrate.sh backfill immutable typed ids and display names.
- scripts/ipallowlist-normalize.sh and lego/backend/internal/core/cidr.go preserve older flat string entries.
- A non-zero preflight count requires migration and a second zero-count audit before code removal.

## Steps

1. Run migrations in maintained dev environments, validate behavior, then run them against production with before/after inventories.
2. Verify REST, GraphQL, MCP, operator, and official CLI reads use stable ids, display names, and structured allowlist entries.
3. Delete migration scripts, metadata-name fallbacks, and flat-CIDR parsing branches only after zero-count proof.

## Files

- scripts/postgres-name-migrate.sh
- scripts/keyvalue-name-migrate.sh
- scripts/ipallowlist-normalize.sh
- lego/backend/internal/core/cidr.go
- lego/backend/internal/postgres/
- lego/backend/internal/keyvalue/
- lego/operator/internal/controller/

## Acceptance criteria

- [ ] Production and maintained dev environments report zero legacy datastore identity and IP allowlist shapes.
- [ ] Canonical datastore lifecycle and allowlist operations remain green across exposed surfaces.
- [ ] The migration scripts and legacy fallback branches are removed.
- [ ] Before/after counts and rollback evidence are recorded without credentials.

## Out of scope

- Deleting Render-supported CIDR compatibility fields still present in the current contract.
