# Shrink the cold-start build silence: pre-warm the BuildKit image on build nodes

Evidence (2026-07-17, `dep-d9d8r3h07a5s73dj32ag`): the build Job pod sat Pending ~20:16:16→20:18:19 pulling the BuildKit image on a cold node — a ~2-minute window in which zero build lines exist anywhere. w1/m48 makes the feed *narrate* that window; this note makes the window itself small.

First verify which pool actually runs `BEX_BUILD_NAMESPACE` (bex-system) build Jobs, then either:

- add the build images (BuildKit; cosign when `BEX_TENANT_SIGNING_KEY_SECRET` is set; kpack builder if applicable) to the baked `bex-worker` snapshot (w1/m36 machinery) so autoscaled nodes arrive warm, or
- a minimal pre-pull DaemonSet on build-capable nodes in `deploy/gitops/` (platform infra, not operator code).

Relates `docs/ADR034-scalable-build-pipeline.md`. Likely sub-hour if the snapshot bake list is the answer; promote if the DaemonSet route + verification grows past that.
