---
id: w1/m34/t003
title: REST: create + `PATCH` + read-back (Render shape)
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m34/t001]
---

## Objective

Accept `buildFilter` on `POST /v1/services` and `PATCH /v1/services/{id}` and return it on the service object, matching Render's `serviceDetails.buildFilter` placement + field names exactly.

## Context

- `lego/backend/internal/apps/{rest.go,service.go}` + `AppView`; the `rootDir`/`autoDeploy` PATCH plumbing is the pattern to follow.

## Files

- `lego/backend/internal/apps/`

## Acceptance criteria

- [ ] Create/PATCH/read round-trip asserted; placement matches Render's OpenAPI.

## Out of scope

- GraphQL/MCP (t004), UI (t005).
