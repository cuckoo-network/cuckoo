---
id: w1/m34/t004
title: GraphQL `setBuildFilter` + create arg; MCP tool + args
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m34/t003]
---

## Objective

Mirror t003 on the other adapters: GraphQL `createService(buildFilter:)` + a `setBuildFilter` mutation (the `setRootDir`/`setAutoDeploy` grammar), MCP `set_build_filter` tool + create-tool args (the `set_root_directory` precedent; note it as a bex extension if Render's MCP lacks one).

## Files

- `lego/backend/internal/apps/{graphql.go,mcp.go}`

## Acceptance criteria

- [ ] All three surfaces accept and return the identical shape (adapter-parity test).
