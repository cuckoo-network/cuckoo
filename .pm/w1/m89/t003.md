---
id: w1/m89/t003
title: Migrate the remaining call sites and add the lint guard
worker: worker1
status: todo
estimate: 1h
depends_on: [w1/m89/t002]
---

## Objective

Finish the sweep and make regression impossible: a new hand-rolled AlertDialog should fail CI rather than quietly becoming the 28th copy.

## Context

- Mechanical, but each file's tests need re-checking for the queried role/name — that is where a careless migration breaks things.
- The dashboard already guards structural invariants this way (`routes/__tests__/one-rail-invariant.test.ts`); follow that pattern rather than inventing a new one.

## Steps

1. Migrate the remaining files.
2. Add a guard test that fails when a component outside the primitive imports `AlertDialogCancel` directly.
3. Include the disk tab's `SudoConfirmDialog`, which exists precisely because this primitive did not.

## Files

- the remaining call sites, `dashboard/src/common/components/__tests__/`

## Acceptance criteria

- [ ] `grep -rl AlertDialogCancel` returns only the primitive and the shadcn kit
- [ ] the guard fails on a deliberately reintroduced copy

## Out of scope

- Changing any dialog's copy or behavior.
