---
id: w1/m21/t008
title: Closeout — verify DoD holds, then move the milestone to done/
worker: worker1
status: todo
estimate: 10m
depends_on: [w1/m21/t007]
---

## Objective

When every other m21 task is done **and the DoD is actually met**, close the milestone.

## Context

- DoD: a real static build deploys as a `static_site`, serves from the CDN origin with working redirects/rewrites/headers, and is manageable across REST/GraphQL/MCP/UI.

## Steps

1. Confirm the observable end state (a deployed static site serving with edge rules) — not just code written.
2. Mark all tasks done, move `tNNN.md` → `w1/m21/done/`, update the README rows + status.
3. Move `w1/m21/` → `w1/done/m21/` and check `- [x]` in the w1 README.

## Files

- `.pm/w1/m21/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Milestone lands in `w1/done/m21/` only once the observable end state holds.

## Out of scope

- Any new work — this is the move.
