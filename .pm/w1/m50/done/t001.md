---
id: w1/m50/t001
title: Record the investigation — ADR023 § observability-vs-billing split + prod evidence
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Write the 2026-07-18 investigation down where the fix will be judged against it: an ADR023 section deciding the observability-vs-billing split (the strict health gate stays for usage metering; the metrics read path becomes best-effort), with the term-by-term prod evidence.

## Context

- Symptom: Outbound Bandwidth = "No data in range" on `dashboard.bex.co/services/srv-d9bj8s3eg85c7390eb9g/metrics` while every other panel renders (Prometheus/Traefik/cAdvisor fine).
- Captured wire evidence: `Metrics(name: BANDWIDTH, 12h)` → `{"data":{"metrics":null},"errors":[{"message":"egress source direct unhealthy"}]}`; `MonthToDateBandwidth` → zeros + `"egress source http unhealthy"`.
- The gate: `egressquery.Health()` (`lego/backend/internal/egressquery/query.go:98-141`) is a PRODUCT of window-wide terms — `min_over_time(up[W])`, scrape density `count_over_time(up[W]) ≥ W*8/150`, the same pair over the source health metric, `resets(counter[W]) == 0`, process-start-before-window, and (direct) no counter-loss in-window; `health < 1` makes the verb ERROR for the whole window (`internal/metrics/source.go:111-121`, `:311-323`).
- Prod term evaluation (2026-07-18, 12h window): **direct** — `up{job="bex-egress-meter"}` min 0, 26 of ≥2304 expected samples (single meter instance absent ~12h: tenant node `bex-tenant-0-8fkmg-2dw27` cordoned, DS DESIRED=1 on the 8-node cluster since CP/platform nodes are tainted), `bex_egress_meter_healthy` min 0. **http** — `up{job="traefik"}` min 1 but `resets(traefik_router_responses_bytes_total[12h])` = 32 (Traefik recreates router counters at zero on each deploy's Ingress reload; a month window can never be reset-free). **websocket** — all terms pass, never evaluated (first failing source errors the verb).
- Amplifier: the egress-meter DaemonSet template is at generation 67 in 45h — it rolls with image pins, so every deploy opens an `up` gap that poisons every window containing it.
- Dashboard masking: the GraphQL ERROR renders as the empty-series copy "No data in range" (`network-metrics-card.tsx:262-271`, `svg-line-chart.tsx:68`); `MetricUnavailable` only fires on `ErrMetricsUnavailable`.

## Steps

1. Add an ADR023 section (e.g. "§ Observability reads vs billing reads") recording: the two consumers, why the strict gate is correct for the hourly rollup and wrong for the interactive panel, the prod evidence above, and the decided fix shape (t002's design note back-references this).
2. Cross-link from docs/ADR010-observability.md's metrics section.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR023-usage-metering.md`
- `docs/ADR010-observability.md`

## Acceptance criteria

- [ ] The section states the split decision and carries the term-by-term prod numbers (26/2304 samples, 32 resets, generation 67).
- [ ] A reader can reconstruct why the panel was structurally blank on prod without re-running the investigation.

## Out of scope

- Any code change.
