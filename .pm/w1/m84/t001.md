---
id: w1/m84/t001
title: Store migration + id.Disk kind + projector ownership
worker: worker1
status: todo
estimate: 45m
depends_on: []
---

## Objective

Control-plane persistence for disks: `dsk-` ids mapped to services, sizeGB + lifecycle timestamps (the billing source of truth), and projector ownership of `spec.disk`.

## Context

- `lego/backend/internal/id/id.go` — register `Disk = Kind{prefix: "dsk"}` (Render's observed prefix; guard test updates itself from the registry).
- Latest migration is `0094_git_connections_tenant_fk` → add `0095_service_disks` (disk id, tenant, service id, name, mount_path, size_gb, created_at, deleted_at; resize history or an updated_at trail for the meter's step integration).
- Projector: `internal/store/reconciler.go` `projectSpec` (:1241-1270) / `applyOwnedSpec` (:1277-1322) — the store owns `spec.disk` when set.

## Steps

1. Migration + store CRUD helpers with lifecycle timestamps.
2. `id.Disk` registration.
3. Projector: `spec.disk` round-trips store → App CR; removal projects nil.

## Files

- `lego/backend/internal/store/` (migration + store.go + reconciler.go)
- `lego/backend/internal/id/id.go`

## Acceptance criteria

- [ ] Round-trip test: create → projected CR carries the disk; delete → projected nil.
- [ ] id guard test green with the new kind.

## Out of scope

- HTTP surfaces (t002+), metering queries (t004).
