---
id: w1/m84/t004
title: "disk_gb_seconds meter: rollup, catch-up, usage surfaces"
worker: worker1
status: todo
estimate: 1h
depends_on: [w1/m84/t001]
---

## Objective

Meter provisioned disk capacity from store lifecycle records into `usage_hourly`, per ADR082 D9.

## Context

- New meter kind in `internal/store/usage.go:33-41`; resource kind stays `service` (attribution to the owning `srv-` id).
- Quantity: allocated sizeGB × seconds from t001's lifecycle rows — creation, grow steps (monotonic mid-hour integration), deletion; independent of replicas/suspension. **No Prometheus** — deliberately unlike `queryStorageGBSeconds` (`internal/usage/service.go:875-918`).
- Hourly windows + restart backfill mirroring the datastore catch-up (`catchUpDatastoreMeterThrough`, `:614`); a `disk` source joins the usage-health vocabulary (`usage.go:47-64`).
- Surfaces: `GET /v1/usage`, GraphQL `usage`, MCP `get_usage`, `estimatedCost` line (empty tier — flat per-GB rate).

## Steps

1. Store-driven collector + rollup + catch-up + health source.
2. Surface plumbing (the meter appears wherever the others do).

## Files

- `lego/backend/internal/store/usage.go`, `internal/usage/service.go`

## Acceptance criteria

- [ ] Lifecycle test: create → grow mid-hour → suspend → delete produces exact GB-seconds per hour window, backfilled correctly after a simulated outage.

## Out of scope

- Pricing/Stripe (t005), the dashboard usage graph (m86 — kubelet stats, observability only).
