---
id: w1/m29/t004
title: "Live verification: preamble-mode client connects; direct-TLS client unaffected"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m29/t002]
---

## Objective

Prove the fix live: a default-mode `psql`/driver (preamble TLS negotiation, no `sslnegotiation=direct`) connects successfully to a managed Postgres instance's external endpoint, and a PG 17+ direct-TLS client still works unchanged.

## Steps

1. From outside the cluster, connect with a standard `psql` (or common driver default) to a real managed Postgres instance's external endpoint.
2. Confirm the connection succeeds and queries work.
3. Repeat with a `sslnegotiation=direct` client, confirm it still works unchanged.

## Acceptance criteria

- [ ] A preamble-mode client connects successfully externally.
- [ ] A direct-TLS client is unaffected.
