---
id: w1/m29/t003
title: "Wire connection-info surface changes through REST/GraphQL/MCP if the endpoint shape changes"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m29/t002]
---

## Objective

If t002's design changes the external endpoint's host/port shape (e.g. a distinct port per routing scheme, or a different external host), update `connection-info` across REST, GraphQL, and MCP so callers see the correct connection string. If the design keeps the same host/port (transparent proxy), confirm and document that explicitly instead.

## Context

- `postgres/rest.go`'s `connection-info` endpoint; GraphQL `databaseConnectionInfo`; MCP equivalent — the existing ✅ parity row this milestone sits underneath.

## Steps

1. Determine whether t002's implementation changed the external connection string shape at all.
2. If yes, update all three surfaces consistently.
3. If no, note that explicitly (nothing to change) rather than leaving it ambiguous.

## Acceptance criteria

- [ ] `connection-info` is correct and consistent across REST/GraphQL/MCP for the new proxy path, or explicitly confirmed unchanged.
