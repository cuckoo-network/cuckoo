---
id: w1/m64/t003
title: /agents route: sessions list, sidebar nav, new-session composer with typed error mapping
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m64/t001]
---

## Objective

The `/agents` page: workspace sessions list with phase chips + PR badges, one sidebar nav entry, and the Devin-style new-session composer submitting `createAgentSession`.

## Context

- Routing: flat file-based `routes/agents.tsx`; workspace from cookie/`useWorkspace()`, never the path. Sidebar: one `NAV_GROUPS` entry in `dashboard-sidebar.tsx` (lucide icon; real i18n `labelKey`, en + zh).
- Create contract: `repo`, `branch` (`bex-agent/*` — guide/prefill; `AGENT_SESSION_INPUT_INVALID` inline), `agentConfig{agent: claude|gemini|codex, model?, modelEndpoint?, task, template?}`, `egressAllowlist[]` (≤32 exact public hostnames; invalid entries surface `{reason, entry}`).
- Composer shape (Devin screenshots, ADR047 D9): prominent task textarea, repo/branch selectors, agent select, collapsed Advanced section. Form stack: `react-hook-form` + `ui/form.tsx` + `ui/textarea.tsx`.

## Steps

1. `routes/agents.tsx` (list + composer), `blueprints.tsx` shape; pending-component/code-split convention as applicable.
2. Sidebar entry + locale keys.
3. `components/session-list.tsx`: phase chip, PR `#number` badge → `prUrl`, relative `createdAt`, `empty-state.tsx`.
4. `components/new-session-composer.tsx`: form → `createAgentSession` → navigate to detail; inline mapping for every `AGENT_SESSION_*` code + the 503 unavailable error.

## Files

- `dashboard/src/routes/agents.tsx`
- `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx`
- `dashboard/src/features/agent-sessions/components/{session-list,new-session-composer}.tsx`
- locale files (en/zh)

## Acceptance criteria

- [ ] List renders with phase chips + PR badges on t001 hooks.
- [ ] Composer creates against a live/dev backend and navigates; invalid branch/egress entries show inline human-readable errors.
- [ ] 503 (unconfigured) shows the house callout on the composer while the list still renders.

## Out of scope

- The detail page (t004); Agent/Ask mode toggle (rejected in D9 research).
