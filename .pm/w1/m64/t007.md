---
id: w1/m64/t007
title: Test coverage
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m64/t005]
---

## Objective

Meaningful tests for the behavior this milestone shipped — real assertions on hooks, the stream transport, conversation rendering, and degraded states; no tautological or snapshot-everything tests.

## Context

- House pattern: Vitest + Testing Library, `__tests__/` adjacent, injected fakes (`use-live-logs.test.ts`, `web-shell-panel.test.tsx` precedents); t002's injectable fetch + recorded v1 stream fixtures are the harness.

## Steps

1. Hook tests: phase-aware poll switching; 503 → typed unavailable error; `AGENT_SESSION_*` mapping (incl. egress `{reason, entry}`).
2. Transport tests: ticket-header injection; per-reconnect re-mint via attach; ticket never in a URL; reconnect resumes without duplicate parts (sequence fixture).
3. Conversation tests: recorded terminal stream → full grouped transcript + clean `[DONE]` settle; recorded live stream → incremental append; `data-acp` parts render as the right group components; malformed part handling.
4. Component tests: composer validation + inline errors; steering composer state-routing (chat POST vs redispatch) and disabled reasons; PR card + evidence truncation; cancel confirm.
5. Route tests: list + detail with mocked hooks/transport, per `routes/__tests__` precedent.

## Files

- `dashboard/src/features/agent-sessions/**/__tests__/*`
- `dashboard/src/routes/__tests__/agents*.test.tsx`

## Acceptance criteria

- [ ] All new suites pass in `yarn test`; failure paths (not just happy paths) asserted.

## Out of scope

- The live m43 walk (t004's DoD); E2E browser automation.
