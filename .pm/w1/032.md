# Verify m48 deploy narration on prod (Loki-backed reads) after the next deploy

`w1/done/m48` live-proved the platform progress lines on dev-1: SSE tail, dashboard, and the official Render CLI (`--tail`). What dev-1 could NOT prove is the **Loki-backed history leg** — it runs storeless, so `GET /v1/logs?type=build` 503s there by design and the history-side synthesis (`internal/logs/progress.go` `synthesizeProgress`) is pinned only by unit tests.

After the next prod deploy of bex-api, on a real deploy of any repo-backed service:

1. `render logs --resources srv-… --type build --start <createdAt> --end <finishedAt>` (the prod-configured CLI) — expect `==> Build queued` / `==> Building from …` / the terminal line interleaved with real BuildKit stdout from Loki, deterministic ids across two reads.
2. The dashboard deploy page after a completed deploy — platform lines present in the closed window without the SSE tail (pure history read).
3. Spot-check that `type=app`/untyped reads still carry no `==>` lines (explicit-build-only condition).

Sub-hour; pure verification, no code expected. Evidence home: `docs/render-artifacts/live-deploy-following.md`.
