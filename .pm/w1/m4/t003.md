---
id: w1/m4/t003
title: Implement the wake path (chosen in t002)
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m4/t002]
---

## Objective

Build the production wake-on-request path chosen in t002: either wire KEDA's HTTP add-on (interceptor + HTTPScaledObject) for free-tier apps, or build the lean custom Go activator (receive → buffer → patch replicas up → wait Ready → forward).

## Context

- If **custom**: new small Go service `operator/cmd/activator/`; keep the first cut single-app-at-a-time, correct over fast (buffering + cold-start are the hard part — this is what Render ended up owning).
- If **KEDA**: an HTTPScaledObject per free-tier app, driven by the control plane / operator.

## Steps

1. Implement the chosen mechanism for a single free app.
2. Handle the cold-start window (hold/queue the first request until Ready, then forward).

## Files

- `operator/cmd/activator/` (custom) OR KEDA HTTPScaledObject wiring in the operator/control plane

## Acceptance criteria

- [ ] A request to a 0-replica free app scales it up and returns the app's response after cold start.

## Out of scope

- Edge routing to the activator/interceptor (t004).
