---
id: w1/m4/t001
title: Idle → scale Deployment to 0 (per idleTTLSeconds)
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

Detect inactivity per `App.spec.idleTTLSeconds` and set the Deployment's replicas to 0 (the *idle→0* half already exists as an idea; wire it). Pairs with the allocator's idle-evict.

## Context

- `App.spec.idleTTLSeconds` (existing field). Free tier only.
- Inactivity signal: request metrics / last-access; assumption is a simple last-request timestamp to start.

## Files

- `operator/internal/controller/app_controller.go` (scale-to-0 on idle)

## Acceptance criteria

- [ ] An idle free App's Deployment goes to 0 replicas after `idleTTLSeconds`.

## Out of scope

- The wake path (t002/t003).
