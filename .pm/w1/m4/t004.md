---
id: w1/m4/t004
title: Traefik routing — sleeping app → activator/interceptor
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m4/t003]
---

## Objective

Route traffic for a sleeping (0-replica) app to the activator/interceptor instead of the empty Service.

## Context

- Traefik route/middleware; the operator (or the wake component) flips the route when replicas hit 0 / come back.
- `deploy/gitops/base/traefik.yaml`. (KEDA's http-add-on ships its own interceptor Service that Traefik can target directly.)

## Files

- Traefik config / operator Ingress logic

## Acceptance criteria

- [ ] Requests to a sleeping app reach the activator/interceptor; once awake, traffic goes back to the Service.

## Out of scope

- Wake-path internals (t003).
