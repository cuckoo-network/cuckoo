# apps/service.go split into 7 sub-files

From the 2026-08-19 refactor review (§4.2). `backend/internal/apps/service.go` is 3,864 lines holding 8 unrelated clusters; the topical test files (`ssh_test.go`, `autoscaling_test.go`, `static_site_test.go`, `cron_runs_test.go`, `maintenance_mode_test.go`, `create_normalization_test.go`, …) already mark the seams. Pure same-package moves: `ssh.go` (:1142-1370 + eligibility helpers), `cron_runs.go` (:933-996 + :2547-2692), `static_site.go` (:482-526 + :3473-3730), `autoscaling.go` (:3730-3864), `maintenance_mode.go` (:527-671 + :3338-3383), `create.go` (:1371-1956) + `create_spec.go` (:1957-2443), `settings.go` (:2811-3418 — the ~25 `Set*` verbs; w1/m78's `ApplyServicePatch` lands here). Leaves service.go at ~1,000 lines (struct, views, lifecycle verbs, the `writeThroughStoreFetched`/`patch` write core).

**Do NOT sub-package**: `TestAuthzGuardsEveryVerb` (`internal/api/server_test.go:890`) reflects over `sweepableServices` — a second service means verbs silently escape the sweep (the regression :961/:976 record happening once); `server.go:691` would need a second 20-field literal; ADR006 says one service per feature. One commit per file. **After it lands, re-measure the `internal/blueprints` package question**: the blueprint cluster (~7,200 lines) crosses ~23 unexported helpers today — if the split shrinks that to <10 the package becomes cheap; if not, decline again.
