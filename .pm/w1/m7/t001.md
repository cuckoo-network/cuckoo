---
id: w1/m7/t001
title: Firewall the app nodes (Hetzner firewall, Terraform)
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

The app node has **no** firewall today → kube-API `:6443` is internet-exposed. Attach a Hetzner firewall: allow `:80/:443`, lock `:6443` and `:22` to CI egress + admin IPs.

## Context

- Mirror the existing `bex-infra` firewall pattern in Terraform.

## Files

- `infra/terraform/` (app-node firewall, mirroring the infra firewall)

## Acceptance criteria

- [ ] `:6443` and `:22` are reachable only from CI/admin IPs; `:80/:443` stay open; applied via Terraform.

## Out of scope

- WireGuard (t004); private network (t005).
