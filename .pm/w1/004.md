# Render scale API (`POST /v1/services/{id}/scale` → spec replicas)

Expose Render's manual-scaling verb — `POST /v1/services/{id}/scale` (body `{numInstances}`) + GraphQL parity — as an App CR spec patch the operator converges, alongside the existing lifecycle verbs (`docs/bex-api.md`). Today `replicas` is status-only surfaced; check whether the App spec already carries a replica field or needs one (`lego/types`). Verify shape against Render's OpenAPI spec.

UI half is `w5/005` (manual-scaling section in service Settings) — promote both together when this lands. Interacts with w1/m3 (bin-pack/autoscale) and w1/m4 (scale-to-zero): manual scale is the degenerate case, so keep the spec field compatible with those plans.

Source: `/pm-brainstorm more milestones for w5` (2026-07-08). Goal: Render parity, pillar-1 API-first.
