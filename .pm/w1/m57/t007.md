---
id: w1/m57/t007
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m57/t006]
---

## Objective

Close the milestone once every other task is complete **and the definition of done actually holds** on prod (Argo Synced/Healthy static-server on the CI image, one config-complete instance, zero hand-applied residue, `scripts/samples-lifecycle.sh static-site` green through a redeploy — t004's recorded evidence).

## Context

- Standing closing task; completing it **is** the move (see `.claude/commands/pm.md` § Closeout).

## Steps

1. Re-check the DoD against t004's evidence; do not close on written-but-unverified state.
2. Set every task `status: done`, move each `tNNN.md` to `w1/m57/done/`, mark rows `— **DONE**` and `**Status:** done` in the milestone README.
3. Move `w1/m57/` → `w1/done/m57/` and check the `- [x]` box in `w1/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m57/`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] DoD verified against recorded evidence before any move.
- [ ] Status synced in all three places; the whole milestone lives under `w1/done/m57/` with no tombstone left behind.

## Out of scope

- New work discovered at closeout — file as inbox notes.
