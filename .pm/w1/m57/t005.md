---
id: w1/m57/t005
title: Simplify pass over the manifests/scripts this milestone changed
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m57/t004]
---

## Objective

Run `/simplify` over the code this milestone touched — the new/changed GitOps Application(s), `lego/operator/config/staticserver` manifests, and any `scripts/registry-secrets.sh` changes — for reuse, simplification, and efficiency. Behavior-preserving only.

## Context

- Standing closing task (see `.claude/commands/pm.md`).
- Likely candidates: duplicated env/config declarations between overlays; the static-server Application following (or not) the exact pattern of the sibling `bex-operator`/`dashboard` apps; leftover references to the deleted second Deployment.

## Steps

1. `/simplify` over the milestone's diff.
2. Apply what's clearly worth it; file trigger-gated residuals as inbox notes instead of stretching scope.

## Files

- Whatever t002/t003 changed (`deploy/gitops/`, `lego/operator/config/staticserver/`, `scripts/registry-secrets.sh`, docs)

## Acceptance criteria

- [ ] Simplify pass executed over the milestone's changes; applied or explicitly-deferred findings recorded.

## Out of scope

- Bug-hunting (that's `/code-review`), new behavior.
