---
id: w1/m28/t007
title: Update CLAUDE.md docs to reflect CI-enforced tests
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m28/t006]
---

## Objective

Update the root `CLAUDE.md`, `lego/operator/CLAUDE.md`, and `dashboard/CLAUDE.md`'s command sections to note that `make test`/`go test`/`yarn test` are now CI-enforced on every push/PR touching their respective paths, not just local dev commands.

## Context

- Root `CLAUDE.md`'s "Commands" section currently documents `make test`/`make lint` as things run "from `lego/operator/`" without mentioning CI enforcement.
- `dashboard/CLAUDE.md`'s "Development commands" section lists `yarn test` etc. as local commands only.

## Steps

1. Add a line to each relevant CLAUDE.md noting the new CI workflows and what they gate.
2. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `CLAUDE.md`
- `lego/operator/CLAUDE.md`
- `dashboard/CLAUDE.md`

## Acceptance criteria

- [ ] Each doc mentions the CI enforcement, matching the shipped workflow behavior.

## Out of scope

- Rewriting the full Commands sections — additive notes only.
