---
id: w1/m62/t012
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m62/t011]
---

## Objective

Close the milestone once every other task is complete **and the definition of done actually holds** (observable, not merely code-written): set remaining task statuses to done, move each `tNNN.md` to `w1/m62/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone README, move the whole `w1/m62/` directory to `w1/done/m62/`, and check the `- [x]` box in `w1/README.md`.

## Context

- The DoD in `w1/m62/README.md` requires demonstrated behavior on a gate-enabled stack (dev-1) — 402 across surfaces, webhook-unlocked success, emitter withholding, env-off invariance — not just green tests.
- Board rule: the milestone's original path must not exist after the move; no tombstones.

## Steps

1. Verify the DoD end state against a running gate-enabled stack.
2. Execute the closeout moves (`/pm done w1/m62/t012` last).
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m62/` → `.pm/w1/done/m62/`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] DoD verified; all three status locations in sync; original milestone path gone.

## Out of scope

- Shipping (`/ship` is user-triggered).
