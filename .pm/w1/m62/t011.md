---
id: w1/m62/t011
title: Test coverage for gate, marker, emitter withholding, env-off invariance
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m62/t009]
---

## Objective

Standing closing task: consolidate meaningful behavior tests for what m62 shipped, closing the gaps the per-task tests left. Tests assert real behavior and failure modes — no tautological or snapshot-everything coverage.

## Context

Coverage the milestone must end with (some lands in t002–t007; this task audits and fills):

- A guard-style sweep (in the spirit of `TestAuthzGuardsEveryVerb`) that every paid-intent verb consults the gate — a future paid-capable verb that forgets the gate must fail a test, not a review.
- End-to-end: card-less workspace → paid create 402 on all three surfaces → simulated `checkout.session.completed` → same request succeeds; exemption matrix (stamped / excluded / comped).
- Emitter: withheld rows never stamp `emitted_at`; bind → next pass ships; comped ships throughout.
- Env-off invariance: with `BEX_REQUIRE_PAYMENT_METHOD` unset, the full existing suites pass with the new code paths provably dormant (nil gate).
- Startup validation: the env var without Stripe/store config refuses boot.
- Dashboard: interception → poll → resume, and the cancel path.

## Steps

1. Audit per-task tests against the list above; write the missing ones.
2. Run backend suites (`cd lego/backend && go test ./...`), `make lint` from `lego/operator/`, and dashboard `yarn test`.

## Files

- `lego/backend/internal/{billing,apps,postgres,keyvalue,store,api}/…_test.go`
- `dashboard/src/**/__tests__/`

## Acceptance criteria

- [ ] The verb-sweep guard exists and fails when a gate call is removed (verified by mutation).
- [ ] All suites + lint green; CI-enforced paths (`backend-test.yml`, `dashboard-test.yml`) pass.

## Out of scope

- E2E against production or live-mode Stripe.
