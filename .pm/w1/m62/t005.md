---
id: w1/m62/t005
title: Gate paid intent in postgres + keyvalue (create, plan update)
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m62/t004]
---

## Objective

Extend the t004 gate + refusal shape to the two managed datastores: Postgres create / plan update landing on `basic-256mb`/`basic-1gb`, and Key Value create / plan update landing on `starter`/`standard`. `free` stays ungated in both families.

## Context

- Paid ids per `lego/types/tiers/tiers.yaml`: postgres non-`free` (`basic-*`), valkey non-`free` (`starter`, `standard`); both families default to `free` when the plan is absent.
- Verbs: `internal/postgres` create + plan-change (`service.go` validate ~626–687 area), `internal/keyvalue` create + plan-change — each already starts with `Authorize`/`AuthorizeDatabase`/`AuthorizeKeyValue`; the gate call follows the same authz-then-payment order as t004.
- Reuse t004's shared paid-tier helper and the sentinel→surface mapping; no new error shapes.

## Steps

1. Gate Postgres create and plan update when the target plan is paid.
2. Gate Key Value create and plan update when the target plan is paid.
3. Mirror t004's table-driven tests for both features across the three surfaces.

## Files

- `lego/backend/internal/postgres/service.go` (+ rest/graphql/mcp fragments as needed, + tests)
- `lego/backend/internal/keyvalue/service.go` (+ fragments, + tests)

## Acceptance criteria

- [ ] Card-less workspace: paid Postgres/KV create or upgrade refuses with the same per-surface shape as services; `free` creates succeed.
- [ ] Exempt (stamped/excluded/comped) workspaces pass; env-unset behavior unchanged.

## Out of scope

- Blueprint `fromDatabase`-style linkage (not a plan-setting path).
