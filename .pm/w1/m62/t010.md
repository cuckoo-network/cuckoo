---
id: w1/m62/t010
title: Simplify pass over the changed code
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m62/t009]
---

## Objective

Standing closing task: run `/simplify` over the code this milestone changed (gate seam, verb call sites, emitter filter, webhook stamp, dashboard hook) — reuse, simplification, and efficiency only; behavior-preserving.

## Context

- Likely candidates: one shared paid-tier helper instead of three per-family checks; a single sentinel→surface error mapping instead of per-adapter branches; the dashboard gate hook shared across the three resource flows.

## Steps

1. `/simplify` scoped to the m62 diff.
2. Apply accepted findings; keep suites green.

## Files

- Whatever the m62 diff touched (backend `internal/{billing,apps,postgres,keyvalue,store,core,api}`, `dashboard/src`)

## Acceptance criteria

- [ ] Simplify findings applied or explicitly declined with reasons; backend + dashboard suites and lint green.

## Out of scope

- Behavior changes of any kind.
