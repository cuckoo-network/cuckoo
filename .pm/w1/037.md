# Confirm m52 zero-error rolls on the first post-ship prod deploy

w1/m52 shipped the roll fixes (bex-api in-process drain + `/readyz`, dashboard preStop/readiness gate, client self-recovery) and verified them on the local cluster (bex-api: two rolls, 712 requests, 0 non-200), on prod for the dashboard's manifest mechanism (preStop rolls, ~189 loads, 0× 502), and live in a browser (hands-off recovery incl. title). What could not run pre-ship is the strict DoD run: **two consecutive rolls of both prod Deployments with the shipped image under the load loop** — the bex-api image with `/readyz` + drain only reaches prod via `deploy.yml`.

After the next `/ship`: wait for the deploy roll (that roll itself is measurement #1 if watched), or run a quiet-hour `kubectl rollout restart` of `bex-system/bex-api` and `dashboard/dashboard` twice under `scripts`-style curl loops (the m52 evidence section has the exact shape). Expect zero non-2xx and zero error pages; append the counts + timestamps to `w1/done/m52` (or `w1/m52`) README and close this note.

Also riding that deploy: the dashboard readiness probe flips to the new `/healthz` (upstream-latch) route — confirm fresh pods go Ready only after reaching bex-api, and that no `GET /` cold-SSR probe-timeout events recur.
