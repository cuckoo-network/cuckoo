---
id: w1/m35/t002
title: `fromGroup` links a declared group to the service
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m35/t001]
---

## Objective

Honor `envVars: [{fromGroup: <name>}]` on a service entry: link the (t001-materialized or pre-existing) env group to the service via the existing link verb, ordered after group creation within the same apply.

## Files

- bex.yml apply path + `internal/envgroups` link caller

## Acceptance criteria

- [ ] A service declaring `fromGroup` comes up with the group's vars injected; unknown group name is a per-entry validation error, not a partial apply.
