---
id: w1/m35/t003
title: `sync: false` semantics on blueprint sync
worker: worker1
status: todo
estimate: 40m
depends_on: []
---

## Objective

Honor `sync: false` on a bex.yml env var: the value seeds the var at first apply, but a later blueprint `sync` never overwrites the live value (Render's semantics — the dashboard-edited value wins). Record the flag on the stored manifest so `sync_blueprint` can skip it.

## Context

- `POST /v1/blueprints/{id}/sync` (w2/m15) re-applies all-or-nothing today; verify Render's exact first-apply-vs-sync behavior from render.yaml docs before coding.

## Files

- `lego/backend/internal/blueprints/`

## Acceptance criteria

- [ ] Apply → edit var live → sync: the edited value survives; a `sync: true`/unset var is overwritten as today.
