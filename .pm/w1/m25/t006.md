---
id: w1/m25/t006
title: Dashboard Postgres-detail Insights panel surfacing processes/top-queries/sizes/table-scans
worker: worker1
status: todo
estimate: 1h
depends_on: [w1/m25/t001, w1/m25/t002, w1/m25/t003, w1/m25/t004]
---

## Objective

Add an "Insights" (or similarly-named) panel to the dashboard's Postgres detail page, surfacing at minimum processes + sizes, matching Render's Postgres-info tabs.

## Context

- `dashboard/` — see `dashboard/CLAUDE.md` before editing; Databases pages already shipped in `w5/m8`.

## Steps

1. Add GraphQL queries in the dashboard client for processes/sizes (and top-queries/table-scans if time allows).
2. Add an Insights tab/section to the Postgres detail page rendering the above as tables.
3. Handle the empty/loading/error states consistently with the rest of the Databases page.

## Files

- `dashboard/` Postgres detail page + its GraphQL queries (locate via existing `w5/m8` Databases page code)

## Acceptance criteria

- [ ] The Postgres detail page shows live processes and sizes data for a real instance.
- [ ] Loading/error/empty states match the rest of the dashboard's conventions.

## Out of scope

- Parameter-overrides UI (t005 may be read-only backend-first; UI can follow in a later milestone if needed).
