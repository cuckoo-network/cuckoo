---
id: w1/m59/t004
title: Simplify (`/simplify` over the diff)
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m59/t003]
---

## Objective

Run `/simplify` over the deploy-workflow changes (reuse / simplification / efficiency; behavior-preserving) and apply justified findings.

## Context

- The diff is `.github/workflows/deploy.yml` + any guard script; the staleness check and the write-back guard likely share the same path-filter/diff logic and are the simplification surface.

## Steps

1. Run `/simplify` over the m59 diff.
2. Apply behavior-preserving cleanups (e.g. share the path-filter between the pre-build check and the write-back guard); record declines with reasons.

## Files

- `.github/workflows/deploy.yml`

## Acceptance criteria

- [ ] `/simplify` run; justified findings applied, declines recorded.

## Out of scope

- Bug hunting (use `/code-review`).
