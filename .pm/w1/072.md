# core/base.go + envgroups/service.go file splits

From the 2026-08-19 refactor review (§4.4 backend). Same-package file moves, ~zero risk, no lines saved — pure comprehension; do opportunistically alongside anything touching them:

- **`core/base.go`** (1,687 lines, 46% comments — the fail-closed authz family deserves to be readable end-to-end): split into `vocabulary.go` (:38-283 — relation/label constants, enums, the 6 interfaces), `authorize.go` (:448-860 — the security-critical `Authorize*`/`AuthorizeMintClass`/`checkAuthz` family), `resolve.go` (:854-1350 — workspace + App/Database/KeyValue resolution), `pods.go` (:1493-1687). The tests already mirror these boundaries (`authorizeapp_test.go`, `authorizedatastore_test.go`, `datastore_resolution_test.go`, `workspace_test.go`).
- **`envgroups/service.go`** (1,754 lines, six areas): first move the runtime name-claim code (:1346-1457) into the `nameclaims.go` that already exists holding only its audit half (the half-done split's tell); then `links.go`, `vars.go`, `meta.go`.
- Runner-up from the same review pass: `notifications/service.go` mixes push-settings normalization (:308-660) with deploy-email rendering (:662-923) — a two-file split, low value, only if in the area.

Declined by the review — do not do: grouping `api.Deps` (84 fields) into sub-structs ranks below everything else (wide mechanical diff, zero lines saved); the 102 `if s.Store == nil` guards are a deliberate per-verb 503 contract.
