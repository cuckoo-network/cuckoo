---
id: w1/m43/t003
title: Verify `gitops-validate.sh` clean locally and the check green on `main`
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m43/t001, w1/m43/t002]
---

## Objective

Prove the guarantee is restored: the full local validation script exits 0, and after the fixes land on `main`, the `gitops (render)` workflow run is green — with the run URL recorded as evidence.

## Steps

1. `bash scripts/gitops-validate.sh` → exit 0 (it must exercise both fixed failure modes — see t005 if it doesn't).
2. After merge, capture the green check run URL for the closeout record.

## Files

- none expected (verification only)

## Acceptance criteria

- [ ] Local script exits 0; green `gitops (render)` run on `main` linked in this task on completion.

## Out of scope

- The independent `deploy (bex via Argo)` workflow.
