---
id: w1/m43/t001
title: Fix traefik 41.0.0 values schema drift (`logs` key) in the gitops values
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

`helm template traefik 41.0.0 -f deploy/gitops/base/values/traefik.values.yaml` fails schema validation: `additional properties 'logs' not allowed`. The chart was bumped to 41.0.0 without migrating the `logs` values key (removed/renamed in that version's schema). Migrate the values to the 41.0.0 shape so the rendered output preserves today's intended logging config.

## Context

- `deploy/gitops/base/values/traefik.values.yaml` — the offending `logs` key.
- Check the traefik chart 41.0.0 values schema/CHANGELOG for the key's new home before editing — preserve semantics, don't just delete.
- GitOps is Argo-reconciled (`deploy/gitops/`); a values change here rolls to the platform Traefik.

## Steps

1. Find where 41.0.0 moved the log settings; translate the existing values.
2. `helm template` locally with the pinned chart version to confirm it renders, and diff the rendered log-related output against the pre-bump intent.

## Files

- `deploy/gitops/base/values/traefik.values.yaml`

## Acceptance criteria

- [ ] `helm template` with chart 41.0.0 and the fixed values succeeds.
- [ ] Rendered logging config is semantically unchanged from the pre-bump intent (or the difference is documented in the task on completion).

## Out of scope

- Any other chart bumps or values refactors.
