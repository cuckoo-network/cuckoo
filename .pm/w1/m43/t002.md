---
id: w1/m43/t002
title: Sync the stale `BackupCronJobStale` promtool test fixture
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

`promtool check rules` fails because the `BackupCronJobStale` rules-test fixture in `deploy/gitops/base/prometheus.yaml` expects the alert annotation text from before the rule's annotation gained its `docs/ADR031-platform-data-backup.md` reference. Sync the fixture's expected annotation with the actual rule.

## Context

- `deploy/gitops/base/prometheus.yaml` — both the rule and its test fixture live in this tree; the rule is the source of truth (its runbook link is intentional, per ADR031).

## Steps

1. Update the fixture's expected annotation to match the rule verbatim.
2. Run promtool locally (the same invocation `scripts/gitops-validate.sh` / the CI check uses).

## Files

- `deploy/gitops/base/prometheus.yaml`

## Acceptance criteria

- [ ] `promtool check rules` (and its unit tests) pass locally.

## Out of scope

- Changing the alert rule itself.
