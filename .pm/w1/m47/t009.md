---
id: w1/m47/t009
title: Closeout - verify DoD met, move milestone to done
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m47/t008]
---

## Objective

Final verification that the Definition of Done is met, then move the completed milestone to `done/`.

## Context

This is the standing closing task for every milestone. Before moving to done, confirm:
1. **DoD checklist from README.md** — Events page shows deploy + audit events in one timeline
2. **No regressions** — service management flows work end-to-end on dev-1
3. **All tasks marked done** — every task through t008 is complete
4. **Render parity confirmed** — t006 verified REST/GraphQL/MCP consistency

## Steps

1. Review the milestone's Definition of Done (in this README.md)
   - [ ] Events page displays deploy transitions and audit events in one timeline
   - [ ] Dashboard navigation shows unified Events tab
   - [ ] REST/GraphQL/MCP all return the same event stream
   - [ ] Deploy details (commit, image, trigger) visible alongside audit events
   - [ ] Existing deploy detail route handled or redirected
   - [ ] No dead code remains
   - [ ] Full test coverage in place

2. Manually test on dev-1:
   - Open a service page → Events tab should show both deploys and config changes
   - Check pagination works
   - Try a deploy and watch it appear in Events in real time
   - Verify old Deploys tab is gone from navigation

3. Run test suite:
   - `cd lego/backend && make test` (backend events tests)
   - `cd dashboard && yarn test` (frontend Events component tests)
   - `cd lego/operator && make test` (no events changes, but verify nothing broke)

4. Once all above pass and all tasks are marked `status: done`:
   - Mark this task `status: done`
   - Set milestone `**Status:** done` in README.md
   - Mark each task row `— **DONE**` in the README table
   - Move each completed task file to `wN/mN/done/tNNN.md`
   - Move the entire `w1/m47/` directory to `w1/done/m47/`
   - Check the `- [x]` box next to `m47` in `w1/README.md`

5. Final check: run `prettier` on the markdown files to ensure consistency

## Files

- `.pm/w1/README.md` (check the milestone box)
- `.pm/w1/m47/README.md` (set Status to done, mark rows DONE)
- All `tNNN.md` files (set status: done, move to done/)

## Acceptance criteria

- [ ] All prior tasks (t001–t008) are marked `status: done`
- [ ] No regressions in service management flows
- [ ] Test suite is green
- [ ] Milestone README shows `**Status:** done`
- [ ] All task rows marked `— **DONE**`
- [ ] Milestone directory moved to `w1/done/m47/`
- [ ] Workstream checkbox `- [x] m47` in `w1/README.md`

## Out of scope

- Post-ship communication or blog post
- Analytics/adoption tracking
- Future improvements to the Events view (those are new backlog items)

