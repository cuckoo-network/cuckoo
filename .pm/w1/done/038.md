# Flaky operator CI test blocked a deploy: kv-sni-proxy meter assertion

2026-07-18 10:24 UTC, deploy run for `3cb335cf`: `TestPublicProxyPreservesEndToEndTLSAndMetersOnlyBackendWrites` (`lego/operator/cmd/kv-sni-proxy/main_test.go:177`) failed on the CI runner with `meter delta = 0; want backend TLS response bytes only` — the metered bytes hadn't landed when the assertion ran. Passes locally 5×/5 (`go test -count=5`), unchanged since it last passed CI (`4c10d006`), so a timing race on a slow runner, not a regression; `gh run rerun --failed` unblocked the deploy.

Sub-hour fix when it recurs: make the assertion poll/wait for the meter delta (eventually-style, like the envtest suites) instead of reading it once after the client write returns — the proxy meters backend→client bytes asynchronously to the test's write path.
