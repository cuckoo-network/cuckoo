---
id: w1/m44/t002
title: "Dashboard seat display: \"X of Y seats\" + invite-full state"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m44/t001]
---

## Objective

Show t001's seat usage in the Team panel — "X of Y seats" (Render shows seat usage in its Team Members section header) — and derive the invite button's full-seat state from it up front instead of only learning the cap from the invite dialog's refusal error.

## Context

- `dashboard/src/features/team/components/team-panel.tsx` — panel header is where the count belongs.
- `dashboard/src/features/team/api/team.graphql` + `yarn codegen` (needs `CODEGEN_SESSION_TOKEN`, see dashboard/CLAUDE.md § GraphQL).
- The existing blocked-invite CTA (`invite-member-dialog.tsx:45-74`, w6/m15/t001) stays — it is the recovery path when the state races; this task adds the proactive display.
- Unlimited plans: hide the "of Y" (render just the member count), don't show "of 0".

## Steps

1. Extend `team.graphql` with the seat-usage read; regenerate types.
2. Render "X of Y seats" in the TeamPanel header; every-role visible (matches `RelCanView` gating).
3. When `used >= limit` (limited plans), disable the Invite button with the same upgrade CTA the dialog shows, so admins see the wall before the click.
4. i18n: all new strings through `useTranslations()` (`team.*` keys, en + zh).

## Files

- `dashboard/src/features/team/api/team.graphql`
- `dashboard/src/features/team/components/team-panel.tsx`
- `dashboard/src/features/team/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] Hobby single-member workspace shows "1 of 1 seats" and a disabled Invite button with the plan CTA.
- [ ] Unlimited-plan workspace shows the plain member count, Invite enabled.
- [ ] Vitest covers both states.

## Out of scope

- Any layout move (t006).
