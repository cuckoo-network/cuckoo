---
id: w1/m44/t008
title: Render parity check across REST/GraphQL/MCP/UI
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m44/t004, w1/m44/t006, w1/m44/t007]
---

## Objective

Standing closing task: verify every change this milestone shipped is consistent across all the surfaces it touches — REST, GraphQL, MCP (same fields, semantics, error shapes; docs/ADR006-bex-api.md) and the dashboard — and compare against Render's live behavior, flagging any drift as follow-up work rather than silently diverging.

## Context

- New/changed surface: seat usage read (t001/t002), resend-invite verb (t003), token acceptance (t004), `mfaEnabled` (t005), team-panel placement (t006), audit rows (t007).
- Evidence baseline: `docs/render-artifacts/team-members.graphql`, `docs/render-artifacts/owners-api.md`, the live Render workspace-settings capture from the 2026-07-16 investigation.
- Update `docs/ADR018-render-parity.md`'s members row and `docs/ADR024-members.md`'s Known-divergence section to reflect what closed (token links, MFA visibility) and what deliberately remains (avatar, name trait).

## Steps

1. Cross-surface diff each new verb/field (parity test in `members_surface_parity_test.go` is the existing pattern — extend it).
2. Compare wire shapes against the Render artifacts; capture fresh evidence if a shape was guessed.
3. Sweep ADR018 members row + ADR024 divergences; file follow-up notes for any drift found, don't fix inline.

## Files

- `lego/backend/internal/members/members_surface_parity_test.go`
- `docs/ADR018-render-parity.md`, `docs/ADR024-members.md`

## Acceptance criteria

- [ ] Parity test asserts the new fields/verbs are shape-identical across REST/GraphQL/MCP.
- [ ] ADR018 + ADR024 updated; any residual drift filed as inbox notes.

## Out of scope

- Fixing drift beyond this milestone's features.
