---
id: w1/m44/t010
title: Test coverage for the shipped behavior
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m44/t008]
---

## Objective

Standing closing task: meaningful tests for what this milestone shipped — real behavior and failure modes, no tautological or snapshot-everything tests.

## Context

- Highest-value cases: token redemption (cross-email join, single-use, expiry, plan-guard refusal — real-Postgres via `BEX_TEST_DB_URI` where store logic is involved); resend refreshing expiry while keeping token stable; seat-usage formula counting pending invites; MFA-field degradation with the lookup unwired; audit rows per mutation; dashboard seat states + relocated panel (Vitest).
- Backend integration tests are not skipped in CI (`.github/workflows/backend-test.yml`); dashboard tests CI-enforced (`dashboard-test.yml`).

## Steps

1. Backend: unit + real-Postgres integration coverage for the store paths; cross-surface parity already covered by t008's test.
2. Dashboard: Vitest for seat display, resend action, invite-redeem hook, placement.

## Files

- `lego/backend/internal/members/*_test.go`, `internal/store/*_pg_test.go`
- `dashboard/src/features/team/**/__tests__/`, `dashboard/src/routes/__tests__/`

## Acceptance criteria

- [ ] `cd lego/backend && go test ./...` and `cd dashboard && yarn test` green with the new cases.
- [ ] Every acceptance criterion from t001–t007 has at least one asserting test or a recorded manual verification.

## Out of scope

- Coverage-number chasing.
