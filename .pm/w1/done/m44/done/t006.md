---
id: w1/m44/t006
title: Mount team management under `/workspace/settings`
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m44/t002, w1/m44/t003, w1/m44/t005]
---

## Objective

Move the Team panel to the workspace settings page, where Render puts it. Members are workspace-scoped objects (docs/ADR024-members.md); their management living on the *account* settings page (`/settings`) was w4/m12's placement, ruled "IA choice, not a gap" by w5/m36 — this task deliberately revisits that ruling per user direction (2026-07-16 investigation), now that `/workspace/settings` exists and the invite dialog already deep-links into it (`?plan=change`).

## Context

- `dashboard/src/features/auth/pages/settings-page/index.tsx:66` — current mount (`<TeamPanel />` between the account cards).
- `dashboard/src/routes/workspace.settings.tsx` — target page (WorkspaceDetailsCard + DeleteWorkspaceCard, `max-w-2xl`); TeamPanel is workspace-switcher-aware already (`useTeam` reads the current workspace).
- The `?plan=change` CTA (`invite-member-dialog.tsx:73`) becomes an on-page anchor rather than a cross-page jump — keep it working either way.
- Decide move vs mirror: a straight move matches Render; if anything still on `/settings` links to "Team", update it. Check `docs/render-artifacts/dashboard-walk/workspace.md:11,14,21` and amend the placement note so the walk doc stays truthful.

## Steps

1. Mount `<TeamPanel />` on `/workspace/settings` between the details card and the danger zone; remove it from `/settings`.
2. Re-test the blocked-invite CTA flow (now same-page): dialog → plan-change dialog opens.
3. Update `docs/render-artifacts/dashboard-walk/workspace.md`'s Team/placement rows (the "placement differs" caveat dissolves).
4. Sweep tests that assert the old mount (`settings-page` tests, `workspace.settings` route tests).

## Files

- `dashboard/src/features/auth/pages/settings-page/index.tsx`
- `dashboard/src/routes/workspace.settings.tsx`
- `docs/render-artifacts/dashboard-walk/workspace.md`
- affected `__tests__`

## Acceptance criteria

- [ ] Team management (members, invites, seat display, resend/revoke) renders and functions on `/workspace/settings`; gone from `/settings`.
- [ ] Blocked-invite CTA still opens the change-plan dialog.
- [ ] `yarn typecheck && yarn lint && yarn test` green.

## Out of scope

- Rebuilding the rest of Render's workspace-settings sections (Email/Avatar/Build Pipeline/etc. — separate features, separately triaged).
