---
id: w1/m51/t002
title: Resolver fix — TraefikRouterNames includes the TLS sibling names (one fix point)
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m51/t001]
---

## Objective

Make `core.TraefikRouterNames`/`TraefikRouterNamesForIngress` return the complete router set per t001's rule — the base name plus its entrypoint-prefixed TLS sibling(s) — so every consumer (bandwidth chart, `monthToDateBandwidth`, the http health spec, the usage rollup) starts counting HTTPS bytes through the one shared resolver.

## Context

- `lego/backend/internal/core/traefik.go:103` builds `<ns>-<name>-<host>-<path>@kubernetes` only. Consumers flow through `TraefikRouterNames` → `egressquery.App`'s `router=~"^(…)$"` matcher (`TraefikRouterMatcher`), so one fix point covers chart + month + health + billing.
- Keep the cross-Ingress ambiguity guard coherent: the collision check compares normalized keys across Ingresses — sibling prefixes are per-key derivations, so derive siblings AFTER the collision-hash step (a hashed key gets the same prefix treatment).
- The health spec's `Matchers`, `resets` term, and `Increase` inherit the wider matcher automatically — note that the http health product now also sees websecure resets (stricter, correct: those are the counters being billed).
- Existing tests: `core`'s traefik tests + `internal/metrics` tests pin the exact matcher strings (e.g. `router=~"^(default-web-web-onbex-co@kubernetes)$"`) — update them to the sibling-inclusive form.

## Steps

1. Extend `TraefikRouterNamesForIngress` to emit the sibling name(s) per t001's rule (deterministic order, deduped); land the rule as the function's doc comment.
2. Sweep the pinned matcher strings in backend tests; update `egressquery` fixtures where matchers appear.
3. `go build ./... && go test ./...` in `lego/backend`; `make lint-backend`.

## Files

- `lego/backend/internal/core/traefik.go` (+ its tests)
- `lego/backend/internal/metrics/*_test.go`, `lego/backend/internal/egressquery/*_test.go`, `lego/backend/internal/usage/*_test.go` (pinned matcher strings)

## Acceptance criteria

- [ ] For a standard operator-owned Ingress the resolver returns both the base and `websecure-` names (and any other t001-ruled variants), anchored-matcher tested.
- [ ] The ambiguity guard still rejects the cross-Ingress collision case.
- [ ] Full backend suite green.

## Out of scope

- Rollup gating (t003); prod verification (t004).
