---
id: w1/m51/t006
title: Simplify — /simplify over the changed code
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m51/t005]
---

## Objective

Run `/simplify` over the milestone's changed code (resolver, rollup gating) — reuse/simplification/efficiency, behavior-preserving; file residuals as an inbox note.

## Context

- Candidate: the skip-vs-defer split may duplicate the m50 best-effort health loop's shape — a shared per-source health evaluation helper in egressquery could serve both.

## Steps

1. `/simplify` scoped to the diff; apply accepted findings; residuals → inbox note.

## Files

- `lego/backend/internal/{core,usage,egressquery}/`

## Acceptance criteria

- [ ] Pass ran; suites green; residuals filed, not dropped.

## Out of scope

- Bug hunting.
