---
id: w1/m51/t007
title: Test coverage — resolver siblings, per-source rollup fixtures
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m51/t005]
---

## Objective

Meaningful coverage for both behaviors: the resolver's sibling emission (incl. the collision-hash interaction) and the rollup's skip/defer/zero trichotomy — asserting failure modes, not snapshots.

## Context

- t002/t003 seed the core cases; this task closes gaps: collision-hashed key + sibling prefix, Ingress with multiple hosts, static-site (no direct source) with siblings, matcher round-trip through `egressquery.App` health+increase queries, and the rollup trichotomy against a fake Prometheus (partial-health, transport-error, all-unhealthy, all-healthy).
- The renamed/rewritten `TestQueryEgressBytesRejectsPartialSourceHealth` successor must FAIL if someone reverts to all-or-nothing deferral.

## Steps

1. Fill the matrix above where t002/t003 didn't.
2. `go test ./...` green in `lego/backend`.

## Files

- `lego/backend/internal/core/*_test.go`, `internal/usage/service_test.go`, `internal/egressquery/*_test.go`, `internal/metrics/*_test.go`

## Acceptance criteria

- [ ] Every matrix cell has an assertion; reverting either fix fails a named test.
- [ ] Full backend suite green.

## Out of scope

- E2E (t004 covers live).
