---
id: w1/m32/t001
title: "Store: environments table + nullable environment_id FK on apps"
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Add the control-plane Postgres schema for named environments, mirroring `0014_projects.up.sql`'s shape exactly: a workspace-scoped label table, with a nullable FK column on `apps` for opt-in membership (not a join table).

## Files

- `lego/backend/internal/store/migrations/0019_environments.up.sql` / `.down.sql`
- `lego/backend/internal/store/environments.go` (`Environment` struct + `PGStore` methods: Create/Get/List/Rename/Delete/SetEnvironmentServices/ListEnvironmentServices)
- `lego/backend/internal/id/id.go` (new `id.Environment` kind, prefix `env`)

## Acceptance criteria

- [x] Migration applies cleanly against a fresh Postgres and is idempotent on re-run.
- [x] `environment_id` FK `ON DELETE SET NULL`; `environments.project_id` FK `ON DELETE CASCADE`.
- [x] `SetEnvironmentServices` also stamps `apps.project_id` (assignment auto-joins the project).
- [x] `id.Environment` registered, guard test (`TestRegistryContract`/`TestNewIsWellFormedAndDNSSafe`) passes.
