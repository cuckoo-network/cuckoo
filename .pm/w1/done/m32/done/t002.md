---
id: w1/m32/t002
title: "Core verbs: internal/environments service"
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m32/t001]
---

## Objective

Implement the `environments.Service` verbs (List/Get/Create/Rename/Delete/SetServices), mirroring `internal/projects.Service`'s shape, with proper `s.Authorize`-first authz (the two-gate `requireProject`/`requireEnvironment` pattern) so the feature is covered by `TestAuthzGuardsEveryVerb`.

## Files

- `lego/backend/internal/environments/service.go`

## Acceptance criteria

- [x] Every verb starts with `s.Authorize`, satisfying the reflection sweep.
- [x] `requireProject`/`requireEnvironment` fetch-then-`AuthorizeOn` resolves the actual tenant, allowing cross-workspace access the same way `GetApp` does for App CRs.
- [x] Store-unavailable and not-found/conflict error mapping match `internal/projects`' conventions.
