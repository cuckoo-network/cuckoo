---
id: w1/m32/t005
title: "Wire into the composition root + authz sweep"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m32/t003, w1/m32/t004]
---

## Objective

Register `environments.Service` into `internal/api.Server`/`Deps`/`NewServer`/`features()` and `cmd/api/main.go`'s `BEX_CP_DB_URI` wiring block, and add it to `internal/api/server_test.go`'s `sweepableServices` so `TestAuthzGuardsEveryVerb`/`TestAuditCoversEveryWriteVerbExactlyOnce`/`TestSurfaceParityAndWiring` actually cover it.

## Files

- `lego/backend/internal/api/server.go`
- `lego/backend/cmd/api/main.go`
- `lego/backend/internal/api/server_test.go`

## Acceptance criteria

- [x] `go build ./...` clean.
- [x] `TestAuthzGuardsEveryVerb`, `TestAuditCoversEveryWriteVerbExactlyOnce`, `TestSurfaceParityAndWiring` all pass with environments included.
