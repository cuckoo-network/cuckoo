---
id: w1/m32/t003
title: "REST/GraphQL CRUD surface"
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m32/t002]
---

## Objective

Mirror `internal/projects`' REST/GraphQL fragments exactly for environments: `GET/POST /v1/environments`, `GET/PATCH/DELETE /v1/environments/{id}`, `PUT /v1/environments/{id}/service-links`; GraphQL `environments`/`environment` queries + CRUD/set-services mutations.

## Files

- `lego/backend/internal/environments/rest.go`
- `lego/backend/internal/environments/graphql.go`

## Acceptance criteria

- [x] `?projectId=` required on the list route (400 if missing), matching Projects' required `?ownerId=`.
- [x] `writeErr` local wrapper maps `ErrEnvironmentsUnavailable` to 503 (fixing a gap `core.WriteErr` can't close for a package-local sentinel).
- [x] GraphQL `Environment` type name has no collision with the pre-existing, unrelated `EnvGroup` type.
