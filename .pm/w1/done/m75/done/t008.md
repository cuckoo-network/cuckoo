---
id: w1/m75/t008
title: Test coverage — meaningful tests for the shipped behavior
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m75/t006]
---

## Objective

Confirm every fix in this milestone is held by a test that asserts real behavior and failure modes — each regression test must fail on the pre-fix code — and close any coverage gap the per-task tests left (e.g. the predeploy naming audit from t001, the byte-identical guarantee for already-covered MCP adapters from t005). Never game coverage with trivial, tautological, or snapshot-everything tests.

## Context

- t001: >63-char name agreement (both modules) + Cancel path.
- t002: unset `BEX_RUNTIME` ⇒ `ModeKubernetes`.
- t003: shared default limiter instance / shedding.
- t004: patch-shaped writes + concurrent-modification semantics.
- t005: code survives through a previously-uncovered tool; parity guards green.

## Steps

1. Revert-check each fix (or use the recorded red runs) to confirm each test fails pre-fix.
2. Fill any gap found; run `make test` (operator), `go test ./...` (backend), `make lint`.

## Files

- test files touched by t001–t005

## Acceptance criteria

- [ ] Each of the five fixes has a test proven red on pre-fix code.
- [ ] All suites + lint green.

## Out of scope

- Coverage for code the milestone didn't change.
