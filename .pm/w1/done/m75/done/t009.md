---
id: w1/m75/t009
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m75/t008]
---

## Objective

When the milestone's other tasks are all complete and its definition of done actually holds, close the milestone.

## Context

- The DoD is in `w1/m75/README.md` — five fixes each held by a red-then-green regression test, all suites and lint green, parity guards untouched.

## Steps

1. Verify the DoD holds (not merely that code was written).
2. Set every remaining task's `status: done`; move each `tNNN.md` to `w1/m75/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w1/m75/README.md`.
4. Move the whole `w1/m75/` directory to `w1/done/m75/` and check `- [x]` in `w1/README.md`.

## Files

- `.pm/w1/m75/`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Milestone lives at `w1/done/m75/` with all three status locations in sync; no tombstone left at the old path.

## Out of scope

- Closing before the DoD is real.
