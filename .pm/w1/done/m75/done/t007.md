---
id: w1/m75/t007
title: Simplify — run /simplify over the changed code
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m75/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving) and apply the cleanups.

## Context

- Scope: the diffs from t001–t005 across `lego/backend/internal/{deploys,keyvalue,sshgateway/agentcred,core}`, `lego/operator/{cmd/manager,internal/build}`, and any `lego/types` addition.

## Steps

1. `/simplify` over the milestone's diff.
2. Apply accepted findings; keep behavior identical (suites stay green).

## Files

- as surfaced by the review

## Acceptance criteria

- [ ] /simplify run recorded; accepted findings applied; all suites still green.

## Out of scope

- Bug hunting (that is /code-review's job) and the survey's larger refactor themes.
