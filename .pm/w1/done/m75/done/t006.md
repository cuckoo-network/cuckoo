---
id: w1/m75/t006
title: Render parity — verify surfaces stayed consistent
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m75/t001, w1/m75/t002, w1/m75/t003, w1/m75/t004, w1/m75/t005]
---

## Objective

Check that the milestone's changes are consistent across every surface they expose — REST, GraphQL, and MCP in `lego/backend/` (same fields, semantics, error shapes; bex-api is Render-compatible, `docs/ADR006-bex-api.md`) and the `dashboard/` UI — and compare against equivalent render.com behavior, flagging any drift as follow-up work rather than silently diverging.

## Context

- t004 changes setter write semantics reachable from all three API surfaces; verify REST/GraphQL/MCP still produce identical CR results for the same KeyValue mutation (the m71 pattern: assert MCP and REST produce identical specs).
- t005 changes MCP error payloads; verify the code vocabulary matches what REST `params` and GraphQL `extensions` emit for the same failures, and that Render's REST error dialect is untouched.
- t001–t003 are operator/gateway-internal (no surface change) — confirm and note.

## Steps

1. For a KeyValue project/env mutation, assert REST, GraphQL, and MCP produce the same stored result.
2. For a representative coded failure, compare the code across the three surfaces' error shapes.
3. Record any intentional divergence from render.com behavior as a follow-up inbox note.

## Files

- `lego/backend/internal/keyvalue/`, `lego/backend/internal/core/`, relevant surface tests

## Acceptance criteria

- [ ] Cross-surface consistency asserted or existing assertions confirmed green.
- [ ] Any drift filed as a follow-up note, not silently shipped.

## Out of scope

- New surface features.
