# w1/039 — ADR038: consolidate cron-job design into a dedicated ADR — DONE (2026-07-18)

Authored `docs/ADR038-cron-jobs.md`, a dedicated ADR for the `cron_job` service type. It consolidates the cron-job design that previously lived spread across three places:

- **ADR006** (bex-api) — the REST/GraphQL/MCP surface
- **ADR018** (parity ledger) — the parity row
- **docs/render-artifacts/cron-runs.md** — the pinned Render run contract

Covers: `spec.schedule` → `batch/v1` CronJob (`ForbidConcurrent` = Render's at-most-one-active), `spec.command` entrypoint override, manual runs via a `spec.runAt` one-off Job, cancel via `spec.cancelRun` foreground-delete, durable `status.runs[]` history (≤10, survives Job GC), the `crr-…` run-id derivation, the `lastSuccessfulRunAt` service field, and the trigger/list/get/cancel verb family with the Render-vs-bex-extension divergences (first-class history, terminal-cancel 409, omitted actor fields).

Also: added the entry to the CLAUDE.md docs index and discoverability backlinks from ADR006, ADR018, and cron-runs.md → ADR038. Ran `npx prettier@3.4.2` (clean).

Docs-only, sub-hour → recorded as an inbox note, not a milestone. The cron-job **feature** itself already shipped and stays done — w1/m15 (type + CronJob reconcile, 2026-07-09) and w2/m36 (first-class run history + trigger/cancel). Neither milestone was reopened.
