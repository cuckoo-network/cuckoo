---
id: w1/m61/t007
title: "Test coverage: purge retry, sandbox teardown, Stripe cancel, FK cascade"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m61/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — asserting real teardown outcomes and failure modes, not implementation details. The headline scenario is the one that motivated the milestone: a transient purger failure followed by a retry must leave zero orphans of any class.

## Context

- t001–t004 each carry their own unit tests; this task covers the cross-cutting scenarios those miss:
  - end-to-end delete over the full wired purger set (all phases, correct order) with one purger failing once — retry completes, nothing orphaned in any class;
  - the projector-resurrection hazard: App CRs deleted post-cascade never reappear;
  - ordering pin: pre-cascade purgers observe the tenant row (and `sandbox_tenant_keys` / billing lifecycle rows) still present.
- Existing patterns: `internal/workspaces/workspaces_test.go`, `internal/apps/purger_test.go`, the `BEX_TEST_DB_URI`-gated store tests (run for real in CI).

## Steps

1. Write the cross-cutting delete-retry test over the full purger set.
2. Pin the phase ordering (a test that fails if a pre-cascade purger is moved post-cascade).
3. Run the backend suite + `make lint-backend`.

## Acceptance criteria

- [ ] The retry-after-partial-failure scenario is covered end-to-end and fails if the t001 ordering regresses.
- [ ] No tautological/snapshot tests; each asserts an observable teardown outcome.
- [ ] Backend suite + lint green.

## Out of scope

- Live-prod verification (closeout's DoD check covers what needs live evidence).
