---
id: w1/m61/t006
title: "Simplify: /simplify over the code this milestone changed"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m61/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (workspaces delete ordering, the two new purgers, the migration, wiring) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Likely candidates: the purger-phase split in `workspaces.Service.Delete` (keep it one obvious mechanism, not two parallel lists with drift potential); the two new purgers vs the existing `core.Base` purge helpers (`internal/core/purge.go`).

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply the accepted findings; keep behavior identical (suites stay green).

## Files

- Whatever the milestone's diff touched.

## Acceptance criteria

- [ ] `/simplify` run; accepted findings applied; backend suite + lint green.

## Out of scope

- Bug-hunting (that's `/code-review`, and t007's tests).
