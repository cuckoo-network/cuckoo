---
id: w1/m61/t005
title: "Render parity: delete-verb behavior consistent across surfaces"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m61/t002, w1/m61/t003, w1/m61/t004]
---

## Objective

Verify the workspace-delete verb stays consistent across every surface it exposes after the teardown changes: same confirmation contract, same error shapes, same retry semantics on GraphQL, any REST/MCP exposure, and the dashboard danger-zone card. Compare against Render's live workspace-deletion behavior and flag drift as follow-up rather than silently diverging.

## Context

- `docs/render-artifacts/workspace-lifecycle.md` — the captured Render contract (`sudo delete workspace <name>` guard, Hobby-only delete).
- `dashboard/` danger zone: `delete-workspace-card.tsx` renders the confirmation phrase from `DeleteConfirmation`; the retry-semantics change (t001) may alter what a failed delete looks like to the UI.
- `docs/ADR018-render-parity.md` — update the relevant row if evidence changes.

## Steps

1. Sweep the delete verb's registrations (GraphQL, REST if present, MCP if present) for changed fields/error shapes; confirm the confirmation contract is unchanged.
2. Walk the dashboard delete flow against a dev-1 stack: happy path + a simulated purger failure (the user-visible error and successful retry).
3. Update ADR018 / render-artifacts notes if behavior or evidence changed; file follow-ups for any drift.

## Files

- `lego/backend/internal/workspaces/graphql.go` (+ any REST/MCP fragments)
- `dashboard/src/**/delete-workspace-card.tsx` (verification only)
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] All exposed surfaces present identical delete semantics and error shapes.
- [ ] Dashboard delete flow verified live on dev-1, including the failure/retry path.
- [ ] Any Render drift is documented as follow-up, not silent.

## Out of scope

- New surface exposure (e.g. adding a REST delete verb if none exists today).
