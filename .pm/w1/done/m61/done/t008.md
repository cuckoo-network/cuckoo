---
id: w1/m61/t008
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m61/t006, w1/m61/t007]
---

## Objective

Close the milestone once every other task is complete **and the definition of done actually holds** (retryable purge proven by test, sandbox key cascade + sandbox stop proven, Stripe cancel proven, suites green).

## Context

- One residual to check before closing: do orphaned `sandbox_tenant_keys` rows or active Stripe subscriptions for already-deleted workspaces exist on prod? Migration 0056 clears the key rows; pre-existing Stripe orphans (t004's out-of-scope note) need a one-off ops sweep — file an inbox note if any exist.

## Steps

1. Verify the milestone DoD against reality (tests, CI, and the prod-orphan check above).
2. Set every remaining task `status: done`; move each `tNNN.md` to `w1/m61/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in this README.
4. Move `w1/m61/` to `w1/done/m61/`; check `- [x]` **m61** in `w1/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m61/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] DoD verified before any files move.
- [ ] Status synced in all three places (task frontmatter, milestone README, workstream README).
- [ ] `w1/m61/` lives at `w1/done/m61/` with no tombstone left behind.

## Out of scope

- Shipping (`/ship` is the user's call).
