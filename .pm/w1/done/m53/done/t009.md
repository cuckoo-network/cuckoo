---
id: w1/m53/t009
title: "Simplify the code this milestone changed"
worker: worker1
status: done
estimate: 30m
depends_on: [t008]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency;
behavior-preserving). Fold the SSRF dial-guard, the `Image` validator, and the nonce store into
existing shared helpers rather than duplicating.

## Context

- Likely reuse points: `lego/types/netutil.SafeDialContext` (already shared), the existing store
  validator helpers (`ValidRepo`/`ValidGitRef`/`ValidRootDir`), and the store's existing
  single-use/expiry patterns for the nonce table.

## Steps

1. `/simplify` over the milestone diff.
2. Apply behavior-preserving reuse/simplification; keep tests green.

## Files

- The files touched by t001–t007.

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly declined with reason.
- [ ] `go build ./... && go test ./...` (backend) + `make test` (operator) green.

## Out of scope

- New behavior or new findings (those are separate tasks).
