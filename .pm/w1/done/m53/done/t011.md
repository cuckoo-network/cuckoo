---
id: w1/m53/t011
title: "Closeout"
worker: worker1
status: done
estimate: 15m
depends_on: [t010]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

- Run only when every other task is complete AND the DoD in the milestone README is real (fail-closed
  CP API in the deployment; SSRF fixes test-proven; authz sweep covers all services; V1 determined +
  documented; LOWs fixed or deferred-with-rationale).

## Steps

1. Verify the milestone DoD is genuinely met (not just code written).
2. Update `docs/ADR028-security-review.md` (or a follow-up register) noting these findings + their
   resolution, so the audit record stays current.
3. Set every task `status: done`, move each `tNNN.md` to `w1/m53/done/`, mark rows `— **DONE**` and
   `**Status:** done` in the milestone README, move the whole `w1/m53/` to `w1/done/m53/`, check
   `- [x] **m53**` in `w1/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m53/**`, `.pm/w1/README.md`, `docs/ADR028-security-review.md`

## Acceptance criteria

- [ ] DoD verified real; audit record updated.
- [ ] Milestone moved to `w1/done/m53/`; workstream checkbox `[x]`.

## Out of scope

- Any remaining deferred LOW work is filed as its own inbox note, not left implicit.
