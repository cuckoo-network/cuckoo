---
id: w1/m53/t008
title: "Render parity: Image validation + deploy-hook across REST/GraphQL/MCP"
worker: worker1
status: done
estimate: 30m
depends_on: [t002, t005]
---

## Objective

The two surface-touching fixes in this milestone must stay consistent across every surface they
expose and match Render's behavior.

## Context

- t002 adds `Image` reference validation at the create/set path — its rejection error must be
  identical across REST, GraphQL, and MCP (same field, same error shape), and should mirror how
  Render rejects a bad image reference.
- t005/L10 may change deploy-hook `?imgURL=` behavior — verify the deploy-hook contract is presented
  consistently and matches Render's documented deploy-hook semantics.
- bex-api is Render-compatible per `docs/ADR006-bex-api.md`; the dashboard (`dashboard/`) consumes
  the GraphQL surface.

## Steps

1. Confirm the `Image` validator's error surfaces identically on REST, GraphQL, and MCP create/set
   verbs; fix any drift.
2. Confirm the deploy-hook change (if any) is reflected consistently and doesn't diverge from Render;
   flag drift as follow-up rather than silently diverging.
3. Check the dashboard create flow still surfaces the new validation error sanely.

## Files

- `lego/backend/internal/apps/{rest.go,graphql.go,mcp.go}`, `lego/backend/internal/deploys/*`
- `docs/ADR018-render-parity.md` (note any new/closed row)

## Acceptance criteria

- [ ] `Image` validation error is byte-consistent across the three surfaces.
- [ ] Deploy-hook behavior consistent across surfaces + checked against Render; drift filed if any.

## Out of scope

- Non-surface fixes (M3, M2, operator/SSH LOWs) have no Render-parity dimension.
