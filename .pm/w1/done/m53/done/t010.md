---
id: w1/m53/t010
title: "Test coverage for the shipped fixes"
worker: worker1
status: done
estimate: 1h
depends_on: [t008]
---

## Objective

Add meaningful tests asserting the new failure modes this milestone introduced — real behavior, not
coverage-gaming.

## Context

Each fix has a concrete negative case to pin:

- M3: bex-api aborts (or `:8091` is unreachable) with `BEX_CP_DB_URI` set + `BEX_CP_TOKEN` empty;
  401 without the bearer.
- M1: webhook rejects `zot.bex-registry.svc:5000.attacker.tld/...`; operator httpclient refuses a
  private/metadata dial; `Image` validator rejects malformed refs (adversarial table).
- M2: the divergence guard fails when a wired service is missing from the sweep.
- L2: unbound-key delete refused cross-workspace. L6: hashed-token redemption round-trips. L1:
  multi-member fail-open guard fires. L7: cross-replica ticket reuse rejected.

## Steps

1. Write the negative-path tests above, co-located with each package's existing suite.
2. Ensure they fail against the pre-fix code (spot-check) and pass after.

## Files

- `*_test.go` alongside each touched package (webhook, httpclient, store, api, apikeys, sshgateway).

## Acceptance criteria

- [ ] Every fix has a test asserting its new failure mode.
- [ ] Backend + operator suites green in CI.

## Out of scope

- E2E/live-cluster verification (covered by existing verify scripts where applicable).
