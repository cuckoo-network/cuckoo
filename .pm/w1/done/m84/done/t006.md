---
id: w1/m84/t006
title: Namespace quota re-derivation for disks
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m84/t001]
---

## Objective

Make workspace quotas admit disks: free/hobby `requests.storage` 20Gi → 120Gi and `persistentvolumeclaims` 4 → 8; paid 5Ti/200 unchanged (ADR082 D10).

## Context

- `lego/backend/internal/store/namespaces.go:408-460 quotaForPlan` — the sizing comment derives from datastore counts only; rework the derivation note to include the disk term so the numbers stay explainable.
- Quota-blocked create/grow must surface `StorageBlockedByQuota` and self-clear (operator side landed in m83 t005; here ensure the API-side create doesn't mask it).

## Steps

1. Update the derivation + values + comment; adjust the quota tests.

## Files

- `lego/backend/internal/store/namespaces.go` (+ tests)

## Acceptance criteria

- [ ] A hobby workspace fits one default 10 GB disk beside its datastore floors; the quota test table matches ADR082 D10.

## Out of scope

- Per-plan disk-count product limits (none — Render has none).
