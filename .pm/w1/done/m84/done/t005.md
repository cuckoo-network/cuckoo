---
id: w1/m84/t005
title: pricing.yaml disk rate + Stripe disk_gb_hours
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m84/t004]
---

## Objective

Price the meter: $0.175/GB-month provisioned (Render $0.25 × 0.70), estimate + Stripe catalog.

## Context

- `lego/backend/internal/pricing/pricing.yaml` gains the `disk:` block from ADR082 D8 (`usdPerGBSecond: 0.000000066590563165905631659056`, comment included); `tiers.yaml` stays price-free (ADR030 invariant).
- `pricing.go:70-95 BillableMeterNames()` gains `disk_gb_hours` (GB-seconds rebase like `storage_gb_hours`); `internal/billing/stripe.go:368-391` maps the kind; `scripts/stripe-billing-setup.py` provisions Meter + Price.
- ADR030's discount table gets a disk row (docs sync can ride m86 t005 — here just the code).

## Steps

1. Sheet + estimate wiring; Stripe meter name + mapping + setup script.

## Files

- `lego/backend/internal/pricing/pricing.yaml`, `pricing.go`
- `lego/backend/internal/billing/stripe.go`, `scripts/stripe-billing-setup.py`

## Acceptance criteria

- [ ] Estimate fixture: a 10 GB disk for a 730 h month prices at $1.75.
- [ ] Stripe catalog test/dry-run shows `disk_gb_hours` at $0.0002397/GB-hour.

## Out of scope

- ADR030/docs edits (m86 t005).
