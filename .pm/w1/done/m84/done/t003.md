---
id: w1/m84/t003
title: GraphQL + MCP mirrors + AppView.Disk
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m84/t002]
---

## Objective

The same disk verbs and view on the other two surfaces, one core (ADR006 one-service/three-fragments).

## Context

- `AppView` (`internal/apps/service.go:306`) gains `Disk *DiskView`.
- GraphQL: field map `internal/apps/graphql.go:326-434`, mutations at `:1133-1302` — `addDisk` / `updateDisk` / `deleteDisk`.
- MCP: `add_disk` / `update_disk` / `delete_disk` tools; `internal/api/mcp_parity.go` divergence lists updated.
- Error identity across surfaces (409/400 codes byte-identical) is what t007 verifies — build for it here.

## Steps

1. View + fragments, delegating to the t002 service verbs.
2. Parity/registry list updates.

## Files

- `lego/backend/internal/apps/graphql.go`, `mcp.go`, `service.go`
- `lego/backend/internal/api/mcp_parity.go`

## Acceptance criteria

- [ ] Cross-surface test: identical create/refuse round-trips on REST vs GraphQL vs MCP.

## Out of scope

- Dashboard UI (m86).
