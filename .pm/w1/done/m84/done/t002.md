---
id: w1/m84/t002
title: REST /v1/disks CRUD + scope matrix + OpenAPI route pin
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m84/t001]
---

## Objective

Render-shaped disk REST surface: `GET/POST /v1/disks`, `GET/PATCH/DELETE /v1/disks/{diskId}` (ADR082 D6; snapshots routes land in m85).

## Context

- New `lego/backend/internal/apps/disks.go` beside `rest.go` (route mounting :680-1110). Render body/response shapes per [docs/render-artifacts/disks.md](../../../docs/render-artifacts/disks.md) § API.
- Semantics: POST requires `serviceId`, 409 on second disk, 400/402 on free tier or ineligible type (mirror the CEL refusals with named errors), triggers a deploy; PATCH grow-only (400 on shrink), name/mountPath mutable (mountPath change → deploy); DELETE immediate + destructive.
- `internal/api/render_openapi_test.go:443-446` pins `/v1/disks` → 404 — remove the pin, join the route inventory.
- `internal/api/scope_matrix_ops.go`: an `OpClass*` entry per route; verbs authorize via `AuthorizeApp` on the owning service (single-gate seam) so the standing sweep tests cover them.

## Steps

1. Service verbs in `apps/service.go` (+ `disks.go` fragment), store-backed, deploy-triggering.
2. Route mounting + scope matrix + OpenAPI test update.

## Files

- `lego/backend/internal/apps/disks.go`, `service.go`, `rest.go`
- `lego/backend/internal/api/scope_matrix_ops.go`, `render_openapi_test.go`

## Acceptance criteria

- [ ] Shape-pinned tests against Render's disk schemas (ids `^dsk-[0-9a-z]{20}$`).
- [ ] 409 second-disk, 400 shrink, free-tier refusal — all asserted.
- [ ] Authz sweep + OpenAPI tests green.

## Out of scope

- GraphQL/MCP (t003), snapshots (m85).
