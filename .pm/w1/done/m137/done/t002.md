---
id: w1/m137/t002
title: Reconcile node images after partial imports
worker: worker1
status: done
estimate: 50m
depends_on: [w1/m137/t001]
---

## Objective

Replace invocation-local reload intent with reconciliation against the desired image identity on every node.

## Context

- Source: w1/done/081.md and scripts/dev-env.sh agent_build_images / node-load loop. Reload intent currently survives only one invocation.
- w3/m79 owns the separate real-GitHub draft-PR proof; this milestone does not replace that gate.

## Steps

1. Reproduce a rebuild followed by interruption before every node imports it.
2. Import missing or mismatched images and skip images proven current.
3. Keep retry correctness independent of AGENT_RELOAD_IMAGES and source edits.

## Files

- scripts/dev-env.sh
- scripts/dev-env.test.sh

## Acceptance criteria

- [ ] A second invocation repairs a node retaining the old same-named tag.
- [ ] Matching images avoid reimport; a newly added node receives the desired image.

## Out of scope

- Production image distribution, source-freshness policy redesign, registry provisioning, and w3/m79 draft-PR delivery proof.
