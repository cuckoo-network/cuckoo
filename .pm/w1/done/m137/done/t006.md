---
id: w1/m137/t006
title: Test coverage
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m137/t004, w1/m137/t005]
---

## Objective

Add meaningful behavioral regression coverage for interrupted imports and false-success paths.

## Context

- Source: w1/done/081.md and scripts/dev-env.sh agent_build_images / node-load loop. Reload intent currently survives only one invocation.
- w3/m79 owns the separate real-GitHub draft-PR proof; this milestone does not replace that gate.

## Steps

1. Extend scripts/dev-env.test.sh with stateful Docker/containerd fixtures or equivalent focused behavioral tests.
2. Cover interrupted update followed by a new invocation, missing nodes/images, matching identity, inspection failure, import failure, and post-import mismatch.
3. Prove the interrupted-import regression fails against the pre-fix behavior; run shell syntax checks and the relevant script suite.

## Files

- scripts/dev-env.sh
- scripts/dev-env.test.sh

## Acceptance criteria

- [ ] Regression tests fail on the original tag-presence skip and pass with reconciliation.
- [ ] Tests verify observable import/rollout decisions rather than echoing mocked outputs.
- [ ] Relevant checks pass and the live t004 evidence still applies.

## Out of scope

- Production image distribution, source-freshness policy redesign, registry provisioning, and w3/m79 draft-PR delivery proof.
