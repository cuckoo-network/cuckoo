---
id: w1/m52/t005
title: Render parity check
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m52/t004]
---

## Objective

Confirm the milestone changed no REST/GraphQL/MCP contract (it should be purely availability + client-recovery behavior) and that the roll-window UX now matches Render's bar: Render's own dashboard never surfaces its deploys to users as errors.

## Context

- Expected surface deltas: a new `/readyz` on bex-api (infra endpoint, not a Render-compatible API route — confirm it's outside the documented surface and unauthenticated-but-harmless) and a dashboard health server route. Neither should appear in `docs/ADR018-render-parity.md` rows.
- The dashboard's error-state UX (`projects.errorTitle` et al.) is unchanged in copy — only when it appears/recovers changes.

## Steps

1. Diff sweep: no changes under `lego/backend/internal/*/rest.go|graphql.go|mcp.go` that alter fields/semantics/error shapes.
2. Spot-check the official Render CLI against dev-1 (one `services list` + one `deploys list`) to confirm nothing regressed at the wire level.
3. Note in the milestone README that parity holds (or file drift as follow-up).

## Files

- `docs/ADR018-render-parity.md` (only if something actually drifted)

## Acceptance criteria

- [ ] No REST/GraphQL/MCP contract change; CLI spot-check green.
- [ ] Parity note recorded.

## Out of scope

- New parity rows — this milestone adds no Render-visible capability.

## Outcome (2026-07-18)

Zero diffs under `internal/*/{rest,graphql,mcp}.go`. Wire spot-check on the new binary: `/v1/services` + `/graphql` return the identical Render-dialect 401 error shape; `/healthz` unchanged; `/readyz` additive, outside `/v1`, unauthenticated-but-harmless (mirrors /healthz). No ADR018 rows added — no Render-visible capability changed. Roll-window UX now matches Render's bar (their dashboard never surfaces deploys as user errors).
