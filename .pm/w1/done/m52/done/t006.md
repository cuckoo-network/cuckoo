---
id: w1/m52/t006
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m52/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (bex-api shutdown/readiness helper, dashboard Apollo chain + loader-retry hook, health route) — reuse / simplification / efficiency, behavior-preserving.

## Context

- Likely candidates: the t001 drain logic should live once in the shared m30 helper (api + ssh-gateway), not per-binary; the t003 hydration-retry hook should share shape with `useNotFoundRedirect`.

## Steps

1. `/simplify` over the milestone diff; apply what's clearly worth it, file residuals as an inbox note if any.

## Files

- The milestone's diff.

## Acceptance criteria

- [ ] Simplify pass run; applied or residuals filed.

## Out of scope

- Bug hunting (that's /code-review's job).

## Outcome (2026-07-18)

Four-angle review (reuse / simplification / efficiency / altitude) over the milestone diff. Applied: shared `SAME_LOCATION_REPLACE` const in `use-loader-error-retry`; SSR Apollo factory builds the link array once and hoists the stateless retry link to module level (one instance, not one per SSR request); `health.ts` derives the upstream base URL once at module load; cross-linked the `DashboardDocumentTitle` cleanup and `useLoaderErrorRetry` comments (two halves of one recovery loop). Reuse + altitude came back clean — per-shell hook mounting matches the `useNotFoundRedirect` precedent, `/readyz` outside `internal/api` is deliberate parity hygiene, `internal/serve` is the right leaf. Skipped: deduplicating the one-line shell comments (established per-shell comment style). Residual noted in the milestone README: migrate ssh-gateway's listeners to `serve.UntilShutdown` when its lifecycle is next touched.
