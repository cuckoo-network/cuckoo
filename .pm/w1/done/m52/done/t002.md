---
id: w1/m52/t002
title: "Dashboard roll: readiness gated on upstream reachability + post-roll SSR error page (folds 036)"
worker: worker1
status: done
estimate: 60m
depends_on: []
---

## Objective

A freshly rolled dashboard pod must not serve the "Something went wrong · bex Dashboard" SSR error page (inbox `036`, observed twice on 2026-07-18 after the m50 and m51 rolls), and a recovered page must not keep the stale error `<title>` on the tab. Root cause per 036's hypothesis + the manifest: the readiness probe is `GET /` — which renders fine even when the pod cannot reach api.bex.co yet — so Kubernetes hands traffic to a pod whose SSR data fetches fail.

## Context

- `dashboard/deploy/deployment.yaml`: single replica, readiness + liveness both probe `/`; default rollout strategy for replicas=1 is already surge-then-kill, so the readiness gate is the whole handover decision — it just measures the wrong thing.
- 036 names three candidate fixes: a readiness gate on upstream (bex-api) reachability, an SSR retry, or letting the client error boundary recover without serving the error page. Reproduce first (controlled roll, read the actual SSR error from pod logs), then decide — the decision and evidence go in this task's notes.
- Stale `<title>`: the head/error-title logic is in `dashboard/src/common/lib/document-head/` (`stateTitle`, `titleHead`) + the root error component (`src/common/root-route/`); fix whichever path leaves the error title behind after the client recovers, independent of the main fix chosen.
- SSR reaches bex-api via `VITE_SSR_API_URL` (in-cluster Service DNS); also drain the Node server on SIGTERM (same pattern as t001 — the dashboard pod currently gets no drain at all) if the reproduce shows kills mid-request.

## Steps

1. Reproduce: controlled `kubectl rollout restart deploy/dashboard` while curling full-page loads; capture the SSR error from the new pod's logs.
2. Decide readiness-gate vs SSR-retry vs client-boundary-recovery from the evidence; implement (readiness gate = a `/healthz` server route that checks bex-api reachability, probed by readiness; liveness stays `/`).
3. Fix the stale error `<title>` regardless of the main fix.
4. Re-run the controlled roll: no error page served, title correct.

## Files

- `dashboard/deploy/deployment.yaml`
- `dashboard/src/` server route for the health endpoint (TanStack Start server route, alongside `routes/api.*.tsx`)
- `dashboard/src/common/lib/document-head/` + `dashboard/src/common/root-route/`

## Acceptance criteria

- [ ] A controlled dashboard roll under repeated full-page loads serves zero SSR error pages.
- [ ] A page that did hit an SSR failure recovers with a correct `<title>` (no lingering "Something went wrong").
- [ ] The chosen mechanism + reproduce evidence recorded in the task/milestone notes.

## Out of scope

- Client-side loader/mutation retry behavior (t003).
- Old-asset/new-pod bundle-mismatch handling (036's alternate hypothesis) — if the reproduce shows it's real, file it as its own note.

## Outcome (2026-07-18)

Reproduce (prod, quiet hour): dashboard roll under a ~1 rps loop ⇒ **4× 502 over ~3s exactly when the old pod died** — the Node server exits instantly on SIGTERM while Traefik still routes to it. The 036 SSR-error-page/readiness hypothesis also confirmed from events: fresh pods fail `GET /` readiness probes with context-deadline-exceeded (cold SSR > 1s default timeout), i.e. readiness measured the wrong thing.

Decision (mechanism): (1) `preStop: sleep 10` — the pod leaves endpoints at deletion and keeps serving until SIGTERM; (2) readiness → new `/healthz` server route that LATCHES on first successful reach of bex-api's /healthz (a steady-state bex-api outage degrades to inline error states, never a cluster-wide NotReady cascade); (3) explicit surge-then-kill strategy + minReadySeconds; (4) liveness stays `/` with `timeoutSeconds: 5` (tolerates + warms cold SSR).

Verified on prod by patching the manifest fields the running image supports (preStop/strategy/minReady): rolls killing preStop-equipped pods ⇒ **0× 502 across ~189 loads**. The `/healthz` probe swap ships with the image. Stale error `<title>`: root-caused deeper in t003 (head/meta not recomputed after loader recovery) + `DashboardDocumentTitle` unmount reconciliation for the SSR-orphan case.
