---
id: w1/m78/t003
title: MCP adapter: applyServicePatch fills ServicePatch
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m78/t001]
---

## Objective

Reduce the MCP `update_service` path to tool-args → `ServicePatch` mapping + one `ApplyServicePatch` call, deleting the duplicate `core.PatchOps` table.

## Context

- `lego/backend/internal/apps/mcp.go:739-841` (`updateServiceArgs` + the twin table).
- MCP-only fields stay: `notificationsToSend`, `autoscaling` fill their `ServicePatch` fields; REST simply doesn't.

## Steps

1. Map `updateServiceArgs` onto `ServicePatch`.
2. Delete the local table; call `ApplyServicePatch`.
3. Run `update_service_mcp_test.go` (504 lines) — should pass unchanged.

## Files

- `lego/backend/internal/apps/mcp.go`
- `lego/backend/internal/apps/update_service_mcp_test.go`

## Acceptance criteria

- [ ] Existing MCP update-service tests pass without modification.

## Out of scope

- Adding the MCP-only fields to REST (follow-up decision).
