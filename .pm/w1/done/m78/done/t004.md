---
id: w1/m78/t004
title: Render parity
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m78/t002, w1/m78/t003]
---

## Objective

Verify the refactor preserved surface behavior field-by-field: a multi-field PATCH behaves identically on REST and MCP (same order, same error shapes), matching render.com's `PATCH /v1/services/{id}` semantics, and GraphQL setters (which call the per-field verbs directly) are unaffected. Enumerate the four preserved divergences explicitly and file them as candidate follow-up notes rather than silently keeping them.

## Context

- `docs/ADR006-bex-api.md`; `docs/ADR018-render-parity.md` services row.
- Divergences: `notificationsToSend`/`autoscaling` (MCP-only), `repo/image/imageOwnerId` + maintenance-before-plan reorder (REST-only).

## Steps

1. Cross-surface diff of accepted fields + application order (drive both surfaces against a fake store).
2. Check the Render API reference for the PATCH field set; flag drift as follow-ups.
3. File one note per divergence worth resolving.

## Files

- review-only; notes filed to the board

## Acceptance criteria

- [ ] Parity result written up; divergence follow-ups filed.

## Out of scope

- Fixing divergences inline.
