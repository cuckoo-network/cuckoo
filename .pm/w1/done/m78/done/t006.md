---
id: w1/m78/t006
title: Test coverage
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m78/t004]
---

## Objective

Add the cross-surface tests the old structure made impossible: one test driving the same multi-field patch through REST and MCP and asserting identical resulting spec + application order; a guard asserting every `ServicePatch` field appears in the core table exactly once (so a new field can't be added to the type and forgotten in the table).

## Context

- Existing nets: `patch_pipeline_test.go`, `update_service_mcp_test.go`, `cli_compat_test.go` — keep them; this adds the cross-surface layer they couldn't express.

## Steps

1. Cross-surface equivalence test.
2. Field-completeness guard over `ServicePatch` (reflection-based, mirroring the authz-sweep style in `internal/api/server_test.go`).

## Files

- `lego/backend/internal/apps/*_test.go`

## Acceptance criteria

- [ ] Both new tests in place and meaningful (fail on a deliberately introduced drift); suite green.

## Out of scope

- Coverage-percentage chasing.
