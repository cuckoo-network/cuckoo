---
id: w1/m78/t002
title: REST adapter: patchService fills ServicePatch
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m78/t001]
---

## Objective

Reduce `patchService` to wire-body → `ServicePatch` mapping (via the existing `resolveFields`) + one `ApplyServicePatch` call, deleting the hand-rolled collector and runner.

## Context

- `lego/backend/internal/apps/rest.go:738-812` (`resolveFields`), `:864-942` (collector + runner to delete).
- REST-only behaviors stay: `repo/image/imageOwnerId` handling and the maintenance-before-plan reorder must produce byte-identical responses (the reorder now lives in the core table as data).

## Steps

1. Map `resolveFields` output onto `ServicePatch`.
2. Delete the local collector/runner; call `ApplyServicePatch`.
3. Run the REST patch suites.

## Files

- `lego/backend/internal/apps/rest.go`
- `lego/backend/internal/apps/patch_pipeline_test.go`, `cli_compat_test.go` (should pass unchanged)

## Acceptance criteria

- [ ] Existing REST patch + CLI-compat tests pass without modification.

## Out of scope

- Any wire-shape change.
