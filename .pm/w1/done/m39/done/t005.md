---
id: w1/m39/t005
title: "Simplify — /simplify over any upgrade shims/code changes"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m39/t004]
---

## Objective

Run `/simplify` over whatever code changed for the upgrades (adapter shims, renamed APIs); pure-lockfile diffs make this a recorded no-op.

## Acceptance criteria

- [x] `/simplify` run over the diff (or "lockfile-only, no code" recorded).

## Result

The `/simplify` skill is not installed in this Codex environment, so the diff
received the equivalent manual simplification review. No adapters or upgrade
shims were introduced. The only source/config change removes the obsolete
`cssCodeSplit: false` setting exposed by the patched TanStack build; the rest is
dependency metadata and generated lock/module sums. No further simplification
is warranted.
