---
id: w1/m39/t006
title: "Test coverage — regression tests where an upgrade changed behavior"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m39/t004]
---

## Objective

Where a major upgrade changed observable behavior (serialization, defaults, error text a feature relies on), pin the new behavior with a real test; if no behavior changed, record that explicitly rather than adding filler tests.

## Acceptance criteria

- [x] Behavior-changing upgrades pinned by tests, or an explicit "no observable behavior change" record; suites green.

## Result

No tenant-facing behavior changed, so no filler unit test was added. The only
compatibility adjustment affects Vite's client/SSR manifest handoff, which the
production `yarn build` gate exercises directly; that build passes. Operator
`make test`, backend `go test ./...`, dashboard `yarn typecheck`, `yarn test`
(196 files, 1,165 tests), and `yarn build` are all green.
