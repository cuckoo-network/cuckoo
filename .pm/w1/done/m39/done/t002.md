---
id: w1/m39/t002
title: "Apply the safe batch (patch/minor bumps); suites green"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m39/t001]
---

## Objective

Apply every patch/minor bump from t001's safe set across the Go workspace (`lego/go.work` modules) and `dashboard/` in one reviewed batch; run all three suites (`make test` from `lego/operator/`, `go test ./...` from `lego/backend/`, `yarn test` in `dashboard/`).

## Acceptance criteria

- [x] Safe-set versions applied locally; all suites green; `go.mod`/`yarn.lock` diffs contain only classified bumps.

## Result

Applied `x/crypto` 0.52.0 in backend/operator, `spdystream` 0.5.1 in backend,
and every npm patch/minor listed in `triage.md`. The final operator, backend, and
dashboard gates all pass. Dependabot's live count cannot fall until the changes
are published to the default branch; that explicit confirmation remains t004.

## Out of scope

- Majors (t003).
