---
id: w1/m77/t001
title: Create lego/types/proxyproto and move the parser + tests
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Create `lego/types/proxyproto` (leaf — the parser already has zero bex deps) holding the PROXY v1/v2 parser, and give it the merged conformance suite (union of the backend's 8 tests and the operator's 5, ~170 shared lines dedup'd).

## Context

- Source of truth to move: `lego/backend/internal/proxyproto/proxyproto.go:39-214` (byte-identical to `lego/operator/internal/sniproxy/proxy.go:31-197`).
- Also duplicated: `proxyproto.go:68-75` ↔ `sniproxy/tls.go:26-32` — keep the TLS-record pieces in the operator wrapper (t003), move only what both need.
- Test suites to merge: `lego/backend/internal/proxyproto/proxyproto_test.go` + `lego/operator/internal/sniproxy/proxy_test.go` (~105 differing lines of ~250).
- New Go files carry the Apache-2.0 header from `lego/operator/hack/boilerplate.go.txt`.

## Steps

1. Create `lego/types/proxyproto/` with the parser, exporting what both wrappers need; carry the security comments verbatim.
2. Port the union of both test suites against the new package.
3. `go build ./... && go test ./...` in `lego/types`.

## Files

- `lego/types/proxyproto/proxyproto.go` (new)
- `lego/types/proxyproto/proxyproto_test.go` (new)

## Acceptance criteria

- [ ] The shared package compiles standalone in `lego/types` and its merged suite passes.
- [ ] Every conformance case from both original suites is present.

## Out of scope

- Touching either consumer module (t002/t003).
