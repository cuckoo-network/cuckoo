---
id: w1/m77/t003
title: Retarget operator/internal/sniproxy to the shared package
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m77/t001]
---

## Objective

Point `lego/operator/internal/sniproxy`'s PROXY parsing at `lego/types/proxyproto`, keeping the operator-only TLS pieces (`TLSHandshakeType`/`ReadTLSRecord`/`ExtractSNI` in `sniproxy/tls.go`) where they are so pg-sni-proxy and kv-sni-proxy call sites don't change.

## Context

- Consumers to leave untouched: `lego/operator/cmd/pg-sni-proxy/main.go:410-425`, `lego/operator/cmd/kv-sni-proxy/main.go:279-293`.
- Delete the parser body in `sniproxy/proxy.go:31-197` and the duplicated test cases now owned by the shared suite.

## Steps

1. Delegate `ReadProxySource` (and friends) to `types/proxyproto`; keep the sniproxy API surface identical.
2. Remove duplicated tests; keep sniproxy-specific ones (TLS record, SNI extract).
3. From `lego/operator/`: `make test && make lint`.

## Files

- `lego/operator/internal/sniproxy/proxy.go`
- `lego/operator/internal/sniproxy/proxy_test.go`
- `lego/operator/go.mod` (types dependency already present — verify)

## Acceptance criteria

- [ ] Both SNI proxy binaries build; operator suite + lint green; no `cmd/` call-site diffs.

## Out of scope

- The pg/kv proxy `Accept`/`Splice`/`Bootstrap` consolidation (inbox note `w1/059`).
