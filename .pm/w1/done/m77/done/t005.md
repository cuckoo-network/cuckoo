---
id: w1/m77/t005
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m77/t002, w1/m77/t003]
---

## Objective

Verify the merged conformance suite genuinely is the union (no case dropped in the move) and add what the union still misses — e.g. malformed-header shedding, v1/v2 boundary cases, and a guard that both wrapper packages delegate to the shared parser (so a future re-fork fails a test, not a review).

## Context

- Prior suites: backend 8 cases vs operator 5 — enumerate and diff before/after.

## Steps

1. Cross-check case inventory old→new.
2. Add a delegation guard test per wrapper.

## Files

- `lego/types/proxyproto/proxyproto_test.go`, wrapper test files

## Acceptance criteria

- [ ] Union verified case-by-case; delegation guards in place; suites green.

## Out of scope

- Coverage-percentage chasing.
