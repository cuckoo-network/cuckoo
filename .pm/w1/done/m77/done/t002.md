---
id: w1/m77/t002
title: Retarget backend/internal/proxyproto to the shared package
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m77/t001]
---

## Objective

Reduce `lego/backend/internal/proxyproto` to a thin wrapper over `lego/types/proxyproto`, keeping the backend-only `net.Conn`-level pieces (`Wrap`/`Conn`, `proxyproto.go:216-247`) in place so no backend call site changes.

## Context

- Consumers to leave untouched: `nativessh/server.go:281-288` and the other gateway admission paths that resolve the PROXY-asserted source.
- Delete the now-shadowed parser body and the local copies of the merged tests (keep any wrapper-specific tests).

## Steps

1. Replace the parser body with re-exports/delegation to `types/proxyproto`.
2. Remove duplicated tests now owned by the shared suite.
3. `cd lego/backend && go build ./... && go test ./...`.

## Files

- `lego/backend/internal/proxyproto/proxyproto.go`
- `lego/backend/internal/proxyproto/proxyproto_test.go`
- `lego/backend/go.mod` (types dependency already present — verify)

## Acceptance criteria

- [ ] No backend call site outside the wrapper changed; backend suite green.

## Out of scope

- Behavior changes of any kind — byte-identical parsing semantics.
