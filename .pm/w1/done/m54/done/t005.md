---
id: w1/m54/t005
title: Rewire notifications deploy emails through email.Message (text byte-parity)
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m54/t002, w1/m54/t003]
---

## Objective

Deploy started/succeeded/failed emails (w7/m44) compose an `email.Message` — branded HTML with a real "View logs" button — with the text part byte-identical to today's, preserving the Render-register wording unchanged.

## Context

- Current composition: `deployEmail` (`lego/backend/internal/notifications/service.go:315-336`); interface `notifications.Mailer` (`service.go:56`); fan-out precomposes once then sends per-recipient (`notifyDeploy`/`notifyOne`).
- Byte-parity mapping: kind sentence = paragraph 1; commit = a multiline paragraph `"Commit:\n" + msg` (renders as `<br />` lines in HTML); logs link = CTA `{Lead: "View logs", Label: "View logs", URL: logsURL}`. `Message.Title` = the subject (e.g. "Deploy failed: web").
- Keep the compose-once-before-fan-out property: render `Text()`/`HTML()` once in `notifyDeploy`, pass both strings through `notifyOne`.
- Tests: `service_test.go` `fakeMailer` gains the html param; keep the recipient-policy assertions untouched.

## Steps

1. `deployEmail` returns `(subject string, msg email.Message)`; map per the parity table above.
2. `notifyDeploy` renders once; `notifyOne` gains the html argument; `notifications.Mailer` moves to the five-param signature.
3. Text byte-parity pin for all three kinds × (bare / +commit / +logsURL).

## Files

- `lego/backend/internal/notifications/service.go`
- `lego/backend/internal/notifications/service_test.go`

## Acceptance criteria

- [ ] All existing recipient-preference tests pass unmodified in behavior.
- [ ] Text part byte-identical to today for every kind/detail combination (test-pinned).
- [ ] HTML "View logs" button href equals the computed deploy-logs URL.
- [ ] `go test ./internal/notifications/` green.

## Out of scope

- New notification kinds or preference changes.
