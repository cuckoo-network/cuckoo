---
id: w1/m54/t004
title: Rewire members invite email through email.Message (text byte-parity)
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m54/t002, w1/m54/t003]
---

## Objective

The workspace-invite email composes an `email.Message` and sends both bodies; the plain-text part stays byte-identical to today's `inviteBody` output so nothing observable changes for text-only clients.

## Context

- Current composition: `lego/backend/internal/members/email.go` (`sendInvite` + `inviteBody`); interface: `members.Mailer` (`service.go:156`).
- Byte-parity mapping (verified against `Text()`'s section layout): paragraph = the "You've been invited to join the %q workspace on bex as a %s." sentence; CTA = `{Lead: "Sign up or log in with <email> to accept", Label: "Accept invitation", URL: <base>/auth/sign-up?invite=<token>}`; footer = the expiry line. No-`InviteBaseURL` case: second paragraph instead of a CTA. Title (HTML-only): "Workspace invitation".
- Tests to update: `members_test.go` `fakeMailer` (gains the html param; keep recording text as `body` — `m33_test.go:140` asserts the resent mail's text carries the fresh token) and add an assertion that the HTML body carries the invite link too.
- Delivery stays best-effort (log, never fail the verb) — unchanged.

## Steps

1. Replace `inviteBody` with `inviteMessage(inv, tenant) email.Message`; `sendInvite` calls `s.Mailer.Send(ctx, inv.Email, subject, msg.Text(), msg.HTML())`.
2. Update `members.Mailer` to the five-param signature; update `fakeMailer` and any compile-touched call sites.
3. Add a text byte-parity pin: the composed `Text()` for a linked and an unlinked invite equals the exact current strings.

## Files

- `lego/backend/internal/members/email.go`
- `lego/backend/internal/members/service.go` (interface)
- `lego/backend/internal/members/members_test.go`, `m33_test.go`

## Acceptance criteria

- [ ] Invite text part byte-identical to today for both the linked and linkless shapes (test-pinned).
- [ ] HTML part contains the `?invite=<token>` URL and the workspace name HTML-escaped.
- [ ] `go test ./internal/members/` green.

## Out of scope

- Any change to invite semantics (TTL, token rotation, redemption paths).
