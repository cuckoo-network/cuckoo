---
id: w1/m54/t008
title: Render parity — compare bex's transactional emails against Render's equivalents
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m54/t007]
---

## Objective

Standing closing task, scoped to the surface this milestone touches: the emails themselves. Compare bex's three mail shapes (invite, deploy started/succeeded/failed, webhook failure/disable) against Render's equivalent emails — subjects, register, structure, CTA — and flag drift as follow-up work rather than silently diverging.

## Context

- No REST/GraphQL/MCP wire shapes or dashboard UI change in this milestone, so the usual three-surface consistency sweep is vacuous; the parity object here is Render's own transactional email (Render does send branded HTML for team invites and deploy failure notices).
- The deploy-email wording was already written to Render's register (w7/m44) and its text is byte-pinned by t005 — this task checks the HTML presentation and subjects, not the copy.
- Evidence home: `docs/render-artifacts/` (the established pattern for captured Render behavior); ADR018 is the parity ledger if any row deserves an update.

## Steps

1. Collect what Render's equivalents look like (existing `docs/render-artifacts/` captures, Render docs, or a live invite to a test workspace if available).
2. Diff subjects/structure/CTA against bex's three shapes; file drift worth closing as `w1/NNN` inbox notes.
3. Record findings (even "no actionable drift") in the milestone README or a short artifact note.

## Files

- `docs/render-artifacts/` (possible new capture note)
- `.pm/w1/` (possible inbox notes)

## Acceptance criteria

- [ ] A written comparison exists for all three shapes with an explicit verdict each (parity / acceptable divergence / follow-up filed).

## Out of scope

- Implementing any drift fixes found — file, don't scope-creep.
