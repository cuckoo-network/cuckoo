---
id: w1/m54/t007
title: Visual verification: all three mail shapes rendered in dev-1 Mailpit
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m54/t004, w1/m54/t005, w1/m54/t006]
---

## Objective

See the actual pixels: trigger a workspace invite, a deploy notification, and a webhook failure notice against the dev-1 stack and verify each renders as branded HTML in Mailpit (and that the plain-text tab shows the expected fallback).

## Context

- dev-1 has its own Mailpit (`.pm/w1/dev-1/`, namespace `dev-1-auth`; `bash .pm/w1/dev-1/up.sh`, ports in `dev-1/ports.env`). Mailpit's UI renders both the HTML part and the text part of a multipart message — this is the preview loop, no new tooling.
- Cheapest triggers: invite — `POST` the members invite verb (or the dashboard Team panel) with `BEX_SMTP_ADDR` pointed at dev-1 Mailpit; deploy — flip a dev-1 App through a deploy with a notify recipient seeded; webhook — `BEX_WEBHOOK_BACKOFF="1s,1s,1s"` against an unreachable endpoint URL walks the failure path in seconds (`scripts/webhooks-verify.sh` pattern).
- Check at least one client-hostile detail: long URLs wrap (`word-break`), the button is tappable-sized, workspace name with `<`/`&` renders escaped, layout survives Mailpit's Gmail-ish preview.

## Steps

1. Bring up dev-1; point a locally-run bex-api's `BEX_SMTP_ADDR` at its Mailpit.
2. Trigger the three shapes; open each in Mailpit — screenshot each HTML rendering into `.playwright-mcp/` (bare filenames).
3. Verify the text tab matches the pinned plain bodies; note any rendering defects as fix-forward edits or follow-up notes.

## Files

- none (verification; screenshots under `.playwright-mcp/`)

## Acceptance criteria

- [ ] Three Mailpit screenshots: invite (with Accept button), deploy (with View logs button), webhook notice — all carrying the brand green `#307d03` card layout.
- [ ] Text tab of each equals the expected plain body.
- [ ] An HTML-metacharacter workspace name shows escaped, not injected.

## Out of scope

- Real-client matrix testing (Gmail/Outlook/Apple Mail) — Mailpit preview is the bar for this milestone.
