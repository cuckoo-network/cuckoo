---
id: w1/m54/t011
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m54/t010]
---

## Objective

Close the milestone once — and only once — its definition of done actually holds: brand codegen CI-gated, all three senders shipping branded multipart mail, suites green, Mailpit verification done.

## Context

- The move is the completion: set every remaining task `status: done`, move each `tNNN.md` to `w1/m54/done/`, mark every README row `— **DONE**` and `**Status:** done`, move the whole `w1/m54/` to `w1/done/m54/` (no tombstone at the old path), check `- [x] **m54**` in `w1/README.md`.
- Verify the DoD list in the milestone README item by item before moving anything.

## Steps

1. Re-check each DoD bullet against reality (run the suites, open the Mailpit screenshots).
2. Run `/pm done w1/m54/t011` last — its step 4 performs the milestone move.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m54/` → `.pm/w1/done/m54/`
- `.pm/w1/README.md`

## Acceptance criteria

- [ ] `w1/m54/` no longer exists; `w1/done/m54/` holds everything; workstream checkbox checked.
- [ ] Status synced in all three places (task frontmatter, milestone README, workstream README).

## Out of scope

- Declaring done on written-but-unverified code.
