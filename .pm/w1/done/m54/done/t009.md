---
id: w1/m54/t009
title: Simplify — /simplify over the code this milestone changed
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m54/t008]
---

## Objective

Standing closing task: run `/simplify` over the milestone's changed code — reuse, simplification, efficiency; behavior-preserving.

## Context

- Changed surface: `dashboard/scripts/generate-email-brand.mjs` (+ test), `lego/backend/internal/email/`, `internal/mailer/`, and the compose sites in `internal/members/`, `internal/notifications/`, `internal/webhooks/`.
- Watch for: the three near-identical per-feature `Mailer` interfaces (kept narrow by convention — consolidate only if `/simplify` finds a clean seam that doesn't couple features), duplicated Message-construction patterns, and template/QP plumbing that stdlib already covers.

## Steps

1. Run `/simplify` scoped to the files above.
2. Apply behavior-preserving improvements; anything larger becomes an inbox note.
3. All suites re-green.

## Files

- as listed in Context

## Acceptance criteria

- [ ] `/simplify` pass applied or explicitly no-op'd with reasons; residuals filed as `w1/NNN` notes.
- [ ] `go build ./... && go test ./...` (backend) and dashboard `yarn test` green after.

## Out of scope

- Behavior changes of any kind.
