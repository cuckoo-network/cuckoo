---
id: w1/m54/t006
title: Rewire webhooks failure/disable notices through email.Message
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m54/t002, w1/m54/t003]
---

## Objective

The webhook failing / webhook-disabled notices (w3/m11) send through the branded module. Strict byte-parity is not required here (the current bodies lack a trailing newline; the module adds one) — the sentences themselves stay identical.

## Context

- Current composition inline in `notifyFailure` (`lego/backend/internal/webhooks/worker.go:472-498`); interface `webhooks.Mailer` (`worker.go:127`).
- Mapping — failing: Title "Webhook delivery failing", paragraphs = the three current sentences (failure count / `Last error: …` / retry note). Disabled: Title "Webhook disabled", paragraphs = the two current sentences. No CTA (the worker has no dashboard base URL — don't add config for it here).
- `worker_test.go`'s `fakeMailer.Send` ignores the body (`worker_test.go:183`) — only the signature changes.
- Rate-limit (`shouldEmail`) and degrade-quietly logging unchanged.

## Steps

1. Build the two `email.Message` shapes in `notifyFailure`; send `msg.Text(), msg.HTML()`.
2. Move `webhooks.Mailer` to the five-param signature; update the test fake.
3. Confirm `internal/api/server.go` + `cmd/api/main.go` wiring still compiles untouched (structural satisfaction).

## Files

- `lego/backend/internal/webhooks/worker.go`
- `lego/backend/internal/webhooks/worker_test.go`

## Acceptance criteria

- [ ] Both notice shapes carry the same sentences as today in the text part; `Last error:` renders on its own paragraph in HTML.
- [ ] `go test ./internal/webhooks/` green; full `go build ./...` green (composition root untouched).

## Out of scope

- A "re-enable" CTA link (needs dashboard URL plumbing into the worker — inbox-note it if wanted).
