---
id: w1/m54/t010
title: Test coverage — layout, multipart, escaping, text parity
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m54/t008]
---

## Objective

Standing closing task: audit the tests t001–t006 added and fill the gaps with meaningful assertions — real behavior and failure modes, never coverage-gaming.

## Context

Behavior worth pinning if not already covered by the implementation tasks:

- **Codegen:** drift test failure message names the regen command; oklch conversion sanity (white → `#ffffff`, primary → green-dominant); `--check` exit codes.
- **email:** `Text()` section contract for every shape (paragraph-only, +CTA, +footer, multiline paragraph); HTML escaping of user data in title/paragraph/CTA label; URL appears in both href and visible fallback; no `oklch(`/`var(` anywhere in output.
- **mailer:** single-part path byte-identical when html==""; multipart parses via stdlib, text part first, QP round-trip; random boundary differs across sends; CRLF header-injection neutralized; unicode subject Q-encoded while ASCII subjects stay literal.
- **features:** invite/deploy text byte-parity pins (t004/t005); webhook sentences intact; fan-out/preference/rate-limit behavior unchanged.

## Steps

1. Sweep the new/changed test files against the list above; add what's missing.
2. Ensure failure modes are asserted (drift, injection, missing config), not just happy paths.
3. Full suites: backend `go test ./...`, dashboard `yarn test`.

## Files

- `dashboard/scripts/__tests__/generate-email-brand.test.mjs`
- `lego/backend/internal/{email,mailer,members,notifications,webhooks}/*_test.go`

## Acceptance criteria

- [ ] Every Context bullet is either covered by a test or consciously waived with a reason in this file.
- [ ] All suites green; no tautological/snapshot-everything tests introduced.

## Out of scope

- Coverage-percentage targets.
