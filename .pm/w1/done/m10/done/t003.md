---
id: w1/m10/t003
title: Prod sizing overlay — server.ha.replicas + drop the local storageClass patch
worker: worker1
status: done
estimate: 20m
depends_on: []
---

## Objective

docs/secrets.md "Prod deploy path" step 4: a prod values layer for the OpenBao chart — patch `server.ha.replicas` and drop the local overlay's `storageClass: local-path` override so prod falls back to the cluster default (`hcloud-volumes`), mirroring how `auth-dbs`'s local patch shrinks storage only for CAPD.

## Context

- Chart + values: `deploy/gitops/base/` (openbao 0.28.4) and `deploy/gitops/overlays/{local,prod}/`.

## Steps

1. Add the prod values patch; keep local behavior unchanged.
2. `scripts/gitops-validate.sh` (or `kustomize build` both overlays) to verify.

## Files

- `deploy/gitops/overlays/prod/` (+ base values file if the knob isn't exposed yet)

## Acceptance criteria

- [ ] `kustomize build` of prod shows the replica/storage settings; local build unchanged.

## Out of scope

- Actual raft HA join/quorum testing (single-node acceptable this milestone; backup task lives in w1/m7/t006).
