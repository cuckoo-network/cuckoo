---
id: w1/m57/t001
title: Verify current prod state of the three hand-applied fixes; snapshot what still drifts
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Twelve days and several static-site changes (w7/m54 browser-isolation work, the PSL gate waiver, the dashboard header fix) have passed since `w9/014` was filed. Before codifying anything, establish what still drifts on prod today: which of the three hand-applied fixes are still load-bearing, which have been superseded by shipped manifests, and whether the second unmanaged static-server still exists. The snapshot recorded here scopes t002/t003 honestly.

## Context

- `w9/done/014.md` (the promoted note) and `w9/done/012.md` (the full fix chain) describe what was applied by hand on 2026-07-18:
  1. `bex-registry-pull` re-minted from the `bex-registry-push` `bex-builder` credential (prod's copy had authenticated as the stale `bex-puller`, no Zot ACL grant under per-App mode).
  2. ClusterRoleBinding `bex-static-server-apps-reader` covering both static-server ServiceAccounts; the durable config fix shipped as `bb92f86d` (`config/staticserver` Role→ClusterRole).
  3. `default/bex-static-server` hand-updated to the working image + `BEX_BASE_DOMAIN=onbex.co`; a second unmanaged `bex-system/bex-static-server` also exists.
- Prod access: `scripts/fetch-app-kubeconfig.sh` with `BEX_SSH_KEY_PATH=~/.ssh/bex`. **Caution:** `infra/local/bex.kubeconfig` currently points at the local CAPD cluster — always `kubectl get nodes` first to confirm which cluster you are on.
- Never commit or print kubeconfig or secret contents; report secret *identity* (which htpasswd user it authenticates as), not values.

## Steps

1. Fetch the prod kubeconfig and confirm the target cluster (`kubectl get nodes` shows the Hetzner node names, not CAPD).
2. `bex-registry-pull`: in the build namespace, decode only the username field of the dockerconfigjson to identify which Zot user it authenticates as (`bex-builder` expected; stale `bex-puller` = drift).
3. CRB: diff the live `bex-static-server-apps-reader` ClusterRoleBinding against what `lego/operator/config/staticserver` (post-`bb92f86d`) declares; determine whether the shipped manifests, once applied, fully supersede the hand-applied binding.
4. Static-server Deployments: list every `bex-static-server` Deployment across namespaces; for each record image (vs the current CI image), `BEX_BASE_DOMAIN` presence/source, ServiceAccount, and whether anything (Argo app, operator) manages it.
5. Check whether `bex-static-config` (the ConfigMap the design `envFrom`s) exists on prod and whether it carries `BEX_BASE_DOMAIN`.
6. Record the drift snapshot in this task file (or the milestone README) with exact resource names/namespaces, and adjust t002/t003 scope notes if anything already shipped.

## Files

- `.pm/w1/m57/t001.md` (snapshot recorded here)
- Read-only: `lego/operator/config/staticserver/`, `deploy/gitops/`, `scripts/registry-secrets.sh`, `.pm/w9/done/012.md`

## Acceptance criteria

- [ ] A written drift snapshot names, for each of the three fixes: still-drifting / superseded-by-shipped-manifests / gone, with the exact prod resources checked.
- [ ] The number and namespaces of live static-server Deployments on prod are recorded, with image + `BEX_BASE_DOMAIN` state for each.
- [ ] The `bex-registry-pull` identity (which Zot user) is recorded without printing credential values.

## Out of scope

- Fixing anything found — t002/t003 do the codifying; this task only observes and scopes.

## Completion (2026-07-30)

Live prod snapshot via `scripts/fetch-app-kubeconfig.sh` (read-only):

- **One** static-server — `bex-system/bex-static-server`, Argo-managed (`tracking-id: bex-operator:apps/Deployment:…`, `argocd-controller(Apply)` manager), running the **CI-pinned digest** (same `ghcr.io/bex-co/bex-operator@sha256:…` as the manager). Argo `bex-operator` app **Synced/Healthy**; all static-server resources (Deployment/Service/SA/ClusterRole/ClusterRoleBinding) Synced. **No orphan `default/bex-static-server`** — the 2026-07-18 topology migrated to a single bex-system server + ExternalName aliases.
- **`bex-registry-pull`** (bex-build) authenticates as **`bex-builder`**, not the stale `bex-puller` — chain #3 already holds live.
- **`bex-static-config`** ConfigMap present but **hand-applied** (`kubectl.kubernetes.io/last-applied-configuration`, no Argo tracking-id), with the 4 correct keys — the only remaining ConfigMap drift (→ t002 adopts it into git).
- Residue: two hand-applied CRBs (`bex-static-server-apps-reader`, `bex-static-server-default-apps-reader`, the latter binding a now-orphan `default/bex-static-server` SA) + a hand-applied ClusterRole `bex-static-server-apps-reader` — superseded by the shipped Argo-managed `bex-static-server` CRB (→ t003 removes them).
- `hello-static.onbex.co` and `static-site.onbex.co` both serve **HTTP 200**.
