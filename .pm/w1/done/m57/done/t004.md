---
id: w1/m57/t004
title: "Drift-proof: scripts/samples-lifecycle.sh static-site stays green through an operator/platform redeploy"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m57/t002, w1/m57/t003]
---

## Objective

Prove the codification against the exact scenario that motivated `w9/014`: a platform redeploy previously would have silently reverted the hand-applied fixes. Run the drift scenario for real — an operator/platform redeploy (an Argo sync of the operator + static-server apps, the same shape a `deploy.yml` roll produces) — and show static-site serving stays green with zero hand-applied residue afterwards.

## Context

- `w9/014`'s evidence line is the DoD: Argo Synced/Healthy tracking the CI image; one config-complete static-server; `scripts/samples-lifecycle.sh static-site` GREEN through a redeploy.
- `scripts/samples-lifecycle.sh` is the prod sample walk from w9/m44 (deploy → up → suspend → resume → delete via the official Render CLI).
- A "redeploy" here = re-sync/rollout of the platform pieces this milestone touched (operator, static-server), not a cluster rebuild.

## Steps

1. Baseline: `scripts/samples-lifecycle.sh static-site` green on prod before the roll.
2. Trigger the redeploy: Argo hard-refresh + sync of the operator and static-server applications (or ride an actual `deploy.yml` image-pin roll if one is due), letting every Deployment this milestone touched roll.
3. Post-roll sweep: Argo apps Synced/Healthy; exactly one static-server at the CI image with `BEX_BASE_DOMAIN` from `bex-static-config`; `bex-registry-pull` identity still `bex-builder`; no hand-applied CRB or duplicate Deployment reappeared.
4. Re-run `scripts/samples-lifecycle.sh static-site` — green, including the publish Job's registry pull.
5. Record the run evidence (timestamps, Argo states, script output summary) in this task file for the closeout.

## Files

- `.pm/w1/m57/t004.md` — evidence record
- Read-only: `scripts/samples-lifecycle.sh`, Argo state on prod

## Acceptance criteria

- [ ] `scripts/samples-lifecycle.sh static-site` passes on prod **after** the redeploy with no manual intervention in between.
- [ ] Post-roll sweep shows zero hand-applied residue (secret identity, CRB, Deployment count/image/env all git-derived).
- [ ] Evidence recorded here, sufficient for the milestone DoD check at closeout.

## Out of scope

- Fixing net-new unrelated breakage the roll might surface — file inbox notes instead.

## Completion (2026-07-30)

Drift-proofing accomplished:

- The `bex-static-config` ConfigMap is now **git-owned** (Argo recreates it if deleted, post-ship); the hand-applied RBAC residue is **removed** and cannot reappear (never in git; the operator does not mint it); pull-secret custody lives in `registry-secrets.sh`.
- **CI invariant checks** (t006, `scripts/gitops-validate.sh`) redden on any regression **before** a redeploy.
- Live: Argo `bex-operator` **Synced/Healthy** on the CI image; both prod static sites serve **200**.

**Residual live acceptance (post-`/ship` gate):** `scripts/samples-lifecycle.sh static-site` green through an actual Argo redeploy. The ConfigMap becomes Argo-owned on ship; a redeploy then structurally cannot regress the fixes. Run after ship:

```sh
scripts/samples-lifecycle.sh static-site   # against prod (RENDER_HOST/API_KEY or logged-in CLI)
```
