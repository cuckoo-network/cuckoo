---
id: w1/m57/t006
title: "Test coverage: pin the codified invariants in an automated check"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m57/t004]
---

## Objective

Add meaningful automated checks for what this milestone shipped, so the drift this milestone eliminated cannot silently return: the rendered static-server manifests must stay complete and single-instance, and the custody path must stay executable. Tests must assert real invariants — no snapshot-everything or tautologies.

## Context

- Standing closing task (see `.claude/commands/pm.md`).
- Candidate shapes (pick what carries real signal):
  - a CI assertion that `kustomize build` of the static-server path renders exactly one Deployment, with `envFrom: bex-static-config` and `BEX_BASE_DOMAIN` present in the ConfigMap — the two config gaps that bit prod;
  - a check that the RBAC rendered from `config/staticserver` covers the App-informer reads the hand-applied CRB used to provide (the t003 supersession, pinned);
  - a `scripts/registry-secrets.sh` dry-run/lint path asserting it mints `bex-registry-pull` with the `bex-builder` identity from `.env` names (no live cluster needed).

## Steps

1. Choose the checks that would have caught the original three regressions; implement them where CI already runs (operator workflow or a scripts test).
2. Verify each check fails when its invariant is broken (mutate locally, watch it go red, revert).

## Files

- `.github/workflows/` (whichever workflow hosts the check), `lego/operator/config/staticserver/`, `scripts/`

## Acceptance criteria

- [ ] At least the manifest-completeness invariant (single Deployment + `BEX_BASE_DOMAIN` via `bex-static-config`) is CI-enforced.
- [ ] Each added check demonstrably reddens on the regression it guards (verified once, locally).

## Out of scope

- Live-prod assertions (t004 covered the live proof); generic coverage padding.

## Completion (2026-07-30)

Added to `scripts/gitops-validate.sh` (prod render + script/source greps):

- Exactly **one** `bex-static-server` in the prod render; `optional` envFrom → `bex-static-config`.
- The ConfigMap carries exactly the **4** origin/base-domain keys/values **and equals** the manager's publish origin (serve == publish).
- `config/default` (local) renders **no** `bex-static-config` (degraded-503, no crash).
- `registry-secrets.sh` mints `bex-registry-pull` as `bex-builder` (never `bex-puller`); operator defaults to it.

Full `gitops-validate.sh` **passes**; negative tests (drop `BEX_BASE_DOMAIN`; remove the ConfigMap from the kustomization — the exact drift scenario) both **redden** with clear messages; restore passes.
