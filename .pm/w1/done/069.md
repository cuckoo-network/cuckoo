> **Closed 2026-08-19 (w1 triage).** Deferred — not a live hazard. High-priority items from the 2026-08-19 architecture review already shipped as `w1/m76`–`m80`. Trigger-gated in `.pm/FUTURE-MAYBE.md` § "Architecture-review leftovers (2026-08-19)". Original text below kept for provenance.

# Dashboard file-shape splits: use-env-groups.ts + EnvironmentEditor

From the 2026-08-19 refactor review (§4.4 dashboard). Two shape fixes, no behavior change:

- **`features/env-groups/hooks/use-env-groups.ts`** (651 lines, 8 hooks + 7 helpers vs the repo's one-hook-per-file norm — `features/services/hooks/` has 48 single-hook files). Split along existing seams: `use-env-groups.ts` (list+detail), `use-env-group-mutations.ts`, `use-env-group-vars.ts`, `use-env-group-secret-files.ts`, `lib/env-group-mapping.ts`. Pure moves + imports; ~zero risk.
- **`EnvironmentEditor`** (`features/services/components/service-environment-editor.tsx:187` — a 684-line single function, 16 hook calls, one return; correctly shared by services env tab + env-groups editors, so the abstraction is right and only the file shape is wrong). Extract `useEnvironmentDraft()` (the 8 useState + 10 handlers, ~330 lines — the pure helpers already live in `lib/environment-draft.ts`) and move the 8 sub-components already defined at :871-1233 into an `environment-editor/` directory, leaving a ~120-line presentational shell. ~2 days total with tests; the hook boundary becomes independently testable. Runner-ups if in the area: `EnvironmentCard` (`environment-card.tsx:112`, 366 lines → `useEnvironmentCardActions` + pure `toEnvironmentRows`), `BlueprintDetailPage` (`blueprints.$blueprintId.tsx:186`, 398 lines → 3 components + a thin route).
