---
id: w1/m40/t006
title: "Test coverage — content negotiation + wake still fires"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m40/t004]
---

## Objective

Real behavior: a browser Accept gets HTML (with the refresh directive present), an API Accept gets 503 + Retry-After, an ambiguous/`*/*` Accept defaults to the JSON 503 (no agent regression), and `wakeApp` is invoked in every case. Assert the actual responses, not just status codes.

## Acceptance criteria

- [ ] The negotiation matrix + wake-fired assertions pass; suite green.
