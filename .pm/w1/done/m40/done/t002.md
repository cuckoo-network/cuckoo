---
id: w1/m40/t002
title: "Activator serves the default wake interstitial (content-negotiated)"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m40/t001]
---

## Objective

When the activator handles a hibernated App's request: for a browser (`Accept: text/html`) serve an HTML wake interstitial that auto-refreshes per t001's cadence; for an API client keep today's `503` + `{"error":"service hibernated","retryAfter":5}` and `Retry-After`. Both still trigger `wakeApp`. Reuse m37's page-serving/render path rather than a second templating mechanism.

## Context

- `lego/operator/cmd/activator/main.go` — `wakeApp` (line ~119), the hibernated branch (line ~152); m37's maintenance-page serving is the mechanism to share (grep `maintenance`/page-render in the same file).
- Content negotiation: parse `Accept`; default ambiguous/unknown to the existing JSON 503 (don't regress API/agent callers).

## Files

- `lego/operator/cmd/activator/main.go` (+ a wake page template)

## Acceptance criteria

- [ ] Browser Accept → HTML wake page (auto-refresh); JSON Accept → unchanged 503 + Retry-After; wake fired in both; shared render path with maintenance.

## Out of scope

- A tenant-customizable wake page (maintenance mode has custom pages; wake stays default-only unless Render shows otherwise in t001).
