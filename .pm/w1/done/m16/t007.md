---
id: w1/m16/t007
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m16/t006]
---

## Objective

Standing closeout: close this milestone only when the Definition of Done actually holds — a tenant can create an env group, add vars + secret files, link it to services (linked services receive the group's values in their `<name>-env` Secret), and add per-service secret files mounted at `/etc/secrets/<name>`, all exposed over REST/GraphQL/MCP + the dashboard Environment tab, with `make test` + dashboard tests green. Then move the milestone to `done/` per `.pm/CLAUDE.md`.

## Context

- DoD lives in `README.md`; do not close on partial surfaces (all three adapters + dashboard must be present, parity checked).
- `.pm` done-move rules (`.pm/CLAUDE.md`): a done task moves to `wN/mN/done/tNNN.md`; a milestone with no open tasks moves **whole** to `w1/done/m16/` (`mv` the directory, `rmdir` the original — no tombstone), and its box is checked in the workstream README. Sync status in all three places (task frontmatter, milestone README `**Status:**` + `— DONE` rows, workstream README checkbox).
- Markdown is CI-checked: run `npx prettier@3.4.2 --write` over changed docs before finishing.

## Steps

1. Verify the DoD holds end-to-end (all surfaces + dashboard, parity checked, tests green).
2. Mark every task `status: done`, flip the milestone README rows to `— DONE`, set `**Status:**` to done, check the workstream README box.
3. Move the whole `w1/m16/` directory to `w1/done/m16/` (leave no tombstone) and any done inbox note (`w1/010`) to `w1/done/010.md`.
4. Run prettier over changed markdown.

## Files

- `.pm/w1/m16/` → `.pm/w1/done/m16/` (whole-directory move).
- `.pm/w1/README.md` — workstream checkbox.
- `.pm/w1/010.md` → `.pm/w1/done/010.md` (if the inbox note is fully resolved).

## Acceptance criteria

- [ ] DoD verified end-to-end; not closed on partial surfaces.
- [ ] All task frontmatter + milestone README + workstream README statuses in sync.
- [ ] Milestone moved whole to `w1/done/m16/`, no tombstone left behind.
- [ ] Changed markdown prettier-clean.

## Out of scope

- Follow-on config work beyond this milestone's DoD (open a new note/milestone instead).
