---
id: w1/m65/t015
title: Test coverage
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m65/t013]
---

## Objective

Ensure every finding closed in this milestone has a regression test that asserts real behavior and **fails on the pre-fix code** — never a trivial, tautological, or snapshot-everything test. Consolidate the per-task tests into a coherent security-regression set.

## Context

- Per-finding assertions (from t001–t012 acceptance criteria):
  - F1: non-GitHub host with granted suffix mints no token/Secret.
  - F2: foreign-admin installation connect rejected, no mutation.
  - F3: PR workflow exposes no production secret (documented evidence + any assertable check).
  - F7: injected grant/revoke failures converge to exact tuple state.
  - F16/F13: OpenFGA-off fails closed; unverified session never redeems an email invite.
  - F4/F14: external `next` → `/`; post-logout refresh stays signed out.
  - F5: `-race` concurrent wake, one coalesced patch.
  - F6/F12: over-budget connections/misses rejected with bounded dials/buffers.
  - F8/F9: cumulative exec cap + truncation; expensive query rejected pre-resolver.
  - F10: over-limit part / over-quota session bounded; replay paginated.
  - F11: oversized no-Content-Length body → 413 before full read.
  - F15: RFC 6598 rejected across literal/DNS/dual-stack/mapped.

## Steps

1. Add/verify the regression test for each finding at the right layer (Go unit/integration, dashboard test, mobile test, `-race` where applicable).
2. Confirm each test fails when reverted against the pre-fix behavior (spot-check).
3. Run all suites.

## Files

- Test files across `lego/backend/`, `lego/operator/`, `lego/types/`, `dashboard/`, `mobile/`.

## Acceptance criteria

- [ ] Each of the 16 findings has a regression test asserting the fixed behavior.
- [ ] `cd lego/backend && go test ./...`, `make test`, `yarn test`, and lint are green.

## Out of scope

- Live load/race testing against production (report's deferred runtime-validation unit — operator follow-up).
