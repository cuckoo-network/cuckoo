---
id: w1/m65/t014
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m65/t013]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving). Fold duplicated admission/quota/bounded-reader logic into shared helpers where the fixes repeated a pattern across subsystems.

## Context

- Likely convergence points: a shared bounded-body/bounded-reader helper (t009/t010/t011 all add byte caps), an admission/semaphore helper (t008 proxies + static server), a same-origin normalizer reused across auth flows (t006), and the SSRF prefix policy (t012).
- Do not change behavior; the milestone's tests must stay green.

## Steps

1. Run `/simplify` over the changed files.
2. Extract genuinely shared logic; drop dead/duplicated code.
3. Re-run the suites; file any deferred residuals as an inbox note.

## Files

- All files touched by t001–t012.

## Acceptance criteria

- [ ] `/simplify` applied; no behavior change; backend + operator + dashboard suites + lint green.
- [ ] Residuals (if any) filed as `w1/NNN.md`.

## Out of scope

- New refactors unrelated to this milestone's changes.
