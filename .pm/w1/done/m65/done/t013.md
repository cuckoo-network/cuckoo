---
id: w1/m65/t013
title: Render parity
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m65/t012]
---

## Objective

Several fixes in this milestone change user/tenant-facing behavior; verify the change stays consistent across every surface it exposes (REST, GraphQL, MCP in `lego/backend/`, and the `dashboard/` UI) with the same fields, semantics, and error shapes, and compare against equivalent render.com behavior. Flag any drift as follow-up rather than silently diverging.

## Context

- Surface-touching fixes to check:
  - **F9 GraphQL cost rejection** (t009) — the new refusal must return a consistent, documented error shape; confirm REST/MCP equivalents aren't inconsistently un-gated.
  - **F8 MCP `sandbox_exec` truncation metadata** (t009) — same truncation contract across REST/GraphQL/MCP where the exec surface is exposed.
  - **F2 GitHub connect callback** (t002) — rejection semantics consistent across surfaces.
  - **F7 member role-change / removal** (t004) and **F13 invite redemption** (t005) — consistent semantics + error shapes on REST/GraphQL/MCP + dashboard.
  - **F4 login redirect** (t006) — dashboard-only, no API surface.
- bex-api is meant to be Render-compatible — see `docs/ADR006-bex-api.md`, `dashboard/CLAUDE.md`.

## Steps

1. For each surface-touching fix, diff the REST / GraphQL / MCP field names, semantics, and error shapes; confirm the dashboard consumes them consistently.
2. Compare new refusal/truncation shapes against render.com behavior where an equivalent exists (agent sessions / sandbox exec are bex extensions — note that).
3. File any drift as a follow-up note rather than diverging silently.

## Files

- `lego/backend/internal/api/*`, `internal/graphql|apps|sandbox|github|members/*` (read/verify)
- `dashboard/` (verify consumption)
- `docs/ADR018-render-parity.md` (update if a row moves)

## Acceptance criteria

- [ ] Each surface-touching fix is confirmed identical across REST/GraphQL/MCP (fields, semantics, error shapes) or its drift is filed as follow-up.
- [ ] Dashboard consumption verified for the changed error/behavior shapes.

## Out of scope

- New feature surface — this is a consistency check only.
