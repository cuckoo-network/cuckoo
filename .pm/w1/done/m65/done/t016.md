---
id: w1/m65/t016
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m65/t015]
---

## Objective

Close the milestone once its definition of done actually holds — all 16 findings remediated with passing regression tests, not merely code written.

## Context

- Standing closeout task per `.claude/skills/pm/SKILL.md`. Do not run until the DoD is real.

## Steps

1. Confirm every finding's control is in place and its regression test passes (t015 green).
2. Set every remaining task's `status: done`; move each `tNNN.md` to `w1/m65/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w1/m65/README.md`.
4. Move the whole `w1/m65/` directory to `w1/done/m65/` and check `- [x]` in `w1/README.md`.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m65/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] DoD met: all 16 findings closed + regression-tested; all suites + lint green.
- [ ] Milestone moved to `w1/done/m65/`; workstream README box checked.

## Out of scope

- Committing/pushing — only on `/ship`.
