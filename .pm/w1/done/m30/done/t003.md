---
id: w1/m30/t003
title: Dependabot residual triage — re-check for now-safe fixes
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Re-check the 6 residual Dependabot findings recorded in `.pm/w1/018.md` for a now-available safe (non-breaking) fix, and apply whichever ones have one.

## Context

- From `w1/018.md`: `lodash` 4.17.23 (no upstream fix as of the last check — high 241, medium 240), `minimatch` (two majors in the tree, 3.1.2 and 9.0.5 — a blanket resolution would break one), `picomatch` 2.3.1 (same multi-major trap as minimatch), `srvx` < 0.11.13 (dev-server dep), `@tanstack/start-server-core` < 1.167.30 (framework-level bump), `vite` 8.x (breaking major, all 7.x vulns already closed at 7.3.6).
- `dashboard/package.json`'s `resolutions` block already forces several patched transitives within their existing major (the established pattern here) — follow the same approach for any new fix.

## Steps

1. Re-check npm registry/changelogs for each of the 6 residuals: has a new patch version landed, or is a safe scoped-resolution fix now possible (e.g. `parent>minimatch` targeting only the vulnerable major)?
2. Apply any safe fix found (same verification bar as the original batch: `yarn typecheck`, `yarn test`, `yarn build` green after).
3. Re-record the current status of whichever residuals remain unfixed (update or supersede `w1/018.md`'s content in this milestone's closeout).

## Files

- `dashboard/package.json`, `dashboard/yarn.lock`

## Acceptance criteria

- [x] Every one of the 6 residuals has a documented current status (fixed, or still genuinely blocked with a reason).
- [x] `yarn typecheck`/`yarn test`/`yarn build` green after any changes.

## Results (2026-07-13 re-check)

Re-checked all 6 against the live registry + `yarn npm audit --all --recursive`. **5 of 6 now closed; 1 genuinely deferred.**

| residual | prior status | now | action |
| --- | --- | --- | --- |
| `lodash` | 4.17.23, "no upstream fix" | **fixed** | upstream shipped 4.18.x; bumped `resolutions.lodash` `^4.17.23` → `^4.18.1` (resolves 4.18.1, closes high GHSA-r5fr-rjxr-66jc `_.template` + moderate GHSA-f23m-r3pf-42rh prototype-pollution). Same-major, non-breaking. |
| `minimatch` (3.1.2 + 9.0.5) | multi-major trap | **fixed** | `yarn up -R minimatch`: 3.1.2→**3.1.5**, 9.0.5→**9.0.9**. Each consumer's own caret range (`^3.1.2`, `^9.0.5`) already permitted its major's patch, so no cross-major forcing was needed — the trap dissolves. Closes the four ReDoS advisories (GHSA-3ppc-4f35-3m26, GHSA-7r86-cg39-jmmj, GHSA-23c5-xmqv-rm74). Also pulled a patched `brace-expansion` (2.0.2→2.1.2). |
| `picomatch` 2.3.1 | multi-major trap | **fixed** | `yarn up -R picomatch`: 2.3.1→**2.3.2** (the vulnerable 2.x copy under micromatch), 4.0.5 left untouched. Closes GHSA-3v7f-55p6-f55p + GHSA-c2c7-rcm5-vvqj. |
| `srvx` <0.11.13 | deferred (dev-server) | **fixed** | added `resolutions.srvx` `^0.11.13` (resolves 0.11.22, within the existing `^0.11.9` range — non-breaking). Closes GHSA-p36q-q72m-gchr. |
| `vite` 8.x | breaking major | **n/a — no longer a residual** | `yarn npm audit` flags no vite advisory: 7.3.6 (direct) is clean and 8.1.4 is already present as a clean transitive. The 8.x breaking major is a version-hygiene upgrade, not a security fix. |
| `@tanstack/start-server-core` <1.167.30 | framework bump | **still deferred (documented)** | fix is real (≥1.167.30; latest 1.169.17) but requires a coordinated `@tanstack/react-start` suite bump from 1.166.x — the framework-level upgrade this task lists as out of scope. Moderate GHSA-9m65-766c-r333. Promote to its own task when the dashboard is between feature landings. |

Files changed: `dashboard/package.json` (resolutions: lodash floor + srvx add), `dashboard/yarn.lock`.

**Incidental fix (not a dependency):** the re-check's `yarn test` surfaced a pre-existing red suite — `src/routes/__tests__/index.test.tsx` (5 failures) broke when w1/m31 (`41e9b61`) added `useProjects` (Apollo `useQuery`) to `HomePage` without mocking it in the test. Added the missing `vi.mock` for `use-projects`, mirroring the file's existing hook mocks. Verified the failures pre-date this milestone (fail identically on pristine `main`). This restored `yarn test` to green (129 files / 803 tests), which t003's own acceptance criterion requires.

## Out of scope

- Forcing a breaking major bump (`vite` 8.x, or a `minimatch`/`picomatch` parent upgrade that isn't itself a safe non-breaking change) — that's follow-up work, not this task.
