---
id: w1/m30/t004
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m30/t002, w1/m30/t003]
---

## Objective

Standing closing task: run `/simplify` over the code this milestone changed (reuse/simplification/efficiency, behavior-preserving).

## Context

- Applies to `lego/backend/cmd/api/main.go`'s shutdown handling and the dashboard's `package.json`/`yarn.lock` changes.

## Steps

1. Run `/simplify` over the diff from t001-t003.
2. Apply any behavior-preserving cleanups it finds.

## Files

- Everything touched by t001-t003.

## Acceptance criteria

- [x] `/simplify` run completed and findings applied or explicitly dismissed.

## Result

Ran `/simplify` (4 parallel cleanup agents) over the t001–t003 diff. Applied: folded the `ErrServerClosed` filter into `serveUntilShutdown`'s `select` (goroutine now `serveErr <- srv.ListenAndServe()`), removing the dead `nil` send and the duplicated error check; added a comment noting the CP server's drain is best-effort (main returns when the public server finishes). Skipped: making the 10s shutdown timeout env-configurable (task-scoped-out; would add a new env var + `.env` mirror + CLAUDE.md docs, outside this diff — recorded as a follow-up). Reuse + altitude passes found the helper and the test mock already right-altitude (the helper improves on the operator's ad-hoc `staticserver` shutdown pattern). Tests still green post-simplify.

## Out of scope

- Bug hunting — that's `/code-review`, not this task.
