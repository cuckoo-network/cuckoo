---
id: w1/m30/t006
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m30/t005]
---

## Objective

Standing closing task: when every other task is complete and the milestone's Definition of Done actually holds, close the milestone.

## Context

- Per `.claude/commands/pm.md`'s `done` subcommand: this task's completion moves the whole milestone to `w1/done/m30/`.

## Steps

1. Confirm the DoD in `README.md` is genuinely met (SIGTERM fix verified by the regression test; Dependabot residuals re-checked).
2. Set every task's `status: done`, move each `tNNN.md` to `w1/m30/done/`.
3. Mark every row `— **DONE**` and `**Status:** done` in this milestone's `README.md`.
4. Move the whole `w1/m30/` directory to `w1/done/m30/`.
5. Check `- [x]` in `w1/README.md`.

## Files

- `.pm/w1/m30/` (moves to `.pm/w1/done/m30/`)
- `.pm/w1/README.md`

## Acceptance criteria

- [ ] Milestone directory moved, all three status locations in sync.

## Out of scope

- Re-opening scope not already in this milestone's task list.
