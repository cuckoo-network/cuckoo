---
id: w1/m89/t002
title: Migrate the heaviest call sites
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m89/t001]
---

## Objective

Move the files with the most copies first — they exercise the API hardest and will expose a bad abstraction while it is still cheap to change.

## Context

- Heaviest: `features/services/components/cron-runs-section.tsx` (5), `features/agent-sessions/components/session-detail-header.tsx` (5), `features/webhooks/components/webhook-deliveries-card.tsx` (3), `features/sessions/components/active-sessions-panel.tsx` (3), `features/services/components/service-environment-editor.tsx` (3), `features/services/components/secret-file-row.tsx` (3).
- Each site has tests that query by role and accessible name; those queries must keep working or the migration changed behavior.

## Steps

1. Migrate one file, run its tests, and only then continue.
2. If a site does not fit the API, fix the API — do not special-case the site.

## Files

- the six files above

## Acceptance criteria

- [ ] all six migrated, their tests passing unchanged
- [ ] no site needed a bespoke escape hatch

## Out of scope

- The remaining ~21 files (t003).
