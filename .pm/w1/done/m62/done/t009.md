---
id: w1/m62/t009
title: Render parity check (REST/GraphQL/MCP/UI vs render.com)
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m62/t008]
---

## Objective

Standing closing task: verify the paid-gating change is consistent across every surface it touches — the same paid-tier definition, refusal semantics, and error shape on REST, GraphQL, and MCP (`lego/backend/`), and matching dashboard behavior — and compare against Render's actual card-at-paid-intent behavior (t001 artifact), flagging any drift as follow-up work rather than silently diverging.

## Context

- Surfaces changed: apps/postgres/keyvalue create + plan-change verbs on all three adapters (t004/t005), dashboard interception (t007).
- Render baseline: `docs/render-artifacts/paid-create-without-card.md` (t001); `docs/ADR006-bex-api.md` (one core, thin adapters); `docs/ADR018-render-parity.md` row from t008.
- Also sweep the official Render CLI path (`docs/cli-compatibility-checklist.md`): a card-less paid `render services create`-equivalent must surface the 402 message intelligibly, not a parse error.

## Steps

1. Exercise a card-less paid create on each surface (REST curl, GraphQL, MCP tool, dashboard, Render CLI) against a dev stack with the gate enabled; capture the five responses.
2. Diff semantics/fields/status across surfaces and against the t001 artifact.
3. File any drift as inbox notes or ADR018 divergence rows.

## Files

- `docs/ADR018-render-parity.md` / `.pm/w1/` inbox notes (only if drift found)

## Acceptance criteria

- [ ] All five surfaces refuse a card-less paid create with consistent semantics; evidence captured in the task or ADR018.
- [ ] Any accepted divergence is written down, none silent.

## Out of scope

- Fixing non-gating parity gaps discovered incidentally (file them, don't fix here).
