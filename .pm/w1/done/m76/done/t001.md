---
id: w1/m76/t001
title: KeyValue connect panel: add the canViewSensitive reveal gate
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Gate the KeyValue connection-info Reveal on `canViewSensitive`, exactly as the databases twin does. The keyvalue panel's own header comment says the whole `redis://` URI is the sensitive field, yet the component never imports `useCapabilities`.

## Context

- `dashboard/src/features/databases/components/connection-info-panel.tsx:35,65` — the correct pattern (gates Reveal on `canViewSensitive`).
- `dashboard/src/features/keyvalue/components/connection-info-panel.tsx` — missing the gate; comment at ~:22-26 states the URI is sensitive.
- `canViewSensitive` appears in exactly 3 components repo-wide; this is the one that should have it and doesn't.

## Steps

1. Mirror the databases panel's `useCapabilities` + `canViewSensitive` gating into the keyvalue panel.
2. Match copy/i18n keys to the databases treatment (add keyvalue keys in both locales if needed).
3. Add a component test: without the capability, Reveal is absent/disabled and the URI is never rendered.

## Files

- `dashboard/src/features/keyvalue/components/connection-info-panel.tsx`
- `dashboard/src/features/keyvalue/components/__tests__/` (new/extended test)

## Acceptance criteria

- [ ] Without `canViewSensitive`, the redis URI cannot be revealed; with it, behavior matches the databases panel.
- [ ] Component test covers both capability states.

## Out of scope

- The broader databases/keyvalue shared `<ConnectionInfoPanel>` consolidation (tracked as inbox note `w1/066`) — this task is the minimal gate fix only.
