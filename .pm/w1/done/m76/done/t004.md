---
id: w1/m76/t004
title: admissionListener: emit shed metrics on aux gateway listeners
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Make connection shedding on the auxiliary ssh-gateway listeners observable. `admissionListener.Accept` closes shed connections with a bare `_ = conn.Close()` — floods against the web-shell, sandbox-exec, agent-attach, git-proxy and model-proxy listeners are invisible, while every other shed path in the fleet emits a metric.

## Context

- `lego/backend/cmd/ssh-gateway/main.go:420-464` — `admissionListener`; the silent close is at :452.
- `lego/backend/internal/sshgateway/nativessh/server.go:246,285` — the pattern to match: `LimitRejected("preauth")` / `LimitRejected("preauth_source")`.
- Existing metric: `bex_ssh_limit_rejected_total` (`sshgateway/metrics.go`).

## Steps

1. Thread `*sshgateway.Metrics` into `newAdmissionListener`.
2. Emit `LimitRejected("listener_global")` / `LimitRejected("listener_source")` on the two shed branches.
3. Test: saturate a tiny-cap listener and assert the counter increments per scope.

## Files

- `lego/backend/cmd/ssh-gateway/main.go`
- `lego/backend/internal/sshgateway/metrics.go` (if a scope label needs registering)
- gateway tests

## Acceptance criteria

- [ ] Shed connections on every aux listener increment `bex_ssh_limit_rejected_total` with a listener scope.
- [ ] `cd lego/backend && go test ./...` green.

## Out of scope

- Delegating `admissionListener` to a generalized `SourceLimiter` (inbox note `w1/057`); also the review's flag that aux listeners default `global == perSource == 128` (per-source fairness a no-op for the two sandbox-direct proxies) — surface that as a config question, don't change defaults here.
