---
id: w1/m76/t007
title: Operator doc-comment repairs (reconcileStaticSite + orphans)
worker: worker1
status: done
estimate: 20m
depends_on: []
---

## Objective

Repair the misattached and orphaned doc comments the review found in the operator: `reconcileStaticSite` — the 105-line entry point for the whole static-site type — has no doc comment because two doc blocks were merged, so `go doc`/hover shows the wrong prose on `publishStaticRevision`; four orphaned comments document functions that moved to `internal/execution`.

## Context

- Misattached block: `lego/operator/internal/controller/app_controller.go:2316-2329` (opens "// reconcileStaticSite materializes…" but the next decl is `publishStaticRevision:2329`; `reconcileStaticSite` itself is at :2387).
- Orphans: `lego/operator/internal/predeploy/predeploy.go:260-264` (jobCondition/jobFailureMessage prose), `lego/operator/internal/publish/publish.go:473` and `:551-552` (same two; :473 now reads as PurgeJob's doc).

## Steps

1. Split the merged block back into two doc comments; reattach the first to `reconcileStaticSite`.
2. Delete the four orphaned comment blocks.
3. `make lint` from `lego/operator/`.

## Files

- `lego/operator/internal/controller/app_controller.go`
- `lego/operator/internal/predeploy/predeploy.go`
- `lego/operator/internal/publish/publish.go`

## Acceptance criteria

- [ ] `go doc` shows correct prose for `reconcileStaticSite` and `publishStaticRevision`; no comment documents a function that isn't below it.

## Out of scope

- The app_controller.go file split (inbox note `w1/060`) — these comment repairs should land first so the split moves correct comments.
