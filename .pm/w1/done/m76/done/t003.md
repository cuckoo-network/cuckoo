---
id: w1/m76/t003
title: modelproxy: no-follow CheckRedirect on the upstream client
worker: worker1
status: done
estimate: 20m
depends_on: []
---

## Objective

Apply agentcred's copy-and-force `CheckRedirect = http.ErrUseLastResponse` pattern to modelproxy's injected upstream client, so the BYO model key can never be replayed to a redirect target. Latent today (only tests inject `b.HTTP`), but the no-follow guarantee is stated as a contract in `sshgateway/upstream.go:57-59` and modelproxy doesn't hold it.

## Context

- `lego/backend/internal/sshgateway/agentcred/agentcred.go:386-391` — the correct pattern: copies the injected client and force-sets `CheckRedirect`, with a comment explaining the token-replay risk.
- `lego/backend/internal/sshgateway/modelproxy/modelproxy.go:484-486` — returns `b.HTTP` as-is.

## Steps

1. Copy agentcred's clone-and-force block into modelproxy's `httpClient()` memoization, carrying the explanatory comment.
2. Add a test injecting a redirecting fake upstream and asserting the credential is not sent to the redirect target.

## Files

- `lego/backend/internal/sshgateway/modelproxy/modelproxy.go`
- `lego/backend/internal/sshgateway/modelproxy/*_test.go`

## Acceptance criteria

- [ ] An upstream 302 is returned to the caller as-is; no second request carries the injected key.
- [ ] `cd lego/backend && go test ./internal/sshgateway/...` green.

## Out of scope

- The shared `credproxy` base for agentcred/modelproxy (inbox note `w1/058`) — that refactor should make this guarantee hold by construction; this task is the 4-line direct fix.
