---
id: w1/m76/t006
title: kv-sni-proxy: SetHealthy on the deletion path
worker: worker1
status: done
estimate: 15m
depends_on: []
---

## Objective

Fix the stuck health gauge: kv-sni-proxy's watcher returns early on CR deletion without calling `meter.SetHealthy(w.router.healthy())`, while the pg twin falls through and does. Since `delete` clears the `invalid` map, deleting the last malformed KeyValue CR leaves `bex_kv_proxy_healthy` stuck at 0 until an unrelated reconcile fires.

## Context

- Bug: `lego/operator/cmd/kv-sni-proxy/main.go:166-169` (early return).
- Correct twin: `lego/operator/cmd/pg-sni-proxy/main.go:273-279`.

## Steps

1. Call `SetHealthy` on the deletion path before returning (match the pg proxy's ordering).
2. Regression test: mark a CR invalid, delete it, assert the gauge recovers.

## Files

- `lego/operator/cmd/kv-sni-proxy/main.go`
- `lego/operator/cmd/kv-sni-proxy/main_test.go`

## Acceptance criteria

- [ ] Deleting the last invalid CR flips `bex_kv_proxy_healthy` back to 1.
- [ ] Operator module tests green.

## Out of scope

- The shared `sniproxy.Table`/`Bootstrap` consolidation that makes this class of drift structurally impossible (inbox note `w1/059`).
