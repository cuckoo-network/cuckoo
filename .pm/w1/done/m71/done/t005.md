---
id: w1/m71/t005
title: Render parity
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m71/t004]
---

## Objective

Confirm the change is consistent across every surface it touches, and that the ADR018 ledger still tells the truth.

## Context

- Repo rule: check REST, GraphQL, and MCP in `lego/backend/` for the same fields, semantics, and error shapes, plus the `dashboard/` UI; compare against equivalent render.com behaviour and flag drift as follow-up rather than diverging silently.
- Here REST and GraphQL are deliberately unchanged — the check is that MCP still reaches every field they do, not that all three changed together.
- ADR018's MCP column scores capability reachability, not tool-name identity (made explicit by m70/t006). That is what makes this fold parity-preserving; verify it rather than assert it.

## Steps

1. For every ADR018 row naming a folded tool, confirm the capability is still reachable over MCP and update the row's tool name.
2. Diff the fields reachable over MCP against those reachable over REST `PATCH` and GraphQL — the sets should match as they did before.
3. Confirm error shapes and stable action codes are unchanged for the folded verbs.
4. Confirm no `Parity1to1` tool moved (m70's guard test is the mechanism; this is the human read).
5. Record any drift found as a new inbox note rather than widening this milestone.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Every affected ADR018 row updated; zero ✅ cells lost.
- [ ] MCP-reachable field set matches REST and GraphQL.
- [ ] Error shapes unchanged; drift filed as notes, not fixed here.

## Out of scope

- Closing pre-existing parity gaps m70/t003 found.
