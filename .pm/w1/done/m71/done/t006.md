---
id: w1/m71/t006
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m71/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — behaviour-preserving only.

## Context

- The fold should have *removed* code. If `internal/apps/mcp.go` did not shrink meaningfully from 1,207 lines, the argument-struct handling probably grew to absorb what the tool registrations used to carry — check that.
- Look for a shared patch-dispatch helper across `update_service` / `update_postgres` / `update_env_group` rather than the same present-argument switch written several times.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the behaviour-preserving fixes.
3. Record the before/after line count and tool count.
4. File anything larger as an inbox note.

## Files

- the milestone's changed files

## Acceptance criteria

- [ ] Findings applied or deferred with a filed note.
- [ ] Net line count recorded and negative.
- [ ] `make lint` and the backend suite green.

## Out of scope

- Behaviour changes.
