> **Closed 2026-08-19 (w1 triage).** Deleted unused shadcn kit (`menubar`/`calendar`/`date-picker`/`pagination`/`input-otp`), unused hooks and scaffold, unused ai-elements barrel plus exclusive files (kept `task`/`reasoning`/`collapsible`), and moved `deploy-status.test.ts` into `__tests__/`. Dropped `nanoid`, `react-day-picker`, `@radix-ui/react-menubar`, `input-otp`. Kept `rxjs` and `@floating-ui/dom` as peer deps of `@apollo/client` / `@tiptap/suggestion`; kept `date-fns` (still used).

# Dashboard dead code + unused npm dependency deletion

From the 2026-08-19 refactor review (§3 dashboard, all verified by whole-src grep). ~1,680 source lines + ~835 test lines + 6 npm packages; very low risk (tree-shaking likely already excludes the code from the bundle — the win is install size, dependency-audit surface, CI time, and not certifying dead code with tests). Sub-2h; can stay a note or ride another dashboard milestone:

- Unused shadcn kit: `common/components/ui/menubar.tsx` (274, frees `@radix-ui/react-menubar`), `calendar.tsx` (211, frees `react-day-picker`), `date-picker.tsx` (168), `pagination.tsx` (127), `input-otp.tsx` (75, frees `input-otp`).
- Unused scaffold: `common/hooks/use-local-storage-state.ts` (202 + 402 test), `use-session-storage-state.ts` (118), `use-on-mounted.tsx` (56 + 171 test), `use-apollo-cache.ts` (21 + 64 test, referenced only by its own test), `common/components/page-header/` (21 — `routes/agents.tsx:72` defines its own), `responsive-tab-trigger-list/` (59 + 198 test).
- ai-elements: `index.ts` barrel (zero importers) + its exclusive referents `conversation/message/response/terminal/tool` (~349); keep `task.tsx`/`reasoning.tsx` (imported directly by `session-conversation-impl.tsx:42,47`) and `collapsible.tsx`.
- Unreferenced npm deps: `@floating-ui/dom`, `nanoid`, `rxjs` (+ the 3 freed above).
- Convention nit: move `features/deploys/lib/deploy-status.test.ts` into the sibling `__tests__/` per dashboard/CLAUDE.md.
