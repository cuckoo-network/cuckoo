---
id: w1/m64/t008
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m64/t007]
---

## Objective

Close the milestone once every other task is done **and the definition of done actually holds** (composer → session → evidence/PR → steer verified against a real backend).

## Context

- Follow `.pm/CLAUDE.md`: statuses to done, move tasks to `done/`, mark rows `— **DONE**`, set `**Status:** done`, move `w1/m64/` → `w1/done/m64/`, check the box in `w1/README.md`.

## Steps

1. Verify the DoD end to end.
2. Execute the closeout moves and status sync.

## Files

- `.pm/w1/m64/**`, `.pm/w1/README.md`

## Acceptance criteria

- [x] Milestone directory lives at `w1/done/m64/`; all three status locations agree.

## Status (2026-08-04)

**Superseded by the 2026-08-18 waiver below.** _(Original note:)_ BLOCKED on the DoD's live-substrate browser walk. t001–t007 are done; the full UI (list, composer, detail, conversation column, steering) is shipped + deployed to prod (dashboard image `b31301c8`) and render-verified locally against the offline stub — including a real prod bug the walk caught and fixed (the detail route was nested under the Outlet-less list route → renamed to the flat `agents_.$agentSessionId.tsx`). The one remaining DoD item — a logged-in walk against **real m43** (create → replay+live → steer → PR) recorded — needs an authenticated prod Kratos session that a headless Playwright browser can't obtain (dashboard uses host-scoped session-cookie auth; the API-key→Hydra flow only yields bearer tokens, not a Kratos session). The live conversation stream itself is independently proven by w3/m43's green live E2E; the dashboard's transport is unit-tested against that exact v1 wire format. Close this once an operator runs the logged-in walk (or explicitly waives it given the m43 E2E + local render evidence).

## Out of scope

- `w3/m43` closeout (its own t010); `w7/023` (token metering) promotion.

## Closeout (2026-08-18) — live-substrate walk **waived** by the user

The DoD's one outstanding item — an operator running the logged-in prod walk (create → replay+live → steer → PR) — was **explicitly waived by the user** on 2026-08-18, as this task's own status note anticipated ("or explicitly waives it given the m43 E2E + local render evidence"). Recorded here so the waiver, not an unverified claim, is what closes the milestone.

Evidence the waiver rests on:

- **The live stream is proven where it lives**: `w3/m43`'s green live E2E exercises the real `api.bex.co/v1/agent-sessions/{id}/stream` endpoint end to end on the real substrate.
- **The dashboard transport is unit-tested against that exact v1 wire format** (ticket-header transport, `prepareReconnectToStreamRequest` re-mint, `data-acp` part decoding) — 58 tests added in t007, 1905 total green.
- **The full UI is render-verified locally**, including the degraded states; the walk that was performed found and fixed a real prod bug (the detail route nested under the Outlet-less list route → flat `agents_.$agentSessionId.tsx`, now pinned by the route-inventory test).
- **The code is shipped and deployed to prod** (dashboard image `b31301c8`).

What is therefore *not* claimed: no logged-in browser walk against a real prod agent session was recorded. Anyone wanting that evidence should run it manually — headless Playwright cannot obtain a prod Kratos session (host-scoped session cookie; the API-key→Hydra flow yields bearer tokens only), and driving it would create a real billable session and a real draft PR.
