---
id: w1/m64/t004
title: /agents/$id detail: metadata header + PR/evidence cards + live conversation + steering
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m64/t002, w1/m64/t003, w3/m43]
---

## Objective

The session detail page: control-plane metadata (header, PR card, evidence, failure) from polling + t002's conversation column live against the real m43 endpoint + the steering composer routed by session state. This task's verification is the milestone's product moment.

## Context

- `routes/agents.$agentSessionId.tsx`; `services.$serviceId` pending-component/code-split convention (mirror vite + vitest configs).
- Metadata (t001 hooks): `phase`, `turns`, `deliveryMode`, `headSha`/`prUrl`/`prNumber`, `evidence` (bounded: commandLog≤40, testOutput≤40, outputTail≤8KB, changedFiles≤100, `truncated`), `failureReason`.
- Conversation: t002's column — replay+live for running, full replay for terminal. The transcript (not evidence) is the primary narrative; evidence panels are the durable summary cards beside it.
- Steering routing (§ D9): live session ⇒ chat `POST` via the conversation composer (`sendMessage`); idle/terminal-successful ⇒ `resume`/steer redispatch via t001 mutations; disabled with reasons on `canceling`/`canceled` and while a turn is in flight.
- Cancel: `cancelAgentSession` behind confirm; copy states pushed work is preserved.

## Steps

1. Route + pending component per convention.
2. `components/session-detail-header.tsx`: phase chip, duration, turns, deliveryMode, cancel-with-confirm.
3. Compose: header + conversation column (center) + `components/{pr-card,evidence-panel}.tsx` (side/below).
4. `components/steering-composer.tsx`: one composer, state-routed (chat POST vs redispatch mutation); disabled reasons.
5. Degraded states: m43 unavailable ⇒ conversation column shows the house "not configured/unavailable" callout, metadata still renders; mid-turn stream drop reconnects without duplicate parts (t002 transport).
6. **Live walk against m43 on the real substrate**: create → watch replay+live → steer mid-run → cancel/complete → terminal replay; record evidence.

## Files

- `dashboard/src/routes/agents.$agentSessionId.tsx`
- `dashboard/src/features/agent-sessions/components/{session-detail-header,pr-card,evidence-panel,steering-composer}.tsx`
- `dashboard/vite.config.ts` + `dashboard/vitest.config.ts`

## Acceptance criteria

- [ ] Terminal session: full transcript renders from replay; PR card links the draft PR; evidence panels show truncation honestly.
- [ ] Running session: live parts append; mid-run steer via chat POST executes on the same branch and appears in the transcript; `turns` increments on next poll.
- [ ] Failed/canceled/unavailable states all render honestly (named reason / preserved-work copy / house callout).
- [ ] The live walk is recorded (m41-style evidence note).

## Out of scope

- Mobile surfaces (`w11/m6`/`m7`); raw-ACP IDE attach; token/cost display (w7/023).
