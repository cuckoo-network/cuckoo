---
id: w1/m64/t005
title: Render parity
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m64/t004]
---

## Objective

Agent sessions are a bex extension (Render has no equivalent product — ADR018 bex-extension row), so parity here means **cross-surface consistency**: the dashboard must expose the same fields, semantics, and error shapes as REST (`/v1/agent-sessions*`), GraphQL, and the six MCP tools (m39/m41 `View` contract), and its interaction patterns must match the house Render-derived conventions (chips, confirms, degraded states) used by the services/deploys pages.

## Context

- Contract: `lego/backend/internal/agentsessions/{models.go,graphql.go,mcp.go}` — one `View` across surfaces.
- Exemplar-shape check (ADR047 D9 table stakes): grouped activity, diff-first PR card, steering composer, explicit cancel semantics, status/duration metadata.

## Steps

1. Field-by-field walk: every `View` field either rendered or deliberately omitted (note why).
2. Error-shape walk: each `AGENT_SESSION_*` code has a distinct, human-readable UI surface.
3. Flag any drift (missing fields, semantic divergence) as follow-up notes rather than silently diverging.

## Files

- audit only; notes to `w1/NNN.md` if drift found

## Acceptance criteria

- [ ] Written field/error coverage checklist in the task file or PR description; drift items filed.

## Out of scope

- Building the flagged follow-ups.
