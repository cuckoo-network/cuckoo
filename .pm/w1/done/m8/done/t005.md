---
id: w1/m8/t005
title: "Simplify — run /simplify over the code this milestone changed"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m8/t004]
---

## Objective

Run `/simplify` over the milestone's diff (types catalog, operator lookup, store validation/default, apps feature plan verb) — reuse, simplification, efficiency; behavior-preserving. Likely candidates: the plan↔tier mapping helpers appearing in more than one package, and validation error-message construction.

## Context

- Standing closing task (board convention).

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; `make test` + backend tests + `make lint` green.

## Files

- `lego/types/`, `lego/operator/internal/controller/`, `lego/backend/internal/{store,apps}/`

## Acceptance criteria

- [ ] `/simplify` run and findings applied or explicitly declined.
- [ ] `make test`, backend tests, and lint green afterward.

## Out of scope

- New behavior of any kind.
