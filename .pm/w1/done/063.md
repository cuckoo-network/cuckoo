> **Closed 2026-08-19 (w1 triage).** Implemented the dead-code half: deleted `Service.SetSource` (callers already used `SetSourceAndRegistryCredential`; historical `apps.SetSource` audit rows still map in `events/service.go`) and deleted the `GetBlueprint` alias (GraphQL already called `GetBlueprintByID`). Remaining extracts (`stampNewApp`, `blueprintScope`, `recordSyncRun`, `ListDomainsPage`) ride `.pm/FUTURE-MAYBE.md` § "Architecture-review leftovers (2026-08-19)".

# apps package dedup: stampNewApp, blueprintScope, recordSyncRun, ListDomainsPage, dead verbs

From the 2026-08-19 refactor review (§2.8, §3 backend-apps). ~160 lines, all LOW risk; promote together when scheduled:

- **`stampNewApp`** — `create` (service.go:1607-1622) and `createNewApp` (:1665-1702) duplicate the CR identity stamp (name → `core.CRName` → tenant labels → namespace → environment membership) on the w4/m19 bare-name-collision safety path; the second copy's comment even points at the first. Extract; keep each caller's own tenant resolution bit-identical.
- **`blueprintScope(ctx, ownerID, relation)`** — the 4-line workspace+authorize+store-guard preamble repeated at blueprint.go:242, 342, 384, 471, 500, 544, 710, 736, 790 (validate must NOT require the store — :126 documents why; make the guard a flag).
- **`recordSyncRun`** — `CreateBlueprint` (:437-463) and `runSync` (:645-686) hand-roll the same insert-run→apply→stamp-status lifecycle; the `AutoSync→Paused` asymmetry (:673) becomes an explicit parameter instead of an accident.
- **`ListDomainsPage`** — domain filter + `core.StablePage` currently in both adapters (rest.go:1025-1041, mcp.go:872-892) against ADR006's filtering-in-core rule.
- **Dead:** delete `SetSource` (service.go:2986 — zero production callers, one test) and the `GetBlueprint` alias (blueprint.go:494 — point graphql.go:993 at `GetBlueprintByID`).

**Declined by the review — do not do:** a generic setter helper over the `Set*` wall (already factored via `writeThroughStoreFetched`); unifying the create-owned field tables (`applyCreateToSpec` vs `blueprintServiceFieldAppliers` — omission semantics genuinely differ, a merge is a behavior change; at most extract the shared field-name list); `applyStackDatastore` generics (only if already in the file); `ConfigureMaintenanceMode` indirection (load-bearing for `callerVerb` audit vocabulary).
