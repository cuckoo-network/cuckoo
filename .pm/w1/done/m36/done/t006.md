---
id: w1/m36/t006
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m36/t005]
---

## Objective

Run `/simplify` over the changed provisioning/CI files (cluster.yaml templates, bake script, workflow); behavior-preserving.

## Acceptance criteria

- [x] `/simplify` run (4 cleanup agents over the working-tree diff); accepted findings applied.

## Outcome (2026-07-14)

Ran ahead of t005 (its blocker was the live measurement, which is operator-gated; the diff was ready to review). Four parallel agents (reuse/simplification/efficiency/altitude) confirmed the diff is already minimal and every altitude call is correct. Applied the behavior-preserving findings:

- **Pin-parity guard** (reuse + altitude, independently): `scripts/clusterapi-validate.sh` now asserts the packer pins (k8s/containerd/runc) equal `cluster.yaml`'s `version:` + the CP `preKubeadmCommands` exports — closing the one drift the "duplicated but bumped together" design was exposed to.
- **Dropped 2 redundant swap lines** from the packer bake (each node's `preKubeadmCommands` already own swap-disable; kubeadm never runs at bake time).
- **Single-pass KCT check** in the validator (was an N+1 nested-yq loop) + **hoisted the trimmed-package regex** to one `TRIMMED_PKGS` var reused by the overlay + packer checks.

Skipped (correct as-is, per the reviews): unifying the version literals across packer/overlay (two different tools apply them; the new parity guard covers the drift), consolidating the sibling validate scripts / workflows (correct reuse of the repo's per-tree convention), and YAML-anchoring the two worker `preKubeadmCommands` blocks (they legitimately diverge — tenant has the zot certs.d, platform doesn't).
