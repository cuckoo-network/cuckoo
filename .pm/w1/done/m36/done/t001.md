---
id: w1/m36/t001
title: Bake a Hetzner snapshot image + `HCloudMachineTemplate.imageName`
worker: worker1
status: done
estimate: 60m
depends_on: []
---

## Objective

Produce a Hetzner snapshot image with runc, containerd, and kubelet (the ~150MB the preKubeadmCommands download today) preinstalled at the pinned versions, and point the worker `HCloudMachineTemplate`s at it via `imageName`. Packer if cheap, else a documented one-off bake (the FUTURE-MAYBE entry allows either — record which and why).

## Context

- `infra/clusterapi/overlays/hetzner-caph/cluster.yaml` — preKubeadmCommands carry the downloads; version pins must match what kubeadm expects.
- ⚠️ Any `KubeadmConfigTemplate`/template change rolls the worker pools — safe now (w1/m26: images pullable from Zot), but sequence the roll deliberately and watch Apps come back.
- Record the bake recipe in the repo (script or doc) so the image is reproducible at the next k8s version bump.

## Files

- `infra/clusterapi/overlays/hetzner-caph/cluster.yaml`, new bake script/doc under `infra/` or `scripts/`

## Acceptance criteria

- [ ] A node provisioned from the snapshot joins without downloading runc/containerd/kubelet (provision log verified); recipe committed.

## Out of scope

- CP node image (CP is quota-parked per FUTURE-MAYBE; workers only).
