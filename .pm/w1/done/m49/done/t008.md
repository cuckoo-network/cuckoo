---
id: w1/m49/t008
title: Render parity — cross-surface consistency + live dev-1 walk + ADR018 update
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m49/t005, w1/m49/t006, w1/m49/t007]
---

## Objective

Standing parity gate: confirm the milestone left REST/GraphQL/MCP/UI consistent, walk the whole webhook lifecycle live on dev-1 the way a Render user would, and update the parity ledger.

## Context

- The 2026-07-17 backend audit found REST/GraphQL/MCP already Render-aligned (full `/v1/webhooks` family incl. PATCH + `{id}/events`; `eventFilter` alias; mint-once documented); this milestone is UI-side, so the check is that no drift was **introduced** (e.g. the dashboard's update payload uses the same field semantics as REST's `updateEndpointRequest`; a t006 reveal — if chosen — landed on both REST and GraphQL, not just one).
- Live walk on dev-1 (`bash .pm/w1/dev-1/up.sh`): create via `/webhooks/new` (real click, ~800px viewport — the old bug's repro), land on `/webhook/<id>`, deliveries appear after triggering a subscribed event (a deploy on a dev-1 app), edit name/URL/events in Settings, toggle status, type-to-confirm delete.
- Compare side-by-side against dashboard.render.com using the t001 artifact; new divergences get recorded there, not silently shipped.
- `docs/ADR018-render-parity.md` webhook row's UI cell updates with evidence (route paths + what was walked).

## Steps

1. Cross-surface check: field names/semantics/error shapes for update across dashboard GraphQL usage vs REST PATCH vs MCP tool.
2. The live dev-1 walk above, screenshots into `.playwright-mcp/`.
3. Update `docs/ADR018-render-parity.md` (webhook UI cell) and append walk evidence to `docs/render-artifacts/webhooks-ui.md`; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/webhooks-ui.md`

## Acceptance criteria

- [ ] Update semantics identical across surfaces (or divergence filed as follow-up work, never silent).
- [ ] Full lifecycle walked live on dev-1 with real clicks, including at the old bug's viewport height.
- [ ] Ledger + artifact updated with evidence.

## Out of scope

- Prod deploy (rides the normal `deploy.yml` pipeline).
