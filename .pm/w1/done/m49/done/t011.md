---
id: w1/m49/t011
title: Closeout — verify DoD, sync status, move to done
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m49/t009, w1/m49/t010]
---

## Objective

Close the milestone once its definition of done actually holds — the observable end state is real on dev-1, not merely written.

## Context

- DoD checklist lives in `w1/m49/README.md`.
- Closing procedure is `/pm`'s `done` subcommand: every task `status: done` and moved to `w1/m49/done/`, README rows `— **DONE**` + `**Status:** done`, whole directory to `w1/done/m49/`, checkbox in `w1/README.md`. Never leave a tombstone at the old path.

## Steps

1. Re-verify each DoD bullet against the live dev-1 stack.
2. Run the closing procedure; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m49/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Every DoD bullet demonstrably true at close time.
- [ ] Status synced in all three places; milestone lives at `w1/done/m49/` with no stub left behind.

## Out of scope

- New feature work (file follow-ups as inbox notes instead).
