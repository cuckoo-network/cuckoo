---
id: w1/m49/t010
title: Test coverage — picker logic, route flows, edit wiring
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m49/t008]
---

## Objective

Meaningful tests for the behavior this milestone shipped — asserting real interactions and failure modes, never snapshot filler.

## Context

- Test conventions: vitest, `__tests__` adjacent to code (`dashboard/CLAUDE.md`); the webhooks feature already has dialog/panel tests to migrate rather than delete (the modal tests' behavioral assertions move to page tests).
- High-value coverage: picker tri-state/cascade/search/unknown-key fallback (t002 seeds these; extend); create-page submit gating + secret-shown-once flow; detail-page deliveries filtering; Settings dirty-tracking + update mutation payload shape + named-error surfacing; type-to-confirm delete gate (exact-match only); ScrollArea regression (t007).
- Mock at the Apollo layer per existing feature tests; no live backend in unit tests.

## Steps

1. Migrate the modal's behavioral tests to the create page; delete only what is modal-chrome-specific.
2. Add the coverage list above where t002/t007 didn't already.
3. `yarn typecheck && yarn lint && yarn test` green.

## Files

- `dashboard/src/features/webhooks/**/__tests__/`
- `dashboard/src/routes/__tests__/` (if route-level tests exist as a pattern)

## Acceptance criteria

- [ ] Every acceptance criterion from t002–t007 that is unit-testable has a test.
- [ ] Failure modes covered: invalid URL error surfaced, empty event selection blocked, wrong delete-confirm text keeps the button disabled.
- [ ] Full dashboard suite green.

## Out of scope

- E2E/browser automation (t008's live walk covers that).
