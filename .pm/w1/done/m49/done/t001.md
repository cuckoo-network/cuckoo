---
id: w1/m49/t001
title: Capture the Render webhook-UX artifact (docs/render-artifacts/webhooks-ui.md)
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Write `docs/render-artifacts/webhooks-ui.md` recording Render's webhook dashboard UX and bex's current state, from the 2026-07-17 live walk of both dashboards — the reference the rest of the milestone builds against (the pattern of `docs/render-artifacts/owners-api.md` and `live-deploy-following.md`).

## Context

- Walk already performed 2026-07-17; screenshots exist in `.playwright-mcp/`: `render-webhooks-new.png`, `render-webhook-settings.png`, `bex-webhook-modal.png`, `bex-webhook-modal-overlap.png`, `bex-webhook-secret.png`.
- Render `/webhooks/new` (full page, "Create a new Webhook"): Name ("A unique name for this webhook."), Endpoint URL ("The webhook sends each notification to this URL as a POST request."), Subscribed Events picker with "N events selected" counter, "Search for events" box, tri-state "All events" master checkbox, collapsible groups (Autoscaling, Cron Job Run, Deploy, Disk, Key Value, Maintenance, Maintenance Mode, Postgres, Server Status, Suspension, Zero Downtime Redeploy + ungrouped singles: Branch Deleted, Commit Ignored, Instance Count Changed, Instance Type Changed, Job Run Ended) with group-checkbox cascade; human-readable labels; ~60 event types.
- Create → `/webhook/<whk-id>`: header (name + Enabled badge, "Webhook ID:" + copy, URL link + copy, event chips with "Show N more", "Created by <email> on <date>"), per-webhook sidebar (All webhooks / Activity / Settings), Activity = "Recent deliveries" (All/Successful/Failed tabs, refresh, empty state), Settings = Status toggle ("Webhooks do not send any notifications while disabled."), editable Name/URL, **Signing secret retrievable anytime** (masked `whsec_…`, Show secret + Copy, "A token used to sign and verify request payloads."), editable events + "Save changes", Delete with type-to-confirm `delete webhook <name>`.
- bex today: `/webhooks/new` 404s; modal create with a flat alphabetical list of 17 raw snake_case keys; one-time secret reveal; delivery-history modal; enable/disable + delete only — **no edit**, despite `updateWebhookEndpoint`/`PATCH /v1/webhooks/{id}`/`update_webhook_endpoint` all being live backend verbs (w3/m11).
- The live modal bug (evidence for t003/t007): `create-webhook-dialog.tsx:149` puts `max-h-56` on the shadcn ScrollArea **root**; the Radix Viewport is `size-full` inside an auto-height root, so nothing constrains — the 17-row list renders ~680px tall, overflows `DialogContent` (no max-h/overflow), and the footer's Create button is hit-test-intercepted by event-row labels (rows measured to y≈1209 vs dialog bottom y≈839; Create at y≈778 resolves to the `maintenance_mode_enabled` row). Real clicks toggle a checkbox instead of submitting, at ~800px and 1100px viewport heights.

## Steps

1. Write the artifact with sections: Render `/webhooks/new` shape (fields, helper copy, picker anatomy), Render `/webhook/<id>` detail (header, Activity, Settings incl. the retrievable secret + type-to-confirm delete), bex current state, the modal bug (mechanism + measurements), and a divergence table (secret contract, created-by, event-vocabulary size).
2. Reference the screenshot filenames; do not commit binary screenshots into `docs/`.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/render-artifacts/webhooks-ui.md` (new)

## Acceptance criteria

- [ ] Artifact reproduces both dashboards' shapes precisely enough to build t002–t005 without re-visiting Render.
- [ ] The modal bug is recorded with its mechanism and measurements.
- [ ] Divergences (secret contract, created-by, vocabulary size) each have an explicit keep/close disposition pointer (t006 for the secret).

## Out of scope

- Any code changes.
