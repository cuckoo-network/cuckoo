---
id: w1/m49/t003
title: /webhooks/new create page; retire CreateWebhookDialog
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m49/t002]
---

## Objective

Add the first-class `/webhooks/new` route (Render's exact path — today it 404s on bex) rendering the create form as a page — Name, Endpoint URL, the t002 event picker — and make the `/webhooks` page's "Add webhook" button navigate there instead of opening the modal. Delete `CreateWebhookDialog`.

## Context

- Routes are TanStack file-based under `dashboard/src/routes/`; the create-landing precedent is w1/m45's `new.*` aliases and `services.new.tsx`. New file: `webhooks.new.tsx`.
- The existing create mutation + one-time-secret handling live in `use-create-webhook.ts` (fetchPolicy `no-cache`; secret only in local state) — reuse the hook unchanged; the mint-once display contract stays unless t006 decides otherwise.
- Render's page shape (t001 artifact): heading "Create a new Webhook", helper copy under each label, "Create Webhook" submit at the bottom; on success Render redirects to the detail page. bex flow: on success show the secret step (same copy-once warning as today), whose Done navigates to `/webhook/<id>` (t004; until t004 lands, `/webhooks` is an acceptable interim target).
- This kills the live blocking bug: the modal's events list overflows `DialogContent` and hit-test-intercepts the Create button (`create-webhook-dialog.tsx:149`, mechanism in t001/t007). The modal is deleted, not fixed.
- `webhooks-panel.tsx:71` hosts the dialog trigger today.

## Steps

1. Create `dashboard/src/routes/webhooks.new.tsx` (auth-guarded like sibling routes) rendering the create page with the t002 picker; disable submit until URL + ≥1 event (current `submittable` rule).
2. On success, render the secret step in-page (code block + Copy + the "won't see it again" warning), then navigate to the created webhook's detail page.
3. Point "Add webhook" on `/webhooks` at the new route; delete `create-webhook-dialog.tsx` and its dialog-specific tests; keep `use-create-webhook.ts`.
4. `yarn generate-routes` / typecheck; update any `?new=` create-landing wiring if `/new/…` menus reference webhooks.

## Files

- `dashboard/src/routes/webhooks.new.tsx` (new)
- `dashboard/src/features/webhooks/components/webhooks-panel.tsx`
- `dashboard/src/features/webhooks/components/create-webhook-dialog.tsx` (delete)
- `dashboard/src/features/webhooks/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] `GET /webhooks/new` renders the create page (no 404, no dialog); "Add webhook" navigates there.
- [ ] Create succeeds through the page — the Create button is genuinely clickable with 17+ events listed at an ~800px-tall viewport (the modal-bug repro case).
- [ ] Secret shown once with copy affordance; Done lands on the webhook's page; refreshing never re-exposes the secret.
- [ ] `CreateWebhookDialog` no longer exists in the tree.

## Out of scope

- The detail page itself (t004) and edit (t005).
- Changing the secret contract (t006).
