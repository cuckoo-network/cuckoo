---
id: w1/m22/t009
title: Closeout — verify DoD holds, then move the milestone to done/
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m22/t008]
---

## Objective

When every other m22 task is done **and the DoD is actually met**, close the milestone.

## Context

- DoD: an HA Postgres provisions as a replicated CNPG cluster, failover promotes a standby, read replicas are reachable, and `highAvailabilityEnabled` is truthful across REST/GraphQL/MCP/UI.

## Steps

1. Confirm the observable end state (a running HA DB with a successful failover + reachable replica) — not just code written.
2. Mark all tasks done, move `tNNN.md` → `w1/m22/done/`, update the README rows + status.
3. Move `w1/m22/` → `w1/done/m22/` and check `- [x]` in the w1 README.

## Files

- `.pm/w1/m22/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Milestone lands in `w1/done/m22/` only once the observable end state holds.

## Out of scope

- Any new work — this is the move.
