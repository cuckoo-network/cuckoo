---
id: w1/m22/t004
title: REST/GraphQL/MCP HA surface (enableHighAvailability, failover, readReplicas)
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m22/t002, w1/m22/t003]
---

## Objective

Expose HA over all three bex-api surfaces with Render-identical shape: `enableHighAvailability` (create/update), a `failover` action, and `readReplicas` (named replica resources) in the connection-info; `highAvailabilityEnabled` reports the real state.

## Context

- `lego/backend/` imports `types/`; feature-package-per-feature (m2.5). The Postgres surface already exists from m17 — extend it.
- Render (verified against the OpenAPI captured in t001): `enableHighAvailability` (bool) + `readReplicas` (array of named objects) are **two independent create-body fields**; failover is `POST /v1/postgres/{postgresId}/failover` → `202` (no `promote`); read model reports `highAvailabilityEnabled`. Match field names + error shapes exactly.
- Today bex hardcodes `highAvailabilityEnabled: false`; wire it to the CR state.

## Steps

1. Extend the Postgres core: `enableHighAvailability` toggle, `failover` verb, `readReplicas` (named) in connection info — keeping HA and read replicas as independent inputs.
2. Wire REST, GraphQL, and MCP adapters; make `highAvailabilityEnabled` reflect the CR.
3. Match Render's field names, error shapes, and the primary-vs-per-replica URL distinction.

## Files

- `lego/backend/internal/<postgres feature>/…` (core + REST/GraphQL/MCP adapters)

## Acceptance criteria

- [ ] Enable HA, trigger failover, and read replica URLs over REST; GraphQL + MCP identical; `highAvailabilityEnabled` is truthful.
- [ ] Backend `go build`/`go test` green; `make lint-backend` clean.

## Out of scope

- Mechanism (t001–t003); dashboard (t005).
