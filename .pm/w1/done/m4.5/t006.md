---
id: w1/m4.5/t006
title: Test coverage — meaningful tests for the sleep UI + idleTTL surface
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m4.5/t004]
---

## Objective

Meaningful tests for what shipped: the Sleeping-vs-Suspended discrimination (all phase×suspended combinations), the Settings control's round-trip + per-tier disable, and the backend idleTTL mutation (bounds, authz gate — `TestAuthzGuardsEveryVerb` covers the new verb). Assert real behavior and failure modes; no snapshot-everything.

## Acceptance criteria

- [ ] Mapper matrix tested; invalid TTL rejected with a clear error; unauthorized mutation 403; dashboard vitest + backend `go test` green.
