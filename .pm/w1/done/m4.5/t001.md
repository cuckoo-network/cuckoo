---
id: w1/m4.5/t001
title: Capture Render's free-instance sleep UX + map bex states (Hibernated vs Suspended)
worker: worker1
status: done
estimate: 25m
depends_on: []
---

## Objective

Ground the design in Render's actual UX before building (the m7/m6.5 pattern): capture how Render presents a spun-down free instance (banner copy, status badge, wake behavior, where the "spins down with inactivity" notice lives), then write the bex state mapping: `PhaseHibernated` set by **manual suspend** today vs the m4 **auto-sleep** — decide whether the App CR/status needs a way to tell them apart (e.g. a status reason/condition) and record the decision for t002.

## Context

- Playwright MCP is configured; screenshots with **bare filenames** land in `.playwright-mcp/` (gitignored). Read-only browsing of a Render free service.
- Current states: `status.phase` (`Running`/`Hibernated`/…), `spec.suspended` (manual). m4 adds idle→sleep on `spec.idleTTLSeconds`.
- Dashboard status rendering: `dashboard/src/features/services/` (list + overview badge components).

## Steps

1. Capture Render's sleeping-service list row, service page, and settings copy — expected finding to confirm with evidence: Render shows a spun-down free service as live (banner only, no sleeping badge) and has no idle-timeout setting (fixed ~15-minute window).
2. Write the state-mapping note (Sleeping = hibernated && !suspended, or a new status reason — pick with evidence) into this task file / the milestone README, and record the confirmed divergences (badge, knob) as bex extensions.
3. List the exact UI strings needed (i18n keys planned up front); the wake/cold-start hint copy should track Render's banner wording.

## Files

- `.playwright-mcp/` captures (gitignored); state-mapping note appended to `.pm/w1/m4.5/README.md`

## Acceptance criteria

- [ ] Captures exist; the Sleeping-vs-Suspended discrimination rule is written down and implementable from `services` query fields (or names the missing field for t003's API gap list).

## Out of scope

- Building any UI (t002+).
