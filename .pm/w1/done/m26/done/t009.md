---
id: w1/m26/t009
title: Test coverage
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m26/t007]
---

## Objective

Standing closing task: add meaningful tests/checks for the behavior this milestone shipped — real behavior and failure modes, not tautological or snapshot-everything tests.

## Context

- Most of this milestone is infra/config (CAPH template, NetworkPolicy, Zot retention) which is harder to unit-test — favor CI-checkable assertions over unit tests where code doesn't apply.

## Steps

1. Migration-ownership guard (t006): a real test/CI check that catches a table with the wrong owner.
2. Reconciler fix (t005): a test that asserts `generation` doesn't increment on a no-op reconcile.
3. Where full automation isn't feasible (e.g. the CAPH template itself), document the manual verification performed in t007 as the test record.

## Files

- `lego/backend/internal/store/*_test.go` (ownership guard)
- `lego/operator/internal/controller/*_test.go` (reconciler no-op test)

## Acceptance criteria

- [ ] The ownership guard has a test that fails on a mis-owned table.
- [ ] The reconciler fix has a test that fails without the fix (regression-proof).

## Out of scope

- Testing CAPH/cloud-init changes with anything beyond the live verification in t007.
