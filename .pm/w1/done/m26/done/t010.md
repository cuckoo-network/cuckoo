---
id: w1/m26/t010
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m26/t008, w1/m26/t009]
---

## Closeout record (2026-07-13)

DoD confirmed genuinely met, live on prod:

- **Fresh-node pull (t007): PASS** — a brand-new autoscaled tenant node pulled a
  git-built image on first attempt, zero manual config (evidence in `done/t007.md`).
- **Cilium policy survives Argo sync** — `allow-node-image-pulls` is Argo-managed
  (`bex-platform-prod`, Synced) and VALID.
- **Zot volume bounded** — GC + `storage.retention` (keep 5) active on the running
  registry; `ZotRegistryFillingUp` alert live (de-duplicated in t008).
- **Migration-ownership drift fixed + guarded** — `0017_fix_ownership` +
  `store.CheckOwnership` + `TestMigrationNumbersAreUnique` in HEAD.

Milestone moved to `w1/done/m26/`; box checked in `w1/README.md`.

The two config fixes this milestone made (`deploy/gitops/base/zot.yaml`
retention-placement, `prometheus.yaml` alert de-dup) ride the next `/ship` to
become durable in Argo — the standard commit→ship→sync flow every milestone
closes through.

## Objective

Standing closing task: when every other task is complete and the milestone's Definition of Done actually holds (live-verified, not just code-complete), close the milestone.

## Context

- Per `.claude/skills/pm/SKILL.md`'s `done` subcommand: this task's completion moves the whole milestone to `w1/done/m26/`.

## Steps

1. Confirm the DoD in `README.md` is genuinely met (t007's live verification passed).
2. Set every task's `status: done`, move each `tNNN.md` to `w1/m26/done/`.
3. Mark every row `— **DONE**` and `**Status:** done` in this milestone's `README.md`.
4. Move the whole `w1/m26/` directory to `w1/done/m26/`.
5. Check `- [x]` in `w1/README.md`.

## Files

- `.pm/w1/m26/` (moves to `.pm/w1/done/m26/`)
- `.pm/w1/README.md`

## Acceptance criteria

- [ ] Milestone directory moved, all three status locations in sync.

## Out of scope

- Re-opening scope not already in this milestone's task list.
