---
id: w1/m26/t008
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m26/t007]
---

## Simplify record (2026-07-13)

Reviewed the m26 diff (CAPH template, `network-policies.yaml`, `zot.yaml`,
`prometheus.yaml`, `autoscale.go`/`app_controller.go`, `webhook.go`, `migrate.go`).

**Applied:** removed a duplicate `ZotRegistryFillingUp` alert in
`deploy/gitops/base/prometheus.yaml` — a residual of the w1/w2 milestone collision.
Two rules with the same name + identical expr fired together with contradictory
text ("keep 5 generations" `for:10m` in the `platform` group vs the stale "last 10
tags" `for:15m` in the `bex` group; retention actually keeps 5). Kept the correct
one; validated the rule pack still extracts clean (`yq` + the CI promtool gate).

**Considered, kept as-is (behavior-preserving, not simplify targets):**

- `webhook.go` error-swallow (log + 200 on a per-app redeploy failure) and the
  `autoscale.go` annotation-not-`spec.replicas` fix guard two _independent_ churn
  sources (GitHub webhook retries vs. autoscaler ticks) — defense-in-depth, removing
  either changes behavior.
- The Zot alert has no promtool unit test — consistent with `alerts_test.yml`'s own
  documented convention (trivial threshold rules aren't unit-tested).

## Objective

Standing closing task: run `/simplify` over the code/config this milestone changed (reuse/simplification/efficiency, behavior-preserving).

## Context

- Applies to the CAPH machine template changes, the NetworkPolicy manifest, the Zot retention config, the Alertmanager rule, the reconciler fix, and the migration-ownership guard.

## Steps

1. Run `/simplify` over the diff from t001-t007.
2. Apply any behavior-preserving cleanups it finds.

## Files

- Everything touched by t001-t007.

## Acceptance criteria

- [ ] `/simplify` run completed and findings applied or explicitly dismissed.

## Out of scope

- Bug hunting — that's `/code-review`, not this task.
