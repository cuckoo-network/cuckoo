---
id: w1/m26/t004
title: Zot volume-usage alert rule in the platform Alertmanager pack
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m26/t003]
---

## Objective

Add a Zot PVC volume-usage alert rule to the platform Alertmanager rule pack shipped in `w3/m6`, so a filling registry pages instead of silently 500ing on pushes.

## Context

- `w3/m6` (Platform alerting: Alertmanager + rules for bex's own health) is done — this extends its rule pack rather than standing up new alerting infra.
- The incident this closes: Zot's volume hit 99% and blob PUTs started returning 500 with no warning before that point.

## Steps

1. Find the existing Alertmanager rule-pack location (from `w3/m6`'s shipped config).
2. Add a rule on Zot's PVC usage (e.g. `kubelet_volume_stats_used_bytes / kubelet_volume_stats_capacity_bytes` for the Zot PVC), warning threshold before it's critical.
3. Verify the rule fires in a dry-run/test scenario (or via `promtool` rule check if used elsewhere in the repo).

## Files

- wherever `w3/m6` put its Prometheus rule files (check that milestone's shipped diff)

## Acceptance criteria

- [ ] Rule is present and passes `promtool check rules` (or equivalent) if the repo uses that pattern.
- [ ] Threshold gives enough lead time to act before pushes start failing (learn from the retention policy's cap in t003).

## Out of scope

- General Zot dashboarding beyond the alert rule itself.
