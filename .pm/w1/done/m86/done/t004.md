---
id: w1/m86/t004
title: Service-disk metrics wiring for the usage graph
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m86/t003]
---

## Objective

Expose the disk PVC's used/capacity series to the dashboard graph — observability only, never billed (ADR082 D9).

## Context

- Extend the datastore disk-metrics path with a third PVC-name pattern `^disk-<name>$`: `lego/backend/internal/metrics/` (datastore.go, source.go, rest/graphql/mcp fragments) reading `kubelet_volume_stats_{used,capacity}_bytes`.
- Scope-matrix entries exist for datastore disk routes (`scope_matrix_ops.go:513-514`) — mirror for the service-disk route/field.

## Steps

1. Series plumbing + surface field; dashboard graph binds to it.

## Files

- `lego/backend/internal/metrics/`, `dashboard/src/features/metrics|disks`

## Acceptance criteria

- [ ] Graph renders real series on dev-1 (or the metrics fake in tests); no new usage meter rows appear.

## Out of scope

- Billing (m84 owns the meter; this is display only).
