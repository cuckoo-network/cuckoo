---
id: w1/m86/t001
title: "Blueprint: flip the four disk capability entries"
worker: worker1
status: done
estimate: 1h
depends_on: []
---

## Objective

Turn the fail-closed `disk` Blueprint fields into translated handlers with ADR049 D5 presence-aware semantics (ADR082 D7).

## Context

- `lego/backend/internal/apps/schema/capabilities.json` — the four `unsupported` entries (`#/definitions/disk/{mountPath,name,sizeGB}` at :257-267, `#/definitions/serverService/properties/disk` at :595-597) currently cite the reversed anti-goal.
- Compiler: `blueprint_compiler.go` / `blueprint_ir.go` / `blueprint_plan.go`. Semantics: create default `sizeGB: 10`; omission on adopt/sync preserves the existing disk; explicit shrink fails validation pre-write; block removal never deletes (no-sync-delete); web/pserv/worker only.
- Fail-closed both ways: translate fully or refuse the file — no lossy approximation (ADR049 invariant).

## Steps

1. Registry flips with new reason strings citing ADR082; handler + plan actions; validation errors with source locations.

## Files

- `lego/backend/internal/apps/schema/capabilities.json`, `blueprint_*.go`

## Acceptance criteria

- [ ] Corpus rows (t008) for: create default, adopt-omission preserve, shrink refusal, removal no-delete, cron/static disk refusal.
- [ ] No remaining registry reason cites disks as a non-goal.

## Out of scope

- Previews (still rejected — ADR049 unchanged there).
