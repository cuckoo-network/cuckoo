`gitops (render)` CI check is red on `main` — two pre-existing, unrelated failures, confirmed (via a clean worktree at the commit before w4/m11/t009's fix) to predate that work entirely:

1. `helm template traefik 41.0.0 -f deploy/gitops/base/values/traefik.values.yaml` fails schema validation: `additional properties 'logs' not allowed`. The traefik chart was likely bumped to 41.0.0 without updating `deploy/gitops/base/values/traefik.values.yaml`'s `logs` key (removed/renamed in that chart version's schema).
2. `promtool check rules` fails a rules test: `BackupCronJobStale`'s expected annotation text in `deploy/gitops/base/prometheus.yaml`'s test fixture is stale — missing a `docs/ADR031-platform-data-backup.md` reference the actual alert rule's annotation now carries.

Doesn't gate `deploy (bex via Argo)` (independent workflow) so it hasn't blocked any actual deploy, but it means the "gitops tree always renders" guarantee that check exists for (see its own header comment) has quietly been broken for a while. Fix locally with `bash scripts/gitops-validate.sh`.
