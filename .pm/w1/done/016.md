# 016 — Sweep stale "single-node / data-loss" comments inverted by the rebuild

The w1/m19 rebuild made several in-tree claims false in bex's FAVOR — hcloud-csi network volumes are the prod default StorageClass, so Loki/Prometheus history now SURVIVES node loss, and the private network is CAPH's `bex` 10.10.0.0/16:

- `deploy/gitops/base/loki.yaml`, `prometheus.yaml`, `log-shipper.yaml` — comments still claim local-path/single-node history loss.
- `docs/ADR010-observability.md:42` — same inverted claim.
- `deploy/gitops/base/values/traefik.values.yaml:9-15` — cites the dead 10.0.0.0/16 network (now 10.10.0.0/16).
- `docs/ADR002-architecture.md` §Data layering — "business state lives only in the single app node's etcd" predates the multi-node world (still single etcd MEMBER until the quota revert, but no longer a single-node cluster).

Comment/doc-only sweep, sub-hour, zero behavior change. Found by the 2026-07-11 board review (theme F).
