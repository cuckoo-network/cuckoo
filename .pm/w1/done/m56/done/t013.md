---
id: w1/m56/t013
title: Simplify — run /simplify over the changed platform code
worker: worker1
status: done
estimate: 20m
depends_on:
  [w1/m56/t003, w1/m56/t008, w1/m56/t009, w1/m56/t010, w1/m56/t011, w1/m56/t012]
---

## Objective

Run the repository simplification workflow over the platform diff and collapse abstractions that existed only to support retired versions or fleet shapes.

## Context

- Plugin migration and backfill removal can leave unused configuration branches, helpers, RBAC, fixtures, comments, and workflow variables.

## Steps

1. Run /simplify over files changed by w1/m56.
2. Remove unreachable compatibility branches and duplicate plugin configuration without broad refactors.
3. Re-render manifests and re-run focused tests.

## Files

- All platform files changed by w1/m56.

## Acceptance criteria

- [x] No unused native-Barman, Kubernetes-1.31, Node-20, or one-time migration abstraction remains in changed code.
- [x] Simplification preserves current backup, reconciliation, build, and CI behavior.
- [x] Focused validation remains green.

## Out of scope

- Unrelated platform redesign.

## Completion evidence

- No repository `/simplify` command or skill is installed, so the task used the equivalent diff-scoped source/config audit plus the repository's full custom golangci-lint. The first lint found exactly one issue: `reconcileExternalRoutes` retained an unused `context.Context` after t011 deleted every fallible Traefik cleanup call.
- Commit `b40a95f6` renamed the helper to `updateExternalAddressStatus`, removed its impossible error return and dead `RouteFailed` branch, and corrected the last test comment that referred to the retired build-adoption spec. The focused diff removed five net lines without changing the address projection.
- Active-code scans found zero in-tree `barmanObjectStore`, kpack compatibility override, old Traefik GVK/cleanup helper, removed datastore backfiller, metadata adoption helper, retired migration script, or unreviewed/Node 20 Action ref. Historical drill evidence and current desired-state convergence were deliberately retained.
- `make lint` passed with zero issues across operator and backend. Focused controller/build/execution/predeploy/publish/registry tests and `scripts/gitops-validate.sh` passed locally; the later full t014 suites independently cover every module.
- [Operator run 30359262177](https://github.com/bex-co/bex/actions/runs/30359262177) and [deployment run 30359262323](https://github.com/bex-co/bex/actions/runs/30359262323) passed. The deployment completed test, image, signing/SBOM, CVE, publish, and production rollout gates; bex-controller-manager 1/1, bex-api 2/2, dashboard 1/1, and both datastore SNI proxy DaemonSets 3/3 were Ready afterward.
