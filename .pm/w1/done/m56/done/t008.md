---
id: w1/m56/t008
title: Remove in-tree barmanObjectStore code, manifests, guards, and stale runbook instructions
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m56/t007]
---

## Objective

Delete the deprecated CNPG in-tree Barman implementation after both plugin restore drills pass.

## Context

- Removal spans operator Go structs/builders, GitOps YAML, structural tests, documentation, alerts, and recovery examples.
- Historical PM records and applied database migrations are evidence and are not rewritten.

## Steps

1. Remove every live barmanObjectStore and native ScheduledBackup-method construction.
2. Delete obsolete validation branches and rewrite monitoring/runbook language around plugin resources.
3. Render all manifests and scan active product/docs paths for stale native-field references.

## Files

- lego/operator/internal/controller/
- deploy/gitops/
- scripts/
- docs/ADR009-postgresql-management.md
- docs/ADR031-platform-data-backup.md
- docs/runbooks/

## Acceptance criteria

- [x] Active code and rendered manifests contain no in-tree barmanObjectStore configuration.
- [x] Backup alerts, validation, recovery procedures, and environment-variable documentation describe the plugin path.
- [x] CNPG reconciliation emits no native-Barman deprecation warning.

## Completion evidence

- Removed the final active native-field assertions and wording from operator/backend code and `scripts/gitops-validate.sh`; the remaining exact native-field references are confined to historical drill/PM evidence.
- `kubectl kustomize deploy/gitops/charts/bex-postgres` and the production overlay rendered without a native backup field. `bash scripts/gitops-validate.sh`, `go test ./internal/controller`, and `go test ./internal/postgres` passed.
- ADR031 now records the completed migration and production plugin drill. `BexDbBackupStale` explicitly diagnoses the Barman Cloud plugin, ObjectStore, deployment, sidecar, and credential-reference path.
- Production reported one plugin WAL-archiver Cluster and one plugin-method ScheduledBackup, with zero native Cluster fields and zero non-plugin ScheduledBackups. The CNPG controller's preceding six hours of logs contained no native-Barman deprecation warning.

## Out of scope

- Editing historical completed PM evidence solely to erase old terminology.
