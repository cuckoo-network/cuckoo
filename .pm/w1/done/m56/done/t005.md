---
id: w1/m56/t005
title: Migrate operator-managed tenant Postgres backup and recovery to the plugin
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m56/t004]
---

## Objective

Project tenant database backup, scheduled backup, recovery, and external-cluster configuration through the Barman Cloud plugin instead of spec.backup.barmanObjectStore.

## Context

- lego/operator/internal/controller/database*controller.go currently builds in-tree backup and recovery structures from BEX_DB_BACKUP*\* settings.
- The Database CR contract should remain stable unless the plugin requires a typed contract change; operator mechanism owns the projection.

## Steps

1. Replace Cluster backup and externalClusters projections with plugin references and plugin configuration.
2. Change ScheduledBackup method to the plugin method while preserving plan durability, retention, destination, and credentials.
3. Update envtest coverage for enabled, disabled, malformed, suspend, recovery, and deletion paths.

## Files

- lego/operator/internal/controller/database_controller.go
- lego/operator/internal/controller/database_controller_test.go
- lego/operator/config/
- lego/types/v1alpha1/
- docs/ADR009-postgresql-management.md

## Acceptance criteria

- [x] Newly reconciled backed-up tenant Databases use only plugin-based backup and ScheduledBackup configuration.
- [x] Recovery targets the same repository and supports the existing PITR API semantics.
- [x] Backup-disabled plans remain byte-equivalent apart from unavoidable CNPG schema changes.
- [x] Operator tests cover failure and recovery paths.

## Completion evidence

- Operator projection now emits only the `barman-cloud.cloudnative-pg.io` Cluster plugin, plugin-backed external recovery source, and plugin-method ScheduledBackup/Backup objects. Immutable database/per-major `serverName` values preserve the existing archive and PITR contract; the in-tree `spec.backup.barmanObjectStore` path is absent.
- Envtest covers backed-up and backup-disabled plans, malformed/incomplete transport configuration, scheduled and on-demand backups, recovery target time, suspend, deletion, and major-upgrade archive identity. `make test` passed with controller coverage at 77.5%; GitOps validation and all required deployment workflow jobs passed.
- Production smoke `dpg-m56t005plugin0000000` reconciled to a suspended plugin-only Cluster and plugin ScheduledBackup; `dpg-m56t005restore000000` rendered the same ObjectStore/server plus the requested PITR timestamp. Both disposable Databases and all owned resources were deleted normally.
- The delete smoke exposed a namespace-wide Cilium deny accidentally opting non-App workloads into default-deny. `90bcc640` set `enableDefaultDeny.egress: false`, removed the now-obsolete per-identity allow patches, and added structural validation. On the affected worker, DNS and Wasabi HTTPS recovered immediately (`303`, 21 ms connect), the purge finalizer completed without bypass, and metadata deny precedence remains intact.

## Out of scope

- Changing tenant backup product promises or retention policy.
