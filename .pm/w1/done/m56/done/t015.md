---
id: w1/m56/t015
title: Closeout — verify DoD, mark done, move milestone
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m56/t013, w1/m56/t014]
---

## Objective

Close the milestone only after supported versions are live, plugin restores are proven, fleet legacy counts are zero, and all corresponding scaffolding is removed.

## Context

- Code completion alone is insufficient: the definition of done includes live rollout health and restorable backup evidence.
- PM closeout moves tasks and the milestone and synchronizes the workstream checkbox.

## Steps

1. Verify the definition of done, live inventory, rollout health, restore evidence, and every task acceptance criterion.
2. File any residual with an owner rather than hiding it in closeout prose.
3. Run PM done for this final task so filesystem and status move together.

## Files

- .pm/w1/m56/
- .pm/w1/README.md

## Acceptance criteria

- [x] All preceding tasks are done and their files are under the milestone's done directory.
- [x] Production versions, zero-count inventories, and restore drills satisfy the observable definition of done.
- [x] PM status and filesystem placement are synchronized.

## Out of scope

- Closing while a deprecated production path remains load-bearing.

## Completion evidence

- t001–t014 all report `status: done`, their acceptance criteria are checked, and their files are under the milestone's `done/` directory. Closeout corrected t001's checkbox-only PM drift and the stale `.pm/w10/README.md` file pointer in this task; neither represented unfinished implementation.
- Final production inventory on 2026-07-28 UTC found 9/9 Ready nodes at Kubernetes v1.34.9; Barman plugin v0.13.0 at 1/1; four CNPG Clusters with zero native fields; one WAL plugin; one plugin ScheduledBackup and zero non-plugin schedules; bex-db 2/2 with `ContinuousArchiving=True`; and the completed dual PITR drill still linked.
- Five Apps included four with active revisions; active missing artifact/release fingerprints were 0/0. Fifteen current build execution artifacts had exact App UIDs, kpack missing-UID count was zero, and old `bex-system` clone copies were zero. Datastore legacy names/allowlists were zero in production and maintained local.
- Production and local each retained only the current `IngressRouteTCP/bex-system/bex-ssh-gateway`; legacy datastore routes were zero. `bex-traefik` retained zero explicit targets, one private canonical worker selector resolving six workers, exact ports 22/80/443/5432/6379, and no listener without a healthy backend.
- Final rollout health was bex-controller-manager 1/1, bex-api 2/2, dashboard 1/1, and both SNI proxy DaemonSets 3/3. The new scripts, Cluster API, and GitOps checks passed 3/3 with zero annotations; the preceding deploy passed all test/build/security/rollout gates.
- No m56 residual remains load-bearing. Completed native-era Backup CR history remains under its normal retention policy, but the live Cluster, recovery, WAL archiver, and ScheduledBackup authority are plugin-only; historical evidence is intentionally retained rather than treated as active compatibility code.
