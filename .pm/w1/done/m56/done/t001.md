---
id: w1/m56/t001
title: Production preflight — inventory deprecated state, backups, and rollback gates
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Establish a read-only production baseline and explicit rollback gates before deleting any migration or backup compatibility path.

## Context

- The research environment could inspect only the local kind/CAPD cluster; it had hand-applied Apps and unlabeled build artifacts that still exercised adoption fallbacks.
- Production must be queried separately without printing kubeconfigs, Secret data, database credentials, or private keys.

## Steps

1. Record Kubernetes, CAPI, CNPG, kpack, and GitHub Action versions and compatibility matrices.
2. Count legacy datastore names/IP shapes, build artifacts without App UID labels, releases without fingerprints, old Traefik routes, and legacy load-balancer targets.
3. Verify current etcd and Postgres backups and document rollback checkpoints for each mutation task.

## Files

- infra/clusterapi/overlays/hetzner-caph/
- deploy/gitops/
- scripts/
- docs/ADR031-platform-data-backup.md
- docs/runbooks/

## Acceptance criteria

- [x] A timestamped, redacted inventory records counts and exact go/no-go gates for t002 through t011.
- [x] Existing backups needed for rollback are current and their locations are recorded without secret material.
- [x] Any non-zero legacy count has a named normalization step before its fallback can be deleted.

## Out of scope

- Mutating production during the inventory.

## Completion evidence

- `docs/drills/2026-07-27-platform-deprecation-preflight.md` records the timestamped compatibility matrix, production and maintained-local zero/non-zero counts, backup checkpoints, redacted object-store locations, and per-task go/no-go gates without Secret data.
- Fresh etcd/OpenBao recovery Jobs and the current bex-db backup were required before mutation. Every fleet hop and backup-path transition retained a named manifest/data rollback point; the subsequent transition sections record those checkpoints and successful verifications.
- The only non-zero migration count was 28 unreferenced old clone Secret copies in `bex-system`; t010 owned their exact-reference recheck and deletion before adoption code removal. Every other fallback had a zero-count recheck owner in t009–t011.
