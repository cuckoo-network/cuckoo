---
id: w1/m17/t008
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m17/t007]
---

## Objective

Close the milestone only when the Definition of Done actually holds: managed Postgres supports backups + PITR (restore-to-new), suspend/resume/restart, an IP allowlist gating the external URL, PgBouncer pooler strings, and Postgres users — over REST/GraphQL/MCP and the database detail page, parity-checked vs render.com, with `make test` + dashboard tests green. HA/failover/replicas stay out of scope (in `w1/013`).

## Context

- DoD + scope: `.pm/w1/m17/README.md`.
- `.pm` done conventions (`.pm/CLAUDE.md`): move done tasks to `wN/mN/done/tNNN.md`; when no open tasks remain, move the **whole** milestone dir to `w1/done/m17/` and check its box in the workstream README — leave no tombstone at the old path.
- The source inbox note `w1/011` moves to `w1/done/011.md` once this milestone closes (HA/replica split → new inbox note `w1/013`).

## Steps

1. Verify each DoD clause against the shipped surfaces (REST/GraphQL/MCP + dashboard) and confirm `make test` + dashboard tests green.
2. Confirm HA/failover/replicas are recorded as out of scope in inbox note `w1/013` (create it if not already present).
3. Mark t001–t007 done (frontmatter + milestone README rows + `**Status:**`), then move each to `done/`; move the whole `m17` dir to `w1/done/m17/` and check the workstream README box; move `w1/011.md` → `w1/done/011.md`.

## Files

- `.pm/w1/m17/` (→ `.pm/w1/done/m17/`)
- `.pm/w1/README.md` (workstream checkbox)
- `.pm/w1/011.md` (→ `.pm/w1/done/011.md`), `.pm/w1/013.md` (HA/replica split note)

## Acceptance criteria

- [ ] Every DoD clause verified against the real surfaces; tests green.
- [ ] Milestone + all tasks moved to `done/` per `.pm/CLAUDE.md`; no tombstone left at the old path.
- [ ] Source note `w1/011` filed to `done/`; HA/replica scope captured in `w1/013`.

## Out of scope

- Any remaining feature work (must be complete before closeout runs).
- HA / failover / read replicas (deferred to `w1/013`).
