> **Closed 2026-08-19 (w1 triage).** Duplicate of `.pm/DO_NOT_DO.md` #PSL and the FUTURE-MAYBE `onbex.co` PSL entry. Do not submit. Prepared package below kept for when the eligibility trigger fires.

# 050 — PSL submission for `onbex.co`: prepared package (HELD — do not submit yet)

> **Rewritten 2026-08-18 to match `.pm/DO_NOT_DO.md` #PSL** (user decision 2026-08-17), which overrides this note's original "submit now — lead time dominates" posture: **do not open the PSL submission yet.** It fails four submission requirements today — registration must have >2 years remaining (onbex.co expires 2027-06-29, ~10 months), a role-based mailbox is required and `bex.co` has no MX, the project must serve 2000–3000+ distinct users (bex has 17 workspaces), and an accessible abuse contact must exist on a reachable website. Re-open when the user count approaches the bar (and the registration is renewed long enough). The prepared package below is kept so the re-open starts from done work, not from scratch. The finding itself remains an accepted external blocker (ADR055 F9 lineage); the standing guardrail is that `BEX_BASE_DOMAIN` must never be unset (two production outages came from exactly that).

The accepted fix for the round-4/5/7 cross-tenant cookie findings (ADR055 F9 / `046.md` R5-F4 / round-7 #10): list `onbex.co` in the Public Suffix List's **private domains** section so browsers refuse `Domain=onbex.co` cookies between sibling tenants. Everything below is ready; the two actions must come from the operator's own accounts (GitHub PR + Cloudflare DNS) **once the hold above lifts**. Lead time after submission is months (PSL merge, then browser releases).

## 1. Open the PR against `publicsuffix/list`

Fork `github.com/publicsuffix/list`, edit `public_suffix_list.dat`, and add this block in the **`===BEGIN PRIVATE DOMAINS===`** section, alphabetically by organization name (`bex` sorts among the B blocks, between `Besties` and `BinaryLane`):

```
// bex : https://bex.co
// Submitted by Tian Pan <puncsky@gmail.com>
onbex.co
```

PR title: `Add onbex.co`. Fill their PR template honestly: the suffix hosts independent tenant applications on customer-chosen subdomains (`<slug>.onbex.co`), the same shape as `onrender.com`, which is already listed — cite that precedent. Note the rollout expectation (no urgency claim; this is a standing platform boundary).

## 2. Prove domain control

**After** the PR is open, create in the Cloudflare zone:

```
_psl.onbex.co  TXT  "https://github.com/publicsuffix/list/pull/<NNNN>"
```

(The record must contain the actual PR URL, so it can only be created after step 1.)

## 3. After the merge eventually lands

- `lego/operator/internal/hostingdomain/suffix.go` re-checks against the embedded PSL snapshot in `golang.org/x/net/publicsuffix` — the warning in `cmd/manager` + `cmd/staticserver` goes quiet only after that dependency is bumped past a release that vendors the updated list. Bump it then.
- Only then consider flipping the shared-suffix check from warn to fail-closed (the `046.md` R5-F4 residual).
- Until browsers ship the update, the interim guidance stands: tenant apps should use host-only or `__Host-` cookies; the platform Kratos cookie is on `bex.co`, a different registrable domain, and unaffected throughout.
