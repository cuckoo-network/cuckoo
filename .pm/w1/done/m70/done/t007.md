---
id: w1/m70/t007
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m70/t005, w1/m70/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behaviour-preserving only.

## Context

- Likely targets: overlap between the new MCP pin loader and the existing `loadRenderOpenAPIContractData` in `lego/backend/internal/api/render_openapi.go`; overlap between `scripts/render-mcp-drift.sh` and `scripts/render-schema-drift.sh`.
- The registry wrapper touches 213 call sites — check the seam did not force boilerplate at each one.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the behaviour-preserving fixes.
3. File anything larger as an inbox note rather than widening this milestone.

## Files

- the milestone's changed files

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly deferred with a filed note.
- [ ] `make lint` and the backend suite stay green.

## Out of scope

- Behaviour changes.
