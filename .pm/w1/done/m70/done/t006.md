---
id: w1/m70/t006
title: Record the real inventory in ADR018 + ADR006
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m70/t003]
---

## Objective

Replace the current position — where the MCP column's parity is asserted across ten hand-written comments with manual check dates — with the measured per-class inventory, and state plainly what the MCP column scores.

## Context

- `docs/ADR018-render-parity.md`'s method line already says the MCP pass compares "bex's MCP tools vs the current `render-oss/render-mcp-server`", and its legend already says a Render capability is "a noun/verb Render exposes on _any_ of its surfaces" — i.e. the ✅ is capability-level, not tool-name-level. That distinction is load-bearing for m71 and is currently easy to misread; make it explicit.
- `docs/ADR006-bex-api.md` holds the "one core, three adapters" design record.

## Steps

1. Add the per-class counts + pin version to ADR018's MCP method note.
2. State explicitly that the MCP column scores **capability reachability**, not tool-name identity, and that folding tools is therefore parity-preserving while renaming a `Parity1to1` tool is not.
3. List the Render-only tools t003 found as parity gaps.
4. Note in ADR006 that MCP now has a pin + drift job, closing the asymmetry with REST.

## Files

- `docs/ADR018-render-parity.md`
- `docs/ADR006-bex-api.md`

## Acceptance criteria

- [ ] ADR018 carries the measured counts and the pin's upstream version.
- [ ] The capability-vs-tool-name distinction is stated where a reader will hit it.
- [ ] `npx prettier@3.4.2 --write "**/*.md"` clean.

## Out of scope

- Proposing the m71 collapse in the ADR — that lands with m71.
