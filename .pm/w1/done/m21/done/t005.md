---
id: w1/m21/t005
title: Render parity — verify static-site surfaces + edge rules against Render
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m21/t002, w1/m21/t003, w1/m21/t004]
---

## Objective

Confirm the static-site feature is consistent across REST/GraphQL/MCP/UI and matches render.com — including the edge rules (redirects/rewrites/headers). Flag drift as follow-up.

## Context

- bex-api is Render-compatible (`docs/ADR006-bex-api.md`); Render's `static_site` type + `/routes` + `/headers`.
- `docs/ADR018-render-parity.md` static-site rows (build→CDN, redirects/rewrites, headers) move gap → ✅.

## Steps

1. Diff fields/semantics/error shapes vs Render per surface; verify redirect/rewrite/header behavior matches.
2. Update the `docs/ADR018-render-parity.md` static-site rows with evidence.
3. File follow-ups for any intentional divergence.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] All surfaces + edge rules verified against Render; ledger updated with evidence.

## Out of scope

- New feature work beyond reconciling drift.
