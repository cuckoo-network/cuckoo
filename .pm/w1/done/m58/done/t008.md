---
id: w1/m58/t008
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m58/t007]
---

## Objective

Close m58 once the multi-replica DoD actually holds (not merely when the code is written).

## Context

- This is the security/correctness gate for bex-api HA event delivery, not bookkeeping.

## Steps

1. Recheck every DoD item and the t005 live evidence.
2. Confirm backend tests green.
3. Run `/pm done w1/m58/t008`; the milestone moves to `done/m58/`.

## Files

- `.pm/w1/m58/`
- `.pm/w1/README.md`

## Acceptance criteria

- [ ] The observable DoD holds (zero duplicates under two replicas + restart).
- [ ] m58 moved to `done/` and checked off in `.pm/w1/README.md`.

## Out of scope

- Further webhook feature work.

## Completion (2026-07-30)

DoD met: two replicas deliver each event exactly once (SKIP LOCKED claim + `(endpoint_id,event_id)` unique index), a restart neither re-delivers (lease) nor re-emails (persisted `notified_at`), and the deploy-hook limit is documented per-replica with the reason. Unit + integration tests assert the claim and the marker; the two-worker run shows zero duplicates. All tasks `status: done`; milestone moved to `w1/done/m58/`; w1 README box checked. Shipped on top of the concurrent `origin/main` advance (c8e8cd4d) — see ship log.
