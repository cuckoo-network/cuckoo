---
id: w1/m50/t007
title: Render parity — cross-surface consistency for the changed bandwidth surface
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m50/t006]
---

## Objective

Standing parity gate: the bandwidth payload changed (best-effort semantics + a per-source health field) — confirm REST, GraphQL, and MCP expose identical fields/semantics/error shapes, and compare against Render's bandwidth chart behavior, recording any deliberate divergence.

## Context

- Render's dashboard bandwidth chart is best-effort and unannotated; the per-source health field is a bex extension (named in t003 to avoid future collisions) — record it in `docs/ADR018-render-parity.md`'s metrics row the way other bex extensions are.
- The one-core-three-adapters rule (`internal/api/CLAUDE.md`) should make consistency near-automatic; the check is that the t003 field actually landed on all three surfaces and that no surface still returns `egress source X unhealthy` for interactive reads.

## Steps

1. Diff the three adapters' bandwidth responses (same field names, same degraded semantics, same error taxonomy).
2. Update the ADR018 metrics row with the extension note + evidence pointer.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] No surface can return the old whole-window error for interactive bandwidth reads; all three carry the health field identically.
- [ ] ADR018 records the bex-extension field and the best-effort alignment with Render.

## Out of scope

- New surface work.
