---
id: w1/m50/t009
title: Test coverage — bucket/degraded gating against synthetic reset+gap fixtures
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m50/t007]
---

## Objective

Meaningful tests for the split-gate behavior: synthetic Prometheus fixtures reproducing the exact prod failure modes (mid-window `up` gap; mid-window router-counter reset; counter-loss event) proving the interactive path serves data while the rollup path refuses.

## Context

- Test conventions: backend table tests against a fake Prometheus HTTP server (the existing `internal/metrics` and `internal/egressquery` test patterns); dashboard vitest in `__tests__` adjacent to components.
- The three prod failure modes to encode as fixtures come from the t001 evidence: 26/2304-sample `up` gap, `resets=32` window, `bex_egress_meter_last_counter_loss_time_seconds` in-window.
- Failure-mode coverage, not snapshots: the billing-path pin (strict gate refuses) is the regression that must never silently flip.

## Steps

1. Backend fixtures + assertions for interactive-serves / rollup-refuses across the three modes.
2. Dashboard tests for the three panel states (may exist from t004 — extend, don't duplicate).
3. Full backend + dashboard suites green.

## Files

- `lego/backend/internal/metrics/*_test.go`, `lego/backend/internal/egressquery/*_test.go`
- `dashboard/src/features/metrics/**/__tests__/`

## Acceptance criteria

- [ ] Each of the three prod failure modes has a fixture asserting both paths' behavior.
- [ ] A change that lets the rollup inherit best-effort fails a test by name.
- [ ] `go test ./...` (backend) and `yarn test` (dashboard) green.

## Out of scope

- E2E (t006 covered live).
