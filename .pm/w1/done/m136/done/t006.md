---
id: w1/m136/t006
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m136/t005]
---

## Objective

Close the milestone once its definition of done actually holds — not merely when the code is written.

## Context

- The DoD is observable, so check it rather than infer it: a session started in `dev-1` against a workspace with **no** key at `tenants/data/default/agent-sessions/<ws>/model-key` must end `failed` with the actionable reason, on the first refused mint.
- Bring-up for that check is `.pm/w1/dev-1/README.md` (`dev-env.sh 1 up`, then `agent-up`, then `agent-stub` if no real provider key is available — the stub exercises the mint path, which is the path under test here).

## Steps

1. Re-read the milestone DoD and verify each bullet against a real run, including the negative case (a stale-sandbox refusal does not terminalize).
2. Record the outcome in `README.md` under `## Outcome`, including the t003 wording decision.
3. Set every remaining task `status: done`, move each `tNNN.md` to `.pm/w1/m136/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in the milestone `README.md`.
5. Move `.pm/w1/m136/` to `.pm/w1/done/m136/` and check `- [x] **m136**` in `.pm/w1/README.md`.
6. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m136/**`
- `.pm/w1/README.md`

## Acceptance criteria

- [ ] The DoD is verified against a real run, not assumed.
- [ ] Status is consistent across the workstream README, the milestone README, and every task's frontmatter.
- [ ] The milestone directory is under `.pm/w1/done/m136/` with no stub left behind.

## Out of scope

- Any remaining work — it becomes a new note or milestone, not a loosened DoD.
