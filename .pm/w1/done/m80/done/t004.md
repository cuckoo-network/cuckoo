---
id: w1/m80/t004
title: Database/KeyValue narrow carve-outs + ptr consolidation
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Take only the narrow shared pieces between the Database and KeyValue reconcilers the review judged genuinely mechanical — explicitly NOT a "managed datastore" abstraction layer (the substrates are incompatible: CNPG unstructured Cluster vs typed StatefulSet; readiness logic is parallel in name only).

## Context

- `growOnlyIntent`: identical grow-only storage arithmetic at `database_controller.go:164-169` ↔ `keyvalue_controller.go:314-319` (only observed-size sources differ) → one pure function next to `growOnlyStorage` in `storage.go`. Callers keep their own Gets, `*Fail` reasons, and condition writes (`rejectKeyValueStorageShrink` writes two conditions, Database one — do not merge those).
- `secretClient()`: 5 byte-identical lines ×2 (`database_controller.go:463`, `keyvalue_controller.go:279`).
- `setNotReadyCondition`: the shared 8-line body behind `dbFail`/`kvFail`/app `fail` (`database_controller.go:1137`, `keyvalue_controller.go:1062`, `app_controller.go:3344`); phase assignment stays per-reconciler.
- `ptr[T]` defined 4× (`app_controller.go:3186`, `execution/security.go:118`, `build/build.go:1112`, `predeploy/predeploy.go:265`) → export one (`execution.Ptr`) or a shared `internal/ptr`.

## Steps

1. Extract `growOnlyIntent` as a pure unit-tested function; convert both storage-intent callers.
2. Share `secretClient` and `setNotReadyCondition`.
3. Consolidate `ptr`.
4. `make test && make lint`.

## Files

- `lego/operator/internal/controller/storage.go`, `database_controller.go`, `keyvalue_controller.go`, `app_controller.go`
- `lego/operator/internal/execution/`, `internal/build/build.go`, `internal/predeploy/predeploy.go`

## Acceptance criteria

- [ ] Grow-only invariant stated once with direct unit tests (no fake client needed); zero behavior change; suites pass unmodified.

## Out of scope

- Any broader shared interface over the two reconcilers (explicitly declined by the review); merging the per-resource condition-writing rejection paths.
