---
id: w1/m80/t003
title: deleteStaleChildren call sites + shared currentRevisionPodsReady
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Two mechanical dedups inside the controllers: (a) route the two inline reimplementations of `deleteStaleChildren` (get-skip-NotFound-delete-tolerant) through the existing helper; (b) extract `currentRevisionPodsReady` for the identical 22-line `deploymentPodsReady`/`keyValuePodsReady` bodies, parameterized by selector + revision label key.

## Context

- Helper: `lego/operator/internal/controller/app_controller.go:1829-1844`; inline copies at `:2155-2164` (`reconcileIngress`) and `:2283-2295` (`reconcileHostRedirects`).
- Twins: `app_controller.go:2027-2058` ↔ `keyvalue_controller.go:882-903`; both already call the shared `podReady` (`deployment_projection.go:34`).
- **Do NOT fold in `currentRevisionFullyReady` (`app_controller.go:2060-2090`)** — its opposite inconclusive-default is load-bearing for `RolloutSettling` vs `RolloutProgressing` (w3/m78); keep it and its comment separate.
- Leave `deleteOwnedObject` (`:3736`) separate — its owner gate and `(done, err)` contract are deliberate.

## Steps

1. Replace the two inline blocks with `r.deleteStaleChildren(ctx, obj)` calls.
2. Add `currentRevisionPodsReady` to `deployment_projection.go`; convert both callers.
3. `make test`.

## Files

- `lego/operator/internal/controller/app_controller.go`
- `lego/operator/internal/controller/keyvalue_controller.go`
- `lego/operator/internal/controller/deployment_projection.go`

## Acceptance criteria

- [ ] ~45 lines removed; identical semantics (NotFound tolerance on both get and delete; inconclusive-true on list error); suites pass unmodified.

## Out of scope

- `currentRevisionFullyReady`, `deleteOwnedObject` (both deliberately different).
