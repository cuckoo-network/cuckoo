---
id: w1/m80/t006
title: Test coverage
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m80/t001, w1/m80/t002, w1/m80/t003, w1/m80/t004]
---

## Objective

Ensure the consolidations are pinned by meaningful tests: the credential-helper guard (t001), `EnsureOwnedJob` behavior under create-race/adoption/foreign-owner (the `checkOwnedArtifact` semantics), `growOnlyIntent` truth-table unit tests (allocated/observed/spec permutations incl. shrink rejection), and byte-identical emitted-manifest assertions where golden tests exist.

## Context

- Existing nets: `internal/build/jobname_contract_test.go`, the controllers' envtest suites (67 files).

## Steps

1. Audit the milestone diff for consolidation seams without direct tests.
2. Add the missing units (pure functions first — they need no fake client).

## Files

- `lego/operator/internal/execution/*_test.go`, `internal/controller/storage_test.go` (new), guard tests from t001

## Acceptance criteria

- [ ] Each shared helper has direct tests asserting the exact semantics its callers relied on; `make test` green.

## Out of scope

- Coverage-percentage chasing.
