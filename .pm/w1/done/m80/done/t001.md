---
id: w1/m80/t001
title: Hoist the git credential helper to one guarded constant
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Hoist the host-bound git credential-helper shell expression to one exported constant (e.g. `execution.GitHubCredentialHelper`), carrying `build.go:904-912`'s SECURITY comment with it, and add a test asserting both Job constructors embed it — so a future edit to one copy can no longer offer the tenant GitHub token to a non-GitHub host from the other plane.

## Context

- Byte-identical copies: `lego/operator/internal/build/build.go:919` and `lego/operator/internal/publish/publish.go:400`.
- The SECURITY comment explaining host-binding exists only in build.go; publish.go gives the next editor no signal the string is load-bearing.
- Emitted Job bytes must not change.

## Steps

1. Add the constant (in `internal/execution` or a tiny `internal/gitclone`), comment moved verbatim.
2. Interpolate it at both call sites.
3. Guard test: assert `build.BuildJob(...)` and `publish.PublishJob(...)` clone-container args contain the constant.

## Files

- `lego/operator/internal/execution/` (or new `internal/gitclone/`)
- `lego/operator/internal/build/build.go`, `internal/publish/publish.go`
- guard test file

## Acceptance criteria

- [ ] One definition; both constructors reference it; guard test fails if either stops.
- [ ] Emitted Job manifests byte-identical (existing job-shape tests unchanged).

## Out of scope

- Changing the helper's semantics in any way.
