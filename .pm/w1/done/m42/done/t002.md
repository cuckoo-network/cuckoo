---
id: w1/m42/t002
title: Implement cursor/limit in projects REST; check GraphQL/MCP project lists
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m42/t001]
---

## Objective

Make `GET /v1/projects` honor `cursor` and `limit` using `core.PageParams` + `StablePage` (cursor = project id, matching the per-item cursors it already emits), per the t001 convention. Check the GraphQL `projects` query and MCP project list tool for the same drift and align them (bex-extension surfaces, so no Render-MCP constraint).

## Context

- `lego/backend/internal/projects/rest.go:103-124` — returns `[]renderProjectWithCursor{Project, Cursor: p.ID}` from the full list; never reads `r.URL.Query()` paging params.
- Pattern to copy: `environments/rest.go:142-143`, and `postgres/graphql.go:356-360` for the GraphQL side (89c42936).

## Steps

1. Wire `core.PageParams` into the REST handler; page with `StablePage` keyed on project id.
2. Grep `internal/projects` for the GraphQL/MCP list surfaces; add `cursor`/`limit` args where the REST contract gained them.
3. `cd lego/backend && go build ./... && go test ./...`.

## Files

- `lego/backend/internal/projects/rest.go`
- `lego/backend/internal/projects/graphql.go` / `mcp.go` (if drift confirmed)

## Acceptance criteria

- [ ] Echoing the last cursor advances the page; the final page returns an empty list or terminates per convention.
- [ ] `limit` respected; absent `limit` uses the documented default.

## Out of scope

- env-groups and custom-domains lists (w6/m32, w7/m38).
