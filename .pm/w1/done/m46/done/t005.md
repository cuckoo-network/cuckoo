---
id: w1/m46/t005
title: Render parity check across REST/GraphQL/MCP/UI
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m46/t004]
---

## Objective

Standing closing task: assert the three API surfaces now agree on service identity — REST, GraphQL, and MCP all emit the same `srv-` id for the same service (REST/MCP were already there; the flip must not have drifted them) — and the dashboard's URL shape matches what Render's CLI/API mint (`/web/{srv-id}` alias → `/services/{srv-id}`). Flag any drift as follow-up, never silently.

## Context

- Cross-surface id test precedent: `members_surface_parity_test.go`, `TestSurfaceParityAndWiring`.
- Render evidence: the CLI constructs `/web/{srv-id}` dashboard links client-side (`dashboard-routes.md`); `cli-compatibility-checklist.md` rows touching service ids.

## Steps

1. Cross-surface test: create/fetch one service via REST, GraphQL, MCP — identical `id`.
2. Re-check the CLI checklist rows that key on service ids; note any row whose evidence improves.

## Files

- `lego/backend/internal/api/*_test.go` or `internal/apps/*_test.go`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] One test pins REST=GraphQL=MCP service id equality; checklist evidence current.

## Out of scope

- Fixing unrelated checklist rows.
