---
id: w1/m46/t004
title: "Docs: close the ADR020 deviation, sweep ADR018 + dashboard-routes.md"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m46/t002, w1/m46/t003]
---

## Objective

Close the paperwork the flip falsifies: ADR020's Known-deviations entry ("GraphQL `Service.id` returns the App name") comes out (replaced by a one-line closure note citing this milestone), ADR018's affected rows get the update, and `dashboard-routes.md`'s canonical-route notes reflect that `/services/{id}` now means the srv- id with a name fallback.

## Context

- `docs/ADR020-identifiers.md` § Known deviations (the GraphQL bullet; keep the Postgres/KeyValue grandfathering and Hydra bullets).
- `docs/ADR018-render-parity.md` — the services/GraphQL rows referencing the deviation.
- `docs/render-artifacts/dashboard-routes.md` — the m39 decision section's "bex canonical route" framing.
- File `w1/028` for the `Domain.id` sibling deviation if not already filed.

## Steps

1. Update the three docs; `npx prettier@3.4.2 --write` them.

## Files

- `docs/ADR020-identifiers.md`, `docs/ADR018-render-parity.md`, `docs/render-artifacts/dashboard-routes.md`, `.pm/w1/028.md`

## Acceptance criteria

- [ ] No doc still claims GraphQL serves name-as-id for services; the Domain deviation is tracked, not silently inherited.

## Out of scope

- Any code.
