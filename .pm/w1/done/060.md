> **Closed 2026-08-19 (w1 triage).** Deferred — not a live hazard. High-priority items from the 2026-08-19 architecture review already shipped as `w1/m76`–`m80`. Trigger-gated in `.pm/FUTURE-MAYBE.md` § "Architecture-review leftovers (2026-08-19)". Original text below kept for provenance.

# app_controller.go in-package file split

From the 2026-08-19 refactor review (§4.1). `operator/internal/controller/app_controller.go` is 4,195 lines / 140 funcs — a file-organization problem, not a design problem (avg 30 lines/func, no dead code, no pre-ADR060-D1 blocking residue). The 67 test files are already split along exactly the missing seams (`hosts_test.go`, `slug_service_test.go`, `maintenance_mode_test.go`, `app_deletion_test.go`, `idle_test.go`, …).

Split by the review's measured section map, same package, pure moves: `app_build.go` (~466: `buildFromSource`, caps, pinning, `buildEnv`), `app_registry_creds.go` (~371: :1079-1449), `app_deletion.go` (~554: :3642-4195), `app_status.go` (~416), `app_routing.go` (~353: traefik + ingress + host redirects), `app_cron.go` (~287), `app_staticsite.go` (~208), `app_predeploy.go` (~144), `app_opensandbox.go`, `app_alias.go`, `app_networkpolicy.go`; fold the ~45 pure spec→object funcs into the existing `deployment_projection.go`.

**Do NOT sub-package** (tests are `package controller` on unexported symbols; every helper is a method on the 33-field reconciler; most write status). One commit per file so `git diff -M` renders moves. Document the shared `(value, ctrl.Result, halted, error)` 4-tuple convention once. Leave the entangled status-writing gates (`buildFromSource`, `reconcileStaticSite`, `reconcileCronJob`, `reconcilePreDeploy`) as they are. Do after w1/m76 t007's comment repairs and ideally after w1/m80 so the moves carry final code. ~Half a day, very low risk.
