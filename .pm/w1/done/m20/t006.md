---
id: w1/m20/t006
title: Simplify — /simplify over what this milestone changed
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m20/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (reconciler, backend adapters, dashboard section) for reuse / simplification / efficiency — behavior-preserving.

## Context

- Scope: the autoscale helper + `app_controller.go` changes (t002), the backend surface (t003), and the dashboard Scaling section (t004).

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving cleanups; re-run `make test` / backend tests.

## Files

- Files touched by t002–t004.

## Acceptance criteria

- [ ] `/simplify` findings applied or consciously declined; tests still green.

## Out of scope

- Bug hunting (that's `/code-review`) and new behavior.
