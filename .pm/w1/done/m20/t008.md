---
id: w1/m20/t008
title: Closeout — verify DoD holds, then move the milestone to done/
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m20/t007]
---

## Objective

When every other m20 task is done **and the definition of done is actually met**, close the milestone.

## Context

- DoD: autoscaling stanza + reconciler + REST/GraphQL/MCP + dashboard all live and consistent with Render; observable scale up/down on load.

## Steps

1. Confirm the DoD end state is real (not just code written) — a service actually scales up under load and down when idle.
2. Set every remaining task `status: done`, move each `tNNN.md` to `w1/m20/done/`, mark rows `— **DONE**` and `**Status:** done` in the README.
3. Move `w1/m20/` → `w1/done/m20/` and check `- [x]` in the w1 README.

## Files

- `.pm/w1/m20/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Milestone lands in `w1/done/m20/` only once the observable end state holds.

## Out of scope

- Any new work — this is the move.
