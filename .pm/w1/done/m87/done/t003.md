---
id: w1/m87/t003
title: Mirror the new variable names into .env.example
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m87/t002]
---

## Objective

Repo rule: `.env.example` mirrors `.env` names (no values), and `cp .env.example .env` must never fall out of date. Seven new operator variables and six new bex-api ones were just introduced.

## Context

- `scripts/gh-secrets.sh` pushes `.env` → GitHub secrets, so a name missing here is a name missing in CI.
- Names only. No values, ever — and never print the real `.env`.

## Steps

1. Add every `BEX_DISK_SNAPSHOT_*` name to `.env.example` in the same grouping style as its neighbours.
2. Confirm the secret-bearing ones are pushed by `scripts/gh-secrets.sh`.

## Files

- `.env.example`

## Acceptance criteria

- [ ] every `BEX_DISK_SNAPSHOT_*` name read by `lego/**` appears in `.env.example`
- [ ] no values committed

## Out of scope

- Rotating any existing secret.
