# Complete the email-verification UX, then require verified email for invites

Filed by w1/m53/t004 (security review V1). Kratos email verification is enabled
(`verification.via: email` in `kratos.values.yaml`) but the dashboard has **no
`/auth/verification` route**, so the emailed verification link dead-ends and no
user can reach a verified state. Because of that, w1/m53 shipped the invite-
redemption gate (`BEX_REQUIRE_VERIFIED_INVITE_EMAIL`, `Identity.EmailVerified`)
**off by default** — turning it on now would block every invited teammate.

Do:

1. Add the dashboard `/auth/verification` route (submit-code + resend), wiring the
   Kratos verification flow (`ui_url: https://dashboard.bex.co/auth/verification`
   is already configured). Confirm a user can complete verification end to end.
2. Flip `BEX_REQUIRE_VERIFIED_INVITE_EMAIL=1` on bex-api so login-time invite
   redemption requires a Kratos-verified email — closing the window where an
   attacker registers with a victim's not-yet-signed-up invited address and
   claims the invite.

Refs: `docs/ADR024-members.md` § Email verification and login-time redemption;
`internal/api/{auth.go,tenancy.go}`; `internal/core/identity.go`.

> **Done 2026-07-20.** (1) The `/auth/verification` route already existed — shipped
> by w4/008 (Ory Elements `Verification` over Kratos's `use: code` flow) after this
> note was filed; verified end to end live on dev-1: emailed Kratos link redirects
> into the page with the code pre-filled + a Resend button, submit yields
> "successfully verified" and `verifiable_addresses.verified=true` via the admin
> API. Flipped the dashboard Ory client config's stale `verification_enabled:
> false` (a w5/m1 scaffold leftover) to match. (2) `BEX_REQUIRE_VERIFIED_INVITE_EMAIL=1`
> set for prod (`lego/operator/config/api/deployment.yaml`, rides the next
> `/ship`) + dev-1; gate walked live: an unverified signup for an invited address
> logged "skipping invite redemption … not verified" and stayed out of the
> workspace; after completing verification the next resolver cache-miss redeemed
> the invite (member row + accepted_at). CLAUDE.md env table + ADR024 + the
> kratos.values.yaml stale comment updated. Bonus fix found live: the invite
> mailer passed the display-name `BEX_SMTP_FROM` (`bex <no-reply@bex.co>`, prod's
> exact shape) as the SMTP envelope MAIL FROM — a 501 at every relay strict about
> syntax (Mailpit refused it); `internal/mailer` now parses out the bare address
> for the envelope, keeping the display form in the From: header (test updated —
> it had pinned the buggy behavior). Moved to `done/`.
