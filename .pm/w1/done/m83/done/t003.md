---
id: w1/m83/t003
title: RBAC markers + admission-policy PVC CREATE/DELETE rules
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m83/t002]
---

## Objective

Grant the operator the PVC verbs t002 needs — today deliberately absent — in both the RBAC markers and the workload admission policy.

## Context

- App controller RBAC markers: `app_controller.go:313-330` — no `persistentvolumeclaims` today; the cluster role's existing PVC grant (`role.yaml:28`) is get/list/watch/update/patch only, minted by the KeyValue controller (`keyvalue_controller.go:288-290`).
- `deploy/gitops/base/operator-workload-admission.yaml:205-221` — PVC resourceRules match `UPDATE` only; add `CREATE`/`DELETE`, scoped to the operator service account. Volume grammar at `:83` (no hostPath) is unchanged.

## Steps

1. Add `persistentvolumeclaims` create/delete to the App controller markers; regenerate (`make manifests`).
2. Extend the admission policy's PVC rule; validate with `bash scripts/gitops-validate.sh` if it covers this manifest.

## Files

- `lego/operator/internal/controller/app_controller.go`
- `lego/operator/config/rbac/role.yaml` (generated)
- `deploy/gitops/base/operator-workload-admission.yaml`

## Acceptance criteria

- [ ] envtest PVC create succeeds under the generated role.
- [ ] Admission manifest admits operator-SA PVC CREATE/DELETE and still refuses others.

## Out of scope

- Namespace quota re-derivation (m84 t006).
