---
id: w1/m83/t008
title: "Test coverage: envtest for CEL + reconcile + resize + delete"
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m83/t007]
---

## Objective

Meaningful envtest coverage of the milestone's whole behavior surface — real assertions on refusals and reconcile outputs, not snapshots.

## Context

- Matrix from ADR082 § Verification: CEL rejections (type/tier/replicas/autoscaling/shrink/mountPath denylist — each proven red), PVC create with ownerRef + class, `Recreate` flip and its absence without a disk, resize intent + `DiskResizePending`, quota-blocked condition, suspend keeps the PVC, disk removal cleanup.
- Golden-manifest guard: a disk-less App's objects byte-identical to pre-milestone.

## Steps

1. Extend the operator envtest suites with the matrix; keep each case's failure mode observable.

## Files

- `lego/operator/internal/controller/*_test.go`

## Acceptance criteria

- [ ] Every matrix row has a test that fails when its behavior is reverted.
- [ ] `make test` green.

## Out of scope

- Backend/API tests (m84), e2e mock-cluster drill (tracked in ADR082 Verification, runs with m86 closeout).
