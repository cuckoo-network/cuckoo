---
id: w1/m83/t005
title: Grow-only resize path + disk status conditions
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m83/t002]
---

## Objective

`sizeGB` increases expand the PVC through the shared grow-only machinery; status mirrors the datastore vocabulary (ADR082 D4).

## Context

- Reuse `lego/operator/internal/controller/storage.go` (`growOnlyIntent`, `quantityGiCeil`) and the KeyValue expander's behaviors: patch the PVC, back off 5 min when the class lacks `AllowVolumeExpansion`, catch quota Forbidden → `StorageBlockedByQuota` (self-clearing) — `keyvalue_controller.go:297-320, 920-990`.
- Status: `AppStatus.Disk{AllocatedSizeGB, ObservedSizeGB}` + conditions `DiskReady`, `DiskResizePending` (set while fs growth unobserved — hcloud CSI may complete only on restart), `StorageBlockedByQuota`.

## Steps

1. Add the status struct + conditions to `app_types.go` (+ codegen).
2. Resize intent in the App reconcile: compare spec vs PVC, patch grow-only, set/clear conditions.

## Files

- `lego/types/v1alpha1/app_types.go` (+ generated)
- `lego/operator/internal/controller/app_controller.go`
- `lego/operator/internal/controller/storage.go` (only if a helper needs generalizing)

## Acceptance criteria

- [ ] envtest: grow patches the PVC and sets `DiskResizePending`; shrink is unreachable (CEL) and the reconciler never shrinks a PVC; non-expandable class backs off without erroring; quota-blocked grow sets the condition and recovers when quota rises.

## Out of scope

- Disk autoscaling (Render has none for service disks — deliberate absence).
