---
id: w1/m79/t001
title: Add the 3 missing operations to .graphql files + regenerate
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Close the three gaps that keep the shadow layer alive: `DatabasePooledConnection` (the one databases op not in definitions), a `query Project($id: String!)` (projects hand-rolls it despite `ProjectFields` existing), and a new `features/blueprints/api/blueprints.graphql` holding the 10 operations currently hand-maintained in TS. Then regenerate.

## Context

- `dashboard/src/features/databases/api/operations.ts:154` — `DatabasePooledConnectionDocument`, missing from `databases.graphql`.
- `dashboard/src/features/projects/api/operations.ts:14-27` — hand-rolled `ProjectDocument`; `projects.graphql:1` already has `ProjectFields`.
- `dashboard/src/features/blueprints/api/*.ts` — 10 operations, no `.graphql` at all (`use-blueprints.ts:3`, `use-blueprint.ts:3`, routes).
- Offline regeneration: the `SCHEMA_JSON` path documented in `dashboard/CLAUDE.md` § Offline codegen.

## Steps

1. Add the operations to their `.graphql` files (create `blueprints.graphql`), byte-matching the selections the hand-written documents send today.
2. Regenerate `definitions.ts` + `schema-types.ts` offline.
3. `yarn typecheck && yarn test`.

## Files

- `dashboard/src/features/databases/api/databases.graphql`
- `dashboard/src/features/projects/api/projects.graphql`
- `dashboard/src/features/blueprints/api/blueprints.graphql` (new)
- `dashboard/src/graphql/definitions.ts`, `schema-types.ts` (generated)

## Acceptance criteria

- [ ] All 3 gaps present in `definitions.ts` with selections identical to what ships today.
- [ ] Regeneration is clean (no unrelated churn beyond the new operations).

## Out of scope

- Repointing imports (t002); fragment hygiene (inbox note `w1/067`).
