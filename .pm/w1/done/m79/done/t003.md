---
id: w1/m79/t003
title: Resolve the undeclared graphql-tag/typed-document-node deps
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m79/t002]
---

## Objective

After t002, few or no source files should still import `graphql-tag` / `@graphql-typed-document-node/core` — both currently resolve only as transitives of a devDependency, so a hoisting change breaks `yarn build`. Eliminate the remaining imports if trivial; otherwise declare both in `dependencies`.

## Context

- Pre-t002 importers: the five shadow files plus `features/agent-sessions/hooks/use-agent-session-mutations.ts`, `features/services/hooks/use-field-mutation.ts`, `src/graphql/definitions.ts`.
- `definitions.ts` is generated — whatever it imports must be a declared dependency.

## Steps

1. Grep remaining importers post-t002.
2. Declare in `package.json` `dependencies` (or convert the stragglers), lockfile update.
3. Clean-install build check.

## Files

- `dashboard/package.json`, lockfile; straggler hooks if converted

## Acceptance criteria

- [ ] `yarn build` succeeds from a clean install; no source import resolves through a devDependency transitively.

## Out of scope

- Broader dependency pruning (inbox note `w1/068`).
