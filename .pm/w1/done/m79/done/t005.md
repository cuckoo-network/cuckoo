---
id: w1/m79/t005
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m79/t002, w1/m79/t003]
---

## Objective

Add the guard that keeps the shadow layer from growing back: a test (or lint rule) asserting no feature defines a `TypedDocumentNode` outside generated code — every operation document imports from `@/graphql/definitions`. Verify the blueprints operations behave identically post-migration (the one feature whose documents changed provenance).

## Context

- 341 existing test files; the blueprints routes/hooks have coverage to lean on.

## Steps

1. Add the no-hand-written-documents guard (grep-based test or ESLint restriction on `graphql-tag` imports under `features/`).
2. Run blueprint feature tests; add a smoke test if provenance changed any behavior.

## Files

- dashboard test/lint config + `__tests__`

## Acceptance criteria

- [ ] Guard fails on a deliberately added hand-written document; suites green.

## Out of scope

- Coverage-percentage chasing.
