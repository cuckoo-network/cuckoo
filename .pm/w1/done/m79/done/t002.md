---
id: w1/m79/t002
title: Reduce shadow operations.ts files to re-export shims
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m79/t001]
---

## Objective

Convert the five shadow files (`databases`, `keyvalue`, `services`, `projects`, `blueprints`) to the pattern `features/webhooks/api/operations.ts` already models — a ≤5-line re-export from `@/graphql/definitions` — or delete them and repoint the ~25 importing hooks directly. Kills the `__typename` type lie.

## Context

- Target end-state pattern: `dashboard/src/features/webhooks/api/operations.ts` (5-line re-export).
- Shadow files: `features/{databases,keyvalue,services,projects,blueprints}/api/operations.ts` (~840 lines total).
- Codegen types carry `__typename` non-optional (`codegen.ts:40`); expect some downstream type adjustments where code depended on the shadow types' looser shape.
- Per `dashboard/CLAUDE.md` splice discipline: migrate feature-by-feature, typecheck between each.

## Steps

1. Feature-by-feature: shim (or delete + repoint imports), `yarn typecheck`, fix fallout, `yarn test`.
2. Verify no `gql\`` template remains in the five features.

## Files

- the five `operations.ts` files + their ~25 importing hooks/routes

## Acceptance criteria

- [ ] No hand-written `TypedDocumentNode` bodies remain in the five features; typecheck + tests green after each feature.

## Out of scope

- `use-agent-session-mutations.ts` / `use-field-mutation.ts` `gql` usage (t003 decides declare-vs-eliminate).
