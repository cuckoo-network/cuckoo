---
id: w1/m90/t002
title: "Detail header: title from the derived name; suppress the empty branch row"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m90/t001]
---

## Objective

Make `/agents/$agentSessionId` name the session it is showing, and stop rendering a branch row that has no branch.

## Context

- `session-detail-header.tsx:194` is `<h1 className="truncate text-sm font-semibold">{session.repo}</h1>`.
- Immediately below, the meta row renders a `GitBranch` icon followed by `{session.branch}`. For a repo-less session that is an icon with no text next to it — verified in `dev-1`.
- The rest of the meta row (duration, turn count) is correct for both shapes and should not change.

## Steps

1. Render the t001 display name in the `<h1>`; keep `truncate` and add the full text as a `title` attribute.
2. Render the branch element only when `session.branch` is non-empty — drop the whole `<span>`, icon included, not just the text.
3. Check the phase chip + title still line up when the title is long, at desktop and narrow-mobile widths.

## Files

- `dashboard/src/features/agent-sessions/components/session-detail-header.tsx`

## Acceptance criteria

- [ ] A repo-less session renders a non-empty `<h1>`.
- [ ] A repo-less session renders no `GitBranch` icon anywhere in the header.
- [ ] A repo-backed session is unchanged: repo as the title, branch row present.
- [ ] Verified in a browser at desktop and narrow-mobile, not only in tests.

## Out of scope

- The `<title>`/breadcrumb — t004.
