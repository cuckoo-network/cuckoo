---
id: w1/m90/t007
title: Test coverage for both session shapes
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m90/t005]
---

## Objective

Lock the repo-less shape so it cannot silently regress to an empty title again.

## Context

- The bug shipped because every existing test fixture had a repo. `dashboard/src/test/mocks/agent-session.ts` is the shared mock and is the reason the gap was invisible.
- These assertions must read **rendered text**. A test that only asserts the `<h1>` exists would have passed against the broken build — the element was present and empty.

## Steps

1. Add a repo-less fixture alongside the existing one in the shared mock.
2. Assert rendered text for: detail `<h1>` non-empty; no `GitBranch` icon; list meta with no empty separator; resolved document title.
3. Add the same assertions for the repo-backed shape so the fix cannot be made by breaking that path.
4. Add the t001 unit tests if not already added there.

## Files

- `dashboard/src/test/mocks/agent-session.ts`
- `dashboard/src/features/agent-sessions/components/__tests__/`

## Acceptance criteria

- [ ] A repo-less fixture exists in the shared mock.
- [ ] Every assertion checks rendered text/absence, not just that a component mounted.
- [ ] Reverting any of t002–t004 turns its test red — verify by actually reverting.
- [ ] `yarn test` passes.

## Out of scope

- Snapshot tests of whole components.
