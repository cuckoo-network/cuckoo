---
id: w1/m41/t001
title: Design/capture: Terraform-owned LB + CCM adopt-by-name
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Pin the mechanism before touching prod: verify the Hetzner CCM's `load-balancer.hetzner.cloud/name` annotation adopts a pre-existing LB (version on the cluster, adoption vs conflict semantics, what happens on Service delete — does the CCM delete an adopted LB?), and decide the Terraform ownership shape.

## Context

- `.pm/FUTURE-MAYBE.md` entry names the two candidates: Terraform-owned LB adopted by name, or Cloudflare Tunnel (no public origin). The Terraform option is the entry's preferred fix; record why (or why not) after verification.
- hcloud CCM docs + the deployed CCM version in deploy/gitops.

## Steps

1. Verify annotation semantics against the deployed CCM version's docs/source.
2. Decide deletion-protection (Terraform `prevent_destroy`, CCM non-deletion of adopted LBs).
3. Record the design + prod adoption plan (existing LB: adopt in place vs migrate).

## Files

- (design notes into the milestone/ADR002 as fits)

## Acceptance criteria

- [ ] Written mechanism + adoption plan with version-specific evidence.

## Out of scope

- Cloudflare Tunnel implementation (recorded alternative only).
