---
id: w1/m41/t002
title: Implement: Terraform LB + `load-balancer.hetzner.cloud/name` annotation
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m41/t001]
---

## Objective

Terraform owns the LB (stable name, `prevent_destroy`), and the Traefik Service in gitops carries the adopt-by-name annotation so any future cluster's CCM re-attaches instead of re-creating.

## Files

- infra/terraform/
- deploy/gitops/ (traefik service annotations)

## Acceptance criteria

- [ ] `terraform plan` clean on re-apply; annotation present in the rendered manifests; gitops-validate passes.

## Out of scope

- Prod cutover (t003).
