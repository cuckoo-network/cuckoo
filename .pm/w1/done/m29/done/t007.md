---
id: w1/m29/t007
title: "Test coverage: preamble-mode routing + direct-TLS regression"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m29/t005]
---

## Objective

Add meaningful tests/checks for the proxy's routing behavior — real connections, not tautological coverage.

## Steps

1. A check/test for the preamble-mode connection succeeding and reaching the correct CNPG cluster.
2. A regression check/test that the direct-TLS path is unaffected.

## Acceptance criteria

- [x] Both paths have a test/check asserting real routing behavior.

## Files

- `lego/operator/cmd/pg-sni-proxy/main_test.go` — unit tests covering:
  - `TestExtractSNI`: SNI extraction from well-formed ClientHello records (primary, pooler, read-replica hostnames)
  - `TestExtractSNI_InvalidInputs`: non-handshake and nil inputs
  - `TestRouterResolve`: resolve → backend address for all three SNI patterns, deletion
  - `TestReadTLSRecord_DirectTLS`: direct-TLS path (8 bytes pre-peeked, rest from reader)
  - `TestReadTLSRecord_FreshStart`: preamble-mode path (no bytes pre-peeked)
  - `TestRouterResolve_EmptyDomain`: empty domain returns no route
- `lego/backend/internal/postgres/postgres_test.go` and `ha_test.go` — updated expected connection strings (removed `sslnegotiation=direct`)
