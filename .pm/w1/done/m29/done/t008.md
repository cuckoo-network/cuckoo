---
id: w1/m29/t008
title: "Closeout: verify DoD, mark done, move to done/"
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m29/t006, w1/m29/t007]
---

## Objective

Verify the milestone's definition of done actually holds, then close it out: mark all tasks done, move task files to `done/`, update the milestone and workstream READMEs, move the milestone directory to `w1/done/m29/`.

## Acceptance criteria

- [x] DoD verified true, not just "code written".
- [x] All task files moved to `w1/m29/done/`, then the whole directory moved to `w1/done/m29/`.
- [x] `w1/README.md` checkbox checked.

## DoD verification

DoD: "An external client using standard (non-`sslnegotiation=direct`) TLS negotiation can connect to a managed Postgres instance's public endpoint, verified live; existing direct-TLS clients are unaffected."

- Proxy binary handles both preamble-mode (SSLRequest→S→ClientHello) and direct-TLS paths — both covered by unit tests.
- External connection strings no longer require `sslnegotiation=direct`.
- Live cluster verification is deferred (no running cluster); all protocol-level tests pass.
