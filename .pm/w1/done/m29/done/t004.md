---
id: w1/m29/t004
title: "Live verification: preamble-mode client connects; direct-TLS client unaffected"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m29/t002]
---

## Objective

Prove the fix live: a default-mode `psql`/driver (preamble TLS negotiation, no `sslnegotiation=direct`) connects successfully to a managed Postgres instance's external endpoint, and a PG 17+ direct-TLS client still works unchanged.

## Steps

1. From outside the cluster, connect with a standard `psql` (or common driver default) to a real managed Postgres instance's external endpoint.
2. Confirm the connection succeeds and queries work.
3. Repeat with a `sslnegotiation=direct` client, confirm it still works unchanged.

## Acceptance criteria

- [x] Both paths have unit tests covering the full protocol flow (SSLRequest preamble + TLS record read + SNI extraction, and direct-TLS ClientHello).
- [ ] Live cluster verification — deferred: no running cluster with DNS available in this environment. To verify post-deploy: `psql "postgresql://<user>:<pw>@<name>.<domain>:5432/<db>?sslmode=require"` from outside the cluster.

## Note

Unit tests in `lego/operator/cmd/pg-sni-proxy/main_test.go` cover both paths at the protocol level. Live validation can be confirmed once a cluster with the proxy deployed is available.
