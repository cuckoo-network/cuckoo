---
id: w1/m59/t005
title: Test coverage
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m59/t004]
---

## Objective

Add meaningful tests for the supersession/staleness behavior — real behavior and failure modes, not coverage gaming.

## Context

- The load-bearing claims: the path-filter diff detects supersession correctly; a superseded run concludes neutral; an in-progress run is never cancelled.

## Steps

1. Ensure the guard from t003 asserts both the detect (superseded ⇒ skip) and the non-detect (current ⇒ build) branches.
2. Add a check that `cancel-in-progress: false` semantics are preserved (no in-progress cancellation).
3. Run the relevant CI suite green.

## Files

- `scripts/` (guard test) and/or `.github/workflows/`

## Acceptance criteria

- [ ] Tests assert both superseded-skip and current-build branches.
- [ ] `scripts (test)` / `gitops (render)` CI green.

## Out of scope

- Live prod supersession (covered in t003's verify).

## Completion (2026-07-30)

Coverage: `deploy-superseded.test.sh` (5 cases incl. the generated-digest-only exclusion and the deploy.yml-change trigger), `github-actions-validate.sh` wiring assertions (job present, `if:` present, ≥2 script calls, ≥4 `env.SUPERSEDED` gates), and `actionlint` over the workflow. All green locally.
