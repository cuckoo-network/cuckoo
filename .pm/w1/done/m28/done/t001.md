---
id: w1/m28/t001
title: CI workflow — backend go test
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Add a CI workflow that runs `cd lego/backend && go test ./...` on every PR and push touching `lego/backend/**` — today only `golangci-lint` runs for this module; the test suite never executes automatically.

## Context

- Mirror `go-lint.yml`'s trigger shape (`pull_request`/`push` on `main`, `paths: ["lego/backend/**", ...]`) and its `actions/setup-go@v5` step.
- `lego/backend` is its own Go module (`lego/go.work` workspace) — `go test ./...` from `lego/backend/` is the correct invocation, matching `CLAUDE.md`'s documented command.
- Most tests will pass without extra setup; the `BEX_TEST_DB_URI`/`BEX_TEST_OPENFGA_URL`-gated ones will still skip until t004 wires ephemeral services — that's fine for this task, which just gets the workflow running at all.

## Steps

1. Create a new workflow file (or add a job to `go-lint.yml`) that checks out, sets up Go, and runs `go test ./...` in `lego/backend/`.
2. Confirm it triggers on the same path filters as `go-lint.yml` plus itself.
3. Push and confirm it runs green against current `main`.

## Files

- new `.github/workflows/go-test-backend.yml` (or a new job appended to `go-lint.yml`)

## Acceptance criteria

- [ ] Workflow runs `go test ./...` in `lego/backend/` and passes against current `main`.

## Out of scope

- Ephemeral Postgres/OpenFGA services for the currently-skipped tests — t004.
