---
id: w1/m38/t004
title: "Execute Push 2b: platform pool onto the baked bex-worker image, one node at a time"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m38/t002, w1/m38/t003]
---

## Objective

With the pool now drain-safe, flip the platform pool to the baked snapshot exactly like m36 did the tenant pool: apply the `STAGED ROLL` markers in `infra/clusterapi/overlays/hetzner-caph/cluster.yaml` (`bex-platform` → `bex-platform-baked`, `imageName: bex-worker`, trim the platform `preKubeadmCommands`), then roll one node at a time — watching CNPG switchover and performing the t003 OpenBao unseal per member.

## Context

- `w1/m36`'s tenant-pool roll is the template (its README § Measurements has the timing baseline and the pitfalls it hit).
- `.pm/w1/done/023.md` step 3 (renumbered from `022` on promotion) — the exact marker list.

## Steps

1. Apply the marker changes; push (CI applies via CAPI).
2. Roll node 1 → verify CNPG promotions + OpenBao unseal + all platform workloads Ready; repeat per node.
3. Remove the now-dead `STAGED ROLL`/TEMP comments; record bring-up timing vs. the ubuntu baseline.

## Files

- `infra/clusterapi/overlays/hetzner-caph/cluster.yaml`

## Acceptance criteria

- [x] All platform nodes on `bex-worker`; the final one-node-at-a-time acceptance showed no sustained auth/control-plane outage (isolated misses recorded in the milestone evidence); markers gone.

## Out of scope

- Tenant pool (done in m36); Hetzner quota-raise shape (FUTURE-MAYBE).
