---
id: w1/m33/t005
title: Dashboard - Build & Deploy field + deploy-detail pre-deploy status
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m33/t003, w1/m33/t004]
---

## Objective

Expose `preDeployCommand` as an editable field in the dashboard's Build & Deploy settings, and show the pre-deploy step's status/logs on the deploy-detail view.

## Context

- `dashboard/CLAUDE.md` — dashboard conventions.
- `w1/done/m21` or wherever Build & Deploy settings currently live (check the existing settings tab shipped for `w6/m21`'s `dockerfilePath`/`startCommand` fields and follow the same layout).

## Steps

1. Add a `preDeployCommand` input to the service's Build & Deploy settings tab.
2. On the deploy-detail view, show the pre-deploy step distinctly (pending/running/succeeded/failed) with a link/panel to its logs when failed.

## Files

- `dashboard/` (Build & Deploy settings tab, deploy-detail view)

## Acceptance criteria

- [ ] Setting `preDeployCommand` from the dashboard persists and round-trips.
- [ ] A failed pre-deploy step is visually distinguishable from a failed health check on the deploy-detail view.

## Out of scope

- Log streaming UI beyond what the existing logs viewer already provides.
