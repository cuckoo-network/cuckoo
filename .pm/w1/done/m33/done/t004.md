---
id: w1/m33/t004
title: bex.yml/REST/GraphQL/MCP wiring for preDeployCommand
worker: worker1
status: done
estimate: 1h
depends_on: [w1/m33/t001]
---

## Objective

Accept `preDeployCommand` on App create/update across bex.yml ingestion, REST, GraphQL, and MCP, and stop silently ignoring it.

## Context

- `docs/ADR006-bex-api.md:124` — `preDeployCommand` is currently in the "ignored" bucket of bex.yml fields; update this doc once implemented.
- `lego/backend/internal/apps/deploy.go` handles bex.yml/render.yaml-shaped manifest ingestion (same file `w6/m21/t004` touches for `dockerfilePath`/`startCommand`).

## Steps

1. Add `preDeployCommand` to the bex.yml schema and App create/update REST/GraphQL/MCP request types.
2. Thread through to `App.Spec.PreDeployCommand` on CR create/update.
3. Update `docs/ADR006-bex-api.md`'s ignored-fields list to remove `preDeployCommand`.

## Files

- `lego/backend/internal/apps/deploy.go`
- `lego/backend/internal/apps/rest.go`, `graphql.go`, `mcp.go`
- `docs/ADR006-bex-api.md`

## Acceptance criteria

- [ ] Creating/updating an App with `preDeployCommand` via bex.yml, REST, GraphQL, and MCP all set the CR field.
- [ ] Omitting the field is fully backward compatible.

## Out of scope

- Dashboard UI (t005).
