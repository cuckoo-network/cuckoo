---
id: w1/m33/t006
title: Render parity check - preDeployCommand
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m33/t005]
---

## Objective

Verify `preDeployCommand` field shape/semantics and the deploy-record pre-deploy status are consistent across REST, GraphQL, MCP, and the dashboard UI, and match render.com's Pre-Deploy Command behavior.

## Steps

1. Confirm identical field shapes across REST/GraphQL/MCP.
2. Confirm the dashboard reflects the same pre-deploy status states as the API.
3. Compare against render.com's Pre-Deploy Command semantics (blocking, failure behavior, log visibility); document any deliberate drift.

## Files

- `lego/backend/internal/apps/*.go`
- `dashboard/` (Build & Deploy settings, deploy-detail view)

## Acceptance criteria

- [ ] Field shapes verified identical across REST/GraphQL/MCP.
- [ ] Any drift from render.com documented as a follow-up note, not silently shipped.

## Out of scope

- New capability beyond what t001-t005 shipped.
