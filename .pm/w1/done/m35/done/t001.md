---
id: w1/m35/t001
title: `envVarGroups:` blocks create/update env groups at apply
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Accept a top-level `envVarGroups:` list in `bex.yml` (Render's shape: name + envVars) and materialize each as a bex env group at apply time — create if absent, update vars if present — through the existing `internal/envgroups` verbs.

## Context

- Blueprint parsing/apply: find via `docs/ADR006-bex-api.md` §bex.yml (w1/m24's all-or-nothing apply path + w2/m15's `internal/blueprints`).
- Env groups: `lego/backend/internal/envgroups` (w1/m16). Verify Render's exact `envVarGroups` schema from render.yaml docs before coding.

## Files

- `lego/backend/internal/blueprints/` (or the bex.yml apply path), `internal/envgroups` callers

## Acceptance criteria

- [ ] A bex.yml with `envVarGroups` creates the groups; re-apply updates them idempotently; all-or-nothing semantics preserved.

## Out of scope

- Linking to services (t002).
