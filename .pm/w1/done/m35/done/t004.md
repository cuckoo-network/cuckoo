---
id: w1/m35/t004
title: `fromService.envVarKey` cross-service references
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Honor `envVars: [{fromService: {name, type, envVarKey}}]`: at apply, resolve the referenced sibling service's env var (or property, if Render's schema includes `property: host/port` — verify) and inject the value, with ordering across the stack (referenced service applied first — the `fromDatabase` DB-first precedent from w1/m24).

## Files

- bex.yml apply path (`internal/blueprints/`), ordering logic beside `fromDatabase`'s

## Acceptance criteria

- [ ] A two-service bex.yml where B references A's var deploys with B seeing A's value; a dangling reference is a per-entry validation error.

## Out of scope

- Live re-sync when A's var later changes (Render doesn't either — apply-time resolution).
