---
id: w1/m35/t005
title: Accept `generateValue` in bex.yml (rides w8/m10's core verb)
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m35/t001]
---

## Objective

Accept `generateValue: true` on bex.yml env vars (service-level and `envVarGroups`), delegating to the `generateValue` core verb w8/m10/t001 ships. Idempotency matters: re-apply/sync must NOT re-mint (the first generated value persists — compose with t003's sync semantics; Render treats generated values as sync-exempt once set — verify).

## Context

- **Cross-milestone dependency: `w8/m10` t001** must land first; if picked up before it, do w8/m10/t001 there, not a parallel implementation here.

## Files

- bex.yml apply path

## Acceptance criteria

- [ ] First apply mints; second apply/sync preserves; asserted by test.
