---
id: w1/m34/t002
title: Webhook: glob matching in the push path-filter
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m34/t001]
---

## Objective

Honor `buildFilter` in the git-push webhook's auto-deploy decision: `ignoredPaths` matches exclude, `paths` (when non-empty) require a match, composed with the existing `rootDirMatches` scoping — matching Render's documented buildFilter semantics.

## Context

- The push webhook + `rootDirMatches` live in `lego/backend/internal/` (w1/m18 shipped the rootDir path filter; find it via `rootDirMatches`).
- Render uses glob patterns (e.g. `src/**`); pick the glob library deliberately (`path.Match` has no `**` — document the choice).
- Decide + document the interaction: are globs relative to repo root or rootDir? Check Render's docs; don't guess silently.

## Files

- `lego/backend/internal/**/webhook.go` (rootDirMatches call sites)

## Acceptance criteria

- [ ] Table-driven tests: ignored-only push ⇒ no deploy; paths-match ⇒ deploy; empty filter ⇒ today's behavior unchanged.

## Out of scope

- Manual `POST /deploys` triggers (buildFilter gates pushes only).
