---
id: w1/m34/t001
title: CRD: `spec.buildFilter{paths[],ignoredPaths[]}` + codegen
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Add `BuildFilter` (`paths []string`, `ignoredPaths []string`, glob patterns) to the App spec in `lego/types`, mirroring Render's `buildFilter` object, and regenerate deepcopy/CRD manifests.

## Context

- `lego/types/v1alpha1/app_types.go`; codegen runs from `lego/operator/` (`make test` auto-runs manifests/generate).
- Check Render's OpenAPI for the exact field names/optionality before writing the type.

## Files

- `lego/types/v1alpha1/app_types.go`, generated deepcopy + `lego/operator/config/crd/`

## Acceptance criteria

- [ ] CRD round-trips the two arrays; `make test` (codegen included) green.

## Out of scope

- Any matching logic (t002).
