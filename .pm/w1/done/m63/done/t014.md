---
id: w1/m63/t014
title: Simplify pass over the Blueprint compiler and adapters
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m63/t013]
---

## Objective

Standing closing task: run `/simplify` over the m63 diff, focusing on one source of field policy, removal of old parser/helpers, shared adapter translation, deterministic planning, and avoiding parallel representations while preserving behavior.

## Context

- Likely duplication risks: schema registry versus Go switches, IR versus old `bex*` structs, adapter-specific validation envelopes, and repeated filename discovery.
- ADR049 requires one grammar/compiler; compatibility aliases must terminate at the boundary.

## Steps

1. Run `/simplify` scoped to the m63 diff.
2. Delete superseded parsers/structs/helpers and consolidate repeated mappings where tests prove equivalence.
3. Apply accepted findings and keep all focused suites green.

## Files

- `lego/backend/internal/apps/**`
- `lego/backend/internal/store/blueprints.go`
- `dashboard/src/features/blueprints/**`
- `scripts/app-apply.sh` (if retained as a thin client)

## Acceptance criteria

- [ ] Findings are applied or explicitly declined with reasons; no semantic YAML parser or field-policy table remains outside the compiler/registry.
- [ ] No behavior or wire-shape change is introduced by the pass; focused suites and lint remain green.

## Out of scope

- New parity capabilities or contract changes.
