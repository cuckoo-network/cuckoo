---
id: w1/m63/t013
title: Render parity check across REST/GraphQL/MCP/dashboard/official CLI
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m63/t012]
---

## Objective

Standing closing task: compare the shipped Blueprint contract against current Render behavior and prove every bex surface exposes the same accepted subset, validation semantics, action plan, and unsupported-field failures.

## Context

- Primary baseline: Render Blueprint reference, pinned schema, Validate Blueprint endpoint, and IaC lifecycle docs recorded in ADR049.
- Surfaces: REST multipart + JSON compatibility input, GraphQL, MCP, dashboard review/deploy/sync, deploy-from-chat, and the unmodified official Render CLI pointed at bex-api.
- The registry is the expected-state map; drift is filed, never silently waived.

## Steps

1. Re-fetch/check the upstream schema digest and review any drift before comparing behavior.
2. Exercise accepted, deprecated, unsupported, unknown, omission, explicit-empty, nested, and reference fixtures through every applicable surface.
3. Run representative validation through the unmodified official Render CLI and walk the dashboard create/review/sync flow.
4. Record evidence in ADR018/CLI checklist and file any remaining divergence as a concrete follow-up.

## Files

- `docs/ADR018-render-parity.md`
- `docs/cli-compatibility-checklist.md`
- `docs/render-artifacts/**` (only for new dated evidence)
- `.pm/w1/*.md` (only for residuals found)

## Acceptance criteria

- [ ] Every surface returns the same validity, error code/path, and action semantics for the shared corpus.
- [ ] The official CLI decodes both valid and invalid bex responses without a compatibility workaround.
- [ ] Any remaining drift is explicit in the registry/ADR and filed; none is described as full parity.

## Out of scope

- Fixing unrelated Render API parity gaps discovered during the sweep.
