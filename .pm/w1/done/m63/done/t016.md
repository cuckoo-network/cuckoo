---
id: w1/m63/t016
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m63/t015]
---

## Objective

Close the milestone only after ADR049's observable contract is real: all Blueprint entrypoints share the strict compiler/plan, the stored-manifest audit is resolved, the official CLI and dashboard evidence is captured, and every unsupported field fails before mutation.

## Context

- The DoD in `w1/m63/README.md` is behavior-based, not “compiler code merged.”
- Any temporary audit/legacy enforcement switch must be removed; the end state has one grammar and one compiler.
- Board closeout requires all task/status locations and moves to remain synchronized.

## Steps

1. Verify the milestone DoD against a running dev-1 stack and the conformance/official-CLI checks.
2. Confirm the stored Blueprint audit has no unresolved silent-ignore or unnamespaced-extension case.
3. Execute the closeout moves (`/pm done w1/m63/t016` last) and update the w1 milestone summary with evidence.
4. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m63/` → `.pm/w1/done/m63/`
- `.pm/w1/README.md`
- `docs/ADR049-render-yaml-parity.md` (implementation status only)

## Acceptance criteria

- [ ] DoD evidence is recorded, all three status locations agree, every task is under `done/`, and the original milestone path is gone.
- [ ] No temporary parser, audit mode, or filename-specific grammar remains.

## Out of scope

- Shipping (`/ship` remains user-triggered) or removing the deprecated `bex.yml` alias.
