---
id: w1/m63/t007
title: Close datastore adapter gaps: storage autoscaling, PgBouncer, and pooled references
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m63/t003]
---

## Objective

Map Render Postgres `storageAutoscalingEnabled`, `connectionPool`, and `fromDatabase.property: connectionPoolString` onto bex's existing `DatabaseSpec.DiskAutoscaling`, CNPG PgBouncer Pooler, and pooler connection Secret/status paths.

## Context

- `lego/types/v1alpha1/database_types.go` already has `DiskAutoscaling`, `Pooler`, and pooler host status.
- The current Blueprint database struct documents autoscaling as omitted and the DB reference property map lacks `connectionPoolString`.
- Presence-aware create/sync policy comes from t005; this task supplies the missing adapters.

## Steps

1. Add the three official fields/properties to IR validation with plan/tier constraints.
2. Translate `pgbouncer`/`none` and true/false without losing omission.
3. Resolve pooled connection references to secret-backed values, never plaintext manifest injection.
4. Verify create, sync, disable, and reference behavior through backend/operator tests.

## Files

- `lego/backend/internal/apps/deploy.go`
- `lego/backend/internal/apps/blueprint_plan.go`
- `lego/backend/internal/postgres/**`
- `lego/types/v1alpha1/database_types.go`
- `lego/operator/internal/controller/database*_test.go`

## Acceptance criteria

- [ ] Render fields produce the same CRD state as existing Postgres API verbs.
- [ ] Omission preserves existing settings; explicit `none`/`false` disables them.
- [ ] `connectionPoolString` remains SecretRef-backed and works only when a pooler is enabled, with a path-specific error otherwise.

## Out of scope

- Building a new connection-pooling implementation.
