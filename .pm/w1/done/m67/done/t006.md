---
id: w1/m67/t006
title: Render parity
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m67/t001, w1/m67/t003, w1/m67/t004, w1/m67/t005]
---

## Objective

Check this milestone's tenant-visible changes for consistency across REST, GraphQL, MCP, and the dashboard, and against render.com's equivalent behavior; flag drift as follow-up rather than diverging silently.

## Context

- t004/t005 add a refusal and a history-retention semantic that tenants observe (Render's own webhook product has delivery history with its own retention — compare and record).
- t001 changes token acceptance for third-party clients (`docs/ADR025-connect-an-agent.md` recipe is the live test).
- t003 changes the connect callback contract and its error surface (`docs/ADR026-github-integration.md`).
- `docs/ADR018-render-parity.md` is the ledger; `docs/ADR006-bex-api.md` is the one-core/thin-adapters rule.

## Steps

1. Exercise the endpoint-cap refusal on all three API surfaces; confirm identical codes/messages/shapes.
2. Confirm the dashboard's webhooks pages (`/webhooks`, `/webhook/$id`) handle the cap and the retention boundary gracefully.
3. Re-run the agent-OAuth recipe end to end after the token-class change.
4. Walk the GitHub connect flow in the dashboard after t003.
5. Update `docs/ADR018-render-parity.md` only where a cell's evidence actually changes.

## Files

- `docs/ADR018-render-parity.md` (only if a row changes)

## Acceptance criteria

- [ ] Cap refusal identical across REST/GraphQL/MCP; dashboard shows it as a human message, not a raw error.
- [ ] Agent/MCP OAuth still works after the audience tightening.
- [ ] Connect flow walked end to end post-change.
- [ ] Drift, if any, filed as a note.

## Out of scope

- New parity features.
