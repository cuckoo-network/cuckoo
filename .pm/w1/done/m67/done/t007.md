---
id: w1/m67/t007
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m67/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- Touched: `lego/backend/internal/api/` (auth + server middleware), `lego/backend/internal/github/`, `lego/backend/internal/webhooks/`, `lego/backend/internal/store/`.
- Specifically look for: a single limiter-construction shape shared by the four IP-keyed limiters; one durable single-use-nonce mechanism shared with the existing ticket store rather than a second one; one retention-sweep shape shared with audit/push.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving cleanups; file anything larger as a note.

## Files

- as touched by t001–t005

## Acceptance criteria

- [ ] No second implementation of a mechanism the repo already has (nonce store, retention sweep, IP-keyed limiter).
- [ ] All suites still green after the pass.

## Out of scope

- Bug hunting and behavior changes.
