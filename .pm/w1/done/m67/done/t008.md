---
id: w1/m67/t008
title: Test coverage
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m67/t006]
---

## Objective

Ensure each finding closed here has a regression test that fails on the pre-fix code, including the multi-replica and concurrency properties that only a real Postgres can prove.

## Context

- `cd lego/backend && go test ./...`; integration tests gated on `BEX_TEST_DB_URI`/`BEX_TEST_OPENFGA_URL` run for real in CI (`.github/workflows/backend-test.yml`), so concurrency assertions belong there.
- Prior art for the real-Postgres style: `w1/m61`'s FK-cascade test, `w1/m58`'s multi-replica claims.

## Steps

1. Confirm each of t001–t005 landed its failing-before/passing-after test; add what is missing.
2. Add the concurrency/replica tests: endpoint-cap race, nonce single-use race, sweep idempotence under two runners.
3. Add an upstream-call-count assertion for the pre-auth limiter (the property that actually matters for F1).
4. Run the full suites and record results in the milestone README.

## Files

- `lego/backend/internal/api/*_test.go`
- `lego/backend/internal/github/*_test.go`
- `lego/backend/internal/webhooks/*_test.go`
- `lego/backend/internal/store/*_test.go`

## Acceptance criteria

- [ ] Each closed finding has a test that fails when its fix is reverted (spot-checked by reverting).
- [ ] Race/replica properties covered against real Postgres.
- [ ] Backend suite + `make lint` green.

## Out of scope

- Coverage-percentage targets.
