---
id: w1/m19/t011
title: Test coverage — scripted substrate assertions (verify-substrate.sh)
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m19/t007, w1/m19/t008]
---

## Objective

Make t009's acceptance repeatable: a script asserting the substrate invariants so regressions fail loudly, in the m13 style (asserting, non-tautological).

## Context

- Candidate checks: hcloudNetwork enabled + machines' private IPs present; CP count==3 and tainted; platform pods' node placement; CSR event scan; public port scan (nc against node IP); LB target privateness (hcloud API); KCP Available; scheduler --config flag. verify-elastic.sh already covers the elastic behaviors - do not duplicate.

## Steps

1. Expand `scripts/verify-substrate.sh` from the m19.1 pivot checks to all ten final-substrate invariants.
2. Rewrite `scripts/verify-elastic.sh` for the self-managed world: select the annotated tenant MachineDeployment, derive the scale-up load from the live baseline, guard app-bearing nodes, score both CPU and memory, and poll scale-down without a fixed sleep.

## Files

- scripts/verify-substrate.sh

## Acceptance criteria

- [x] Scripts exit 0 on rebuilt production and use named `FAIL: <invariant>` exits for every broken assertion.

## Completion evidence (2026-07-15)

- `verify-substrate.sh` passes its normal fetched-kubeconfig path: access, self-management, target shape, placement, data, scheduler, CSR, network, autoscaler, and no-pet.
- `verify-elastic.sh` passes from a two-node production baseline: anti-affinity forced 2→3, the probe selected the unique live MostAllocated winner, and the surplus node returned to the baseline after the five-minute autoscaler window.
- Cleanup removes the test namespace and temporary scale-down guards on both success and named failure.

## Out of scope

- CI scheduling of the script (candidate follow-up note).
