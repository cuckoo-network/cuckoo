---
id: w1/m19/t010
title: Simplify — /simplify over what this milestone changed
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m19/t009]
---

## Objective

_Rescope note 2026-07-11: most of the rebuild's diff was already simplified by m19.1 t007. The closeout review covered the quota-revert residue, both verification scripts, and confirmed `infra/clusterapi/autoscaler-values.yaml` already describes the in-cluster deployment correctly._

Run /simplify over the milestone's diff (overlay, gitops scheduling sweep, workflows, terraform, scripts) - reuse/simplification/efficiency, behavior-preserving.

## Context

- The t003 sweep especially is copy-paste-prone (same toleration block in ~20 files) - look for a kustomize patch/component to seal it once.

## Steps

1. Run /simplify; apply accepted fixes; gitops-validate + terraform plan stay green.

## Files

- whatever the review flags

## Acceptance criteria

- [x] Simplification review run; accepted fixes applied; validations green.

## Completion evidence (2026-07-15)

- Reused one MachineDeployment JSON read for pool/min/max selection instead of repeated live reads.
- Replaced the fixed four-minute scale-down sleep with one bounded 20-second polling loop.
- Kept substrate assertions grouped by invariant with shared `wl`, `hc`, `pass`, and `fail` helpers; no duplicate platform manifests or infrastructure declarations were introduced.
- `bash -n` and the live production proofs pass after the cleanup.

## Out of scope

- Bug-hunting (that is /code-review's job).
