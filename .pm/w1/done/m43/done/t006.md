---
id: w1/m43/t006
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m43/t005]
---

## Objective

When the other tasks are complete **and the definition of done is actually met** (green `gitops (render)` run on `main` linked as evidence), close the milestone: sync every task's status, move task files to `done/`, mark the README rows `— **DONE**` + `**Status:** done`, move `w1/m43/` to `w1/done/m43/`, and check the box in `w1/README.md`.

## Acceptance criteria

- [ ] DoD verified before any move; board synced in all three status locations.
