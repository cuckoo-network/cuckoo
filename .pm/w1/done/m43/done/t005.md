---
id: w1/m43/t005
title: Test coverage
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m43/t003]
---

## Objective

The promtool fixture *is* the test for t002; the coverage question is the guard itself: confirm `scripts/gitops-validate.sh` (and the CI check that wraps it) actually exercises both fixed failure modes — helm chart schema validation against the pinned chart version, and promtool rules + unit tests — so either regression re-trips the check. Close any gap in the script rather than adding tautological tests elsewhere.

## Acceptance criteria

- [ ] Both failure modes demonstrably covered by the script (verified by reading it, or by a deliberate local breakage that trips it).
