---
id: w1/m13/t005
title: Simplify — run /simplify over anything this milestone changed
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m13/t004]
---

## Objective

Run `/simplify` over whatever the audit changed (docs, any capture tooling, index rows) — reuse / simplification / efficiency; behavior-preserving. If the milestone shipped no code, apply it to the doc structure (dedupe against docs/ADR006-bex-api.md's existing parity notes rather than restating them).

## Acceptance criteria

- [x] `/simplify` run over the milestone's diff; accepted fixes applied.

## Closure (2026-07-08)

Docs-only milestone, so `/simplify` ran over the doc structure (two parallel dedup/altitude reviewers over `docs/ADR018-render-parity.md` + the 5 notes). Applied: added the missing **Health checks** matrix row (an orphan `w1/005` backlog entry had no matrix source); trimmed the env-vars divergence cell to point at `docs/ADR006-bex-api.md` instead of re-deriving it; reworded the header-rules `—` so it reads as a scoped non-goal, not a dodge; flipped **one-off jobs** from ✖ to — for consistency with Shell/SSH (both are the off-roadmap exec surface); fixed a prettier emphasis-collision (`_type_` next to `DO_NOT_DO`). Skipped as by-design: the Headline's frontier summary and the per-note boilerplate footer. Prettier idempotent-clean.
