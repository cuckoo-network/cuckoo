> **Closed 2026-08-19 (w1 triage).** Deferred — not a live hazard. High-priority items from the 2026-08-19 architecture review already shipped as `w1/m76`–`m80`. Trigger-gated in `.pm/FUTURE-MAYBE.md` § "Architecture-review leftovers (2026-08-19)". Original text below kept for provenance.

# Dashboard mechanical bundle: useFieldMutation, route factory, dialogs, section rail, GraphQL hygiene

From the 2026-08-19 refactor review (§3 dashboard). Independent, all typecheck-verified; promote as one or two milestones when scheduled:

- **Promote `useFieldMutation`** (`features/services/hooks/use-field-mutation.ts:22-53`) to `common/hooks/` (+ a `busyId` variant): the mutate→toast→busy shape it absorbs is hand-rolled in 44 files across 15 features, several byte-identical (`use-set-environment-{services,databases,keyvalues,env-groups}.ts` ×4; `use-rename-{environment,project,workspace}.ts`). Leave the conditional-messaging hooks alone — the doc comment at :17-20 already draws the line. ~−500.
- **`detailRouteOptions` factory** for the 8 byte-identical detail-route config blocks (`staticData`/`pendingComponent`/`pendingMs: 0`/`requireAuth`/`loadRouteResource`/`titleHead` — services, static, databases, keyvalue, blueprints, env-groups, project, webhook routes). Constraint: the `component === pendingComponent` identity must stay in the route module for the code-splitter (dashboard/CLAUDE.md § Navigation pending states) — the factory takes the component. ~−180.
- **`<SudoConfirmDialog>`** absorbing the 5 independent type-to-confirm gates around the existing `common/components/sudo-command-field.tsx` (databases, keyvalue ×2, env-groups ×2, workspaces, services delete cards) — standardizes a destructive-action gate with 5 chances to be subtly wrong. **`<NameOnlyCreateDialog>`** for the projects/environments twins whose comments say they mirror each other. ~−320.
- **`<SectionRailPage nav sections={[{id, when, render}]}>`** — the 200-char rail class literal is verbatim in 5 files, and `services.$serviceId.settings.tsx:73-87` restates 12 visibility predicates twice (nav list + JSX guards) with unenforced ordering agreement. Derive both from one table. Ship the service-settings page last. ~−250.
- **GraphQL hygiene:** add the 7 missing shared fragments (6 exist for 212 ops; registry-credentials repeats one selection 4×, workspaces 4×, cron-runs 4×; the databases list/detail pair forces `status.ts:33-34`'s structural sniffing); delete dead selections (`dashboardUrl` in services+server.graphql, `poolerEnabled`, `ipAllowList` in databases.graphql); publish the detail layout's `service` through context so 9 `useServer` call sites stop hand-remembering `{poll: false}` (`use-server.ts:13-20`'s footgun). ~−120 + smaller generated types.
- Also apply the `useNewServiceForm` extraction pattern (`routes/services.new.tsx:64`) to `keyvalue.new.tsx:89-100` (9 inline useState) and `create-database-dialog.tsx:70` (285-line function).
