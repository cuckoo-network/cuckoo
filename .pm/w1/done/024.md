# Verify/close the w1/m36 manual default-SA imagePullSecrets patch (undeclared prod drift)

`w1/done/m36/README.md:53` flagged a follow-up that was never filed: a live
`kubectl patch` attached `bex-registry-pull` to the `default` ServiceAccount's
`imagePullSecrets` in prod — state that exists nowhere in git (undeclared
gitops drift). `w7/m36`'s per-App `reg-pull-<name>` credentials likely made it
moot (t004 explicitly replaced the shared pull secret for tenant workloads,
keeping the shared path only for the unset/dev case), but nothing confirms the
patch was reverted or declared.

Verify-first, sub-hour:

1. Inspect the live `default` ServiceAccount in the apps namespace.
2. If the patch is gone or moot under per-App creds ⇒ revert any remnant and
   close this note.
3. If it is still load-bearing ⇒ declare it in `deploy/gitops` (or switch the
   dependency to the operator-attached path per docs/ADR022 § Registry access
   control) — if that grows beyond an hour, promote to a milestone.

Source: `/pm-brainstorm` round 18, 2026-07-15 — closeout-residual sweep.
