# Verify/close the w1/m36 manual default-SA imagePullSecrets patch (undeclared prod drift)

`w1/done/m36/README.md:53` flagged a follow-up that was never filed: a live `kubectl patch` attached `bex-registry-pull` to the `default` ServiceAccount's `imagePullSecrets` in prod — state that exists nowhere in git (undeclared gitops drift). `w7/m36`'s per-App `reg-pull-<name>` credentials likely made it moot (t004 explicitly replaced the shared pull secret for tenant workloads, keeping the shared path only for the unset/dev case), but nothing confirms the patch was reverted or declared.

Verify-first, sub-hour:

1. Inspect the live `default` ServiceAccount in the apps namespace.
2. If the patch is gone or moot under per-App creds ⇒ revert any remnant and close this note.
3. If it is still load-bearing ⇒ declare it in `deploy/gitops` (or switch the dependency to the operator-attached path per docs/ADR022 § Registry access control) — if that grows beyond an hour, promote to a milestone.

Source: `/pm-brainstorm` round 18, 2026-07-15 — closeout-residual sweep.

## Resolution (2026-07-15, w10/m4/t003) — verified live, no undeclared drift remains

Inspected the prod apps namespace (`default`) via the CP-fetched admin kubeconfig:

1. **The manual SA patch is gone** — the `default` ServiceAccount carries no `imagePullSecrets` at all; nothing to revert.
2. **The load-bearing path is the operator-attached one** (option 3 of this note, per `docs/ADR022-tenant-isolation.md` § Registry access control): every tenant Deployment carries `imagePullSecrets: [{name: bex-registry-pull}]` attached by the operator from `BEX_REGISTRY_PULL_SECRET=bex-registry-pull`, which **is declared in git** (`lego/operator/config/manager/manager.yaml`, deployed by CI `make deploy`). The Secret itself is minted out-of-band by `scripts/registry-secrets.sh` — the documented credential custody, not drift.
3. `BEX_REGISTRY_NS` is unset in prod, so the shared-pull-secret path (w7/m8) is the active mechanism; `w7/m36`'s per-App `reg-pull-<name>` path is built but not yet enabled there (no `reg-pull-*` Secrets exist — consistent, not drift). All tenant pods `Running` at inspection time.
