---
id: w1/m3/t007
title: Test coverage — script-level checks for the elastic behavior (else close n/a)
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m3/t005]
---

## Objective

This milestone ships yaml, not Go — coverage means making the t005 verification repeatable: a script (mock cluster) that asserts pack-onto-fullest-node, pending-pod→node-join, and drain→scale-down, failing non-zero when the behavior regresses. If nothing scriptable beyond t005's one-shot run is worth keeping, close n/a with a one-line note — never write tautological checks.

## Acceptance criteria

- [x] Either a repeatable verify script asserting the three elastic behaviors, or an explicit n/a closure note. _Met 2026-07-09:_ `scripts/verify-elastic.sh` — repeatable, asserting, fails non-zero; env-var-driven (`MGMT_CTX`/`WL_KUBECONFIG`/`SCALE_DOWN_WAIT`) so it also targets prod pairs. Passed end-to-end on the mock.
