---
id: w1/m3/t006
title: Simplify — run /simplify over what this milestone changed
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m3/t005]
---

## Objective

Run `/simplify` over what t002–t005 touched (scheduler config in the CAPH overlay, autoscaler Argo app, MachineDeployment annotations, verify script) — reuse / simplification / efficiency; behavior-preserving.

## Acceptance criteria

- [x] `/simplify` run over the milestone's diff; accepted fixes applied; `kustomize build` of the touched overlays still green. _Met 2026-07-09:_ 4-angle review (reuse/simplification/efficiency/altitude). Applied: single `scripts/install-autoscaler.sh` owning the chart pin (CI + mock call it; `--repo`/`--wait` replace repo-add + a hardcoded deployment name); comment-only `deploy/gitops/base/autoscaler.yaml` deleted from the kustomize build (values-file header is the one decision record, others point at it); `mock-cluster.sh scale N` now raises the autoscaler min-size floor instead of fighting over `replicas`; verify script: one cluster-wide pods dump, `kubectl wait` over a poll loop, no pre-window polling. Skipped (reviewer consensus): deduping the scheduler YAML across the two overlays — no shared templating layer; cross-referencing comments are the mitigation. `gitops-validate.sh` green; `scripts/verify-elastic.sh` re-run end-to-end after the fixes: exit 0.
