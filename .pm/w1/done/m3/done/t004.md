---
id: w1/m3/t004
title: Autoscaler min/max on the bex-worker-0 MachineDeployment
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m3/t003]
---

## Objective

Bound the elastic range: set the autoscaler min/max node counts on the worker pool.

## Context

- cluster-autoscaler reads min/max from MachineDeployment annotations on `bex-worker-0`.

## Files

- `infra/clusterapi/overlays/*` (MachineDeployment `bex-worker-0` annotations)

## Acceptance criteria

- [x] The `bex-worker-0` MachineDeployment carries the autoscaler min/max annotations and the autoscaler respects them. _Met 2026-07-09:_ CAPH min 0 / max 3 + cx33 capacity hints (4 CPU/8G, scale-from-zero); local topology min 1 / max 5 — propagation to the generated MachineDeployment verified live; scale-up honored max, scale-down stopped at the min floor. `replicas` removed in both overlays (webhook seeds min-size; re-applies never stomp the autoscaler).

## Out of scope

- Autoscaler install (t003); end-to-end verify (t005).
