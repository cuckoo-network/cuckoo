---
id: w1/m45/t003
title: "`/u/$` alias: Render's canonical account-settings shape"
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m45/t001]
---

## Objective

Accept `/u/{any-id}/settings` (Render's canonical account-settings URL — live-verified: their `/settings` redirects to `/u/{usr-id}/settings`) as a redirect to bex's `/settings`. The page is caller-scoped regardless of the id in the URL, so no id validation is needed — exactly the m39 "never validate segment ↔ id" rule.

## Context

- Alias precedent: `web.$.tsx` + `redirectRenderAlias` (`src/common/lib/render-alias.ts`).
- bex's own account settings live at `/settings` (Kratos settings + panels), one long page like Render's.

## Steps

1. Add `u.$.tsx`: any splat ending in `/settings` (and, per the m39 fallback posture, any `/u/{id}` at all) → `/settings`, preserving query/hash.

## Files

- `dashboard/src/routes/u.$.tsx` (new)

## Acceptance criteria

- [ ] `/u/usr-anything/settings` and `/u/own-anything/settings` land on account settings.

## Out of scope

- Emitting `/u/…` URLs anywhere (bex mints no user dashboardUrl).
