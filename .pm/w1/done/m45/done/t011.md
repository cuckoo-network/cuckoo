---
id: w1/m45/t011
title: "Global sidebar parity: Render's nav groups + entries"
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m45/t001, w1/m45/t005]
---

## Objective

Match Render's global left-sidebar structure for every entry bex supports. Live capture (2026-07-16, identical on `/`, workspace settings, and account settings): ungrouped **Projects** (`/`), **Blueprints**, **Environment Groups**; group **Integrations** — Observability, **Webhooks**, **Notifications**; group **Networking** — Private Links, Dedicated IPs; group **Workspace** — **Billing**, **Settings**; footer — Changelog, Invite a friend, Contact support, Render Status. bex today: one flat "Dashboard" group (Projects, Blueprints, Env Groups, Usage, Settings).

## Context

- `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx` — `NAV_ITEMS` + a single `SidebarGroup`; the shadcn sidebar kit already supports multiple labeled groups (`SidebarGroupLabel`).
- Mapping: keep the three ungrouped entries; add **Integrations** with Webhooks + Notifications (the pages t005 creates — this task wires the nav); add **Workspace** with Usage (bex's deliberate Billing counterpart, ADR023) + Settings (`/workspace/settings`). **Omit** the Networking group entirely (both entries are DO_NOT_DO non-goals) and Observability (drains non-goal) — an empty group is worse than none.
- Footer: out of scope (Changelog/Invite/Status have no bex counterparts; not navigation to capabilities).
- i18n: group labels through `useTranslations()` (`common.*` keys, en + zh).

## Steps

1. Restructure `NAV_ITEMS` into Render's grouping (ungrouped / Integrations / Workspace) with the mapping above.
2. Record the captured sidebar structure + bex's disposition per entry in `docs/render-artifacts/dashboard-routes.md` (or a sidebar section of the walk doc — wherever t001 put the scheme).
3. Update sidebar tests / `nav-active` behavior for the new groups.

## Files

- `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx`
- `dashboard/src/common/locales/{en,zh}.ts`
- `docs/render-artifacts/dashboard-routes.md`

## Acceptance criteria

- [ ] Sidebar shows Projects/Blueprints/Env Groups ungrouped, Integrations (Webhooks, Notifications), Workspace (Usage, Settings) — visually grouped like Render's.
- [ ] Every omitted Render entry is recorded with its non-goal rationale, not silently dropped.

## Out of scope

- Footer links, the Upgrade button, search/⌘K parity.
