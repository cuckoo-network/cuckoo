---
id: w1/m45/t007
title: "Render parity check (routes + `dashboardUrl` metadata shapes)"
worker: worker1
status: done
estimate: 30m
depends_on:
  [
    w1/m45/t002,
    w1/m45/t003,
    w1/m45/t004,
    w1/m45/t005,
    w1/m45/t006,
    w1/m45/t011,
    w1/m45/t012,
  ]
---

## Objective

Standing closing task: verify the shipped routes against the live Render shapes recorded in t001's artifact, and check bex-api's emitted `dashboardUrl` metadata (service/Postgres/Key Value — w5/m39's type-aware shapes) still matches; assess whether any workspace-shaped `dashboardUrl` (owners/workspace surfaces) exists in Render's REST responses to mirror — flag drift as follow-up, never silently diverge.

## Context

- The alias layer is dashboard-only; bex-api's REST/GraphQL/MCP are untouched except the `dashboardUrl` audit. Evidence: `docs/render-artifacts/dashboard-routes.md` (post-t001), `docs/render-artifacts/owners-api.md`.
- Walk every URL from the artifact's new section on a dev-N stack; update `docs/ADR018-render-parity.md` if any row's evidence changes.

## Steps

1. Paste-walk each captured shape; record pass/fail against the artifact.
2. Grep Render's captured owners/workspace payloads for a `dashboardUrl`; mirror or record its absence.
3. Sweep ADR018 + the artifact dispositions; file follow-up notes for drift.

## Files

- `docs/render-artifacts/dashboard-routes.md`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Every artifact shape has a recorded live-walk result; drift filed, not ignored.

## Out of scope

- Fixing drift beyond this milestone's routes.
