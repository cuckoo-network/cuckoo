---
id: w1/m45/t005
title: "`/webhooks` + `/notifications` as first-class pages"
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m45/t001]
---

## Objective

Render's sidebar treats Webhooks and Notifications as first-class pages (`/webhooks`, `/notifications` — live-verified); bex has both capabilities (outbound event webhooks w3/m11, notification settings w3/m9) but only as panels stacked on account `/settings`. Give each its own route hosting the existing panel — the same placement-parity move m44 made for Team.

## Context

- Panels: `features/webhooks/components/webhooks-panel.tsx`, `features/notifications/components/notification-settings-panel.tsx`, both mounted in `features/auth/pages/settings-page/index.tsx`.
- Decide move-vs-both: Render's IA has them OFF the settings page entirely — a straight move matches (remove from `/settings`, like m44's Team move); check nothing else deep-links to the panels on `/settings` first.
- Note the scope boundary: bex's `/webhooks` page is the outbound-webhook management panel; Render's page also carries integration marketing — panel content parity is NOT this task, only the route/placement.

## Steps

1. `webhooks.tsx` + `notifications.tsx` routes (DashboardLayout + the existing panel, `p-4 sm:p-6` content padding per dashboard/CLAUDE.md rhythm).
2. Remove both panels from `/settings`; sweep tests asserting the old mount.
3. Sidebar: decide whether they join the nav (Render groups them under an "Integrations" heading) — smallest honest change wins; note the choice.

## Files

- `dashboard/src/routes/webhooks.tsx`, `dashboard/src/routes/notifications.tsx` (new)
- `dashboard/src/features/auth/pages/settings-page/index.tsx`
- `dashboard/src/common/components/dashboard-layout/dashboard-sidebar.tsx` (if nav entries are added)

## Acceptance criteria

- [ ] `/webhooks` and `/notifications` render their capability panels inside the authenticated chrome; `/settings` no longer duplicates them.
- [ ] `yarn typecheck && yarn lint && yarn test` green.

## Out of scope

- Panel content changes; `/observability` (drains are a non-goal).
