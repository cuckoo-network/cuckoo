---
id: w1/m45/t008
title: Simplify pass over the milestone's changes
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m45/t007]
---

## Objective

Standing closing task: run `/simplify` over the code this milestone changed — reuse / simplification / efficiency, behavior-preserving. Likely candidates: the alias route files converging on one helper (`render-alias.ts` grew several shapes), and the URL-owned dialog params on the overview.

## Context

- Changed surface: `dashboard/src/routes/{w.$,u.$,billing.$,new.*,login,register,webhooks,notifications}.tsx`, `dashboard/src/common/lib/render-alias.ts`, workspaces context, overview route.

## Steps

1. `/simplify` over the diff; apply behavior-preserving findings; file out-of-scope residuals as `w1/NNN` inbox notes.

## Files

- as above

## Acceptance criteria

- [ ] Pass run; findings applied or filed; suites green.

## Out of scope

- New behavior.
