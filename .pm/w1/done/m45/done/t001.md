---
id: w1/m45/t001
title: Extend the captured route artifact with the workspace/user/create scheme
worker: worker1
status: done
estimate: 20m
depends_on: []
---

## Objective

Record the 2026-07-16 live probes in `docs/render-artifacts/dashboard-routes.md` so the alias work has its design source in the artifact, not a chat transcript — the same evidence-first rule w5/m39 followed.

## Context

Live-verified findings to record (authenticated `dashboard.render.com`, 2026-07-16):

- `/w/settings` (id-less) canonicalizes to `/w/{tea-id}/settings` — Render resolves the caller's current workspace into the URL. Sibling `/w/{tea-id}/billing`.
- `/settings` canonicalizes to `/u/{usr-id}/settings` — account settings are user-scoped; the page is one long page (Profile / Appearance / Account Security / CLI Tokens / API Keys / SSH Public Keys / PR Requests), no sub-URLs.
- New-menu inventory: services create under the type segment (`/web/new`, `/static/new`, `/pserv/new`, `/cron/new`, `/worker/new` — all already redirect onto `/services/new` via the existing aliases); datastores/projects under `/new/` (`/new/database`, `/new/redis`, `/new/project`); `/workflow/new` (non-goal).
- Breakage: `/d/new` exists on Render (New menu) but bex's `d.$` alias redirects it to nonexistent `/databases/new` → 404 catch-all.
- `/billing/update-plan` (plan-change page, seen throughout the workspace-settings capture).

## Steps

1. Add a "Workspace / user / create scheme (2026-07-16 probes)" section to the artifact with the table above + evidence notes.
2. Note the `/d/new` alias breakage and the disposition each m45 task assigns.
3. `npx prettier@3.4.2 --write docs/render-artifacts/dashboard-routes.md`.

## Files

- `docs/render-artifacts/dashboard-routes.md`

## Acceptance criteria

- [ ] Artifact records every URL shape named above with its evidence and bex disposition.

## Out of scope

- Any route implementation (t002–t006).
