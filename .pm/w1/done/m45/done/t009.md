---
id: w1/m45/t009
title: Test coverage for the shipped behavior
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m45/t007]
---

## Objective

Standing closing task: meaningful tests for the routes this milestone shipped — real behavior and failure modes, no snapshot-everything.

## Context

- Highest-value cases: `/w/{foreign-tea-id}/settings` shows the denied/not-found state and never the caller's own workspace (the confused-deputy case); `/w/{member-tea-id}/settings` switches the context (assert the switcher/page workspace); query/hash preservation through each alias (`/register?invite=tok` reaching the stash); `/d/new` reaching the database create dialog; the m39 alias walk still green (existing `render-alias` tests — extend, don't duplicate).
- Vitest, `__tests__` adjacent to routes/lib per dashboard/CLAUDE.md.

## Steps

1. Extend the existing render-alias tests with the new shapes; add route tests for the context-switching and dialog-param landings.

## Files

- `dashboard/src/common/lib/__tests__/`, `dashboard/src/routes/__tests__/`

## Acceptance criteria

- [ ] `yarn test` green with the new cases; the foreign-workspace refusal is asserted.

## Out of scope

- E2E browser automation (the closeout walk covers it manually).
