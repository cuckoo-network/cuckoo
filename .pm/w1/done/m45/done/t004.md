---
id: w1/m45/t004
title: "Create-flow aliases: fix `/d/new`, add `/new/database` `/new/redis` `/new/project`"
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m45/t001]
---

## Objective

Every create URL Render's New menu mints for a bex-supported resource reaches a working create surface. `/d/new` is **broken today**: the `d.$` alias redirects it to `/databases/new`, which doesn't exist (404 catch-all). `/new/database`, `/new/redis`, `/new/project` all 404.

## Context

- bex's create surfaces: services `/services/new` (the five service segments' `/{seg}/new` already land there via the m39 aliases — verify and keep); Key Value `/keyvalue/new` (a real route — `/r/new` already lands ✓); **databases and projects are dialogs** (`features/databases/components/create-database-dialog.tsx`, `features/projects/hooks/use-create-project.ts`) with no routes.
- Landing options for dialog-backed creates: URL-owned dialog state on the overview (`/?new=database`, the `?plan=change` pattern — workspace.settings.tsx:13 documents the idiom) or thin dedicated routes. Prefer the URL-owned dialog param: it reuses the existing dialogs and stays honest about where creation lives.
- `/new/redis` → `/keyvalue/new` is a plain redirect.

## Steps

1. `new.database.tsx` + `new.project.tsx` (or a `new.$` splat beside the existing `new.workspace.tsx`) → `/?new=database` / `/?new=project`; wire the overview to open the matching dialog from the search param (validateSearch, `replace: true` history like the plan dialog).
2. `new.redis.tsx` → `/keyvalue/new`.
3. Fix `/d/new`: either a `/databases/new` route that opens the create dialog (then the existing alias just works) or special-case `new` in the `d.$` alias → the database create landing. Pick whichever keeps `redirectRenderAlias` uniform.
4. Verify all five `/{seg}/new` service shapes still land on `/services/new`.

## Files

- `dashboard/src/routes/new.database.tsx`, `new.redis.tsx`, `new.project.tsx` (new), `dashboard/src/routes/index.tsx` (dialog search params)
- `dashboard/src/common/lib/render-alias.ts` / `dashboard/src/routes/d.$.tsx` (the `/d/new` fix)

## Acceptance criteria

- [ ] `/d/new` and `/new/database` open the database create flow; `/new/redis` lands on `/keyvalue/new`; `/new/project` opens the project create flow — no 404s.
- [ ] `/web/new` (and the four sibling segments) still land on `/services/new`.

## Out of scope

- Type preselection on `/services/new` from the inbound segment (nice-to-have; file a note if wanted).
- `/workflow/new` (non-goal).
