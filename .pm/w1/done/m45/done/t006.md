---
id: w1/m45/t006
title: "Auth aliases: `/login`, `/register`"
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m45/t001]
---

## Objective

Render's auth pages live at `/login` and `/register` (habit + docs links); bex's live at `/auth/login` and `/auth/sign-up`. Add thin redirects so pasted links land.

## Context

- Pure `beforeLoad` redirects, the `web.$.tsx` shape; preserve query/hash (an `?invite=` token arriving on `/register` must survive to the sign-up page's stash — the m44 invite flow).

## Steps

1. `login.tsx` → `/auth/login`; `register.tsx` → `/auth/sign-up`, both preserving search/hash.

## Files

- `dashboard/src/routes/login.tsx`, `dashboard/src/routes/register.tsx` (new)

## Acceptance criteria

- [ ] `/login` and `/register?invite=tok` land on the auth pages with params intact.

## Out of scope

- `/verify-otp`, `/sso` (Kratos owns bex's flows; no counterpart shapes).
