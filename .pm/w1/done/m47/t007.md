---
id: w1/m47/t007
title: Simplify - reuse patterns, clean up dead code
worker: worker1
status: todo
estimate: 1h
depends_on: [w1/m47/t006]
---

## Objective

Run `/simplify` over the code this milestone changed. Remove any dead code from the old separate Deploys page, deduplicate component patterns, and clean up any temporary workarounds.

## Context

This milestone consolidates two pages into one, so there's likely:
- Dead/unused components from the old `DeploysListPage` (if we didn't fully reuse them)
- Duplicate event-type constants or helpers
- Temporary query aliases or fallback logic
- Unreachable routes

## Steps

1. Run `/simplify` over:
   - `lego/backend/internal/events/` (if any dead codepaths were added)
   - `dashboard/src/features/events/` and `dashboard/src/features/deploys/` (check for overlap)
   - `dashboard/src/routes/services.$serviceId.events*.tsx`
2. Delete the old `DeploysListPage` if its functionality is fully merged
3. Check for duplicate deploy-status or event-type mappings — consolidate into one module
4. Remove any temporary feature flags or fallback logic
5. Consolidate GraphQL queries — ensure one shared `serviceEvents` query, not a separate `deploys` query still being used somewhere

## Files

- Varies based on `/simplify` findings (typically the above paths)

## Acceptance criteria

- [ ] `/simplify` runs without major warnings
- [ ] No dead imports in events or deploys features
- [ ] DeploysListPage and related components deleted (not just unused)
- [ ] Shared event/deploy helpers consolidated into one location
- [ ] No old `/deploys` routes remain except the optional redirect
- [ ] Dashboard tests pass (no import errors from deleted components)

## Out of scope

- Refactoring unrelated code
- Performance optimization beyond cleanup

