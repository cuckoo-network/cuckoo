---
id: w1/m82/t006
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m82/t005]
---

## Objective

Close the milestone once its definition of done is actually met.

## Context

- Only run this when t001–t005 are complete **and** the DoD in the milestone
  README holds: inviting an existing member is refused with "already a member"
  and never changes their role (across REST/GraphQL/MCP + UI), the redeem path
  can't overwrite an existing member, and the dialog validates the email +
  surfaces the specific reason — all with passing regression tests proven red on
  the pre-fix code.

## Steps

1. Verify the DoD is real — re-check that inviting an existing member is refused
   and leaves the role unchanged (ideally a live check plus the tests).
2. Set every remaining task `status: done`, move each `tNNN.md` to
   `w1/m82/done/`, mark every row `— **DONE**` and set `**Status:** done` in
   `w1/m82/README.md`.
3. Move the whole `w1/m82/` directory to `w1/done/m82/` and check `- [x]` for
   m82 in `w1/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m82/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] DoD verified real; board synced across task frontmatter, milestone README,
      and workstream checkbox; milestone moved to `w1/done/m82/`.

## Out of scope

- Any new implementation.
