---
id: w1/m82/t003
title: "Render parity: invite verb + error shape across REST/GraphQL/MCP/UI"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m82/t001, w1/m82/t002]
---

## Objective

Confirm the "already a member" refusal and the invite behavior are consistent
across every surface the invite verb exposes, and match Render.

## Context

- The invite verb is on REST, GraphQL (`inviteWorkspaceMember`), and MCP, plus
  the dashboard Team page. The new coded refusal (t001) must render identically
  on all three API surfaces and be surfaced by the dashboard (t002).
- Render rejects re-inviting an existing member with an "already a member"
  error; compare bex's coded error + status against Render's shape.

## Steps

1. Verify the "already a member" refusal returns the same coded error/shape on
   REST, GraphQL, and MCP (status class, code, message), and that the dashboard
   shows it.
2. Compare against render.com's invite-existing-member behavior; record any
   residual divergence in `docs/ADR018-render-parity.md` / `docs/ADR024-members.md`
   rather than diverging silently.
3. Note any drift as a follow-up inbox note rather than expanding scope.

## Files

- `docs/ADR018-render-parity.md` / `docs/ADR024-members.md` (if a note is needed)
- follow-up inbox note(s) for any drift

## Acceptance criteria

- [ ] The refusal is confirmed identical across REST/GraphQL/MCP and surfaced in
      the UI, or drift is filed as a tracked follow-up.
- [ ] Members docs/parity ledger reflect the "already a member" semantics.

## Out of scope

- New members features beyond this fix.
