---
id: w1/m82/t004
title: Simplify the touched invite/redeem + dialog code
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m82/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification,
efficiency — behavior-preserving.

## Context

- Touched: `members.Invite` existing-member guard, `store.redeemInvite`/
  `AcceptInvitesForEmail`, any new coded error, and the dashboard invite hook +
  dialog + locales. Prefer reusing an existing email validator and the
  established error-surfacing pattern (`classifyAddError` / webhook form) over
  new bespoke code.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving cleanups; keep tests green.

## Files

- The files changed by t001–t002.

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly deferred with reason.
- [ ] No behavior change; backend + dashboard suites green.

## Out of scope

- New features or bug hunting (that's `/code-review`).
