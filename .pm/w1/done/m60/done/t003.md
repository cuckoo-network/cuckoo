---
id: w1/m60/t003
title: "Real-FGA E2E: extend the w7/m61 role-ladder matrix — billing allowed on billing verbs, denials unchanged"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m60/t001, w1/m60/t002]
---

## Objective

Prove the new gating against a real OpenFGA store: extend the w7/m61 role-ladder deny matrix and multiworkspace E2E so a billing-role member is **allowed** on exactly the billing verbs t002 assigned — and remains **denied** on every resource mutation and sensitive read, with the completeness guard still forcing new verbs to be classified.

## Context

- `lego/backend/internal/api/roleladder_test.go` — the m61 matrix: `roleGrants` pinned to `deploy/gitops/authz/model.fga` (parse+compare), completeness-guarded {viewer,contributor,billing} × sweepable-verb matrix, anti-tautology self-tests. Flip only the intended billing rows; the guard must fail if a billing verb is added later without classification.
- `lego/backend/internal/api/multiworkspace_e2e_test.go` — the real-OpenFGA E2E (`TestMultiWorkspaceTargetingE2E`) that validated billing-member denials live; add the positive case (billing member succeeds on Status/Checkout/Portal — Stripe-off mode returns the estimate-only/503 shape, which still proves authz passed vs 403).
- Integration tests gate on `BEX_TEST_DB_URI`/`BEX_TEST_OPENFGA_URL` and run for real in CI (`.github/workflows/backend-test.yml`).

## Steps

1. Update `roleGrants`/matrix expectations for the billing role per t002's verb→relation matrix.
2. Add the billing-member allowed-path E2E case against real OpenFGA; assert the failure is never 403 for assigned verbs.
3. Re-run the full matrix: assert zero unintended expectation changes (only the billing-verb rows moved).
4. Verify the anti-tautology/completeness guards still bite (mutate a guard input locally, watch it fail, revert).

## Files

- `lego/backend/internal/api/roleladder_test.go`
- `lego/backend/internal/api/multiworkspace_e2e_test.go`

## Acceptance criteria

- [ ] Billing-role member passes authz on every t002-assigned billing verb against a real OpenFGA store.
- [ ] Every other m61 matrix expectation is byte-identical to before this milestone.
- [ ] Completeness guard still fails on an unclassified new verb (spot-checked).
- [ ] `cd lego/backend && go test ./...` green locally with the integration env set.

## Out of scope

- Dashboard-side tests (t007); doc updates (t004).
