---
id: w1/m60/t001
title: Add core.RelCanManageBilling + flip billing.authorize() to it
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Give the modelled `can_manage_billing` FGA relation its Go consumer: add the `RelCanManageBilling` constant to `internal/core` and switch the billing feature's shared authorize helper from admin-only `RelCanManage` to it, so billing-role members (and admins, per the model's `billing or admin`) pass.

## Context

- `deploy/gitops/authz/model.fga` already defines `can_manage_billing: billing or admin` — no model or `model.json` change is needed; this is Go-side only.
- `lego/backend/internal/core/base.go:96` holds the relation constants (`RelCanManage = "can_manage"` etc.).
- `lego/backend/internal/core/audit.go:538` has the relation allowlist map that includes `RelCanManage`; the new relation must be added there too so billing verbs keep auditing.
- `lego/backend/internal/billing/service.go:176-185` — the shared `authorize()` used by `Status`/`Checkout`/`Portal` gates on `core.RelCanManage`; flip this one call site.
- The w7/m61 role-ladder matrix (`lego/backend/internal/api/roleladder_test.go`) pins expectations per verb — its billing rows flip from denied to allowed for the billing role in t003; if the suite cannot stay green between t001 and t003, land the minimal expectation flip here and extend coverage in t003.

## Steps

1. Add `RelCanManageBilling = "can_manage_billing"` beside the other `RelCan*` constants in `core/base.go`, with a comment naming the model relation.
2. Add the constant to the audit allowlist map in `core/audit.go`.
3. Change `billing.authorize()` to `s.Authorize(ctx, core.RelCanManageBilling)`.
4. Run `cd lego/backend && go test ./...`; minimally adjust any test that pinned admin-only billing gating (full matrix work is t003).

## Files

- `lego/backend/internal/core/base.go`
- `lego/backend/internal/core/audit.go`
- `lego/backend/internal/billing/service.go`
- (minimal expectation fixes) `lego/backend/internal/api/roleladder_test.go`

## Acceptance criteria

- [ ] `git grep RelCanManageBilling lego/backend` is non-empty and includes `billing/service.go`.
- [ ] No billing management verb still authorizes via `RelCanManage`.
- [ ] `cd lego/backend && go test ./...` green.

## Out of scope

- Per-verb role decisions for billing reads (t002) and the full matrix/E2E extension (t003).
