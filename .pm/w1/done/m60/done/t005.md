---
id: w1/m60/t005
title: "Render parity: billing-role behavior consistent on every surface; compare against render.com and flag drift"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m60/t004]
---

## Objective

Standing closing task: verify the billing-role change is consistent across every surface it exposes — REST, GraphQL, MCP (`lego/backend/internal/billing/`), and the dashboard — same semantics and error shapes, and compare against Render's actual BILLING-role behavior, flagging any drift as follow-up work rather than silently diverging.

## Context

- bex-api is Render-compatible by design (`docs/ADR006-bex-api.md`); the role ladder mirrors Render's five roles (`docs/ADR024-members.md`).
- Render's public role documentation is the reference; capture what it says at execution time (docs/render-artifacts/ is the precedent home for captured evidence).
- A denied billing verb must return the same error shape a viewer gets today (Render REST error dialect `{id,message}`) — check the deny path didn't change shape, only membership.

## Steps

1. Exercise a billing-role member on Status/Checkout/Portal via REST, GraphQL, and MCP — identical allow/deny outcomes and error shapes.
2. Walk the dashboard billing UI as a billing-role member (dev-1) — visibility matches the backend.
3. Compare the final reach against Render's documented BILLING role; file a note for any divergence kept deliberately.
4. Update `docs/ADR018-render-parity.md`'s members/roles row if its evidence text mentions the billing-role gap.

## Files

- `lego/backend/internal/billing/{rest,graphql,mcp}.go` (verification, not necessarily edits)
- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/` (evidence, if captured)

## Acceptance criteria

- [ ] All three API surfaces + dashboard agree on the billing role's reach and error shapes.
- [ ] Render comparison recorded; any drift filed as a note, not silently accepted.

## Out of scope

- New feature work discovered during comparison — file notes.
