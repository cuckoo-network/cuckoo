---
id: w1/m60/t002
title: "Role-matrix sweep: billing-verb gating vs Render's BILLING semantics across REST/GraphQL/MCP + dashboard"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m60/t001]
---

## Objective

Decide and implement, verb by verb, what a billing-role member may reach — matching Render's documented BILLING role (billing management + billing/invoice visibility, no resource control) — and make every surface route through the same core decision so REST, GraphQL, MCP, and the dashboard agree.

## Context

- Billing surfaces: `lego/backend/internal/billing/{rest,graphql,mcp}.go` all call into `Service`; the management verbs (`Status`/`Checkout`/`Portal`) now gate on `RelCanManageBilling` (t001).
- `lego/backend/internal/billing/read.go` and the invoice/cost reads: check what relation they gate on today (the w7/m61 sweep found billing members denied "sensitive reads" — decide whether invoice/billing-status reads should open to the billing role, per Render's behavior).
- Usage reads (`internal/usage`) have their own gating — decide whether they're in scope for the billing role or stay as-is; document the choice either way.
- Dashboard: find where billing/usage settings UI is gated (admin-only nav or capability checks) and align it — a billing member should see and use the billing pages, not a 403 dead end.
- Render reference: render.com's role documentation (Billing role manages billing/payment, views invoices; cannot manage resources). Capture what the docs actually say at execution time — do not rely on memory.

## Steps

1. Inventory every billing-feature verb across `rest.go`/`graphql.go`/`mcp.go` and its authz relation.
2. For each, assign the relation per Render's BILLING semantics: management → `RelCanManageBilling`; reads → decide (open to billing role or not) and record the rationale.
3. Sweep the dashboard's billing/usage pages for role gating and align visibility with the backend decision.
4. Record the final verb→relation matrix in the task (or milestone README) for t003 to pin.

## Files

- `lego/backend/internal/billing/rest.go`
- `lego/backend/internal/billing/graphql.go`
- `lego/backend/internal/billing/mcp.go`
- `lego/backend/internal/billing/read.go`
- `dashboard/src/` billing/usage settings components (locate exact files during the sweep)

## Acceptance criteria

- [ ] A written verb→relation matrix exists covering every billing-feature verb on all three API surfaces.
- [ ] No billing verb's gating differs between REST, GraphQL, and MCP.
- [ ] The dashboard shows billing-role members the billing UI the backend now permits (no admin-only hiding of verbs they may call).
- [ ] `cd lego/backend && go test ./...` and the dashboard suite (`yarn test`, typecheck, lint) green.

## Out of scope

- The real-FGA E2E proof (t003); render.com side-by-side drift capture (t005).
