---
id: w1/m60/t007
title: "Test coverage: unit-level authz + dashboard gating tests beyond the t003 E2E"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m60/t005]
---

## Objective

Standing closing task: add meaningful tests for the behavior this milestone shipped beyond t003's real-FGA E2E — unit-level pins that the billing verbs authorize via `RelCanManageBilling` (so a future refactor back to `RelCanManage` fails a test), the audit allowlist includes the relation, and the dashboard's billing-role visibility is component-tested. Tests must assert real behavior and failure modes — no tautologies.

## Context

- `lego/backend/internal/billing/service_test.go` — existing unit harness for the billing verbs.
- `lego/backend/internal/core/audit_test.go` — allowlist coverage precedent.
- Dashboard component tests colocated with the billing/usage settings components t002 touched.

## Steps

1. Unit-pin each billing verb's relation (fake authorizer recording the relation asked for).
2. Pin the audit allowlist entry for `RelCanManageBilling`.
3. Component-test the dashboard gating states (billing role sees billing UI; viewer doesn't).
4. Full suites: `cd lego/backend && go test ./...`, dashboard `yarn test` + typecheck + lint.

## Files

- `lego/backend/internal/billing/service_test.go`
- `lego/backend/internal/core/audit_test.go`
- dashboard component test files (per t002's touched components)

## Acceptance criteria

- [ ] Reverting t001's relation flip makes at least one unit test fail.
- [ ] Dashboard gating states covered by component tests.
- [ ] All suites green.

## Out of scope

- Re-testing what t003 already proves against real OpenFGA.
