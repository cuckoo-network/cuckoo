# Terraform: `ignore_changes = [labels]` on the Traefik LB (CCM label drift fight)

`infra/terraform/main.tf:135-143` (`hcloud_load_balancer.traefik`, w1/m41) declares
no `lifecycle { ignore_changes = [labels] }`, while its own comment says hcloud
CCM owns the `hcloud-ccm/service-uid` label on the same object — the next
`terraform apply` will try to strip the CCM's label, exactly the drift fight the
block was written to avoid. Mitigated (CCM v1.33.0 falls back to name-adoption)
but wrong. `prevent_destroy`/`delete_protection` are correctly present.

Sub-hour: add `ignore_changes = [labels]` to the existing `lifecycle` block and
verify with a plan diff that the label survives.

Source: `/pm-brainstorm` round 19, 2026-07-15 — shipped-diff mine over
`436fd9c2..bb9cc60b` (F2, commit 4dd753af).

---

**Absorbed into `w10/m6` (t001) 2026-07-15** (`/pm-brainstorm` round 21, cross-stream burn-down); claim re-verified at HEAD before absorption (`main.tf` lifecycle still `prevent_destroy`-only).
