---
id: w1/m66/t007
title: Render parity
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m66/t001, w1/m66/t003]
---

## Objective

Check that this milestone's user-facing changes are consistent across every surface they touch and against render.com's equivalent behavior, flagging drift as follow-up rather than diverging silently.

## Context

- t001 changes secret-file storage keying, which is observable through REST (`/v1/services/{id}/secret-files`), GraphQL, MCP, and the dashboard's env/secret-files UI.
- t003 changes an OAuth consent refusal observable to MCP/agent clients and the official Render CLI device flow.
- `docs/ADR006-bex-api.md` (one core, thin adapters), `docs/ADR018-render-parity.md` (the ledger), `dashboard/CLAUDE.md`.

## Steps

1. Exercise secret-file create/read/list/delete by **both** `srv-…` id and service name across REST, GraphQL, and MCP; confirm identical fields, semantics, and error shapes.
2. Confirm the dashboard's secret-files surface is unaffected (or fixed) by the canonicalization.
3. Confirm the consent gate's new refusals do not affect the Render CLI device login, and that a legitimate S256 client is unchanged.
4. Update `docs/ADR018-render-parity.md` only if a cell's evidence actually changes; otherwise record "no drift" in the closeout.

## Files

- `docs/ADR018-render-parity.md` (only if a row changes)

## Acceptance criteria

- [ ] Secret-file behavior verified identical on REST, GraphQL, and MCP for both addressing forms.
- [ ] Device-flow login unaffected by the PKCE tightening.
- [ ] Any drift found is filed as a note, not silently accepted.

## Out of scope

- New parity features; this is a consistency check on what m66 changed.
