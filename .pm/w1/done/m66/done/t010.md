---
id: w1/m66/t010
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m66/t009]
---

## Objective

Close the milestone once its definition of done actually holds: all six findings fixed at root cause, regression-tested, suites green.

## Context

- `.pm/CLAUDE.md` — done items move to `done/` folders; nothing left behind.
- Activation caveat to record: t005's host-key pin is inert until the operator provisions the `BEX_SSH_KNOWN_HOSTS` secret — the closeout note must say so plainly rather than implying the control is live.

## Steps

1. Verify the DoD line by line against real evidence (test output, rendered manifests, `packer init` idempotence).
2. Set every remaining task `status: done`, move each `tNNN.md` to `.pm/w1/m66/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `.pm/w1/m66/` to `.pm/w1/done/m66/` and check `- [x]` in `.pm/w1/README.md` with a resolution summary.
4. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m66/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] DoD verified with evidence, not assumed.
- [ ] Board synced in all three places; milestone directory moved whole, no tombstone.
- [ ] Operator activation step (SSH known-hosts secret) recorded explicitly.

## Out of scope

- Committing/pushing (`/ship` only, per repo rule).
