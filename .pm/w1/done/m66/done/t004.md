---
id: w1/m66/t004
title: Pin + lock the Packer hcloud plugin (F12)
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Close Finding 12 (unverified-code, CWE-494): `infra/packer/bex-worker.pkr.hcl` accepts any hcloud plugin `>= 1.6.0`, no `.packer.lock.hcl` is committed (verified — `infra/packer/` holds only `README.md` and the template), and `.github/workflows/snapshot.yml` runs `packer init` immediately before `packer build` in a job that carries `HCLOUD_TOKEN`. Identical repository revisions can therefore resolve different plugin bytes into a credentialed job.

## Context

- `infra/packer/bex-worker.pkr.hcl:27-40` — `required_plugins { hcloud = { source = "github.com/hetznercloud/hcloud", version = ">= 1.6.0" } }`.
- `.github/workflows/snapshot.yml:35-75` — `packer init` → `packer build` with the cloud token in scope.
- `packer` is available locally (`/opt/homebrew/bin/packer`), so the lock file can be generated for real rather than hand-authored.
- Report: report.md Finding 12.

## Steps

1. Pin the plugin to an exact reviewed version (`version = "= <x.y.z>"`), choosing the current release.
2. Run `packer init` locally in `infra/packer/` to generate `.packer.lock.hcl` with platform checksums; commit it (ensure `.gitignore` does not exclude it).
3. Add a CI drift guard to `snapshot.yml`: after `packer init`, assert the lock file is unchanged (`git diff --exit-code infra/packer/.packer.lock.hcl`) so a resolution change fails the build instead of silently shipping.
4. Note the linux/amd64 (CI) and darwin/arm64 (local) platform hashes both being present, so neither environment re-resolves.

## Files

- `infra/packer/bex-worker.pkr.hcl`
- `infra/packer/.packer.lock.hcl` (new)
- `.github/workflows/snapshot.yml`

## Acceptance criteria

- [ ] Exact version constraint, committed lock file with checksums for the CI and local platforms.
- [ ] `packer init` in a clean checkout leaves the lock byte-identical; the CI step fails if it does not.
- [ ] `packer validate` (or `packer init && packer validate`) still succeeds on the template.

## Out of scope

- Digest-pinning the `packer` binary itself (already version-pinned in the workflow).
