---
id: w1/m66/t009
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m66/t007]
---

## Objective

Ensure every finding closed by this milestone has a regression test that asserts real behavior and **fails on the pre-fix code** — the standard this milestone's lineage (`w1/m53`, `w1/m65`) set.

## Context

- Backend: `cd lego/backend && go test ./...` (CI-enforced).
- Dashboard: `cd dashboard && yarn test`.
- Charts/scripts: render assertions and shell negative tests where a Go test is not the right tool.

## Steps

1. Confirm each of t001–t006 landed its own failing-before/passing-after test; add the missing ones.
2. Add the cross-cutting sweep tests that prevent the class from returning: no post-authorization raw-token storage key in `internal/secrets`; no dial-time-guarded transport inheriting env proxy; every rendered backup image digest-pinned.
3. Run the full suites and record results in the milestone README.

## Files

- `lego/backend/internal/secrets/*_test.go`
- `lego/backend/internal/webhooks/*_test.go`
- `dashboard/src/common/server-fn/*.test.ts`
- chart/script test harness as appropriate

## Acceptance criteria

- [ ] Each closed finding has a test that fails when its fix is reverted (spot-check by reverting, not by assertion alone).
- [ ] Backend + dashboard suites green; `make lint` green over both Go modules.
- [ ] No tautological or snapshot-everything tests added.

## Out of scope

- Coverage-percentage targets.
