---
id: w1/m66/t008
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m66/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- Touched: `lego/backend/internal/secrets/`, `lego/backend/internal/webhooks/`, `dashboard/src/common/server-fn/hydra-consent.ts`, `infra/packer/`, `scripts/fetch-app-kubeconfig.sh` + `scripts/verify-substrate.sh`, `deploy/gitops/charts/*-backup/`.
- Watch for the shared shapes this milestone creates: one canonical storage-key assignment, one PKCE policy parser used by both consent paths, one known-hosts branch used by both SSH scripts.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving cleanups; file anything larger as a note.

## Files

- as touched by t001–t006

## Acceptance criteria

- [ ] Duplicated logic across the two consent call sites and the two SSH scripts is factored, not copy-pasted.
- [ ] All suites still green after the pass.

## Out of scope

- Bug hunting (that is `/code-review`) and behavior changes.
