---
id: w1/m31/t003
title: GraphQL — projects CRUD + service assignment
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m31/t002]
---

## Objective

Mirror the REST surface from t002 on GraphQL.

## Context

- Follow `docs/ADR006-bex-api.md`'s "one Core, three adapters" pattern.

## Steps

1. `projects`/`project(id)` queries, `createProject`/`deleteProject` mutations.
2. `assignServiceToProject`/`unassignServiceFromProject` mutations (or fold into an existing `updateService`-style mutation, whichever fits the codebase's conventions).
3. Regenerate GraphQL codegen artifacts.

## Files

- `lego/backend/internal/projects/graphql.go`
- dashboard codegen outputs

## Acceptance criteria

- [ ] Same authz scoping as REST; codegen committed with no drift.

## Out of scope

- MCP — t004.
