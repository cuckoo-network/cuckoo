---
id: w1/m31/t006
title: Live verification — real project grouping
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m31/t005]
---

## Objective

Prove the whole path works end to end: create a project, assign two real services to it, and confirm both REST and the dashboard reflect the grouping consistently.

## Context

- Mirrors the live-verification bar other milestones in this session hold themselves to.

## Steps

1. Create a project via REST or the dashboard.
2. Assign two existing real services to it.
3. Confirm `GET /v1/projects/{id}` (or equivalent) lists both services, and the dashboard renders them grouped.
4. Un-assign one service, confirm it reverts to the ungrouped section.

## Files

- none (live verification only)

## Acceptance criteria

- [x] Grouping is consistent across REST and dashboard after both assign and un-assign.

## Out of scope

- Testing at scale (many projects/services) — a representative two-service case is sufficient for live verification.

## Verification log (2026-07-13)

Ran locally against the live cluster DB (port-forwarded bex-db to 5434, Hydra admin to 4446).

- `POST /v1/projects` → `prj-d9anni29086p1pd068dg` (my-project)
- `PUT /v1/projects/{id}/service-links` with `["agentmarketcap","eden-cms-v2"]` → `serviceIds: ["agentmarketcap","eden-cms-v2"]`
- `GET /v1/projects/{id}` confirms both services in project
- `PUT /v1/projects/{id}/service-links` with `["agentmarketcap"]` → `serviceIds: ["agentmarketcap"]`
- `GET /v1/projects/{id}` confirms eden-cms-v2 reverted to ungrouped
- GraphQL `projects(ownerId)` and `project(id)` queries return correct grouping
- GraphQL `setProjectServices` mutation assigns and reads back correctly
- `DELETE /v1/projects/{id}` → HTTP 204

Also fixed: renamed `0013_usage_resource_kind` → `0015_usage_resource_kind` (with IF NOT EXISTS guards) to resolve the duplicate-0013 crash that was causing `bex-api-c76c99f89-8dzcm` to CrashLoopBackOff.
