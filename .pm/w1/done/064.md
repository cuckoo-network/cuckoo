> **Closed 2026-08-19 (w1 triage).** Deferred — not a live hazard. High-priority items from the 2026-08-19 architecture review already shipped as `w1/m76`–`m80`. Trigger-gated in `.pm/FUTURE-MAYBE.md` § "Architecture-review leftovers (2026-08-19)". Original text below kept for provenance.

# core.CursorList: one Render list envelope + settle the requested policy

From the 2026-08-19 refactor review (§2.5). The Render list envelope (`[{<key>: item, cursor}]`) is re-implemented 21× (`…WithCursor` structs) + 13 `to…List` loops across deploys/secrets/projects/workspaces/envgroups/postgres/keyvalue/audit/jobs/events/webhooks/environments/apps — and "GET with no cursor/limit" has drifted into **three** wire behaviors: always-sorted-capped-20 (projects, via `StablePage(requested: true)`), complete unsorted list (postgres/keyvalue, via `q.Has()` sniffing), capped-but-unsorted (environments/workspaces, via `core.Page`). `core/pagination.go:255-258` documents this exact bug class being fixed once for limit-clamping; the `requested` predicate is the same bug, unfixed.

Shape: `core.CursorList[T, V](items, q, key, cursorOf, view)` owning parse → one `requested` decision → `StablePage` → wrap (dynamic JSON key via a `Cursored[V]` MarshalJSON or `[]map[string]any`). ~−150 lines. **Carries a deliberate, stated behavior change** — pick the surviving no-params policy against Render's actual behavior (check render.com API + the CLI checklist) and let `render_openapi_test.go`/conformance surface every difference. Render parity task mandatory when promoted. LOW-MED risk.
