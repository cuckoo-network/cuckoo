---
id: w1/m69/t005
title: "Closeout"
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m69/t004]
---

## Objective

Close the milestone once the definition of done actually holds: sync creates are tenant-attributed end to end, matching is scoped, unresolvable tenant refuses, tests red-on-pre-fix and green now, backend suite green.

## Context

- Standing closing task (see `.claude/skills/pm/SKILL.md`): set remaining tasks `status: done`, move `tNNN.md` files to `wN/mN/done/`, mark rows `— **DONE**`, set `**Status:** done`, move the milestone to `wN/done/mN/`, and check the workstream README box.
- Work stays **uncommitted** until the user runs `/ship`.

## Steps

1. Verify the DoD end to end (build + tests green, acceptance boxes checked).
2. Perform the moves/status sync per `.pm/CLAUDE.md`.
3. Run `npx prettier@3.4.2 --write "**/*.md"` over touched markdown.

## Files

- `.pm/w1/m69/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] All tasks done and moved; milestone lives in `.pm/w1/done/m69/`; workstream README box checked
- [ ] No tombstones/stubs left at the old path

## Out of scope

- Committing/pushing (`/ship` only)
