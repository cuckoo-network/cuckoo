---
id: w1/m69/t004
title: "Test coverage — auto-sync attribution + scoped matching + fail-closed refusal"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m69/t002]
---

## Objective

Pin every seam the w9/001 regression test missed: assert CR labels, target namespace, store-row identity, scoped display-name matching, workspace-correct registry-credential resolution, and the fail-closed refusal — with tests that fail on the pre-fix code.

## Context

- `TestBlueprintAutoSyncPreservesTenantContext` (`lego/backend/internal/apps/blueprint_test.go:1383-1426`) asserts only `resolveTenantID` (store rows), never CR labels/namespace — that gap let the deploy half stay broken after w9/001.
- Round-15 verification evidence: labels/namespace decided in `service.go:1671-1687` + `provisionAppIdentity`; scoping decided by `Tenant(ctx)` at `deploy.go:918`, `deploy.go:2715-2716`, `deploy.go:2780-2781`.

## Steps

1. Extend/replace the auto-sync tenant-context test to assert the created App/Database/KeyValue CRs carry `bex.co/tenant` and land in the tenant namespace (not the shared one).
2. Add a scoped-matching regression: a same-display-name foreign/unlabeled CR in the shared namespace is not matched/patched by the sync.
3. Add a fail-closed regression: unresolvable tenant on the sync path refuses the apply (no CR written).
4. Prove each new test red on the pre-fix code (temporarily revert the binding) before finalizing.

## Files

- `lego/backend/internal/apps/blueprint_test.go`
- adjacent `_test.go` files in `lego/backend/internal/apps/` as needed

## Acceptance criteria

- [ ] Labels/namespace asserted on auto-sync creates (test fails on pre-fix code)
- [ ] Scoped-matching + fail-closed regressions in place (fail on pre-fix code)
- [ ] `cd lego/backend && go test ./...` green

## Out of scope

- Operator/envtest suites (no operator change)
