---
id: w1/m69/t003
title: "Simplify — /simplify over the code this milestone changed"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m69/t002]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving): the core acting-tenant mechanism, the webhook/blueprint binding, and any guard added on the apply path.

## Context

- Standing closing task (see `.claude/skills/pm/SKILL.md`).
- Files touched live in `lego/backend/internal/core/` and `lego/backend/internal/apps/`.

## Steps

1. Run `/simplify` scoped to the changed files.
2. Apply only behavior-preserving cleanups; re-run the backend suite.

## Files

- Whatever t001/t002 touched.

## Acceptance criteria

- [ ] `/simplify` pass applied (or consciously skipped with a reason recorded here)
- [ ] Backend suite still green

## Out of scope

- Anything beyond the milestone's diff
