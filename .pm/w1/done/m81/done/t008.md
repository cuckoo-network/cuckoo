---
id: w1/m81/t008
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m81/t007]
---

## Objective

Close the milestone once its definition of done is actually met.

## Context

- Only run this when t001–t007 are complete **and** the DoD in the milestone
  README holds: custom-domain rejections surface to the user, no #418 on the
  checked pages, the env-triggered rollout is discoverable (or the divergence is
  documented), and the free-tier forms validate plan limits before submit — all
  with passing regression tests.

## Steps

1. Verify the milestone DoD is real (not merely code-written) — re-check the
   custom-domain dialog against a live rejection and the console on a couple of
   pages.
2. Set every remaining task `status: done`, move each `tNNN.md` to
   `w1/m81/done/`, mark every row `— **DONE**` and set `**Status:** done` in
   `w1/m81/README.md`.
3. Move the whole `w1/m81/` directory to `w1/done/m81/` and check `- [x]` for
   m81 in `w1/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w1/m81/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] DoD verified real; board synced across task frontmatter, milestone README,
      and workstream checkbox; milestone moved to `w1/done/m81/`.

## Out of scope

- Any new implementation.
