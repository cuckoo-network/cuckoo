---
id: w1/m81/t006
title: Simplify the touched dashboard code
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m81/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification,
efficiency, altitude — behavior-preserving.

## Context

- Touched areas: the custom-domain add dialog error handling (t001), the shared
  form-error primitive it should now reuse, the hydration fix's shared
  component (t002), any deploy-list producer/reader change (t003), and the
  free-tier form/scaling validation (t004).
- Prefer reusing the existing webhook-form error-display pattern over a new
  bespoke one; prefer deriving plan caps from the shared tier catalog over new
  hard-coded constants.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving cleanups; keep regression tests green.

## Files

- The files changed by t001–t004.

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly deferred with reason.
- [ ] No behavior change; dashboard test suite + typecheck + lint green.

## Out of scope

- New features or bug hunting (that's `/code-review`).
