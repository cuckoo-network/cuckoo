---
id: w1/m81/t007
title: Test coverage for the fixed behaviors
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m81/t005]
---

## Objective

Add meaningful tests asserting the behaviors this milestone shipped and their
failure modes. Tests must assert real behavior and fail on the pre-fix code.

## Context

- Behaviors to cover:
  - **t001:** the Add Custom Domain dialog renders the server's `errors[]`
    message on a rejected `addCustomDomain` (assert the message is shown and the
    dialog stays open), and closes on success. Test with a mocked mutation error
    (mirror how the webhook form's error path is tested).
  - **t002:** the previously-mismatching component renders identical
    server/client output for the fixed value (a hydration/SSR-render test, or at
    minimum a unit test pinning the deterministic/placeholder render).
  - **t003:** the deploy-list includes the env-triggered rollout (backend
    producer/reader test) — or, if divergence, the Events-only surfacing is
    pinned and ADR018 updated.
  - **t004:** the free-tier forms block out-of-range disk size / instance count
    (component test against plan caps).

## Steps

1. Write the tests above, each demonstrated red against the pre-fix code where
   feasible.
2. Run the relevant suites (`dashboard` yarn test + typecheck; backend
   `go test ./...` if t003 touched the backend).

## Files

- `dashboard/src/**` test files for t001/t002/t004.
- `lego/backend/**` test files if t003 changed the deploy list.

## Acceptance criteria

- [ ] Each fixed behavior has a test that fails on the pre-fix code and passes
      after.
- [ ] No trivial/tautological/snapshot-everything tests; each asserts a real
      behavior or failure mode.
- [ ] Affected suites green.

## Out of scope

- Coverage of unrelated dashboard areas.
