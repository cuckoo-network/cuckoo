---
id: w1/m68/t011
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m68/t010]
---

## Objective

Close the milestone once its definition of done actually holds — not merely when the code is written. Three of this milestone's seven implementation tasks (t003, t006, t007) are only complete when a **deployed or out-of-band** state changes, so "the diff is done" is explicitly not the bar here.

## Context

Per `.pm/CLAUDE.md`: set every remaining task `status: done`, move each `tNNN.md` to `w1/m68/done/`, mark every row `— **DONE**` and `**Status:** done` in the milestone README, move the whole `w1/m68/` directory to `w1/done/m68/` (leaving no tombstone at the old path), and check `- [x]` in `.pm/w1/README.md`.

## Steps

1. Verify the definition of done item by item. In particular: is `BEX_TRUSTED_PROXY_CIDRS` actually deployed, is `BEX_SSH_KNOWN_HOSTS` actually captured, is `BEX_OAUTH_REQUIRE_AUDIENCE` actually `1` in production? If any is not, the milestone is not done.
2. Write the outcome section: what was closed, what was found to be already-known, and — for the lineage — what this round says about partial fixes to an authorization class.
3. Perform the moves and the three status syncs.
4. Leave the work uncommitted pending `/ship` (repo rule).

## Files

- `.pm/w1/m68/**`
- `.pm/w1/README.md`

## Acceptance criteria

- [ ] Definition of done verified item by item, including the three deployed/out-of-band items.
- [ ] Task files moved to `done/`, milestone moved to `w1/done/m68/`, no tombstone left at `.pm/w1/m68/`.
- [ ] Status synced in task frontmatter, milestone README, and workstream README.

## Out of scope

- Committing or pushing (`/ship` only).
