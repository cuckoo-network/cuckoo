---
id: w1/m68/t009
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m68/t008]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

The likely findings, given the shape of this milestone:

- t001 introduces the same conditional-relation pattern in two places (`SetCronJob`, `deploys.Trigger`). If a third appears, that is a helper, not a third copy.
- t004 may add a third copy of the `RequirePaymentMethod` + `RequireBillingMutation` pair; a shared provisioning guard is the altitude fix, and it is the one that stops a fourth create-like path from omitting it.
- t001/t002 both add "this relation is stronger because the parameter selects code / reveals sensitive data" comments — keep one consistent phrasing, matching the existing `codex #1` precedent rather than inventing a second idiom.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving cleanups only; anything that changes an authorization outcome belongs in its own task, not here.

## Files

- whatever this milestone touched

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff; findings applied or explicitly declined with a reason.
- [ ] No behavior change (test suite identical before and after).

## Out of scope

- Refactoring adjacent code this milestone did not touch.
