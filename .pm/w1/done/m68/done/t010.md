---
id: w1/m68/t010
title: Test coverage
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m68/t008]
---

## Objective

Add tests that assert the real behavior and failure modes this milestone shipped. Following this lineage's established bar: each regression test must **fail on the pre-fix code** — verify that by reverting the fix, not by assuming.

## Context

The finding this milestone is proudest of closing (F2) is a *partial-fix recurrence*: the previous round fixed five verbs of a class and left four, and nothing failed. So the highest-value test here is the one that makes the class hard to leave half-done — a table-driven assertion over every executable-selection verb, not five separate tests that a sixth verb can quietly avoid joining.

## Steps

1. **F2/t001** — table-driven role × verb matrix covering all four corrected verbs plus the two must-stay-permitted cases (bare trigger, schedule-only cron). Assert at least one non-REST surface so the "shared service method" claim is proven rather than asserted.
2. **F7/t002** — a contributor cannot enable statement logging; the previously-passing `insights_test.go:81-93` expectation is inverted; a safe parameter still round-trips.
3. **F5/t004** — paid recovery refused without a payment method; recovery refused under a billing-mutation lock; permitted recovery still produces the intended plan/version/`Recovery` spec.
4. **F10/t005** — generated backup Jobs carry a bounded `EmptyDir` and ephemeral-storage requests/limits on every container.
5. **F8/t003, F3/t006, F1/t007** are deployed-configuration and operator-action tasks; assert what is assertable in-repo (manifest/workflow shape — e.g. a guard that every `admin.conf`-fetching workflow wires the pin, in the spirit of m66's `gitops-validate.sh`) and record the live verification as evidence in the milestone outcome.
6. Run the full backend suite, the operator suite, and the shell suites.

## Files

- `lego/backend/internal/api/roleladder_test.go`
- `lego/backend/internal/apps/`, `internal/deploys/`, `internal/jobs/`, `internal/postgres/` test files
- `lego/operator/internal/controller/` test files
- possibly a new workflow-shape guard script

## Acceptance criteria

- [ ] Every regression test fails on the pre-fix code (verified by reverting each fix, not assumed).
- [ ] The F2 coverage is structured so that adding a new executable-selection verb without a relation decision is caught.
- [ ] Backend, operator, and shell suites green; lint green on the modules touched.

## Out of scope

- Coverage for the Tier-3 residuals recorded in `w1/046.md`.
