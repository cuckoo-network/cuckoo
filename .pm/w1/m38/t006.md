---
id: w1/m38/t006
title: "Test coverage — structural guard: platform DBs stay HA"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m38/t004]
---

## Objective

Add a `scripts/gitops-validate.sh` structural guard asserting each of the four platform CNPG Clusters declares `instances >= 2` and required pod anti-affinity, so a future edit can't quietly regress the pool to un-drainable. Prove the guard by mutation (set one back to 1 → guard fails).

## Acceptance criteria

- [ ] Guard in CI path; mutation check recorded; suites green.
