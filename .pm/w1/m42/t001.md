---
id: w1/m42/t001
title: Decide + document the absent-limit convention for the remaining unpaginated lists
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

bex REST currently has three absent-`limit` behaviors: Render's default-20 (services, owners, environments, events), documented full-history drift (deploys, env-vars, jobs, postgres, key-value — ADR018 rows record it as deliberate), and ignore-everything (projects, env-groups, custom-domains — the actual bugs). Pick one convention for the endpoints being fixed (recommend default-20 via `core.PageParams` + `StablePage`, the 89c42936 pattern) and write it down so w6/m32 (env groups) and w7/m38 (custom domains) inherit the decision instead of re-deciding.

## Context

- Commit `89c42936` (datastore pagination) established `StablePage` + requested-flag handling.
- docs/ADR006-bex-api.md is the natural home for the convention paragraph.

## Steps

1. Confirm Render's default page size for `/projects` in the pinned OpenAPI copy.
2. Write the convention (one paragraph) into docs/ADR006-bex-api.md's pagination section.
3. Cross-reference it from this milestone and note it for w6/m32 t002 / w7/m38 t002.

## Files

- docs/ADR006-bex-api.md

## Acceptance criteria

- [ ] Convention documented with the Render evidence cited; prettier clean.

## Out of scope

- Changing the documented full-history drift endpoints (deploys, env-vars, jobs, datastores) — that drift is deliberate and ledger-recorded.
