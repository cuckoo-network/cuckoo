---
id: w1/m42/t003
title: Re-verify the CLI projects row against a populated list; update checklist + ADR018
worker: worker1
status: todo
estimate: 25m
depends_on: [w1/m42/t002]
---

## Objective

The CLI checklist `projects` row was verified against an empty list, which is why the cursor-loop bug slipped through. Re-run the official `render` CLI's projects listing against a bex-api with ≥ 2 pages of projects and capture the evidence; update docs/cli-compatibility-checklist.md and the ADR018 projects/environments row.

## Context

- docs/cli-compatibility-checklist.md:53 (`projects` row).
- `scripts/cli-compat.sh` if it covers projects; otherwise manual capture as the checklist's other rows do.

## Steps

1. Seed > 20 projects (or > default-limit) in the dev control plane.
2. Run the CLI list; confirm termination + full coverage.
3. Update both docs with dated evidence.

## Files

- docs/cli-compatibility-checklist.md
- docs/ADR018-render-parity.md

## Acceptance criteria

- [ ] Checklist row cites populated-list evidence with today's date.

## Out of scope

- Other CLI rows (owned by w9/m4).
