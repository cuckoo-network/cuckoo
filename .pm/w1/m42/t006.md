---
id: w1/m42/t006
title: Test coverage
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m42/t004]
---

## Objective

Add a pagination walk test for `GET /v1/projects`: seed 2×limit+1 projects, page by echoed cursor, assert termination, full coverage, no duplicates — the exact failure mode that existed (identical full pages forever) must be asserted against.

## Files

- `lego/backend/internal/projects/` tests

## Acceptance criteria

- [ ] Test fails against the pre-milestone handler (cursor ignored ⇒ walk never terminates / duplicates detected).
- [ ] `go test ./...` passes.
