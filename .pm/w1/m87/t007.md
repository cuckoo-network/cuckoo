---
id: w1/m87/t007
title: Render parity check
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m87/t004, w1/m87/t005, w1/m87/t006]
---

## Objective

Standing gate. This milestone changes a tenant-facing surface (snapshot list/restore availability, the Snapshots card's states, the usage chart), so check it across every surface it exposes.

## Context

- REST/GraphQL/MCP snapshot verbs live in `lego/backend/internal/apps/disk_snapshots.go`; the dashboard card in `disk-section.tsx`.
- Compare against Render's own Snapshots card — see `docs/render-artifacts/disks.md` § Live walk, with a disk attached (2026-08-24), which records its copy and its info banner verbatim.

## Steps

1. Confirm the not-configured state and any new error code are consistent across REST, GraphQL and MCP — same code, same semantics.
2. Compare the card against Render's captured copy; record any deliberate divergence in ADR082 rather than leaving it silent.

## Files

- review task

## Acceptance criteria

- [ ] all three API surfaces agree on the new error identity
- [ ] divergences from Render either removed or written down

## Out of scope

- The REST-shape divergences already tracked in w1/076 → m88.
