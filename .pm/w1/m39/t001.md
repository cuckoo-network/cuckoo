---
id: w1/m39/t001
title: "Triage all 18 alerts: classify real vs vendored vs dev-only"
worker: worker1
status: todo
estimate: 45m
depends_on: []
---

## Objective

Classify every open Dependabot alert before touching anything: which module/manifest it comes from (`lego/` Go modules, `dashboard/` yarn, the vendored `cli/` checkout if committed, GitHub Actions), whether the vulnerable path is reachable in shipped code or only in tests/dev tooling, and which fixes are patch/minor (safe batch) vs major (breaking).

## Context

- https://github.com/bex-co/bex/security/dependabot — 18 alerts as of 2026-07-15 (7 critical, 3 high, 7 moderate, 1 low).
- Precedents: `w1/done/m23` (first triage), `w1/done/m30` (residual close), `.pm/w1/done/018.md`.
- The SSH gateway (`lego/backend/cmd/ssh-gateway`, new dep surface) and any vendored CLI checkout are the likely sources — verify, don't assume.

## Steps

1. Pull the alert list (`gh api /repos/bex-co/bex/dependabot/alerts --paginate` or the UI); table: package · severity · manifest · reachable? · fix type.
2. Mark the safe batch (t002) vs breaking set (t003) vs not-actually-ours (vendored fixtures → dismiss with reason).

## Files

- triage table recorded in this milestone dir

## Acceptance criteria

- [ ] All 18 alerts classified with manifest + reachability + fix type; safe/breaking/dismiss sets named.

## Out of scope

- Applying any fix (t002/t003).
