---
id: w1/m12/t002
title: GraphQL + MCP parity for scale
worker: worker1
status: todo
estimate: 25m
depends_on: [w1/m12/t001]
---

## Objective

The scale verb on the other two adapters: GraphQL mutation (`scaleService(id, numInstances)` unless a Render dashboard capture in `docs/render-artifacts/` shows the real name) and MCP tool `scale_service` (`{serviceId, numInstances}`), both delegating to the t001 service method. `replicas` already rides on the service object — verify it reflects the new spec value on all surfaces.

## Files

- `lego/backend/internal/apps/graphql.go`, `lego/backend/internal/apps/mcp.go`
- `docs/bex-api.md`

## Acceptance criteria

- [ ] GraphQL mutation and MCP tool both scale the same App and return the updated service object; docs tables updated.

## Out of scope

- Dashboard UI (w5/005).
