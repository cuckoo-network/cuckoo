---
id: w9/m3/t008
title: Update ADR009/ADR020 and the CLI compatibility checklist with migration and live evidence
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m3/t007]
---

## Objective

Replace the documented “architectural, not a bug to fix” exception with the shipped stable-id/mutable-name contract and reproducible evidence from dev and production.

## Context

- `docs/cli-compatibility-checklist.md` RC11 and the `postgres update --name` row currently say rename is unsupported because name is id.
- `docs/ADR020-identifiers.md` lists both Postgres and KeyValue name-as-id as a known deviation; only the Postgres half changes here.
- `docs/ADR009-postgresql-management.md` must explain which identifiers/endpoints remain stable on rename and how legacy CRs are adopted.
- The checklist currently has an unrelated unresolved merge conflict in the working tree; preserve/resolve that existing work deliberately before editing rather than overwriting either side.

## Steps

1. Document new-vs-legacy identity behavior, mutable `spec.name`, workspace uniqueness, and the no-recreation migration invariant in ADR009/ADR020.
2. Update RC11 and the command census to mark `postgres update --name` ✅ with exact official-CLI, dev, and production evidence.
3. Document the migration/verification command sequence without credentials and link the scripts/tests that make the claim reproducible.
4. Check ADR018's Postgres row and any docs/comments that still claim Database name is id; update only statements invalidated by this milestone.
5. Run the pinned Markdown formatter over all changed Markdown.

## Files

- `docs/ADR009-postgresql-management.md`
- `docs/ADR020-identifiers.md`
- `docs/cli-compatibility-checklist.md`
- `docs/ADR018-render-parity.md`
- root `AGENTS.md`/`CLAUDE.md` docs index only if its summary becomes stale

## Acceptance criteria

- [ ] Docs distinguish immutable id, mutable resource name, and physical PostgreSQL database/user/host identities.
- [ ] The legacy fallback/backfill and “never recreate live CNPG state for rename” rule are explicit.
- [ ] The checklist row is ✅ and cites a reproducible official-CLI run in dev and production.
- [ ] KeyValue's remaining name-as-id deviation is still stated accurately.
- [ ] `npx prettier@3.4.2 --write "**/*.md"` completes and no conflict markers remain in edited docs.

## Out of scope

- Rewriting historical CLI evidence unrelated to Postgres rename.

