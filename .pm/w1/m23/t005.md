---
id: w1/m23/t005
title: Simplify — /simplify over the code changed (t001, t003)
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m23/t001, w1/m23/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — the ReadinessProbe wiring (t001) and the mock-cluster labeling (t003). Behavior-preserving. (t002 is dependency bumps, t004 is docs — nothing to simplify there.)

## Context

- Scope: `app_controller.go` probe wiring + `scripts/mock-cluster.sh` label step.

## Steps

1. Run `/simplify` over the t001 + t003 diff.
2. Apply behavior-preserving cleanups; re-run `make test`.

## Files

- Files touched by t001, t003.

## Acceptance criteria

- [ ] `/simplify` findings applied or consciously declined; tests still green.

## Out of scope

- t002/t004 (no code to simplify); bug hunting (`/code-review`).
