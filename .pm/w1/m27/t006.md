---
id: w1/m27/t006
title: Re-verify etcd restore against the current post-m19 multi-node topology
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

Re-confirm the etcd restore runbook still works against bex's current production topology — the existing "What was tested" record in `docs/ADR011-etcd-backup-restore.md` is from 2026-07-05 against a single-node local CAPD cluster, predating the `w1/m19` rearchitecture (CAPH-owned network, tainted control plane, self-managed CAPI pivot).

## Context

- `docs/ADR011-etcd-backup-restore.md`'s "Restore" section, path A ("Selective re-apply onto a fresh cluster") is marked "recommended, tested" — but the test predates the current substrate.
- The backup mechanism itself (nightly CronJob) has been running continuously since and was part of the m19.1 op log's "backup chain verified end-to-end" note — this task specifically re-proves the *restore* path, not the backup-taking path.

## Steps

1. Take a fresh etcd snapshot from the current prod cluster (or a faithful non-prod replica).
2. Perform path A's selective re-apply against a throwaway environment, following the documented runbook exactly.
3. Confirm the recovered App CR data matches current prod.
4. Record the drill (date, topology, any runbook corrections needed) — feeds into t007's ADR.

## Files

- none (drill only, unless the runbook needs a correction, in which case update `docs/ADR011-etcd-backup-restore.md`)

## Acceptance criteria

- [ ] Restore succeeds against the current topology and matches prod state.
- [ ] ADR011's "What was tested" note is updated with the new verification date (final update happens in t007).

## Out of scope

- Path B (full in-place restore) — path A is bex's recommended/primary path; re-verifying B is a lower-priority follow-up if ever needed.
