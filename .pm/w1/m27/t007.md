---
id: w1/m27/t007
title: "docs/ADR031-platform-data-backup.md — consolidated backup/restore policy"
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m27/t003, w1/m27/t005, w1/m27/t006]
---

## Objective

Write the ADR documenting bex's platform data-backup policy across all three critical stores (etcd, OpenBao, `bex-db`) in one place: what's backed up, where, retention, the restore mechanism for each, and the drill results from t003/t005/t006 — then update `ADR011`/`ADR015`'s stale "untested"/"2026-07-05" notes in place so they point at this consolidated record instead of going stale independently.

## Context

- Follow the existing ADR format/index convention (`CLAUDE.md`'s Docs index).
- **ADR030 is reserved** by `w8/m7/t003`'s pricing ADR — use **ADR031** to avoid a numbering collision (check `ls docs/ADR03*` again at execution time in case ADR030 landed first).
- Cross-reference: `docs/ADR011-etcd-backup-restore.md`, `docs/ADR015-openbao-backup-restore.md`, `docs/ADR009-postgresql-management.md` (the tenant-Postgres recovery mechanism this borrows from), `docs/ADR003-control-plane.md` (what `bex-db` holds).

## Steps

1. Write `docs/ADR031-platform-data-backup.md`: one table/section per store (etcd, OpenBao, bex-db) — backup mechanism, schedule, retention, restore mechanism, last-verified-restore date, and a recommended re-drill cadence.
2. Update `ADR011`'s "What was tested (2026-07-05...)" line with t006's new verification.
3. Update `ADR015`'s "⚠ Untested against a live cluster" line with t005's verification (remove the warning, replace with a verified-date note).
4. Add the new ADR's line to `CLAUDE.md`'s Docs index.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- new `docs/ADR031-platform-data-backup.md`
- `docs/ADR011-etcd-backup-restore.md`
- `docs/ADR015-openbao-backup-restore.md`
- `CLAUDE.md` (Docs index)

## Acceptance criteria

- [ ] ADR031 exists, is indexed, and consolidates all three stores' backup/restore status.
- [ ] ADR011 and ADR015's stale warnings are updated, not left dangling.

## Out of scope

- Any payment/billing-adjacent content (unrelated; not to be confused with ADR030's pricing doc).
