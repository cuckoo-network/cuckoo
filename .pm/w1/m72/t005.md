---
id: w1/m72/t005
title: Simplify
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m72/t003, w1/m72/t004]
---

## Objective

Run `/simplify` over what this milestone changed — behaviour-preserving only.

## Context

- The point of the milestone is deletion; confirm the net tracked-LOC change is strongly negative and that `scripts/dev-env.sh` did not simply absorb ten copies' worth of special cases as branches.
- Check for overlap with the existing local-cluster tooling: `scripts/mock-cluster.sh`, `scripts/up.sh`, `scripts/lib`.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the behaviour-preserving fixes.
3. Count the per-N conditional branches in `dev-env.sh` — more than a couple suggests t001 kept drift it should have dropped.
4. File anything larger as an inbox note.

## Files

- `scripts/dev-env.sh` and the milestone's changed files

## Acceptance criteria

- [ ] Findings applied or deferred with a filed note.
- [ ] Net tracked-LOC change recorded and negative.
- [ ] Per-N branch count recorded and justified.

## Out of scope

- Consolidating `mock-cluster.sh` — file it if it looks worthwhile.
