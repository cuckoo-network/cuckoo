---
id: w1/m72/t007
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m72/t005, w1/m72/t006]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

- Do not run this until the DoD is real: two workstreams — one of them `w5`, the 274-line outlier — verified up / healthy / down on the live local cluster through the shared script, all ten migrated, logs bounded, no worker's running environment destroyed.
- Record the numbers: tracked LOC before and after, `du -sh .pm` before and after, `up.sh` variant count before (8) and after (1).

## Steps

1. Verify the DoD on the live local cluster, including the `w5` path.
2. Set every remaining task `status: done` and move each `tNNN.md` to `.pm/w1/m72/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in this README, recording the measured numbers.
4. Move `.pm/w1/m72/` to `.pm/w1/done/m72/` and check `- [x]` in `.pm/w1/README.md`.

## Files

- `.pm/w1/m72/**`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Two workstreams verified live, one of them `w5`.
- [ ] Before/after tracked LOC, `du -sh .pm`, and variant count recorded.
- [ ] Milestone moved whole to `.pm/w1/done/m72/`; nothing left at the old path.

## Out of scope

- Migrating any future `dev-11`.

## Status (2026-08-18)

**BLOCKED on the live-cluster half of the DoD.** t001–t006 are done: `scripts/dev-env.sh` implements the reconciled union, all ten workstreams are migrated, the copies are gone, logs are bounded, and `scripts/dev-env.test.sh` (wired into `scripts.yml`) is green alongside shellcheck.

What is not done is the DoD's *"two workstreams verified up / healthy / down on the live local cluster, one of them `w5`"*. **The shared CAPD cluster is dead and needs rebuilding, not restarting**: its control-plane and LB containers had merely stopped (exit 130, 24h ago) and restarting them brought back etcd, the scheduler, and the controller-manager — but the apiserver crashloops on `open /etc/kubernetes/pki/apiserver.crt: no such file or directory`. The node's PKI is gone, so the machine has to be re-provisioned (`kind delete cluster` for both, then `bash scripts/mock-cluster.sh`), which is a ~15-minute destructive rebuild of the operator's local Docker state and their call to authorize.

Close this once that rebuild happens and `bash scripts/dev-env.sh 5 up && bash scripts/dev-env.sh 5 status && bash scripts/dev-env.sh 5 down` (plus one more N) is walked green.
