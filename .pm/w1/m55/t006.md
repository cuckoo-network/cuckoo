---
id: w1/m55/t006
title: Remove dashboard workspace localStorage migration and dead compatibility helpers
worker: worker1
status: todo
estimate: 45m
depends_on: []
---

## Objective

Finish the dashboard's cookie migration and delete the audited dead compatibility code without changing current workspace-switching behavior.

## Context

- dashboard/src/features/workspaces/context/index.tsx and lib/selection.ts still read and double-write bex.selectedWorkspaceId in localStorage alongside the cookie.
- dashboard/src/common/types/route-loader.ts has no imports, and resource-health.ts exports an unused isRowHealthy helper.
- No auth token or Kratos session material may move into browser-readable storage; the existing HttpOnly authentication design is unchanged.

## Steps

1. Remove localStorage reads, writes, migration branches, and tests; retain cookie-backed workspace selection.
2. Delete the unused route-loader types file and isRowHealthy export.
3. Exercise first load, persisted selection, deleted-workspace fallback, switching, and hard reload.

## Files

- dashboard/src/features/workspaces/context/index.tsx
- dashboard/src/features/workspaces/lib/selection.ts
- dashboard/src/features/workspaces/\*_/_.test.tsx
- dashboard/src/common/types/route-loader.ts
- dashboard/src/features/projects/lib/resource-health.ts

## Acceptance criteria

- [ ] Workspace selection persists through the existing cookie and never reads or writes bex.selectedWorkspaceId in localStorage.
- [ ] Switching and deleted-workspace fallback remain correct across hard reload.
- [ ] The dead route-loader file and isRowHealthy helper have no remaining references and are removed.
- [ ] Dashboard typecheck and tests pass.

## Out of scope

- Changing first-party dashboard authentication from Kratos session cookies.
