---
id: w1/m6/t003
title: Spike — vcluster per tenant via Argo ApplicationSet
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m6/t001]
---

## Objective

De-risk phase 2: a vcluster per tenant gives each its own apiserver view (stronger API isolation) while still sharing nodes. Prototype one via an Argo `ApplicationSet` and document the trade-offs.

## Context

- Design-heavy: needs a decision on vcluster distro + how tenant Apps target the vcluster. Keep this a scoped spike, not production wiring.

## Acceptance criteria

- [ ] A one-tenant vcluster stands up via ApplicationSet; a short note captures cost, operability, and the go/no-go for productionizing.

## Out of scope

- Rolling it out for all tenants (follow-up milestone).
