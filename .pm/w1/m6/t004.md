---
id: w1/m6/t004
title: Design note — opt-in hard isolation (Kata/Firecracker)
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

Capture the opt-in hard-isolation design for security-sensitive/compliance tenants: OpenSandbox / Kata / Firecracker microVM per workload, on a dedicated node pool (taints + nodeSelector) reserved for paid/compliance.

## Context

- Isolation units ≠ worker pools; a dedicated tainted pool is the *only* place pool-pinning is justified (compliance), per `docs/control-plane.md`.

## Acceptance criteria

- [ ] A short design note: when to offer it, the runtime choice, node-pool/taint model, and cost — enough to scope the build later.

## Out of scope

- Implementation (future milestone).
