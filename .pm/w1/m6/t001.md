---
id: w1/m6/t001
title: Namespace per tenant + RBAC + ResourceQuota + LimitRange
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m2/t005]
---

## Objective

Phase 1 soft isolation: the control plane creates a namespace per tenant with RBAC, a ResourceQuota, and a LimitRange; tenant Apps deploy into that namespace.

## Context

- Driven by the control plane (m2). Isolation is by namespace, on shared nodes.

## Files

- control-plane provisioning (per-tenant namespace + RBAC + quota + limitrange)

## Acceptance criteria

- [ ] Creating a tenant yields a namespace with RBAC, ResourceQuota, and LimitRange; the tenant's Apps land there.

## Out of scope

- NetworkPolicy (t002); vcluster (t003).
