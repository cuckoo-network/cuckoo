---
id: w1/m6/t002
title: Default-deny cross-tenant NetworkPolicy + deploy into tenant ns
worker: worker1
status: todo
estimate: 25m
depends_on: [w1/m6/t001]
---

## Objective

Stop cross-tenant traffic: apply a default-deny NetworkPolicy per tenant namespace, and ensure App deploys are routed into the tenant namespace.

## Context

- Cilium CNI enforces NetworkPolicy. Default-deny ingress/egress across tenant boundaries.

## Files

- control-plane / operator (NetworkPolicy per tenant ns; ns-scoped App deploys)

## Acceptance criteria

- [ ] A pod in tenant A cannot reach a pod/Service in tenant B; intra-tenant traffic still works.

## Out of scope

- Stronger API isolation (t003).
