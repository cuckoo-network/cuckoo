---
id: w11/m8/t005
title: Audit Tier-2 scope exclusions and Render parity
worker: worker11
status: done
estimate: 30m
depends_on: [w11/m8/t009]
---

## Objective

Audit env/cron/datastore/usage/invite fields, mutations, errors, and mobile omissions against REST/GraphQL/MCP/dashboard and Render.

## Context

- Deliberate mobile omissions are product scope, not permission to invent API behavior.

## Steps

1. Run contract and route/action inventory diffs; fix drift or record follow-ups in ADR018/ADR048.

## Files

- `mobile/src/features/**`
- `dashboard/src/{common,features}/**`
- `lego/backend/internal/{apps,core,members}/**`
- `docs/{ADR018-render-parity.md,ADR048-mobile.md}`

## Acceptance criteria

- [x] Shared verbs match across surfaces and every D3/D4 omission remains unreachable and documented.

## Outcome

- Invite capability minting fails closed on entropy errors, and dashboard/native handoffs reject stale, malformed, duplicate, or ambiguously transported bearers without exposing them.
- Cron actions use stable REST/GraphQL/MCP codes; cached/erroring evidence cannot expose mobile writes; Postgres restart acceptance is never presented as observed convergence.
- The native policy test closes over every production route, direct write, GraphQL operation, variable, and selected field, keeping D4 configuration, credential, admin, bulk, and destructive reads/writes unreachable without explicit review.
- ADR018 and ADR048 now record the actual usage-coverage, invite-token rotation, datastore evidence, authorization, and billing-exclusion contracts.

## Out of scope

- Filling omissions with desktop UI.
