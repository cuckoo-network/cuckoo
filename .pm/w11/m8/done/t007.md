---
id: w11/m8/t007
title: Test coverage
worker: worker11
status: done
estimate: 60m
depends_on: [w11/m8/t005]
---

## Objective

Test env secret boundaries/conflicts, cron idempotency/phases, datastore degraded states, usage partial data, invite verification, and negative scope inventory.

## Context

- Assert behavior/failures, not blanket snapshots.

## Steps

1. Add backend-adapter/component/device tests for all Tier-2 paths and exclusions.

## Files

- `mobile/src/features/**/__tests__/**`
- `mobile/e2e/**`

## Acceptance criteria

- [x] Tests fail on secret leakage, duplicate cron action, false health/usage, invite bypass, or forbidden control.

## Outcome

- Environment tests now close over the exact one-key CAS document, no-cache reveal path, secret sinks, immutable plaintext session, and stale-operation boundary; cron tests reject duplicate/replayed confirmations and unrelated or nonterminal convergence evidence.
- Datastore and usage cases reject invalid timestamps, counters, capacity, partial coverage, malformed totals, and invented healthy-zero evidence. The audit fixed two false-health defects exposed by those regressions.
- Invite capture now verifies the exact HTTPS origin, path, query, and router-token match before persisting a bearer, rejecting custom schemes and URL tricks while clearing stale pending capabilities.
- An independent negative inventory denies desktop/admin/destructive routes, writes, and sensitive GraphQL fields even if the positive allowlist is later edited.
- The full mobile gate passes 255 unit tests, Expo configuration validation, and fresh iOS and Android production bundles.

## Out of scope

- Admin/bulk workflows.
