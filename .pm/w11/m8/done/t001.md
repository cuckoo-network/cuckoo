---
id: w11/m8/t001
title: Add one-at-a-time env-var quick view and edit
worker: worker11
status: done
estimate: 60m
depends_on: [w11/m3/t009, w11/m4/t008]
---

## Objective

Support deliberate view/edit of one service env var at a time with confirmation and server-side authorization.

## Context

- No bulk import/export, env groups, secret files, casual reveal, or configuration workbench.
- The current OpenBao path is last-write-wins. Add a backward-compatible whole-environment revision and KV-v2 CAS path before claiming concurrent-edit safety.

## Steps

1. Add an opaque revision to masked/single-key reads and an optional one-key CAS argument to the sparse patch contract.
2. Add list/masked state, explicit reveal boundary, one-field editor, conflict refresh, and save result.

## Files

- `lego/backend/internal/{core,secrets,apps,api}/**`
- `mobile/src/features/environment/**`

## Acceptance criteria

- [x] Masked list responses contain no values; explicit reveal fetches only one selected value and never caches or persists it.
- [x] Two saves from one revision produce one success and one coded conflict without overwriting the winner or leaking keys, values, or revisions.
- [x] Invalid key/value, denial, source/projection failure, and compensation conflict are honest; existing callers that omit the revision retain their contract.

## Out of scope

- Bulk/group/file management.
