---
id: w11/m7/t002
title: Render plans, diffs, terminal output, tools, and evidence compactly
worker: worker11
status: todo
estimate: 60m
depends_on: [w11/m7/t001]
---

## Objective

Render ordered AI SDK UI-message parts into accessible native conversation rows for text, reasoning, plan, diff, terminal/tool output, decisions, and evidence.

## Context

- Extract platform-neutral ACP part schemas, activity grouping, diff parsing, safe JSON, and duration logic from the dashboard; keep a separate native presentation.
- AI Elements and dashboard JSX/Tailwind are web-only references. Gifted Chat's bubble model and a WebView are not suitable for heterogeneous ACP activity.
- `@assistant-ui/react-native` may be evaluated later, but is not the core dependency while bex remains on AI SDK v6 and needs a custom ticket transport.

## Steps

1. Add a virtualized, memoized typed-part registry with throttled streaming updates, truncation/expansion, safe native Markdown/link handling, monospace views, and an unknown-part fallback.

## Files

- `mobile/src/features/agent-sessions/conversation/**`

## Acceptance criteria

- [ ] Mixed/large/unknown/out-of-order fixture streams remain readable, accessible, and stable.
- [ ] Shared pure parsing logic has one implementation; no dashboard DOM component, raw HTML, remote image, or WebView crosses into mobile.

## Out of scope

- Editing code or reviewing PRs.
