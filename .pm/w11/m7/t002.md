---
id: w11/m7/t002
title: Render plans, diffs, terminal output, tools, and evidence compactly
worker: worker11
status: todo
estimate: 60m
depends_on: [w11/m7/t001]
---

## Objective

Render AI SDK UI-message/raw parts into accessible mobile conversation cards for text, plan, diff, terminal/tool output, decisions, and evidence.

## Context

- Reuse `.pm/w5/035.md`'s wire-format decisions while keeping a separate native presentation; large code/output must be bounded and copyable.
- m66 consumer audit (2026-09-04): mobile currently has lifecycle/result views and no transcript renderer. Consume the direct ACP driver's AI SDK v6 stream: native dynamic tool parts with real titles/input/output, `data-acp-plan`, `data-acp-diff`, and `data-acp-terminal`. Available-command and other informational updates are transient. Do not implement the retired generic `data-acp` envelope or synthetic provider tool unwrapping. Use `lego/agent-image/driver/fixtures/acp-agent.mjs` and the dashboard conversation fixtures as the contract examples.

## Steps

1. Add typed part registry, streaming updates, truncation/expansion, monospace views, and unknown-part fallback.

## Files

- `mobile/src/features/agent-sessions/conversation/**`

## Acceptance criteria

- [ ] Mixed/large/unknown/out-of-order fixture streams remain readable, accessible, and stable.

## Out of scope

- Editing code or reviewing PRs.
