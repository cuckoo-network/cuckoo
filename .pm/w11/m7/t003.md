---
id: w11/m7/t003
title: Add explicit needs-decision lifecycle and prompt-turn contracts
worker: worker11
status: todo
estimate: 60m
depends_on: [w11/m7/t002]
---

## Objective

Model waiting-for-input and prompt-turn acceptance durably and consistently instead of inferring a decision request from transcript text.

## Context

- Extend the one `agentsessions` core and REST/GraphQL/MCP adapters; preserve phase-1 steering and optimistic concurrency/idempotency.

## Steps

1. Define state transitions, decision metadata, prompt-turn idempotency, audit, authorization, and adapter fields/errors.

## Files

- `lego/backend/internal/agentsessions/**`
- `lego/backend/internal/api/**`
- `lego/backend/internal/store/migrations/**`

## Acceptance criteria

- [ ] Invalid phase, duplicate turn, stale decision, cross-workspace, cancel race, and resume failure are named/tested.

## Out of scope

- Agent permission bypass modes.
