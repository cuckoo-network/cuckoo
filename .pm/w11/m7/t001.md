---
id: w11/m7/t001
title: Implement Expo AI SDK attach, replay, and live reconnect
worker: worker11
status: todo
estimate: 60m
depends_on: [w11/m6/t009, w11/m7/t011]
---

## Objective

Consume the hardened conversation contract through Vercel AI SDK v6 and `expo/fetch`: reconstruct history, resume after the durable cursor, then follow live output with bounded reconnect.

## Context

- ACP terminates in the sandbox driver; mobile consumes AI SDK UI messages and must not become a raw ACP client.
- Pin the AI SDK v6 generation to the dashboard/driver until a coordinated upgrade; do not adopt a floating AI SDK v7 dependency in this task.
- Gateway owns nonce claim, sandbox ingress, transcript tee/replay, and multi-client semantics; tickets stay memory-only.

## Steps

1. Add an Expo `useChat`/`DefaultChatTransport` integration using `expo/fetch`, separate `read` and `turn` ticket preparation, durable cursor resume, ticket expiry, disconnect, foreground resume, and cancellation.

## Files

- `mobile/src/features/agent-sessions/attach/**`

## Acceptance criteria

- [ ] Reattach across disconnect/background yields one ordered conversation with no missing/duplicate part or user turn.
- [ ] Every GET mints a fresh `read` ticket, every POST mints a fresh `turn` ticket, and neither ticket is persisted or logged.
- [ ] The AI SDK stream uses `expo/fetch`; the XHR log transport and a raw ACP client are not reused.

## Out of scope

- Gateway/transcript implementation owned by t010/t011.
- An AI SDK major-version upgrade.
