Before any TestFlight, Play internal-track, or production-store distribution, execute `mobile/e2e/m2-real-device.md` on one physical iOS device and one physical Android device against a phone-reachable environment with the `bex-mobile` Hydra client provisioned. Record system-browser PKCE/MFA callback, SecureStore cold restore and rotation, workspace isolation, offline expiry, logout revocation, iOS reinstall, and Android backup/restore evidence. This is release qualification, not a source-merge gate; no store release may claim completion without it.
