---
id: w11/m5/t003
title: Project deploy, crash, and cron events into idempotent push jobs
worker: worker11
status: done
estimate: 60m
depends_on: [w11/m5/t002]
---

## Objective

Reuse the durable composed platform-event feed to enqueue deploy success/failure, service crash, and cron failure push jobs exactly once per device/event.

## Context

- Do not add new lifecycle instrumentation or duplicate `w8/001` usage-threshold production.

## Steps

1. Extract/reuse event projection, persist delivery identity/status, collapse safe updates, and dispatch after commit.

## Files

- `lego/backend/internal/{events,webhooks,notifications}/**`
- `lego/backend/internal/store/migrations/**`

## Acceptance criteria

- [ ] Replay/restart/concurrent-worker tests produce one logical delivery and retain retryable state.

## Out of scope

- Agent events, added in m6.
