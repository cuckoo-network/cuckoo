---
id: w11/m5/t002
title: Add the provider-neutral push transport and credential configuration
worker: worker11
status: done
estimate: 60m
depends_on: [w11/m5/t001]
---

## Objective

Define a provider-neutral delivery seam and production configuration for the chosen Expo/APNs/FCM path with safe credential rotation.

## Context

- Unset configuration must create no client/network and report push unavailable; secrets stay out of logs and repo.

## Steps

1. Implement transport, payload limits, typed permanent/transient errors, env validation, rotation/runbook, and fake server.

## Files

- `lego/backend/internal/notifications/push/**`
- `lego/backend/cmd/api/**`
- `deploy/gitops/**`
- `docs/runbooks/**`

## Acceptance criteria

- [ ] Disabled, success, throttled, invalid-token, outage, malformed-config, and rotation paths are deterministic and tested.

## Out of scope

- Slack or marketing notifications.
