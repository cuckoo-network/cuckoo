---
id: w11/m6/t009
title: Closeout
worker: worker11
status: todo
estimate: 10m
depends_on: [w11/m6/t008, w11/m6/t010]
---

## Objective

Close m6 only after the real phone-to-draft-PR/evidence/push DoD holds.

## Context

- Completing this task performs canonical board moves and is a prerequisite for m7.

## Steps

1. Verify DoD, synchronize status, and move milestone/tasks to `done/`.

## Files

- `.pm/w11/m6/**`
- `.pm/w11/README.md`

## Acceptance criteria

- [ ] DoD holds and board state is synchronized.

## Out of scope

- Closing before w3/m41 and m5 are truly complete.

## Status (2026-08-08) — BLOCKED, not closed

t001–t008 are implemented, verified, and **shipped + deployed to prod** (`3bbe956d`, `deploy (bex via Argo)` green; the mobile CI codegen-diff gate was fixed in `4f58e455`). Board state is synchronized: t001–t008 marked `done` with per-task evidence in the milestone README, ADR018's parity ledger updated, and the w11 README m6 line annotated.

The milestone is **not** closed, per this task's own DoD + out-of-scope:

- The DoD requires the real phone→draft-PR/evidence/push loop verified live — needs the operator's live agent-session substrate (same gate as `w3/m41/t004`), unavailable in a dev environment.
- Closing is explicitly forbidden until `w3/m41` and `w11/m5` are truly complete; both are code-complete but their own closeouts (`w3/m41/t008`, `w11/m5/t011`) are blocked on live-prod + **physical iOS/Android device** verification (`w11/m5/t007`) not available here.

To close: first complete t010's Expo MCP visual pass. Then, when a live substrate + signed devices are available, run the w3/m41 phone→draft-PR E2E through the mobile client, confirm a terminal push arrives on a physical device and opens the session, then `/pm done w11/m6/t009` (move tasks + milestone to `done/`, check the w11 README box).
