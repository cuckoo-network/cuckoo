---
id: w11/m1/t005
title: Add mobile documentation, dependency audit, and CI gates
worker: worker11
status: done
estimate: 45m
depends_on: [w11/m1/t004]
---

## Objective

Make `mobile/` a maintainable repository package with accurate instructions, a minimal dependency set, and CI-enforced checks.

## Context

- Source docs misstated installed Expo/RN versions; derive documentation from package/config truth.
- Keep GraphQL, router, secure-storage-ready, linking, haptics, SVG/D3, safe-area, forms/Zod, theme, and test dependencies only when reachable.

## Steps

1. Add `mobile/README.md`, `mobile/CLAUDE.md`, and the required sibling `AGENTS.md` symlink.
2. Prune dependencies/locales/assets, regenerate lockfile, and add path-scoped CI.

## Files

- `mobile/{README.md,CLAUDE.md,AGENTS.md,package.json,yarn.lock}`
- `.github/workflows/mobile-test.yml`

## Acceptance criteria

- [x] Docs, license notices, commands, dependencies, lockfile, and CI agree with the clean tree.

## Out of scope

- Store release automation.

## Resolution

Added package-specific README/CLAUDE guidance and AGENTS symlink, generated a frozen Yarn lockfile, pruned unreachable product dependencies, aligned Expo-native versions, and added a path-scoped workflow that tests and exports both platforms.
