---
id: w11/m1/t002
title: Strip Beancount product, repository, release, and credential identity
worker: worker11
status: done
estimate: 45m
depends_on: [w11/m1/t001]
---

## Objective

Remove every accounting route/screen/query/type and every inherited deployment, analytics, auth, store, or project identity from the copied tree.

## Context

- Remove accounting `app/` routes, `src/screens`, GraphQL queries/generated schema, domain utilities/components/tests/locales, Fastlane/docs/deploy scripts.
- Remove WebView/JWT/AsyncStorage credential logic, camera/receipt/editor permissions, EAS ids, update URLs, bundle ids, server URLs, Sentry/Mixpanel ids.

## Steps

1. Delete domain/release/auth material and inventory the residue with `rg` plus dependency reachability.
2. Leave only neutral foundations needed by t003.

## Files

- `mobile/app/**`
- `mobile/src/**`
- `mobile/{app.json,eas.json,package.json}`

## Acceptance criteria

- [x] No inherited credential, product endpoint, project/store identity, accounting behavior, code editor, or dangerous permission remains.

## Out of scope

- Implementing bex auth or feature screens.

## Resolution

Removed the source product routes, screens, generated API surface, release automation, auth/storage code, endpoints, Expo/EAS identifiers, analytics identities, and dangerous permissions. The residue scan is clean outside required attribution/license files.
