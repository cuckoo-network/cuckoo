---
id: w11/m9/t007
title: Simplify the drawer implementation
worker: worker11
status: done
estimate: 30m
depends_on: [w11/m9/t006]
---

## Objective

Run `/simplify` over the complete m9 diff, removing copied source-domain residue and consolidating drawer/profile/selection mechanics without weakening security or interaction guarantees.

## Context

- The Beancount source is a reference, not an import target; ledger names, analytics, generated GraphQL, and its theme/auth assumptions must not survive in bex.
- Likely reuse seams are open/close state, action completion, list-state rendering, initials, and authenticated REST request boundaries.

## Steps

1. Run `/simplify` over every file changed by m9.
2. Remove Beancount-specific residue and duplicated open/close, identity fallback, or workspace-row logic.
3. Preserve the established feature boundaries (`features/workspaces`, `features/profile`, `features/auth`, generic drawer component).
4. Re-run focused tests plus format/typecheck/lint after accepted cleanup.

## Files

- `mobile/src/components/app-drawer/**`
- `mobile/src/features/{workspaces,profile,auth}/**` touched by m9
- `mobile/app/(app)/_layout.tsx`
- Milestone-touched translations/tests

## Acceptance criteria

- [ ] `/simplify` has reviewed the whole m9 diff and accepted changes preserve observable behavior.
- [ ] No Beancount ledger/analytics/auth assumptions or unused copied code remain.
- [ ] Drawer state, identity-bound profile reads, and workspace boundary switching each have one clear owner.
- [ ] `yarn format:check`, `yarn typecheck`, and `yarn lint` pass after simplification.

## Out of scope

- Unrelated mobile-wide refactors or changing m2's auth/data-boundary architecture.
