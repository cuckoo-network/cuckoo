---
id: w8/m31/t005
title: Test coverage — credentials menu behavior and states
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m31/t003]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — the credentials
menu's list/connect/disconnect/claim actions, the focus refetch, and the
edge states — asserting real behavior and failure modes, not snapshots.

## Context

- Test conventions: `__tests__` dirs adjacent to code; existing references —
  `dashboard/src/features/git/components/__tests__/connect-github-card.test.tsx`
  and `dashboard/src/routes/__tests__/services.new.test.tsx`.
- Mock the git hooks / Apollo `gitConnections`+`repos` documents as those tests do.

## Steps

1. `GitCredentialsMenu`: renders account rows with repo counts; Disconnect fires
   `disconnectGit(installationId)`; Connect-another opens a new tab (assert
   `window.open`, not a location change); claim fallback present.
2. Edge states: unavailable (503), error, loading, disconnected — each renders
   the right affordance.
3. `ServiceSourcePicker` integration: focus refetch updates connections + repos;
   disconnect removes a repo group in place.

## Files

- `dashboard/src/features/git/components/__tests__/git-credentials-menu.test.tsx` (new)
- `dashboard/src/features/services/components/__tests__/` (source-picker integration, if not covered)

## Acceptance criteria

- [ ] Tests cover list/disconnect/connect-in-new-tab/claim and the four edge
      states, asserting real behavior (mutation variables, `window.open`, in-place
      repo refresh).
- [ ] Tests fail if the new-tab-connect regresses to same-tab navigation.
- [ ] `yarn test` green.

## Out of scope

- Live end-to-end verification (Closeout, t006).
