---
id: w8/m31/t006
title: Closeout — verify DoD live, sync status, move to done/
worker: worker8
status: todo
estimate: 15m
depends_on: [w8/m31/t005]
---

## Objective

When t001–t005 are complete **and the DoD actually holds on a live environment**,
close the milestone.

## Context

- DoD (milestone README): the "Credentials (N)" menu on `/services/new` lists
  connected accounts with repo counts, Open/Configure-in-GitHub, per-account
  Disconnect, Connect-another (new tab → in-place repos on focus), and claim;
  disconnect removes a repo group in place; the create form survives the connect
  flow; same control in create-Blueprint and Settings→Update Source; REST/GraphQL/
  MCP unchanged; suites green.
- Do not run this until the observable end state is real, not merely coded.

## Steps

1. Verify the DoD live (a workspace with ≥1 GitHub account; ideally exercise
   connect-another and disconnect).
2. Set every task `status: done`; mark all rows `— **DONE**`; set
   `**Status:** done` in the milestone README.
3. `/pm done w8/m31/t006` — moves `w8/m31/` → `w8/done/m31/` and checks the box in
   `.pm/w8/README.md`.

## Files

- `.pm/w8/m31/*`, `.pm/w8/README.md`

## Acceptance criteria

- [ ] DoD verified live (evidence noted).
- [ ] Milestone moved to `w8/done/m31/`; workstream README box checked; statuses
      synced across all three places.

## Out of scope

- Any further feature work.
