---
id: w8/m7/t001
title: Live-capture Render's current pricing into a render-artifact snapshot
worker: worker8
status: todo
estimate: 40m
depends_on: []
---

## Objective

Capture Render's actual current published prices across every surface bex meters or offers a tiered equivalent for, into `docs/render-artifacts/pricing.md` — the same live-evidence discipline every other `docs/render-artifacts/*.md` capture follows (e.g. `workspace-lifecycle.md`, `team-members.graphql`).

## Context

- Already verified this session (2026-07-13, via web search): compute instance types — Starter $7/mo (0.5 CPU/512MB), Standard $25/mo (1 CPU/2GB), Pro $85/mo (2 CPU/4GB), Pro Plus $175/mo (4 CPU/8GB), Pro Max $225/mo (4 CPU/16GB), Pro Ultra $450/mo (8 CPU/32GB) — matches `lego/types/tiers/tiers.yaml`'s existing compute ladder id-for-id.
- Bandwidth: $0.15/GB beyond the workspace's included allowance (dropped from $0.30/GB earlier in 2026).
- Still needed: managed Postgres tier prices (bex's `basic-256mb`/`basic-1gb` etc. in `tiers.yaml`), managed Key Value tier prices, build-minute overage rate, and workspace-plan (Hobby/Professional/Organization) included bandwidth + build-minute allowances — capture these live from render.com/pricing and render.com/docs, don't rely on secondary/SEO sources.

## Steps

1. Fetch render.com/pricing directly (a real browser session or an authenticated fetch, since a plain WebFetch of the page returned no pricing content this session — the page may be JS-rendered).
2. Cross-check compute/bandwidth figures already gathered; capture Postgres, Key Value, build-minutes, and workspace-plan figures.
3. Write `docs/render-artifacts/pricing.md` with the full price list + capture date + source URLs, following the existing render-artifacts format.

## Files

- `docs/render-artifacts/pricing.md` (new)

## Acceptance criteria

- [ ] Every tier id in `lego/types/tiers/tiers.yaml` (compute, postgres, valkey) has a corresponding captured Render $ figure.
- [ ] Bandwidth and build-minute overage rates are captured with source + date.

## Out of scope

- Computing bex's discounted price sheet — that's t002, which reads this snapshot.
