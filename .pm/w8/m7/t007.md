---
id: w8/m7/t007
title: Dashboard Usage page shows the estimated-cost figure
worker: worker8
status: todo
estimate: 45m
depends_on: [w8/m7/t005, w8/m7/t006]
---

## Objective

Surface the `estimatedCost` figure on the dashboard's existing Usage page (`w8/m3`), alongside the current quantity charts — with a clear tooltip/note that it's an estimate, not an invoice.

## Context

- `dashboard/CLAUDE.md` conventions; reuse the existing Usage feature's chart component (per `w8/m6`'s planned reuse of the same component for its multi-month view).
- Follow the reveal-once / clearly-labeled pattern other estimate-like surfaces use — this isn't a secret value, just needs to not be mistaken for a bill.

## Steps

1. Add an estimated-cost stat tile/card to the Usage page, near the existing quantity metrics.
2. Add a tooltip or inline note: "Estimate based on bex's price sheet — not an invoice."
3. Reflect per-meter breakdown if there's room (compute/bandwidth/build-minutes), or at minimum a total.

## Files

- `dashboard/src/features/usage/` (existing feature directory)

## Acceptance criteria

- [ ] Estimated cost renders correctly for a workspace with real usage.
- [ ] `yarn test`/`yarn lint` green.

## Out of scope

- Multi-month trend view for cost (that's `w8/m6`'s scope, this task only needs current-period).
