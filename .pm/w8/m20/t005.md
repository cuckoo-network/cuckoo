---
id: w8/m20/t005
title: Render parity check (refusal/error shapes consistent REST/GraphQL/MCP/UI)
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m20/t002, w8/m20/t003, w8/m20/t004]
---

## Objective

Verify the milestone's tenant-visible changes are consistent across surfaces: the quota refusal renders one coded body on REST/GraphQL/MCP (and a sane dashboard error state on `blueprints/new`/sync), disconnect semantics descriptions match actual behavior everywhere they're stated, and the quota is documented as a bex extension (Render has no grouping quota) rather than silent drift.

## Context

- `docs/ADR006-bex-api.md` error-dialect rules; extension-labeling precedent from the usage API (ADR023, "bex extension — Render has no usage API").

## Steps

1. Exercise over-quota + disconnect flows on all three API surfaces + dashboard; diff shapes.
2. Note the extension in ADR018's Blueprint row divergence list if not already covered by m19's t007 refresh.

## Files

- verification across `lego/backend/internal/apps/{rest,graphql,mcp}.go`; `docs/ADR018-render-parity.md` note

## Acceptance criteria

- [ ] Identical coded refusal across surfaces; dashboard shows a handled error, not a raw failure.
- [ ] Extension documented; drift (if found) filed as notes.

## Out of scope

- m19's broader row refresh.
