---
id: w8/m30/t005
title: Complete the queued OpenBao restore drill (run 32814333448)
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m30/t001]
---

## Objective

Land the OpenBao production restore drill dispatched during the 2026-08-25 verification (workflow `openbao-restore-drill.yml`, run `32814333448`, target/confirm `restore-bao-verify`), which was still queued behind the downed runner fleet at session end. It is the missing live-restore proof for OpenBao **and** re-proves ADR050 Tier A decrypt custody — etcd, OpenBao, and KeyValue all encrypt to the same age recipient, and `AGE_BACKUP_PRIVATE_KEY` exists only in GitHub Actions custody.

## Context

- The run may be stale by the time runners return; cancelling (`gh run cancel 32814333448`) and re-dispatching (`gh workflow run openbao-restore-drill.yml -f target_namespace=restore-bao-verify -f confirm=restore-bao-verify`) is equivalent.
- The workflow writes a disposable `tenants/` marker, takes a fresh snapshot, force-restores into the throwaway namespace, unseals with the original Shamir keys, verifies, and tears down — see the 2026-07-31 precedent in ADR031.
- t001 ordering: not strictly required, but the same runner recovery unblocks both; sequence them so one runner-health confirmation serves both.

## Progress 2026-08-25

Run `32814333448` completed **failure** once the runner fleet recovered: the step env predated ADR050 Tier A encryption and never passed `AGE_BACKUP_PRIVATE_KEY`, so the restore died at the decrypt step. Fixed in the working tree (`.github/workflows/openbao-restore-drill.yml` env + a derived drift guard, check 4b in `scripts/github-actions-validate.sh`, red/green cases in its self-test). The failed run left no residue: the marker cleanup `trap` ran, no `restore-*` namespace was created, and the fresh encrypted snapshot it minted was uploaded. Re-dispatch is ship-gated — `workflow_dispatch` executes main's definition.

## Steps

1. After `/ship` lands the workflow fix, re-dispatch: `gh workflow run openbao-restore-drill.yml -f target_namespace=restore-bao-verify -f confirm=restore-bao-verify`; watch to green.
2. Confirm zero `bex.co/restore-target=true` namespaces remain and the source StatefulSet is 3/3.
3. Update ADR031: OpenBao + etcd "last verified" evidence (the shared-recipient decrypt proof covers etcd's `.age` objects) and append to the 2026-08-25 drill record.

## Files

- `docs/ADR031-platform-data-backup.md`
- `docs/drills/2026-08-25-backup-verification.md` (addendum)

## Acceptance criteria

- [ ] Workflow run green; teardown verified; sources healthy
- [ ] ADR031 + drill record updated with the run id and outcome

## Out of scope

- KeyValue restore drill (t006)
