---
id: w8/m30/t008
title: Test coverage — meaningful tests for the shipped behavior
worker: worker8
status: todo
estimate: 45m
depends_on: [w8/m30/t001, w8/m30/t003, w8/m30/t004]
---

## Objective

Ensure the milestone's behavior is pinned by real tests: cross-UID ciphertext readability (landed with t001 — verify it survived simplify), the purge guard's both-directions behavior (migration delete preserves the prefix; normal delete purges), and the Prometheus rule/routing changes covered by `deploy/gitops/base/rules/alerts_test.yml`.

## Context

- Existing patterns: `lego/operator/cmd/backup-encrypt/main_test.go`, `keyvalue_backup_test.go` / `keyvalue_backup_envtest_test.go`, `deploy/gitops/base/rules/alerts_test.yml` (promtool-style unit tests — see the existing `BackupCronJobStale` age-math cases).
- Tests must assert behavior and failure modes — no tautological or snapshot-everything tests.

## Steps

1. Audit each shipped change for a test that would catch its regression; add the missing ones.
2. Run the relevant suites (`go test ./...` in `lego/operator`, the alerts rule tests).

## Files

- `lego/operator/cmd/backup-encrypt/main_test.go`
- `lego/operator/internal/controller/keyvalue_backup_test.go`
- `deploy/gitops/base/rules/alerts_test.yml`

## Acceptance criteria

- [ ] Each milestone behavior has a failing-before/passing-after test
- [ ] All touched suites green

## Out of scope

- Coverage for unrelated operator paths
