---
id: w8/m30/t001
title: Ship the backup-encrypt 0644 fix and roll the operator
worker: worker8
status: todo
estimate: 30m
depends_on: []
---

## Objective

Land the already-written fix — `lego/operator/cmd/backup-encrypt/main.go` writes its age ciphertext `0644` instead of `0600`, plus the cross-UID readability regression test — and roll the production operator so the kvbak CronJob templates pick up the fixed image digest via `internal/selfimage`.

## Context

- Root cause: the w7/m85 `/backup-encrypt` stage wrote `0600` output owned by the operator image's non-root UID; every kvbak container drops ALL capabilities (including `DAC_OVERRIDE`), so the `amazon/aws-cli` upload stage cannot read it — AWS CLI skips the file and exits 2. Every scheduled kvbak Job has failed since 2026-08-23.
- The fix sits in the working tree since the 2026-08-25 verification session (`main.go` + `main_test.go`, `go test ./cmd/backup-encrypt/...` green), alongside the drill record, ADR031 updates, and this board change.
- **Gates:** the self-hosted runner fleet was down (all Actions jobs queued since 2026-08-25T05:01Z) — CI must be back first; and shipping requires the user-run `/ship` (repo rule: never commit/push otherwise).

## Steps

1. Confirm the runner fleet is processing jobs again (`gh run list` shows movement).
2. Have the operator run `/ship` with the pending working-tree changes; watch `deploy.yml` to green.
3. Confirm the operator Deployment rolled to the new image and each `kvbak-*` CronJob's `encrypt` container now references the new digest (selfimage keeps them in lockstep).

## Files

- `lego/operator/cmd/backup-encrypt/main.go` (already changed)
- `lego/operator/cmd/backup-encrypt/main_test.go` (already changed)

## Acceptance criteria

- [ ] `deploy.yml` green; operator rolled to the fixed digest in `bex-system`
- [ ] All three `kvbak-*` CronJob templates reference the rolled digest
- [ ] A manually triggered kvbak Job (one instance) completes and uploads a fresh `.rdb.gz.age` object

## Out of scope

- Nightly-cadence verification across all instances (t002)
- Purge semantics and alert routing (t003, t004)
