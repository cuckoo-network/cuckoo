---
id: w8/m33/t004
title: Re-baseline ADR018 + the CLI checklist with dated evidence; file new gaps
worker: worker8
status: todo
estimate: 45m
depends_on: [w8/m33/t001, w8/m33/t002, w8/m33/t003]
---

## Objective

Fold the three evidence streams (OpenAPI diff, changelog/MCP sweep, CLI walk at the new pin) into `docs/ADR018-render-parity.md` and `docs/cli-compatibility-checklist.md` as a dated "round 3" re-baseline block, and file every newly surfaced gap as an owned board item so nothing is silently accepted.

## Context

- Follow the w2/m79 precedent: a dated re-baseline paragraph in ADR018, per-row updates only where evidence changed, and a CLI-pin move note in the checklist header.
- `.pm/DO_NOT_DO.md`: do not reopen ADR018 `—` rows (deliberate non-goals) without an explicit user re-open; findings matching `FUTURE-MAYBE.md` entries get a re-verified stamp on the entry, not new board work.
- Gap filing follows the ledger convention: "nothing here is filed twice; new notes were opened only where no owner existed."

## Steps

1. Write the dated re-baseline block in ADR018 summarizing all three streams; update/add rows where evidence changed.
2. Update `docs/cli-compatibility-checklist.md`: pin version, re-run evidence, re-graded conditional bullets.
3. For each genuinely new gap: file a `.pm` inbox note (sub-hour) or flag it for a milestone proposal, and cross-reference it from the ledger row.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`
- `docs/cli-compatibility-checklist.md`
- `.pm/wN/NNN.md` notes for new gaps (numbering per board convention)

## Acceptance criteria

- [ ] ADR018 carries a dated round-3 re-baseline block; every touched row cites its new evidence.
- [ ] The checklist records the v2.26.0 pin, the verify run, and re-graded conditional bullets.
- [ ] Every new gap has an owned board item cross-referenced from the ledger; no `—` row was reopened.

## Out of scope

- Implementing any filed gap.
