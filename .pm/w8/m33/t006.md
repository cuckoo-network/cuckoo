---
id: w8/m33/t006
title: "Test coverage: meaningful tests for the behavior this milestone shipped"
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m33/t004]
---

## Objective

Add meaningful tests for what this milestone shipped: the launcher's version/branding seams at the new pin (e.g. the `isRootVersionRequest` mirror, help-template overlay, command-collision guard) and any compat-walk assertion worth pinning. Tests must assert real behavior and failure modes — no tautological or snapshot-everything tests.

## Context

- Existing guards: `lego/cli/main_test.go`, `scripts/bex-cli-validate.sh`, the CI comparison of `UPSTREAM_RENDER_CLI.md` vs `go.mod`.

## Steps

1. Review which pin-sensitive seams gained or changed behavior at v2.26.0.
2. Strengthen or add tests where a regression would be silent (overlay drift, new upstream command collision).
3. `cd lego/cli && go test ./...` green.

## Files

- `lego/cli/main_test.go`, `lego/cli/internal/branding/` tests

## Acceptance criteria

- [ ] New/strengthened tests fail if the seam they guard drifts (spot-verified by mutation or reasoning recorded in the task).
- [ ] Full `lego/cli` suite green.

## Out of scope

- Backend test additions (no backend behavior shipped here).
