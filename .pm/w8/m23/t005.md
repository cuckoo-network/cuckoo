---
id: w8/m23/t005
title: Test coverage (ownership stamping, conflict refusal, takeover, disconnect)
worker: worker8
status: todo
estimate: 45m
depends_on: [w8/m23/t003]
---

## Objective

Integration coverage on the real Postgres + OpenFGA harness: create stamps ownership; adopt stamps; second blueprint's preview/create/sync all report the coded conflict pre-write; takeover transfers ownership exactly once and proceeds; webhook auto-sync conflicts land as `error` sync runs, never takeover; disconnect clears markers and a later blueprint adopts freely; dashboard PATCH preserves the marker. Adapter-parity table for the conflict body.

## Steps

1. Two-blueprint fixture scenario driving all paths.
2. Adapter-parity test (REST/GraphQL/MCP conflict body).
3. `cd lego/backend && go test ./...` green.

## Files

- `lego/backend/internal/apps/blueprint_*_test.go`
- `lego/backend/internal/store/` tests

## Acceptance criteria

- [ ] All seven behaviors asserted; suite green locally and in CI.

## Out of scope

- Concurrency stress (two simultaneous takeovers) beyond a basic last-write check.
