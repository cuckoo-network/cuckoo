---
id: w8/m32/t008
title: Closeout — land when the live DoD holds
worker: worker8
status: todo
estimate: 10m
depends_on: [w8/m32/t007]
---

## Objective

Close the milestone once every task is done **and** the definition of done is actually met on live surfaces — not merely when the code is written.

## Context

- DoD lives in `.pm/w8/m32/README.md`. The two live checks that must pass before closing:
  - dev-8 API probe: `type: static_site` + image is rejected 4xx across REST/GraphQL/MCP, and the CRD refuses it.
  - `dashboard.bex.co` (once deployed) `/services/new?type=static_site` shows only GitHub + Public Git URL; the four other types still show "Existing Image"; the Update Source card refuses image for a static site.

## Steps

1. Verify each DoD bullet against the live surface (dev-8 for the API/CRD probe; dashboard for the wizard/card once deployed).
2. Only then: set every remaining task `status: done`, move each `tNNN.md` to `w8/m32/done/`, mark rows `— DONE` and `**Status:** done` in the milestone README, move `w8/m32/` to `w8/done/m32/`, and check `- [x]` in `.pm/w8/README.md`.
3. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w8/m32/**`, `.pm/w8/README.md`

## Acceptance criteria

- [ ] Every DoD bullet observed true on its live surface before the move.
- [ ] Milestone directory lives at `w8/done/m32/`; workstream README box checked.

## Out of scope

- Any new implementation work (would be a new task before this one).
