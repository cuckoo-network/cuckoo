---
id: w8/m32/t006
title: Simplify — the create/source-picker code this milestone changed
worker: worker8
status: done
estimate: 20m
depends_on: [w8/m32/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse, simplification, efficiency; behavior-preserving) — the create wizard, the source picker prop gating, the submit/payload builder, the settings source card, and the backend/CRD guard.

## Context

- Touched: `dashboard/src/routes/services.new.tsx`, `dashboard/src/features/services/components/service-source-picker.tsx`, `dashboard/src/features/services/lib/create-service-input.ts`, `dashboard/src/features/services/components/service-source-card.tsx`, `lego/backend/internal/apps/service.go`, `lego/types/v1alpha1/app_types.go`.
- Both the create wizard and the source card now gate `image` by service type — look for a single shared helper (e.g. `imageSourceAllowed(serviceType)`) instead of two inline conditionals.

## Steps

1. `/simplify` the diff.
2. Prefer one shared "is image source allowed for this type" predicate reused by both UI callers and mirrored by the backend guard, so the allowlist lives in one place.

## Files

- The files listed in Context.

## Acceptance criteria

- [x] No behavior change; the static-vs-image rule is expressed once and reused.
- [x] `make lint` and dashboard lint pass.

## Out of scope

- Any behavior change (covered by t001–t004).
