---
id: w8/m29/t004
title: Document branding overlay + pin-bump re-diff checklist; name residual runtime Render copy
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m29/t002, w8/m29/t003]
---

## Objective

Update user-facing and maintainer docs so the branding overlay is honest: what Bex now owns, what still says Render and why, and what to re-check on every upstream pin bump.

## Context

- `docs/bex-cli.md` § Compatibility and branding limits currently says help/TUI/User-Agent retain Render and points fork to a future decision.
- `lego/cli/UPSTREAM_RENDER_CLI.md` pin checklist covers version intercept + login-view `RepoURL`; add overlay re-diff items.
- `lego/cli/README.md` may need a one-line branding note.
- Residuals to name explicitly (accepted this milestone): login TUI "Render Dashboard"; `config.ErrLogin` / `run \`render login\``; logout RunE copy; User-Agent `render-cli/`; OAuth client id; `~/.render/skills.yaml` + render-oss/skills; login update banner via const `cfg.RepoURL` (pin-bump mitigation; do not reopen withdrawn PR).

## Steps

1. Rewrite `docs/bex-cli.md` branding section for Layer-1 overlay reality + residual list.
2. Extend `UPSTREAM_RENDER_CLI.md` update procedure: re-diff RootCmd Use/help template/`cliVersion`/docs override after each pin; confirm no new command name collides; confirm Example/Short patterns still covered by the replacer.
3. Align `lego/cli/README.md` if it still over-promises or under-documents.
4. Optionally note `docs/cli-compatibility-checklist.md` `docs` row: launcher now opens Bex docs (checklist's unmodified-CLI grade for `render docs` can stay as historical oracle — clarify rather than conflate).
5. Run `npx prettier@3.4.2 --write` on touched markdown.

## Files

- `docs/bex-cli.md`
- `lego/cli/UPSTREAM_RENDER_CLI.md`
- `lego/cli/README.md`
- maybe `docs/cli-compatibility-checklist.md` (clarifying note only)

## Acceptance criteria

- [ ] Docs state overlay is intentional and not a fork.
- [ ] Residuals are named; fork / upstream-RepoURL reopen called out as explicit future decisions.
- [ ] Pin-bump checklist includes branding overlay re-diff.

## Out of scope

- Implementing residual fixes.
- Reopening the withdrawn upstream PR.
