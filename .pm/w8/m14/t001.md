---
id: w8/m14/t001
title: Capture Render's trigger/step/cap semantics
worker: worker8
status: todo
estimate: 30m
depends_on: []
---

## Objective

Pin the loop constants before building: at what usage % does Render grow, by how much (fixed step? percentage?), is there a max, how is the user notified, and can autoscaling shrink (expect no — PVCs can't)? Record in `docs/render-artifacts/postgres-disk-autoscaling.md` (render.com/docs postgres pages + OpenAPI descriptions).

## Acceptance criteria

- [ ] Capture pins threshold/step/cap/notification; names divergences (e.g. if Render's constants aren't public, bex picks documented ones — recorded, not guessed silently).
