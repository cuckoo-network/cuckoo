---
id: w8/m14/t002
title: Operator: usage-watch → grow `storageGB` (cap + events)
worker: worker8
status: todo
estimate: 60m
depends_on: [w8/m14/t001]
---

## Objective

When `spec.diskAutoscaling` is enabled, the Database reconciler compares PVC usage vs capacity (kubelet volume stats via the metrics pipeline, or CNPG status — pick the source that works store-less and justify) and bumps `spec.storageGB` by the captured step when past the threshold, bounded by the cap; each resize emits a k8s Event + status entry. Anti-flap: one growth per reconcile window; growth only (never shrink).

## Context

- `lego/operator/internal/controller/database_controller.go`; `spec.storageGB` grow-only contract already enforced (`database_types.go:40`).
- CNPG expands PVCs on spec storage change (verify the deployed CNPG's resize path on the mock cluster — same verify-first rule as w8/m12/t002).

## Files

- `lego/operator/internal/controller/database_controller.go`, `lego/types/v1alpha1/database_types.go` (+ codegen)

## Acceptance criteria

- [ ] Envtest: past-threshold ⇒ one bounded bump + Event; below ⇒ none; disabled ⇒ never; cap respected.
