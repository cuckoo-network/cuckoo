---
id: w8/m12/t003
title: Operator: version bump → CNPG major upgrade, status surfaced
worker: worker8
status: todo
estimate: 60m
depends_on: [w8/m12/t002]
---

## Objective

The Database reconciler maps a `spec.version` change to the CNPG image change that triggers the declarative major upgrade, and surfaces the upgrade's progress on Database status (upgrading → ready, failure reason on failure) — never reporting ready mid-upgrade.

## Files

- `lego/operator/internal/controller/database_controller.go` (+ envtests)

## Acceptance criteria

- [ ] Envtest: version bump patches the CNPG Cluster image as CNPG's upgrade path requires; status transitions asserted.

## Out of scope

- Backend verb (t004).
