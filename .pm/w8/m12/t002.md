---
id: w8/m12/t002
title: Confirm deployed CNPG supports declarative major upgrades
worker: worker8
status: todo
estimate: 30m
depends_on: []
---

## Objective

Verify the CNPG version pinned in `deploy/gitops/` supports declarative major upgrades (offline `pg_upgrade` on imageName major bump) and prove it once by hand on the mock cluster: bump a test Cluster's image major, watch the upgrade complete, data intact.

## Context

- If the deployed CNPG predates the feature, STOP and re-scope: the milestone's first real task becomes upgrading CNPG in gitops (its own risk), and the board should say so — do not build the verb against a mechanism that isn't there.

## Files

- `deploy/gitops/` (CNPG version pin — read-only for this task)

## Acceptance criteria

- [ ] A hand-driven major upgrade of a throwaway CNPG cluster succeeds on the mock cluster, or a re-scope note lands on the milestone README.
