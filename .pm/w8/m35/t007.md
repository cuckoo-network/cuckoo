---
id: w8/m35/t007
title: "Test coverage: meaningful tests for the behavior this milestone shipped"
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m35/t005]
---

## Objective

Audit t002–t004's tests against the DoD's failure modes and fill gaps: not-found shape byte-identical to absent-id (so no oracle can distinguish deleting from absent), list-omission unchanged, write-verb guards untouched, and the timing edge (read racing the `DeletionTimestamp` write). Tests must assert real behavior — no tautological coverage.

## Context

- m81's test suite is the reference for which failure modes mattered on the service side (its incident was a by-id/list split surviving unnoticed).

## Steps

1. Map DoD clauses to existing assertions; list uncovered edges.
2. Add the missing tests; mutation spot-check that each fails when its guarded behavior breaks.
3. Backend `go test ./...` green.

## Files

- `lego/backend/internal/postgres/` + `keyvalue/` tests; surface adapter tests

## Acceptance criteria

- [ ] Every DoD clause has a failing-on-regression test.
- [ ] Full backend suite green.

## Out of scope

- Service-family (apps) coverage — owned by m81.
