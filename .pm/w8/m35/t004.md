---
id: w8/m35/t004
title: Cross-surface tests + finalizer-bound story + ADR closure
worker: worker8
status: todo
estimate: 45m
depends_on: [w8/m35/t002, w8/m35/t003]
---

## Objective

Prove the contract end to end and close the paper trail: REST/GraphQL/MCP all answer not-found the instant deletion starts for both families; the datastore finalizer window is confirmed bounded/observable (or a gap is filed); ADR006/ADR009/ADR021 and the ADR018 rows record the decision.

## Context

- m81's service-side fixtures are the template (its cross-surface deleting-read tests); GraphQL and MCP call the same family cores, so surface tests mostly guard adapter wiring.
- Finalizer story: `w6/m129`'s `terminating` reconciliation covers all three families for quota; check whether a stuck Postgres/KeyValue finalizer (CNPG teardown + Barman purge Job, Valkey RDB purge) has an m81-style observable bound (`DeletionStalled`-equivalent). Consistent-absence without a bound risks a silently hung invisible deletion — if no bound exists, file the gap as owned follow-up work rather than widening this milestone.
- Docs owed: ADR006 (§ Reads while a deletion finalizes — extend to datastores), ADR009 + ADR021 (delete/finalizer sections), ADR018 (Postgres CRUD + Key Value rows; note the documented-evidence boundary from t001).

## Steps

1. Add cross-surface deleting-read fixtures for both families (REST, GraphQL, MCP; list-omission unchanged).
2. Trace the datastore finalizer paths; record the bound or file the follow-up.
3. Update the four docs; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- Backend surface tests (families + adapters)
- `docs/ADR006-bex-api.md`, `docs/ADR009-postgresql-management.md`, `docs/ADR021-keyvalue-management.md`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Cross-surface tests prove not-found-on-deleting for both families on all three surfaces.
- [ ] Finalizer bound confirmed in writing, or a concrete follow-up filed.
- [ ] All four docs updated with the decision and evidence boundary.

## Out of scope

- Building a new operator status channel (FUTURE-MAYBE's durable operator-event entry).
