---
id: w8/m35/t003
title: Key Value by-id reads gain core.NotFoundIfDeleting
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m35/t001]
---

## Objective

Same gate, second family: every by-id read verb in `internal/keyvalue/` calls `core.NotFoundIfDeleting` after `AuthorizeApp`, so a deleting `KeyValue` answers `not found` on get, connection-info, and sibling by-id reads across REST, GraphQL, and MCP.

## Context

- Mirror t002's pattern exactly (same gate, same placement, same safe-404 error shape); see `core/base.go:1016-1027` and the m81 call sites in `apps/service.go`.
- Inventory first: `grep -n 'AuthorizeApp' lego/backend/internal/keyvalue/*.go`.

## Steps

1. Enumerate the family's by-id read verbs; classify read vs write.
2. Insert the gate on each by-id read; keep the absent-id error shape.
3. Extend the family's unit tests with a deleting-App fixture.

## Files

- `lego/backend/internal/keyvalue/service.go` (and sibling files carrying by-id reads)
- Key Value family tests alongside

## Acceptance criteria

- [ ] Every Key Value by-id read verb answers not-found for a deleting App, byte-identical to an absent id.
- [ ] List behavior unchanged; write-verb guards untouched.

## Out of scope

- Postgres (t002); docs and cross-surface fixtures (t004).
