---
id: w8/m8/t003
title: REST PATCH + toRenderService mapping for displayName
worker: worker8
status: todo
estimate: 1h
depends_on: [w8/m8/t002]
---

## Objective

Expose `displayName` on the REST service-update and service-read surfaces.

## Context

- `lego/backend/internal/apps/rest.go:269-330` — the PATCH handler (`patchServiceRequest`) currently supports `plan`, `idleTTLSeconds`, `rootDir`, `autoDeploy`, cron `schedule`/`command`, `healthCheckPath` only.
- `toRenderService` (same package) maps CR fields to the Render-compatible response shape.

## Steps

1. Add `displayName *string` to `patchServiceRequest`; wire to `SetDisplayName` when present.
2. Add `displayName` to `toRenderService`'s output mapping.

## Files

- `lego/backend/internal/apps/rest.go`

## Acceptance criteria

- [ ] `PATCH /v1/services/{id}` with `displayName` updates it; omitting the field leaves it unchanged.
- [ ] `GET`/list responses include `displayName`.

## Out of scope

- GraphQL/MCP (t004).
