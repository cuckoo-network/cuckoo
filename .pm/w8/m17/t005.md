---
id: w8/m17/t005
title: Test coverage — meaningful tests for the shipped behavior
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m17/t002, w8/m17/t003]
---

## Objective

Standing closing task. Audit the milestone's diff for untested failure modes beyond what t002/t003 added — e.g. counter reset on success, signal debounce across manager restart (if annotation-persisted), guard tests actually failing on drift — and add meaningful tests. Never tautological coverage.

## Context

- m14's existing coverage (decision math, cooldown, fullest-PVC selection) is the bar: tests that assert real behavior and failure modes.

## Steps

1. Review the diff for untested branches.
2. Add the missing tests; run the touched suites.

## Files

- `lego/operator/internal/controller/`, `lego/backend/internal/postgres/`, `dashboard/src/features/databases/`

## Acceptance criteria

- [ ] Every new behavior branch has a test that fails if it regresses.

## Out of scope

- Coverage-number chasing.
