---
id: w8/m17/t003
title: Single-source the 16 TB cap across operator, dashboard, and MCP description
worker: worker8
status: todo
estimate: 30m
depends_on: []
---

## Objective

The 16 TB disk cap is a contract value living in four places with nothing keeping them aligned. Default approach: a guard test that pins the operator constant, the dashboard display constant, and the MCP tool-description figure to one value so drift fails the build. If a cheap API surface for the cap falls out naturally instead, that's acceptable — but then file the Render-parity follow-up the milestone README notes.

## Context

- `lego/operator/internal/controller/database_autoscale.go:38` — `databaseDiskAutoscaleCapGB = int32(16 * 1024)`.
- `dashboard/src/features/databases/components/database-disk-autoscaling-control.tsx:5` — `DISK_AUTOSCALING_CAP_GB = 16 * 1024`.
- `lego/backend/internal/postgres/mcp.go:186` — "16 TB" in the tool description string.
- `docs/render-artifacts/postgres-disk-autoscaling.md` — the pinned Render contract.
- Cross-language (Go + TS) — a guard test likely lives per side: a Go test asserting the operator/MCP values, a dashboard test (`yarn test`) asserting the TS constant, each citing the same contract doc line as the single source.

## Steps

1. Pick the single-source mechanism (shared doc-cited guard tests is the low-ceremony default).
2. Implement; make the MCP description derive from or be asserted against the constant.
3. Run `go test ./...` (backend), `make test` (operator), `yarn test` (dashboard) as touched.

## Files

- `lego/operator/internal/controller/database_autoscale.go` (+ test)
- `lego/backend/internal/postgres/mcp.go` (+ test)
- `dashboard/src/features/databases/components/database-disk-autoscaling-control.tsx` (+ test)

## Acceptance criteria

- [ ] Changing any one cap site without the others fails a test naming the other sites.

## Out of scope

- Changing the cap value itself; REST/GraphQL cap exposure (file parity follow-up if chosen).
