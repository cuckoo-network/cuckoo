---
id: w8/m34/t001
title: Rename the emitted types + all backend references
worker: worker8
status: todo
estimate: 45m
depends_on: []
---

## Objective

Rename the two divergent disk event types to Render's spellings at every backend reference: `disk_attached` → `disk_created` and `disk_detached` → `disk_deleted`. `disk_updated` already matches; `disk_restored` stays as a labeled bex extension (Render has no equivalent).

## Context

- Emit constants: `lego/backend/internal/events/service.go:230-233` (`TypeDiskAttached`/`TypeDiskRestored` block), written by the `apps.AddDisk`/`UpdateDisk`/`DeleteDisk`/`RestoreDiskSnapshot` audit verbs.
- The webhook vocabulary carries the same strings: `lego/backend/internal/webhooks/service.go`, `graphql.go`, `mcp.go`, plus `service_test.go` and `surface_contract_test.go`.
- Render's pinned enum (`lego/backend/internal/api/openapi/render-public-api-1.json`, `eventTypeParam`, 39 values) already contains `disk_created`/`disk_updated`/`disk_deleted` — no validator change is needed or allowed.
- New rows are written with the new spellings from this task on; old-row/subscription compatibility is t002's job, so land the two tasks together (this task alone would briefly strand old-spelling subscriptions if shipped in isolation — do not ship before t002).

## Steps

1. Rename the constants and their string values in `events/service.go`; chase every compile-time reference (`grep -rn 'disk_attached\|disk_detached' lego/`).
2. Update the webhook advertised-vocabulary lists on all three surfaces to the new spellings.
3. Update backend tests that assert the old spellings (`events_surface_test.go` here only as far as compilation; its parity assertions flip in t004).

## Files

- `lego/backend/internal/events/service.go`
- `lego/backend/internal/webhooks/service.go`, `graphql.go`, `mcp.go`, `service_test.go`, `surface_contract_test.go`
- `lego/backend/internal/api/events_surface_test.go` (compile-level updates)

## Acceptance criteria

- [ ] `grep -rn 'disk_attached\|disk_detached' lego/backend/internal --include='*.go'` returns only deliberate compatibility-alias references (t002) or nothing.
- [ ] New disk add/delete verbs emit `disk_created`/`disk_deleted`; `disk_updated`/`disk_restored` unchanged.
- [ ] Backend compiles; suites that don't depend on t002's compatibility layer pass.

## Out of scope

- Historical-row aliasing/migration and subscription-filter compatibility (t002).
- Dashboard catalog (t003), docs and the parity-positive guard (t004).
