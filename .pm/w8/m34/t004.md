---
id: w8/m34/t004
title: Parity-positive filter guard + docs closure
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m34/t002, w8/m34/t003]
---

## Objective

Flip the drift record into a parity guard and close the paper trail: `TestServiceEventSurfaceCarriesDriftedTypes` becomes a test asserting `?type=disk_created` and `?type=disk_deleted` return 200 and narrow correctly, and the docs stop describing the divergence as open.

## Context

- The drift test: `lego/backend/internal/api/events_surface_test.go` (currently proves `disk_attached` → 400 against `renderRequestValidator` at `render_openapi.go:357`).
- Docs owed: `docs/render-artifacts/service-events.md:110` (names `w6/068` as the open decision — record the resolution), the ADR018 service-events row (the "bex-named configuration events" ◐ note no longer covers the disk family), and the ADR018 gap-backlog table if a row references this.
- `w6/068` was retired to `w6/done/068.md` pointing at this milestone when it was materialized — no further note bookkeeping needed here.

## Steps

1. Rewrite the surface test: renamed types filter correctly end to end (emit → store → REST `?type=` → narrowed result), and keep the negative half for genuinely bex-named types (e.g. `disk_restored` still 400 through the Render-shaped parameter, documented).
2. Update `service-events.md` and the ADR018 events row with the closure + dated evidence.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `lego/backend/internal/api/events_surface_test.go`
- `docs/render-artifacts/service-events.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Guard asserts 200-and-narrows for `disk_created`/`disk_deleted` and would fail if either regressed to a bex-only spelling.
- [ ] `disk_restored`'s deliberate extension status (unfilterable via the Render enum) is asserted and documented, not silent.
- [ ] Docs record the closure with dates.

## Out of scope

- Widening `renderRequestValidator` (rejected option 3 of `w6/068`).
