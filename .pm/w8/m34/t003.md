---
id: w8/m34/t003
title: "Dashboard catalog: labels, icons, locales, vocabulary test"
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m34/t001]
---

## Objective

Move the dashboard's event vocabulary to the new spellings so the Events tab, filters, and webhook event pickers label `disk_created`/`disk_deleted` correctly — including rows that were stored under the old spellings if t002 chose aliasing over migration.

## Context

- Catalog + labels: `dashboard/src/features/events/service-event-catalog.ts` (and its `LABEL_KEYS`), icons: `dashboard/src/features/events/components/event-icon.tsx`, vocabulary drift guard: `dashboard/src/features/events/__tests__/backend-vocabulary.test.ts`.
- Both locales carry the label strings — keep them in sync (dashboard i18n convention, `dashboard/CLAUDE.md`).

## Steps

1. Rename the catalog keys/labels/icons; update both locales.
2. Update the backend-vocabulary test to the new advertised set (it exists to catch exactly this drift — make it pass by matching the backend, not by loosening it).
3. If t002 chose aliasing, ensure the catalog resolves old-spelling rows to the same entry; if migration, no alias entries.
4. `cd dashboard && yarn test && yarn typecheck && yarn lint`.

## Files

- `dashboard/src/features/events/service-event-catalog.ts`
- `dashboard/src/features/events/components/event-icon.tsx`
- `dashboard/src/features/events/__tests__/backend-vocabulary.test.ts`
- Locale message files carrying the disk event labels

## Acceptance criteria

- [ ] Events tab and webhook picker show correctly labeled `disk_created`/`disk_deleted` with the right icons in both locales.
- [ ] Vocabulary test asserts the new set; dashboard suite green.

## Out of scope

- Backend changes; docs (t004).
