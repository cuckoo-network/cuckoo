# 010 — Dashboard outbound-IPs affordance on the service page

Why: the outbound-IPs row shipped REST/GraphQL/MCP (`w2/023`, ADR018 2026-08-19) but deliberately left the dashboard cell ✖ "pending product confirmation" — that confirmation landed 2026-09-07 (`/pm for w8` #4), so a small service-page card reading `Service.outboundIps` closes the row's UI cell.

- Render shows outbound IPs in its service **Connect** panel; bex's equivalent is a card in the service detail's connect/networking region (`dashboard/src/features/services/`).
- Data is already on GraphQL: `Service.outboundIps` (`{type, ips, dedicatedIpId?}`; bex always `type: "shared"`, `dedicatedIpId` never present). Render truthfully: these are the tenant pool's current node ExternalIPs and move with the autoscaler — say so in the card's helper text; an empty list (local CAPD) renders honestly, not as an error.
- Copy-per-IP buttons per the connection-info card conventions; both locales.
- Close the loop: flip the ADR018 outbound-IPs row's UI note (✖ → ✅) when this ships.

← from `/pm-brainstorm for w8` 2026-09-07 #4, approved same day. Sub-hour, so an inbox note per the sizing rule.
