---
id: w8/m3/t001
title: Capture Render's billing/usage page live (Playwright) as the layout reference
worker: worker8
status: todo
estimate: 30m
depends_on: []
---

## Objective

Capture the usage section of Render's live billing page (dashboard.render.com → workspace Billing) with Playwright as the design reference for the bex Usage page — the same "learn the design live" step w3/m3 and w5/m7 used before building their pages.

## Context

- Playwright MCP writes to `.playwright-mcp/` — pass **bare** filenames (e.g. `render-billing-usage.png`), never repo-root paths (repo rule).
- Capture: month-to-date compute (per-service breakdown if shown), bandwidth against included allotment, pipeline minutes against included allotment, period selector if any. The payment/invoice section is explicitly out of scope.
- Note the vocabulary and units Render displays (e.g. "pipeline minutes", GB) — m3 renders build time in minutes for this reason.

## Steps

1. Log into dashboard.render.com and screenshot the billing page's usage widgets.
2. Write a short findings note (layout regions, fields, units) into this task before closing it.

## Files

- `.playwright-mcp/render-billing-usage*.png` (gitignored evidence)

## Acceptance criteria

- [ ] Screenshots captured to `.playwright-mcp/`; findings (regions, fields, units) recorded in this task.

## Out of scope

- Payment methods, invoices, plan-upgrade widgets.
