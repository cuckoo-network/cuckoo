---
id: w8/m2/t009
title: Closeout — verify DoD, mark done, move milestone to done/
worker: worker8
status: todo
estimate: 15m
depends_on: [w8/m2/t008]
---

## Objective

Close w8/m2 once the DoD actually holds — three surfaces returning identical month-to-date quantities, denial enforced, docs + parity ledger updated — not merely when the code is written.

## Context

- Standing closing task; completing it _is_ the move (`/pm done w8/m2/t009` triggers the milestone move).

## Steps

1. Re-verify the DoD (live query of all three adapters).
2. Set remaining task frontmatter `status: done`; move each `tNNN.md` to `w8/m2/done/`.
3. Mark rows `— **DONE**`, set `**Status:** done` in `w8/m2/README.md`.
4. Move `w8/m2/` to `w8/done/m2/`; check `- [x]` for m2 in `w8/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w8/m2/`, `.pm/w8/README.md`

## Acceptance criteria

- [ ] DoD verified true before any move; board state consistent in all three places.

## Out of scope

- m3 kickoff.
