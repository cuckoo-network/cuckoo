# Key Value (Valkey) version upgrade — assessment

`KeyValue.spec.version` exists create-time only (`keyvalue_types.go:36`), same shape as the Postgres gap `w8/m12` is closing. Before proposing the mirror milestone: assess whether Render supports Redis/Key Value version changes at all (OpenAPI + docs — no evidence either way as of 2026-07-14). If yes, mirror the m12 pattern (verb + operator StatefulSet image bump + guards); if no, record the finding here and close — bex matching Render's absence is also parity. Sub-hour assessment.

Source: `/pm-brainstorm more milestones for each worker` round 3, 2026-07-14 (item 4).
