---
id: w8/m7/t006
title: "MCP — get_usage tool gains estimatedCost"
worker: worker8
status: done
estimate: 20m
depends_on: [w8/m7/t004]
---

## Objective

Mirror t004's `estimatedCost` field on the MCP `get_usage` tool, so an agent asking "what's my spend this month" gets a real dollar estimate, not just raw quantities.

## Context

- `get_usage` already exists (`docs/ADR023-usage-metering.md`'s "bex ahead of Render" — Render has no usage API at all, REST/GraphQL/MCP/billing).

## Steps

1. Add `estimatedCost` to the `get_usage` tool's result shape.

## Files

- `lego/backend/internal/usage/mcp.go`

## Acceptance criteria

- [ ] Tool result includes the same estimate shape as REST/GraphQL.

## Out of scope

- Dashboard — t007.
