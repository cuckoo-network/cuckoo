---
id: w8/m7/t009
title: Render parity — estimatedCost consistency across REST/GraphQL/MCP/UI
worker: worker8
status: done
estimate: 20m
depends_on: [w8/m7/t008]
---

## Objective

Standing closing task: verify the new `estimatedCost` field is consistent across REST, GraphQL, MCP, and the dashboard (same computation, same rounding, same currency) — a bex-only superset since Render has no equivalent estimate surface to diff against.

## Context

- `docs/ADR006-bex-api.md` — "one Core, three adapters" is the standard.
- `docs/ADR023-usage-metering.md`'s "bex ahead of Render" section already documents `get_usage`/usage API as a bex superset — add this estimate field to that same section.

## Steps

1. Diff the `estimatedCost` shape across all four surfaces.
2. Update `docs/ADR023-usage-metering.md`'s "bex ahead of Render" note to mention the estimate field.

## Files

- `docs/ADR023-usage-metering.md`

## Acceptance criteria

- [ ] All four surfaces agree on shape and value.
- [ ] ADR023 updated.

## Out of scope

- Any Render-side comparison (there is none to compare against).
