---
id: w8/m7/t011
title: Test coverage
worker: worker8
status: done
estimate: 40m
depends_on: [w8/m7/t009]
---

## Objective

Standing closing task: add meaningful tests for the price-sheet lookup and estimate computation — real behavior and edge cases, not tautological or snapshot-everything tests.

## Context

- Precedent: `lego/types/tiers/tiers_test.go`'s style for tier-catalog tests.

## Steps

1. Price-sheet lookup: known tier id → known price; unknown tier id → explicit error/zero (not a panic).
2. Estimate computation: zero usage → 0; a representative non-zero usage row → correct $ figure; rounding behavior at a boundary value.
3. REST/GraphQL/MCP: each surface returns the same estimate for the same underlying usage data.

## Files

- `lego/backend/internal/pricing/*_test.go`
- `lego/backend/internal/usage/*_test.go`

## Acceptance criteria

- [ ] Tests assert real computed values, not just "no error thrown."
- [ ] `go test ./...` green.

## Out of scope

- Dashboard component tests beyond what t007 already requires for `yarn test`.
