---
id: w8/m7/t005
title: "GraphQL — usage query gains estimatedCost"
worker: worker8
status: done
estimate: 20m
depends_on: [w8/m7/t004]
---

## Objective

Mirror t004's `estimatedCost` field on the GraphQL `usage` query, delegating to the same Core computation (per `docs/ADR006-bex-api.md`'s "one Core, three adapters").

## Context

- GraphQL resolver lives alongside the REST handler in `lego/backend/internal/usage/`.
- Regenerate any dashboard-consumed codegen artifacts.

## Steps

1. Add `estimatedCost` to the GraphQL `usage` type/query.
2. Regenerate codegen (`dashboard/src/graphql/definitions.ts` etc.) and confirm no drift.

## Files

- `lego/backend/internal/usage/graphql.go`
- dashboard codegen outputs

## Acceptance criteria

- [ ] Same computation and field shape as REST, just GraphQL-native naming.

## Out of scope

- MCP — t006.
