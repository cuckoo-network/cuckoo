---
id: w8/m30/t006
title: KeyValue live restore re-drill (ADR031 cadence trigger)
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m30/t002]
---

## Objective

Re-earn the paid-KeyValue row in ADR031's drill ledger: a scripted `scripts/restore-keyvalue.sh` recovery from a **post-fix** nightly snapshot, proving decrypt-then-restore end to end. The snapshot job changed twice since the last drill (w7/m85 first-party encrypt entrypoint, then this milestone's 0644 fix) — each is an explicit ADR031 re-drill trigger.

## Context

- Requires `AGE_BACKUP_PRIVATE_KEY` custody: it lives in `.env`/GitHub Actions per ADR050 §1, and was absent from the verifying workstation's `.env` on 2026-08-25 (the only reason the restore half wasn't proven that session). Run from a machine whose `.env` holds it, or mirror the OpenBao pattern with a manual workflow if operator-laptop custody is not desired.
- Shape per the 2026-07-31 drill: known marker in a paid instance → fresh snapshot → `DRY_RUN=1` → real run with `--confirm restore-kv-<name> --teardown-on-success` → marker verified with AOF off then on.
- Use a real paid instance's snapshot with a pre-agreed non-sensitive marker key, or a throwaway paid instance as in 2026-07-31.

## Steps

1. Confirm a post-fix nightly `.rdb.gz.age` object exists for the chosen instance.
2. `DRY_RUN=1 scripts/restore-keyvalue.sh --id <red-id> --target-namespace restore-kv-redrill --verify-key <marker> --expect <value>`.
3. Real run with `--confirm restore-kv-redrill --teardown-on-success`; confirm teardown.
4. Update ADR031's KeyValue "last verified" cell + re-drill cadence row and append to the 2026-08-25 drill record.

## Files

- `docs/ADR031-platform-data-backup.md`
- `docs/drills/2026-08-25-backup-verification.md` (addendum)

## Acceptance criteria

- [x] Marker verified post-restore with `aof_enabled:0` then `aof_enabled:1`
- [x] Teardown verified; no drill residue
- [x] ADR031 ledger updated

## Evidence — 2026-09-01

- CI-custodied workflow run `33467191581` restored scheduled object `2026-09-01T03:39:20Z.rdb.gz.age` into `restore-kv-m30-20260901`.
- The script reported the marker with AOF disabled, then completed AOF rewrite/restart and reported the marker with AOF enabled.
- Recovery ran `03:53:24Z`–`03:53:56Z`; teardown completed at `03:54:15Z`. The target namespace and source marker are absent and the source remains `Ready`.

## Out of scope

- Free-tier KeyValue (deliberately PVC-only per ADR031)
