---
id: w8/m26/t001
title: Add accessible inline create and Settings validation
worker: worker8
status: done
estimate: 45m
depends_on: []
---

## Objective

Replace silent disabled submission and toast-only validation with field-local, accessible feedback matching the live Render flow while preserving server authority.

## Context

- `dashboard/src/routes/webhooks_.new.tsx`
- `dashboard/src/features/webhooks/components/webhook-settings-card.tsx`
- Backend validation codes already distinguish name, conflict, URL, and event-filter failures.

## Steps

1. Validate empty/duplicate names, missing events, malformed URLs, and HTTPS before submit where possible.
2. Map coded server errors to the responsible field and retain a form summary.
3. Connect labels, descriptions, errors, focus, and disabled-reason text accessibly.

## Files

- `dashboard/src/routes/webhooks_.new.tsx`
- `dashboard/src/features/webhooks/components/webhook-settings-card.tsx`
- `dashboard/src/features/webhooks/hooks/`
- `dashboard/src/features/webhooks/locales/`

## Acceptance criteria

- [ ] Every invalid case tested in the live audit renders beside its field.
- [ ] Keyboard/screen-reader users can identify and focus the first failure.
- [ ] Server-coded conflicts remain authoritative under races.
- [ ] No signing secret or destination value is logged.

## Out of scope

- Changing the HTTPS-only policy.
