---
id: w8/m26/t004
title: Gate mutation controls by role and present coded refusals
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m26/t001, w8/m26/t002]
---

## Objective

Align mutation affordances with workspace authorization so read-only members see an honest inspection surface and admins receive actionable coded failures.

## Context

- `dashboard/src/features/webhooks/components/webhooks-panel.tsx`
- `dashboard/src/features/webhooks/components/webhook-row.tsx`
- `dashboard/src/features/webhooks/components/webhook-settings-card.tsx`
- Server authorization remains the final boundary.

## Steps

1. Resolve the existing workspace capability for webhook management.
2. Gate create, toggle, update, delete, and Resend controls consistently.
3. Render safe permission and stale-state errors without optimistic cache corruption.

## Files

- `dashboard/src/features/webhooks/components/`
- `dashboard/src/features/webhooks/hooks/`
- `dashboard/src/features/workspaces/`

## Acceptance criteria

- [ ] Admin/manage-capable users retain every mutation.
- [ ] Read-only users can list/read activity but see no active mutation affordance.
- [ ] Direct unauthorized mutation still fails server-side with the shared code.
- [ ] Permission changes invalidate or re-evaluate cached controls.

## Out of scope

- New workspace roles or authorization-model tuples.
