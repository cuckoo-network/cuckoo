---
id: w8/m27/t007
title: Simplify
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m27/t006]
---

## Objective

Run `/simplify` over m27's identity, scope-gate, audit, consent, and UI changes and apply behavior-preserving cleanup.

## Context

- The capability vocabulary must have one authoritative semantic registry even though Go, TypeScript, scripts, and docs each present it.
- Authorization, error mapping, and audit enrichment already have shared seams that should not be forked by transport.

## Steps

1. Review changed code for duplicate scope parsing, relation classification, platform-client lookup, provenance conversion, and UI labels.
2. Collapse repeated logic onto the existing shared auth, audit, GraphQL helper, and localization patterns without loosening fail-closed behavior.
3. Re-run focused backend/dashboard checks after every accepted cleanup and document any finding intentionally retained.

## Files

- `lego/backend/internal/core/`
- `lego/backend/internal/api/`
- `lego/backend/internal/audit/`
- `lego/backend/internal/store/audit.go`
- `dashboard/src/common/server-fn/hydra-consent.ts`
- `dashboard/src/features/auth/`
- `dashboard/src/features/connected-agents/`
- `dashboard/src/features/audit/`

## Acceptance criteria

- [ ] `/simplify` findings are applied or explicitly dispositioned.
- [ ] Scope semantics, error shapes, and audit safety remain unchanged.
- [ ] No adapter or UI component owns a second authorization matrix.

## Out of scope

- New capabilities or user-facing behavior.
