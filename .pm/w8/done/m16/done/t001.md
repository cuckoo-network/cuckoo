---
id: w8/m16/t001
title: ipAllowList on GraphQL createDatabase + MCP create_postgres, mirroring keyvalue
worker: worker8
status: done
estimate: 40m
depends_on: []
---

## Objective

GraphQL and MCP Postgres creation accept `ipAllowList` and wire it to the same `CreatePostgresRequest.IPAllowList` the REST body feeds, exactly as keyvalue already does on all three surfaces; the stale MCP comment claiming the args "mirror the create body the REST/GraphQL surfaces accept" becomes true again.

## Context

- REST decode: `lego/backend/internal/postgres/rest.go:135-144` → `service.go:236`.
- Missing: `graphql.go:451-490` (createDatabase args), `mcp.go:44-53` (createPostgresArgs); stale comment `mcp.go:42-43`.
- Keyvalue reference: `keyvalue/{rest.go:212-219, mcp.go:60, graphql.go:149,179}`.
- Mind `w4/m24`: if it lands the `{cidrBlock, description}` object shape first, mirror that shape, not bare strings.

## Steps

1. Add the arg/field on both surfaces; validate identically to REST.
2. Fix the MCP comment + tool description.

## Files

- `lego/backend/internal/postgres/{graphql,mcp}.go`

## Acceptance criteria

- [x] Cross-surface test: the same create-with-allow-list round-trips identically via REST, GraphQL, MCP
- [x] Invalid CIDR fails with the same error shape on all three

## Out of scope

- Bad-request error bodies (w9/m38); descriptions persistence (w4/m24)
