---
id: w8/m16/t003
title: KV version-change parity assessment (either-outcome acceptance)
worker: worker8
status: done
estimate: 30m
depends_on: []
---

## Objective

Establish with evidence whether Render supports Redis/Key Value version changes at all. If yes: file the w8/m12-mirror milestone (verb + operator StatefulSet image bump + guards). If no: record parity-by-absence in ADR021/ADR018 and close — bex matching Render's absence is also parity.

## Context

- `KeyValue.spec.version` is create-time-only (`lego/types/v1alpha1/keyvalue_types.go:36`) — same shape as the Postgres gap w8/m12 closed.
- Sources: the fresh pinned OpenAPI (`/key-value`, `/redis` families), render.com docs; no evidence either way as of 2026-07-14 (`.pm/w8/003.md`).

## Steps

1. Check the spec + docs; capture evidence with dates.
2. Record the outcome (ledger note or filed milestone proposal).

## Files

- `docs/ADR021-keyvalue-management.md` and/or `docs/ADR018-render-parity.md`; `.pm/w8/` (new note/milestone if yes)

## Acceptance criteria

- [x] A dated, evidenced answer exists in the docs; the follow-up (milestone or parity-by-absence record) is filed

## Out of scope

- Implementing the upgrade verb (its own milestone if warranted)
