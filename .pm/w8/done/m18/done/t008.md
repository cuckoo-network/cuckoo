---
id: w8/m18/t008
title: Closeout
worker: worker8
status: done
estimate: 15m
depends_on: [w8/m18/t007]
---

## Objective

Close the milestone once the definition of done actually holds: the panel renders real prices for the beancount fixture on `blueprints/new`, all-free blueprints hide it, and REST/MCP carry the same object.

## Context

- `.pm` conventions: set remaining tasks `status: done`, move `tNNN.md` files to `w8/m18/done/`, mark rows `— **DONE**` + `**Status:** done` in the milestone README, move the whole `w8/m18/` to `w8/done/m18/`, check `- [x]` in `w8/README.md`.

## Verification record (2026-08-15)

Live walk on the dev-8 stack (real `bex-api` binary against the shared CAPD
cluster, real Kratos session, real GitHub fetches):

- **GraphQL `blueprintPreview`** of `render-examples/nextjs-hello-world`
  (public repo, `plan: starter`): `found=true`, real commit SHA
  (`a0f64db8…`), `estimatedPricing` = one line `(Starter) $4.90 / month`,
  total `$4.90`. Same query for `render-examples/express-hello-world`
  (`plan: free`): `found=true`, empty lines, total `$0.00` — the payload the
  dashboard hides the panel on.
- **REST `POST /v1/blueprints/preview`**: byte-identical `estimatedPricing`
  object for the paid repo.
- **MCP** (`api mcp-stdio`, real binary): `validate_bex_yml` with the
  DoD beancount manifest (standard web + standard keyvalue + basic-1gb
  database) → valid, three lines (`Standard $17.50`, `Basic 1gb $15.05`
  incl. `$1.05` storage on the 5 GB floor, `Standard $22.05`), total
  **$54.60**.
- **blueprints/new render**: route-level tests
  (`dashboard/src/routes/__tests__/blueprints-new.test.tsx`) mount the real
  `NewBlueprintPage` and assert the panel renders the exact live-captured
  payload (rows, breakdown, `$54.60 per month` total) and hides for the
  all-free payload; the panel component itself has 5 further tests. Full
  suites green: backend `go test ./...`, dashboard 2114 tests + typecheck +
  lint, `make lint-backend` 0 issues.
- A full headless-browser pass (login → blueprints/new → live preview) was
  attempted and got through login + page load, but the shared local
  cluster's apiserver/etcd became unstable mid-walk (host overload — etcd
  request timeouts, no 20s healthy window over ~40min). **Residual:** run a
  pixel-level smoke on prod `dashboard.bex.co/blueprints/new` after the next
  `/ship`. Note two live findings fixed en route: the GitHub client sent an
  empty `Authorization: token ` header on the documented anonymous
  public-repo path (401; fixed with `setTokenAuth`), and `dev-8/up.sh`
  predated the fail-closed authz/CP-token guards + needed a throwaway
  GitHub-App identity to enable public-repo previews (all fixed in place).

## Steps

1. ~~Verify the definition of done live (dev-8 stack or prod).~~ Done — see the verification record above.
2. ~~Run `/pm done w8/m18/t008` last — completing this task is the move.~~ Done.

## Files

- `.pm/w8/**`

## Acceptance criteria

- [x] Milestone directory lives at `w8/done/m18/` with all statuses in sync across task frontmatter, milestone README, and workstream README.

## Out of scope

- Any new feature work discovered during verification — file as inbox notes.
