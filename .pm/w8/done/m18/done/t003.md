---
id: w8/m18/t003
title: Expose `estimatedPricing` over GraphQL · REST · MCP preview adapters
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m18/t002]
---

## Objective

Fan the core `estimatedPricing` object out to all three adapters per the one-core-three-adapters rule (`lego/backend/CLAUDE.md`), so the dashboard, REST callers, and MCP agents see the same estimate.

## Context

- `lego/backend/internal/apps/graphql.go` — `blueprintPreview` resolver (:1024-1036) and the `BlueprintPreview`/`BlueprintValidation`/`BlueprintValidationPlan` GraphQL types (:726-799); add `estimatedPricing` with line/variable/total sub-types.
- REST + MCP preview/validate verbs in the same feature package serialize the core structs — confirm the JSON tags flow through and the MCP tool description mentions the field.
- Money as string USD (matches `EstimatedCost.TotalUSD` precedent in `internal/usage`).

## Steps

1. Add GraphQL object types + field wiring for `estimatedPricing` (nullable — absent on invalid/all-free-total-zero handled client-side by empty lines).
2. Verify REST preview/validate responses include the JSON object; adjust tags if needed.
3. Update the MCP blueprint preview tool schema/description.
4. Extend adapter-level tests (GraphQL query selects the new field; REST golden JSON).

## Files

- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/apps/rest.go` / `mcp.go` (whichever host the preview verbs)
- adapter tests alongside

## Acceptance criteria

- [ ] `blueprintPreview { validation { estimatedPricing { totalUsd lines { name kind planLabel monthlyUsd } variable { name reason } } } }` resolves.
- [ ] REST and MCP preview payloads carry the identical object.

## Out of scope

- Dashboard consumption (t004).
