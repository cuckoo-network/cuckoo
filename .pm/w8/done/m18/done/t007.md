---
id: w8/m18/t007
title: Test coverage
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m18/t005]
---

## Objective

Meaningful tests for the shipped behavior and its failure modes — beyond the inline tests each implementation task added.

## Context

- Golden fixtures: the real beancount pair (`_infra/bex/bex-beancount.yaml` shape) plus edge fixtures.
- Failure modes worth asserting: unknown tier (must not price, consistent with `rateFor` $0 policy — never a crash), storage floor fallback when `diskSizeGB` omitted, HA + replica multiplication, variable-exclusion classification, invalid blueprint ⇒ no estimate, all-free ⇒ hidden panel.

## Steps

1. Backend: table tests over `MonthlyEstimate` edge cases + `blueprintValidationFor` fixture matrix.
2. Adapters: GraphQL selection + REST JSON golden for a priced and an all-free blueprint.
3. Dashboard: panel component tests (render, hide, footnote wording, breakdown row).

## Files

- `lego/backend/internal/pricing/*_test.go`, `lego/backend/internal/apps/*_test.go`
- `dashboard/src/features/blueprints/**/*.test.tsx`

## Acceptance criteria

- [ ] All failure modes above asserted with real expected values (no tautological/snapshot-everything tests).
- [ ] `cd lego/backend && go test ./...` and `yarn test` green.

## Out of scope

- e2e cluster tests.
