---
id: w8/m18/t005
title: Render parity check (REST/GraphQL/MCP/UI vs dashboard.render.com/blueprint/new)
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m18/t004]
---

## Objective

Verify the shipped panel and payloads are consistent across every surface and against Render's actual behavior; flag drift as follow-up work rather than silently diverging.

## Context

- Reference behavior captured 2026-08-04 from Render's shipped bundles: free plans filtered; DB row = instance + tier-default storage (basic-256mb + 15 GB = $10.50); HA standby doubles; cron per-minute displayed but $0 in total; panel absent when total is 0; "Additional pricing" heading on updates.
- Deliberate divergences to document (not fix): bex prices are 30% off Render's (ADR030); no service-disk Disks group (persistent disks are a DO_NOT_DO non-goal); bex's narrower tier catalog (no KV `pro`, no Postgres `pro-*`/`accelerated-*`).
- `docs/ADR018-render-parity.md` Blueprint row — update evidence/notes.

## Steps

1. Cross-check REST vs GraphQL vs MCP preview payloads for identical `estimatedPricing` fields/semantics.
2. Compare the dashboard panel against Render's for the beancount pair of files (`_infra/render/render-beancount.yaml` vs `_infra/bex/bex-beancount.yaml`).
3. Record deliberate divergences + any gaps in ADR018's Blueprint row; file follow-up notes for real drift.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] All three API surfaces return the same object for the same blueprint.
- [ ] ADR018 updated with evidence and the documented divergences.

## Out of scope

- Fixing divergences that require catalog growth (new tiers) — follow-up notes only.
