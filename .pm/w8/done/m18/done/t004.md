---
id: w8/m18/t004
title: Dashboard "Estimated pricing" panel on the blueprints/new review step
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m18/t003]
---

## Objective

Render the estimate on the review step of `blueprints/new`, matching Render's panel semantics: grouped rows `(PlanName) $X / month`, database rows with an instance/storage breakdown, a `Total $X per month` footer, subtitle "Compute costs are prorated by the second. Billing occurs at the start of the month.", and a `*` footnote listing excluded variable costs. Hidden entirely when there are no priced lines.

## Context

- `dashboard/src/routes/blueprints.new.tsx` — review step (:382-476), `PlanGroup` badge rows (:55-68).
- `dashboard/src/features/blueprints/api/operations.ts` — extend the `BlueprintPreview` selection set (:124-168); `types.ts` (:35-66) mirrors.
- `dashboard/src/features/usage/components/usage-page.tsx` `EstimatedCostSection` — existing estimate-display idiom + the "30% below Render" copy in `features/usage/locales/en.ts`; reuse tone, and add a one-line "estimate only" disclaimer (bex storage is metered on used bytes, so provisioned-size is an upper bound).
- Established rule: prices come from the API, never hardcoded client-side (`instance-type-picker.tsx` deliberately omits price — leave pickers unchanged).

## Steps

1. Extend the TypedDocumentNode + TS types with `estimatedPricing`.
2. Add the panel component to the valid-preview state, below the resource groups; hide when `lines` is empty.
3. Variable entries listed with reason labels and the asterisk footnote ("Excluding autoscaling…" etc., only applicable words).
4. i18n strings in the blueprints feature locales; component test for render/hide/total.

## Files

- `dashboard/src/routes/blueprints.new.tsx`
- `dashboard/src/features/blueprints/api/operations.ts`, `types.ts`, locales, new panel component under `features/blueprints/components/`

## Acceptance criteria

- [ ] Beancount fixture preview shows three priced rows + correct total; an all-free blueprint shows no panel.
- [ ] No dollar amounts hardcoded in dashboard source.
- [ ] `yarn test` green.

## Out of scope

- The existing-Blueprint sync view's "Additional pricing" variant (follow-up inbox note if wanted).
- Plan-picker price display on create forms (separate decision, deliberately omitted today).
