---
id: w8/m18/t002
title: Attach `estimatedPricing` to the `BlueprintPreview` core payload
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m18/t001]
---

## Objective

Build the `estimatedPricing` object inside the blueprint dry-run core so every preview surface gets it. The compiler already parses everything needed; the estimate is assembled from the parsed stack **before** `blueprintValidationPlanFromIR` strips it to name lists.

## Context

- `lego/backend/internal/apps/blueprint.go` — `blueprintValidationFor` (:216-248, the shared dry-run core), `BlueprintValidation`/`BlueprintPreview` structs (:143-182), name-only plan builder (:848-871).
- `lego/backend/internal/apps/deploy.go` — parsed `bexService.Plan/NumInstances/Scaling` (:221-252), `bexDatabase.Plan/DiskSizeGB/HighAvailability/ReadReplicas` (:301-317); defaults already Render-matching (service plan omitted → `starter` under maintenance mode :1396-1400, DB plan → `basic-256mb` :1591).
- Secret-safety note on `BlueprintFieldChange` (`blueprint_ir.go:96-101`) stays untouched — plan labels and dollar figures are not secrets; changed-field **values** remain stripped.
- Only attach the estimate when compilation succeeded (`validation.valid`); invalid blueprints keep today's payload.

## Steps

1. Map the parsed stack (services incl. keyvalue + cron kinds, databases) to t001's estimator input; storage GB from `DiskSizeGB` else the tier floor.
2. Attach the result as `EstimatedPricing` on `BlueprintValidation` (present on both `PreviewBlueprint` and `ValidateBlueprint`).
3. Classify variable entries: autoscaling enabled, fixed `numInstances > 1`, cron jobs — listed, excluded from total, each naming its reason.
4. Table-test through `blueprintValidationFor` with YAML fixtures, including `_infra/bex/bex-beancount.yaml`'s shape (standard web + standard KV + basic-1gb DB).

## Files

- `lego/backend/internal/apps/blueprint.go`
- `lego/backend/internal/apps/blueprint_test.go` (or the existing preview test file)

## Acceptance criteria

- [ ] Preview of the beancount fixture yields three lines (≈ $17.50 + $21.00 + $15.05) and the correct total.
- [ ] An all-free blueprint yields empty lines and zero total (dashboard will hide the panel).
- [ ] Invalid blueprints carry no `estimatedPricing`.

## Out of scope

- Adapter/GraphQL wiring (t003); dashboard (t004).
- `current_state` diff-aware pricing ("Additional pricing" for updates — noted as follow-up if not trivial).
