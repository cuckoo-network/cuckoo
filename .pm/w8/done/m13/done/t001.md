---
id: w8/m13/t001
title: REST paging on both datastore lists
worker: worker8
status: done
estimate: 40m
depends_on: []
---

## Objective

Wire `core.PageParams` + `core.Page` into `GET /v1/postgres` (+ `/v1/databases` alias) and `GET /v1/key-value`, cursoring on the instance name exactly as the services list does (`apps/rest.go:307-311`), returning Render's `{postgres|keyValue, cursor}` per-item envelope — check the OpenAPI for each type's exact wrapper key.

## Files

- `lego/backend/internal/postgres/rest.go`, `lego/backend/internal/keyvalue/rest.go`

## Acceptance criteria

- [x] Both endpoints page; omitted params return the identical pre-change body (golden-diff test).

## Done

Shipped in `dfff3034` ("fix(backend): make bex-api REST responses compatible with the official Render CLI") — both `postgres/rest.go` and `keyvalue/rest.go` now wrap list items in `{postgres|keyValue, cursor}` via `core.PageParams`/`core.Page`. Live-verified this session (2026-07-15) against the real `render-oss/cli` (`./cli`, built fresh) run through a rebuilt dev-9 `bex-api`: `postgres create/list/get/update/suspend/resume/delete` all now round-trip real data end-to-end — previously (RC3, `docs/cli-compatibility-checklist.md`) `list`/`get`/`update`/`suspend` silently decoded to zero-value objects. `keyvalue`'s identical code-level fix landed in the same commit but wasn't independently re-verified live this session.
