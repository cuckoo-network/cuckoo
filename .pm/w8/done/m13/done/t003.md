---
id: w8/m13/t003
title: MCP args checked against Render's tools
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m13/t001]
---

## Objective

Check `render-oss/render-mcp-server`'s `list_postgres_instances`/`list_key_value_instances` for cursor/limit args; mirror exactly what Render's tools accept (add args if they page, deliberately don't if they don't — a documented match either way).

## Files

- `lego/backend/internal/postgres/mcp.go`, `lego/backend/internal/keyvalue/mcp.go`

## Acceptance criteria

- [x] MCP tool args match Render's tool signatures, decision recorded in the task/ADR006.

## Done

Checked current `render-oss/render-mcp-server` commit `9fb5c708f52dc84cf8d33e5853b29f38d5b6c058` (2026-07-14). `list_postgres_instances` and the current Key Value spelling `list_key_value` accept no arguments; the Postgres repository internally exhausts REST pages. bex now exposes those exact no-argument schemas and returns the complete selected-workspace lists. The former `list_key_value_instances` spelling remains a deprecated no-argument compatibility alias. MCP schema assertions and ADR006 record the decision.
