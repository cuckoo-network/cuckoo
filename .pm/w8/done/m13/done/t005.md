---
id: w8/m13/t005
title: Simplify
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m13/t004]
---

## Objective

Run `/simplify` over the changed adapters; behavior-preserving. (Watch for triplicated paging glue — if postgres/keyvalue/apps now share shape, hoist into `core`.)

## Acceptance criteria

- [x] Simplification review run; accepted findings applied; suites green.

## Done

The `/simplify` workflow is not installed in this Codex session, so its reuse/simplification/efficiency review was performed manually. Stable clone+sort+page behavior was hoisted into generic `core.StablePage`, shared by both datastore adapters; page-limit normalization remains in `core.PageLimit`; the comparator uses standard-library `cmp.Compare`. `make lint-backend` reports 0 issues and the full backend/dashboard suites pass.
