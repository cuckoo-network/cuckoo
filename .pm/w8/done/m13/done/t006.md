---
id: w8/m13/t006
title: Test coverage
worker: worker8
status: done
estimate: 40m
depends_on: [w8/m13/t004]
---

## Objective

Meaningful tests: full-walk-visits-every-instance-once for both types, omitted-params golden diff, cursor-past-end behavior, adapter parity. Real failure modes, no tautologies.

## Acceptance criteria

- [x] Suite covers the DoD claims and fails if any regresses.

## Done

Added >20-item reverse-order fixtures for each datastore. The tests prove omitted REST bodies equal the complete pre-page projection, aliases match, repeated cursor pages visit every id exactly once, item cursors equal stable ids, unknown cursors return empty, omitted GraphQL args stay unbounded, REST and GraphQL page boundaries match, and MCP list schemas expose no arguments. Core coverage pins stable ordering without mutating the source slice.
