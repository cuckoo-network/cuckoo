---
id: w8/m13/t007
title: Closeout
worker: worker8
status: done
estimate: 15m
depends_on: [w8/m13/t006]
---

## Objective

When the DoD actually holds, close the milestone: mark tasks done, move files to `done/`, sync READMEs, move `w8/m13/` to `w8/done/m13/`.

## Acceptance criteria

- [x] DoD verified; board synced in all three places.

## Done

Completion audit:

- REST accepts `cursor`/`limit` on Postgres, its Databases alias, and Key Value; all return the existing Render item envelopes.
- Omitted params return the complete original projection and order, proven with 23-item byte-for-byte body comparisons.
- Requested pages sort by stable id, use exclusive cursors and Render's 1–100/default-20 bounds, return empty for an unknown/final cursor, and walk all 23 reverse-seeded instances exactly once for both datastores.
- GraphQL accepts the same optional vocabulary and page boundaries while preserving the dashboard's existing plain-list types; MCP matches the current official no-argument/all-items signatures.
- `go build ./...`, full backend `go test ./...`, `make lint-backend` (0 issues), dashboard `yarn typecheck`, and all 1,140 dashboard tests pass. Changed Markdown passes Prettier 3.4.2 and `git diff --check` is clean.
