---
id: w8/m13/t004
title: Render parity
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m13/t002, w8/m13/t003]
---

## Objective

Check the three surfaces' consistency and the envelope against Render's OpenAPI; update the ADR018 datastore rows + ADR006; confirm the dashboard lists still render (no UI change expected — verify, don't assume); file drift as follow-ups.

## Acceptance criteria

- [x] ADR018/ADR006 updated; dashboard verified unaffected; drift filed.

## Done

ADR006 and ADR018 now record REST envelopes/semantics, the dashboard-compatible GraphQL list shape, current MCP tool names/signatures, and the deprecated Key Value alias; ADR021 and the CLI compatibility ledger were synchronized too. The dashboard generated query-argument types include optional cursor/limit, while its existing operations remain unchanged. `yarn typecheck` and all 1,140 dashboard tests pass, so no UI drift or follow-up gap remains.
