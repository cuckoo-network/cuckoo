---
id: w8/m33/t005
title: "Simplify: /simplify over the code this milestone changed"
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m33/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (the `lego/cli` pin bump and any overlay-seam adjustments) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Expected surface: `lego/cli/main.go`, `lego/cli/internal/branding/`, and any script touched by the compat re-run.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted cleanups; re-run `cd lego/cli && go test ./...`.

## Files

- Whatever the milestone's diff touched under `lego/cli/` and `scripts/`.

## Acceptance criteria

- [ ] `/simplify` pass completed; accepted cleanups applied with tests still green.

## Out of scope

- Behavior changes of any kind.
