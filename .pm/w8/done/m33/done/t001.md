---
id: w8/m33/t001
title: Diff Render's live OpenAPI against the pinned 208-op baseline
worker: worker8
status: done
estimate: 45m
depends_on: []
---

## Objective

Fetch Render's current public OpenAPI from `api-docs.render.com` and diff it against the w2/m79 baseline (208 operations, sole new path then: `GET /services/{serviceId}/outbound-ips`), recording every added/removed operation and changed request/response shape so t004 can update the ledger from evidence, not memory.

## Context

- Previous round: `docs/ADR018-render-parity.md` § "Upstream drift re-baseline (w2/m79, 2026-08-19)".
- The request-validator pin `lego/backend/internal/api/openapi/render-public-api-1.json` is a separately captured artifact guarded by the OpenAPI gate (w6/m96); do **not** swap it in this task — if the diff shows the validator contract needs a refresh, that is a finding for t004 to file as owned follow-up work.

## Steps

1. Fetch the live OpenAPI document from `api-docs.render.com`.
2. Diff operations (paths × verbs) and schemas against the 2026-08-19 baseline; list adds, removals, and shape changes with the affected ADR018 rows.
3. Note anything that would change a pinned enum or validator behavior as a candidate follow-up (do not change pins here).

## Files

- `docs/ADR018-render-parity.md` (read; findings recorded by t004)
- `lego/backend/internal/api/openapi/render-public-api-1.json` (read-only reference)

## Acceptance criteria

- [ ] A written diff (operation-level adds/removes/changes) against the 208-op baseline exists for t004 to consume.
- [ ] No pinned artifact was modified by this task.

## Out of scope

- Updating ADR018 rows (t004) or the request-validator pin (follow-up work if needed).
