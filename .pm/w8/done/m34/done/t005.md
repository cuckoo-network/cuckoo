---
id: w8/m34/t005
title: "Render parity: cross-surface consistency check (REST/GraphQL/MCP/UI)"
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m34/t004]
---

## Objective

Standing closing task. Verify the renamed vocabulary is consistent across every surface it touches — REST events list/get + `?type=` filter, GraphQL `serviceEvents`/`serviceEvent`, MCP `list_service_events`/`get_service_event`, the outbound-webhook advertised vocabulary and delivered payload `type`, and the dashboard Events tab + webhook picker — same fields, semantics, and error shapes, compared against Render's documented behavior (its events enum and webhook event list).

## Context

- bex-api is meant to be Render-compatible: `docs/ADR006-bex-api.md`; dashboard conventions: `dashboard/CLAUDE.md`.
- Flag any drift found as follow-up work rather than silently diverging.

## Steps

1. Walk one disk add + one disk delete through all surfaces; confirm identical `type` strings everywhere.
2. Compare against Render's enum/webhook vocabulary; record any residual divergence (expected: only `disk_restored`, as a labeled extension).

## Files

- Evidence recorded in the milestone README or the ADR018 row (no new code expected).

## Acceptance criteria

- [ ] All surfaces carry identical spellings for the same event; any drift is filed, not ignored.

## Out of scope

- Fixing unrelated event-family divergences (file them).
