---
id: w8/m34/t007
title: "Test coverage: meaningful tests for the behavior this milestone shipped"
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m34/t005]
---

## Objective

Ensure the shipped behavior has real failure-mode coverage beyond t004's guard: old-spelling rows surviving (render + filter), old-spelling webhook subscriptions delivering, the migration (if chosen) being idempotent/re-runnable, and the dashboard vocabulary test locking the advertised set. No tautological or snapshot-everything tests.

## Context

- Real-Postgres store tests (`TestPGStore` family) are the right home for migration/alias assertions; webhook delivery has existing multi-replica-safe test scaffolding.

## Steps

1. Audit t001–t004's tests for gaps against the DoD's failure modes.
2. Add the missing assertions; verify each new test fails when its guarded behavior is broken (mutation spot-check).

## Files

- `lego/backend/internal/store/` tests, `lego/backend/internal/webhooks/` tests, `dashboard/src/features/events/__tests__/`

## Acceptance criteria

- [ ] Every DoD clause has a test that fails when it regresses.
- [ ] Backend `go test ./...` and dashboard `yarn test` green.

## Out of scope

- Coverage for untouched event families.
