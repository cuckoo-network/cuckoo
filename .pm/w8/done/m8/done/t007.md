---
id: w8/m8/t007
title: Test coverage
worker: worker8
status: done
estimate: 1h
depends_on: [w8/m8/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: `displayName` set/clear, authorization, and round-trip across surfaces.

## Steps

1. Unit test for `SetDisplayName` (authorized vs. unauthorized caller).
2. REST PATCH test: set/clear `displayName`, verify `GET` reflects it, verify omitting the field leaves it unchanged.
3. GraphQL/MCP round-trip test.

## Files

- Test files alongside each package touched in t001-t004.

## Acceptance criteria

- [ ] Tests assert real behavior and failure modes (unauthorized rejection, no accidental mutation of `Name`), not tautological checks.
- [ ] `go test ./...` (backend) passes.

## Out of scope

- Load/performance testing.
