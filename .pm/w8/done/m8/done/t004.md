---
id: w8/m8/t004
title: GraphQL + MCP displayName field
worker: worker8
status: done
estimate: 1h
depends_on: [w8/m8/t002]
---

## Objective

Expose `displayName` on the GraphQL service type/mutations and, if apps expose MCP update fields today, on the MCP surface too.

## Context

- `lego/backend/internal/apps/graphql.go` — service type + update mutation.
- `lego/backend/internal/apps/mcp.go` — check whether an existing MCP update tool accepts patchable fields; if apps have no MCP write surface for these fields today, note that and scope `displayName` in only where an equivalent already exists (don't introduce a new MCP write surface just for this field).

## Steps

1. Add `displayName` to the GraphQL `Service` type and the update-service mutation input.
2. If an MCP update tool already exists for comparable fields (e.g. plan/autoDeploy), add `displayName` there; otherwise document the omission in the Render-parity check (t005).

## Files

- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/apps/mcp.go` (if applicable)

## Acceptance criteria

- [ ] GraphQL query/mutation for `displayName` round-trips.
- [ ] MCP parity confirmed or explicitly scoped-out with reasoning recorded.

## Out of scope

- Introducing a net-new MCP write surface where none exists for comparable fields.
