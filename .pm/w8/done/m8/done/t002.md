---
id: w8/m8/t002
title: SetDisplayName verb
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m8/t001]
---

## Objective

Add a `SetDisplayName` verb to `internal/apps/service.go` that updates `AppSpec.DisplayName` on the CR.

## Context

- `w6/done/m17`'s `core.Base.AuthorizeApp` is the established authorize-and-fetch-in-one-call chokepoint for App verbs — use it rather than the older `Authorize` + `GetApp` pair.

## Steps

1. Add `SetDisplayName(ctx, name, displayName string) error` to `internal/apps/service.go`, authorizing via `AuthorizeApp` against the App's own workspace.
2. Patch `AppSpec.DisplayName` on the CR.

## Files

- `lego/backend/internal/apps/service.go`

## Acceptance criteria

- [ ] Setting/clearing `displayName` persists to the CR.
- [ ] Non-member callers are rejected consistently with other App verbs.

## Out of scope

- REST/GraphQL/MCP wiring (t003/t004).
