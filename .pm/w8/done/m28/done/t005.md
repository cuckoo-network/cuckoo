---
id: w8/m28/t005
title: Render parity
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m28/t004]
---

## Objective

Confirm this milestone changed only the dashboard `/agents` UX and introduced no REST/GraphQL/MCP or Connected-agents drift.

## Context

- Standing closeout: same fields/semantics across REST, GraphQL, MCP, and UI (`docs/ADR006-bex-api.md`, `docs/ADR018-render-parity.md`).
- Render has no cloud coding-agent sessions product. ADR018 has no agent-sessions row (w2/m71 t010 precedent). Reference is ADR047 D9/D9a + Devin-shaped dashboard, not render.com.
- Settings → Connected agents (`features/connected-agents/`) is a different product (authorized OAuth clients). This milestone must not touch it.

## Steps

1. `git diff` against the milestone’s changes: expect empty under `lego/`. Stop and explain if not.
2. Confirm `dashboard/src/features/agent-sessions/api/agent-sessions.graphql` is unchanged.
3. Confirm Connected agents panel/route/locales are untouched.
4. Confirm ADR018 needs no edit (no Render row to flip). Record that agent sessions remain a bex extension.
5. Note the outcome on the milestone README Status line.

## Files

- verification only; `docs/ADR018-render-parity.md` only if a cell is genuinely stale

## Acceptance criteria

- [ ] No `lego/` changes attributable to this milestone.
- [ ] No GraphQL document or backend field added/changed.
- [ ] Connected agents surface untouched.
- [ ] ADR018 confirmed accurate (or edited only if already stale).
- [ ] Outcome recorded in the milestone README.

## Out of scope

- Implementing more UI.
