---
id: w8/m28/t008
title: Closeout
worker: worker8
status: done
estimate: 15m
depends_on: [w8/m28/t006, w8/m28/t007]
---

## Objective

Close the milestone only when the definition of done is actually met: default `/agents` is prompt-first, GitHub-empty is a CTA, recents replace the phase table, create payload unchanged.

## Context

- Completing this task _is_ the move (`/pm done w8/m28/t008`): remaining tasks → `w8/m28/done/`, milestone → `w8/done/m28/`, workstream checkbox `- [x]`.
- Do not close on “code written” if the default view still shows PageHeader + Phase dropdown + round arrow.

## Steps

1. Confirm t001–t007 are done and the milestone README DoD holds (composer-as-room, toolbar agent+repo, GitHub CTA, recents, no phase filter, quiet empty, matching skeleton, no `lego/`/GraphQL change, one rail).
2. Prefer a logged-in walk on `dev-8` or prod `/agents` for: zero repos, zero sessions, populated Current, Archived. If auth isn’t available, record that the unit tests are the evidence (do not fake a prod walk).
3. Run `/pm done w8/m28/t008` last.

## Files

- `.pm/w8/m28/` → `.pm/w8/done/m28/`
- `.pm/w8/README.md` checkbox

## Acceptance criteria

- [ ] DoD is true, not aspirational.
- [ ] Milestone directory lives at `.pm/w8/done/m28/` with no tombstone at `.pm/w8/m28/`.
- [ ] Workstream README m28 row is checked.

## Out of scope

- Shipping (`/ship`) unless the user asks.
