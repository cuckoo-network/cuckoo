---
id: w8/m28/t006
title: Simplify
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m28/t005]
---

## Objective

Run `/simplify` over the files this milestone changed. Behavior-preserving: reuse, less duplication, no extra abstraction.

## Context

- Composer, recents, empty, and skeletons will have grown new branches (default vs archived, repos-empty vs writable). Look for duplicated status-phrase mapping, membership labels, and skeleton markup.
- `AgentSessionPhaseChip` may become unused on the list; delete or keep if detail still needs it (detail header likely still does — don’t yank a shared export blindly).
- Locale keys: drop unused `pageSubtitle` / `colPhase` / `filterPhase*` if nothing references them.

## Steps

1. Run `/simplify` on the milestone diff.
2. Apply only behavior-preserving cleanups.
3. Re-run the touched dashboard tests.

## Files

- whatever t001–t004 actually changed under `dashboard/src/`

## Acceptance criteria

- [ ] Simplify pass applied or an explicit “nothing to simplify” note in this task when done.
- [ ] No behavior change; tests still green.

## Out of scope

- New UX.
