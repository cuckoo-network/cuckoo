---
id: w8/m28/t007
title: Test coverage
worker: worker8
status: done
estimate: 60m
depends_on: [w8/m28/t005]
---

## Objective

Pin the new `/agents` behavior with tests that fail if the page regresses to a titled CRUD table, an unlabeled send button, or a GitHub-empty dead end.

## Context

- Existing: `new-session-composer.test.tsx`, `session-list.test.tsx`, `agents-search.test.ts`, `agent-sessions-nav-section.test.tsx`, `one-rail-invariant.test.ts`.
- Coverage must assert real UI behavior (roles, names, navigation, empty-repos CTA), not snapshots of class names.

## Steps

1. Composer: labeled Start; agent select on the toolbar; repo chip; submit-without-repo highlights the chip/`@`; zero repos → Connect GitHub, no create call.
2. Recents: default view has no “Phase” column header and no phase combobox; row is a link; status phrase not raw `redispatching`.
3. Search: `?archived=true` still selects Archived; `?view=list` remains a no-op; `?phase=` does not resurrect the dropdown.
4. Empty: default empty does not render the old dashed-Bot title “No agent sessions yet” with the long body (or asserts the new quiet copy).
5. Skeleton: `/agents` route pending is not the 3-card `ListPageSkeleton` grid (query the pending component or a testid).
6. `yarn test` for the touched files; do not snapshot-everything.

## Files

- `dashboard/src/features/agent-sessions/components/__tests__/`
- `dashboard/src/routes/__tests__/agents-search.test.ts`
- `dashboard/src/common/components/dashboard-layout/__tests__/agent-sessions-nav-section.test.tsx` if labels changed

## Acceptance criteria

- [ ] Tests cover the DoD bullets that are unit-testable (Start label, empty-repos CTA, no phase filter, recents row link, quiet empty, pending shape).
- [ ] Failures would catch a revert to the old table + round arrow.
- [ ] `yarn test` green for the touched suites.

## Out of scope

- Live prod Playwright (host-scoped Kratos cookie; same constraint as w1/m64 t008). A local/dev-8 logged-in walk is welcome in Closeout, not required here.
