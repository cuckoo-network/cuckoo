---
id: w8/m6/t001
title: Design month-picker/trend UX for usage history
worker: worker8
status: done
estimate: 30m
depends_on: []
---

## Objective

Decide the UX for viewing past-month usage on the dashboard — a month picker, a trailing-N-months trend, or both — referencing Render's historical/previous-period billing view as the layout reference.

## Context

- `w8/m3`'s t001 captured Render's current billing/usage page live (Playwright) as a reference — this task extends that capture to Render's historical/previous-period view if one exists.
- The existing `UsagePage` (`dashboard/src/features/usage/components/usage-page.tsx`) is current-month-only with no period selector.

## Steps

1. Capture (or re-verify from existing notes) how Render presents historical usage/billing periods.
2. Decide: month picker (discrete period selection) vs. trend chart (last N months) vs. both.
3. Document the decision and its rationale in this milestone's README or a short design note.

## Files

- `docs/render-artifacts/` (if a new capture is added)

## Acceptance criteria

- [ ] A documented UX decision exists before t002–t005 are implemented.

## Out of scope

- Implementation (t002 onward).
