---
id: w8/m24/t004
title: Add GraphQL and MCP single-event adapters
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m24/t003]
---

## Objective

Expose the same authorized single-event read as GraphQL `serviceEvent(id:)` and MCP `get_service_event` without a second lookup implementation.

## Context

- `lego/backend/internal/events/graphql.go`
- `lego/backend/internal/events/mcp.go`
- MCP event reads are deliberate Bex extensions over the Render REST contract.

## Steps

1. Add both thin adapters over the shared core method.
2. Keep field semantics and coded failures aligned with REST.
3. Document the MCP extension rather than claiming an official Render tool name.

## Files

- `lego/backend/internal/events/graphql.go`
- `lego/backend/internal/events/mcp.go`
- `lego/backend/internal/events/*test.go`

## Acceptance criteria

- [ ] The same event ID returns equivalent structured data on all three surfaces.
- [ ] Foreign/missing IDs have equivalent authorization-safe behavior.
- [ ] Tool descriptions explain accepted ID format and returned details.

## Out of scope

- Dashboard event-detail navigation.
