---
id: w8/m24/t003
title: Serve Render's GET /v1/events/{eventId} contract
worker: worker8
status: done
estimate: 60m
depends_on: [w8/m24/t002]
---

## Objective

Mount the missing Render REST route over the shared single-event lookup with Render-compatible fields, status codes, and coded Bex errors.

## Context

- `lego/backend/internal/events/rest.go` currently mounts only the service-scoped list.
- Webhook receivers are given only `data.id` plus thin resource metadata.

## Steps

1. Register `GET /v1/events/{eventId}` in the events REST fragment.
2. Serialize the same type, timestamp, service identity, and typed details as the list surface.
3. Apply caller authorization and stable 404/503 behavior.

## Files

- `lego/backend/internal/events/rest.go`
- `lego/backend/internal/events/service.go`
- `lego/backend/internal/events/*rest*_test.go`

## Acceptance criteria

- [ ] A listed/webhook event round-trips through the new route byte-for-byte on shared fields.
- [ ] Missing and foreign event IDs return the pinned safe contract.
- [ ] An unwired control-plane store degrades only the events route family.

## Out of scope

- Delivery-attempt history and manual resend.
