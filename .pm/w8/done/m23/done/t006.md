---
id: w8/m23/t006
title: Closeout
worker: worker8
status: done
estimate: 15m
depends_on: [w8/m23/t005]
---

## Objective

Close the milestone once the DoD holds — two-blueprint conflict + takeover proven by the integration suite and one live dev-stack run.

## Context

- Closeout = the move: tasks to `w8/m23/done/`, README rows `— **DONE**` + `**Status:** done`, milestone to `w8/done/m23/`, checkbox in `w8/README.md`.

## Steps

1. Verify DoD evidence.
2. Execute the move + status sync; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w8/` board files.

## Acceptance criteria

- [ ] DoD verified before the move; board consistent.

## Out of scope

- New work discovered late (file inbox notes).

## Closeout record (2026-08-16)

DoD verified by the automated battery (all four paths):

- **Ownership record (t001):** `bex.co/blueprint-id` CR label on all three
  resource kinds — stamped post-apply on create/adopt/takeover inside the
  blueprint apply (`stampBlueprintOwnership`; re-stamping on every sync is
  the lazy backfill), cluster-wide label-scoped matching via the
  service-name label (store-managed CR names carry the tenant prefix).
  Spec-only PATCHes cannot strip a label. Storage decision recorded:
  CR label over a store column — one mechanism for all kinds, works
  store-less, disconnect clears by label selector.
- **Conflict + takeover (t002):** `preflightBlueprintOwnership` refuses in
  `deployParsedStack` (shared by create/sync/deploy) pre-write with the
  conflict-class `BLUEPRINT_RESOURCE_CONFLICT` coded error carrying the
  server-issued `takeover blueprint <id>` phrase in the established
  `retry with confirm=` convention — the dashboard's existing
  ProtectedConfirmationDialog drives takeover with a one-line matcher
  extension (unit-tested). Takeover transfers per-owner; a second distinct
  owner needs its own confirmation. Preview/create-review report conflicts
  as validation entries (self-identity resolved by repo+branch so an
  existing blueprint never conflicts with itself); webhook auto-syncs never
  take over (blueprint status records error). Disconnect clears markers;
  unmarked resources adopt freely.
- **Parity (t003):** the coded error is the shared cross-surface CodedError
  machinery; confirm rides the existing REST/GraphQL/MCP parameter. ADR018
  documents the deliberate divergence: Render warns "last sync wins"
  without enforcing; bex refuses with explicit takeover.
- Suites: backend green (4 ownership tests), dashboard 2162 green,
  lint 0 issues.

**Residual:** live dev-stack walk rides `w8/007` with the rest.
