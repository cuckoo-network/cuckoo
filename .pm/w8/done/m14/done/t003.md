---
id: w8/m14/t003
title: Field on REST/GraphQL/MCP create + PATCH + read
worker: worker8
status: done
estimate: 40m
depends_on: [w8/m14/t002]
---

## Objective

Render's exact names: `enableDiskAutoscaling` accepted on create + PATCH, `diskAutoscalingEnabled` returned on the postgres object — one core verb, three thin adapters (the `SetPlan`/m12 grammar).

## Files

- `lego/backend/internal/postgres/{service,rest,graphql,mcp}.go`

## Acceptance criteria

- [x] Round-trip identical on all three surfaces (adapter-parity test); write/read name asymmetry matches the spec exactly.
