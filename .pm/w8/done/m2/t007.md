---
id: w8/m2/t007
title: Simplify — /simplify over the code this milestone changed
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m2/t006]
---

## Objective

Run `/simplify` over the usage API code (behavior-preserving reuse/simplification/efficiency) — watch for adapter boilerplate that neighboring feature packages already abstract.

## Context

- Standing closing task (`.claude/skills/pm/SKILL.md`).

## Steps

1. `/simplify` scoped to the milestone's diff; apply accepted findings; tests green.

## Files

- `lego/backend/internal/usage/`, `lego/backend/internal/api/`

## Acceptance criteria

- [ ] `/simplify` run; findings applied or explicitly declined; tests still green.

## Out of scope

- Bug hunting (that's `/code-review`).
