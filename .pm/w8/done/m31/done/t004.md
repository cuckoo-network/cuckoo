---
id: w8/m31/t004
title: Simplify — /simplify over the milestone's changed code
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m31/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification /
efficiency; behavior-preserving) — in particular, factor shared logic between the
new `GitCredentialsMenu` and the existing Settings `ConnectGithubCard` so the two
credential surfaces cannot drift.

## Context

- Changed code: `dashboard/src/features/git/components/git-credentials-menu.tsx`,
  `use-connect-git.ts`, `service-source-picker.tsx`, `use-repos.ts`, git locales.
- `connect-github-card.tsx` already renders the same account list / disconnect /
  claim / unavailable-error logic — a prime candidate for a shared subcomponent or
  hook rather than duplicated JSX.

## Steps

1. `/simplify` the milestone's changed files.
2. Extract any duplicated connection-list/disconnect/claim logic shared with the
   Settings card into one reusable unit.
3. Keep behavior identical; re-run typecheck/lint/tests.

## Files

- The files changed in t001/t002 (+ `connect-github-card.tsx` if a shared
  extraction is warranted).

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly dispositioned.
- [ ] No duplicated credential-list logic between the menu and the Settings card.
- [ ] `yarn typecheck && yarn lint && yarn test` green (behavior unchanged).

## Out of scope

- New features or behavior changes.
