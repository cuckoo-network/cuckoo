---
id: w8/m31/t003
title: Render parity — verify /web/new credentials-dropdown match, record divergence
worker: worker8
status: done
estimate: 20m
depends_on: [w8/m31/t002]
---

## Objective

Confirm the in-place credentials menu matches render.com's `/web/new`
"Credentials (N)" dropdown behavior and that no cross-surface drift is
introduced, then record the deliberate divergence.

## Context

- Render's `/web/new` credentials dropdown (screenshot 2026-08-28): ACCOUNTS &
  ORGS list with per-account repo counts + external-link, "Configure in GitHub",
  "Disconnect credential".
- Backend REST/GraphQL/MCP for git connections is already at parity (ADR075 /
  ADR018 line 166) and unchanged by this milestone — so this task is a UI-surface
  consistency check, not a REST/GraphQL/MCP field audit.
- Deliberate divergence: bex opens the install in a new tab (preserves the create
  form); Render navigates same-tab and returns via a `return_to`.

## Steps

1. Compare the bex menu's actions/labels against Render's dropdown; flag any
   missing affordance as follow-up rather than silently diverging.
2. Confirm REST/GraphQL/MCP git-connection surfaces are untouched by this
   milestone.
3. Record the new-tab-install divergence in `docs/ADR018-render-parity.md` (or the
   ADR026 GitHub-integration notes) alongside the existing Git-connections row.

## Files

- `docs/ADR018-render-parity.md` and/or `docs/ADR026-github-integration.md`

## Acceptance criteria

- [ ] Menu affordances match Render's `/web/new` dropdown (or gaps filed as
      follow-up).
- [ ] No REST/GraphQL/MCP drift introduced (backend unchanged).
- [ ] The new-tab-install divergence is recorded in the parity ledger.

## Out of scope

- Implementing a same-tab `return_to` callback (deferred).
