---
id: w8/m3/t005
title: Render parity — UI matches the captured Render layout/semantics; drift flagged, not silent
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m3/t004]
---

## Objective

The standing parity check for the UI surface: side-by-side the shipped Usage page against t001's captured Render billing/usage reference — regions, vocabulary, units, zero-states — and against the m2 API (the page must show exactly the API's numbers). Flag drift as follow-up notes rather than silently diverging.

## Context

- Standing closing task (`.claude/skills/pm/SKILL.md`), included because m3 ships a user-facing UI surface.
- Expected declared drift: no dollar amounts (quantities only — the FUTURE-MAYBE pricing boundary) and no payment section. Anything else is a finding.

## Steps

1. Side-by-side review (Playwright screenshots of both).
2. Cross-check displayed numbers vs a direct API call for the same workspace/month.
3. File follow-up notes for real gaps.

## Files

- `.playwright-mcp/` (evidence); findings → `.pm/w8/` notes

## Acceptance criteria

- [ ] Drift list is exactly the two declared items (no dollars, no payments); numbers match the API.

## Out of scope

- Re-checking m2's adapter parity (done in w8/m2/t006).
