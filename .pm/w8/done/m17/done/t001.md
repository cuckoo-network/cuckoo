---
id: w8/m17/t001
title: Design the persistent sample-failure signal — Event vs status condition, N-failure debounce
worker: worker8
status: done
estimate: 30m
depends_on: []
---

## Objective

Decide the shape of the durable signal emitted when the disk-usage sample is persistently unavailable: a Warning Event, a status condition, or both; and the debounce (N consecutive failures) that separates a real outage from benign first-reconcile noise (no PVC yet, cluster still provisioning). Write the decision into the task/PR description before implementing.

## Context

- `lego/operator/internal/controller/database_autoscale.go:176-179` — sample errors are logged at Info and swallowed; the success path emits a `DiskAutoscaled` Event and appends `status.diskResizeHistory`; persist failures go through `r.dbFail(..., "DiskAutoscalingFailed", ...)`.
- Failure modes to cover: Prometheus unreachable, kubelet PVC series missing, `^<name>-[0-9]+$` selector matching nothing.
- Where to persist the consecutive-failure counter: annotation (the `lastResize` cooldown precedent) vs in-memory (lost on manager restart — probably fine for a debounce, but decide explicitly).

## Steps

1. Enumerate the failure modes and pick signal + debounce + counter storage.
2. Check CNPG/kubebuilder conventions for Warning Events vs conditions on the Database CR.
3. Record the design (a short paragraph) for t002 to implement.

## Files

- `lego/operator/internal/controller/database_autoscale.go` (read-only this task)

## Acceptance criteria

- [x] A written design naming the signal kind, the N threshold, and where the counter lives, with the first-reconcile noise case explicitly handled.

## Decision — 2026-07-16

Emit a Kubernetes `Warning` Event with reason `DiskAutoscalingSampleUnavailable` on the transition to **three consecutive** failed samples. Persist the count in the Database annotation `app.bex.co/disk-autoscale-sample-failures`, so a manager restart cannot reset the debounce; cap the count at three so a sustained outage emits one event instead of one per minute. A successful sample (or disabling autoscaling) removes the annotation and rearms the signal. Count failures only after `Database.status.phase` is `Ready`: provisioning databases with no PVC/series yet remain quiet, while Prometheus transport failures and missing series on an established database converge to the same operator-visible warning. The annotation is the durable debounce state and the Event is the `kubectl describe database` operator signal; no tenant API/status contract is added.

## Out of scope

- Implementation (t002).
