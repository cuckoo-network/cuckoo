---
id: w8/m12/t004
title: Backend: version-update verb + validation + backup guard
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m12/t003]
---

## Objective

One core verb (the `SetPlan` grammar from w2/m16) exposed as REST `PATCH /v1/postgres/{id}` (`version` field), GraphQL `updateDatabaseVersion`, MCP `update_postgres_version`: upward-only against the known-versions catalog (unknown/downgrade ⇒ named 400), and refused with a named error when a durability-plan instance has no backup to fall back to.

## Files

- `lego/backend/internal/postgres/{service,rest,graphql,mcp}.go`

## Acceptance criteria

- [ ] Upgrade accepted, downgrade/unknown 400, no-backup guard 409/400 (pick + document), identical on all three surfaces (adapter-parity test).
