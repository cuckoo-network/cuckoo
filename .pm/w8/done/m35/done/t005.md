---
id: w8/m35/t005
title: "Render parity: cross-surface consistency check (REST/GraphQL/MCP/UI)"
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m35/t004]
---

## Objective

Standing closing task. Verify the deleting-read contract is consistent across every surface: REST datastore get/connection-info, GraphQL, MCP (same not-found shape as an absent id), and the dashboard — the Postgres/Key Value detail pages must handle a mid-session deletion the way the service pages do post-m81 (graceful not-found, no stuck `deleting` spinner), compared against Render's delete-then-read behavior as pinned by t001.

## Context

- `docs/ADR006-bex-api.md` (error-shape contract), `dashboard/CLAUDE.md`; the m81 dashboard handling is the precedent.

## Steps

1. Walk a deleting fixture across all four surfaces for both families; confirm identical semantics and error shapes.
2. Check the dashboard datastore detail pages' behavior when a poll/read starts returning not-found mid-deletion; flag drift as follow-up rather than silently diverging.

## Files

- Evidence recorded in the milestone README or ADR018 rows; dashboard follow-ups filed if found.

## Acceptance criteria

- [ ] All surfaces agree; any dashboard gap is filed, not ignored.

## Out of scope

- Unrelated datastore UI polish.
