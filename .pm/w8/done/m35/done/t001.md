---
id: w8/m35/t001
title: Pin Render's deleted-datastore read evidence; confirm option 1
worker: worker8
status: done
estimate: 30m
depends_on: []
---

## Objective

Establish, from Render's documented behavior (public OpenAPI response declarations + docs), what a by-id read of a deleted/deleting Postgres or Key Value returns on Render — expected: 404 — and record it with an explicit evidence boundary, confirming option 1 (fold onto consistent-absence) before any code moves. If the documented evidence contradicts 404 (e.g. a first-class deleting state), STOP and re-open the option decision with the user instead of proceeding.

## Context

- `w3/034`'s recommendation: "pick option 1 if Render 404s"; option 2 (deliberate visible `deleting` + finalizer bound) remains the fallback.
- Live capture is blocked: no `RENDER_API_KEY` exists in this environment (`.pm/w10/002.md`). Prior ledger rows (e.g. paid-intent gate) model how to record a documented-evidence boundary honestly.
- Render artifacts directory conventions: `docs/render-artifacts/`.

## Steps

1. Read Render's OpenAPI declarations for `GET /postgres/{id}` / `GET /key-value/{id}` (and connection-info reads) — response codes and any deleting-state vocabulary; check its public docs for delete-lifecycle statements.
2. Record findings + the no-live-capture boundary in a dated note (render-artifacts or the ADR018 row draft).
3. Confirm option 1 or STOP per the objective.

## Files

- `lego/backend/internal/api/openapi/render-public-api-1.json` (read)
- `docs/render-artifacts/` (evidence note)

## Acceptance criteria

- [ ] Dated evidence note exists with the documented behavior and the explicit live-capture boundary.
- [ ] Option 1 confirmed (or the milestone paused with the contradiction surfaced).

## Out of scope

- Any code change.
