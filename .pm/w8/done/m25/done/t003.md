---
id: w8/m25/t003
title: Expose attempt-level history and exact evidence on every API surface
worker: worker8
status: done
estimate: 60m
depends_on: [w8/m25/t001, w8/m25/t002]
---

## Objective

Return every attempt with stable identity, exact send time, status/error/response evidence, and request payload access while preserving Render's REST history envelope.

## Context

- `lego/backend/internal/webhooks/rest.go`
- `lego/backend/internal/webhooks/graphql.go`
- `lego/backend/internal/webhooks/mcp.go`
- Render's public REST fields are narrower than its dashboard attempt detail.

## Steps

1. Map each attempt to Render's `webhookEvent` fields on REST.
2. Extend GraphQL/MCP with the same core attempt identity and bounded request evidence.
3. Page on immutable `(sent_at,id)` and keep time filters exact.

## Files

- `lego/backend/internal/webhooks/rest.go`
- `lego/backend/internal/webhooks/graphql.go`
- `lego/backend/internal/webhooks/mcp.go`
- `lego/backend/internal/webhooks/surface_contract_test.go`

## Acceptance criteria

- [ ] One notification with three sends returns three ordered history entries.
- [ ] Request and response/error evidence is bounded, UTF-8 safe, and never includes the signing secret.
- [ ] REST fields remain compatible; GraphQL/MCP extensions are documented.

## Out of scope

- Dashboard and Resend mutation.
