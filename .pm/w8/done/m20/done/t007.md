---
id: w8/m20/t007
title: Test coverage (tx rollback, quota refusal, audit emission, disconnect sweep)
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m20/t005]
---

## Objective

Integration coverage against the real ephemeral Postgres + OpenFGA CI harness (`BEX_TEST_DB_URI`/`BEX_TEST_OPENFGA_URL`): injected mid-loop failure ⇒ zero grouping rows; over-quota ⇒ coded refusal on all three adapters before any write; grouping mutations ⇒ audit events; idempotent re-sync ⇒ no ACL churn; disconnect ⇒ empty minted groupings reclaimed, populated ones and resources untouched. Assert real behavior and failure modes — no tautological tests.

## Context

- CI runs these unskipped (`.github/workflows/backend-test.yml`).
- Adapter-parity test precedent: the round-7 metrics-budget tests (REST + GraphQL + MCP refusal parity).

## Steps

1. Build a grouping-heavy fixture manifest; drive it through sync with failure injection.
2. Adapter-parity table test for the quota error.
3. `cd lego/backend && go test ./...`.

## Files

- `lego/backend/internal/apps/blueprint_*_test.go`
- `lego/backend/internal/store/` tests

## Acceptance criteria

- [ ] All five behaviors covered with meaningful assertions; suite green locally and in CI.

## Out of scope

- Load/perf testing of grouping counts.
