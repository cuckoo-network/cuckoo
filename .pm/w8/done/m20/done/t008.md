---
id: w8/m20/t008
title: Closeout
worker: worker8
status: done
estimate: 15m
depends_on: [w8/m20/t007]
---

## Objective

Close the milestone once the definition of done holds, and annotate the `w1/049` #5 register entry as closed by this milestone so scan round 8 doesn't re-report it.

## Context

- Closeout = the move: tasks to `w8/m20/done/`, README rows `— **DONE**` + `**Status:** done`, milestone to `w8/done/m20/`, checkbox in `w8/README.md`.

## Steps

1. Verify DoD evidence (tests + behavior).
2. Update `w1/049.md` #5 with the closed-by pointer.
3. Execute the move + status sync; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w8/` board files; `.pm/w1/049.md`

## Acceptance criteria

- [ ] DoD verified before the move; board and register consistent.

## Out of scope

- Other `w1/049` register items.

## Closeout record (2026-08-16)

DoD held at close, verified against real Postgres locally (fresh postgres:17
container) and by the full backend suite:

- **Transactional writes:** `TestPGGroupingTxRollsBackPartialSets` — an
  injected mid-loop failure leaves zero project/environment rows; a
  successful run commits everything (store.RunGroupingTx + tx facade over
  shared query helpers; pool methods delegate to the same SQL).
- **Quota:** `BEX_MAX_BLUEPRINT_GROUPINGS` (default 1000, 0 disables),
  checked incrementally inside the transaction so refusal rolls back to
  zero writes; `BLUEPRINT_GROUPING_LIMIT` via core.NewConflictError — the
  same coded-error machinery all three adapters render identically
  (AGENT_SESSION_LIVE_LIMIT precedent). Documented in CLAUDE.md +
  .env.example as a bex extension (Render has no grouping quota).
- **Audit + conditional ACL:** grouping mutations emit
  apps.BlueprintGrouping.* audit events post-commit with actor/workspace;
  unchanged re-syncs produce zero new rows, zero ACL writes, zero events
  (`TestBlueprintGroupingAuditAndIdempotentResync`).
- **Disconnect reclaim:** `DisconnectBlueprint` derives minted groupings
  from the stored manifest and sweeps only empty ones in one transaction —
  populated (apps-member) and CR-referenced (datastore label) groupings
  survive (`TestPGReclaimEmptyBlueprintGroupings`); the datastore reference
  scan fails CLOSED (skips the sweep on list errors) so a transient k8s
  failure can never delete a populated grouping. Deployed resources are
  never touched (Render disconnect semantics; MCP description updated).
- `w1/049` #5 register entry updated to materialized-and-closed.

Note: the CI harness (backend-test.yml) runs the same BEX_TEST_DB_URI-gated
tests against ephemeral Postgres on every push.
