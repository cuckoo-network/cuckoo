---
id: w8/m1/t007
title: Simplify — /simplify over the code this milestone changed
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m1/t006]
---

## Objective

Run `/simplify` over the metering pipeline code (reuse / simplification / efficiency; behavior-preserving) — in particular check that the usage package reuses `internal/metrics`'s Prometheus client rather than duplicating it, and that the three kinds share one rollup/upsert path.

## Context

- Standing closing task (`.claude/commands/pm.md`).

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; re-run the usage tests.

## Files

- `lego/backend/internal/usage/`, `lego/backend/internal/store/`, `lego/backend/cmd/api/`

## Acceptance criteria

- [ ] `/simplify` run; findings applied or explicitly declined; tests still green.

## Out of scope

- Bug hunting (that's `/code-review`).
