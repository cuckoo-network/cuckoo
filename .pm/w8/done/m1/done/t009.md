---
id: w8/m1/t009
title: Closeout — verify DoD, mark done, move milestone to done/
worker: worker8
status: done
estimate: 15m
depends_on: [w8/m1/t008]
---

## Objective

Close w8/m1 once its definition of done actually holds — rows accruing hourly for all three kinds with idempotent, restart-safe rollups — not merely when the code is written.

## Context

- Standing closing task; completing it _is_ the move (`/pm done w8/m1/t009` triggers the milestone move).

## Steps

1. Re-check the milestone DoD against t006's recorded evidence.
2. Set remaining task frontmatter `status: done`; move each `tNNN.md` to `w8/m1/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w8/m1/README.md`.
4. Move `w8/m1/` to `w8/done/m1/`; check `- [x]` for m1 in `w8/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w8/m1/`, `.pm/w8/README.md`

## Acceptance criteria

- [ ] DoD verified true before any move.
- [ ] Board state consistent in all three places (task frontmatter, milestone README, workstream README).

## Out of scope

- Prod activation (w1/m2/t007 owns the `BEX_CP_DB_URI` flip).
