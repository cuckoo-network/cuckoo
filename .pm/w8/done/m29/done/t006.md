---
id: w8/m29/t006
title: Test coverage
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m29/t005]
---

## Objective

Add meaningful hermetic tests for the branding overlay: command path, help chrome, safe replacer edge cases, docs destination, and version line — without snapshotting the entire upstream help tree.

## Context

- Existing `lego/cli/main_test.go` builds a test binary and asserts config isolation + version string.
- Replacer must assert `render.yaml` preserved and `render ` → `bex ` in examples.
- Prefer unit tests on the branding package plus one process-level `--help` / `-v` / docs smoke via the test binary.

## Steps

1. Unit-test the Short/Long/Example replacer fixtures (including `render.yaml`, `Render CLI`, `on Render`, backtick command forms).
2. Process-level: `bex --help` contains `bex` usage and does not advertise `Use: render` / `Render CLI v` as the product chrome; `bex -v` matches the softened line; if docs is testable without opening a browser, assert the overridden URL/handler (or Short text) without calling the real browser.
3. Keep asserting `.render/cli.yaml` isolation unchanged.
4. Update assertions that still expect `run \`render login\`` only if this milestone did not change that path (it should not — leave those tests as residual documentation of upstream copy).

## Files

- `lego/cli/internal/branding/*_test.go`
- `lego/cli/main_test.go`

## Acceptance criteria

- [ ] `cd lego/cli && go test ./...` green.
- [ ] Tests fail if Use stays `render` or help chrome reverts to `Render CLI v`.
- [ ] Replacer regression for `render.yaml` covered.

## Out of scope

- Live device-login e2e (unchanged; still `scripts/bex-cli-auth-e2e.sh` when auth env available).
- Asserting every upstream Short line.
