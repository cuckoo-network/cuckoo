---
id: w8/m19/t007
title: Official Render CLI blueprint evidence + refresh the ADR018 Blueprint row
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m19/t003, w8/m19/t004, w8/m19/t005, w8/m19/t006]
---

## Objective

Run Render's official CLI (`render-oss/cli` — the Jan-2026 release added blueprint validation) against bex-api, capture the evidence in `docs/cli-compatibility-checklist.md`, and refresh `docs/ADR018-render-parity.md`'s Blueprint row: ADR018 explicitly keeps the row ◐ partly for "outstanding full cross-surface CLI evidence". Move cells ◐→✅ only where the captured evidence supports it; record honest divergences otherwise.

## Context

- bex already implements Render's multipart `POST /v1/blueprints/validate` wire contract (w1/m63 t010) — the CLI should work unmodified.
- The parity review (2026-08-15) confirmed bex's blueprint API superset (create/sync-trigger endpoints Render lacks) — note extensions as extensions, not parity.
- Standing rule: gaps the CLI surfaces become bex-api work or documented workarounds, never a CLI fork.

## Steps

1. Install the current `render-oss/cli`; point it at a dev-stack bex-api; exercise every blueprint-related command (validate at minimum; sweep `render --help` for others).
2. Record one row per command in `docs/cli-compatibility-checklist.md` with captured output.
3. Update the ADR018 Blueprint row (line ~140): cell states, divergence notes (including this milestone's newly-translated fields), evidence links.
4. File inbox notes for any newly surfaced wire-format gaps instead of fixing inline.

## Files

- `docs/cli-compatibility-checklist.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Blueprint CLI command rows exist with captured evidence.
- [ ] ADR018 Blueprint row cells match the evidence; no cell upgraded without it.
- [ ] Any discovered gaps filed as inbox notes with repro.

## Out of scope

- Fixing CLI-surfaced wire gaps (separate notes/milestones).
