---
id: w8/m19/t010
title: Test coverage (registry dispositions, promoted handlers, path relaxation)
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m19/t008]
---

## Objective

Meaningful tests for the milestone's behavior: golden render.yaml fixtures per promoted field (compile → plan → App spec assertions), registry-validator assertions that no placeholder reasons remain, path-relaxation accept/reject tables, operator unit tests for `dockerContext`/`imagePullSecrets` Job/workload construction, and a drift-check dry-run. Tests assert real behavior and failure modes — no tautological or snapshot-everything tests.

## Context

- Existing patterns: `blueprint_pricing_test.go` fixture style; registry validator tests in `blueprint_schema.go`'s test file; operator envtest under `lego/operator/`.

## Steps

1. Add/extend fixtures for static `buildCommand`, `dockerContext`, `image.creds`, custom path.
2. Negative tests: still-unsupported fields fail at exact field paths; path escapes rejected.
3. `cd lego/backend && go test ./...` and `make test` from `lego/operator/`.

## Files

- `lego/backend/internal/apps/*_test.go` + `schema/` fixtures
- `lego/operator/` tests

## Acceptance criteria

- [ ] Both suites green with the new assertions.
- [ ] Each promoted field has positive + negative coverage.

## Out of scope

- Dashboard e2e (covered by existing route tests unless the path field changed behavior).
