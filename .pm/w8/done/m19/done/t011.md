---
id: w8/m19/t011
title: Closeout
worker: worker8
status: done
estimate: 15m
depends_on: [w8/m19/t010]
---

## Objective

Close the milestone once the definition of done actually holds: the mixed-field custom-path fixture deploys end to end, zero placeholder reasons remain, the drift check runs, and ADR018/checklist evidence is in place.

## Context

- Closeout = the move: set remaining tasks `done`, move `tNNN.md` files to `w8/m19/done/`, mark rows `— **DONE**` + `**Status:** done` in the milestone README, move `w8/m19/` to `w8/done/m19/`, check the box in `w8/README.md`.

## Steps

1. Verify DoD evidence.
2. Execute the move + status sync in all three places.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w8/` board files.

## Acceptance criteria

- [ ] DoD verified before the move; board consistent in all three status locations.

## Out of scope

- New work discovered late (file inbox notes).

## Closeout record (2026-08-16)

DoD held at close: the promoted-fields fixture (static `buildCommand` +
`dockerContext` + `image.creds` + custom subdirectory path) validates through
REST, GraphQL, and MCP with identical results
(`TestValidateBlueprintPromotedFieldsCrossSurface`); zero `unsupported`
registry entries carry the m63 placeholder (validator-enforced); the schema
pin is byte-identical to upstream (re-fetched 2026-08-16) and
`scripts/render-schema-drift.sh` passes locally with the weekly workflow's
scheduled runs green; ADR018/ADR049/cli-compatibility-checklist updated.
Build-plane behavior is proven by operator envtest + unit tests (static
native Dockerfile shape, BuildKit context args, containment). **Blocked
residual, filed as `w8/007`:** the live walk — the shared local CAPD
cluster's apiserver/etcd stayed degraded through two sessions; re-grade
against production after this milestone deploys.
