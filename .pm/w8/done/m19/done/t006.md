---
id: w8/m19/t006
title: Custom Blueprint filenames + subdirectory paths (relax `approvedBlueprintPath`)
worker: worker8
status: done
estimate: 45m
depends_on: []
---

## Objective

Match Render's 2026-02-09 feature: a Blueprint file may have any YAML filename in any subdirectory, set at creation and editable later (`path` is already PATCHable in bex). Relax `approvedBlueprintPath`/`discoverBlueprintFile` in `lego/backend/internal/apps/blueprint.go` from the basename allowlist (`render.yaml`/`bex.yml`) to any clean relative `*.yaml`/`*.yml` path, keeping the existing safety checks (clean, relative, no escapes) and the implicit-discovery defaults exactly as they are.

## Context

- Explicit path currently must have basename `render.yaml` or `bex.yml`; implicit discovery (`render.yaml` else `bex.yml`, ambiguous ⇒ error) should stay unchanged — only the explicit-path restriction relaxes.
- `bex.yml` deprecation warning behavior stays.
- Dashboard: `dashboard/src/routes/blueprints.new.tsx` already has a path field defaulting to `render.yaml` — update any client-side validation/copy; detail page Settings already PATCHes `path`.
- Webhook auto-sync (`triggerBlueprintSync` in webhook.go) matches by repo — confirm a non-default path still syncs on push.

## Steps

1. Relax the explicit-path check; keep traversal/absolute/dirty rejections.
2. Confirm push-to-sync and manual sync fetch the stored custom path (`GitFetcher.FetchBlueprintFile`).
3. Update dashboard path-field validation + locale strings (en/zh) if they hardcode the basename rule.
4. Tests: accepted (`infra/bex/stack.yaml`), rejected (`../escape.yaml`, absolute, non-yaml extension), discovery unchanged.

## Files

- `lego/backend/internal/apps/blueprint.go` (`approvedBlueprintPath`, `discoverBlueprintFile`)
- `lego/backend/internal/apps/webhook.go` (verify only)
- `dashboard/src/routes/blueprints.new.tsx` + `dashboard/src/features/blueprints/locales/{en,zh}.ts`

## Acceptance criteria

- [ ] Create/update/sync work with a subdirectory custom-named yaml across REST/GraphQL/MCP/dashboard.
- [ ] Traversal/absolute/non-yaml paths still rejected with the existing error shape.
- [ ] Implicit discovery + ambiguity error byte-identical to before.

## Out of scope

- Multiple blueprints per repo (Render allows one per resource; existing uniqueness semantics unchanged).
