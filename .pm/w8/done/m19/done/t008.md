---
id: w8/m19/t008
title: Render parity check (REST/GraphQL/MCP/UI consistency for every promoted field)
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m19/t007]
---

## Objective

For each surface this milestone changed — static `buildCommand`, `dockerContext`, registry credentials, custom paths, updated registry reasons — verify the behavior is identical across REST, GraphQL, and MCP (same fields, semantics, error shapes at the exact field path) and the dashboard, and compare against render.com's observable behavior. Flag any drift as follow-up inbox notes rather than silently diverging.

## Context

- bex-api is Render-compatible by design (`docs/ADR006-bex-api.md`); the blueprint validate error contract is the multipart multi-error shape from w1/m63 t010.
- Newly accepted fields must produce identical plans/validation results through `validate_bex_yml` (MCP), `validateBlueprint` (GraphQL), `POST /v1/blueprints/validate` (REST), and the `blueprints/new` preview.

## Steps

1. Run one fixture yaml exercising all promoted fields through all four surfaces; diff results.
2. Compare validation-failure shapes for still-unsupported fields (previews, disk, region) across surfaces.
3. Spot-check against Render docs/dashboard behavior for the promoted fields.
4. File notes for any drift.

## Files

- `lego/backend/internal/apps/` (rest.go / graphql.go / mcp.go blueprint registrations — verification, fixes only if trivial)

## Acceptance criteria

- [ ] Identical validation/plan output across REST/GraphQL/MCP for the promoted-field fixture.
- [ ] Divergences (if any) filed, not silently shipped.

## Out of scope

- Non-blueprint surfaces.
