---
id: w8/m19/t002
title: "Disposition sweep: replace the m63 audit-baseline placeholder on all unsupported entries"
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m19/t001]
---

## Objective

Walk all ~50 `unsupported` entries in `capabilities.json` and replace the placeholder reason `"m63 audit baseline: fail closed until a reviewed handler is implemented"` with a true disposition each: (a) **promote candidate** — handled by t003/t004/t005; (b) **permanent anti-goal** — cite `.pm/DO_NOT_DO.md` / ADR018 `—` row (previews, service disks, per-resource region, `checksPass`); or (c) **keep unsupported** with the concrete semantic mismatch stated (e.g. cron/static `preDeployCommand`: no pre-deploy phase for those types). Reasons only — no behavior change in this task beyond registry text.

## Context

- Registry: `lego/backend/internal/apps/schema/capabilities.json` (50 `unsupported` field pointers + enum values `checksPass`, PG `10`/`11`/`12`).
- Validator: `validateRenderBlueprintCapabilityRegistry` in `lego/backend/internal/apps/blueprint_schema.go` requires a reason on every entry — it must stay green.
- The blueprint parity review (2026-08-15) found nearly all entries still carry the conservative baseline reason, which reads as "unreviewed" rather than "decided".

## Steps

1. Enumerate every `unsupported` entry; bucket into (a)/(b)/(c).
2. Rewrite each `reason` accordingly; anti-goal entries cite their source decision.
3. `cd lego/backend && go test ./...` (registry validator + fixtures).
4. Update ADR049's D7 unsupported-subset list if the bucketing refines it.

## Files

- `lego/backend/internal/apps/schema/capabilities.json`
- `docs/ADR049-render-yaml-parity.md`

## Acceptance criteria

- [ ] Zero entries retain the m63 placeholder reason.
- [ ] Anti-goal entries cite DO_NOT_DO / ADR018; keep-unsupported entries state a concrete mismatch.
- [ ] Backend tests pass; validation error messages at those field paths still fire (behavior unchanged).

## Out of scope

- Implementing any handler (t003–t005).
