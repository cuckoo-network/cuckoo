---
id: w8/m21/t002
title: Pre-sync plan/diff on the blueprint detail page (reuse `PreviewBlueprint`)
worker: worker8
status: done
estimate: 45m
depends_on: []
---

## Objective

The detail page's Sync button first fetches `blueprintPreview` (Git-fetched manifest → validation plan + estimated pricing + errors) and shows it in a confirm dialog — create/update resource groups, `totalActions`, pricing panel, any validation errors — applying only on confirm. Render shows a computed resource diff before sync; bex currently applies blindly.

## Context

- `useBlueprintPreview` + the plan/pricing components already exist and render on `blueprints.new.tsx` — reuse, don't duplicate (`EstimatedPricingPanel`, plan-group badges).
- Current sync path: `handleSync` in `dashboard/src/routes/blueprints.$blueprintId.tsx` posts directly; keep the existing protected-resource `confirmation_required` dialog as the second step when returned.
- Preview requires `can_view_sensitive` (manifest content) while sync needs `can_create` — a syncer always holds both; handle a preview 403 by falling back to the current direct-sync-with-warning rather than blocking.

## Steps

1. Extract the review/plan presentation from `blueprints.new.tsx` into a shared component under `features/blueprints/components/`.
2. Wire Sync → preview dialog → confirm → existing sync call; loading/error states; locale strings.
3. Route test: sync click shows plan; confirm fires mutation; cancel fires nothing.

## Files

- `dashboard/src/routes/blueprints.$blueprintId.tsx`
- `dashboard/src/features/blueprints/components/` (shared review panel)
- `dashboard/src/routes/blueprints.new.tsx` (consume the extraction)
- locales

## Acceptance criteria

- [ ] Manual sync always shows the plan first; blind apply gone; protected-confirm still works after.
- [ ] Preview failure degrades gracefully (warning + proceed option), never a dead button.

## Out of scope

- Auto-sync (push-triggered syncs stay headless by design).
