---
id: w8/m21/t003
title: Editable name + path in blueprint detail settings
worker: worker8
status: done
estimate: 30m
depends_on: []
---

## Objective

Expose the already-supported PATCH fields in the detail page: inline edit for blueprint name and file path (path validated client-side as clean relative `.yaml`/`.yml`, matching the m19-relaxed `approvedBlueprintPath`). Branch stays read-only — it is part of the blueprint identity key and not PATCHable.

## Context

- `UpdateBlueprint` binds name/autoSync/path (`lego/backend/internal/apps/blueprint.go`); the dashboard `update()` hook already exists and is used for the autoSync toggle (`blueprints.$blueprintId.tsx` ~line 120).
- Render exposes path editing in Blueprint Settings (Feb-2026 feature); name rename is standard.

## Steps

1. Add edit affordances (pencil/inline form) for name + path; optimistic update via the existing `useUpdateBlueprint`.
2. Surface the backend's path-validation error shape inline; locale strings.
3. Route test: PATCH fired with only the changed field; branch has no edit control.

## Files

- `dashboard/src/routes/blueprints.$blueprintId.tsx`
- `dashboard/src/features/blueprints/` (hooks/locales as needed)

## Acceptance criteria

- [ ] Name and path editable end to end; next sync fetches the new path.
- [ ] Branch not editable; invalid paths rejected with the backend's message shown.

## Out of scope

- Re-pointing repo/branch (disconnect + recreate is the flow, matching Render).
