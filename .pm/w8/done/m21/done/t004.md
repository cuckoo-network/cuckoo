---
id: w8/m21/t004
title: Render parity check (create-prompt/sync-review flows vs render.com; envVarValues across surfaces)
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m21/t001, w8/m21/t002, w8/m21/t003]
---

## Objective

Compare the finished flows against Render's: sync:false prompting at creation, pre-sync diff presentation, settings editability. Verify `envVarValues` is accepted consistently across REST/GraphQL/MCP create verbs (check whether MCP `create_blueprint` exposes it — if not, that's drift to fix or file), with the same unknown-key rejection shape. File notes for any drift.

## Context

- `docs/ADR006-bex-api.md` (one core, thin adapters); MCP registration in `lego/backend/internal/apps/mcp.go`.

## Steps

1. Walk Render's documented create/sync flows; diff against the shipped UX.
2. Exercise `envVarValues` on all three API surfaces; compare error shapes for unknown keys.
3. Update ADR018 Blueprint row notes if a divergence closes or opens.

## Files

- verification; `docs/ADR018-render-parity.md` note if warranted

## Acceptance criteria

- [ ] Cross-surface `envVarValues` behavior identical or drift filed.
- [ ] UX divergences vs Render recorded, not silent.

## Out of scope

- Non-blueprint dashboard surfaces.
