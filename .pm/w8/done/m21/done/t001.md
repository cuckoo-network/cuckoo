---
id: w8/m21/t001
title: "`sync: false` prompt step on blueprints/new + `envVarValues` wiring"
worker: worker8
status: done
estimate: 45m
depends_on: []
---

## Objective

When the create-page preview's validation plan reports `syncFalseVars`, render one secret-masked input per prompt key (grouped by service, Render-style) on the review step, and send the supplied values as `envVarValues` on create. Keys left blank are simply omitted (backend seeds nothing — current behavior).

## Context

- Backend done: prompt keys surface on the validation plan (`syncFalseVars`, `lego/backend/internal/apps/blueprint.go` ~line 906); REST `POST /v1/blueprints` and GraphQL `createBlueprint` accept `envVarValues`; unknown keys are rejected by `validateBlueprintEnvVarValues`.
- Dashboard gaps: `CreateBlueprintDocument` (`dashboard/src/features/blueprints/api/operations.ts`) lacks the variable; `blueprints.new.tsx` review section never reads `syncFalseVars`. Verify the GraphQL arg name/shape against `graphql.go` (`envVarValues` list) before wiring.
- Values are secrets: input `type="password"`, never logged, not kept in router state after submit.

## Steps

1. Add `envVarValues` to `CreateBlueprintVars`/document; confirm against the schema.
2. Render the prompt inputs on the review step when `syncFalseVars` is non-empty; disable Deploy until required interaction is acknowledged (blank allowed, but show a warning that unset secrets deploy empty).
3. Thread values into the create call; add en/zh locale strings.
4. Route test: manifest fixture with prompt keys ⇒ inputs render, values included in mutation variables.

## Files

- `dashboard/src/features/blueprints/api/operations.ts`
- `dashboard/src/routes/blueprints.new.tsx`
- `dashboard/src/features/blueprints/locales/{en,zh}.ts`
- `dashboard/src/routes/__tests__/blueprints-new.test.tsx`

## Acceptance criteria

- [ ] Prompt inputs appear for a sync:false manifest; supplied values reach the mutation; blanks omitted.
- [ ] Deployed dev-stack service has the supplied value set; re-sync preserves it (seed-once).
- [ ] No secret value appears in URLs, logs, or persisted client state.

## Out of scope

- Editing sync:false values post-create (normal env editor covers it).
