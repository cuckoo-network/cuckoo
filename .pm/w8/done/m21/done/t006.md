---
id: w8/m21/t006
title: "Test coverage (route tests: prompt step, preview dialog, settings PATCH)"
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m21/t004]
---

## Objective

Meaningful dashboard route tests: prompt inputs render only for sync:false manifests and values land in mutation variables (blanks omitted); Sync shows the plan and confirm/cancel behave; name/path PATCH sends only changed fields and surfaces backend validation errors; secret values never persist in client state. Assert behavior and failure modes, not snapshots.

## Context

- Existing pattern: `dashboard/src/routes/__tests__/blueprints-new.test.tsx` (mocked Apollo operations).

## Steps

1. Extend/add route tests per flow, including the preview-403 graceful-degrade path.
2. `yarn test` green.

## Files

- `dashboard/src/routes/__tests__/` (blueprints-new + new detail-page test file)

## Acceptance criteria

- [ ] All flows covered incl. negative paths; suite green in CI.

## Out of scope

- Playwright e2e (live walkthrough is the closeout evidence).
