---
id: w8/m9/t001
title: storage_gb_seconds meter schema
worker: worker8
status: done
estimate: 45m
depends_on: []
---

## Objective

Add a `storage_gb_seconds` column/meter type alongside the existing hourly usage rollup table.

## Context

- `w8/m1`'s hourly usage rollup table already meters instance-seconds by tier, egress bytes, and build minutes; this adds a fourth meter dimension.

## Steps

1. Add a migration for the new `storage_gb_seconds` column on the usage rollup table (or a new row-type in an existing metered-quantity model, whichever matches the existing schema shape).
2. Extend the retention/compaction logic (`w8/m4`) to include the new column if it operates generically over meter columns, or add it explicitly if not.

## Files

- `lego/backend/internal/store/migrations/`
- `lego/backend/internal/usage/` (or wherever the rollup model lives)

## Acceptance criteria

- [ ] New column/meter exists, defaults to zero for existing rows.
- [ ] Retention/compaction (`w8/m4`) handles it without special-casing failures.

## Out of scope

- Populating the meter (t002).
