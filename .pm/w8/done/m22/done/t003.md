---
id: w8/m22/t003
title: "Dashboard entry: select resources → download render.yaml"
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m22/t002]
---

## Objective

A "Generate Blueprint" affordance on the blueprints list page (mirroring Render's dashboard placement): resource multi-select (services/datastores/env groups) → preview the generated yaml → download as `render.yaml`. Client-side download only; nothing persisted.

## Context

- List route: `dashboard/src/routes/blueprints.tsx`; resource lists available via existing services/datastores queries.
- Show the yaml in a read-only code block with copy + download; note that secret values are omitted (`sync: false`) so users aren't surprised.

## Steps

1. Selection UI + `generateBlueprint` query hook + download/copy actions; locale strings (en/zh).
2. Route test: selection → query variables; download blob content matches response.

## Files

- `dashboard/src/routes/blueprints.tsx`
- `dashboard/src/features/blueprints/` (hook, component, locales)

## Acceptance criteria

- [ ] Select → preview → download works live; empty selection disabled with hint.
- [ ] Secrets note visible; i18n complete.

## Out of scope

- Auto-committing the file to a repo (user commits it themselves, as on Render).
