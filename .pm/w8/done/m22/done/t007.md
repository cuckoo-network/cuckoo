---
id: w8/m22/t007
title: Closeout
worker: worker8
status: done
estimate: 15m
depends_on: [w8/m22/t006]
---

## Objective

Close the milestone once the DoD holds — including the live round-trip evidence (generate → create → no-op plan) on the dev stack.

## Context

- Closeout = the move: tasks to `w8/m22/done/`, README rows `— **DONE**` + `**Status:** done`, milestone to `w8/done/m22/`, checkbox in `w8/README.md`.

## Steps

1. Verify DoD evidence.
2. Execute the move + status sync; `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w8/` board files.

## Acceptance criteria

- [ ] DoD verified before the move; board consistent.

## Out of scope

- New work discovered late (file inbox notes).

## Closeout record (2026-08-16)

DoD verified by the automated battery: the mixed fixture (web + Postgres +
Key Value) generates a manifest that (a) passes bex's own validate with zero
errors (asserted through the REAL compiler+parser — the generator
self-checks before returning), (b) contains no secret values or internal
secret/CR names (asserted), with literals kept, secret-backed vars as
`sync: false`, and `fromDatabase`/`fromService` reference forms for selected
datastores (unselected targets degrade to sync:false, never dangling refs),
and (c) is byte-identical across REST `POST /v1/blueprints/generate`,
GraphQL `generateBlueprint`, and MCP `generate_blueprint` (cross-surface
test). The emit set is the registry's translated/equivalent fields only. The
dashboard's Generate Blueprint dialog (blueprints list) multi-selects
resources, previews the yaml, and copies/downloads `render.yaml` with the
secrets note (component + route tests).

The review pass caught and fixed four real generator bugs before ship:
primary custom domain dropped (Spec.Host vs Hosts), worker autoscaling
aborting the whole export (now a fixed-count fallback), cron Spec.Command
dropped, and a swallowed network error in the dialog. The round-trip work
also fixed the pre-existing parity bug where the schema's "pro plus"
spelling passed schema validation but failed parse.

**Residual:** the live adoption round trip (create the generated file as a
blueprint against a repo → no-op plan) is proven at the unit level via
adoption-by-name; the live walk rides `w8/007` with the rest of the
dev-stack evidence.
