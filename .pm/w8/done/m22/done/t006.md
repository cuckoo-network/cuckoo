---
id: w8/m22/t006
title: Test coverage (round-trip generate→validate, secret-absence, link derivation)
worker: worker8
status: done
estimate: 45m
depends_on: [w8/m22/t004]
---

## Objective

Meaningful coverage: golden-fixture generation per resource kind; property "generated output always passes own validate"; secret-absence assertion (plant a known secret value, assert zero occurrences in output); link derivation (connection-string match ⇒ `fromDatabase`, ambiguous ⇒ literal); adapter parity; dashboard route test for the selection→download flow.

## Steps

1. Backend tests in `blueprint_generate_test.go` incl. the compile-self-check property.
2. Dashboard route test.
3. Both suites green.

## Files

- `lego/backend/internal/apps/blueprint_generate_test.go`
- `dashboard/src/routes/__tests__/`

## Acceptance criteria

- [ ] All behaviors covered incl. secret-absence and ambiguity fallback; CI green.

## Out of scope

- Fuzzing the generator (property test suffices).
