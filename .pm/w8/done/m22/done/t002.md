---
id: w8/m22/t002
title: Expose `generate` across REST/GraphQL/MCP
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m22/t001]
---

## Objective

Thin adapters for the generate verb: `POST /v1/blueprints/generate` (body: resource ids; response: yaml string + suggested filename), GraphQL `generateBlueprint` query, MCP `generate_blueprint` tool. Identical semantics and error shapes (unknown/foreign resource id ⇒ the standard not-found dialect; empty selection ⇒ 400-class).

## Context

- Render has **no** public generate API (dashboard-only) — this verb is a bex extension; describe it as such in the MCP tool description (extension-labeling precedent: usage API).
- Route registration: `registerBlueprintRoutes` (`rest.go`), `graphql.go` blueprint block, `registerBlueprintTools` (`mcp.go`).

## Steps

1. Add the three adapters over the core function; audit-log the read like other sensitive reads.
2. Adapter-parity test: same fixture ids ⇒ byte-identical manifest on all three surfaces.

## Files

- `lego/backend/internal/apps/{rest,graphql,mcp}.go`

## Acceptance criteria

- [ ] Byte-identical output across surfaces; consistent error shapes for bad ids.
- [ ] MCP description labels it a bex extension.

## Out of scope

- CLI (no Render CLI command exists to mirror).
