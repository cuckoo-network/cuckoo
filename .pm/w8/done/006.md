# Backfill `pg_stat_statements` on pre-m25 Postgres clusters

`w2/m25` enabled `pg_stat_statements` in the CNPG spec for new clusters, but databases provisioned earlier degrade to an empty Top Queries list forever (`lego/backend/internal/postgres/insights.go:257` — graceful, but never self-heals). Reconcile the extension onto existing CNPG clusters (spec update ± `CREATE EXTENSION` on the primary) so pre-m25 databases gain query insights. Sub-hour: the operator already reconciles parameter overrides (w2/m25's write path) — this rides the same seam.

Source: `/pm-brainstorm` round 12, 2026-07-15 (code miner).
