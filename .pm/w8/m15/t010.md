---
id: w8/m15/t010
title: Render parity — audit REST, GraphQL, MCP, dashboard, and documented category semantics
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m15/t009]
---

## Objective

Verify every tenant-visible surface reports the same expanded `egress_bytes` quantity and that category labels accurately describe similarities and differences from Render outbound bandwidth.

## Context

- The usage schema stays stable, but values expand from HTTP-only to full documented outbound categories.
- `monthToDateBandwidth` exposes Render-shaped HTTP/NAT/WebSocket fields and currently reports an HTTP-only subset.
- Dashboard copy and estimated-spend labels must not retain the old narrower meaning.

## Steps

1. Compare REST, GraphQL, MCP, metrics GraphQL, and dashboard semantics and totals.
2. Re-check Render's current first-party outbound categories and included-private-traffic rules.
3. Update `docs/ADR018-render-parity.md` and flag any deliberate residual drift.

## Files

- `lego/backend/internal/usage/`
- `lego/backend/internal/metrics/`
- `dashboard/`
- `docs/ADR018-render-parity.md`
- `docs/ADR023-usage-metering.md`

## Acceptance criteria

- [ ] All usage surfaces agree on the same workspace/resource totals.
- [ ] Render-shaped category fields contain real categories or an explicit documented omission.
- [ ] Dashboard and pricing copy no longer call an HTTP-only value full outbound bandwidth.

## Out of scope

- Overage billing, invoices, or payment collection.
