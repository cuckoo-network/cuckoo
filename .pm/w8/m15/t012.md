---
id: w8/m15/t012
title: Test coverage — source failures, counter resets, attribution, and traffic matrix
worker: worker8
status: todo
estimate: 45m
depends_on: [w8/m15/t010]
---

## Objective

Add meaningful regression coverage for the complete accounting contract, especially cases that would silently undercount, double count, or cross-attribute tenant traffic.

## Context

- Meter correctness spans eBPF lifecycle, proxy directionality, Prometheus reset behavior, and hourly collector retries.
- Snapshot-only or happy-path tests would not protect quota safety.

## Steps

1. Add unit tests for packet classification, identity reuse, proxy byte direction, and query composition.
2. Add integration tests for source unavailability, reset, partial-source failure, retry, and idempotency.
3. Wire the focused Cilium traffic matrix into an appropriate non-casual CI target.

## Files

- `lego/operator/internal/egressmeter/`
- `lego/operator/cmd/pg-sni-proxy/`
- `lego/operator/cmd/kv-sni-proxy/`
- `lego/backend/internal/usage/`
- `scripts/verify-egress-meter.sh`
- `.github/workflows/`

## Acceptance criteria

- [ ] Tests fail for a silently dropped source, reused identity, reversed proxy direction, or duplicate composition.
- [ ] Operator and backend suites pass, including race tests for concurrent counters.
- [ ] The live traffic-matrix target is isolated from routine local test runs.

## Out of scope

- Artificial line-coverage targets or tautological snapshots.
