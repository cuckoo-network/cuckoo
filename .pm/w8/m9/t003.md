---
id: w8/m9/t003
title: REST/GraphQL/MCP usage surface for storage meter
worker: worker8
status: todo
estimate: 1h
depends_on: [w8/m9/t002]
---

## Objective

Expose `storage_gb_seconds` on the usage API across REST, GraphQL, and MCP, following `w8/m2`'s existing pattern.

## Steps

1. Add the new meter to the usage response schema on all three surfaces.
2. Include it in `w8/m6`'s multi-month history support if that's a generic-over-meters implementation (verify, extend if needed).

## Files

- `lego/backend/internal/usage/{rest,graphql,mcp}.go` (or equivalent)

## Acceptance criteria

- [ ] `GET /v1/usage` (and GraphQL/MCP equivalents) include storage-GB-seconds alongside existing meters.

## Out of scope

- Dashboard UI (t004).
