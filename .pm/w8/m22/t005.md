---
id: w8/m22/t005
title: Simplify (`/simplify` over the changed code)
worker: worker8
status: todo
estimate: 30m
depends_on: [w8/m22/t004]
---

## Objective

Run `/simplify` over the milestone diff — the generator should share serialization with `render.go` rather than fork it; the compiler self-check should reuse existing entry points. Behavior-preserving only.

## Steps

1. `/simplify` scoped to the diff; apply accepted cleanups.
2. `cd lego/backend && go test ./...` + `yarn test` in `dashboard/`.

## Files

- Milestone diff surface.

## Acceptance criteria

- [ ] `/simplify` run; cleanups applied; tests green.

## Out of scope

- Bug hunting.
