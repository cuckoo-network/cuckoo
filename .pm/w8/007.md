# Live re-grade: m19 promoted blueprint fields + CLI validate against a running stack

`w8/done/m19` promoted static `buildCommand`, `dockerContext`, and
`registryCredential`/`image.creds`, and relaxed Blueprint paths to any clean
repo-relative `.yaml`/`.yml` — proven by fixture/envtest/cross-surface tests,
but the live legs were blocked by the shared local CAPD cluster's chronic
apiserver/etcd degradation (2026-08-15/16 sessions).

After the m19 deploy is green on prod:

1. `render blueprints validate` (unmodified v2.21.0, `RENDER_HOST=https://api.bex.co/v1/`)
   with a fixture using every promoted field + a custom `.yaml` path — upgrade
   the `blueprints validate` row in `docs/cli-compatibility-checklist.md` from
   repository-regression to live evidence.
2. A real static site with `buildCommand` (no Dockerfile) reaching `deployed`
   and serving the BUILT output (native static build end to end).
3. A monorepo `dockerContext` build and a private-image `image.creds` pull.

Also: the shared dev cluster's own health (etcd on the CI-runner-loaded Docker
host; `bex-t598l-g95zs` apiserver restart counts in the hundreds) deserves a
look — consider moving dev clusters off that host.
