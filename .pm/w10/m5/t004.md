---
id: w10/m5/t004
title: Dashboard audit page renders friendly target names (id fallback)
worker: worker10
status: todo
estimate: 30m
depends_on: [w10/m5/t003]
---

## Objective

The dashboard audit table shows what each row acted on by its human name — `targetName` when present, falling back to the existing raw `resource` id for pre-0038 rows — instead of only kind-prefixed ids.

## Context

- `dashboard/src/features/audit/api/audit.graphql` — selects `resource` today, no target field.
- `dashboard/src/features/audit/components/audit-event-row.tsx` — row rendering; no target-name rendering exists.
- t003 ships the `targetName` GraphQL field (null/empty for pre-0038 rows).
- Follow `dashboard/CLAUDE.md` conventions; add en/zh locale strings if a new column header is introduced.

## Steps

1. Add `targetName` to the audit query selection; regenerate typed GraphQL artifacts.
2. Render the name in the audit row with the id fallback (name absent → current behavior, byte-identical).
3. Hook/component test: one row with a name, one without; `yarn test` + `yarn lint`.

## Files

- `dashboard/src/features/audit/api/audit.graphql`
- `dashboard/src/features/audit/components/audit-event-row.tsx`
- `dashboard/src/features/audit/locales/` (if a header/label is added)

## Acceptance criteria

- [ ] An audit row whose event carries a target name shows the friendly name.
- [ ] Pre-0038 rows (empty `target_name`) render exactly as today.
- [ ] Dashboard tests cover both cases.

## Out of scope

- Any new audit filtering/search by target name.
