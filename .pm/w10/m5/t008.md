---
id: w10/m5/t008
title: Closeout
worker: worker10
status: todo
estimate: 15m
depends_on: [w10/m5/t007]
---

## Objective

When the other tasks are complete and the definition of done actually holds (verbs truthful, plan pair recorded, target names rendered end-to-end, tests green), close the milestone: set remaining tasks `status: done`, move each `tNNN.md` to `w10/m5/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone README, move `w10/m5/` to `w10/done/m5/`, and check the box in `w10/README.md`.

## Context

- `.pm/CLAUDE.md` done-conventions; running `/pm done w10/m5/t008` last triggers the milestone move.

## Steps

1. Verify the DoD line by line against live behavior, not just merged code.
2. Execute the move + status sync in all three places.

## Files

- `.pm/w10/m5/` → `.pm/w10/done/m5/`
- `.pm/w10/README.md`

## Acceptance criteria

- [ ] DoD verified real; milestone in `done/`; checkbox, README status, and task frontmatter all in sync; no tombstone left at the old path.

## Out of scope

- New work discovered at closeout — file it as notes/follow-ups instead.
