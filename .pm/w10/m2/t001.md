---
id: w10/m2/t001
title: Fix stale ADR006 claims + `render-artifacts/key-value.md:27`
worker: worker10
status: todo
estimate: 30m
depends_on: []
---

## Objective

Three doc claims contradict shipped reality — correct each with a pointer to the shipping milestone: `docs/ADR006-bex-api.md:521` still says "Not yet: rollback" (shipped `w2/m10`); `:285` still says "Still deferred: failover/HA + read replicas (→ w1/013), and Postgres runtime observability" (shipped `w1/m22` + `w2/m25`); `docs/render-artifacts/key-value.md:27` still says plan changes post-create are unsupported (shipped `w2/m16`).

## Steps

1. Verify each shipped claim against the code/board before editing (don't swap one stale line for another).
2. Rewrite the three passages to current truth with evidence pointers.

## Files

- docs/ADR006-bex-api.md
- docs/render-artifacts/key-value.md

## Acceptance criteria

- [ ] Grep for the three quoted stale phrases returns nothing; replacements cite the shipping milestones.

## Out of scope

- ADR018 markers (`w5/013` owns those).
