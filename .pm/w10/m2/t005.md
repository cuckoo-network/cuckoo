---
id: w10/m2/t005
title: Clear the 16 pre-existing `pg-sni-proxy`/`build_test` lint findings
worker: worker10
status: todo
estimate: 45m
depends_on: []
---

## Objective

`make lint` has carried 16 findings in `cmd/pg-sni-proxy` + `internal/build/build_test.go` through multiple closeouts (recorded in `w8/done/m12`, `w2/done/m36`) — fix them properly (no blanket `//nolint`s) so a clean lint stops being caveated.

## Files

- lego/backend/cmd/pg-sni-proxy/ (or its actual module home — locate first)
- lego/operator/internal/build/build_test.go

## Acceptance criteria

- [ ] `make lint` from lego/operator reports zero findings across both modules; no suppression comments without a stated reason.
