---
id: w10/m2/t003
title: Thread `maxShutdownDelaySeconds` through the `bex.yml` parser
worker: worker10
status: todo
estimate: 45m
depends_on: []
---

## Objective

Close ADR006:160's recorded drift: the CRD/operator and REST/GraphQL/MCP/UI support `maxShutdownDelaySeconds` (`w6/m25`), but the `bex.yml` Blueprint parser drops it. Accept the field with w6/m25's validation and map it onto the App spec.

## Files

- lego/backend/internal/apps/deploy.go (bex.yml types + mapping)
- docs/ADR006-bex-api.md (remove the recorded-drift note)

## Acceptance criteria

- [ ] A bex.yml carrying the field produces an App with it set; out-of-range values get the same named validation error as REST.

## Out of scope

- Other bex.yml field gaps (each needs its own evidence).
