---
id: w10/m2/t002
title: Backfill the 5 missing `set_*` MCP tools into ADR006's reference table
worker: worker10
status: todo
estimate: 30m
depends_on: []
---

## Objective

`docs/ADR006-bex-api.md`'s MCP table omits `set_auto_deploy`, `set_health_check_path`, `set_root_dir`, `set_build_filter`, `set_pre_deploy_command` — all real tools in `lego/backend/internal/apps/mcp.go` (the table's own :352 note admits it; `w4/done/m21` filed it as a follow-up that never landed). Backfill them, and sweep `mcp.go` for any other tool the table missed.

## Files

- docs/ADR006-bex-api.md

## Acceptance criteria

- [ ] Every registered MCP tool in `apps/mcp.go` has a table row; the self-referential ":352" admission note is removed.
