---
id: w10/m6/t002
title: Prune the orphaned DatabaseLogs query document + generated types
worker: worker10
status: todo
estimate: 15m
depends_on: []
---

## Objective

Delete the dashboard-side `DatabaseLogs` GraphQL document and its generated types — dead since `6f1bbaa7` re-pointed the Postgres log viewer at the generic `LogsDocument` — leaving the backend `databaseLogs` field alone (MCP consumes it; `w3/m30/t006` extends it).

## Context

- `dashboard/src/features/databases/api/databases.graphql:382` (`query DatabaseLogs`); `operations.ts:608-645` (`DatabaseLogsDocument`, `DatabaseLogsQuery/Vars`, `DatabaseLogEntry`).
- Verified: `grep DatabaseLogsDocument dashboard/src` hits only the definition files.

## Steps

1. Remove the query + regenerate/trim the generated types.
2. `yarn typecheck` + tests green.

## Files

- `dashboard/src/features/databases/api/databases.graphql`, `api/operations.ts`

## Acceptance criteria

- [ ] No `DatabaseLogs*` symbols remain in `dashboard/src`; typecheck + tests green.

## Out of scope

- The backend `databaseLogs` GraphQL field (kept — MCP consumer).
