---
id: w10/m4/t002
title: "ADR018 stale-marker sweep: cron build-command + email-recovery rows (from w5/013)"
worker: worker10
status: todo
estimate: 30m
depends_on: []
---

## Objective

Verify and correct two potentially stale ◐ markers in `docs/ADR018-render-parity.md`: the Cron row still defers "Build Command" though `w6/m22`'s closeout claims Render-native runtime/build/start compatibility shipped for kpack; the email-recovery/verification row cites "live SMTP courier → w4/m7" though w4/m7 closed 2026-07-12.

## Context

- Scope inherited from `.pm/w5/done/013.md`; the Header-rules item in that note was already resolved by the w5/m32 walk — out of scope here.
- Verify against code/closeouts, not claims: does a git-sourced cron honor a build command today? Is the SMTP courier live?
- **Coordinate:** `w4/m20/t003` owns the MFA-row + email-recovery *pointer* fixes — touch only what `w5/013` scoped; if m20/t003 already landed the email-recovery row, record that and skip.

## Steps

1. Check cron build-command support in the kpack path; update the Cron row with evidence or record why ◐ stands.
2. Check the email-recovery row's current state vs w4/m7's closeout; update if stale and unclaimed by m20/t003.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Both rows reflect verified current truth with evidence pointers, or a recorded reason they stand.

## Out of scope

- Any other ADR018 row (w4/m20/t003's rows in particular).
