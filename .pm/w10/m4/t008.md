---
id: w10/m4/t008
title: Closeout
worker: worker10
status: todo
estimate: 15m
depends_on: [w10/m4/t007]
---

## Objective

When the other tasks are complete **and the definition of done is actually met**, close the milestone: set every remaining task's `status: done`, move each `tNNN.md` to `w10/m4/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone README, move the whole `w10/m4/` directory to `w10/done/m4/`, and check `- [x]` in the workstream README. The four source notes were moved to their workstreams' `done/` at materialization — confirm each outcome is recorded where its note promised.

## Acceptance criteria

- [ ] DoD verified real (all four note outcomes evidenced), not merely code-written.
- [ ] Board state synced in all three places.
