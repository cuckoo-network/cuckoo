# w10 — project workstream (worker10)

**Worker:** worker10. This is a general-purpose bex workstream. It may take work anywhere in the project; the milestones below are scheduled work and historical records, not a permanent purpose, specialty, or ownership boundary.

## Local dev environment

Develop against `.pm/w10/dev-10/`, this worker's own isolated stack on the shared local kind/CAPD cluster — never the shared cluster's default `auth`/`bex-system` namespaces or standard ports (5173/4433/4445/8090/8091/5432), which any other worker's session may also be using. `dev-10` gets its own Kratos + Hydra + Mailpit (namespace `dev-10-auth`) and app namespace (`dev-10`), reusing the shared cluster's CNPG operator and bex operator, plus a locally-built `bex-api` on dedicated ports derived from N=10 (`dev-10/ports.env`) so it never collides with any other workstream's `dev-N`.

- `bash scripts/dev-env.sh 10 up` — bring it up (idempotent — safe to re-run)
- `bash scripts/dev-env.sh 10 status` — health check (processes, pods, HTTP) + a pass/fail verification inventory
- `bash scripts/dev-env.sh 10 down` — tear it down (leaves the shared cluster and every other workstream's `dev-N` untouched)
- `bash scripts/dev-env.sh 10 clean` — reclaim `logs/` and `bin/` (refuses while the environment is up)

`up` prints the dashboard command to point at it once bex-api is running. One shared
implementation serves every workstream since `w1/m72`; `.pm/w10/dev-10/` keeps only
`ports.env` (a generated record of the derivation), this README, and `.gitignore`.

## Milestones

- [x] **m1** — Restore operator reconciliation under namespace-scoped Secret RBAC (6 tasks) ← from `w10/001` production incident
- [x] **m2** — Docs & code truth sweep (9 tasks) ← from `/pm-brainstorm` round 12, 2026-07-15 (this round's miners returned three false gaps traced to stale docs — ADR006:521/:285, `render-artifacts/key-value.md:27`; plus the twice-filed MCP-table backfill, `maxShutdownDelaySeconds` bex.yml drift, `gqlStr`×7, and the recurring 16-finding lint debt); coordinates with, never duplicates, `w5/013`
- [x] **m3** — Ledger & board truth sweep round 2: ADR018 backlog + FUTURE-MAYBE sync (8 tasks) ← from `/pm-brainstorm` round 13, 2026-07-15 (five ADR018 gap-backlog rows cite resolved `done/` items as open owners; ADR028/events-row pointer gaps; FUTURE-MAYBE entries 2+4 out of sync; coordinates with, never duplicates, `w5/013` and w10/m2); t008 added by round 15 (w7/done/m36 task-file drift + w4 README m23 checkbox) — done 2026-07-15 (three ADR018 backlog rows + events/PR-previews/untracked rows corrected with verified pointers; FUTURE-MAYBE w8/m7 → Done + double-delivery clause; w7/done/m36 bookkeeping synced; several cited drifts found already fixed by their own closeouts and verified as such), moved to `done/m3/`
- [x] **m4** — Verification & evidence chores: cross-stream note burn-down round 1 (8 tasks) ← from `/pm-brainstorm` round 19, 2026-07-15 (user directive to give w10 a milestone; bundles the four open cross-stream sub-hour notes — `w4/020` mfaEnabled webauthn-stub false positive, `w5/013` ADR018 stale markers, `w1/024` undeclared prod default-SA `imagePullSecrets` patch, `w4/024` orphaned live Render audit capture — the w7/m37 / w8/m16 chores pattern applied across workstreams per w10's spare-capacity charter; notes moved to their workstreams' `done/`) — done 2026-07-15 (mfaEnabled fix + per-shape tests; both ADR018 rows verified already truthful; prod SA patch verified gone with the declared operator path load-bearing; audit capture blocked on a real RENDER_API_KEY, re-owned by `w10/002`), moved to `done/m4/`
- [x] **m5** — Audit-log fidelity: correct verbs, plan detail, friendly target names (8 tasks) ← from `/pm-brainstorm` round 20, 2026-07-15 (shipped-diff mine findings 2–3: no-op `SetPlan` records `Update*`, datastore plan changes drop `PlanFrom`/`PlanTo`; dashboard-gap mine G4: migration 0038's `target_name` never reached GraphQL/dashboard — the code's own "until a consumer exists" scope cut, picked up now that all three defect sites are in day-old code) — done 2026-07-15 (SetPlan always records the plan verb + typed pair per apps' precedent; `targetName` GraphQL field + dashboard name-with-id-fallback cell; the colliding-0038-migration renumber it hit was fixed upstream in `42e3289e` mid-flight), moved to `done/m5/`
- [x] **m6** — Cross-stream note burn-down round 2: Traefik LB label lifecycle + orphaned DatabaseLogs prune (5 tasks) ← from `/pm-brainstorm` round 21, 2026-07-15, per the user's standing "for w9 and w10" capacity directive (the m4 pattern): absorbs `w1/025` (missing `ignore_changes = [labels]` on the production Traefik LB — armed on every `terraform apply`) + the round's dashboard-gap mine G2 (the `DatabaseLogs` query document orphaned by the Postgres log-viewer re-point) — done 2026-07-15 (guard shipped with production plan-diff proof — which also showed `w1/025` overstated the arming: the provider leaves an omitted labels map unmanaged, so the strip never planned; DatabaseLogs document + hand-written types pruned, definitions regenerated, 1230 dashboard tests green), moved to `done/m6/`
- [x] **m7** — Device-authorize page consistency: shell-styled confirm page, bex CLI branding, streamlined flow (7 tasks) ← from user report 2026-08-18 (live `/auth/device` confirm page is an unthemed inline-HTML interstitial off-brand from the rest of the auth workflow; locales say "Render CLI") — done 2026-08-19 (rebuilt the confirm page as an AuthPageShell React page, fixed a route-split regression to `/auth/device/success` found live, swept "Render CLI" → "bex CLI"), moved to `done/m7/`
- [x] **m8** — Auth-flow consistency round 2: error states, logout page, CLI client branding (8 tasks) ← from `/pm-brainstorm` 2026-08-18 auth-flow mine, continuing m7's user report: every device/consent failure path still renders bare `text/plain` on a document navigation (`hydra-device.ts` `error()`, `hydra-consent.ts` refusals), the logout page is the last off-shell auth layout, and the reported "Render CLI" branding survives at its source — the Hydra client registered as `"Render CLI (bex platform)"` (`scripts/auth-bootstrap-client.sh:170`), visible on the consent hero and Settings → Connected Agents — done 2026-08-19 (every device/consent failure now carries a typed error code through the loader-context pipeline instead of bare `text/plain`, POST failures redirect into the same shell-rendered GET path, logout page rebuilt on `AuthPageShell`, Hydra client renamed to "bex CLI"; 339/339 files, 2341/2341 tests green; live dev/prod verification blocked on the shared cluster being unreachable this session, recorded + re-owned as `w10/003`), moved to `done/m8/`
- [x] **m9** — Cross-stream note burn-down round 3: revocation watchdog, typed sandbox errors, attach-URL shape, admission-policy note (8 tasks) ← from `/pm-brainstorm` 2026-08-18 inbox survey (31 open notes triaged), the m4/m6 pattern on the four code-shaped notes not claimed by `w2/020`'s live-verification charter: `w4/034` (live-log SSE tail can outlive revocation — `w1/046` F18, the round-9 watchdog family's last gap), `w3/011` (quota-denied sandbox create surfaces as an opaque 502), `w3/013` (attach-ticket `url` vs `streamUrl` — orphaned when w1/m64 closed 2026-08-18), `w2/021` (ADR057 round-6 #4 admission-policy scoping note); notes move to their workstreams' `done/` — done 2026-08-19 (`w4/034`/`w3/011` found already shipped by an unrelated session mid-milestone, closed as duplicates per DO_NOT_DO's anti-duplication rule rather than re-implemented; `streamUrl` shipped end to end REST/GraphQL/MCP + dashboard; ADR057 #4's broad ask found already shipped, two narrow residuals filed as `w2/022`; backend `go test ./...` + dashboard 344/2330 tests green), moved to `done/m9/`
- [x] **m10** — Board & docs truth sweep round 3: w5 m71 collision, FUTURE-MAYBE & ADR018 sync (7 tasks) ← from `/pm-brainstorm` 2026-08-18 drift audit (m2/m3 lineage): the open "Key Value metrics" milestone at `w5/m71/` collides with the done milestone in `w5/done/m71/` and has no README row anywhere (hidden from board scans); FUTURE-MAYBE's replica-local-buckets entry still Deferred though it shipped as `w1/done/m58`; ADR018 :286 stale path + :306 completed-tense on open `w3/m41`; `w5/m66` t003 marker; w1/w5 README inbox sections missing open notes 049/050 and 036–040 — done 2026-08-19 (three of four findings found already fixed by unrelated concurrent sessions before implementation started — the m71 collision was already repaired as `w5/m73`, `w5/m66` t003 already marked, `w1/049`/`050` already closed — verified rather than re-done; FUTURE-MAYBE m58 entry moved to Done and the two ADR018 pointers fixed were genuinely open; the re-verification pass also caught a fresh gap, `w5/046` missing from its README, and added it), moved to `done/m10/`
- [x] **m11** — Billing charges honesty + blueprint sync diagnostics (9 tasks) ← from a live Playwright QA hunt of `dashboard.bex.co` 2026-08-21/22 (`/agents`, `/billing`, `/blueprints`, `/workspace/settings`); 5 of the original 8 `/agents` findings turned out to be exact duplicates of `w9/done/m92` (same source hunt, already fixed same-day) and are not re-filed; the Team-members "internal id instead of email" observation was investigated and found to be intentional fallback behavior, not a bug. This milestone covers the three findings that are genuinely new: the `/billing` Charges card flashes a contradictory "estimate, not an invoice" vs "amount Stripe will invoice" claim depending on load timing, a failed blueprint sync (`blp-d9nqg95cavls73fp8m10`) gives zero diagnostic info anywhere in the dashboard because the real Go error is discarded rather than persisted, and Sync History's "success" state falls through `BlueprintStatusBadge`'s unstyled raw-value fallback next to a properly styled "Error" badge
