---
id: w10/m10/t001
title: Repair the w5 m71 number collision (renumber the open milestone, add its row)
worker: worker10
status: done
estimate: 45m
depends_on: []
---

## Objective

The open "Key Value metrics: dashboard sends typed id + bex-api matches on the resolved CR name" milestone stops sharing number `m71` with the *done* agent-session-persistence milestone in `w5/done/m71/`, gets a real `- [ ]` row in `w5/README.md`, and its promotion from note `w5/044` is recorded — so board scans see it again.

## Context

- `.pm/w5/m71/README.md:3` — open (t001–t005 done, t006 closeout todo). `.pm/w5/README.md:18` `- [x] **m71**` points at the *different, done* milestone in `w5/done/m71/`. The open milestone has no checkbox row anywhere; source note `w5/044` (now `w5/done/044.md`) was never recorded as promoted.
- `/pm` canon: IDs must match the path — renumbering means moving the directory to the next free `w5/mN` **and** rewriting every task file's `id:` frontmatter + `depends_on` references + the README title line.
- Scan `w5/` and `w5/done/` first for the true next free number; don't collide again.

## Steps

1. Determine the next free w5 milestone number (open + done trees both count).
2. `git mv .pm/w5/m71 .pm/w5/m<free>`; rewrite `id:`/`depends_on:` frontmatter in all task files (including `done/` subfolder ones) and the README heading.
3. Add the `- [ ] **m<free>** — Key Value metrics… (N tasks) ← from w5/044` row to `w5/README.md`, noting the renumber-from-m71 in the row's source text.
4. Verify no reference elsewhere (docs, other notes) cites the open milestone as `w5/m71`; fix any found.

## Files

- `.pm/w5/m71/` → `.pm/w5/m<free>/` (README + all tNNN.md)
- `.pm/w5/README.md`

## Acceptance criteria

- [ ] `find .pm/w5 -maxdepth 1 -name 'm71'` returns nothing; the open milestone's path, README title, and every task `id:` agree on the new number.
- [ ] `w5/README.md` has exactly one unchecked row for it; the existing `- [x] m71` row (done agent-session milestone) is untouched.
- [ ] No stale `w5/m71` reference to the *open* milestone remains anywhere in the repo.

## Out of scope

- Doing the milestone's own remaining work (its t006 closeout stays with w5).
