---
id: w10/m10/t006
title: Test coverage — verification re-run of the drift audit checks
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m10/t002, w10/m10/t003, w10/m10/t004]
---

## Objective

Re-run the 2026-08-18 drift audit's checks against the finished tree and record the evidence: no w5 milestone-number collision, checkbox↔directory agreement across w1–w11, the five fixed drift sites reading true, and no *new* drift introduced by the renumber (stale `w5/m71` references to the open milestone).

## Context

- This milestone ships markdown, so "tests" here are the audit's own verifiable checks, scripted where cheap (grep/find one-liners), not unit tests — the deliverable is recorded evidence in the closeout, never tautology.

## Steps

1. Script/re-run: collision check (`find`-based number uniqueness across `wN/` + `wN/done/`), checkbox↔dir agreement, the five fixed pointers, inbox-section completeness for w1/w5.
2. Record commands + output in this task's resolution.

## Files

- Evidence recorded in the milestone closeout (no repo files beyond it).

## Acceptance criteria

- [ ] Every DoD clause has a recorded passing check; any failure loops back to the owning task before closeout.

## Out of scope

- Auditing beyond the DoD's scope (a future truth-sweep round).
