---
id: w10/m7/t005
title: Simplify — /simplify over the changed code
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m7/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (the new confirm page, `hydra-device.ts`, locale files) — reuse / simplification / efficiency, behavior-preserving. Likely wins: dead `htmlEscape`/inline-HTML remnants, shared Card layout with the consent page.

## Context

- `/simplify` skill; scope = this milestone's diff only.

## Steps

1. `/simplify` over the milestone diff; apply behavior-preserving cleanups.
2. `yarn typecheck && yarn lint && yarn test` green.

## Files

- `dashboard/src/common/server-fn/hydra-device.ts`
- `dashboard/src/features/auth/pages/device-confirm-page/` (or equivalent)

## Acceptance criteria

- [x] /simplify pass completed; no behavior change; suite green.

## Out of scope

- Bug hunting (that's /code-review).

## Resolution

Reviewed the full diff (`git diff --stat`: 8 files changed, 130 insertions/111 deletions, net near-neutral). The net effect already trims complexity: the 61-line hand-rolled `deviceConfirmationPage`/`htmlEscape` HTML+CSS blob is gone, replaced by a 5-line `DeviceView` type; `device-confirm-page/index.tsx` deliberately mirrors `consent-page/index.tsx`'s existing Card/CardFooter/submitting-state pattern rather than extracting a shared abstraction for two call sites (matches this repo's established per-page convention and CLAUDE.md's "three similar lines beats a premature abstraction"). No further simplification found; `yarn typecheck && yarn lint && yarn test` green (335/335 files, 2294/2294 tests).
