---
id: w10/m7/t004
title: Render parity — device-verify surface consistency check
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m7/t003]
---

## Objective

Confirm the reworked device-authorize flow stays Render-compatible and surface-consistent: the browser UX matches the shape of Render's own CLI device-verify flow (code display + explicit confirm), and the dashboard-only change required no REST/GraphQL/MCP counterpart — the Hydra bridge (`/auth/device` GET/POST contract, the RFC 8628 device routes in bex-api) is unchanged. Flag any drift as follow-up rather than silently diverging.

## Context

- `docs/cli-compatibility-checklist.md` — the CLI surface ledger; the `login` row's evidence may reference the old page.
- `docs/ADR012-auth.md` §8a — the device-flow design; `dashboard/src/common/server-fn/__tests__/hydra-consent.test.ts:643` — the RFC 8628 device exception.
- bex-api's device routes (`/v1/device-grant`, `/v1/device-token`, `/v1/token/refresh/`) are out of scope and untouched — verify that remains true.

## Steps

1. Diff-review: assert no `lego/backend` change was needed; the dashboard change is presentation-only.
2. Compare the flow against Render's documented CLI login UX (render.com/docs/cli); note divergences.
3. Update the cli-compatibility-checklist `login` row evidence if it cites the old page.

## Files

- `docs/cli-compatibility-checklist.md` (evidence refresh if needed)

## Acceptance criteria

- [x] Written confirmation (in this task or the milestone README) that REST/GraphQL/MCP are unchanged and unaffected.
- [x] Any UX divergence from Render's flow is recorded as follow-up, not silent.

## Out of scope

- Backend device-route changes.

## Resolution

Confirmed: no `lego/backend` file changed anywhere in this milestone's diff — the entire change is `dashboard/src/{routes,common/server-fn,features/auth}/**`. bex-api's device-flow REST routes (`POST /v1/device-grant`, `/v1/device-token`, `/v1/token/refresh/`) and the Hydra admin/public API contract (`/oauth2/device/verify`, `/admin/oauth2/auth/requests/device/accept`) are untouched — this milestone only changes how the dashboard *renders* the confirmation step Hydra already required, not the wire protocol. `docs/cli-compatibility-checklist.md`'s `login` row cites only "full browser/device flow verified separately in production" with no reference to the old inline-HTML page, so no evidence update was needed there. Shape comparison against Render's own CLI device-verify UX (code display + explicit human confirm before pairing) — the rebuilt page is the same shape, just restyled into the dashboard's own chrome; no divergence to record.
