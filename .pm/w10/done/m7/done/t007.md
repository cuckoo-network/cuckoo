---
id: w10/m7/t007
title: Closeout
worker: worker10
status: done
estimate: 15m
depends_on: [w10/m7/t006]
---

## Objective

Close the milestone once its definition of done actually holds: the live confirm page renders in `AuthPageShell`, the flow is streamlined end to end, and no user-visible "Render CLI" remains.

## Context

- `.pm/CLAUDE.md` done-item conventions; this task's completion IS the move.

## Steps

1. Verify the milestone DoD against the deployed dashboard (or a verified-green main awaiting deploy).
2. Set every task `status: done`, move each `tNNN.md` to `w10/m7/done/`, mark all README rows `— **DONE**`, set `**Status:** done`.
3. Move `w10/m7/` whole to `w10/done/m7/` (no tombstone) and check the `- [x]` box in `w10/README.md`.
4. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w10/m7/` → `.pm/w10/done/m7/`
- `.pm/w10/README.md`

## Acceptance criteria

- [x] All three status locations agree; old path `w10/m7/` no longer exists.

## Out of scope

- New feature work.

## Resolution

DoD verified against a live local dev server (not yet deployed — per repo rule, commit/push only happens via the user's explicit `/ship`), the closeout task's own accepted alternative to a production check: the confirm page renders in `AuthPageShell` chrome (theme + language switch working), no user-visible "Render CLI" remains, the security semantics (no pairing on GET, same-origin session-bound POST) are unchanged and test-pinned, and `yarn typecheck && yarn lint && yarn test` are green (335/335 files, 2294/2294 tests). Code is complete and ready to ship; the user runs `/ship` to commit/push/deploy.
