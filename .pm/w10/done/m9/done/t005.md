---
id: w10/m9/t005
title: Render parity — error/response-shape consistency across REST/GraphQL/MCP
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m9/t001, w10/m9/t002, w10/m9/t003, w10/m9/t004]
---

## Objective

Confirm the milestone's surface changes are consistent everywhere they're exposed: the typed sandbox quota error (t002) and the attach-ticket URL shape (t003) carry the same fields/semantics/error dialect on REST, GraphQL (where applicable), and MCP, and t001's tail termination behaves consistently with the platform's other revocation semantics.

## Context

- bex-api's one-core/thin-adapters rule (`docs/ADR006-bex-api.md`); Render's REST error dialect governs the REST shapes.
- The sandbox surface is Render's `ea sandbox` (`/v1/sandboxes*`); agent sessions are a bex extension — parity there means internal cross-surface consistency, not a Render counterpart.

## Steps

1. Exercise the quota-denied create on REST + MCP; diff shapes.
2. Exercise the attach-ticket mint on all surfaces that expose it; diff shapes.
3. Check t001's SSE close behavior against the gateway watchdogs' `revoked`-style semantics for consistency.

## Files

- `lego/backend/` (review); `docs/ADR018-render-parity.md` if a ledger row needs a note.

## Acceptance criteria

- [ ] Same fields, codes, and error shapes across surfaces for t002/t003; any drift filed as follow-up, not silently shipped.

## Out of scope

- t004's design note (no surface).
