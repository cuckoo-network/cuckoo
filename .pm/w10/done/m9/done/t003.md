---
id: w10/m9/t003
title: w3/013 — attach-ticket stream URL shape: decide + ship explicit streamUrl
worker: worker10
status: done
estimate: 30m
depends_on: []
---

## Objective

The agent-session attach-ticket mint response carries a correct, documented stream URL: today `url` returns the phase-2 wss origin rather than the phase-1 SSE base, and the note's open decision — add an explicit `streamUrl` alongside — is made and shipped (or explicitly declined with rationale).

## Context

- Source note: `.pm/w3/013.md` — the `agent-session-verify.sh` bug it found is already fixed; what remains is the API-shape decision + any dashboard consumer follow-up. Its nominal owner `w1/m64` closed 2026-08-18 (`ce37c0a5`) without shipping it, so this task is the new owner.
- Transport background: ADR047 D9 — the gateway serves `GET/POST /v1/agent-sessions/{id}/stream` (SSE, `BEX_AGENT_ATTACH_ADDR`); the ticket is the sole credential.
- Any field addition must be mirrored across REST/GraphQL/MCP and checked against how the dashboard + mobile consume the mint response today.

## Steps

1. Re-read the note; inventory current consumers of the mint response's `url` (dashboard, mobile, scripts).
2. Decide the shape (e.g. keep `url` for compatibility + add `streamUrl` for the SSE base), or document why not.
3. Ship the field across the three surfaces + update consumers and docs (ADR047 D9 section or the API docs the mint verb lives in).
4. Move `.pm/w3/013.md` → `.pm/w3/done/013.md` with the decision recorded.

## Files

- `lego/backend/` agent-session attach-ticket mint (REST/GraphQL/MCP)
- `dashboard/` mint-response consumer (if it reads `url`)
- `.pm/w3/013.md` → `.pm/w3/done/013.md`

## Acceptance criteria

- [ ] The mint response's documented URL field(s) point where a client must actually connect for each phase; test asserts the shape.
- [ ] Same fields/semantics on REST, GraphQL, and MCP.
- [ ] Decision + rationale recorded in the note's resolution (and docs if shape changed).

## Out of scope

- Gateway transport changes (the stream itself works; this is response-shape only).
