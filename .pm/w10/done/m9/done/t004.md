---
id: w10/m9/t004
title: w2/021 — design note scoping the ADR057 round-6 #4 ValidatingAdmissionPolicy slice
worker: worker10
status: done
estimate: 45m
depends_on: []
---

## Objective

A short written design note scopes the remaining slice of ADR057 round-6 finding #4 (the operator's cluster-wide workload authoring, deferred on an admission-policy follow-up): what a ValidatingAdmissionPolicy on the operator identity would enforce, what the rounds since (ADR063 #13's admission grammar, ADR064's operator delete/PVC containment) already cover, and an explicit go/no-go.

## Context

- Source note: `.pm/w2/021.md` — "the decision is the work, not the edit"; sub-hour by itself but it anchors this burn-down's design deliverable.
- Read: `docs/ADR057-security-review-round6.md` (#4), `docs/ADR063-security-review-round9.md` (#13 workload-admission grammar), `docs/ADR064-security-review-round10.md` (operator delete/PVC admission containment), plus the operator's current ClusterRole in `lego/operator/config/rbac/`.
- Deliverable is a note/ADR-stub, not implementation; if the residual proves > ~1h of real work, the note proposes the milestone rather than doing it.

## Steps

1. Diff what round-6 #4 asked for against what ADR063/ADR064 already shipped; identify the uncovered residual precisely.
2. Sketch the VAP (match conditions on the operator ServiceAccount, denied verbs/kinds/namespaces) and its failure modes (upgrade paths, break-glass).
3. Write the go/no-go with rationale; if "go", size the milestone it would need.
4. Move `.pm/w2/021.md` → `.pm/w2/done/021.md` pointing at the note.

## Files

- New note under `docs/` (or an ADR stub, per what the content warrants)
- `.pm/w2/021.md` → `.pm/w2/done/021.md`

## Acceptance criteria

- [ ] The note exists, names the exact uncovered residual, and ends in an explicit go/no-go (with a sized proposal if go).
- [ ] ADR057's #4 deferral line gains a pointer to the note.
- [ ] Source note moved to `w2/done/`.

## Out of scope

- Implementing the admission policy itself.
