---
id: w10/m9/t002
title: w3/011 — typed quota error (not opaque 502) on quota-denied sandbox creation
worker: worker10
status: done
estimate: 45m
depends_on: []
---

## Resolution (2026-08-19)

**Already shipped — no new code.** Between this milestone's scoping (2026-08-18) and starting implementation, the same w6 session that closed w4/034 also picked `.pm/w3/011.md` directly from the inbox: the timeout inversion had already landed separately as `a64b703a` (330s client timeout > 300s server pod-ready wait), and the same pass added the typed-error half — `internal/sandbox` detects the server's `KUBERNETES::POD_READY_TIMEOUT` signature on the create path and maps it to a coded 409 `SANDBOX_CAPACITY_LIMIT`, following the `AGENT_SESSION_LIVE_LIMIT` precedent. Verified against current `main`: `lego/backend/internal/sandbox/service.go:217` (`core.NewConflictError("SANDBOX_CAPACITY_LIMIT", ...)`); as core-layer error construction under bex-api's one-core/thin-adapters design (ADR006), it surfaces identically on REST and MCP without per-adapter mapping. The note itself is `.pm/w3/done/011.md`. No duplicate implementation attempted — see DO_NOT_DO's anti-duplication rule.

## Objective

A sandbox create refused by the per-workspace quota surfaces as a typed, coded 4xx error across REST and MCP — not the opaque 502 found live in w3/m40 t004 — and the client/server timeout inversion that can mask the real error is fixed.

## Context

- Source note: `.pm/w3/011.md` — two composable fixes: (1) error-shape: propagate the quota denial as a typed error (house precedent: coded 409s like `AGENT_SESSION_LIVE_LIMIT`, identical across surfaces); (2) timeout inversion: `lego/backend/internal/sandbox/client.go` uses a 30s client timeout under the server's 60s wait, so the client aborts before the server answers.
- Pillar 5 is on-roadmap (DO_NOT_DO #18 re-opened 2026-07-27); this is correctness on a shipped surface, not new sandbox scope.

## Steps

1. Re-verify the note is still open/unfixed; reproduce against the dev harness if cheap.
2. Fix the timeout inversion (client deadline ≥ server wait, or a streaming/early-fail contract).
3. Map the quota denial to a typed coded error with the same shape on REST and MCP; follow Render's error dialect for the REST surface.
4. Move `.pm/w3/011.md` → `.pm/w3/done/011.md` with resolution.

## Files

- `lego/backend/internal/sandbox/client.go` + the sandbox create handler/service
- `.pm/w3/011.md` → `.pm/w3/done/011.md`

## Acceptance criteria

- [ ] Test: quota-denied create returns the typed coded error (correct status, code, message) on REST and MCP; no 502.
- [ ] Test or reasoned proof that the client no longer times out before the server's quota answer.
- [ ] Source note moved to `w3/done/`.

## Out of scope

- Changing quota values or admission policy itself.
