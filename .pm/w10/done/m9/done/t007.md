---
id: w10/m9/t007
title: Test coverage — revocation-ends-tail, typed quota error, stream-URL shape
worker: worker10
status: done
estimate: 30m
depends_on: [w10/m9/t005]
---

## Objective

Meaningful regression tests for the three code deliverables: a revoked subject's tail closes within the interval; a quota-denied create yields the typed error on every surface; the mint response's URL field(s) are shape-asserted.

## Context

- t001/t002/t003 each carry inline acceptance tests — this task closes remaining gaps (failure modes, disable-knob behavior, cross-surface assertions), never tautological coverage.

## Steps

1. Audit what t001–t003 already added; fill the failure-mode gaps (e.g. revalidation disabled ⇒ old behavior; authz-check error ≠ revocation).
2. Full backend suite green.

## Files

- `lego/backend/internal/**/_test.go` beside the changed code.

## Acceptance criteria

- [ ] Every shipped behavior has a test that fails if reverted; `cd lego/backend && go test ./...` green.

## Out of scope

- Live-prod verification (belongs to the notes' evidence trail where required).
