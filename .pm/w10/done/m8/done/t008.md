---
id: w10/m8/t008
title: Closeout
worker: worker10
status: done
estimate: 15m
depends_on: [w10/m8/t007]
---

## Objective

Close the milestone once every other task is complete **and the definition of done actually holds** (shell-rendered error states verified live in both locales, "bex CLI" visible on consent + Connected Agents after the prod re-bootstrap).

## Context

- `.pm/CLAUDE.md` conventions: set remaining tasks `status: done`, move each `tNNN.md` to `w10/m8/done/`, mark rows `— **DONE**`, set `**Status:** done`, move `w10/m8/` → `w10/done/m8/`, check the box in `w10/README.md`.

## Steps

1. Verify the DoD end to end (live screenshots, prod client name).
2. Execute the closeout moves; sync status in all three places.
3. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w10/m8/**`, `.pm/w10/README.md`

## Acceptance criteria

- [ ] DoD verified before any move; `w10/m8/` no longer exists at its original path; box checked.

## Out of scope

- Shipping (`/ship` is user-invoked).
