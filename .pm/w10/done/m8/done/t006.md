---
id: w10/m8/t006
title: Simplify — /simplify over the changed code
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m8/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (device/consent error states, logout page, bootstrap script) — reuse, simplification, efficiency; behavior-preserving.

## Context

- t001/t002 likely introduce a shared shell error-state component — the prime dedup candidate.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; keep behavior identical (tests stay green).

## Files

- Whatever the milestone's diff touched.

## Acceptance criteria

- [ ] /simplify run recorded; accepted findings applied; `yarn typecheck && yarn lint && yarn test` green.

## Out of scope

- Bug-hunting (that's /code-review, not /simplify).
