---
id: w10/m8/t005
title: Render parity — auth-surface consistency check
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m8/t002, w10/m8/t003, w10/m8/t004]
---

## Objective

Confirm the milestone's changes are consistent across every surface they touch and drift-free against Render's equivalent UX: the device/consent Hydra bridge endpoints keep their wire behavior, and no REST/GraphQL/MCP shape changed.

## Context

- bex-api is Render-compatible (`docs/ADR006-bex-api.md`); the device flow's browser step is bex's counterpart to Render's CLI device-verify UX.
- m7's parity check (w10/done/m7 t004) confirmed the dashboard-only change needed no REST/GraphQL/MCP counterpart — this round adds error-state rendering and a Hydra client rename, which likewise must not alter wire shapes.

## Steps

1. Diff the device/consent endpoints' request/response behavior before/after (document GETs excepted — their content type legitimately changes from `text/plain` to HTML for navigations).
2. Confirm the OAuth error semantics (RFC-6749 codes from `cliauth`, ADR069 `invalid_scope` refusals) are unchanged.
3. Compare the failure UX against Render's CLI device-verify flow; flag drift as follow-up, never silently diverge.

## Files

- `dashboard/src/common/server-fn/hydra-device.ts`, `hydra-consent.ts` (review)
- `lego/backend/internal/cliauth/` (review only)

## Acceptance criteria

- [ ] No REST/GraphQL/MCP field, status code, or error-shape change; existing backend tests untouched and green.
- [ ] Any observed UX drift from Render's device-verify flow is recorded as follow-up (inbox note), not silently shipped.

## Out of scope

- Fixing any newly discovered drift (file it, don't absorb it).
