---
id: w10/m8/t007
title: Test coverage — error-state pages, logout shell, bootstrap rename
worker: worker10
status: done
estimate: 30m
depends_on: [w10/m8/t005]
---

## Objective

Meaningful tests for what this milestone shipped: each device/consent document-GET failure renders the shell error page (not `text/plain`), the logout page renders in the shell, and route titles are correct.

## Context

- Existing suites to extend: `dashboard/src/common/server-fn/__tests__/hydra-device.test.ts`, `hydra-consent.test.ts`, `dashboard/src/common/lib/document-head/__tests__/route-heads.test.ts`, plus component tests beside `device-confirm-page`/`logout-page`.
- Tests must assert real behavior and failure modes — e.g. "expired code → shell error variant with `deviceExpired*` copy; POST semantics unchanged" — never snapshot-everything.

## Steps

1. Server-fn tests: each failure site yields the error loader variant (and correct status) instead of a `text/plain` body for document requests.
2. Component tests: error variants render title/subtitle from locales in the shell; logout states render in the shell.
3. Route-head tests: reset-password title key.

## Files

- `dashboard/src/common/server-fn/__tests__/`
- `dashboard/src/features/auth/pages/*/__tests__/` (or colocated test files per repo convention)

## Acceptance criteria

- [ ] Every new behavior has a test that fails if reverted; full dashboard suite green.

## Out of scope

- Backend test changes (no backend behavior changed except optional identifier renames).
