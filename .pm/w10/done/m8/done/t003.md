---
id: w10/m8/t003
title: Move the logout page onto AuthPageShell + fix the reset-password document title
worker: worker10
status: done
estimate: 40m
depends_on: []
---

## Objective

The logout page — the last hand-rolled layout in the auth workflow — renders inside `AuthPageShell` with the same chrome, language switcher, and theming as every other auth page; and the reset-password route stops titling its tab "Forgot password".

## Context

- `dashboard/src/features/auth/pages/logout-page/index.tsx:66-160` hand-rolls a centered layout (`min-h-screen flex flex-col items-center justify-center bg-gradient-to-br from-background to-muted/20`) plus its own icon-badge treatment — no language switcher, different background/max-width from the shell.
- Existing `auth.logout*` locale keys carry the copy for its four status states.
- `dashboard/src/routes/auth.reset-password.tsx:17` sets the document title to `auth.forgotPasswordTitle` on the post-recovery reset landing.
- `dashboard/src/common/lib/document-head/route-inventory.ts` + `__tests__/route-heads.test.ts` pin route titles — update alongside.

## Steps

1. Wrap the logout page's status states in `AuthPageShell` (title/subtitle from the existing `auth.logout*` keys); add a small shell affordance for the status icon if needed.
2. Add a reset-specific title key (en + zh) and point `auth.reset-password.tsx` at it; update the route-heads inventory/test.
3. Live-verify logout (all reachable states) and the reset-password tab title; screenshots to `.playwright-mcp/`.

## Files

- `dashboard/src/features/auth/pages/logout-page/index.tsx`
- `dashboard/src/routes/auth.reset-password.tsx`
- `dashboard/src/features/auth/locales/en.ts`, `dashboard/src/features/auth/locales/zh.ts`
- `dashboard/src/common/lib/document-head/route-inventory.ts` + tests

## Acceptance criteria

- [ ] `/auth/logout` renders inside `AuthPageShell` in both locales; no bespoke full-screen layout remains in `logout-page/`.
- [ ] The reset-password route's document title is reset-specific in both locales; `route-heads` tests updated and green.

## Out of scope

- `settings-page` (correctly renders inside `DashboardLayout`, not the auth shell — verified intentional).
