---
id: w10/m11/t007
title: Simplify
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m11/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, and altitude cleanups, behavior-preserving only.

## Context

- Touches: `dashboard/src/features/usage/components/charges-card.tsx`; `lego/backend/internal/store/blueprints.go` + new migration; `lego/backend/internal/apps/blueprint.go`, `graphql.go`, `rest.go`, `mcp.go`; `dashboard/src/routes/blueprints.$blueprintId.tsx`; `dashboard/src/features/blueprints/components/blueprint-status-badge.tsx`; `dashboard/src/features/blueprints/hooks/use-blueprint-syncs.ts`; locale files under both `usage/locales/` and `blueprints/locales/`.

## Steps

1. Run `/simplify` scoped to the files this milestone touched.
2. Apply any resulting fixes; do not change observable behavior.

## Files

- All files listed above.

## Acceptance criteria

- [ ] `/simplify` run and findings addressed (or explicitly dismissed with reason).
- [ ] `yarn typecheck && yarn lint && yarn test` (dashboard) and `go test ./...` + `make lint` (backend) still green after any changes.

## Out of scope

- New behavior — this task is cleanup only.
