---
id: w10/m11/t006
title: Render parity check (REST/GraphQL/MCP + dashboard)
worker: worker10
status: done
estimate: 30m
depends_on: [w10/m11/t001, w10/m11/t003, w10/m11/t004, w10/m11/t005]
---

## Objective

Verify the `blueprint_syncs` `errorMessage` field (t002-t004) is consistently named, typed, and semantically identical across REST, GraphQL, and MCP, and that the dashboard (t001, t004, t005) is the only consumer needed — no divergence from Render's own behavior where a comparable concept exists.

## Context

- Render's Blueprints API has no direct `errorMessage`-on-sync-run equivalent to compare against (this is a bex extension — Render's dashboard shows sync failures inline in its own UI without a documented API field), so the parity check here is primarily internal consistency: REST/GraphQL/MCP must agree on field name, nullability, and content, per `docs/ADR006-bex-api.md`'s "one core, thin adapters" principle.
- t001 is dashboard-only (no backend shape change) — confirm it introduces no new field that needs cross-surface exposure.

## Steps

1. Confirm `errorMessage` is spelled identically (not `error_message`/`errMsg`/etc.) on the GraphQL type, the REST JSON response, and the MCP tool output.
2. Confirm all three surfaces agree on when the field is present vs. omitted/null (success/running/created rows vs. error rows).
3. Manually exercise all three surfaces against a real induced sync failure (REST `curl`, a GraphQL query, and the MCP tool) and diff the three responses' `errorMessage` values.
4. Confirm the dashboard's Sync History (t004) renders the same message text as the raw API responses — no lossy transformation.

## Files

- `lego/backend/internal/apps/blueprint.go`, `graphql.go`, `rest.go`, `mcp.go`
- `dashboard/src/routes/blueprints.$blueprintId.tsx`

## Acceptance criteria

- [ ] REST, GraphQL, and MCP return byte-identical `errorMessage` text for the same failed sync run.
- [ ] No surface silently drops or renames the field.
- [ ] Any drift found is fixed in place (this task is a check-and-fix pass, not report-only).

## Out of scope

- Adding new capabilities beyond what t002-t005 shipped.
