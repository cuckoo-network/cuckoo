---
id: w10/m11/t009
title: Closeout
worker: worker10
status: done
estimate: 10m
depends_on: [w10/m11/t008]
---

## Objective

Close the milestone once its definition of done actually holds — not merely once the code is written.

## Steps

1. Verify every item in the milestone README's "Definition of done" is observably true (re-check live against a running dev/prod build where practical, matching this workstream's own established discipline — see `w10/002`, `w10/003` for the pattern of recording a live-verification blocker rather than asserting success from code review alone).
2. Set every task's `status: done`.
3. Move each `tNNN.md` to `w10/m11/done/`.
4. Mark every row `— **DONE**` and set `**Status:** done` in this milestone's `README.md`.
5. Move the whole `w10/m11/` directory to `w10/done/m11/`.
6. Check `- [x]` for `m11` in `w10/README.md`.
7. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Acceptance criteria

- [ ] All of the above completed; `w10/m11/` no longer exists at its original path, only under `w10/done/m11/`.
