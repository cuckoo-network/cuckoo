---
id: w10/m6/t001
title: Terraform ignore_changes = [labels] on the Traefik LB + plan-diff proof
worker: worker10
status: done
estimate: 30m
depends_on: []
---

## Objective

Add `ignore_changes = [labels]` to `hcloud_load_balancer.traefik`'s existing `lifecycle` block so Terraform stops trying to strip the hcloud CCM's `hcloud-ccm/service-uid` label from the production LB on every apply, and prove it with a plan diff.

## Context

- `infra/terraform/main.tf:137-146` — the block's own comment already documents that the CCM owns that label; only `prevent_destroy` made it into `lifecycle`. Mitigated today by CCM v1.33.0's name-adoption fallback, but a stripped label is exactly the drift fight w1/m41 built the block to avoid.
- Origin: `w1/025` (round 19's shipped-diff mine, F2 on commit `4dd753af`).
- Terraform state/credentials: the usual `infra/terraform` workflow (see ADR019 for custody; never print secrets).

## Steps

1. Add `ignore_changes = [labels]` to the existing `lifecycle` block.
2. Run `terraform plan` against production state; confirm no label modification is planned (before/after if the label currently diverges).
3. Record the plan evidence in the milestone README.

## Files

- `infra/terraform/main.tf`

## Acceptance criteria

- [ ] Plan output shows no label changes on the LB; evidence recorded.

## Out of scope

- Any other terraform drift found en route (file notes).
