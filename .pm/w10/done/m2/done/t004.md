---
id: w10/m2/t004
title: Fix `envVarKey` stale comment + graduate `gqlStr` into `internal/gqlutil`
worker: worker10
status: done
estimate: 30m
depends_on: []
---

## Objective

Two flagged code-hygiene items: (1) `lego/backend/internal/apps/deploy.go:325` says envVarKey is "not yet wired" while `resolveRef` (~:1247) resolves it — verify which is true and fix the comment (or the gap, if real); (2) `gqlStr` exists as ~7 verbatim copies across feature packages (flagged in `w6/done/m24`) — move one copy to `internal/gqlutil`, delete the rest.

## Files

- lego/backend/internal/apps/deploy.go
- lego/backend/internal/gqlutil/
- lego/backend/internal/*/graphql.go (the copies)

## Acceptance criteria

- [ ] Comment matches behavior (verified by a quick test, not assumption); one `gqlStr` definition repo-wide; backend builds + tests green.
