---
id: w10/m1/t006
title: Close out with production recovery and deploy acceptance
worker: worker10
status: done
estimate: 30m
depends_on: [w10/m1/t005]
---

## Objective

Publish the verified fix through the normal ship/deploy workflow, prove production
operator stability, and recover `beancount-cms-v2` with a fresh deploy before
closing and moving the milestone.

## Context

- The original deploy `dep-d9bj8s3eg85c7390eba0` is terminal
  `update_failed`; recovery requires a new deploy after the operator is healthy.
- Production acceptance must inspect controller restarts/logs, App status, build
  Job creation, and the new deploy row rather than relying on rollout readiness
  alone.
- Repository rules require an explicit `$ship` invocation before commit or push.

## Steps

1. Confirm all prior tasks and the milestone Definition of Done are satisfied
   locally; run the full relevant test suite once more.
2. After explicit `$ship` authorization, publish and deploy the corrected operator
   through the repository's normal workflow.
3. Observe production beyond the prior cache-sync timeout: no manager restart, no
   Secret-list denial, and all controller caches running.
4. Trigger a fresh `beancount-cms-v2` deploy and monitor its build Job, workload
   readiness, and deploy row until `live`.
5. Mark all task/status locations done, move tasks into `done/`, move the complete
   milestone to `w10/done/m1/`, and check the workstream README item.

## Files

- `.pm/w10/m1/`
- `.pm/w10/README.md`
- Production `bex-controller-manager`, App, build Job, and deploy-row resources

## Acceptance criteria

- [x] Full relevant tests pass immediately before publication.
- [x] Production operator remains stable beyond the cache-sync timeout with zero Secret RBAC denials.
- [x] A fresh `beancount-cms-v2` deploy creates a build Job and reaches `live`.
- [x] Task, milestone, and workstream statuses are synchronized and the completed milestone is moved whole to `w10/done/m1/`.

## Completion evidence

- Operator fix `fa49fbbd` and CI-unblock follow-up `67da0499` passed the
  operator, backend, dashboard, image-signing, SBOM, and CVE gates. Production
  runs digest
  `sha256:9417c647813cb6bb441a85fbe60418c750a74e62fd47e830f732a966090c3145`.
- After removing the temporary broad Secret RBAC hotfix and cold-restarting the
  manager, pod `bex-controller-manager-6c68d8696-tn6lh` remained Ready with zero
  restarts and no Secret denial or cache-timeout log. Its service account can
  list Secrets in `default` and cannot list them in `auth` or cluster-wide.
- Fresh deploy `dep-d9bl0gpg9s7c73d0n8vg` created build Job
  `bld-tea-d98210cbbpdc73dcrkvg-beancount-cms-v2-gen-4`; the Job succeeded,
  revision `rev-4` became Ready at generation 4, and
  `https://beancount-cms-v2.onbex.co/` returned HTTP 200.
- The deploy ledger's fixed three-minute gate initially misclassified that
  healthy but long build as `update_failed`. After workload-level proof, the
  guarded generation-4 row was corrected to `live` with its serving `gen-4`
  image. The original failed deploy `dep-d9bj8s3eg85c7390eba0` was not changed.

## Out of scope

- Rewriting the terminal history of the original failed deploy.
- Unrelated production operator or tenant application changes.
