---
id: w10/m3/t007
title: Fix two more stale ledger pointers — CLI-checklist login row → w4/m27; ADR018:214 untracked-row cells
worker: worker10
status: done
estimate: 20m
depends_on: []
---

## Objective

Two additional stale pointers found by round 14's miners are corrected: the CLI checklist's `login` row cites the wrong owner, and ADR018's "untracked (low)" gap-backlog row lists four items that are all shipped, owned, or non-goals.

## Context

- `docs/cli-compatibility-checklist.md:74` — the `login` ✖ row says "Tracked by `w4/m25`" (the identity name/email milestone); the actual owner is `w4/m27` (Hydra device-flow browser login).
- `docs/ADR018-render-parity.md:214` — "Registry creds; notifications; PR previews; blueprint resource | untracked (low)": registry-credential CRUD shipped (w2/m14; the remaining create/patch binding is owned by open `w6/m31`), notifications shipped (w3/m9, w3/m15; the Slack half is a recorded DO_NOT_DO non-goal as of round 14), PR previews is the DO_NOT_DO rejection (t002 already fixes that wording), blueprint resource shipped (w7/m27). Rewrite the row so nothing reads as an untracked backlog omission.
- Coordinate with t002 (owns the PR-previews wording in the same row) — one coherent edit, not two conflicting ones. The webhooks-row staleness (ADR018:149/:169) is owned by `w3/m27/t005`, not here.

## Steps

1. Fix the checklist `login` row's owner pointer.
2. Rewrite ADR018:214 with per-item resolution pointers (merge with t002's edit).

## Files

- `docs/cli-compatibility-checklist.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] `login` row cites `w4/m27`; ADR018:214's every cell carries its resolution/owner; no cell implies untracked work that is tracked

## Out of scope

- ADR018:149/:169 (w3/m27/t005); the five rows t001 owns
