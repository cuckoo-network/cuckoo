---
id: w10/m3/t003
title: "FUTURE-MAYBE sync: w1/m41 promotion note, w8/m7 → Done, delivery-dedup clause"
worker: worker10
status: done
estimate: 20m
depends_on: []
---

## Objective

Bring `.pm/FUTURE-MAYBE.md` in line with its own conventions: (1) entry 2 (Traefik LB survives rebuilds) fired — open `w1/m41` is exactly that work; add the promotion note (or move per the file's convention). (2) Entry 4 (pricing/estimation) says it "moves to Done once w8/m7's closeout lands" — w8/m7 is done; execute the move. (3) The multi-replica in-memory-state entry names the deploy-hook limiter and email-suppression map but not webhook delivery-worker dedup (`.pm/w3/done/m11/done/t003.md:41` — a second replica would double-deliver); add that clause to the same trigger.

## Files

- .pm/FUTURE-MAYBE.md

## Acceptance criteria

- [ ] Both trigger-state drifts resolved per the file's own conventions; the multi-replica entry lists all three replica-local states.
