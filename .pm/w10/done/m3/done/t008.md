---
id: w10/m3/t008
title: "Sync stale done-work bookkeeping: w7/done/m36 task files + w4 README m23 checkbox"
worker: worker10
status: done
estimate: 20m
depends_on: []
---

## Objective

Two freshly-found board-truth drifts make done work look undone. (a) `w7/done/m36`'s eight task files all still read `status: todo` with unchecked boxes, the milestone README reads `**Status:** todo` with no `— DONE` rows and no `done/` subdir — it looks like a milestone moved to `done/` without being executed, but the work shipped (`0a782eff feat(operator): per-App Zot pull credentials (w7/m36)`; `docs/ADR022-tenant-isolation.md` § Per-App pull credentials records the implementation; `w7/README.md` marks it `[x]`). (b) `w4/README.md`'s milestone list still shows `- [ ] **m23**` while `w4/done/m23/` exists — verify m23's actual task state (its README line claims t006 + t008 "remain") and reconcile whichever side is wrong.

## Context

- **(b) resolved 2026-07-15 by w4** (per `w4/done/021.md`'s coordination line): m23's t006 (simplify pass over `cc2156b8`) + t008 (closeout) were actually run, statuses synced, and the w4 README m23 checkbox checked — only (a), the `w7/done/m36` half, remains for this task.

- Round-15's residual miner nearly re-opened w7/m36 off this drift — exactly the false-finding class this milestone exists to stop.
- For (a): set each task's `status: done`, move them to `w7/done/m36/done/`, mark rows `— **DONE**`, set `**Status:** done`, add a one-line Resolution pointing at `0a782eff` + ADR022.
- For (b): read `w4/done/m23/` — if all tasks are genuinely done, check the box and fix the line; if t006/t008 are genuinely open, the milestone should not be in `done/` — flag to the user rather than silently moving it back.

## Steps

1. Fix (a) mechanically.
2. Investigate (b); reconcile or flag.
3. `npx prettier@3.4.2 --write` over touched files.

## Files

- .pm/w7/done/m36/
- .pm/w4/README.md (and .pm/w4/done/m23/ if reconciliation requires)

## Acceptance criteria

- [ ] `w7/done/m36` reads as a properly closed milestone (status/rows/task files consistent, resolution pointer present).
- [ ] w4's m23 checkbox agrees with `w4/done/m23/`'s actual state, or the contradiction is flagged with evidence.

## Out of scope

- Re-verifying w7/m36's implementation itself (ADR022 + the shipped verifier script own that).
