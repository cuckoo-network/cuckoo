---
id: w10/m3/t001
title: Fix the five stale ADR018 gap-backlog rows with resolution pointers
worker: worker10
status: done
estimate: 30m
depends_on: []
---

## Objective

Correct the five gap-backlog rows whose claimed status contradicts the board, each with a dated resolution pointer verified against the `done/` artifact:

- ADR018:198 — "Manual-scaling control | `w5/004` | todo (blocked)": `w5/004` is done, the manual-scale row (:32) is all-✅ (w5/m16 stepper) → mark done/remove.
- ADR018:217 — "`w2/008` ◐ … `Deploy.image` mismatch not yet retested, leave open": `w2/done/008.md` records both wire-shape bugs shipped + re-verified live, note retired.
- ADR018:218 — "`w8/m13`'s broader scope … still open": w8/m13 shipped (rows :75/:90 themselves say so).
- ADR018:221 — "`GET /v1/users` … `w4/016` ◐ … leave open": `w4/done/016.md` flipped the row ◐→✅.
- ADR018:222 — Key Value `red-` gap owner: point at open `w9/m6`, not retired `w1/021`.

## Context

- Verify each `done/` file before editing (`.pm/w5/done/004.md`, `w2/done/008.md`, `w8/done/m13/`, `w4/done/016.md`, `w9/m6/README.md`).

## Files

- docs/ADR018-render-parity.md

## Acceptance criteria

- [ ] Each row's status matches its verified artifact; no backlog row cites a `done/` item as its open owner.

## Out of scope

- The three table-cell markers owned by `w5/013` (cron Build Command, header-rules UI, email recovery) — coordinate, don't duplicate.
