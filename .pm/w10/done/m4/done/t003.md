---
id: w10/m4/t003
title: Verify/declare the w1/m36 prod default-SA imagePullSecrets patch (from w1/024)
worker: worker10
status: done
estimate: 30m
depends_on: []
---

## Objective

Resolve the undeclared prod gitops drift `w1/done/m36/README.md:53` flagged: a live `kubectl patch` attached `bex-registry-pull` to the apps namespace's `default` ServiceAccount — state existing nowhere in git. `w7/m36`'s per-App `reg-pull-<name>` credentials likely made it moot; nothing confirms it.

## Context

- Verify-first, from `.pm/w1/done/024.md`: (1) inspect the live `default` ServiceAccount in the prod apps namespace; (2) if the patch is gone or moot under per-App creds ⇒ revert any remnant, record, done; (3) if still load-bearing ⇒ declare it in `deploy/gitops` or switch the dependency to the operator-attached path per `docs/ADR022-tenant-isolation.md` § Registry access control.
- Needs prod kubeconfig access (the path w9/m39's probes used). Never print kubeconfig contents.
- If option (3) grows beyond the estimate, stop and file the remainder — don't balloon a chores task.

## Files

- `deploy/gitops/` (only if declaration is needed)

## Acceptance criteria

- [ ] Live state inspected and recorded.
- [ ] Either no remnant remains (reverted/moot) or the state is declared in git — no undeclared drift.

## Out of scope

- Registry credential mechanics themselves (w7/m36 shipped, w7/m39 hardened).
