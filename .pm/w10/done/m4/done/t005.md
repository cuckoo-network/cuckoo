---
id: w10/m4/t005
title: Render parity
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m4/t001]
---

## Objective

t001 changes `mfaEnabled`'s semantics — verify every surface exposing it (owners/members REST per `docs/render-artifacts/owners-api.md`, GraphQL, the dashboard team page) reports the corrected value consistently, anchored to Render's semantics (an enrolled second factor, not credential presence). Flag drift as follow-up rather than silently diverging.

## Acceptance criteria

- [ ] All surfaces report the same corrected value for the stub-only shape.
- [ ] Any divergence from Render's semantics written down.
