---
id: w10/m5/t005
title: Render parity
worker: worker10
status: done
estimate: 30m
depends_on: [w10/m5/t002, w10/m5/t004]
---

## Objective

Check the milestone's changes are consistent across every surface they expose: REST, GraphQL, and MCP audit reads carry the same semantics (verb choice on no-op plan sets, plan from/to detail, target naming), and the dashboard matches. Compare against Render's audit-log behavior (captures in `docs/render-artifacts/`; the confirmatory live capture remains blocked per `w10/002` — use the recorded evidence, don't fake new evidence) and flag any drift as follow-up work rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` § Audit log; `docs/render-artifacts/` audit captures (w4/m26).
- REST maps status onto Render's closed enum while GraphQL keeps bex's richer dialect — the recorded, intended asymmetry; verify this milestone introduced no new unintended ones.

## Steps

1. Diff the audit event shape across REST/GraphQL/MCP for the changed fields; verify the dashboard rendering.
2. Compare with the Render artifacts; file follow-up notes for any drift found.

## Files

- `lego/backend/internal/audit/`
- `docs/render-artifacts/`

## Acceptance criteria

- [ ] Cross-surface consistency verified for verb choice, plan pair, and target name; findings (or "clean") recorded.

## Out of scope

- Re-running the blocked live Render capture (owned by `w10/002`).
