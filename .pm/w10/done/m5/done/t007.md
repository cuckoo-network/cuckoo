---
id: w10/m5/t007
title: Test coverage
worker: worker10
status: done
estimate: 30m
depends_on: [w10/m5/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: the no-op `SetPlan` verb decision, the datastore `PlanFrom`/`PlanTo` pair, the GraphQL `targetName` field (named + pre-0038 empty cases), and the dashboard fallback rendering. Tests assert real behavior and failure modes — no tautological or snapshot-everything tests.

## Context

- Backend: existing audit tests in `lego/backend/internal/audit/` and store integration tests (`store_pg_test.go` pattern, gated on `BEX_TEST_DB_URI`).
- Dashboard: `dashboard/src/features/audit/` component/hook tests (t004 seeds them; extend if thin).

## Steps

1. Backend: table cases for same-plan vs changed-plan audit rows (both datastores); GraphQL field resolution for named/empty target.
2. Dashboard: both rendering branches asserted.
3. All three suites green.

## Files

- `lego/backend/internal/audit/`, `lego/backend/internal/core/`, `lego/backend/internal/store/`
- `dashboard/src/features/audit/`

## Acceptance criteria

- [ ] A regression in any of the four shipped behaviors fails a test.

## Out of scope

- Coverage-gaming.
