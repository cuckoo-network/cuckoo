---
id: w10/m11/t008
title: Test coverage
worker: worker10
status: todo
estimate: 40m
depends_on: [w10/m11/t006]
---

## Objective

Add meaningful regression tests for every behavior this milestone shipped — tests that assert real behavior and failure modes, not tautologies or snapshot-everything.

## Context

Each implementation task (t001-t005) already lists targeted acceptance-criteria tests. This task is the sweep to confirm they exist, are meaningful, and nothing was missed once the whole milestone is assembled together.

## Steps

1. Confirm `charges-card`'s test suite covers: loading state shows neither committed description; resolved-invoiced state shows `chargesDescriptionInvoiced`; resolved-estimate-only state shows `chargesDescriptionEstimate`.
2. Confirm backend tests cover: a failed sync persists `error_message`; a successful sync leaves it null; the field round-trips through REST, GraphQL, and MCP.
3. Confirm dashboard tests cover: Sync History renders the error affordance when `errorMessage` is present, and today's plain badge when it's absent (legacy row).
4. Confirm `BlueprintStatusBadge` has a test asserting every `BlueprintStatus*`/`BlueprintSyncState*` constant renders a non-fallback variant + label.
5. Fill any gaps found.

## Files

- `dashboard/src/features/usage/components/__tests__/`
- `dashboard/src/features/blueprints/components/__tests__/`
- `dashboard/src/features/blueprints/hooks/__tests__/`
- `lego/backend/internal/apps/*_test.go` (blueprint-related)
- `lego/backend/internal/store/*_test.go` (blueprints-related)

## Acceptance criteria

- [ ] Every acceptance criterion in t001-t005 has a corresponding automated test.
- [ ] `yarn test` (dashboard) and `go test ./...` (backend) green.

## Out of scope

- Coverage of code this milestone didn't touch.
