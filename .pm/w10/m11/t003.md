---
id: w10/m11/t003
title: Expose sync error message via GraphQL, REST, and MCP
worker: worker10
status: todo
estimate: 30m
depends_on: [w10/m11/t002]
---

## Objective

Surface the `error_message` t002 now persists through every API surface a blueprint sync run is exposed on.

## Context

- `lego/backend/internal/apps/blueprint.go`: `BlueprintSyncView` (~line 159) is the API shape; `toBlueprintSyncView(r store.BlueprintSync) BlueprintSyncView` (~line 1174) maps the store row to it. Add an `ErrorMessage` field here mirroring t002's store field.
- `lego/backend/internal/apps/graphql.go`: `blueprintSyncGQLType` (~line 697-706) enumerates `id`, `commitId`, `state`, `startedAt`, `completedAt` via `gqlutil.StrField`. Add `errorMessage` the same way — check whether `gqlutil` has a nullable-string field helper (`StrField` looks non-nullable; `BlueprintSyncView.ErrorMessage` will often be nil) and use the appropriate one.
- `lego/backend/internal/apps/rest.go` (~line 1166, `ListBlueprintSyncs` handler): confirm the REST response marshals `BlueprintSyncView` directly via its JSON tags — if so, t002's `omitempty` json tag on `BlueprintSyncView`/`BlueprintSync` is sufficient and no REST-specific code change is needed beyond the shape change; verify by reading the handler.
- `lego/backend/internal/apps/mcp.go`: grep for the existing blueprint-sync-listing MCP tool definition and add the field to its declared output schema/mapping the same way the other three fields are declared.

## Steps

1. Add `ErrorMessage *string` to `BlueprintSyncView` and populate it in `toBlueprintSyncView`.
2. Add `errorMessage` to `blueprintSyncGQLType` in `graphql.go`.
3. Verify (don't just assume) the REST handler in `rest.go` picks up the new field automatically from the shared view type; add an explicit mapping if it doesn't.
4. Add the field to the MCP tool's blueprint-sync output shape in `mcp.go`.
5. Confirm field naming is identical (`errorMessage`) across all three surfaces — this is exactly what t006 (Render parity) will check, but get it right here rather than relying on that pass to catch a mismatch.

## Files

- `lego/backend/internal/apps/blueprint.go`
- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/apps/rest.go`
- `lego/backend/internal/apps/mcp.go`

## Acceptance criteria

- [ ] `GET /v1/blueprints/{id}/syncs` (or the equivalent existing REST route) returns `errorMessage` for a failed run and omits/nulls it for a successful one.
- [ ] The GraphQL `BlueprintSync` type exposes `errorMessage` with the same semantics.
- [ ] The MCP blueprint-sync tool's output includes `errorMessage`.
- [ ] `cd lego/backend && go test ./...` green, with a new/updated test per surface asserting the field round-trips.

## Out of scope

- Dashboard consumption of the field (`t004`).
