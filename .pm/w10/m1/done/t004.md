---
id: w10/m1/t004
title: Simplify the operator watch fix
worker: worker10
status: done
estimate: 20m
depends_on: [w10/m1/t003]
---

## Objective

Run `$simplify` over the milestone's operator changes and apply behavior-preserving
improvements that reduce special cases, duplication, and controller-runtime
configuration complexity.

## Context

- Watch construction is security-sensitive and easy to duplicate incorrectly
  across controllers.
- Simplification must preserve the namespace boundary and event behavior proven in
  t003.

## Steps

1. Run `$simplify` over the files changed by t001-t003.
2. Apply safe reuse, naming, and control-flow improvements.
3. Re-run focused tests after every material change.

## Files

- Files changed by `w10/m1/t001` through `w10/m1/t003`

## Acceptance criteria

- [ ] `$simplify` findings are recorded and applicable improvements are applied.
- [ ] Namespace-scoped Secret behavior and RBAC invariants remain unchanged.
- [ ] Focused tests remain green.

## Out of scope

- Behavior changes beyond the milestone's cache-startup and Secret-watch fix.

## Completion evidence

- The named `$simplify` skill was not installed in this Codex session, so the same
  review was performed directly over the milestone diff.
- Secret cache construction was kept in one small
  `NamespacedSecretCacheOptions` helper instead of duplicating controller-runtime
  cache options in manager setup.
- KeyValue predicates were scoped per watched kind: generation changes for desired
  resources and resource-version changes for owned Secrets. No additional safe
  reduction was found without obscuring the RBAC boundary.
- Focused controller and manager tests remained green after the review.
