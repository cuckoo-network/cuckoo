# CLAUDE.md

bex is the open-source Render alternative — AI-native ([docs/ADR008-vision.md](docs/ADR008-vision.md)). A Go Kubernetes operator reconciles `App` CRs (`app.bex.co/v1alpha1`, namespace `bex-system`) into running services. All Go lives in **`lego/`** (Latin _legō_, "I assemble" — bex assembles bought bricks; the modules are the bricks): one image with multiple entrypoints from a Go workspace (`lego/go.work`) of three modules. **lego/types/** — the `App`/`Database` CRD contract (leaf; imports nothing); **lego/operator/** — the **manager** (`cmd/manager`), a mechanism-only, DB-free reconciler; **lego/backend/** — **bex-api** (`cmd/api`, the Render-compatible REST/GraphQL/MCP surface on :8090 + OpenFGA authz + API keys + metrics) and the isolated **SSH gateway** (`cmd/ssh-gateway`). Dependency arrows point one way: `operator → types ← backend`; the operator never imports the backend.

## Repo map

- `lego/` — **the product: all Go**, self-contained (workspace `go.work`, `Dockerfile` + `.dockerignore`; build context is `lego/`). Overview: [`lego/README.md`](lego/README.md).
  - `lego/types/` — the `App`/`Database` CRD types (`app.bex.co/v1alpha1`); leaf, imports nothing.
  - `lego/operator/` — the operator (kubebuilder): manager reconciles CRs → Deployment/Service/Ingress (mechanism, no DB). Owns codegen, `config/`, `hack/`. See `lego/operator/CLAUDE.md`.
  - `lego/backend/` — the business-logic services: **bex-api** (Render REST/GraphQL/MCP + authz + API keys + metrics) and the isolated SSH gateway. Imports `types/`, never `operator/`. See `lego/backend/CLAUDE.md`.
- `dashboard/` — the human-facing dashboard (TanStack Start + Apollo + shadcn), client of `bex-api`'s GraphQL. See `dashboard/CLAUDE.md` before editing.
- `infra/` — day-0 provisioning: Terraform + Cluster API; overlays `local-capd` (Docker) ⇄ `hetzner-caph`. The operator never references `infra/`.
- `deploy/gitops/` — day-1+: what Argo CD reconciles into the cluster (zot registry, opensandbox controller, CAPI, autoscaler, bex itself). GitOps is for platform infra, not user deploys.
- `examples/` — `whoami-app.yaml` (prebuilt image), `hello-go/` (build-from-git sample).
- `scripts/` — local-cluster and app helpers (below). `up.sh` / `start-opensandbox*.sh` are the legacy single-host path.
- `docs/` — the real documentation; one file per topic, indexed below.
- `.pm/` — internal PM notes and milestone logs. Not documentation; may be stale or aspirational.

## Commands (run from `lego/operator/`)

All Go is a workspace under `lego/` (`lego/go.work` over `types/` `operator/` `backend/`). The `make` targets live in **`lego/operator/`** (codegen, manager build, image build with context `lego/`, deploy). Build/test bex-api from `lego/backend/` (`cd lego/backend && go build ./... && go test ./...`); the CRD types are in `lego/types/`. Run `make` below **from `lego/operator/`**:

- `make test` — unit + envtest; auto-runs manifests/generate/fmt/vet first. First run downloads envtest binaries to `bin/`. (codegen reads CRD/deepcopy markers from `../types`, RBAC from `./...`.) **CI-enforced** on every push/PR touching `lego/operator/**` or `lego/types/**` (`.github/workflows/operator-test.yml`).
- `cd lego/backend && go test ./...` — backend unit + integration tests; integration tests gated on `BEX_TEST_DB_URI`/`BEX_TEST_OPENFGA_URL` are **not** skipped in CI — real ephemeral Postgres + OpenFGA containers run them (`.github/workflows/backend-test.yml`). **CI-enforced** on every push/PR touching `lego/backend/**` or `lego/types/**`.
- `make lint` / `make lint-fix` — golangci-lint over **both** modules (operator + backend); `make lint-backend` runs the backend module alone (its `.golangci.yml` depguard-enforces the id convention, [docs/ADR020-identifiers.md](docs/ADR020-identifiers.md)).
- `make build` — build the manager binary.
- Dev inner loop against a cluster: `make install && BEX_RUNTIME=kubernetes make run` (runs the operator from the host).
- `make docker-build IMG=…` / `make deploy IMG=…` — image build / kustomize deploy to the current kubeconfig.
- ⚠️ `make test-e2e` creates and deletes a kind cluster (`control-plane-test-e2e`) — slow, CI territory; don't run casually.
- All three test suites (`make test`, `go test ./...`, `yarn test`) **must pass before `deploy.yml` builds or pushes any image**: `build-and-deploy` `needs:` all three test jobs.

## Local cluster workflow

- `bash scripts/mock-cluster.sh` — full local mock (kind infra cluster + Cluster API + CAPD app cluster). Writes kubeconfig to `infra/local/bex.kubeconfig` (gitignored credential — never commit or print it).
- `bash scripts/mock-cluster.sh scale N` — add/remove worker machines.
- `scripts/app-apply.sh <bex.yml>` — apply a `render.yaml`-shaped `bex.yml` as an App CR (`DRY_RUN=1` to preview).
- `scripts/deploy-sample.sh` — deploy the hello-go sample.
- `kubectl get apps.app.bex.co` — App status: phase / revision / url.

## Environment variables

| Component | Variable | Meaning |
| --- | --- | --- |
| operator | `BEX_RUNTIME` | `kubernetes` (Deployments) or `opensandbox` (host sandboxes) |
| operator | `BEX_REGISTRY`, `BEX_KPACK_REGISTRY`, `BEX_CNB_BUILDER` | canonical image registry (Zot, e.g. `zot.bex-registry.svc:5000`), optional kpack push alias (local HTTP default `zot.local:5000`; omit to use the canonical registry), and the CNB builder imported by the platform `ClusterBuilder` |
| operator | `BEX_REGISTRY_PUSH_SECRET`, `BEX_REGISTRY_PULL_SECRET` | registry authn (w7/m8 + w2/m59, docs/ADR022-tenant-isolation.md § Registry access control): Zot denies anonymous catalog/pull/push. `PUSH_SECRET` names the build-namespace bootstrap/shared-fallback docker config used by post-build Skopeo/Cosign and by the admission verifier; with `BEX_REGISTRY_NS`, the operator instead mirrors each repository-scoped `reg-pull-<name>` credential into `bex-build` for that App's push/sign phases. BuildKit exports an OCI archive and never receives either output credential. `PULL_SECRET` is the legacy shared kubelet `imagePullSecret` fallback when per-App credentials are disabled; unset ⇒ unauthenticated dev behavior. |
| operator | `BEX_REGISTRY_NS` | namespace of the Zot registry Secrets (`zot-htpasswd`, `zot-config`) managed by the operator for per-App pull credentials (w7/m36, docs/ADR022-tenant-isolation.md § Per-App pull credentials); set ⇒ operator mints per-App `reg-pull-<name>` Secrets + htpasswd entries + Zot ACL entries, superseding the shared `BEX_REGISTRY_PULL_SECRET`; unset ⇒ shared pull-secret path (byte-identical to w7/m8) |
| operator | `BEX_REGISTRY_BUILD_PULL_SECRET` | build-namespace dockerconfigjson Secret used as the `imagePullSecret` for the static-site publish Job's extract initContainer, which pulls the just-built tenant image from Zot (w9/m44, docs/ADR029-static-sites.md). The apps-namespace tenant pull secret (`reg-pull-<name>` / `BEX_REGISTRY_PULL_SECRET`) is **unreachable** when the publish Job runs in a separate `BEX_BUILD_NAMESPACE`, so this is a distinct build-ns credential (`scripts/registry-secrets.sh`'s `bex-registry-pull`, the `bex-builder` user with wildcard read); defaults to `bex-registry-pull` when `BEX_REGISTRY_PUSH_SECRET` is set; unset ⇒ anonymous pull (dev, byte-identical) |
| operator | `BEX_ZOT_HTPASSWD_SECRET`, `BEX_ZOT_CONFIG_SECRET` | names of the Zot htpasswd and config Secrets in `BEX_REGISTRY_NS` (defaults `zot-htpasswd` and `zot-config`); override only when the Secrets are renamed |
| operator | `BEX_ZOT_RETENTION_COUNT` | `mostRecentlyPushedCount` in the Zot per-App repo retention policy (default `5`); increase to retain more historical image tags; decrease to save registry storage |
| operator | `BEX_BUILD_NAMESPACE` | namespace the in-cluster BuildKit Jobs, kpack Images/builds, and the pre-deploy Jobs (`predeploy-<name>-gen-<generation>`, w1/m33, docs/ADR004-app-deployment.md § Pre-deploy command) run in; unset ⇒ the App's own namespace |
| operator | `BEX_APPS_NAMESPACE` | tenant App/Database/KeyValue namespace whose Secrets the manager caches (default `default`); must match bex-api's `BEX_API_NAMESPACE` / `BEX_CP_APPS_NAMESPACE` and the namespace containing `bex-operator-apps` RoleBinding. Other resource kinds remain watched cluster-wide |
| operator | `BEX_TENANT_SIGNING_KEY_SECRET`, `BEX_TENANT_SIGNING_IMAGE` | opt-in tenant-image signing + admission verification (w6/006, w7/m11): a Secret name (keys `cosign.key`+`cosign.password`+`cosign.pub`, in the build namespace) that makes the build Job cosign-sign each pushed tenant image (build+push becomes an initContainer, cosign runs as the main container); when `cosign.pub` is also present in the Secret the `/validate-v1-pod` admission webhook rejects pods with unsigned or tampered tenant images; unset ⇒ tenant images unsigned and unverified (default, byte-identical). `BEX_TENANT_SIGNING_IMAGE` overrides the cosign image |
| operator | `BEX_OPENSANDBOX_URL` | OpenSandbox endpoint (opensandbox runtime) |
| operator | `BEX_BASE_DOMAIN`, `BEX_CLUSTER_ISSUER` | `*.onbex.co` app URLs, cert-manager issuer |
| operator | `BEX_DB_DOMAIN` | public managed-Postgres hostnames `<name>.<domain>` via Traefik TCP/SNI (docs/ADR009-postgresql-management.md); unset ⇒ internal-only |
| operator | `BEX_PROM_URL` | Prometheus base URL for the Postgres disk-autoscaling loop's already-scraped `kubelet_volume_stats_*` series; unset ⇒ the field round-trips but automatic disk growth is disabled |
| operator | `BEX_DB_BACKUP_DESTINATION`, `BEX_DB_BACKUP_ENDPOINT`, `BEX_DB_BACKUP_S3_SECRET` | Non-secret contract for managed-Postgres backups + PITR (docs/ADR009-postgresql-management.md): it must match the namespaced `bex-tenant-postgres` Barman Cloud `ObjectStore`, and supplies the S3 URL prefix, endpoint, and Secret name used by logical-export/purge Jobs. The Secret contains `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`; any variable unset ⇒ backups disabled for every plan (recovery unavailable) |
| operator | `BEX_KV_BACKUP_DESTINATION`, `BEX_KV_BACKUP_ENDPOINT`, `BEX_KV_BACKUP_S3_SECRET` | Non-secret contract for paid-plan managed-KeyValue RDB backups (docs/ADR021-keyvalue-management.md): destination prefix (production `s3://bex-tfstate/keyvalue`), S3-compatible endpoint, and apps-namespace Secret name. The Secret contains `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`; all three set ⇒ each non-Free instance gets a staggered nightly coherent RDB snapshot with keep-7 retention and delete-time prefix purge; any unset ⇒ backups disabled and no CronJobs/finalizer are added to new instances |
| operator | `BEX_KV_DOMAIN` | public managed key-value (Valkey) hostnames `<name>.<domain>` via Traefik TCP/SNI (docs/ADR021-keyvalue-management.md); unset ⇒ internal-only |
| operator | `BEX_BACKUP_AGE_PUBLIC_KEY` | ADR050 Tier A opt-in: the `age` recipient **public** key (safe to set in clear) that makes the paid-KeyValue backup Job age-encrypt each RDB before upload (`.rdb.gz.age`). The private half stays out-of-band in `.env`/CI (`AGE_BACKUP_PRIVATE_KEY`), consumed only by `scripts/restore-*.sh` to decrypt; unset ⇒ plain `.gz` upload (byte-identical to pre-ADR050). The two static backup CronJobs (etcd, OpenBao) read the same public key from the optional `bex-backup-age` ConfigMap in their namespace instead (docs/ADR050-encrypted-platform-backups.md) |
| operator | `BEX_ACTIVATOR_SERVICE` | k8s Service name of the wake activator (e.g. `bex-activator`); unset ⇒ auto-sleep disabled |
| operator | `BEX_ACTIVATOR_PORT` | activator service port (default `8888`) |
| activator | `BEX_ACTIVATOR_ADDR` | listen address (default `:8888`) |
| pg-sni-proxy | `BEX_PROXY_ADDR`, `BEX_PROXY_METRICS_ADDR`, `BEX_PROXY_PROTOCOL_TRUSTED_CIDRS` | TCP listen address (default `:5432`), internal Prometheus address (default `:9092`), and comma-separated immediate-peer CIDRs trusted to assert the public client through PROXY protocol; the proxy handles PostgreSQL SSLRequest preamble, routes by SNI to CNPG backends using `BEX_DB_DOMAIN`, and meters backend→client bytes (docs/ADR009-postgresql-management.md §SNI proxy) |
| pg-sni-proxy | `BEX_PROXY_MAX_CONNS`, `BEX_PROXY_MAX_CONNS_PER_SOURCE`, `BEX_PROXY_IDLE_TIMEOUT`, `BEX_PROXY_MAX_LIFETIME` | connection-admission + copy-loop bounds (F6): global concurrent-connection cap checked at accept time before dispatching a handler goroutine (default `1024`), per-source (client IP) cap applied after PROXY-header resolution before the backend dial (default `128`), and idle (default `1h`) + max-lifetime (default `24h`) budgets on the routed proxy copy loop; overload sheds immediately (accepted conn closed, metered `bex_pg_proxy_connections_rejected_total`) rather than queueing. `0` disables that dimension |
| kv-sni-proxy | `BEX_KV_PROXY_ADDR`, `BEX_KV_PROXY_METRICS_ADDR`, `BEX_KV_DOMAIN`, `BEX_PROXY_PROTOCOL_TRUSTED_CIDRS` | public TLS/SNI front door (defaults `:6379` and `:9093`), wildcard domain, and comma-separated immediate-peer CIDRs trusted to assert the public client through PROXY protocol; routes exact public KeyValue intent to Valkey TLS port `6380`, enforces source allowlists, and meters backend→client bytes |
| kv-sni-proxy | `BEX_KV_PROXY_MAX_CONNS`, `BEX_KV_PROXY_MAX_CONNS_PER_SOURCE`, `BEX_KV_PROXY_IDLE_TIMEOUT`, `BEX_KV_PROXY_MAX_LIFETIME` | the Valkey front door's mirror of the pg-sni-proxy admission + copy-loop bounds (F6), same defaults (`1024` / `128` / `1h` / `24h`); `0` disables that dimension |
| egress-meter | `BEX_EGRESS_NODE_NAME`, `BEX_EGRESS_METRICS_ADDR`, `BEX_EGRESS_EXCLUDED_CIDRS`, `BEX_APPS_NAMESPACE` | node identity, internal Prometheus address (default `:9091`), extra comma-separated private/cluster CIDRs, and watched App namespace for the post-policy direct-public L3 meter |
| operator | `BEX_STATIC_S3_ENDPOINT`, `BEX_STATIC_S3_BUCKET`, `BEX_STATIC_S3_REGION`, `BEX_STATIC_PUBLISH_S3_SECRET` | static-site (`static_site`) publish target (docs/ADR029-static-sites.md): S3-compatible endpoint + a bucket **dedicated to static content** (never `bex-tfstate`) + optional region + a bucket-scoped publish/purge Secret name in `BEX_BUILD_NAMESPACE` (production `bex-static-publish-s3`); before dispatch the operator requires both AWS credential keys, and any setting/Secret missing ⇒ the App fails with `StaticCredentialUnavailable` |
| operator | `BEX_STATIC_SERVER_SERVICE`, `BEX_STATIC_SERVER_PORT` | k8s Service name + port (default `8080`) of the shared static-server a static-site host's Ingress backs onto (docs/ADR029-static-sites.md); service unset ⇒ `static_site` serving unavailable |
| static-server | `BEX_STATIC_ADDR`, `BEX_STATIC_NAMESPACE`, `BEX_STATIC_S3_ENDPOINT`/`_BUCKET`/`_REGION`, `BEX_STATIC_CACHE_BYTES`, `BEX_STATIC_RESYNC` | listen addr (default `:8080`), watched App ns (empty ⇒ all), object-store origin (endpoint/bucket unset ⇒ degraded 503 mode), in-memory cache budget (default 256 MiB), host→site refresh interval (default `10s`); AWS creds come only from required `bex-system/bex-static-read-s3`, a read-only `bex-static` identity distinct from the publisher; startup fails if its signed origin check is denied |
| bex-api | `BEX_API_ADDR` (:8090), `BEX_API_NAMESPACE`, `BEX_API_CORS_ORIGIN`, `BEX_API_PUBLIC_URL` | listen addr, watched ns, CORS origin allowlist (comma-separated), and externally reachable API origin used in copy-ready deploy-hook URLs (e.g. `https://api.bex.co`; unset ⇒ relative hook paths) |
| bex-api | `BEX_REGION` | explicit platform placement name projected onto Service/Postgres/Key Value REST metadata (e.g. `fsn1`); unset ⇒ region omitted, never inferred from S3/provider settings |
| bex-api | `BEX_SSH_HOST` | public SSH gateway hostname (for example `ssh.bex.co`) advertised as Render-compatible `serviceDetails.sshAddress` for running instances AND — w2/m65, docs/ADR054-open-in-zed.md D5 — as `agentSession.sshAddress` (`ags-<id>@<host>`) for the dashboard "Open in Zed" affordance when the session's sandbox is live; unset ⇒ SSH addresses omitted everywhere |
| bex-api | `BEX_SHELL_TICKET_SECRET`, `BEX_SHELL_WS_URL` | Browser Web Shell (w2/m55, docs/ADR035-ssh.md § Browser Web Shell): the HMAC-SHA256 key shared **only** with the isolated gateway that signs the short-lived exec ticket bex-api mints after `AuthorizeApp(can_operate)`, and the browser-reachable gateway WebSocket origin (e.g. `wss://ssh.bex.co/shell`) handed to the dashboard terminal. Either unset ⇒ the ticket verb (`POST /v1/services/{id}/shell-ticket`, GraphQL `createShellSession`) returns 503; native `ssh` is unaffected. bex-api mints the ticket but never gains `pods/exec` |
| bex-api | `BEX_AGENT_SESSION_GATEWAY_URL`, `BEX_AGENT_SESSION_IMAGE`, `BEX_SHELL_TICKET_SECRET` | Cloud coding-agent sessions (ADR047 D3/D9, w3/m39+m43): browser-reachable origin of the agent-session conversation stream — per ADR047 D9 the primary API origin `https://api.bex.co/v1/agent-sessions`, which the edge path-routes to the gateway process (one origin, no CORS; a dedicated origin is the fallback) — plus the platform-registered driver/ACP-adapter sandbox image (defaults to `BEX_SANDBOX_IMAGE`), and the existing Web-Shell HMAC trust key. Create/resume/steer/**attach-ticket** mint a distinct short-lived ticket (90s TTL) whose nonce is claimed through the shared DB-backed single-use mechanism and whose claims bind subject + session + exact sandbox pod + workspace; origin or secret unset ⇒ those mint verbs 503, while list/get remain available. bex-api authorizes + signs but never connects to the session driver's network path (the gateway alone dials the sandbox). |
| bex-api | `BEX_SANDBOX_EXEC_SECRET`, `BEX_SANDBOX_EXEC_URL` | `render ea sandbox exec` (w3/m33): the HMAC-SHA256 key shared **only** with the isolated SSH gateway that signs the per-exec ticket bex-api mints after authorizing `can_operate` on a sandbox (binding the exact `<ws>-sandbox` pod + signed argv), and the gateway's internal exec endpoint (e.g. `http://bex-ssh-gateway.bex-system.svc:8081/sandbox-exec`) bex-api reverse-proxies the SSE stream from. Either unset ⇒ `POST /v1/sandboxes/{id}/exec` + MCP `sandbox_exec` return 503; create/list/stop unaffected. bex-api authorizes + signs but never gains `pods/exec` (Option A — the gateway alone execs) |
| ssh-gateway | `BEX_SSH_ADDR`, `BEX_SSH_METRICS_ADDR`, `BEX_SSH_HOST_KEY_PATH` | isolated gateway SSH address (default `:2222`), internal health/Prometheus address (default `:9090`), and required mounted stable host private-key path; the key is installed out of band by `scripts/ssh-host-key-secret.sh` |
| ssh-gateway | `BEX_SHELL_WS_ADDR`, `BEX_SHELL_TICKET_SECRET` | Browser Web Shell gateway transport (w2/m55): the plain-HTTP WebSocket listen address Traefik terminates TLS onto (default `:8080`, path `/shell`) and the HMAC key that must match bex-api's `BEX_SHELL_TICKET_SECRET`. The gateway verifies the ticket, re-runs `ResolveSSHSession` (targeting + `AuthorizeApp(can_operate)`), and bridges the browser to `pods/exec`. `BEX_SHELL_TICKET_SECRET` unset ⇒ the WebSocket listener is not started (feature disabled, byte-identical default) |
| ssh-gateway | `BEX_SANDBOX_EXEC_ADDR`, `BEX_SANDBOX_EXEC_SECRET` | `render ea sandbox exec` gateway transport (w3/m33): the **internal-only** plain-HTTP listen address (default `:8081`, path `/sandbox-exec`, reachable only from bex-api — never the edge) and the HMAC key that must match bex-api's `BEX_SANDBOX_EXEC_SECRET`. The gateway verifies the signed ticket, runs the signed argv in the ticket's `<ws>-sandbox` pod via one `pods/exec`, and streams stdout/stderr + exit as SSE (`event: output`/`exit`). The agent-session Completer reuses this same exec boundary to harvest the driver's transcript log at completion (ADR051, w3/m77) — no separate route. `BEX_SANDBOX_EXEC_SECRET` unset ⇒ the listener is not started (feature disabled, byte-identical default) |
| ssh-gateway | `BEX_AGENT_CREDENTIAL_ADDR`, `BEX_AGENT_CREDENTIAL_API_URL` | ADR047 D2's cluster-internal Git credential broker (default `:8082`, path `/session-credential`) and bex-api's internal mint URL. A sandbox calls the gateway, which authenticates its source Pod and HMAC-proxies to bex-api using the domain-separated `BEX_SANDBOX_EXEC_SECRET`; the listener has no edge route and is disabled when that secret is unset. |
| ssh-gateway | `BEX_AGENT_ATTACH_ADDR`, `BEX_AGENT_SESSION_DRIVER_PORT`, `BEX_SHELL_TICKET_SECRET` | ADR047 D9 agent-session conversation transport (w3/m43): the **browser-facing** plain-HTTP SSE listen address (default `:8083`) the edge terminates TLS onto and path-routes `api.bex.co/v1/agent-sessions/{id}/stream` to, and the in-sandbox driver stream port the gateway dials directly (default `8787`, must match the driver's `BEX_AGENT_LISTEN_PORT`). The gateway verifies the agent-session ticket (reusing `BEX_SHELL_TICKET_SECRET`), replays the durable transcript, then byte-transparently splices + tees the live driver stream (`GET`) or forwards a live prompt turn (`POST`). Cookies on this path are ignored — the ticket is the sole credential; the `x-vercel-ai-ui-message-stream: v1` marker is preserved end to end. `BEX_SHELL_TICKET_SECRET` unset ⇒ the listener is not started (feature disabled). |
| ssh-gateway | `BEX_CP_DB_URI`, `BEX_OPENFGA_URL`, `BEX_OPENFGA_TOKEN`, `BEX_API_NAMESPACE` | required key/audit store, required authorization endpoint, optional FGA token, and the one tenant App namespace the gateway may discover/exec in |
| ssh-gateway | `BEX_SSH_HANDSHAKE_TIMEOUT`, `BEX_SSH_SESSION_TIMEOUT`, `BEX_SSH_MAX_SESSIONS`, `BEX_SSH_MAX_SESSIONS_PER_IDENTITY` | connection bounds; defaults `10s`, `4h`, `100`, and `5` |
| ssh-gateway | `BEX_SSH_MAX_PREAUTH_CONNS` | pre-authentication connection cap: concurrent SSH handshakes (accept → `NewServerConn` + key-lookup) allowed in flight before the post-handshake session limiter applies, bounding an anonymous connection flood's goroutine/descriptor/DB-lookup cost; a full buffer sheds immediately (metered `bex_ssh_limit_rejected_total{scope="preauth"}`). Default `256` |
| ssh-gateway | `BEX_SSH_MAX_CHANNELS_PER_CONN` | "Open in Zed" per-connection channel cap (w2/m65, docs/ADR054-open-in-zed.md D3): an agent-session sandbox target (`ags-…@ssh.bex.co`) may open this many concurrent `session` channels over ONE connection — what Zed's ControlMaster remoting multiplexes for its protocol + terminals + tasks — each bridged to its own `pods/exec` stream; default `16`, `0` disables the exception and restores single-channel for sandbox targets too. App (`srv-…`) targets are always single-channel regardless |
| bex-api | `BEX_BUILD_NAMESPACE` | the deploys feature's Cancel verb (w2/m10): namespace a repo-backed App's in-flight build Job runs in, so Cancel can compute its identity (`bld-<name>-gen-<generation>`) to delete it. Also where the logs feature reads pre-deploy Job pod logs (`type=predeploy`, w1/m33) from — must match the operator's own `BEX_BUILD_NAMESPACE` above; unset ⇒ the App's own namespace |
| bex-api | `BEX_HYDRA_ADMIN_URL` (required), `BEX_KRATOS_URL` | OAuth2 API keys via Hydra introspection; Kratos sessions (docs/ADR012-auth.md) |
| bex-api | `BEX_KRATOS_ADMIN_URL` | Kratos' admin API for the owners/members read API's email/MFA lookup (docs/render-artifacts/owners-api.md); unset ⇒ those fields omitted |
| bex-api | `BEX_OPENFGA_URL`, `BEX_OPENFGA_TOKEN` | authorization via OpenFGA; unset ⇒ allow-all (docs/ADR012-auth.md) |
| bex-api | `BEX_ALLOW_INSECURE_AUTHZ` | fail-closed override (w1/m65 F16, docs/ADR045-security-review-round3.md lineage): when `BEX_CP_DB_URI` is set but `BEX_OPENFGA_URL` is unset, bex-api **refuses to start** — authorization would be fail-open (every member admin-equivalent, and explicit-workspace verbs bypass membership resolution, so cross-tenant isolation would not hold). `1` ⇒ start anyway with a loud warning (single-member workspace / local dev only); unset/other ⇒ fail closed |
| bex-api | `BEX_BASE_DOMAIN` | platform wildcard domain (e.g. `onbex.co`) — names custom-domain DNS targets `<app>.<domain>` in the DNS-instructions surface (docs/ADR005-custom-domain.md); unset ⇒ derived from the App's status URL |
| bex-api | `BEX_PROM_URL` | Prometheus base URL for request metrics (Traefik) and resource-metrics history (cAdvisor); unset ⇒ request metrics 503, resource metrics fall back to the metrics-server snapshot |
| bex-api | `BEX_USAGE_RETENTION_MONTHS` | usage hot window (docs/ADR023-usage-metering.md): calendar months (current included) kept at hourly detail before daily compaction folds older months into `usage_monthly` and purges the hourly rows; default `3`, minimum 1 |
| bex-api | `BEX_AUDIT_RETENTION_DAYS` | audit retention (w4/m10 + w2/m39, docs/ADR006-bex-api.md § Audit log): days `audit_events` and SSH-session metadata survive before the daily sweep purges them; default `90`, minimum 1 |
| bex-api | `BEX_STRIPE_SECRET_KEY` | restricted Stripe runtime key (w7/m50–m51, docs/ADR040-billing-metronome.md) enabling metadata-keyed Customers, metered Subscriptions, sealed meter-event export, real invoice reads, and hosted payment onboarding; needs `BEX_CP_DB_URI`. **Unset ⇒ no Stripe client/network, emitter, billing object, onboarding sessions, or webhook route; estimate-only behavior is unchanged.** Keep out of band and never log it |
| bex-api | `BEX_REQUIRE_PAYMENT_METHOD` | `1` enables ADR046's paid-intent gate: every non-free Service/Postgres/Key Value create or plan change (including Blueprint) requires the local `checkout.session.completed` marker; cardless meter rows remain pending instead of provisioning Stripe objects. Requires both `BEX_STRIPE_SECRET_KEY` and `BEX_CP_DB_URI` or startup fails; unset/`0` ⇒ nil gate and prior behavior |
| bex-api | `BEX_STRIPE_API_URL` | test/stub API override; unset in production ⇒ Stripe SDK default `https://api.stripe.com` |
| bex-api | `BEX_STRIPE_SEAL_HOURS` | rewrite horizon (hours) before a `usage_hourly` row is final and exportable (ADR040 §3, aligned with ADR023's 48h compaction clamp); default `48`, minimum 1 |
| bex-api | `BEX_STRIPE_EPOCH` | RFC3339 billing-start floor: the emitter never ships a row before `max(epoch, now − 34d)`, staying inside Stripe's 35-calendar-day meter-event timestamp limit; unset while Stripe is enabled ⇒ `now − 34d`; malformed while enabled fails startup |
| bex-api | `BEX_STRIPE_WEBHOOK_SECRET`, `BEX_STRIPE_COMP_COUPON_ID` | Stripe endpoint signing secret for public `POST /v1/webhooks/stripe` (route absent when unset), and the stable perpetual 100%-off Mode-B coupon id (default `bex-comp-100`); both are provisioned/custodied per `docs/runbooks/stripe-billing-setup.md` |
| bex-api | `BEX_STRIPE_PORTAL_CONFIGURATION_ID` | optional operator-owned Stripe Customer Portal `bpc_*` configuration; runtime creates only Customer-scoped sessions and never edits the configuration |
| bex-api | `BEX_STRIPE_TAX_CODE`, `BEX_STRIPE_TAX_BEHAVIOR` | fail-closed Stripe Tax gate (w7/m51): operator-confirmed canonical `txcd_*` Product code plus explicit `exclusive`/`inclusive` Price behavior. Both plus a matching catalog and active same-mode registration are required before automatic tax can be enabled; either unset ⇒ reported unconfigured, collection off |
| bex-api | `BEX_LOKI_URL` | Loki base URL for the durable log store (docs/ADR010-observability.md); set ⇒ `QueryLogs`/`Logs` read Loki (history survives pod restarts) **and** the request-log split (`type=request`) + structured filters (`level`/`statusCode`/`method`/`path`/`host`) + label discovery (`list_log_label_values`) work; unset ⇒ live pod-log fallback (app logs, `text`/`time`/`instance` only — the store-only filters return 503, never silently ignored). The SSE live tail always reads pod logs |
| bex-api | `BEX_OPENBAO_URL` | OpenBao base URL for the tenant env-vars store (docs/ADR013-secrets.md); unset ⇒ env-vars verbs 503 |
| bex-api | `BEX_OPENBAO_JWT_PATH` | override the ServiceAccount token path for OpenBao k8s-auth login (off-cluster/dev; default is the pod's projected token) |
| bex-api | `BEX_CP_DB_URI` | Postgres URI for the control-plane store (docs/ADR003-control-plane.md); set ⇒ runs migrations + the apps-rows→App-CR projector + the internal tenant API + the NamespaceReconciler, which unconditionally provisions each workspace's per-tenant `<ws>` and `<ws>-sandbox` namespaces (identity labels + plan-scaled `ResourceQuota`/`LimitRange` + default-deny `NetworkPolicy`, ADR043) and prunes them on workspace delete; unset ⇒ bex-api alone |
| bex-api | `BEX_CP_APPS_NAMESPACE`, `BEX_CP_ADDR` (:8091), `BEX_CP_RESYNC`, `BEX_CP_TOKEN` | control-plane knobs: projection target ns, internal API addr, resync interval, internal API bearer |
| bex-api | `BEX_OPENSANDBOX_URL`, `BEX_SANDBOX_IMAGE` | hosted agent sandboxes (pillar 5, ADR042/w3/m32): set `BEX_OPENSANDBOX_URL` ⇒ bex-api wires the OpenSandbox lifecycle client and serves the Render-compatible `/v1/sandboxes*` surface (`render ea sandbox create/list/stop`), sending a per-workspace `OPEN-SANDBOX-API-KEY` tenant key; unset ⇒ the sandbox verbs 503 and the feature is unregistered (byte-identical). `BEX_SANDBOX_IMAGE` overrides the default template image (default `opensandbox/base:latest`) |
| bex-api | `BEX_AGENT_SETUP_REGISTRIES` | ADR047 agent-session setup egress (w3/m40): optional comma-separated exact FQDN catalog replacing the built-in npm/PyPI/Go/crates/Maven/RubyGems/Docker setup registries. Invalid or empty members abort startup. The model endpoint is not global config: each session's selected agent/provider supplies one validated HTTPS endpoint. The credential helper hop is fixed to identity-confined `bex-ssh-gateway.bex-system.svc.cluster.local:8082`. |
| bex-api | `BEX_WEBHOOK_SECRET` | shared HMAC-SHA256 key for the git push webhook (`POST /v1/webhooks/git`); unset ⇒ webhook 503 (docs/ADR006-bex-api.md) |
| bex-api | `BEX_WEBHOOK_RETENTION_DAYS`, `BEX_WEBHOOK_RETENTION_KEEP` | outbound-delivery retention (w1/m67 F3, docs/ADR006-bex-api.md § Outbound event webhooks): how long a **terminal** delivery row (delivered or retries-exhausted) survives before the worker's hourly bounded sweep purges it (default `90` days, matching the audit log), and how many terminal rows one endpoint keeps regardless of age (default `1000`). `webhook_deliveries` is both the durable queue and the dashboard's history view, so before m67 nothing ever reclaimed a finished row. A pending or retryable delivery is never purged; `0` on either ⇒ that default |
| bex-api | `BEX_WEBHOOK_BACKOFF` | outbound-webhook retry schedule override (w3/m11, docs/ADR006-bex-api.md § Outbound event webhooks): comma-separated Go durations (`"2s,3s,4s"`) replacing the documented 8-retry/~33h default — a dev/verification knob (`scripts/webhooks-verify.sh` walks the auto-disable path in seconds with it); unset ⇒ the production schedule |
| bex-api | `BEX_GITHUB_APP_ID`, `BEX_GITHUB_APP_PRIVATE_KEY`, `BEX_GITHUB_APP_SLUG` | self-hosted GitHub App (docs/ADR026-github-integration.md): app id (JWT `iss`), RSA private key PEM (out-of-band secret; signs the app JWT and HMAC-signs the short-lived browser callback state), and slug (builds the `github.com/apps/<slug>/installations/new` install URL). Any unset ⇒ every git-connect verb 503 |
| bex-api | `BEX_GITHUB_APP_CLIENT_ID`, `BEX_GITHUB_APP_CLIENT_SECRET` | the App's OAuth credentials (w1/m65 F2): both set (and the App configured to "Request user authorization during installation") ⇒ the browser connect callback verifies via the callback `code` that the user actually administers the installation before binding it, closing the cross-tenant installation-attach path. Client secret is out-of-band. Unset ⇒ the unique installation→workspace binding (always enforced) alone protects the callback |
| bex-api | `BEX_GITHUB_WEBHOOK_SECRET` | the GitHub App's webhook HMAC-SHA256 key (docs/ADR026-github-integration.md): a **second** accepted key on `POST /v1/webhooks/git` so app-signed pushes redeploy hands-free; verified alongside `BEX_WEBHOOK_SECRET` (valid under either ⇒ accept; 503 only when neither set) |
| bex-api | `BEX_SMTP_ADDR`, `BEX_SMTP_FROM` | SMTP relay (`host:port`) + envelope `From` for workspace-invite email (w4/m12, docs/ADR024-members.md); same relay as the Kratos courier (SendGrid prod, Mailpit local). Either unset ⇒ invites recorded but not emailed |
| bex-api | `BEX_SMTP_USERNAME`, `BEX_SMTP_PASSWORD` | optional SMTP PLAIN-auth credentials (secret, out-of-band); unset ⇒ unauthenticated relay (Mailpit) |
| bex-api | `BEX_REQUIRE_VERIFIED_INVITE_EMAIL` | login-time workspace-invite redemption requires the caller's Kratos-verified email (w1/m53 V1 + w1/040 + **w1/m65 F13**, docs/ADR024-members.md § Email verification) — needs the dashboard `/auth/verification` page so invitees can verify. **Secure by default now: on unless set to `0`/`false`** (email is an authorization key, so ownership must be proven); set `0` only for local dev without a verification UX. The `?invite=<token>` bearer-link path is unaffected either way |
| bex-api | `BEX_DASHBOARD_URL` | trusted dashboard origin for GitHub install-callback redirects, workspace-invite links, and Service/Postgres/Key Value `dashboardUrl` metadata (e.g. `https://dashboard.bex.co`); unset ⇒ callbacks return JSON, invite email has no deep link, and resource dashboard URLs are omitted |
| bex-api | `BEX_MCP_STDIO` | `1` ⇒ serve only the MCP tools over stdio (same as `api mcp-stdio`) |
| bex-api | `BEX_OAUTH_ISSUER`, `BEX_OAUTH_RESOURCE` | OAuth 2.1 discovery for MCP/agent clients (docs/ADR012-auth.md §7): Hydra public issuer + this API's canonical resource URI — drives RFC 9728 metadata, 401 `resource_metadata` hints, and the token-audience check; both unset ⇒ prior behavior |
| bex-api | `BEX_OAUTH_REQUIRE_AUDIENCE` | `1` narrows the empty-audience token exception (w1/m67 F1, docs/ADR012-auth.md §7): a **human** token carrying no `aud` is then accepted only from an OAuth client Hydra's own record marks `bex.co/platform-client` (the official Render CLI's device flow, bex-mobile — stamped by `scripts/auth-bootstrap-client.sh`); `client_credentials` API keys keep the exception unconditionally. Requires `BEX_OAUTH_RESOURCE`. **Enable only after re-running the bootstrap script**, or the CLI's legitimately audience-less logins are refused; unset/`0` ⇒ prior blanket exception |
| bex-api | `BEX_RATE_LIMIT` | per-caller token-bucket fill rate in requests/min (default 500 — Render's documented budget); `0` disables rate limiting (docs/ADR006-bex-api.md §Rate limits) |
| bex-api | `BEX_RATE_BURST` | token-bucket burst capacity (default = `BEX_RATE_LIMIT`); `0` defaults to `BEX_RATE_LIMIT` |
| bex-api | `BEX_AUTH_FAILURE_LIMIT`, `BEX_AUTH_FAILURE_BURST`, `BEX_AUTH_MAX_INFLIGHT` | pre-auth admission (w1/m67 F1, docs/ADR006-bex-api.md §Rate limits): the per-caller limiter runs **inside** the auth gate, keyed on the resolved identity, so it cannot bound the work of resolving one — and with no negative cache every unique invalid bearer/session costs one Hydra/Kratos round trip. These bound it: an IP-keyed budget charged **only** for credentials that come back invalid (default `60`/min, burst = limit; a successful auth is never charged, so the dashboard's one-pod-IP SSR traffic is never throttled) plus a process-wide cap on concurrent upstream auth calls (default `64`). Trusted-proxy aware via `BEX_TRUSTED_PROXY_CIDRS`; `0`/`0` disables both (byte-identical to before m67) |
| bex-api | `BEX_DEVICE_RATE_LIMIT` | IP-keyed token-bucket fill rate in requests/min (default 30) guarding the three credential-less Render CLI device-flow routes (`POST /v1/device-grant`\|`/v1/device-token`\|`/v1/token/refresh/`, w4/m31/t002) — they mount outside the auth gate and `BEX_RATE_LIMIT`'s identity-keyed limiter, so nothing else meters them; a shed request never reaches Hydra and gets an OAuth-shaped `{"error":"slow_down"}` 429, never Render's REST error dialect; `0` disables |
| bex-api | `BEX_DEVICE_RATE_BURST` | device-route token-bucket burst capacity (default = `BEX_DEVICE_RATE_LIMIT`); `0` defaults to `BEX_DEVICE_RATE_LIMIT` |
| bex-api | `BEX_WEBHOOK_RATE_LIMIT` | IP-keyed token-bucket fill rate in requests/min (default 600) metering the two unauthenticated webhook intakes (`POST /v1/webhooks/git`\|`/v1/webhooks/stripe`, w7/m60) — they mount outside the auth gate and `BEX_RATE_LIMIT`'s identity-keyed limiter, so nothing else meters them; a shed request gets a plain 429 + `Retry-After` **before** the body read + HMAC/signature verification. The default is deliberately generous (no legitimate GitHub/Stripe source IP delivers 10 req/s, and both senders retry); `0` disables (byte-identical to before m60) |
| bex-api | `BEX_WEBHOOK_RATE_BURST` | webhook-intake token-bucket burst capacity (default = `BEX_WEBHOOK_RATE_LIMIT`); `0` defaults to `BEX_WEBHOOK_RATE_LIMIT` |
| bex-api | `BEX_TRUSTED_PROXY_CIDRS` | comma-separated CIDRs of the trusted edge proxies (the Traefik pod network) for rate-limit identity (w4/m33): when a request's immediate peer is inside one, every IP-keyed limiter (per-caller, device-flow, webhook-intake, deploy-hook) derives the real client IP from `X-Forwarded-For` (rightmost-to-leftmost first-untrusted walk) / `X-Real-IP`; unset or an untrusted peer ⇒ the peer IP is used and forwarding headers are ignored (byte-identical to before); malformed ⇒ startup fails |
| bex-api | `BEX_MAX_BODY_BYTES` | max non-GET request body size in bytes (default 2097152 = 2 MiB); `0` disables |
| bex-api | `BEX_MAX_QUERY_HOURS` | max `startTime`..`endTime` window for log/metrics queries in hours (default 720 = 30 days); enforced on the log reads across REST, GraphQL, and MCP (w9/004) and — since w1/049 — on the metrics reads in the shared service across the same three surfaces; `0` disables |
| bex-api | `BEX_MAX_SSE_CONNS` | max concurrent `GET /v1/logs/subscribe` SSE connections (default 100); `0` disables |
| operator | `BEX_APP_RECONCILE_WORKERS` | concurrent App reconcile loops (default `1`); source builds currently wait synchronously inside a reconcile, so production uses `2` for two independently dispatched builds (ADR034) |
| operator | `BEX_MAX_CONCURRENT_BUILDS` | max concurrent active build Jobs per workspace; `0` = unlimited. Newest-wins per App is always active (independent of this cap) |
| dashboard (SSR) | `HYDRA_ADMIN_URL`, `HYDRA_PUBLIC_URL`, `OAUTH_TRUSTED_CLIENTS` | OAuth2 consent + official Render CLI device verification at `/auth/consent` and `/auth/device` (docs/ADR012-auth.md §7/§8a): Hydra's admin API, its browser-reachable public issuer, and the allowlist of clients that skip the consent screen. Server-only (not `VITE_`); missing URLs make their corresponding routes answer 503. |

## Docs index

- [docs/ADR008-vision.md](docs/ADR008-vision.md) — mission, AI-native pillars, roadmap.
- [docs/ADR002-architecture.md](docs/ADR002-architecture.md) — the map: self-managed cluster + disposable bootstrap, two layers, node pools + substrate decisions, panorama diagram.
- [docs/ADR003-control-plane.md](docs/ADR003-control-plane.md) — Postgres source of truth (built, opt-in via `BEX_CP_DB_URI`) vs. operator mechanism.
- [docs/ADR020-identifiers.md](docs/ADR020-identifiers.md) — ADR: typed opaque resource ids `<prefix>-<xid>` (`tea-`/`srv-`/`cdm-`), hyphen not underscore; minted + guarded in `lego/backend/internal/id`.
- [docs/ADR006-bex-api.md](docs/ADR006-bex-api.md) — REST/GraphQL/MCP design: one core, thin adapters, Render compatibility.
- [docs/ADR025-connect-an-agent.md](docs/ADR025-connect-an-agent.md) — recipe: Claude Code/Cursor → bex `/mcp` over OAuth 2.1 (discovery, DCR, PKCE, API-key alternative, troubleshooting).
- [docs/ADR026-github-integration.md](docs/ADR026-github-integration.md) — ADR: self-hosted GitHub App (manifest flow) for private-repo deploys + zero-config push-to-deploy; installation tokens → clone Secret, app webhook as a second key.
- [docs/ADR018-render-parity.md](docs/ADR018-render-parity.md) — the parity ledger: one row per Render capability × REST/GraphQL/MCP/UI, each cell ✅/◐/✖/— with evidence; gaps mapped to owning milestones.
- [docs/cli-compatibility-checklist.md](docs/cli-compatibility-checklist.md) — bex's fifth surface: Render's own official CLI (`render-oss/cli`) run unmodified against bex-api, one row per command, ✅/◐/✖/— with captured evidence; the systemic wire-format bugs it found are the parity ledger's CLI-surface counterpart.
- [docs/ADR017-deploy-from-chat.md](docs/ADR017-deploy-from-chat.md) — ADR: deploy-from-chat rides `Core.Create` (no bespoke endpoint) + the HMAC push-to-deploy webhook (pillar 4).
- [docs/ADR010-observability.md](docs/ADR010-observability.md) — Logs (query + live-tail) and metrics over REST/GraphQL/MCP.
- [docs/ADR023-usage-metering.md](docs/ADR023-usage-metering.md) — Hourly usage rollup (instance-seconds/egress/build-seconds) + REST/GraphQL/MCP surface (`GET /v1/usage`, `usage` query, `get_usage` tool); bex extension (Render has no usage API).
- [docs/ADR004-app-deployment.md](docs/ADR004-app-deployment.md) — deploy flow, health gating, revisions.
- [docs/ADR005-custom-domain.md](docs/ADR005-custom-domain.md) — `App.spec.hosts[]`, Traefik + cert-manager.
- [docs/ADR007-restart-suspend-and-resume.md](docs/ADR007-restart-suspend-and-resume.md) — lifecycle verbs.
- [docs/ADR014-sandboxes.md](docs/ADR014-sandboxes.md) — ADR: E2B-compatible, idle-hibernated sandboxes over opensandbox (pillar 5).
- [docs/ADR011-etcd-backup-restore.md](docs/ADR011-etcd-backup-restore.md) — nightly etcd snapshot → object storage; restore runbook.
- [docs/ADR015-openbao-backup-restore.md](docs/ADR015-openbao-backup-restore.md) — nightly OpenBao Raft snapshot → object storage; restore runbook.
- [docs/ADR012-auth.md](docs/ADR012-auth.md) — ADR: Ory Kratos (identity) + Hydra (OAuth2) on CNPG; secrets out-of-band.
- [docs/ADR027-sso.md](docs/ADR027-sso.md) — ADR: SSO — social login (consumer OIDC, e.g. Sign in with GitHub) shipped via Kratos `oidc`; enterprise SSO/SAML/SCIM a deferred non-goal.
- [docs/ADR024-members.md](docs/ADR024-members.md) — workspace members & roles: invite by email, Render's five roles, `tenant_members` + OpenFGA tuples, invite-accept-on-login.
- [docs/ADR019-infra-credentials.md](docs/ADR019-infra-credentials.md) — ADR: bootstrap credential inventory + trust chain (`bex` SSH key → mgmt cluster CAPI PKI → app admin cert) and `.env` custody.
- [docs/ADR013-secrets.md](docs/ADR013-secrets.md) — ADR: OpenBao for tenant credentials; integrated Raft storage, Shamir unseal via `.env`, Kubernetes auth scoped to `tenants/*`.
- [docs/ADR016-sealed-secrets.md](docs/ADR016-sealed-secrets.md) — infra creds encrypted at rest in git (SealedSecrets), controller + `kubeseal` workflow.
- [docs/ADR009-postgresql-management.md](docs/ADR009-postgresql-management.md) — managed tenant Postgres: `Database` CR → CNPG Cluster, plans, internal/external URLs.
- [docs/ADR021-keyvalue-management.md](docs/ADR021-keyvalue-management.md) — managed tenant key-value (Valkey): `KeyValue` CR → Valkey StatefulSet, plans, internal/external URLs.
- [docs/ADR029-static-sites.md](docs/ADR029-static-sites.md) — the `static_site` type: build → object-store origin, the shared static-server, redirects/rewrites + custom headers, REST/GraphQL/MCP surface.
- [docs/ADR038-cron-jobs.md](docs/ADR038-cron-jobs.md) — the `cron_job` type: `spec.schedule` → Kubernetes CronJob (`ForbidConcurrent`), manual runs via `spec.runAt`, cancel via `spec.cancelRun`, durable `status.runs[]` history, and the REST/GraphQL/MCP trigger/list/cancel surface.
- [docs/ADR041-service-addresses.md](docs/ADR041-service-addresses.md) — internal (`<slug>:<port>`) + public (`https://<slug>.<BEX_BASE_DOMAIN>`) addresses for web/private services: slug-named ClusterIP Service, `fromService` slug resolution (fixed the store-managed bare-name gap), `serviceDetails.internalAddress` + dashboard Connect Internal / Service Address, contract methods in `types/v1alpha1`, Render-parity divergences.
- [docs/ADR001-go-and-gitops.md](docs/ADR001-go-and-gitops.md) — why bex (Go product) ≠ GitOps (platform infra).
- [docs/ADR022-tenant-isolation.md](docs/ADR022-tenant-isolation.md) — ADR: east-west network enforcement — threat model, label-scoped NetworkPolicy mechanism, dialect choice, reachability matrix.
- [docs/ADR028-security-review.md](docs/ADR028-security-review.md) — evidence-backed audit (RBAC, supply chain, injection surface, network isolation, secrets hygiene, OAuth) with severities, remediation status, and a follow-up register.
- [docs/ADR039-operator-audit-and-platform-reuse.md](docs/ADR039-operator-audit-and-platform-reuse.md) — code-level operator audit, ten correctness/security findings, and the reuse boundary/candidate analysis for OpenChoreo, Korifi, Kubero, and focused Kubernetes operators.
- [docs/ADR040-billing-metronome.md](docs/ADR040-billing-metronome.md) — **Accepted, revised by w7/m51**: direct Stripe Billing — sealed `usage_hourly` rows → meter events via the durable `emitted_at` outbox, metadata-keyed Customers, metered Subscriptions, invoice reads, Mode-A exclusion/Mode-B comping, Stripe-hosted payment onboarding/Portal, fail-closed Tax activation, and signature-verified webhook intake; env-gated by `BEX_STRIPE_SECRET_KEY`.
- [docs/ADR031-platform-data-backup.md](docs/ADR031-platform-data-backup.md) — consolidated platform data-backup policy: etcd, OpenBao, and bex-db backup mechanisms, one-time setup, restore runbooks, drill records, and re-drill cadence.
- [docs/ADR050-encrypted-platform-backups.md](docs/ADR050-encrypted-platform-backups.md) — **Proposed**: encrypts every backup in ADR031 and replaces the shared root S3 credential with write-only per-store credentials — an `age` keypair (public in git, private out-of-band like the OpenBao unseal keys) for client-side encryption of etcd/OpenBao/KeyValue, provider-side SSE for the Barman Cloud plugin-backed Postgres stores.
- [docs/ADR032-environments.md](docs/ADR032-environments.md) — Environments: named subsets of a Project's services (staging/production), layered on `internal/projects` (w1/m31); assignment auto-joins the project, REST/GraphQL/MCP surface, MVP scope (no protected-environment ACLs, no dashboard UX).
- [docs/ADR034-scalable-build-pipeline.md](docs/ADR034-scalable-build-pipeline.md) — scalable in-cluster builds: ephemeral BuildKit workers, reconcile/admission concurrency, Pod-versus-machine capacity, and the non-blocking evolution path.
- [docs/ADR035-ssh.md](docs/ADR035-ssh.md) — running-instance SSH: identity keys, Render-compatible instance targeting, isolated gateway, Kubernetes exec bridge, and production activation gates.
- [docs/ADR036-ca-rotation-runbook.md](docs/ADR036-ca-rotation-runbook.md) — Kubernetes CA rotation and admin-cert renewal: annual cert renewal (non-disruptive) and full CA rotation (emergency, disruptive); AdminCertExpiringSoon alert response.
- [docs/ADR037-openbao-rekey-runbook.md](docs/ADR037-openbao-rekey-runbook.md) — OpenBao root-token rotation (`bao operator generate-root`) and Shamir re-key (`bao operator rekey`): when to use each, exact commands, `.env` + GitHub Actions update.
- [docs/ADR044-sandbox-runtime-comparison.md](docs/ADR044-sandbox-runtime-comparison.md) — survey: AgentENV (Firecracker microVMs, overlaybd/ublk memory snapshots, Rust node + prototype control plane) vs. OpenSandbox (containers, k8s-native CRDs, full SDK/CLI/MCP surface) — convergences, the three dominant differences (memory snapshots, isolation substrate, platform completeness), and why bex stays on OpenSandbox per ADR014. (Renamed from ADR041 to resolve the number collision with ADR041-service-addresses.)
- [docs/ADR045-security-review-round3.md](docs/ADR045-security-review-round3.md) — security review round 3 (w7/m57): adversarial audit of the in-window (2026-07-20→HEAD) billing, static, tenant-namespace, and cross-cutting planes; severities + evidence + one disposition per finding. Headline: a cross-tenant host-hijack unguarded on the create paths (fixed in place). Third pass in the ADR028 → w1/m53 lineage.
- [docs/ADR055-security-review-round4.md](docs/ADR055-security-review-round4.md) — security review round 4 (codex-security repo scan, 2026-08-10): disposition of 12 findings (6 high, 6 medium). Root cause of F1–F5: workspace-local `App.Name` used as a global identity at shared sinks. Six fixed in place with tests (F1/F4/F5/F8/F11/F12); F6 + F10 ship the code path but leave enablement to an operator step (bootstrap-marker / known-hosts-secret prerequisites); F2/F3 deferred (data-migration-gated), F7 deferred (digest-pinning infra), F9 accepted risk (PSL). Fourth pass in the ADR028 → ADR045 → this lineage.
- [docs/ADR056-security-review-round5.md](docs/ADR056-security-review-round5.md) — security review round 5 (codex-security repo scan, 2026-08-13): disposition of 17 findings (8 high, 7 medium, 2 low). 13 fixed in place with regression tests — metrics point budget (2), uncached fresh authz re-check before credential/privilege issuance (4), UID-scoped build/pre-deploy cancellation (5), agent scrub-before-deliver + fail-closed pre-push history scan (6), wildcard custom-domain rejection (7), workspace-scoped execution NetworkPolicy (8), `canceling` in the credential-mint deny-set (10), `can_create` + redirect/header/publish-path hardening on static setters (11), `can_manage` on protected-environment ACLs (12), live-phase+billing gate on agent turn tickets (13), allowlist cardinality cap + pg-sni-proxy match-then-scan (15), Check-error-is-not-absence in invite reconciliation (16), source-side log-record truncation (17). Findings 1/3 stay migration-gated (= ADR055 F3/F2), 9 accepted risk (PSL, = ADR055 F9), 14 accepted (GitHub installation-token has no ref scope; tenant branch rulesets enforce). Fifth pass in the ADR028 → ADR045 → ADR055 → this lineage.
- [docs/ADR042-sandbox-cluster-substrate.md](docs/ADR042-sandbox-cluster-substrate.md) — re-opens pillar 5: realize the E2B-compatible agent-sandbox product on a **multi-node OpenSandbox Kubernetes-runtime substrate** (in-cluster control-plane server + the BatchSandbox controller), **Kata/microVM isolation** in a dedicated `bex-sandbox` node pool, **Render CLI `ea sandbox`** (`/v1/sandboxes*`) as the primary bex-api surface, **single-trusted-hop security** (NetworkPolicy + OpenSandbox multi-tenant per-workspace keys + anti-lateral-movement), and v1 **rootfs-only snapshots** (memory hibernation via CRIU as a watch item). Refines ADR014; supersedes its single-host substrate assumptions.
- [docs/ADR046-payment-onboarding-and-paid-gating.md](docs/ADR046-payment-onboarding-and-paid-gating.md) — **Accepted (w1/m62)**: just-in-time payment-method onboarding — signup stays card-free; paid intent (any non-free tier on Service/Postgres/Key Value create or plan change) requires a bound payment method via the existing setup-mode Checkout + `checkout.session.completed`-stamped local marker, enforced by an injected `PaymentGate` in the service layer (402/`PAYMENT_REQUIRED` across REST/GraphQL/MCP), env-gated by `BEX_REQUIRE_PAYMENT_METHOD`; the meter-event emitter withholds cardless rows and avoids auto-provisioning Stripe Customers (bounded 34-day write-off).
- [docs/ADR047-cloud-coding-agent-sessions.md](docs/ADR047-cloud-coding-agent-sessions.md) — **Accepted**: managed cloud coding-agent sessions (pillar 5 product layer) — sandbox-per-session on the ADR042 substrate, per-session GitHub App tokens with gateway-refreshed credential helper + `bex-agent/*` branch confinement, ACP as the agent interface fronted by an in-sandbox Vercel AI SDK session driver (`@mcpc-tech/acp-ai-provider`; gateway proxies the SSE UI-message stream + raw-ACP WebSocket on the HMAC-ticket pattern), draft-PR delivery, dual metering (compute-seconds + token passthrough — explicitly no per-request billing), BYO-API-key first; phase 1 fire-and-forget, phase 2 live attach. The control-plane lifecycle and credential broker are shipped; gateway attach/transcript streaming remains phase 2.
- [docs/ADR048-mobile.md](docs/ADR048-mobile.md) — **Proposed**: mobile strategy — supervision-first (push notifications → status → live logs → one-tap deploy/lifecycle verbs), differentiated by ADR047 agent sessions as the mobile centerpiece (assign/steer/approve from the phone); PWA + web push first, native on explicit triggers; the settings/configuration surface and destructive verbs stay desktop-only.
- [docs/ADR049-tenant-billing-credits.md](docs/ADR049-tenant-billing-credits.md) — **Proposed**: finite promotional/purchased tenant service credits through Stripe Billing Credit Grants; invoice-time credit-first/payment-method-overage semantics, durable idempotent operations, platform requester/approver separation instead of superadmin, provider-ledger reconciliation, correction/deletion policy, and a dedicated restricted credit key.
- [docs/ADR049-render-yaml-parity.md](docs/ADR049-render-yaml-parity.md) — **Accepted**: `render.yaml` is the canonical Blueprint contract; legacy `bex.yml` is a filename-only alias. A pinned upstream schema + exhaustive capability registry, one presence-aware compiler/planner, and fail-closed unsupported fields replace the current permissive parsers and silent semantic drift; implementation is `w1/m63`.
- [docs/ADR051-agent-session-transcript.md](docs/ADR051-agent-session-transcript.md) — **Proposed**: durable agent-session **conversation/chat history** — the w3/m43 transcript tee only writes during a **live** attach, so the shipped fire-and-forget product records nothing and every completed session shows "No conversation yet."; specifies a Completer-triggered **headless recorder** that tees the driver's still-live full-history replay into `agent_session_transcripts` before teardown (byte-transparent, seq-keyed idempotent, composes with the live tee, no new `pods/exec`). Splits ADR047 D10 into its own ADR.
- [docs/ADR052-notifications.md](docs/ADR052-notifications.md) — **Accepted (consolidating)**: the notification system end to end — one composed event feed (`deploys` + `audit_events` + `service_event_facts`, no emitter bus) fanned out to three independent channels: policy-aware deploy email (at-most-once, reconciler callback → SMTP), signed outbound webhooks (at-least-once, retry/auto-disable, the Slack answer), and release-gated native Expo push (durable inbox + per-device deliveries); Render's two-tier policy adapted with per-member defaults (`App.spec.notificationsToSend` authoritative, operator-inert); caller-self-service settings surface; gap register (items 1–2 resolved by w3/m78: all four observed lifecycle facts now project to push, crash edge integration-proven; `usage_threshold` unproduced, push release-gated).
- [docs/ADR053-node-instance-types.md](docs/ADR053-node-instance-types.md) — **Accepted (2026-08-08)**: Hetzner node instance-type policy — **cx33** (Intel, 4 vCPU / 8 GB) is the default worker + control-plane shape, **~4.7× cheaper than cpx32** at identical specs now that fsn1 cx stock returned; **cpx32** is the documented fallback during an fsn1 cx stock-out (the 2026-07 rotation, reversed here). Immutable-template rotation mechanism, apply-time rollout order (workers first, control plane last — one etcd member at a time), CP stays on 8 GB cx33 (never 4 GB cx23), and the `clusterapi-validate.sh` guard that enforces the current default.
- [docs/ADR054-open-in-zed.md](docs/ADR054-open-in-zed.md) — **Accepted (w2/m65)**: "Open in Zed" — real SSH remoting (`zed://ssh/ags-…@ssh.bex.co/workspace`) into agent-session sandboxes by reusing the ADR035 gateway: composite `ags-*` target resolver, `can_view_sensitive` on the `agent_session` FGA type, sandbox-only multi-channel (`BEX_SSH_MAX_CHANNELS_PER_CONN`) + SFTP-bridge carve-outs, `GRANT SELECT ON agent_sessions` for the scoped role, editor-aware Completer teardown deferral, `agentSession.sshAddress` + dashboard button; amends ADR035, complements ADR047 D8's ACP attach.
- [docs/ADR043-tenant-namespace-isolation.md](docs/ADR043-tenant-namespace-isolation.md) — **workspace = namespace**: per-tenant namespace isolation (`<ws>` hosting + `<ws>-sandbox` regime split), zero-trust east-west (both regimes untrusted), plan-tier caps pushed down to per-namespace `ResourceQuota` (replacing `BEX_MAX_*` app-code caps), and scale-to-zero preserved (logical namespace + shared physical node pool). **Deprecates ADR022's "Option B"** (shared namespace + label-scoped per-App policies as the tenant boundary) while retaining ADR022's other mechanisms (Cilium egressDeny, platform lockdown, registry ACLs, container hardening) re-scoped to the namespace; engages ADR022's objections to namespace-per-workspace point by point.

## Rules

- **Never `git commit` or `git push` unless the user runs `/ship` (Claude) or invokes `$ship` (Codex).** Leave work uncommitted otherwise.
- Never commit or print `.env` or `*.kubeconfig` contents.
- **Keep skill implementations canonical under `.claude/skills/<name>/SKILL.md`.** Every `.agents/skills/<name>` entry must be the relative symlink `../../.claude/skills/<name>`, the two directories must have identical names, and the retired `.claude/commands/` directory must remain absent. Run `bash scripts/skill-layout-validate.sh` after changing skills.
- **Keep `.env.example` in sync with `.env`'s variable names.** It's the single checked-in, value-less mirror of `.env`, serving both the local runtime env and the CI-secrets source (`bash scripts/gh-secrets.sh` pushes the filled `.env` into this repo's GitHub Actions secrets). Whenever a var is added, renamed, or removed, mirror the change (name + comment, never the value) so `cp .env.example .env` never falls out of date. (There is no longer a separate `.env.template` — the two were merged into `.env.example`.)
- New Go files carry the Apache-2.0 header from `lego/operator/hack/boilerplate.go.txt`.
- **Mint every resource id through `lego/backend/internal/id` (`id.New(kind)`), never by hand.** Ids are typed opaque `<prefix>-<xid>` strings with a **hyphen** separator (never an underscore — they must stay valid DNS/k8s names); a new id-bearing resource adds its `id.Kind` to that package's registry (which the guard test then enforces). Rationale + known deviations: [docs/ADR020-identifiers.md](docs/ADR020-identifiers.md).
- Markdown is CI-checked: `npx prettier@3.4.2 --write "**/*.md"` before finishing doc changes.
- **`.pm` done items move to `done/` folders — move the entire folder, leave nothing behind.** When a task or milestone is completed, never leave it in place: a done task moves to `wN/mN/done/tNNN.md`; a milestone with no open tasks moves **whole** to `wN/done/mN/` (`mv` the directory, then `rmdir` the empty original — **never leave a tombstone, stub, or redirect README at the old path**; a done milestone's original path must simply not exist); a done inbox note moves to `wN/done/NNN.md`. Sync status in all three places (task frontmatter, milestone README `**Status:**` + `— **DONE**` row, workstream README checkbox). If a `/goal` (or other tooling) references a milestone by its pre-move path, that path is _expected_ to disappear on completion — do not recreate it. Full conventions: `.pm/CLAUDE.md`.
- Playwright MCP writes to `.playwright-mcp/` (`--output-dir` in `.mcp.json`, gitignored). When taking screenshots, pass a **bare** filename (e.g. `render-logs.png`) so it lands there — never a path that resolves to the repo root. If an image ever appears at the project root, move it into `.playwright-mcp/`.
